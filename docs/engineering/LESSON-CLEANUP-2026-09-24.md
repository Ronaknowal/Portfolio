# Completed lesson working-file cleanup — 24 September 2026

The user requested removal of unnecessary material left from earlier implementation,
while preserving researched lessons awaiting their later implementation.

Removed **67 files (2,364,363 bytes)** after checking ownership, references and
retained sources:

| Kind | Files | Why retired |
| --- | ---: | --- |
| One-off patch, archive, freeze and completion helpers | 29 | Their changes are complete; rerunning fixed historical updates could overwrite newer source, evidence or instructions. |
| Superseded scratch manuscripts | 19 | Their owners have completed implementations and retained canonical lesson bodies; none is a pending packet. |
| Unused initial mobile captures | 3 | Final topic review evidence is retained. |
| Byte-identical redundant screenshots | 7 | Each retained equivalent and digest is recorded. |
| Unreferenced working/build/preview logs | 4 | No active consumer; useful integration/evidence records remain. One raw listing was outside scratch. |
| Generated Python bytecode | 5 | Recreated from retained source programs. |

The active handoff was reduced from **80,259 to 14,038 bytes** by removing repeated
completed-batch narratives and stale progression statements. Current phase state,
canonical instructions and useful topic links remain. Reusable engineering lessons
are consolidated in [verification pitfalls](LESSON-VERIFICATION-PITFALLS.md), including
independent checks, actual painted geometry, honest evidence and safe mutation tools.
The preserved workspace history is optional context, no longer a second required
handoff. Retention guidance now specifies how to avoid rebuilding this accumulation.

The read-only image audit no longer treats the completed first Classical ML batch
as permanently active. It protects shared environments and accepts explicit active
folder prefixes. A temporary fixture verified exclusions, duplicate identification
and nonmutation; that fixture was removed.

## Preservation and validation

SHA-256 comparisons confirm **5,739 protected existing files unchanged**, including
runtime source, public assets, teaching packets, ledgers, curriculum and retained
evidence. The one separately allowed instruction-file edit labels preserved workspace
history as historical; it is not a lesson/checkpoint change. The deliberately removed
unreferenced raw build log and regenerable caches were excluded from the protected set.

All **27** prepared-but-unimplemented topics retain matching content checkpoints and
`canFinish: true`. The delivery ledger remains byte-identical: **177 content complete,
150 implementation complete**. Its eight behavior groups pass. Repository ignore
checks and affected local-document links pass. No lessons, curriculum, website UI,
publication state, learner progress or original-source archives were rewritten.

Referenced historical inputs, original baselines, recovery material, final evidence,
shared runtimes and work whose purpose remained uncertain were kept. Missing direct
filename references alone were not treated as deletion permission. No broad scratch
wipe, environment purge, build, numerical rerun or browser campaign was needed.

The [exact removal and verification record](evidence/lesson-cleanup-2026-09-24.json)
contains paths, reasons, digests, retained counterparts and checkpoint verification.
Temporary cleanup inventories were discarded after validation. This is a closed
cleanup record, not another queue of lesson work.
