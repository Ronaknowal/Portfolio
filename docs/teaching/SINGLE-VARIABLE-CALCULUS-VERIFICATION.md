# Single-variable calculus: author verification

The complete lesson at mathematics position 47 is author-verified on 11 September 2026. [The frozen author packet](evidence/single-variable-calculus-author-review.json) binds its six production files and the actual test records. Independent review, production integration and user acceptance are separate gates. The original entry was planned, not an earlier published body; its archived plan, stable identity, module position and intended foundations remain preserved.

## Teaching and scope

Fourteen connected sections follow one motion model from average change through local derivatives, accumulated displacement and a changed final motion problem. Limits introduce neighborhoods and a quantified guarantee before notation; the center value and a jump are separate experiments. Product and chain rules follow from local errors, extrema require the permitted interval, and accumulation explains signed displacement versus distance before the fundamental theorem connects the two operations. Logarithms are defined through area, exponentials through inversion and proportional growth. Finite Taylor remainders precede series claims; improper integrals separate endpoints, tails and principal values. Work, distributed mass, volume, curve length and a first decay equation provide transfer without replacing their later owners.

Eight investigations address different mechanisms: motion secants/tangents, an exact limit neighborhood, composed local changes, interval candidates, signed rectangles, proportional growth, Taylor approximation and improper cutoffs. Four inline figures retain essential explanations in the reading flow. Fifteen complete Python programs include setup, prediction questions, actual captured output and interpretation. Thirteen changed practice tasks include hints and explained solutions. These counts describe this lesson, not a template or quota.

The proof review checked the hypotheses and reasoning for the derivative rules, inverse derivative, Rolle/mean value theorem, extrema, square-function Riemann limit, both fundamental-theorem directions, substitution, integration by parts, logarithm product law, exponential derivative, integral Taylor remainder, the stated exponential/logarithm convergence claims, both power-integral convergence tests and uniqueness of proportional growth. Integrability alone is not incorrectly used to claim an everywhere differentiable antiderivative. Increasing velocity is distinguished from strictly positive acceleration everywhere.

## Actual execution

`node scripts/verify-single-variable-calculus.mjs` invokes a separate Python oracle using exact fractions, SymPy and 65-digit mpmath. The final pass at 01:43:18 UTC covers 90 rate, 240 limit, 49 composition, 423 extrema, 480 accumulation, 264 growth, 251 Taylor and 450 improper states; all fifteen actual displayed programs; sixteen changed calls to their actual helpers; and sixteen application/practice checks. It also parses all 24 displayed formulas and rejects 25 invalid model inputs. Adjacent and subnormal intervals and four roundoff-dominated Taylor cases are included. These finite checks do not replace general proofs.

`node scripts/review-single-variable-calculus-lesson.cjs` passed on the final six hashes at 1440, 390 and 320 pixels in actual-font Edge. Each width exercised 26 captured states, all eight investigations, fourteen real keyboard checks, fourteen section anchors, all 24 equation bounds, four inline figures and thirteen practice disclosures. It checked errors, document overflow, finite SVG geometry, text bounds, reset behavior and invalid interval retention. The separate final-reading script passed again on the final sources, checking exact questions, code and output for all fifteen programs and 161 plotted motion points against an independently evaluated cubic. The largest SVG float difference was approximately 3.23e-7, below the explicitly chosen 2e-6 drawing tolerance.

The author opened all 24 narrow-screen equation images and representative motion, limit, composition, extrema, accumulation, growth, Taylor and improper-integral views during authoring, plus all four inline figures and changed practice/program captures. After the final complete rerun, the amended cusp, Taylor and invalid-interval phone images were reopened and checked. The packet lists those final image hashes explicitly; generated screenshots are not all claimed to have been visually opened. Complete-panel captures retain the tested width and extend viewport height to fit the panel; each recorded image includes that capture viewport.

## Findings repaired before the freeze

- An open limit neighborhood whose supremum equals epsilon was misclassified by binary rounding. Decimal-rational comparison now handles the boundary exactly. A strict interior rational counterexample is distinguished from a schematic float that cannot resolve a near-boundary witness.
- Nearly adjacent extrema endpoints could round their midpoint or function values into a wrong sign or tie. Candidate ordering now uses exact decimal rational values; derivative signs follow the critical-point partition. The UI states that drawn heights are rounded and nearby points can overlap.
- A Taylor readout appeared to compare floating-point error with a truncation-only bound. Labels and explanation now distinguish exact-polynomial truncation, approximate evaluation of that bound and floating-point roundoff, including a concrete degree-12 example. The mathematical bound was not inflated to hide the issue.
- The velocity checkpoint now says acceleration is nonnegative where differentiable and gives a zero-acceleration counterexample to the stronger claim.
- Signed-rectangle examples include three panels so that signed cancellation and the initially coincident distance estimate are visible. Large improper-integral labels receive adequate plot margin. Long equations were broken at mathematical boundaries for 320-pixel reading.
- Early harness issues with a slider at its maximum, code markup and hidden capstone order were corrected in the checks. One run was interrupted by an unrelated transient Vite authoring overlay; it was not counted as passing. The final complete run passed after the overlay's source was repaired.

The formatter records normalized AST conservation and zero owned-source esbuild warnings. Ordinary educational text was checked after formatting; AST equality alone does not establish readable prose.

## Sources and practical limits

Selected primary OpenStax Calculus sections on limits, derivatives, mean value theorem, the fundamental theorem, exponential/logarithmic calculus, improper integrals and Taylor series were read. Local proofs do not repeat overbroad antiderivative or auxiliary-function wording found in those selected pages. The linked 3Blue1Brown integration companion article was read; video identity was checked without claiming full viewing. The lesson's annotated references specify the concept and level each resource supports.

Plots are sampled mathematical illustrations, not measurements or performance benchmarks. Exact classifications have explicit bounded model domains. The Taylor displayed decimal is not a certified interval. Programs were run in local Python 3.12.14; browser displays are not a Python runtime. No production deployment, beginner study or user approval is implied by author verification.
