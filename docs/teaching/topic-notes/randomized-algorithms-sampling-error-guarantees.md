# Randomized Algorithms, Sampling & Error Guarantees

## 2026-09-10 — Transfer fingerprint guarantees without losing the random experiment

- Status: included and adapted in the completed destination,10 September2026; introductory instance remains in String Matching.
- Origin: [String Matching design](../STRING-MATCHING-LESSON-DESIGN.md), sections 7–8 of `string-matching-prefix-functions-rolling-hashes`.
- Destination: `randomized-algorithms-sampling-error-guarantees`. Exact topic-plan run confirms a planned specialist lesson on Las Vegas/Monte Carlo, repetition, independence and sampling. This is a better home for broader amplification/adaptive-input reasoning than repeatedly enlarging one string lesson.
- Existing coverage: String Matching derives a random-base collision upper bound for two unequal fixed length-m strings, prime Q larger than all symbol codes, and uniform B in 2…Q−1: min(1,(m−1)/(Q−2)). A nonzero difference polynomial has degree at most m−1. Union bounds do not require independence; multiplying two error probabilities requires independent choices under the declared fixed-input experiment. The lab uses fixed tiny parameters and explicitly does not claim this experiment or error rate.
- Useful transfer: contrast exact verification (collisions change runtime) with unverified candidates (answers can be wrong). Demonstrate why a fixed seed enables replay but does not prove a probability claim; inputs selected after seeing the base change the model. Avoid saying that all-match verified Rabin–Karp is always expected-linear: even with no false collisions, z true hits require zm comparisons in that verifier. First-hit and all-hit contracts have different accounting.
- Proposed treatment: retain a concise bridge to the existing derivation, then choose a new visual random experiment and independently check error amplification. Do not repeat the entire KMP/rolling chapter. Distinguish upper bounds from measured frequencies, failure-event union bounds from independent repetition, and exact versus randomized-answer algorithms.
- Evidence: [MIT rolling-hash notes](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/160b3b5f9da2e03815ca1e6ee0dba62a_MIT6_006F11_lec09.pdf) inspected pages 3–5; our explicit random-base root argument is an original derivation rather than the notes' different prime-selection setup. `scripts/verify-string-matching-native.py` independently enumerates 780 unequal fixed pairs over digits0–2, lengths1–3, prime17, bases2–16; enumeration corroborates those finite cases, not the general proof by itself.
- Prerequisites: expectation/error events explained locally or linked to their actual probability lesson; finite-field root reasoning is optional depth, not a hidden prerequisite for sampling.
- Resolution required: assess inclusion, adaptation or reasoned deferral during destination authoring; retain the underlying distinction even if another example explains it better.

### Resolution — generalize with a new verification experiment

Sections6–8 of the destination now introduce exact-integer Freivalds probes, independently proved one-sided error, independent versus reused vectors, majority versus all-miss events, a union budget and fixed/adaptive claim timing. Section8 links the fingerprint mechanism and retains the exact-verification-versus-false-match distinction, fixed-input random-base assumption and `zm` true-hit accounting. The polynomial/KMP chapter is not duplicated. The browser's four-vector enumeration and independent exact integer/Fraction native checks are recorded in [RANDOMIZED-ALGORITHMS-VERIFICATION.md](../RANDOMIZED-ALGORITHMS-VERIFICATION.md); all the requested distinctions are assessed with local exercises. Detailed concentration and correlated-estimation extensions have their own destination notes.
