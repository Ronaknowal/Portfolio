# Backpropagation & Automatic Differentiation — destination authoring note

Canonical ID: `backpropagation-automatic-differentiation`.

## 2026-09-11 — Make gradient checks evidence with stated numerical conditions

- Status: open for destination assessment. No Backpropagation body is changed by this note.
- Origin: scoped [Numerical Methods design](../NUMERICAL-METHODS-LESSON-DESIGN.md), prepared after reading its complete old body and the relevant actual Backpropagation section.
- Destination verified: `node scripts/build-curriculum-inventory.mjs --topic backpropagation-automatic-differentiation` reports the published Deep Learning Fundamentals / Core Concepts lesson, currently requiring individual design and review. `src/learn/data/lesson-manifest.json` maps this ID to `topics/backprop.jsx`; the filename alone is not the canonical ID.
- Actual coverage: `src/learn/data/topics/backprop.jsx`, section **4f. Correctness check: finite differences and PyTorch**, lines 342–393 at discovery. Line 345 presents a fixed perturbation of `1e-5` and a typical relative-error threshold of `1e-8`; line 393 concludes that the engine is correct after the displayed comparisons. The finite-difference program and framework comparison are useful and should be retained or improved, rather than removed.
- Proposed treatment: explain that central differences combine a smooth-function truncation term with amplification of function-evaluation error by division by the perturbation. Choose scales and tolerances appropriate to the function, parameter units, dtype and derivative magnitude. Sweep several perturbations, use absolute as well as relative error near zero, and control randomness and state during paired evaluations. Address nondifferentiable branches and memory aliasing when relevant to the actual engine. Agreement on tested inputs supports those cases; it is not a proof of every primitive, graph or boundary condition.
- Concrete reason: a single perturbation cannot certify a universal relative error. For the smooth function `f(x)=1e12+x` at `x=1`, binary64 evaluates `f(x+1e-5)` and `f(x-1e-5)` to the same value, producing a zero central difference although the derivative is one. Executed in the workspace's Python 3.12.14 during design preparation: both samples were `1000000000001.0`, and the central difference was exactly zero. Reproduce this in the destination's actual runtime before using it there. This does not claim that the current neural-network example fails.
- Ownership: Numerical Methods owns the local error balance and scalar/directional checking interpretation. Backpropagation is the better home for primitive coverage, repeated paths, broadcasting, graph accumulation, framework comparisons, dtype-specific checks and distinguishing derivative conventions at nonsmooth points. Its author should adapt this bridge to the actual engine and include varied independently checked cases.
- Primary evidence inspected 10 September 2026 UTC: Driscoll and Braun, [Convergence of finite differences](https://fncbook.com/python/fd-converge/), sections 5.5.1–5.5.2, develops Taylor error and cancellation; [PyTorch 2.14 gradcheck](https://docs.pytorch.org/docs/2.14/generated/torch.autograd.gradcheck.gradcheck.html), signature and notes/parameters, specifies double-precision-oriented defaults and qualifications for nonsmooth points, overlapping storage and nondeterminism. The moving stable URL redirected to 2.14; recheck the destination's actual installed version before implementing API examples.
- Resolution required: include/adapt, reroute or give a reasoned deferral after inspecting the complete destination. Link the implemented changes and actual runtime/visual evidence. This is a scoped discovery, not a completed audit of the Backpropagation lesson.

## 2026-09-12 — Content assessment and adaptation

Assessed after reading the entire manifest-owned original backprop.jsx. Adapted in the content-first [manuscript](../drafts/backpropagation-automatic-differentiation/lesson.md), especially §4 and investigationF, with complete [design/evidence record](../drafts/backpropagation-automatic-differentiation/design.md). The original useful engine/checking scope is preserved and repaired. Author calculations reproduce the offset-linear cancellation, sweep sine perturbations, handle zero-reference/nonsmooth interpretation and compare all parameter groups of an explicit smooth initial network against both finite differences and PyTorch2.14.0+cpu.

Content disposition: adapted. Runtime lesson implementation, browser laboratory, rendered evidence and formal finishing review remain pending under the separate implementation phase; this note does not mark them complete. See the central delivery ledger for the current content checkpoint.
