# Authoring notes: Neural Architecture Search (NAS)

Canonical topic ID: `neural-architecture-search-nas`

## 2026-09-12 — Build specialist NAS on the prepared classical foundation

- Status: open
- Origin: `automl-neural-architecture-search-nas`, [complete foundation and research record](../drafts/automl-neural-architecture-search-nas/design.md), especially manuscript sections 4–7.
- Destination and ownership rationale: this existing topic in Model Optimization has the neural prerequisites for convolutional search graphs, shared-weight implementation and benchmark evaluation. The earlier combined AutoML/NAS topic introduces the mechanisms locally before the deep-learning module; it must retain that foundation.
- Existing coverage: the originating packet teaches search space/strategy/evaluator, three actual MLP architectures inside an eleven-candidate study, parameter shapes, exact softmax mixture gradients, a scalar bilevel derivative, discretization failure, proxy provenance and latency constraints. The destination's title/module identity was verified; its body has not been audited during this task. Assess existing destination coverage before adding or replacing material.
- Idea and learning benefit: the learner should construct a valid searched graph and explain what evidence its score represents, including when a cheap proxy selects an inferior independently trained candidate.
- Proposed treatment: after a concise prerequisite bridge, develop actual convolutional cell shapes and graph construction; distinguish independently trained candidates from shared-weight subnetworks; provide a bounded documented native comparison with a simple search baseline and a checked failure or null. Explain training/search resource accounting, separate search-seed from candidate-training-seed variability, and preserve target-hardware measurement conditions. Choose the exact experiment after reviewing the destination's existing scope; no fixed lab count or expensive benchmark campaign is required.
- Explanation/example: contrast a continuous mixture with the committed cell, then trace where each candidate's weights came from. DARTS convolutional discretization retains two strong nonzero operations from distinct incoming nodes per intermediate node, rather than one independent argmax on every edge. Distinguish the direct first-order derivative from a nonzero one-step unroll with its mixed derivative. A proxy-versus-independent-rank display should use actual recorded values, not invented positive correlation.
- Prerequisites and boundaries: locally refresh tensors, convolutional shapes, gradients and residual connections as needed. A small sklearn MLP experiment does not establish full-scale CNN NAS performance. Soft latency penalties are not mathematical enforcement of hard caps; parameter counts and MACs are not measured latency. Repeated benchmark test consultation is selection evidence.
- Evidence: [DARTS, sections 2.1–2.4](https://arxiv.org/pdf/1806.09055), [NASWOT, section 3 and ablations](https://proceedings.mlr.press/v139/mellor21a/mellor21a.pdf), [MnasNet](https://arxiv.org/pdf/1807.11626), and the [AutoML book's NAS chapter](https://automl.org/wp-content/uploads/2019/05/AutoML_Book.pdf), inspected for the originating packet on 12 September 2026. Its design gives exact reading extent. No specialist native NAS benchmark or hardware timing was run in this content-only task.
- Resolution: not yet reviewed by the destination's author; assess, adapt, route or reject with reasons under the current note workflow.
- Implementation/verification links: none yet; this note does not authorize implementing the destination.
