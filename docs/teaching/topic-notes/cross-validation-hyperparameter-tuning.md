# Cross-Validation & Hyperparameter Tuning — incoming ensemble protocol finding

11 September 2026. Open destination review; originating Ensemble lesson is implemented and author-reviewed. See [its design](../ENSEMBLE-METHODS-LESSON-DESIGN.md) and [verification](../ENSEMBLE-METHODS-VERIFICATION.md). Independent review and production integration remain distinct.

The destination owns general resampling/model-selection theory. Ensemble implementation inspected the existing CV lesson's sections on K-fold and LOOCV, which currently describe a generic monotonic training-set-correlation explanation for variance. Reassess those conditions when rewriting this destination: overlap of fitted training sets alone does not determine the correlation or variance of held-out losses, and LOOCV does not universally have higher variance. Keep conclusions tied to the data-generating/learning procedure or an actual source-backed comparison.

The directly verified library contract is narrower and useful: scikit-learn 1.9.1 `StackingClassifier` uses `cross_val_predict` for its meta-training inputs; this requires held-out sets that partition the supplied rows. A directly supplied TimeSeriesSplit leaves the initial prefix unpredicted and raises `cross_val_predict only works for partitions`. A complete manual forward-only OOF loop can retain those missing rows explicitly and fit its meta-model only where predictions exist. This differs from `cross_val_score`, which may accept those temporal splits for evaluation. Preserve group/time ownership inside preprocessing and any inner tuning as well.

Sources inspected: [cross_val_predict API](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.cross_val_predict.html), [StackingClassifier API](https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.StackingClassifier.html) and actual installed source/fixture. Ensemble owns its immediate complete manual construction; this destination should integrate and deepen the distinction rather than reassigning all CV foundations to the Ensemble lesson. Review proposed examples and record inclusion, adaptation or reasoned deferral when this topic is implemented.

12 September 2026 content disposition: incorporated in the [research/write manuscript](../drafts/cross-validation-hyperparameter-tuning/lesson.md), §3's score-versus-OOF distinction and §7's covariance counterexample, with [visual contracts](../drafts/cross-validation-hyperparameter-tuning/visual-specifications.md) for explicitly absent prefix predictions. The manuscript preserves group/time ownership inside every learned transformation and selection loop. The original universal overlap/variance explanation is replaced by a conditional formula and a fixed-rule counterexample. This note remains open for phase-two implementation; content completion does not claim production integration or another installed-library failure campaign.

14 September 2026 phase-two disposition: **implemented and closed for this note's finding.** The published `src/learn/data/topics/cross-validation-hyperparameter-tuning.jsx` carries the distinction this note asked for in two places. Section 3's "What an out-of-fold prediction table requires" states the `cross_val_predict` exactly-once partition contract against `cross_val_score`, and says what a manual forward table must contain at an unpredicted prefix — retain the entries as absent, never fill them from a model fitted on that same prefix, and train a stacking stage only where legitimate predictions exist. Figure 2 computes that contrast from the topic's own `foldPlan`/`outOfFoldCoverage` models rather than asserting it: the forward plan train 0–3/test 4–5 then train 0–5/test 6–7 renders rows 0–3 with an explicit "no held-out prediction" mark beside a four-fold partition where every row is held out exactly once. Group and time ownership is preserved inside every learned transformation and inner selection loop; the real penguin program learns medians, categorical fill and the one-hot vocabulary inside each candidate training fold.

The overlap-implies-variance explanation the note flagged is gone. Section 7 gives the general covariance formula, the equal-variance/equal-correlation simplification as an explicit assumption with a computed table of what it buys, and the always-predicts-zero counterexample whose leave-one-out losses are independent with variance 1/(4n) despite near-total training overlap — verified in `scripts/verify-validation-models.mjs` by enumerating every label pattern up to n = 8. LOOCV is not claimed to have universally higher variance anywhere.

No second installed-library failure campaign was run: the `cross_val_predict only works for partitions` behaviour is cited to the API contract and to the ensemble lesson's recorded finding, and the page's own demonstration is the computed coverage table, not a re-run of that error. Implementation evidence: `docs/teaching/evidence/validation-{models,native,data,browser}.json` and the phase-two section of [the design record](../drafts/cross-validation-hyperparameter-tuning/design.md). The [independent review](../CROSS-VALIDATION-INDEPENDENT-REVIEW.md) has since been completed and its findings dispositioned in the design record; blueprint registration and navigation regeneration are done. Only the phase ledger entry remains outstanding, and it belongs to the integration owner.
