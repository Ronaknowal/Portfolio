# Authoring notes: DBSCAN & Density-Based Clustering

Canonical topic ID: `dbscan-density-based-clustering`.

## 2026-09-12 — Precise density graph, shared Iris thread and hierarchy conventions

- Status: implemented
- Origin: current content-stage rewrite; [DBSCAN design](../DBSCAN-LESSON-DESIGN.md) and [complete manuscript](../drafts/dbscan-density-based-clustering/lesson.md). Coordinated directly with Clustering Evaluation and Anomaly authors.
- Destination and ownership rationale: this lesson owns core/border/noise, density reachability, parameter geometry and OPTICS/HDBSCAN bridges. Evaluation owns general metric derivations; Anomaly owns reference sets, score/threshold decisions and costs. Keep the stable title and module position.
- Idea and learning benefit: a shared border can attach to two eligible components without transmitting between them; its type is order-invariant while its assigned membership can change. An actual variable-density fixture can prove that no single radius meets a requested partition. The same real Iris rows from Evaluation expose coverage versus conditional score changes.
- Existing coverage: original published body has useful mechanism/algorithm/hierarchy/scaling material, but several unsupported claims and convention errors. Complete original read recorded at baseline SHA `d3a24bab9c33fd7b50c9afd8bb34d4288f1ae466f22b2e4f52ba11f4c0fe9aff`; runtime remains unchanged.
- Proposed treatment: manuscript §§1–8 supply the first-pass local graph, complete algorithm and offline Iris analysis; §§9–12 provide optional ordering/hierarchy/scalability/new-row depth. Shared street A–J is `[-1.75,-1.5,-1.25,-1,1,1.25,1.5,1.75,0,4]`, ε=1,m=4; I is shared border, J density noise. This does not assert that J is erroneous or dangerous.
- Prerequisites and boundaries: refresh distance, graph paths and self-count locally. All-row metric reports treat −1 as one label; excluded-noise reports name retained IDs and coverage. New observations need a declared frozen-reference rule rather than an invented sklearn DBSCAN `predict`. Do not confuse OPTICS's source-core reachability, HDBSCAN's two-core maximum, or LOF's neighbor-core convention.
- Evidence: primary original/JSS definitions and current sklearn 1.9.1 API, author calculations and exact data provenance are linked in the design. The current sklearn guide has a prose inequality typo in its HDBSCAN explanation; the maximum formula requires exclusion when core distance is **greater** than ε. A high OPTICS reachability can still begin a cluster if its own core radius qualifies. The manuscript uses a direct A/E example.
- Resolution: included in the completed content draft and visual specifications; awaiting the separately authorized implementation checkpoint. `open` is intentional because the policy reserves `adapted`/`implemented` for ideas actually installed and verified. No new catalogue item is proposed.
- Implementation/verification links: implemented on 12 September 2026 in `src/learn/data/topics/dbscan-density-based-clustering.jsx` (sections 1-3, 7, 9-10, 12), `dbscan-models.js` (self-inclusive closed neighbourhoods, eligible components for borders, analytic interval fixture) and the four labs in `DbscanLabs.jsx`. Verification: `scripts/verify-dbscan-models.mjs`, `verify-dbscan-examples.py`, `verify-dbscan-browser.cjs`; evidence `docs/teaching/evidence/dbscan-models.json`, `dbscan-native.json`, `dbscan-browser.json`. The phase-two section of the design record lists the checks; independent review is recorded there when it completes.

## 2026-09-12 — Density-based validation has its own graph definitions

- Status: deferred
- Origin: scope coordination with `clustering-evaluation-validation-silhouette-ari-nmi` while writing the present lesson.
- Destination and ownership rationale: DBCV's density-aware validation is a specialist continuation here after local density and MST concepts. The preceding Evaluation lesson explains why an ordinary distance criterion can prefer a different shape; it does not silently teach DBCV, nor does this lesson's ordinary core graph substitute for DBCV's construction.
- Idea and learning benefit: a future optional derivation should separate DBCV's all-points-core distance, within-cluster density sparseness and between-cluster density separation from DBSCAN's fixed m-neighbor core radius. Treat rejected observations and tiny/degenerate clusters explicitly rather than paste an unexplained library score.
- Existing coverage: conceptual metric-choice warning in the preceding lesson; this manuscript teaches the prerequisite density/MST mechanisms and transparent coverage reporting, but does not calculate DBCV.
- Evidence: [Moulavi et al. 2014 primary publication record](https://researchonline.jcu.edu.au/46783/) and [authors' implementation overview](https://github.com/pajaskowiak/dbcv) were identified in research; abstract/search-indexed source overview inspected, not the complete paper or implementation. A formerly indexed LMU PDF URL redirects to the institute homepage and is not endorsed as a functioning paper link.
- Resolution: defer the specialist derivation/implementation until an explicitly scoped density-validation extension can read the full definitions and supply a worked validity graph with meaningful degenerate/noise cases. The current first-pass finish line already supports a complete, honestly reported DBSCAN analysis; adding an unexplained score would weaken it. Retain this topic as primary owner, without creating a new catalogue topic or claiming coverage completed.
- Implementation/verification links: none.
