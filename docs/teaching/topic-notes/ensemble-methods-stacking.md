# Ensemble Methods & Stacking — incoming Naive Bayes bridge

Recorded 11 September 2026 during the authorized first-ten Classical ML rewrite. Status: adapted and resolved in the Ensemble author implementation. Independent review and production integration are separate. The originating Naive Bayes lesson remains preserved.

- Origin: [Naive Bayes design](../NAIVE-BAYES-LESSON-DESIGN.md), copied-alarm and probability/calibration branches. Three copies of one alarm do not become three independent measurements; with true prior .2 and positive rates .8/.4, the actual posterior is1/3 while a three-factor product reports2/3. This is an exact model example, not a benchmark.
- Destination owns combination of model predictions. Distinguish averaging probabilities, voting labels and multiplying likelihood evidence. Model outputs generally share training data/inputs, so a product is not justified merely because several models agree. Averaging well-calibrated probabilities does not automatically preserve calibration either.
- Teach out-of-fold rows with exact case identities and training ownership. The meta-model must not be trained on in-sample predictions from bases fitted on that same row's target; preprocessing must also remain within each base fold. Final base refitting and its distribution difference from fold-trained predictions deserve explicit interpretation.
- A useful adjacent comparison: GaussianNB normalized probabilities, a margin classifier's decision score and a binary class label are different possible meta-features. Explain actual StackingClassifier stack_method behavior and dropped binary probability column rather than mixing meanings in one unlabeled table.
- Evidence status: [Final origin verification](../NAIVE-BAYES-VERIFICATION.md) and [exact author packet](../evidence/naive-bayes-author-review.json) preserve the Naive Bayes implementation. The Ensemble original body and all three old programs were separately read, archived and executed; see [its design](../ENSEMBLE-METHODS-LESSON-DESIGN.md).
- Destination disposition: sections 2 and 10 distinguish posterior mixing from likelihood multiplication and supply a complete exact calibrated-member/uncalibrated-average counterexample. Sections 7–9 teach OOF row ownership, preprocessing, missing temporal rows and full-data refitting, with actual native loops and model-linked investigations. The mixed GaussianNB/LinearSVC program verifies current class-column and score semantics. [Ensemble verification](../ENSEMBLE-METHODS-VERIFICATION.md) binds final source and author evidence; these local outcomes close this note without rewriting another topic.
