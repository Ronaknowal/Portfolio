# Incoming discovery: normalized propagation limits and spectral filter conventions

- **Status:** open; receiving author must assess during its scoped rewrite.
- **Origin:** Spectral Graph Theory (`spectral-graph-theory`),10 September2026; sections4,7 and individual design SPECTRAL-GRAPH-THEORY-DESIGN.md.
- **Destination:** Message Passing & Graph Convolutions (GCN, GAT, GraphSAGE), stable ID `message-passing-graph-convolutions-gcn-gat-graphsage`.
- **Inspected current coverage:** the published body's spectral paragraph near line244 uses the self-loop-normalized operator Ã=Dhat⁻¹/²(A+I)Dhat⁻¹/², then says repeated propagation leaves only the constant zero-frequency component. This is not generally the right node-coordinate shape. The paragraph also calls repeated (1−λ) gains increasingly aggressive low-pass without distinguishing signed gain and its magnitude. This was a scoped search of the relevant propagation explanation, not a full review of that large lesson or its benchmark claims.
- **Why this owner:** Spectral31 now teaches normalized zero direction √d, the distinction between function averaging and probability evolution, periodicity/laziness, and scalar-filter basis invariance. This destination owns what those facts mean for the actual self-loop GCN propagation and learned nonlinear layers.
- **Proposed treatment:** for a fixed connected nonnegative undirected self-loop graph, state the normalized zero direction proportional to √dhat. In the purely linear repeated-propagation model, the limit is the projection onto that direction; use a small unequal-degree graph to show that its raw node coordinates are not all equal. Explain that recovering random-walk coordinates by dividing by √dhat changes that shape to a constant. Positive self-loops remove the bipartite period under the finite connected assumptions; a missing self-loop/periodic variant has a different limit. The magnitude |1−λ| is not globally monotone on[0,2], even though modes with |1−λ|<1 eventually decay. Do not automatically transfer this fixed-linear-operator conclusion to trainable weights, nonlinearities, residuals, changing graphs or normalization.
- **Useful visual/practice:** same unequal-degree graph, raw normalized eigenvector versus degree-divided function, and two propagation steps. Ask learners to identify which quantity becomes constant. Compare with a two-node no-self-loop alternating counterexample. For polynomial graph filters distinguish degree r from r terms, since only the highest power determines the r-hop bound.
- **Prerequisites:** Graph Fundamentals and Spectral Graph Theory; supply a local normalization refresher. No new catalogue entry or prerequisite reordering is proposed.
- **Sources and certainty:** [Shuman et al., II.F](https://arxiv.org/pdf/1211.0053) distinguishes the normalized zero vector and spectrum; [von Luxburg, section3.2](https://arxiv.org/pdf/0711.0189) relates symmetric and random-walk coordinates. The finite algebra follows directly from Ã√dhat=√dhat. Actual GCN implementation/version conventions and any empirical generalization claims must be researched by the receiving author; they were not verified by this note.
- **Resolution required:** record inclusion, adaptation, rerouting or justified deferral with the implemented explanation and its numerical/browser evidence. Do not mark implemented from this proposal alone.
