# Incoming discovery: normalized propagation limits and spectral filter conventions

- **Status:** open; receiving author must assess during its scoped rewrite.
- **Origin:** Spectral Graph Theory (`spectral-graph-theory`),10 September2026; sections4,7 and individual design SPECTRAL-GRAPH-THEORY-DESIGN.md.
- **Destination:** Message Passing & Graph Convolutions (GCN, GAT, GraphSAGE), stable ID `message-passing-graph-convolutions-gcn-gat-graphsage`.
- **Inspected current coverage:** the published body's spectral paragraph near line244 uses the self-loop-normalized operator Ã=Dhat⁻¹/²(A+I)Dhat⁻¹/², then says repeated propagation leaves only the constant zero-frequency component. This is not generally the right node-coordinate shape. The paragraph also calls repeated (1−λ) gains increasingly aggressive low-pass without distinguishing signed gain and its magnitude. This was a scoped search of the relevant propagation explanation, not a full review of that large lesson or its benchmark claims.
- **Why this owner:** Spectral31 now teaches normalized zero direction √d, the distinction between function averaging and probability evolution, periodicity/laziness, and scalar-filter basis invariance. This destination owns what those facts mean for the actual self-loop GCN propagation and learned nonlinear layers.
- **Proposed treatment:** for a fixed connected nonnegative undirected self-loop graph, state the normalized zero direction proportional to √dhat. In the purely linear repeated-propagation model, the limit is the projection onto that direction; use a small unequal-degree graph to show that its raw node coordinates are not all equal. Explain that recovering random-walk coordinates by dividing by √dhat changes that shape to a constant. Positive self-loops remove the bipartite period under the finite connected assumptions; a missing self-loop/periodic variant has a different limit. The magnitude |1−λ| is not globally monotone on[0,2], even though modes with |1−λ|<1 eventually decay. Do not automatically transfer this fixed-linear-operator conclusion to trainable weights, nonlinearities, residuals, changing graphs or normalization.
- **Useful visual/practice:** same unequal-degree graph, raw normalized eigenvector versus degree-divided function, and two propagation steps. Ask learners to identify which quantity becomes constant. Compare with a two-node no-self-loop alternating counterexample. For polynomial graph filters distinguish degree r from r terms, since only the highest power determines the r-hop bound.
- **Prerequisites:** Graph Fundamentals and Spectral Graph Theory; supply a local normalization refresher. No new catalogue entry or prerequisite reordering is proposed.
- **Sources and certainty:** [Shuman et al., II.F](https://arxiv.org/pdf/1211.0053) distinguishes the normalized zero vector and spectrum; [von Luxburg, section3.2](https://arxiv.org/pdf/0711.0189) relates symmetric and random-walk coordinates. The finite algebra follows directly from Ã√dhat=√dhat. Actual GCN implementation/version conventions and any empirical generalization claims must be researched by the receiving author; they were not verified by this note.
- **Resolution required:** record inclusion, adaptation, rerouting or justified deferral with the implemented explanation and its numerical/browser evidence. Do not mark implemented from this proposal alone.

## 13 September 2026 — accepted into prepared content

Receiving author/root accepted this discovery in the [complete research/write manuscript](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/lesson.md), section7, with [visual contracts](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/visual-specifications.md) and [author design/evidence](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/design.md). The unequal-degree self-loop path now shows the raw limit proportional to square-root degree and the degree-divided constant, signed versus absolute filter gain, and a no-loop alternating counterexample. The packet's executed exact calculation records eigenvalues−1/6,1/2,1 and the40-step limit; a separate real graph study does not claim this theorem covers trained nonlinear models. Polynomial degree/hop convention is stated as highest-power locality, not a terms-count claim.

Content disposition: included/adapted, author arithmetic checked. **The note remains open for implementation verification**: the published original has not changed, browser evidence and independent phase-two review are deferred. The finish agent must implement and review the explanation rather than treat this content acceptance as production completion.

## 2026-09-22 — Turn the dense reference into a matched PyG operation

**Historical triage finding and writing request.** The coverage and proposed treatment below describe the packet before the bridge was authored. The following content disposition supersedes those writing actions; package execution and implementation verification remain open.

- Status: open
- Origin: [prepared Deep Learning implementation-depth triage](../implementation-depth/PREPARED-DEEP-LEARNING.md).
- Original coverage finding: The then-prepared learner program `message-passing-study.py:14–52` implemented normalized GCN, mean GraphSAGE and dense masked GAT, but the manuscript at line469 only linked GCNConv. No supplied Python program imported or constructed a PyG layer at that checkpoint.
- Original destination and proposed treatment: This lesson owns the graph-library bridge. After the tiny dense example, add a complete sparse `edge_index`/feature construction and an actual GCNConv forward with copied weights, mapped bias and identical self-loop/normalization conventions. Explain source-to-target edge order, directed versus undirected construction, duplicate edges and isolated-node policy. Add GraphSAGE/GAT parameter maps only as needed for advertised core outcomes; do not equate their different normalizations.
- Verification, customization and efficiency: Use an unequal-degree graph and nonconstant features so normalization/transpose bugs are visible. Compare outputs and one gradient under the exact contract, then change an edge and check both routes. State O(E d) aggregation versus the dense N² reference; no universal speed claim. Make cache invalidation explicit for changing graphs.
- Evidence and uncertainty: [PyG GCNConv API](https://pytorch-geometric.readthedocs.io/en/latest/generated/torch_geometric.nn.conv.GCNConv.html), already cited by the packet. Read the current installed version before implementation; this triage does not verify new package execution.
- Resolution: confirmed specialist-use bridge gap under the new requirement; assess and implement during the next authorized finish/revision. This note does not change the prepared content or mark phase two complete.
- Implementation/verification links: the existing [prepared packet](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/lesson.md) remains intact; no new API route implemented by this note.

## 2026-09-22 — Bridge written into the prepared content

**Content disposition: prepared. Implementation status: open.** The [revised manuscript](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/lesson.md) now explains and supplies the complete program `graph_library_bridge.py`, with sparse GCN/GraphSAGE/GAT, matching PyG parameter/state conventions, output/feature/parameter gradients, one SGD update and isolated/empty/edited graph cases. The [design](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/design.md) owns scope and the [visual specifications](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/visual-specifications.md) own placement, state and checks. The concrete source is [graph_library_bridge.py](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/graph_library_bridge.py). No missing bridge code is delegated to finishing.

The PyG 2.9.0 contracts were inspected; the package route has not been executed in this content-only revision. Phase two must execute the supplied paired checks in a compatible environment, review/fix any discrepancies, integrate the taught code/downloads and visuals, and record native/browser evidence. It must not label this preparation as publication or verified implementation. The earlier proposal is retained as the discovery history and is superseded on its content-writing action by this disposition.
