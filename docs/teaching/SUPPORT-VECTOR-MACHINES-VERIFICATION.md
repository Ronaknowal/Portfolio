# Support Vector Machines: author verification

Completed 11 September 2026 for Classical Machine Learning position 5, stable ID `support-vector-machines-svm`. The [final author packet](evidence/support-vector-machines-author-review.json) binds the seven production sources, actual executions, final browser closure and opened images. This is author review, not user acceptance or the parent's production integration.

## What changed and why

The original 54,398-byte lesson and all six extracted CodeBlocks remain in the [original archive](archive/support-vector-machines-before-rewrite/manifest.json). The connected original SMO demonstrations and connected library fragments actually reproduce their original stdout in the pinned environment; the archive is not described as fabricated output. Their implementation and evaluation contracts nevertheless required repair. The comments-only sweep was not a complete executable experiment.

The complete replacement retains hard/soft margin geometry, the Lagrangian/dual/KKT derivation, kernels and explicit features, SMO's two-coordinate mechanism, original Gaussian/XOR demonstration data, SVC/LinearSVC/SVR workflows, C/gamma selection, probability handling, multiclass, cost and approximation depth. It adds first-use definitions, a save/run command, locally derived counterexamples, fourteen complete programs with visible questions and actual output, twelve independent hint/answer tasks and annotated alternate written/video resources.

Important repairs are conditional support-point movement, tight versus positive-coefficient distinctions, unique weights versus potentially nonunique bias/coefficients, positive semidefiniteness versus strict curvature, finite-box boundedness, zero-curvature and atomic pair updates, an explicit primal–dual stopping criterion, fold-local preprocessing, sum-versus-average loss normalization, current scikit-learn calibration, target-unit SVR equivalence and measured rather than invented selection plots. One-class anomaly detection and the full RKHS/calibration theories remain with their existing owners.

## Representations and their contracts

| Form | Actual learning job and model | Limits and accessibility |
| --- | --- | --- |
| Margin corridor and projection | Real two-dimensional points, equal coordinate scales, clipped score lines, perpendicular projection and signed margin readouts | Distinguishes raw score scaling from distance; bounded angle/offset/scale, labeled classes also differ by shape; exact table and keyboard controls |
| Support-point motion | Original and refitted one-dimensional constraints with the active dual certificate and movable outer point | Preserving old feasibility is explicit; coincident labels use separate positions; a particular duplicate certificate is not claimed unique |
| Hinge/KKT investigation | Two-point separated or conflicting data, optimal slope, a potentially nonunique bias interval and primal/dual costs | A zero weight explicitly has undefined geometric margin; overlapping class markers remain visible; reset and table |
| Gram comparison and XOR kernel investigation | Two finite Gram examples, actual degree-two features, analytic XOR certificate, sampled input score field and individual signed kernel contributions | PSD versus singular versus indefinite; contribution bars expand to actual values; dark cells are near-zero scores, not probability; memoized fitted fields do not recompute when only the query moves |
| Pair feasible segment and gain curve | Box and equality constraints, atomic candidate update, zero-curvature endpoint behavior | Same-label versus opposite-label directions; at most sixteen manual updates; a disabled button is not a whole-problem convergence certificate |
| Actual validation selection | Nine saved SVC fits from a declared 120/40/40 split, train-only scaler, saved support predictions and observed train/validation accuracy | No test labels in controls, finite 32×32 rendering, actual observation values and provenance; no fictitious heatmap or universal benchmark |
| SVR tube and sequence-count figure | Analytic symmetric three-point SVR, target units and paid vertical excess; actual overlapping sequence windows/counts | Dynamic tube extent; epsilon is not uncertainty coverage; identical count vectors demonstrate lost ordering information |

There is no diagram quota. Each representation exposes a different mechanism; tables supply exact text alternatives and progressive disclosure. Seven investigations and two inline figures happened to meet this topic's needs.

## Executed programs and independent finite checks

`scratch/lesson-tools/Scripts/python.exe scripts/build-support-vector-machines-examples.py` executed fourteen complete programs at **13:15:09 UTC** using Python 3.12.14, NumPy 2.3.5, SciPy 1.18.1 and scikit-learn 1.9.1. The records preserve each program source hash and actual stdout. The example schema was subsequently mapped from `output` to the renderer's `expected` field without changing code or output; the browser checks the actual question/code/output blocks. The final flat-pair repair re-executed only the affected standalone pair program; its stdout was unchanged. Thirteen other executions remain valid and were not unnecessarily repeated.

`node scripts/verify-support-vector-machines-models.mjs` passed at **13:59:30 UTC**. Independent checks cover 45 projection configurations against constrained Euclidean optimization, seven support-motion fits against LIBSVM, 40 soft-margin epigraph optimizations, 144 XOR kernel cases against constrained dual optimization and explicit features, 113 pair states against a separately parameterized two-dimensional constrained optimization, 48 SVR tube states against LIBSVM, 18 sequence-count states, 36 changed actual complete-solver inputs, repeated-fit and step-cap cases, nine saved fitted-model comparisons, and twelve rejected browser-model inputs. Maximum scaled discrepancy was **1.3168×10⁻⁸**. These are finite checks of the stated domains, not arbitrary-input production optimizer certification.

The parent found one additional tie-policy mismatch before freeze: a zero-curvature, zero-gradient same-label duplicate pair chose a bound although prose promised no movement. The JS function and actual standalone Python helper now choose delta zero. `node scripts/verify-support-vector-machines-pair-update.mjs` checked three flat duplicate coefficient distributions and four opposite-label linear directions, reproduced the actual affected stdout, preserved the thirteen unaffected executions and saved its previous code. The full solver already tests gain before committing a zero-gain step. The final browser imports the actual amended model and checks the flat tie, then exercises the visible opposite-duplicate update.

The initial independent verifier itself needed a dtype correction: converting all-integer zero coefficients without `dtype=float` caused its temporary NumPy array to truncate SLSQP trial values. That was a test-harness defect, not a lesson-model failure; the corrected independent run above is the passing evidence.

## Actual browser and ordinary reading

The full interaction pass reached **50 states, 35 focusable controls, 33 keyboard-tested disclosures and all fourteen question/code/output comparisons at each of 1440, 390 and 320 pixels**, using Edge with actual Space Grotesk, JetBrains Mono and KaTeX fonts loaded. It covers all seven labs, both point fixtures, all kernel families, changed penalties/query, same-label and opposite-label pair choices, zero curvature, every saved validation model, training/validation toggling, regression tolerance, reset, actual range arrow keys and checkbox/summary activation.

Those runs exposed a long axis tick and several narrow formulas. Axis ticks now use readable rounded labels while exact data remain in tables. Support coincidence, overlapping hinge markers and the polynomial contribution scale were also resolved during authoring. The 390/320 interaction records deliberately retain their later formula-layout assertion failures; they are not mislabeled as wholly passing suites.

The **final focused three-width run** rechecked every actual program block, all eleven anchors, all ten displayed formulas, document width, loaded fonts and errors, the actual loaded flat-pair model and the visible duplicate-pair commit/reset. All passed with zero page errors, failed requests or page overflow. Only the intentionally closed HMR WebSocket diagnostic was excluded. This is the correction closure for the same previously checked interactions, not a repeated all-suite claim.

Eleven purposeful final images were actually opened: desktop and phone margin geometry, moved support points, conflicting bias, desktop feature lift, phone contribution bars, zero-curvature feasible geometry, measured validation selection, target-unit tube, narrow dual reading and target-unit/program reading. They are retained under `evidence/support-vector-machines/` with exact hashes and their original capture mapping. Three failed/superseded screenshots and the regenerable model-case working file were retired after the successful closure; their JSON failure/correction history remains in the packet. No full-page image dump or screenshot per control is retained.

## Sources, notes and remaining boundaries

The [design](SUPPORT-VECTOR-MACHINES-LESSON-DESIGN.md) records primary source review and retention decisions. Final research completed LIBSVM's C-SVC/bias and epsilon-SVR formulation sections, spectrum-kernel section 4's count definition, and the RBF/Nyström documentation. LIBSVM names its two SVR multipliers in the opposite order to our locally declared constraints; the resulting prediction signs were checked consistently rather than copied by symbol name. The finite kernel/dual proofs, RBF feature expectation and target scaling are derived locally.

The official MIT6.034 lecture identity and its substantive transcript geometry/normalization segment were reviewed (roughly pages 1–11); the complete video was not watched. The optional resource annotation records the historical lecture versus current API distinction. No paper's benchmark numbers were reused as lesson measurements.

Both incoming SVM destination notes are implemented and resolved with this evidence. The exact calibration destination receives a follow-up about the 1.9 deprecation, the full estimator's score interface, whole-pipeline out-of-fold calibration and selection dependence. Existing calibration notes remain intact.

No generalization guarantee, universal accuracy ranking, GPU run, large-scale memory benchmark or general-purpose optimizer claim is made. Exact stdout can change with versions/platforms. Independent review, shared production build/loading and learner approval are separate gates owned outside this author packet.
