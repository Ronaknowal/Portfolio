# Survival Analysis: author verification

The complete 16-section lesson is implemented and author-verified. The exact eight-source freeze, original baseline, actual check versions and selected image hashes are in [the author packet](evidence/survival-author-review.json). Parent independent review and production integration are separate steps, not implied by publication or this record.

The [assessed design](SURVIVAL-ANALYSIS-LESSON-DESIGN.md) records original-content retention, learning mechanisms and inspected sources. All five old Python blocks were archived and executed. The replacement retains the original RandomState(42) experiment and useful Rossi/KM/Cox/AFT/forest coverage, repairs their contracts, and makes the programs standalone. It adds a complete supported-horizon reliability workflow and an independently changed mechanism without claiming a clinical or industrial benchmark.

## Teaching and representation review

The first-event observation unit, time origin and equality versus inequality come before notation. Risk lanes and a changing denominator drive KM; a staircase area shows restricted mean and follow-up limits; hazard and clock controls distinguish rates, probabilities and time ratios. Local Cox competition changes actual subject weights and tie denominators. A pair board exposes which rankings are observable, a finite observation-probability tree explains IPCW, and common-pool mass transfer distinguishes CIF from a net-risk quantity. Four inline figures explain observation, density versus survival tail, tree-leaf curves and changing covariate histories. Their displayed values are derived from the model or exact stated fixtures, not illustrative benchmarks.

The lesson contains 17 complete, executed Python programs, each preceded by its visible question. Twelve changed practice tasks have optional hints followed by separate explained answers. The reliability capstone makes subject splits, baseline, training-only preprocessing/censor estimate, validation selection, supported horizons and frozen test reporting explicit. The optional derivatives, AFT, neural PH, competing-risk, delayed-entry, discrete and interval branches state their additional assumptions and limits. The next actual module topic is K-Means & Hierarchical Clustering, outside the authorized ten-topic rewrite.

## Executed numerical evidence

The separate survival environment uses Python 3.12.14, NumPy 2.3.5, SciPy 1.18.1, pandas 2.3.3, scikit-learn 1.9.1, lifelines 0.30.3 and scikit-survival 0.28.0. It preserves the pandas 3 environment used by earlier lessons.

- [Program execution](evidence/survival-program-execution.json): all 17 complete programs executed at 14:49 UTC. A factual Rossi output sentence was then corrected and only that program rerun at 14:53 UTC; the other 16 exact executions are explicitly reused. The final example-module hash matches the output record.
- [Bounded native verification](evidence/survival-native-verification.json): 121 finite fixtures and 13 invalid-input contracts passed at 15:13 UTC. This scoped author-verification contribution was implemented by `testing_documentation_completion`, complementary to the root's independently owned invariance checks. It includes exact censor-mass KM/RMST, Fraction Greenwood and library intervals, high-precision Cox derivatives and separate fits, enumerated conditional log-rank variance, library pair/CIF comparisons, analytic time/hazard laws, exact known-G expectations, changed interval/exposure helpers and practice B–L. The 17 existing executions were reused by exact hash, not rerun under a new attribution.

The reusable command is `node scripts/verify-survival-models.mjs`; it regenerates its bounded input and invokes `scripts/verify-survival-models.py` with the separate survival interpreter and that input path. The example generator is `scripts/generate-survival-examples.py`, run with the existing `lesson-tools` Python for formatting and invoking `survival-tools` explicitly for actual execution. Completed results do not prescribe another routine full rerun.

## Actual browser and image review

The original-font Edge review used 1440, 390 and 320 pixel viewports. [Behavior evidence](evidence/survival-browser-behavior.json) records 26 changed states, 69 keyboard actions, all 16 section-anchor arrivals and exact question/code/output pairs for all 17 programs at each width. It covers resets, event/censor changes, unsupported medians, exhausted risk sets, zero-age rates, the exact log(2) preset, tie methods, crossing/pooled hazards, rank reversal/ties, rare observation weights and competing-event transitions. No page error or failed request occurred. Its initial mobile formula-overflow findings remain visible in the historical record rather than being relabelled a complete pass.

[Final focused reading](evidence/survival-browser-final-reading.json), at 15:26 UTC, closes those display findings on the final teaching source. All 16 display equations fit at all three widths, with no KaTeX error or page geometry overflow. Long formulas use equivalent lines and explicitly introduced local symbols. The correct module-scoped prerequisite link, readable prose/label spacing and exact DOM Cox preset are included. Tables and code keep deliberate horizontal scrolling where needed. The models, program module, CSS and four figures were unchanged, so their passing numerical/behavior results were reused. A subsequent [root-owned integration amendment](evidence/survival-integration-amendment.json) removes a redundant named-export keyword for metadata extraction and entity-escapes greater-than characters in two JSX text nodes. Exact archived pre-amendment bytes and canonical parsed AST conservation bind this change without claiming another native or browser run.

The four static figures have a separately attributed [preflight record](evidence/survival-figures-preflight.json): exact values, all three widths, original fonts, keyboard disclosure and eight images actually opened by their author. Their integration is included in the full route's geometry and source checks. The main author opened the selected ordinary-reading, formula, area, lane, hazard, pair and output captures listed in the packet. A stale or misleading preview appeared to omit two gold Cox bars at 320 pixels. The [final paint evidence](evidence/survival-cox-paint-proof.json) binds actual 18-pixel bar heights and normalized widths to exact gold PNG pixels; the fresh immutable image was opened by both author and root. There was no missing-fill production defect.

Representative final images are `scratch/survival/browser/cox-gold-bars-verified-320.png`, `final-logrank-equation-320.png`, `final-score-equation-320.png`, `final-censor-equation-390.png`, `final-cox-reading-1440.png` and `final-competing-reading-1440.png`. Older retained mechanism/output captures are labelled with their earlier source version; subsequent spacing-only prose changes are not silently attributed to those images.

## Resolved review findings and limits

The Rossi data's financial-aid assignment was randomized; the descriptive fitted example is not itself a complete causal analysis. The Cox range uses `step="any"`, keeping log(2) consistent between its native control and calculations. Censor-only uncertainty rows are described as risk-table rows. The competing-risk control displays days rather than array indices. Numerical tie tolerances, positive-lifetime left-censor bounds and linked prerequisites are explicit. The final source preserves these corrections and the declared censoring/support assumptions.

These checks verify finite model and program contracts. They cannot certify independent censoring, causal identifiability, future predictive performance, or clinical utility from observed data. Videos are annotated from inspected official metadata/chapter descriptions; full playback is not claimed. The primary-source ledger records actual written sections read. No full neural implementation, unavailable Fine–Gray API, exhaustive survival-method coverage or fabricated performance comparison is claimed.

The root owns the final independent review and integrated build. Retention is bounded to source archives, compact results, necessary runtime/data inputs and selected images; unused intermediate captures and the installed duplicate draft are retired with their historical status recorded in the author packet.
