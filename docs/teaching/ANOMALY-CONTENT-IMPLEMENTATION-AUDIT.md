# Anomaly Detection: prepared-content implementation audit

14 September 2026. Scope: Classical ML position 15 only, within the user's PCA-through-GMM verification request. The reviewer first compared the complete prepared manuscript and specifications with the production lesson and its owned implementation, then made the focused repairs listed below. Root owns the production build, browser verification and shared phase-ledger updates. This is a heuristic learner-experience review, not a learner study or user acceptance.

## Coverage comparison

All thirteen manuscript sections, all five complete Python programs, all ten changed practice tasks A–J with closed hints and solutions, the annotated primary/alternate resources, offline data and the correct GMM continuation are present. This is substantive coverage conservation, not merely a section-count check.

| Prepared content | Implemented teaching and assessment |
| --- | --- |
| Introduction and §§1–2: observation, point/contextual/collective cases, outlier versus novelty task, reference/calibration/test roles and score direction | Intro, explicit first-pass route, concrete machine scenario, task table, shutdown checkpoint, API table and provenance figure. Annotation-to-evaluation geometry repaired below. |
| §3: first-cut probabilities, terminal population correction, exact expectations versus finite forests, actual fitted sample, masking and axis-aligned invariance | First-cut figure, editable isolation investigation and path trail, harmonic/path derivation, exact table, complete seeded program, practices A/B. All offered depth caps now have editable cuts. |
| §§4–5: neighbour-owned floor, inverse average reach, dimensionless LOF, two local scales, ties/duplicates and training identity versus new query | Radius figure and exact companion table; editable neighbourhood investigation; fitting-mode comparison; two complete LOF programs; practices C/D. Added the specified linked neighbour strips and repaired feedback after reference edits. |
| §6 and optional §9: RBF similarity, weighted support, rho, disconnected positive regions, nu and primal/dual/KKT conditions | Two-anchor investigation, complete program, formal deeper branch and practices E/G. Prediction disclosure and numerical-resolution wording repaired. |
| §§7–8: applications, hypothetical population arithmetic, threshold ties, ranking versus policy, row versus event metrics | Four representation-specific applications, queue investigation, strict-threshold ruler, precision and budget reasoning; practice F. Both queue questions now commit before either result is disclosed. |
| §10: pinned NAB observations, duplicates, exact lag, chronology, baseline, fixed detectors, row/window workload and annotation limits | Original downloadable CSV/JSON/license; full offline program and retained executed output; real trace, exact precomputed outcomes, editable quantile/direct-level investigation, worked comparison and practices H/I. Window rows are now all reachable through bounded pagination. |
| Optional §11, §12 and §13 | Local LOF bounds, costs and frozen-reference update policy retained; practices A–J and complete solutions retained; readiness table and GMM density/responsibility bridge retained. |
| References and another way to learn | All named source families retained: survey, three original-method papers, current library APIs and novelty example, Goix recorded talk plus slides, pinned NAB descriptions/windows and duplicate report. The limits of the prior video review remain explicit. |

Recorded adaptations remain reasonable: formulas are re-set for narrow columns without omitting their terms; the radius/tie tables gain diagrams; the lab adds the 0.975 comparison; native temperature results are precomputed rather than fitted in the browser. The direct threshold mode clearly discloses snapping to a finite table of levels; it does not claim arbitrary-score recomputation. The four-row practice-B terminal example stays a manual calculation outside the five-position lab, as its solution explicitly says. The optional LOF ×10 button is replaced by the complete changed-units exercise. The isolation path uses a selected reference identity rather than an independent new-query coordinate; the retained Python program covers arbitrary queries. None of these choices removes a promised core derivation or practice solution.

## Correctness findings and repairs

1. **Edited LOF references kept the original distance story.** `AnomalyDetectionLabs.jsx`, `LofNeighbourhoodLab`: options still called query 4 closer and query 17 farther, and feedback assumed the original tight/loose groups after coordinates changed. Set P3 from 20 to 16, leaving `[0,1,2,16,24,28]` with k=2: nearest distances become 2 and 1 while factors are 35/24 and 11/12. Labels now name the query identities; captions report current distances and ratios; the original k=3 explanation is explicitly conditional on the original references. An independent Python `Fraction` calculation, importing no lesson helpers, confirmed both changed values and their reach/density intermediates.
2. **Different neighbour sets were said to imply different scores.** Production lesson §5 now says they *can* differ and gives the equal-score null: coordinate 0, k=2, both 7/8. This agrees with the existing fitting-mode model and manuscript distinction.
3. **Annotations appeared to enter scoring/alert creation.** `AnomalyDetectionFigures.jsx`, `ProvenanceFigure`: added an explicit evaluation node. Alerts and annotations meet there; annotations have no arrow into scoring, fitting or threshold selection. The nearby reference-period prose again states that absent annotations do not certify fault-free data.
4. **A finite kernel scan was presented as an exact empty-region result.** At large admitted anchor/gamma settings, positive intervals can be too narrow for the scan and floating-point arithmetic to resolve. The empty-scan feedback now describes that limitation rather than claiming all other points lie outside. The 1e-12 classification tolerance is explicit; the filled interval widths no longer have an artificial one-pixel minimum. Corresponding coordinate scaling now explicitly includes the query, not only the anchors.
5. **Zero unmatched workload was labelled zero total alerts.** The temperature choice now says “No unmatched alerts,” matching the outside-window quantity actually scored. Inside-window alerts need not vanish when unmatched workload is zero.

## Learner-experience findings and repairs

1. **Kernel answer visible before prediction.** Numeric decisions, contributions, accepted-region text and accessible descriptions were rendered unconditionally. Before commitment/reveal the lab now shows only the reference anchors and query on a number line. Result curves, arithmetic, region and descriptive answer appear after reveal and disappear when inputs become stale. This restores the saved predict/manipulate/observe contract without removing the geometry.
2. **One queue result disclosed the second unanswered question.** Revealing precision also exposed the alert total and budget result. Both precision and workload must now be committed for the current input snapshot before either reveal is enabled. Each prediction retains its own assessment.
3. **Most retained temperature detail rows could not be inspected.** `.slice(0,60)` permanently hid the rest of each 711-row block. Added keyboard-operable previous/next row pages and announced ranges, keeping at most 60 rows in the table. Window/filter/reset actions restore page one; sparse filtered results clamp to a valid page. Alert filtering still waits for commitment and reveal.
4. **Training/query identity contrast had only tables.** Added two aligned number-line strips using actual neighbour identities: the fitted row's own ID is crossed out, while the distinct query can use the coincident reference. Exact tables remain beside the explanation.
5. **Offered isolation depth caps exceeded available cut controls.** Depths 4 and 5 now expose corresponding editable cuts. The exact expectation and the independently chosen path remain distinct.
6. **A selected LOF radius could draw past its axis.** Radius bars keep their exact length but choose the direction that fits the number line. This changes drawing placement, not the reachability calculation.

## Learning-experience checklist

1. Route: first-pass route exists after the intro; optimization and costs are labelled deeper branches. The core route reaches real-data interpretation independently.
2. Cautions: main score/decision caution has a clear early home; method- and annotation-specific limits remain at their mechanism. Some repeated caution prose remains, but deleting valid conditions is unnecessary for this verification. Code does not print cautionary sentences.
3. Real question: the hot-machine scenario returns through an openly reusable real series and an honest workload comparison, including a competitive simple baseline.
4. Investigations: the six jobs differ; coordinates, k, anchors, query, rates and thresholds are genuinely editable. Original contrasts/nulls remain. This review repaired result leaks and added a reference-edit transfer case.
5. Figures: the mechanisms have inline support. The annotation flow and identity comparison were repaired; final desktop/narrow perceptibility is assigned to root's browser pass rather than inferred from JSX parsing.
6. Connections: exact path versus seeded forest, repeated LOF ratios, coordinate scaling and the GMM handoff are explicit. Canonical scope and routed ownership remain as in the design record.
7. Code: all five programs remain complete and unchanged; the isolation example retains three short validation checks already documented in the prior review. No new defensive machinery was added to displayed code.
8. Practice: all A–J prompts/hints/solutions are retained, with changed numbers, exact target values and reporting/representation transfer.
9. Screenshots: this reviewer did not open a new browser or claim visual acceptance. Root must inspect the new provenance and fitting-mode figures, the kernel unsolved/revealed states, queue commitment gate and row-pagination controls at desktop and narrow widths.

## Checks, reuse and closure

Read the complete current manuscript, visual specifications, production JSX, models, examples, four component files, CSS, provenance and relevant current policies; inspect the design/earlier review and exact-source evidence. Reused unchanged model/native/data results, not the old browser result for changed components: [model evidence](evidence/anomaly-detection-models.json), [executed programs](evidence/anomaly-native.json), [native data preparation](evidence/anomaly-temperature-data.json). The model, generated dataset, displayed programs, three served assets, shared controls, CSS and blueprint match the preceding browser evidence's hashes. No native refit or full model rerun was necessary because no arithmetic, data or displayed program changed.

Executed this review: independent changed-reference rational calculation; JSX parsing through esbuild for all four changed JSX files; `node --check` on the amended browser verifier; scoped `git diff --check`. These passed. New browser regressions cover the reversed nearest-distance relation, unset/stale kernel answer, both queue commitments, two identity strips, all offered depth controls, access to the final row page, keyboard paging and correct unmatched label. Root owns running that verifier against a production build and recording its result. Do not treat this source-only closure as a successful browser run.

The remaining documentation issue is shared status reconciliation: the anomaly ledger's next-action sentence still describes GMM as pending, and its destination note retains an old pending paragraph alongside its implemented status. Root owns correcting those present-tense statements without rewriting historical evidence.

## Source versions

SHA256 values are recorded below for the prepared inputs and final topic-owned implementation reviewed here. The subsequent production browser evidence supplies the integration version; any later source repair must update that evidence rather than silently reusing these checks.

| File | SHA256 |
| --- | --- |
| `docs/teaching/drafts/anomaly-outlier-detection-isolation-forest-one-class-svm-lof/lesson.md` | `aac80854ec802bcd384e96bd5fb42648709caf003362f20d665d5f4917c8c100` |
| `docs/teaching/drafts/anomaly-outlier-detection-isolation-forest-one-class-svm-lof/visual-specifications.md` | `00f022a527237228e5063ef1512b4e4cc4daf7bfb1ea8f4ec79531460620447b` |
| `docs/teaching/drafts/anomaly-outlier-detection-isolation-forest-one-class-svm-lof/dataset-provenance.md` | `cbe2729d2b85fee81598e7374a3a442cc5d33f788c82894d9ce5eafc3dcf821e` |
| `src/learn/data/topics/anomaly-outlier-detection-isolation-forest-one-class-svm-lof.jsx` | `c6ef41d5727c1587f1e4f6e28f08dc30b7a42b19bad33c2a53cbd24d3cf7c932` |
| `src/learn/data/anomaly-detection-models.js` | `7d8a2dd0225426f507a2e57ff7d8dcf2c1f3e6233c7097b0b6e9d7bafa782d67` |
| `src/learn/data/anomaly-temperature-data.js` | `750d462024c0321687bd1b98c8f0755616351f60d5b9465d1f6dfc59d53625d4` |
| `src/learn/data/anomaly-detection-examples.js` | `472d6bfbc707a848fb77795444ab6f0666639e44c75d009797c8ef781a8066b2` |
| `src/learn/components/lesson-labs/AnomalyDetectionLabs.jsx` | `8bbcb33caef37586640afa91809d6b45b8d66fac60c5db92d9b656dcfd5f3a04` |
| `src/learn/components/lesson-labs/AnomalyTemperatureLab.jsx` | `8196d9141e801a2460f861f93b7749ab36c1fafefb9701a49fce493dbc439b82` |
| `src/learn/components/lesson-labs/AnomalyDetectionFigures.jsx` | `0a171ecf655d23c712af7e1a70a8310ae34e3ea655a3bae7052c5e88db6a7884` |
| `src/learn/components/lesson-labs/AnomalyDetectionShared.jsx` | `7678139fc40508674b417f1b62629a29c6687c8545cbcfe0fbcc33a3012e8143` |
| `src/learn/components/lesson-labs/anomaly-detection-labs.css` | `4f066dc8dad28ce9921e36f1085555514f10066a35b156d739fe1f3d5f86b164` |
| `scripts/verify-anomaly-detection-browser.cjs` | `04ac76d0e38b091cc2b3e3a5eb0a672b9202087f096d0efee67c07ae38e77602` |

## Coordinator browser closure, 14 September 2026

The reviewer-only browser-pending statements above describe the state and responsibility at the time of that review. The coordinator has now completed the affected production browser checks and inspected selected informative captures; the [five-topic closure](PCA-THROUGH-GMM-IMPLEMENTATION-AUDIT.md) names the cases, actual visual observations, evidence reuse and limitations. Its source-comparison evidence binds the final repaired files. This closure does not attribute the coordinator's browser work to the independent reviewer.

## Final fitting/query presentation correction

The coordinator's revealed 320 px capture exposed dense, undersized labels in the new identity strips and long table captions trapped inside horizontal scrolling. The reviewer inspected both desktop and phone captures and made a local presentation repair in AnomalyDetectionLabs.jsx: larger fanned labels with leaders preserve exact horizontal coordinates, the query has a separate upper lane, and wrapping native-text captions enumerate selected IDs/coordinates and the excluded identity. Long explanations now precede short table captions outside the scroller. Model, reference points and arithmetic are unchanged. The coordinator reran the 13-case browser verifier and inspected the final revealed 320 px capture. Final source hashes live in the browser and integration evidence; earlier hashes above retain the earlier source-review checkpoint.
