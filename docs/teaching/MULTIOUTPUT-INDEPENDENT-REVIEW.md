# Multi-Label & Multi-Output: independent review

11 September 2026. Root independently read the complete current lesson and its pure model, then reviewed the author evidence and four selected final images. [The versioned record](evidence/multioutput-independent-review.json) closes this topic with no open material finding. Shared production integration remains separate.

The lesson follows one observation through target schema, available information, annotation masks, error denominators, marginal fitting, decision loss, chain factorization, association, threshold selection, set representations, numerical outputs and a frozen report. Continuous-output learning receives substantive treatment rather than a brief mention. Deeper feature-sharing and compression branches remain accessible after the basic workflow. Twelve changed tasks include calculations, diagnosis and a complete changed-cost report.

The review checked the distinction between dependence and parameter sharing, marginal versus joint actions, greedy chain paths versus marginal probabilities, missing labels versus negatives, empirical threshold ties, weighting versus calibration, coherent action constraints, squared-loss conditional means, rank/intercept assumptions, shared split units, grouped shrinkage and sparse observations versus a sparse conditional mean. The held-out hand-authored corpus is clearly a teaching fixture. No accuracy correction was required.

Eighteen complementary cases passed: consistent row/label permutations; entirely unknown annotations; count replication and marginal Hamming-risk minimization; pooled covariance as variance of the conditional mean; target-translation invariance of split objectives; and rotation equivariance of group-norm shrinkage. All seven production hashes matched the frozen author version. The broader unchanged native/program/browser suites were reused, including the documented final equation-layout amendment.

The reviewer opened the author's final 320px reversed probability tree, shared numerical split, grouped coefficient geometry and ridge-equation reading captures. Branch probabilities, label order, physical units, data points, penalty geometry, formulas and surrounding prose were readable. This is independent assessment of existing captures, not a new execution of all browser interactions. No learner study, broad deployment guarantee or user acceptance is claimed.
