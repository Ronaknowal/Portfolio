# Weighted graph objectives — verification record

10 September 2026. Stable topic `shortest-paths-spanning-trees-topological-ordering`, actual DSA module position 15. **Authored implementation and local verification complete; source frozen for root integration.** Root owns shared registration/build and rollout status. Publication alone was not used as a completion claim.

## Scope and teaching assessment

The former individual brief was a plan, not a complete lesson. The new body separates three objectives before algorithm names. It follows candidate relaxation into Dijkstra, layered Bellman–Ford and target-specific negative-cycle influence; optional specialized route branches; cut exchange into Kruskal/Prim; and ready dependencies into Kahn/DFS and DAG routes/schedules. The title and stable ID remain accurate. There is no module reorder or prerequisite detour.

Complete native programs teach: lazy Dijkstra/edge witnesses; layered Bellman–Ford/tight-edge finite witnesses; edge budgets; 0/1 deque; Floyd–Warshall; Johnson; Kruskal/DSU; lazy Prim/MSF; dense implicit Manhattan Prim; Kahn and explicit-frame DFS/cycle witness; signed DAG shortest paths; earliest schedule/critical-chain witness. There are twelve displayed standalone programs because several use shared within-topic helper text; no reader imports another lesson's code. The programs use only Python's standard library and support Python 3.10+; checked in Python 3.12.14.

Independent local practice changes route finalization, negative-cycle reach, tied cut/cycle quantifiers, constant edge shifts, bottleneck objectives and constrained scheduling. Hints and explained solutions begin closed. Seven curated official LeetCode statements cover different output/constraint adaptations rather than a quota. A* physical planning has a verified existing future owner; a destination note makes the prerequisite and reopening/collision gap explicit. The unresolved broad bit-manipulation inbox is not forced into this graph topic: 0/1 edge weights do not require teaching bitwise operations.

## Model and visual contracts

| Representation | Data, controls and invariant | Boundary / interpretation |
| --- | --- | --- |
| Dijkstra directed graph + physical priority ledger | Editable bounded directed integer edges; source/target selection; each relaxation/extraction snapshot updates distances, parent, current edge, final labels and entries. Current B2 and stale B10 coexist. | Nonnegative validation, strict improvement, sorted priority view explicitly differs from heap storage. Full trace permits stale inspection. Selected path is shown only after target finalization. Empty graph and isolated source remain valid. |
| Bellman–Ford generations + affected graph | Same exact edge list supplies every candidate from the previous row. Six vertices yield rows 0…5 and a sixth detection classification. Source selection changes reachability. | Negative cycles influence only reachable downstream targets. Seeds and affected descendants are not presented as an actual cycle-vertex list. +∞, finite candidate and −∞ have distinct meanings. Native keeps two rows; browser retains bounded teaching history. |
| Kruskal/Prim forest investigation | Accepted occurrence IDs, costs and DSU groups come from traced joins. Prim tracks visited vertices and physical crossing entries; Kruskal exposes global remaining candidates. | Undirected editable graph, negative/tied costs, empty/disconnected forests. A rejected current edge is amber while accepted edges are green. Queue display sorting is not a native complexity claim. |
| Ready dependency investigation | Learner selects any remaining zero-indegree job; output, remaining indegrees and graph labels update. Undo and reset rebuild the exact applied state. | Kahn residual is distinct from active-stack cycle witness; C↔E can block F without putting F on the cycle. No weights are implied for ordering edges. Empty graph has all six jobs ready. |
| Objective comparison | Same three vertices and weights compare MST total4 against source-A tree total5 and A→C distances4/3. | Edge costs and route costs are different quantities; not sampled/measured data. |
| Critical-path timeline | Native-aligned exact jobs [3,2,4,6,2,1] and dependency fixture produce starts [0,0,3,2,8,10], finish11 and chain B,D,E,F. | Bars share modeled 0…11 time scale. Unlimited capacity, fixed nonnegative durations, only dependencies and no release/setup constraints. No runtime benchmark claim. |

Editor: A–F fixed, max12 edges and1,000 characters, integer weights−25…25 (Dijkstra0…25), blank graph accepted, exact uppercase vertices, no self-loop/duplicate pair. Directed opposite edges are legal and drawn separately. Native graph helpers preserve parallel edge IDs and self-loops; their algorithm behavior is explained/tested. Draft edits apply atomically; invalid edits retain applied graph. There are no timers, network computations, random geometry or runtime dependencies beyond the existing React/lesson elements.

SVG direction/weights and state meanings are repeated by exact HTML tables and nearby legends. Layout coordinates never determine costs. Small graph geometry uses two columns and three rows; the seven-column generation table fits320px and retains local keyboard scrolling if enlarged text needs it. Every trace has previous/next/restart and finite immutable snapshots. The manual dependency lab has ready choices and undo rather than another generic replay box.

## Independent execution evidence

- `node scripts/verify-weighted-graph-examples.mjs`: **12 exact stdout programs passed**, then Python independent oracles passed **697 signed/nonnegative/binary/budget source cases**, **220 exhaustive spanning forests**, **160 order/cycle/schedule graphs**, **80 implicit geometric graphs**. The all-pairs checks compare Floyd, Johnson when feasible and independently enumerated simple paths/negative cycles; Johnson must reject any negative cycle. Finite route witnesses are checked edge by edge, by original occurrence ID, total and no repeated vertex. Both returned forest variants also match an independent all-simple-path minimax oracle between every vertex pair, validating the bottleneck application with ties and negative weights. Explicit empty graph/source contracts and parallel/self-loop cases are included. Evidence/programs: `scratch/weighted-graph-native-verification/`.
- `node scripts/verify-weighted-graph-models.mjs`: **1,056 signed/nonnegative source cases**, **320 exhaustive spanning forests**, **200 permutation/cycle/schedule checks**, immutable snapshots and parser contracts passed. Oracles enumerate simple paths/cycles, connected edge subsets, valid permutations and every dependency chain. They do not repeat the optimized implementation's recurrence as their sole oracle.
- Source formatted through the installed Babel generator; normalized AST equality proved for `weighted-graph-models.js`, `WeightedGraphLabs.jsx`, `verify-weighted-graph-models.mjs`; model check rerun afterward. CSS formatted through PostCSS without rewriting declaration values. No dependencies added.

Accuracy decisions found during authoring: layered row-copy Bellman–Ford is described as O(V(V+E)), not blindly assigned O(VE) when isolated vertices dominate; lazy priority storage/bounds differ from indexed decrease-key; finite Bellman witnesses use tight-edge BFS to avoid mixing budget-generation parents; Floyd returns costs and refuses to imply route recovery; Johnson rejects globally versus pair-specific Floyd classifications; a negative installation weight is legal for a forest even though unrestricted route walks can repeat a negative undirected edge; a light tied cut edge is safe for some compatible optimum rather than mandatory in every optimum.

## Sources actually reviewed

- Princeton's [weighted shortest paths chapter](https://algs4.cs.princeton.edu/44sp/): full returned chapter read, especially relaxation, nonnegative finalization, DAG/critical paths and the precise source-target negative-cycle proposition. Its broad earlier negative-cycle phrasing and mixed lazy/indexed complexity prose were investigated rather than repeated.
- Princeton's [MST chapter](https://algs4.cs.princeton.edu/43mst/): cut/tree principles, tied-weight simplification, lazy/eager Prim, Kruskal, disconnected forests and bottleneck exercise. General tie-safe proof is derived locally; Java API is not confused with native Python.
- Princeton's [digraph chapter](https://algs4.cs.princeton.edu/42digraph/): active cycle, reverse finish order and indegree exercise; vertices, parallel/self-loop definitions and valid-order theorem.
- [Python heapq priority notes](https://docs.python.org/3/library/heapq.html#priority-queue-implementation-notes) and [deque](https://docs.python.org/3/library/collections.html#collections.deque): ticket payload ties, lazy invalidation and endpoint operation costs. Current docs are3.14.7; examples use older min-heap/deque APIs compatible with3.10.
- [MIT2011 Dijkstra video page](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-16-dijkstra/) and its linked seven-page PDF: all PDF pages' extracted text read, original video description checked. Full playback not claimed. Notes' decrease-key assumptions differ from local lazy heap. This is the annotated spoken alternative, with a verified written companion.
- [MIT2020 Johnson notes](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/resources/mit6_006s20_lec14/): all five PDF pages read, including failed uniform shift, telescoping potentials, supersource and conversion. Full local implementation/oracles support the optional scope addition.
- Official LeetCode direct statements checked: **743 Network Delay Time (Medium); 1584 Min Cost to Connect All Points (Medium); 210 Course Schedule II (Medium); 787 Cheapest Flights Within K Stops (Medium); 2050 Parallel Courses III (Hard); 1334 Find the City With the Smallest Number of Neighbors at a Threshold Distance (Medium); 1368 Minimum Cost to Make at Least One Valid Path in a Grid (Hard)**. Relevant constraints, direction, index conversion, impossible result, threshold tie, grid arrows and unlimited parallelism inspected. Statements accessible; no account/submission/editorial/playback claim.

## Browser evidence

`node scripts/review-weighted-graph-lesson.cjs` **passed at1440,390,320px**, with actual interactions at each width:28 combined Dijkstra states across normal/isolated/zero-cycle fixtures,21 Bellman states across changed source and removed-cycle cases,39 forest states across both methods/signed/disconnected/empty cases; manual topological B-first order, undo, C↔E cycle versus residual C/E/F, and empty-graph readiness. Nine route links were clicked and their actual headings arrived in view;12 native program blocks and7 official practice destinations were present. Enter/Space stepped controls, native disclosures and practice hints; matrix focus had a visible outline. Invalid negative/duplicate drafts were rejected while prior applied states remained intact. Page errors were empty and page width equaled viewport width at each size.

Evidence: `scratch/weighted-graph-lesson-review/results.json`,27 screenshots in the same directory, and `final-source-hashes.json`. Actual screenshots opened: desktop Dijkstra improvement and objective comparison;390px reading entry and directed cycle;320px Dijkstra improvement, negative-cycle graph/matrix, Prim forest, objective comparison and timeline; desktop timeline. After visual refinements, the full browser suite was rerun and final desktop Dijkstra/390px cycle/320px negative-cycle screenshots were opened again. These show every vertex, weight and cycle direction with no page clipping. Optional code/algorithm branches remain native disclosures, not an undifferentiated wall of algorithms.

Defects found and corrected during review:

1. Implicit label lookup included descendant option or textarea content; explicit accessible names now identify source, target, growth rule and edge draft exactly.
2. Stroke-scaled arrowheads became oversized on highlighted edges; fixed user-space arrowheads preserve direction without covering vertices.
3. Floating state words crossed edges; a trial background obscured an upward reciprocal arrow. Final source removes those redundant words from the geometry, retaining vertex-state outlines/fills, per-node titles, exact state tables and nearby legends. Actual arrows are fully visible in the final opened image.
4. A small generation matrix needlessly hid F at320px; its own minimum width now fits all seven columns without reducing the existing readable text size.
5. Before the detection row, a finite number is now explicitly called a budgeted candidate and ∞ means no route within that budget; unrestricted classification wording is reserved for the final row.

`node scripts/verify-curriculum.mjs` passed after parent registration:28modules,1218stabletopics,308individualbriefs,7paths. Shared build remains the parent's integration check; this agent did not run a concurrent production build or alter shared navigation/manifest/ledger files.
