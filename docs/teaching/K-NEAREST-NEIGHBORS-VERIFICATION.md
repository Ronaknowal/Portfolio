# K-Nearest Neighbors: author verification

11 September 2026. This is author verification of the current scoped rewrite; independent review and final increment integration are separate. Linux remains the user-approved teaching reference.

The original lesson's useful breadth is retained: explicit classification/regression, distance families, Voronoi geometry, complete NumPy estimator and KD search, held-out tuning, dimensionality, asymptotic risk, approximate candidates and applications. The rewrite repairs the original worked distances, incomplete program context, unsupported cosine/BallTree combination, fabricated validation trends, and overly broad consistency, dimension and performance claims. Stable identity, title, publication mapping and module position are unchanged.

Six investigations teach different mechanisms: spatial retrieval and class aggregation; feature units; numerical local averages; exact branch-and-bound; candidate recall versus prediction; and the uniform-cube volume model. Inline figures explain fit/inference, cosine direction and the actual validation experiment. Twelve changed tasks have separate hints and solutions, including a complete independently changed evaluation report. Source links supplement the self-contained explanation; video inspection limits are stated in the design.

## Executed evidence

- [Native review](evidence/knn-native-review.json): 180 neighborhood states, 152 regression states, 625 exact-search queries, eighteen changed library comparisons, invalid/boundary inputs and all eleven complete program/output identities. Separate NumPy/sklearn calculations verify the browser model. Programs ran in Python 3.12.14 with NumPy 2.3.5 and sklearn 1.9.1; unchanged code executions were reused rather than repeated for formatting.
- [Browser review](evidence/knn-browser-review.json): actual Edge at 1440, 390 and 320 pixels; six investigations, all thirteen controls and resets, keyboard changes, twelve practice hint/solution pairs, eleven code/output pairs, thirteen section anchors, no KaTeX errors, no document overflow, and SVG labels within their bounds.
- [Author record](evidence/knn-author-review.json): seven production source hashes, evidence hashes, eight opened representative captures, and the correspondence between the chart's eight values and the executed validation output.

The opened captures show equal-scale spatial geometry, ordered neighbors and votes, units contributions, local regression, KD search, candidate coverage, dimension/volume, and the validation graph with its exact table. A fixed navigation strip in some tall element captures is a screenshot stitching artifact; the live page's local figures and controls passed the recorded viewport checks. Narrow coordinate figures scroll inside their own container with a visible cue and accessible numeric equivalents.

## Limits and next action

These are deterministic example, model, code, source and browser checks, not a learner study or a guarantee of performance on other datasets. The validation chart reports one seeded experiment and makes no universal U-shaped or timing claim. The approximate-search lab explicitly restricts a tiny candidate pool; it does not simulate HNSW. Asymptotic risk statements retain their conditions and do not imply finite-sample guarantees.

The [independent review](K-NEAREST-NEIGHBORS-INDEPENDENT-REVIEW.md) is closed against the amended source below. Continue with the ten-topic integration; reuse the recorded passing evidence for unchanged mechanisms.

## Focused input-contract amendment — 11 September 2026

Independent review identified sparse-array validation and, for KNN, numerical norm edge cases. The final corrected source identities and focused checks are recorded in evidence/knn-input-amendment.json. Forty-five JavaScript input/conservation cases passed. KNN additionally passed six native distance scales, two explicit overflow cases and actual browser checks of the two changed code/output blocks. Unchanged full author checks are reused; independent closure binds this amended version.

The [working-file retention record](evidence/knn-working-file-retention.json) records removal of eleven temporary Python extractions after verifying their complete contents against the lesson programs, allowing only Windows newline differences. The source examples, generator, actual execution cache and final evidence remain. The generator recreates working extractions when changed code needs execution.
