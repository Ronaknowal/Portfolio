# GMM prepared-content implementation audit

14 September 2026. Independent source review within the user's PCA-through-GMM verification request. This reviewer did not author the GMM manuscript, implementation, or current corrections. Scope: the complete manuscript, visual specifications, production lesson, pure models, three investigations, six figures, shared controls, CSS, displayed programs, destination note, and documented implementation departures. This is a heuristic learning-experience assessment, not a beginner study. The coordinating agent owns the current build, browser execution, screenshot inspection and ledger closure.

## Coverage and current corrections

All twelve manuscript sections, three complete programs and eight changed practice tasks remain in sequence. The comparison evaluated explanations and mechanisms, rather than treating those counts as proof of coverage.

| Prepared learning requirement | Current production treatment and assessment |
|---|---|
| §§1–2: meaningful density task, hidden selector, prior weight, density and responsibility | The flower-density question and first-pass route lead into the selector figure, Bayes normalization and the three-location contrast. I1 changes the actual means, weights, variance and measurement. Its two saved locations explicitly distinguish decisive responsibility from low total density. No category-probability claim is substituted. |
| §3: weighted EM statistics, around the new mean | F2 preserves the four fractional allocations and effective counts. The current body restores the full weighted numerator and second-moment arithmetic: count 2, numerator −2.689649316, second moment 2.5, variance 0.691446639. The matrix is retained inside the figure rather than duplicated in prose. |
| §4: runnable mechanism, underflow, floor, null and collapse | The complete NumPy program teaches log-sum-exp and constrained weighted updates. The fixed floor is distinguished from additive library regularization. Identical-component stationarity is explained separately from a small observed objective change. F3's shared-window height scale now preserves the peak and width contrast. |
| I2: prediction, E/M half-steps, old allocations and changed data | The input draft must be applied, edits retire the recorded answer, reasons are frozen with a prediction, and old responsibilities stay labelled after the M-step. Pending drafts cannot advance or record. A 50-cycle cap bounds history. See the tick-density finding below. |
| §5 and I3: covariance geometry, inverse, projections and families | The inverse and equal-Euclidean-distance example connect to the explicit Mahalanobis quadratic form. I3 now constructs perpendicular projection feet and lists squared-coordinate/eigenvalue contributions. Its zero-correlation comparison uses the same two points. F4 shows actual matrices and computed eigen-contours on equal physical axis scales. |
| §§6–7: declared evaluation rule and real-data result | All sixteen candidates, K=1 baseline, training-only transform, fixed split, reserved result and narrow-component diagnosis are present. F5 now discloses all 150 observation IDs, measurements and split roles. The validation-selected mixture's worse test result is retained. Species remain separate from fitting and selection. |
| §8: score versus action, sampling, moments and practical costs | The earlier anomaly lesson is connected to a declared illustrative threshold. Forward sampling chooses a component before a Gaussian draw; within/between variance remains explicit. Costs and diagnostic limitations are given without claiming a universal alert cutoff. |
| §9: touching bound and support | Q, entropy, ELBO, Jensen and the old/new bound chain remain connected to the exact earlier EM cycle. The omitted support qualification is now explicit: zero allocations cannot cancel positive joint terms in the displayed equality. |
| §10: k-means limit, conditioning and Bayesian weights | The actual negative-distance softmax equation and conditional mixing-weight normalization have been restored. Conditional means and variances belong to a fixed joint model, with the worked u=1 result and changed u=0 practice. Finite-variance EM, hard assignments and library spherical covariance remain distinct. The finite-cap Bayesian program and threshold-dependent active counts remain qualified. |
| §§11–12: transfer, readiness and alternative learning | All eight tasks retain changed inputs, closed hints and explained solutions, including an exact reduced-grid variation. The core/deeper readiness distinction and the next manifold-learning topic remain intact. Resources include annotated primary texts, official documentation and a Stanford lecture/transcript; recorded access limits are not represented as watched video or read full text. |

The documented alternatives remain reasonable: splitting long equations preserves their meaning; the shared collapse window replaces the originally requested inset; printed matrices plus the covariance lab's explicit eigen-projections supply the matrix-to-shape connection without requiring linked per-cell controls; the real-data figure uses a true-scale fitted contour and magnified strip. These choices do not omit a teaching outcome. Final rendered perceptibility is a separate check below.

## Correctness review and complementary evidence

No unresolved substantive mathematical or content-conservation defect was found in the current corrected source. In particular:

- Derived the crossing polynomial independently from the difference of weighted log-Gaussian densities. A bounded Node calculation checked a linear root, two quadratic roots, one tangent root, no quadratic root, identical shapes with unequal weights, equality everywhere, and a legitimate root outside the displayed range. The changed practice root is 0.4506938556659451; both quadratic roots are ±0.6797779934458726. Returned finite roots give responsibility 0.5. The UI distinguishes no root in range from identical distributions, and equal weights are required for the everywhere-tie message.
- Independently calculated conditional weights as `w = 1/(1 + exp(2 − 2u))`, conditional means `u/2` and `3 + u/2`, and total variance `0.75 + 9w(1−w)`. Both u=0 and u=1 match the helper and published explanations. The hand EM weighted sum and second moment were also derived directly from logistic allocations, rather than obtained from the EM helper as their oracle.
- Checked 25 covariance projection cases: five correlations from −0.9 to 0.9 and five points, including the centre. The sum of squared projected coordinates divided by eigenvalues equals the independently expanded inverse form `(x² − 2ρxy + y²)/(1−ρ²)`. The SVG uses orthogonal feet and equal coordinate scaling. The zero-correlation axis convention is stated.
- Confirmed the ordinary separated fit reaches gain about 6.65e−13 on cycle 5 while its means still differ. It therefore takes the numerical-plateau explanation, not the identical-component explanation. The identical start has gain about −8.88e−16 and retains symmetry. Feedback distinguishes numerical tolerance from a global-optimum claim.
- Rebuilt the corrected collapse scale from the analytic Gaussian peak. Across all 241 plotted samples for each of the three curves, the vertical coordinates lie in [60, 144], preserving the peak instead of clipping it. The square-root density scale is explicitly labelled and exact peaks are tabulated.

The focused calculations ran from stdin and created no scratch artifact. They supplement, rather than duplicate, existing evidence. Current model, generated data and displayed-program bytes match the **81 grouped checks** in [gmm-models.json](evidence/gmm-models.json). All three program-code hashes and all three displayed-output hashes match the executed [gmm-native.json](evidence/gmm-native.json); the served CSV matches the refitted [gmm-iris-data.json](evidence/gmm-iris-data.json). Those native executions and sixteen fits were not repeated. Earlier source/resource analysis remains recorded in [GMM-INDEPENDENT-REVIEW.md](GMM-INDEPENDENT-REVIEW.md); that historical report is not represented as an execution against today's changed UI.

## Learning-experience review

1. **Route:** the first-pass plan appears immediately after the concrete introduction; proof, limiting arguments and conditional/Bayesian extensions are identified as deeper material. Readiness does not silently require the skipped proof.
2. **Cautions:** interpretation, optimization and evaluation cautions have sensible local homes. Later examples use those distinctions operationally. None of the three programs prints a generic cautionary paragraph.
3. **Real question:** the lesson asks whether the mixture predicts unseen flower measurements better than one Gaussian and answers with the actual reserved test, including the unfavorable outcome. The data units, sampling limitations and ID roles remain available.
4. **Investigations:** density allocation, half-step fitting and correlated geometry are different mechanisms with appropriate controls. The corrected result/choice handling prevents live regrading. I1 includes no-crossing and identical-shape alternatives; I2 distinguishes stationary symmetry from a separated plateau; I3 includes sign reversal and a zero-correlation comparison. The complementary calculations above check the key null/contrast mathematics; the current browser behavior remains the coordinator's responsibility.
5. **Figures:** the inline reading progression is selector → fractional allocation → collapse → covariance geometry/families → held-out model comparison → bound chain. These representations expose entities and transformations near the explanation. Quantitative baseline and scale choices are explicit. Source review found that I2 passed every iteration as an x-axis label, causing 51 labels inside a 286-unit plot at the supported 50-cycle state. **Source repair confirmed:** visible ticks now use an interval of `ceil(iteration / 5)`, always including the initial/current iteration. A focused check of cycles 1–50 confirms at most six ticks and both endpoints; the plotted polyline, markers, accessible description and history still retain every point. Rendered late-cycle inspection remains part of the coordinator's browser closure.
6. **Connections:** scalar weighted scatter becomes covariance; eigendirections connect to PCA; the later test-row responsibility returns to the early density/allocation distinction; the proof reuses the earlier exact EM cycle; anomaly scoring and the next manifold lesson preserve the module sequence. The restored softmax and conditional-weight equations close explicit prepared-content gaps.
7. **Code:** the first program exposes the mechanism directly; the Iris program includes the complete reproducible split/transform/baseline/selection pipeline; the Bayesian extension deliberately shares that program's established namespace. Validation is small and relevant.
8. **Practice:** changed weights, supplied allocations, numerical diagnosis, changed covariance coordinates, a restricted model grid, proof repair, a different small-variance point and conditional u=0 require transfer. Exact expected values and explanations are retained, rather than only links or answer buttons.
9. **Screenshots:** this reviewer did not run a browser or inspect today's captures. The coordinator must close visual/interaction evidence against final source, including wrong prediction feedback, pending inputs, both crossing roots, identical and nonidentical plateaus, projection selection, collapse height and late-cycle history. Source geometry checks cannot certify rendered phone/desktop readability.

## Source versions reviewed

The coordinator's subsequent visual pass found two additional presentation defects. A focused source reread confirms the covariance panel text now receives an explicit light fill and monospace font, its SVG width is bounded at 380 px, the mean label moves above-left of the origin, and unnecessary ±1 labels are removed while the coordinate geometry stays unchanged. The hand-arithmetic formula now names the already stated shares `r_A` through `r_D`, gives A's numeric multiplication in prose, and splits the first numerator's summation onto its own line. These edits preserve the full weighted-sum/second-moment explanation and its numerical results. This reviewer confirms the source changes, not the coordinator's visual observations or final browser result.

SHA256 values below identify the final source reread after those focused corrections, not a correctness proof. The labs, CSS and lesson body differ from the earlier mathematical-evidence snapshot because presentation changes came later. The pure models, data, displayed programs and outputs remain unchanged, so their recorded execution evidence remains applicable. Only affected browser/source evidence needs refresh.

| File | SHA256 |
|---|---|
| `docs/teaching/drafts/gaussian-mixture-models-gmm-em-algorithm/lesson.md` | `f5af9c95de0c3fd5066e3944fa92280b2b321e31c4bc3ae3e970df168e4d76d6` |
| `docs/teaching/drafts/gaussian-mixture-models-gmm-em-algorithm/visual-specifications.md` | `1d96bb1392b3f49f6f83aea42687ef3516f7d4e7d408d5a8de5a963fc9f6a85f` |
| `src/learn/data/topics/gaussian-mixture-models-gmm-em-algorithm.jsx` | `67538ac7e7b8da6d1b50a0ea301c05d467a2e4a22158498c470ce6df140c567c` |
| `src/learn/components/lesson-labs/GmmLabs.jsx` | `c941c458ace40303ea338fd556976e200be4caf006fec4876db0ac6d8f387531` |
| `src/learn/components/lesson-labs/GmmFigures.jsx` | `165f946b13449913f70f4476a1095e112b3443c5f197973825f5417e61bd5c22` |
| `src/learn/components/lesson-labs/GmmShared.jsx` | `57c29475518b3052a8bf4e50b9b23aaba413885569f2a8e59c8e36ee71bc15d0` |
| `src/learn/components/lesson-labs/gmm-labs.css` | `63588ff01ce924be969419cda2ded023227bb276e7a97989a908d727d675efd7` |
| `src/learn/data/gmm-models.js` | `a04b58a2b4e5e46ac6f4f1cc1069f71e49328c350d0f5909513a580abe00dcb2` |
| `src/learn/data/gmm-iris-data.js` | `2dfe2a5c028a0271f172b2ea38a2efbceae8eeeb0e35c95ca56d2f590b615611` |
| `src/learn/data/gmm-examples.js` | `811b9139f69595c30fc0dd42c7366bc6f1c6fbbe6e8aa5a39f2c7c3c1ea7105a` |
| `public/learn-assets/gmm/iris.csv` | `c6fd24e7f41dd55405cbc30f344e648c2f31eedfb24f9dcaba6290998bc26eb7` |

Disposition: content conservation, corrected source mathematics and the source-level tick-density finding are closed in this review. Current integrated browser/visual verification remains the coordinator's responsibility. No learner mastery or user acceptance is claimed.

## Coordinator browser closure, 14 September 2026

The reviewer-only browser-pending statements above describe the state and responsibility at the time of that review. The coordinator has now completed the affected production browser checks and inspected selected informative captures; the [five-topic closure](PCA-THROUGH-GMM-IMPLEMENTATION-AUDIT.md) names the cases, actual visual observations, evidence reuse and limitations. Its source-comparison evidence binds the final repaired files. This closure does not attribute the coordinator's browser work to the independent reviewer.
