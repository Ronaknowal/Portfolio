# Active Learning independent review — 19 September 2026

Reviewer: the Gaussian Processes implementer; the root agent authored Active Learning. Read the complete prepared manuscript/design/visual contracts and reader, all figure/lab/model source, the downloadable Python program and implementation evidence. The reviewer did not implement the lesson or repair its runtime findings. This review complements the author's native and browser campaign instead of repeating the same fit or binary numerical grid.

Status: **no remaining material finding in the reviewed scope**. Exact source hashes, actual independent checks and four new image digests are in [the independent receipt](evidence/active-learning-independent.json). The increment owner still owns final production integration, source reconciliation and phase-ledger completion. User acceptance is separate.

## Findings resolved by the author

1. **P2 — initial query could disagree with the selector.** Starting with seed observation `(4,0)` removed query4 from eligible options, but the state still selected4. A browser could display0 while Record rejected the selected query. Initialization now chooses from genuinely unused inputs, preferring4 only when eligible. The independent browser test starts with `(-1,0),(4,0),(9,1)`, verifies selected0, predicts4/0 survivors, and successfully acquires it. A second case seeds every configured query and verifies the legitimate exhausted state without an acquisition action.
2. **P2 — mathematical entropy ties ignored the stated ID order.** With A's probability .01 and B's .99, a floating difference of 2.78e−17 nats selected B even though binary entropy is symmetric and displayed scores tie. The author added a stated absolute tolerance of 10⁻¹² nats before identifier ordering. Independent checks cover several symmetric pairs and a live batch containing A=.01, B=.99, C=0, D=1, budget1. Highest entropy now selects A, while the geometric policy independently chooses C. The retained native experiment is unchanged; its documented score policy is not retroactively altered.

These were bounded implementation defects. No rewrite or phase-one checkpoint change was needed.

## Correctness review

- Verified score definitions and direction, nats versus bits, binary score equivalence, the confident-error gradient, finite threshold assumptions, weighted expectations and oracle boundaries. The threshold experiment counts seed and acquired labels separately and retains contradictory evidence rather than restoring nonexistent survivors.
- Checked committee entropy decomposition, vote ties, posterior-sampling caveats and BALD interpretation. Reopened the primary [Houlsby et al. paper](https://mlg.eng.cam.ac.uk/pub/pdf/HouHusGha11a.pdf) for the information-theoretic acquisition context. Entered vectors and bootstrap fits are not described as exact Bayesian draws.
- Complementary calculation: **30 committees** with 2–6 members and 2–4 classes, including zero cells, compare implementation disagreement with `H(member index) + H(class) − H(joint)`. Class/member permutation checks also pass. This extends beyond the author's binary mean-KL grid.
- Complementary geometry: **12 cases with multiple fixed anchors** compare covering radii with independent squared-distance enumeration, enumerate every three-center subset for an exact optimum, and check 90-degree rotation invariance. The factor-two assertion concerns metric covering radius, not label accuracy.
- Inspected all native experiment branches. Pool-only feature scaling, explicitly oracle-balanced initial labels, paired strategy starts, masked acquisition, stratified observed-label bootstrap, final refit and development-only CLI boundary are coherent. The reported five runs share one evaluation set and are not presented as independent population samples. Reused the author's 5,981 native-value comparisons for unchanged program/data; no new native fit is claimed.
- Deeper BADGE blocks, GP variance reduction, fixed-model importance arithmetic, temperature-ranking argument and changed practice solutions are mathematically consistent. The implementation keeps those assumptions near their claims.

Executed `node scripts/review-active-learning-independent.cjs`: **84 numerical comparisons, nine grouped checks and four informative phone captures passed**. The browser cases independently test weighted thresholds (expected survivors2.875), three deterministic class members (D=ln3 and votes1/1/1), the symmetric entropy tie, changed initial eligibility, exhausted input and a single measured run containing all31 vertices. Unrevealed results remain absent; keyboard commitment works; no page overflow or runtime error occurred. Author native/model/browser evidence remains linked from [the implementation record](drafts/active-learning/implementation.md), with affected final refresh owned by the author.

## Learning-experience review

1. **Purpose and route:** labeling cost and the oracle question precede formulas. Sections1–7 provide the practical path, with optional deeper objectives and a clear metrics continuation.
2. **Cautions at their point of use:** finite noiseless thresholds, bootstrap interpretation, geometric guarantees and evaluation label costs are introduced where they affect a choice. The manuscript does not open with a long warning list.
3. **Concrete anchors:** a complete threshold family, explicit probability vectors, an authored coordinate plane and real measured Banknote trajectories give the abstractions inspectable objects.
4. **Distinct investigations:** the learner edits rules/observations, model-member distributions and candidate geometry. Nulls, contradictions, predictions and changes carry different learning jobs. The oracle and fit counts remain visible.
5. **Visual inspection:** opened all four new390px captures: weighted threshold elimination, three-class entropy decomposition, symmetric-tie batch result and seed31's measured curve. Also opened author desktop captures of the label boundary, multiclass strips, geometry and annotation timeline. The eight inspected images cover all seven figure mechanisms plus changed lab results. Labels and tables remain legible; no distorted scale, clipping, crossing annotation or unintended gradient was observed. The observed nonmonotonic run is retained visibly. Color is supported by labels, numbers or shapes.
6. **Connections:** the semi-supervised distinction leads into query selection; uncertainty becomes committee information; geometry adds batch context; the GP branch explicitly carries forward fixed-parameter covariance reasoning. The next-topic link stays in module order.
7. **Code and evidence:** the complete program is a download and reader disclosure, with a short control-flow excerpt explained locally. Measured replay is identified as saved evidence and never suggests live browser refitting.
8. **Independent practice:** eight tasks change values, criteria or protocol, including an actual development-only experiment. Sixteen hints/solutions are separately closed. No fabricated answer is supplied for the open experiment.
9. **Interaction and limits:** tested new configurations and informative outcomes, not only reset screens. The author additionally covers desktop, intermediate and320px layouts, increased text and reduced motion. This is expert heuristic review, not a novice learning study or a guarantee of mastery.

No additional subject coverage or resource claim is required to close this implementation. Keep optional video review claims truthful and the existing research packet intact.
