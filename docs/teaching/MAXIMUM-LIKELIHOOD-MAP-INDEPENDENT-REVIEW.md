# Maximum Likelihood & MAP — independent source review

10 September2026. Root read the entire final lesson, its eight changed practice answers and the pure estimation models. The [author record](MAXIMUM-LIKELIHOOD-MAP-VERIFICATION.md) owns the seven executed programs, independent numerical oracles and actual1440/390/320 browser evidence; this review does not claim to rerun those suites.

The sequence/count likelihood distinction preserves the binomial factor for event probabilities and removes it only for same-event parameter optimization/ratios. Conditional factorization is kept distinct from iid assumptions. Positive-count log zero and zero-count log zero have explicit different behavior. No-data, boundary and empty-argmax cases are not forced through an interior derivative formula.

The normal SSE identity, mean and positive-variance MLE, finite-sample variance bias and Laplace median argument agree with the declared model. The original[2,3,4,7] values and outlier transfer are preserved. Mean versus median is tied to the intended target as well as robustness.

Beta MAP, mean and next-trial prediction are separately derived, with flat and boundary cases and the general fractional-shape qualification. Normal-prior precision, sum/mean penalty scaling and scalar Laplace soft thresholding use the appropriate positive constants. The AdamW distinction concerns trajectories and specified objectives, not an unsupported Bayesian interpretation of every optimizer.

The sample proportion's exact variance and its boundary plug-in failure are correct. The transformed posterior uses the parameter Jacobian while a likelihood relabeling does not; corresponding event probabilities are invariant although modes move. The Beta/log-odds coincidence is explicitly limited.

The uniform model states its closed-support density convention. The identical-normal and separated-logistic examples distinguish an unbounded density from an unattained finite supremum; offset nonidentifiability is not described as cured by selecting a prior. All eight changed answers agree with their model/support/units, and the next-topic bridge preserves Hypothesis then Bayesian.

No actionable mathematical or scoped teaching defect was found. The six frozen author files include body hash `00064b38ea3f1fc6607a522d2514faa1e9c5f08e0128ed789644aee8117169f9`. This bounded review and source freeze still need production selected-body integration; neither implies observed learner mastery or user approval.
