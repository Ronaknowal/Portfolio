# Linear & Logistic Regression — bounded independent review

11 September 2026. Full current lesson, its design, every practice answer, all ten displayed programs, pure models and complete lab/CSS sources were read. This review is complementary to the author's evidence; it does not repeat the entire author suite or certify production integration or user acceptance.

## Mathematical and teaching assessment

The prediction-time parcel example establishes features, outcomes and the baseline before optimization. The scalar and matrix least-squares explanations connect actual residuals to their objective, explain rank deficiency and distinguish numerical solving from statistical assumptions. The gradient geometry uses coefficient space rather than confusing it with the shipment plot. Logistic score, probability, loss and decision cost are separate quantities. The complete workflow and changed capstone retain actual ownership of training, validation and test data.

The full read found no additional material defect in the centered least-squares proof; quadratic Hessian and step-size condition; Bernoulli loss/gradient; weighted probability-risk optimum; separation/coercivity and intercept qualifications; nonlinear-feature boundaries; Gaussian versus Gauss–Markov conditions; pointwise mean/outcome intervals; or actual current library/deployment caveats. This is a bounded expert reading assessment, not a novice learner study.

Complementary execution in [verify-linear-logistic-independent.py](../../scripts/verify-linear-logistic-independent.py) passed at 12:11:54 UTC: all ten actual programs matched stored stdout; 45 changed evaluations of the actual logistic objective/gradient agreed with independently formulated 80-digit likelihood derivatives; 12 weighted-risk minima agreed with exact probability arithmetic; seven actual fitted preprocessing objects used only their training rows; and four changed interval locations agreed with exact centered-leverage calculations. That passing evidence is reused for unchanged programs. Results: `scratch/linear-logistic-independent-review/native.json`.

## Findings and narrow repairs

1. **Avoidable MSE overflow, repaired by the author.** The originally frozen `residualReport(1e154, 0)` returned infinite MSE even though all four individual squared errors and their mean are representable. The total overflowed before division. Averaging each finite term before summing corrects that operation. [verify-linear-logistic-independent.mjs](../../scripts/verify-linear-logistic-independent.mjs) now checks nine changed constant-prediction cases against the separately expanded fixture-moment polynomial, including both signs at 1e154; all pass. A case with genuinely unrepresentable individual squares still rejects. The initial observation and original model hash are recorded separately from the final check in `scratch/linear-logistic-independent-review/model-boundaries.json` (12:57:26 UTC).
2. **Baseline/control grid alignment, narrow source repair by the author.** The baseline sets intercept 2.25 or 3.25, while the original range grid advanced by .1 from −1. The author changed that one control to .05. The source now represents both baselines exactly on its legal range grid. The interrupted reviewer run did not preserve an inputValue record, so this report does not claim a freshly observed DOM mismatch or completed reviewer browser verification of the repair. The author owns the focused final control check.

## Browser evidence and honest limits

The independent browser work was interrupted before producing a completed three-width JSON record. Retained actual captures establish a readable 390px baseline view, the operated .2-rate gradient state and ordinary uncertainty/assumption prose at 1440px. They were opened in this review. The script had also reached the corresponding mathematical assertions, but no full pass is inferred from screenshots or script existence.

After cleanup, the first continuation failed because the shared preview refused connections; the next lost its connection during the run. Following the parent's one-retry instruction, the final attempt timed out after 60 seconds waiting for the lesson to mount. No further browser suite was launched. This environment limitation is not reported as a production lesson error and is not hidden by claiming the author's checks as independent operation.

The reviewer also opened the author's final `information-reading-320.png`, `regression-logistic-score-changed-390.png` and `uncertainty-reading-320.png` from `scratch/linear-logistic-browser/`. These show the timeline, score/loss controls, focus treatment and explicitly scrollable detailed plot/interval table. They are **author-captured evidence inspected independently**, not new reviewer screenshots. Local plot scrolling is explained and preserves readable labels; ordinary narrow prose and table values were legible in these captures. Original author browser evidence remains [linear-logistic-browser-review.json](evidence/linear-logistic-browser-review.json).

The final independent [evidence packet](evidence/linear-logistic-independent-review.json) binds the inspected source hashes and these six opened images, with numerical results and browser limitations. All six production hashes match the separately retained [author amendment](evidence/linear-logistic-author-amendment.json), which confirms actual 390px baseline values 2.25 and 3.25 with valid range inputs. That focused check is attributed to the author. No unresolved mathematical finding remains from this scoped review; complete independent live three-width interaction coverage is **not claimed**.

## Retention

The final numerical JSON, actual-program input JSON required by the reusable check, six opened image attachments and durable packet are retained. Uninspected working captures and redundant `programs.txt` were removed. That cleanup mistakenly caught a new parent-owned amendment image saved into the reviewer directory; the parent was immediately notified and is re-saving that one image with its final hash. This was an ownership-coordination error, not a new browser test requirement. No further cleanup of that active shared directory will occur. Author-owned original archives, production sources and shared runtime were unchanged.
