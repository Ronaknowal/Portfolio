# K-Means revision: retained teaching and visual contracts

21 September 2026. This full-mode lesson's manuscript is the complete [implemented body](../../src/learn/data/topics/k-means-hierarchical-clustering.jsx). The [original design](K-MEANS-HIERARCHICAL-LESSON-DESIGN.md) retains its learning contract, coverage decisions and 12 September proposal; the [current review](K-MEANS-REVISION-REVIEW.md) records the independent assessment and corrections. This document specifies the final intended teaching behavior so another agent can continue without the original conversation.

## Reading and evidence

Preserve the fourteen-section progression and explicit first-pass route. Use the six labeled points to explain assignment/means and linkage; use the rectangle to reveal a bad fixed point and changing geometry. Keep the real 272-row Old Faithful question, the executed synthetic/library comparisons, six complete Python programs, ten changed practice tasks with hints/solutions, and the reproducible held-out capstone. Optional proofs and specialist applications extend the core route.

Old Faithful's fitted joint groups use both duration and the subsequent waiting time. Their within-group waiting spread is descriptive. A visitor forecast must use available duration for assignment, development-owned waiting summaries and untouched later evaluation; the lesson describes that protocol without claiming to have run it. Rounding, feature units, source version, initialization and recorded library versions remain explicit. Do not turn any constructed pixel/ring fixture into an empirical claim.

## Five live investigations

The 21 September live-exploration request supersedes this revision's earlier prediction/reveal contract. There are no learner-prediction fields, optional quiz modes, grading banners or answer-gated views. Useful current results are visible at first paint. Changes update the same underlying state across charts, tables and explanatory readouts.

| Investigation | Manipulation and live result | Process, comparison and edge behavior |
| --- | --- | --- |
| Lloyd geometry | Choose either point fixture and any two starting rows. The initial assignment and its SSE are visible immediately. Scrub the phase slider to follow point ownership, centers, nearest-center boundary, residuals and exact objective. | Step/back or run to the fixed point; changing the fixture or seeds starts a new trace at its first assignment. Compare the rectangle's SSE 1 and 9 fixed points. Retain coincident starts, empty-center policy and lower-index ties. |
| Feature geometry | Change vertical units or continuously vary the squared-coordinate weight from .01 to 4. The transformed point geometry, optimal split, centers, seven-partition objective table and readout update together. | Unit factor 10 changes the optimum; weight .01 compensates that factor and restores the original distances. Both coordinate axes keep equal plotted scale. Reset restores meters and weight 1. |
| D² seeding | Change the first center, dataset or cumulative draw slider. Show conditional probabilities, selected candidate, exact probability bars and 200 seeded frequencies immediately. | Accept a draw to add a center and recompute the next distribution; Undo reverses that step. Differentiate exact probability from finite frequency, including the 53.25% versus 90/200 example. Stop at zero total squared distance; selected rows have no uniform-remaining-row marker. |
| Merge hierarchy | Switch data, linkage and height/count cuts. The tree, original-coordinate membership map, cluster count, partition SSE and exact merge table update together. | Step/undo individual merges, use the exact tied-height preset and compare the four rules on the chain. Height cuts include whole ties while merge-count cuts can stop inside them. Point ID and cluster number occupy separate leaf-label rows at 320 px. |
| Weighted palette | Change the constructed image or slide palette size. Original/reconstruction, color entries, counts, floating-center SSE and rounded-byte error update immediately; an adjacent calculated comparison shows the next palette size's error. | The six-color mosaic can be exact; eight entries do not reconstruct the 37-color sky or 160-color gradient losslessly. Keep the teaching cap explicit and separate numeric RGB error from perceptual quality. Reset restores the mosaic with three entries. |

Each investigation supports a concrete decision: choosing initialization, selecting a justified distance geometry, understanding probabilistic seeding, choosing linkage/resolution or balancing palette size against reconstruction loss. No model result is fabricated to make a slider appear responsive. Changes that preserve the answer are explained as meaningful invariances. Natural algorithm operations remain explicit; no answer is required before using them.

## Seven inline figures

| Figure | Representation and invariant |
| --- | --- |
| Raw Faithful scatter | All 272 recorded duration/wait pairs, both in minutes; honest rounding/provenance. It introduces a grouping question without claiming forecast success. |
| Mean representative | Points 1, 2, 9 and candidate centers 2/4. Lines show ownership; horizontal differences calculate SSE 50/38. The drawn vertical offset is layout. |
| Linkage definitions | A={0,2}, B={5,9}; cross-distance table supplies single 3, complete 9, average 6, Ward Δ=36 and SciPy height √72. |
| Forty-leaf Faithful Ward tree | Saved deterministic sample and actual SciPy linkage. Height is √(2ΔSSE), horizontal placement is layout. Right-hand cut labels are cluster counts. Preserve the top gap, finer cut and correspondence with full-fit colors. |
| Fitted Faithful scatter | The same original coordinates, executed standardized-fit labels, mapped-back means and group sizes. This is a fitted description of joint geometry. |
| Measured diagnostics | All five starts at each k=1…8. Logarithmic inertia with best-of-five line, separate median-of-run-silhouettes panel and exact HTML table. No silhouette point at k=1; its annotation sits above the plotting area and must not intersect the k=2…3 curve. Later improvements need not decrease monotonically. |
| Ring limitation | Constructed inner/outer rings versus a straight nearest-center separator. Schematic centers are explicitly not a measured fit. Shapes/text supplement colors. |

Every representation needs a useful text/value counterpart. Keep plotted data fixed when moving annotations. Preserve responsive chart stacking, scrollable wide tables, keyboard controls, reset and visible focus. With dense trees, reflow or separate label rows rather than shrinking labels until they overlap.

## Runtime and verification boundaries

Use the stable topic's on-demand body and existing pure model/data modules. Memoize bounded computations against their actual inputs so unrelated UI changes and draw-slider movement do not repeat unchanged enumeration, clustering or simulation. Disconnect the scatter resize observer on unmount. No new global registry, external runtime data fetch, unbounded simulation or additional heavy dependency is required.

Source-bound model/native evidence establishes calculations; real browser operation and opened images establish visible teaching behavior. Check desktop, intermediate and narrow widths, increased text size, and informative states. The browser verifier asserts the actual site font families have loaded. When the sandbox blocks the existing font endpoints, unchanged downloaded font snapshots can satisfy those requests for testing, with provenance recorded; do not silently report fallback-font screenshots as actual-font evidence.

The earlier model/native reviews remain applicable to unchanged numerical sources; the live-exploration review and central phase ledger bind the current interaction revision. Historical evidence is reused only for matching source/program identities. User acceptance remains separate from technical completion.
