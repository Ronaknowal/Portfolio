# Backpropagation & Automatic Differentiation — independent implementation review

Date: 21 September 2026. Stable topic ID: `backpropagation-automatic-differentiation`.

**The independent content, numerical and learning-experience review is closed.** The concrete findings below have been repaired and checked against the final author sources. The topic is ready for the integration owner's final production build, generated navigation reconciliation and production browser run. This record is not evidence that those remaining integration steps have already passed, nor is it user acceptance.

## Scope and source binding

Read the complete [prepared packet](drafts/backpropagation-automatic-differentiation/design.md): manuscript, detailed visual specifications, design, provenance, teaching engine, author calculations and recorded observations. Reviewed the final `backprop.jsx` body, the two topic data/model modules, `BackpropLabs.jsx`, `BackpropFigures.jsx`, `BackpropShared.jsx`, scoped stylesheet, lesson generator, published programs and author verifiers. The separate stable-ID blueprint is registered by the integration owner.

The [independent browser receipt](evidence/backprop-independent-browser.json) binds the eight reviewed runtime/generator files. Its final body SHA-256 is `a2d57d9ba99bae53f6fde115b371a1550811cf4e6240f6d165dc75b12135d6bd`. The complete downloadable teaching engine has SHA-256 `74f16553bde85cd7bf6731e34c4219185e55b9f8e0197759eced1799402afb1d`. Reconciled every recorded source hash in the author model, native-program and browser receipts and the independent browser receipt against the current files: no stale binding remained at review closure.

Reused the source-matching [174 author model assertions](evidence/backprop-models.json), [nine complete native programs and 51 assertions](evidence/backprop-native.json), and [94 development-browser assertions with 30 captures](evidence/backprop-browser-author.json). These are author evidence, not independently repeated experiments. The native record retains its actual environment and the PyTorch directional-products deprecation warning. No fresh environment installation, GPU comparison or full training campaign was repeated for this review.

## Findings and closure

| Finding | Repair and closure |
| --- | --- |
| Prepared word/number joins were emitted into prose, including reading-time and loss explanations. | The generator now normalizes the affected prose spacing while preserving code and math. Checked the rendered reading-time sentence and full manuscript preservation. The frozen prepared manuscript is retained unchanged. |
| A long lesson lacked an integrated section route and the metadata flag identifying its own guide. | Added eight resolving section anchors after the core/deeper route and `hasIntegratedGuide: true`. Independently checked all eight DOM destinations. Suppression of the generic guide depends on the integration owner's generated navigation update. |
| Generic numbered table captions gave insufficient context while reading exact values. | Replaced them with six semantic captions, including the one-neuron forward/sensitivity table. Captions remain outside the horizontally scrolling table area. |
| Changed lab inputs displaced the original reference without a persistent comparison. | All three investigations now retain explicitly input-labelled starting references. Independently changed the shared-graph inputs and confirmed the starting reference remained unchanged beside current evidence. |
| Highlighted reverse-trace edges still pointed in the forward direction. | The author reversed the highlighted arrowheads at the two reverse stages and added a current-direction caption. Independently checked both stages, their SVG marker direction and the rendered phone capture. Current derivatives remain visible throughout; the trace does not unlock an answer. |

No unresolved mathematical, model or native-program defect was found in the reviewed scope.

## Complementary correctness and coverage checks

`scripts/verify-backprop-independent.py` checks independent algebraic invariants against the actual downloadable engine. Its [13 passing named checks](evidence/backprop-independent-semantics.json) cover a nonuniform explicit output seed through a repeated operand; broadcast bias reduction and mean normalization; gradient flow through a frozen matrix without parameter accumulation; row-specific large-logit-offset invariance; zero-sum cross-entropy logit gradients; one matrix used in both matrix-product operands; copied/read-only forward values; explicit seed shape; and invalid label types, dimensions and ranges. Expected values use direct NumPy/algebra rather than another call to the engine's own backward formulas. This bounded check does not retrain XOR or digits.

`scripts/verify-backprop-coverage.mjs` verifies [all 96 prepared prose paragraphs, 16 display equations, nine code blocks and six tables](evidence/backprop-independent-coverage.json). No prepared paragraph is missing. The comparison permits whitespace normalization and three documented native-execution wording updates. The retained manuscript's normalized-line-ending hash is `588cbf3398f47f1826044a9f308c29f57ea07837e883d56d9dcd8a5fa4c16f55`.

The independent browser closure used the shared development preview on port 4197 at 390 px. In addition to the route and repaired references, it changed the shared graph to x = −2 and c = −3 and independently checked loss −8 and total derivative 8: the direct contribution is −4 and the scaled contribution is 12. Both backward stages preserve that output and show the correct arrow direction. Keyboard ArrowRight scrolls the exact-values table, all current content remains reachable, and no page overflow or JavaScript error occurred. The [final phone screenshot](evidence/backprop-independent-backward-390.png) was opened and visually inspected.

## Independent learning-experience checklist

1. **A usable first pass.** The page states a core route through sections 1–5 and a deeper route through section 6 and practices 5–8. The eight resolving anchors make those routes usable in the long article. Mechanisms precede framework details and matrix-free extensions.
2. **Cautions at their actual point of use.** The page distinguishes a derivative from a chosen optimization step, shared graph contributions from separate copies, sums from mean normalization, numerical checking from certification, and saved forward state from unsafe mutation. These distinctions support the nearby example rather than becoming repeated warning banners. Actual environment warnings are retained in evidence.
3. **A concrete question and honest data.** The opening asks how each weight contributed to error. A scalar neuron leads to a two-row fit and an actual UCI digits experiment with the stated 280/120 teaching split. XOR is presented as fitting its truth table, without implying held-out generalization. Displayed training observations remain bound to executed programs, including their actual outcomes.
4. **Live investigations that reveal decisions.** The fit, shared-graph and finite-difference investigations respond immediately to edits and keep their starting references visible. Zero step size, graph cancellation, zero inputs and an exact quadratic check are meaningful contrasts. Large proposed steps visibly worsen the fit. Trace controls inspect already visible calculations; there is no learner prediction, optional guess, grading or output-unlock feature.
5. **Representations suited to each mechanism.** The scalar graph, repeated-use graph, broadcast contribution grid, paired tensor shapes, measured training traces, signed JVP/VJP bars and checkpoint slots perform different teaching jobs. Exact values remain accessible in local scrolling tables. The repaired reverse arrowheads communicate the direction actually being discussed. Label placement and mobile flow were assessed from screenshots, not inferred from passing assertions.
6. **Connections and conventions are explicit.** The route joins the scalar chain rule, shared nodes, broadcasting, tensor gradients and stable cross-entropy before framework behavior. It connects JVP/VJP duality, Hessian-vector products, unequal-microbatch normalization and the next loss lesson. The scalar half-squared loss versus MSE factor of two and the teaching weight orientation versus `nn.Linear` transpose are explained rather than silently changed.
7. **Complete code with bounded claims.** The complete offline engine and seven standalone examples are downloadable, and all nine prepared code blocks survive. The author executed nine complete programs, including the full engine/author calculations and the seven examples; their source hashes still match. The engine's two-dimensional float64 scope, copied forward values, explicit nonscalar seeds and strict labels are visible. It does not claim GPU performance or framework-level completeness.
8. **Practice transfers the mechanism.** Eight practices use changed graphs, inputs or failure cases and keep hints separate from solutions. They include repeated operands, a changed broadcast gradient, overshoot diagnosis, a different JVP direction, a Hessian-vector product rather than only its diagonal, 4/4/2 microbatch weighting and a sigmoid extension with the known local slope. Solutions provide working reasoning without withholding the separate live labs.
9. **Rendered evidence is interpreted.** Independently opened the author's desktop/phone training, overshoot/fit, offset-cancellation/finite-difference and scalar captures; the phone shared graph, broadcast, tensor-shape, products and checkpoint captures; and the final independent reverse-graph capture. This covers representative 1366, 390 and 320 px states, not a claim that every one of the 30 author screenshots was separately inspected. The training trace uses actual update positions and log-loss values; paired finite-difference displays expose cancellation; signed product bars share a scale; checkpoint slots and exact tables remain legible and reachable.

## Remaining integration and limits

The integration owner must regenerate the compact navigation to carry the integrated-guide flag, register the stable-ID blueprint, run the final application build and execute the production browser verifier against that exact build. The author's browser verifier is reusable through `LEARNING_BASE_URL`, `BACKPROP_EVIDENCE` and `BACKPROP_PREVIEW`; development evidence must not be relabelled as production evidence.

This review does not certify assistive hardware, every browser engine, a fresh dependency installation, GPU execution or every possible learner input. It does cover the complete prepared content, the delivered mechanism implementations, complementary numerical edge cases, actual author program evidence and the concrete review findings above. Final production integration and user acceptance remain distinct from this closed independent review.

The disposable independent browser closure helper was removed after preserving its source-bound receipt and selected screenshot. The reusable independent semantic and prepared-content coverage verifiers remain under `scripts/`; the author's reusable browser verifier includes the corresponding repaired behaviors.
