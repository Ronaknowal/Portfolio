# Dropout implementation: independent review

Reviewer: `/root/residual_implementation`, 22 September 2026. Author: `/root/decision_depth_prose`; integration/browser owner: `/root`. The reviewer authored the neighboring Residual lesson, not this Dropout implementation. Scope includes the complete prepared revision-three manuscript, visual specifications, design record, rendered topic, blueprint, all eight investigations, native programs, saved experiment and author evidence. No author source was edited by the reviewer. No training campaign, browser session or build was repeated by this reviewer.

## Conclusion and delivery boundary

The full teaching route, transparent mask implementation and ordinary-library bridge are present and technically sound within the reviewed scope. Complementary independent numerical checks pass. Four representation/evidence findings were returned to the author and all four corrections are now confirmed. Final source hashes are in [the review record](evidence/dropout-implementation/independent-review.json).

Production browser interaction, actual desktop/phone image inspection, focus/keyboard/pointer behavior, final integration and delivery-ledger closure remain with the parent. This source/native review does not certify those surfaces or imply user acceptance. The author’s 27-fit exact replay remains its own evidence; this reviewer did not repeat or relabel that campaign.

## Complete manuscript and teaching depth

All twelve main section homes, the seven main changed-input practice problems with hints and worked solutions, the separate locked-feature customization task, and the annotated further-learning route remain in the rendered page. The implementation introduces temporary hiding before Bernoulli terminology and tensor axes; a two-value worked loss/gradient/SGD example connects masking to an actual parameter update. Probability-weighted outcomes then explain inverted scaling, variance, p0/p1 and why a preserved mean does not preserve a nonlinear prediction or loss. The ReLU counterexample is retained rather than replaced with an ensemble slogan.

The sequence explicitly explains element/channel/row/batch broadcasting before branch placement, schedule conventions, mode/state effects and real training comparisons. It links the residual owner for the direct/correction-path mechanism and the classical regularization owner for the exact input-dropout squared-loss penalty. Convolution is previewed through locally explained spatial axes rather than silently required. Optional MC inference and specialist noise families are separated from the first-pass route. The loss, differentiator and optimizer are appropriately reused foundations rather than duplicated here.

The code route is substantive: the exposed `mask_values` function owns broadcast shapes, one sampled Bernoulli mask, inverted scaling, evaluation identity and endpoint guards. Forward/backward fixtures explain reuse of that same mask. The complete CPU program exposes dataset preparation, matched initial parameters within each family, explicit dropout placement, actual fitting, deterministic measurement and selective MC modes. The ordinary route includes `nn.Dropout`, `nn.Dropout2d` and torchvision row/batch stochastic depth; the separate downloadable library program executes real operator/gradient checks and the `[B,1,D]` locked-feature solution. It does not claim cross-library seed equality. Broadcasting makes the implementation linear in the number of activation values with only the necessary independent bits; an entire new tensor/autograd engine is outside this lesson's declared ownership.

Subtle statements remain qualified: zero local gradients do not mean permanent parameter deletion; shared masking changes covariance over masks, not data correlations; original stochastic-depth and zero-first schedules differ; layers and residual blocks differ; no_grad and module mode are independent; BatchNorm uses different training and stored-variance divisors; LN/GN avoid that specific running-statistic mismatch without being mask-invariant. A skip path and low uncertainty are not guarantees. MC averaging uses probabilities, its Brier convention is explicit, hypothetical examples are labeled, and the Bayesian interpretation is conditional rather than asserted for arbitrary masks. Attention-row dropout need not preserve the sampled row sum. Optional DropBlock, DropConnect, Zoneout and Shake/variational families are conceptual distinctions with primary pointers, not falsely advertised implemented training algorithms.

## Evidence and representation fidelity

The 400 actual 8×8 UCI digit records, 280/120 teaching split, known pixel scaling, three seeds, two different model families, parameter counts and 400-update budget are explicit. Matched comparisons stay within family/seed. The retained observations include non-improvement and disagreement between accuracy and cross-entropy; a smaller train/development gap is not treated as the objective. Saved trajectories have actual six-point measurements and explicitly labeled connecting lines. The full retained experiment is preserved and downloaded, while the current lesson uses its compact projection.

All eight investigations show current outputs without a learner-prediction gate: value/gradient lanes, weighted probability leaves, broadcast channel cells, branch/join placement, independent execution counters, normalization-state transitions, recorded fit curves, and MC probability distributions. Inputs change the underlying tiny computation. Mask fixtures remain fixed while other parameters change; no new backward mask is fabricated. The normalization panel explicitly describes a bounded state-machine analogue rather than browser PyTorch execution. The execution panel now actually invokes its declared tiny branch and distinguishes a missing invocation from a numerically zero result. Geometry presents fixture bits separately from effective behavior at endpoint/evaluation overrides.

Only the three specimens with retained individual MC vectors receive prefix controls. No individual trace is invented for source ID 299. One-draw sample standard deviation is explicitly undefined; disagreement is zero then by construction. Prefix controls inspect saved draws, not fresh fitting or posterior sampling. Probability bars have numerical alternatives. Local calculations are bounded to small arrays, at most twelve branch jobs and the retained 100×3×10 probabilities. Canonical programs load on disclosure. CSS is local, neutral/amber, with dedicated mobile stacking; actual paint and interaction verification remains the parent's task.

## Complementary checks actually executed

The author already replayed all 27 fits and 100 MC draws, with 7,629 numerical values matching retained outputs exactly, and ran its native library comparisons. That expensive work was not repeated. Reviewer commands were:

```text
scratch/lesson-tools/Scripts/python.exe -X utf8 -B scripts/verify-dropout-independent.py
node scripts/verify-dropout-independent.mjs
```

The native command required approved access beyond the sandbox to import the already-installed isolated torchvision directory, whose ACL denied sandbox reads. No package was installed or modified. The first sandbox attempt stopped at import and wrote a non-passing running record; the subsequent successful scoped execution replaced that record with actual results.

- **40 native mask/locked-mask cases**: arbitrary mixed-sign cotangents through element/channel/row/batch masks; changed shapes, zero-valued activations, train/eval and p0/p1; changed locked-time axes and rejection of nonbinary/wrong-shaped masks. A matched ones probe within the same scratch operation exposes the exact sampled factor even at a zero activation. The reference is the diagonal operator's adjoint, not merely another summed-loss fixture or assumed cross-library RNG parity.
- **24 native BatchNorm cases**: changed batch sizes 2/5/11, train/eval, independent gradient recording, and momentum 0.17/1. Actual PyTorch outputs and running buffers form an oracle subsequently consumed by the JavaScript comparison. These are native state calculations, not fabricated expected fixture JSON.
- **15 exact objective cases**: weighted enumeration of every two-bit mask agrees with the independent clean half-squared loss plus its exact input-dropout penalty and gradient, using signed/zero features and nondefault probabilities. This checks the connection to the reused classical owner without adding that whole lesson again.
- **60 moment cases**: arbitrary signed/zero features and survivor multipliers agree with analytic Bernoulli means and variances, not only the prepared `[1,2]` fixture.
- **300 saved-prefix cases**: all 100 prefixes for each of the three retained specimens are checked using average KL divergence versus entropy difference, permutation invariance, and a second-moment expression for sample variance. The independent identity test normalizes tiny float32 probability-sum drift before comparing the two formulations; the UI and retained experiment remain unchanged. Author fixture parity separately covers their actual stored float32 values.
- **15 execution and 24 effective-geometry cases**: eager/lazy outputs agree while actual invocation counts differ; zero branch input does not imply skipped work. Endpoint/eval factors override fixture bits, including zero activations that cannot reveal a mask by output alone. These are scoped checks for the corrected representation contracts.

The separate [native record](evidence/dropout-implementation/independent-native.json) and [model record](evidence/dropout-implementation/independent-model.json) bind their checked sources. Counts above are executed case groups, not an invented total of assertions or universal proof. The scripts write non-passing state before executing and never mutate authored sources, retained fits or author evidence.

## Findings and confirmed corrections

1. **Missing full numerical trajectory alternative.** The original measured chart showed all six points but only its final values had a table, despite the shared SVG's numerical-alternative label. The author added a six-row table for selected and baseline train/development CE beside the chart. Each value is taken from the corresponding saved checkpoint; the interpolation disclaimer remains.
2. **Historical public provenance presented as current status.** The public opening still described content-only work. It now separates the original preparation from the actual complete replay, current bounded implementation and outstanding browser closure, while retaining historical attribution/protocol and source identity.
3. **Projected branch count presented as executed work.** The former UI inferred calls from active bits. The author added `executeDepth`, whose counter increments inside `F(x)=2x`, with eager and conditional call sites. The UI shows called/skipped, raw `F` or “not computed,” scaled correction and totals. It explicitly identifies independent scalar jobs at x=1, not a fitted residual stack or GPU speedup. Source inspection and the complementary changed-input/null checks confirm the distinction.
4. **Stored mask bits confused with effective endpoint action.** Cells originally kept their dropped/kept appearance at p0/p1/eval even when output followed a different boundary. They now show the stored bit separately and derive appearance/action from the effective multiplier. Branch prose and its diagram use finite factors with explicit boundary overrides instead of describing a p1 kept bit as a surviving correction. Formula/source checks and the scoped geometry cases confirm those changes; parent tests cover the actual DOM/paint.

No additional content or native mathematical defect was found in the reviewed scope. Author fixes did not alter the canonical training program, fits, CSV or library program; their full campaign need not be rerun for these presentation/model refinements.

## Primary contract spot-check

On 22 September the reviewer checked the complete current [torchvision stochastic-depth function body](https://docs.pytorch.org/vision/main/_modules/torchvision/ops/stochastic_depth.html), confirming row/batch shape, evaluation/p0 identity and p1 scaling guard. The [PyTorch Dropout stable page](https://docs.pytorch.org/docs/stable/generated/torch.nn.Dropout.html) resolves to its 2.14 documentation. The original [stochastic-depth abstract/version record](https://arxiv.org/abs/1603.09382) and [Gal–Ghahramani paper record/abstract](https://proceedings.mlr.press/v48/gal16.html) support the lesson's distinction between actual branch bypassing and approximate Bayesian motivation. These were scoped external contract checks; the reviewer does not claim a new exhaustive proof, appendix or historical benchmark review.
