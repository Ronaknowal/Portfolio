# Gradient Boosted Trees: author verification

Stable ID: `gradient-boosted-trees-xgboost-lightgbm-catboost`. Classical Machine Learning, Supervised Learning, position 4. The title and module position are preserved. The authoritative freeze time and six production hashes are in [the author packet](evidence/gradient-boosted-trees-author-review.json). Author verification is separate from the parent's independent review, integration and any future user approval.

## Preservation and resulting learning experience

The [complete original archive](archive/gradient-boosted-trees-before-rewrite/manifest.json) preserves the old 61,818-byte lesson, five complete programs, all five claimed outputs and the parent baseline identity. No unsupported old output was silently relabeled as an executed result. The [design](GRADIENT-BOOSTED-TREES-LESSON-DESIGN.md) contains the per-section retention/repair assessment and the final resource/disposition addendum.

The original depth-2, 100-row scratch experiment survives as a complete executable program with its substantive algorithm and exact random dataset. Its actual first predictions are `[2.869, .668, -3.851]`, rather than the old unsupported values. Unsplittable features now return a leaf, refitting replaces the prior tree collection, and the negative-gradient comment uses the stated half-squared convention. The original five-row regularized split, including gain 2.677083…, is retained. Library workflows keep the useful original synthetic regression setting while separating training, validation and untouched test data. The categorical example has explicit schema and shape assertions; a deliberately manufactured `(n,1)` comparison demonstrates broadcasting without falsely claiming the tested CatBoost class output had that shape.

The eleven-section lesson begins with a six-row calculation, derives why residual means are optimal and when the squared-loss shrinkage identity holds, then develops loss-specific gradients, binary logits, quantile loss, regularized Newton leaves, split gains, histograms, missing routes, growth policies, GOSS, exclusive bundling and ordered information boundaries. It closes the workflow with complete current library programs, stopping/inference contracts, diagnosis and changed practice. Every complete program has a visible question before its code and an explanation after the exact output. Optional hints precede explained answers. The larger report changes the data-generating process and requires a justified selection protocol; it does not prescribe an invented winning score.

Previously unsupported RMSE/importance curves, universal package rankings, arbitrary speedup/cardinality thresholds and false zero-expected-gradient claims have been replaced by traceable calculations and qualified explanations. All ten features in the retained `make_regression` fixture are informative. Gamma is a leaf-count cost, alpha is L1, and positive net surrogate gain is compared with zero. Logistic quadratic improvement is not promised to equal actual loss improvement. Ordered target statistics, Ordered boosting, symmetric trees and split validity are separate mechanisms.

## Visual and interaction contracts

Six labs have distinct learning jobs, supported by three inline figures:

- **Additive prediction figure:** one new feature value passes through two saved rules and adds their scaled leaf scores to the mean baseline. It never needs the new target.
- **Correction investigation:** aligned observed/current points and residual stems connect the prediction being improved to the tree's actual leaf corrections. A row selector links the views; editable six-value drafts have explicit application/reset and bounded validation. Eight actual rounds, changed targets and learning rates recalculate the model.
- **Newton investigation:** per-row gradient/Hessian chips accumulate into child weights and parent-versus-children gain. L1, L2, gamma, split, loss and rate controls expose distinct effects. The mixed confident binary fixture visibly worsens actual loss after a large Newton step despite positive surrogate gain.
- **Histogram investigation:** raw rows sit inside their actual bins; coarse boundaries remove the useful fine split. A separately identified missing row changes its learned route as its training target changes. Every candidate score is available in a table.
- **Growth figure:** computed level-wise and best-first topologies spend the same split/leaf budget with different depths. Candidate gains are explicitly hypothetical inputs to isolate the policy, not a fabricated fit or benchmark. The adjacent explanation distinguishes a symmetric shared predicate from merely visiting a whole level.
- **GOSS investigation and EFB figure:** six equally likely subsets show reweighted contributions and the difference between the expectation of a sum and its square. Exact exclusive bin ranges decode the admitted sparse rows; a conflicting row is explicitly outside that reversible scheme.
- **Categorical investigation:** a visible permutation prefix marks eligible earlier matching targets, the selected row, and excluded rows. Flipping the selected label preserves its fixed-prior prefix encoding but changes naive/full and later statistics. Numerator, denominator and a table expose the mechanism.
- **Validation investigation:** actual deterministic training/validation curves, selected prefix and stopping round share state. The prediction view renders constant pieces from the actual saved thresholds; vertical joins indicate jumps rather than interpolation. MSE has a nonnegative axis. A model fit is memoized when only the cursor changes. Separate tables expose losses, prediction regions and all observed/selected-prediction pairs.

Labels use actual container-sized SVG coordinates instead of shrinking one large chart to a phone. Dense tables scroll locally and can receive keyboard focus. Color is reinforced by labels, signs, hollow points, border styles and inclusion text. Controls have labels, visible focus, disabled endpoint states and reset paths. There are no timers or network-trained browser models.

## Executed programs and independent numerical checks

Run `scratch/lesson-tools/Scripts/python.exe scripts/build-gradient-boosted-trees-examples.py` to execute all fifteen purpose-named programs and regenerate the exact code/output bundle. Then run `node scripts/verify-gradient-boosted-trees-models.mjs` for the focused model/native verification. Both use the existing workspace environment. Dependencies actually executed are Python 3.12.14, NumPy 2.3.5, scikit-learn 1.9.1, XGBoost 3.4.1, LightGBM 4.7.0, CatBoost 1.2.10 and pandas 3.0.1.

The independent verifier uses exact rational partition costs and projection identities, SciPy scalar minimization, finite derivatives, exact subset expectations, independent prefix counts, and NumPy least-squares tree fits. It checks 1,336 all-partition objectives, 3,644 leaf centers, 480 projection identities, 2,880 additive predictions, 1,728 scalar leaf minima, 1,920 gradient and 1,920 curvature finite differences, all fine/coarse missing routes, 53 finite sampling laws, 648 categorical prefix values and 732 values of each validation/training curve. Exact counts, timestamps, source hashes and tolerances are embedded in the packet.

The step-plot extension adds 708 comparisons with independently fitted least-squares trees inside constant regions and 354 boundary checks using independently enumerated saved-tree leaf intervals. Exact discontinuity endpoints use the saved numeric thresholds: algebraically equivalent midpoint formulas can differ by an ulp and should not be mistaken for an interior function mismatch. Seven JS and six actual Python adjacent-float cases verify that the threshold never rounds into an empty right child. The actual quarter-rate changed practice and 101 changed quantile decisions are checked. Sixteen invalid inputs are rejected; refit and constant-feature behavior are exercised in the actual preserved recursive helper.

The maximum recorded scaled discrepancy is approximately 3.49e−8 in finite-difference checks, whose explicit tolerance is 2e−7; it is not represented as exact arithmetic agreement. Rational and direct checks use tighter stated tolerances. All fifteen complete program outputs are reexecuted and compared with their literal expected results. Reported CPU scores are outcomes of these stated demonstrations, not an equal-budget package ranking or an estimate of every future run.

## Actual browser and ordinary-reading review

Run `node scripts/review-gradient-boosted-trees-lesson.cjs` against the shared local development route. The final recorded run uses the actual Space Grotesk and JetBrains Mono fonts at 1440, 390 and 320 pixels. It verifies 82 changed model states per width, actual range/checkbox/reset keyboard actions, hint-before-answer behavior, focusable data disclosures, all fifteen displayed question/code/output triples, eleven navigation anchors and fourteen display equations. The final packet binds each width's exact checks and screenshot hashes to the same six production sources.

The full run asserts zero page errors, failed resource requests, document overflow or clipped equation widths. SVG points stay inside their plotting rectangles; text stays within the SVG; saved predictor polylines contain horizontal pieces and vertical discontinuity markers. Only the intentionally disconnected Vite HMR diagnostic is excluded. Font requests are allowed during local QA; their successful load is asserted.

Ordinary screenshots were actually opened after the final run for each distinct mechanism, changed and default validation states, desktop and phone composition, code/questions and narrow derivations. The exact opened-image list is in the packet; captured-but-unopened images are not called visually reviewed. Author review repaired one practice prediction typo, an adjacent-float threshold boundary, narrow equation wrapping, the sampled-step rendering, a negative padded MSE tick range and missing accessible prediction tables. These were pre-freeze corrections, not post-publication claims of unchanged evidence.

## Research bounds and remaining review

Primary papers and current library documentation are linked and annotated in the lesson and detailed in the design's completed source ledger. Friedman's original manuscript and XGBoost's accessible model tutorial were substantively read. StatQuest's creator description, prerequisites, exact video identity and chapters were verified in an actual browser; its transcript stayed empty and selected playback seeks failed. CatBoost's official video index and mechanism-talk identity were verified. The lesson includes these optional video links without pretending they were fully watched or relying on them for technical claims.

No GPU/distributed execution, uncontrolled speed ranking, real-world deployment, universal calibration or learner-efficacy experiment is claimed. The JS models are bounded teaching calculations; positive-curvature arithmetic has an explicit minimum denominator, and invalid or out-of-range values are rejected rather than presented as a production optimizer. A full code-level CatBoost replica, advanced attribution, ranking objectives and production tuning services belong to their specialist owners. This lesson supplies the mechanisms and complete finite workflows needed to approach them. Parent-owned independent review and production integration follow this author freeze.
