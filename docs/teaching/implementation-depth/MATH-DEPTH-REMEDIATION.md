# Thirteen mathematics implementation-depth dispositions

**Final integration, 22 September 2026:** independent review and production/browser checks are complete for this scope; the ledger records both phases complete. [The final report](REMEDIATION.md) closes the integration work reserved in the dated author checkpoint below.

Author assessment and focused remediation, 22 September 2026. This report covers the thirteen mathematics rows marked “needs deeper review” in `FOUNDATIONS.md`. It does not change the central delivery ledger or certify unrelated lessons. All thirteen received an assessment of the actual learner-visible programs against the current preflight outcomes, rather than an import/formula count.

**Author result:** ten new executable tool bridges, one explicit reuse route, and two retained-mechanism dispositions with clearer production boundaries. Thirteen bodies changed; no original example program or historical expected output was overwritten. All thirteen JSX bodies parse. `scripts/verify-math-depth-remediation.py` passed **71 groups**: ten complete new programs, forty additional invariance/edge checks, and exact replays of all nine Second-Order and twelve Stochastic Processes examples. Author work is distinct from root's independent review and final shared production/browser integration.

## Source and execution binding

- [`MATH-DEPTH-REMEDIATION.json`](MATH-DEPTH-REMEDIATION.json) is the machine-readable per-topic manifest: stable ID, exact body path (including legacy aliases), status, section anchor, changed files, source assets, author evidence, and SHA-256 for every reviewed body, original example owner and new program/output module. It also binds the unchanged shared code disclosure UI. These are the exact source hashes for this report; do not substitute current hashes after an unreviewed edit.
- [`math-depth-remediation-author.json`](../evidence/math-depth-remediation-author.json) records each executed Python source hash, exact stdout/stderr, all 71 named checks, runtime versions, and retained-program hashes. Expected-output modules were generated from these actual executions, not handwritten numerical claims.
- Preflight JSON for each stable ID is retained under `scratch/math-depth-remediation/`; all thirteen preflights passed. Queueing's stable ID contains `little-s-law`, while its body uses `littles-law`; Itô's stable ID starts `it-calculus`, while its body starts `ito-calculus`.
- Reproduce with `scratch/lesson-tools/Scripts/python.exe scripts/verify-math-depth-remediation.py`. The tested Python is 3.12.14, NumPy 2.3.5, SciPy 1.18.1, CPU PyTorch 2.14.0, scikit-learn 1.9.1 and NetworkX 3.6.1. Additional comparison packages are POT 0.9.7, statsmodels 0.15.0, SimPy 4.1.2 and sdeint 0.3.0. They were installed only in `scratch/math-depth-remediation/extra-dependencies` with their needed Patsy dependency; the existing shared runtime was not modified. Each learner program states ordinary installation requirements without depending on this workspace layout.
- `--refresh-manifest` refreshes prose/doc bindings only after proving the executed program hashes still match the retained receipt. It does not fabricate a new execution or independently review changed code.

The ten new programs live under semantic `public/learn-assets/<topic>/` directories. Each body imports only its own small `<topic>-mechanism-program.js` output metadata. The existing `MechanismProgram` fetches source only when the disclosure opens, supplies a direct download, exposes keyboard-scrollable code/output regions, handles failed fetch/retry and supports cached reopening. No shared runtime, global CSS, navigation, manifest publication entry, package lock or historical experiment was changed. Root owns the final desktop/phone, light/dark, disclosure and download checks against production. No author build or server was started.

## 1. Matrix Calculus & Jacobians

**Disposition: concrete tool-route gap repaired.** Existing `matrix-calculus-examples.js` already implements explicit Jacobians, chain products, a full affine batch pullback, a scalar dual-number JVP, every-parameter finite differences, matrix-square differentials, solve sensitivity, softmax products and a Hessian example. These are genuine mechanisms. Its framework route previously ended with an optional documentation pointer.

New `matrix-calculus/derivative-library-bridge.py` gives one runnable manual-to-PyTorch path. It compares the local two-output Jacobian, JVP, VJP and adjoint pairing; exposes a reusable rectangular affine objective/pullback; maps `[input,output]` weights to `F.linear`'s transposed layout; and tests the same scalar fixture used by Multivariate Calculus with `torch.func.grad`/`jvp`. Both implementations use float64 and sum-over-outputs divided by twice the batch size. A broadcast target is rejected before it can silently change the objective.

The visible section `matrix-calculus-code-route` explains the mapping, actual outputs, dense-Jacobian storage, affine O(Ndk) arithmetic and reverse-mode saved-state boundary. Customization duplicates a batch and changes a direction; solutions specify unchanged parameter gradients and halved per-copy input gradients. Native tests use a new 5-by-3 input/3-by-4 weight fixture for this invariance. The general AD graph remains owned by Backpropagation, not duplicated here. Tested APIs are documented by [PyTorch JVP](https://docs.pytorch.org/docs/2.14/generated/torch.func.jvp.html) and its [function-transform tutorial](https://docs.pytorch.org/tutorials/intermediate/jacobians_hessians.html).

## 2. Multivariate Calculus & Gradients

**Disposition: missing practical mapping repaired through explicit reuse.** Existing `multivariate-calculus-examples.js` contains the tangent remainder, path counterexample, normalized versus unnormalized directions, constrained motion, inconclusive Hessian cases, exact gradient-descent recurrence, physical unit conversion and a small forward derivative engine. It is not just symbolic differentiation.

The new `multivariate-calculus-code-route` links the actual Matrix-owned downloadable program rather than copying an AD engine or importing another topic's data bundle. Its scalar fixture is exactly `x²*y + sin(x)` at `(1.2,-.7)`, with gradient `(-1.317642,1.44)` and velocity `(2,-1)` giving `-4.075284`. The visible practice changes speed to a unit direction (slope approximately `-1.822523`) and rescales coordinates while preserving the physical displacement. The source, setup and complete runnable tool mapping are one click away under a named owner.

Boundary: AD's local rule at a kink is not proof of total differentiability. The retained counterexamples own that distinction. No artificial second package or new optimization experiment was added.

## 3. Second-Order Methods

**Disposition: deeper assessment confirms the current mechanisms; production scope clarified.** All nine programs in `second-order-methods-examples.js` were replayed exactly. They include safeguarded Newton with damping/backtracking, a reusable L-BFGS two-loop application compared with a dense BFGS oracle, an actual CPU `torch.optim.LBFGS` nonlinear fit with a correct closure/final reevaluation, Bernoulli natural-gradient coordinates, exact versus factored one-layer Fisher matrices, damped K-FAC solves, original matrix Shampoo with eigendecomposition and row-rotation checks, matrix-free HVP/CG with a dense solve oracle, and a changed-data fit comparison.

The new `second-order-code-route` names each actual owner, the maintained optimizer workflow, and the intended primitive boundary. NumPy's solve/eigensolver is the correct tool underneath the explicit finite K-FAC/Shampoo mechanisms; a new opaque optimizer wrapper would not improve this comparison. The body states L-BFGS O(md) work/storage, matrix-Shampoo statistic sizes and cubic dense root refresh cost. Existing practice already changes curvature history, Fisher damping and fit budgets.

Limit: one-layer K-FAC and original matrix Shampoo are not production distributed training implementations. Such a route needs separately specified statistics, cadence, blocking, precision and communication. The destination note's Muon/polar-factor extension is an additional optimizer geometry, not an omitted step in these declared algorithms; this assessment does not silently close or implement the pending extension from the prepared Advanced Optimizers owner.

## 4. Constrained & Multi-Objective Optimization

**Disposition: general projection and maintained solver route added.** Existing `constrained-multiobjective-examples.js` already exposes the coupled 2D projection, gradient mapping, penalty/barrier models, a full two-block ADMM loop with primal/dual stopping, Pareto comparisons preserving duplicates, unsupported efficient points, unit sensitivity and common-descent geometry. Its numerical tool route did not show a maintained constrained optimizer on the same problem.

New `constrained-optimization/simplex-solver-bridge.py` derives and implements the sorting/shared-threshold projection for arbitrary positive simplex budget. It uses this in an extendable positive-diagonal quadratic projected solver with a measured mapping and explicit exhausted-budget result. SciPy SLSQP gets identical objective, analytic derivative, bounds and linear equality. Success is followed by independent feasibility/mapping checks, not treated as the mathematical certificate itself. The changed 4D fixture returns `(6/7,0,3/7,3/14)` at budget 1.5; a one-step run reports nonconvergence.

Projection O(d log d), K-step descent O(Kd log d), O(d) working memory; moderate finite scales and positive curvature are explicit. Author checks add conservation, idempotence and common-translation invariance at three budgets. Visible practice changes the budget and introduces inconsistent constraints. Full ADMM and multi-objective preference handling remain owned by the existing programs. [SLSQP controls](https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html) support the API mapping, with different solver/mapping tolerances explicitly distinguished.

## 5. Optimal Transport

**Disposition: vectorized reusable Sinkhorn/tool mapping added.** Existing `optimal-transport-examples.js` already owns exact primal/dual certificates, sorted weighted 1D transport, an executed HiGHS linear program, a complete scalar log-domain Sinkhorn loop with residual/budget state, objective constants, debiasing, barycentric projection and bounded variant examples. Exact transport did not need replacement.

New `optimal-transport/sinkhorn-library-bridge.py` vectorizes the row/column log-sum-exp updates, shifts a common cost baseline, fixes a dual gauge, and verifies both marginals. It compares an equal-weight 2D and unequal-weight 3-by-2 fixture with POT's `method='sinkhorn_log'`, then independently computes linear and both entropy-objective conventions. Unit total mass explains the epsilon constant difference. A sharp five-step problem deliberately retains its nonconverged state.

The visible `optimal-transport-code-route` gives the derivation-to-array bridge, setup, costs and a joint cost/epsilon scaling exercise. Native checks add rectangular conservation, scaling invariance and zero-mass rejection. Dense returned plans require O(nm) memory and O(Knm) arithmetic; support is strictly positive probability marginals with representable cost/epsilon ranges. Zero masses need caller removal/reinsertion; acceleration, implicit kernels, unbalanced mass and AD are separate extensions. [POT's API](https://pythonot.github.io/all.html#ot.sinkhorn) is linked at the usage point.

## 6. Causal Inference & Do-Calculus

**Disposition: finite fitted estimation workflow added without claiming automated identification.** Existing `causal-inference-examples.js` implements explicit SCM intervention, collider selection, cell standardization, observationally indistinguishable causal models, the scoped do-calculus rule example, frontdoor success/failure, counterfactual coupling, population AIPW expectations under nuisance misspecification, IV/LATE and conditional bootstrap calculations.

The missing bridge was from identified cell quantities to fitted nuisance models. New `causal-inference/causal-estimation-bridge.py` expands the original 200-row table, computes cell means/propensities with arrays, and fits statsmodels binomial GLMs with explicit intercept, treatment/stratum interaction and convergence checks. It forms treatment-zero/one design matrices and evaluates g-computation, IPW and the complete AIPW score explicitly. All produce `.1`, compared with raw `.22`. A changed table produces `.2`. The fixed-cell bootstrap is vectorized over four binomial counts per replicate.

Visible interpretation and practice distinguish causal identification, overlap, target population and estimator computation. Saturated finite cells require positive cell sizes and both outcomes; the score rejects zero/one propensity. Flexible nuisance learners need suitable cross-fitting, diagnostics and a separately specified inference design. The bootstrap conditions on cell sizes and does not assess unmeasured confounding. [statsmodels GLM](https://www.statsmodels.org/stable/generated/statsmodels.genmod.generalized_linear_model.GLM.html) is the ordinary fitted-model route; importing a graph package could not validate the assumptions.

## 7. Mutual Information & Information Bottleneck

**Disposition: discrete estimator API/count contract added; IB retained.** Existing `mutual-information-examples.js` already contains finite MI/conditional MI, XOR synergy, data processing, a full finite-IB alternating update with log weights and initialization-sensitive traces, variational bounds, continuous unit invariance, plug-in bias and identifier leakage. The finite-IB iteration is an actual algorithm and is not replaced by a feature-score call.

New `mutual-information/mutual-information-library-bridge.py` implements reusable integer-count MI using masked subtracted logs; compares it with scikit-learn's contingency MI and explicitly discrete feature estimator; and converts nats to bits. Row duplication, relabeling, unused categories, independent tables and identifier memorization are exercised. The implementation rejects normalized probabilities at the integer-count boundary. `[[30,10],[10,30]]` yields `.130812036` nats/`.188721876` bits.

The body explains O(rc) dense count storage/work, keeping large sparse counts sparse, and the small-fixture-only row expansion. It reuses the already implemented Feature Selection lesson for train-only fitting and held-out workflow. Continuous nearest-neighbor MI is a different estimator with its own tuning; neither route certifies population independence from a zero estimate. [Count MI](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.mutual_info_score.html) and [feature MI](https://scikit-learn.org/stable/modules/generated/sklearn.feature_selection.mutual_info_classif.html) are linked by contract.

## 8. f-Divergences & Integral Probability Metrics

**Disposition: bounded-memory exact MMD and ordinary test mapping added.** Existing `divergence-ipm-examples.js` covers support-aware finite divergences, event/TV decisions, coarsening, 1D transport/IPM critics, biased/unbiased MMD, exact permutations including selection-aware bandwidth logic, variational KL/GAN critics and importance-weight/risk examples. Its pairwise loop is readable but needed an extendable memory-conscious implementation and maintained comparison route.

New `divergences/mmd-library-bridge.py` streams block kernel sums through `math.fsum`, using direct squared distances rather than cancellation-prone norm expansion. Block scratch is O(B²); arithmetic remains quadratic in observations times dimension. A full scikit-learn Gram exists only for the six-point oracle. SciPy performs the same exact independent-sample right-tail test over 20 allocations (p=.1). Relative entropy and the squared SciPy Jensen–Shannon distance verify log/square conventions.

Visible practice proves identical-sample biased zero versus allowed negative unbiased value and varies block size; author checks retain the negative estimate at three block sizes. No clamp or hidden change of estimator is introduced. Exact enumeration is combinatorial; Monte Carlo and data-dependent selection require their own calibration. Moderate representable squared distances/bandwidths are the declared support. References: [permutation test](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.permutation_test.html), [JS distance](https://docs.scipy.org/doc/scipy/reference/generated/scipy.spatial.distance.jensenshannon.html).

## 9. Graph Fundamentals

**Disposition: sparse matrix/tool convention bridge added.** Existing `graph-fundamentals-examples.js` implements adjacency-list components, weighted additive adjacency, walks, incidence/energy identities, exact small nullspace calculations, normalization/isolate/loop conventions, averaging, anchored harmonic interpolation and a bounded GCN example. No new traversal library is used to hide the component mechanism.

New `graph-fundamentals/laplacian-library-bridge.py` constructs COO/CSR adjacency with additive duplicates and removed zero entries, compares a NetworkX MultiGraph in explicit node order, forms both Laplacians, checks SciPy components and solves the anchored sparse system. Every component needs an anchor, including an isolate. A self-loop leaves ordinary L unchanged but changes row-sum normalization: the center becomes `3/7`, while SciPy's loop-excluding normalized convention gives `1`.

The visible route explains this difference, O(V+E) sparse storage/action after assembly, conversion sorting and factorization fill. The changed-edge practice and author check give interior value `.6`; zero-weight pseudo-connections cannot rescue an unanchored isolate. Large graphs remain sparse; the dense oracle is small-fixture-only. References: [SciPy Laplacian](https://docs.scipy.org/doc/scipy/reference/generated/scipy.sparse.csgraph.laplacian.html), [NetworkX sparse conversion](https://networkx.org/documentation/stable/reference/generated/networkx.convert_matrix.to_scipy_sparse_array.html).

## 10. Stochastic Processes

**Disposition: deeper assessment confirms implementations; primitive/application ownership clarified.** All twelve programs in `stochastic-processes-examples.js` were replayed against exact recorded outputs. They cover distinct path laws with equal marginals, Markov propagation and departure fitting with unidentified rows, exact first passage, stable inverse-exponential arrivals including a zero-rate case, integrated-intensity clocks, two-state CTMC exposure with final censoring, shared-grid Brownian coupling, bridge crossing and quadratic-variation/stopping diagnostics.

The new `stochastic-processes-code-route` explains that Python's maintained random generator supplies primitive draws while the explicit programs own process composition. Changing generator API with the same integer seed does not imply path equality. It states dense propagation O(d²), linear event/increment work, retained-path storage versus streaming sufficient sums, and the bounded exact first-passage oracle's sparse-solver boundary.

An implementation exercise converts the CTMC estimator to streaming counts/exposures and requires final clipped exposure without an invented departure. Actual downstream owners are linked: Queueing's SimPy resource workflow and Itô's common-noise solver bridge. A separate process package would duplicate these already supplied routes rather than improve the mechanism. This is not a claim of general state-space inference, arbitrary stationary convergence, or a production event framework.

## 11. Queueing Theory

**Disposition: ordinary discrete-event tool route added, efficient specialized simulation retained.** Existing `queueing-examples.js` implements FCFS traces and clipped areas, M/M/1 laws and tails, M/G/1 residual mechanisms, pooling/Erlang-C, finite capacity and duration-based Lindley simulation with retained independent-run evidence. Its measured long-run outputs were not rerun or replaced.

New `queueing/fcfs-simpy-bridge.py` supplies a reusable O(N) finite-cohort recurrence and a SimPy Resource scheduler on identical arrivals/service requirements. It compares every event time, listed-order ties, zero service, idle time and cohort/observation-window occupancy. The four-job complete area is 12, versus censored area 8 at time 4. A changed forty-job trace with ties/zero service passes the author comparison.

The body explains scheduler overhead and why a general simulator is useful for richer resources, not faster than the specialized recurrence. Capacity-change practice requires a new earliest-available-server oracle instead of reusing the single-server equation incorrectly. The existing duration-based simulation remains the long-run single-server owner. Stable law/PASTA claims do not follow from a drained finite trace. Setup copy now explicitly distinguishes standard-library core examples from the NumPy/SimPy bridge. [SimPy resources](https://simpy.readthedocs.io/en/latest/topical_guides/resources.html) supports the request/release mapping.

## 12. Itô Calculus & SDEs

**Disposition: general callback solver and supplied-noise library mapping added.** Existing `ito-sde-examples.js` contains adapted integral/isometry calculations, GBM/OU exact laws and stable small-step moments, covariance orientation, Itô/Stratonovich conversion, coupled EM/scalar Milstein, exact strong/weak error calculations, generators and martingale examples. The new route extends rather than replaces their analytic references.

New `ito-calculus/sde-library-bridge.py` implements reusable vector-state Euler–Maruyama with a state-by-driver diffusion matrix and explicit input increments; scalar Milstein receives the diffusion derivative explicitly. `sdeint.itoEuler` gets the exact same uniform grid and increments, yielding entrywise parity. A second non-diagonal two-state additive case checks driver orientation against its exact summed-noise result. Coarse increments sum groups of fine increments; no independent resampling masquerades as discretization error.

The body gives O(Kdr) diffusion arithmetic plus coefficient costs, O(Kd) path and O(Kr) supplied-noise storage, and a zero/unused-driver exercise. No arbitrary coefficient stability, adaptivity, positivity, stiff integration or multidimensional Lévy-area claim is made. One realized path cannot establish a convergence order. The tested sdeint version is pinned to 0.3.0; its [integrator source](https://github.com/mattja/sdeint/blob/master/sdeint/integrate.py) exposes the comparison contract.

## 13. Numerical PDEs

**Disposition: extendable banded P1 route added; existing sparse multidimensional owner retained.** Existing `numerical-pde-examples.js` already contains reusable SPD tridiagonal LDL/factor reuse, finite differences and boundary loads, whole-field error certificates, refinement, cached theta-method solves, conservative interface/Neumann flux, transport/Godunov steps, explicit P1 hats/point sources/energy error, a genuine sparse 2D rectangle solve, triangle geometry and a deliberately bounded two-grid correction. The small dense P1 assembly was the specific extension gap.

New `numerical-pdes/finite-element-banded-bridge.py` assembles only the main/off diagonals for a nonuniform 1D P1 mesh, handles nonzero Dirichlet values and off-node point-load hat weights, and specializes this topic's LDL mechanism into a standalone file. SciPy banded Cholesky/factor reuse uses identical arrays. Both factors solve a quadratic forcing case and a second point-source load without refactorization. This stays within the same numerical-PDE ownership; no new general sparse solver is implemented elsewhere.

Assembly/factor/solve are O(N) in this tridiagonal setting. Increasing nodes, positive element conductivity, piecewise-constant forcing and a positive-pivot contract are explicit. Native practice checks an on-node load, exact Green nodal values and rejected invalid coefficients/indefinite pivots. Nodal exactness is explicitly not exact piecewise-linear reconstruction. General forcing needs quadrature; the already implemented sparse rectangle/triangle routes own higher-dimensional handling. References: [banded Cholesky](https://docs.scipy.org/doc/scipy/reference/generated/scipy.linalg.cholesky_banded.html), [factor solve](https://docs.scipy.org/doc/scipy/reference/generated/scipy.linalg.cho_solve_banded.html).

## Review and integration handoff

Each new bridge section provides a concrete hand mechanism, same-input tool mapping, an executed output, customization with separately disclosed reasoning, explicit cost, and a production boundary. Earlier lessons, visual labs, mathematical counterexamples, practice and references remain intact. The two assessment-only mechanisms received explanatory body additions, so the manifest correctly marks their bodies changed even though their Python example sources are unchanged.

Root's independent numerical review is a separate evidence owner (`scripts/review-math-depth-remediation.py` and `math-depth-remediation-independent.json`); author rereading is not described as independent. The author addressed the identified streaming-memory and Queueing setup-copy issues before freezing. Remaining integration work is root's source-bound independent learning-experience closure and final production checks of all changed bodies, code disclosures, downloads, themes, narrow widths and route aliases. No unresolved author numerical failure remains.
