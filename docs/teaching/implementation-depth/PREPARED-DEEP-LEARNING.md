# Prepared Deep Learning implementation-depth triage

**Historical triage; superseded as the active writing plan by [the 37-packet content revision](PREPARED-WRITING-REVISION.md).** The user subsequently required missing scratch/library teaching to be written before content completion. Findings below identify the original source and proposed work; instructions to add core content during finishing are superseded. They remain historical evidence, not the current phase boundary.

Reviewed: 21–22 September 2026. Scope: the **37 content-complete, implementation-pending packets**, beginning with Weight Initialization; the first five Deep Learning implementations are reviewed separately. No manuscript, prepared program, phase status or browser implementation is changed by this report.

This is a **breadth triage of actual code**, not a complete re-review of 37 manuscripts or a fresh native execution claim. For each packet, the inspection read the main mechanism function(s), the supplied ordinary-framework route or relevant API prose, and the manuscript links that make those programs learner-visible. Exact locations below show what was examined. Not every helper, secondary algorithm, native assertion, resource or advanced claim was rechecked. In particular, previous content-complete status is not evidence of compliance with a newly added implementation-depth requirement.

A framework-composed block can be the correct ordinary implementation: an attention lesson must expose attention, but a later transformer lesson may reuse that already-taught operator. A CPU reference is not an efficient accelerator kernel. An official API link is not an implemented package workflow, and a supplied optional program marked unexecuted must stay unexecuted until evidence changes. No nonexistent specialist API is demanded for conceptual or research-architecture outcomes.

## Packet-by-packet source findings

| Prepared order / topic | Disposition | Inspected implementation and remaining boundary |
| --- | --- | --- |
| 1. [Weight Initialization: Xavier, Kaiming, Orthogonal Methods & μP](../drafts/weight-initialization-xavier-kaiming-p/lesson.md) | **Core routes present; μP depth needs separate review** | `fill_weight` computes Xavier/Kaiming standard deviations explicitly and uses the ordinary random-fill primitive; the manuscript calls nn.init.kaiming_normal_/xavier_normal_. Signal and width experiments expose learned-state and scale effects. Orthogonal factorization and full μP package integration are not claimed locally reconstructed; record their owner before strengthening those outcomes. [Inspected source](../drafts/weight-initialization-xavier-kaiming-p/initialization-experiments.py:20). |
| 2. [Residual Connections & Skip Connections: Keep a Path, Learn a Correction](../drafts/residual-connections-skip-connections/lesson.md) | **Core mechanism and framework composition present** | `Refinement.forward` computes the branch, checks matching shape and adds a fixed/learned residual scale; `DigitNetwork` composes real nn.Modules and optimizer training. Conv/linear/norm/autograd primitives are reused. Torchvision ResNet source is linked, but a pretrained-model use example is not locally demonstrated: reuse the implemented Transfer Learning owner if that becomes an advertised outcome. [Inspected source](../drafts/residual-connections-skip-connections/residual-experiments.py:15). |
| 3. [Dropout, DropPath & Stochastic Depth](../drafts/dropout-droppath-stochastic-depth/lesson.md) | **Core routes present; exact random-parity scope limited** | `mask_values` implements inverted Bernoulli masks at element/channel/row/batch granularity, including p=0/1 and eval mode; the same program uses nn.Dropout/BatchNorm and contains mode/MC experiments. Mathematical/stochastic-contract comparisons are appropriate; matching seeds alone does not prove identical random-number consumption across APIs. [Inspected source](../drafts/dropout-droppath-stochastic-depth/dropout-experiments.py:17). |
| 4. [Convolution, Pooling & Receptive Fields](../drafts/convolution-pooling-receptive-fields/lesson.md) | **Strong paired core present** | Direct NCHW loops expose channel groups, stride, padding, dilation and bias; fixtures compare ordinary/strided/dilated/grouped/depthwise-multiplier cases with F.conv2d at 1e-12. The direct algorithm is a tiny correctness reference, not production convolution. Native gradients, unfold/fold and model composition reuse tensor/autograd primitives. [Inspected source](../drafts/convolution-pooling-receptive-fields/convolution-experiments.py:16). |
| 5. [Landmark Architectures: LeNet, AlexNet, VGG, ResNet & EfficientNet](../drafts/landmark-architectures-lenet-alexnet-vgg-resnet-efficientnet/lesson.md) | **Core block composition present; named-family scope needs review** | Plain/residual, parallel and inverted-gated modules implement their routing and squeeze/excitation; cost hooks and a controlled CNN comparison are supplied. They are not complete replications of every historical architecture named in the title. The manuscript separates architecture/weight/preprocessing contracts; decide which model-builder application is locally needed versus reuse of Transfer Learning. [Inspected source](../drafts/landmark-architectures-lenet-alexnet-vgg-resnet-efficientnet/architecture-experiments.py:16). |
| 6. [Depthwise Separable & Dilated Convolutions](../drafts/depthwise-separable-dilated-convolutions/lesson.md) | **Strong paired core present** | Per-input-channel truncated SVD constructs depthwise/pointwise factors and reconstructs effective weights; manuscript has an exact Conv2d/F.conv2d comparison and linked direct-loop author checks. Convolution primitives are properly reused from the preceding owner. No nonlinearity is inserted between linear factors; rank/computation claims remain distinct from observed runtime. [Inspected source](../drafts/depthwise-separable-dilated-convolutions/convolution-factorization.py:35). |
| 7. [ConvNeXt & Modern CNN Designs](../drafts/convnext-modern-cnn-designs/lesson.md) | **Core mechanism/composition present; deployment reuse needed** | ResponseNorm, DropPath, complete block/stage/head construction and masked reconstruction training expose the actual operations. Library Conv2d/LayerNorm/Linear and meta-tensor shape checks are ordinary tools. Pretrained ConvNeXt use is prose plus official reference; link an actual Transfer Learning program or add a short checkpoint/preprocessing route if deployment is retained as an outcome. [Inspected source](../drafts/convnext-modern-cnn-designs/convnext-blocks.py:21). |
| 8. [Capsule Networks: learning which parts belong together](../drafts/capsule-networks/lesson.md) | **Mechanism and trainable framework route present** | NumPy routing and bounded diagonal EM illustration expose coupling/squash/update state; capsule-learning.py implements the differentiable Torch routing/model and real-data fit. No standard opaque capsule API is required. Full EM-capsule paper reproduction is explicitly not claimed; numerical/extreme-input and zero-iteration contracts still require focused finish review. [Inspected source](../drafts/capsule-networks/capsule-mechanics.py:22). |
| 9. [RNNs, LSTMs & GRUs](../drafts/rnns-lstms-grus/lesson.md) | **Strong paired core present** | NumPy manual_sequence opens RNN/LSTM/GRU affine gates and reconstructs saved native weights, including PyTorch GRU reset placement. The full pen classifier uses native recurrent layers; chunk/detach/padding checks are supplied. Preserve gate order, bias addition, state ownership and native parity rather than replacing them with a generic stepper. [Inspected source](../drafts/rnns-lstms-grus/recurrent-mechanics.py:18). |
| 10. [Sequence-to-Sequence Encoder–Decoder: Read an Input, Generate an Output](../drafts/sequence-to-sequence-encoder-decoder/lesson.md) | **Mechanism and ordinary framework workflow present** | Scalar joint derivative, manual GRU sequence trace, greedy/beam search and real inflector are supplied; inflection-seq2seq.py composes embeddings, native GRUs, loss and optimizer. Reuse the recurrent owner instead of reimplementing its solver again. Input/target shifts, EOS and beam state need their existing explicit boundaries retained. [Inspected source](../drafts/sequence-to-sequence-encoder-decoder/sequence-mechanics.py:19). |
| 11. [Attention: Let Each Output Read the Input It Needs](../drafts/attention-mechanism-bahdanau-luong/lesson.md) | **Mechanism and ordinary framework workflow present** | Additive/general score, padding mask, weighted read and decoder order are explicit Torch operations; source-only key projections are cached. A NumPy/native trace companion opens recurrence/scoring. MultiheadAttention is correctly not treated as a drop-in Bahdanau decoder. This audit did not re-run trained-model parity or every padding edge. [Inspected source](../drafts/attention-mechanism-bahdanau-luong/attentive-inflection.py:53). |
| 12. [Long-Context Sequence Models: Transformer-XL, Griffin and Perceiver](../drafts/long-context-sequence-models-transformer-xl-griffin-perceiver/lesson.md) | **Mechanisms present; architecture/package bridge needs review** | Segmented attention and gated recurrence are explicit NumPy; the latent trajectory classifier is a complete trainable Torch composition. Preserve the distinction between restricted pedagogical mechanisms and full Transformer-XL/Griffin/Perceiver systems. Select actual prerequisite owners and a usable package/configuration route for any stronger deployment promise; no full architecture equivalence is certified here. [Inspected source](../drafts/long-context-sequence-models-transformer-xl-griffin-perceiver/sequence_mechanisms.py:18). |
| 13. [State Space Models: S4 and the Mamba Family](../drafts/state-space-models-s4-mamba-mamba-2/lesson.md) | **Mechanisms present; hardware/API boundary explicit** | Block exponential handles singular A; bilinear solves, recurrence, convolution and SSD recurrence/chunk calculations are present. Trainable S4D-style/selective models use Torch. The manuscript explicitly excludes full S4 DPLR and hardware selective-scan implementation. Official Mamba is referenced; a production loader/scan API route remains an assessment item if advertised, not evidence of executed kernels. [Inspected source](../drafts/state-space-models-s4-mamba-mamba-2/state_space_mechanisms.py:13). |
| 14. [RWKV & Linear Attention Models](../drafts/rwkv-linear-attention-models/lesson.md) | **Mechanisms and framework models present** | Positive-kernel recurrence/chunking, log-rescaled RWKV state and matrix update are implemented; trainable trajectory models are supplied. Keep current-output bonus separate from persistent state. Accelerated Flash Linear Attention/RWKV interfaces are references, not measured native parity; choose a concrete deployment owner if needed rather than claim the CPU loop is optimized production code. [Inspected source](../drafts/rwkv-linear-attention-models/linear_memory_mechanisms.py:13). |
| 15. [Self-Attention & Multi-Head Attention](../drafts/self-attention-multi-head-attention/lesson.md) | **Strong paired core present** | Complete SelfAttention exposes projections, head layout, masked scores, softmax and output merge; copies identical weights into nn.MultiheadAttention and compares outputs/weights. A second runnable SDPA route maps opposite mask semantics and explicit eval dropout. Keep this as a reusable attention owner and preserve the square-output memory caveat. [Inspected source](../drafts/self-attention-multi-head-attention/lesson.md:256). |
| 16. [Transformer Block Architecture](../drafts/transformer-block-architecture/lesson.md) | **Strong paired core present** | Custom pre/post-norm TransformerBlock copies attention/norm/feed-forward state into TransformerEncoderLayer and compares both orders; RMSNorm/SwiGLU branches are explicit. Attention itself is properly reused from the preceding owner. Distinguish a block from a whole pretrained model; do not duplicate the attention implementation here. [Inspected source](../drafts/transformer-block-architecture/lesson.md:222). |
| 17. [Positional Encodings: Sinusoidal, Learned, RoPE and ALiBi](../drafts/positional-encodings-sinusoidal-learned-rope-alibi/lesson.md) | **Mechanism and model composition present; external parity pending** | Runnable sinusoid, learned-table, RoPE, ALiBi slopes and full/cached attention preserve absolute positions. The real Torch experiment provides practical model use. Rotation pairing, partial widths and scaling variants are version-specific: exact checkpoint-library parity was not established by this breadth read; make that a scoped finish decision. [Inspected source](../drafts/positional-encodings-sinusoidal-learned-rope-alibi/lesson.md:430). |
| 18. [Grouped-Query Attention and Multi-Query Attention](../drafts/grouped-query-attention-gqa-multi-query-attention-mqa/lesson.md) | **Strong paired core present** | NumPy grouped attention opens the query-to-KV map and stable masked softmax; tied MHA and compact cache are checked. Manuscript shows SDPA enable_gqa=True with explicit legal mask/dropout. Future implementation must retain head/order/scale checks and avoid claiming repeated K/V tensors save memory. [Inspected source](../drafts/grouped-query-attention-gqa-multi-query-attention-mqa/lesson.md:246). |
| 19. [Multi-Head Latent Attention: store a smaller description, read it exactly](../drafts/multi-head-latent-attention-mla/lesson.md) | **Mechanism and model composition present; external parity pending** | Runnable MLA expanded vs absorbed reads compare both weights and output, using the original attention scale and separate rotary branch. Full CPU forecasting model is supplied. Official DeepSeek model code is a reference, not an executed backend; local absorption identity does not validate every deployed cache layout. [Inspected source](../drafts/multi-head-latent-attention-mla/lesson.md:244). |
| 20. [Sparse & Linear Attention Variants](../drafts/sparse-linear-attention-variants/lesson.md) | **Core recurrence present; family scope needs review** | Explicit causal nonnegative-feature attention accumulates state/normalizer and rejects zero overlap. Supplied training/mechanism programs compare alternatives. Sparse/linear is a broad family; evaluate each advertised mechanism and choose kernel/library owners rather than count this one recurrence as an implementation of every named variant. [Inspected source](../drafts/sparse-linear-attention-variants/lesson.md:400). |
| 21. [Vision Transformers (ViT, DeiT, Swin, DINOv2)](../drafts/vision-transformers-vit-deit-swin-dinov2/lesson.md) | **Mechanisms plus optional application code present** | Actual shifted-window attention has padding/wrap masks, relative bias and reverse layout; DINO two-view loss/EMA, patch merging and models are supplied. inspect-pretrained-features.py uses AutoImageProcessor/AutoModel with local-only DINOv2 weights and explicit token/grid checks. That application is honestly marked unexecuted; preserve the status and test it when dependencies/assets are available. [Inspected source](../drafts/vision-transformers-vit-deit-swin-dinov2/vision-mechanisms.py:28). |
| 22. [Mixture-of-Experts Transformers (MoE)](../drafts/mixture-of-experts-transformers-moe/lesson.md) | **Core mechanism and framework workflow present** | Top-2 selection dispatches only assigned tokens, combines with index_add_ and computes auxiliary balance from actual loads; trainable experts are nn.Modules. MegaBlocks is an optional production/backend owner, not secretly used by this CPU implementation. Capacity/overflow/gradient variants require their own stated contracts; all backend performance remains unverified. [Inspected source](../drafts/mixture-of-experts-transformers-moe/moe_study.py:22). |
| 23. [Interleaved / Cross-Attention Architectures](../drafts/interleaved-cross-attention-architectures/lesson.md) | **Core routes present; stale prose noted below** | NumPy masked cross-attention and a complete nn.MultiheadAttention question-conditioned/gated model are supplied. Base attention and residual owners are reusable; image/query roles are explicit. Retain the distinction between model predictions and a learner-prediction UI. A stale “record two expectations before running” instruction is recorded below for finish cleanup. [Inspected source](../drafts/interleaved-cross-attention-architectures/cross-attention-study.py:20). |
| 24. [Message Passing & Graph Convolutions (GCN, GAT, GraphSAGE)](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/lesson.md) | **Confirmed specialist bridge gap** | Dense GCN normalization, GraphSAGE self/neighbor contributions and GAT masked scores are explicit and trainable. Manuscript line469 references PyG GCNConv, but no supplied program uses PyG or maps edge_index/normalization/weights to the dense reference. Add a tiny actual package bridge; dense 34-node code is not an efficient sparse-graph deployment route. [Inspected source](../drafts/message-passing-graph-convolutions-gcn-gat-graphsage/message-passing-study.py:14). |
| 25. [Graph Transformers & Geometric Deep Learning](../drafts/graph-transformers-geometric-deep-learning/lesson.md) | **Confirmed specialist bridge gap; reuse primary graph owner** | Shortest-path/return-probability structure and explicit biased multihead attention are supplied with graph models. Manuscript line431 references GPSConv but no supplied program shows its local/global composition/batching. Reuse the message-passing package bridge for edge conventions; this topic should own only the added positional/structural and local/global mapping. [Inspected source](../drafts/graph-transformers-geometric-deep-learning/graph-transformer-study.py:13). |
| 26. [Boltzmann Machines & Restricted Boltzmann Machines (RBM)](../drafts/boltzmann-machines-restricted-boltzmann-machines-rbm/lesson.md) | **Confirmed specialist bridge gap** | Stable free energy, exact hidden enumeration, sufficient statistics and Gibbs transitions are implemented; small hidden state makes an exact learning study possible. Manuscript line586 names BernoulliRBM and correctly qualifies pseudo-likelihood, but no learner package fit/transform program exists. Add the ordinary PCD route with an honest objective/metric mapping; exact enumeration is exponentially bounded. [Inspected source](../drafts/boltzmann-machines-restricted-boltzmann-machines-rbm/rbm-study.py:22). |
| 27. [Spectral Normalization & Gradient Penalty](../drafts/spectral-normalization-gradient-penalty/lesson.md) | **Core routes present** | Explicit input-gradient penalty uses create_graph=True and detached endpoints; the complete experiment calls maintained spectral_norm parametrizations. Power-iteration/derivative calculations are supplied separately. Maintain buffer/mode/access-count and detach contracts; do not replace the tested library wrapper with fragile custom mutable state. [Inspected source](../drafts/spectral-normalization-gradient-penalty/critic-regularization-study.py:33). |
| 28. [Modern Hopfield Networks](../drafts/modern-hopfield-networks/lesson.md) | **Mechanism and framework workflow present; optional specialist route unverified** | Binary asynchronous retrieval and continuous softmax-energy retrieval are implemented; digit_memory.py builds/trainable Torch retrieval. Official Hopfield module APIs are described and linked, not executed. A specialist package adapter may be useful, but ordinary tensor-bank composition is already a practical implementation; do not declare a missing core merely from absence of HopfieldLayer. [Inspected source](../drafts/modern-hopfield-networks/associative_memory.py:12). |
| 29. [xLSTM: learn what to keep, and how to read it back](../drafts/xlstm-extended-lstm/lesson.md) | **Mechanisms present; package/backend parity pending** | Stabilized scalar and matrix memory, correct denominator floor, parallel and chunk routes are supplied; trainable scalar/matrix digit readers use Torch. Moderate-input unscaled chunk code is explicitly scoped. Official xLSTM integration is a resource; it has not been reconciled with the local cell variants in this audit. [Inspected source](../drafts/xlstm-extended-lstm/memory_mechanisms.py:10). |
| 30. [Hyena & Long Convolution Models](../drafts/hyena-long-convolution-models/lesson.md) | **Core mechanisms and Torch workflow present** | Direct/FFT/modal/overlap-add routes expose causal convolution and state; the trainable implicit filter and gated sequence model use Torch. The simple overlap-add reference uses direct convolution per block, so do not assign an FFT cost to that implementation. Full deployed Hyena model loading is an optional specialist owner, not implied by this study. [Inspected source](../drafts/hyena-long-convolution-models/convolution_mechanisms.py:11). |
| 31. [Ring Attention & Sequence Parallelism: one sequence, several devices](../drafts/ring-attention-sequence-parallelism/lesson.md) | **CPU arithmetic present; distributed implementation deliberately deferred** | Online softmax forward and blockwise gradient accumulation are explicit, with exact shard ownership, masks and uneven shards. Section11 clearly says one CPU owns all arrays and no distributed/FlashAttention execution occurred. A real process-group/backend route is missing if the outcome is distributed operation; preserve the current honest simulation scope and name a systems owner before claiming end-to-end production readiness. [Inspected source](../drafts/ring-attention-sequence-parallelism/ring-attention-reference.py:22). |
| 32. [Advanced Optimizers: Lion, Sophia, Prodigy and Schedule-Free](../drafts/advanced-optimizers-lion-sophia-prodigy-schedule-free/lesson.md) | **Confirmed specialist bridge gap** | Complete float64 single-array AdamW/Lion/Sophia/paper-Prodigy/Schedule-Free updates expose persistent state. Manuscript maps package differences and optimizer train/eval but no runnable specialist optimizer-class use is supplied. Add a compact actual training/evaluation/checkpoint bridge with declared algorithm versions; do not assert paper/package trajectory equality where variants differ. [Inspected source](../drafts/advanced-optimizers-lion-sophia-prodigy-schedule-free/optimizer_rules.py:10). |
| 33. [Neural ODEs: learn a rule for change, then follow it](../drafts/neural-ode-continuous-depth-models/lesson.md) | **Confirmed specialist bridge gap** | Differentiable Euler/classical-RK4 integration, continuous-depth/augmented models and full fit are supplied. torchdiffeq is explicitly documentation-only/unexecuted at lesson823. Add a small ordinary odeint route with solver/tolerance/time/gradient contract; named RK4 is insufficient because its stated package tableau differs from the local one. [Inspected source](../drafts/neural-ode-continuous-depth-models/neural_ode_study.py:54). |
| 34. [Hybrid SSM–Transformer Architectures: Jamba and Complementary Memory](../drafts/hybrid-ssm-transformer-architectures-jamba/lesson.md) | **Core mechanisms plus optional deployment code present** | Selective recurrent state and convolution history, causal attention KV cache, residual composition and full/stream paths are explicit. deployment_example.py provides actual Transformers model/tokenizer/revision/generation code with resource checks. It requires provisioned CUDA and is not executed by this audit; retain the published distinction between local evidence and optional full-model deployment. [Inspected source](../drafts/hybrid-ssm-transformer-architectures-jamba/stroke_models.py:59). |
| 35. [Titans: a memory that learns while a sequence arrives](../drafts/titans-multi-memory-architecture/lesson.md) | **Mechanism and ordinary differentiable workflow present** | Read/write network and autograd-derived persistent parameter/momentum updates are explicit; differentiable vs detached state paths are intentional. Rental replay defines causal keys, targets and time boundaries. No maintained universal Titans one-call API is assumed; tensor/autograd composition is the useful ordinary route, with the paper-level architecture limits retained. [Inspected source](../drafts/titans-multi-memory-architecture/neural_memory.py:6). |
| 36. [Mini-Batches, Training Loops & Gradient Accumulation](../drafts/mini-batches-training-loops-gradient-accumulation/lesson.md) | **Strong workflow mechanism/tool mapping present** | Explicit microbatch accumulation weights summed losses by actual effective-group size; optimizer steps once per group, including the short remainder. Manuscript connects parameters/gradient slots/momentum to the native optimizer and code. Reuse backprop/loss owners; no extra opaque training framework is necessary to satisfy ordinary tool use. [Inspected source](../drafts/mini-batches-training-loops-gradient-accumulation/train_iris.py:41). |
| 37. [Neural Training Diagnostics & Reproducible Experiments](../drafts/neural-training-diagnostics-reproducible-experiments/lesson.md) | **Strong workflow mechanism/tool mapping present** | Real controlled diagnosis separates loss, gradients and actual parameter changes; checkpoint_replay.py restores model/optimizer/scheduler/RNG/sample-position state through native state_dict interfaces. Reuse training/model primitives; the local outcome is reproducibility and failure localization, not reimplementing every layer. [Inspected source](../drafts/neural-training-diagnostics-reproducible-experiments/wine_diagnostics.py:44). |

## Confirmed additions to assess during finishing

Five destination notes record specific missing specialist bridges: [message passing](../topic-notes/message-passing-graph-convolutions-gcn-gat-graphsage.md), [graph transformers](../topic-notes/graph-transformers-geometric-deep-learning.md), [RBM](../topic-notes/boltzmann-machines-restricted-boltzmann-machines-rbm.md), [advanced optimizers](../topic-notes/advanced-optimizers-lion-sophia-prodigy-schedule-free.md), and [Neural ODE](../topic-notes/neural-ode-continuous-depth-models.md). Their additions should connect existing excellent mechanisms to actual tool use, not replace those mechanisms or add a second unrelated benchmark. They need current primary-source/version checks and meaningful native verification in the scoped finish/revision.

Several broad-family rows explicitly require deeper ownership or external-parity assessment. This is an **unchecked scope boundary**, not evidence of a defect in every named family. For example, the SSM lesson expressly excludes full S4 DPLR and accelerator scan kernels; Ring Attention expressly provides arithmetic simulation. A finish agent must retain those boundaries or implement the newly promised outcome, and must identify the named destination if broader engineering is taught later.

## High-quality scratch implementation review during finish

Preserve observed strengths: stable log-domain normalization, explicit empty-mask/state policy, immutable fitted state where applicable, matched state/weights for genuine parity, scoped shape checks, residual/cache/session identity and separation of exact arithmetic from model fitting. Then inspect the actual code contract for the topic: safe defaults on declared inputs, meaningful failure cases, state reset/continuation, trailing chunks, numerical branches, output-size costs and algorithmic work. Do not mistake array vectorization or a small reference loop for universal efficiency. Choose the best understandable algorithm for the promised scope and state when the maintained package is the practical route.

No new timing comparison is necessary merely to claim a correct mathematical operation. A performance claim needs a measured workload and implementation. Reuse already-taught primitives and link the actual section/program; verify that a currently planned prerequisite is not falsely described as already available. Add a changed-input/customization exercise that tests the mechanism rather than only copying API syntax.

## Incidental preparation cleanup identified, not silently closed

The current live-exploration header is correct, but deeper text still includes a stale instruction at `interleaved-cross-attention-architectures/lesson.md:193` to record expectations before running, and `mini-batches-training-loops-gradient-accumulation/lesson.md:252` says to predict before reading a result. During finishing, make these direct edit/inspect comparisons with immediate output; preserve independent calculation practice and mathematical model predictions. This breadth pass did not search every sentence for old prediction-first language and does not certify the rest of that migration.

## Evidence boundaries

No native training, package installation, GPU/distributed execution, browser render, production build or scientific full-packet review was performed for these 37 packets in this triage. API names come from inspected source and existing cited documentation; proposed additions are not represented as newly verified external behavior. The [source snapshot](../evidence/prepared-dl-implementation-depth-sources.json) binds the packet lesson/program bytes available during inspection; inclusion in the snapshot does not mean every helper was read. Existing author calculations remain their original evidence, separate from this report.
