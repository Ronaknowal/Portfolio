# Classical ML: scratch-to-library bridges

**Final integration, 22 September 2026:** independent review and production/browser checks are complete for this scope; the ledger records both phases complete. [The final report](REMEDIATION.md) closes the integration work reserved in the dated author checkpoint below.

22 September 2026. Author: root integration agent. Author/native checks complete; independent review and production integration pending. Existing real-data experiments and mathematical teaching are retained. This scope adds three runnable bridges and two short same-fixture convention comparisons.

| Topic | Repair and mechanism boundary | Learner source |
| --- | --- | --- |
| t-SNE, UMAP & Manifold Learning | Exact neighbors with row-wise distances/partial selection, local scales, sparse fuzzy graph, differentiated ideal pair objective and explicit sampled edge step. Same graph checked against supplied-neighbor UMAP and fitted estimator. Full package scheduler remains delegated, clearly distinguished from ideal pair descent. | `public/learn-assets/manifold-learning/umap-mechanism.py`, `src/learn/data/manifold-mechanism-program.js`, section 6 in its topic JSX |
| CRF | Keep existing custom inference/training. Fit CRFsuite, reconstruct numeric feature emissions/transitions and match marginals, Viterbi and whole-path probabilities. Text-dump precision and summed-versus-mean penalties are explicit. | `public/learn-assets/crf/crfsuite-bridge.py`, `src/learn/data/crf-mechanism-program.js`, subsection following custom training |
| Active Learning | Keep complete scratch controller and real sklearn experiment. Map acquired-label sentinel, original pool IDs, same fitted entropy, paid oracle call and final refit to scikit-activeml. Empty pool/budget prevents query and oracle access. | `public/learn-assets/active-learning/query-library-bridge.py`, `src/learn/data/active-mechanism-program.js`, subsection before section 8 |
| Linear & Logistic Regression | Append matched ordinary LogisticRegression comparison to the existing stable scratch loss/GD/BFGS program. Explicit C=1/(n lambda), intercept convention, gradient residual and changed-sample-count practice. | `src/learn/data/topics/linear-logistic-regression.jsx`; existing program unchanged |
| Gaussian Processes | Append same fixed-kernel, same-noise mean/covariance/log-evidence comparison to existing Cholesky code. Teach alpha versus WhiteKernel and latent versus observation covariance, with changed-noise practice. | `src/learn/data/topics/gaussian-processes-gp.jsx`; existing program unchanged |

Every addition explains its operations and API state, setup, numerical comparison and complexity where relevant. Changed-contract practice has a reasoned solution. No learner-prediction gate or generic replacement lab was added. Three complete program bodies are fetched only when opened; their download is their canonical source. Existing inline scratch programs remain their owners.

## Author evidence

`scripts/verify-classical-depth-bridges.py` writes [native evidence](../evidence/classical-depth-bridges-native.json) only after assertions. Run `manifold` with `scratch/manifold-learning-runtime/Scripts/python.exe -B`; run `crf` and `active` with `scratch/classical-depth-runtime/Scripts/python.exe -B`; run `conventions` with the unchanged shared `scratch/lesson-tools/Scripts/python.exe -B`. New optional dependencies live in the isolated classical runtime, not the shared numerical environment.

The 19 named groups cover deterministic neighbor ties/duplicates, sparse ranges/symmetry, missing support, full-pair finite differences, zero-rate/scalar sampled updates; CRF exhaustive path sums/probabilities, one-token boundary and changed transition; entropy zero/uniform values and guards that must never touch the model/oracle; three GP noise settings and WhiteKernel decomposition; logistic penalty and duplicated-row equivalence. Program mains also assert the actual package bridges. These are native author checks, not an independent review or performance benchmark.

Primary references inspected for this revision: [UMAP graph source](https://umap-learn.readthedocs.io/en/latest/_modules/umap/umap_.html), installed UMAP 0.5.12 layout source, [CRFsuite API](https://sklearn-crfsuite.readthedocs.io/en/latest/api.html), [python-crfsuite model/feature API](https://python-crfsuite.readthedocs.io/en/latest/pycrfsuite.html), [uncertainty sampler](https://scikit-activeml.github.io/latest/generated/api/skactiveml.pool.UncertaintySampling.html), [classifier adapter](https://scikit-activeml.github.io/latest/generated/api/skactiveml.classifier.SklearnClassifier.html), [logistic objective](https://scikit-learn.org/stable/modules/linear_model.html#logistic-regression), [GP alpha/prediction API](https://scikit-learn.org/stable/modules/generated/sklearn.gaussian_process.GaussianProcessRegressor.html). Installed versions are in the execution receipt. Documentation gives the contract; actual execution supplies the result.

## Review focus

Independently inspect the sampled UMAP rule versus the installed package, constrained local-scale boundary explanation, CRFsuite coefficient/rounded-state semantics, active-learning no-hidden-label invariant and inline GP/logistic code execution. Production checks must open the three lazy programs, match displayed/downloaded bytes, verify code scrolling and themed links at phone/desktop widths, and inspect the two inline additions. Old broad native experiments need not be rerun: their code is unchanged.
