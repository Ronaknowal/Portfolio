# Independent review of ten DSA implementation routes

22 September 2026. Reviewer: mathematics implementation author, separate from the DSA author. Scope: the ten topics and source files in `dsa-remediation.json`, their learner-visible bridge components, all twelve new runnable comparisons (eleven Python and one C++), the source extractor, relevant imported scratch mechanisms, and the surrounding body insertion points. This is not a new full audit of every older algorithm or a production browser report.

**Result:** no material numerical, algorithmic, ownership or package-contract defect found in the added routes. One stale learner-flow sentence in the existing String Matching body was reported for correction; closure is recorded below. Source review and complementary execution are distinct from the author's larger campaign and root's final production checks.

## Evidence

[`DSA-REMEDIATION-INDEPENDENT.json`](DSA-REMEDIATION-INDEPENDENT.json) records exact frozen source hashes, the author manifest hash and **20 passing complementary groups**. Reproduce with `scratch/lesson-tools/Scripts/python.exe -B scripts/review-dsa-depth-remediation.py`.

The independent checks compare every extracted definition/decorator AST with the displayed source owner, without using the author's extractor implementation. All ten generated mechanism modules match. Hash checks cover every source named by the author's per-topic manifest. The official AtCoder archive entries and extracted headers/license match the recorded pinned provenance hashes; this check binds the supplied archive, rather than claiming a second network retrieval of upstream history.

The compiled `scripts/fixtures/dsa-range-action-independent.cpp` includes the actual learner adapter. It verifies **1,215** affine composition/action/distribution/empty-summary cases and a lazy-tree update path with negative multipliers. This complements the author's signed set/add enumeration rather than rerunning its 14,000 cases.

Additional cases check mixed hashable graph labels and neighbor-order invariance; an unreachable negative cycle under source Bellman–Ford; Unicode code-point chunks crossing every symbol boundary; flow invariance under parallel-capacity splitting and a self-loop; actual persistent off-path node identity and old-root preservation; and external-sort fault behavior. Injected merge failure closes every opened run and removes the temporary workspace. An output that appears during sorting is protected by exclusive creation. Injected final-copy failure leaves the documented partial new output, cleans temporary runs and preserves the source.

No original 99-program campaign or historical experiment was unnecessarily repeated. The author's native receipt and stronger broad finite enumerations remain their own evidence, not relabeled independent results.

## Per-topic assessment

| Topic | Independent assessment and declared boundary |
| --- | --- |
| Trees/BST | The imported insertion/deletion/query owner is real. Bisect correctly implements ordered-array boundaries and set duplicates; its O(n) edits are explicit. Rebuilding or one rotation is not sold as a balancing guarantee. Reservation practice uses ceiling then the returned-root deletion contract. |
| Graph traversal | The reusable builder preserves declared isolates, direction and simple-edge semantics. Distances/path witnesses/component sets are appropriate comparisons; tied predecessor trees need not match. The section warns that materializing all paths can use quadratic output storage and offers length-only output. |
| Range structures | Official pinned AtCoder setup, language label, local-helper replay and half-open intervals are concrete. The affine lazy adapter uses newer-after-older composition and summary length, including zero-length padding. Fenwick's delta API and O(n log n) demonstration initialization are distinguished from the local O(n) build. C++ intermediate overflow and string-combine costs are explicit limits. |
| Weighted graphs | Dijkstra, Bellman–Ford, forest and topological order each have their own objective/witness check. Parallel edge occurrences and unreachable vertices survive the representation. Source-reachable negative-cycle exception versus local per-destination −∞ is correctly distinguished. graphlib's predecessor orientation does not masquerade as finite-worker scheduling. |
| String matching | The built-in adapter retains overlaps with start indices, without suffix allocations; empty pattern and code-point offsets match KMP. Streaming retains one matcher and does not join input. Repeated find is not assigned KMP's global comparison bound, and normalized offsets/chunk normalization limits are explained. |
| Randomized algorithms | Separate Random objects and tagged occurrence identities preserve the actual experiment. State replay is not confused with same-seed parity between algorithms. Integer tickets versus float choices, short-reservoir behavior and the two-stage subset proof are accurate. No finite frequency sample is called proof. |
| Flow and matching | Parallel capacities aggregate only under the stated capacity-only objective; antiparallel arcs remain distinct and loops carry no useful throughput. Bounds, conservation and cut equality certify value without demanding the same optimal witness. Tagged partitions/top_nodes and matching-dictionary double entries are handled correctly. Original-edge allocation loss is explicit. |
| Geometry | Small exact-integer corner sets form a bounded comparison with float64 Qhull; a cyclic start is irrelevant. Area/perimeter field meanings, all-collinear failure and pre-conversion loss near 2⁵³ are accurately distinguished. No jitter silently changes the problem or makes the library an exact-predicate oracle. |
| Persistence | The generated dataclass retains its frozen/identity decorators. Path-copy tree and immutables.Map genuinely branch from old versions. The latter is explicitly a hash-trie binding collection, not a range-aggregate index. Transient publication, shared mutable payloads and reference-dependent reclamation are correctly bounded. |
| External memory | The new sorter is a substantive file-output/resource-lifetime extension over the original counted small-pass model. It retains bounded merge streams, cleans consumed runs and carries singleton runs. SQLite uses binding, ordered queries, explicit transaction mode, rollback and close. File/page model, query-plan diagnostics and actual durability claims remain separate. |

The extractor intentionally owns no second hand-maintained algorithm. For current selected owners its imported definitions and decorators are faithful, and the comparison calls exercise the exposed mechanisms. This is a current-source result: adding future module-level constants or changing selected definitions still requires extraction and execution review; the present evidence does not certify an arbitrary Python source-extraction framework.

## Separate learning-experience checklist

1. **Purpose and first decision — pass.** Each addition starts from the workload or question that motivates the tool choice: ordered edits, graph direction, interval algebra, path objectives, stream search, sampling law, certificates, numerical geometry, persistent bindings or stored output.
2. **Prerequisite reuse — pass.** Complete helper downloads come from actual earlier displayed code, with explicit same-folder setup. Weighted/flow routes link the implemented graph construction section; SQLite links the existing SQL transaction/binding owner; the sorter reuses the earlier heap frontier primitive.
3. **Mechanism before API — pass.** Every inserted bridge follows the relevant scratch mechanism. The library does not replace the hand algorithm or obscure which part remains custom.
4. **Worked executable route — pass.** Programs are complete, topic-owned and version/setup-specific. The C++ route is clearly labeled and has the required pinned header archive; the Python replay remains runnable. Exact author output is retained, with complementary independent cases here.
5. **Representation and cognitive load — source pass.** Each addition uses focused prose, code and interpreted output alongside the preserved topic-specific visuals. It does not add unrelated charts or uniform decorative labs. The code remains initially closed and lazily fetched; actual rendered layout is root's browser responsibility.
6. **API mapping and honest comparison — pass.** Duplicates, units, direction, edge identity, tie choices, update composition, stochastic state and numerical domains are visible. Where representations answer different questions, the distinction is stated rather than hidden behind an equality assertion.
7. **Meaningful independent practice — pass.** Each new route changes a real constraint and provides separate hint/reasoning disclosures. Expected invariants and counterexamples make the tasks testable; they are not only “run this cell again.”
8. **Efficiency, stability and production boundary — pass.** Time/storage and nonconstant primitives are named. Streaming buffers, source/output ownership, numeric overflow and exact-vs-floating boundaries receive practical treatment. No unsupported universal “optimal” or benchmark claim was found.
9. **Learner operation and access — source pass, production pending.** Downloads, source loading/error recovery, copyable keyboard-scrollable code, language-aware labels and setup are available. Root owns actual desktop/390/320, light/dark, focus and network-failure browser verification; this review does not infer those outcomes from JSX.

## Finding and closure

**DSA-R1 — resolved: stale prediction-first instruction.** Immediately after the new String Matching bridge, the older §11 paragraph said “First predict the output, then compare it with the displayed result.” The author replaced it with a direct run/inspect-state/compare instruction, parsed the body, and refreshed the exact body/manifest bindings while preserving unchanged executable evidence. I inspected the corrected sentence and reran the 20 complementary review groups against the final manifest. Final source closure is bound by the independent JSON receipt. No prediction-entry control was introduced.

No material algorithmic repair is requested. Final delivery still requires root's integration result, and this report deliberately leaves that separate from the independent source/native pass.

**Source-only mobile amendment:** root's production inspection found long helper filenames overflowing 320-pixel pages in Sampling and Geometry. Root added `overflowWrap: 'anywhere'` only to those two download anchors. I verified that removing that exact attribute recovers each previously reviewed SHA-256 byte for byte, and both JSX components parse. The executable sources and native evidence are unchanged; they were not rerun for this wrapping-only repair. The final receipt records the amended component/author-manifest hashes. Verification of the corrected rendered widths remains with root's production browser pass.
