# Remediation link and integration-check review

22 September 2026. Bounded source and integration-check review of the 35 baseline topic IDs. The reviewer authored seven mathematics bridges earlier, so this link/setup pass is not their independent numerical review; a separate agent owns that review. The [inventory](remediation-links-review.json) lists each exact body, expected canonical program URLs and source hashes. No native fit, build or browser campaign was repeated for this pass. No lesson body, shared policy or ledger was changed by the link/checker review.

## Links and setup

Read the new setup and ownership sections across all 35 bodies and ten directly imported library bridge components. The literal scan found 67 internal href/to references, no unknown catalogue IDs and no missing literal fragment targets. The supported route is /learn/topic/:topicId (src/main.jsx:26); Reader resolves topicMap, and TopicContent scrolls an existing fragment after the lazy lesson becomes ready. Catalogue membership and publication were checked separately.

Matrix Calculus (matrix-calculus-jacobians.jsx:122) and Multivariate Calculus (multivariate-calculus-gradients.jsx:140) now correctly target backpropagation-automatic-differentiation, published as topics/backprop.jsx. Their previously incorrect backpropagation-computational-graphs destinations are absent from these sections. The graph-library-bridge, matrix-calculus-code-route, queueing-code-route and ito-calculus-code-route targets exist in their actual owners. SQL transactions, Feature Selection and Backpropagation owner links are published.

Four unpublished destinations are valid catalogue application/future-study links: three downstream Optimal Transport applications and Weighted Graphs' motion-planning link. They are not presented as current downloadable mechanism owners; the transport paragraph explicitly distinguishes their review status.

All 50 distinct local learn-assets references found in the scoped bodies, bridges and mechanism metadata exist. All ten mechanism-module imports in DSA comparisons have linked companion downloads and matching same-folder instructions. The optional C++ range route specifies a C++17 compiler, the matching -Iac-library-v1.6 root and a separate Python replay. Its zip contains all three required headers and license under the specified root. No setup filename or missing-companion defect was found.

Thirty topics own 31 new lazy program views: External Memory owns two. Five intentionally have no new program view: Multivariate reuses Matrix; Second Order and Stochastic Processes identify existing implementations; Linear/Logistic and GP extend inline programs. Their zero count must not substitute for an assertion that the intended new section rendered. Per-topic setup and expected program ownership are recorded in the inventory; this is not a new full algorithm-quality certification.

## Browser-verifier findings sent to integration owner

These describe the initial scripts/verify-depth-remediation-browser.cjs, whose inspected hash is retained in the inventory. Root owns repairs and final production checks. A later script needs its own source-bound result; this review does not certify it automatically.

| Finding | Initial location | Required closure |
| --- | --- | --- |
| A subset run preserves old topic passes while replacing the single global build hash, allowing mixed-build results to appear current. | Lines 16–18 and final overall status | Bind the build per row and invalidate retained rows when it changes, or require the same prior hash before reuse. |
| Program count comes only from the DOM. Missing additions, stale content or zero count can pass with an unrelated screenshot. | Lines 37–40, 90–97 | Assert exact source-derived program URLs/count and explicit section/inline-code markers for the five intentional zero-program pages. Require ready article state and topic identity. |
| The first pre can be the recorded-output region while source fetch is pending. This creates a false-failure race. | Lines 48–49, 74–75; MechanismProgram.jsx:29–32 | Wait for the Complete Python/C++ program region by accessible label before exact source comparison. |
| ArrowRight is issued without confirming focus or a scrollLeft change. CSS overflow:auto cannot prove keyboard scrolling. | Lines 76–82 | Assert focus and actual movement when content overflows; retain page-containment checks. |
| Rejecting only browser-default blue does not prove the intended gold theme. | Lines 80–82 | Compare with the relevant theme token or allowed state; bound contrast and visual claims to actual evidence. |

The h2 wait normally excludes the loading paragraph, but the load-error fallback also contains h2. The later lesson-load-error assertion does reject that fallback; moving it ahead of discovery improves the readiness contract. Preserve the useful existing page-error, KaTeX, containment, lazy-request and exact normalized source/download checks. One first-program screenshot at 320px per topic is not a visual review of every new section at all three widths.

## Coverage boundaries

This review checks newly added ownership/setup surfaces and their actual local targets. It does not revisit every historical link, externally fetch all resource URLs, replay native experiments, certify every algorithm, or claim final rendered correctness. The completed topic-specific reviews remain the numerical/teaching evidence; root owns final integration. A separately assigned optimizer-family repair later extended the same canonical download without changing its local links; those body/program bindings were refreshed after its author checks. Its independent closure remains in the mathematics review.
