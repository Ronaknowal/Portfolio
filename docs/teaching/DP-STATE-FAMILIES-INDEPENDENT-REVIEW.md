# Dynamic programming state families — independent review

Completed **11 September 2026, 10:08:47 UTC**, against the author's **10:04:23.333 UTC** amended freeze. **No unresolved material finding.** The [independent exact-source packet](evidence/dp-state-families-independent-review.json) binds all seven changed/new production sources, complementary native checks, changed browser checks and twelve images actually opened by this reviewer. Production integration and user acceptance remain separate.

## Scope and source preservation

This is an independent review of the bounded interval/tree/digit addition to `dynamic-programming-states-transitions-optimization`, not a new rewrite of the original lesson. The full [assessed extension design](DP-STATE-FAMILIES-DESIGN-ADDENDUM.md), added explanations/proofs/practice, all four new complete programs, pure models, investigations, scoped styles, individual brief and practice data were read.

The [starting source record](evidence/dp-state-families-independent-start.json) and exact seven-file copies under `archive/dp-state-families-independent-start/` preserve the preliminary read. The author's [initial 09:57 packet](evidence/dp-state-families-author-review-before-hints.json), [hint amendment](evidence/dp-state-families-hint-amendment.json) and [final packet](evidence/dp-state-families-author-review.json) remain distinct. Reversing only the five reviewed hint attributes/component names and removing their topic-owned wrapper reproduces the complete preliminary normalized body AST; the other six production hashes are unchanged.

Separately, the original [94-file baseline manifest](evidence/dp-state-families-original.json) retains the earlier lesson/program/practice/evidence history. This reviewer directly checked the four original model/example/lab/CSS hashes, all sixteen original program code/output identities and all twelve old practice objects. The inspected author preservation checker retains 144 original ordered normalized lesson elements, allowing the documented spacing-only repair of one old inline formula. The broader original algorithm regression is attributed to its author; this review freshly executes all sixteen original programs but does not claim to rerun that entire old numerical suite.

## Proof and teaching assessment

The matrix-chain state names a half-open interval of matrix positions and exports a fixed result shape. The replacement argument therefore justifies optimal children and the last split. The multiplication objective excludes wall time, memory and floating-point accuracy; scalar-operation and integer-bit costs are distinguished. Child-before-parent recovery produces an actual evaluable witness, and first-split ties do not imply a unique optimum.

The balloon transfer preserves fixed surviving boundaries by guessing the last removal. Its witness is a chronological postorder, with original identities retained through equal values and zeros. The signed-singleton task correctly distinguishes the supported nonnegative program from a hypothetical all-removed signed extension; initializing that extended maximum to zero would invent an unattainable answer.

The tree recurrence depends on the unique parent edge and absence of cross-child conflicts. It does not confuse graph independence with probability, include the external parent's reward or merge free/forced-off subtree values. Signed weights and an empty selection have correct bases. Reconstruction propagates the actual chosen parent condition, while the inspection cursor is honestly labelled as reading a completed table. A cycle or sibling conflict invalidates the decomposition rather than being repaired by a visited set.

Digit states preserve the equality-to-bound flag, start status and actual used digits. Padding zero and real zero differ, loose prefixes stay loose, and the cache belongs to one bound. Legal next digits partition the continuations without duplication. The all-padding endpoint is excluded from the positive-only count, inclusive ranges handle a=0 explicitly, and the adjacent-only variation changes both state sufficiency and the chosen zero convention. The larger native bound has a bounded recursion depth and exact integer counts; no enumeration of all values is hidden in its complexity claim.

The new local matrix/tree prerequisites are refreshed at the point of use. The three deeper branches are navigable and pauseable rather than presented as one short introductory read. The finite coverage does not promise universal interview mastery.

## Finding and resolved amendment

Five new changed reasoning tasks initially offered only their full explanation, although the assessed design promised an optional hint. The adjacent-digit changed task already supplied an appropriate separate hint. The reviewer reported the gap before closure.

The author added a topic-owned `StateFamilyCheckpoint`: visible task, optional hint, then separately hidden explanation. Original checkpoints, task answers and all numerical algorithms remained unchanged. The reviewer read all five hints, reversed the precise AST amendment to confirm its narrow scope, and independently operated every hint/answer by keyboard at all three widths. Opening a hint leaves the answer closed. Final 320px and desktop images show the actual revised reading flow. The issue is resolved; it was a scaffold correction, not a mathematical or numerical failure.

## Complementary native checks

`node scripts/verify-dp-state-families-independent.mjs` exports changed actual models and independently checks old support/program/practice conservation. `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-dp-state-families-independent.py` passed at **10:01:30 UTC**, with source hashes matching the final amended packet:

- All sixteen original and four new complete displayed programs execute and reproduce their exact stored stdout.
- Nineteen changed matrix chains are evaluated with actual exact integer matrices. Alternative products agree, and the actual expression's counted elementary multiplications equal the native/JS optimum.
- Fifteen balloon cases are compared with a search over changing live value sequences and original-ID witness replay; forced signed removal is checked separately.
- Three hundred twelve subtree/boundary queries are compared with an independently constructed **bipartite minimum-cut** calculation, rather than another parent-state recurrence. Eighteen larger native cases vary roots and parent conditions on 23-, 41- and 77-node trees.
- Six hundred seventy prefix states and every digit branch are compared with exact numeric prefix intervals in a directly enumerated valid-number list. Sixty-six large native bounds use an independent combinatorial rank formula; five changed inclusive ranges and the adjacent-versus-distinct answer sets are checked.

These checks complement the author's exhaustive small oracles. They neither replace the written proofs nor claim universal correctness over unspecified domains.

## Actual browser and visual review

`node scripts/review-dp-state-families-independent.cjs` passed at **10:04:42 UTC** in Edge with actual **Space Grotesk and JetBrains Mono** at **1440, 390 and 320 pixels**. Each width includes eighteen named changed states, three actual anchor arrivals, all five new hint/answer sequences, keyboard operations/reset, invalid drafts retaining previous valid state and the fifteen official practice links.

Changed cases include a candidate cost of 150 while the whole-chain optimum stays 66, one-matrix base cells, star-versus-chain weights with different feasible optima, the external-parent condition, empty trees, bound 1023 with padding then a real zero, backward prefix inspection, invalid bound syntax and the all-padding zero endpoint. There were no page errors or document overflow. The small diagrams retain readable native text; larger trees and tables use intentional local scrolling.

Twelve exact final reviewer captures were opened: ordinary interval/tree/digit reading, a 320px candidate expression tree, postorder operations, balloon neighbor/order snapshots, a changed star witness, chain conditional values, padding/real-zero states and the new hint flow. Their hashes are in the independent packet. Generating a screenshot alone was not counted as visual inspection.

## Official practice and research attribution

The reviewer freshly opened the public statements for [312 Burst Balloons](https://leetcode.com/problems/burst-balloons/), [337 House Robber III](https://leetcode.com/problems/house-robber-iii/), [2376 Count Special Integers](https://leetcode.com/problems/count-special-integers/) and [CSES Counting Numbers](https://cses.fi/problemset/task/2220/). IDs, titles, LeetCode difficulties and objective/range/zero conventions agree with the annotations. The existing twelve selections remain preserved at their earlier inspection dates; they were not all freshly re-researched in this pass.

The author's MIT/JHU selected notes, slides and transcript research is documented in the design and remains attributed to that author. This reviewer does not claim full audiovisual playback, judge submissions or editorial access. The links supplement self-contained proofs and practice.

The finalizer `scripts/finalize-dp-state-families-independent.mjs` checks final author/native/browser hashes, the narrow hint-only body transformation and the exact opened-image identities before writing the independent packet. Any later source change needs a new explicit amendment and relevant checks.
