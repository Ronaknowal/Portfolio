# Algebra, Functions, Exponentials & Logarithms — independent review

Closed against the author's **2026-09-10 23:48:28 UTC** amended freeze. Reviewer: `workflow_visual_improvements`, independently of author `testing_documentation_completion`. No remaining material finding was identified within this bounded review. Exact six production hashes, scripts, changed-case results and six actually opened image hashes are in [the durable evidence packet](evidence/algebra-functions-independent-review.json). Integration and learner acceptance remain parent/user decisions.

## Read and mathematical assessment

Read the complete actual body, design, stable-ID brief, all pure models, all nine complete Python programs, labs/CSS and twelve independent practice tasks. The teaching sequence genuinely introduces signed arithmetic, units and grouping before solving; it distinguishes reversible transformations from candidate-producing squaring and keeps original rational exclusions. Functions, range/codomain, composition order and inverse branches have sufficient local explanation. The positive-base real-power convention, odd-root qualification, descending logarithm base and continuous-rate conversion are explicit. A finite subdivision table motivates the stated limit without pretending to prove it.

The general quadratic derivation correctly handles a negative leading coefficient; the ± alternatives absorb the sign from the absolute value introduced by taking a square root. Exact thresholds are decided by integer comparison rather than rounded logarithms. The restricted inverse exercise requires a nonnegative output and checks the original composition. Real-world growth is presented as a declared model rather than evidence of a mechanism. There was no old published body/program to preserve.

## Material finding and resolution

The original `GrowthScaleLab` supplied only the two endpoint samples of `100+20t` to a renderer that transforms y coordinates before connecting them. On a logarithmic axis that segment implies `sqrt(100×260) ≈ 161.245154965` at t=4, while the true additive rule and its table give180. It incorrectly made both additive and exponential curves straight on the semilog plot, directly weakening the lesson's intended comparison.

This finding came from reading the actual original lab source and calculating its geometric implication; no pre-fix browser screenshot is claimed. Original lab hash: `fbbe251af2c1d82e7dd82d07c6320ed56830b98c128b07253c1ecd54ee4214e4`.

The author now evaluates the additive rule at81 time points before applying the axis transform. Only the lab changed, to hash `477594286e2ff402d95bf578322705ae0cd0b1b0b4d40c8ad3104342498a8c99`. The body, model, all program/output records, CSS and brief retain their reviewed hashes. The author preserved the first freeze and documented its focused1440/390/320 amendment in [author verification](ALGEBRA-FUNCTIONS-VERIFICATION.md).

Independent final Edge checks at390/320 inspect every actual polyline sample for growth rates0.2,−0.5 and1. All81 additive log samples follow `log((100+20t)/100)/log(260/100)`, all81 exponential log samples follow their straight-line exponent position, and all81 additive linear samples retain their correct linear positions. The plotted additive midpoint fraction is **0.6151539763976774**, rather than0.5; the visible period4 result remains180. Both final original-font graph captures were opened and inspected. This closes the actual geometry defect, separately from the arithmetic that was already correct.

## Complementary calculations and execution

Executed `node scripts/verify-algebra-functions-independent.mjs`, then `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-algebra-functions-independent.py`. Final independent native result: **2026-09-10 23:51:54 UTC**.

- All nine actual standalone program stdout values match their frozen expected outputs.
- Eight changed rational-coefficient calls check exact substitution and both zero-coefficient cases beyond the integer UI presets.
- Thirty-three composition states satisfy the independently expanded difference `S(A(x))−A(S(x))=2x(x+2)`, including its two equality points;42 domain/preimage states preserve restrictions.
- Forty-two growth states use off-preset target factors and70-digit Decimal references, covering past/future/constant equality and every table row. Worst relative repeated-growth error:7.94×10⁻¹⁶ or less. Eighteen changed logarithmic states match the high-precision inverse definition.
- Nine general quadratic cases are independently constructed from specified rational roots and signed leading coefficients, then checked against the prose formula.
- Thirty-six actual threshold calls use non-power-of-two initial sizes, offsets around exact thresholds and up to217-bit powers. The returned count is verified minimal by integer comparisons on both sides.

The final independent browser result is **2026-09-10 23:53:55 UTC**, from `scripts/review-algebra-functions-independent.cjs`. Public Space Grotesk is confirmed loaded; both widths have no document overflow or console/page errors. Two reviewer-created final graph images and four author-created images were actually opened: equation reading, restricted-inverse hint/solution, final fractional powers and the rational-function hole. The author's broader keyboard, anchor, code-output and full-width evidence is read and attributed, not claimed as independently rerun.

## Scope and evidence limits

Finite checks support these implementations and selected algebraic identities; they do not replace the complete source/proof read or prove continuum statements by enumeration. Numerical API guards are deliberately narrower than the mathematical domains explained in prose. Research annotations preserve the author's actual written-source scope and metadata-only video review; this independent pass does not claim new video playback or source research. No full integration build, deployment, learner study or universal mastery guarantee is implied.

The evidence also records review-harness repairs: a coordinate-ratio tolerance for subpixel lines, removal of an intentionally blocked development socket, and a capture-position assertion after page restoration moved one early screenshot. Those were harness issues, not additional content defects. Final source fingerprints and final images are checked again by `scripts/finalize-algebra-functions-independent.py`.
