# Single-variable calculus: independent review

The final six-source author freeze of 11 September 2026 is independently reviewed with no unresolved material finding. [The exact review packet](evidence/single-variable-calculus-independent-review.json) preserves the initial source identities, final fingerprints, actual execution and twelve visually opened images. Author verification and production integration are separate records. No production source was edited by this reviewer.

The review read the complete lesson, all thirteen changed practice solutions, fifteen actual executable programs, pure models, eight investigations, four inline figures, CSS, individual blueprint, design and incoming compounding note. It checked the hypotheses and derivations for limits and derivatives, inverse and chain rules, extrema and mean value theorem, signed accumulation and both FTC directions, integration operations, logarithm/exponential construction, Taylor remainders and series, improper integrals, the stated L’Hôpital case and applications. Proof reasoning was reviewed separately from finite numerical evidence.

Three concrete findings were reported to the author and repaired before closure. Adjacent endpoints could produce false derivative signs or extrema ties through midpoint/value rounding; exact decimal rational comparisons and analytical critical partitions now govern classification. The velocity checkpoint now claims nonnegative acceleration only where it exists, with a counterexample to strict positivity. The Taylor readout now distinguishes computed floating subtraction from analytic truncation and approximate bound evaluation; it does not inflate the theorem to conceal roundoff.

`node scripts/verify-single-variable-calculus-independent.mjs` passed on the final formatted model at 01:46:49 UTC. Separate exact-fraction and 90-digit oracles checked fifteen actual programs, 72 limit threshold states and 34 strict interior rational witnesses, 51 changed/adjacent/subnormal extrema intervals, 180 signed power-sum and absolute quadrature states, 110 Taylor integral remainders, 42 improper integrals after a logarithmic coordinate change, eighteen integrated growth rates and 26 changed actual native helper calls. The Taylor oracle integrates the exact remainder rather than comparing a rounded subtraction with a truncation-only bound. Large absolute errors and small cancellation residuals retain their explicit scale/tolerance context in the JSON.

The reviewer's actual-font browser subset passed at 1440, 390 and 320 pixels at 01:47:02 UTC. It exercised the repaired interval and Taylor states, strict limit/hole/jump cases, invalid-draft preservation, real keyboard application/reset/disclosure, the acceleration checkpoint and three signed rectangles whose SVG dimensions were independently calculated. Page errors, failed requests and document overflow were absent. The author's full eight-investigation and ordinary-reading checks remain separately attributed in [the author verification](SINGLE-VARIABLE-CALCULUS-VERIFICATION.md).

Eight reviewer screenshots and all four author inline figures at 320 pixels were actually opened. The motion diagram preserves signed displacement versus distance; the product diagram distinguishes edge terms and its second-order corner; the FTC example shows the exact triangular residual; and the density diagram places the center of mass on the same coordinate scale as the midpoint. Text and updated numerical labels remain readable. No claim is made that every generated screenshot, external video or entire referenced textbook was inspected. Final source matching and evidence hashing are reproducible with `scratch/lesson-tools/Scripts/python.exe scripts/finalize-single-variable-calculus-independent.py`.
