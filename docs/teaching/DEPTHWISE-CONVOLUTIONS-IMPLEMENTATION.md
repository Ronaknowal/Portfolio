# Depthwise Separable & Dilated Convolutions implementation

22 September 2026. Stable ID `depthwise-separable-dilated-convolutions`, Deep Learning position 11. Prepared revision 3 is consumed without changing its frozen manuscript, specifications, programs or evidence. The root integration owner controls current phase status and final completion.

## Teaching conservation and deliberate adjustments

The production body preserves all ten major sections, the forward/gradient/rank derivations, cost and receptive-field formulas, MobileNet/Xception/DeepLab distinctions, six measured training runs and 24 fixed factorizations, complete scratch and ordinary-library programs, seven changed-problem exercises plus the adaptive-energy task, hints/solutions and annotated references. It is generated statically from the full packet; there is no browser Markdown dependency. The exact prior convolution operator/pullback downloads and published lesson are now linked as published rather than described as still prepared.

The dated stencil specification retained a prediction-first sentence below its current superseding live-exploration policy. Implementation follows the current policy: every lab shows current outputs and updates immediately, with no learner prediction input, submission, correctness gate or optional guessing feature. Rank residuals describe the mathematical construction, not a pre-output assessment.

Six mechanism-specific investigations replace the author-directed visual placement paragraphs:

1. Two editable channel lanes trace local products and pointwise contributions; a disclosure exposes simultaneous parameter gradients and a proposed step, including zero-rate and overshoot cases.
2. Two independent probes show the rank-one restriction. Add a second zero-initialized pattern and construct arbitrary valid factors; dependent probes demonstrate the limitation of one-direction agreement.
3. A nine-cell signal and three tap products expose read addresses, padding, dilation and zero-weight/null cases.
4. Exact serial layers expose reachable offsets, 2D Cartesian support and holes. A separate finite 8×8 map counts actual data/padding taps. Bounded 2D support uses one SVG path rather than thousands of interactive DOM nodes.
5. Five parallel context branches expose shared inputs, distinct read addresses, concatenation and projection. Editing branch rates is an additional bounded generalization; reset returns the exact original fixture.
6. A deferred saved-model investigation performs actual JS inference for two retained seed-one models, four ranks and edited 8×8 pixels. Signed logits use a common scale; full numeric scores and signed kernel/residual tiles are available. Recorded all-seed outcomes remain distinct from live fixed-model inference.

Supporting figures show a common-scale weight budget, incoming field/jump units, the stride-two inverted-residual shape flow, a formula-derived hard-swish graph, and full-map ASPP branch shapes. The numeric/shape views wrap on phone layouts instead of compressing a single large diagram.

## Source ownership

- Body: `src/learn/data/topics/depthwise-separable-dilated-convolutions.jsx`.
- Components and styling: `src/learn/components/lesson-labs/DepthwiseConvolutionLabs.jsx`, `depthwise-convolutions.css`.
- Bounded pure mechanisms: `src/learn/data/depthwise-convolution-models.js`.
- Topic blueprint: `src/learn/data/curriculum/blueprints/depthwise-separable-dilated-convolutions.js`.
- Canonical downloads: `public/learn-code/depthwise-separable-dilated-convolutions/`. The programs, CSV, six-run output and author calculations are byte-conserved copies of the prepared sources; `digit-inference.json` extracts only the two editable model records.
- Author-time renderer: `scripts/generate-depthwise-convolution-lesson.mjs`, using the existing prepared-lesson renderer.
- Verifiers: `scripts/verify-depthwise-convolution-models.mjs` and `scripts/verify-depthwise-convolution-native.py`.

No shared publication/blueprint indexes, generated files or central ledger were edited by the author. The topic source already had a historical publication mapping; root should retain it and register the semantic authored blueprint as needed.

## Actual checks and evidence

`node scripts/verify-depthwise-convolution-models.mjs` passes seven named groups: explicit channel fixtures and numerical derivatives; rank probes including a noncanonical factorization; every supported stencil/finite-map position; serial support checked against independent path enumeration; parallel branch/null cases; complete saved-model inference versus independent native calculations; JSX/coverage/deferred ownership checks. The source-bound record is `docs/teaching/evidence/depthwise-convolution-author.json`.

`scratch/lesson-tools/Scripts/python.exe -X utf8 -B scripts/verify-depthwise-convolution-native.py` passes five groups under Python 3.12.14, NumPy 2.3.5, PyTorch 2.14.0+cpu and scikit-learn 1.9.1. It executes the entire direct-loop calculation program and reproduces its JSON exactly, runs the context blocks on real supplied inputs with gradients, reconstructs both saved models and checks all rank conversions against spectral discarded-energy and native grouped/effective-kernel calculations, and executes the full displayed float64 equality program. Record: `docs/teaching/evidence/depthwise-convolution-native.json`.

Maximum JS saved-original versus float32 logit discrepancy is 1.1513e−5 (specified limit 3e−5); JS factorized/edited scores match the independent double loop within 1e−10. Maximum native float32 grouped/effective-kernel difference is 4.7684e−6. No new training campaign was needed for a presentation-only implementation: the unchanged six recorded fits remain their original source-bound evidence, explicitly distinct from current inference/operator execution.

API and research refresh inspected PyTorch 2.9 Conv2d's group/dilation contract and the original MobileNet V2/DeepLab V3 source records on 22 September. The packet's more extensive primary-source reading and annotated D2L/Distill alternatives are retained; no video watching or new benchmark is claimed.

## Independent review and browser handoff

Author model/native verification is complete. Independent review and root production/browser integration remain pending at this author handoff. Do not call this document a completed integration report until the root record closes those stages.

Exercise all six `data-lab` regions: `depthwise-channels`, `depthwise-rank`, `depthwise-stencil`, `depthwise-coverage`, `depthwise-context`, `depthwise-digits`. Test numeric blank/invalid retention and actual range dragging/keyboard, contrast/null/reset, addition/removal controls, the rank second filter, the largest coverage lattice, finite-map dilation 8, quick pixel edits and model changes, score-only view changes, code/model load failures and retry, and leaving an in-flight disclosure. The model/data and source fetches use abort cleanup; only their explicit disclosure loads them.

Review desktop/390/320 figures, long downloads/code/table scrolling, KaTeX radicals/accents and supporting graph labels. Styles intentionally scope diagram geometry to `.depthwise-chart` and override the older shared lab green surfaces with neutral charcoal/amber locally. Shared final build, catalogue order/identity conservation and central checkpoint hashes belong to root integration.
