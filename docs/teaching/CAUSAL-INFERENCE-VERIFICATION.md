# Causal Inference & Do-Calculus — author verification

Stable identity: `causal-inference-do-calculus`, Mathematics 25. The [scoped design](CAUSAL-INFERENCE-LESSON-DESIGN.md) records the original weaknesses, prerequisites, concept progression, representation contracts, actual research scope and ownership decisions. The final timestamp and exact fingerprints are in the [durable author evidence](evidence/causal-inference-author-review.json). Author implementation and local verification are complete; independent cross-review, root production integration and user acceptance are separate.

## Content, preservation and teaching choices

The original full body, exact targeting program and original `0.22` / `0.1` output are preserved in [the original-content record](evidence/causal-inference-original-content.json). The final lesson retains that complete program, with an exact source comparison excluding only surrounding whitespace, and executes its original output. Its 200-user dataset is now explicitly distinguished from the simulator's population probabilities and from a causally justified population estimate.

Twelve navigable sections develop observing versus intervening, structural mechanisms and hidden inputs, path selection and d-separation, backdoor adjustment and potential-outcome assumptions, nonidentification, all three do-calculus rules including the `Z(W)` ancestor exclusion, a complete frontdoor derivation, same-unit counterfactuals, standardization/IPW/augmented estimation, sampling uncertainty, and instrumental-variable local effects. Random assignment, treatment receipt, population ATE and complier effects are kept distinct. Further designs and richer causal branches are introduced with their defining assumptions rather than presented as interchangeable estimators.

The seven representations serve different jobs: an inline mechanism-surgery comparison; a path/conditioning investigation; population-composition bars with fixed outcome mechanisms; an inline pair of observationally equivalent structural tables; a do-calculus graph/equality workbench; a frontdoor graph and staged mixture; and paired counterfactual response types with prior/posterior weights. Five are interactive. Graphs and bars show explicitly specified finite causal models, not invented measurements. Dashed hidden nodes and cut arrows, conditioned rings, composition versus conversion units, and unsupported observational quantities all have text explanations.

Ten complete standard-library Python programs have visible questions, actual output and interpretation; setup instructions precede the first program. Eleven substantial independent tasks have separate optional hints and explained solutions. They change populations, selection, graph paths, unobserved mechanisms, rule assumptions, mediator parameters, counterfactual evidence/coupling, nuisance values and compliance types, then finish with a design capstone. Counts describe this topic's teaching requirements, not a reusable quota.

## Actual native and mathematical verification

`scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-causal-inference.py` passed on **10 September 2026 at 17:20:23 UTC**, using Python 3.12.14. The complete JSON result is embedded in the durable record; transient programs and fixtures are under `scratch/causal-inference-verification`.

| Contract | Independent evidence |
| --- | --- |
| Path separation | 1,536 endpoint/conditioning cases across all 64 four-node DAGs consistent with a fixed topological order. The independent ancestor-moralization connectivity oracle agrees with the browser model's simple-path collider classification. This is a distinct algorithm, not an assertion that the function agrees with itself. |
| Three-rule graph operations | Six valid/invalid presets checked against the independent separation oracle. Rule 3 with downstream selection has an empty eligible action set and retains the relevant input arrow; incoming versus outgoing removal is checked. |
| Adjustment/support | 294 population/assignment configurations checked against exact Fraction weighted probabilities, including zero/one assignments, zero-size strata, no naturally treated/untreated cases, unavailable conditional risks and causal versus observed differences. |
| Frontdoor | 98 mediator/direct-effect states compared with an independently derived conditional-risk oracle. Checks include deterministic mediator cases, missing cross-treatment mediator support, constant mediators with a supported target, and a direct pathway that breaks the criterion. |
| Counterfactuals | 88 overlap/evidence/action states compared with exact response-type conditioning. Experimental marginals remain fixed; posterior masses sum to one; unchanged treatment reproduces each observed outcome; different couplings change conditional counterfactuals. |
| Augmented-estimator expectation | 48 configurations integrate treatment analytically conditional on strata, independently of the JS joint-outcome enumeration. Either correct nuisance route gives the target; deliberately wrong pairs need not. |
| Model contracts | Twelve invalid-input cases cover cyclic/duplicate graphs, unknown/conditioned endpoints, invalid rule, nonfinite/out-of-range probabilities, invalid coupling/treatment and zero estimated propensity. The numeric formatter retains a positive `1e-12`. |
| Actual programs and changed practice | All ten displayed learner programs execute in isolated Python and match actual stdout; the original program/output are preserved. Actual learner functions also run changed collider probabilities, downstream action/selection probabilities, frontdoor mediator parameters and nuisance values. Exact changed-population, hidden-mechanism, counterfactual, augmented-arm and instrument calculations support the worked solutions. The education/training adjustment exercise is independently checked by graph separation. |

Maximum absolute numerical comparison error was **2.78e−16**. The 200-user bootstrap is explicitly a seeded fixed-cell percentile illustration with finite sampling variability; neither a coverage theorem nor removal of identification bias is claimed. The design capstone and general causal theorems received source/derivation review, not a claim that numerical grids prove unrestricted identification theory.

## Actual browser and ordinary-reading verification

Final command: `node scripts/review-causal-inference.cjs` with `REQUIRE_WEB_FONTS=1`, using actual Edge through Playwright on the existing 5173 route at **1440×1000, 390×1000 and 320×1000**. The final browser timestamp, loaded font faces, every tested contract and all capture paths are embedded in the durable record.

- All twelve real route anchors arrive at their heading below the fixed header. The actual next topic remains Entropy, Cross-Entropy & KL Divergence.
- Keyboard conditioning works for forks, chains, colliders and conditioned descendants; reset restores the original graph and empty conditioning set. Graph edges remain causal inputs rather than being deleted by conditioning.
- Randomized assignment, reversed targeting, zero overlap, no treated cases, empty irrelevant strata and population reset give the expected displayed comparisons. Missing quantities are unavailable rather than silently extrapolated.
- Every rule preset checks its displayed candidate expression, cut-arrow count and valid/invalid message; rule 3's selected-descendant state retains the input arrow and states why. Reset restores rule 1.
- Frontdoor direct-path toggling changes the actual graph and separates the invalid formula from the model-known intervention; support failure and a supported constant-mediator case behave correctly. Keyboard reset works.
- Counterfactual coupling endpoints, all four observed-outcome consistency combinations, posterior rows and reset pass. Population experimental marginals remain unchanged.
- All ten visible questions, complete code blocks and displayed stdout match the topic data. All eleven tasks preserve a closed solution when only its hint opens; both disclosures are keyboard operable. Five annotated external learning resources retain the current lesson in a new tab.
- All fourteen display equations, seven representation containers and SVG node labels fit the narrow reading area; no page overflow, KaTeX error or lesson-render exception remains. Long code/tables use explicit local scrolling. Keyboard buttons have visible focus.
- The final pass captures normal unoperated reading before controls, all section introductions, every static figure and initial investigation, then meaningful changed states, program/question/output, exercises and sources. There are 101 captures across the three widths. The durable record separately identifies the actual screenshots opened and inspected, including both inline figures, all investigation types and changed states, repaired narrow equations, questions/output, solution and sources.

Initial browser review found a JSX set notation mistakenly evaluated as undefined variables; the exercise now names its valid adjustment sets plainly. Several long equations needed explained equivalent shorthand and logical line breaks. The rule selector needed shorter visible options on 320px. The workbench now shows its actual candidate equality and qualifies the result by the separation test. Its ambiguity table explicitly calls unseen cells model-specified mechanism risks instead of observed conditionals on zero-support events. A floating subtraction for the empty untreated group was replaced by direct group-mass accumulation before final native checks.

The initial strict console assertion also exposed two environmental messages: deliberately closed development HMR and denied public Google Fonts. The harness records only those exact known messages separately; it does not ignore arbitrary console errors. The final read-only browser pass was authorized with public font access and explicitly requires loaded Space Grotesk and JetBrains Mono faces. Final screenshots use the intended typography. No global CSS or font-loading policy was changed by this author.

Formatting command `node scripts/format-causal-inference.cjs` passed at **17:19:40 UTC** with normalized JavaScript AST/string and CSS-rule/value conservation. Source ownership remains per topic; no global eager registry, route logic, shared style, module order or shared ledger was edited.

## Research and remaining scope

The design records the exact inspected sections of Pearl's primary causal-inference paper, Neal's author text/slides and verified video-course index, Angrist–Imbens–Rubin's instrument paper, and PyWhy's residual-correction documentation. The Hernán/Robins author resource page was inspected and is a useful alternative; failed full-PDF retrieval is not reported as a completed book review. No full video playback or empirical paper replication is claimed.

There was no incoming Causal destination note. A scoped read of the related Bayesian Networks body found a contradictory adjustment exercise and an overbroad hidden-confounding claim; the [destination note](topic-notes/bayesian-networks-causal-graphical-models.md) preserves the specific graph, reasoning and requested reassessment. That other body remains untouched.

Finite controls illustrate declared mechanisms; they are not a general causal-discovery or ID engine. Full longitudinal treatment, transport, path-specific/counterfactual identification and semiparametric inference remain advanced branches. The lesson's broad core is self-contained under its stated probability prerequisites; it does not promise that one page, numerical tests or a finite practice set establishes universal causal mastery. Root owns production build/loading integration and final curriculum status, and learner acceptance remains pending.
