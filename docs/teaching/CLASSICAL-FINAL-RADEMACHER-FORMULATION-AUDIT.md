# Final completed-lesson audit: Rademacher and problem formulation

Reviewed 21 September 2026, sequentially in curriculum positions 36 and 37. Both topics pass this bounded content and implementation review after the repairs below. Production build, full route integration and central completion receipts belong to the coordinating review. This record does not claim those later steps.

The current authority was `AGENTS.md`, `LESSON-AUTHORING-HANDOFF.md`, `LESSON-TEACHING-STANDARD.md` and the code-example standard named by the handoff. The review compared the prepared scope and visual specifications with the live prose, model definitions, examples, figures and investigations; it also read the original designs and independent-review findings. The original packets and designs remain byte-identical to the incoming content checkpoints. This follow-up supplements them rather than replacing their history.

## Rademacher Complexity & Generalization Bounds

Prepared material: [manuscript](drafts/rademacher-complexity-generalization-bounds/lesson.md), [visual specifications](drafts/rademacher-complexity-generalization-bounds/visual-specifications.md), [original design and disposition](drafts/rademacher-complexity-generalization-bounds/design.md), [independent review](RADEMACHER-INDEPENDENT-REVIEW.md).

The live lesson covers the prepared sequence: sample restrictions and the order of supremum/expectation; signed versus absolute conventions; empirical versus expected complexity; the loss class and theorem probability; ghost-sample symmetrization; Euclidean and kernel geometry; alternative norm constraints and contraction; ramp margins and vacuity; simulation error; the bounded-norm experiment with separate representation, fitting, validation and assessment roles; convex mixtures; practice and limitations. Its four investigations and seventeen inline figures serve those arguments. The current manuscript's fixed-class assumptions, bounded-loss requirements, finite comparison accounting and dataset caveats remain warranted. No missing subject required a new lesson or a packet rewrite.

Closed findings:

1. **The absolute-value investigation mixed conventions.** Its headline could say .5 while the singleton's enumeration still contained signed negative values averaging to zero. The absolute model now exposes the same per-pattern correlations, winners, maxima and average-first diagnostic as the signed model. Every displayed quantity follows the selected convention; the closing singleton caption explicitly names the signed convention. Editable outputs such as 2 and 0 now retain those values in the prediction choices instead of being rendered as signs.
2. **Zero budget had the wrong uniqueness result.** With B = 0 the feasible ball contains only the origin, even when the signed sum is zero. The model now returns that unique optimizer, the investigation offers a distinct origin choice, and the equality feedback explains zero budget rather than falsely inferring equal signed-sum lengths. The model verifier's prior incorrect expectation was corrected.
3. **A preset could crash an edited margin sample.** Moving the first input had replaced the whole input vector with four entries while retaining three or five labels. It now edits only the first coordinate and preserves alignment.
4. **Scale comparisons used the shipping defaults.** The comparison now starts from the learner's first valid committed margin state, and subsequent comparisons advance from the state actually checked. An out-of-ball coefficient disables commitment instead of receiving a misleading vacuous or unchanged verdict.
5. **The manual zero candidate disclosed assessment early.** Its assessment count now remains hidden until assessment has been opened through the selection commitment.
6. **Two descriptions overstated what was shown.** The concluding prose now distinguishes exact small enumerations from seeded Monte Carlo estimates and corrections. Figure 11's accessible description now matches its actual margin range of −3 to 4 and displayed loss range of 0 to 4.05.

The theorem constants and binary-loss factor were checked against [Mohri's primary lecture notes](https://cs.nyu.edu/~mohri/mls/lecture_3.pdf). This was a targeted reference check, not a claim to independently prove every theorem.

## ML Problem Formulation, Baselines & Data Leakage

Prepared material: [manuscript](drafts/ml-problem-formulation-baselines-data-leakage/lesson.md), [visual specifications](drafts/ml-problem-formulation-baselines-data-leakage/visual-specifications.md), [original design and disposition](drafts/ml-problem-formulation-baselines-data-leakage/design.md), [independent review](PROBLEM-FORMULATION-INDEPENDENT-REVIEW.md).

The live lesson implements the prepared prediction contract, entity and time availability, as-known version selection, output/decision distinction, costs and capacity, majority and training-prior baselines, fitting boundaries, the bank experiment, leakage taxonomy, and propensity-versus-intervention and stocking counterexamples. Two investigations and seven figure groups connect those concepts to editable mechanisms. The recorded experiment continues to call the feature set proposed rather than proven available, disclose unknown client identities, separate reserved rows from validation scores, and distinguish observational subscription probability from causal effect. The source limitations are teaching content rather than footnotes that the interface contradicts.

Closed findings:

1. **A pending exchange could outlive its membership.** Selecting a drop/take pair and then changing capacity could leave an invalid pair eligible for application. Capacity edits and presets now retire that pair, as do the other ranking/membership changes. A new exchange must be selected from the actual current blocks.
2. **The “other sensor” null was hard-coded.** It always edited B, including when the query was about B. It now edits only entities other than the selected one, so the demonstrated null remains valid after an entity change.
3. **A translation preset escaped the editor's domain.** Adding 5 is disabled if any current value would exceed 100; it no longer creates a state the value editor itself rejects.
4. **Three local prose claims were inaccurate.** The taxonomy caption now describes five leakage mechanisms plus a deployment mismatch; calibration is an earlier lesson, not the immediately preceding lesson; and practice 1 asks the reader to apply the same eligibility/version steps to its changed records without claiming that cutoff controls alone reproduce those different values.

The distinction between an event-time backward join and a separate knowledge-time cutoff was checked against [pandas merge_asof](https://pandas.pydata.org/docs/reference/api/pandas.merge_asof.html). The version-sensitive Feast passage agrees with [the merged primary change](https://github.com/feast-dev/feast/pull/6617): its created-timestamp cutoff is opt-in and distinct from event-age eligibility. No Feast runtime installation or backend verification is claimed.

## Verification and visual review

Fresh read-only checks:

- `node scripts/build-curriculum-inventory.mjs --topic rademacher-complexity-generalization-bounds --work finish`: incoming topic was content-complete and implementation-complete, and eligible to finish. The equivalent formulation preflight ran after local edits and correctly reported the implementation checkpoint stale while content remained complete. The coordinator owns rebinding it.
- `node scripts/verify-rademacher-models.mjs --no-evidence`: passed 327 grouped assertions in 52 groups, including 1,005 kernel checks, 7,230 ramp checks, 410 movement checks, 16,001 loss checks, 1,205 geometry checks and 252 hull checks. The corrected zero-radius case is included.
- `node scripts/verify-formulation-models.mjs --no-evidence`: passed 138 grouped assertions in 60 groups, including 50,700 as-known checks, 2,197 as-known nulls, 1,800 geometry checks, 300 capacity checks, 58,675 exchanges, 300 ranking nulls and 144 cost checks.
- `scratch/lesson-tools/Scripts/python.exe scripts/verify-rademacher-sources.py --no-evidence`: passed 110 checks over 16 files after the final edits.
- The equivalent formulation source check passed 82 failable assertions and 58 scans over 16 files, with 11 sources parsed, after the final edits.
- `node scripts/verify-rademacher-formulation-audit.cjs`, with `LEARNING_BASE_URL=http://127.0.0.1:4184` and the bundled Playwright package: passed nine complementary browser cases, six selected captures, no page errors and no page-level overflow at 320 px. The test uses the shared development server; it neither builds nor starts a server.

The new browser cases independently inspect the absolute enumeration's eight nonnegative maxima and mean, edited integer labels, the degenerate origin, three/five-row presets, a nondefault committed scale null, infeasible commitment, hidden assessment, the sensor-B null, out-of-range translation, stale capacity exchanges, the measured 50-to-25 movement and a subsequent membership-preserving reversal. They supplement the existing campaigns rather than repeat every historical state.

The [fresh browser receipt](evidence/classical-final-audit-rademacher-formulation-browser.json) identifies its six captures. The absolute table and narrow verdict captures were visually inspected at their original pixel dimensions: signs/values agree, text wraps without clipping, and comparison explanations name the actual committed states. Selected retained figure captures were also inspected: Rademacher figures 13 and 15 at 390/320 px, and formulation figures 4 and 5 at 320 px. The Monte Carlo endpoint is visibly one-sided, bound stacks remain above the ceiling, and the formulation figures distinguish fitting boundaries, time availability and measured metrics. At narrow widths the dense drawings are supported by readable exact-value tables. These retained images are historical visual evidence for unchanged drawing paths, not fresh captures of all current source bytes. Figure 11's accessibility wording changed; its plotted coordinates did not.

The final [source-bound audit evidence](evidence/classical-final-audit-rademacher-formulation.json) contains all thirteen current lesson-owned source/asset hashes for each topic, checkpoint-bound packet hashes, changed-file maps, verifier hashes, screenshot hashes and explicit native/data reuse reconciliation. Existing native/data receipts were retained only after matching their manuscript, program, example-module, dataset, generated-data and result-file bytes. Their numerical/refit execution was **not rerun**. Existing browser receipts are historical support rather than blanket receipts for the edited lab code; the new regression and coordinating integration cover the changed behavior. No prior receipt was overwritten.

Limits: this browser work used one Chromium-family engine (Edge) and selected desktop/320 px changed states. It is not assistive-technology testing, a fresh native refit campaign or the final production integration. No source-mutating falsification harness, build, server lifecycle change, shared ledger mutation or unrelated lesson edit was performed by this reviewer. No specific unresolved content or implementation blocker remains in these two topics.
