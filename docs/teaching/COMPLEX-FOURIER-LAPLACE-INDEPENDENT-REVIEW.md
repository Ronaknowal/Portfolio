# Complex Numbers, Fourier & Laplace Transforms — independent review

The six production sources match the author's amended **11 September 2026, 07:06:43.823923+00:00** freeze. The [independent packet](evidence/complex-transforms-independent-review.json) binds their exact hashes, this review's checks and the separate original **06:30:31 UTC** author record. All six archived original files were also verified against the original packet; nothing was silently replaced. No unresolved material finding remains. The reviewer did not edit production source. Parent production integration and user acceptance remain separate.

## Inspected scope and mathematical reasoning

The reviewer read the entire lesson, its seventeen actual complete programs, pure models, complete lab source and CSS, individual brief, assessed design and author verification. The original baseline is a planned outline with no legacy authored programs, as preserved in the author's original-plan record. The review considered all fourteen changed practice questions and their explained solutions, rather than inferring their correctness from a test count.

The local proof and convention review covered complex multiplication and branches; radians versus hertz; real-signal conjugate coefficients; finite orthogonal projection and its square-integrability condition; the stated piecewise-smooth midpoint theorem and paired Dirichlet-kernel argument; pointwise, mean-square and moving-neighborhood distinctions; the Gibbs jump fraction; ordinary continuous inversion versus the L² extension; finite DFT inversion, Parseval and radix-two reuse; alias phase and the bandlimited sampling hypothesis; periodic windows, padding and no-detrending power normalization; circular versus linear convolution; steady response versus initial-condition correction; support-dependent Laplace regions; ordinary 0+ derivatives versus generalized 0− impulse bookkeeping; and rational final-value/stability conditions. No unresolved material mathematical or practice defect was found after the corrections below.

Primary passages revisited independently were [Lebl's piecewise-smooth convergence theorem](https://www.jirka.org/diffyqs/html/moreonfourier_section.html), [NIST's Gibbs formulas and constant](https://dlmf.nist.gov/6.16), and [SciPy's periodogram detrending, one-sided and scaling contracts](https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.periodogram.html). These support specific conditions and conventions; the numerical evidence below comes from separately formulated checks of actual source. This reviewer did not watch the linked videos or claim an independent audit of every external reference.

## Findings and author repairs

The [initial observed record](../../scratch/complex-transforms-independent-review/initial-findings.json), saved at **06:50:49 UTC**, and [original actual example payload](../../scratch/complex-transforms-independent-review/initial-native-payload.json) preserve the pre-repair evidence.

1. **Native removable-limit cancellation.** The displayed `truncated` helper returned `0j` at `decay=0, sigma=1e-18, omega=0, horizon=1`, although the 90-digit independent result is approximately `0.9999999999999999995`. The author replaced the small-argument subtraction with a bounded series, retained the exact zero case and all seventeen saved stdout strings, and documented its arithmetic scope. The final independent run checks eighteen real/complex argument and horizon combinations against `mpmath.expm1` at ninety digits. The existing browser integral model was already stable and remains unchanged.
2. **Least-squares hypothesis.** The general projection proof followed a coefficient discussion permitting integrable signals but did not explicitly require a finite squared norm. The author now states that `x` is square-integrable immediately before the norm expansion. The browser check verifies the actual paragraph, and a changed nontrigonometric periodic example separately checks the coefficient and finite Pythagorean identity.
3. **Reconstructed DFT plot range.** The editable sample list `[4,-4,4,4,4,4,4,-4]`, with the bin-1 conjugate pair removed, produces a reconstructed first value `4+2√2 ≈ 6.828427`, beyond the former fixed vertical range. The corrected range includes the actual input and reconstruction. The independent native result agrees with NumPy's inverse transform, and all sixteen actual SVG points remain inside the plot in each browser width.
4. **Transient plot range.** Decay rate `20`, third-tone phase `π/4` and initial output `−3` produce a transient correction about `−5.547748`, outside the former fixed range. The corrected plot derives its extent from all three actual series. The final browser examines all 1,539 rendered series points at each width, with no escaped point. A separate augmented-matrix exponential oracle verifies the response rather than repeating the lesson's gain/lag formula.

The fixes belong to the author; this review retains the original failures and the repaired checks separately. Bounded helper checks do not certify every possible binary64 input or a general-purpose transform library.

## Complementary executable evidence

Run `node scripts/verify-complex-transforms-independent.mjs`, which imports the real production examples and models, then invokes [the reviewer Python oracle](../../scripts/verify-complex-transforms-independent.py). The [final numerical result](../../scratch/complex-transforms-independent-review/results.json) passed at **07:00:39 UTC**, with seventeen exact-output programs and **680 complementary numerical comparisons**:

- Eighteen model and eighteen actual native finite integrals against ninety-digit removable-limit calculations.
- Twenty-four changed filter configurations at five times each, using a five-dimensional constant-coefficient ODE matrix exponential, plus independent checks of the visible steady/transient sum.
- Eighteen changed record/window/frequency configurations. Complex finite spectra are evaluated as polynomials; periodic Hann weights use an independent sine-squared form. Ninety density scalings and both weighted-energy identities are checked separately.
- Four first-peak values from integration of the Dirichlet derivative kernel, including a changed jump from levels 2 to 5. The asymptotic excess is approximately `0.0894898722360836` of the jump.
- Eleven coefficients of the unrelated periodic example `exp(t)` on `[-1/2,1/2]`, verified with quadrature, and three finite projection-energy identities.
- Twenty-eight bins each from the actual direct DFT and FFT helpers on changed known DC/cosine/sine mixtures; eight extreme filtered inverse entries and the exact `4+2√2` peak.
- An actual changed capstone, with `N=48`, sampling rate 96 Hz, decay rate 9, initial output 1.25, and changed 3 Hz/6 Hz modes. It reports output `−0.573022` at 0.25 seconds, independently checked by the augmented ODE.

The largest scaled discrepancy was **1.16×10⁻¹³**. The comparison denominator is `max(1, |reference|)`; this is not a relative-accuracy guarantee near zero. These finite checks complement the author's larger model sweep. General theorem validity was assessed by reading the stated hypotheses and arguments, not by extrapolating from these examples.

## Actual browser and opened images

Run `node scripts/review-complex-transforms-independent.cjs`. The [reviewer browser result](../../scratch/complex-transforms-independent-review/browser-results.json) uses the actual registered route and loaded Space Grotesk font at **1440, 390 and 320 pixels**. All seventeen displayed questions, complete code strings and expected outputs match the executed examples. It operates Enter to apply edited samples, Space to toggle filtering, changed bin/rate/phase/initial-state controls, the Laplace boundary and left-sided ROC, and the 64-term moving-peak view. All thirteen display equations fit, the document has no horizontal overflow, and page errors and React console warnings/errors are empty. The six source hashes are unchanged during the run.

Eleven named screenshots were actually opened: seven captured by this reviewer and four captured by the author. [The opened-image list](../../scratch/complex-transforms-independent-review/opened-images.json) records their exact hashes and separate attribution. These include the repaired phone DFT and transient, the desktop transient, ordinary projection proof, actual stable native program, left-sided ROC, moving Gibbs neighborhood, complex-plane geometry, padded Hann record, FFT butterfly and final changed practice. The repaired extrema are visible; the phone proof equations fit; wide code retains the existing local horizontal scrolling. Capture alone is not counted as visual inspection.

This is a finite independent teaching and implementation review. It does not claim a beginner user study, full-video viewing, user approval, or production build/loading integration. The parent owns the integration decision.
