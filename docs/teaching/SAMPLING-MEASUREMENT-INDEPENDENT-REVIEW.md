# Sampling and measurement: independent review

Mathematics49 is independently reviewed on 11 September 2026. [The final packet](evidence/sampling-measurement-independent-review.json) binds six production sources and preserves the [original author freeze](evidence/sampling-measurement-author-review.json). Only the pure model required an amendment. Root production integration and user acceptance remain separate.

The reviewer read the complete body, eleven changed practice answers, twelve actual programs, full model and visual source, all five inline figures, CSS, individual brief, design and incoming Random Variables note. The review checked the SRS indicator derivation and variance convention; coverage bias and MSE; first-order versus joint inclusion; fixed-N HT and repeated-draw HH; strata, systematic samples and cluster units; shared covariance and the balanced design-effect assumptions; finite randomization and Neyman variance; block weighting and a harmful pairing counterexample; factorial identification; and bounded missing assigned outcomes versus causal counterfactual uncertainty. The concrete student-weighted capstone correctly requires equal known class sizes within each randomized pair. NIST's uncertainty-component distinction was rechecked in its primary source.

## Model amendments and exact checks

Sparse arrays could silently drop distribution values, and sparse weights could yield NaN. Values and probabilities now require dense own indices. Missing observations use explicit null, and inherited preset names reject explicitly. Accepted probability sums within rounding tolerance are normalized before arithmetic. A constant law near a large baseline now has exactly zero variance. Residuals remain in centered coordinates, so rounding the displayed mean does not double the variance of two adjacent values. Nonzero variance contributions and squared coverage bias that underflow now reject instead of appearing as exact zeros. Original six-file copies and all original author evidence remain intact.

`node scripts/verify-sampling-measurement-independent.mjs` passed on the final amended source. It replays **4,546 author states**, comparing **305,048 numeric fields** with a maximum change of **7.11e-15**. That is regression evidence, not a second independent proof. Separate Fraction calculations cover **68 moment laws**, **69 finite-sample designs**, **75 unequal-inclusion cases**, **102 assignments**, **nine full covariance-matrix sums**, and **four missing-value corner enumerations**. The constant and adjacent-large-baseline variance checks require relative agreement or exact zero, rather than an absolute tolerance that would hide the error. **230 invalid cases** reject.

All twelve actual programs reproduce their stored output. **41 changed native checks** include different finite populations, heterogeneous assignments, and execution of the actual protocol with seed50 and one unperturbed reading per class. The changed run assigns C2/C4/C6, gives contrasts5/5/5 and estimate5; the stated student weights also give5. This does not change the synthetic underlying effect3.

## Rendered teaching and interaction

`node scripts/review-sampling-measurement-independent.cjs` passed at **1440,390,320 pixels** with actual loaded fonts. Every width checks all five investigations on changed states, five inline figures, twelve exact rendered program/question/output triples, nine fitting equations, ten valid anchors and nine real keyboard actions. It independently verifies sample-law bar masses and variance-contribution widths, observed versus disclosed potential outcomes, a negative factorial interaction, the concrete weighted protocol, and final model repairs through actual browser imports. There are no page errors, failed requests, document overflow or SVG text-bound failures in this pass.

An early harness check incorrectly required the fully revealed numerical table to overflow merely because the viewport was narrow. The corrected check measures actual overflow on the longer observed table and waits for real keyboard scrolling. This repair changes verification logic, not production behavior.

Eleven final screenshots were actually opened: all five changed investigations, all five inline representations, and the complete weighted-protocol solution. Selection/frame omissions, inverse-inclusion contributions, shared reading groups, assignment observability, factorial cells and missing-outcome bounds remain readable with their mathematical meaning intact. Tall panel captures preserve the tested width and record their extended capture height. Generated images that were not opened are not claimed as visually reviewed.

No unresolved material finding remains in this bounded review. The lesson uses exact finite teaching populations and declared analytic models, not real experiment data. The checks do not certify arbitrary machine-precision arithmetic, actual educational effectiveness or user approval.
