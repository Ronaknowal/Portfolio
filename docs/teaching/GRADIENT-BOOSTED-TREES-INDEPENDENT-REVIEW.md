# Gradient Boosted Trees — bounded independent review

Closed 11 September 2026 against the author's unchanged **12:10:28.707Z** six-source freeze. Exact source hashes, calculations, operated states and opened-image hashes are in [the independent packet](evidence/gradient-boosted-trees-independent-review.json). No production file or original author evidence was changed. No actionable material mathematical, teaching or visual defect remained in this reviewed scope; integration remains parent-owned.

Read the complete current lesson, proofs, changed practice, all fifteen actual program sources, pure models, chart/lab contracts, CSS, design and original-retention record. The mean-residual projection, shrinkage hypotheses, nonlinear logistic surrogate, L1/L2/leaf-count penalties, missing routes, GOSS expectation caveat, coordinated prefix information boundaries and current best-iteration conventions are consistent. The training/validation fixture is labeled as a construction; package results are not claimed as an equal-budget ranking. The advanced report is appropriately open-ended with explicit acceptance criteria.

Complementary execution used a different risk focus from the author suite: six finite-population variance identities for GOSS; nine affine-transformed repeated-feature fits and changed queries; four exact candidate-minimum checks of the actual Python regularized leaf helper; five changed prefix target-influence checks; three actual recursive projection/refit cases; and three current XGBoost multiclass round-versus-tree slice checks. All passed. Scripts: `verify-gradient-boosted-trees-independent.mjs` and `.py`; results are embedded in the packet. The author's already-passing fifteen-program stdout suite was reused, not regenerated.

The independent Edge review operated seven scoped cases at each of 1440, 390 and 320 pixels: constant targets, invalid draft preservation, reversed prefix order, changed prior strength, keyboard own-target flip, changed depth and validation-selected prefix. It checked the unchanged prefix statistic, actual step geometry, anchors, equation widths, fonts and absence of document overflow/page errors. All passed. Three new captures and three selected author captures were actually opened; the latter retain author attribution. The phone residual view connects every correction to its leaf; the narrow regularization derivation fits, and the changed permutation visibly distinguishes earlier eligible rows from the selected and later rows. Dense code/tables retain intentional local scrolling.

The current [XGBoost prediction documentation](https://xgboost.readthedocs.io/en/stable/prediction.html) independently supports the round, group and native-versus-wrapper contracts. The [original LightGBM paper](https://papers.nips.cc/paper_files/paper/2017/file/6449f44a102fde848669bdd9eb6b76fa-Paper.pdf) was checked for the sampling mechanism; the complementary variance calculation is derived independently. No video-viewing, GPU execution, universal numerical safety or learner-efficacy claim is added. Only useful final review JSON and the three opened captures were created in the review's scratch directory; no obsolete working copies need cleanup.
