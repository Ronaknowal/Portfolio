# Category Theory — author verification

10 September 2026 UTC /11 September local; mathematics position 41, stable ID `category-theory-emerging-use-in-ml`. The [design](CATEGORY-THEORY-LESSON-DESIGN.md) records scope, prerequisites, research and representation contracts. The [source-versioned author packet](evidence/category-theory-author-review.json) preserves actual checks. Independent review, production integration and user acceptance are separate.

## What changed and why

The previous lesson introduced useful vocabulary and an arithmetic composition program, but did not develop enough concrete constructions to justify its abstract claims. The replacement retains the title, stable identity and original complete program, then builds typed composition, category laws, schema functors, natural transformations, universal properties, monoidal/effect composition, stochastic maps, differentiation, adjunction and Yoneda from actual objects and maps. Twelve sections distinguish a first-pass route from explicitly deeper branches. Nothing is claimed to model every ML architecture.

Seven investigations expose different mechanisms: a highlighted typed path; inconsistent direct/composed database lookups; both routes around a naturality square; missing and duplicate universal-property witnesses; correlated versus independent joint distributions; retained primal points with forward tangents and backward cotangents; and linked image/preimage memberships. Three inline figures connect quotient homology to a commuting chain square, show separate parallel wires, and trace successful/missing Option values. These counts describe this lesson, not a design quota. All figures identify their entities and explain what their arrows mean.

The preceding TDA lesson's destination note is adapted and resolved: section 3 proves the chain-to-quotient induced-map construction and identity/composition preservation, while the edge-cycle-to-filled-triangle example demonstrates information loss. The arithmetic program is conserved as an executed code string, allowing only its terminal newline. The original source SHA256 is `3497266dbada7cf4a7a88bf2a0ecfe1acb1b2ff4a4cc0790474040ca4f8f63bf`; its actual output remains `7 7` and `5 5`. The original archive is retained.

Eleven complete standard-library Python programs have prediction questions, captured stdout and interpretations. Twelve independent practice groups plus two earlier checkpoints supply hints and reasoned solutions, including changed maps, missing universal witnesses, a prior-dependent reversal, adjunction intersection failure and migration joins. The capstone shows why merging site identities can create compatible pairs with no pre-migration antecedent. The exact next module topic remains Differential Geometry.

## Mathematical and native evidence

Run from the repository:

```text
node scripts/verify-category-theory-models.mjs
scratch/lesson-tools/Scripts/python.exe scripts/verify-category-theory-native.py
```

The final native result records 14,386 model comparisons: 192 finite compositions, 968 naturality cases, 12 product candidates, 101 analytic probability states, 2,745 polynomial tangent/cotangent states and 10,368 image/preimage containments. Independent constructions include exact polynomial coefficients and rational arithmetic, elementary sample-space joint distributions, and membership-relation calculations. Fifteen invalid-input contracts and empty-function cases are checked separately by the JavaScript exporter. These are bounded implementation checks; the body separately proves general laws.

All eleven actual displayed programs ran under Python 3.12.14. Their actual helpers were exercised on 8 changed schemas, 448 Option combinations, 113 finite F2 chain complexes and 625 rational channel cases. The homology check uses quotient cosets and an independent XOR-rank construction; its relabeling test applies the induced map on both sides of the boundary equation. It does not substitute a tautological self-comparison. The stochastic example retains row-matrix versus categorical-composition order, explicitly rejects conditioning on zero evidence, and demonstrates that Bayesian reversal is not an inverse channel. The derivative example computes the second derivative factor at the saved intermediate point and accumulates the repeated parameter's two contributions.

The models perform only bounded, small calculations in the browser. Exhaustive enumeration and native oracles run in verification scripts. There is no new large runtime library, empirical benchmark, random simulation, dynamic code evaluation or eager shared lesson registry. Production loading validation belongs to the separate integration record.

## Actual browser and visual review

```text
node scripts/review-category-theory-lesson.cjs
node scripts/review-category-theory-keyboard.cjs
```

The final full Edge 152.0.4191.66 pass checked 272 states at each of 1440,390 and320 CSS pixels with the site's actual Space Grotesk fonts. It verified all seven investigations, intermediate values and linked representations, repairs, counterexamples, resets, all eleven displayed programs and outputs, all fourteen checkpoints/practice groups, twelve section anchors, 35 focusable lab controls, native range/checkbox keys, ten displayed equations, readable SVG labels and zero browser or KaTeX errors. A focused follow-up exercised native select keyboard navigation and reset activation with Enter at all three widths, and captured the preserved first program in ordinary reading.

Actual image inspection covers the seventeen final images named and hashed in the author packet. They include every default investigation, every inline figure, desktop composition/naturality, mobile introduction, probability derivation, Yoneda derivation, practice and a complete original program/output view. Other generated screenshots are not counted as opened. The large relation arrays are tiny by construction; a long native code line can pan within its code block without widening the document.

Earlier author passes exposed readable-layout issues at390 and320: pullback notation, stochastic normalization, Option laws, tangent composition and Yoneda derivations exceeded available width. These were split into explicit mathematical steps with their domains/notation retained in adjacent prose. The final ten equation boxes fit without shrinking mathematical text or hiding content. Phone derivative arrows now follow their vertically stacked lanes. A stale development overlay after the initial source replacement was cleared by restarting the root-owned server; the final evidence is from the clean server and complete parseable source.

The source formatter records normalized Babel/PostCSS AST conservation and zero esbuild warnings for this topic's production files. It does not claim that formatting validates the mathematics.

## Source research and limits

The design's annotated research ledger distinguishes the selected passages actually read from entire books or papers downloaded. It includes Riehl's author-hosted second-edition definitions, Yoneda and universal-property/adjunction passages; Seven Sketches chapter 3; Fritz v8 finite stochastic conventions; and Backprop as Functor v3 theorem hypotheses and quadratic-loss example. The position paper Categorical Deep Learning was reviewed at abstract/introductory scope only and is labelled accordingly. Exact downloaded PDF hashes are preserved in the author packet.

The references include written primary sources, MIT's official database/category lecture chapter and a verified Topos Institute YouTube introduction. Official pages and video identities were inspected; no full-video viewing or invented timestamps are claimed. The formal backpropagation theorem is not represented as a consequence of the elementary tangent lab alone. General measure-theoretic conditional probability, higher/enriched categories, Kan extensions and full categorical architecture design remain scoped continuations.

This is an author heuristic and technical review, not an observed beginner study. Independent mathematical review, integrated production loading and user acceptance remain distinct statuses.

## Independent-review text amendment

The independent reviewer found a space inside the greater-than HTML entity in the Bayes paragraph. The intended positive-evidence condition was therefore displayed as literal entity text. It is now correctly rendered as `q(y)> 0`. Only this one body substring and its CRLF-to-LF line ending changed; reversing both reconstructs the exact prior body hash. The other five production files are byte-identical. The original frozen packet remains at `evidence/category-theory-author-review-before-entity-fix.json`.

`node scripts/review-category-theory-conditional-text.cjs` checks the actual corrected paragraph, page errors and document width at1440/390/320 using the site fonts. All pass; the final320 paragraph screenshot was actually opened. The original full interaction/model/native evidence retains its original source identity and is not described as rerun after this text-only amendment. The amended author packet records both versions and the focused final check.
