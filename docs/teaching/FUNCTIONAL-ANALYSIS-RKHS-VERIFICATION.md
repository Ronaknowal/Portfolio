# Functional Analysis & RKHS — author verification

Mathematics position 39, stable ID `functional-analysis-rkhs`. Completed 11 September 2026 local / 10 September UTC under the approved [individual design](FUNCTIONAL-ANALYSIS-RKHS-LESSON-DESIGN.md). [The durable author record](evidence/functional-analysis-author-review.json) contains exact source hashes, runtime results and the actual opened screenshot list. Parent owns shared production integration and rollout status; publication alone is not that integration or user approval.

## Coverage and conservation

The replacement retains the original title/route, useful norms/Hilbert/PSD/RBF/representer/ridge/scale/capstone coverage, and the exact decoded original Python program and output. [Original source](evidence/functional-analysis-rkhs-original.jsx) and [preservation/execution evidence](evidence/functional-analysis-rkhs-original-content.json) are durable. The original three output lines remain `0.368`, `-0.342 1.024`, `0.531`; only subprocess newline comparison normalizes CRLF. No other topic body or shared manifest was edited.

The complete learning route now derives completeness, bounded measurement, closed projection and Riesz before building the anchored min-kernel space. It distinguishes L² equivalence classes from unbounded evaluation, PSD from informative/characteristic kernels, existence from the representer form, unique functions from redundant coefficients, sum from average loss, empirical validation from theorems, integration bounds from confidence intervals, and RKHS geometry from GP sample paths. Longer completeness/approximation/operator branches follow local prerequisites. Seven different investigations and two inline figures were chosen for these hurdles, not as a template or quota. Eleven complete programs and nine changed practice sets have visible questions, hints and explained solutions. The next topic remains Topology.

The incoming mean-embedding note is [resolved](topic-notes/functional-analysis-rkhs.md). New SVM and GP discoveries are in their destination notes; those future lessons were not rewritten. Research locators and honest review limits are retained in the design: MIT projection/Riesz notes and the identified direct lecture video, the representer paper, kernel/GP survey, characteristic-kernel paper, Fourier-feature paper and current API documentation. The video was identified through the official page and substantive notes; no playback, transcript or timestamp review is claimed.

## Executed numerical evidence

`node scripts/verify-functional-analysis.mjs` executes the actual eleven stored code strings using Python 3.12.14 / NumPy 2.3.5 / SciPy 1.18.1, and runs the independent Python oracle in `scripts/verify-functional-analysis.py`. Final result: `scratch/functional-analysis-verification/results.json`, passed at 22:08:12 UTC.

Actual checks: 30 independently integrated spikes; 205 exact Fraction slope/evaluation states; three feature/invalid Gram constructions; 122 changed wiggles; 144 independent shifted NumPy systems including duplicate inputs and prediction/norm comparisons; twelve independent kernel product enumerations; 288 quadrature residual integrals; 56 changed actual native interpolants; twenty changed actual native ridge systems checked by eigensolve; 35 changed actual native quadrature rules including duplicate/zero nodes; six numerical-range boundaries; four independently calculated practice checks; nineteen rejected model inputs. The recorded maximum absolute discrepancy, 1.10834e−7, includes comparison to the deliberately rounded six-decimal practice answer; it is not a uniform numerical error certificate.

The native helper's domain guards were strengthened after independent review found `anchored_interpolate([1e-310],[1.0])` returned infinities. It now rejects nonfinite coefficients/energy, underflow of a positive interval energy and lost numerical positivity, without emitting an overflow warning. The paired `1e-308` input, an exact zero target and a tiny representable energy remain valid. Eleven displayed outputs, including the original, stayed unchanged. The literal KRR coefficient norm is never used as the RKHS norm. The UI Gaussian norm uses a stated bounded feature-series approximation, checked independently against the Gram expression; it is not presented as an exact finite RBF map.

Finite checks do not prove infinite-dimensional completeness, Mercer, characteristic-kernel or GP path theorems. Those claims received explicit assumptions, derivations where locally supplied, and scoped primary references. The independent reviewer also checked further exact projection/quotient identities, high-precision ridge systems and Fourier-domain discrepancy integrals in a separately attributed record.

## Browser and visual evidence

`node scripts/review-functional-analysis-lesson.cjs` passed at 22:09:47 UTC using headless Edge, actual Space Grotesk fonts and viewports 1440, 390 and 320. It verifies all seven investigations, presets/resets, changed values, invalid-draft preservation/recovery, a real keyboard range change, keyboard access to the locally scrolling sequence, and agreement between the exact 2/3 quadrature preset and its slider. It checks both actual checkpoint prompts/revealed answers, nine independent practice solutions, all eleven rendered programs/questions/outputs, eleven navigation anchors, fourteen successfully rendered and fully fitting display equations, page geometry, and both browser and React console errors. All three document widths equal the viewport; no errors or equation clipping remained.

`scripts/review-functional-analysis-reading.cjs` supplies final focused code/explanation/approximation-rank and lower-plot reading evidence at the same three widths. Its `reading-results.json` is stored alongside `scratch/functional-analysis-browser/results.json`. It checks the actual repaired code and visible numerical limitation paragraph, rather than assuming stored example metadata is rendered.

The exact opened screenshot list/hashes is in the durable author record. It includes ordinary reading across sections 1–11, point-evaluation/error state, signed Gram sum, changed observation/wiggle, duplicate fit, extrapolation, distribution witness, manual/optimal quadrature, completeness rows, changed practice, references, and the repaired equations/program explanation. Captures were opened and inspected, not merely generated. The conceptual curves retain readable phone labels; wide code and the finite-coordinate sequence have local scrolling. Equations were split at mathematical steps so they fit 280-pixel phone content without reducing text size.

## Findings closed and limits

Early browser attempts failed while a formatting utility briefly emitted an unescaped JSX `<` and Vite retained that overlay. The utility now preserves encoded JSX text; actual esbuild transformations and the parent dev restart resolved it. Those failed screenshots are not acceptance evidence. The first successful reading pass revealed long formulas, weak initial evaluation scaling, an extra-observation caption mismatch, an off-step quadrature preset, and minor prose joins; these were corrected before the final pass. A keyboard-scroll assertion initially read before the native scroll settled; the final check waits and verifies actual scroll position.

The independent review's Nyström correction is explicit: a thresholded retained eigenspace gives a thresholded inverse, equal to the exact mathematical pseudoinverse only if every positive eigenvalue remains. Its extreme native interpolation finding was repaired and retested as described above. No new broad redesign is implied by these narrow fixes.

This record establishes author-side source/numerical/browser review, not a user learning study, universal floating-point safety, full video viewing or production deployment. Independent exact-hash review and parent build/integration are separate records. Read-time/practice estimates are estimates, not measured learner results.

## Post-freeze practice amendment

After the initial 22:14:36 UTC freeze, the independent reviewer recommended making practice H's specified noise change directly checkable. Its solution now adds the actually executed fixed-seed selected settings, training/norm and validation/test results, plus an explicitly chosen linear-target repeat and the single-run caveat. The original reporting rubric remains. Values come from the reviewer's actual changed program executions in `scratch/functional-analysis-independent-review/results.json`, whose complete outputs and changed-code hashes were read. No model, program, lab, CSS or blueprint changed.

The focused actual-font 1440/390/320 reading script passed again at 22:16:30 UTC, asserting both output pairs and capturing the explained solution. All three new captures were opened; the durable record now lists 37 opened images and preserves the initial freeze beside the amended body hash. The full behavior suite remains the 22:09:47 run because this final amendment changes prose only.
