# Calibration and conformal prediction — data and calculation provenance

Author calculations and data retrieval occurred on 2026-09-12. The two complete programs are instructional assets and bounded author checks; they do not establish completed implementation or independent review. Python3.12.14, NumPy2.3.5, SciPy1.18.1, scikit-learn1.9.1 were used. No package was installed into the shared runtime.

## Constructed and exact examples

`calibration_calculations.py` generates `checked-results.json`: ten-card reliability/ECE, smoothed sigmoid fit, stack PAV with tied-score grouping and all24 tied-row permutations, stable softmax and positive-temperature fit, exact decimal/rational rank, rank rotations with/without ties, probability sets, score-comparison rounding case, normalized residual/units invariance, APS cumulative scores, and changed independent practice. It also retains simple population arithmetic for calibration resolution, operational cost, prevalence change and group-weighted coverage. These are designed mathematical examples, not sampled real observations. Sigmoid and temperature fits are numerical optimizations, not exact rational results; bounds/convergence are recorded.

## Banknote Authentication

- Source: [UCI dataset267](https://archive.ics.uci.edu/dataset/267/banknote+authentication), Volker Lohweg (2012), DOI10.24432/C55P57, CC BY4.0. Original1372 rows, four wavelet features: variance, skewness, source-spelled `curtosis`, entropy; target0/1. Class semantics are not inferred beyond the codes.
- Actual archive retrieval used in earlier adjacent author packets: https://archive.ics.uci.edu/static/public/267/banknote+authentication.zip on2026-09-12. ArchiveSHA256 `1e2acd9a2085fadf3d8145c12d3d22af853320d52294a6590c2eaf75fdc05227`; member`data_banknote_authentication.txt` SHA256 `d0539aaed2139ba7a587b3e34fb345ce503ff7d5d33dbf9912d8e195ce425cb9`.
- Retained self-contained CSV copied byte-for-byte from the completed adjacent PAC packet, not redownloaded: `banknote-subset.csv`, SHA256 `d28fa993ed459d2f706816395475af08eebd2f394be67f2ad42dd9b511fc6b5a`.
- Selection: NumPy`default_rng(23).permutation(1372)[:480]`, original one-based source row retained. CSV legacy split labels are320pool/80dev/80test. **This lesson explicitly subdivides pool into first240 base training and next80 probability calibration; the80dev rows are conformal calibration; final80 are assessment.** No role is silently inferred from a generic `cal` variable.
- Fixed comparisons: StandardScaler+RBF SVC C1; frozen base sigmoid/isotonic/temperature calibration, naive sigmoid of raw decision score, and training-prior constant. Primarymethodsigmoid fixedbeforeassessment; no method selected on final results. Proper calibration outputs each receive their own conformal scores on the separate80 examples. All raw scores/IDs/probabilities/sets/counts retained in`experiment-results.json`.
- This randomized corpus comparison is not evidence that source acquisition groups match a future deployment. The illustration supports mechanics and a declared finite-corpus prediction task, not a domain certification.

## Airfoil Self-Noise

- Source: [UCI dataset291](https://archive.ics.uci.edu/dataset/291/airfoil+self+noise), Thomas Brooks, D. Pope and Michael Marcolini (1989), DOI10.24432/C5VW2C, [CC BY4.0](https://creativecommons.org/licenses/by/4.0/). UCI donateddate2014; creatorcitation1989. Original1503 rows, no missing values. NASA aerodynamic/acoustic wind-tunnel data; reuse is attributed to the UCI record and creators.
- Actual downloaded archive https://archive.ics.uci.edu/static/public/291/airfoil+self+noise.zip on2026-09-12, SHA256 `5c7767ba53ad827d3f48ba1eb9434117f4892df8f10bc4c99e118a9e8a7ae07c`; member`airfoil_self_noise.dat` SHA256 `74c75fd71783f1e6b71f8a622b993dc592897a97cd689c5090a07147a1b097b3`. Archive read in memory and not retained redundantly.
- `airfoil-subset.csv` SHA256 `5839135483aa9c45bac8416d9bbb0b1966858a21758beb4a8fb9b1fa1dd83bb5`; NumPy`default_rng(67).permutation(1503)[:480]`, first240train/next120conformal/last120test. All original numeric strings preserved; added one-based source_row and split columns, descriptive unit-bearing column names. `airfoil-source.json` retains machine-readable retrieval/selection hashes.
- Features: frequencyHz, attack angledegrees, chordlengthmetres, free-streamvelocitym/s, suction-side displacementthicknessmetres; targetscaled soundpressureleveldB. UCI's table tags angle “Binary,” but its own prose and actual multi-valued measurements describe an angle; we treat the observed numeric degree values as continuous input rather than propagate the apparent metadata-type error.
- Fixed comparison before test results: meanconstant; standardizedRidge alpha1; two GradientBoostingRegressor quantile models at.05/.95,80treesdepth2,seed67. Base crossed quantiles are rearranged pointwise before both calibration and test scoring; zero crossings occurred. Alpha.1 throughout. qabsolute8.185218454303879, qCQR.7663505894767866, qconstant9.495958333333348. Every test target/endpoint/width/frequency retained.
- Known interpretation limit: frequency sweeps share experimental settings. Randomization makes the held-out row roles exchangeable in the fixed-corpus experiment conditional on separately fitted objects; it does not establish a new-campaign/new-airfoil generalization guarantee. Frequency2000Hz diagnostic split was declared in the program before running; no new model selected using its failures.

## Author execution record and corrections

Both complete programs executed successfully under the versions above. Checked PAV knots against sklearn's independent implementation, all24 permutations of tied-score data, rank and infinity/null cases, all ten distinct-score rotations(8covered) versus tied rotations(10covered), plus normalizedq2/changedq4/all-scales-doubledq1. Numerical sigmoid optimization checked gradient residual; temperaturefit reports objective and searchbounds.

An initial empirical run exposed a real boundary defect: the prior-constant baseline returned only class0 and covered51/80 after using `p >= 1-q`. The direct, consistently evaluated score comparison `(1-p)<=q` includes its tied class1 boundary and correctly returns both classes,80/80coverage and meansize2. The underlying probability, data, model and threshold did not change. A dedicated constructed fixture retains this reproducible floating-point distinction. Final experiment results were regenerated after that correction; all other method counts were unchanged.

A subsequent small-program run encountered a NumPybool JSON serialization error after adding temperaturefit metadata. Converting the metadata flag to nativebool fixed the output; the numerical fit was unchanged and the complete program was rerun. These are bounded author checks, not a formal independent review. No fabricated stdout, unexecuted benchmark table, browser plot, production code or framework demo is claimed. MAPIE documentation was read but its library was not installed/executed; PyTorch-specific plumbing is not a delivered program here.

Retain manuscripts, specifications, source/provenance, CSV files, complete programs and checkedJSON for later implementation. The local Python import cache is disposable and removed after author checks. Production may consolidate shared downloadable data behind an explicit ownership decision, preserving offline examples and source IDs.
