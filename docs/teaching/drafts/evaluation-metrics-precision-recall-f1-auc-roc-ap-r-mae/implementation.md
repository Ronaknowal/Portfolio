# Evaluation Metrics — prepared-content implementation

19 September 2026. Stable ID `evaluation-metrics-precision-recall-f1-auc-roc-ap-r-mae`. Authorized phase-two work as the fifth sequential prepared lesson in the current increment. **Author implementation and verification complete.** A separate agent performs independent review; the increment owner completes production integration and phase-ledger binding. No commit, deployment or user acceptance is claimed here.

## Consumed inputs and scope

Read the complete prepared manuscript, visual specifications, design and source-inspection record, both complete Python programs, fixture/result JSON, data provenance, and the entire previous published lesson. The parent confirmed the finish preflight. Original checkpoint-bound packet files were not edited. Stable topic identity, curriculum order and canonical title are retained.

The reader contains all 11 manuscript sections, nine inline visual groups, four distinct investigations and eight changed practice tasks with closed hints/solutions. Preserved depth includes evaluation units; all binary denominators; F1/Fβ and explicit costs; population prevalence; score ties, AUC pair credit and grouped AP; AP versus PR trapezoids; curve dominance versus areas; probability losses and their calibration limitations; loss/estimand choice; residual geometry, R² references and constant targets; MAPE limitations; query-level retrieval and gain conventions; real threshold selection; multiclass/multilabel aggregation, balanced accuracy/MCC; streaming, complexity and uncertainty. The complete programs are expandable inline and downloadable with their actual outputs.

The design's conservation/correction decisions are preserved. Unsupported historical claims, synthetic numbers pretending to be measured results, test-fitted preprocessing, epsilon-masked denominators, false AP/trapezoid equivalence and invented probability-loss-versus-threshold curves are replaced by the approved packet's correct treatment. No broad new curriculum audit or scope expansion was performed.

Prepared primary-source research is reused with its recorded inspection limits. The annotated video retains its known correction; neither the original author nor this implementer claims full playback. Executing the actual pinned library programs verifies the behavior used in the code, rather than inferring it from a moving documentation page.

## Semantic ownership and loading

- Reader: `src/learn/data/topics/evaluation-metrics-precision-recall-f1-auc-roc-ap-r-mae.jsx`.
- Figures/controls/style: `src/learn/components/lesson-labs/EvaluationMetricsFigures.jsx`, `EvaluationMetricsLabs.jsx`, `evaluation-metrics.css`.
- Pure bounded calculations: `src/learn/data/evaluation-metrics-models.js`.
- Compact frozen development/test report: `src/learn/data/evaluation-metrics-data.json`.
- Exact displayed source and stdout: `src/learn/data/evaluation-metrics-examples.js`, generated by the native verifier from the executed downloads.
- Complete offline CSV, programs, generated reference outputs and attribution: `public/learn-assets/evaluation-metrics/`.
- Topic-owned blueprint: `src/learn/data/curriculum/blueprints/evaluation-metrics-precision-recall-f1-auc-roc-ap-r-mae.js`; registration belongs to the parent.
- Reusable checks: `scripts/verify-evaluation-metrics-native.py`, `verify-evaluation-metrics-models.mjs`, `review-evaluation-metrics-browser.cjs`.

Only the selected lesson imports these modules. The browser does no model fitting or remote dataset download. Classification arrays are bounded to 4–16 items, regression to 3–12 and retrieval to 3–10. Calculations are small synchronous operations. The full 480-row dataset and complete result JSON remain downloads; the runtime imports only development candidates and the four frozen test summaries. No graph framework, Python runtime, timer loop or animation engine is added.

## Visual and investigation implementation

1. Four prediction contracts pair the required kind of prediction with its observation and evaluation question.
2. Named score groups, a quantitatively scaled gate ruler and an ID-preserving confusion matrix show which observations enter each denominator. Tied B/C coordinates coincide; actual class is encoded with shape and lane as well as color.
3. Two count-based population explanations and normalized alert trays preserve all TP/FP/FN/TN counts and the fixed-conditional-rate assumption.
4. Exact grouped ROC/PR points come from the same editable data, with counts and AP contributions in an accessible table. The no-alert precision plotting convention and PR-connector limitation are explicit.
5. The full positive-negative grid exposes 1, 0.5 and 0 pair credits with IDs and scores.
6. Every p→p² transformation is listed; the selected observation has linked probability rulers and separately scaled contribution bars. Moving a threshold changes alert counts while probability losses remain fixed. Constant 0.5 totals are retained.
7. Residual rulers use a shared value extent; squared-error areas use one common geometry scale across A/B. Zero area has text rather than a fabricated visible square. Fitted training constant and evaluation-mean reference appear separately.
8. Actual and ideal retrieval shelves use the same judged candidates, explicit gain/discount contributions and a common bar scale. Keyboard rank movement preserves document identity/focus; grade/gain/cutoff changes recompute the ideal.
9. The measured development-cost curve identifies its frozen choice, lists all actual candidate counts, and separates test reporting. The no-alert candidate has no finite x coordinate, so its cost 195 is explicitly stated and retained in the table. Test changes show 17 extra false alerts versus 3 recovered positives; test cost rises 24→26. Shared ranking/probability scores appear once.

The four investigations ask different questions: decisions crossing a gate, admitting an entire tied score block, comparing error lengths/areas and reordering judged documents. Fresh predictions require explicit answers and reasons, then Commit and Reveal. No prediction is preselected. Edits retire the result; restoring an old input does not revive it. Mathematical undefined cases are distinct from invalid input and never filled with a fake epsilon.

Implementation refinements to the prepared interaction details:

- The manuscript already solves the initial fixtures. Those tuples therefore open as ungraded exploration; a changed scenario is required for a fresh graded attempt. Reset clears visible edits/answers/results but retains the mounted investigation's memory of exposed answers, preventing reset from pretending known answers are unseen.
- Full-ranking exploration explicitly ends graded steps for that score/label dataset. Its exposure history is keyed by stable-ID-sorted data and survives edit/restore and tied-card display permutations. This replaces a mere extra commitment that would still expose future answers.
- Invalid edits clear the proposed output and show a localized explanation instead of retaining a result that might be mistaken for the invalid draft's calculation.
- Phone layouts stack both ROC and PR views in reading order rather than requiring a tab switch. Wide exact tables scroll locally and are keyboard-focusable.
- Card reordering is immediate, with no autoplay or decorative movement. At a list boundary focus stays on another enabled movement control for the same document.

## Actual execution and verification

`scratch/lesson-tools/Scripts/python.exe scripts/verify-evaluation-metrics-native.py` **passed** under Python 3.12.14, NumPy 2.3.5 and scikit-learn 1.9.1. Both exact learner-download programs executed. All prepared calculation fixtures and real score arrays reproduced recursively at relative tolerance 1e-10/absolute 1e-12; the complete calculation program's library assertions passed. Source row sets are disjoint and total 480. CSV SHA-256 remains `d28fa993ed459d2f706816395475af08eebd2f394be67f2ad42dd9b511fc6b5a`. The measured result honestly retains selected-policy test cost 26 versus fixed-policy cost 24. The shared Python environment was not modified.

`node scripts/verify-evaluation-metrics-models.mjs` **passed eight grouped checks**. Browser kernels agree with the independently executed Python fixtures for threshold counts/rates/costs, grouped curves, no-class domains, constant scores, edited C, probability losses, residual references/units/nulls and both retrieval gain conventions. Forty additional changed score datasets compare direct pair credit with separately accumulated ROC area and reversed display order. Correct/wrong certain probability forecasts, invalid inputs and zero denominators are covered. All **14 actual JSX mathematical strings** are parsed and rendered by KaTeX; all eight practice hints and exact displayed/downloadable programs are checked.

`review-evaluation-metrics-browser.cjs` **passed 18 grouped checks across 1440, 390 and 320 px**. Exercises changed committed answers in every investigation, hidden outputs before reveal, state invalidation and edit/restore, full-ranking exposure history, no-class curves, unit conversion, constant-target R², keyboard document movement/focus, gain changes, invalid inputs/reset, navigation anchors, figure/lab counts, exact download responses, viewport containment and actual square geometry. No runtime or KaTeX error was observed. It retains **26 informative captures**: all nine figures plus changed results for each of the four investigations, at desktop and phone widths.

All 26 images were actually opened for author inspection. This found and corrected narrow-chart tick readability and metric-token wrapping. The final measured plot uses a 320-unit viewBox with proportional x coordinates, a bounded desktop width and legible ticks. A focused follow-up verified effective tick size ≥11.5 px at all three viewports, checked the no-alert cost annotation and refreshed/reopened the two affected images. Element captures hide the fixed site navigation only during screenshotting; this avoids a sticky-header overlay inside tall element crops and does not change the real page or interaction checks.

Evidence: `docs/teaching/evidence/evaluation-metrics-native.json`, `evaluation-metrics-models.json`, `evaluation-metrics-browser.json`; exact author source binding in `evaluation-metrics-author-review.json`. A source compile and targeted whitespace check also passed. The parent owns the single production build, publication load/recovery checks and shared metadata/ledger updates.

## Learning-experience checklist — author assessment

1. **Entry/progression:** the opening alert question requires no prior metric vocabulary; a local route distinguishes core and optional branches and supplies all eleven anchors.
2. **Cautions at their point of use:** undefined precision, cost assumptions, tie grouping, probability interpretation, R² reference, gain convention and data roles appear beside their relevant operation.
3. **Concrete meaning:** every initial number is traceable to a named item or task; a real imperfect two-feature model supplies the measured example.
4. **Investigations:** four mechanisms expose actual editable entities, fresh predictions, nulls, contrasting cases and transfers. Revealed answers cannot silently re-enter graded practice.
5. **Visual accuracy:** ruler positions, curve points, bar lengths and square areas follow the numbers; no invented benchmark curve or gradient. Exact values remain available in text/tables.
6. **Connections:** threshold policy is separated from score ordering and probability loss; absolute/squared error connects to median/mean forecasts; retrieval extends binary relevance to graded gains; the next lesson is PAC Learning & VC Dimension in curriculum order.
7. **Code:** full mechanism programs and exact outputs remain available without interrupting the first-pass explanation. Native checks do not substitute for browser controls.
8. **Practice:** all eight tasks change data or require defensible interpretation; hints/solutions remain closed. Streaming and real-result reporting require actual numerator/denominator and split explanations.
9. **Informative states:** author images include changed threshold membership, edited ranking coordinates, a reversed residual preference and changed retrieval gains, alongside all explanation figures at desktop/phone widths. This is an author heuristic assessment, not a novice study.

## Independent review and handoff

The separate reviewer identified and helped close full-ranking answer exposure, unit-sensitive near-tie grading and verbose accessible names. Preferences now compare in base minutes with a declared tolerance; signed scientific differences prevent rounded labels from hiding a disagreement. The independent report/evidence is owned by that reviewer; it is not an author self-review. Final chart-only changes were communicated for a scoped source rebind and affected-image review.

No known material author issue remains. Complete production integration and phase-two checkpointing remain with the increment owner. Prepared checkpoint files are unchanged; one-off conversion/capture helpers are removed after closure so they cannot overwrite reviewed production source. Retained scratch images are referenced review evidence, not unused decoration.
