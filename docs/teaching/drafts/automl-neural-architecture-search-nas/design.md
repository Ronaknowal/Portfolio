> **Current interaction amendment, 21 September 2026:** Read [live-exploration.md](live-exploration.md). Labs now update directly from valid edits and contain no learner prediction feature, including optional predictions. The older prediction/commit/reveal clauses below are historical design records; their numerical, scope, layout and evidence requirements remain applicable where unchanged.

# AutoML & NAS — content design and handoff

Stable ID: `automl-neural-architecture-search-nas`. Display title retained: **AutoML & Neural Architecture Search**. Content-only packet, 12 September 2026. No published source, runtime component, manifest, blueprint, route order or shared ledger was edited by this author. Root owns content checkpointing and shared destination-note reconciliation.

## Scope, preflight and conservation baseline

Consumed `node scripts/build-curriculum-inventory.mjs --topic automl-neural-architecture-search-nas --work content`, including the returned absence of an individual blueprint and existing topic note. Read the complete original published body in `src/learn/data/topics/automl-neural-architecture-search-nas.jsx` before drafting. Its SHA-256 was `7e2a8177e673b57f583b6ec3dc39c3998fd3fc6836530c96332258b44206979e`; the parent scope baseline commit is `8c5da59f18516be77c29d5aeeafca3decca4f738`.

The current handoff, full teaching standard, topic-design brief, ML domain playbook, ownership/coordination/retention policies and thirty-topic scope were read and applied. This is a research/write assignment: a complete manuscript with visual/lab specifications, substantive accuracy work and author review. It is not a request to implement initial widgets and defer merely their tests.

Actual module adjacency is **Imbalanced Learning → AutoML & NAS → Hidden Markov Models**. Local prerequisites are the earlier classical pipeline, validation, regularization, selection and imbalance lessons. A full neural-network course is not yet a safe prerequisite, so §4 defines a unit, nonlinear activation, layers, output interpretation, trainable weights and parameter shapes locally. The neural branch is part of core scope; the deeper differentiable derivation follows only after those foundations.

Inventory also contains separate later `neural-architecture-search-nas` and `automl-as-meta-learning` topics, plus a later classical `gaussian-processes-gp`. This packet owns a full local AutoML/NAS foundation and a bounded actual small-network comparison. The later topics own their specialized implementations and cross-task/native benchmark depth. Do not rename this topic to “AutoML only” or discard its original NAS content because another NAS title exists.

### Original-content disposition

| Original useful idea or claim | Decision in the new packet |
| --- | --- |
| AutoML motivation; Auto-WEKA/CASH history | Preserve the problem and accurate CASH objective in §§1–2; omit unsupported classifier counts and claims of universal expert replacement. Historical systems remain available through canonical references. |
| Conditional pipeline choices; model/preprocessor interactions | Expand into an executable eleven-configuration grammar, count and sampling-measure discussion, I1 and complete real-data program. |
| Bayesian proposals and expected improvement | Preserve and deepen with a derived EI expression, exact constructed values and I2; avoid the false claim that Gaussian processes cannot represent conditional spaces. |
| Meta-learning warm starts, auto-sklearn ensembles | Preserve with a complementary-portfolio counterexample and explicit greedy selection versus stacking distinction; route deeper cross-task evidence to the dedicated owner. |
| NAS search space/strategy/evaluator; MLP and graph views | Preserve all three axes; add local neural definitions, matrix/bias counts and observed small-network alternatives. Full CNN cells belong to the later dedicated topic. |
| Bilevel objective and differentiable relaxation | Preserve with exact operation-mixture gradient, full one-step chain rule, finite-difference product explanation and independent scalar counterexample. Correct the original first-order/one-step conflation. |
| Discretization | Replace a generic per-edge argmax description with the original DARTS cell-construction distinction and an exact mixture-to-discrete function gap. |
| RL, evolution, shared weights, proxy ranking | Preserve explanatory paragraphs and weight-provenance diagram. Avoid unmeasured speed-ups and guarantees about proxy rankings. |
| Multi-fidelity, early stopping, Hyperband | Expand with complete constructed curves, survivor decisions, restart/resume accounting, slow-starter failure and meaningful author alternative resource. |
| Full pipeline/NAS Python examples | Replace synthetic favorable outputs and mismatched seeded reports with a fixed openly licensed observed-data study, complete teaching program and actual stored predictions. |
| FLAML, KerasTuner, AutoGluon, NNI guidance | Retain current useful APIs and complete optional programs; identify explicit holdout, conditional activation and unexecuted status. AutoGluon presets are version-dependent; NNI is archived. Remove imaginary universal library-quality heatmap and installation/performance promises. |
| Search trajectories, preprocessing heatmap, Pareto models | Replace invented empirical curves with observed finite replay and fold/inspection outcomes; use anonymous explicitly hypothetical Pareto points. No performance ranking is transferred across incomparable devices/models. |
| Search overfitting, costs, reproducibility, domain limits | Preserve at the relevant mechanism home. Correct search work versus wall time and hard constraints versus soft penalties; no fixed trial quotas or automatic KS-test deployment verdict. |
| Exercises and references | Replace with ten changed questions and complete solutions, independent altered values, design task, primary sources and genuinely inspected alternative learning paths. |

Specific inaccurate original claims not retained include converted historical NAS compute figures, a claim that all joint architecture updates necessarily memorize, “one-step means first-order,” architecture L2 guarantees eliminating skip collapse, exact-input FLOP counts predicting hardware latency, zero-cost proxies meaning no computation or only Gaussian input, permanent library ranking values, and a purported KerasTuner Greedy tuner. The new observed-data result is not rewritten to support a preferred winner: scaling loses for the two fixed logistic C choices, k = 3 neighbors is very competitive, and the deeper network does not beat the width-16 network.

## Learning design and complete scope

The goal is to let a learner design, run and interpret a bounded automated model search while understanding which choices and evidence determine its answer. The manuscript is connected prose rather than a glossary or recipe list. Each nontrivial symbol is introduced through a question and concrete calculation.

| Hurdle | Local bridge and representation | Evidence of readiness |
| --- | --- | --- |
| “AutoML discovers what matters automatically.” | Task contract and evidence-loop F1, error-cost connection to preceding lesson. | Explain which objective, split and constraints remain human task definitions. |
| “All option counts multiply.” | Conditional grammar/I1, active versus inactive settings, sampling probabilities. | Count changed 16-configuration practice space and 64 CV fits. |
| “Best predicted mean is always the next trial.” | Exact expected-improvement distribution/I2. | Separate surrogate mean, uncertainty, acquisition and observed loss. |
| “Cheap early winners remain best.” | Rung/curve I3 with a deliberately eliminated slow starter. | Trace survivors and distinguish restart/continuation work. |
| “NAS requires unexplained deep-learning knowledge.” | Local neuron, activation, layers, shape counts and F2 before real comparison. | Count changed 61-parameter network and explain weight/architecture distinction. |
| “High CV score is a final claim.” | Protected group split, fold-local pipelines, observed F3/I5, separate inspection. | Diagnose leakage and refuse to borrow another prefix's inspection result. |
| “A large model or more searching must help.” | Actual eleven-candidate outcomes and fixed replay with ID/score distinction. | Explain deeper model's shared error and the finite-search limitation. |
| “Deployment quality is one universal number.” | Portfolio example and hard-constraint Pareto I6. | Separate average default, complementary portfolio, trade-off and feasibility. |
| “Soft architecture choice equals final architecture.” | I4 operation mixer and exact commitment gap. | Compute mixture gradient and explain the changed discrete function. |
| “One training step has only a first-order architecture effect.” | Full chain rule and F4 three-derivative calculation. | Distinguish zero direct, −0.098 one-step and −0.8 exact-inner gradients. |
| “Proxy or shared-weight score is independently trained accuracy.” | F5 weight provenance and exact NASWOT code/kernel inset. | State what the score measured and which final evaluation remains necessary. |

First-pass route appears immediately after introduction. §§1–5 contain the core complete search and local NAS bridge; §6 adds practical method choices and applications. §7 is an explicit deeper branch, not hidden first-pass readiness. §8 optional executable translations use the already defined development boundary. §9 separates core questions 1–6 from deeper 7–9 and applied design 10. §10 links actual next HMM, prior foundations and later specialized owners.

There is no fixed laboratory quota. Six distinct investigations were chosen because conditional composition, surrogate acquisition, fidelity scheduling, observed replay, constrained selection and operation calculus are distinct hurdles. Static/explorable figures handle boundaries, parameter shapes, actual outcomes, derivative dependencies and weight provenance. All specifications identify inputs, exact data, labels, outcomes, prediction binding, meaningful edits, contrast, null, accessible text/mobile behavior and bounded phase-two checks.

## Canonical coverage check

The canonical resource is Hutter, Kotthoff and Vanschoren's openly licensed **Automated Machine Learning: Methods, Systems, Challenges**, available at [the official book page](https://www.automl.org/book/) and [author-hosted full PDF](https://automl.org/wp-content/uploads/2019/05/AutoML_Book.pdf). Read the book's full chapter list and the complete section/subsection lists for relevant chapters 1–3. Relevant section bodies were inspected as recorded below; this is not a claim to have read all 222 pages or every historical system chapter.

The chapter list includes HPO, Meta-Learning, NAS, Auto-WEKA, Hyperopt-Sklearn, auto-sklearn, Auto-Net, TPOT, Automatic Statistician and AutoML Challenge. Historical implementations are examples of the core mechanisms, not a required catalogue of every retired API.

| Canonical section inventory | Coverage / reasoned disposition |
| --- | --- |
| 1.1 Introduction; 1.2 Problem Statement; 1.2.1 Alternatives: Ensembling and Marginalization; 1.2.2 Multiple Objectives | §§1,2,6 cover CASH, ensembling and Pareto/constraints. Full Bayesian marginalization needs likelihood/prior/posterior treatment; the site's Bayesian inference/model lessons own it. Do not present ensemble-selection weights as posterior model probabilities. |
| 1.3 Blackbox HPO; 1.3.1 Model-Free Methods | Enumeration/random sampling, distributions and brief evolutionary strategy; full evolutionary implementation belongs to specialized optimization/NAS treatment. |
| 1.3.2 Bayesian Optimization; 1.3.2.1 Nutshell; 1.3.2.2 Surrogates; 1.3.2.3 Configuration Space; 1.3.2.4 Constrained BO | §2 and I2 teach surrogate/acquisition computation and conditional representation; §6 teaches constraints. Full GP posterior/kernel/acquisition optimization is explicitly routed to GP. A separate constrained-surrogate solver is unnecessary to understand current selection constraints. |
| 1.4 Multi-Fidelity; 1.4.1 Learning-Curve Prediction/Early Stopping; 1.4.2 Bandit-Based Selection; 1.4.3 Adaptive Fidelities | §3/I3 explain cheap evidence, halving, Hyperband and ranking failure. Full curve-extrapolation model and BOHB implementation are deeper algorithm variants; preserve the distinction rather than imply all adaptive fidelity uses halving. |
| 1.5 Applications | Real tabular classifier search, small NAS, portfolio and Automatic Statistician application. |
| 1.6 Open Problems; 1.6.1 Benchmarks/Comparability; 1.6.2 Gradient-Based Optimization; 1.6.3 Scalability; 1.6.4 Overfitting/Generalization; 1.6.5 Arbitrary-Size Pipeline Construction | Evaluation, budget, leakage and gradient sections address the core. Conditional grammar introduces pipeline validity; unrestricted program synthesis is outside this foundation, not claimed solved by the eleven-candidate search. |
| 2.1 Introduction; 2.2 Learning from Model Evaluations; 2.2.1 Task-Independent Recommendations; 2.2.2 Configuration-Space Design | §6 portfolio and negative-transfer example introduce cross-task selection. Deeper task-level splits and learned spaces go to `automl-as-meta-learning`. |
| 2.2.3 Configuration Transfer; 2.2.3.1 Relative Landmarks; 2.2.3.2 Surrogate Models; 2.2.3.3 Warm-Started Multitask Learning; 2.2.3.4 Other Techniques; 2.2.4 Learning Curves | Current scope explains transferable experience and its limits; dedicated meta-learning owner receives concrete instruction to teach task similarity/landmarks, warm starts and negative controls when justified. No unsupported new-task guarantee. |
| 2.3 Learning from Task Properties; 2.3.1 Meta-Features; 2.3.2 Learning Meta-Features; 2.3.3 Warm-Starting from Similar Tasks; 2.3.4 Meta-Models; 2.3.4.1 Ranking; 2.3.4.2 Performance Prediction; 2.3.5 Pipeline Synthesis; 2.3.6 To Tune or Not to Tune | Bounded local definitions and portfolio mechanism; full task embeddings/rankers/performance prediction/pipeline synthesis belong with the later cross-task topic. Those depend on task-level evaluation rather than more banknote row folds. |
| 2.4 Learning from Prior Models; 2.4.1 Transfer Learning; 2.4.2 Neural Meta-Learning; 2.4.3 Few-Shot Learning; 2.4.4 Beyond Supervised Learning; 2.5 Conclusion | §6 distinguishes transfer from search/ensembling and mentions current pretrained tabular choices. Representation transfer, few-shot adaptation and non-supervised task distributions belong to their dedicated learning modules; they are not prerequisites for current core readiness. |
| 3.1 Introduction; 3.2 Search Space; 3.3 Search Strategy; 3.4 Performance Estimation; 3.5 Future Directions | All three NAS axes taught locally in §§4,5,7; continuous relaxation, weight reuse, independent training/proxies and deployment comparisons covered. Full CNN cell, controller/evolution implementation and benchmark campaigns are the later dedicated NAS owner. |

This inventory is a conservation/coverage check, not the page's heading template. The learner-facing order follows the actual prerequisites and concrete experiment.

## Substantive research record and consequential claims

All listed bodies were inspected in this authoring task on 12 September 2026, using primary sources or official implementation documentation. Search results or metadata alone were not treated as technical evidence. The complete dataset page and in-memory ZIP member were inspected; no remote images or large model data were downloaded.

| Source and inspected material | What it supported or corrected |
| --- | --- |
| [AutoML book PDF](https://automl.org/wp-content/uploads/2019/05/AutoML_Book.pdf): full relevant section lists; chapter 1 problem/ensemble/BO representation passages; chapter 2 introduction; chapter 3 search-space body and full strategy/performance-estimation passages; §6.3.2 greedy ensemble selection and §6.8.3 portfolio discussion; §§9.3.1–9.3.3 grammar/search/descriptions | Canonical coverage and terminology; conditional spaces do not rule out structured GP kernels; original auto-sklearn greedy selection is not ordinary stacking; portfolio complementarity; unusual interpretable grammar application. Chapter-2 specialized bodies were not all read, and no implementation claim about them depends on uninspected text. |
| [DARTS paper](https://arxiv.org/pdf/1806.09055), §§2.1–2.4 including Algorithm 1, equations 6–8 and end of discretization section | Softmax relaxation, bilevel target, direct/mixed one-step derivative and ξ = 0 terminology; top-two nonzero convolutional incoming choices versus top-one recurrent choice. The paper's pointwise-stationarity wording is not copied: independent scalar calculation demonstrates why a zero gradient value does not generally remove its derivative. |
| [Hyperband JMLR paper](https://www.jmlr.org/papers/volume18/16-558/16-558.pdf), §§3.1–3.2, Algorithm 1 and bracket table | Successive-halving allocation, breadth/depth hedge, explicit rounding. Claims are not derived from the paper's entire theoretical proof or benchmark suite. |
| [Jamieson's Hyperband teaching page](https://homes.cs.washington.edu/~jamieson/hyperband.html): compact algorithm, bracket table, learning-rate caveat and experimental-protocol paragraphs | A meaningful alternate explanation of scheduling, distinct from metadata endorsement. No claim of interacting with a widget or reproducing its historical timings. |
| [MnasNet paper](https://arxiv.org/pdf/1807.11626): target-latency objective, hard constraint versus soft reward, factorized space and device-comparison discussion | Why operation/parameter counts are not latency measurements. A finite penalty does not mathematically enforce feasibility even if a paper uses informal “hard” language. No timing is transferred into the hypothetical Pareto fixture. |
| [NASWOT paper](https://proceedings.mlr.press/v139/mellor21a/mellor21a.pdf): §3 activation-code kernel, score, benchmark discussion and §3.1 ablations | Actual batch at initialization, shared activation decisions/log determinant, Gaussian-input ablation distinction, initialization/batch sensitivity. Independent two-code calculation checks matrix/singularity. |
| [UCI Banknote page](https://archive.ics.uci.edu/dataset/267/banknote+authentication), full feature/source/citation/license body, official ZIP data | Real offline input, author/license attribution, numeric labels preserved, duplicate grouping and physical metadata limits. See provenance for exact bytes and split IDs. |
| [FLAML task-oriented AutoML](https://microsoft.github.io/FLAML/docs/Use-Cases/Task-Oriented-AutoML/): task/metric/time/iteration settings, estimator list, holdout/CV modes, supplied validation and refit behavior, grouping/custom split and thread controls | Complete bounded optional holdout program; no inaccurate “best CV” label when method is auto/holdout. Framework not installed or executed. |
| [KerasTuner API](https://keras.io/keras_tuner/api/), [conditional hyperparameters](https://keras.io/keras_tuner/api/hyperparameters/), and [getting started](https://keras.io/keras_tuner/getting_started/): tuner list, actual conditional-scope behavior, model build/search, retrieval and retraining passages | No invented Greedy tuner; inactive conditional parameters can be None while Python scope body still runs; complete declared one/two-layer program and honest unexecuted state. |
| [AutoGluon stable tabular essentials](https://auto.gluon.ai/stable/tutorials/tabular/tabular-essentials.html): current 1.6.1-era preset table, ensemble/learned-portfolio passages and resource advice | Current version-dependent scope rather than fixed two-layer stack or universally best library. Documentation's own benchmark claims are not imported as this site's comparative evidence. |
| [Microsoft NNI repository](https://github.com/microsoft/nni), repository header/status | Archived by its owner 18 September 2024, now read-only; kept as historical reference, not current maintained default. |

Meaningful alternative resources are the author Hyperband explanation, canonical chapter route and complete official code guides. No video was inspected; none is labeled recommended or watched. A future video addition should record the actual segment/transcript and its educational contribution, not simply a channel's popularity.

## Calculations, data and author review

The fixed observed calculation is 35 sklearn fits, one campaign, no fitting warnings, with the exact source and saved fold/inspection predictions described in `data-provenance.md`. The reserve was never scored. All important illustrative outcomes derive from explicit equations or recorded inputs; there are no fabricated runtime, library-quality or empirical uncertainty charts.

The main teaching code is complete, with setup, local CSV path, role creation, conditional constructors, fold-local preprocessing, selection, refitting and reported outcomes. Its role/candidate definitions are checked against the actual author calculation without rerunning training. Optional FLAML/Keras programs are complete but explicitly unexecuted; exact version/API execution is phase two. No native framework installation or model download is hidden in the research phase.

Author checks to preserve in checkpoint review:

- Read the whole manuscript in sequence, including math, observed interpretation, optional-program boundaries, changed practice and next-topic link; check that every first-pass concept is locally defined before use.
- Read every figure/investigation contract and match its labels, parameters, predictions, contrast and null to the manuscript and saved inputs. Check that the observed replay never borrows a final result from another candidate.
- Verify exact EI, softmax/gradient, finite-difference derivative, halving/cost, parameter-count, portfolio, Pareto, NASWOT and changed-practice calculations. Distinguish observed outcome verification from new fitting.
- Syntax-check every displayed Python block. Compare the main program's split/fold/candidate definitions to the retained author program; do not count this as native FLAML/Keras execution.
- Verify source-file hash; role/fold row and group disjointness; OOF ID alignment, selected criterion, ties, inspection matrices and error IDs; confirm no reserved predictions exist.
- Check local topic links against the actual navigation inventory and scope-only diff whitespace. No build/browser campaign is appropriate for a content-only packet.

The learner-experience review checks causal continuity, real purpose, editable unsolved entities, separate worked and changed exercises, closed hints/solutions, interesting applications with enough mechanism, accurate source limits, and a clear next action. **Author review completed 12 September 2026:** the full manuscript and all visual contracts were read sequentially after drafting. The review added local definitions of the normal CDF/density, ReLU and Hamming distance; corrected two local route spellings; and clarified specification wording without changing observed outcomes. All three displayed Python blocks parse. The main program's roles/folds and candidate constructors match the actual author program. Source hash, row/group boundaries, all saved candidate means and inspection matrices, EI contrast/null, operation gradients, halving contrast/null, replay prefixes/null, Pareto choices/null, portfolio/kernel calculations, and the scalar derivative check passed. No further native fits were performed. `calculated-inputs.json` records these bounded author checks. Topic text has no trailing whitespace. Root independently read the complete manuscript, specifications, design and provenance. There is no unresolved material writing gap.

## Proposed destination notes for root to route

### Destination: `neural-architecture-search-nas`

**Proposed note text:** “The earlier classical `automl-neural-architecture-search-nas` lesson now teaches search space/strategy/evaluator, local MLP shapes and parameter counts, an observed three-architecture comparison inside a fixed pipeline study, exact softmax mixture gradients, first-order versus one-step DARTS derivatives, a discretization gap, shared-weight/proxy provenance, and hard-latency Pareto decisions. Build on those foundations with actual convolutional cell shapes and graph construction, independently trained versus shared-weight candidates, a bounded documented native benchmark/proxy comparison, resource accounting and target-hardware measurement. Preserve DARTS's top-two nonzero incoming convolutional choices from distinct nodes; do not collapse it to one independent argmax per edge. Separate search-seed, training-seed and benchmark selection effects; expose a checked proxy failure or null rather than assuming correlation. Explain any departure from the previous scope and retain its computational/validation boundaries.”

**Reason:** There is an existing later NAS topic with the right neural prerequisites. It can own full CNN and benchmarking depth without either repeating the current introductory packet or losing the valuable original NAS branch. Current content deliberately does not claim a small sklearn MLP proves full-scale NAS quality.

### Destination: `automl-as-meta-learning`

**Proposed note text:** “The earlier AutoML lesson introduces configuration transfer, distinguishes search/ensembling/stacking, and uses a constructed portfolio example: A/B complement each other on old tasks while C is the best single default; a new task reverses the portfolio's advantage. Develop task-level train/validation/test separation, relative landmarks or justified task features, portfolio construction, warm starts/rankers/performance prediction where useful, and an observed task-transfer comparison including a simple default and negative-transfer control. Investigate current learned portfolios and pretrained tabular models with their pretraining/task-overlap, licensing and resource assumptions. Avoid treating row-level CV as evidence of transfer to new tasks or describing all ensembles as posterior marginalization.”

**Reason:** Canonical chapter 2 has meaningful material beyond a short AutoML warm-start paragraph. The dedicated topic is the natural owner of cross-task evidence and modern learned defaults; the source chapter list and current AutoGluon documentation supplied concrete reasons to preserve this expansion. The note invites reasoned scope decisions, not a fixed quota or forced new model campaign.

Root has saved both proposals, with rationale and open status, in [the dedicated NAS note](../../topic-notes/neural-architecture-search-nas.md) and [the AutoML as meta-learning note](../../topic-notes/automl-as-meta-learning.md). They remain inputs for future authors to reason about under the topic-note workflow. This author owns only the current packet and did not mutate other authors' destinations or shared ledgers.

## Handoff status and next authorized phase

Content packet: full manuscript, data/calculations, explicit visual/lab contracts, design and provenance. Shared status is set by root only after final reconciliation. Implementation has not started. The next phase implements and verifies the specified topic UI, executes displayed code as warranted, checks runtime/mobile/accessibility and publication wiring, and closes the implementation checkpoint under the current standard.

No unresolved material content gap is intended to be deferred under the label “phase two.” Phase two owns executable and rendered realization, not writing missing core explanations. API execution is honestly pending; observed numbers and mathematical claims are already substantiated. Necessary pending packet files remain; no disposable scratch or downloaded media was created for this topic.

## Phase two — implementation record, 15 September 2026

This section is appended by the implementing author. The manuscript, visual specifications, `calculated-inputs.json`, `data-provenance.md`, `data-source.json` and `banknote-data.csv` above were consumed unchanged; none of them was edited. Every departure from the specification is listed below with its reason.

### What was built

| Path | Role |
| --- | --- |
| `src/learn/data/topics/automl-neural-architecture-search-nas.jsx` | The reader body, replacing the published historical body. |
| `src/learn/data/automl-models.js` | Every pure model a figure or investigation draws from. |
| `src/learn/data/automl-data.js` | Generated: the recorded banknote study. Not hand-edited. |
| `src/learn/data/automl-examples.js` | Generated: the displayed programs, extracted verbatim from the manuscript. |
| `src/learn/components/lesson-labs/AutomlShared.jsx` | The predict-then-check primitives, plot frame and stacking table. |
| `src/learn/components/lesson-labs/AutomlLabs.jsx` | Investigations I1, I2, I3, I5, I6 and I4. |
| `src/learn/components/lesson-labs/AutomlFigures.jsx` | Figures F1, F2, F3, F4 and F5. |
| `src/learn/components/lesson-labs/automl-labs.css` | Their styles, including the narrow-width table stacking. |
| `src/learn/data/curriculum/blueprints/automl-neural-architecture-search-nas.js` | The authored plan. **Not registered** in `blueprints/index.js`, which the increment owner owns. |
| `public/learn-assets/automl-nas/banknote-data.csv`, `ATTRIBUTION.txt` | This lesson's own copy of the source bytes, plus attribution. |
| `scripts/verify-automl-{models.mjs,examples.py,data.py,browser.cjs}` | The four verifiers. |
| `docs/teaching/evidence/automl-{models,native,data}.json` | Their evidence. The browser record is phase C. |

The original published body was preserved to `scratch/automl-nas/original-automl-neural-architecture-search-nas.jsx` and checked against git `HEAD`: SHA-256 `7e2a8177e673b57f583b6ec3dc39c3998fd3fc6836530c96332258b44206979e`, identical to the packet's recorded baseline and to `HEAD`, so nothing uncommitted was overwritten.

### Verification actually performed

- `node scripts/verify-automl-models.mjs` — **206 grouped checks across 53 groups.** Every number the lesson states, recomputed. Independent second routes rather than the same helper twice: expected improvement against a 40,001-point trapezoidal integration of its own definition, and the normal CDF against an integration of its density; the halving schedule against a second implementation written inside the verifier; every architecture gradient and all three bilevel derivatives against central differences of their own objectives; the configuration count against a full enumeration of the space; the 6h + 1 shortcut against the block sum at all 32 permitted widths; every recorded fold accuracy against its own numerator and denominator; every pooled accuracy against the stored list of mistaken rows; both confusion matrices reconciled against the partition's class counts. It also asserts that the assertion helpers themselves can fail, that each model result carries every field the lesson body reads off it, that every entry point refuses out-of-range, duplicated, empty and malformed input, that every value the prose asks a learner to type lands on its control's step, and that the served dataset's bytes hash to the digest the page prints.
- `scratch/lesson-tools/Scripts/python.exe scripts/verify-automl-data.py` — **289 grouped checks.** The whole 35-fit campaign recomputed from the served CSV: 1,348 exact-feature groups, the three roles, the three group-respecting folds, all eleven candidates' 919 out-of-fold predictions each, both refits, the seed-75 order — matched value by value against `calculated-inputs.json`. It then **re-derives that packet's own trust root**: every value of its `constructed` block, from the declared settings alone, using `math.erf` rather than SciPy's `ndtr`, a hand-written softmax, central differences for both gradients, an explicit halving loop, pairwise Pareto dominance and Hamming-distance kernels, without importing `author-calculations.py` — **82 trust-root values**. A fresh regeneration of `automl-data.js` is byte-identical.
- `scratch/lesson-tools/Scripts/python.exe scripts/verify-automl-examples.py` — **104 oracle assertions.** The displayed study is extracted **verbatim from the manuscript's fenced code block**, pinned by the SHA-256 of the manuscript (`7604d8ca4947c0271a11358df4272715b8d9bc07bc299b324a88e86ab32ab0d4`) and of each of its seven fences, then executed against the served CSV in a temporary directory with one BLAS thread. Its printed output is matched against the separately generated data module, line by line, including both NumPy-rendered confusion matrices.
- `node scripts/generate-learning-artifacts.mjs --check` — 0 stale files. `node scripts/verify-curriculum.mjs` — passes.
- A production build (`npx vite build --outDir dist-automl`) succeeds; the lesson chunk is 204 kB raw, 65 kB gzipped, in line with the neighbouring prepared lessons. The build directory was deleted and the preview stopped.

**Not done in phase A, by instruction:** browser verification and screenshots. `scripts/verify-automl-browser.cjs` is written and syntax-checked but not run.

**One exception, declared:** a minimal render smoke check was run against the production build — no evidence file, no screenshots, no recorded cases. It caught a real render-time crash: `portfolioAnalysis` never returned the `oldTaskLosses` field the section 6 table reads, so the page rendered the load-error boundary instead of the lesson. Fixed, and the model verifier now asserts the returned shape of every model result, so that class of defect fails a verifier rather than a page. The same check also found the only display equation that overflowed 320 px.

### Departures from the specification, with reasons

1. **Title kept as "AutoML & Neural Architecture Search (NAS)".** This record's header says "Display title retained: AutoML & Neural Architecture Search", which is the manuscript's H1. The live catalogue entry in `track-definitions.js` is the parenthesised form, and the artifact generator lets a lesson's own `title` override the catalogue's in generated navigation. Changing it would desynchronise the two without a compatible identity change, which is not this task's to make. Flagged for the increment owner.
2. **Two display equations restructured; the same expression.** The cross-validation objective is written as a two-line stack, naming the fold loss first and then averaging it; the three-block parameter sum is split across two lines. Both were measured overflowing at 320 px on the real page. `\widehat` was replaced by `\hat` throughout for the same reason — this packet's own gotcha list records a 336 px-wide `\widehat` accent SVG defeating three rewrites of a line.
3. **The one-step chain rule names its vector.** The manuscript writes the mixed-Hessian product with the validation gradient inline; the page introduces `v` for that vector one sentence earlier and writes the product against `v`, purely so the formula fits 320 px. The manuscript already uses `v` for exactly this vector in the sentence that follows.
4. **Section 8's programs are displayed without recorded output.** FLAML and TensorFlow/KerasTuner were deliberately **not** installed. Installing them into the shared `scratch/lesson-tools` runtime can move NumPy or scikit-learn and invalidate other completed topics' recorded outputs — a larger risk than the benefit of two printed traces the manuscript does not contain. Instead the verifier parses both programs, checks the settings the prose claims for them (FLAML's explicit holdout evaluation method and its seed; KerasTuner's conditional scope and its depth guard), and **executes** their one substantive data claim for real: it imports the extracted `banknote_search.py` and resolves the first fold to confirm that neither program touches an inspection or reserved row. Their status is recorded as not executed in `docs/teaching/evidence/automl-native.json`, with the reason, and the page says so in prose.
5. **I1 keeps all four family branches present.** The specification's guard "require at least one family and one valid configuration in every enabled family" is enforced as an invariant — every dimension must keep at least one value, and emptying one is refused with a field-specific message — rather than by adding a family enable/disable control whose only teaching job duplicates removing its values.
6. **I1's prediction is a direction plus a total, not a choice between two prepared edits.** The specification's first task offers "add tree depth 8" or "add logistic C = 10". The implementation lets the learner type any permitted value into any branch and grades the direction of the count change plus an optional exact total, with both specified edits, the inactive-setting null, a removal and a width-pattern addition available as suggested setups. This satisfies "browser tests must edit actual values rather than only click prepared scenarios" and keeps the specified oracles (11, then 12, then 13) as asserted cases rather than as the only reachable cases.
7. **I1's sampling-rule comparison is a display, not a second graded question.** The specification marks that second question optional. Both measures are drawn as proportional bands with their exact fractions after the reveal; a second radio group inside the same investigation would have crowded the primary task.
8. **I4's architecture step and commitment are separate recorded predictions.** The specification asks for a new prediction at each. Each is its own stage with its own radio group, keyed on the applied mixture inputs so any edit remounts and retires it.
9. **F2 uses a selector rather than three panels side by side.** The specification allows one architecture at a time on mobile; the implementation does so at every width, with the comparison table always visible. Three 340-unit diagrams side by side at 1366 px would render their type at about 8 px.
10. **F3's plot labels candidates by a short code** (`log std 1`, `knn 3`, `net 8,8`) with the full registry label in the adjacent exact table, because the full labels need about 260 of the 340 viewBox units.
11. **The inspectable mistake table covers every candidate, not four rows.** The data module stores the out-of-fold error rows of all eleven candidates, so the weak candidates' mistakes are inspectable too. The packet narrates only rows 349, 107, 195 and 345, and those four are asserted individually.
12. **Internal links use the module-preserving route** `/learn/path/full-curriculum/<id>?module=classical-ml` for the seven classical-ML destinations, and the manuscript's `/learn/topic/<id>` for the two that live in other modules — `neural-architecture-search-nas` in Model Optimization and `automl-as-meta-learning` in Meta-Learning — where a `classical-ml` module parameter would be wrong. Every target ID was resolved against the generated navigation before use.

### What the two destination notes changed here

Both notes were read in full. They describe **later specialist topics**; nothing in them was implemented, and neither note file was edited.

- From [the NAS note](../../topic-notes/neural-architecture-search-nas.md): the specialist lesson is to own convolutional cells, shared-weight implementation and benchmark evaluation, and must preserve DARTS's top-two nonzero incoming operations from distinct nodes rather than one argmax per edge. Section 7 therefore states that rule explicitly and deliberately stops at scalar, parameter-free operations, so the specialist topic inherits a correct rule rather than a correction to make. F5's three lanes name what each score measured precisely because the note asks the specialist to expose a checked proxy failure; the foundation supplies the vocabulary — independent fit, shared weights, untrained proxy — without claiming any correlation between them. The note also warns that a small sklearn MLP does not establish full-scale CNN NAS performance; the closing paragraph of section 7 says exactly that, and the readiness table stops at "what a shared-weight score or an untrained proxy actually measured".
- From [the meta-learning note](../../topic-notes/automl-as-meta-learning.md): cross-task portfolios, warm starts and learned recommenders belong to that topic. Section 6 therefore keeps the constructed A/B/C portfolio as a single counterexample — best single default 0.25, complementary pair 0.10, new task reversing it, 0.40 against 0.20 — introduces the word *portfolio*, and routes the task/configuration matrix, relative landmarks, ranking and performance prediction onward by name. It also keeps the note's specific corrections visible: greedy ensemble selection is not "take the top few and stack them", and ensemble weights are not posterior model probabilities.

Both notes remain **open**. Nothing here resolves them; their destination authors still owe the assessment the note workflow requires.

### Disagreements with the packet

None. Every recorded number in `calculated-inputs.json` was reproduced independently — the observed study by refitting it from the served CSV, the constructed block by re-deriving it from the declared settings through different implementations — and all 82 trust-root values and all 33 fold accuracies agreed to the tolerances recorded in the evidence files. One detail worth a reviewer's attention rather than a disagreement: `constructed.halving.survivors` lists survivors in loss order, not index order, which is harmless but means any comparison against it has to sort first.

### Known limits carried into phase C

- No browser evidence, no screenshots, and no figure-by-figure visual pass yet. The teaching standard's visual completion check has not happened.
- The blueprint is written but unregistered, by instruction.
- Section 8's two optional programs remain unexecuted, by the decision recorded above.
- `src/learn/data/generated/navigation.js` now carries this lesson's new `readTime`. It is a reserved generated file; it was not hand-edited, `--check` reports it consistent with a fresh generation, and the increment owner will regenerate it when registering the blueprint.

## Phase three — browser verification and the visual pass, 16 September 2026

The blueprint was registered by the increment owner between phases, as
`'AutoML & Neural Architecture Search (NAS)'`. The title stays in its
parenthesised catalogue form: the owner confirmed that `track-definitions.js`
is both what `authoredBlueprints[topic.title]` resolves against and what the
stable ID is slugified from, so a rename would change the ID. Departure 1 in the
phase-two list above is therefore settled, not outstanding.

Section 8's two optional programs remain **unexecuted by design**. FLAML and
TensorFlow/KerasTuner were deliberately not installed, because resolving them
into the shared `scratch/lesson-tools` runtime can move NumPy or scikit-learn
and invalidate other completed topics' recorded outputs. What is executed
instead is their one substantive data claim: the verifier imports the extracted
`banknote_search.py` and resolves `folds[0]` to prove that neither program
touches an inspection or reserved row. The page displays no output for them and
says why; `docs/teaching/evidence/automl-native.json` records the status and the
reason. A reviewer should judge that trade, not discover it.

### Browser verification

`scripts/verify-automl-browser.cjs`, against a production build previewed on
127.0.0.1:4189: **18 cases, 31 screenshots, status passed.** It covers the header
and sequence position (26 of 39), all ten section anchors, all six
investigations under the predict-then-check contract including every contrast
and null, the five figures and their alternative states, drawn geometry at 1366,
390 and 320 px, loading closure, narrow-width layout and table stacking,
keyboard reach, completion and stable-ID persistence, and controlled import and
render failures with recovery. Every screenshot has a unique path *and* unique
bytes, checked in the verifier.

The two geometry sweeps both assert a non-empty subject set before asserting
anything about it, so neither can pass by inspecting nothing.
`sampleCurvesThroughLabels` is carried in from the bias-variance verifier
because the shared inspector reads straight `<line>` elements only; it earned
its place here, catching a collision the shared inspector reported clean.

### What the verifiers caught

Nine findings, all fixed: six "Add" buttons and four "describe this family"
buttons sharing one accessible name apiece; two long explanatory sentences set
inside SVG and overflowing their viewBox in F4 and F5, plus a third in F1; the
fit→score arrow running through "fitting rows only"; a label outside the F3 SVG;
`∂w/∂α = 0 by convention` wide enough to overlap the node labels on both sides;
the A and B trace end labels overlapping; "point mass" overlapping "b"; the
inclusive-cap label running off the left edge at a 1 ms cap; and — the one only
the copied curve sampler could see — the dashed counterfactual traces starting
exactly where the candidate letters sat, travelling 2.8% of their length through
them, over the 2% backplate allowance.

### What only looking caught

Every screenshot was opened and read. Twelve further defects, none of which any
verifier flagged:

1. **F3 said "Three other candidates" and then listed seven.** The figure took
   the most-widely-shared mistake rather than the runner-up score. Seven
   candidates miss development row 349; only three *tie* at 0.998907. The tied
   set is now computed in the model layer as `tiedRunnersUp`, with
   `sharedByTied` and `tieExplainedByOneMistake`, and the verifier asserts that
   the two sets are different and must not be quoted for one another. This was
   the most serious finding of the pass: the page was making a false statement.
2. **A grading bug in the mixture.** In the commitment fixture both architecture
   gradients are exactly zero — the table says `exactly 0` on every row — yet
   answering "None, the gradient is exactly zero" was marked wrong, because
   `descendId` took an argmin over a vector of zeros. A learner reasoning
   correctly from the displayed table was graded wrong. The model now reports
   `zeroGradient` and `zeroGradientReason`, distinguishing the two ways it
   vanishes (identical outputs, or the mixture already on the target), and
   returns a null `descendId`.
3. **The Pareto cap contradicted itself at the boundary.** Candidate D sits at
   exactly 5 ms under a 5 ms cap. The applied rule, the table and the model
   grid-sweep all call it feasible; the shaded infeasible band began *at* the cap
   line and visually swallowed its marker. The band now starts strictly to the
   right of the line, the line is drawn over it, the legend and a caption state
   that the cap is inclusive, and the browser verifier asserts that every
   feasible marker is drawn at or left of the band's left edge and every
   infeasible one inside it.
4. **"inspection rows, once" floated directly above the Reserved rows box**,
   inviting exactly the misreading F1 exists to prevent. Every row source now
   sits inside the node it feeds, and the reserved box carries "no arrow in or
   out" inside itself.
5. **Every box in F5 truncated to an ellipsis** — "fresh initializa…", "train
   supernetwo…" — so the diagram degraded to cut-off words. The lanes now carry
   short `stepLabels` for the drawing and full `steps` for the prose and text
   equivalent, and the model verifier checks each short label against the box
   width the component will give it.
6. **Figure buttons rendered as unstyled light-grey system buttons** on the dark
   page: the control styling was scoped to `.am-investigation`. Now shared with
   `.am-figure`, with a pressed state that is more than a colour change.
7. **A logit field displayed `0.69314718055994…`**, longer than its own
   six-decimal control accepts and wider than its box. `NumberField` now displays
   at its own resolution, and the browser verifier asserts that no displayed
   number carries more decimals than its control will take.
8. **The mixture bars varied in width and height while the caption declared only
   width.** Height is now constant.
9. **The replay opened with applied and proposed budgets both 3**, so its first
   question was "moving from 3 to 3" and the answer was "nothing changes". The
   specification says the initial available budget is 2 and the proposed budget
   is 3. `useInvestigation` gained an optional `initialDraft`, and the opening
   state is now exactly the comparison section 5 walks through.
10. **The halving plot was dominated by nine dashed counterfactuals**, so
    "purchased evidence stops early" was hard to see, and the 0.07-against-0.02
    contrast the prose turns on was not perceptible. The counterfactual overlay
    is now a deliberate reveal, off by default; the two lettered ends carry their
    actual losses; and the editable curve table lost its redundant per-cell
    labels, which had repeated every column header nine times.
11. **The eleven-row dot plot in F3 was illegible at 320 px.** It now has a wide
    label gutter, full-size row labels and its own max-width, so the labels are
    neither squashed on a phone nor magnified in a desktop column.
12. A missing space in the F1 caption (`selection` immediately followed by
    `can`), and F5's kernel table row headers differing from its column headers
    only by subscript styling.

### Verifier changes made during this pass

Two assertions were weakened by scope and one could never fail. The
"nothing revealed on first paint" checks were scoped to `.am-investigation`,
because a figure is allowed to show its own content and F5 legitimately prints a
determinant; an `.am-ledger` check that matched a class no component used was
replaced by per-investigation assertions that every investigation shows at least
fifteen controls and exactly zero verdicts, readouts, rungs or SVGs before a
prediction — which can fail, and does if a drawing escapes its commitment guard.
Added: accessible-name uniqueness inside the grammar, displayed-number
resolution, the cap-boundary geometry assertion, provenance box fit, the
zero-gradient verdict, the tied-set/shared-mistake distinction, and every value
the practice text tells a learner to type checked against its control's step and
range.

### Final verifier results

| Verifier | Result |
| --- | --- |
| `verify-automl-models.mjs` | **316 grouped checks across 61 groups** |
| `verify-automl-data.py` | **289 checks**; 35 fits recomputed, 82 trust-root values re-derived, module byte-identical |
| `verify-automl-examples.py` | **104 oracle assertions**; 1 of 4 displayed blocks executed, 35 fits |
| `verify-automl-browser.cjs` | **18 cases, 31 screenshots, passed** |
| `generate-learning-artifacts.mjs --check` | 0 stale files |
| `verify-curriculum.mjs` | 28 modules, 1,218 topics, passing |

A standing escape audit over this lesson's own math payloads is folded into the
model verifier: 99 payloads, 146 LaTeX commands, every backslash intact, no
`\widehat` anywhere, and the narrow accent used where a hat is meant. No display
equation overflows 320 px on the real page.

### Still open

- Section 8's two optional programs remain unexecuted, by the decision above.
- SVG text in the inline figures renders at about 8.4 px at a 320 px viewport.
  The browser verifier asserts a floor of 8 px; every figure's numbers are also
  present in an adjacent stacking table, so nothing depends on reading the
  drawing at that width.
- An independent review has not yet happened.

## Disposition of the independent review, 16 September 2026

The [independent review](../../AUTOML-NAS-INDEPENDENT-REVIEW.md) refitted the whole 35-fit campaign from the served CSV with its own implementation — 97 field comparisons over 10,109 out-of-fold labels, 410 inspection predictions, 1,372 role ids and 2,757 fold ids — and re-derived all 149 leaves of the packet's `constructed` block from the manuscript's declared settings in exact rationals and 60-digit `mpmath`, each by a second genuinely independent route. It found **no numerical disagreement anywhere**, confirmed that across 72,427 grid points every drawn rule matches every applied rule, and confirmed the lesson's central caution: the shipped data module exposes 119 rows, every one legitimately seen, and no reserved row. It raised three blocking findings, eight to fix and twelve observations.

| Finding | What was wrong | Resolution |
|---|---|---|
| B1 | A zero-length step is typeable, and "nowhere at all" was graded **wrong**; the verdict then printed identical before-and-after logits, output and loss while asserting that a weight increases | The verdict follows whether the displayed output actually moved — `stepMoved`/`stepOutcome`/`stepUnchangedReason` — rather than enumerating the reasons it might not have; the explanation is conditional on a step having been taken, and names which of the two things was zero |
| B2 | I6 shipped A over an identical E, against a `selectionRule` that existed in the model layer and was **rendered nowhere**, and captioned it "A has the greatest accuracy", which its own table refuted | The rule is stated in the investigation's note before the task; `paretoAnalysis` returns `tiedOnAccuracyIds`, `selectionTied` and `tieBrokenBy`; the readout names the tie and the rule that settled it, and the verdict explains the tie to a learner who picked the other tied candidate |
| B3 | I1 displayed `2 × 2 = 4`, `2 = 2`, `1 × 2 = 2`, `1 × 1 × 3 = 3` from the draft space on first paint, so both the graded direction and the exact total were readable before any prediction | Each branch shows only how many dimensions it has until a prediction is recorded; the permitted values stay visible, so the task is still answerable, and a caption says what is being withheld and why |
| S1 | The escape audit's regex matched single-quoted payloads only, so it skipped the three template literals — the only interpolated payloads, carrying exactly the `\begin{bmatrix}` escapes it exists to protect | Both quotings are matched, and the count is asserted equal to the number of `<Math>`/`<MathBlock>` openings in the file, so a payload the pattern cannot read fails instead of being silently omitted |
| S2 | The "8 px SVG floor" read `getComputedStyle().fontSize` in SVG **user units**, so it was scale-invariant, recorded 10.5 px at both 390 and 320 px, and passed vacuously on an empty set (`Math.min(...[])` is `Infinity`) | Measured in rendered pixels as user size × box-to-viewBox ratio, with a non-empty guard; `is-small` was raised from 10.5 to 11 px, and the evidence records the true minimum and median per width — now 9.75/11.32 at 390 px and 7.48/8.91 at 320 px, two different numbers, as they must be |
| S3 | The cap-boundary guard used `<=`, which accepts the exact regression it was added for: a band starting *at* the cap line rather than past it | Strict `<`, plus a direct assertion that the band's left edge is greater than the cap line's x, plus non-emptiness of both the feasible and infeasible partitions. Falsified: removing the `+ 1.5` now reports "198 versus 198" |
| S4 | F4 printed `0.020000000000000004` where its own box and table print `0.02` | Formatted at the figure's own resolution, and a page-wide sweep of every visible text node now catches over-precise prose, not only `input[type=number]` values |
| S5 | Three model-verifier checks could not fail: one compared a helper to itself, one restated a ternary (and used filter-then-`every`), and a 15-case sweep compared two of the verifier's own transcriptions of a control step | The self-comparison is deleted (its genuine second route already sat on the next line); the weighted density is recomputed from each point's own loss and density with both partitions required non-empty; and `controlSteps` is now exported from the model layer, rendered by the components and consumed by both verifiers, so the check compares the prose against the page |
| S6 | "82 trust-root values re-derived" was a hand-maintained counter that double-counted and credited checks reading no packet key; actual leaf coverage was 78 of 149, and `halving.selected` was asserted by nothing | Replaced by a set of packet leaf paths actually asserted, reported as coverage and **asserted complete**. `halving.selected` and `discretization.probabilities` are now re-derived. 107 of 149 leaves are re-derived here; the other 42 are declared *inputs*, which cannot pin themselves, and are pinned instead against the manuscript's own printed tables (S-O5) |
| S7 | F3's key caption measured 829 px inside a 745 px scroller at desktop, clipping the sentence that disambiguates its two numeric columns | Captions moved outside the scroll box into a `<figcaption>`, so they wrap to the container rather than the table; the check now runs at 1366 px as well as 390 and 320 |
| S8 | `halvingSchedule`'s guard admits three or more curves, but three or four emptied the field at the second rung and threw a bare `TypeError` | Each rung keeps at least one candidate; the model verifier now drives every curve count the guard admits, from three to nine |

### Observations acted on

O1 — the public `ATTRIBUTION.txt` had every number right and **nothing asserted any of them**; sixteen claims in it are now checked against the data itself, including both digests, the row, distinct-vector, repeated-vector and extra-row counts, the licence, DOI, archive member and the "curtosis" spelling note. O3 — `sources.filter(fs.existsSync)` silently dropped a declared source file that no longer existed and still wrote `passed: true`; a missing source now fails. O4 — the dead `am-grid` skip is documented and the sampler's real coverage stated honestly: it covers the investigation plots, where the curves are, and the figures contain only straight lines that the shared inspector reads. O5 — the manuscript's prose numbers were checked against nothing; the halving table, the surrogate table, the portfolio matrix, the deployment points, the eleven observed means with their parameter counts and the role table are now parsed out of `lesson.md` and asserted, 77 checks. O6 — `author_checks` and `tie_rule` were read by nothing; the claims that this run independently establishes are now asserted against it. O7 — the departure-12 arithmetic slip is corrected: **seven** classical-ML destinations plus two out-of-module, nine internal targets in all. O10 — F4's dependency labels sat flush against both node edges; they now sit above the row with clear space.

### Observations recorded and left

O2 — the archive SHA-256 remains unfalsifiable locally, because the ZIP was deliberately not retained; everything downstream of the extracted member is checkable and is checked. O8 — the 320 px type mitigation stands as the reviewer judged it: every figure's numbers are duplicated in an adjacent stacking table and in the SVG `aria-label`, and it was the *claim* about it that needed fixing, which S2 did. O9 — the §8 trade is endorsed and unchanged; both programs remain unexecuted by design, with the reason recorded and their one substantive data claim executed for real. O11 and O12 — the two discarded leads and the departures audit need no action. A2 — the one-ulp `pooled_oof_accuracy` difference is an association-order artifact in the reviewer's own arithmetic, not a defect, and is recorded here so the next reviewer does not spend time on it.

### Every repaired guard was falsified

The review's standing instruction was that a repaired guard must be shown to go red against a deliberately broken input. Sixteen breakages were applied one at a time, each verifier re-run, and each file restored byte for byte; both harnesses confirm green before and after. **All sixteen fired.** S1 caught the narrowed regex; S5's weighted density caught a 1% perturbation; S5's step map caught a changed step; S8 caught the unclamped rung; B1 caught the reverted ternary; B2 caught the unconditional readout; O3 caught a phantom source; S6 caught a removed coverage mark; O1 caught a wrong vector count in the attribution; O5 caught a drifted manuscript digit; B3 caught the restored branch counts; S3 reported "198 versus 198"; S2 read 3.88 px where user units would still have read 11; S7 caught a clipped caption; the step map caught an unmapped `step="0.07"`; and S4 caught `0.020000000000000004`.

S4's guard was **inert on its first attempt**, and that is worth recording: it had been placed in the keyboard section, which runs after the figure toggles, so the over-precise value was no longer on screen when it looked. It was moved to the untouched page and then fired. A guard can be correct and still blind if it runs at the wrong moment.

### Fixing the class rather than the instance

B1 and B2 were each the second instance of a class the Phase C pass had fixed once. The general repair is a sweep that asserts the *property* at degenerate inputs rather than the example: 80 step cases across five step sizes, four inputs and four targets assert that `stepOutcome` is `'stay'` exactly when the printed output did not move, whatever the reason; 30 frontier cases assert that `selectionTied` equals whether more than one feasible candidate holds the greatest accuracy and that the readout never claims uniqueness when it does not hold; and the sibling tie rules in I2, I3 and I5 are asserted rather than assumed. On the browser side, the container-enumerating guard is replaced by one that asks whether the answer string is on screen: fourteen distinctive renderings of graded quantities across all six investigations, none of which may appear before a prediction. Finding two of those needles were themselves too loose — "on the frontier" appears in I6's own question, "softmax weights" in I4's explanatory prose — was the check working as intended.

### Final verifier results

| Verifier | Result |
|---|---|
| `verify-automl-models.mjs` | **329 grouped checks across 65 groups** |
| `verify-automl-data.py` | **414 checks**; 35 fits recomputed, 107 of 149 trust-root leaves re-derived and 42 pinned against the manuscript, 77 manuscript checks, 16 attribution claims, module byte-identical |
| `verify-automl-examples.py` | **104 oracle assertions**; 1 of 4 displayed blocks executed |
| `verify-automl-browser.cjs` | **21 cases, 34 screenshots, passed** |
| Falsification harnesses | **16 of 16 repaired guards fire** when their own breakage is applied |

`generate-learning-artifacts.mjs --check` reports 0 stale files and `verify-curriculum.mjs` passes at 28 modules and 1,218 topics. Every screenshot was opened and read again after the repairs; the informative states now include the grammar *before* any prediction, the zero-length step, and the exact accuracy tie.
