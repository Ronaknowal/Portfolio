# Multi-Head Latent Attention — authoring record

Research/write complete, 13 September 2026; implementation not started. Actual content preflight consumed; no individual destination note or blueprint exists. The resolved unrelated bit-manipulation routing history adds no scope. Full original published source read beginning to end; SHA-256 `2b8ca2d831ebd2704cbbde34de240151ddd2ba971613d73aefd1fe7f871ad732`. Source/runtime remains unchanged.

## Experiment declaration, before fitting

The primary question is whether one trained MLA predictor can execute exactly through reconstructed heads, an absorbed latent cache, and incremental causal steps, then how an explicitly lossy reduction differs. Use the verified CC BY 4.0 UCI Libras Movement originals and the preceding seed-73 exact-duplicate-safe whole-trajectory row split: 220 training, 50 validation, 60 test. Reuse the source-bound data evidence and fixed transform `2*x-1`; no new download or fitted test normalization. Forecast the next coordinate from the observed causal prefix, inputs 0–43 to targets 1–44. Performer/session IDs are unavailable; this is a row-level diagnostic, not a new-person evaluation. It is not a direct architecture ranking against earlier, differently shaped models.

Train one model, seed 131: a 2-to-24 stem, one pre-norm causal block, four query heads, content Q/K width 4, value width 4, rotary width 2, normalized KV latent width 8 and normalized query latent width 12, FFN width 48/GELU and a final two-coordinate forecast. Residual pre/final LayerNorm epsilon 1e-5; latent RMSNorm epsilon 1e-6, trainable scales, no bias on attention projections, bias on stem/FFN/forecast. Adjacent-pair ordinary RoPE base 10000, no YaRN, no dropout or weight decay. Train 200 full-batch Adam updates at 0.003; select minimum validation MSE, earliest exact tie. Report original-coordinate RMSE `sqrt(transformed MSE)/2`. Persistence and a training-fitted affine coordinate map provide simple baselines.

After checkpoint selection, form the joint output-by-latent matrix by stacking all content-key and value up-projections. Its right singular vectors define a predeclared rank-4 post-normalization projection. Compare the full selected model with this lossy cache intervention without further optimization or test-driven rank selection. Also use the complete rank-8 basis as an exact coordinate-change control. The reduced cache still computes the original eight-coordinate normalized latent for each new input before projection; it is not a separately trained four-coordinate RMSNorm architecture. Save singular values, parameter-space reconstruction error, aggregate outcomes and actual model traces.

Predeclare source row 77 and its first 32 observed points. Save reconstructed/absorbed/full-cache equivalence, all-parameter float64 derivative equivalence on a small input, a common-position-shift null, changed observed point 23, the wrong softmax-scale contrast, and full/rank-4 actual forecast traces. Keep the original score scale `sqrt(content_width + rotary_width)` after absorption; a library default based on latent width changes the operator. No GPU timing, fitting extension or invented architecture win. Numerical fixtures supply independent hand geometry, matrix reassociation, rotary noncommutation and byte/work counts. Whole manuscript/specifications and full author checklist must be completed before readiness.

## Scope, title and sequence

Retain the stable catalogue title and ID. The learner-facing subtitle explains the mechanism without changing navigation. Previous GQA/MQA owns query versus stored-head counts and causal cache dependencies; preceding Positional Encodings owns rotation geometry and extension protocols; Transformer Block owns residual/norm/FFN state. This lesson refreshes every prerequisite needed locally, then owns a precise V2/V3-style latent representation, decoupled rotary score, exact key/value reassociation, correct scale, compact/expanded cache distinction, actual derivative equivalence, rank/capacity limits and meaningful conversion/deployment boundaries.

The next Sparse/Linear Attention lesson owns sequence-level sparse/approximate operators and their current variations. MLA's latent-coordinate rank does not imply low-rank softmax or linear sequence complexity; the exact counterexample provides the boundary. The forthcoming MoE lesson owns expert selection/dispatch; here it receives a scoped attention-versus-FFN connection. Later cross-attention owns source/target alignment, which this page motivates without importing an inappropriate self-attention position convention. No prerequisite detour or catalogue reorder is needed.

Current FlashMLA documentation includes a 10 September 2026 V4.1 release and newer sparse/compressed formats. Root was notified of the exact source and next-owner boundary. This manuscript explicitly teaches the V2/V3 core, includes one source-bound V3.2 mixed-precision record to demonstrate real metadata, and routes the newer sequence/sparsity extension to this author's next Sparse/Linear topic. It does not portray V4 as hypothetical or claim every present MLA model has a 576-element record. No topic-wide audit or new unowned essential curriculum item was discovered.

## Original-source conservation and consequential corrections

The entire original published JSX was read before authoring, including eleven sections, complete code, figures, benchmark claims and self-check. Useful depth is retained through the following dispositions:

| Original coverage | Retention, correction or reasoned replacement |
|---|---|
|KV-cache motivation and MHA/GQA/MQA history|Retain compact historical bridge and full cache dependency. Remove universal cache dominance, impossible device-fit arithmetic, proprietary adoption speculation and fixed quality-loss claims already contradicted by primary evidence|
|Shared low-rank KV representation and per-head reconstruction|Retain shapes and complete mechanism; correct latent-width arithmetic, distinguish one-head versus all-head width, and explain learned coordinates rather than semantic/probability assumptions|
|Query compression|Retain independent query latent and training-activation rationale; correct 1536/512 being described as 1.5 instead of 3, and remove unconditional peak-memory/no-overhead claims|
|Key/value absorption|Retain full proof and runnable expanded/absorbed paths; preserve original softmax scale, independent head mixtures and optional output-map fusion. Correct claim that wider latent dot products are inherently cheaper or cache ratio equals FLOP reduction|
|Decoupled RoPE|Retain concrete two-branch formula and full positional dependency; add editable commutator and consistent-position null. Avoid universal noncommutation, impossible-order-learning and automatic long-context-quality claims|
|Latent normalization|Correct LayerNorm to source-supported RMSNorm, show local formula and cached post-normalization state. Remove unsupported mandatory-FP32/guaranteed-NaN claims and explain what cannot commute through nonlinear normalization|
|Implementation and GPU stdout|Replace incomplete benchmark methods/unsupported CUDA stdout and synthetic copy-task assertions with complete executed NumPy and CPU trained-model programs. Preserve full forward, training, caching, interventions and meaningful gradients without requiring a GPU|
|Memory plots and comparison heatmaps|Retain exact dimension-derived counts but label bytes/KiB/MiB/GiB correctly; distinguish paper-style plain-MHA dimensions, literal same-MLA-function expanded cache and compact cache. MQA 256 is smaller than MLA 576 in that stated example. The source headline 93.3% has a different whole-model baseline|
|Quality/cache frontier|Remove fabricated composite MMLU-like points, invented latency crossover and universal quality-safe 50–100× claims. Retain actual Appendix D scope and a counterexample metric; the complete local table reports real full/truncated outcomes and simple baselines|
|Production/library discussion|Read official current code/configurations and scoped kernel documentation. Distinguish runtime contraction from export premerge, naive buffers from compact buffers, prefill from decode, API mode from trained architecture, data precision from cache format and measured workload from formula|
|Backward/training stability|Remove false instability attributed to absorbed-form normalization statistics. Supply actual same-objective float64 gradients for every model parameter, with numerical precision and stale-detached-product caveats at their home|
|Conversion decision|Replace “cannot initialize usefully/requires trillions of tokens” with actual ACL 2025 partial-RoPE/joint-SVD evidence, a bounded factorization explanation and explicit adaptation/variant limits. No guarantee of lossless conversion or reproduction claim|
|Scaling and application recommendations|Keep capacity, long-cache, streams, encoders, cross-attention, sparse/MoE, quantization and deployment connections. Remove universal small-model/short-context bans, free-head claims, single-server assurances, multiplied throughput/pricing stories and “only sensible combination” assertions|
|Failure modes and self-check|Place each caution beside its mechanism and replace answer-visible recall with nine changed transfer problems, initially closed hints/solutions and explicit first-pass readiness|
|Primary references|Correct attention ablation location to Appendix D/Table 9 and full formula location to Appendix C. Preserve useful primary origins without endorsing invented historical timings or uninspected video content|

Additional useful depth is driven by actual hurdles: post-normalization cache semantics, invertible coordinate basis versus loss, nonlinear-map obstruction, rank-one logits with full-rank softmax, input-sensitive SVD truncation, exact all-parameter derivative checks, wrong-scale coincidence when dimensions match, and one real compact record with precision metadata. None is included merely to add a mandatory advanced section.

## Canonical section-list coverage

The canonical source is the actual DeepSeek-V2 report v5, rather than the original lesson's misnumbered summary. Full table of contents inspected:

- §1 Introduction.
- §2 Architecture: §2.1 MLA with 2.1.1 Standard MHA, 2.1.2 Low-Rank Key-Value Joint Compression, 2.1.3 Decoupled Rotary Position Embedding, 2.1.4 Comparison of KV Cache; §2.2 DeepSeekMoE with Basic Architecture, Device-Limited Routing, Auxiliary Loss for Load Balance (expert/device/communication), and Token-Dropping Strategy.
- §3 Pre-Training: §3.1 Experimental Setups (Data Construction; Hyper-Parameters with model/training; Infrastructures; Long Context Extension); §3.2 Evaluations (benchmarks, results, training/inference efficiency).
- §4 Alignment: SFT, RL with algorithm/strategy/training efficiency, Evaluation Results, Discussion with SFT-data amount/alignment tax/online RL.
- §5 Conclusion, Limitation, and Future Work; References.
- Appendix A Contributions/Acknowledgments; B DeepSeek-V2-Lite with model description/architecture/training and performance evaluation; C Full Formulas of MLA; D Attention Ablations with MHA/GQA/MQA and MLA/MHA comparisons; E Data Debiasing; F Math/Code Evaluations; G Evaluation Formats.

All §2.1 and Appendix C mechanism ideas are taught locally with independently implemented calculations. Relevant §3.1.2 practical RMSNorm/configuration details and §3.1.4 position/training distinction are incorporated. Appendix D's actual task/protocol and metric limitations replace the false composite plot; large-model training is cited evidence rather than a claimed reproduction. Appendix B's smaller model context prevents a universal small-model prohibition; no need to retell its complete benchmark table.

The full MoE subsection belongs to the later dedicated MoE owner. Pretraining corpus construction, alignment/RL, full benchmark protocols, expert parallel deployment, data-bias evaluation and complete model-release history are other curriculum homes, not omissions from understanding this attention mechanism. Their section-list disposition is explicit; this page provides necessary local boundaries and links rather than duplicating an entire language-model training course. No core readiness is hidden inside an optional source link.

## Hurdle map and representations

| Learning hurdle | Explanation/evidence | Representation or action |
|---|---|---|
|Shared latent means identical heads|Different head-specific maps and actual distinct outputs|Input/latent/head branch diagram and linked hand calculation|
|Compressed coordinate is a semantic probability|Signed learned coordinates and invertible-basis identity|Editable latent-basis view with exact compensated null|
|RoPE can pass through any projection|Explicit unequal stretch/rotation counterexample and special commuting cases|Two geometric operation paths with independent matrix/vector equality feedback|
|Content and position require separate softmax|They add before one scale/mask/normalization|Two signed score rails meeting before one distribution|
|New feature width gives a new default scale|Algebra preserves original score; real wrong-scale difference and equal-width null|Exact expanded/absorbed workspace and actual model contrast|
|Cache 57× smaller means FLOPs 57× smaller|Cache axis count versus two actual dense-core formulas|Record-building investigation plus independently labelled arithmetic panel|
|Same matrices imply same stored buffers|Official naive/compact cache branches and actual allocated tensors|Persistent versus transient storage brackets|
|SVD-optimal implies prediction-optimal|Discarded small singular direction can carry the entire actual input|Editable 2×2 rank/input geometry and real frozen-model truncation|
|Absorption must have different gradients|Same function and exact chain rule; all-parameter derivative check|Complete code/provenance, no redundant generic gradient lab|
|Low-rank logits imply low-rank softmax|Actual rank 1 → rank 3 counterexample|Static full matrices and determinant/rank explanation|
|Changing historical coordinates leaves cache valid|Actual earlier-output causal null with changed later forecasts|Real prefix/latent workspace with input-bound cache invalidation|

Five distinct investigations are fully specified with meaningful edits, unset predictions, computed controls/nulls, accessible/mobile counterparts and bounded runtime. Visuals explain mechanisms before deeper systems details. Complete transparent NumPy and full native study programs support the text; short code is not a placeholder for missing implementation.

## Research actually inspected

Research date 13 September 2026. Technical facts rely on primary papers/code/documentation; alternative resources supplement rather than replace local teaching.

- [DeepSeek-V2 v5](https://arxiv.org/html/2405.04434v5): full real section list; complete §2.1, §3.1.2 model/training settings, infrastructure/context-extension portions, full Appendix C and D tables/methods. Actual RMSNorm wording appears in §3.1.2 even though a literal “RMSNorm” search did not find the spaced “RMS Norm.” The abstract baseline identifies the 93.3% whole-model comparison. Small-MoE C-Eval 51.6→50.9 is a concrete counterexample to universal quality preservation. Appendix A is acknowledgments, not implementation.
- [DeepSeek-V3 v2](https://arxiv.org/html/2412.19437v2): relevant §2.1.1, §3.3.1 mixed precision, §3.4 prefill/decode deployment, §4.2 configuration and §4.3 extension body inspected. Normalization/attention retain higher precision; numerical training format cannot establish inference-cache dtype. Distributed deployment is workload-specific; no launch-pricing or single-server causality claim retained.
- [Official V3 inference source](https://raw.githubusercontent.com/deepseek-ai/DeepSeek-V3/main/inference/model.py): rotary function and entire MLA class constructor/forward read, including full versus compressed buffers, optional direct Q, latent RMSNorm, shape slices, original scale/YaRN modification, runtime key contraction and latent-value up-projection. Exact online branch is mutable; phase-two reproduction pins it. No official model weights were downloaded or run.
- [Official V2 configuration](https://huggingface.co/deepseek-ai/DeepSeek-V2/raw/main/config.json) and [V3 configuration](https://huggingface.co/deepseek-ai/DeepSeek-V3/raw/main/config.json): complete files read, including hidden width/layers, q/kv ranks, content/rotary/value widths, norm epsilon, rope scaling and precision metadata. V2 width 5120 / layers 60 differs from V3 width 7168 / layers 61; shared attention widths do not make entire architectures identical.
- [vLLM 0.20.0 MLA source documentation](https://docs.vllm.ai/en/v0.20.0/api/vllm/model_executor/layers/attention/mla_attention/): extent definitions, vector/matrix shapes, expanded compute-friendly path, latent data-movement path and chunked-prefill algorithm read. The opening ratio wording is reversed; pseudocode also contains shorthand/typos and does not show an explicit preserved scale. Independently derive the correct T/L interpretation and operator rather than copying those errors. No vLLM/native GPU execution.
- [FlashMLA official README](https://github.com/deepseek-ai/FlashMLA): current introduction/news, dense/sparse model capability table, decoding API and full documented FP8/FP4 cache-format/indices sections read. The date-specific V4.1 release and changed widths are routed to the next variants owner; the local 656-byte V3.2 example is format-specific. No benchmark claims imported or installation performed.
- [MHA2MLA ACL 2025](https://aclanthology.org/2025.acl-long.1597.pdf): background and core architecture, full partial-RoPE high/low/uniform/contribution-aware selection, split/joint SVD treatment, declared SmolLM/Llama experiments and initial commonsense-results table/interpretation inspected. Published version differs from the earlier arXiv abstract percentages. Shape/frequency shorthand in the PDF is not copied as universal exact implementation. The manuscript cites conversion existence/method boundaries without promising those results. Official ACL record inspected; it supplied no verified video link, so none was invented. An attempted arXiv-v3 HTML route was unavailable; actual publisher PDF supplied the substantive evidence.
- [Raschka's MLA guide](https://sebastianraschka.com/llms-from-scratch/ch04/05_mla/): complete short body read for the compact GQA→latent-cache conceptual recap. Gallery adoption statements are not used as sole evidence of model internals or current state-of-the-art status.
- [3Blue1Brown attention adaptation](https://www.3blue1brown.com/lessons/attention/): earlier substantive written Q/K/softmax/value evidence reused and clearly annotated as a prerequisite visual/video option, not a new MLA lecture inspection.
- [UCI 181](https://archive.ics.uci.edu/dataset/181/libras%2Bmovement): prior source-bound license/full original metadata evidence reused with byte-exact topic-owned copies. New forecast/latent-rank protocol and all results are independently recorded here.

## Author checks and learning-experience review

The one declared fit completed, selected at update 200; no extension/retuning followed. Full and rank-4 outcomes, full-basis/null controls, expanded/absorbed/incremental equality, causal edits, wrong-scale contrast and all-parameter float64 derivative comparison passed. The independent NumPy fixture program also completed, with actual geometry/rank/cost evidence. No hardware timings or benchmark curves were fabricated.

The complete manuscript, specifications, provenance and design were reread for mathematical continuity, evidence boundaries and learner flow. The displayed NumPy program was separately extracted and executed: both equivalence checks print `True`, with the stated head and final outputs. Three JSON documents parse; all four Markdown files pass control-character and relative-link checks; nine exercises have eighteen initially closed hint/solution disclosures; all five unique local topic routes exist in generated navigation. The source JSX and both original data hashes remain unchanged. Eleven required files are retained. A first route-check helper searched the wrong catalogue directory; it was corrected to the actual generated navigation before recording the passing result. No model rerun was needed for prose spacing or this helper correction.

1. **Route:** the introduction establishes the problem before terminology, then gives an immediate first-pass path through the full core mechanism and real example. Deeper rank/conversion/deployment material is optional without hiding core readiness.
2. **Cautions:** positional, scaling, normalization, approximation and performance limits live beside the corresponding mechanism. They do not become repeated blocks or code-printed warnings.
3. **Real question and data:** observed, openly licensed trajectories support a concrete causal forecast and a declared post-normalization intervention; duplicate boundaries, selection, baselines and unavailable performer IDs are explicit. The small gain and substantial truncation loss remain visible.
4. **Investigations:** five distinct contracts allow genuine vector, matrix, position, dimension or observed-coordinate edits with input-bound, initially unanswered predictions. Exact nulls, unequal-width scale contrast and the rank-four coincidental null are computed rather than prescribed outcomes.
5. **Perceptible figures:** branching representations, two score rails, operation-order geometry, storage/work accounting, singular directions and actual trajectory/cache traces answer different questions. Mobile/text equivalents and bounded execution are specified.
6. **Connections and coverage:** actual prior/next routes connect GQA, positions and self-attention to MLA, then to sparse/linear attention and MoE. The canonical section-list audit preserves mechanism depth while routing full expert/alignment/system subjects to their owners.
7. **Code and core understanding:** complete runnable NumPy and trained CPU programs define all projections, norms, scales, masks, cache fields, selection and interventions. Actual full-network and all-parameter derivative comparisons substantiate exactness; hardware performance is not invented.
8. **Changed practice:** nine new problems require calculation, diagnosis or experimental design. Hints and explained exact solutions are initially closed; later problems extend the advanced branches.
9. **Rendered experience:** browser, keyboard/screen-reader, narrow-viewport, visual parity and formal independent phase-two review are explicitly deferred. Written specifications and native author calculations are not claimed as rendered acceptance.

Focused reconciliation found that the five investigations' initial defaults reused the manuscript's already-solved examples. Corrected this by explicitly designating those as ungraded walkthroughs and supplying a different fresh gated baseline for **each** investigation, with named unsolved inputs/proposed changes and author-only expected results. The independent `practice-calculations.py` executed from existing saved weights only; `practice-fixtures.json` retains the checked changed/null results, including a 27-point prefix and frame-19 y edit. No fits were repeated. All five contracts specify separate resets and hidden-before-commit feedback; arbitrary valid edits still compute actual results. The additions were reread for consistency with the complete packet and its evidence limits.

There are no unresolved material writing gaps. Root owns the shared ledger/hash checkpoint. Next authorized action for this packet is implementation from all thirteen retained files, including the specified five investigations and figures, compact lazy-loaded assets, independent numerical/content review, accessibility/browser checks and integration. Do not regenerate the manuscript or rerun unchanged fits merely because another agent continues it.
