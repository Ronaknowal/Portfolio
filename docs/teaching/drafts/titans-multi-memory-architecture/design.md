# Titans: research, scope and implementation handoff

13 September 2026 · stable ID `titans-multi-memory-architecture` · revision1 · content-first. Scope is module position40 of42, immediately after Jamba and before training mechanics. The [module-completion record](../../DEEP-LEARNING-MODULE-CONTENT-COMPLETION.md) owns the current batch. The central ledger owns phase status and exact final hashes.

## Scope and preflight

Actual `node scripts/build-curriculum-inventory.mjs --topic titans-multi-memory-architecture --work content` was read. The old lesson is published but individually unreviewed, frontier-level, with no explicit prerequisite IDs or saved destination note. Full original file `src/learn/data/topics/titans-multi-memory-architecture.jsx` was read in bounded ranges, including the source code, both graph datasets and all exercises/references. Baseline SHA256 is5ca5d5d9e269a3dd672adca3bc9884befacfb51d931d2e024e69afb28d6ee861 at HEAD8c5da59f18516be77c29d5aeeafca3decca4f738. Phase one does not alter that source or register a new publication.

Keep the stable title **Titans (Multi-Memory Architecture)**. Proposed explanatory page heading: **Titans: a memory that learns while a sequence arrives**. The broadened teaching covers the architecture's existing scope with necessary update/causality/evaluation detail; it does not require a catalogue rename or another topic. MIRAS is a proportionate design lens rather than an unbounded new survey. The mechanics of ordinary gradient accumulation belong to the next lesson and are linked; this lesson owns the distinct chunk-anchor versus online fast-weight convention. No unrelated catalogue audit was performed.

Local refreshers introduce keys/values/queries, loss, gradient, matrix shapes, SiLU, residual direction, attention weighting and source-target timing. The learner should be able to distinguish three state lifetimes, hand-compute a write and its effect on another query, interpret momentum/decay, trace the three integration topologies, understand outer-through-inner differentiation, distinguish gradient anchors, account for state payload, reproduce a real replay and diagnose a causal/state error.

## Coverage conservation and corrections

| Original useful intent / problem | Final treatment |
| --- | --- |
| Three memory roles and comparison to RNN/attention | §§1–2 preserve it with separate position-addressable representations, finite compressed state and input-independent parameters. No exact textual-storage or unlimited-capacity claim. |
| Surprise loss and momentum/decay equations | §§2–3 use one explicit half-squared convention, parameter gradient Jᵀr, zero-key counterexample, different residual directions and separate update components. No assumed semantic-importance or loss-to-gradient ratio equivalence. |
| Deep memory advantage and capacity probe | §4 gives shapes and an affine impossibility example. The old rehearsal/random-key capacity table and rough H-slots law are unsupported and removed; finite capacity/interference is demonstrated directly. |
| MAC/MAG/MAL | §5 corrects MAL order (memory then attention), MAC retrieved context plus attention-driven write and gated postwrite read, MAG flexible nonlinear gate. The old summed-output pseudo-MAC and task-name-to-best-variant table are removed. |
| From-scratch code and whole-model connection | Complete functional neural-memory cell; analytic scalar and nonlinear outer derivatives; complete causal MAG-topology specialization with stated simplifications; real nonlinear predictor adaptation. Do not relabel these as a full trained Titans model. |
| Real-looking scalar outputs from random/probe mismatch | Replace with executed matched copies, declared keys/queries, all intermediate arrays and controlled baselines. The old code computed all memory reads before all writes, shared batch state, used mismatched probes and did not train the outer task. Those output claims are not conserved as evidence. |
| Weight norm, recall accuracy and surprise heatmap | Replace invented points/monotonic stories with exact write traces and recorded real outcomes. No new drawn empirical curve or product ranking. |
| Production choice and scaling | §§9–10 count both fast weights and momentum and actual KV heads; separate payload from full device use, and benchmark conventions from asymptotic work. Remove unsupported platform availability, defaults, universal crossover, kernel, hardware-runtime and unpublished-Gemini claims. |
| Instability, decay, reset, leakage, tuning | §3 derives a qualified linear stability condition; §§5,7,11 distinguish causal timing and state boundaries. No universal per-parameter clipping default, no tuning on the test set, no claim that a gate alone resets state or persistent tokens guarantee anchoring. |
| Historical and reading route | Source-submission date is31 Dec2024 (Google publication entry2025), not inferred from the2501 ID. TTT is prior related work; no false subsequent Infini-attention chronology. Chronological reference dump becomes focused annotated alternatives. |
| Original five self-checks | Eight changed exercises cover old-read interference, actual reset, chunk convention, real baseline decision, outer derivative, corrected state bytes, exact momentum sign reversal and read-schedule diagnosis. Hints and explanatory solutions are closed separately. |

## Canonical agenda and actual source reading

The appropriate canonical reference is the original architecture paper, not a textbook chapter that predates the model. Its actual section list was inspected:1 Introduction;2 Preliminaries (2.1 Backgrounds);3 Learning to Memorize at Test Time (3.1 Long-term Memory,3.2 How to Parallelize the Long-term Memory Training,3.3 Persistent Memory);4 How to Incorporate Memory (4.1 Context,4.2 Gated Memory,4.3 Layer,4.4 Architectural Details);5 Experiments (5.1 Setup,5.2 Language Modeling,5.3 Needle in a Haystack,5.4 BABILong,5.5 Deep Memory,5.6 Time Series,5.7 DNA,5.8 Efficiency,5.9 Ablation);6 Conclusion;AppendixA Related Work (linear recurrence, transformer architectures, TTT/fast weights);B task datasets;C LMM as a sequence model.

The central missing headline idea was chunk-anchor parallelization, whose computation differs from sequential gradient reevaluation; it is now an explicit deeper branch and numerical investigation. The second was outer training through writes, now derived and run. DNA/time-series sections are represented in the research map, with a full real time-series application; no unsupported claim of deployment use or universal victory. Theoretical state-tracking expressivity is signposted without pretending to supply an uninspected proof. A general complexity-theory chapter would be a different scope. AppendixC's half-loss convention and linear delta-rule connection are explained by this packet's own linear update/interference derivation. General histories of LSTM, NTM, RMT, LongMem and hardware kernels are not prerequisite detours; their useful compression and state concepts remain taught here or in preceding architecture lessons.

| Source / locator | What was inspected; claim it supports | Disposition / nuance |
| --- | --- | --- |
| [Titans v1](https://arxiv.org/html/2501.00663v1), §§1–4, equations8–31; §§5.1–5.9, Tables1–5; AppendixA–C | Actual agenda, main mechanism/integration/parallelization prose and equations, experiment scope/tables and appendix comparisons were read. | Use as primary specification, with own shape-consistent math. Paper shorthand has factor/index/wording inconsistencies; parameter gradients follow optimized weights, not the prose's casual input-gradient wording. Chunk derivation needs an explicit anchor convention. |
| [arXiv version record](https://arxiv.org/abs/2501.00663) and [Google publication](https://research.google/pubs/titans-learning-to-memorize-at-test-time/) | Submission history shows v1 on31 Dec2024; Google entry uses2025. | Resolve date explicitly. No larger-model/checkpoint availability assertion is derived from the publication date. |
| [TTT v1](https://arxiv.org/html/2407.04620v1), §§2.1–2.4, Figure6 pseudocode, equations3–6 and mini-batch discussion | Read relevant self-supervised views, inner/outer parameters and online versus batch/chunk gradient discussion. | Provides a second explanation of differentiation and anchors. Full benchmark suite was not reproduced. |
| [Google Titans/MIRAS article](https://research.google/blog/titans-miras-helping-ai-have-long-term-memory/),4 Dec2025 | Conceptual memory, momentum/forgetting and four design-choice sections, experiment overview. | Dated alternate article, not authority for literal important/permanent-memory metaphors or universal benchmark statements. Primary equations control the numerical lesson. |
| [TTT2020 project](https://yueatsprograms.github.io/ttt/home.html), introduction/method/online explanation | Substantive description of rotation self-supervision, shared feature updates and available test input; official talk link. | Distinct interesting vision application and video companion. Talk destination fetch failed; did not watch video or inspect oversized slides. No timestamps or claims of full video review. |
| [PyTorch2.14 grad](https://docs.pytorch.org/docs/2.14/generated/torch.autograd.grad.html), parameters create_graph/inputs; [no_grad](https://docs.pytorch.org/docs/2.14/generated/torch.no_grad.html), class semantics | Versioned function bodies/semantics were inspected after stable links redirected. | Supports local differentiation and graph-lifetime modes. Own finite difference and replay check actual execution. |
| [UCI Bike Sharing](https://archive.ics.uci.edu/dataset/275/bike+sharing+dataset), variables/license/DOI; retained Readme | Current page and full retained provider description read. | Exact daily data identity and license; report-arrival assumption and full protocol retained separately. |

Research was checked13 September2026. Searching also surfaced video-generation TTT work; its direct project page failed, so it was not adopted as the learner-facing application. A mistaken unrelated arXiv URL opened during a follow-up was discarded as irrelevant. The original paper PDF was available, but screenshot extraction was incomplete; no unobserved PDF visual is claimed inspected. The substantive HTML equations/figure captions informed original new specifications. No third-party repository is called an official Titans implementation or endorsed as faithful without inspection.

## Hurdle-to-representation and application rationale

The [visual specifications](visual-specifications.md) place lifetime lanes, key geometry, update-vector decomposition, nonlinear shapes, three distinct architecture junctions, outer-gradient dependencies, forecast timing and state bands beside the relevant prose. Three investigations have separate questions: associative interference; request/causal state through a gated topology; and gradient-anchor dependence. The real-study paired-dot and dated forecast figures supply data-based interpretation rather than another generic interactive panel. Shared control behavior stays consistent while each visual uses the actual entities of its concept.

The beginning maintenance-log scenario motivates recent versus compressed context and returns in the work-based architecture decision. The numerical associations let learners calculate every step. The real daily stream supplies a measurable forecasting question and a target-arrival boundary; its role and limitations are explicit. The vision self-supervision branch transfers that boundary to a different kind of available target. No example is included merely as decorative trivia.

## Author checks and phase boundary

Executed the declared real study once without tuning its configuration; all three outcomes are retained. Executed `memory_mechanisms.py` for constructed traces, payload arithmetic and outer derivatives. Executed `check_author_packet.py` for arithmetic/null/contrast/causality/state/replay/metric checks; see [author-checks.json](author-checks.json). The data/protocol/command/environment details live in [provenance](data-provenance.md). These are author research/correctness checks, not an independent implementation review.

Full manuscript/specification reread and learning-experience assessment are recorded below. Root owns final source checkpointing. Intended production destination remains the existing topic JSX, with separately named lazy topic models/labs only when needed under the code standard. Do not combine unrelated topics in a batch-named implementation file. Preserve the prepared packet until phase two consumes and source-binds equivalent content.

Phase two must implement diagrams and genuine prediction-state interactions, choose a faithful causal memory integration for the offered teaching model, verify browser arrays against retained results, assess the whole lesson independently, inspect mobile/keyboard/reduced-motion behavior, and perform scoped integration/loading checks. The manuscript already resolves its own numerical model; implementing a broader paper-level MAC model would require additional indexing choices and validation rather than pretending its segment shorthand is complete executable code. No build, browser work, runtime edit, publication, commit or deployment occurred in this content-only task.

## Completed author reread and learning-experience assessment

Root read the entire manuscript in four bounded consecutive ranges and the entire visual specification in two ranges, then inspected the focused corrections. This is the author's heuristic assessment, not a recruited learner study or an independent review. The reread found and corrected missing explicit learned-view shapes, missing explanation of the full block's causal convolution/normalization bridge, an unstated outer target in the nonlinear derivative example, compressed prose spacing, and author-facing drawing imperatives inside the learner manuscript. Specifications retain the drawing instructions; the lesson now supplies reader-facing captions and explanations. A prediction-timing ambiguity in the gated investigation was resolved by committing the candidate configuration before recording the fresh prediction and enabling replay.

1. **Route:** the opening workbench/log question leads to a concrete two-write calculation. The first-pass route separates core reading, programs and practice from outer derivatives, parallelization, accounting and research branches. Core reading does not require the later training-mechanics chapter.
2. **Cautions/voice:** finite storage/analogy limits live in §1; surprise metrics in §3; the executable specialization's omissions in §5; real-study scope in §7; payload scope in §9; research interpretation in §10. Removed repeated figure-creation admonitions from the learner captions. Code prints structured values rather than cautionary prose. Qualifiers retained elsewhere explain a distinct condition or diagnosis rather than repeating an all-purpose disclaimer.
3. **Real question:** the maintenance-log motive explains recent versus compressed state. The real rental study follows an observable target-arrival protocol and supplies every datum, forecast and metric. All matched seeds and the strong previous-day baseline are visible, including the seed7 assessment result that favors the simple baseline.
4. **Investigations:** the three labs act on keys/values/queries, token/request state and gradient anchors. Each specifies an unset prediction, committed input identity, actual consequences, changed examples, nulls, reset and invalidation. Numerical core/default/contrast/null cases were executed; control implementation remains deferred. Added changed-practice arithmetic and a fixed-Jacobian counterexample to the author checker after rereading their explanations.
5. **Figures:** the lifetime, geometry, update components, shape flow, architecture junctions, derivative path, timing and payload representations each address a different hurdle. Paired MAE dots preserve baseline crossings and seed variation; date plots retain raw observations rather than invented smooth curves. Axis/label/layout acceptance is specified, not claimed rendered. The inline reading pass confirmed that explanations remain understandable before an interactive control is operated.
6. **Connections:** explicitly connect the old-read difference to the overlap identity, scalar outer derivative to nonlinear finite differences, request-state continuation to dated adaptation, and key normalization to the derived scale condition. The next chapter owns ordinary accumulation; no change to module ordering. The canonical agenda and omissions have explicit dispositions above.
7. **Code:** complete single-purpose programs and data are supplied; the manuscript excerpts keep read→loss→gradient→write visible. The verification script carries most assertion machinery. A re-run of the expanded author checker passed; the actual study and generator bytes were unchanged, so no new training run was needed or claimed.
8. **Practice:** eight changed tasks require arithmetic, state/reset reasoning, altered anchor inputs, data-based model choice, a new outer derivative, new memory dimensions, exact momentum reversal and debugging. Every substantial task has a separately closed hint and explanatory solution. Numerical exercises include exact expected values checked in the author program.
9. **Screenshots:** none captured in phase one. The specification names the nondefault contrasts, checked prediction state, nulls and mobile/desktop figures needed in phase two. Browser/accessibility and formal independent review remain not started.

Final author state: complete manuscript, complete visual/lab specifications, resolved material content findings, 14 retained packet files and passed author numerical checks. Ready for the source-bound content checkpoint; implementation remains not-started. No separate scratch file or disposable media was created. The full packet is intentionally retained for a later authorized finish request.
