# Data and calculation provenance

Real input: Brooks, T., Pope, D., and Marcolini, M. (1989), **Airfoil Self-Noise**, [UCI DOI 10.24432/C5VW2C](https://doi.org/10.24432/C5VW2C). UCI identifies its license as [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/); retain attribution and this unchanged-file description. The original measurement context and six-column units are described in the learner manuscript.

[data-source.json](data-source.json) records the exact downloaded archive URL, retrieval12September 2026, original member name and SHA-256 values. The ZIP was read in memory; only its 59,984-byte numeric member is retained as [airfoil-self-noise.dat](airfoil-self-noise.dat). Its bytes were unchanged; the filename uses the topic's descriptive naming style. Member SHA-256 is 74c75fd71783f1e6b71f8a622b993dc592897a97cd689c5090a07147a1b097b3. No whole-book PDF, source ZIP or download helper remains.

All 1,503 rows are retained. Column order is the provider's five predictors followed by scaled sound pressure. Source row order is preserved, with zero-based array indices used in calculated split records. No imputation, filtering, scaling or feature engineering alters the data file. The UCI web variable table labels attack-angle “Binary,” but its additional variable description and actual numeric column include multiple degree values; the manuscript correctly treats it as a numeric angle rather than a binary variable. Models use the first five columns only.

The recorded study is a row-level diagnostic of the supplied collection, not an independent airfoil/new experimental run claim. Development has 1,200 rows, reserved 303; seed 41 defines the first split. Five-fold training/validation is shuffled seed 42; learning-curve prefixes are shuffled seed 44 and have 60/120/240/480/900 rows. Per-fold available training is 960. Tree random state 43; Ridge scaling fits within each subset. The separate boosting trajectory uses seeds 45/46 and 960 fit/240 monitor rows. Reserved rows have no fitted prediction or score in the saved result.

[author-calculations.py](author-calculations.py) ran once for the prespecified models with NumPy 2.3.5/scikit-learn 1.9.1 in the existing read-only runtime. [calculated-inputs.json](calculated-inputs.json) contains actual fold errors, trajectories, split IDs and finite exact-world outputs. No parameter/seed search was performed to engineer a U shape. The native calculation is content evidence; the assembled displayed code, production model export and browser implementation have not been formally verified.

The three-world sensor distribution, ±noise polynomial worlds, hidden-setting example and practice matrices are **constructed**, not measurements. The complete 8- or 32-world distributions are enumerated; noise sign draws have equal weights. The double-descent numbers are **asymptotic theoretical calculations** from the specified formula, not finite-sample simulations. Distinguish these categories in every figure. The JSON's fixture data is retained for the implementer; no need to load its full research volume in the browser.
