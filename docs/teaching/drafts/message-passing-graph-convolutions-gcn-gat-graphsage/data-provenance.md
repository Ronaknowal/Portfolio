# Real graph and computed evidence

Prepared13September2026. Source: NetworkX3.6.1 karate_club_graph, the openly distributed representation of Wayne W.Zachary's “An Information Flow Model for Conflict and Fission in Small Groups,” Journal of Anthropological Research33,452–473(1977). [NetworkX documentation](https://networkx.org/documentation/stable/reference/generated/networkx.generators.social.karate_club_graph.html) and [source](https://networkx.org/documentation/stable/_modules/networkx/generators/social.html#karate_club_graph) were inspected. The distribution's [BSD3-clause license](NETWORKX-LICENSE.txt) is retained with this derivative serialization. No source-specific conflicting restriction was found in the inspected generator. This claims the provenance/license of that distributed representation, not ownership of the original paper.

karate-club.json retains all34 node IDs, original one-based paper IDs, all78 unique undirected edges and their positive integer interaction-context counts. Node club=0 is “Mr.Hi”, club=1 is “Officer” according to NetworkX's documented after-split labels. All34 IDs are unique; no duplicate undirected pairs or original self-loops. The classroom adjacency converts each positive edge to1; original weights remain only as source information. The observed graph is not a synthetic Cora-like graph. It is one historical, socially dependent network, not34 independent survey examples.

Feature extraction used installed NetworkX3.6.1 without modifying the environment: constant1, binary degree/33, and unweighted clustering coefficient. Clustering is triangle fraction among a node's possible neighbor pairs, defined0 at degree<2. Feature values are serialized; neither reader reproduction nor the program requires NetworkX. Reproduce source by nx.karate_club_graph(), sorted nodes with g.nodes[v]['club'], g.degree(v), nx.clustering(g,v), sorted edges including weight; set paper_id=v+1 and club=int(label=='Officer'). The complete package license accompanies the derivative.

The study explicitly permits the whole graph/features for transductive fitting, while labels are partitioned with NumPy default_rng133 per class:5fit/3development/9assessment. Actual IDs are in calculated-inputs.json (10/6/18). No label is supplied as a neural feature. Fit loss reads only fit labels. All structural preprocessing is a deterministic operation on the permitted known graph, not a held-out-label fit. There is no independent-network, future-time, deployment or causal-behavior claim. The displayed assessment is inspected evidence and cannot remain a future untouched holdout.

Fixed protocol:12 fits, MLP/GCN/full-neighbor mean-SAGE/one-head GAT × seeds11/29/47; input3, hidden16, output2, ReLU;300 epochs, AdamW.02, weight decay.01, no dropout, minibatch sampling or early stopping. Parameter counts98/98/178/134. GCN bias is after aggregation. SAGE keeps separate self/neighbor matrices and does not apply the original paper's optional-for-this-variant L2 normalization; it is named precisely. GAT uses original additive score and slope.2. CPU PyTorch2.14.0+cpu, NumPy2.3.5, Python3.12.14, one torch thread. Program ran successfully once; no fitting choices were revised to force favorable results.

Baseline label propagation uses only the10fit one-hot labels,200 closed-neighborhood averaging steps with those labels clamped each step; it scores16/18 assessment. Structural MLP10/18; GCN8/18; SAGE10/11/9; GAT9/18. These are weak neural outcomes, all retained. Same-class edges67/78 is a descriptive inspected-label statistic, not a training feature. Graph dependence, tiny fit/assessment sets and poor feature expressiveness limit generalization; no confidence interval or architecture ranking is invented.

All probabilities are saved for all12 models. Seed11 additionally retains full state dictionaries, hidden vectors and first GAT weights for bounded future inference. Relabeling check permuted all node rows and adjacency axes, maximum observed logit discrepancy2.8611e−6, below declared1e−4. The “propagation removed” diagnostic uses zero adjacency but holds original graph-derived features fixed; it does not erase every trace of graph information. There is no synthetic assertion that every removed edge must hurt.

Exact path calculations retain degrees/normalization, loop-versus-matrix discrepancy8.88e−16, two propagation steps, signed eigenvalues, limit/raw versus degree-divided iterates, bias-location counterexample and a scalar backprop update. These are constructed mechanisms, separately labeled from observed graph results. Permutation and linear limit checked by direct arithmetic; the manuscript's changed practice and sampling/collision examples are small independently derivable quantities. Do not reuse inherited hand-entered Cora accuracy or depth-collapse charts as measured evidence.

All eight packet files, including licensed data and calculations, are necessary finish inputs. They are not disposable scratch. No application runtime files, graph library installation or browser execution was part of this content phase.
