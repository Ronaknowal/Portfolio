# Grouped-Query and Multi-Query Attention — authoring record

Research/write phase complete, 13 September 2026; implementation not started. Actual inventory `--work content` consumed, no destination note or bespoke blueprint; resolved unrelated routing inbox read. Full original source read from beginning to end, SHA-256 `423821bbbbb9f33cf2c0a73c8261f2bab3b7de354e118eadd9237118564bebcf`. Preserve source/runtime unchanged. This packet owns the grouped read/write mechanism, causal compact cache and bounded checkpoint conversion; MLA follows next.

## Experiment declaration before fitting

Use the verified CC BY 4.0 UCI Libras Movement input from the preceding positional packet, byte-exact metadata and data, exact duplicate handling and seed-73 split 220/50/60. The question changes explicitly from classification to next-coordinate forecasting: input positions 0–43 predict positions 1–44, with a causal mask so no target or later point is visible. Each original trajectory remains one split unit. Fixed transform `2*x-1`; no fitted test normalization. This row-level study has no reliable performer/session grouping and is not a long-context language-model benchmark.

Train one small causal decoder: input Linear(2,24), one pre-norm block, four query heads of width six, four K/V heads for the MHA parent, full Q/K RoPE base 10000 with positions 0–43, biased projections, FFN 48 GELU, final LayerNorm epsilon 1e-5 and per-row Linear(24,2) next-coordinate output. One seed 101. MSE over both transformed coordinates and all 44 prediction slots; report original-coordinate RMSE by dividing transformed-coordinate RMSE by 2. Adam lr 0.003, 180 full-batch updates; select lowest validation MSE, earliest exact tie. Retain all outcomes, no test-driven retuning.

From the selected parent, create three branches: unchanged MHA continuation, group-mean converted GQA with two KV heads, and group-mean converted MQA with one KV head. Copy all other parameters exactly, including Q/output maps. Evaluate the zero-update boundary, then continue each branch 45 full-batch Adam updates at 0.001 with a fresh optimizer. Select lowest validation MSE including step 0. This equal additional-update MHA control distinguishes conversion recovery from simply doing more optimization. It does not reproduce the paper's pretraining recipe or imply that 45/180 training steps equal its compute fraction. No random-init architecture campaign is needed for this question.

Non-neural baselines: persistence predicts the latest point unchanged; an affine one-step coordinate map is least-squares fitted using training transitions only. Score the same held-out slots. Predeclared visual source row 77, regardless of whether forecasts look favorable. Save all selected weights, full versus incremental causal parity, actual next-point output after a 32-point known prefix, a changed observed point, a common position shift/null and a five-step autoregressive rollout without unseen true inputs. Edited forecasts do not acquire new ground truth. Runtime should evaluate only one small selected model, never train in the browser. No latency benchmark or synthetic quality curve.

The declared run is complete; actual parent/continuation outcomes, causal equivalence and model states are retained. Scope, sources and checks follow. Final author readiness is recorded only after the whole-packet reread.

## Scope, title, sequence and teaching decisions

Keep the stable ID and catalogue title. The learner title spells out the two mechanisms without inventing a new topic or changing navigation. Previous Positional Encodings supplies RoPE and logical cache offsets; refresh the exact convention locally. Earlier Self-Attention and Transformer Block supply per-head Q/K/V, causal processing and residual state. This packet owns the distinction between query-head diversity and KV-head representation diversity, explicit grouping, compact cache assembly, memory/parameter/arithmetic accounting, checkpoint mean conversion, shared gradients and a bounded actual adaptation study. Next MLA needs these distinctions before its latent-cache algebra.

The real task intentionally becomes causal coordinate prediction instead of duplicating the preceding classification lab. It supplies an actual cache/decode computation and makes teacher-forced assessment versus generated rollout understandable. Reusing licensed data/split supports continuity while the lesson explicitly prevents cross-task architecture ranking. A small converted model failing to match MHA is useful evidence of the mechanism, not a reason to tune the experiment into a desired winner.

Later Sparse/Linear Attention owns changes to legal positions, kernelized/recurrent approximations and exact-operator comparisons. Here the comparison identifies these axes without duplicating those algorithms. MoE owns expert routing; this packet makes the limited attention-versus-FFN cache distinction. Earlier sequence models and root's cross-attention owner own source/target state alignment; local cross-attention refresh makes fixed encoder versus growing decoder caches explicit. Detailed serving allocation, speculative acceptance and distributed kernels remain systems topics, with bounded explanations and evidence limits here. No new unowned essential topic was discovered; no catalogue-wide audit or source modification occurred.

## Conservation and correction of the original

| Original useful coverage | Disposition in this packet |
|---|---|
|Motivation and historical MQA/GQA line|Retain incremental bandwidth motivation and primary origins; remove unsupported claims about proprietary model sizes, universal cache dominance and all-frontier-model adoption|
|MHA/MQA/GQA analogy and layouts|Preserve intuitive read/store distinction with complete wiring and real per-reader distributions; separate Hkv from readers-per-group throughout|
|Projection/head/output equations|Retain and generalize dk/dv, query/memory lengths and exact contiguous mapping; explain noncontiguous conventions without calling all alternatives invalid|
|Cache estimates and Llama-like dimensions|Retain full arithmetic as explicitly illustrative architecture counts; correct bytes versus elements, MB/MiB/GB/GiB, per-layer/whole-model scope and device-fit claims; counts do not certify long-context training|
|Parameter and compute estimates|Retain exact projection count; distinguish reduced KV projection work from unchanged dense per-query-head attention arithmetic. Remove fixed whole-model percentages and universal training-memory claims|
|Mean-head uptraining|Retain complete weight/bias conversion and continued training, derive limited Frobenius mean justification, add exact nonlinear counterexample and tied-head null. Remove false averaging-of-own-logits argument, claimed 90% recovery / 10× convergence and invented Meta conversion history|
|Large GPU implementation and synthetic shift task|Replace unsupported stdout/speed claims and position-free shift task with a complete executed transparent NumPy operator and real causal forecasting program. Full fitting/conversion/caching depth is retained without a GPU requirement|
|repeat/expand and gradients|Correct repeat_interleave being described as a view; preserve exact routing checks and add direct grouped einsum plus an independently checked sum-of-reader gradients|
|RoPE/cache timing|Explain native-head rotation's efficiency and equivalent consistent post-repeat rotation. Remove claim that mathematically equivalent ordering inherently forces wrong or expanded cache; maintain explicit logical IDs and actual offset-mask checks|
|SDPA/HF/FlashAttention source|Use inspected current/versioned contracts, explicit enable_gqa, dropout0, boolean-mask polarity, non-square causal alignment and actual compact cache/backend dispatch. Replace ellipsis-heavy pseudo-production block and automatic backend assertions|
|Visuals and benchmarks|Retain head maps, step flow and code-derived cache counts. Remove nonexistent seven-point quality curve attributed to GQA Figure 5. Actual paper Table 1 is a bounded source table; real local conversion histories are separate measured data|
|Decision rules and scaling|Replace hard size thresholds/eight-groups-always-optimal with task/checkpoint/kernel/layout criteria. Retain composability but remove multiplied speculative/paging/GQA throughput claims and implausible sessions-per-device arithmetic|
|Failure modes and self-check|Place each caution at its mechanism, retain shape/group/mask/conversion diagnoses, replace answer-visible recall with nine changed problems and initially closed hints/solutions|
|Primary sources|Full original MQA and GQA papers inspected plus substantive API/source/architecture documentation and an annotated short article/official presentation option; no transcript or timing campaign fabricated|

Useful added depth includes a shared-key edit whose orthogonal reader is unchanged, wrong-route versus consistent-relabeling distinction, parameter-coordinate proximity versus functional proximity, paired conversion/continuation control, real causal invalidation, general value width, compact logical versus physical replicated state and explicit modeled-versus-measured speed. Each addition resolves a concrete local misconception.

## Canonical section-list coverage

The original MQA paper is the canonical mechanism reference for this narrow architecture topic. Read all nine PDF pages, not just abstract metadata. Actual structure: §1 Introduction; §2 Background: Neural Attention; §2.1 Dot-Product Attention; §2.2 Multi-head Attention; §2.3 Multi-head Attention (Batched); §2.3.1 Performance Analysis of Batched Multi-head Attention; §2.4 Multihead Attention (Incremental); §2.4.1 Performance Analysis; §3 Multi-Query Attention; §3.1 Performance Analysis for Incremental Multi-Query Attention; §4 Experiments and Results; §4.1 Experimental Setup; §4.2 Model Quality; §4.3 Speed; §5 Conclusion; References.

All core operator, batched/incremental, head-sharing and performance-assumption ideas are taught locally in §§1–5. Code is independently implemented with stable normalization and explicit masks rather than copied old TensorFlow snippets. The source's projected-code typos and its simplified complexity assumptions are not reproduced blindly. Its actual task/parameter matching and metric-dependent differences inform §7; large TPU experiments remain cited source evidence rather than claimed local reproduction. Full language-model/translation training is not required for the reader to understand this operator.

The complementary GQA paper's actual structure was read in full: §1 Introduction; §2 Method, with 2.1 Uptraining and 2.2 Grouped-query attention; §3 Experiments, with 3.1 Experimental setup (Configurations, Uptraining, Data, Fine-tuning, Timing), 3.2 Main results, 3.3 Ablations (Checkpoint conversion, Uptraining steps, Number of groups); §4 Related Work; §5 Conclusion; Limitations; Appendix A Training Stability. Group definition, mean conversion, parameter adaptation, actual table protocol, figure identities, sharding rationale and limitations are all included at appropriate depth. Training instability is recorded as a source observation, not a theorem or a reproduced local issue. Related methods become the bounded axis-comparison and actual next-topic links, not an unowned missing-method list.

The current topic was published but not approved under this new teaching approach. The conservation audit preserves useful depth while replacing inaccurate mechanisms/empirical claims. Canonical source sections alone do not dictate the learner's article order: the new flow starts with the data dependency and concrete head outputs before conversion and systems details.

## Hurdle map and selected representations

| Hurdle | Local explanation/evidence | Representation/action |
|---|---|---|
|Why keep old values but not old queries?|Causal dependency and prefill/decode distinction|Write-once/read-again timeline with separate layer state|
|Several query heads share one memory, so must outputs agree?|Exact four-query/two-memory calculation|Head wiring linked to distinct positional weight distributions and vector mixtures|
|Every reader of an edited key must change|Orthogonal-query null plus value-path fan-out|Edit Q/K/V coordinates and predict exact affected outputs|
|Head groups and token positions are confused|Explicit Hq/Hkv/R and B, H, T, S, dk, dv axes|Wiring versus attention matrices; compact cache rails and group/position labels|
|Eightfold cache saving must mean eightfold speedup|Payload, projection work, dense score work, reuse assumption and Amdahl model|Tensor-budget construction and separately labelled modeled timing panel|
|A valid shape means a valid cached call|Logical IDs, API-specific non-square masks and actual native contrast|Repair an initially wrong mask/position with computed output evidence|
|Parameter means should preserve predictions|Frobenius identity, nonlinear scalar counterexample, tied null|Editable old/shared parameter view and separate distribution/output differences|
|More training after conversion proves no quality cost|Equal-extra-update MHA control and untouched selection boundary|Actual validation histories and before/after held-out outcomes with simple baselines|
|Teacher forcing and generation look like the same forecast|Visible observation boundary and actual five-step feedback|Editable real trajectory, head/group trace, full/cache check and generated rollout|
|Logical heads equal physical copies on every device|Explicit replicated eight-device scenario|Labelled device/group count inset without invented timing|

I1–I5 are distinct investigations with complete inputs, prediction state, controls/nulls, feedback, accessible/mobile forms and bounded runtime. Core explanatory diagrams are not delayed until the final chapter. All first-pass readiness is supported before §7; advanced derivatives/distribution details remain optional deeper understanding, not hidden gates to MLA.

## Substantive research and alternative resources

Research date 13 September 2026; current policies and preceding source-bound evidence reused. Actual inspected material:

- [Shazeer 2019](https://arxiv.org/pdf/1911.02150): complete nine-page paper, all sections listed above, code/arithmetic assumptions, WMT and Billion-Word protocols, parameter matching, actual quality/speed tables. This resolves universal quality-loss and paper-length errors and separates QKV projection arithmetic from dense score work.
- [Ainslie et al. 2023 v3](https://arxiv.org/html/2305.13245v3): complete methods, T5 setup, task metrics, actual Table 1, all ablations, limitations and Appendix A. Figure 5 is uptraining proportion, Figure 6 inference time by groups. Original published JSX's supposed Figure 5 quality curve is unsupported and removed. Eight groups was the authors' selected tradeoff, not a universal quality threshold. The source notes no from-scratch XXL GQA comparison and encoder-decoder-only evaluations.
- [PyTorch 2.14 SDPA](https://docs.pytorch.org/docs/2.14/generated/torch.nn.functional.scaled_dot_product_attention.html): full signature/reference computation, explicit enable_gqa, head constraints, masks, dropout, backend/numerical notes and non-square upper-left convention. Native CPU float64 checks support only the local observed operator behavior, not CUDA kernel availability/performance. Stable redirect was unhelpful; exact 2.14 documentation was retrieved successfully.
- [Maintained Llama source](https://raw.githubusercontent.com/huggingface/transformers/main/src/transformers/models/llama/modeling_llama.py): actual RoPE implementation, repeat_kv, eager attention, native head projections, compact cache update and backend dispatch read. Mutability/version pinning is explicit; no installed Transformers model or speculative private architecture attribution.
- [FlashAttention README](https://github.com/Dao-AILab/flash-attention): full relevant `flash_attn_func` and `flash_attn_with_kvcache` argument/behavior blocks, grouping example, bottom-right causal convention, rotary layout, cache update, no-backward cache path and causal-alignment changelog inspected. No dependency install or GPU run; native API comparison is stated at its supported scope.
- [Mistral 7B §2](https://arxiv.org/html/2310.06825v1#S2): actual architecture table, sliding window, rolling cache and prefill/chunking text. Only scoped architecture dimensions and composability enter the lesson; its broad quality claims are not treated as an isolated GQA ablation.
- [PagedAttention](https://arxiv.org/html/2309.06180v1): background/autoregressive and memory-management sections plus relevant §4.1–4.3 block/manager/decoding explanation and distributed-layout text inspected. Mechanism-level layout/sharing statements are bounded; source system throughput numbers are not multiplied into new invented claims. Further serving details remain optional systems reading.
- [Raschka's complete GQA guide](https://sebastianraschka.com/llms-from-scratch/ch04/04_gqa/): actual short body, block-state distinction, cache plot context and composability comparison inspected. Useful compact alternate recap, not sole technical authority or proof that all current models use GQA.
- [ACL paper record](https://aclanthology.org/2023.emnlp-main.298/): official author/paper provenance and direct presentation MP4 link verified. The associated complete paper was substantively read; no video transcript was available/inspected, so annotation explicitly states that scope. Do not claim the linked presentation was watched.
- [3Blue1Brown attention article](https://www.3blue1brown.com/lessons/attention/): preceding packets' substantive Q/K/softmax/mask/weighted-value and orientation evidence reused as a prerequisite alternate visualization, not a new GQA-source claim.
- [UCI 181](https://archive.ics.uci.edu/dataset/181/libras%2Bmovement): previous verified license/full original metadata/source hashes reused; byte-exact copies support the new declared forecast task. Data provenance records the actual boundary and non-independence across earlier lessons.

No unverified GPU stdout, hypothetical proprietary-model internals, synthetic benchmark curves or blanket 2026 adoption claims remain. Formula-derived examples, source-reported experiments, local measured outcomes and future implementation requirements are separately labelled.

## Current author checks and next action

The declared parent plus three continuation runs passed and saved all intended results; no test-driven retuning. Both complete programs executed. Native fixtures check direct versus SDPA grouped output, distinct repeated storage, actual shared-value gradients, tied-MHA/null mappings and non-square mask differences. A singleton-stride leaf warning was corrected by constructing tiny native fixture leaves from value lists; the bounded mechanism rerun passed, with no neural-fit repeat. The manuscript's full NumPy block was separately executed as displayed. Reloaded selected models match exported forecasts and preserve all earlier-than-edited-slot predictions exactly.

Full manuscript, specifications, design and provenance were reread for continuity and numerical/interaction consistency. The final scoped checks passed: all three JSON files parse; all four Markdown files have clean control characters and existing relative packet links; nine changed exercises have eighteen initially closed hint/solution disclosures; all five stable local topic routes resolve to existing sources; the full original lesson and both offline source-data hashes remain unchanged. Minor joined prose and a projection-shape wording ambiguity were corrected without repeating model fits.

Author learning-experience checklist:

1. **Route:** intuition and actual prerequisite links precede terminology; the first-pass route appears immediately after the introduction. Its arithmetic, cache, conversion and interpretation readiness is taught before the optional deeper branches.
2. **Cautions:** each consequential warning lives beside its mechanism: masks beside cache assembly, averaging beside conversion, physical allocation beside payload and teacher forcing beside rollout. No code-printed disclaimer or repeated warning wall interrupts the lesson.
3. **Real question/data:** original licensed observations, exact duplicate handling, declared row boundary, simple baselines and actual conversion outcomes are retained. The selected input was predeclared; no forced architecture winner or unobserved benchmark curve remains.
4. **Investigations:** five distinct investigations use genuine coordinate/shape/mask/parameter/input edits, initially unset input-bound predictions, checked contrasts and exact or approximate nulls, reset and explanatory feedback. Model changes cannot silently reuse stale cache state.
5. **Perceptible figures:** wiring, value-vector mixtures, cache axes, offset masks, conversion distributions and observed/generated trajectories represent their actual mechanisms. Text/numeric counterparts, units and mobile inspection preserve the same information.
6. **Connections/canonical coverage:** full MQA/GQA source section lists and the original lesson conservation record are reconciled. Local positional, Transformer, MLA and MoE links are explicit; serving and later sparse/linear boundaries are reasoned rather than vague omissions.
7. **Code:** the complete displayed NumPy mechanism and native checks executed; the complete CPU study performs real training, conversion, caching and rollout. Downloaded programs contain full implementations; the short API call is honestly labelled a call-site fragment.
8. **Changed practice:** nine new calculations/diagnoses require transfer to altered inputs. Optional closed hints and exact explained solutions were checked; recall questions and answer-visible presets do not stand in for investigation.
9. **Screenshots and implementation:** visual contracts specify accessibility, mobile layout, numerical parity and bounded lazy loading. Screenshots, browser behavior, formal independent review and integration are explicitly deferred to phase two, not claimed as author checks.

No unresolved material writing gap remains. Root owns shared content checkpoint; the next action is phase-two visual/lab implementation and its formal independent/rendered/integration checks. Retain all eleven pending packet inputs. Continue sequence with MLA without repeating this unchanged research or fit campaign.
