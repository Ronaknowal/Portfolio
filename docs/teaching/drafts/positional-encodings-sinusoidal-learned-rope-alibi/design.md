# Positional Encodings — authoring record

Research/write phase complete, 13 September 2026; implementation not started. Author owns this stable-ID packet only. Full original `src/learn/data/topics/positional-encodings-sinusoidal-learned-rope-alibi.jsx` read in full (all 1,158 lines), source SHA-256 `403cc0706f77768d87421ed5d671e7897f92d387639201e3033041e3cd072ff6`. Actual `--work content` inventory consumed; no destination note or bespoke blueprint. The full eleven-file packet is ready for the root-owned content checkpoint.

## Experiment declared before fitting

Question: which information about a real ordered trajectory becomes available under each positional mechanism, and which transformations are exact symmetries? Use the already rights-checked UCI Libras Movement input, original bytes and metadata copied from the preceding Transformer Block packet. Reuse its exact duplicate removal, seed-73 classwise 220/50/60 split, fixed coordinate transformation `2*x-1`, and source-row 77 display example. This is a fixed row-level study without reliable performer/session IDs, not an independent generalization benchmark across chapters.

Five models: no position control, sinusoidal input, learned input table, RoPE Q/K, symmetric bidirectional ALiBi score bias. All use a 2-to-24 stem, one pre-normalized block, two heads of width 12, FFN width 48 with GELU, final LayerNorm, valid mean pooling and 24-to-15 classifier. All maps biased; LayerNorm epsilon 1e-5; no dropout. Same seed 101 initializes the shared tensors identically; learned 45 × 24 table independently initialized with generator 303 and normal std 0.02. Sinusoidal and RoPE base 10000; ALiBi official two-head slopes. This symmetric encoder adaptation has no causal direction cue, and reversal with fixed slots is therefore an exact architectural symmetry for ALiBi and the no-position control. No claim that ALiBi's original causal language model has that symmetry.

Each model receives exactly 180 full-batch Adam epochs at 0.003, no weight decay. Select validation macro-F1, lower validation CE, earliest tie. Test once after checkpoint selection. One fixed seed is adequate to observe mechanisms and real failures; it cannot rank architectures robustly. Do not tune after test outcomes. Preserve all five outcomes. Primary interventions: jointly reverse points and their original positions (representation permutation null); reverse points against fixed positions (direction change); append masked points preserving original positions (padding null); reflect frame 23 x coordinate (genuine edit). Store actual selected weights and traces for source row 77. No synthetic performance curves or GPU timings. Five modest fits are the complete planned campaign.

## Scope, title and actual sequence

Keep stable ID and topic title; a concise learner-facing subtitle clarifies the four named mechanisms without renaming the catalogue. This is the local home for additive absolute positions, rotary geometry/basis/partial rotation, linear score bias, Shaw/T5 contrasts, positional IDs/cache semantics and bounded context-extension mechanisms. No curriculum-wide audit was performed or authorized.

Previous topic Transformer Block introduced real trajectory position tags and carried-state/attention distinctions; refresh these locally instead of presupposing specialized language modeling. Self-Attention supplies rowwise Q/K/V and permutation equivariance. Earlier Long-Context/SSM/RWKV lessons own segment memory, recurrent states and exact operator families. This packet explicitly handles the causal NoPE exception and does not equate relative encodings with guaranteed long-context behavior. Next topic GQA/MQA shares K/V heads; it needs the positional convention and cache-axis distinction here. MLA subsequently needs the rotary/unrotated score split and consistent positional coordinates. All important previous/next references use actual full-curriculum module routes.

Later owned Vision Transformers develops two-dimensional patch geometry, interpolation and window-relative positions. The application here introduces the coordinate question without duplicating its full training methods. Sparse/Linear Attention owns exact feature-map, sparse, low-rank and recurrent-operator comparisons; RoFormer linear-attention generalization is a boundary reference, not silently presented as standard row-softmax equivalence. Root's Interleaved/Cross-Attention owns cross-stream coordinate contracts. No open external destination discovery requires a new topic; the scoped connections belong to these already identified owners.

## Conservation of the original lesson

| Original section / useful idea | Final disposition |
|---|---|
|1 history and motivation|Retain mechanism motivation and primary origins; replace blanket bag-of-tokens claims with precise encoder/readout proof and causal-mask exception; avoid obsolete model popularity rankings|
|2 absolute/additive/rotary/bias intuitions|Retain and concretize with vector addition, clock hands, signed pair projections and score/odds decomposition. Remove guaranteed capacity/signal-loss/superiority assertions|
|3 formulas, shift identity, frequencies, PI/base/YaRN/cost|Retain full derivations, exact index/dimension conventions and numeric examples; correct high/low frequency arithmetic, PI endpoint, temperature squaring and cost/storage distinctions|
|4 GPU from-scratch programs and shift task|Preserve complete encoding/attention implementation depth through an executed standalone CPU cache program and complete real-data five-model program. Discard unsupported GPU/stdout/perplexity tables and ellipsis/undefined scaling functions; a different dataset study is explicitly not a reproduction of the old shift experiment|
|5 PyTorch/HF/rotary tools and model examples|Keep position injection, strict table bounds, cache offsets and checkpoint/version awareness. Replace expensive model-loading snippets/unsupported outputs with exact local code and inspected maintained source. Correct blanket model adoption, partial-RoPE/XPos conflation and context-at-load-only claims|
|6 heatmaps, frequency/bias curves, step trace|Regenerate actual formula-derived values; old width 32 heatmap inconsistent with formula is removed. Retain intuitive planes/traces but use code-derived signed diagrams and genuine edits. Old unverified perplexity plot is replaced by clearly separated actual movement results and a geometric scaling plot|
|7 decision matrix|Use information semantics, checkpoint compatibility and evidence needs; no ungrounded all-model 2026 ranking or universal base rule|
|8 scaling and cache/GQA|Retain beyond-training distinction, frequency changes, cache positions, partial channels and full storage formula. Add dynamic-frequency versus multilayer cached-hidden-state distinction; rotary transformation does not reduce cache bytes|
|9 implementation failures|Place each consequential caution once at its owning mechanism: table bounds; basis; padding/masks; odds; temperature; frequency/cache consistency. Remove repeated global warning chapter and unsupported claims about inevitable training outcomes|
|10 primary sources|Retain substantive original papers plus author code/current utility docs, real data provenance and annotated article/video alternatives. Scope exactly what was inspected|
|11 self-check|Replace answer-visible recall with eight changed exercises, each with optional initially closed hint and explained exact solution. Core readiness 1–4; deeper branches 5–8|

Useful adjacent material added: explicit equivariance versus invariant readout, causal prefix-mean example, content-plus-position four-term expansion, arbitrary-content Toeplitz counterexample, exponential odds versus normalized probability, non-power-of-two slopes, symmetric-ALiBi reversal impossibility, T5 signed bucket table, basis conversion, dynamic-cache scope, irregular timestamp units and image-grid coordinates. These additions solve actual learning hurdles in the selected topic rather than expanding it indiscriminately.

## Canonical section-list audit

Canonical teaching reference read from the actual D2L 1.0.3 page, not its title/search metadata: [§11.6 Self-Attention and Positional Encoding](https://d2l.ai/chapter_attention-mechanisms-and-transformers/self-attention-and-positional-encoding.html). Full relevant body, source snippets, formulas and final exercise list inspected. Sections are 11.6.1 Self-Attention; 11.6.2 Comparing CNNs, RNNs, and Self-Attention; 11.6.3 Positional Encoding; 11.6.3.1 Absolute Positional Information; 11.6.3.2 Relative Positional Information; 11.6.4 Summary; 11.6.5 Exercises.

Disposition: self-attention refresh is local §1 with a direct earlier lesson link. Architecture-complexity comparison belongs to preceding architecture/attention owners; here §9 retains the exact full-attention cost relevant to position and links next cache-sharing. Positional construction, binary/multiscale intuition, sine/cosine code and shift identity are fully developed in §2 with actual calculated tables and clock views. The claim that float encodings are categorically more space-efficient than binary bits is not carried over; representation precision/storage needs an explicit convention. Canonical learned-position and learned-offset exercise ideas become full §2/§5 teaching and changed practice rather than unanswered missing core concepts. Stacking/positional issues enter block context, score terms and cache/precision/context sections. Newer RoPE/ALiBi/extension material is outside that canonical chapter's era; original papers and maintained implementations supply it.

RoFormer inspected section inventory also helps check breadth: §2.1 preliminary, 2.2 absolute, 2.3 relative; 3.1 formulation; 3.2.1 two-dimensional case, 3.2.2 general form; 3.3 properties; 3.4 theoretical explanation. Cover exact construction and controlled distance behavior here. Full complex-number uniqueness and Abel-transform derivation are optional source depth; a direct real-vector proof and an explicit nonmonotone counterexample support the actual learner claims. Its linear-attention proposal changes operator details and is routed to the later Sparse/Linear owner. No claim to have replicated every paper experiment.

## Hurdle map and chosen representations

| Learning hurdle | Explanation / evidence | Representation / changed action |
|---|---|---|
|An ordered array is not necessarily used as order|Permutation proof and causal prefix means|Point cloud versus directed trajectory, paired-record versus slot reassignment|
|Adding vectors seems to mix incompatible meanings|Exact learned-table example and four score terms|Content/slot vectors with editable entries; sinusoidal clocks and signed heatmap|
|Rotations apparently use absolute positions yet yield relative scores|Orthogonal inverse/composition and pairwise sine/cosine expansion|Two-plane arrow/projection view, m/n edits, shared-shift null|
|A nice diagonal heatmap is mistaken for every real attention matrix|Constant-content versus doubled-content matrix|Edit a content vector and break Toeplitz while preserving the RoPE identity|
|Linear bias is mistaken for a hard window or half-probability|Exact softmax odds and stronger distant content|Timeline, content/bias/final-score bars, editable key/score competition|
|Relative distance can omit direction|Reversal identity of symmetric ALiBi encoder|Real 45-point data, all five model weights, reversal and point-edit contrasts|
|Cache tensor length is confused with logical position|Separate slot, ID and legality rails|Editable cache repair; correct full/cached null, wrong angle, wrong mask and stale frequency|
|Computable long positions are equated with performance|Three length questions and precise extension formulas|Wavelength/phase/temperature investigation with no fake empirical axes|
|One-dimensional text indices are applied to every data modality|Actual time-unit conversion and grid coordinate construction|Three-event timeline and 2D patch adjacency diagram|

Investigations are independent mechanism tasks, not six mandatory identical widgets. Each uses genuine editable entities and input-bound unset predictions. Text alternatives, keyboard/mobile designs, quantitative contrasts/nulls, exact feedback, bounded computation and phase-two checks are fully specified. The real workspace does not hide original errors, grade edited trajectories against invented labels or train in the browser.

## Research record — substantive inspection

Research dates 13 September 2026. Current policies and source-bound preceding-topic evidence were reused. The following pages/sections were actually read; this is not a list inferred from abstracts alone.

- [Vaswani et al.](https://arxiv.org/pdf/1706.03762), §3.5 and the preceding Q/K/FFN/block context, inspected in the preceding packets and reused for exact original addition/formula/comparison. No repeated full-paper rerun.
- [D2L 11.6](https://d2l.ai/chapter_attention-mechanisms-and-transformers/self-attention-and-positional-encoding.html), actual section list and full relevant code/formulas/exercise body as recorded above; source binary bit/float-storage statement not adopted.
- [RoFormer v5](https://arxiv.org/html/2104.09864v5), §§2.1–2.3, 3.1–3.3 equations, two-dimensional/general construction, properties and linear-attention bridge. Real-vector derivation checked independently; selective source claims about universal distance decay are bounded by the explicit counterexample and PI discussion. Full appendix proof and large experiments not represented as reproduced.
- [Shaw et al.](https://aclanthology.org/N18-2074.pdf), §§3.1–3.3 vector score/value equations, clipped signed offsets, parameter sharing and efficient two-term decomposition. Learned relation is query-dependent, not merely scalar T5 bias.
- [ALiBi v2](https://arxiv.org/html/2108.12409v2), §2 comparison context and §3 actual causal formula, score scaling footnote, slope choices and implementation discussion. [Author source](https://raw.githubusercontent.com/ofirpress/attention_with_linear_biases/master/fairseq/models/transformer.py), slope routine and row-constant tensor construction read. Runtime assertions kept scoped to implementations, not promoted to a theorem.
- [T5 maintained source](https://raw.githubusercontent.com/huggingface/transformers/main/src/transformers/models/t5/modeling_t5.py), full relative-bucket routine, compute_bias query offset and explicit attention scaling convention 1.0 read; the independent NumPy bucket table matches that rule. Main branch is mutable, so later checkpoint reproduction must pin it.
- [Position Interpolation v2](https://arxiv.org/html/2306.15595v2), §§2.1–2.3 exact rescaling, fixed-score interpretation and interpolation-bound discussion; §§3.1–3.2 training setup and sliding-window evaluation protocol. Did not run that GPU study or endorse its bound as end-to-end generalization proof.
- [YaRN v3](https://arxiv.org/html/2309.00071v3), §§2.1–2.3 notation, 3.1–3.4 complete methods/formulas, 4.1–4.3 protocol and displayed comparison table inspected. Correct PI uses t/s; its displayed earlier Eq 9 t×s is inconsistent with the source definition and primary PI formula, so not blindly copied. Distinguish q/k multiplier from squared score multiplier, paper rotation-count ramp from library index discretization, and fixed-content key rephasing from full-model recomputation.
- [XPos/LEX PDF](https://arxiv.org/pdf/2212.10554), §§3.1–3.2 relation/amplitude/decay definitions and boundary to 3.3 blockwise masks; [TorchScale entire 59-line implementation](https://raw.githubusercontent.com/microsoft/torchscale/main/torchscale/component/xpos_relative_position.py), centered exponent, scale_base=512, interleaving and reciprocal branch fully read. Code resolves the original lesson's unused scale_base and incorrect decay direction.
- [NoPE/length generalization](https://arxiv.org/pdf/2305.19466), §§2–4 architecture/mask distinction, task definitions, metrics/seeds and results discussion, plus opening §5 formulation inspected. Not all 37 appendix pages read; local prefix-mean argument independently illustrates the structural exception without claiming a full theorem replication.
- [Llama 3 report](https://arxiv.org/html/2407.21783v3), §3.2 stated base and actual parameter table,§3.4.2 staged long-context training,§4.3.4 data/short-context retention context inspected. This corrects the old implication that the 128K capability results solely from changing config at load time; no whole model download.
- [Transformers RoPE utility documentation](https://huggingface.co/docs/transformers/main/en/internal/rope_utils), actual named variants/configuration and per-layer-type body read. Main page reports stable 5.17.0 but that versioned page fetch failed; avoid claiming an executed Transformers 5.17 program. The learner program instead uses available NumPy/PyTorch and exact mechanisms.
- [Raschka complete FAQ](https://sebastianraschka.com/faq/docs/rope-vs-absolute-positional-embeddings.html), full table, geometric identity, partial/cache/length remarks read; useful alternate compact article, not borrowed structure/wording.
- [3Blue1Brown attention notes](https://www.3blue1brown.com/lessons/attention/), actual Q/K, dot-product, softmax, masking and row/column-convention explanations read, including made-up semantic-example qualification. Creator video is linked through the actual page; no claim to have watched it. It is an annotated prerequisite visualization resource, not the positional mechanism source.
- [Karpathy course](https://karpathy.ai/zero-to-hero.html) and its actual [GPT video](https://www.youtube.com/watch?v=kCc8FmEb1nY) link verified; YouTube did not expose a transcript. [nanoGPT model source](https://raw.githubusercontent.com/karpathy/nanoGPT/master/model.py), actual attention/block and GPT learned table/addition/length-check body read. The unavailable older ng-video repository is not endorsed as inspected; same-author maintained implementation is explicitly distinguished from the video. Annotation honestly scopes this optional code-first learning route.
- [UCI Libras](https://archive.ics.uci.edu/dataset/181/libras%2Bmovement), verified preceding acquisition/complete metadata/rights and per-file hashes reused with scoped new comparison. Actual code, full outcomes, fixed source selection and limitations recorded separately.

## Consequential author checks and continuation

The five declared fits and independent NumPy mechanisms completed successfully. All original mode outcomes were retained; no test-driven retuning, resampling or favorable-case substitution. The displayed standalone program was separately extracted and executed. The author reread the full manuscript, all visual/investigation contracts, this design and provenance. That pass corrected escaped inline mathematics, prose spacing and the resource annotation's inspected ALiBi section range; it did not change the programs or empirical outcomes.

Bounded final checks passed: all four Markdown files have no unexpected control characters; relative packet links resolve; all three JSON files parse; all eight practice questions have separate, initially closed hints and explained solutions; all five distinct local route IDs exist in the actual catalogue. The original published JSX and both original data-file hashes still match the recorded baseline. The first check script stopped on a Windows default text-encoding read; the corrected explicit UTF-8 read passed without changing data. Reuse the successful unchanged fits and displayed-program execution rather than rerunning them for editorial changes.

### Author learning-experience checklist

1. **Route:** the immediate first-pass path is explicit; core readiness is §§1–7 and exercises 1–4. Context extension and advanced exercises remain a named deeper route, not hidden prerequisites for GQA.
2. **Cautions:** each important limitation sits at its mechanism or real-study protocol. No repetitive warning chapter or code-printed caveat substitutes for explanation.
3. **Real question/data:** the full, licensed trajectory input, duplicate boundary, fixed split, actual model weights and all five outcomes are retained. The predeclared example's five original failures remain visible.
4. **Investigations:** six distinct tasks use real editable entities, unset input-bound predictions, computed feedback and checked contrasts/nulls. No generic preset-only lab or automatic success message.
5. **Perceptible figures:** explicit arrows, clocks, signed projections, score competition, trajectory/slot distinctions, cache rails and wavelength units are specified beside the relevant prose. Every visual has a table/text equivalent.
6. **Connections:** previous/next actual module routes, canonical section dispositions, position/causality/cache boundaries and later MLA/vision applications are explicit. No unowned essential concept remains.
7. **Code:** complete transparent NumPy mechanisms and the actual five-model CPU study are supplied with setup and observed outputs. No large-model run or browser implementation is claimed.
8. **Changed practice:** eight independent situations cover rearrangement, rotary geometry, odds, cache repair, symmetry, scaling, partial temperature and physical time units, with exact reasoning in closed solutions.
9. **Screenshots and implementation:** intentionally deferred with browser parity, accessibility, mobile and loading checks specified for phase two. Content completion is not rendered or formal implementation acceptance.

No unresolved material writing gap remains. Next action is the root-owned content checkpoint, then an authorized future finish request to implement the figures/investigations and perform formal review. Preserve all eleven pending files; no disposable scratch was created by this packet.
