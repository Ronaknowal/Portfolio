# Regularization data and calculation provenance

Observed input: Brooks, T., Pope, D., and Marcolini, M. (1989), **Airfoil Self-Noise**, [UCI DOI 10.24432/C5VW2C](https://doi.org/10.24432/C5VW2C), licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/). Retain attribution and this unchanged-file description. On 12 September 2026 this packet copied the exact numeric member already retrieved by the parallel Bias–Variance author from https://archive.ics.uci.edu/static/public/291/airfoil%2Bself%2Bnoise.zip. No second network download or retained ZIP was needed. Original member `airfoil_self_noise.dat`; local descriptive filename `airfoil-self-noise.dat`; 59,984 bytes; SHA-256 `74c75fd71783f1e6b71f8a622b993dc592897a97cd689c5090a07147a1b097b3`. The source archive hash recorded by that author is `5c7767ba53ad827d3f48ba1eb9434117f4892df8f10bc4c99e118a9e8a7ae07c`.

All 1,503 rows and six columns are unchanged. Predictors, in source order: frequency Hz, attack angle degrees, chord m, free-stream speed m/s, suction-side displacement thickness m; target: scaled sound pressure dB. The UCI variable table's binary attack-angle tag conflicts with its numerical degree measurements and additional description; the lesson correctly treats it as numeric. No filtering or imputation changes the input. Zero-based row indices preserve source identity.

The seed-41 split retains 1,200 development rows and 303 reserved rows. **No reserved prediction or score is calculated.** Related experimental settings are not independent airfoil/run identifiers, so this is a row-level diagnostic of the supplied collection. It cannot substantiate performance on a new airfoil or experimental run. Reusing the same outer boundary as Bias–Variance avoids quietly changing which familiar rows are protected; the two lessons use different features, fits and inner protocols and must not present their scores as one controlled head-to-head study.

This packet uses `KFold(3, shuffle=True, random_state=202)` over development indices: 800 fit and 400 validation rows in every fold. A degree-two expansion of five inputs creates twenty nonconstant terms. StandardScaler fits within each fold. Six predeclared normalized strengths are .001, .01, .1, 1, 10, 100; families are ridge, lasso and elastic net with mixing fraction .5. Ridge's native alpha is fitted-row-count times normalized strength; Lasso and ElasticNet native alpha equal normalized strength. The script performs 54 candidate fits, three OLS comparator fits and three selected final development refits: 60 fits total. Training means supply the baseline without extra model fitting. No search was repeated to obtain a preferred curve or winning family.

`author-calculations.py` executed the real-data calculation serially using Python 3.12.14, NumPy 2.3.5 and scikit-learn 1.9.1 in the existing read-only runtime. No convergence warning occurred; lasso/elastic net used tolerance 1e-8 and 50,000 maximum iterations. `calculated-inputs.json` retains split/source IDs, actual fold predictions/errors, coefficient paths, fitted final preprocessing and model parameters, fit accounting and method/version information. Subsequent tiny inference and analytic fixtures were computed from saved results or exact formulas and added to the same reproducible author script; they did not trigger another real-data fit campaign.

The displayed complete teaching programs implement the same equations/protocol as the executed author calculations. Their separately assembled verbatim execution remains a phase-two task; do not label this as production or browser verification. There is no required PyTorch installation or promised native network score in this packet.

The orthogonal and duplicate designs, coordinate histories, coefficient curves, dropout masks, singular-value filters, two-factor optimum, three-position smoothness system, fitted-likelihood records and bit-code examples are explicitly **constructed calculations**, not observed measurements. All four dropout masks are enumerated with their actual probabilities; no Monte Carlo error is hidden. Objective history is computed, not painted. A bounded final arithmetic check independently confirmed the changed shrinkage, nonuniform-dropout, AIC/BIC and shifted-smoothness practice answers.

The author JSON is a retained evidence record, not a required browser payload. Phase two should extract only the selected small model/path/fixture values needed by this topic, keeping source attribution and full evidence offline. No downloaded media, temporary archive, disposable helper or Python cache is needed by this packet.
