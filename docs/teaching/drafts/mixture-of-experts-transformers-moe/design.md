# Mixture-of-Experts Transformers: content design

Stable ID mixture-of-experts-transformers-moe, Deep Learning Fundamentals & Architectures position 27. Research/write only, 13 September 2026. Root took this untouched packet from classical_feature_content through explicit coordination. Implementation remains not started.

Actual content preflight completed, no destination note applied. The complete original published JSX was read in contiguous ranges, recovering truncated sections. SHA256 fad5a8e9f48f6d51a7a94825aa09ded73e7ef84443253762e4659d6e64d0f230, baseline HEAD 8c5da59f18516be77c29d5aeeafca3decca4f738. Preserve source, stable title and module order.

## Learning contract and scope

Start with choosing a small number of computational branches, then show selected paths and their weighted reunion. Build from familiar FFN/attention residual blocks to conditional compute, routing normalization, gradients, dispatch/capacity, training objectives, hardware/storage, contemporary variants and a real patch-classification model. The expert's name is an index, not an assigned profession. Explain meaningful applications without speculative claims about undisclosed commercial architectures.

Read the immediately preceding vision-transformer packet for the final bridge when available. MoE changes which FFN parameters process each token; sparse attention changes which tokens interact. Interleaved/cross-attention owns external-source attention. Jamba combines mixers with MoE later.

## Experimental declaration before fitting

Use verified byte-original optical digits from the xLSTM packet, reread full optdigits.names and UCI attribution/license page. Make an attributed topic-owned derived CSV with original file and one-based source IDs. Fixed NumPy seed 427, per class: permute original train IDs, choose first 50 for fitting and next 15 for validation; permute original test IDs and choose first 30 for assessment. Total 500/150/300; verify no duplicate feature vectors across roles. Original train/test writers differ, but internal fit/validation writer identity is unavailable. Inputs divide counts by 16; no learned preprocessing.

A compact image transformer converts an 8x8 image to sixteen 2x2 row-major patches, projects 4→16 and adds sixteen learned positional vectors. One pre-LayerNorm two-head all-to-all attention residual block; pre-LayerNorm FFN residual; final LayerNorm, average patch vectors, linear 16→10 classifier. The dense comparator has one bias-free SwiGLU FFN width 32. Sparse alternatives have four bias-free SwiGLU experts width 16, top two selected-logit softmax gates, bias-free 16→4 router, dropless gather/weighted scatter-add. Thus routed expert matrix MACs match the dense width-32 FFN, but total parameters and routing overhead differ. Both use the same attention architecture. No pretend text-copy task or claim this reproduces Mixtral.

Four conditions: dense; MoE load-balance coefficient 0; MoE .01; MoE .1. Three initialization seeds 17/41/73, twelve fits. Each run 240 minibatch AdamW updates, batch64 sampled with replacement using a dedicated fixed generator, lr .003, weight decay .001, betas defaults .9/.999, gradient norm clip1. CPU float32, single thread, no dropout, augmentation, capacity dropping or timing benchmark. No z-loss in this small experiment; numerical objective is taught separately. Validate at update1 and every20; choose minimum-validation task cross-entropy. Report all twelve fits, clean assessment outcomes and declared zero-lower-half stress; do not retune after assessment. Keep full selected weights, all checkpoint curves, per-role confusion, route counts and actual parameter counts. Uniform ten-class baseline CE ln10, chance expectation 10%; no claim it is a fitted comparator.

Follow-up figures and investigations change pixels/patches, router temperature or an expert output using saved weights only. Such edits are mechanism explorations, not selected validation variants or new fits. Source-only inference interface; labels are used only to score. Distinguish batch route-count summaries from demonstrated semantic specialization. Hand-derived routing/capacity/gradient/cost fixtures are constructed mathematical calculations, separate from model measurements.

## Conservation and corrections

| Original learning agenda | Manuscript disposition |
| --- | --- |
| Conditional compute and model size motivation | §§1/6 separate stored parameters, active matrix arithmetic and whole-model costs; remove unverified commercial-architecture and budget assertions. |
| Router, top-k, expert formula and code | §§2–4 give both normalization conventions, local derivatives, full working sparse dispatch and independent dense reference. |
| Load balance, capacity, z-loss and precision | §§4–5 distinguish assignments/tokens, group scope, bilinear surrogate, stable log-normalizer and floating-point range. |
| Complete trainable demonstration | §8 uses actual image tokens with attention, all declared fits, source-only prediction, observed/synthetic boundaries and full source, rather than a positionwise FFN asked to copy an independent neighboring random vector. |
| Specialized expert heatmaps | §§1/8 use actual measured assignments with causal intervention questions; remove unbound “language/syntax expert” labels and invented curves. |
| Expert/data/tensor/pipeline parallelism and cost | §6 explains actual axes, payload assumptions, small expert batches and backend work. No N-times-token-traffic formula or universal inference capacity guarantee. |
| Fine-grained/shared experts and DeepSeek | §7 separates sigmoid affinity, selection bias and original-score weight, complementary sequence auxiliary term and feasible node choices. |
| Fine-tuning, distillation, upcycling and applications | §9 includes exact copy-function argument, symmetry derivative, task-granularity extraction and connection to vision/hybrid architectures. |
| Practice and alternate resources | Twelve changed questions with closed hints/solutions; original arithmetic, scenarios, real experiment and annotated direct sources/video route. |

Specific errors corrected: selected-logit softmax equals full-softmax-then-selected-renormalization both in function and local gradients. Normalized top1 has zero task router gradient; full-softmax top1 need not. Unselected router logits can receive task gradients through a full normalizer even when their expert is not executed. Switch-style uniform balance reference depends on top-k count normalization and is not a guaranteed global minimum; the independent .88275 counterexample is retained. Z-loss is not entropy or a mean-zero constraint. BF16 and FP16 have different ranges. A capacity factor1 is not a no-drop guarantee. Fine granularity/shared branches are not free, parameter labels need per-layer/full-model distinction, and hardware timings cannot be manufactured from complexity.

The title remains accurate; added scope fits MoE teaching without a rename. Sparse attention, expert routing and cross-attention are explicitly distinct. The actual module successor remains Interleaved / Cross-Attention, not Jamba. No new catalogue audit or publication occurred.

## Canonical-reference coverage and actual research

Retrieval 13 September 2026. Canonical agenda uses ST-MoE's actual complete contents plus the original sparse-layer/Switch contents, all read through primary HTML. The inspected ST-MoE map is: background; training stability (multiplicative interactions, noise, activation/gradient constraints, precision); fine-tuning (generalization, parameter subsets, protocols, drops, sentinels); expert count/capacity/routing design; experiments; encoder/decoder/multilingual specialization; related work/discussion; appendices A balance, B z-dynamics, C architecture, D prioritized routing, E data, F fine-tuning sensitivities, G threshold, H parallel layout, I communication and J negative results.

Disposition: background and routing are core §§1–4; stability and precision §5; tuning/design/specialization §§7–10; architecture and parallelism §§1/6. Source-specific large experiments, sentinel recipes, threshold tuning grids and negative-result lists remain annotated deeper reading because importing them as a universal current recipe would be misleading. The lesson includes complete local evidence and exact contrasting mechanisms instead. Upcycling, current affinity/selection-bias separation and task-level deployment fill useful gaps beyond that historical agenda.

| Primary source | What was actually read | Use and limit |
| --- | --- | --- |
| Shazeer 1701.06538v1 HTML | Complete contents; §§1–4 including equations/noisy gates, shrinking batches, bandwidth, importance/load distinction; beginning §5 historical setups | Sparse component and historical mechanism; no unreviewed Appendix A estimator implementation or reproduced language benchmark claim. |
| Switch 2101.03961v3 HTML | Complete contents; introduction, §§2.1–2.2 equations/normalization/capacity/load; selected precision/initialization excerpts | Top1 retained-probability route and capacity conventions. §5 is an annotated further-reading location, not a claimed full distributed reproduction. |
| ST-MoE 2202.08906v2 HTML | Complete contents; background and §§3.1–3.3 stability/noise/multiplicative/numerical objective discussion and eq5 | Canonical completeness map and z-loss. No claim every fine-tuning/appendix experiment was read or reproduced. |
| Mixtral 2401.04088v1 HTML | Complete contents, §2 architecture/configuration and §5 routing analysis | Selected FFNs and empirical limits on topic-specific expert interpretation; no imported leaderboard/timing plot. |
| DeepSeek-V3 2412.19437v2 HTML | Complete contents, architecture through §2.1.2 full equations/bias/sequence loss/node/no-drop, §§3.1–3.2.3 infrastructure/context | Distinct selection/weighting and placement costs. Broader training/posttraining benchmarks not reproduced. No claim about undisclosed modern proprietary architectures. |
| Sparse Upcycling 2212.05055v2 PDF | Pages1–4 through background, architecture, algorithm and §3.1 design decisions | Reuse weights, selection directions and transition; own exact-preservation derivation. OpenReview bot challenge and v3 HTML failure were bypassed with official arXiv PDF; no source archive retained. |
| MegaBlocks MLSys2023 paper/official repository | Paper primary landing/abstract and repository README for dropless/block-sparse/grouped GEMM support | Bounded backend explanation and further reading. Full kernel paper body was not read; no latency reproduction claimed. |
| Expert Choice author article, Google Research | Full substantive article including row/column allocation and reported experiment discussion | Alternative representation; no source speed ratio imported as a universal result. |
| TaskMoE author article, Google Research | Full substantive article including task identity, extracted subnetworks, setup and reported comparisons | Useful non-token deployment application, no local replication or retained speed claim. |
| UCI Optical Digits | Current page metadata/license; complete original names from verified local bytes | Data rights and actual feature/source-writer semantics. |
| Stanford CS336 | Official2025/2026 schedule, linked pinned2025 lecture PDF location, official2026 recording playlist destination | Annotated advanced alternative route. Raw PDF fetch failed octet-stream parsing; recording retrieval failed. Neither slides nor video are claimed substantively reviewed; no technical statement depends on them. |

The primary mathematical calculations, resource examples and local experiment are original work, with formulas explained rather than lifted prose. Fresh source checks were limited to the questions needed for this packet. No broad source archive, new dependency or unrelated topic rewrite was necessary.

## Author closure and phase-two handoff

The author read the complete final manuscript in contiguous ranges, the full final visual specifications, data provenance and this design. These author checks are distinct from formal independent review. All original runtime source stays untouched.

Actual author closure on 13 September 2026: the declared twelve CPU fits ran once, retaining every condition/seed, selected checkpoint, curve and unfavorable result. Saved-model metric reload error is zero. A separate dense-all-experts reference agrees with sparse dispatch to 8.89e-16 in outputs and 1.78e-15 across parameter gradients. The closure program also checked the original source/data hashes, exact embedded training-program correspondence, a real saved-model prediction, twelve changed exercises, fourteen closed disclosures, resource arithmetic, balancing counterexample, fresh inputs and the expert-choice matrix. See `author-checks.json` and the complete numerical records. No fit was repeated for prose/specification repairs.

The author's learning-experience checklist is complete: the first-pass route introduces one routed FFN before distributed details; selected versus stored weights and full versus selected normalization have concrete arithmetic; loss gradients connect the routing mechanism to training; actual patch-image states make expert dispatch visible; each of the five investigations has a fresh unset prediction, meaningful edit, computed contrast and a checked null. The twelve independent tasks vary calculation, diagnosis, transfer and experimental design, with closed reasoning and solutions. Optional distributed, affinity/bias, upcycling and task-routing applications have local explanations and clear limits. Figures distinguish exact examples, measurements and cost models; no invented benchmark, universal expert specialization or interview/mastery guarantee is used. Measured plots preserve denominators and all outcomes. The full source program remains available without interrupting the first reading route.

Focused revisions clarified that selected softmax and full-softmax-then-renormalization are the same function away from selection ties; zero router task gradients can also occur for a particular scalar loss with top-two routing; temperature can change predictions' probabilities while preserving their leading class and routing counts; and an already-zero image patch is a valid unchanged-input case. The final specifications grade the quantity actually affected instead of promising a changed class. These are author assessments, not a learner study. Browser perceptibility, accessibility, interaction behavior, production payload, independent correctness and learning review, publication and integration remain deferred.

Retain the complete manuscripts/specifications/design/provenance, optical-digits.csv, two complete author programs, all selected weights/measurements/calculated fixtures and closure script/results. A finish request consumes this packet through the topic --work finish preflight, implements figures/labs, verifies portable calculations and actual UI states, resolves independent review and integrates publication. Do not retrain merely on resume; use the saved evidence unless a relevant correction justifies a new run.
