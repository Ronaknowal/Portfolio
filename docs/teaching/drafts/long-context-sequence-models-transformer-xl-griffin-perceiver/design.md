# Long-context sequence models — design and author record

Canonical ID: `long-context-sequence-models-transformer-xl-griffin-perceiver`. Current title retained. Module `deep-learning-fundamentals`, position17; predecessor Attention Mechanism (Bahdanau, Luong), successor State Space Models (S4, Mamba, Mamba-2). Mode: content first, stages1–2 only. Root owns the shared scope/ledger and checkpoint. Production publication remains planned.

## Actual baseline and scope

Read current `AGENTS.md`, authoring handoff, the full current teaching standard, domain strategy, topic design workflow, semantic code ownership/coordination/retention and topic-note workflow. Read the new `DEEP-LEARNING-ARCHITECTURES-CONTENT.md` scope. The per-topic command `node scripts/build-curriculum-inventory.mjs --topic long-context-sequence-models-transformer-xl-griffin-perceiver --work content` returned the title/advanced level, planned status, inherited model strategy and individual-design-required status, with no bespoke blueprint and no destination note. A scoped search of actual non-generated authoring sources found the title/level entry in `track-definitions.js`; it did not find a complete authored blueprint. This is the full existing teaching baseline for this planned topic, not an original manuscript that was silently discarded. The resolved unassigned bit-manipulation note is unrelated.

Starting source commit for the increment is `8c5da59f18516be77c29d5aeeafca3decca4f738`; this planned topic has no original JSX file/hash. Earlier packets and production sources are unchanged. The current task creates an individual design and full content packet without publishing it.

The title coherently promises three information-management strategies. Include Transformer-XL segment memory/positions; Griffin's RG-LRU and local-attention hybrid; Perceiver latent reads/processing; and short but complete IO/AR bridges needed to avoid implying every Perceiver is an autoregressive long-context model. IO output queries and the AR two-mask failure clarify the actual scope without requiring a title expansion. This is not an encyclopedic list of every long-context model.

## Ownership and local prerequisites

The preceding attention lesson owns Bahdanau/Luong encoder-decoder attention. This manuscript refreshes query/key/value meaning, softmax normalization, tensor row counts and causal legality before using self-attention-like operators. Dedicated Self-Attention, Transformer Block, Position Encodings, GQA/MQA/MLA and Sparse/Linear Attention appear later; do not assume those lessons were already read or silently reorder the route.

Coordination with `classical_feature_content` established ordinary scaled row-softmax as the local operator. An asymmetric Perceiver read changes query positions; it is not claimed to be a finite-feature exact reformulation of dense softmax. The next SSM topic owns continuous/discrete state equations, LTI convolution, selective Mamba and SSD; RWKV owns its versioned recurrence. This lesson does not substitute RG-LRU for those systems. Root's later Interleaved/Cross-Attention lesson owns modality/pretrained-model adapter integration; this lesson supplies the latent input/output interface bridge. Hopfield energy and Jamba-specific hybrids remain later independent lessons.

The author inspected the named mechanisms and plausible owners in this exact increment. No broad curriculum audit, source rename, prerequisite registry edit or runtime sequence change occurred. No unresolved new ownerless discovery needs a catalogue addition.

## Learner contract and continuing examples

The learner can follow short recurrent updates, weighted averages and an existing neural classifier but may not know transformer internals. The plain-language entrance-detail example introduces useful dependency versus mere input acceptance. Tiny scalar examples then expose each memory operation. A new real-data question is introduced explicitly rather than pretending those hand-made key/value numbers came from measured hand trajectories.

Observable outcomes: identify what remains stored; calculate a causal weighted read; trace segment memory and eviction; distinguish forward influence from gradient flow; explain relative-distance coherence; calculate RG-LRU retention/injection; distinguish individual recent access from recurrent summary; calculate a latent bottleneck and permutation null; repair a Perceiver AR indirect future leak; train/read a small classifier under complete-trajectory roles; and interpret resource counts without fabricated benchmarks.

| Hurdle | Local support | Evidence of learning |
| --- | --- | --- |
| Context length conflates four ideas | Three concrete workspaces plus input/direct span/state/useful dependency table | Identify which object retains an earlier detail and what must be tested |
| A mask is treated as zeroing values only | Exact2,1,3 weighted read; denominator change | Changed masked-average exercise |
| Cache reuse is imagined as recomputing history | Segment/layer paths, L×(M+L) query/key shapes | Build legal sets and predict a cache-eviction outcome |
| Detach means “ignore old state” | Identical forward/different derivative example | Changed derivative exercise and exact native probe |
| Position labels reset ambiguously | Relative-distance rulers with global-shift null | Explain two identical local labels and their distinct distances |
| Every gate is a generic forget switch | Retained/injected contribution trace | Input-only versus recurrence-gate comparison and changed decay exercise |
| A latent is confused with an old token | Asymmetric read plus mean-collision counterexample | Construct a collision and an informative query |
| Permuting storage equals reversing time | Attached position/value records | Paired permutation versus temporal reassignment |
| A final causal mask repairs an earlier leak | Two-mask Perceiver AR path example | Identify target→other latent→current latent detour |
| Larger architecture should always win | Actual mean and ordered-coordinate baselines and four seed results | Interpret unfavorable/variable results without tuning test |
| Quadratic compute implies quadratic KV cache | Exact score products versus separate byte formula | Calculate a changed cache budget and exclusions |

The first-pass route is stated immediately after the introduction: sections1–6, the two programs in7 and selected practice. Section8 is explicitly deeper and carries the remaining practice. Eighteen inline figure contracts and four distinct investigations support actual relationships; the count is a consequence of the scope, not a quota. No generic model-selection scoreboard replaces the memory/gate/latent entities.

## Canonical-reference coverage decisions

The canonical references here are the defining papers; there is no single textbook chapter covering this exact trio. The actual mechanism section lists were inspected, not inferred from model names.

| Inspected canonical structure | Content decision |
| --- | --- |
| Transformer-XL3.1 vanilla segmented LM;3.2 state reuse;3.3 relative positions;4.1 main results;4.2 ablations;4.3 relative effective context;4.4 generation; appendixB relative computation | Core3 covers fragmentation, detached lower-layer memory, causal access and all four relative-score terms. Deeper8 covers gradient/resource/evaluation distinctions. Relative-score implementation optimization remains an annotated research route; historical generated text and speedup tables are not reproduced as local measurements |
| Griffin2.1 residual block;2.2 gated MLP;2.3 temporal mixers;2.4 RG-LRU;3 scaling/downstream;6.1 context benefit;6.2 copy/retrieval; appendixA gate behavior | Core4 gives exact RG-LRU and complete architectural placement; distinguishes R/R/local attention and finite state. Deeper8 covers affine scan and evaluation design. Large training/scaling/throughput studies are referenced in context, not rerun or converted into a universal model ranking |
| Perceiver3.1 asymmetric attention/latent tower/iterative reads and sharing;3.2 position features;4 modalities and supplement ablations | Core5 covers all mechanism categories with exact weighted reads, Fourier-feature explanation, set invariance and explicit bottleneck collision. Small real hand-trajectory example trains a two-read latent model; ImageNet/AudioSet/point-cloud benchmarks remain original-source routes |
| Perceiver IO3.1 encode/process/decode;3.2 output query construction; structured-output examples | Core5.3 explains output count from queries and a coordinate request. Optical-flow values are not fabricated. Root owns the later broader cross-attention integration lesson |
| Perceiver AR input/query/target alignment and causal masks, training/inference; task/evaluation section list | Core6 fully teaches the causal fix and indirect leak. Creator slide sequence supports it. Full generation, sampling, caching and long music/book reproduction are deeper reference tasks, outside the CPU demonstration |

Important nuanced corrections made before writing: a long input is not successful long-range retrieval; Griffin's fixed state is not a separately addressable token cache; the paper's local-attention paragraph has an imprecise “no longer quadratic” KV-cache phrase, so this lesson derives linear cache bytes separately; RG-LRU is not defined as continuous-time discretization; original Perceiver is noncausal; output masking cannot repair a future-contaminated latent; paired permutation differs from temporal reassignment; detached cache values still contribute; ordinary caching's equivalence needs the same projections, positions and legal keys; and historical speedup figures do not compare against every modern cached/fused implementation.

## Research and claim locators

Research date13 September2026. The list distinguishes actual read material from located recordings or unexecuted large-model code.

1. [Transformer-XL original paper](https://arxiv.org/pdf/1901.02860): introduction/problem statement and model sections3.1–3.3 in full relevant passages, including equations, layer-shift explanation and memory-length paragraph on pages2–5. Read4.2 ablations and4.3 RECL explanation/table context on pages6–8, plus4.4 heading/role. AppendixB was identified as the efficient relative-position route; its entire implementation proof was not claimed executed. These passages support core3/deeper8. No1800× headline imported as a new benchmark.
2. [Griffin original paper](https://arxiv.org/pdf/2402.19427): architecture sections2.1–2.4, equations1–4, c8 and initialization/continuous-system distinction; section3 family's R/R/local mix and window default;6.1/6.2 context/copy/retrieval setup; appendixA log-space/gate comparison passages. The source's mean-so-far loss and retrieval studies are different tasks, reflected in the evaluation discussion. No full training or hardware replication claimed.
3. [Google Developers RecurrentGemma article](https://developers.googleblog.com/en/gemma-explained-recurrentgemma-architecture/): August29,2024 illustrated article, high-level hybrid, recurrent block/projection discussion, local-MQA section and linked code/tutorial route. Used as a second explanation, with exact equations from the paper. The article's broad context/performance wording is not a theorem or a current universal recommendation. No embedded image download or pretrained-model run.
4. [Perceiver PMLR paper](https://proceedings.mlr.press/v139/jaegle21a/jaegle21a.pdf): paper metadata, introductory figure description; section3.1 complete mechanism passage through iterative attention/weight sharing; section3.2 position/permutation and Fourier feature construction; section4 organization. Critical locators: explicit noncausal modules on page3; asymmetric N×M read and depth decoupling on pages3–4; paired position-permutation experiment and encoding on pages4–5. No full supplement evaluation replicated.
5. [Perceiver IO paper](https://arxiv.org/pdf/2107.14795): sections3.1–3.2, figure2/3 captions, input/latent/output shape contracts and output query examples; optical-flow example section with54-value patch description and study context. Used shape/meaning rather than unsourced new flow results. No video was watched and no full architecture implemented in production.
6. [Creator Perceiver/IO article](https://deepmind.google/blog/building-architectures-that-can-handle-the-worlds-data/): entire relevant article, architecture figure captions, optical-flow/modality examples and linked demonstration descriptions. It is an accessible alternative; historical state-of-the-art statements are not presented as current facts. Linked YouTube demonstrations were discovered but not played or technically endorsed as complete tutorials.
7. [Perceiver AR paper](https://proceedings.mlr.press/v162/hawthorne22a/hawthorne22a.pdf): introduction/figure1, autoregressive factorization and task length table, causal-fix description and evaluation section references. [ICML spotlight](https://icml.cc/virtual/2022/spotlight/17886): title/authors/abstract, paper/slide links and Video heading. [Actual20-slide deck](https://icml.cc/media/icml-2022/Slides/17886.pdf): extracted slide text, especially pages3–10 input/cross-mask/latent-mask/target/training explanation. The deck was read; video playback/access/transcript was not verified. Learner annotation says exactly that.
8. [PyTorch2.14 MultiheadAttention](https://docs.pytorch.org/docs/2.14/generated/torch.nn.MultiheadAttention.html): shapes and Boolean padding/attention-mask semantics. [Scaled dot product attention](https://docs.pytorch.org/docs/2.14/generated/torch.nn.functional.scaled_dot_product_attention.html): opposite Boolean mask convention, evaluation dropout, shape and backend/numerical notes. [CrossEntropyLoss](https://docs.pytorch.org/docs/2.14/generated/torch.nn.CrossEntropyLoss.html): unnormalized logits/class indices and loss contract. Stable URLs redirected to2.14; installed CPU version2.14.0 actually used. The displayed explicit attention implementation avoids assuming APIs share mask meaning.
9. [UCI Libras Movement](https://archive.ics.uci.edu/dataset/181/libras%2Bmovement): read the dataset page, purpose, collection summary, DOI and CC BY4 license. Read the full original .names file in Latin1, including coordinate interleaving,45-frame extraction,15class meanings,four performers/two sessions and absent row-level grouping identifiers. Byte-original files were reused from this increment's self-attention packet with that author's permission; exact source URL/digests are in data-provenance.md. Original source files remain unchanged.

Before freeze, source reconciliation exposed a concrete licensing conflict in an initially tried Japanese Vowels dataset: current UCI metadata says CC BY4, but the [original donor page](https://kdd.ics.uci.edu/databases/JapaneseVowels/JapaneseVowels.data.html), read in full, requires donor permission for commercial use. Donor-authorized relicensing was not established. The packet consequently replaces that dataset and all dependent results/program/provenance with clearly attributed Libras inputs; no disputed data or old model output is retained. This is a scoped resolution of one conflicting source, not a claim that all UCI licensing is unreliable.

Research is sufficient for the scoped mechanisms and alternatives; historical model specifications stay dated. An article's illustrations inspired the choice of representations, but the packet's layouts/examples are original. The real classifier and exact numerical fixtures supply new calculations, not reproduced prose or benchmark plots.

## Author computations, findings and phase boundary

`sequence_mechanisms.py` is complete and executed. It computes causal attention with actual masks, segmented cache states, distance bias, RG-LRU scalar updates, asymmetric latent reads, invariants and resource arithmetic. `latent_trajectory_classifier.py` is complete and executed on the retained real data, fitting two fixed logistic baselines plus four small neural runs and storing selected weights/logits/confusion matrices and epoch histories. Both programs received an author reread.

The mean baseline has54/60 test errors; ordered baseline22/60; one-latent seeds11/29 have32/25; four-latent seeds11/29 have28/34. These unfavorable/variable neural results are kept. They separate the value of preserving ordered geometry from the successful fitting of a learned bottleneck. No post-test optimization or universal architecture claim is made.

Additional bounded probes check the detached derivative6versus12; exact duplicate grouping and disjoint330-row roles; validation source row7's first-point-only class change1→10; a point23x reflection that changes probability while leaving class1; retimed versus paired records; and masked-padding invariance. Equal-value softmax evaluates to7 within1e−12 and two associations of three affine maps agree within floating-point tolerance at(.08,2.52). `investigation-checks.json` retains exact outputs and the fixture-selection rule. These are supporting author calculations, not independent phase-two review.

Author closure records the final complete manuscript and visual-contract reread plus the distinct learning-experience checklist after replacement: the entrance-detail motivation and first-pass route lead into refreshed Q/K/V and three concrete memory objects; every new symbol/model shape is introduced locally;18inline figures place help at the hurdle;4investigations use genuine inputs, unset bound predictions and checked contrasts/nulls;8changed practice problems retain closed hints/solutions; resource alternatives distinguish read slides/articles from unplayed video; quantitative resource claims are arithmetic rather than hardware results. The real-data episode teaches setup, training, measured results and their implications, including duplication/group limitations and a stronger simple baseline. Local file links, displayed model counts/data roles/results, mask conventions, position attachments and mathematical fixtures receive bounded author reconciliation. Root binds the content checkpoint after scoped reconciliation. No browser rendering, screenshot, accessibility/performance campaign, production integration, publication or formal independent review has occurred.

Phase two must build the complete manuscript and topic-specific figures/investigations, export the small model/data arrays into semantic lazy-loaded runtime files, verify the translated forward math against the retained outputs, execute relevant current example checks and complete independent teaching/correctness plus desktop/narrow/keyboard/visual/integration review. It may improve a representation with a recorded reason. It must preserve the memory/position/mask distinctions and the honest empirical comparison, not replace them with a generic simulation or a larger model's unmeasured result.

All pending manuscript/specification/research/data/program/result files are required handoff inputs. Remove only this packet's disposable Python import cache after author closure. No shared scratch/runtime deletion or catalogue edit is needed.
