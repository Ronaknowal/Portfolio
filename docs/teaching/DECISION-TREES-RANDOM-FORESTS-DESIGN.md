# Decision Trees & Random Forests — authoring design

11 September 2026. Classical ML position 2, within the first-ten request. Stable title/ID/source preserved. Read the complete 1,113-line original, including both full programs, all visual fixtures, decision matrix, theory and six exercises. Exact original bytes are archived in the supervised baseline. Retrieved the individual topic plan and read its returned routing notes: no unresolved destination instruction for this owner. Linear & Logistic Regression is the local predecessor; KNN is the actual successor. Current teaching/code standards and approved Linux philosophy apply; the arrangements below fit tree mechanisms.

## Coverage and repairs

Preserve CART classification/regression, impurity, greedy threshold search, complete from-scratch tree and forest, library workflow, per-node feature sampling, bootstrapping/OOB, variance/correlation theory, pruning, hyperparameters, importances, categorical/missing/weighted behavior, extrapolation, implementation complexity, Extra Trees, comparison/limitations and changed exercises. Add needed cost-complexity mechanism and coherent fit/validation/test protocol; these were previously named without a complete treatment. No title change is necessary.

Original errors to repair: no-scaling rhetoric incorrectly describes linear models and overstates nonlinear-monotone invariance of unseen predictions; tree interactions are incorrectly portrayed as impossible; random-forest mean probabilities are confused with hard voting and called automatically calibrated; invented accuracy/speed/size rankings replace evaluation; finite-sample OOB is called unbiased/free and allowed unlimited tuning; bootstrap independence is not conditioned on the shared dataset; feature-sampling gains are guaranteed. The scratch tree does not enforce child min_samples_leaf and its documented max_depth=None errors, uses global RNG, stops all zero-gain splits, and stores only labels. Thresholds at training values differ from midpoint inference between them; neither convention changes those distinct training-value partitions under <=. sklearn tie-breaking is randomized among features even with splitter=best and reproducible with a fixed seed, not an unconditional deterministic rule.

Other repairs: sklearn numeric/category/missing support is version-specific, class weighting is not fully equivalent to arbitrary resampling, 99:1 labels do not mean no usable split signal, permutation importance is model/metric/distribution reliance rather than true or causal importance, a leaf mean is not necessarily an extreme target or a nearest-neighbor prediction, and log targets/time features/monotonicity alone do not make constant-leaf extrapolation possible. Separate depth-dependent inference, node search implementation and balanced/worst-case training cost. Remove unrun benchmark heatmaps while retaining useful qualitative comparisons. The original two-moons experiments remain as fresh complete comparisons with actual output, not inherited numbers.

## Learning route and representations

1. Define a synthetic inspection task: two available measurements, observed pass/fail, train/validation/test ownership, prior baseline. A small learned question routes to a leaf distribution. Inline training-versus-inference map.
2. Connect one <= question simultaneously to a branching rule and a cut through the feature plane. An eight-row fixture carries IDs/labels throughout. Inspect a query, equality, depth and min leaf. Rectangles encode actual leaf probability, not an invented smooth boundary; symbols also encode labels. A text path and row/count tables remain available.
3. Derive Gini from two independent draws with replacement, entropy and weighted gain. Enumerate candidate thresholds, sort, enforce both child counts; inspect chosen split and its exact weighted ledger. Midpoints represent training partitions, not invariance under arbitrary transform for new points.
4. Greedy search versus expressive capacity: four XOR corners have zero root gain yet a two-level tree can represent them. Explicitly compare allow-zero versus stop-at-zero policies. Explain ties and stopping separately from inability to represent interaction.
5. Derive regression leaf mean/SSE reduction and demonstrate piecewise-constant extrapolation with actual predictions. Extend absolute-error/Poisson choices with stated target/leaf contracts, not a claim all trees predict means.
6. Grow, constrain and prune. Cost-complexity R(T)+alpha*leaves uses root-weighted leaf impurity. Enumerate every pruned subtree of the tiny fitted tree, draw actual objective lines and choose the minimum for changed alpha; native sklearn pruning is a separate complete run. Hyperparameters selected on development data only.
7. Complete from-scratch binary numeric tree/forest with valid shapes, local RNG, true per-node feature subsampling, minimum child sizes, distributions, repeated bootstrap multiplicities and exact OOB eligibility. Scope limits explicit; native optimized threshold prefix counts contrasted with bounded browser search.
8. Bootstrap/OOB ledger: visible sampled row IDs (duplicates are multiplicities), selected query and eligible OOB trees. Missing OOB coverage shows unavailable. Additional tree probabilities and OOB counts are calculated from the same finite ensemble. Distinguish one-tree OOB from aggregated row predictions and from a final test.
9. Mean probabilities versus hard votes shown with contrasting leaf distributions; covariance sum derived before equal-variance average-correlation simplification. A parameterized variance figure is a mathematical model, not measured forest performance. More trees reduce finite-ensemble Monte Carlo fluctuation; they do not necessarily remove data uncertainty, bias or error.
10. Fit/evaluate a full pipeline retaining the moons use case and adding baseline, held-out candidates, probability/cost metrics and appropriate OOB interpretation. Explain categorical encoding, train-only imputation and current missing routes with complete actual examples.
11. Explain what importance answers. Inline row permutation diagram and changed grouped/correlated-feature experiment distinguish original feature blocks, model reliance and impossible combinations. Native MDI/held-out permutation comparison uses an explicit metric; no universal rank claim.
12. Independent changed calculations, implementation bugs, XOR, OOB, categorical ordering, extrapolation, weighted probabilities, pruning and an executed changed-data report. Extend to Extra Trees and balanced/degenerate complexity, memory and actual next topic.

Representations have no quota. Keep code-native SVG/HTML labels readable at 1440/390/320, bounded computations, local horizontal scroll only where justified and visible scroll cues. All plots use their live source model or recorded native outputs. Controls need prediction, meaningful changed states, keyboard labels, reset and numeric alternatives. Essential explanation remains visible before optional technical branches.

## Research and verification contract

Read current sklearn1.9.1 tree/forest guides and relevant estimator APIs; inspect actual installed code where compressed documentation is ambiguous. Inspect Breiman2001 strength/correlation assumptions rather than equating his margin correlation with arbitrary predictor-output correlation. Inspect official cost-complexity and permutation-importance examples. Curate a direct lecture with substantive course notes/companion review, recording what was actually read versus video metadata. Historical priority is optional, not a substitute for mechanism.

Native programs must all execute independently with full data, imports, actual outputs and tested versions. Verify split masks/gains through independent enumeration/Fraction; tree partitions and probability sums; all pruned subtrees and equality boundaries; explicit bootstrap/OOB identity; covariance formula with independent matrices; and fresh library outputs. Browser QA covers every control, equality/corner/reset/keyboard cases, questions/hints/answers, all code/output and headings, errors/overflow and actual opened screenshots. Freeze only after fixes and checks; independent review and integrated production checks follow. No claim of learner-study validation or user acceptance.

## Implemented and author-checked

The completed body has fourteen connected sections, seven mechanism-specific investigations, two inline figures, ten complete executed programs and thirteen changed practice tasks. Its references annotate the current sklearn tree, forest, pruning and permutation-importance documentation, Breiman's forest paper, and an instructor lecture paired with substantive notes. The text separates the general covariance identity from Breiman's strength/correlation result, and library behavior is tied to the executed sklearn 1.9.1 environment.

[The source-versioned author record](evidence/decision-tree-author-review.json) binds the native review, full three-width browser review and final focused figure amendment. Native evidence includes 1,952 node checks, 56 tree variants, 64 explicit OOB cases, all ten actual programs and the learned-proximity positive-semidefinite construction. Browser checks cover every investigation's controls, reset and keyboard behavior, practice, code/output, anchors, sources and overflow. Opening the figures exposed an inherited mobile SVG font rule; the final topic-scoped 13px rule and axis placement were checked at 1440/390/320 without repeating unchanged numerical programs. Independent review follows this author stage.

## Focused input-contract amendment — 11 September 2026

Independent review identified sparse-array validation and, for KNN, numerical norm edge cases. The final corrected source identities and focused checks are recorded in evidence/decision-tree-input-amendment.json. Forty-five JavaScript input/conservation cases passed. KNN additionally passed six native distance scales, two explicit overflow cases and actual browser checks of the two changed code/output blocks. Unchanged full author checks are reused; independent closure follows this amended version.
