# Decision Trees & Random Forests — independent review

Closed 11 September 2026 at 14:32:00 UTC by `/root/scientific_visual_improvements`. No unresolved material finding remains within this bounded review. This is implementation evidence, not user acceptance or a learner study.

The [independent packet](evidence/decision-tree-independent-review.json) binds the six final production hashes, original [author packet](evidence/decision-tree-author-review.json), and [input amendment](evidence/decision-tree-input-amendment.json). The original author freeze at 13:17:59 remains intact. The original model is retained in the author's [amendment archive](archive/tree-knn-input-amendment/manifest.json); only its input validation changed. Body, examples, labs, CSS and blueprint remain at their original frozen hashes.

## Source and teaching assessment

Read the complete fourteen-section body, thirteen changed practice tasks, pure numerical model, all seven investigations and CSS, individual blueprint and design, and the exact displayed `BinaryTree`/`BinaryForest` definitions. Reviewed the author's actual program and browser records separately; the original pre-rewrite body's preservation is attributed to the recorded baseline, rather than claimed as a fresh line-by-line comparison here.

The lesson explains the split-to-region-to-leaf relationship before optimization vocabulary. Its weighted Gini/entropy arithmetic, regression mean and SSE decomposition, zero-gain XOR distinction, midpoint qualification, complete scratch implementation and root-normalized pruning costs are consistent. OOB eligibility respects bootstrap multiplicity and unavailable coverage. Probability averaging, shared-data dependence, the covariance identity, conditional variance assumptions, Breiman's specialized margin result, held-out feature reliance and the leaf-one-hot proximity construction are appropriately distinguished. The changed practice has worked acceptance reasoning and a genuinely changed held-out experiment.

The current [sklearn forest guide](https://scikit-learn.org/stable/modules/ensemble.html#forest) explicitly distinguishes its averaged class probabilities from historical hard votes; that supports the lesson's aggregation example. The relevant guide passage was read directly. No video viewing is claimed by this reviewer.

## Finding and focused closure

`Array.some` skipped holes: `impurity(Array(2))` returned zero, and a sparse two-coordinate query traversed a plausible leaf using undefined values. The author changed validation to inspect explicit own indices before arithmetic. This is outside the dense UI controls, so a complete browser repetition was unnecessary.

The [complementary numerical record](evidence/classical-tree-knn-independent-numerical.json), checked at 14:31:21 UTC, includes 24 positively affine-transformed tree queries, the three sides/tie of the exact pruning crossover `15/128`, six bootstrap-prefix/OOB checks, and sparse rejections. The Python part executes the exact displayed definitions with changed XOR stopping policy, repeat fits and four adjacent-float threshold pairs, including very large and subnormal coordinates. It does not rerun the already verified complete demonstrations. The author's additional focused contract/conservation evidence is preserved in the amendment packet.

Reproduce the bounded complementary pass with `node scripts/verify-classical-tree-knn-independent.mjs`; it calls the actual workspace Python environment and saves its numerical results. It is shared with the separate KNN independent review, with each case's purpose explicit.

## Actual visual review and limits

Actually opened five author captures, individually hashed in the packet: desktop final partition, 390px final XOR, 320px final pruning, and narrow bootstrap/permutation states. The figures show actual fitted regions, objective lines and row identities; the bootstrap and permutation mechanisms use their own suitable visual forms. The narrow geometry retains readable labels through explicitly signposted local scrolling and equivalent text/data. These are selected reading checks, not a claim to have newly operated every control.

Reuse the author's full 1440/390/320 keyboard, reset, disclosure, code/output, anchor and layout checks, together with its later figure-font repair. Their unchanged render sources and ordinary fixtures are bound to the final packet. Arbitrary floating-point inputs, every possible data distribution and learning outcomes are not exhaustively proven by this finite review.
