# Perceptrons, Neurons & Activation Functions — independent implementation review

Reviewed 21 September 2026 by the Backpropagation implementation agent, independently of this lesson's author. **The scoped content, models and learner-experience review is closed with no remaining material finding.** Final shared production build, loading and browser integration remain the integration owner's work. This is a reviewer heuristic assessment, not a study with a beginner or a screen-reader audit.

## Scope and source binding

Read the complete prepared manuscript, visual/lab specifications, design and provenance in `docs/teaching/drafts/perceptrons-neurons-activation-functions/`, then the complete implemented body, pure models, retained data structure, displayed programs, three component modules, stylesheet and stable-ID blueprint. Compared all prepared obligations with the actual mechanisms and source. The source hashes in [the independent receipt](evidence/perceptron-independent.json) bind the final repaired implementation; [the reusable verifier](../../scripts/verify-perceptron-review.mjs) regenerates those checks and hashes.

Reused source-applicable evidence rather than rerunning the complete experiments: [three native programs and all 18 matched training runs](evidence/perceptron-native.json), [author model checks](evidence/perceptron-models.json), [the dense activation oracle](evidence/perceptron-activation-oracle.json), and [the refreshed author browser receipt](evidence/perceptron-browser.json). The latter was run at the shared development preview on port 4197, at 1366, 390 and 320 px, with seven behavior groups per width and 39 captures. It is not a production receipt. The oracle compares values and slopes for nine functions at 481 points each against the actual retained PyTorch runtime (8,658 comparisons).

## Complementary correctness and coverage

Ran **100 complementary assertions** after the author's final source freeze. The checks deliberately target relationships beyond the saved oracle: 54 off-grid finite-difference slopes for all nine functions; projection feet on the boundary; displacement parallel to the weight vector; agreement between geometric distance and the reported signed distance; positive common-scaling invariance at changed states, including a tie and an off-window projection; the changed XOR repair that breaks other corners; triangle area; feature-width parameter counts and separate sequence-axis activation storage. The largest slope discrepancy was **2.043 × 10⁻¹⁰**, below the stated 2 × 10⁻⁸ threshold. The checker also confirms current displayed programs match the executed native receipt, and that every retained training/count observation lies inside its stated chart domain. These finite checks support the offered models; they are not a general proof of arbitrary neural-network behavior.

The complete prepared depth is present: weighted sums, geometry and scaling; the perceptron update and convergence argument; XOR impossibility and explicit ramp construction; row-batch tensor shapes; core and deeper activation values/derivatives; the selected real UCI digit images and 18 matched training runs; diagnostic distinctions; smooth gates, SwiGLU, parameter/storage calculations and approximation by ramps; eight independent practices with 16 separate hint/solution disclosures; complete code and annotated further reading. No scientific scope was replaced by a generic lab template. The same-state/matched-start protocol and the distinction between training loss and validation counts are explicit. The canonical-reference coverage decisions in the packet remain represented, including the deeper routes and deliberately deferred systems/training subjects.

The native evidence executes the displayed programs, and the measured curves preserve the actual observation positions, including update zero. Logarithmic loss makes late differences visible without hiding initialization. The validation count axis starts at zero by default, with an explicitly labeled optional truncated zoom; equal counts remain equal. No per-image output beyond the retained observations is invented.

## Findings and author repairs

1. **Readable prose:** emitted text inherited collisions such as “weight−2,” “between0 and1” and “images,40.” The author normalized ordinary prose and table text while preserving executable code, mathematical expressions, stable identifiers and the frozen packet. Reviewed the resulting source and refreshed captures.
2. **Separate tensor axes:** §7's parameter-count paragraph used “the same widths” after an activation-memory example whose 4096 was a sequence length. The author now explicitly defines input/output feature width d = 4096 and hidden width h = 16384, and distinguishes d from the coincidentally equal sequence length. The arithmetic is unchanged and independently checked.
3. **Related integration repair:** the author also adopted the integration owner's fixed-domain contribution bars (geometry ±16, live XOR ±8). Reviewed the final source and affected phone captures; the refreshed browser assertions require a doubled contribution to have double bar width. Constant static worked cases retain their declared scale. The boundary caption now accurately distinguishes filled target-one corners from the selected correspondence.

These findings are resolved. No numerical model repair or repeated full training run was warranted.

## Independent nine-point learning-experience assessment

| Requirement | Concrete review finding |
| --- | --- |
| Route | The introduction gives a first-pass core route and useful prerequisite links. Deeper activations, gating, memory and approximation are labeled; the route is integrated into the body rather than duplicated by a generic guide. |
| Cautions | Local caveats explain distinct limitations: hard thresholds, zero normals, operating-point sensitivity, real-data validation reuse and resource accounting. The programs show mechanisms and numerical outputs rather than printing warnings. The repaired resource paragraph removes an actual ambiguity rather than adding a generic caution. |
| Real question | The opening follows how an input becomes evidence and a decision. Real 8 × 8 digit images become an explicit 64-coordinate row, then a 280 × 64 training matrix; the measured comparison returns concrete loss and validation-count results. |
| Live investigations | Geometry, XOR and activation labs expose current results immediately. Inputs change the linked diagram, values and explanation; pin/reset enables comparison. Controls include numeric values beyond named cases. Author evidence covers rescaling, null weights, invalid input, ties, failed one-corner repair, inactive features, SiLU's negative slope and ReLU's corner. No learner prediction or answer-unlock feature is present. |
| Figures | Inspected the weighted sum, plane/projection, XOR correspondence, digit flattening, recorded comparison, SwiGLU coordinate multiplication and ramp construction. Baselines and axes are explicit. Phone layouts stack related stages, retain numeric tables with local horizontal scroll and keep mechanism labels readable. Fixed live bar scales make magnitude edits perceptible. |
| Connections | Score scaling is linked to unchanged geometry and changed sigmoid confidence; activation slope is separated from incoming weight and input sensitivity. XOR's failed linear conditions connect to its hidden-feature repair. Projection shapes, measured loss/count distinctions and the correspondence between summed ramps and changed slopes are stated. |
| Code | Three complete displayed programs preserve their actual native outputs. Mechanism and experiment setup occupy the code; dense oracle and review validation live in separate tools. The data path and attribution are provided. |
| Practice | Eight tasks vary inputs and decisions: changed geometry, XOR constraints, tensor shapes, activation behavior, experimental interpretation, gates and resource accounting. Hints and solutions are closed separately, and exact checkpoints support independent numerical reproduction. |
| Screenshots | Independently looked at desktop weighted/XOR/comparison/SwiGLU/triangle figures and phone geometry, XOR, activation, digit, comparison, SwiGLU and triangle states. Also inspected the final repaired 320 px geometry and one-corner failure, zero-normal case and SiLU negative-slope capture. These complement the author's complete 39-capture set; not every capture was independently reopened. |

Inline visuals were assessed as representations of their mechanisms, separately from controls: summation bars explain signed contributions; the equal-scale plane preserves perpendicular distance; XOR coordinates match the full truth table; image flattening keeps source pixels; the gate/value table aligns corresponding coordinates; shared-axis ramp panels expose each change in slope. The rendered full-width training chart makes the late-loss contrast visible while preserving the tied-count conclusion. No unresolved representation mismatch was found in this scope.

The final shared build and production browser run should reuse these checks against unchanged source, then record their own result. No deployment, learner acceptance, fresh-install reproduction or watched-video validation is claimed.
