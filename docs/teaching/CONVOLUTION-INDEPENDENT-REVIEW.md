# Convolution implementation: independent review

Reviewer: `/root/residual_implementation`, 22 September 2026. Author/integration owner: `/root`. This reviewer authored the neighboring Residual lesson, not this Convolution implementation. Scope: full prepared revision-3 manuscript/specification and the new Convolution page, mechanism models, visual components, canonical native programs, recorded outputs and existing author evidence. No retraining, browser automation or mutation of authored sources was performed by the reviewer.

## Current conclusion

The complete teaching route and owned scratch/library mechanisms are present and mathematically sound in the reviewed scope. Complementary independent checks pass. **All four scoped presentation/evidence findings are corrected and confirmed below.** Parent owns production build, actual desktop/phone image inspection, keyboard/pointer closure and final delivery ledger. This record does not certify those pending surfaces or user acceptance.

## Full-packet teaching and correctness assessment

The rendered page preserves the written path from local weighted sums through channels, a worked filter update, window geometry, pooling, receptive coordinates, real digit comparison, effective versus theoretical fields, shift sensitivity, transpose scatter, execution strategies and explicit pullbacks. Thirteen section headings, all eight main changed-input problems/solutions and the new pooling/stride/adaptive modification tasks remain. Scalar arithmetic is introduced before dense notation; ordinary library conventions and prerequisites are explained. No convolution or pooling API call replaces the owned scratch operation: the general direct oracle exposes groups/stride/dilation, while the separate vectorized NumPy route exposes dense-valid forward, weight/input/bias pullbacks and explicit max/mean/adaptive pooling gradients.

The cost and abstraction boundaries are appropriate: vectorized tap contractions avoid an explicit im2col allocation; the direct indexing reference is identified as an oracle rather than an optimized image processor. Channels-last, fusion, fixed-stat BatchNorm folding, transposed output-size ambiguity and shape-versus-alignment distinctions remain. The headline computational claims do not extend the valid dense pullback beyond its stated stride-one/no-padding boundary. Stride-two is an independent extension task with a concrete same-state derivative comparison. The backend owns optimized kernels and autograd composition, which is an appropriate reuse boundary.

The real-data experiment keeps an explicit dense baseline, matched CNN initial tensors, 280/120 development split, three seeds and actual shift failures. It does not conflate accuracy with CE, parameter count with runtime, development selection with untouched testing, or global pooling with whole-image shift invariance. Saved maps correctly remain seed-1/update-400 cases even when other evidence is selected; raw preactivations/logits and missing bias values are not invented. No GPU benchmark, arbitrary model inference or additional fit is implied.

Representation choices match the subject: editable image/filter/output grids expose spatial correspondence; sharing tables and gradient rails expose summation; geometry and ancestry rulers expose alignment/holes; pooling contrasts expose nonunique reconstruction; exact influence bars separate path coefficients from empirical gradient maps; scatter footprints expose adjoints and checkerboard overlap. These are live inputs with immediate results, not prediction-entry tasks. Small arrays and bounded integer geometry limit browser work. Complete native programs and recorded image maps load on explicit disclosure, not globally. The remaining concerns are listed below rather than hidden by the numerical passes.

## Complementary checks actually executed

The author already replayed all twelve full fits and checked decoded record equality. That expensive run was not repeated. Reviewer commands:

```text
scratch/lesson-tools/Scripts/python.exe -X utf8 -B scripts/verify-convolution-independent.py
node scripts/verify-convolution-independent.mjs
```

- Native: **77 actual cases** in [the separate native record](evidence/convolution-independent-native.json). Four changed rectangular/single-spatial/batch/channel convolution cases use a different sliding-window lowering plus bilinear directional adjoint identities; 25 adaptive cases compare prefix/difference-array code to a separately constructed dense bin-incidence matrix, including one input and expansion-shaped bins; 48 max/mean cases use non-unit mixed-sign upstream cotangents, ties, negative inputs, kernel edges and overlapping/strided windows against PyTorch.
- Browser-model mathematics: **225 source-ancestry cases**, **18 exact polynomial-profile cases**, and **27 transpose-adjoint cases**, retained in [the separate model record](evidence/convolution-independent-model.json). The ancestry oracle pushes source identity sets forward rather than repeating the implementation’s backward tracing. Integer multinomial coefficients of `(1+z+z²)^L` independently check the recursive averaging profile and threshold sets. Arbitrary signed/zero kernels and stride 1/2/3 test inner-product identities for the transpose.
- Read all authored renderer/components/model code and the native forward/backward bodies. Compare complete prepared prose/specifications against the actual teaching homes, references, downloadable programs and practice. No failure in a complementary numerical case occurred. These are scoped cases, not universal proofs or a count of independent teaching outcomes.

The retained scripts start their evidence as failing/running, never replace author evidence, and make no source mutations. They do not certify browser paint or replace the author’s full fit record. Native source hashes are in the native record; the exact browser-model hash is in its record.

## Findings sent to the author

1. **Recorded learning trajectory missing as a whole.** The measured viewer originally showed one selected update and its baseline in a table, omitting the specified six-point training/development trajectory. Add a small chart containing only saved points, or a complete six-row trace alongside the selector, so the learner can see the optimization trajectory without memorizing six separate screens. Distinguish connecting lines from measured intermediate updates.
2. **Architecture strip identity.** The static `Flatten 64 → 10` strip above the recorded viewer could be read as the currently selected dense or global-average model. Explicitly label it as the reference flatten-head CNN, or bind its structure to the selected architecture. The real global-average model uses a 16-feature head.
3. **Public provenance still described deferred work.** The downloaded provenance opening said no website runtime changed and implementation was deferred. Preserve that historical preparation record but identify the later actual native replay and current implementation scope without claiming browser closure prematurely.
4. **Zero heatmap legend.** The signed Grid legend described zero as neutral, but its zero branch received a small amber alpha. Use the stated neutral zero background or amend the legend. Signed heatmaps already have exact numeric alternatives and explicit per-panel rescaling; retain those distinctions.

No unrelated curriculum or scope changes are requested. Closed-loop model diagnostics, general GPU execution, arbitrary edited-image inference and an additional learned architecture are not missing requirements of this scoped foundation lesson.

## Correction confirmation

All four corrections were inspected in the author’s final source on 22 September 2026:

1. `ConvolutionTrainingTrace` now displays all six saved updates, selected-model and dense-baseline series, a training/development partition control, and a full six-row numerical alternative. Its caption explicitly states that connecting segments are not measurements of intermediate updates. Both series use the actual recorded update and CE values.
2. The architecture strip now explicitly names the reference flatten-head CNN and separately states the dense `64→32→10` and global-average `16→10` heads. The selected saved map model remains independently labeled.
3. The public provenance opening now distinguishes the historical prepared packet, actual twelve-fit native replay, current website integration and saved-data/browser-calculation boundary.
4. The signed-grid zero case now uses neutral `#202020`, matching its stated legend. Positive and negative values retain their numerical alternatives and per-panel normalization notice.

The new chart and local CSS were reviewed for data identity, interpolation disclosure, responsive SVG sizing and exact-data fallback. Existing numerical model/native programs did not change for these corrections; their source-bound complementary checks remain applicable. The final implementation and packet hashes are retained in [the closed source record](evidence/convolution-independent-review.json). Browser and integration checks remain with the parent.
