# Spectral Graph Theory: independent review

Reviewer: workflow_visual_improvements, separate from the Spectral31 author. Scope: full actual lesson body, complete models, nine native programs, per-topic lab source, design, proofs and all changed practice. Review performed 10–11 September 2026 local time. No lesson source was edited by the reviewer.

## Result and concrete finding

No actionable mathematical defect was found in this bounded review. The derivations distinguish combinatorial and degree-weighted geometry, the normalized sweep uses y=D⁻¹/²v, and the isolate convention matches Graph30. RatioCut and Ncut are derived from the actual centered two-valued vectors; the spectral relaxations supply lower bounds rather than a promise of globally optimal rounded partitions.

The optional Cheeger argument was read step by step: a degree-weighted median supplies a smaller-volume strict side, shifting preserves edge energy and increases the centered denominator, and the 2|t| threshold distribution gives the claimed expected volume. The weighted edgewise Cauchy–Schwarz bound has the correct factor two. Exact normalized modes and numerically grouped threshold ties are kept distinct.

The row-normalized disconnected embedding correctly removes degree-dependent row lengths; whole-eigenspace rotations preserve distances, while ending inside a repeated block may change a chosen subspace. Lloyd updates retain a fixed row geometry, explicitly flag empty centers and distinguish occupied converged native restarts from the lab's retained empty-center trace. Heat, ridge, projection error, componentwise limits and the discrete periodicity counterexample use the stated operators. Effective resistance checks connectivity before giving the physical current interpretation; circuit units are separated from document similarities.

One narrow reading-flow finding was sent to the author: missing spaces around numbers in practice/reference prose, such as “bridge0.2”, “is4” and “Its2007”. The author repaired these joins and a subsequent printer-induced literal Unicode escape regression. The final source contains the intended mathematical characters and spaces; the author's actual rendered-question assertions pass. Neither was called a mathematical model failure.

## Complementary executed evidence

Command:

```text
scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-spectral-independent.py
```

The check passed at 2026-09-10T18:38:47.677128Z. Its [source](../../scripts/verify-spectral-independent.py) and raw `scratch/spectral-independent-review/results.json` are separate from the author's broader suite.

- Nine exact Fraction integrations use three changed weighted graphs and three signal patterns each. They integrate threshold probability over distinct coordinate intervals, including tied medians and one-sided shifted values, and verify each node's smaller-side probability, expected cut, expected volume and the squared Cauchy–Schwarz/sweep bound.
- Three changed graphs verify generalized normalized coordinates against an independently assembled Laplacian, eigenpair residuals and the degree-weighted orthogonality condition.
- Eighteen actual browser-model heat/ridge states are compared with independently called SciPy matrix exponentials and NumPy linear solves. The impulse output, positivity, conserved mass and squared-change readout agree.
- Nine changed circuit experiments use exact rational grounded elimination as the oracle for the actual lesson's native spectral-resistance helper. These are weighted paths, a triangle with a dangling branch and a square, rather than copies of the author's two-triangle fixture.
- The actual native row-embedding function is checked on disconnected unequal-degree components, an extra isolate rejection and permuted vertex order. A rational orthogonal rotation preserves the complete zero-subspace geometry. Three actual browser-model Lloyd traces agree with their all-coordinate losses and occupied-center means.
- An analytic square-cycle basis supplies a solver-independent repeated-block test: the full projector and ridge operator are invariant, while a single retained direction changes under rotation.

All nine complete programs were also executed while loading their actual native functions for these changed-input checks. Their displayed stdout preservation/exact-output assertions belong to the author's separate native suite and are not relabeled as new independent stdout assertions here.

Reviewed model SHA-256: `9612dfa5e766706dcad6b7e7b7edd5d54efaf0ae34585c108607ca007377b61e`. The finite complementary checks support the bounded implementation; they do not establish every possible input, empirical clustering quality or a machine-checked theorem.

## Primary-source cross-check and review limits

Independently inspected [Spielman's Lecture 6](https://www.cs.yale.edu/homes/spielman/561/lect06-15.pdf), sections 6.2–6.4 and the weighted threshold calculation, for the conductance/quotient and probability argument. Independently inspected the algorithm and nullspace passages of [von Luxburg's tutorial](https://arxiv.org/pdf/0711.0189), sections 3–4, for eigenvector-column/node-row conventions and the additional symmetric-normalized row step. The lesson's isolate qualification is necessary; the bare identity formula is not generalized to zero degree. Historical empirical or current-field claims from the 2007 tutorial are not inherited.

The author owns the full original-font browser review, native oracle suite, responsive geometry changes, screenshot opening and production-source freeze. This reviewer read their implementations and check scope; the author's reported counts are not a substitute for the complementary proofs and calculations above. Root owns build/loading/curriculum integration, and user acceptance remains separate.

## Final author identity

The author froze seven production sources at 2026-09-10T18:50:00.943703Z. All seven current fingerprints were independently checked against that freeze; the model hash matches the complementary numerical run above. The final body hash is `3f9acc7d7c524e9dbb145818c4ceacdf8c0b84179f0e88016e8e5fe061935e8d`.

The [durable independent evidence](evidence/spectral-graph-independent-review.json) contains the complete complementary results, script fingerprint and all seven final production fingerprints. The [author verification](SPECTRAL-GRAPH-THEORY-VERIFICATION.md) and [author evidence](evidence/spectral-graph-author-review.json) separately attribute the original-font 1440/390/320 interactions and the final responsive/prose checks. The reviewer additionally opened the final 320-pixel coordinate correspondence and checked its three equivalent coordinate contracts. No new full browser run is implied.

This bounded independent review is complete. Root production integration and user acceptance are separate.
