# Monte Carlo methods and MCMC — independent review

10 September 2026. Root reviewed the complete Math20 lesson and eight practice tasks, the transition/trajectory models and the executable original slice-based NUTS construction. Final body SHA256: `b7b5b6fdf3dc6fa27cf364806fb3a3774207cc8b59a5ab524727b970b454d9dc`. [Author verification](MONTE-CARLO-MCMC-VERIFICATION.md) records executed numerical and browser evidence; [durable source/evidence](evidence/monte-carlo-mcmc-author-review.json) captures this author freeze. Production integration is separate.

The lesson distinguishes target variation, Monte Carlo error and convergence conditions. Independent draws, antithetic pairs, importance weights and conditional averaging identify their independent units, support and finite-moment assumptions. The asymmetric Metropolis example includes proposal ratios and rejection mass. Its stationarity argument is distinct from irreducibility, starting-state convergence and finite mixing guarantees.

The harmonic trajectory gives an independently calculable reference for leapfrog, momentum reversal and energy correction. The constrained Beta example includes the log-odds Jacobian and separates parameter-space from transformed-space density. The momentum-covariance convention M and the Gaussian choice M = Σ⁻¹ agree with the inverse-metric convention checked in the [Stan manual](https://mc-stan.org/docs/reference-manual/mcmc.html).

Root checked the candidate construction against Algorithm2 and its surrounding conditions in the [original NUTS paper](https://jmlr.org/papers/volume15/hoffman14a/hoffman14a.pdf), including the explicit permission for early subtree return. The implementation keeps the initial candidate, uses random directional doubling and distinguishes an internally invalid new subtree from a later whole-tree turn. The former cannot enter the pool; the latter can stop expansion after a valid new pool was included. Selection is uniform over the stated slice-based candidate set. The lesson does not combine that rule with a different algorithm's transition weighting or present the educational stored-pool implementation as current production NUTS.

The finite two-state covariance sum, negative-correlation example and equal-budget thinning comparison explain function-specific precision. Rank/split/folded diagnostics and approximate batch-means MCSE have explicit limits; a reassuring trace or diagnostic is not a proof of exploration. The author's independent matrix, exact finite-path, quadrature and rank oracles complement this source review. The one-step stationarity experiment is correctly described as an empirical falsification check.

Root requested a deterministic answer for the changed-target final exercise. The amended solution gives the Beta(3,7) upper tail at0.70 as 0.004290894, with an explicit polynomial integral; exact fractions, independent quadrature and the Beta survival function agree. This reference is distinguished from seed-dependent estimates and acceptance rates.

Root opened the actual390px internal-subtree candidate diagram and320px ordinary Hamiltonian introduction. The candidate statuses, signed integration indices, probabilities and momentum distinction remain legible. The author's broader interaction/reading suite and targeted320/390px amended-solution checks are recorded separately, including its denied external-font request and intentional HMR isolation. This record does not claim root personally opened every saved image, reran the entire author suite, established every-target convergence or obtained user approval. No remaining substantive defect was identified in this bounded review.
