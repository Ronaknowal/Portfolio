# Mathematics control affordance review — 21 September 2026

This is a bounded review of the 57 implemented Mathematical & Statistical Foundations lessons. The user asked whether the misleading controls, incomplete interaction feedback and theme problems reported in Evaluation Metrics and Calibration existed elsewhere. This review does not rewrite the curriculum or certify that every lesson and every possible state is perfect.

## Repairs

| Topic ID | Changed runtime source | Confirmed finding and resolution |
| --- | --- | --- |
| `vectors-matrices-tensor-operations` | `src/learn/components/lesson-labs/VectorTensorLabs.jsx` | Four coordinate ranges were visibly labelled but their wrapping labels associated with the earlier `output` element. Unique input IDs and explicit label associations now support actual label activation and stable accessible names. |
| `matrix-calculus-jacobians` | `src/learn/components/lesson-labs/MatrixCalculusLabs.jsx`; `src/learn/components/lesson-labs/matrix-calculus-labs.css` | The same issue affected two base-coordinate ranges. A 320px exact-font check also found a long select expanding a 280px content column to 320px and the page to 340px. Controls now shrink within the column without truncating lesson content. |
| `gradient-descent-variants-sgd-adam-adagrad-rmsprop-lamb-lars` | `src/learn/components/lesson-labs/GradientVariantsLabs.jsx` | Momentum changes restarted the inspection at update 0, hiding their effect. The full, explicitly labelled loss preview recomputes immediately while the table and parameter-plane trace retain their inspection cursor. A scrubber complements sequential steps. Plain descent disables its unused momentum control; AdaGrad disables the unused recent-square decay control. |
| `learning-rate-schedules-cosine-warmup-onecyclelr` | `src/learn/components/lesson-labs/LearningRateScheduleLabs.jsx` | Plateau and clock settings returned to the initial state where their outcomes were invisible. Clearly labelled full previews now show the prepared-rate and parameter replays, using step curves to preserve the discrete event meaning. Settings retain the selected observation/microbatch; separate scrubbers and existing sequential controls remain available. A one-observation stream renders a point and disables its zero-width scrubber. These are replays on fixed supplied data, not retraining results or forecasts of unknown future losses. |
| `topology-topological-data-analysis-tda` | `src/learn/components/lesson-labs/TopologyTdaLabs.jsx` | A native range serialized the square's maximum `sqrt(8)` as `2.82842712474619`, below the actual computed `2.8284271247461903`. Keyboard End therefore left the loop alive: four edges and no faces. The slider now maps an integer control endpoint to the exact model maximum while its accessible value describes epsilon. End agrees with the existing exact-event button: six edges, four faces and beta1=0. No tolerance was inserted into the mathematics. |
| `numerical-pdes-grids-finite-elements-stability` | `src/learn/components/lesson-labs/NumericalPdeLabs.jsx` | Editing transport direction, method, profile or Courant number reset the selected step, hiding the comparison. Those edits retain the step. The lab explains that step 0 is the shared initial condition and advancing the existing slider reveals evolution. |

All six topic preflights were read. Schedule-clock and Numerical PDE destination notes are already resolved; these UI repairs preserve their scientific contracts. All 58 model files in this mathematics dependency scope retain the preceding production hashes. No learner-answer input, prediction gate, or optional guessing feature was added.

## Evidence and limits

The [baseline survey](evidence/math-control-affordances-survey.json) is tied to the previous production manifest. It loaded all 57 routes at desktop and 390px, exercised 436 visible enabled ranges by keyboard and 365 visible enabled selects with a different offered value, and inspected associated output changes, accessible labels, native gray controls, page overflow, empty diagrams and tiny KaTeX SVGs. It did not enumerate every combination, arbitrary text value, closed disclosure or button. Unchanged output is a triage signal, not by itself a defect.

The first selector harness tried restoring a disabled placeholder programmatically, causing an artificial undefined-preset error. That value is unavailable through normal interaction. The [complementary cases](evidence/math-control-null-cases.json) exercise every actual transition/arrival preset without error and verify the MCMC controls at states where their effects apply. Legitimate nulls remain: a common Bernoulli probability cannot change an equal-total likelihood ratio, mean and sum coincide for one selected observation, two scalar parameterizations can describe the same polynomial, and two persistence cost aggregations can agree for one nonzero match. The Monte Carlo sample count belongs to its explicitly run finite experiment; it should not change the separate analytic curve. Two small PDE KaTeX SVGs are intentional 0.016em horizontal pieces, not collapsed radicals.

The focused regression is `scripts/verify-math-control-affordances.cjs`; its [production receipt](evidence/math-control-affordances-browser.json) binds the seven changed runtime files and the verifier itself. It checks actual accessible label associations and keyboard edits, independently calculated momentum values, preview geometry changes, retained cursors, disabled-state reasons, the singleton stream, all twelve Rips sample/scale endpoint cases, changed transport cell values, and 320/390px layout. Representative desktop and phone captures are retained under `evidence/screenshots/math-control-*.png` and visually inspected.

Final production closure passed: five focused browser groups across the six changed topics, including the repaired 320px select and actual transport-step preservation. All four retained captures were visually inspected. The baseline survey is reused for unchanged topics; none of its source/interaction coverage is presented as a full rereview of all lesson prose, numerical models or hidden states. The parent integration owner records the final build and the six affected ledger revisions. Other existing checkpoints and user acceptance remain separate.

## Broad-survey candidate disposition

The later 135-route UI triage flagged small checkbox glyphs and three Rate–Distortion matrix cells at 320px. The [focused hit-target receipt](evidence/math-control-hit-targets.json) records 45 mathematics checkboxes across 14 lessons with their associated label geometry, actual text-label clicks and Space activation. Forty-four toggle directly from the initial fixture. The final selected gradient observation correctly refuses to leave an empty batch; after adding another observation, its label and Space toggle it normally. One L-BFGS label is 250.8125 × 23.046875px with 12.796875px separation from the next label; the raw 18px glyph does not represent the full target. There is no overlap or inaccessible action requiring a blanket glyph resize.

The three Rate–Distortion cells belong to the named, focusable local reconstruction-matrix scroller. Tab reaches each cell and Enter selects it. Focusing the region and scrolling with ArrowRight exposes the complete cells at scrollLeft=54, with their right edge at 277.390625px inside the region's 287.8125px boundary. This preserves an intentional readable matrix instead of shrinking it to the viewport. No runtime change was needed for these candidates. The receipt binds the actual production manifest and scoped source hashes; this follow-up does not expand the earlier interaction claims to every hidden or arbitrary state.
