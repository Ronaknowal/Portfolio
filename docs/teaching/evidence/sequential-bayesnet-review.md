# Sequential review — Bayesian Networks & Causal Graphical Models (28)

Reviewed 16 September 2026, limited to `bayesian-networks-causal-graphical-models`. Reviewer owns scoped verifier additions and this record; the integration owner owns production repairs, build/browser operation and final source binding.

## Coverage

The full prepared manuscript, design and visual specifications reconcile with ten reader sections, seven numbered figures, four investigations, ten changed-case practices, two displayed runnable programs and the complete offline experiment. The first route retains finite joint construction, normalization and parameter counting; exact posterior enumeration and elimination; path/collider/descendant reasoning, faithfulness and Markov blankets; fitted Wine NB/TAN inference; and observation versus intervention, adjustment and support. The deeper route retains frontdoor conditions and arithmetic, all three do-calculus rules including `Z(W)`, paired counterfactual outcomes, equivalence/structure search/EM, treewidth/junction trees, approximate inference, query-family distinctions and neighboring representations.

The previously unexecuted optional pgmpy packet example was executed during implementation and has source-bound native evidence. The measured experiment keeps its declared 106/36/36 split and selected NB refit on 142 development rows, distinct from the training TAN used in the exploratory lab. The existing documented adaptations—tables for inspectable arithmetic, routed edges, fixed intervention-panel positions and a two-step frontdoor formula—retain the prepared learning claims. All seven figure encodings/styles and all four control/commit/reset contracts received complementary source review. One substantive prepared-example omission was found: the reader showed the derived frontdoor averages but omitted the complete hidden-U model whose intervention supposedly verified them. Its nine declared probabilities are now visible. No remaining substantive prepared coverage omission was found.

## Findings and repairs

| ID | Concrete reproduction | Repair and bounded evidence |
| --- | --- | --- |
| R28-1 | Shared numeric allowance was disclosed only after grading and inclusive endpoints could fail from floating dust; equal response categories asserted numerical invariance. | State relative tolerance and its minimum allowance before entry; add four-ULP slack; describe category agreement honestly. Browser checks both endpoints around the `.001` prior, both just-outside values, and two different cultivar-2 increases. |
| R28-2 | Set burglary prior 0, earthquake prior `.9999`, observe `E=0,A=1,J=1,M=1`, and use `.001` for both alarm-on caller rows. Evidence has positive mass about `1e-13`, but the old `1e-12` support cutoff made the posterior undefined. | Probability normalization tests positive support against zero. Independent four-factor mass pin checks both enumeration and elimination; browser confirms a defined zero burglary posterior. Structural impossibility remains separate from numerical comparison tolerance. |
| R28-3 | An impossible-to-impossible comparison graded “unchanged,” while recovery from impossible evidence graded “no posterior”; I4 had the analogous empty-group problem. Numeric ties were labeled exact equality. | An absent new quantity is undefined; a newly available quantity has its own “defined” answer. State the `10^-12` comparison convention in I1/I3/I4. Model expectations now follow the conditioning semantics; browser covers both repeated absence and recovery in I1/I4. |
| R28-4 | I3 asked about the whole probability vector but graded only cultivar 2. Its changed feedback asserted a crossed median even for reveal/hide edits; its null feedback asserted unchanged model inputs without checking them. Purchase/edit results could also share history. | Name cultivar 2 explicitly, inspect actual visible binary evidence before explaining a null, acknowledge visibility/bin changes, and separate question families in history. Browser covers revealing two measurements, hiding all measurements and changing specimen while all measurements are hidden. |
| R28-5 | I2 Reset restored the graph but retained the pending arrow selectors. | Reset both selectors to `B → K`; browser checks the concrete prior `A → B` selection. |
| R28-6 | I4 hid draft risks in visible captions but printed them unconditionally in each mixture lane's accessible name. | Apply the same commitment gate to accessible risk descriptions; browser checks all four before commit and a revealed name afterward. |
| R28-7 | Assignment `[.001,.999]` prints a mixture weight inside a subpixel span with overflow hidden, making the actual weight unreadable. | Keep proportional widths and move low/high values into an external wrapping key. Browser checks `.1%/99.9%` style proportions, both printed weights and key overflow at 390px, with a targeted capture. |
| R28-8 | Prepared §7 declares the latent prior and all X/M/Y mechanism probabilities, but F5 showed only derived summaries. Its final caption also called the displayed `P(Y|X)` probabilities conditioning on the mediator. | Share declared parameters with the model and print all nine rows beside F5; correct the caption to conditioning on treatment. Browser compares the complete table with the prepared constants. |

## Evidence and reuse

Reviewer execution passes **524 grouped model checks across 64 groups**, including the existing 1,004 preset and 124,232 generated d-separation cases, 26 backdoor candidate sets, 864 measurement comparisons and 2,775 geometry checks. The added group contains two rare-evidence checks; existing absent-value expectations were corrected rather than dropped. Browser additions comprise seven bounded records and two targeted captures; the verifier was syntax-checked and frozen before the production run.

All eight finding groups are closed. The final production browser passes **25 cases with 39 captures**, and the final build passes. The integration owner inspected the 390px tiny-mixture legend and the complete frontdoor figure at desktop/mobile widths: the weights and nine-row model are readable and the layout flows correctly. The final accessible mixture descriptions include the visible weights while retaining the risk gate. No source or verifier edits followed the passing run.

Reuse retains the earlier independent 94 exact-rational checks and 357,634 separation comparisons across three algorithms; unchanged separation logic did not need another independent theorem campaign. Data evidence retains **1,270 checks**, including all **1,236 trust-root scalar leaves**, and native evidence retains **17 oracles** across three executed programs. Data/examples modules, packet/native sources and their verifier identities remain current as checked against the recorded evidence by the integration owner. The final browser evidence and central audit ledger bind current source/verifier/review hashes without duplicating a full table here. This is a code/teaching and bounded browser review, not a new beginner walkthrough or screen-reader study.
