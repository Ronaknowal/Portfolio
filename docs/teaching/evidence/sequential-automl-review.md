# Sequential review — AutoML & NAS (26)

Reviewed 16 September 2026, limited to `automl-neural-architecture-search-nas`. Reviewer owns the scoped verifier additions and this record; the integration owner owns production repairs, build and browser operation.

## Coverage

The complete prepared manuscript, visual specifications and design reconcile with ten reader sections, six investigations, five figures, three complete programs, ten practice tasks, checkpoint, readiness and sources. No substantive omission found. Coverage retains the evidence contract and CASH, conditional grammar and sampling measures, expected improvement, successive halving/Hyperband, a self-contained neural-network bridge, the identified 35-fit banknote study, ensembles/portfolios/transfer and hardware constraints, and the deeper mixture/discretization/bilevel/proxy/NASWOT branch. The distinct classical and neural search mechanisms remain explicit. All five figures received complementary source/geometry/style review.

The main study keeps its 919 development, 205 inspection and 248 unscored reserved rows with exact-feature grouping and fold-owned preprocessing. The optional FLAML and KerasTuner programs remain explicitly unexecuted; their absence from native execution is a recorded limitation, not fabricated output. Practice 1's three-family grammar is mathematically retained; the false claim that the fixed four-family lab could directly reproduce it is removed.

## Findings and repairs

| ID | Concrete reproduction | Repair and focused check |
| --- | --- | --- |
| R26-1 | Shared numerical acceptance omitted its tolerance before commitment and could reject an inclusive endpoint through floating dust. Commitment called differences within `1e-12` exactly equal. | Disclose tolerances, add four-ULP boundary slack, and name numerical ties. Browser checks both EI `±1e-5` endpoints, both just-outside answers and commitment wording. |
| R26-2 | Replay budget 4→1 lowers the best score from 1 to `.9946294942744771` and changes the candidate, but graded the best score as flat. | Compare absolute score changes independently of improvement. Model and browser reproduce the decreasing-budget case. |
| R26-3 | EI at `b=0, μ=.8, σ=.1` was negative through tail cancellation. | Stable normal-tail continued fraction avoids subtracting nearly equal terms. Independent 70-digit quadrature pins at standardized distances −2, −4, −8 and −12 pass relative error `<2e-13`; a separate 369-value sweep is nonnegative. |
| R26-4 | With `b=.4`, A `(.35,.001)`, B `(.4,1)`, C `(.5,0)`, the uniform drawing mesh missed A's peak and gave shaded integral `.11619…` instead of `.05`. The caption falsely implied one vertical normalization. | Add local sigma mesh and critical points, integrate nonuniform trapezoids, and disclose separate density/weighted-density normalizations. Model checks exact peak and area; browser checks the actual painted peak and refined vertices. |
| R26-5 | D first-rung `.13` ties C but was called worse. Setting A and D final losses to `.07` made another identifier at the same best loss falsely count as a missed better candidate. Reset retained revealed counterfactual traces. | Explain cutoff ties, compare final losses strictly and clear overlay visibility on Reset. Model and browser cover both ties and reset gating. |
| R26-6 | Logits `[8,-8,-8]`, input/target `.001` have nonzero gradients around `1.125e-13`, but were called exactly zero. A small output change was falsely attributed to a zero step and denied any weight change. At step 1, the computed next logit also crossed −8 and crashed the control-range guard. | Use exact structural gradient zeros, disclose the output tolerance and honest near-tie cause, and validate entered logits separately from finite computed softmax inputs. Independent derivative check and browser cover this fixture; next logits are neither rejected nor clamped. |
| R26-7 | Logits `[0,0,1]`, input 2, target 1 increase two logits, but the question asked which logit increases while grading only the greatest increase. | Ask for the greatest increase/most negative gradient. Model verifies two increases and the intended winner; browser checks the question and answer. |
| R26-8 | A small softmax weight near `1.125e-7` was drawn as a two-unit bar although width claimed proportional weight. F2's padded/capped layer boxes likewise claimed proportional height. Final screenshot inspection also found clipped scientific-notation probability labels at the right edge and the input label at the left edge of I4. | Remove the quantitative bar floor; describe architecture boxes as schematic with exact printed widths. Compact the diagram's probability labels and size its input label to fit, retaining detailed table values. Browser checks actual SVG bar width, the box disclosure and every text bounding box in this specific tiny-gradient diagram. Final rerun and screenshot inspection confirm the repaired labels fit. |
| R26-9 | Removing tree depth 5 and adding 8 preserves count 11 but feedback said nothing was added or removed. Practice 1 claimed its grammar was directly constructible despite the mandatory neighbour family. | State only count equality, explain replacement, and qualify the practice's different family set. Browser checks replacement feedback; manuscript/runtime delta inspected. |

## Evidence and reuse

Reviewer model execution passes **338 grouped checks across 71 groups**. Nine added grouped checks cover four high-precision tail pins, one 369-value nonnegativity sweep, one narrow-density check, one halving-tie group, one replay-decrease case and one small-gradient/control-bound group. These are bounded additions to the existing independent identities and recorded-outcome checks, not a repeated native proof campaign.

All nine finding groups are closed. The final production browser passes **22 cases with 37 captures** after the latest passing build. Browser additions form one grouped case with three targeted captures. The integration owner inspected the narrow-density capture, which shows the real peak with honestly disclosed common-scale compression, and the final tiny-gradient capture, whose probability and input labels now fit. The repaired diagram also passes its focused text-bounds regression.

The tiny-bar test separately checks its exact SVG attribute and actual browser length. Chromium parses the correct `6.527038664674155e-6` width to `6.499999926745659e-6` user units. The regression therefore requires attribute relative error below `1e-12`, a positive painted width and absolute paint error below `5e-8` user units. This measured subpixel allowance still rejects the original two-unit floor. The final browser run uses this corrected, frozen verifier.

Source-current native/data evidence is reused: 104 native oracles for the executed main program, 414 data checks, and the previous independent 35-fit reconstruction and numerical review. The data module, examples module, packet calculations, dataset bytes and corresponding native/data verifier identities were checked against recorded hashes. Optional programs retain their explicit execution limitation. The final browser evidence and central audit ledger bind exact source/verifier/review hashes; this record does not duplicate their hash tables.
