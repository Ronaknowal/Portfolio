# Sequential review — Hidden Markov Models (27)

Reviewed 16 September 2026, limited to `hidden-markov-models-hmm`. Reviewer owns scoped verifier additions and this record; the integration owner owns production repairs, build/browser operation and source binding.

## Coverage

The prepared manuscript, complete visual specifications and design reconcile with thirteen reader sections, nine numbered figures, six investigations, two complete downloadable programs with visible excerpts/native output, ten practice tasks, checkpoint, readiness and sources. Core coverage retains the generative factorization; filtering, smoothing, forecasting and Viterbi as distinct queries; exact path versus pointwise decisions; backward/pair posteriors; independent-recording expected counts and EM; scaling versus underflow versus genuine impossibility; supervised EWT tagging and its exact-tie band; duration, Gaussian emissions, state count, structured extensions and computational costs.

The optional native program was executed during implementation, resolving the packet's earlier explicit deferral. The real study preserves 120/40/40 sentences and 1,188/341/370 tokens; the reserved forty sentences remain unscored. The independent exact-rational tie audit is retained rather than rerun. All nine figures received complementary source/encoding/style review; existing painted-width and layout evidence remains applicable except for affected current states. Final coverage reconciliation identified and repaired one presentation gap: the prepared complete one-step update gives updated emission rows, while the reader/lab initially printed only updated initial and transition rows. No prepared substantive coverage remains omitted.

## Findings and repairs

| ID | Concrete reproduction | Repair and bounded evidence |
| --- | --- | --- |
| R27-1 | Shared numeric acceptance omitted tolerance before input and rejected inclusive boundaries through floating dust. | Disclose tolerance, retain exact checks when tolerance is zero and allow four-ULP slack otherwise. Browser checks both `±1e-9` path-probability endpoints and both just-outside answers. |
| R27-2 | Applying a probability row containing thirds redisplayed rounded entries summing to `.999999`; tiny entries were rendered with a nonparseable multiplication symbol. The accepted row immediately appeared invalid. | Use round-trippable numeric text in the editor. Browser reapplies thirds and `[1e-7,.5,.4999999]`, checking exact parsed values and absence of phantom errors. |
| R27-3 | At self-transition `a=0`, the lab reported a next-step conditional exit probability after ten survived steps, although that conditioning event has probability zero. | Return undefined for unsupported elapsed histories; compare entry at elapsed zero with ten steps and provide an undefined answer. The existing 1,515-setting hazard sweep now explicitly distinguishes supported and impossible histories. Browser accepts the undefined conditional answer while correctly grading mean duration 1. |
| R27-4 | Load the one-report preset in the evidence lab and submit the fixed time-1 construction: indexing `smoothed[1][0]` crashed. | Query bounds produce null beliefs and a failed construction instead. Model/browser verify the legal one-report case. |
| R27-5 | An eight- or nine-step single recording has seven/eight transitions, but the grading key returned `7`/`8` while the offered answers stopped at 6 plus “more than 6.” | Each event group uses its own option threshold. Browser creates both lengths and correctly commits all three event counts. |
| R27-6 | Editing a successful repair/break identification retained its solved feedback and unlocked aggregate. Reset left the successful guesses prefilled. | Guess edits retire identification; Reset clears all four fields and aggregate access. Browser checks both transitions. |
| R27-7 | With unchanged spelling, sentence 2 at either smoothing and sentence 39 at smoothing 1.0 falsely said “path moved”: fresh inference selected a different exactly tied path than the saved decoder. Changing to a shorter sentence also retained an out-of-range spelling position. | Compare original and edited symbols through the same fresh inference convention, separately from recorded results; reset spelling context on sentence change. Browser covers all three differing-tie cases, two unknown-spelling nulls per case and shorter-sentence reset. |
| R27-8 | Identical response categories in consecutive attempts asserted “Nothing moved” even when the underlying inputs/probabilities differed. | Describe category agreement without asserting numerical invariance. Browser uses two different lower-probability paths with the same categorical answer. |
| R27-9 | Practice 1 pointed to a nonexistent forecast investigation. Free-emission feedback called the whole Rainy row unused although the winning path uses its unchanged Clean entry. Count feedback denied any whole-valued individual count, including zeros. | Point to actual forecast teaching; distinguish unchanged path entries from an unused row and avoid an unconditional posterior-change claim; say counts may be fractional. Source-reviewed copy also names all three count predictions and accurately says duration bars are already visible. |
| R27-10 | Prepared §6 gives both updated emission rows, but the table called the “whole M-step” displayed only updated initial/transition rows. | Add the complete updated emission table with a zero-occupancy retention note. Browser compares all six displayed probabilities with the independently recorded split-update values; the existing split screenshot includes the added table. |

## Evidence and reuse

All ten finding groups are closed. Reviewer model execution passes **18,304 executed assertions across 69 recorded groups**, including the existing 1,024 belief-move verdicts, 231 priors, 486 recording compositions and 1,515 distinct hazard settings. The zero-survival expectations were corrected, not discarded; the assertion-count change reflects the verifier's different helper paths, not fewer tested hazard settings. One new grouped check covers the short construction.

The final production browser passes **22 cases with 58 captures** after the latest passing build. Browser additions are one bounded group plus checks in the existing tagging/split-update cases, with two targeted captures. The integration owner inspected the complete split-update table and zero-survival feedback; both are clear. A separate narrow diagnostic confirmed all six unknown-spelling nulls across the three tie fixtures. Numeric position edits explicitly blur before programmatic selection, matching the real user's focus transition and avoiding a retained local input draft in the test.

Native evidence retains **66 oracles**, and data evidence retains **2,668 checks across 481 groups**. Data/examples modules, packet calculations and native/data verifier hashes were independently compared with those evidence records and remain current. The integration owner also checked the unchanged public programs/extract. Reuse includes the prior exact-rational inference/path enumeration and complete EWT tie analysis, high-precision EM histories and native program execution. No repeated theorem or native-fitting campaign was needed. The final browser evidence and central audit ledger bind source/verifier/review hashes without a duplicate table here.
