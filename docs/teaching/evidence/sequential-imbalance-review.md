# Sequential review — Imbalanced Learning (25)

Reviewed 16 September 2026, limited to `imbalanced-learning-smote-cost-sensitive-learning`. Reviewer owns the bounded verification additions and this record; the integration owner owns production repairs, build and browser operation.

## Coverage

The prepared manuscript, visual specifications and design reconcile with ten reader sections, six figures, five investigations, three complete programs, ten practice tasks, the checkpoint, readiness and sources. No substantive omission found. Core coverage retains confusion counts and undefined metrics; grouped AP and prevalence; posterior action costs versus capacity; normalized weighted gradients and population score inversion; SMOTE geometry, categories and whole multilabel rows; training-only sampler ownership; the identified Yeast study and its four roles/five procedures; and deeper samplers, cleaning, ensembles, focal loss, prior odds, uncertainty and operation costs.

The implementation's separate loss scales with printed values, zoomed ROC inset, sampled candidate table with full underlying sweep, and executed optional pipeline are justified adaptations documented in the design. The 462 reserved proteins remain unscored. Existing historical review findings were reconciled against current source rather than repeated as new findings. The six figures received a complementary geometry/style review; no new figure-specific paint defect was found.

## Findings and repairs

| ID | Concrete reproduction | Repair and bounded regression |
| --- | --- | --- |
| R25-1 | I2 asks for selecting risk but grades selecting minus skipping: default correct `.9` was compared with `-.3`. Shared tolerance was unstated before commitment and lacked rounding slack. | Grade selecting risk; disclose tolerance and add four-ULP slack. Browser checks `.9`, both inclusive `±1e-6` endpoints, both `±1.01e-6` misses and the formerly accepted `-.3`. |
| R25-2 | I3 legal `p=.01,w+=.1,w-=20` gives `q*=.0000505025…`, but the round trip clamped it to `.01` and returned the wrong probability. The plotted domain also excluded the exact extreme optimum. The owner's subsequent screenshot inspection caught the uniform curve stepping over that minimum even after the marker moved inside the axes. | Mathematical forward/inverse guards use open `(0,1)` independently of slider ranges; no inverse clamp; full score axis includes the exact marker. The curve includes the optimum and nearby samples; text distinguishes the uniform diagnostic grid from the augmented drawing. Two extreme round trips and one inverse-derived probability below the slider minimum are model regressions; browser checks the returned `.01`, marker geometry and its presence as an actual loss-polyline vertex. |
| R25-3 | I3 inverse `q=.2,w+=9,w-=1` recovered p correctly but graphed the stale forward `p=.1` and minimum `.5`. History compared forward q with inverse p as one question. | Current/previous curves use each committed mode's population probability; history separates modes. Browser checks the marker and accessible minimum at `.2`, plus different-question history. Endpoint clipping description now matches actual plotting. |
| R25-4 | I1 all labels zero correctly reports undefined recall in the table but coerced null to zero in its recall line and markers. | Omit undefined geometry and explain why. Browser verifies no recall line/markers and captures this state. |
| R25-5 | I4 `u=0` coincides with minority A, yet the no-majority answer said the generated point sat on its own and feedback denied any observed collision. | Answer names majority collision; feedback identifies coincident minority IDs. Browser checks the endpoint case. |
| R25-6 | I4 anchor A `(.1,0)`, B `(-.1,0)`, C `(.3,0)`, `k=1` sorts C before B due to floating dust despite disclosing the distances as tied and promising identifier order. | Sorting and tie disclosure use the same tolerance. Independent expected B/midpoint `(0,0)` model check and browser distance-row check cover the decimal tie. |
| R25-7 | I4 majority-move prompt assumes it is the only edit although other controls remain editable; chosen rank can be 2 while the question calls it nearest. | Source-reviewed prompts explicitly encompass all draft edits; options/selector name the chosen neighbour. Existing null/contrast browser cases retain their numerical checks and use the revised answer label. |
| R25-8 | I5 reveal labels → Reset → recommit retained visible truth. Its table caption claimed every candidate while the rows are sampled. | Reset clears truth visibility; caption identifies near-optimal plus sampled rows. Browser checks both while the numerical sweep still uses every candidate. |

## Evidence and reuse

All eight finding groups are closed. Reviewer model execution passes **390 grouped checks across 61 groups**, including the unchanged 1,000 swept candidates and 168 weighted-optimum grid points. The four added grouped checks are exactly two extreme round trips, one inverse-context case and one decimal-distance tie; they are not a new exhaustive proof campaign. Browser additions constitute one bounded case group and four captures, preserving existing checks. The integration owner's final production build and browser pass **25 cases and 51 captures**, including the exact-minimum-vertex assertion. The owner inspected the final extreme-weight image and the inverse-context/no-positive images and found the repaired states clear and correct. This reviewer checked source and regressions but did not independently operate or view that final browser session.

Native evidence for three programs and **58 oracles** is reused: current examples hash `59345b538c693efed7b5950cbfadd2a15e38884a91e76f5a18430f1ad331813d` matches `imbalance-native.json`. Current data hash `02c9bb5eeee3e0266e3a9ec741155c646ee88bdd3686142ab81739ae5ac498c6` matches the later `imbalance-data.json` (10,514 checks, including 28 trust-root checks); served dataset hash `7cf61776fc04f527f93bf57a327b863893a1225d82df02d457e8950173218258` also matches. That later data evidence is used rather than pretending the original independent review's earlier module hash is current. Its independent refit/data derivations and the original review's unchanged constructed derivations are reused; this pass tests the concrete control/display/tie deltas above. Final runtime/evidence identities belong in the audit ledger and browser evidence, without a duplicate hash table here. No later topic or external-reference re-research was undertaken.
