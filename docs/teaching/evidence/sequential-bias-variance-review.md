# Sequential review — Bias–Variance Tradeoff & Learning Curves (24)

Reviewed 16 September 2026, limited to `bias-variance-tradeoff-learning-curves`. Reviewer edits are confined to the browser verifier and this record; the integration owner owns production repairs and final browser operation.

## Coverage

The complete prepared manuscript/specification/design reconcile with the reader's eleven sections, two investigations, seven figures, three complete programs, eight practices and readiness/references. No substantive omission found. The extra seventh figure makes the manuscript's classification/averaging branch visible. Core coverage preserves the two sources of spread, conditional squared-loss identity, exact eight/thirty-two-world fitting, bootstrap distinction, three curve axes, actual Airfoil crossover/restriction/trajectory and falsifiable next experiments. Deeper coverage preserves same-X versus new-location optimism, smoother traces and adaptive-selection limits, Brier versus zero-one loss, correlated averaging, and explicitly asymptotic sample-wise double descent. The 303-row reserve remains unused. Stacked mobile panels retain both models and their comparison table; they are a reasonable implementation adaptation of the specified single-panel switch.

## Findings and repairs

| ID | Concrete issue | Repair and regression |
| --- | --- | --- |
| R24-1 | Shared optional numeric grading omitted rounding slack and disclosed its tolerance only after commitment. | Owner added four-ULP slack and the advance tolerance hint. Browser accepts I1 total `2±.0001` and rejects `2±.000101`. |
| R24-2 | I1 translation `[8,10,12],f=10` to `[10,12,14],f=12` left error terms unchanged but claimed the average prediction stayed fixed. Changing spread and noise together (or bias and noise) attributed the whole change to only one term. | Feedback now names unchanged error terms and conditions single-cause explanations on both other terms remaining unchanged. Browser covers translation and both simultaneous-noise cases; the latter has a dedicated capture. |
| R24-3 | Both labs called tolerance-based equality exact; I2 stated an absolute tolerance although grading scales it by the reference error. Its world-prediction equality test also used `1e-12` while calling the null exact. | Prompts/options/readouts state the numerical comparison and its scaled threshold, including the separate world-prediction tolerance. Existing null tests are reconciled with this truthful contract. A bounded analytic check of all three-input, two-decimal c/σ/x combinations found no genuine nonzero risk difference swallowed by the existing comparator; no numerical model change was needed. |
| R24-4 | Combined F3 curves requested width2.2 but `.bv-curve` painted1.8; F7's intended green arrow fill lost to `.bv-flow { fill:none }`, and its accessible description said two arrows although only one exists. | Intended width/fill are inline and the description names one arrow. Browser checks computed width of all four curves and computed fill/count of the arrow. |
| R24-5 | F3 fold dots used a fixed0–50 scale even though the observed maximum is51.363045026, placing dots above the plotted axis. | Shared extent includes all training/validation fold values with padding (current0–60). Browser checks all200 dots and shared ticks; original mean-coordinate checks are retained on the corrected scale. |
| R24-6 | F2 says subset bar lengths are proportional, but its minimum-width floor turned the60-row width9 into10. | Removed the floor. Browser checks the five actual widths9/18/36/72/135. |
| R24-7 | F5 said average leverage is1.333333, confusing it with prediction variance; actual average leverage is p/n=1/3 and σ² times it is4/3. F1 said each off-diagonal cell appears twice, rather than each distinct mixed product occupying two cells. | Corrected both quantity descriptions; browser asserts the repaired prose. These are explanatory corrections, with the already-verified formulas/numbers unchanged. |

## Evidence and reuse

All seven finding groups are closed. The integration owner's model run passes **179 grouped checks and 19,764 grid values**; the production build and final browser pass **16 cases and 31 captures**. The reviewer added two bounded browser groups, preserving existing cases and adding one capture. The owner inspected the repaired full-fold-scale figure and multiple-cause feedback captures and found both clear and correct. This reviewer checked the source changes and regressions but did not independently operate or view that final browser session.

Unchanged native evidence covers three programs and **55 oracles**. Independently checked examples SHA256 `fb2acb3099d5898f07939043b7dcccaa51146aa9c310011a579708436ff815a7` and native verifier `11d9f3b6c1a0734a43d710fd14b87d26255ee5b822cceccec9b882460e86794d` match the native record. Data module `f6e4a912ab961c03c633aaef9a5ba645774c8cabf5fe22169e5be109ef9a3a82`, calculated-input trust root `eac92529128d58691b4c771d47fdb8ac0be2097ca817359092f23e5718f3888e`, and served dataset `74c75fd71783f1e6b71f8a622b993dc592897a97cd689c5090a07147a1b097b3` match the earlier independent/data records. Their exact-rational trust-root and independent Airfoil refits are reused, not rerun. No external reference re-research or later-topic review was performed. Final runtime/evidence identities are bound by the audit ledger and browser evidence rather than duplicated here.
