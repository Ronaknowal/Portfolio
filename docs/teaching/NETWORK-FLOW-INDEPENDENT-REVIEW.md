# Network Flow — independent source review

10 September 2026. Root read the final lesson's conservation/residual/cut proofs, integer solver contracts and costs, matching/cover/Hall construction, constraint transformations, binary-label reduction, optional global contraction branch and nine independent practice answers. The [author verification](NETWORK-FLOW-VERIFICATION.md) owns the extensive native/finite-oracle and actual browser/visual evidence; this is a complementary bounded proof/scope review, not another executed test count.

Original-edge identity remains distinct from residual direction, including real opposite and parallel edges. The default second path genuinely cancels an earlier assignment. Capacity and internal incidence equations are checked before source delivery is treated as a lower certificate. The directed-cut proof subtracts incoming flow and counts only outgoing capacity. Residual reachability supplies both saturation and zero incoming-flow conditions needed for equality.

Edmonds–Karp's progress argument charges saturated residual arcs and requires reverse reuse before another charge; dictionary-based reached-state initialization supports the stated graph-operation bound without silently charging every isolated vertex per BFS. Integer bit cost, trace storage, recursive Dinic depth and exact-arithmetic boundaries remain explicit. Integral optimum is not confused with all feasible flows being integral.

The bipartite cover is unreached left plus reached right under alternating reachability from unmatched left starts. The Hall witness uses the complete reached neighbor set and counts unmatched starts; left-saturating and perfect matchings are distinguished. The triangle prevents transferring the cover equality to general graphs. The lower-bound gadget derives its signs from out_x−in_x=in_lower−out_lower, while supply ceilings and mandatory demands remain different contracts.

The binary-label partition accounts for every unary and pairwise cost for every candidate: opposite original neighbor arcs charge one directed crossing. Negative penalties are not implemented as invalid capacities. Global cut and terminal cut have different feasible partitions; contraction preserves edge multiplicity and uses conditional step survival, then independent whole-trial amplification. A replayed seed is not an independent trial or an optimality certificate.

Changed local tasks correctly diagnose an infeasible endpoint total, distinguish real reverse flow from cancellation, return matching/cover/shortage witnesses, repair mandatory circulation signs, enumerate label tradeoffs and expose cardinality versus cost. Practice links keep bipartiteness as a prerequisite check and the exam-seating reduction as matching transfer; the Premium-only gated statement is not counted as verified public practice. More links are not substituted for the missing objective theory.

No additional actionable mathematical issue was identified and no frozen production source changed. All seven author hashes must still match at integration. User acceptance, a beginner study and universal interview readiness are not claimed by these records.
