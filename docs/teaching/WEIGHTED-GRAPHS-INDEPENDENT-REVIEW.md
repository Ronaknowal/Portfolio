# Weighted Graphs — independent source and proof review

10 September 2026. Finite second-author review requested by root after the lesson author froze the source. **No actionable correctness defect found in the reviewed explanations, complete native programs, proof contracts or independent exercises. No lesson, example, model, lab or shared file was edited.** This is a separate source review; it does not claim repetition of the author's extensive native or browser suites.

Read the complete `shortest-paths-spanning-trees-topological-ordering.jsx` body, all twelve exported complete programs and shared helper definitions in `weighted-graph-examples.js`, the individual design and final verification record. Reviewed the model's published interfaces/classification locations to understand the visual contracts, without claiming another full model audit. The author's native/model oracle construction and declared representation limits supplied context, not substitute proof.

Specific checks:

- **Dijkstra:** finite labels are costs of discovered walks and therefore upper bounds; current queued candidates and stale physical entries differ. The first-unsettled-vertex proof uses nonnegative suffixes at exactly the required step. Strict improvements avoid equal-cost/zero-cycle reinsertion. Ticket ties, original edge identity, finalization-only early exit and the incomplete remainder of a single-target result are distinguished. Lazy storage and operation bounds account for physical duplicate entries instead of borrowing indexed decrease-key bounds.
- **Bellman–Ford:** old/new arrays encode at-most-edge budgets. Additional strict improvements seed reachable negative-cycle influence; affected descendants are not described as actual cycle vertices. Finite costs use tight-edge BFS witnesses, avoiding overwritten generation-inconsistent predecessors. If a source is itself unbounded, every vertex reachable from it is likewise downstream, so skipping finite-witness BFS from that source is sound. The explicit route helper is only to be called for finite destinations. The row-copy cost includes isolated vertices.
- **0/1, Floyd and Johnson:** strict zero/one deque insertion preserves the two-level extraction order. Floyd's pivot-row/column justification is confined to absence of negative cycles, with pair classification afterward on a separate result matrix. Johnson's supersource exposes cycles in any component; its potential cancellation is endpoint-specific and independent of hop count. Hand substitution gives potentials [0,−4,0,−2], adjusted edges [6,5,0,0] and converted A→C→B cost1. Global Johnson rejection and target-specific Bellman/Floyd classification remain distinct.
- **Spanning forest:** accepted-edge connectivity preserves Kruskal's light-crossing-edge argument. The exchange cut excludes accepted edges; therefore removing the competing crossing edge preserves the existing accepted forest. Ties guarantee some compatible optimum, not a mandatory edge in every optimum. Prim keys are individual installation costs and its obsolete edges are internal to the visited set. Negative loops cannot enter a tree, while negative non-loop weights are valid. The bottleneck proof compares the maximum edge on an MST path with the cut created by removing it; it does not claim additive shortest paths. Empty and disconnected inputs are preserved.
- **Dependency order and schedules:** all vertices and repeated edge occurrences are included consistently. DFS active-stack positions produce an actual directed cycle; finished vertices do not. Kahn's blocked remainder can include descendants. Multiple ready choices characterize nonunique orders under the stated DAG condition. A computed earliest schedule attains the longest-chain lower bound only under the stated unlimited-capacity assumptions. Durations [3,2,4,6,2,1] give the verified chain B,D,E,F and makespan11; zero-duration and empty cases do not invalidate its cost certificate.
- **Changed-contract practice:** independently hand-checked A→C→B→D costs2+1+2=5, the negative-cycle downstream classifications before/after D→A, constant-edge-shift hop bias versus fixed forest cardinality, strict-versus-tied cycle-edge exclusion, and the P/Q/R makespans9 versus11 with unlimited versus one-worker capacity. Acceptance requirements correctly ask for real witnesses and the assumptions behind a certificate.

No primary-source uncertainty required additional browsing for this finite review; the reviewed derivations are self-contained, and the author separately records actual primary-source inspection. I did not rerun native/browser suites, execute submissions, watch the linked lecture, benchmark distributed routing or inspect unrelated lessons. The [author verification](WEIGHTED-GRAPHS-VERIFICATION.md) owns those actual local tests and limits; source fingerprints are in `scratch/weighted-graph-lesson-review/final-source-hashes.json`. Root retains responsibility for integrated build/loading review and the full rollout ledger.
