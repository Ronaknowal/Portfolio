# Naive Bayes & Probabilistic Classifiers — author verification

11 September 2026. Scoped author review of Classical ML topic 6. The exact freeze time and six production fingerprints are in [the author packet](evidence/naive-bayes-author-review.json). This is not independent approval, production integration or a beginner usability study.

## Preservation and teaching assessment

The entire 899-line original body, its six programs, diagrams, exercises and references were read before design. [Original source/program evidence](evidence/naive-bayes-original-review.json) preserves the exact 59,687-byte body, all six actual rendered code strings and their fresh standalone outputs/errors. Its source SHA256 is 79e60503d2ecbdf3fc2faeb58274f5a21cf6ce09ba3ccce9d9d3b840bba1d9b9. Two original programs execute; two have cooked-newline syntax errors; two depend on undefined variables. That original archive is not overwritten.

The [approved scoped design](NAIVE-BAYES-LESSON-DESIGN.md) maps every useful original teaching family to its retained, corrected or deeper replacement. The three-message vocabulary, eight-row word corpus, twenty-message corpus and original Gaussian draws/fitted comparison remain useful interpreted fixtures. The source rewrite repairs the actual workflows rather than claiming byte-identical revised programs. Original broken code remains available in the archive.

The complete body teaches the observation unit, prior baseline and split ownership before combining evidence. Token/count events, smoothing, Bernoulli absence, Categorical support, continuous density and variance fitting, actual Gaussian geometry, dependence/XOR, Complement weights, sparse pipelines/streaming, probability calibration/costs and integrated Dirichlet prediction are all explained locally. Twelve changed tasks have separate hints and explained solutions, including an executed changed capstone and explicit rare-class diagnosis. Two early checkpoints expose nonempty questions and answers. The title, stable ID and next Ensemble Methods route are unchanged.

The six investigations use different mechanisms: token log-odds ledger, present/absent slots, density plus interval probability, computed Gaussian score regions, copied-observation graph/risk, and finite reliability groups. Inline corpus, assumption fork, complement pooling and fitting-ownership diagrams connect the explanations. Computed geometry is distinguished from a fitted sample and a finite dataset from a population guarantee. All explanatory prose, proofs, programs, practice and source annotations were reread during author review.

## Independent numerical and actual-program checks

Run from the application repository:

- node scripts/verify-naive-bayes-examples.mjs
- scratch/lesson-tools/Scripts/python.exe scripts/verify-naive-bayes-practice.py

The final main run completed at **2026-09-11T11:50:35.479524Z**; [raw results](../../scratch/naive-bayes-verification/results.json) and a full payload are retained in the author packet.

All **11 complete displayed Python programs** execute independently, match their actual stdout and raise no captured warnings. Black formatting preserves their normalized Python ASTs. Actual tested versions are Python 3.12.14, NumPy 2.3.5, SciPy 1.18.1 and scikit-learn 1.9.1. No external data download or hidden notebook state is needed.

Complementary checks use independently formulated oracles rather than self-comparison:

- 3,124 token-sequence states, including every prefix, use exact Fraction class weights across lengths 0–4 and alpha 0, .25, 1 and 10.
- 243 changed count/presence vectors use exact independent Bernoulli and multinomial laws.
- 153 continuous-observation states use SciPy density and adaptive integration, including all plotted curve points. The largest 64-piece Simpson interval absolute error is **3.2064251254e−10**, below the declared 1e−9 test threshold; the lesson labels the numerical integration.
- 14 Gaussian geometry fixtures check independent multivariate-normal scores, every analytic circle/line point and every region-cell label. A discovered floating-point ordering error on exact equal-variance ties was repaired by summing squared distances before the common normalizer. Equal x/y plot scales are preserved.
- 60 copied-alarm states and ten reliability configurations use exact population/group calculations; ties, bin membership and proper scores are checked.
- 2,780 actual CountNaiveBayes changed training/query cases use exact count-law oracles, including zero support and empty rows.
- 180 actual Gaussian changed queries use exact class means/variances and independent multivariate normal densities.
- 900 actual integrated-count cases use exact normalization plus SciPy Dirichlet-multinomial probabilities.
- Fifteen explicit JS validation cases reject sparse arrays, NaN/infinities, unsupported alpha, overflowed score differences, excessive inputs and invalid selectors. Unknown words and all-zero likelihoods have distinct verified outcomes.

Changed-practice checks additionally verify the new prior/opposing-token fraction, missing-versus-absent evidence, both Gaussian crossings, copied-alarm risk, complement query, rare-class prior/cost result and integrated two-token law. The separate capstone actually changes seed to 2027, reading shift to 1 and calibration size to 400. Its output and complete changed program are saved. Reported raw/calibrated log losses are .552812/.510724, changed decisions 134; no probabilities exceed the .8 action threshold. The accepted solution explicitly reports that limitation.

The actual installed calibration source was read: CalibratedClassifierCV.fit, _fit_calibrator, response selection and _CalibratedClassifier.predict_proba. Its sigmoid receives GaussianNB probability outputs; the temperature branch's distinct logit conversion is not silently attributed to sigmoid. [Practice/API evidence](../../scratch/naive-bayes-verification/practice-api-results.json) records source hashes, inspected branches and exact changed solutions.

## Actual browser, keyboard and visual reading

Run:

- node scripts/review-naive-bayes-lesson.cjs
- node scripts/review-naive-bayes-reading.cjs

Edge used the actual route on port 5173 at **1440, 390 and 320 pixels**. The full final behavior run completed **2026-09-11T11:57:28.589Z** with **87 operated states per width**, all eleven actual code/question/output blocks, twelve tasks, two checkpoints, anchors and seventeen KaTeX formulas. There were no lesson console/page errors or document overflow. Textbox errors preserve the active state; empty/OOV/zero-support input, stepping/back/reset, presence saturation, Gaussian changes, exact ties, copied decisions and reliability grouping were operated. Keyboard sliders, submit/reset and local table scrolling were checked. The main and focused payloads record their exact scope.

The focused final reading run additionally measures actual KaTeX descendant bounds, rather than only trusting container scroll widths, and confirms loaded Space Grotesk and JetBrains Mono FontFace records. It rereads the introduction, Gaussian proof, longer equations, rare-class answer and capstone consequence. Its last narrow visual amendment widens the copied-alarm source node to contain its actual phone-font label; all three widths check the text against its box. That display-only amendment follows the full behavior run and has its own final timestamp/captures in the packet. No numerical or interaction algorithm changed.

Actual images were opened for all eleven ordinary section starts at 320px, the intro, corpus/assumption/complement diagrams, token error/evidence, repeated/absent features, Gaussian density and decision geometry, copied evidence/risk, reliability, Gaussian proof, deeper integration formula, changed practice/report and sources. The packet lists the actual opened paths and hashes, including final corrected contexts. This is an author heuristic inspection; it does not claim a learner study, exhaustive assistive-technology certification or watching an entire linked video.

The first browser mount caught two accidental bare JSX brace expressions; both were escaped/reworded. Phone reading exposed wide equations and a reliability count label near the axis title; equations were decomposed with their meanings preserved, and the label was repositioned. A compact four-column alarm table now fits the narrow view. These findings were repaired before the final evidence; no earlier failed run is represented as a pass.

## Research, handoff and limits

The design's dated claim/resource ledger records the inspected primary Stanford IR/CS229 material, Rennie complement paper, Sahami historical cost account and current sklearn guide/API/calibration contracts. The linked Stanford Online lecture is a direct verified official recording whose corresponding substantive notes were read. No full-video viewing or invented timestamp is claimed. The current SLP chapter URL did not match the intended NB content and is not presented as a reviewed NB source.

The canonical [calibration note](topic-notes/calibration-conformal-prediction.md) carries the score-interface, fitted-data and action-resolution discovery; its future destination remains open. The [Ensemble note](topic-notes/ensemble-methods-stacking.md) carries combination semantics and out-of-fold ownership into the next assigned rewrite. No unrelated production topic or shared registry was changed by this author.

Known bounds are explicit: these are teaching-sized synthetic models and declared numerical domains; they do not establish deployment performance, calibration or resilience to arbitrary floating-point magnitudes. Proper-score improvement in the executed capstone is a measured outcome, not a guarantee. Independent review and the parent's production build/integration remain separate from this author freeze.
