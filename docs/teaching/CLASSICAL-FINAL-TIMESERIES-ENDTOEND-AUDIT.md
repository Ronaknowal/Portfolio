# Final Classical ML audit: Time-Series and End-to-End

21 September 2026. Independent follow-up audit of positions 38 and 39, in order. This record supplements the existing implementation reviews. The integration owner updates the central ledger and performs the final production build. Prepared content, data, native outputs and earlier evidence remain preserved.

## 38 — Time-Series Validation & Forecasting Baselines: complete

Original complete [prepared lesson](drafts/time-series-validation-forecasting-baselines/lesson.md), [visual contracts](drafts/time-series-validation-forecasting-baselines/visual-specifications.md), and [design/research/implementation record](drafts/time-series-validation-forecasting-baselines/design.md). These historical sources and the content checkpoint are unchanged.

Read the complete prepared manuscript and specifications, design/research/provenance and author experiment; compared the live lesson, five figures, three investigations, model/data/example modules and the prior independent review. `--topic time-series-validation-forecasting-baselines --work finish` confirmed published content and implementation both complete. The complete prepared coverage is retained: three clocks, all four baselines and donor wrapping, delayed feature and label availability, splitter indexing, recursive versus updated forecasts, rolling refits, horizon metrics, the real 616-fit bicycle comparison, all eight changed practices and deeper uncertainty/scaling connections. The current title remains accurate and no new topic is authorized.

The primary new finding is **outcome leakage through a form value**. The original Investigation 3 initialized its future replacement with `SOURCE_COUNTS[365]`, and horizon/origin selectors copied each corresponding future source count into that input. Thus its hidden outcome table did not protect the actual outcome: selecting the seven horizons exposed 2294, 1951, 2236, 2368, 3272, 4098 and 4521 before issuing. The earlier browser checks inspected rendered text, which does not include input values. The repaired editor starts with an explicitly labeled independent counterfactual zero and never copies a concealed source outcome into the control. The corresponding preset sets the first outcome to 3294 directly rather than revealing its difference from the hidden source. The calculation and original measured data are unchanged.

Three smaller interaction/teaching corrections are complete:

- “Add 5 to every value” silently saturated values at100. The preset now adds the same5 everywhere and is unavailable with an explanation when any starting value exceeds95. This preserves both the manual bounds and the uniform-shift investigation.
- Reset now closes the optional saved-ridge panel as well as clearing the forecast/reveal gates.
- Figure3 calls its first vertically stacked chain “top” rather than “left”. The FPP3 alternative no longer calls all its examples one-step: the [primary section](https://otexts.com/fpp3/tscv.html), re-read on21 September, explicitly includes four-step diagrams and one-to-eight-step executable evaluation. The live annotation now describes both one-step and multi-step examples. These correct the implementation; the historical prepared packet is unchanged.

`scripts/verify-timeseries-final-audit.cjs` checks the actual running reader without building or changing sources. Its independent cases cover28 origin/horizon selections (including both development endpoints), input values before reveal, issue/reveal sequencing, horizon3 forecast1162, source MAE7298/7, a future replacement changing MAE by1000/7 while preserving the forecast, reset of saved-ridge state, shift boundaries95/98, eight-horizon repetition with only four scored outcomes, the empty eligible set, and keyboard reset. All passed. Existing source hygiene (113 checks,12 parsed files) and render (35 checks) passed with `--no-evidence`; no earlier report was replaced. Unchanged native/data/model evidence is reused by source hash rather than repeating the original ridge campaign.

Actual images were inspected for all six inline diagram panels at 1366, 390 and 320px, the six-candidate development chart, the sliding-window/horizon7/third-origin state, the issued forecast, and the committed eight-horizon donor and counterfactual request at 320px. Hatching is visible, arrows distinguish the known initial observation from subsequent inputs, cutoff/arrival boundaries stay separate, the two staircase scales preserve their meaning, and the final curves visibly retain horizon4/5 reversals and the horizon7 shared point. The changed controls, feedback and phone table cards reflow. No material topic geometry finding remains. Eight tall captures were subsequently re-saved with only the fixed reader navigation hidden by the screenshot tool; numerical checks were not repeated for that capture cleanup. The source-dependent screenshot hashes are in `evidence/classical-final-timeseries-browser.json`.

Learning-experience review is a reviewer heuristic, not an observed novice study. The first-pass route reaches the whole weekly forecasting contract without the deeper branches; the natural history-to-donor, row-to-arrival and origin-to-fit representations serve different hurdles. Concrete and real examples are distinguished, code exposes the origin loop, and changed practice assesses decisions as well as arithmetic. The outcome editor was the material failure of that learning contract and is fixed. Existing caution/provenance prose is somewhat prominent, but no new disclaimer layer or manuscript rewrite was added.

Limitations: this complementary browser run used the shared dev server; production loading/recovery belongs to the root integration receipt. It does not claim exhaustive pixel inspection of every editable state. No falsification harness ran, and no source-mutating test was used. User acceptance remains separate. Exact source identities and evidence reuse reconciliation are in `evidence/classical-final-audit-timeseries-endtoend.json`.

## 39 — End-to-End Supervised Learning & Error Analysis: complete

Original complete [prepared lesson](drafts/end-to-end-supervised-learning-error-analysis/lesson.md), [visual contracts](drafts/end-to-end-supervised-learning-error-analysis/visual-specifications.md), and [design/research/implementation record](drafts/end-to-end-supervised-learning-error-analysis/design.md). These historical sources and the content checkpoint are unchanged.

Time-Series was closed before this second topic's substantive source/browser audit. The read-only preflight confirms published content and implementation complete. Read the complete manuscript, visual contracts, provenance and author calculations, reconciled the design's implementation/review dispositions, and compared the complete live lesson, figures, labs, shared state, saved data/example and relevant numerical/geometry paths. The full research sequence is retained: prediction unit and split design; fitted preprocessing; majority and competing model baselines; balanced accuracy versus accuracy/log loss; reproducible offline study; paired improvements and regressions; slices and hypotheses; frozen selection and held-out report; experiment record; deeper uncertainty/nested evaluation/deferral; and all six changed practices. No new topic or manuscript rewrite is introduced.

Four defects and two wording errors were corrected:

- **Held-out answer disclosed in prose before freezing.** Section 7 said “do not tune on its one error”, revealing the held-out count despite the numeric/result gate. The record now says to retain the frozen result and use fresh evaluation for later tuning. Inspection and paired browser checks confirmed that visible program stdout contains exactly the 12 development lines before opening; its four held-out lines, confusion matrix, compact report and numerical Wilson example remain withheld. The full runnable program/download is intentionally retained, along with its existing explanation that local execution prints all results. This is a teaching sequence, not a claim that the downloadable data hide the answer.
- **No visual error encoding after comparison.** The development scatter showed only actual class and slice membership. `referenceWrong`/`candidateWrong` were computed but never painted, despite the prepared contract requiring mismatch marks. After commitment, dashed inner rings now mark reference errors and solid outer rings mark candidate errors inside the chosen slice. Legends name both models, exact error IDs appear beside the plot, and the inspector remains available. Coordinates stay fixed when the model changes; there are no answer rings before commitment.
- **Empty slices printed invalid comparison output.** The grader correctly refused to judge an empty slice, but its table still printed blank “of 0” counts and “Difference in error count: null.” Empty slices now retain only the ungradable explanation and no numerical comparison table. They do not report a zero error rate.
- **Equal or nearby edited confidence scores hid cases.** The accepted-case arithmetic was correct, but cases 6 and 9 edited to 0.80 overpainted case 4 at the same rail coordinate. Final visual inspection found this after the numeric tests passed. A deterministic row assignment now stacks colliding tiles vertically while keeping their horizontal score coordinate exact. The canvas, queue positions and threshold gap grow with the stack. The caption states that vertical position carries no quantity. Both exact ties and adjacent hundredths were checked, including all ten cases at 0.80; all case identities remain visible.
- The scatter's claim that close specimens could not be distinguished “at any size” was too strong. Its caption and model comment now state the actual limitation of this compact plot, preserving the exact-value inspector as the membership authority.
- The fit/generalisation table correctly computes training minus validation, but its footnote described the opposite subtraction. A final one-line copy correction now says it subtracts validation from training, matching both the expression and the existing diagnostic explanation. This was reviewed directly without repeating the numerical campaign; the original browser-run hashes remain recorded alongside the explicit post-run copy correction.

The original packet's one-scatter implementation adaptation is retained: after comparison it now actually shows both candidates' mismatches, with two non-color-only outlines and exact text IDs. The snapshot-dependent full-program/output separation is retained as an intentional documented implementation adaptation. Native program bytes, fitted study inputs, all recorded outcomes, metric rules and substantive authored claims remain unchanged.

`node scripts/verify-endtoend-models.mjs --no-evidence` passed after the final geometry change: 284 grouped checks in 53 groups, including 44,832 slice comparisons, 55,692 acceptance ledgers, 48 selection settings, and 16 paired comparisons. The original native/data receipts are reused only for their still-matching program, manuscript, dataset, packet calculation and generated module hashes. The older model/browser reports are historical evidence, not asserted to bind these changed UI/geometry sources.

`scripts/verify-endtoend-final-audit.cjs` passed 11 complementary browser groups with 31 screenshots on the shared dev server. It checks 12 slice states across all four candidates and both sides, exact observed-value cutoff boundaries, two empty slices, every rendered error-ring ID against independently filtered saved rows, fixed point coordinates and linked specimen selection. It checks same-winner protocol refusal, both held-out commitments, exact stdout before/after and keyboard reset. Cost cases cover fractional costs, two edited scores exactly at the acceptance boundary, an entirely deferred queue, ten identical scores and ten adjacent scores. Real rendered rectangle bounds must not overlap. No page overflow, collapsed radical, KaTeX error or page exception appeared at 1366, 390 or 320px.

Actual visual review covered all seven diagram panels at desktop and both phone widths, the lower/upper slice comparison and numeric verdict, specimen selection, the opened held-out report, the edited-score cost comparison, all-ten-tied rail, and no-answer conditional-error state. The lower slice visibly has two reference and three candidate errors; the three-way score tie now visibly contains three distinct tiles. All ten tied tiles remain readable and at the same score coordinate at 320px. The paired strip retains its regressions, per-class recall shows cultivar 0 worsening, axes preserve the majority anchor, and text equivalents reflow. Compact SVG text remains small on the narrowest phone, as in the prior accepted implementation; the HTML legend, inspector and tables carry the readable details. Screenshot-only styling hides the fixed navigation; it does not alter lesson geometry or calculation.

Learning-experience judgment is heuristic, not a novice user study. The first-pass route produces a coherent reproducible study; the deeper path develops inference limits and decisions with deferral costs without replacing the core. Practice changes counts, grouping, information flow and the proposed study rather than asking for copied output. The numerical and experimental concepts are accurate within the stated sample contract. The gated-outcome prose and absent error marks were material violations of the lesson's own learning task and are fixed. No additional scope is recommended as part of this audit.

Limits: the full historical native/falsification campaign was not repeated. The existing source checker invokes recovery/heartbeat/liveness harness probes, so this audit did not run that checker; changed JavaScript/JSX/CSS received separate read-only parser and raw-control checks, the model suite ran, and the actual reader rendered/interacted successfully. Screen-reader behavior and observed beginner learning are not claimed. Root owns final production build, artifact loading and central ledger reconciliation. Source freeze occurred after the final image review; the exact final hashes and scoped reuse checks are in [the paired machine-readable evidence](evidence/classical-final-audit-timeseries-endtoend.json).

Retention: 58 final captures, two pre-repair diagnostic captures, both reusable browser verifiers and durable baseline/current hashes remain at their recorded paths. Automatic approval review rejected recursive removal of the owned temporary audit folder because it includes potentially useful untracked baseline/evidence files and the reviewer found no explicit cleanup authorization. The folder was left intact; there was no alternate deletion attempt. Prepared packets, original reports, shared runtimes and other agents' work remain preserved.
