# Topology & Topological Data Analysis — verification

10 September2026 UTC /11September local. Mathematics40, stable identity `topology-topological-data-analysis-tda`. The [design](TOPOLOGY-TDA-LESSON-DESIGN.md) owns pedagogical and scope decisions. The [author evidence](evidence/topology-tda-author-review.json) freezes actual source, runnable/native/model/browser results and the images actually opened. Independent review and root integration remain separately recorded; user acceptance is pending.

## Implemented learning route

The full replacement starts with the represented space and distinguishes continuity, homeomorphism and homotopy. It teaches simplices, finite F2 chains, boundary cancellation, quotient homology and rank formulas before persistence maps and complete column reduction. Later sections connect diagram matching and stability to cubical images, delay coordinates, integrated topological features, a reproducible evaluation and Mapper overlap. Optional coefficient/fundamental-group/multiparameter branches state what the core summary forgets. The exact next module topic remains Category Theory, then Differential Geometry.

Seven distinct investigations show chain/filling changes, linked Rips geometry/barcodes, boundary-matrix cancellation, partial diagram matchings, shared pixel cells, landscapes/integrated pixels and Mapper memberships. Three inline figures introduce invertible scaling versus collapse/relative openness, pairwise versus common ball intersections, and delay-coordinate rank. The visual forms are chosen for those mechanisms, not a template quota. Three checkpoints and twelve independent practice groups have explained feedback, changed cases and readiness criteria. Nine complete standard-library Python programs include prediction prompts, actual stdout and interpretation.

The preservation archive has original SHA256 `0e944c5c549399e52954b5a490530c2ee3a068b0342f87095ae785a004292d71`. Its four-point Union-Find program and all three output lines remain byte-exact as decoded code strings. No existing topic identity, publication mapping, module membership or progress was removed.

## Mathematical and native checks

Commands from the repository:

```text
node scripts/export-topology-tda-model-fixtures.mjs
scratch/lesson-tools/Scripts/python.exe scripts/verify-topology-tda-models.py
scratch/lesson-tools/Scripts/python.exe scripts/verify-topology-tda-native.py
```

The model oracle uses independent row-space/nullspace calculations, not another copy of persistence-column pairing. It checks1,024 five-vertex clique complexes,12 scaled point-cloud cases,348 inclusion-image-rank comparisons,512 planar pixel triangulations,108 augmented-assignment matchings,48 Mapper transitive-closure/intersection cases,96 Gaussian intervals and18 persistence-image configurations. Additional export checks cover48 trace identities and12 invalid-input cases. The final oracle records3,892 assertions. The Gaussian pixel quadrature rescales the integrand by its maximum density to avoid the oracle itself losing far-tail accuracy at fixed precision.

The actual production examples execute under Python3.12.14, including the preserved original and the full synthetic classifier. An additional2,504 changed-input comparisons cover all512 binary3×3 row spans,1,024 complex ranks,12 Rips filtrations,512 pixel cases,108 matchings,96 high-precision interval probabilities and240 pixel contributions. Native/JavaScript distance calculations can separate mathematically tied events by a few ULPs; cross-language finite bars are compared after11-decimal rounding, while exact finite-field and cell computations are compared exactly. This rounding is an explicit verification limit, not the browser's persistence computation.

The actual synthetic workflow uses independent14-point clouds and separate training/validation/test splits. Both frozen descriptors score24/24 on the simple test split; irregular sampling gives15/24 topology versus17/24 radial baseline, anisotropy gives12/24 each, and new coordinate noise gives13/24 versus21/24. These are reproducible toy outcomes, not a population accuracy or performance benchmark. The code retains the unfavorable outcomes and does not retune on diagnostics.

Independent review found that subtracting nearly equal normal tail probabilities could return zero for a positive representable narrow interval. Both JavaScript and actual Python now integrate a small-width midpoint expansion with second/fourth derivative terms and log-density prefactor. The custom pixel `[0,1e-20] × [0,1]` with bar`[0,1]` and bandwidth0.5 gives about`3.807903013637525e-21`. The final normal-range oracle's worst relative error is about`7.31e-13`. Subnormal rounding is separately reported, including a below-representability case rounded to zero; no uniform relative bound is claimed for subnormals. The independent review preserves the initial failures and repaired comparisons.

## Browser, accessibility and visible evidence

`node scripts/review-topology-tda-lesson.cjs` ran actual Edge152.0.4191.66 with loaded Space Grotesk fonts at1440,390 and320 pixels. Each viewport passed458 checked states across all seven investigations. The check compares every program/prompt/stdout, opens the practice feedback, checks13 actual section anchors and the exact next-topic link, and exercises reset/back/changed fixtures, exact thresholds, matching choices, pixel contributions and Mapper membership in both directions. All nine equations fit the reading width; no page overflow, nonfinite SVG path, KaTeX error or runtime/console error was observed. Scaled SVG labels have at least14 CSS pixels. Native keyboard focus and arrow controls work; the explicit reduction-matrix local scroll is keyboard-operable.

The author actually opened sixteen final captures: all seven distinct desktop investigations plus the three inline figures, and phone captures of the integrated landscape/image, matrix reduction, Mapper higher-intersection warning, ordinary chain teaching, ordinary landscape equation and practice flow. Filtration barcode endpoint equality, multiplicities, chain/pivot states, common-intersection geometry and actual pixel values match their captions. The linked JSON lists exact image names and hashes. Capturing a file alone is not counted as visually reviewing it. Full-reading screenshots retain the site navigation; focused lab captures hide it during capture so its fixed overlay does not obscure the object being inspected.

The initial browser pass found long-equation overflow and slider step rounding inconsistent with exact-event buttons. Stacked equations and an unrestricted continuous slider step fixed those issues. Plot endpoint labels, mobile text size and the scale-map illustration were also repaired before final review. Source formatting conserves parsed ASTs and performs a fresh JSX transform; no mathematical text is passed through a destructive JSX-entity rewrite.

## Loading, research and limits

The body imports only its own examples, models and labs plus shared teaching/math components. Model inputs are bounded: at most14 points, controlled complex sizes and small matching problems. Changing a threshold/selected column uses the already computed filtration; the full synthetic experiment is displayed code, not an automatic browser workload. No additional package was added. Production build/import/loading/conservation evidence belongs to the root integration record and must identify this exact source revision.

Primary readings and what was actually reviewed are in the design's source ledger. The standalone programs do not invoke GUDHI;3.13.0 is the inspected documentation version. Two direct official lecture recordings are annotated as identity/link checks, not full viewing. The mathematical exercises and high-precision checks establish their finite declared examples, not arbitrary topology reconstruction, statistical significance, clinical interpretations or learner acceptance.
