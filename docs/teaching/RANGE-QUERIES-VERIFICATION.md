# Segment Trees, Fenwick Trees & Range Queries — verification

Authored 10 September 2026 for stable ID `segment-trees-fenwick-trees-range-queries`, DSA position 12. Source has been registered by the coordinating agent; publication is separate from teaching review. Title/order and all existing topics are preserved. This author record does not claim user approval, judge submission, video playback, production timing measurements or deployment.

## Scope and representations

[Individual design](RANGE-QUERIES-LESSON-DESIGN.md) contains the pre-authoring scope decision, prerequisite bridges, representation contracts and primary-source review. The lesson now teaches static cancellation, generic ordered segment folds and point assignment, Fenwick binary blocks/linear build/increment/set/frequency selection, boundary differences/two-Fenwick range sums, lazy range add/set/sum and new-after-old composition, sparse minima/idempotence, fixed maximum/signed shortest deques, and compressed-rank weighted DP with an actual witness. The two incoming notes are resolved to actual teaching; historical versions are handed to their existing later owner.

| Representation | Visible correctness contract | Interaction and accessibility |
| --- | --- | --- |
| Prefix and difference figures | Actual array positions, cancelled beginning, two boundary deltas and multiplicity identity | Readable indexed values and nearby arithmetic; no invented measurement plot |
| Segment range investigation | Computed node-edge geometry, intervals, disjoint selected cover, left/right ordered accumulators and repaired ancestors | Editable bounded signed arrays, sum/min/max, query/set, explicit apply/reset, finite previous/next/finish, exact node table |
| Fenwick block investigation | Each internal cell's binary index/lowbit and external interval, query partition versus containing update blocks | Editable array/end/index/delta; linked values, blocks, visited chain, result and messages; local keyboard panning |
| Lazy investigation | Logical values versus raw stored sums, own tags already included, pending child maps, forced push/clear and dirty-parent pull | Range add/set/query, chronological chaining, explicit draft application, actual finite snapshots and accessible table |
| Sparse overlap figure | Same query shown as two exact length-four blocks; min result 0 versus incorrect overlapping sum 19/true sum 15 | Static labelled numerical example, locally scrollable positions |
| Moving maximum | Original positions, active window, surviving candidate identities, separate expiry/domination reasons, output maxima/argmax | Editable signed values/width; newest-equal policy, partial/no-window and operation-count readouts |
| Signed shortest | Computed prefix totals against boundary index, exact threshold, candidate starts and best actual highlighted interval | Positive target, signed input, two deletion reasons, finite steps, exact prefix table; no runtime or probability claims |
| Weighted rank walkthrough | Original input order, compressed rank, prior score, candidate, retained rank score and recovered original witness | Calculated table with strict smaller-value boundary and duplicate identities; prose supplies complete recurrence and parent proof |

No lab evaluates arbitrary source, makes network requests or uses timers. At most eight bounded integer values, finite events and clone/freeze snapshots keep model arithmetic exact and interaction bounded. The default four-leaf tree uses compact geometry with unchanged 12px interval and 17px sum labels; larger structures intentionally pan locally instead of shrinking text. Static figures and lab outputs expose exact data rather than fabricated performance curves.

## Native and independent model checks

- `node scripts/verify-range-query-examples.mjs`: all **13 complete standalone Python programs** match exact stdout under Python 3.12.14. The verifier then executes `verify-range-query-native.py` against each program's definitions, independently of their printed samples.
- Native oracles: **300** signed/empty/non-power-of-two arrays and all their static intervals; **15,000** mixed operations against directly changed arrays for lazy and two-Fenwick behavior, plus point assignment direct totals; static prefix and difference-batch reconstruction; exact string folds detect noncommutative reversal; **1,093** exhaustive arrays from alphabet `[-2,0,3]` through length six across widths 1–8 and targets 1/3/5/9; **800** weighted-subsequence fixtures against exhaustive subsets with witness score/order/strictness; **400** mutable frequency sets with expanded-occurrence rank oracles; **3,034** independently traversed rooted subtrees; **300** richer-summary arrays/all intervals against enumeration of nonempty subarrays.
- `node scripts/verify-range-query-models.mjs`: **320** arrays, **17,838** interval folds/canonical-cover checks, direct Fenwick blocks/prefix partitions/containing-update sets, **8,000** mixed lazy operations against plain arrays, **13,116** deque cases with exact ties/witnesses and append/pop invariants, **800** weighted-subsequence oracle cases. Invalid parser/range/empty-minimum/positive-target cases are included. Frame snapshots are immutable; completed lazy roots and logical leaves match direct values.
- The exhaustive fixtures are finite independent checks, not proofs for arbitrary input. The lesson supplies interval decomposition/invariant/termination, algebra, map composition, deque dominance/retirement and weighted-state induction arguments separately. Counting all qualifying ranges, arbitrary lazy maps, floating-point associativity and persistent versions are explicitly outside the proved contracts.
- New model/lab source was formatted with the installed Babel generator. `scratch/format-range-sources.cjs` verified normalized AST equality across formatting; it did not change native program text or lesson prose. The models were rerun after final geometry edits.
- `node scripts/verify-curriculum.mjs` passes after setting the blueprint's supported depth value `core`: 28 modules, 1,218 stable topics, 307 individual briefs, 7 paths at this check; all 1,002 pre-expansion IDs preserved and dependency graphs acyclic. Root owns subsequent global registration, generated artifacts and the production build.

## Browser review

`node scripts/review-range-query-lesson.cjs`, Edge through Playwright against shared Vite 5173, covers 1440/390/320px with reduced motion. It blocks unrelated dev-server HMR in the test page to avoid state resets from concurrent authors. Isolated element screenshots temporarily hide the fixed navigation only during capture; entry/reading screenshots use normal navigation. No application UI source is changed for capture.

The first complete pass succeeded at all three widths: segment 26 events, Fenwick 17, lazy 27, moving maximum 52, signed shortest 39 per width; 10 unique anchors, 13 complete code blocks and 5 verified official practice links. No page errors or page-level overflow. Explicit keyboard Enter/Space stepping/backtracking and disclosure opening, visible focus and arrow-key horizontal panning passed.

Actual screenshots were opened, including desktop moving-max, entry390, segment390/320, Fenwick390, lazy deferred/push390, signed-shortest390 and sparse/weighted inline figures. This found the unnecessary four-leaf clipping at320 and awkward accumulator wrap, which were fixed without shrinking labels; wider eight-value diagrams now explain local panning. The full final rerun additionally checks real anchor arrival (waiting for smooth scrolling), empty lazy input and a query with an unused blank amount draft. Final result/evidence is appended after that run succeeds.

Other browser fixes discovered by actual interaction: select labels originally absorbed option text, corrected with explicit sibling labels; blank numeric drafts no longer coerce to zero; a lazy query ignores its hidden amount rather than rejecting an irrelevant draft. Invalid edits preserve applied state, and unfinished mutations must finish before another operation starts. Applying a new array/reset is an explicit restart. Final screenshot review shortened the lazy action options to Set values/Add values/Query sum so their selected labels remain readable in the narrow controls.

Evidence location: `scratch/range-query-lesson-review/results.json` and named entry/inline/segment/Fenwick/lazy/moving/signed PNGs. Verification scripts are reusable and topic-specific. Screenshots show the actual computed outputs; no human beginner study or empirical learning-effect estimate is claimed.

## Final author acceptance

The full final browser run passed **all three widths** with **26 segment, 17 Fenwick, 39 lazy, 52 moving-maximum and 39 signed-shortest events per width**. All ten actual lesson anchors reached their targets; all thirteen native-example blocks and five official practice links were present. Empty/larger-window/no-answer and invalid-draft cases, keyboard stepping/backtracking/disclosures, visible focus and real local panning passed. Page widths remained exactly 1440/390/320 respectively, with no browser page errors. The four-leaf trees now fit the 320px reader content without shrinking labels; final segment320/lazy320/signed320/difference390/Fenwick1440/lazy-push1440 screenshots were opened and inspected. The small final dropdown wording change receives a targeted screenshot/recheck, rather than repeating unrelated algorithm cases.

Final native, independent model and curriculum commands all passed again after the geometry/input revisions. The final dropdown-label check passed at all three widths and reapplied the actual lazy set operation to obtain [5,5,5,7]; its 320px screenshot was opened and the selected label fits. Evidence: `final-label-results.json`, `lazy-final-labels-*.png`, and `final-source-hashes.json` under the same review directory. Ten semantic source files were parsed/fingerprinted after that last wording edit. The source is ready for the coordinating agent's independent review and integrated production checks; no source build or shared manifest edit was performed by this subtask author.

## Independent review and narrow wording correction

[RANGE-QUERIES-INDEPENDENT-REVIEW.md](RANGE-QUERIES-INDEPENDENT-REVIEW.md) records the separate complete source/proof review and additional finite native/model oracles. No algorithmic defect was found. A leaf's apply-frame message incorrectly described composing a child tag even though leaf storage correctly had no tag. The coordinating agent authorized a narrow correction: internal nodes retain their existing explanation; leaves now explain that their sum changes directly and no pending child tag is needed. No numerical or structural operation changed.

After that edit, `node scripts/verify-range-query-models.mjs` passed again (320 arrays; 17,838 intervals; 8,000 lazy operations; 13,116 deque cases; 800 weighted cases). A targeted real-browser check at 1440/390px passed at **10 September 2026, 10:09:14 UTC**, exercising both message branches and keyboard Enter through the singleton set frame. The displayed logical/stored value was 5, no leaf tag was rendered, and no page error or page overflow occurred. Both actual screenshots were opened and their message/leaf/focus layout inspected. The earlier full browser/native evidence remains applicable to unchanged behavior.

Evidence is `scratch/range-queries-independent-review/leaf-message-results.json`, `leaf-message-1440.png` and `leaf-message-390.png`; reusable check: `scratch/review-range-leaf-message.cjs`. Revised `src/learn/data/range-query-models.js` SHA-256 is `5944d2dd0ca621c8cca9e78a5115317a2a4435047cb28bc54264a30d1053877e`, superseding that file's earlier fingerprint only. Source is frozen again for root integration.
