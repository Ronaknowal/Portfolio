# First five Deep Learning lessons — completed implementation

21 September 2026. Scope: finish the first five prepared topics in **Deep Learning Fundamentals & Architectures**, in module order. All five passed the content-first finish preflight. This continues revision 2; it does not start a new authoring revision or authorize topic six.

| Position | Topic | Independent review | Distinct learning mechanisms |
| --- | --- | --- | --- |
| 1 | Perceptrons, Neurons & Activation Functions | [review](PERCEPTRONS-INDEPENDENT-REVIEW.md) | Weighted contributions, boundary geometry, XOR construction, activation values/slopes, real digit evidence, gated units and exact resource counts |
| 2 | Backpropagation & Automatic Differentiation | [review](BACKPROPAGATION-INDEPENDENT-REVIEW.md) | Live line fitting, shared-path gradient accumulation, finite-difference failure/cancellation, broadcasting, shapes, native programs and training traces |
| 3 | Loss Functions | [review](LOSS-NORMALIZATION-INDEPENDENT-REVIEW.md) | Spatial outliers, objective minima, full focal derivatives, recorded threshold decisions, triplet geometry/mining, candidate masks and exact reduction/storage accounting |
| 4 | Batch, Layer, Group & RMS Normalization | [review](LOSS-NORMALIZATION-INDEPENDENT-REVIEW.md) | Tensor membership, centering/RMS geometry, batch-memory transitions, all derivative branches, recorded training and explicit residual forks |
| 5 | Transfer Learning & Fine-Tuning | [review](TRANSFER-LEARNING-INDEPENDENT-REVIEW.md) | Backbone/head ownership, separate freeze controls, fixed partitions, editable LoRA factors, adapter budgets, retained validation/test evidence and checkpoint semantics |

Each packet’s `design.md` maps the complete prepared material to production. Original manuscripts, visual specifications, code, inputs and research remain retained. The [scope baseline](evidence/deep-learning-core-baseline.json) preserves all five exact pre-implementation ledger rows, their earlier histories, all 177 row hashes and the publication-manifest hash. This is preservation evidence, not a substitute for the final checks.

## What changed during review

Independent reviews found and closed concrete issues: sequence length was too easily confused with feature width; some live bar scales concealed magnitude changes; backward arrowheads needed correct direction; Loss needed spatial observations, category shapes, reduction denominators and linked specimen/threshold details; Normalization needed explicit dependency and skip graphs; Transfer needed exact schedule coordinates and readable phone ticks. Phone table cells now retain whole numeric tokens inside labelled local scrollers. Invalid fractional counts are rejected locally. The slash-joined normalization display title was made readable to prevent narrow-header overflow.

All labs expose their current calculations immediately. There is no learner-prediction entry or answer gate. Written practice keeps independent hints and worked solutions. Current numeric calculations, committed state transitions and saved experimental measurements are visibly different. The unfavorable Transfer result, **77/100**, is preserved.

## Evidence and verification

- Perceptrons: actual native examples and the complete recorded fits; 8,658 activation value/slope comparisons; 100 complementary independent assertions. Final production browser: seven groups at each of 1366/390/320 px, 39 captures.
- Backpropagation: 174 model assertions; nine complete native programs with 51 assertions; complementary independent coverage. Final production browser: 94 assertions, 30 captures.
- Loss and Normalization: 114 counted model assertions; complete nine-run and twelve-run CPU experiments reproduce their saved results; three separate displayed API/precision snippets execute. Independent numerical/conservation checks and all seven representation findings are closed. Final production browser: 16 named groups, including actual pointer/keyboard edits, invalid/reset/null cases and all three widths.
- Transfer: fourteen author model/source groups and byte-identical full native replay, plus three independent invariance/selection groups. Final production browser: eleven groups, exact downloadable bytes, keyboard/pointer/touch edits, record-role separation and phone plots.
- [Production integration](evidence/deep-learning-core-production-integration.json): five fresh browser contexts load only the selected published lesson body; all section links resolve; rendered mathematical SVGs remain intact; themed links and downloads pass; Next follows all five lessons and then Weight Initialization. Loss/Normalization full programs are fetched and mounted only when opened and unmounted when closed.
- Curriculum, generated artifacts, direct math/import boundaries, runtime source organization, delivery ledger, inventory and application build pass. Final reconciliation confirms no other ledger row changed and no published ID disappeared.

The browser programs needed two production-specific harness corrections: wait for the actual asynchronous disclosure unmount instead of reading immediately after a key press; and handle Vite’s valid inline `data:` download for a small provenance file. The shared CodeBlock is a formatted `div`, so its deferred-source check reads that actual DOM rather than assuming a `pre`. These are recorded as test assumptions, not lesson defects or new numerical evidence.

## Loading, limitations and continuation

Lesson bodies, figures, data and models remain topic-owned and lazy. Complete long programs use deferred views; the neural training experiments run offline, not automatically in the browser. Exact measured records and bounded small calculations provide the labs’ outputs. Initial route JavaScript byte counts in the integration receipt are uncompressed transferred source sizes including the existing reader/catalogue/math libraries, not compressed wire measurements or timing benchmarks. The existing large catalogue chunk still produces Vite’s size warning; this scope did not redesign the shared catalogue.

The current ledger should show **177 content-complete, 140 implementation-complete and 37 prepared implementations remaining**. All 1,460 stable topics, 231 publication mappings, 29 modules and ten paths remain. These five replaced older published bodies; they did not publish five new topics. Next in this module is **Weight Initialization: Xavier, Kaiming & μP**, under a new request.

No deployment or commit was requested. User acceptance remains separate. Reuse these source-bound checks for unchanged files; do not restart the authoring/review cycle merely because a later session opens.
