# Linked traversal and monotonic-stack extension — author verification

Author checks passed on 11 September 2026. This is the scoped extension of `linked-lists-stacks-queues`, preserving its stable title, placement, prerequisites and original material. It closes the assigned fast/slow and monotonic-stack teaching gaps; parent-owned independent review and production integration are separate.

## Preservation and resulting teaching

The [immutable original snapshot](evidence/linked-traversal-extension-original.json), captured at 08:15:13 UTC before mutation, contains original source, examples, practice, design and historical records. All six original programs and stdout strings remain exact. The verifier conserves 61 original meaningful teaching AST subtrees, the three old lab components/model bytes and all ten old practice placements. Only problem 141's now-outdated transfer deferral changes; its correct visited-identity baseline remains. Old review dates are retained, with 11 September separately attributed to four newly inspected statements.

The extension adds the constant-space positive meeting/reset proof and actual entry-object contract; first/second even-middle guards and a guarded identity-preserving left-heavy split; strict next-greater unresolved indices and aggregate charging; and a separate candidate-dominance proof leading to both strictly smaller boundaries and complete histogram geometry. Four mechanism-specific investigations expose actual links, two distinct query/cut views, literal unresolved stack entries and true bar/rectangle geometry. Four complete new Python programs include actual outputs, local setup and questions. Four changed independent tasks include hints and worked answers; official 876/142/739 transfer follows its prerequisite teaching, with 84 an explicit optional harder branch. No new DSA family is claimed.

## Executed numerical and conservation evidence

`node scripts/verify-linked-traversal-extension.mjs` passed on final source at 08:49:18 UTC. Its Python subprocess executes every actual stored program, rather than copied verifier implementations. Results are in [the native payload](../../scratch/linked-traversal-extension-verification/results.json).

- Ten exact-output programs: six original, four new.
- 3,525 general successor-map/head cases, using an independent first-visit orbit and direct repeated-successor reference. These include null starts, unreachable components, self-loops and changed prefixes/cycles. Actual Python results preserve original Node identity and links.
- 3,280 exhaustive arrays over three nonnegative heights and 243 signed arrays. Direct forward searches independently check strict/equal distances; direct scans check nearest smaller blockers; exhaustive subinterval minimum/width products check histogram area and the actual native witness.
- 52 model middle/split states, 41 actual native chain lengths, and 190 cyclic inputs rejected before any split mutation. Returned halves partition the original objects in order.
- Seventeen invalid model/parser cases, stack invariants, each-index charging and final unresolved zeros. Native/example parsing and original historical-byte conservation also pass.

The six newly written model/lab/CSS files were conventionally formatted with normalized Babel/PostCSS AST equality. Their JSX text was preserved. Later narrow copy/zero-height refinements are documented by the final native and browser run, not incorrectly attributed to formatting conservation.

## Actual browser and opened reading review

`node scripts/review-linked-traversal-extension.cjs` passed at 1440, 390 and 320 px with actual Space Grotesk/public fonts. [The final payload](../../scratch/linked-traversal-extension-browser/results.json) includes 86 operated states per width: original controls, cycle meeting/entry/head/self-loop/empty/acyclic, middle policies and two returned heads, strict/equal comparison, signed readings, multi-pop bursts, both boundary directions, equal/tied/zero/empty histograms, invalid transactional drafts, backward/forward/reset and keyboard controls. Long chains and bars use readable local keyboard scrolling; they do not shrink into unreadable full-width diagrams. The test separately asserts the actual selected histogram rectangle's x/y/width/height and exact zero bar heights.

All ten complete displayed code/output blocks and new program questions match source; all fourteen practice placements render; hints/solutions open by keyboard; route anchors resolve. Selector captions fit; console/page errors and document overflow are absent. Eighteen final ordinary-reading/operated screenshots were actually opened, including the cycle entry, self-loop at 320, both parts of a keyboard-scrolled cut, multi-pop trace, signed readings, selected/tied histogram rectangle, zero-area feedback, narrow proof/practice and annotated references. Their exact paths/hashes and all eleven final production-source hashes are in [the frozen author packet](evidence/linked-traversal-extension-author-review.json). The packet and production files are archived separately under `scratch/linked-traversal-extension-author-freeze/`.

The first browser attempts stopped because the harness used an ID instead of `data-investigation`, then used exact implicit-label text instead of the accessible combobox role for old selects. No old lab source fix was needed. The successful final run supersedes those harness failures. Image review also clarified the reset proof's t-hop route, added an explicit keyboard-scroll hint and made a height-zero bar exactly zero geometrically; the final rerun includes these changes.

## Research and limits

The scoped [design addendum](LINKED-TRAVERSAL-MONOTONIC-EXTENSION-DESIGN.md) records inspected primary book sections, official statement URLs/contracts and accepted differences. The CSES handbook's total-successor graph is adapted with nullable guards; local proofs supply the middle and histogram transfers. Four new official LeetCode public statements were inspected; no editorial access, platform submission execution or new video playback is claimed. Existing annotated CS50 video/notes and Python written alternatives are retained.

No shared catalogue, manifest/index, ledger, coverage map, old historical review or unrelated topic was changed. Bounded exhaustive oracles support, but do not replace, the local invariants and proofs. This packet does not claim a screen-reader session, arbitrary-range Python performance, broader production build or completion of the full rollout.
