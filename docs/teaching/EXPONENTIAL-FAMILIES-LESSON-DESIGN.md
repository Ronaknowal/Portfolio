# Exponential families and sufficient statistics — scoped design

10 September 2026. Mathematics22; stable identity `exponential-families-sufficient-statistics`. Read the exact inventory, complete current body and current teaching/domain/design/code standards before replacement. No incoming destination note exists. Root owns shared registration and integration; this record is design, not completed verification.

## Preservation, scope and learning route

Retain the title: the useful expansion explains its promised mechanism rather than introducing a new subject. The original 6501-byte body (SHA256 `ce8e329beb57157ee2ffeb3ce7600d7ec083498bf50e2ededb13f123c0a05855`) and its complete Bernoulli program/output are preserved in `evidence/exponential-family-original-content.json`. Retain that program exactly on its finite fixture, the Bernoulli/Poisson/Gaussian/categorical connections, model-relative sufficiency, conjugacy, stable losses and the weekday-count question. Repair the unqualified moment-matching claim and the implication that every GLM must use its canonical link. The old body has no native illustration/investigation, derivation, full worked sufficiency test, resource list or explained independent solution.

Main question: when can a compact summary preserve all parameter information under a declared model, and how does this lead to tractable likelihoods, gradients and posterior updates? The beginner finish line is to explain sufficiency using conditional datasets and factorization, identify every term of a concrete family, and diagnose a model change. Intermediate work derives moments and finite interior MLEs, compares Gaussian summaries and computes feature-aware or exposure-aware updates. Advanced depth addresses support, minimal representations, boundary MLEs, conjugate prior coordinates and exponential tilting without pretending a short chapter supplies all statistical theory.

Read after Variational Inference, with Probability Distributions/Bayes and MLE/MAP as actual useful foundations. Supply local bridges for conditional distributions, likelihood versus probability, log-sum-exp, gradients/covariance and densities under coordinate changes. Do not require the next Measure Theory lesson to follow the core; explain counting versus integration now and bridge to Measure Theory & Probability Spaces at the end. The module's actual order stays unchanged.

## Mechanisms, representations and evidence contracts

| Hurdle and mechanism | Concrete representation, placement and operation | Verification and limits |
| --- | --- | --- |
| Information is relative to a model | Before notation, six binary strings with two successes form one group; conditional mass is 1/6 for every interior shared p. Static strings retain positions; count grouping is layout, not a probability scale. | Enumerate all 16 strings and exact rational conditional probabilities; impossible conditioning at p=0/1 is excluded. Text gives the same result. |
| One count can stop being sufficient | Editable eight observations divided into two fixed groups; compare to a fixed dataset with the same total. Select common-p versus two-group model and change group probabilities. Likelihood ratio and group counts expose lost allocation. Predict a within-group versus cross-group swap, then reset. | Independent direct-product enumeration; finite input p in [.05,.95]; no claim that a likelihood ratio is a posterior or that data are actually generated by this toy model. Eight accessible toggle buttons, labels and narrow two-row layout. |
| Normalization couples every probability | Immediately after exp-family construction, three outcome rows show base weight, log-weight, relative weight and normalized mass as η changes; linked A/mean/variance and probability bars show a common denominator. | Stable log-sum-exp; exact finite sums/independent derivatives; h=[1,2,1] is a deliberately declared base weight. All graph values calculated, not measured. Tiny finite values remain scientific notation; no manufactured exact zero. |
| One derivative is a mean; the next is covariance | Derive cancellation explicitly beside the normalizer, then use exponential tilting A(η+u)−A(η) to reconnect to Concentration. | Finite enumeration and analytic/integral oracles; identities concern natural coordinates at interior points with justified differentiation, not arbitrary boundary/curved parameterization. |
| A mean vector must be geometrically possible | Three outcomes x∈{−1,0,1} map to T=(x,x²), forming a triangle. Learner changes η1/η2 and three counts, fits an interior target, or selects a missing-category boundary. Predicted versus empirical points and masses stay synchronized. | Exact three-state algebra, independent covariance/optimization; no universal interior-existence theorem stated without extra conditions. Finite control limits labelled, fit not silently clipped; boundary never represented as a finite fit. Triangle x-axis E[X], y-axis E[X²]; accessible numerical table. |
| A sum cannot retain unknown spread | Inline number lines compare [1,2,3] and [0,2,4], same mean but different squared deviations. Gaussian density expansion explains sufficient pair, then a complete stable merge program gives a useful distributed application. | Direct Gaussian log-density and independent high-precision centered sums; known versus unknown variance explicit; stable summary merge is mathematically equivalent in exact arithmetic, not bitwise associative floating point. |
| Same prior, different density coordinate | Paired p and log-odds density plots for integer-parameter Beta prior; marked corresponding interval and probability mass, with density-height/Jacobian explanation. Adjust α/β; interval endpoints fixed in p and transformed to η; reset. | Independent beta CDF/quadrature and Jacobian; curves are analytic rounded samples, finite η viewing window can omit tails. Heights are densities per distinct units, not interchangeable probabilities. |
| Features and exposure change the retained summary | Feature→natural parameter→mean flow beside fixed-design Bernoulli likelihood; fully worked Xᵀy and Poisson exposure Gamma update. | Executed gradients checked independently and posterior normalization checked; common total is insufficient for general known features, and exposure is held-known context rather than event count. |

There is no lab or diagram quota. Four investigations address distinct operations: changing a model, normalizing weights, matching moments and changing density coordinates. Inline string/spread/feature pictures introduce visible relationships before controls. Reassess during authoring if another hurdle needs a better form. All controls must support keyboard, immediate consistent state, reset and explicit boundary feedback; ordinary reading must work without operating them.

## Complete examples and practice

Preserve the old 6-of-8 Bernoulli program and output, then extend through independent programs for conditional enumeration, Poisson likelihood proportionality, log-normalizer derivatives/MGF shift, Gaussian summaries and stable merge, finite three-state MLE/boundary, fixed-design logistic gradient/Hessian, Gamma exposure update and Beta coordinate Jacobian. Counts describe the intended mechanisms, not a quota. Each program has a visible learner question, complete imports/inputs/output, interpretation and changed-input task. Expected outputs are generated only by actually executing source, then checked independently where numerical contracts matter.

Independent practice covers changed fibers, unequal base factors, normalizer recovery, Gaussian known/unknown variance, boundary/minimality diagnosis, feature-aware weekday counts, exposure updates and prior coordinate density. Each substantial task gets a separately optional hint before its explained solution; solutions explain a tempting wrong route. Capstone asks which summary to retain for an evolving service model and what cannot be reconstructed. Completion is not a universal mastery guarantee.

## Research and conventions ledger

Inspected 10 September 2026. Sources support scoped claims; original derivations, fixtures and visuals are authored locally.

| Source / actual reviewed scope | Use and qualification |
| --- | --- |
| [Fithian, Berkeley Stat210A exponential families](https://www.stat.berkeley.edu/~wfithian/courses/stat210a/exponential-families.html), complete page | Common measure/natural domain, interior derivative identities, iid extension, CGF shift. The Gaussian expansion contains transcription inconsistencies; independently derive η1=µ/σ², η2=−1/(2σ²), A=−η1²/(4η2)+½log(−π/η2), rather than copying those lines. |
| [Linderman, Stanford STATS305B](https://slinderman.github.io/stats305b/lectures/04_expfam.html), log normalizer, MLE, minimality and mean-parameter sections | Useful progression and reference-category derivation. Unrestricted categorical logits require log-sum-exp A; A=0 only on the constrained log-probability surface. Do not transfer a constrained formula into unrestricted derivatives. Restrict moment-polytope surjectivity claim to our explicitly solved finite support example. Fisher information is per observation unless multiplied by n. |
| [MIT18.655 Lecture6 Sufficiency](https://ocw.mit.edu/courses/18-655-mathematical-statistics-spring-2016/resources/mit18_655s16_lecnote6/) and [Lecture7 Exponential Families](https://ocw.mit.edu/courses/18-655-mathematical-statistics-spring-2016/resources/mit18_655s16_lecnote7/) | Graduate written alternatives: factorization and canonical representations. PDF parsing contains algebra/transcription slips; use primary definitions plus independently checked examples, not blind copied formulas. |
| [Geyer, proper conjugate priors](https://www.stat.umn.edu/geyer/f22/5421/notes/prior.html), sections1.1–1.4 | Derive hyperparameter addition with density measure stated. Properness is a mathematical requirement; do not present the author's philosophical preference for conjugacy as a theorem. Teach explicit positive Beta/Gamma cases rather than asserting all-looking kernels normalize. |
| [Rigollet, MIT18.650 Lecture21 video](https://ocw.mit.edu/courses/18-650-statistics-for-applications-fall-2016/resources/lecture-21-video/), official page and transcript pp3–11 | Annotated visual/lecture alternative for links, response distribution choice and decomposition. Transcript reviewed; not full audiovisual playback. Official YouTube embed X-ix97pw0xY is linked by MIT, but direct embed fetch failed in this tool; use the working official lesson page. No invented timestamps. |

Potential advanced branches: minimal sufficiency/Rao–Blackwell/completeness, general curved families and non-steep boundary geometry. The body distinguishes minimal representation from minimal sufficiency and gives the projected-score caveat; a full decision-theory/UMVU course is not promised. Maximum-entropy duality belongs after KL is introduced: [the destination note](topic-notes/entropy-cross-entropy-kl-divergence.md) saves its finite-support certificate and reference-measure limits. It also flags an inconsistent classifier-count exercise found during that scoped read. No destination lesson was rewritten. No title change needed.

## Verification plan and honest status

Native Python runs every displayed program and the preserved original. Complementary exact enumeration/Fraction, NumPy/SciPy quadrature and independent direct likelihoods check models, derivatives, Gaussian formulas, all changed practice and prior interval mass. Browser review covers actual1440/390 behavior, keyboard changes, resets, edge cases, links/anchors/code/output and actual opened ordinary reading/static images; inspect320px equations as needed. Model tests are not a beginner user study. Root does the integrated build and shared route/conservation checks after final freeze.
