# Concentration Inequalities — independent review

10 September 2026. Root reviewed the complete Math19 lesson, all eight changed practice solutions and its topic-owned bound/sampling models. Final author body SHA256: `04c2e8ed5cad44eec55006f612643cc00666f7163939a15ac833ca1d9da569cc`. [Author verification](CONCENTRATION-INEQUALITIES-VERIFICATION.md) and [durable source/evidence](evidence/concentration-author-review.json) own the detailed executed checks; production integration remains separate.

The event-first treatment correctly separates an exact probability from a valid upper bound. The Markov/Chebyshev arguments locate nonnegativity and finite-moment requirements; the exponential proof locates independence specifically in factorization. The tilted-variance proof establishes the bounded-MGF lemma rather than asserting it from the original variance. The unequal-width/weighted sum, Bernstein moment bound and chosen exponential parameter, root inversion and loose relaxation are consistent.

The Bernoulli(1/40) all-zero example proves actual miscoverage of the uncorrected variance substitution: its own event probability exceeds the proposed error budget. It does not mistake an individually permitted interval failure for a coverage counterexample. Multiplicative versus absolute tolerances, the binomial KL limit, impossible/certain thresholds, and the fixed-bucket rather than occupied-bucket application retain the correct assumptions.

The three sampling laws distinguish identical marginals from joint structure. The without-replacement extension uses a separate convex-order theorem. Finite-family selection and countable time budgets use a union argument that does not assume independence between checks. The fixed mean, bounded stream, predeclared finite class and changed-protocol limits are explicit. Numerical rankings are tied to the stated example, not advertised as universal.

Root identified a teaching gap in the original final-practice layout: substantial problems had answer reveals without optional hints. The author added separate hint/solution disclosures for all eight tasks. The nine runnable-program questions are now explicitly rendered by a scoped wrapper, avoiding an unused shared-component property. Root inspected these final bindings and opened the final 320px changed-variance hint, 390px rare-variance inline counterexample and 390px exponential-witness plot. The chosen event, control meaning, exact-versus-bound distinction and independent practice remain legible in ordinary reading.

No remaining substantive defect was identified in this bounded source/proof review. The author’s independent integer/Fraction, root-solving, finite-law and asymmetric-MGF checks complement it. Numerical tests are not substituted for theorem proofs, and this record does not claim a second execution of the author’s complete suite, every screenshot personally opened by root, user approval or real deployment assumptions.
