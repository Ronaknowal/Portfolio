# Linked traversal and monotonic stacks — independent review

Closed 11 September 2026 by `/root` against all eleven production sources in the author's 08:53:13 UTC freeze. No production amendment was required. The [independent packet](evidence/linked-traversal-extension-independent-review.json) binds the original author packet, final sources, complementary native/browser evidence and twelve screenshots actually opened. Production integration and user acceptance remain separate.

## Teaching and correctness assessment

Read the complete lesson and extension design, own brief, all practice placements, both new pure models, all four new complete programs, both lab components and both stylesheets. The core linked-list, queue and stack explanations remain connected to the new branches. Floyd's proof states the finite, unchanging, single-successor contract, distinguishes node identity from equal values, derives the meeting congruence and reset walk, handles a zero-length prefix and explains why changing speeds breaks the reset argument. First/second middle queries have explicit guards; the split deliberately retains its first-middle policy, saves the right head before cutting, and rejects cycles before mutation.

The monotonic-stack branch establishes an unresolved-index invariant and the strict/inclusive equality choice before presenting code. Each index is pushed once and removed at most once; one expensive arrival is not incorrectly described as constant worst-case work. The smaller-boundary branch explains candidate dominance separately, removes equality in both directions, derives the excluded-boundary width and proves global optimality by choosing a minimum-height bar in an optimal interval. Nonnegative unit-width bars, zero-area witnesses and tied optima are explicit. Runnable algorithms' working storage is distinguished from the visual step-history storage.

The four new public LeetCode statements were directly inspected on 11 September: [142](https://leetcode.com/problems/linked-list-cycle-ii/) requires the entry object without modifying the list and treats `pos` as a fixture description; [876](https://leetcode.com/problems/middle-of-the-linked-list/) chooses the second middle; [739](https://leetcode.com/problems/daily-temperatures/) asks for the next strictly warmer distance; [84](https://leetcode.com/problems/largest-rectangle-in-histogram/) uses nonnegative unit-width heights. No judge submission or editorial access is claimed. Earlier written/video annotations retain their author's research attribution.

## Complementary execution

`scripts/verify-linked-extension-independent.mjs` and `scripts/verify-linked-extension-independent.py` passed against the frozen sources. All four actual new Python programs reproduce their full displayed stdout. Independent first-repeat traversal checked 50,069 successor-map/head configurations through five nodes, including unreachable cycles, null heads and self-loops. Every detection frame was checked against actual one/two-step walks; meeting and reset counts, returned identity and nonmutation were checked.

Middle/split checks covered 52 model states and actual object chains through 10,000 nodes, verifying returned chains and the single permitted changed link. For 2,187 length-seven ternary histograms, full interval enumeration independently verified areas, both strict boundaries and witnesses. Signed versions checked first future strict/inclusive positions and stack prefix states. Changed browser fixtures used nine nodes with prefix seven/cycle two, even and odd cuts, signed duplicate readings, and a seven-bar histogram with optimum eight over two width-one bars.

The author's separate conservation checks preserve the original six complete programs and 61 original teaching subtrees. Ten original problem placements remain, with the documented LeetCode141 transfer annotation intentionally updated to point to the now-local proof. This review does not describe every old practice object as text-identical or recast the author's original conservation suite as an independently rerun suite.

## Rendered and visual checks

`scripts/review-linked-extension-independent.cjs` passed on the actual lesson route in Edge at 1440, 390 and 320 px with loaded fonts. Each width exercised 23 grouped changed states, four new code/output blocks, fourteen practice placements, eight keyboard-operated hint/solution disclosures and fourteen section arrivals. The rectangle's actual width and height were checked against its data, and zero bars had zero rendered height. There were no page errors or document overflow. Wide node diagrams and data tables retain keyboard-accessible local scrolling.

Twelve final images were opened: desktop cycle meeting and ordinary cycle reading; phone cycle entry, split boundary, signed event, inclusive result, alias exercise, tied-rectangle solution, and ordinary stack/boundary reading; desktop and phone histogram geometry. Their exact hashes are in the packet. Two initial narrow captures did not show the relevant locally scrolled nodes; the reviewer harness was adjusted to position the entry and cut boundary, the complete browser suite was rerun, and those final images were reopened. No production fix was needed. Captures not listed in the packet are not claimed as visually inspected.

These are inspected proofs and bounded native/browser checks, not a physical-device, screen-reader, observed-learner or performance study. Completion of the curated problems cannot guarantee solving every possible interview question.
