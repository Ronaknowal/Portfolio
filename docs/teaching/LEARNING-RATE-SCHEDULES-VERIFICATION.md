# Learning Rate Schedules — implementation and verification

10 September 2026. Mathematics position 10; stable ID `learning-rate-schedules-cosine-warmup-onecyclelr`. Complete rewrite and author review, with a separate [independent mathematical review](LEARNING-RATE-SCHEDULES-INDEPENDENT-REVIEW.md). Integrated build/loading status is recorded separately in the rollout ledger. User acceptance and an observed learner study are not claimed.

## Result and scope

The original short schedule description becomes a self-contained explanation of what a rate multiplies, which event advances a clock, how discrete endpoints work, and what a schedule changes in an actual optimizer. The useful original ten-update convention is preserved as an explicit comparison; its repeated peak and unconsumed minimum are explained rather than silently presented as a different endpoint guarantee.

The core develops scalar contraction, finite schedules, exact independent-noise moments, one-cycle momentum, warmup/restart limits, strict plateau state, accumulated/skipped updates, and full-state resumption. It ends with controlled configuration comparisons and seven independent practice groups. Nine complete programs include actual CPU PyTorch execution and an original NumPy calibration experiment. The [design/source ledger](LEARNING-RATE-SCHEDULES-DESIGN.md) records preserved coverage, chosen applications, version-specific sources, alternative lecture inspection and the resolved incoming optimizer note. No title, identity or module-order change was needed.

Three immediate figures show successive parameter displacements on a common axis, the before/after update clock and the owners of resumable state. Four investigations have distinct purposes: discrete phase/rate inspection; exact noise contraction/injection; microbatch/attempt/commit event lanes; and a validation threshold/cooldown state machine. They are not a generic panel repeated to meet a count. The rate and momentum have separate labeled axes; the noise plot identifies its calculated expectation and logarithmic display, and its below-10^-12 clipping does not change numeric readouts. Validation inputs are explicitly invented observations. There is no fabricated implementation benchmark or neural-training ranking.

## Executed evidence

- `node scripts/verify-learning-rate-schedule-models.mjs`: all nine complete Python programs execute in isolated fresh files and match displayed stdout exactly. Runtime: Python 3.12.14, NumPy 2.3.5 and CPU PyTorch 2.14.0+cpu. Final native/model pass is stored at `scratch/learning-rate-schedule-review/verification-results.json` (11:17:13 UTC).
- The same suite checks 138 schedule cases, 36 noise cases, 18 clock cases, 108 plateau cases and 19 invalid inputs. Its independent Python oracle uses 72 actual PyTorch scheduler configurations, 18,396 exhaustively enumerated noise outcomes, 104 actual autograd/optimizer clock updates and 1,242 actual plateau states. It checks used rates and intermediate states, not only final outputs.
- `scratch/lesson-tools/Scripts/python.exe scripts/verify-learning-rate-schedule-practice.py`: all seven changed-input exercises checked. Fraction arithmetic gives mean −6/25, variance 113/5000 and second moment 401/5000; exact decay products differ despite equal rate sums. A new three-update checkpoint reproduces nine remaining transitions exactly. A precisely defined changed horizon matches the first resumed update and changes the second used rate from about .057201 to .066142. Restoring the old scheduler state restores its old horizon. Two-peak validation selection followed by untouched test data produces three different policy winners on declared streams; 2,160 selection and 1,080 assessment sample updates are reported, not wall-clock claims.
- The independent reviewer read the full lesson, nine programs, models, all representations and seven exercises. Complementary checks cover 126 actual OneCycle rate/momentum states, exact Fraction closed-product moments over 36 policies/135 states with exhaustive sign paths, 192 actual plateau observations and four hand calculations. No substantive blocker was found. Its source snapshot and results remain separately dated in `docs/teaching/evidence/learning-rate-schedule-independent-review.json`.

Initial test failures were repaired before these passes: redirected Windows CRLF needed normalization for exact stdout comparison; the browser harness had assumed dataset order rather than each example's actual heading; its ranges now use real keyboard Home/ArrowRight actions. The changed-checkpoint exercise initially reused a mutable loaded optimizer-state dictionary across trial branches; the oracle now deep-copies each branch's state. This was a harness independence issue, not a modification of the already serialized lesson checkpoint example. Native/library warnings are asserted absent.

## Actual browser and reading review

`node scripts/review-learning-rate-schedules.cjs` passes at 1440, 390 and 320 pixels on real Edge/Playwright against Vite5173. At each width it verifies all nine genuine keyboard heading arrivals below the fixed navigation, all nine rendered complete code/stdout pairs, seven schedule policy choices, keyboard rate inspection/reset, zero-noise and one-cycle alternatives, 108 microbatch trace states, 12 default plateau observations, invalid-input retention, tie/zero-patience behavior, all seven display equations, titled/described finite SVGs and fourteen HTTPS reference/alternate links. No page error or document overflow. HMR sockets were closed in these isolated review pages to avoid another author's changes resetting traces.

The initially overwide cosine equation at 320px was rewritten as two readable, equivalent equations using an explicitly defined retained-gap fraction. The final test passes without shrinking text to hide the problem. Full lab captures suppress only the fixed navigation overlay during isolated element photography; ordinary reading screenshots retain the real header and its actual layout.

`node scripts/review-learning-rate-schedule-reading.cjs` subsequently verifies all seven practice groups with 21 keyboard reveals per width, the final changed-horizon and configuration results, actual Windows activation spelling, fourteen source links and focusable local table scrolling. The consumed-rate table fits desktop/390px and scrolls locally at 320px; the document does not overflow. Final compact browser and reading evidence is copied into the durable author record.

Actual PNGs opened and inspected, not merely generated: scalar/update/resume inline figures at 320/390px; cosine equation at 320px; one-cycle graphs at 1440/320px; default noise at 390/320px; correct accumulated clock at 390px; plateau ties at 320px; all nine ordinary reading sections distributed across 1440/390/320px; practice 6/7 at 390px; consumed-rate table at 320px; intro at 320px and sources at 390px. Local captures are in `scratch/learning-rate-schedule-browser/`. Definitions and immediate figures precede interaction-dependent reasoning; core mechanisms remain available without operating a lab.

## Ownership and limitations

Six owned source files: stable-ID body and individual brief, `learning-rate-schedule-models.js`, `learning-rate-schedule-examples.js`, `LearningRateScheduleLabs.jsx` and `learning-rate-schedule-labs.css`. Pure bounded calculations use local React state/memoization; no browser PyTorch, added production dependency, timer, worker, eager topic bundle or unbounded dataset. Final hashes and compact evidence: [learning-rate-schedule-author-review.json](evidence/learning-rate-schedule-author-review.json).

The native examples verify small CPU contracts, not GPU throughput, distributed loader state or cross-device/version reproducibility. The lecture's official written outline and link were inspected; video playback was not reviewed. SGDR's specific actual-movement EEG section was read by both authors, and its dated experiment is used only to motivate an anytime/snapshot question. Full rollout conservation, production loading/recovery and user acceptance remain separate from this author evidence.
