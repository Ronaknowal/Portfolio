# Category Theory — independent review

Topic: `category-theory-emerging-use-in-ml`, Mathematics position 41. Reviewer: `scientific_visual_improvements`, separate from the author/root. This review preserves the author's production files and distinguishes independent calculations and screenshot inspection from author-run browser evidence.

Status: independent review closed after the author's narrow Bayes inequality repair. No unresolved material finding remains. The [machine-readable record](evidence/category-theory-independent-review.json) stores exact final source hashes, complementary execution and the actual inspected images.

## Exact scope read

The full installed twelve-section body, all eleven complete displayed programs and interpretations, every practice prompt/hint/solution, the complete numerical models, the seven labs and three inline figures, the individual blueprint and the design/verification records were read. Styles affecting controls, mobile geometry, focus and diagram direction were inspected. The initial body was the author-frozen `bbb866c7ac5fcbb1bd2b87c191d90e37f63d0b033665f93de51941fa6658e120`; all six final hashes belong in the accompanying JSON. No production file was edited by this reviewer.

The original composition program remains present with its exact `7 7` and `5 5` outputs. The closing route points to Differential Geometry, the actual following module topic. The finite schema explicitly assumes total functions and sets rather than SQL bag/NULL semantics. The differentiation and homology branches provide their own local bridges and identify earlier owners.

## Mathematical review

- **Composition, categories and functors:** associativity preserves order; extensional function equality is separated from Python object identity. Free paths, imposed equations, one-sided inverses and category-relative cancellation are distinguished. The list construction correctly lives in Set rather than FinSet when lengths are unbounded. Arrow preservation, constant functors, inverse preservation and full/faithful claims have the needed types.
- **Naturality:** the reversal argument quantifies over every position, length and element map, including empty lists. Sorting is rejected by an explicit failing square, with the allowed-arrow restriction stated. One passing input is never presented as proof. The database migration uses all relevant component squares.
- **Universal constructions:** the product proof constructs the map for an arbitrary cone and proves it unique. Uniqueness up to compatible isomorphism uses the two projection equations. Tagged coproducts and compatible-pair pullbacks retain their premises; a mismatched job is not incorrectly treated as a universal-property counterexample. The singleton visual is correctly extended pointwise to arbitrary Set probes.
- **Homology:** a chain map sends cycles to cycles and boundaries to boundaries, so the quotient map is well-defined. Identity/composition are checked on classes. An injective space inclusion can kill a nonzero class after a face is filled. The actual native relabelling helper is used only for its declared injective vertex map, not claimed to implement arbitrary degenerate simplicial maps.
- **Parallel and effectful arrows:** interchange has compatible factor types; tensor/direct-sum dimensions and the nonlinearity of copying into a tensor square are distinguished. Coherence is not identified with literal equality of nested tuples. Option's explicit successful wrapper keeps `Some(None)` separate from failure; its composition, unit and flattening laws have exhaustive branches and correct indices.
- **Stochastic arrows:** finite row-channel composition preserves normalization and its categorical/matrix order is explicit. The two joint distributions share marginals but disagree away from deterministic endpoints; they supply a failure of uniqueness for the usual tensor projections. Copying is not natural for arbitrary stochastic maps. Bayes reversal depends on the prior and requires positive evidence; it is not an inverse.
- **Differentiation:** the tangent functor retains the intermediate base point, and smoothness is sufficient for its target arrow class. Reverse cotangent composition has the correct transpose order and evaluation points. Shared-parameter paths accumulate gradients before simultaneous updates. The Backprop as Functor discussion states its additional request/update construction and actual loss hypothesis; the elementary tangent example is not passed off as the complete research theorem.
- **Adjunction and Yoneda:** direct image is left adjoint to inverse image; both containment directions and unit/counit inequalities have arbitrary-element proofs. Hom-set naturality is not reduced to unrelated cardinalities. The contravariant Yoneda formula uses precomposition, the recovered identity component, and the correct order for functoriality. Both inverse constructions are proved. Compatible full probes are explicitly distinguished from arbitrary learned features.
- **Capstone and practice:** commuting migration squares induce a well-defined pullback map but do not force surjectivity. The site merge creates new compatible pairs. Bijective component maps with injective site relabelling recover equality and give a bijection. All twelve changed practice solutions and two checkpoints were read and their arithmetic/proof directions checked.

No mathematical or algorithmic blocker was found in that bounded scope.

## Finding and author repair

The Bayes paragraph contained `q(y)&gt ; 0`, with a space breaking the entity. This was a learner-visible malformed inequality, rather than a change to the probability claim. The reviewer reported it before any edit. The author repaired the entity, verified actual-font rendering at 1440/390/320 and refroze at `2026-09-11T00:09:57.999Z`. The reviewer read those final text results and actually opened the corrected 320px capture. Final body SHA is `b4eec093fa40a2daa11a2c88299f5e9e21148dd980d4d10d5e8fb9fda44f2382`. Reversing only the entity edit and that line's CRLF/LF change reconstructs the original exact body SHA; the other five production sources remain byte-identical. No algorithmic or proof correction was needed.

## Complementary execution

Run from the repository root:

```powershell
node scripts/verify-category-theory-independent.mjs
scratch/lesson-tools/Scripts/python.exe scripts/verify-category-theory-independent.py
```

The first script imports actual production models and examples without rewriting them. The Python reviewer executes each actual program and compares exact captured stdout, then uses different mathematical constructions to check changed inputs. At `2026-09-11T00:08:30.115522+00:00` all checks passed:

| Independent check | Actual scope |
| --- | --- |
| Full displayed programs | 11 exact stdout matches |
| Rectangular stochastic composition | 60 three-input/two-intermediate/three-output channels, compared with rational path-event masses |
| Unequal-size pullbacks | 256 three-by-five finite relations |
| Off-grid tangent/cotangent values | 18 compositions, exact rational polynomial derivatives and pairing |
| Universal mediators | 48 two-point source problems, enumerating every possible mediator rather than checking one selected point |
| Yoneda with other target functors | All 1,024 powerset-preimage candidate families and all 128 constant-two candidate families; exactly four and two compatible families respectively, each reconstructed from its identity value |
| Changed homology quotients | All 16 tetrahedron face subsets, checked against independent binary boundary-matrix rank |
| Actual shared-parameter program | Five changed parameter/data assignments, exact independently differentiated loss gradients |
| Actual migration helper | 1,944 changed component-square and compatible-pair problems |

The maximum absolute floating discrepancy was `2.842170943040401e-14`. Universal conclusions rely on the mathematical proof review, not on the finite count. `scratch/category-theory-independent-review/results.json` preserves execution detail. These are complementary reviewer checks; the author's larger native/model and browser suite is separately attributed.

## Primary-source cross-checks actually read

- [Riehl, Category Theory in Context](https://emilyriehl.github.io/files/context.pdf): the browser fetch returned a cache miss, so the already-preserved author-hosted PDF text was read locally. The actual inspected passages were theorem 2.2.4 and its inverse construction/dual-form note, definitions 3.1.9 and 3.1.15, and definition 4.1.1 with both naturality directions. They support the stated Yoneda variance, universal cone and adjunction requirements. This is a selected-passage review, not a full-book claim.
- [Fritz, Markov kernels and statistics](https://arxiv.org/pdf/1908.07021): example 2.5 and the opening of section 10 were independently located and read, including the distinction between arbitrary stochastic arrows and copying-preserving deterministic arrows.
- [Fong, Spivak and Tuyeras, Backprop as Functor](https://arxiv.org/pdf/1711.10455): theorem III.2 and the adjacent request/update formulas were independently read. Its invertibility condition applies to the first-argument derivative as a function of the target, with prediction fixed, matching the lesson's qualification. Complete paper/video playback is not claimed.

## Visual evidence and limits

Eight final author-generated screenshots were actually opened by this reviewer: the narrow product grid, tangent/cotangent lanes, homology comparison, stochastic joint tables, desktop sorting counterexample, narrow Yoneda reading, narrow independent practice and final corrected Bayes paragraph. Their exact paths and hashes are retained in the reviewer JSON. The images show readable node/projection labels, visible direction changes on the mobile derivative layout, coherent probability grids, and a usable equation layout.

The author performed the full 272-state checks at each of 1440, 390 and 320 pixels, actual-font loading, keyboard interactions and all-page equation checks. This reviewer read that evidence and inspected the named images; those complete browser runs are not relabelled as independently executed. This review does not claim novice user testing, unrestricted floating-point arithmetic guarantees, full external-book review, or production integration.
