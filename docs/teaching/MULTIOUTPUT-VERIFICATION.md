# Multi-Label & Multi-Output Learning — author verification

The complete rewrite retains the stable topic identity, original controlled classification dataset and useful advanced scope. Its seven production sources are frozen in [the author packet](evidence/multioutput-author-review.json), with separate native execution, model-oracle and actual-font browser evidence. This is author verification; independent review and the parent’s production integration remain separate.

## What changed and why

The full old body and seven exact code blocks were read and preserved in [the original archive](evidence/multioutput-original.json), independently of the parent’s ten-topic baseline. [Six original blocks actually executed](evidence/multioutput-original-execution.json), restoring their earlier notebook setup where necessary. The last public 20 Newsgroups download was interrupted by the cleanup request and never produced a final result; its code is preserved without claiming its comments were verified. The current text experiment instead contains fully inspectable, meaningful hand-authored multilabel annotations and no download dependency.

The lesson now has fourteen connected sections, fifteen complete executed programs, six distinct investigations, three inline figures, a logical-feature truth table and twelve changed practice tasks with optional hints before separately hidden explanations. Counts describe this particular implementation; they are not quotas or evidence of quality by themselves. The approved scope/retention rationale and actual inspected resource passages remain in [MULTIOUTPUT-LESSON-DESIGN.md](MULTIOUTPUT-LESSON-DESIGN.md).

The rewrite corrects the original worked micro-F1 answer (TP5/FP1/FN2 gives10/13); confusing label structure with independence; treating greedy chains as joint-MAP inference; current chain-path scores as marginal probabilities; an unconditional precision-versus-threshold claim; weighted scores as original posteriors; MultiLabelBinarizer as a powerset class encoder; and sparse sampled labels as sufficient for the cited conditional-mean recovery assumption. Unsupported fixed performance/speed thresholds and invented fitted graphics are removed.

Continuous-output learning now has substantive local teaching: conditional means and finite-second-moment reasoning, separable least squares and rank assumptions, original-unit errors, training-only target scaling, categorical output vocabularies, common versus separate tree splits, regressor-chain information limits, grouped feature penalties and an actual held-out numerical report. This fulfills the existing title instead of treating regression as a short afterthought. Shared parameter structure, a factorized likelihood and a joint decision are explicitly distinguished.

## Representation contracts

| Learning hurdle | Representation and placement | Calculation or limitation |
| --- | --- | --- |
| Several targets for one observation | Section1 target-schema branching figure; section2 complete-row/source-group split flow | Illustrative schemas and a declared workflow, not measured data or probabilistic independence claims |
| Per-cell versus whole-row errors | Section3 editable4×3 error matrix with row/column readouts and one unknown annotation | Counts derive from the displayed matrix; observed-cell metrics and complete-row subset accuracy use different explicit denominators |
| Marginal, greedy and whole-set actions | Section5 four-mass table and actual two-level conditional tree | Exact finite joint masses and expected risks, rounded readouts; zero-support histories are undefined; all joint modes and binary tie rules are explicit |
| Pooled versus conditional association | Section7 low/pooled/high probability mosaics | Rectangle areas equal calculated joint mass; within-X independence remains explicit, and the prose names its initial setting |
| Threshold ties and changing objectives | Section8 ranked score rail and exact F1 staircase | Constant intervals are (left,right] for score≥threshold; none/all candidates included; no fabricated smooth performance curve |
| Whole-vector classes and subset votes | Section9 powerset vocabulary/overlapping vote figure | Missing class cannot be decoded; vote denominators count only participating subsets; diagram is a declared finite construction |
| Sharing and target units | Section11 aligned two-output point/leaf-mean plots, residuals and candidate-loss table | Four exact observations; energy scaling changes the common objective, not physical-unit data; no generalization inference from training fit |
| Shared versus separate feature penalties | Optional section11 coefficient plane with radial and coordinate shrinkage | Exact orthonormal-design minimizers for different convex objectives; arbitrary correlated-design shortcuts are explicitly rejected |

All SVG data, branch probabilities, error counts and predictions come from topic-owned semantic models. No timing curve or external benchmark is fabricated. Figures are code-native and the lesson imports only its own models/examples/labs plus shared teaching primitives.

## Native and complementary checks

`scripts/generate-multioutput-examples.py` formats Python with normalized-AST conservation, runs every complete program and writes its actual stdout. The fifteen results are source-hashed in the packet’s execution record. They include the preserved original NumPy independent/chain comparison, exact rational joint decisions, actual scikit-learn chain-path versus full-leaf marginal values, a macro/micro threshold counterexample, powerset encoding/voting, categorical outputs, held-out regression, native shared stumps, grouped-Lasso coefficients, rare-label projection failure, and both original/changed grouped-text reports.

`scripts/verify-multioutput-models.mjs` exports deterministic supported cases and checks seven malformed/boundary inputs, including sparse/inherited array entries. `scripts/verify-multioutput-native.py` independently checks83 masked/full metric tables with Fraction counts and sklearn,80 finite joint/order cases with exact risks and leaf products,6 changed mixtures,9 tied/none threshold states,12 changed stumps against exact errors and actual native trees,24 convex subgradient certificates for feature shrinkage, and six changed hand-calculation checks. Its final model/examples hashes still match production. The complete changed-cost program was actually executed separately; no unexecuted expected output is presented as measured.

The controlled examples use NumPy2.3.5 and scikit-learn1.9.1 in `scratch/lesson-tools`; package versions and Python are recorded. The cost-changed text report selects independent heads at .5, whereas validation micro-F1 selects a chain at .6. Both outcomes and the tiny hand-authored cohort’s limits are shown. The exact conditional table, source-group integrity, score meaning, test-selection boundary and target-unit denominators are part of the teaching contract, not optional caveats.

## Actual browser and reading checks

`scripts/review-multioutput-lesson.cjs` completed fourteen changed mechanism states,73 keyboard actions, fourteen actual section-anchor arrivals, all fifteen exact question/code/output pairings, and hint/solution/optional-branch disclosure behavior at each of1440,390 and320 pixels. The behavior record preserves its final remaining equation-width failure instead of claiming that partial run passed every check.

The author repaired long equations by splitting lines and defining local intermediates, preserving mathematical meaning. The last correction changed a two-row aligned least-squares display to gathered rows; it removed an artificial cross-row width at320px. CSS was conventionally formatted with a PostCSS AST-conservation check. A short prose clarification names the mixture’s starting setting, and an optional Matrix Decompositions review link was added. Pure models, program bytes and interactions were unchanged by those final reading adjustments.

`scripts/review-multioutput-reading.cjs` then passed final actual-font reading/geometry at all three widths, with every disclosure open and all twelve display equations fitting. Space Grotesk and JetBrains Mono loaded through the real public font path. There were no captured page/console errors, formula-rendering errors or explanatory/control overflow. Full code/output retain intentional horizontal scrolling. Its final source hashes bind the frozen version; unchanged behavior results are reused rather than rerunning unrelated numerical or interaction checks.

Selected final screenshots were actually opened: target schema, grouped split, masked error matrix, reversed probability tree, conditional mosaics, exact threshold staircase, both measurement plots, grouped coefficient geometry, labelset vocabulary, ordinary association/conditional-mean reading, narrow least-squares/group-penalty formulas, changed joint practice, first complete program and changed report output. Exact paths and hashes are in the packet. A transient screenshot paint omission in the desktop shared-split prompt was resolved by a focused capture after two frames plus150ms; the final image was reopened. Unopened/redundant captures are retired with an explicit list, not counted as visually reviewed.

## Reproduction, sources and limits

From the repository root, run the generator with `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/generate-multioutput-examples.py`, then `node scripts/verify-multioutput-models.mjs` and the Python native verifier. The browser scripts use the existing5173 route and installed Edge/Playwright; the focused reading script explicitly binds the prior behavior snapshot and CSS-conservation record. The finalizer refuses to overwrite an existing source freeze. On a future substantive amendment, preserve the prior packet and rerun affected checks.

Primary-source inspection and current API details are recorded in the design, including the probabilistic-chain paper’s sections2–4.3, installed classifier-chain fit/prediction source, metric/threshold documentation, shared-row penalty objective, PLST projection conventions and compressed prediction’s conditional-mean sparsity. The reference list includes annotated written alternatives and an official optional neural-video course listing; the video itself was not watched. Detailed imbalance/precision discoveries were persisted in [the existing Imbalanced Learning owner’s note](topic-notes/imbalanced-learning-smote-cost-sensitive-learning.md). Survival Analysis remains the actual next module topic.

This packet establishes a self-contained author-checked lesson on explicit finite fixtures. It does not guarantee population calibration, every structured-prediction method, deployment success, causal effects, final independent review or user acceptance. The original interrupted download remains explicitly unverified; it does not block the complete current lesson.

The [bounded retention record](evidence/multioutput-working-retention.json) confirms removal of ten unused captures and the now-unneeded interrupted legacy news cache (about35.9MB); all18 selected images, original source/code, compact evidence and shared runtimes remain. A future explicit replay of the archived network example may download its public dataset again.
