# Recommender Systems — bounded independent review

Closed 11 September 2026 against the seven final sources in [the independent packet](evidence/recommender-systems-independent-review.json). The original **12:04:01.109953Z** author packet and bytes remain separate from the **14:41:11.182830Z** narrow amendment. The sole material finding is repaired and independently verified. No production source, shared registry or progress ledger was edited by this reviewer. Parent integration and user approval remain separate.

Read the complete fourteen-section teaching body, local proofs, all fourteen actual program sources, eleven final practice solutions and early exercise, all pure models, eight labs, three inline figures, CSS, design and preservation evidence. The explicit mask, confidence-weighted all-pair objective, per-record versus per-entity regularization, simultaneous old-state updates, block uniqueness, ranking denominators, cold-start information boundaries and randomized-policy assumptions are consistent. The complete report correctly shows the baseline beating MF, and separates finite test outcomes from model selection and population claims.

## Resolved finding

Section 3 and the JS model promise a user-mean → global-mean → declared-prior fallback. The displayed Python neighborhood helper initially only assigned the target user's `nanmean`. With ratings `[[missing,missing,missing],[0,2,4],[1,missing,5]]`, `predict(0,1)` returned `NaN` with a warning, rather than global mean **2.4**. An entirely missing matrix likewise returned `NaN` instead of prior **3**. These actual pre-fix results are preserved in `scratch/recommender-independent/initial-results.json` and embedded in the packet.

The author repaired only that helper and its generator using observed counts and masked division, with global mean or prior as the empty-row default. The final example SHA256 is **139e958776f519010fa77e7a4c9226e53f3dfaa9951f53894422e26c53b93556**. Independent actual-source reruns now return 2.4 and 3 with no warnings. The author's focused checks additionally retain an observed zero, conserve all fourteen displayed stdout strings and all thirteen other example records, and verify exact rendered code/output with actual fonts at all three widths. The final 390px code capture was independently opened. The [preserved pre-amendment packet](archive/recommender-fallback-before/recommender-systems-author-review.json) is not overwritten by this conclusion.

## Complementary evidence

`verify-recommender-independent.mjs` and `.py` check six changed BPR update/rotation equivariance cases, two JS cold-user/empty-catalogue fallbacks, three short/empty slate boundaries, two actual graded/empty Python metric cases, two actual ALS empty-row/column cases and the repaired Python fallback cases. A separately chosen cutoff **19** then checks information isolation: changing only test ratings leaves all seven actual training calls, their exact fitted factor arrays and the selected configuration unchanged. Validation selects rank 2, penalty .05, RMSE approximately 1.302377 in that changed run. Test scores are allowed to change. This checks the intended information boundary without rerunning the author's full numerical or package-comparison suite.

The independent actual-font Edge review at 1440, 390 and 320 pixels operates missing and observed implicit terms, a changed confidence normal system with keyboard removal of a missing term, a graded shortened slate with keyboard reordering, and failing/supported policy endpoints. It checks the resulting values, actual contour bounds, program/lab counts, anchors, equation widths and absence of document overflow or page errors. All pass. Two review-only selector errors were corrected transparently: split React text nodes and a relative Playwright filter. Neither required a production edit.

Ten final images were actually opened: three independent changed-state captures, six selected author reading/practice/mechanism captures and the final author code amendment. Exact identities and attribution are recorded. The changed slate exposes missing-candidate recall and fill rate; the quadratic contours retain their mathematical meaning at narrow width; the timeline and changed report explain what information is available. Code retains intentional horizontal scrolling for complete source. The author's earlier broad browser and stdout evidence is reused with its original timestamps, not relabeled as an independent rerun.

The [incoming Ensemble context note](topic-notes/recommender-systems-collaborative-filtering-matrix-factorization.md) is resolved by existing sections 2, 11 and 13–14: training-only aggregate features, temporal and warm/cold cohorts, prefix context and a complete new-item report. Adding a second stacking chapter would not improve that ownership; combination and OOF mathematics remain in Ensemble.

The public [implicit CPU ALS documentation](https://benfred.github.io/implicit/api/models/cpu/als.html) was checked for user×item, confidence and recalculation contracts; its banner is 0.7.2, while the preserved author execution tested 0.7.3. The [primary BPR paper](https://arxiv.org/pdf/1205.2618) supports the stated preference model; the lesson's derivation and changed invariant were assessed separately. No unperformed video watch, production-scale evaluation, causal claim from arbitrary logs or learner-efficacy study is implied. Only useful final evidence, three opened screenshots and the necessary initial finding remain in this review's scratch directory; no working-copy cleanup is outstanding.
