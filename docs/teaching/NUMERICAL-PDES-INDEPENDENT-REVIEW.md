# Numerical PDEs — bounded independent review

The complete mathematics57 lesson has passed this separate source, mathematical, native and browser review by `/root/scientific_visual_improvements`. One accepted-input endpoint defect was found and repaired by the author. No unresolved material finding remains in the reviewed scope. The [exact-source packet](evidence/numerical-pdes-independent-review.json) owns the final closure time, amended author-packet reference, all six production hashes and versioned evidence. This record does not promote production integration or user acceptance.

## Scope and mathematical read

The entire 15-section body, all 16 actual program/question/output strings, 15 changed practice questions/hints/solutions, pure numerical models, labs, CSS, individual brief and complete design were read. The exact topic inventory and all three destination-note resolutions were assessed. There was no earlier published body or runnable program at the planned baseline. The complete author verification and earlier design-only assessment were read as separately attributed evidence, not as substitutes for this source review.

The progression develops the steady rod, the error of a reconstructed field, time evolution, shared fluxes, finite elements and sparse geometry. The proof read checked the negative-Laplacian and defect signs; nonzero boundary elimination; the discrete maximum principle and barrier; scaling of both residual and inverse; interpolation hypotheses; and the distinction between exact certificates and plotted samples. The finite-grid spectral threshold is correctly separated from positivity. The diffusion convergence argument states its smoothness and compatibility assumptions, and its implicit energy statement does not claim unconditional accuracy or positivity for Crank–Nicolson.

The conservation read checked outward Neumann/Robin signs, compatibility before a gauge, series resistance, upwind direction and the limited scalar Godunov extension. The element read checked actual load integrals, a point-source functional, nonuniform assembly, conforming Galerkin orthogonality, the special constant-coefficient one-dimensional nodal identity and the exact local polynomial error integrals. Unequal 2D spacings, flattened boundaries, inverse-transpose triangle gradients and the norm-specific two-grid correction are consistent. The local derivations do not pretend to establish every adaptive, nonlinear or multigrid theorem.

## Finding and repair

The original exported `poissonProblem` accepted `intervals=3, length=.7` but generated its last mesh coordinate using `3*.7/3`, yielding `.6999999999999998`. Its final plotted position was `.7`, so strict interpolation-domain validation threw. The same endpoint-construction concern applied to the actual displayed Python Poisson helper's returned coordinates. This was an exported-helper corner, rather than an observed failure in the fixed unit-length UI presets.

The author pins the known endpoints to exactly `0` and `length` in mesh and plot generation, and in that native helper. Interior arithmetic and strict out-of-domain rejection remain intact. No general tolerance was inserted to conceal an inconsistent domain. All 16 expected stdout strings remain unchanged, and only the native Poisson program changes among the example records. The final model differs by the two endpoint-construction expressions; the body, labs, CSS and brief retain their original bytes.

The review's [original six-source archive](archive/numerical-pdes-independent-start/) and exact original author packet were saved before mutation. The [initial failure](../../scratch/numerical-pdes-independent-review/initial-findings.json) was reproduced from that archived module after the live repair, retaining the actual failing result. The final independent packet binds both versions rather than attributing the repaired sources to the original author run. The author's own amended packet preserves its separate archive and correction checks.

## Complementary execution

`node scripts/verify-numerical-pdes-independent.mjs` passed at **10:22:21.351155 UTC on 11 September 2026** against the final source pair. It runs the Python companion with the actual exported program strings. All 16 complete stdout blocks pass. The checks use different mathematical constructions from the production algorithms:

- Thirty-six odd-grid, nonbinary-domain problems compare the direct solution with an exact discrete Green kernel and prove its rational residual-to-error identity. Every mesh/curve endpoint equals its declared domain value. Whole-field extrema are then found cell by cell with 80-digit arithmetic and monotonic derivative-root bisection, instead of sampling the plotted curve or merely repeating its error-bound expression.
- Twenty changed actual Python Poisson calls preserve their exact declared endpoint and reproduce a changed cubic field. Four changed native finite-budget reports retain valid whole-field bounds and honest failure/success status.
- Twenty-four diffusion problems vary spatial size, frequency and diffusivity. A full discrete sine expansion includes the actual rounded initial vector's leakage into other modes; 216 stage/method comparisons and 72 changed native calls agree.
- Thirty-six transported profiles compare against a closed binomial distribution of stay/shift choices. Eighteen exact rational periodic pulse-overlap references and eighteen changed native calls independently check the direction and same-time cell averages.
- Forty-eight nonuniform source/conductivity cases compare with a Green-field nodal solution and exactly integrate expanded field-error and gradient-error polynomials, including source alignment and very short legal cells. The corresponding actual native finite-element calls agree.
- Twelve differently oriented triangles reproduce arbitrary ambient affine gradients and their integrated energy; this identifies the stiffness quadratic form without copying its inverse-Jacobian calculation. Seven two-grid modes compare the correction with ordinary QR projection in gradient space, checking the actual energy norm and orthogonality.
- Nine mean-zero Neumann cases are obtained by integrating face balances twice, with exact native agreement. Six material-interface cases minimize the scalar two-segment Dirichlet energy. Both changed field-budget thresholds are checked as exact fractions.

The largest scaled numeric discrepancy is **1.301e−14**. Exact fraction equalities and numerical tolerances are distinguished in [the native result](../../scratch/numerical-pdes-independent-review/native-results.json). The extrema checks support the mathematical proof; they are not themselves a certified all-real arithmetic implementation. No author code was edited by the reviewer.

## Actual browser and visual review

`node scripts/review-numerical-pdes-independent.cjs` passed at **10:20:01.314 UTC** with the same final six production hashes. Edge loaded the intended Space Grotesk and JetBrains Mono fonts at **1440, 390 and 320 px**. Each width operated **17 changed states and 39 keyboard/hint/reset actions** across all eleven investigations. It checked nonzero boundary contributions, an unmet and a met certificate, undefined order for an exact-on-grid family, actual unstable plot bounds, reversed material contrast, incompatible outward flux, leftward cell translation, centered overshoot, stationary-shock traces, source-between-node versus aligned hats, corner indices, stretched triangles and energy-versus-L2 correction.

All 16 actual program questions precede the matching complete code and expected stdout. All 15 practice tasks retain separate hints and solutions; keyboard opening a hint leaves its answer closed. Fifteen outline targets exist, every displayed equation fits, and no plot point or SVG label is clipped in the operated states. Page/console errors, failed requests and document overflow are empty. Intentional local code/table scrolling remains available. The independent browser checks do not replace the author's larger full-state review.

**Sixteen exact reviewer screenshots were actually opened**, out of sixty captured. They cover the ordinary restriction diagram, changed boundary row, separated certificate, desktop diffusion overshoot, phone material profile and periodic cells, nonuniform assembly and missed kink, narrow corner index map, stretched triangle, coarse norm table, stationary face, ordinary interpolation and energy proofs, and the changed practice/output. Exact paths, observations and hashes are in the packet; no claim is made that every captured image was viewed.

Selected primary-source checks revisited [FNC's CFL/upwind discussion](https://fncbook.com/upwind/), [its diffusion stability reference](https://fncbook.com/python/absstab-diffusion/) and [Schöberl's affine/error-analysis notes](https://jschoeberl.github.io/iFEM/FEM/erroranalysis.html). Their scope and assumptions are recorded in the packet. No full video playback, physical-device test, complete screen-reader audit or observed beginner study is claimed. Final build, route/loading conservation and the shared curriculum ledger are the parent's separate integration responsibility.
