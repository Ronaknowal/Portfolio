# Numerical Methods — independent review

This bounded independent review covers Mathematics Foundations position 38, stable ID `numerical-methods-finite-differences-quadrature-root-finding`. It checks the author's frozen implementation, rather than a copy or reconstructed counterpart. The [durable review packet](evidence/numerical-methods-independent-review.json) records all six exact production fingerprints, the review time, replay scripts and nine screenshots actually opened. No production files were edited, and no unresolved material issue was found.

## Source, teaching and mathematical review

Read the entire current lesson, all thirteen complete Python blocks and their visible questions/interpretations, all ten independent practice groups, the pure model/lab/CSS source, individual blueprint, design, author verification, original archive and incoming-note disposition. The original program and its stdout remain byte-for-byte preserved. Its useful root, stencil, quadrature, residual, linear-solver and gradient-check coverage is developed in the new flow. The stronger statements now name their hypotheses; the old automatic-differentiation precision overstatement is corrected and its destination note retains ownership of the broader engine question.

The review checked the bisection half-count convention; continuity versus sign evidence; simple versus multiple Newton roots; central-80% safeguard contraction; finite-difference moment and sample-error constants; trapezoid and Simpson interpolation weights; Richardson's leading-error assumption; terminal adaptive-budget accounting; the five-sample blind polynomial; callable versus sampled integration; Gaussian node mapping; and the pump's derivative lower bound and conversion from litres to minutes. In the nested search, an unresolved inner interval never licenses throwing away a half-bracket. The local/global Euler and modified oscillator statements retain their respective assumptions. The planned calculus prerequisite is declared and refreshed locally, without substituting a later published page for the actual next module topic.

For primary-source spot checks, reviewed [NIST DLMF §3.5](https://dlmf.nist.gov/3.5) for the quadrature constants/hypotheses, [Driscoll and Braun's adaptive integration explanation](https://fncbook.com/python/adaptive/) for the estimator/refinement distinction, and the [SciPy solve_ivp contract](https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.solve_ivp.html) for reported times and event/error-control qualifications. This review does not claim to have watched the linked full video.

## Complementary executed checks

Ran `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-numerical-methods-independent.py`, which imports the actual unchanged JavaScript models through `verify-numerical-methods-independent.mjs` and executes every actual exported Python block. The run passed at 22:40:08 UTC on 10 September 2026 with SciPy 1.18.1.

- All thirteen actual stdout records matched, and the original code/output matched the archive exactly.
- Eighteen shifted polynomial integrals were checked through exact Fraction moments of the integrated five-node interpolant (Boole's rule), including 219 terminal adaptive intervals. This tests the corrected Simpson value through a different mathematical representation.
- Three signed/scaled blind-polynomial cases retained zero observed error and nonzero true area; four exhausted evaluation budgets returned failure without presenting a partial sum as a completed result.
- Sixteen changed exponential-root brackets and sixteen safeguarded traces were checked across positive/negative and very different function scales. Root retention, midpoint radii, tangent intercepts and actual contraction were checked independently.
- Forty-eight stencil cases used analytic trigonometric identities that avoid repeating the same subtractive stencil. The largest absolute discrepancy, about 4.19×10⁻¹¹, arises in these finite-arithmetic differences and is within the explicit amplification allowance.
- Eighty candidate calibration states were checked against independent integration. The root reference used the negative Lambert-W branch, rather than either implementation's bisection.
- The actual complete nested-search helper passed 126 changed target/tolerance/budget cases, including non-power-of-two budgets. Twenty-eight finished within a stated tolerance; 98 deliberately stopped with unresolved inner signs. Every retained bracket included the independent crossing, and every returned bound covered the actual candidate error within the declared numerical comparison tolerance.
- Eighteen relocated two-node Gaussian-rule cases checked exactness through degree three and its failure beyond that degree.

There were 996 explicit numerical comparisons, plus interval, status, budget, preservation and sign assertions. These are complementary finite checks; they do not prove an adaptive estimator works for every function or turn ordinary floating-point bounds into rigorous interval arithmetic.

## Visual evidence and attribution

Actually opened nine final author captures at desktop 1440, phone 390 and narrow 320 pixels: the rate/accumulated-volume pair, Newton cycle geometry, scaled adaptive blind spot, nested-program reading, offset-induced derivative loss, its actual log-error plot, the narrow Gaussian formula, shaded quadrature and conditional calibration uncertainty. The representations distinguish observed algorithm samples from drawing points, the error plot from an idealized trend, and a discretization bound from a confidence interval. Long code/sample tables intentionally remain locally scrollable.

The author owns the recorded 264-state interaction matrix, keyboard checks and final thirteen-program amendment. This reviewer read that evidence and inspected the nine images; it is not misreported as a fresh independent full browser run. Shared production build/loading integration and user acceptance remain separate from this source/math/visual review.
