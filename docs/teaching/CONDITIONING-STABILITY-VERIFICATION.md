# Conditioning, Stability & Numerical Analysis — author verification

The complete Math52 lesson is authored and checked against its actual exported models, displayed Python programs and local browser route. Its exact frozen identity and timestamps are in [the author packet](evidence/conditioning-stability-author-review.json). This record is an author review, not the parent's independent review, production integration, user approval or a measured beginner study.

## Scope and preservation

The stable ID is `conditioning-stability-numerical-analysis`. The baseline was planned, with no legacy body, program or output to preserve; the exact returned original plan is archived in [conditioning-stability-original-plan.json](evidence/conditioning-stability-original-plan.json). The original sensitivity/algorithm distinction, forward/backward/residual outcomes, expression-repair experiment, tolerance/reference task and Goldberg resource are retained and substantially developed. The title and module order remain unchanged. Root owns registration and the approved prerequisite replacement: Numerical Methods and Vectors supply the actual required skills; essential rounding and norm concepts are taught locally.

The [assessed design](CONDITIONING-STABILITY-LESSON-DESIGN.md) records owner boundaries, derivations, primary-source review and alternatives. The final lesson connects a two-channel measurement problem through input representation, conditioning, residuals, backward-error permissions, scaling, factor reuse and an uncertainty-aware report. Rounding cells, two arithmetic paths, measurement-line intersections, changed zero entries, an addition tree/correction channel and signed error propagation address different mechanisms. Two inline figures distinguish reference/computed branches and consistency defects/solver residuals. Counts describe the result, not an authoring quota.

## Actual numerical and native verification

Run `node scripts/verify-conditioning-stability.mjs`. Its [JavaScript driver](../../scripts/verify-conditioning-stability.mjs) imports the actual production exports and forwards their states plus the actual displayed code to [the independent Python verifier](../../scripts/verify-conditioning-stability-native.py). [Final native results](../../scratch/conditioning-stability-native/results.json) pass **13,067 comparisons, all 11 exact-output complete programs, 19 JavaScript rejections and nine native rejection cases** on Python 3.12.14, NumPy 2.3.5 and SciPy 1.18.1.

- All 34 toy rounding inputs are checked by independent nearest-distance Fraction enumeration and even-tie choice. Exact binary64 conversion includes signed zero, the smallest subnormal, small normals, ordinary non-dyadic decimal literals and the largest finite value.
- All 183 signed/zero dyadic cancellation states use actual binary64 results and an integer-square-root enclosure. Independent 150-digit Decimal calculations verify enclosure containment and the relative/absolute bounds. Exact-zero and `x=-1` endpoint states retain their distinct contracts; a nonzero small value is not formatted as zero.
- Seventy measurement configurations, including coincident/inconsistent rows, are checked by exact Cramer determinants. Eighteen backward-witness scale states are checked against the actual rational helper. Changed dense rational systems independently verify that the exhibited matrix/RHS perturbations both meet the allowed norm bounds and solve the proposed nearby problem.
- Five complete addition presets and their intermediate losses are checked against exact stored-input Fractions and actual Python accumulation orders. Changed scales, small contributions and permutations compare the actual compensated result with exact sums. These finite checks do not assert exactness for every compensated sum.
- All 360 bounded propagation configurations are checked by a separately assembled exact disturbance convolution and absolute envelope. Four fixed-time unstable refinements agree with the exact native recurrence. Actual float32-factor/float64-residual refinement is checked at changed exponents 8–23; the collapsed exponent24 factor is rejected. The actual Gram/QR/lstsq program is executed, including its local dtype/rank and rounded-Gram observations.

The complete programs were generated by [generate-conditioning-stability-examples.py](../../scripts/generate-conditioning-stability-examples.py), which formats Python with Black only after asserting AST equality, executes each program, and captures its actual stdout. A production-helper discrepancy found during cross-runtime checking was repaired: `sum_methods` now converts all accepted numeric inputs to float before comparing accumulation methods, avoiding a balanced recursion accidentally using exact integer additions while other methods use floating arithmetic. Final native evidence checks the corrected actual code; all printed teaching fixtures remain accounted for.

The last practice is a genuinely changed report, rather than a request to recite section9. [verify-conditioning-changed-report.py](../../scripts/verify-conditioning-changed-report.py) executes the actual stored report with only the requested separation/radius edits. [Its full changed code/output and independent Cramer checks](../../scratch/conditioning-stability-native/changed-report.json) establish the new exact central pair `(2863311531/8589934592, 5726623061/8589934592)`, initial float32 pair `(0.34375, 0.65625)`, zero final central forward error for this fixture, a failed amount guarantee `1/64` and a passing guarantee `1/65536`. The answer explains that a worst-case radius is not an assertion of the unknown actual measurement error.

The earlier [547 design-fixture checks](../../scratch/conditioning-design/fixtures.json) remain design evidence. They are not substituted for these actual production checks.

## Actual browser and ordinary reading

Run `node scripts/review-conditioning-stability-lesson.cjs` and `node scripts/review-conditioning-final-reading.cjs` against the registered route on local5173. Edge headless uses the actual Space Grotesk and JetBrains Mono fonts, at **1440, 390 and 320 pixels**. The [full interaction result](../../scratch/conditioning-stability-browser/results.json) passes **82 operated states per width**, all six investigations, finite plotted coordinates, reset and keyboard actions, exact active-state readouts, the positive-order exact addition-tree root, and horizontal keyboard access for wide traces. It also verifies all 11 actual program/code/output/question blocks, ten complete final practice answers, both early checkpoint prompts/answers, 17 fitting KaTeX displays, valid section anchors, no page overflow and no page or React-console errors.

The [final reading result](../../scratch/conditioning-stability-browser/final-reading-results.json) checks setup before the first program, six deeper derivation contexts, actual report display, the changed report question/solution, annotated direct references, the direct MIT video link, keyboard access to the wide rounding grid and clean three-width layout. Its final run also verifies the last inline-figure clarification: the exact reference and the computed approximation are separate branches from the same stored input; the algorithm need not know the exact answer first. The full 82-state run precedes that isolated figure amendment; the final reading run closes it, with unchanged numerical models, programs and controls.

Actual opened screenshots are individually named and hashed in the author packet. They cover ordinary introduction/rounding/residual/refinement/convergence/tolerance prose, both equation and defect representations, both error plots, the highlighted backward witness, exact positive-sum labels, default and scrolled wide traces, the complete report and changed solution, and annotated sources. Images were opened and inspected, not inferred from automated geometry alone.

The visual review led to concrete corrections: equations were broken at mathematical boundaries; a long unspaced rational pair and inline call were made readable without hiding page overflow; scientific axis labels received sufficient left margin; exact stored integer values replaced rounded tree labels that obscured recovered units; the tree opens centered and has an explicit sideways-reading cue; the compact input-order option fits phones. Chart label bounds are now asserted. A black intermediate screenshot and an incorrectly guessed output-label locator were capture/test issues; final evidence uses painted captures and the actual shared component's `Expected result` label.

Dense arithmetic grids, trees, tables and the existing complete-code component intentionally scroll locally. Ordinary text and all17 display equations fit the page. The scoped module formatter recorded normalized AST/JSX/string equality; the native suite was rerun after model formatting. Later explicit teaching/visual amendments are recorded by the final six-source fingerprints, not misrepresented as formatting-only changes.

## Sources, future ownership and limits

The design's source ledger records the specific primary textbook/docs/article sections actually inspected, versions and caveats. The MIT Strang alternate video was verified through the official page/embedded identity and its official transcript, including the scalar stability discussion; no full-playback claim is made. Historical MATLAB conventions and reported mixed-precision performance are not adopted as current Python or hardware guarantees.

The [GPU arithmetic note](topic-notes/floating-point-representation-numerical-error.md) and [Numerical PDE normalization note](topic-notes/numerical-pdes-grids-finite-elements-stability.md) now cite the authored origin and remain open for their destination authors to assess. No specialist topic was removed, no PDE proof is outsourced to this one example, and no unrelated body or shared registry was edited by this author.

Theorems retain nonzero-reference, norm, invertibility, perturbation-size, arithmetic-range and uniform-in-refinement hypotheses beside their claims. Finite executable results are local observations; binary64/BLAS/library changes can alter low bits. The reference construction is deliberately bounded to the taught dyadic inputs, and the visual controls are bounded educational models, not general validated numerical software. No GPU benchmark, broad arbitrary-float safety claim, deployment, whole-rollout completion or production integration is asserted here. Root owns the separate independent review and integration decision.
