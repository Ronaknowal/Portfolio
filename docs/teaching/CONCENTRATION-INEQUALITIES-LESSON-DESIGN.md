# Concentration inequalities — scoped lesson design

10 September 2026. Mathematics19, stable ID `concentration-inequalities-hoeffding-bernstein-chernoff`. Design before replacing the published body; publication is not completed author review. Root owns shared registration/integration.

## Existing work, scope and title decision

Read the exact topic-plan output, current standard/domain/design/code guidance, the existing complete 7615-byte body and the incoming Randomized Algorithms note. Original source SHA256: `234855e034120a720d11c2be9055c0e70c2053288fabd1e79e29d76391b56bf4`; preserved initially in `scratch/concentration-preservation/original-topic.jsx` and a durable preservation record before replacement.

Retain the current title and identity. The original has three complete Python/output examples, no topic-native diagram or interactive investigation: Hoeffding100/.1→.271 and185 samples; known-variance Bernstein100/.05/.09→radii.136/.106; multiplicative Chernoff mean20/relative increase.5→.115. Retain those calculations/programs and the changed400-observation exercise, with execution evidence. Preserve independence, boundedness, sufficient-not-tight, unknown-variance and multiple-check cautions while developing their mechanisms and limitations.

Two existing recommendations need correction rather than blind preservation. Ordinary bootstrap/permutation machinery is not an automatic remedy for dependent, heavy-tailed or shifted data; each method has its own assumptions and changes the inferential target/procedure. Occupied-bucket indicators in a balls-and-bins experiment are not generally independent. Use the load of one fixed bucket under independent ball assignments for an elementary Chernoff application, then union-bound over buckets without requiring independence between their loads.

Core scope: finite-sample deviation events; moment and exponential-Markov reasoning; Hoeffding for independent bounded variables including unequal ranges; explicit sample/radius inversion; variance-sensitive bounded Bernstein and its exact quadratic inversion versus convenient relaxation; Chernoff as a method and its independent-Bernoulli multiplicative/KL forms; exact versus guaranteed tails; dependence/copying and a separately justified simple-random-sample-without-replacement extension; finite-family and time-uniform error-budget reasoning; practical changed cases. Teach local expectation/variance/log notation before relying on it. A short optional calculus derivation supplies the log-MGF curvature proof; it does not assume an exponential-family course.

The earlier DSA lesson supplies a record-marking estimator and sample budget, not the full derivation. Previous hypothesis and Bayesian lessons distinguish repeated-sampling guarantees from posterior probability. The next actual module topic is Monte Carlo Methods & MCMC, where dependent chains require additional reasoning; follow the selected route's Next context.

## Learning sequence and representation contracts

| Hurdle | Mechanism and worked question | Chosen representation/action | Verification and boundary |
| --- | --- | --- | --- |
| A tail bound is not an observed error | A count S is random before data; its mean and a chosen count threshold define a fixed event | Exact finite binomial probability bars with highlighted count-tail, explicit threshold and a comparison ledger | Independent exact Fraction sums/SciPy binomial survival; inclusive integer thresholds; endpoints and probability cap1. Numerical finite-law values are not empirical measurements. |
| Why exponentials help | An event indicator is bounded by exp(λ(S−k)); expectation turns domination into a number; independence factors the MGF | Inline indicator→exponential→expectation→product derivation plus a λ objective investigation with actual exponential contributions/curve | Finite enumeration and independent closed Bernoulli MGF; λ=0 is trivial, a poor λ can yield a bound above1; plotting does not change the original distribution. |
| Bounded observations give a universal exponent | Center, bound log-MGF curvature using the interval variance limit, sum independent bounds and optimize λ | Linked proof steps with a small nonidentical-range calculation; local optional full lemma derivation | Exact finite supported distributions and independent optimization; range width/variance proxy are not the actual variance. |
| A guarantee becomes a design budget | Solve for n or an error radius; halve absolute precision versus reduce failure probability | Sample/precision budget and radius curves under stated axes/units; no arbitrary benchmark bars | Decimal sample-count boundaries and exact inversion; round sample counts upward; unknown mean does not become random after observation. |
| Low variance helps only when justified | Derive centered bounded Bernstein, separate total variance v and increment cap c; invert a quadratic | Variance-sensitive radius comparison and explicit Gaussian-like/linear denominator contributions | Independent symbolic substitution/numerical roots; show cases where Hoeffding is tighter and where estimated variance0 is invalid. Known upper bound versus empirical variance is explicit. |
| Rare counts need relative meaning | Bernoulli log-MGF gives optimized KL; a mean-only relaxation gives multiplicative Chernoff | Exact tail comparison at count threshold; concrete1000 requests/.02 mean/30 count example plus lower-tail/rare-event practice | Independent binomial survival and high-precision log calculations; p0/1, thresholds outside support and count versus mean notation. |
| Same marginals need not give same sample information | Compare independent draws, copied one-draw observations and a uniform subset from20 labelled records | Side-by-side exact count distributions, same mean/sample count, chosen absolute event; full-population limit | Exact binomial/hypergeometric/completely shared finite probabilities. Without replacement uses a separate convex-order theorem, not a claim of independence; no automatic Markov-chain extension. |
| One valid check is not a valid selection/stopping procedure | Union-bound a fixed finite family; compare independent versus shared failures; allocate δ over countably many times | Family-failure curve/ledger plus an explicit telescoping time-budget derivation | Exact family probabilities under declared independent/identical events; union bound itself needs no event independence. Finite simulations do not prove unrestricted validity. |

Visual/lab count is determined by these hurdles. Preserve useful shared controls and direct Math.jsx imports; own semantic `concentration-inequalities` models/examples/labs/CSS. Do not import other topics' bodies or examples. Normal reading must explain the event and each plotted quantity before controls are touched; mobile plots need explicit axes, readable labels and exact text equivalents.

## Applications and independent practice plan

1. Fixed offline model evaluation: bounded loss, absolute percentage-point precision and sample budget. Distinguish chosen-before-data metric from selection over a finite fixed class. Retain the original185 result and solve changed tolerances/sample400.
2. Low-failure-rate service/marked-record sampling: exact binomial tail versus Hoeffding/Bernstein/Chernoff. Work the original mean20/threshold30 result and show why an absolute.1 guarantee can be weak for a.02 mean. Relative guarantees need a mean or lower-mean assumption to turn into a uniform sample count.
3. Hash load: independent assignments give Bernoulli indicators for one fixed bucket. Union-bound several bucket overloads while their counts are dependent. Do not transplant the independence proof to occupied-bucket indicators.
4. Finite archive sampling: a uniform labelled subset has hypergeometric counts and negative dependence. Show complete population mean at n=N, and distinguish this justified extension from arbitrary dependence or biased sampling.
5. Repeated monitoring/selection: derive δ_t=δ/[t(t+1)], sum it by telescoping, and obtain a conservative anytime Hoeffding radius under the fixed-time assumptions at each t. Explain that this is an original elementary union-budget construction, not an implementation of the sharper Howard et al. mixture/stitching boundaries. Route deeper empirical-Bernstein, martingale and sequential designs with explicit ownership reasoning when a destination is verified.

Native examples will retain all three original runnable programs and add complete finite-distribution, MGF/unequal-range, variance inversion, dependent-sampling, family and time-budget examples as the finished teaching requires. Independent practice includes hand calculation, event translation, theorem selection, a false variance plug-in, a dependence counterexample, finite-family budgeting and a changed pipeline with explained solutions. No DSA/LeetCode quota applies to this mathematics lesson.

## Claim and resource ledger actually inspected

All checked10 September2026. These are authoritative verification sources, not structures or wording to reproduce.

- [Waterloo CS466 Lecture3, Rafael Oliveira](https://cs.uwaterloo.ca/~r5olivei/courses/2025-spring-cs466/lecture-notes/lecture3/): full Markov/Chebyshev and Chernoff-Hoeffding sections, independent MGF factorization, multiplicative bound and unequal bounded ranges. Resolve notation locally: its δ sometimes denotes relative deviation, while our δ is failure probability. The source's remark that the lemma is used “instead of Markov” is not our proof: exponential Markov still supplies the first step, and the lemma controls the MGF.
- [Berkeley CS281B/Stat241B Lecture4, Peter Bartlett](https://www.stat.berkeley.edu/~bartlett/courses/2014spring-cs281bstat241b/lectures/04-notes.pdf): PDF pages4,8–20 and23–25 inspected for finite-family union, moment/MGF proofs, bounded log-MGF curvature, sub-Gaussian proxy and centered moment expansion. Distinguish actual variance from a sub-Gaussian parameter; provide the relevant calculus steps without unexplained exponential-family notation.
- [Berkeley STAT240 notes, Philip Stark](https://www.stat.berkeley.edu/~stark/Teach/S240/Notes/ch1.pdf): PDF pages13–14/§1.17.3–1.17.7 inspected, including convex-function comparison of sampling with/without replacement and bounded Bernstein. The displayed σ notation has a typographical inconsistency; define our own total variance v and cap c, derive and independently check the resulting bound rather than copy notation.
- [MIT18.200 Spring2024 Lecture9: Chernoff Bounds](https://ocw.mit.edu/courses/18-200-principles-of-discrete-applied-mathematics-spring-2024/resources/18200-lecture-9-version-2_mp4/): official undergraduate video page and its description inspected. It directly identifies the independence-based proof focus. No full video playback or transcript review claimed; useful optional alternate explanation, with written derivations supplying verification.
- [Howard, Ramdas, McAuliffe & Sekhon, Time-uniform, nonparametric, nonasymptotic confidence sequences](https://arxiv.org/html/1810.08240): introduction/definition/properties, Figure1 discussion and §6/Lemma3 statements inspected. Distinguishes fixed-time from simultaneous coverage and arbitrary stopping; sharper constructions require additional source review before implementation. No quoted empirical comparison or universal constant is imported into our lab.
- A Stanford lecture URL failed to open, and an initially opened Berkeley Lecture8 concerns uniform laws rather than the desired Bernstein proof. Neither is claimed as a verified source for the implemented concentration constants.

## Note disposition and finish line

Incoming `topic-notes/concentration-inequalities-hoeffding-bernstein-chernoff.md` is open until its mechanisms are actually implemented and verified: include the estimator/MGF budget bridge and exact/variance comparison; adapt without-replacement using a separate theorem; develop a conservative union-budget time guarantee; route deeper dependence/empirical-variance procedures if needed. The unrelated bit-manipulation inbox does not alter this scoped maths task.

Final evidence must include original-program conservation, real Python execution, independent finite/high-precision oracles, final Math rendering/axes at1440/390 and narrow320 where needed, keyboard/reset/edge/anchor/code/output checks, opened ordinary-reading screenshots, scoped source review, exact final hashes and remaining limitations. Root owns global publication/inventory/build and user acceptance is separate.

## Implemented disposition

The scoped design is implemented and author-verified in [CONCENTRATION-INEQUALITIES-VERIFICATION.md](CONCENTRATION-INEQUALITIES-VERIFICATION.md). Five investigations address finite tails, exponential witnesses, precision/variance budgets, sampling dependence and simultaneous families; two inline figures show the proof chain and actual variance-substitution counterexample. Nine complete programs retain the original three exactly. The short checkpoint and eight changed substantial tasks are separate from the labs; all substantial tasks have optional hints and explained solutions. Visible questions precede all programs.

The incoming note is [adapted with its actual section/evidence mapping](topic-notes/concentration-inequalities-hoeffding-bernstein-chernoff.md). That note explicitly retains deeper empirical-variance and sharper confidence-sequence work as a reasoned extension, not a implemented promise. A [PAC/VC destination note](topic-notes/pac-learning-vc-dimension.md) carries finite-family selection into its stronger generalization owner; no unrelated lesson was rewritten. MCMC's existing resolved note owns the dependent-chain continuation.

Final decisions from source/browser review: preserve probability complements near one, keep support-determined certain events exact, use `step=any` for the true exponential optimum, choose a visible initial finite-tail event, wrap three long equations by their mathematical structure, and enlarge narrow-screen plot labels without clipping endpoint ticks. Exact native/model/browser and opened-image evidence is linked from the verification record. Root's independent full source/proof review found no mathematical blocker; its hint/question findings were resolved before final QA.
