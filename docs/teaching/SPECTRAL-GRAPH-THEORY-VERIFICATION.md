# Spectral Graph Theory — author verification

10 September 2026 UTC. Stable identity `spectral-graph-theory`. This records author checks; parent production integration and user acceptance are separate. Exact frozen production fingerprints, evidence payloads and time are in `evidence/spectral-graph-author-review.json`.

## Delivered learning experience

Retained the approved six-document weak-bridge spectrum, all-mode exploration, C-row inspector, original complete NumPy program and useful original coverage. The original source and component are archived in `scratch/spectral-authoring/`; the native verifier checks the original program byte-for-byte, including its original line endings. The title and route identity remain unchanged.

The lesson now develops weighted energy and kernel, Fiedler variational reasoning, RatioCut versus Ncut versus conductance, normalized coordinate conversion and isolates, a constructive Cheeger argument, row embeddings and actual Lloyd steps, spectral gains and ridge/heat derivations, periodic versus lazy walks, full repeated-eigenspace invariance, and conductance-based resistance. It includes nine complete executed programs, two early checkpoints and ten independently changed practice tasks with hints and explained solutions. The initial graph, coordinate correspondence, walk and circuit figures appear in normal reading order. The spectrum, cut sweep, embedding trace and coefficient/gain investigations show different mechanisms; a lab count did not determine their design.

The open destination note `topic-notes/message-passing-graph-convolutions-gcn-gat-graphsage.md` asks that future GCN authoring repair normalized limiting-coordinate and low-pass claims. It records why that later topic owns the full treatment; its existing lesson was not rewritten here.

## Native and independent numerical checks

`node scripts/verify-spectral-graph.mjs` uses `scripts/verify-spectral-graph-native.py` and the isolated Python 3.12.14 / NumPy 2.3.5 / SciPy 1.18.1 runtime. Final numerical-model check: `scratch/spectral-verification/native-results.json`, 18:23:48 UTC.

- 41 bridge spectra and 240 independently generated weighted graphs; NumPy spectral/projector comparisons, independent connectivity, orthogonality, residual and reconstruction checks.
- 82 normalized sweep cases and 5,084 exhaustive finite partitions; direct cut/volume calculations and independent optimal-conductance/Cheeger comparisons.
- 152 three-community clustering runs; full-coordinate assignment, centroid, loss and empty-cluster contracts.
- 14,432 filter states, including 3,362 independent SciPy matrix exponential or linear-solve comparisons; repeated-boundary cases are checked by subspace properties rather than arbitrary eigenvector identity.
- All nine displayed programs and exact stdout; 288 changed native circuit cases against series and grounded solves, 56 changed native Lloyd trials, and 21 invalid-input contracts.

The model source did not change during later responsive/prose polish. Complementary review is recorded separately in `SPECTRAL-GRAPH-THEORY-INDEPENDENT-REVIEW.md` when finalized; its executable checks are `scripts/verify-spectral-independent.py` and `scratch/spectral-independent-review/results.json`. That reviewer independently integrated weighted threshold probabilities, checked generalized coordinates, matrix functions, rational circuits, disconnected embeddings and a repeated-block counterexample.

## Browser, keyboard and visual reading

`scripts/review-spectral-graph-lesson.cjs` passed actual Edge at 1440, 390 and 320 CSS pixels (`scratch/spectral-browser/results.json`, 18:24:46 UTC). Each width covered 193 states, all ten anchors, all nine displayed code/output/question blocks, twelve nonempty checkpoint/practice disclosures, 59 keyboard targets and eleven rendered display equations. Local tables/large graph views were checked for keyboard scrolling. There were no page errors, console warnings/errors, failed requests or page-level overflow. Public fonts were actually loaded.

The later `scripts/review-spectral-reading.cjs` record supersedes that run for the added introductory graph, final responsive graph/scatter layout and narrow prose corrections. It checks all three widths, paragraph bounds, equation bounds, real SVG text geometry and effective font sizes, graph endpoint correspondence, and literal rendered mathematical question text. The six-node phone graph now uses a readable vertical layout with every node visible. The embedding scatter fits its container; the nine-node source graph retains an explicitly keyboard-scrollable native-size view. Exact program and model behavior remained unchanged.

Actual opened screenshots include the baseline pilot at desktop/390; final ordinary section starts throughout the 390-pixel lesson and selected 1440/320 readings; the introductory graph, C-row inspector, coordinate correspondence, Cheeger proof, circuit, source annotations, practice, changed cut, embedding and filter states. In particular the final `spectrum-graph-final-390.png` and `embedding-plot-final-320.png` were reopened after correcting mobile clipping; `coordinates-final-320.png`, `sources-final-390.png` and the changed practice reading were inspected. The final evidence JSON lists final opened files separately from earlier superseded captures.

## Findings corrected during author review

Repaired a literal JSX brace causing a render error; corrected apostrophe-derived anchor IDs; wrapped five long equations; split a long optional library expression at 320 pixels; fixed an impossible example degree sequence; qualified the weighted-median proof when one shifted side is zero; restored the original program's byte-preserved line endings; enlarged and rearranged graph labels for narrow reading. Late independent prose review identified missing spaces; its scoped AST printer then exposed literal Unicode escapes in three JSX question attributes. Those attributes and the printer were corrected and actual rendered-minus/path assertions added. These were source corrections, not suppressed browser warnings. Rapid long-page capture also exposed transient unpainted tiles; the final ordinary-reading harness uses instant scrolling with reduced motion, then waits two animation frames and 200 ms after each jump before taking its screenshot. A fresh-page capture confirmed this was a scrolling/paint artifact, not missing paragraph content.

Some initial test assumptions also needed correction: a disconnected zero eigenspace has no unique second column, repeated basis columns can differ between JS engines, an initially empty Lloyd cluster may later recover, and rounded numerical output is not an exact binary float. The final checks test the stated contracts rather than demand arbitrary basis identity or false exactness.

## Evidence limits

The finite numerical suites supplement the written proofs; they do not establish arbitrary-range floating-point safety or real-data clustering quality. Graph examples are deterministic teaching constructions, not empirical benchmarks. The current optional scikit-learn API was researched but that package was not installed or claimed to execute here. Primary sources and the inspected MIT transcript portions are logged in the design; no full-video viewing is claimed. No user approval or parent production-build completion is implied by this author freeze.
