# Capsule Networks — author implementation, 22 September 2026

Scope is only `capsule-networks`, Deep Learning position13, authorized finish of prepared revision3. `--topic capsule-networks --work finish` passed and the complete manuscript, visual specification, design, programs, data/provenance and numerical outputs were read. The topic keeps its stable title/ID and actual ConvNeXt → Capsules → RNN sequence. Parent owns blueprint registration, shared publication/ledger/inventory and final production integration. No additional topic is authorized by this record.

## Content conservation and learning experience

The authoring generator renders the complete prepared manuscript into static JSX: eleven major sections, including the added complete-code route, and all seventeen closed worked/practice disclosures. No Markdown engine is shipped. No section was substituted with a shorter summary. Local asset links point to the actual downloadable files; named external references and the optional derivative, EM, scaling, original-architecture and alternative-routing branches remain.

The first-pass route starts with compatible parts, introduces groups/votes and the softmax axis, then follows actual model shapes, loss and masking into a paired real-data experiment. The exact six-run negative/mixed outcomes, reconstruction information boundary, transformed-image failure and independent practice remain. Matrices do not acquire physical semantics merely by being called poses; internal concentration is not a correctness verdict. The optional implementation-temperature task gives changed values and a genuine code modification.

The downloadable NumPy mechanics and independent encoder are transparent scratch routes; the trainable PyTorch program is the ordinary research-tool route, with explicit votes/routing/objective. Convolution and autograd backends are declared abstractions, not hidden routing. Complete source appears only when opened, beside downloadable data/helper/output/provenance. Three source disclosures expose the full training, mechanics and independently reconstructed inference programs.

Author learning-experience reread assessed the full generated article and component flow: plain terms before formalism; a connected three-child calculation and actual handwritten images; input → mechanism → output links; direct manipulations without learner-prediction entry; separate optional depth; altered-case exercises with hidden explanations; exact versus measured evidence identified; and explicit decisions/limits following each experiment. This is an author source assessment, not an independent review or observed beginner study.

## Visual and interaction implementation

- **Grouping figure:** select a real4×4 cell and capsule type, see the four source channels and target child/coordinate pairs. Zero-based spatial/type/coordinate indices are explicit; named children in the vote/EM workshops use1–3 to match the manuscript.
- **Vote workshop:** six actual2D votes, any coordinate editable; fixed equal-axis parent planes show raw and weighted arrows, allocations show each child's outgoing mass, and tables expose logits/sums/outputs/dot agreements/entropy. Step1–8 always recomputes from zero logits after editing. Changed/opposing, zero, symmetric and common-rotation cases are genuine computations. Temperature changes softmax only. Coincident marks retain exact separate named rows rather than distorting coordinates.
- **Squash investigation:** analytic length curve, actual input/output vectors, finite radial/tangent interventions, exact derivative comparison, zero vector and zero-perturbation cases. A radius beyond the stated0–6 curve domain is reported rather than clipped into a false location.
- **Paired evidence:** six real models appear in three distinct metrics with zero-based axes, exact tables and a mean-image reconstruction reference. Fixed-weight inference choices expose changed labels independently of unchanged correct count.
- **Frozen real-image investigation:** opens on demand; two predetermined real specimens, editable44px pixel targets plus row/column/numeric/range equivalents, all four nonwrapping shifts, original input comparison, actual class scores, predicted-mask reconstruction, independently selected decoder mask/latent coordinate and signed difference. Decoder edits do not mutate encoder scores. A masked-out edit is exactly null. Larger image arrays and source are table/code scroll regions, never whole-page overflow fixes.
- **Frame figure:** explicitly supplied matrix routes agree; an editable unconstrained vector-map route can disagree. Identity map and zero-angle comparisons expose the difference between one matching case and a general symmetry claim.
- **EM investigation:** actual votes and child activations drive the full declared three-round diagonal calculation; activation bars, equal-axis vote/mean/spread plots, responsibility/effective-mass rows and variance-floor statements describe the same state. All-zero activation produces no-evidence semantics. Inactive-child edits preserve means while their responsibility rows may change.

Scoped CSS preserves near-black/charcoal and amber, neutral secondary marks, the site's fonts and visible control states. It overrides inherited green helper styles without editing shared historical CSS. Continuous controls use `step="any"` so numeric entries and DOM ranges describe the same current value; structural counts remain integers. The editable8-column pixel grid keeps44px cells and its own labeled horizontal-scroll region at narrow widths, with equivalent row/column controls. No raster imagery, decorative gradients, worker training, autoplay or prediction gate is introduced.

## Execution and packaging evidence

The complete native learning program was actually run once for this implementation: six fits ×600 updates. Its entire parsed `calculated-inputs.json`, plus actual mechanics and NumPy author-check outputs, exactly matches the prepared packet. No new measurement was copied onto changed training code. All five program/data source files retain the same bytes as the prepared packet. The entire prepared packet remains unchanged: author/review notes live in this implementation record, and the design file was restored to its exact recorded content-phase SHA-256 rather than rehashing a modified packet.

`scripts/verify-capsule-native.py` passes six substantive groups: replay/source conservation; fourteen actual float64 frozen-image computations covering original/edited/all shifts/zero shift with batch and mask checks; execution of the displayed simplified Torch route on changed batch/child/parent/dimension shapes; temperature/one-parent checks; loss units and radial/tangent/zero autograd; and changed two-coordinate EM sufficient statistics. Environment is Python3.12.14, PyTorch2.14.0+cpu, NumPy2.3.5 and sklearn1.9.1. Evidence: `capsule-native.json`, `capsule-native-inference.json` and the downloadable `native-verification.json`.

`scripts/verify-capsule-models.mjs` passes seven author groups: complete source parse/conservation, all256 coordinate mappings, all native routing fields plus invariant cases, finite/derivative squash checks, EM weights/responsibilities/null evidence, actual packed-state JavaScript inference and corruption boundaries, and every compact experiment value/identity. Largest capsule-coordinate difference from the14 independent native float64 cases is3.3306690738754696e−16. The original prepared float32-vs-NumPy tolerances remain distinct. Source-bound output is `capsule-author.json`.

The browser's one frozen model is47,184 exact original float32 values,188,736 bytes, loaded with metadata and two images only after disclosure. Compact measured figure data is21,775 uncompressed bytes in this topic's lazy chunk. Full2.8MB experiment evidence remains a downloadable artifact, never an eager browser import. Source/model identity, array length, layout and finite values are validated; failed/corrupt loads show retry instead of substituting a toy model.

Warmed Node inference takes a few milliseconds for this bounded architecture on the author host; exact runs and observed median/p95 are retained in `capsule-author.json`. That result supports simple synchronous derived state here, not a universal phone guarantee. No jobs can accumulate or complete out of order because inference is synchronous and bounded; load effects abort on close/unmount. Final browser integration must measure actual input-to-painted-result behavior and may justify a worker if it reveals a real problem.

## Research and limits

The original full primary-source audit is preserved in the prepared design. Implementation spot checks reopened the original arXiv record and current PyTorch einsum documentation; the stable docs resolve to2.14. The UCF historical tutorial index still returned502. Its link is retained with an availability caveat and the author slides remain the direct alternate route; no video-viewing claim or guessed YouTube URL was added. No substantive architectural assertion changed from the already researched packet.

No full MNIST/affNIST/MultiMNIST, complete trained matrix-capsule model, variational/STAR reproduction, GPU run or learned-pose guarantee is claimed. Constructed EM is the fully declared local statistics example. Native training replay, model parity, formal independent review and browser/perceptual review are different evidence states.

## Independent review and parent handoff

Author source is ready for independent review. Parent must register `src/learn/data/curriculum/blueprints/capsule-networks.js` under exact title **Capsule Networks**; existing publication/stable ID is preserved. The topic itself imports only its scoped components/models/21KB compact measurements; full programs and frozen model are deferred.

`scripts/verify-capsule-browser.cjs` was executed against the parent's final production preview at port 4194. All eight groups passed: actual changes/nulls/invalids/resets, keyboard/pointer behavior, range/numeric equality, model/source demand loading, corrupt-model retry, theme, loaded-font SVG/math and 320/390/desktop containment, and 44px pixel targets. Ten captures are retained in `evidence/capsule-browser/`; their report contains exact hashes. The default desktop and phone views were inspected, followed by the final phone views and guarded-mass state. Fixed navigation and the skip link are suppressed only while taking element screenshots, to prevent the browser's fixed overlays from obscuring long captures; interactions and geometry checks run on the unchanged page. Input-to-painted-result observations were 64–100 ms including automation and two frames, not a pure-kernel benchmark. Author did not run the shared build or CUA. Parent owns final cross-topic integration and ledger acceptance.

Independent review found a valid tiny-mass boundary in the constructed EM view. The native denominator guard preserves finite arithmetic but does not establish an identified parent when mass is at or below `1e-12`. The repaired view labels that parent explicitly, distinguishes its guarded numeric output and suppresses its mean marker/variance ellipse while preserving actual child votes. No activation is silently clamped. Exact author checks include `[0,0,1e-20]` and equality at the guard; production browser checks preserve the typed tiny input, require absent inferred geometry, and verify that reset restores the ordinary display. No historical numerical experiment changed. The parent records independent content/numerical review separately.

The final visible grid hint explains horizontal scrolling and equivalent row/column selection. `verify-capsule-frozen-browser.cjs` passed focused production checks at 390 and 320 px: actual keyboard scrolling, pointer selection of the last column, a real changed pixel, exact numeric/range agreement, reset, minimum target size and no page overflow. Both updated phone captures were inspected. The report preserves the previous full-suite evidence and identifies this bounded follow-up. The hint insertion was checked by reconstructing the exact prior source hash, so unchanged native/model evidence was reused without refitting or broad reruns.

No disposable installer, alternate draft or scratch screenshot was created for this topic. The existing shared Python runtime was used without mutation. Keep the prepared packet and canonical assets/evidence; they are source/evidence dependencies, not cleanup candidates.
