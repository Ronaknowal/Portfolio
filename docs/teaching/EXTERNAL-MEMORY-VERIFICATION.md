# External-Memory Algorithms — author verification

Root integration amendment: one raw comparison character in JSX prose was entity-escaped after this author freeze. Complete normalized-AST equality verifies unchanged text and behavior. Final integrated body SHA-256 is `60c6b3a2ec9f590aa4dc67ef3c95b97d49dfee5602d8328b70a2f93ac9f3384f`; the other six runtime files are unchanged. [Exact before/after evidence](evidence/lesson-comparison-entities.json) and [production integration](DSA-MATH-FOUNDATIONS-INTEGRATION.md) supplement the original dated freeze below.

10 September 2026. Stable ID `external-memory-algorithms-b-trees-i-o-complexity`, DSA22. Author implementation and focused verification are complete. Root owns shared registration, integrated build/navigation/payload checks and the full-goal ledger. User acceptance remains pending; no novice study or production storage benchmark is claimed.

The [design](EXTERNAL-MEMORY-LESSON-DESIGN.md) records scoped preflight, actual source inspection and the representation contracts. There was no prior body or individual blueprint to preserve; the existing catalogue identity, membership, prerequisites and module order remain unchanged. The root registered the complete body and brief separately from final review. The [durable evidence JSON](evidence/external-memory-author-review.json) contains final source hashes, actual result records, freeze time and the list of screenshots actually opened.

## Implemented scope and ownership

- `src/learn/data/topics/external-memory-algorithms-b-trees-i-o-complexity.jsx`: complete progression through page/cache/dirty I/O, multi-key search/height, full textbook B-tree updates, B+ leaf/range conventions, external merge sort, shadow-root durability, practical sorted workflows and seven changed-data exercises with explained answers. Closing completes DSA and correctly names Vectors, Matrices & Tensor Operations next in Complete Curriculum.
- `src/learn/data/external-memory-models.js`: pure bounded topic models for buffer traces, B-tree primitive/completed states, bulk B+ ranges, materialized merge schedules and durable reachable-root states.
- `src/learn/components/lesson-labs/ExternalMemoryLabs.jsx` and `external-memory-labs.css`: five focused investigations and one inline B-tree/B+ comparison. Real values, identity and ordering determine diagrams; no invented benchmark or generic illustration quota.
- `src/learn/data/external-memory-examples.js`: eight complete programs with captured stdout, selected through descriptive named ownership; no cross-topic example import.
- `src/learn/data/practice/external-memory-algorithms-b-trees-i-o-complexity.js`: four scoped official LeetCode transfers, optional variants, hidden hints, changed constraints, prerequisites and reattempt/readiness guidance. Only this dataset is imported.
- `src/learn/data/curriculum/blueprints/external-memory-algorithms-b-trees-i-o-complexity.js`: individual current design brief; shared registration belongs to root.

## Independent native and model evidence

Command: `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-external-memory.py`.

All eight programs were executed as actual isolated Python 3.12.14 processes and matched their displayed stdout. The preparation script, `scripts/prepare-external-memory-examples.py`, creates the source outputs by execution; verification independently checks algorithm contracts rather than trusting those captured strings.

| Contract | Independent check and actual result |
| --- | --- |
| Page residency/dirty I/O | 192 size/capacity/pattern combinations; timestamp-based last-use oracle and dirty membership check every access ledger/resident order, final flush, and native OrderedDict result. |
| Browser B-tree | 5,760 completed states across degrees 2–4, presets and deterministic added operations; set membership, unique sorted keys, ancestor intervals, child count, occupancy and equal leaf depth. Search existence checked for 35 keys per fixture. Primitive replacement/empty-root snapshots are intentionally not falsely labeled completed public states. |
| Native B-tree | 28,000 actual insert/delete operations across degrees 2,3,4,8, independently checked after each mutation. The resulting evidence includes split, merge, left/right borrow, predecessor/successor and root contraction; duplicate/missing/empty boundaries covered. |
| B+ ranges | 15,129 inclusive bounds/capacity/fanout combinations against independent sorted filtering plus the native page route; separator equality, gaps, reversed range and unique visited identities checked. |
| Merge schedules | 4,656 N/B/P cases against a length-only ceiling/page ledger independent of merge comparisons; sorted output and multiplicity checked. |
| Actual external files | 48 changed input/page/memory cases with real temporary binary files and negative/duplicate values; result compared with Python sorted, each stage checked against independent run-length/page ceilings. Input creation and final display readback are excluded consistently. |
| Root recovery | All twelve safe/broken snapshots checked with set containment of the reachable page graph, plus actual native state values and changed interruption questions. |
| Local practice and boundaries | Seven exercise groups checked via changed cache traces, integer byte inequalities/height, explicit borrowing/merging fixtures, all range cases, changed merge schedules, crash states and a complete sort→unique-tree→delete pipeline. Eight invalid bounded model inputs reject with RangeError. |

The native script initially compared its deliberately descriptive invalid-root string with a shorter harness expectation; the assertion was corrected to the actual specified string. This was a harness mismatch, not a storage/model fix. No numerical or data-structure correctness failure was found in the finished mechanisms. `scratch/external-memory-verification/results.json` records the actual results; the durable evidence embeds them for a fresh checkout.

## Browser, keyboard and ordinary reading

Commands: `node scripts/review-external-memory.cjs` and `node scripts/review-external-memory-reading.cjs`, using Playwright with local Edge against Vite5173. WebSocket updates are closed for stable test sessions; no DOM-injected fake model states are used.

At both 1440×1000 and 390×1000:

- Nine route anchors were activated with the keyboard and their actual arrival positions asserted below the fixed header.
- 52 page-buffer frames, 107 B-tree events, six changed B+ configurations, six merge configurations and twelve crash stages were exercised. Inputs, keyboard actions, reset, dirty states, search highlights, custom insertion, empty runs and reversed ranges were checked against the native-verified model contracts.
- All eight rendered code/output pairs matched their source fixture text. Seven local answers and the external hints started closed; keyboard toggles and the optional platform stage worked. Four official LeetCode links and five annotated reference links rendered, with platform links opening separately and preserving the lesson.
- Page/figure/calculation overflow and browser errors were absent. Long native code is intentionally horizontally scrollable on narrow screens, rather than forcing the entire page wider.

Additional ordinary-reading review at 1440,390,320 covers both worked calculations, the height/repair proof prose, B+ record-placement figure, actual program/output, changed pass exercise, references and the tall deletion-tree hierarchy. Actual 320-pixel keyboard/reset and invalid-root interactions also pass. The author opened viewport screenshots without hiding the site's normal reader/sidebar/navigation, including the final notation and nesting states; this is visual inspection in addition to geometric assertions. Full interaction records are in `scratch/external-memory-browser/results.json`; final ordinary-reading records are in `reading-results.json`, both embedded durably.

Inspection refinements: current-event controls precede the tall B-tree and B+ page hierarchies; parent/child labels retain nesting when branches stack; final-tree search highlights its actual page path and clears stale results after key/step changes; root-publication option labels fit narrow controls; explanatory text has spacing below reset controls; height powers/log bases and the multiline scan/scale sorting bound remain legible at320. Early harness selectors assumed “Buffer hits” instead of “Resident hits,” and overlooked an output element's implicit status role; both selectors were corrected without changing those UI contracts.

`node scripts/format-external-memory.cjs` parsed all six owned JS/JSX files, regenerated conventionally formatted code and verified normalized AST/string equality; CSS parsing/regeneration likewise preserved meaning. No math renderer or global style dependency was added. The final hashes establish which source these results cover.

## Independent source review and research limits

Root read the full body: page/record units; B-tree occupancy/repair proofs; B+ routing/payload bounds; materialized sorting, partial pages and RAM caveats; shadow-root durability proof and changed tasks. Root reported no actionable source defect. This is a bounded independent source/proof review, not additional numerical tests or observed learner performance.

Written/video research scope is recorded in the design and annotated in the actual references: MIT6.851 official Lecture7 page and relevant scribe sections; Open Data Structures B-tree search/height/update sections; Cornell external-sort buffer/pass sections; CMU shadow-paging PDF pages7–10; SQLite introduction, hardware assumptions and journal/data staging. The MIT video is a useful advanced alternate route, but full playback was not reviewed. Historical hardware tables are not reused as current specifications.

Official LeetCode statements inspected on10 September2026: **146 LRU Cache (Medium)**, **23 Merge k Sorted Lists (Hard)**, **148 Sort List (Medium, optional)** and **378 Kth Smallest Element in a Sorted Matrix (Medium, optional)**. IDs, titles, difficulty and publicly available statements/follow-ups were verified. No submission, editorial review or free-account universal access guarantee is claimed. These tasks practise transfer, not every page-tree or recovery invariant, and do not guarantee every interview problem.

The [incoming persistence note](topic-notes/external-memory-algorithms-b-trees-i-o-complexity.md) is adapted with actual implementation/reachability checks. Its deferred deeper entry preserves cache-oblivious/buffer-tree/LSM and full concurrent database recovery scope for owner reassessment. Current B+ bulk layout is not dynamic B+ maintenance; the temporary-file sorter is not a strict process-memory limiter; shadow-root publication assumes atomic durable metadata and does not implement real fsync, power-loss recovery or SQLite. Shared integrated build/publication conservation remain root's responsibility.
