# Arrays: bitwise representation extension verification

Author verification completed 11 September 2026. This is a scoped extension of **Arrays, Strings & Hash Maps**, stable ID `arrays-strings-hash-maps`, not a new route entry. Independent review and production integration are still pending. Existing publication is not user acceptance.

## Implemented learning and preservation

The [assessed design](BITWISE-FOUNDATIONS-EXTENSION-DESIGN.md) adds a signposted deeper branch after the original first-unique-event task. Sections 9–14 derive finite-set place values and membership operations; signed versus unsigned word interpretation; Python shift/complement contracts; XOR's prefix invariant and precise multiplicity promises; lowest-bit clearing/population count; the positive power-of-two test; Hamming distance; and optional two-singleton partitioning. Bitmap postings reconnect the branch to the original search index. Four visible changed-contract tasks have separate closed hints and complete explained solutions. Core reasoning is visible without opening solutions.

The existing title, identity, route position, all six original programs/outputs, all ten original LeetCode entries and **62 original teaching AST subtrees** are conserved. Original array/text/hash models, investigations and shared CSS are byte-identical. The earlier next-topic bridge remains. The intro/anchors, estimated reading time, owned brief and annotated references are extended. The original-first-five reviewed increment remains historical evidence; it does not establish that this new branch works.

Four mechanisms use four focused representations: editable aligned membership columns; weighted signed/unsigned word and source-position shift maps; a stepped parity stream with a separately labelled diagnostic frequency audit; and a borrow/removal/count invariant trace. The computed two-bucket figure supports the optional partition proof. Reusing bit columns here follows the common mechanism, while the surrounding representations answer different questions. There is no generic simulation mode, extra runtime dependency or new eager registry.

Five new complete standalone Python 3.12+ programs have executed outputs. They cover bitmap queries, bounded words, XOR promises, population/power/Hamming reasoning, and two singletons. Input and diagnostic overhead are distinguished from algorithmic workspace; fixed words are distinguished from arbitrary-size Python integers. The UI bounds all loops and data sizes and preserves the previous active trace after invalid drafts.

## Native and conservation evidence

[Author packet](evidence/bitwise-foundations-author-review.json); original plan/source manifest at [bitwise-foundations-original.json](evidence/bitwise-foundations-original.json). Original bytes are additionally archived in `evidence/bitwise-foundations-original-sources.json` and the final author bytes in `scratch/bitwise-author-freeze/`.

`node scripts/verify-bitwise-foundations.mjs` invokes the actual Python verifier. Final native evidence is `scratch/bitwise-author/native-results.json`, dated **2026-09-11T09:11:08.312168+00:00**. It passes:

- All eleven actual displayed programs with exact stdout; all six original code/output fixtures and ten original problem objects preserved.
- 436,900 finite-set operation comparisons with ordinary sets, covering every pair of masks for widths 1–8; all membership toggles are checked too.
- 4,096 word/shift cases checked through independent binary strings, negative top-bit weights and actual Python encode/decode/shift operations.
- Every 16-bit nonnegative input for the sparse-bit trace and for actual Python population counting: 65,536 each. Every removal witness, termination count and positive-power interpretation agrees.
- 5,460 complete small event sequences for parity invariants and frequency promises, including 1,236 valid two-singleton partitions; 800 additional signed/zero native singleton-pair cases and 2,400 native posting-set comparisons.
- 19 malformed JS calls and 16 invalid native cases, including holes in arrays, bad widths/positions, negative/oversized words, negative shifts, invalid value types, one-shot reuse and contradictory zero-XOR input.
- The four local changed tasks' masks, signed shifts, parity counterexample and bitmap-report answers match direct computations. General claims are proved in the lesson; finite tests are supporting evidence, not a substitute for proof.

The pure JS model uses bounded arithmetic to avoid silently importing JavaScript's signed-32-bit coercion into an explanation of Python. Python examples deliberately include 100-bit and signed values beyond the visible byte model. The five-program writer runs each full program before storing its displayed output; the verifier independently reruns the stored programs and checks changed inputs. The AST-preserving formatter checks semantic JS/CSS conservation.

## Actual browser and visual reading

`node scripts/review-bitwise-foundations.cjs` passed **2026-09-11T09:07:09.694Z** at **1440, 390 and 320 px**, using actual Edge and Space Grotesk. At every width: 76 operated states, 53 keyboard-opened disclosures, all three retained investigations, all four additions, all eleven full code/output blocks, all fifteen problem placements and fifteen valid lesson anchors. It exercises each new control, reset, invalid-input recovery, zero/negative interpretation, changed promise, custom input and count boundary. No page/console/request error, document overflow, overflowing bit text or clipped select-option label was found. The final native and browser records bind the same eleven source hashes.

Fifteen actual retained screenshots were opened by the author, covering desktop first-pass membership teaching and the default word investigation; narrow changed sets, changed word values and shift origins; borrow/removal state; changed and invalid parity; two-singleton partitions; changed bitmap solutions; signed-word first-pass prose; bitwise practice; references; invalid-draft feedback; and zero-count behavior. The packet names these opened files and their hashes. Other captured images are not claimed as opened.

The first browser attempt found ambiguous implicit select label text; seven controls now have explicit accessible names. The subsequent screenshot harness initially assumed that every section could reach the same top offset, even near the document end. Its bottom-limit handling was corrected; this was a harness constraint, not a lesson defect. A fresh complete three-width pass followed. No full screen-reader session, physical mobile device, observed beginner study or timing benchmark is claimed.

## Research and practice boundaries

New official public statements were directly inspected on 11 September 2026: LeetCode 136, 191, 231, 461 and 260. The existing ten retain their 10 September attribution. Each addition has an original focus, optional hint, changed-contract transfer and relevant prerequisites; 260 is an optional deeper stage. The page explicitly declines a universal interview guarantee. No judge submission or editorial access is claimed.

The Python integer bitwise/method documentation was read for sign extension, floor right shift, complement, errors and absolute-value `bit_count`. The Handbook's chapter 10, printed pages 95–99, was read for bitsets and lowest-bit identities; its C++ width/overflow assumptions and sign/magnitude phrasing were not adopted. The source annotation flags the language difference. Existing annotated MIT lecture/video and Python Unicode routes are retained; they were not newly watched as part of this extension. No unreviewed video was attached merely to meet a media count.

The destination note now records this owner and its author-verified stage. Final independent review, shared practice-coverage refresh, route/count conservation and production loading checks remain the parent integration gate. Those later checks must identify the final sources rather than promote this author packet by publication status alone.
