# Deep Learning: specialized convolutions through sequence generation

Completed 22 September 2026. This closes the user's next-five implementation request: Deep Learning positions 11–15. Both delivery phases are complete for these five revision 3 packets. The evidence below supersedes dated author notes reserving independent review or browser integration for the parent; user acceptance remains separate.

| Position | Topic | Implementation | Independent review |
| --- | --- | --- | --- |
| 11 | Depthwise Separable & Dilated Convolutions | [Author record](DEPTHWISE-CONVOLUTIONS-IMPLEMENTATION.md) | [Independent review](DEPTHWISE-CONVOLUTIONS-INDEPENDENT-REVIEW.md) |
| 12 | ConvNeXt & Modern CNN Designs | [Author record](CONVNEXT-IMPLEMENTATION.md) | [Independent review](CONVNEXT-INDEPENDENT-REVIEW.md) |
| 13 | Capsule Networks | [Author record](CAPSULE-IMPLEMENTATION.md) | [Independent review](CAPSULE-INDEPENDENT-REVIEW.md) |
| 14 | RNNs, LSTMs & GRUs | [Author record](RECURRENT-IMPLEMENTATION.md) | [Independent review](RECURRENT-INDEPENDENT-REVIEW.md) |
| 15 | Sequence-to-Sequence & Encoder-Decoder | [Author record](SEQ2SEQ-IMPLEMENTATION.md) | [Independent review](SEQ2SEQ-INDEPENDENT-REVIEW.md) |

## Complete teaching and topic ownership

The full prepared manuscripts, specifications, explained scratch code, ordinary library/tool routes, worked examples, independent practice/solutions and annotated references were consumed. None was replaced with an outline or a short summary. Original content checkpoints and previous revisions are conserved. Implementation corrections live in semantically named topic JSX, models, components, scoped CSS, blueprints and canonical downloads; a batch name does not own learner content.

The five routes use different representations: convolution channel lanes, rank factors and sampling stencils; modern block diagrams, normalization groups and real masked reconstruction; capsule votes, routing, geometry and pixel/latent interventions; signed recurrent memory, reset edges, actual pen traces and state ownership; and decoder token alignment, temporal credit, candidate-owned search and actual generation. Inputs change current outputs directly. No learner-prediction entry, answer gate or repeated generic lab format was introduced.

Complete programs/data remain available under each topic's `public/learn-assets/` or `public/learn-code/` directory. Large source opens on demand; actual model parameters load only in their selected lesson/investigation. Full experiment JSON remains a download instead of an eager shared import. Bounded browser arithmetic recomputes genuine states; recorded-run selectors are explicitly distinguished from training.

## Numerical, independent and browser evidence

All five have separate author, native and independent review receipts. Capsule's six 600-update fits were actually replayed with conserved results. The unchanged six depthwise, six ConvNeXt, nine recurrent and three sequence fits were reused as source-bound evidence; fresh native inference, parameter/state/gradient checks establish the implemented mechanisms without claiming new training measurements.

Complementary reviewers checked dense-versus-factorized convolution, all 6,561 supported four-layer dilation schedules, signed normalization/fusion behavior, capsule routing under changed shapes/temperatures, real model interventions, nonzero recurrent states and ownership, and unsorted packed encoder/decoder batches. Their reports retain precise cases, tolerances, source hashes and limits. ConvNeXt's executed same-state Torchvision bridge is distinct from its unexecuted optional pretrained photograph route; no large-scale/GPU/sparse-backend or new benchmark claim is implied.

The final topic browser reports contain 41 passing groups in total:

- [Depthwise and ConvNeXt](evidence/modern-convolution-browser/report.json): 17 groups, 12 captures.
- [Capsules](evidence/capsule-browser/report.json): 8 groups, 10 captures.
- [Recurrent networks](evidence/recurrent-browser/report.json): 8 groups, 5 captures.
- [Sequence-to-sequence](evidence/seq2seq-browser/report.json): 8 groups, 12 captures.

These cover actual pointer/keyboard interaction, precise numeric/range agreement, valid/invalid/null inputs, reset, synchronized views, lazy source/model loading and failure/retry, neutral/amber styling and desktop/390/320 layouts. Topic authors and reviewers inspected retained screenshots; root additionally inspected the recurrent views and representative cost, capsule and sequence phone views. Tall element captures can contain a fixed-header stitching artifact, so affected captures suppress only that header during capture or use real viewports. The site itself is unchanged by capture styling.

`verify-deep-learning-sequence-integration.cjs` passes five fresh production contexts: exactly the requested topic body loads, section anchors resolve uniquely, equations render, local downloads match canonical bytes, links follow the theme and the Next action follows actual module order. Import/render failure substitutions recover while the reader shell remains usable and completion is disabled during failure. These are network-only substitutions, without source or old-evidence mutation.

SVG triage is recorded rather than blindly interpreted as a defect list. The recurrent path numbers sit inside opaque circles and GRU edge labels have opaque backgrounds: geometric line intersections behind those masks are intentional and were checked in screenshots. No unmasked line-through-label defect remains in those inspected views. Narrow exact tables and 44px editable pixel grids use labeled local scrolling and ordinary coordinate controls; no whole-page overflow concealment was used.

The production build passes. Its existing >500kB warning belongs to the shared `LearningNav` chunk (approximately 890 kB raw); this scope preserves rather than certifies or rewrites that shared catalogue architecture. Per-route raw JavaScript totals and requested bodies are recorded in `evidence/deep-learning-sequence-production-integration.json`.

## Corrections made before closure

Depthwise cost bars now preserve exact relative widths, with distinct spatial/mixing marks and full-size coordinate alternatives. All continuous controls accept the same values in numeric and range inputs. ConvNeXt patch swaps compute the actual mask-dependent reconstruction. Capsules distinguish negligible guarded EM statistics from identified estimates. Recurrent diagrams now show signed cancellation and gate placement; pen drags preserve the grab offset and numbered nodes keep labels readable. Sequence decoder storage now derives `BOS + padded_targets[:-1]`, retaining EOS at the first ignored input exactly as the runnable program does. Native calculations, targeted browser checks and source binding were refreshed for the affected changes.

## Ledger and continuation

Current totals: **177 content complete, 150 implementation complete, 27 prepared implementations remaining**. The first 15 Deep Learning topics are implemented. The catalogue remains **1461 topics, 29 modules, 10 paths and 231 publication mappings**; all unrelated 172 ledger rows are unchanged. These are authoring delivery counts, not learner completion.

`evidence/deep-learning-sequence-baseline.json` retains the original five entries, all original row digests and publication identity. `verify-deep-learning-sequence-delivery.mjs` checks original content/history conservation, final reviewed source, all 26 current passing receipts, final build identity and the counts above. The resulting closure is `evidence/deep-learning-sequence-reconciliation.json`.

Next is **Attention Mechanism (Bahdanau, Luong)** (`attention-mechanism-bahdanau-luong`), with research/write complete and implementation not started. Continue only under the user's next scoped request and finish preflight. Preserve this work and reuse current evidence instead of reopening closed integration notes or rerunning fits without a changed-source reason.

The isolated temporary ConvNeXt dependency installation was removed after its native/library checks. Durable programs, source data, compact model fixtures, review evidence and selected screenshots remain; shared runtimes and the offline font fixture are still dependencies. The existing user preview is preserved. No commit or deployment was made.
