# Classical ML live exploration — early topics

21 September 2026. This migration covers Classical ML positions 1–22 except K-Means (position 11, owned by the integration agent). All 21 topic dispositions are below. It removes learner prediction entry, prediction grading and guess-to-reveal interfaces; numerical/model predictions and separate independent practice remain.

The first ten lessons already had live mechanisms. Their topic-specific controls and diagrams were retained while prediction prompts were converted to ordinary exploration guidance. The later investigations now update meaningful valid controls and their actual calculations together. Algorithm steps remain where stepping teaches the mechanism.

| Position | Topic ID | Disposition |
| --- | --- | --- |
| 1 | linear-logistic-regression | Retained live fitted line, logistic probability, decision-threshold and separation controls; removed learner-guess prompt components. Gradient descent retains genuine Next step with numeric coefficient/gradient feedback. |
| 2 | decision-trees-random-forests | Retained query routing, split, pruning, bootstrap membership, correlation and permutation views; replaced guess prompts with ordinary conceptual exploration. |
| 3 | k-nearest-neighbors-knn | Retained linked neighbour votes, learned scales, regression, exact search, candidate restriction and dimensional geometry; removed predict-first guidance. |
| 4 | gradient-boosted-trees-xgboost-lightgbm-catboost | Retained bounded live loss, split, histogram, sampling and fitted-validation controls; removed prediction prompts without changing training-data roles. |
| 5 | support-vector-machines-svm | Retained margin/kernel/solver/tube geometry and bounded actual fitting controls; removed learner prediction prompts. Solver operations remain process actions. |
| 6 | naive-bayes-probabilistic-classifiers | Retained linked likelihood, smoothing, event-model, continuous evidence and reliability calculations; learner-guess prompts removed. |
| 7 | ensemble-methods-stacking | Retained blending, bootstrap ownership, exact boosting steps, out-of-fold lineage and fitted prediction comparisons; replaced prediction tasks with exploration guidance. |
| 8 | recommender-systems-collaborative-filtering-matrix-factorization | Retained evidence cells, factor-update steps, confidence, ranking and logging-policy controls; removed predict-first wrapper text. |
| 9 | multi-label-multi-output-learning | Retained actual label/annotation toggles, joint masses, chain order, score thresholds and shared-target scaling; removed learner prediction wrapper text. |
| 10 | survival-analysis-cox-regression-kaplan-meier-hazard-models | Retained risk-set, censoring, event-time, Cox, horizon and competing-risk process views; removed learner-predict wrapper text while preserving model probabilities and observation roles. |
| 12 | pca-dimensionality-reduction | Projection/residual readouts, metric changes, budget selection and label collisions are visible immediately. Added explicit current-v-reference squared-error comparison; controls no longer gate results behind guesses. |
| 13 | clustering-evaluation-validation-silhouette-ari-nmi | All five investigations calculate live; weighted rescoring preserves frozen labels and resample comparison uses aligned probes. Invalid ordering preserves last valid centers with visible error. |
| 14 | dbscan-density-based-clustering | Trail, metric, Iris and incompatible-density views update live. Comparison snapshots and species diagnostics remain distinct operations; no learner answer unlocks geometry or counts. |
| 15 | anomaly-outlier-detection-isolation-forest-one-class-svm-lof | Removed learner guess hooks and all answer gates across isolation, LOF, kernel acceptance, workload and the separately loaded temperature lab. Valid controls synchronize scores, thresholds, timeline, alert counts and inspected rows. |
| 16 | gaussian-mixture-models-gmm-em-algorithm | Responsibilities/densities and covariance comparisons update live. Setup edits restart the EM trace immediately; actual E/M/Back actions remain. Added useful bounded sliders alongside numeric inputs. |
| 17 | t-sne-umap-manifold-learning | Graph routes, affinities, perplexities, fuzzy memberships, ideal pair costs and digit-neighbour comparisons update live. Invalid graph/scale edits preserve an explicitly labelled valid view; reset and undo remain. |
| 18 | independent-component-analysis-ica | Source-family and rotation edits update geometry and moments live; amplitudes/keep-sets and scale compensation are directly playable. Invalid zero scale is reported without replacing the valid reconstruction. |
| 19 | non-negative-matrix-factorization-nmf | Amounts/pattern cells update their actual contributions immediately, with a deliberately saved comparison reference. H/W phases retain true process steps. Component masks update image/pixel error against the fixed full-component reference. |
| 20 | feature-scaling-encoding-imputation | Distance divisors, donor eligibility, frozen-bundle transformations and cross-fitted target encodings calculate live. Numeric validity and training-fitted statistics are preserved. |
| 21 | cross-validation-hyperparameter-tuning | Fold, fair-label selection, nested fitting and halving compute live. Each potentially failing model is now derived once inside a guarded calculation and reused in visual/readout paths. Hindsight remains identified as outside the paid-for schedule. |
| 22 | regularization-l1-l2-elastic-net-dropout | Soft thresholds, coordinate traces, physical-input polynomial effects and full dropout-branch expectations are visible live; bounded sliders support high-leverage parameters. |

## Current contracts and source ownership

Current lesson prose, scoped blueprints and design/visual specification records carry the new UX contract. Six prepared manuscripts with contradictory instructions were also synchronized; their numerical content and historical review receipts are preserved. Dated amendments explicitly supersede older gate requirements without rewriting historical reviews. The [exact source manifest](evidence/live-exploration-classical-early.json) lists every scoped path and digest. K-Means, common wrappers, the global teaching standard and delivery ledger remain root-owned. No lesson publication or module order is changed.

## Verification and review

The [independent interface review](LIVE-EXPLORATION-CLASSICAL-EARLY-INDEPENDENT-REVIEW.md) covered validated ICA/Manifold hooks, NMF comparisons, EM restarts and Validation calculations. It identified two Validation issues: stale prediction wording and an unguarded duplicate calculation after validation. Both are fixed. Fold, nested and halving outputs now reuse a single guarded result.

`node scripts/verify-classical-early-live-source.cjs` parses 30 owned components, checks unbound references, rejects obsolete learner-prediction components and catches functions rendered as native DOM children. All these checks pass.

The final [browser receipt](evidence/live-exploration-classical-early-browser.json) passes all 21 topic groups, 103 meaningful control edits and seven semantic mechanism groups, plus all 21 phone routes at 390 px. After the Survival layout repair, its eight desktop controls and all phone routes were run fresh; twenty unchanged desktop groups are explicitly reused from the retained exact-source prior production receipt. All 107 scoped source hashes match the frozen manifest. The verifier exercises every available first meaningful control in each lab, checks observable output/geometry changes, tests selected arithmetic/process/invalid-input mechanisms, and checks all 21 phone routes for page overflow with the actual site fonts. Genuine solver steps are advanced as process actions; the initial gradient-descent point correctly does not move merely because the future learning rate changes. Intermittent dev-server timeouts caused incomplete development sweeps; they are not represented as final passing evidence. Root owns the final production build and integration.

The [model conservation record](evidence/live-exploration-classical-early-model-conservation.json) hashes 32 direct model/data imports: 19 match the root baseline exactly; 13 were absent from that baseline, so no retrospective byte-identity claim is made for them. This worker made no numerical-model changes. Existing mathematical reviews remain relevant to unchanged code; this is not a new full mathematical audit.

Three initial phone labs (GMM, NMF and Regularization) were captured with the actual site fonts and opened for direct image inspection. Their numeric controls/sliders, diagrams and readouts remain legible; wider tables retain their contained scrolling regions. The [visual receipt](evidence/live-exploration-classical-early-visual.json) identifies the exact captures, source hashes and limits of that review.

The phone sweep identified one real page overflow in Survival: an unbroken RMST arithmetic chain expanded a 350 px prose paragraph to 512 px and the page to 532 px. The unchanged calculation is now a three-line aligned math block. The final production page measures 390 px at a 390 px viewport with no KaTeX errors. Its [production formula capture and repair evidence](evidence/live-exploration-survival-overflow-repair.json) were opened for direct inspection; fractions and alignment are clear without clipping.

## Browser exercises retained for future changes

- PCA: rotate the same four points from 0° to 45°; retained/residual squared lengths change from 10/10 to 18/2.
- GMM: identical bells receive exactly their mixing weights; an E-step holds observed likelihood fixed, an M-step increases it, and setup edits restart the trace.
- ICA: zero compensation scale shows an explicit error and preserves the last valid sensor table; reset clears it.
- NMF: changed activation updates the selected reconstructed cell; saving a reference freezes that comparison while later controls move.
- Clustering evaluation: non-increasing probe locations cannot feed the exact split solver; visible error and last valid centers stay consistent.
- Cross-validation: assigning every row to one fold clears the invalid calculated result and shows an error; reset restores a valid fold trace.

These are bounded regression checks for the changed mechanisms, not an exhaustive exploration of every state. No new full-model reruns or exhaustive visual review are claimed. Eleven one-time migration utilities were removed; the semantic verifiers and durable records remain.
