# Decision Theory — independent review

Closed 11 September 2026 against the seven final production sources in the [independent packet](evidence/decision-theory-independent-review.json). The original author freeze at07:17:31UTC, its seven sources and its differently timed model/full-browser/caption checks remain separately preserved. No production source changed after that freeze during this review. Shared production integration and user acceptance remain pending.

## Teaching and source review

Read the complete thirteen-section body, all fourteen checkpoint/practice groups, the individual brief, models, labs, standalone figures, CSS and all eleven actual programs. The review followed the learner's progression from a full loss table through conditional/procedure/Bayes risk, decision-dependent summaries, fallback/capacity, calibration and validation, information timing, causal qualifications, ambiguity/regret, randomized minimax, utility and atom-aware CVaR into the contingent allocation capstone. Proofs, units, synthetic assumptions and changed cases were checked rather than inferred from a section count.

Three findings from the earlier source read were fixed **before** author freeze: the general attained CVaR minimum now states0<alpha<1 and treats the mean endpoint separately; sparse arrays cannot pass through inherited entries; and the fallback strip identifies its101 sampled probabilities, while selected-point risks use direct finite comparisons. The final sources retain those fixes. The selected Rockafellar–Uryasev general-distribution definition and minimization theorem were revisited, as were Shalizi's information/action/loss definitions. This is not a claim to have read every cited paper or played every linked video.

## Complementary calculation and browser evidence

`scripts/verify-decision-theory-independent.mjs` preserves the author packet/sources and exports changed cases from the actual final model. `scripts/verify-decision-theory-independent.py` executes all eleven actual displayed programs and checks their full stdout, then uses independent linear programs and exact arithmetic. The LP over probability mass verifies42 tail cases, including negative/duplicate/zero-mass losses and cumulative boundaries. Unit-capacity LPs check36 allocation cases and40 contingent two-branch cases. A separate primal/dual game LP gives the minimax value12/5;51 changed mixtures remain above that certificate. Further checks cover24 signal policies and degraded-information comparisons, exact fallback ties, provisioning, conditional risk and known-square-root utility. Changed calls execute the actual published tail, allocation, information, provisioning, procedure, binary and capstone helpers. The packet records actual counts and versions; these finite checks supplement the separately read arguments.

`scripts/review-decision-theory-independent.cjs` passed with intended fonts at1440/390/320. It exercises all eight investigations with independently chosen changes, actual section-anchor arrivals, keyboard selection/toggles/resets, exact loss and policy ties, partial tail atoms, information arriving too late and a three-slot capstone. All fourteen hint/solution groups, all eleven rendered programs/questions/outputs, equations, SVG labels and page widths were checked. The harness intentionally disconnects development hot reload so concurrent unrelated authoring cannot replace the reviewed page. Its resulting Vite socket message is recorded separately, not presented as an application error or silently lost. An initial harness assertion on that expected message was corrected; final lesson page/console error lists are empty.

Fifteen saved final views were actually opened: fourteen phone views spanning changed loss/procedure/provisioning/fallback/allocation/information/minimax/tail/capstone states, both inline figures, changed practice, actual program output and alternate resources, plus the desktop changed capstone. The state-weighting capture includes the native select menu after keyboard operation; its selected rule and calculations agree. Readable phone controls, mathematical labels, contribution values, branch capacity and ordinary explanatory text were inspected. Code/output use the shared horizontal scroll on narrow screens. Captured but unopened images are not counted as manual review.

No unresolved material finding remains within this scope. This is a finite source/mathematical/browser review, not universal floating-point robustness, a beginner study, user approval, production-loading integration or deployment.
