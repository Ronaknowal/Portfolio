# RNNs, LSTMs and GRUs — implementation and author handoff

22 September 2026. Authorized scope: finish prepared revision 3, Deep Learning position 14, in the five-topic Depthwise-to-Encoder–Decoder increment. Content is complete; author implementation/native checks are complete. Independent review and final browser/integration closure remain pending. The parent owns shared registration, central ledger and final evidence.

## Content and design

Read the complete prepared manuscript, visual specifications, design and native mechanism source. The frozen packet remains unchanged. `scripts/render-recurrent-lesson.mjs` renders the entire manuscript to static JSX at authoring time, replacing explicit visual/lab instructions with their implemented investigations. It normalizes compact disclosures, preserves mathematics, practice/solutions and references, and derives the full disclosed training program from its exact downloadable source. The original program is checked against the prepared source before rendering. No runtime Markdown parser or all-topic bundle was introduced.

The nine investigations are ordered representation, scalar BPTT/update, signed LSTM accounting, fixed-path retention with a positioned write, GRU reset placement, all-nine measured fits, editable real pen inference, chunk/state ownership, and bidirectional padding. A learner can edit values immediately, inspect complete current results and reset; there is no learner-prediction entry or answer gate. The old packet's residual prediction/apply wording is superseded by current policy and removed from production through the actual lab replacements. Separate written exercises retain hints and reasoned solutions.

Core scratch equations appear inline, with canonical NumPy cell code, explicit scalar temporal gradients, complete PyTorch fitting code and the ordinary packed-sequence example available locally. The first complete program opens on demand. The custom browser models implement the same native gate conventions, including both biases and GRU reset after the hidden affine. The full state derivative remains distinct from the direct forget path. Existing general autograd is reused through its owner rather than rewritten.

Inputs/gradients, maps, gate readouts, probabilities and tables share one current state. Learned inference uses actual saved seed-1 weights for three real specimens, with all 32 hidden coordinates and ten class probabilities available. Only compact fixtures/weights are fetched when their investigation approaches the viewport. The complete two-megabyte experiment report is a download, not an eager import. Parameter changes in constructed labs do not imply fitting the real model. The observed point edit often changes probabilities without changing the top class; this is retained honestly.

## Verification actually executed

- `scratch/lesson-tools/Scripts/python.exe -X utf8 -B scripts/verify-recurrent-native.py`: published scratch mechanism program reexecuted in an automatically removed isolated directory and matched the entire retained mechanics report. It exports 27 native learned-trajectory cases, 12 carry/detach/reset value-and-gradient cases, and 20 bidirectional padding cases. Saved seed-1 native models also replay original/reversed development predictions and match their full retained probability arrays. Environment: Python 3.12.14, NumPy 2.3.5, PyTorch 2.14.0+cpu. [Native evidence](evidence/recurrent-native.json).
- `node scripts/verify-recurrent-models.mjs`: eight groups compare browser models with that native evidence, check scalar credit/update and signed gates, retention, correct ownership, and parse actual JSX. [Author evidence](evidence/recurrent-author.json) binds the checked source.
- The nine original fits and all prepared data/programs are byte-conserved. Their recorded fit campaign is reused, not claimed as newly executed. New phase-two work replays the saved fitted parameters and verifies their actual browser mechanism.
- Rechecked current official PyTorch 2.14 GRU and LSTM documentation and the original D2L sequence chapter alongside the packet's retained source research. No new architecture ranking, benchmark or online-recognition claim was introduced.

## Production ownership

`src/learn/data/topics/rnns-lstms-grus.jsx`, `src/learn/data/recurrent-models.js`, `src/learn/components/lesson-labs/RecurrentLabs.jsx`, `recurrent-labs.css`, and the topic's blueprint own this revision. Canonical downloads and compact derived browser fixtures are under `public/learn-assets/rnns-lstms-grus/`. The renderer and verifiers are semantic topic authoring tools.

Register the exact `RNNs, LSTMs & GRUs` blueprint key. Existing publication identity stays unchanged. Next is Sequence-to-Sequence & Encoder-Decoder, then Bahdanau/Luong Attention, preserving module order.

## Review and continuation

Reviewer: read the complete rendered manuscript and prepared packet; assess scope conservation, state/gate conventions, cross-view logic, changed practice and independently chosen edge cases. Useful complementary cases include nonzero initial states, LSTM hidden/cell ownership, GRU gate endpoints and stream permutation. Author/native agreement is available; do not repeat original fits by default.

Integration: verify actual pointer and keyboard slider changes, pen-point dragging plus numeric access, interior/zero/invalid inputs, resets, lazy fixture/source loading and recovery, all section hashes, themed controls/links, phone geometry and route sequence. Inspect screenshots of the key mechanisms rather than declaring layout sound from bounding boxes alone. Record final source changes and rerun affected checks. This report does not yet claim browser or independent acceptance.


## Final integration closure — 22 September 2026

Independent review is accepted and final production browser checks pass eight groups, including actual off-center pen dragging, continuous numeric/range parity, signed memory/reset diagrams, retained-data retry and desktop/phone layouts. Five captures were inspected. The final O(n) comparison helper flattens operands once and rejects unequal sizes; author and complementary checks were rerun without changing numerical results. The complete batch, original packet conservation, reviewed source and ledger closure are recorded in [the integration record](DEEP-LEARNING-SEQUENCE-IMPLEMENTATION.md), which supersedes the earlier pending handoff above.
