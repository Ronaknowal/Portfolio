# Recommender Systems — author verification

The complete replacement retains the stable topic and the original five-user/seven-item teaching dataset, while rebuilding the learning route around evidence, objectives and actual decisions. The exact seven-source author freeze, native results, original-font browser results and 21 actually opened final image hashes are in [the author packet](evidence/recommender-systems-author-review.json). This is author verification; independent review and shared production integration remain separate parent responsibilities.

## Preservation and substantive changes

Read the complete original 904-line body and its six executable blocks before designing. [The original archive](evidence/recommender-systems-original.json) preserves exact body/code/comment bytes. [All six original executions](evidence/recommender-systems-original-execution.json) retain stdout separately from old comments and identify restored setup for dependent fragments. The parent ten-topic baseline also preserves the old state. New support files are topic-owned; no shared visual component or global registry was edited by this author.

The original neighborhood, explicit-SGD, ALS, implicit-confidence, BPR, cold-start, metrics and library workflows remain, with deeper local bridges and complete code. Incorrect old output comments, borrowed Surprise predictions, current implicit axis/confidence assumptions, a blanket obsolete compatibility warning, and unsupported universal winner/speed/latent-genre claims were repaired. Existing hard-coded graphics were replaced by calculated representations tied to the shown objectives. The full current route has fourteen connected sections, fourteen complete programs, eight concept-specific investigations, three inline figures, an initial checkpoint and eleven substantial changed exercises with optional hints and separate explanations.

The design and ownership disposition are in [RECOMMENDER-SYSTEMS-LESSON-DESIGN.md](RECOMMENDER-SYSTEMS-LESSON-DESIGN.md). Detailed serving concerns were delivered to [the existing recommendation-system design owner](topic-notes/recommendation-system-design-candidate-generation-ranking-re-ranking.md), rather than silently expanding this topic into a deployment course. The next actual module topic remains Multi-Label & Multi-Output Learning.

## Mathematical and computational contracts checked

- Missing rating is distinct from an observed zero, an unobserved event target and a calibrated probability. All-missing users use a declared fallback.
- Co-rated support, per-person training centering, shrinkage, signed numerator/absolute denominator, deterministic neighbor ordering and fallback are explicit. A one-person cosine of one does not establish reliable similarity; the signed lab can legitimately exceed the rating scale.
- Simultaneous SGD uses one old state. Its sum of regularized record losses is degree weighted; observed ALS and implicit ALS use separately declared once-per-entity objectives. Rolling errors, fixed training RMSE, regularized loss and test error remain distinct.
- Positive regularization makes each fixed-side least-squares block unique; nonincreasing coordinate objective is not a global-recovery claim. The rank-one completion counterexample and rotation invariance distinguish score and coordinate identifiability.
- Implicit sparse corrections retain the complete missing-pair Gram term. The plotted curves are calculated level sets of the same quadratic, sampled for drawing; their axes, minimum and excess levels are explained.
- BPR's assumed triple, stable softplus derivative, simultaneous updates, local penalty counting and sampler-dependent target are stated. Pairwise scores are not click probabilities or universal top-K guarantees.
- Eligible labels, missing candidates, K, empty relevance, fixed-slot precision, min(K,R) AP normalization, NDCG ideal and retrieval Recall@K ceiling are explicitly matched to the native evaluator.
- The optional policy example enumerates a known randomized two-action logger. Assignment propensity, positivity, fixed target, expected value, finite realization and variance are separate. Arbitrary click logs are not asserted to identify a causal effect.

`scripts/verify-recommender-native.py` passed **12,771 independent numerical assertions and all fourteen actual displayed programs** against the final source set. Its complementary oracles include five-point gradients, NumPy augmented least squares, explicit quadratic values on every plotted contour, exhaustive five-item ranking permutations and exact Fraction policy moments. The exported case inventory contains 54 feedback states, 108 neighborhood configurations, 100 explicit updates, 486 implicit blocks, 100 pair updates, 25 rotations, 2,160 ranking configurations and 105 policy states. Invalid dense/sparse/inherited-array inputs, duplicate/ineligible recommendations, zero regularization and unsupported numerical weight range are rejected in focused contract checks. Sixteen actual trace cases include no-step and divergent-step behavior.

Changed practice executes the actual gradient, BPR and ranking helpers with different values, plus an independently solved changed implicit system and exact policy arithmetic. The complete experiment executes both the original and changed cutoff. In the first tiny test, the shrunk-item baseline actually beats MF; that result and its small-sample limitations are shown rather than edited into a favorable benchmark.

`scripts/generate-recommender-examples.py` formats complete programs with Python AST conservation, executes every block and writes its actual stdout. The two optional libraries were actually run in the recorded environment, including implicit 0.7.3 and scikit-surprise 1.1.5. The implicit recalculated user was compared with a direct confidence-weighted normal solve; current factor axes and filtering are checked. Surprise predictions use their own settings and actual cold-ID behavior.

## Actual browser and reading review

`scripts/review-recommender-lesson.cjs` passed at **1440, 390 and 320 pixels**, with **21 changed mechanism states, 61 keyboard actions, fourteen real section-anchor arrivals, fourteen exact question/code/output pairings, and fourteen fitting display equations per width**. Space Grotesk and JetBrains Mono loaded from the actual public font path. There were no captured page/console errors or document overflow. Code blocks intentionally retain horizontal scrolling for complete source/output.

The author opened final ordinary-reading and mechanism images, including the timeline, both rank-one completions, the serving flow, missing-feedback matrix, signed-neighbor support, simultaneous-update table, divergent objective, factor rotation, implicit contours, pair gap, candidate-limited slate and unsupported policy. All five repaired narrow equations were opened. Changed-gradient and changed-capstone hint/solution flow, the actual complete experiment output and the desktop lesson route were also inspected. Exact image paths and hashes are in the packet; generated-but-unopened captures are not counted as visually reviewed.

The review found and repaired two actual visual/accessibility issues: select accessible names included option text, and a large divergent-objective tick exceeded the SVG. Five mathematically equivalent formulas were reflowed after measured 320px width failures. Test-harness mistakes involving numeric CSS IDs, this repository's div-based code renderer and the order of exported example records were corrected as harness issues. Final checks ran on the repaired production sources; earlier partial runs are not completion evidence.

## Reproduce and interpret

From the repository root, run `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/generate-recommender-examples.py`, then `scratch/lesson-tools/Scripts/python.exe -X utf8 -I scripts/verify-recommender-native.py`. The browser script uses the shared local route on port 5173, Edge/Playwright and original fonts. The semantic formatting script checks normalized AST conservation. The finalizer binds native/browser hashes and the explicitly opened images; it refuses to overwrite an existing freeze.

The primary-source ledger states actual inspected scope, including Hu–Koren–Volinsky equations 3–5, BPR's objective/sampling/factor derivatives, Schnabel's selected estimands and IPS argument, current library source/API checks, selected Stanford similarity slides and the official historical YouTube abstract. The public Stanford lecture listing is an optional spoken route; no unperformed full-video review is claimed. This author packet establishes a self-contained, tested teaching implementation on declared fixtures, not mastery of every recommender architecture, guaranteed deployment success or final independent acceptance.

## Independent-review fallback amendment — 2026-09-11T14:41:11.182830+00:00

The independent reviewer found that the displayed `neighbors` Python helper returned NaN for a wholly missing target-user row, although the body and JS model specify a training-global mean and then a declared prior. The original frozen packet, actual examples and generator are preserved under `archive/recommender-fallback-before/`. The corrected helper computes explicit observed counts, preserves an observed zero, and falls back to the training global mean (2.4 in the changed fixture) or declared prior3 for an entirely empty matrix.

Focused native checks pass with warnings treated as failures. All14 printed outputs and the other13 example records are unchanged. Actual-font Edge checks at1440/390/320 confirm the exact amended code and unchanged output; the final390 capture was opened and inspected. Code remains intentionally horizontally scrollable. No body/model/lab/style change and no unrelated broad-suite rerun. Final examples SHA256 `139e958776f519010fa77e7a4c9226e53f3dfaa9951f53894422e26c53b93556`. Exact amendment and source-versioned evidence: [packet](evidence/recommender-fallback-amendment.json). The original full author tests retain their original source attribution.
