# Rate-Distortion Theory — author verification

Stable identity: `rate-distortion-theory`, Mathematics28. The [individual design](RATE-DISTORTION-LESSON-DESIGN.md) records scope, concept hurdles, sources and representation contracts. [Durable author evidence](evidence/rate-distortion-author-review.json) owns the exact final source fingerprints and actual native/browser results. Author verification is complete; independent review, production integration and user acceptance are separate.

## Teaching changes and preserved material

The complete original seven-section lesson is archived with SHA-256 `a4276ba8dd57bd20477e25c7dd39d817d67ae9a21218f9b97f7409eecf324514`. Its binary-entropy Python program and four output lines are retained exactly. Its limited distortion domain is now explicit: extending the positive part of 1−H₂(D) beyond D=.5 would incorrectly make the binary curve rise again.

Nine navigable sections progress from an actual three-bit encoder/decoder to probability constraints, binary limits, the coding theorem, finite-alphabet optimization, Gaussian distortion allocation, application fidelity, actual bytes and independent practice. Definitions precede terminology. The body derives the biased binary backward channel, distinguishes fixed-block exact zero error from the entropy boundary, explains random-coding achievability under its assumptions, derives the finite optimizer's lower bound, and proves the Gaussian construction and allocation rule. Optional depth retains assumptions rather than hiding necessary reasoning.

Four investigations have distinct jobs: select a block and inspect its reconstructed bits; change source bias and read the calculated rate/error frontier and conditional errors; inspect successive conditional-probability matrices and objective bounds; allocate Gaussian error across variance reservoirs. Three inline figures show the transmission boundary, identical output marginals with different fidelity, and transform/quantize/encode/decode responsibilities. Seventeen equations use local notation and logical line breaks. None of the graphs is presented as an empirical benchmark.

Seven complete Python programs include visible prediction questions, actual stdout and interpretation. Two checkpoints and ten independent tasks change codebooks, probability laws, endpoints, distortion units, initialization, component variances and serialization, then develop an audio evaluation plan. The bitstream example really writes a four-byte length header and packed block indices; it contrasts payload rate with total bytes. Rare-event costs, independent perceptual samples and learned-codec losses are concrete mechanisms with stated limitations, not unsupported application lists.

The original title remains appropriate. The new ADC connection belongs to the existing `sampling-aliasing-and-quantization-in-neural-recordings` topic; a [destination note](topic-notes/sampling-aliasing-and-quantization-in-neural-recordings.md) records the high-resolution approximation, clipping and independence qualifications for that author. No other lesson or curriculum order was rewritten.

## Native and independent numerical evidence

Final command: `node scripts/verify-rate-distortion-models.mjs`. It executes the actual displayed Python programs and a separate SciPy/Fraction-style numerical oracle through Python3.12.14, NumPy2.3.5 and SciPy1.18.1. Full timestamps and results are embedded in durable evidence.

| Contract | Actual checks |
| --- | --- |
| Binary law | 861 source-probability/distortion states, including deterministic sources, zero error and flat zero-rate budgets; independent entropy, marginal and distortion comparisons. |
| Actual small codebooks | Every one of255 nonempty three-bit codebooks under five iid source laws:1,275 states with deterministic nearest-word tie handling, average error, supported worst case and rate. |
| Finite optimization |31 scenarios checked against separately solved constrained objectives, including rectangular alphabets, zero source masses, equal costs, missing output support, tiny positive initialization, common cost offsets and sharp costs. Jensen lower bounds bracket the independently solved objective; traces decrease their feasible upper objective. A stuck zero column retains a nonzero gap. |
| Gaussian allocation |147 browser-model states and90 independent constrained allocations. Numerical tests supplement the derivative/KKT derivation; they do not prove it for every input. The one-bit Gaussian quantizer's distortion was independently integrated as3.2704220486917683 for variance9. |
| Actual serializer |2,047 actual displayed-code executions cover every binary input of length0–10, actual reconstruction, declared length, wire size and error limit. Three malformed byte cases reject, including nonzero padding. The changed two-bit practice format is independently calculated. |
| Complete examples |All seven actual program strings execute and match stored stdout. New Python formatting conserves AST; the original program is excluded from reformatting. Changed biased-channel, Gaussian and codec practice values are checked. |
| Numeric contracts |18 invalid JS cases reject. Eleven actual Python Gaussian endpoint/range cases cover zero budget, all-zero variance, invalid values, unrepresentable allocations and valid tiny positive allocations. Three accepted tiny JS allocation cases are also checked. |

There are17,023 ordinary numerical comparisons in the final oracle, plus the explicit endpoint assertions. Final local Node timings for ten bounded finite-optimizer runs are stored as measurements of that process, not a browser or production latency promise. Browser updates use at most120 precomputed steps; no timer, continuous animation or background training loop was added.

Independent review exposed an additional representability issue: dividing the smallest positive floating-point budget among two positive-variance components rounded their allocated errors to zero. Both the JS helper and actual displayed Python function now reject an unrepresentable positive allocation and advise rescaling. Zero-variance components are skipped before dividing the active budget. Positive subnormal allocations that remain representable stay finite. The Python example now solves the piecewise-linear budget equation instead of using a fixed-count bisection that could miss a very small budget, and computes log differences instead of an overflowing variance/error ratio. Exact zero remains a separate mathematical endpoint. Ordinary learner controls are unaffected.

## Browser, visual and reading evidence

`node scripts/review-rate-distortion-lesson.cjs` passed on actual Edge152 at1440×1000,390×1000 and320×1000 after the numerical repair. Each width exercises21 meaningful control states and34 keyboard controls, all four codebook presets, biased/deterministic binary sources, allowed budgets above the zero-rate threshold, optimizer stepping/back/reset/cap and missing support, changed distortion costs, and Gaussian zero/large/unequal/equal budgets. Independent native checks establish model accuracy; browser assertions establish that the intended state is actually displayed.

All seven visible program bodies and output strings match their data. All12 checkpoint/practice disclosures operate with the keyboard. The nine contents links match the real section IDs; this harness does not claim to click every anchor. All17 rendered equations fit, and both SVG plots retain at least14px rendered labels without clipping. Wide code and data tables scroll locally; actual keyboard scrolling is checked where applicable. Page overflow, KaTeX errors, invalid paragraph nesting, application errors and failed requests are absent.

The final focused command `node scripts/review-rate-distortion-copy.cjs` checks the final prose amendment and repaired native program at all three widths, including all seven exact visible questions immediately before their correct programs, actual code/output, the Gaussian maximum-entropy explanation and the audio task. It explicitly requires loaded Space Grotesk and JetBrains Mono. Public font access was authorized for these local checks. The deliberately closed development HMR socket is recorded separately; arbitrary errors are not ignored.

Ordinary reading captures include every section introduction, all inline figures, initial/changed investigations, equations, practice and sources. Durable evidence distinguishes saved captures from the particular screenshots actually opened and visually inspected: all three inline figures, the codebook, biased frontier, weighted optimizer, Gaussian reservoirs, selected reading sections and equations, the corrected Gaussian proof, complete Gaussian program and final audio exercise. No full novice study or full screen-reader session is claimed. The final inline and program captures were opened after the last full browser run.

Review repaired several narrow-screen equations and unbroken arithmetic expressions with semantic line breaks and explained notation; font sizes were not reduced to hide overflow. Final formatting separately checks JavaScript AST/string and CSS-rule/value conservation. Files remain owned by topic/purpose with a directly imported math renderer and no new dependency, global eager registry or route change.

## Source scope and remaining limits

The design's source ledger records actual MIT6.441 chapters23/24, CMU10704 lecture16, the reverse-water-filling equations in Qian and colleagues'2024 paper, Blau/Michaeli2019 sections2–3.2 and Ballé/Laparra/Simoncelli2017 sections2–3 plus its coding appendix. Claims are limited to the inspected passages and independently derived mathematics. The correct2024 authors are recorded; no paper's empirical benchmarks were replicated. The NPTEL course creator and lecture metadata are verified; no full video playback or transcript review is claimed. Failed Stanford/older archive requests are not counted as completed reading.

The finite optimizer gives a numerically evaluated objective-gap bound for its fixed source and distortion matrix, not an interval-arithmetic proof, guaranteed iteration count or implemented universal compressor. The finite code and asymptotic information law have deliberately different contracts. Gaussian statements require the specified source, squared error and rate units. Matching a perceptual marginal does not preserve the actual input, and the cited2019 tradeoff paper does not by itself establish every operational achievability theorem. A learned latent, surrogate loss or KL term is not actual wire length.

Root integration and the independent review record own subsequent completion state. Learner acceptance remains pending.
