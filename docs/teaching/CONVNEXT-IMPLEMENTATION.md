# ConvNeXt & Modern CNN Designs — prepared implementation

22 September 2026. Stable ID `convnext-modern-cnn-designs`, Deep Learning position12. Author: `convnext_finish`. Finish preflight passes the complete revision3 content checkpoint. Author/native stage complete; independent review and root-owned final browser/integration remain pending. This is not a new curriculum queue or a completion declaration for the ledger.

## Preserved teaching and phase boundary

Read the entire current manuscript, design, visual specifications and all four supplied Python programs. The production body is a static rendering of the complete prepared manuscript: ten main sections, named V1/V2 block/hierarchy, normalization reductions, bias-aware cost derivations, residual/drop semantics, GRN equation/initial gradients, masked-encoder boundaries, all six recorded fits, frozen probes, diagnostic limits, checkpoint/storage contracts, branch fusion, hybrid context, nine changed-design exercises including expansion practice, hints/solutions and annotated alternatives. No advanced branch was replaced with a summary. `scripts/generate-convnext-lesson.mjs` identifies every visual replacement and changed execution paragraph, fails if an anchor is missing/duplicated and uses the existing authoring-only renderer. No runtime Markdown parser was added.

The prepared manuscript/program/data bytes remain unchanged. Public code/data copies are byte-identical. Previously measured training fits are conserved rather than rerun; that is explicitly allowed by this packet's phase-two specification. Fresh checks reconstruct the saved models natively and through independent NumPy/JavaScript routes. The old visual-spec Apply/prediction remnants are superseded by its21September live contract and current teaching standard. No guess-entry, learner-prediction or optional answer-unlock feature was implemented.

## Mechanisms and representations

| Placement | Implemented teaching | Live controls and null |
| --- | --- | --- |
| §1 | Nonmonotonic historical experiment genealogy, sourced scores and rejected kernel branches | Static published evidence; no fabricated timings or architecture bonuses |
| §2 | V1/V2 operator ribbon, explicit shapes and saved residual path | Switch topology; GRN stays at expanded4C, V1 scale sits after projection |
| §2 | Two-location, three-channel reduction lattice with exact mean/variance and changed-output markings | Actual cell edits, channel versus whole-specimen grouping; local+10 shift null under channel LN;101→105 contrast |
| §3 | Common-scale56/28/14/7 spatial grids, separate widths and repetitions | Width/expansion/kernel/map dimensions/version edit exact components and MAC shares; doubled sides quadruple work, not parameters |
| §4 | Channel maps, norm lengths, shared denominator, signed response scales and output maps | Cell/gamma/beta edits; other-channel contrast, all-zero input and zero-scale identity |
| §5 | Separate model-input and original-target paths | Labels explicitly belong only to the later supervised probe |
| §6 | Four linked8×8 views of real saved specimens with actual bounded inference | Row/column selection plus live intensity slider/number; model/specimen selection; two-endpoint patch swaps preserving six visible; hidden/visible contrasts, reset and exact-value tables |
| §6 | Recorded reconstruction traces with data markers/table, separate0–120 probe bars and diagnostic table | Select paired seed to inspect existing results; not a training control |
| §7 | Editable kernel stencil and independently evaluated branch/folded routes | Kernel, BN coefficients/statistics and input cells; explicit branch-local-ReLU negative case with negated second input; exact equality in compatible linear cases |

Pixel cells are pictures, not undersized slider/button affordances; full-size row/column controls select a white-outlined pixel. Number/range controls retain the last valid result and label invalid input. Matrix buttons and all actual controls follow the shared keyboard semantics. CSS uses neutral near-black/charcoal with amber, scoped SVG geometry only; no descendant rule reaches KaTeX. Figures and labels reflow; exact tables/code have named horizontal-scroll regions. Browser geometry, focus and touch checks are still owed by integration.

## Scratch, library and execution

Public assets live at `public/learn-assets/convnext-modern-cnn-designs/`. Programs open on demand next to their explanation. `convnext-blocks.py` constructs the complete hierarchy using transparent torch primitive layers. `masked-reconstruction.py` contains all actual reconstruction learning/probing. `convnext_library_bridge.py` copies same-state weights into TorchvisionCNBlock, including `[C]`↔`[C,1,1]` LayerScale mapping. The Python programs keep scratch mechanisms at the owned architectural abstraction; convolution indexing/autograd are earlier owners.

Executed:

- Public `convnext-blocks.py`: ten meta configurations, two actual V1/V2 block forward/backward checks; complete output structurally equals the retained prepared record.
- Public `author-checks.py`: independent NumPy HWC loops and exact-erf GELU reproduce all four saved specimens, including hidden/visible/swap interventions, normalization, GRN finite differences and branch folding. Complete output structurally equals the prepared record.
- Public `convnext_library_bridge.py`: Torchvision0.29.0/PyTorch2.14.0+cpu, float64 outputs/input/all trainable gradients pass declared1e−12 absolute/relative tolerances. No pretrained weights downloaded.
- `scripts/verify-convnext-native.py --library-path scratch/convnext-native-deps`: two extra seeds with large signed LayerScale prevent a near-identity check hiding branch error; maximum output difference0. GRN Jacobian finite differences and other-specimen independence pass. Twenty-four fresh native fixtures cover both models, both specimens, zero/one extremes, edits and patch swaps.
- `node scripts/verify-convnext-models.mjs`: all1536 native output values and24 losses agree. Maximum output difference4.092e−7, maximum MSE difference9.677e−8, both below1e−5. Every hidden edit preserves reconstruction exactly while changing target loss.144 parameter/work cases and27 folding extremes also pass, plus independent fixture comparisons. Numerical guards reject wrong/empty/nonfinite subjects.
- Static JSX compilation and server rendering:256KB HTML, ten main sections, five lab roots,21 disclosures, no KaTeX error or object-string artifact. This is not browser-paint evidence.

Current evidence: [native](evidence/convnext-native.json), [models](evidence/convnext-models.json), [public execution record](../../public/learn-assets/convnext-modern-cnn-designs/native-verification.json). Model evidence records exact current source digests and bounds. Re-run affected checks after changes; do not reopen unchanged six-fit training.

Current primary API recheck: [Torchvision0.29 ConvNeXt](https://docs.pytorch.org/vision/0.29/models/convnext.html), [maintained CNBlock source](https://github.com/pytorch/vision/blob/main/torchvision/models/convnext.py) and [official V2 utilities](https://github.com/facebookresearch/ConvNeXt-V2/blob/main/models/utils.py). This supplements the prepared full paper/code coverage audit, not a claim of a newly repeated literature survey.

Not executed: optional learner-photo/pretrained checkpoint route, full ImageNet training, native sparse FCMAE or hardware timing. Those are clearly labeled extensions; a complete library route exists even though that external photograph example is not an accuracy experiment. JS erf is an approximation, with end-to-end agreement established above. Node's local24-inference timing is recorded only as a bounded-work observation, not a browser performance claim.

## Semantic source ownership and integration handoff

- Topic body: `src/learn/data/topics/convnext-modern-cnn-designs.jsx`
- Blueprint: `src/learn/data/curriculum/blueprints/convnext-modern-cnn-designs.js`; root registers exact catalogue title **ConvNeXt & Modern CNN Designs**. It is already published: preserve manifest identity and counts.
- UI: `src/learn/components/lesson-labs/ConvNeXtLabs.jsx`, `convnext.css`
- Pure mechanisms and compact recorded metrics: `src/learn/data/convnext-models.js`, `convnext-measurements.json`
- Generator/native/model verifiers: `scripts/generate-convnext-lesson.mjs`, `verify-convnext-native.py`, `verify-convnext-models.mjs`
- Downloaded source/data and original full results remain at the topic-owned public path. Saved model tensors are fetched only on this lesson, never through navigation/blueprint imports; long programs only when their disclosure is opened.

Root owns final registry/generated metadata/build/browser and ledger updates. Required browser coverage: default/meaningful edits/null/extremes/reset for each of the five `data-lab` IDs (`convnext-normalization`, `convnext-budget`, `convnext-grn`, `convnext-mask`, `convnext-fusion`); source disclosure loading/error retry; saved-model loading/error retry; rapid slider edits; patch-swap incomplete/complete states; model/specimen changes; phone/desktop graph and KaTeX image inspection; keyboard controls, exact tables and theme. Parent should inspect saved-model request size and response timing on the actual route, not infer browser efficiency from the Node check.

The isolated `scratch/convnext-native-deps` target contains only Torchvision installed with `--no-deps`; it was necessary because shared lesson-tools lacks Torchvision. Approved installation created Windows ACLs unavailable inside the sandbox, so native execution used scoped elevated access. Retain until independent review, then verify its absolute path is within repository scratch before deleting it; leave the shared Python runtime and unrelated scratch files alone.

## Independent and final browser closure —22 September2026

Root completed the [independent review](CONVNEXT-INDEPENDENT-REVIEW.md) and complementary native checks. Its continuous-input finding is repaired with `step="any"`; architecture dimensions remain integral. The final production build on4194 passed the shared modern-convolution browser protocol: **17 groups across Depthwise/ConvNeXt,12 retained viewport captures at1366/390/320**, source-bound in [browser evidence](evidence/modern-convolution-browser/report.json). ConvNeXt coverage includes current results, normalization null/contrast, invalid retention, real slider pointer/keyboard events, exact budgets, GRN controls, actual hidden/visible reconstruction edits, off-step pixel/control parity, incomplete/completed visibility swaps, model/specimen changes, branch-folding counterexample, deferred source/model errors and retry, canonical source bytes,44px coordinate controls, theme and intact rendered math.

The author visually inspected all twelve representative desktop/phone view types across development and final production captures; the four final narrow-view screenshots were inspected after the last build. Scoped tables scroll within the reader; diagrams, labels, neutral surfaces and full-size editing alternatives remain readable without document overflow. Root owns shared integration/ledger closure. These checks supersede the earlier reserved browser work above and remain bounded evidence, not a claim that every future device/input is perfect.

The temporary task-only Torchvision target was deleted after native and independent closure. Its exact resolved scratch path was checked before removal; existing Python runtime, source/downloads and durable evidence were preserved.
