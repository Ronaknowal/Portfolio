# K-Means revision review — complete

Completed 21 September 2026 under the user's explicit request to review the pending K-Means revision. Scope is `k-means-hierarchical-clustering`, Classical ML position 11. The proposed 12 September revision is now independently reviewed, corrected and production-integrated. It retains fourteen sections, seven inline figures, five distinct investigations, six complete Python programs and ten practice tasks. This closes technical implementation review; user acceptance remains separate.

The [incoming snapshot](evidence/k-means-revision-review-baseline.json) preserves the nine incoming runtime sources, design and all 177 ledger rows. The [original design](K-MEANS-HIERARCHICAL-LESSON-DESIGN.md) remains a dated account of the prior completion and proposal. This full-mode revision uses the [integrated lesson](../../src/learn/data/topics/k-means-hierarchical-clustering.jsx) as its manuscript and the [retained visual/teaching contract](K-MEANS-REVISION-VISUAL-CONTRACT.md) as its specification. The current phase ledger records revision 2, preserves revision 1 as history and binds the actual completed sources. The historical eight-source module ledger is unchanged.

## Substantive findings and corrections

| Finding | Final behavior or explanation |
| --- | --- |
| Joint Old Faithful grouping was presented as evidence of improved forecasting even though the subsequent wait helped determine the groups. | The measured within-group spread is explicitly descriptive. A separate duration-only assignment, development-owned summary and held-out evaluation protocol explains how to investigate forecasting; no unexecuted forecast result is claimed. All 272 observations and recorded fits remain. |
| Several investigations exposed the answer before prediction or let an already graded choice be edited. | Input geometry remains available, but future trace length, optimum partitions, probabilities and hierarchy results are revealed at the appropriate step. Checked choices lock; changed inputs and reset start a fresh commitment. |
| Seeding feedback compared an exact probability category with the count from 200 random draws. | The question now asks for exact D² probability, with a separate finite-frequency display. A concrete state has probability 53.25% but only 90/200 draws; the distinction is taught and checked. Duplicate/all-selected stopping states and the remaining-row uniform comparator are correct. |
| Reaching the eight-color teaching cap was described as lossless for every image. | The six-color mosaic can be exact; the 37-color sky and 160-color gradient still have reconstruction error at the cap. Rounded RGB error, floating-center objective and weighted pixel counts stay distinct. |
| Some theoretical or dataset statements were too broad. | Corrected hardness assumptions, smoothed-analysis scope, finite Lloyd termination reasoning, Ward convention conversions, tied-height/count behavior, rounding provenance and the changed-unit exercise. Diagnostic improvements are not claimed to diminish at every step. |
| Tree leaf labels collided at 320 px; an undefined-silhouette annotation crossed the curve; Ward height and cut counts were easy to confuse. | Tree observation/cluster IDs occupy separate rows, the undefined k=1 annotation sits above the axes, and the Ward caption distinguishes √(2ΔSSE) from flat-cluster counts. Plotted values are unchanged. The lesson's prose matches the revised labels. |
| Minimal Python input checks and a stopped-seeding comparator had gaps. | Lloyd requires a positive integer iteration cap; the two mechanism programs require finite nonempty row-by-feature inputs. No-remaining-row uniform probability is zero. No general floating-point robustness guarantee is added. |
| Re-rendering could repeat calculations unrelated to a changed prediction or slider. | Bounded model results are memoized against their actual inputs. The lesson remains on demand with no new heavy dependency or global eager registry. |

## Independent assessment and executed evidence

- **[Content and learning review](K-MEANS-REVISION-CONTENT-REVIEW.md):** complete prose, code framing, claims, scope, examples and practice were read against the incoming revision and primary sources. The reviewer inspected seven informative-state images and three final amended images, and corrected the final one-paragraph tree-label description. Its [source-bound receipt](evidence/k-means-revision-content-review.json) records the reviewed source, exact resource access and limitations. This is an independent heuristic review, not an observed novice study.
- **[Model/native review](K-MEANS-REVISION-MODEL-REVIEW.md):** [159 grouped model cases](evidence/k-means-revision-model-review.json) independently cover seed pairs, all rectangle partitions, D² subsets and histograms, both linkage fixtures and palette configurations. [27 native/data checks](evidence/k-means-revision-native-review.json) reproduce the data, fitted values, diagnostic series and sampled Ward tree. The two changed complete Python programs were executed; four unchanged programs/results were reused only after identity comparison with their earlier execution. The independent [272-row source snapshot](evidence/k-means-revision-faithful-source.csv) is retained.
- **Production integration:** the final build and [15 production browser groups](evidence/k-means-revision-production.json) pass. They cover all five investigations and their meaningful edge states, keyboard operation, formula/control geometry, theme links, completion persistence, actual PCA succession, selected-topic loading and import/render failure recovery. Widths are 1366, 820, 390 and 320 px, plus 125% text at phone width. All advanced/practice disclosures are opened for the width checks.
- **Visual evidence:** the [development pass](evidence/k-means-revision-browser.json) retains 28 informative-state captures. Its images precede the final targeted label/annotation repairs. The [final visual production pass](evidence/k-means-revision-production-before-prose-sync.json) retains eight captures of the repaired figures/tree. Root opened informative images across all seven figures and all five labs; the independent reviewer opened the selected images listed in its report. The last change only synchronized a prose paragraph, after which production was rebuilt and all 15 groups rerun without replacing the inspected captures. Their visual component sources match the final revision.
- **Integration and conservation:** curriculum, generated-artifact currency, on-demand import boundary, route/artifact checks, phase-ledger validation and regenerated inventory pass. The [final reconciliation](evidence/k-means-revision-reconciliation.json) binds the current sources, reviews, production receipt and checkpoint; it asserts preservation of all 176 unrelated ledger rows, all 231 publication mappings and module sequence.

The browser sandbox blocks the existing Google Fonts endpoints. The test fulfilled those exact requests from unmodified downloaded font snapshots and asserted that Space Grotesk and JetBrains Mono loaded. Provenance and hashes are recorded in the browser receipts. Application font/network configuration is unchanged; fallback-font screenshots are not used as final layout evidence. Referenced font fixtures and evidence remain retained. Both review-owned servers were stopped and their disposable Vite cache was removed; the user's preview and unrelated files were preserved.

## Limits and continuation

These checks establish the reviewed learning material, bounded model fixtures and exercised browser behavior. They do not claim exhaustive assistive-technology certification, a real learner study, universal numerical robustness or measured device/network performance. Production payload counts are decoded built bytes and gzip estimates, not observed compression or latency. No commit or deployment was performed.

All 39 Classical ML topics now have current completed implementation checkpoints. The previous six-topic audit's 38-plus-pending count is a historical snapshot closed by this review. Forty-two prepared Deep Learning revisions remain content-complete and implementation-not-started. Neither their implementation nor another lesson is authorized by this review. Future work starts from the user's next request and the central phase ledger, without replaying unchanged historical checks.
