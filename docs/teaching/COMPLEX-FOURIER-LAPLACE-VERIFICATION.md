# Complex Numbers, Fourier & Laplace Transforms — author verification

Stable ID `complex-numbers-fourier-laplace-transforms`, Mathematics position 51. The final [author packet](evidence/complex-transforms-author-review.json) owns the exact six production fingerprints, freeze time and actual evidence. Author verification is distinct from independent review, parent production integration and user acceptance. The scoped [design](COMPLEX-FOURIER-LAPLACE-LESSON-DESIGN.md) preserves the decisions made before implementation and records deliberate adaptations.

## Preservation and learning scope

The entry was planned, with no prior published body or runnable programs. The [original-plan archive](evidence/complex-transforms-original-plan.json) preserves the exact inventory, original catalogue source and fingerprint, starting coverage and missing-body state. The title, stable ID, Mathematics position and the two existing prerequisites were retained. No shared registration, index, route order, progress ledger or other lesson was edited by this author. The parent separately registered the complete body when it became parseable.

The implemented route moves from two-coordinate arithmetic to rotation and synthesis, orthogonal projection, convergence, continuous transforms, exact finite DFT/FFT, sampling ambiguity, finite windows, convolution, filter response, Laplace regions, initial conditions and changed practice. It contains 12 reading sections, ten investigations, three inline figures, 17 independently runnable programs and 14 changed exercises with hints and explained results. Those counts describe this lesson; they are not future authoring quotas.

The essential local derivations are visible: coefficient isolation and finite least squares; the finite Dirichlet kernel and piecewise-C1 midpoint route; the square-wave moving-peak Riemann sum and mean-square error; Gaussian regularized inversion with explicit integrability assumptions; finite DFT inversion and Parseval; the radix-two recurrence; window-density normalization; time/frequency convolution; the initial-value boundary terms; shifted inputs and repeated poles. General L2 completeness/Plancherel and the ideal sampling theorem remain explicitly qualified extensions rather than facts inferred from a plot. The final response distinguishes steady periodic response from a transient chosen by the initial state, unilateral post-initial data from generalized pre-impulse data, and a rational expression from its bilateral ROC.

## Visual contracts and actual reading improvements

| Representation | Mechanism visible in the default state | Author validation |
| --- | --- | --- |
| Complex arithmetic | Equal-axis lengths/directions, conjugate-based division, translated addition and a distinct sum route | Python complex arithmetic and roots; zero vectors have no arrowhead; zero divisor is a visible undefined state |
| Phasor synthesis | Two frequencies rotate at different rates; their horizontal projections add to the waveform | Rotation-matrix and cosine checks; time/phase controls update linked arrows and traces; phase changes retain the first tone |
| Projection | Signal, two signed products, integral trajectory and final coefficient share one fixture | Independent quadrature at partial integration times and positive/negative/absent/DC harmonics; cursor does not recalculate the memoized trajectory |
| Square-wave convergence | Full jump neighborhood plus a moving-scale zoom distinguishes narrowing from persistent peak height | High-precision finite sums, first-peak stationarity, independent integrated squared error and coordinate checks; the zoom states its scaled time and side limit |
| Roots / pulse / FFT figures | Unit-circle roots; pulse-width versus sinc scale; explicit reused E/O routes and add/subtract butterfly | Exact powers, quadrature, DFT fixtures and actual final narrow images; butterfly routes are dependencies, not a quantitative geometric axis |
| Finite DFT | Tip-to-tail summands, complex bins, conjugate-pair filtering and inverse sample values | NumPy forward/inverse including odd/even and complex values; energy identity; applied drafts preserve active state on invalid text; tiny valid coefficients are distinguished from exact zero |
| Sampling | Distinct continuous waves pass through the same stored samples, with the negative-frequency phase reversal shown | Changed frequencies/rates/phases and Nyquist boundary; sample discrepancies independently checked |
| Windows | Actual observation and taper feed one continuous finite-record spectrum with DFT-bin markers | Supplied periodic Hann versus rectangle, observation versus padding, direct transform versus NumPy/SciPy, exact no-detrending density integral; cursor-only edits preserve cached curves |
| Convolution | Every product is tied to its output index; the wrapped tail is visibly highlighted | Independent linear convolution, modular folding and padded-FFT examples; output support and active boundary agree |
| Filter response | Total, periodic and transient parts are separate but linked at one selected time | Independent integrating-factor quadrature, changed initial state and rate, exact default rate preserved by the control |
| Laplace region | Support choice changes the shaded half-plane; finite integral and algebraic formula remain distinct | High-precision finite integrals near removable limits, right/left ROC checks, boundary and large divergent finite states; large axes carry an explicit multiplier |

The reading review added the phasor view before projection, made the roots circle explicitly labelled, replaced the initial text-only FFT layout with real dependency wiring, and added the scaled Gibbs view after seeing the high-cutoff peak compressed in a narrow screenshot. Other optional design controls were intentionally left fixed to preserve one clear fixture: projection amplitudes remain 2 and 1, Laplace decay remains 1, and the window investigation uses a single tunable tone. The domain comparison is an accessible table rather than another panel. The design records why these choices retain the actual learning jobs.

## Reproducible numerical and program evidence

Run `node scripts/verify-complex-transforms.mjs` from the repository. It exports fixtures from the real model and the real example data, then runs `scripts/verify-complex-transforms.py` with the configured Python. The [durable native result](evidence/complex-transforms-native-results.json) records exact model/example fingerprints, package versions, timestamps, counts and maximum scaled error. `scripts/generate-complex-transforms-examples.py` is the reproducible initial generator of the 17 complete programs and actual stdout; normal verification executes the saved final programs rather than replacing their output with a regenerated expectation.

The suite checks all 17 actual programs and output strings; changed actual direct-DFT/inverse/radix-two helpers (including rejected non-power-of-two sizes); changed pulse and filter integrals; changed delayed forcing; symbolic shifted-ramp practice, broken conjugate symmetry, changed convolution and non-real cube roots. Numerical model checks cover complex operations, roots, independently expressed rotation matrices, partial projection integrals, square-wave peaks/error/zoom, NumPy DFT/inverse/energy, phase-aware aliases, 54 window/transform/periodogram configurations, direct continuous window-frequency points, convolution products, 243 time-domain filter integrals, 175 high-precision finite exponential integrals, sided regions and invalid arrays/text.

The comparison tolerance is explicit: error is scaled by `max(1, largest expected magnitude)`, with the observed maximum recorded in the JSON. This is a finite numerical check, not an arbitrary-precision or all-input proof. Native libraries, browser floating-point operations and displayed decimal values have different rounding roles. Tiny numeric readouts and phase suppression are labelled as display conventions; they do not redefine a nonzero coefficient as mathematical zero. The bounded browser record has at most eight editable DFT samples, at most 128 actual window samples and 512 padded points, 64 square-wave terms and finite Laplace horizon at most 8.

Owned production JS/JSX/CSS was formatted with normalized AST conservation, including JSX text, rather than broad edits to unrelated lessons. See [format conservation](evidence/complex-transforms-format-conservation.json) and `scripts/format-complex-transforms-source.cjs`.

## Actual browser and screenshot evidence

Run `node scripts/review-complex-transforms-lesson.cjs` using the installed Edge/Playwright configuration. The final [browser record](evidence/complex-transforms-browser-results.json) checks actual public Space Grotesk fonts at 1440, 390 and 320 pixels and binds the six source hashes before and after the run. Intentional Vite HMR sockets are closed; actual page errors, warning/error messages and failed content requests are asserted empty rather than ignored.

Each width exercises 128 meaningful states, 36 disclosure activations and the 87 rendered native controls; it verifies all 17 visible program questions, code and expected outputs, all 12 section anchors, all 13 equation blocks, numeric readouts, reset, invalid-draft preservation, a tiny valid input, checkbox Space, range arrows, select arrows and keyboard-activated buttons. The plot-label geometry remains within viewboxes and the document/equations do not overflow. The actual 2π rate is checked numerically because browser input serialization can round its last decimal digits.

The author packet lists only the images actually opened with `view_image`, with their exact hashes. These include ordinary reading, the roots/pulse/butterfly figures, desktop complex/projection/synthesis views, mobile finite coefficients, the scaled moving peak, a padded Hann record, wrapped convolution, a changed filter response, finite Laplace states, tiny valid coefficients and an opened changed-practice answer. Capturing all the other images is not claimed as manually opening them. Both behavior and image inspection are necessary; passing a bounding-box script alone is not a visual review.

Issues found and repaired during author QA: one apostrophe-derived anchor mismatch; five overlong equations at 320 pixels; a decay slider whose step initially rounded the exact default; overly long large-number readouts; unnecessary arrowheads at exact zero vectors; a phase label that initially conflated suppressed tiny coefficients with mathematical zero; a too-compressed high-harmonic peak; and an FFT explanation that benefitted from explicit dependency wires. A pulse-example interpretation was corrected to say that the code checks the real quadrature while oddness gives zero imaginary integral. Initial test-harness strictness problems (an ambiguous output/status locator and exact decimal-string comparison) were corrected and are not presented as application defects. All final recorded checks passed after the repairs.

## Research, destination notes and limits

The full [design research ledger](COMPLEX-FOURIER-LAPLACE-LESSON-DESIGN.md#research-ledger-and-alternate-resources) records the portions actually read and their assumptions. Primary references include MIT teaching notes, Lebl's creator-authored convergence treatment, NIST's sine-integral analysis, Stanford's Gaussian/inversion notes, NumPy/SciPy's exact API conventions and the Illinois final-value statement. The lesson has annotated alternate written and video routes. The 3Blue1Brown companion and MIT transcript were read and video identities checked; no full-watch claim or invented timestamp is made. The measured package versions belong to execution evidence, not a claim that current web documentation uses the same release.

The originating [Real Analysis note](topic-notes/real-analysis-sequences-modes-of-convergence.md) now points to the actual midpoint/peak and finite verification, while leaving the wider convergence treatment to its owner. The [neural spectral note](topic-notes/spectral-and-time-frequency-analysis-of-neural-signals.md) keeps statistical PSD estimation and inference in the appropriate destination. Origin implementation does not automatically resolve either receiving author's assessment. No destination lesson was rewritten.

No observed beginner study, user acceptance, universal signal-recovery guarantee, statistical PSD guarantee or hardware benchmark is claimed. The parent owns final independent review, production build/loading and rollout integration.


## Independent-review amendments and final refreeze

The amended author packet is frozen at **2026-09-11T07:06:43.823923+00:00**. The [previous author packet](evidence/complex-transforms-author-review-before-independent-amendment.json) preserves the original six hashes, original native/browser results and eighteen previously opened image records. Exact original production sources are archived under `scratch/complex-transforms-before-independent-amendment/`. The current packet lists nine newly opened final images: both repaired investigations at all three widths and three narrow reading/Laplace captures. Earlier image inspection is attributed to the previous packet rather than represented as newly performed.

The independent reviewer found three repair areas. The finite least-squares paragraph now explicitly requires square-integrability. The actual displayed Python finite Laplace helper now uses a removable-limit series when |qT|<0.5 and rejects inputs beyond its stated |qT|≤64 arithmetic scope; exact zero retains the original return type. All seventeen prior stdout strings and every other program's code/metadata are conserved, as checked by `scratch/complex-transforms-verification/independent-amendment-conservation.json`. The native suite adds 112 actual-helper comparisons against 110-digit mpmath and three rejected-input cases; the complete suite passed at 06:59:43 UTC.

The DFT reconstruction can exceed the editable sample range after removing a conjugate pair, and the filter's transient can exceed the total-output control range. Their vertical extents now include all actually plotted series with a visible margin. Filter curves and extents remain cached when only the cursor changes. The reported eight-sample case reaches approximately 6.82843; the reported filter correction reaches approximately −5.54775. The final full browser run passed at 07:01:26 UTC with 128 states per width and geometric assertions on those exact reachable cases. All six final plot captures were actually opened. An earlier rerun was deliberately interrupted before restoring exact old stdout formatting; it is not counted as a passing final run.

The mathematical model, CSS and brief remain byte-identical to the previous freeze. Parent integration and the independent review's separate final record remain distinct from this author amendment.
