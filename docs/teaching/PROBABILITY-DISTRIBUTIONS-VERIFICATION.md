# Probability Distributions & Bayes' Theorem — verification

10 September 2026. Stable ID `probability-distributions-bayes-theorem`, mathematics position15. The [design](PROBABILITY-DISTRIBUTIONS-BAYES-DESIGN.md) records retained coverage, ownership decisions and actual primary-resource inspection. This is author and independent review evidence; production integration and user acceptance remain separate.

The finished body has nine connected sections, six concept-specific investigations, two immediate figures, nine complete independently runnable Python programs and eight changed practice groups. The original exact mean1.1/variance.49 and diagnostic example are retained with their actual assumptions, normalized event counts and complete solutions. The closing bridge preserves MLE/MAP as the next module topic.

## Executed examples and independent oracles

`node scripts/verify-probability-distributions.mjs` invokes the Python verifier through the local lesson-tools runtime. All nine displayed programs ran with Python3.12.14, standard-library imports and exact captured stdout. The verification oracle uses SciPy1.18.1 separately; neither Python nor SciPy enters the browser.

The final native record at13:45:02UTC is `scratch/probability-distributions-review/native/verification.json`. Companion `independent-oracles.json` records exact Fraction/set enumeration, latent-branch evidence enumeration, independent sequence/subset urn counts, mixture integrals and SciPy quadrature, PMF, survival and quantile checks. Cases:4,096 event sets;343 Bayes tables;1,715 paired-evidence states;686 urn states;750 mixed delays;2,520 arrival states;17 invalid-input cases. These finite checks complement the body derivations; they do not prove arbitrary numerical software correct.

Key checks include posterior19/217 for the original rates and19/27 after the changed prior; fresh/half-copied/exact-copy evidence; a density above1 with unchanged seconds/milliseconds area; separately represented point mass; exact changed practice answers; and a tiny Poisson upper tail checked directly against survival rather than cancellation-prone1−CDF. The fixed arrival realization is disclosed and reports “at least” when its finite supplied events cannot determine the full count. It is not an empirical frequency estimate.

## Actual browser and reading review

`node scripts/review-probability-distributions.cjs` passed at1440/390/320px, with final records through13:58:08UTC in `scratch/probability-distributions-review/browser/results.json`. All nine actual anchor destinations, six labs and meaningful controls/resets were exercised. Cases include impossible evidence, partial/exact copying, impossible paired observations, replacement/full-population draws, seconds/milliseconds, singleton atom intervals, all-atom distributions, zero observation windows and truncated event supply. All nine rendered code/output pairs match their source fixtures. Hints/solutions begin closed and were opened through keyboard controls. No page exceptions, KaTeX errors, SVG clipping or document overflow remained.

`node scripts/review-probability-distributions-reading.cjs` passed at14:03:52UTC at the same three widths. Twenty actual Tab stops per width have visible focus; ordinary figures, examples and section openings were captured; opened disclosures contain no invalid paragraph nesting. Wide tables scroll locally by keyboard:0 desktop,1 at390 and3 at320. Final `reading-results.json` has no application console errors. Deliberately blocked development sockets and denied external Google Fonts requests are separately recorded as environment messages, not silently discarded as lesson failures.

Actual images opened and inspected include all nine final390px ordinary section openings; final320px outcome controls and density/CDF;390px count PMF/CDF, mixed density, arrival timeline, equal-moment tails, normal-unit figure, Bayes area/table, first full native example/output and references. The earlier interaction figures were also inspected; final ordinary viewport captures establish readable size. Code retains local horizontal scrolling instead of shrinking the font or dropping lines. All equations were reflowed where needed down to320px.

## Findings closed and source freeze

The [independent source review](PROBABILITY-DISTRIBUTIONS-INDEPENDENT-REVIEW.md) read the complete body, all nine examples and relevant models/labs. Three findings were corrected: impossible evidence is zero total probability under the current model, rather than necessarily zero under both conditional classes; general CDF endpoints are limits; exponential rate and geometric success-probability conditions are explicit. No other mathematical defect was identified in that bounded review.

Author checks additionally corrected a CDF field mismatch, the final stair extension, a handwritten partial-copy test expectation, tiny-tail cancellation, narrow equations and mobile event controls. Semantic model checks passed after the mathematical repairs. Later formatting preserves normalized AST/JSX/template values in `formatting-results.json`; subsequent full browser checks cover the final rendering. No unnecessary repeated native run is claimed for AST-equivalent whitespace.

Six owned source fingerprints are recorded in `docs/teaching/evidence/probability-distributions-author-review.json`. Figures are calculated synthetic models with declared units and supports, not real diagnostic performance, traffic measurements or benchmarks. Primary written references and video metadata are annotated honestly; no full-video viewing, observed beginner study or user acceptance is implied.
