# Clustering Evaluation & Validation — content-first design

## Current live-exploration contract — 21 September 2026

This dated UX amendment supersedes earlier prediction-entry, grading, commit-to-reveal and prediction-retirement requirements in this document. There is no learner prediction feature, even optional. Historical evidence below records the earlier interface and remains history; it is not the current acceptance contract.

Edit coordinates and memberships and follow silhouette contributions; relabel or refine a candidate partition and inspect pair counts; construct binary labelings and inspect the exact chance baseline; reweight a frozen Iris partition and compare rescoring with refitting; change training multiplicities on fixed probes and follow aligned assignments. Valid edits synchronize outputs immediately; invalid ordered probe locations retain the last valid calculation with an explicit error.

Keep separate independent practice, model predictions, scientific validity checks and training/validation/held-out information boundaries. Meaningful valid control changes must reach the visible calculation and topic-specific diagram together. Natural algorithm Step/Back/Run actions remain where they expose a process; they must never require a learner guess. Reset restores a coherent initial state. A graph or number must not silently describe obsolete inputs; invalid inputs show an error and either clear invalid outputs or explicitly retain the last valid result. See [the current migration evidence](LIVE-EXPLORATION-CLASSICAL-EARLY.md) for implemented checks and limitations.


12 September 2026. Stable ID `clustering-evaluation-validation-silhouette-ari-nmi`, Classical ML position 13. Author: `scientific_visual_improvements`. Authorized work is **research and writing only**, stages 1–2. The live lesson, runtime support and shared catalogue remain unchanged. Parent owns the delivery ledger/checkpoint and cross-topic assessment.

## Contract and preserved baseline

Read all 778 lines of the published body, baseline SHA256 `c4cf5248af8b796b022fe3a7c83a8fcae64c5e22e2b7a94e6c791e135bf2d2bf`, the current instructions, topic CLI `--work content`, destination note and resolved routing inbox. Retain the existing title **Clustering Evaluation & Validation (Silhouette, ARI, NMI)**: its examples remain central and “validation” naturally includes the additional evaluation design. No identity/title/sequence change is proposed.

Previous: PCA & Dimensionality Reduction. Next: DBSCAN & Density-Based Clustering, followed by Anomaly Detection. PCA's representation, scaling and retained-dimension decisions are inputs to evaluation here; this lesson does not repeat eigendecomposition or fitting algorithms. Local readiness: read a row/feature matrix; average distances; know a hard partition and k-means assignment; use elementary fractions and logarithms. Derive pair counting and entropy locally. Suggested prerequisites for phase two are the exact catalogue K-Means and PCA topics; detailed inference/entropy lessons are optional refreshers, not concealed dependencies.

Practical finish line: produce a report that names the observation IDs, representation, evaluation population, reference/null or perturbation, reported scores and the decision those scores support. A beginner can compute a silhouette and an ARI, distinguish a relabeling from a changed partition, and explain a disagreement. An intermediate reader can compare frozen fits, inspect minority clusters and rejection coverage, and avoid selecting a pipeline with its final report. Deeper branches derive chance correction, directional information losses, null-reference k-selection and cost tradeoffs.

### Retention and repair map

| Existing material | Decision |
| --- | --- |
| Silhouette formula, per-point diagnosis, singleton convention, scale/shape cautions | Retain and make a real distance-fan + sorted-bar explanation. Remove universal .25/.5/.7 acceptance thresholds, centroid/boundary misdefinition and universal k=2 claims. |
| RI/ARI pair derivation and contingency table | Retain; correct malformed expected-RI formula. Derive fixed-margin chance correction via one pair, distinguish expectation from every random outcome. Keep RI as a useful descriptive agreement, not “nearly useless.” |
| NMI, geometric normalizer and AMI | Retain with arithmetic default declared, refinement example, zero-entropy cases, exact finite null. Repair “AMI strictly preferable”, k>10 rule and sample-free NMI=.47 assertion. |
| FM, purity, Hungarian mapping and imbalance | Retain in a connected diagnostic branch. FM is not chance-adjusted. Both point/information and pair scores weight groups through their observations, not equally. |
| From-scratch code, sklearn comparisons, Iris | Replace fragments and hardcoded printed answers with complete teaching programs and predicted/executed status. `rand_score` exists. Keep real Iris; fresh numerical probes, not the original unsupported stdout. |
| k sweep, algorithm-agreement heatmap, text-only silhouette trace | Preserve their jobs, use computed real-data panels, common-ID co-membership and actual silhouette bars; no unsupported mixed-dataset matrix or duplicate agglomeration methods. |
| Cost/sampling discussion | Distinguish exact chunked distances, subset silhouette, focal-point sampling against the full population and centroid proxy. AMI's expectation sum has extra cost. |
| Stability, application validation and label-free alternatives | Expand from passing references: same-ID resampling, one-cluster null, discovery/report separation and a precise frozen-protocol code path. |

## Learning and representation map

The full manuscript is [lesson.md](drafts/clustering-evaluation-validation-silhouette-ari-nmi/lesson.md); the implementable contracts are [visual-specifications.md](drafts/clustering-evaluation-validation-silhouette-ari-nmi/visual-specifications.md).

| Hurdle | Mechanism and support | Evidence opportunity |
| --- | --- | --- |
| What does “good” mean? | Real Iris two-versus-three result and four questions; first-pass route | Select evidence for an explicit task, not a universal winner |
| Point versus average geometry | Six points; selected point's distances, foreign-group means and sorted silhouette bars | Record sign prediction, edit a coordinate/membership, compare calculated result |
| Names versus membership | Eight IDs moving between bins; pair and contingency views | Rename, swap, refine; predict invariant score versus changed score |
| Chance and MI | Exact 70 fixed-margin allocations; overlap histogram; refinement entropy tile split | Commit expected direction, construct a partition, inspect actual null and normalization |
| Representation versus refit | Four-feature Iris, scaled/PCA/whitened alternatives | Distinguish rescoring one partition from changing the fit; full-rank unwhitened null |
| Stability of what? | Weighted resample chips feeding a fixed-probe membership strip | Learner changes multiplicities, compares common IDs, explains the one-group null |
| Rejection changes denominator | Full-ID lane versus retained-ID lane | Compare coverage and conditional score on explicitly named subsets |
| Selection versus final evidence | Development/selection/frozen-report lanes | Changed-context report and leakage repair |

No rendered assets, interaction engines or browser checks are produced in this phase. Specs require initially unset committed predictions, learner-entered quantities/memberships, contrast and null cases, accessible exact tables and narrow recomposition. Quantitative figure fit is a phase-two obligation.

## Real data and author calculations

The offline [iris.csv](drafts/clustering-evaluation-validation-silhouette-ari-nmi/iris.csv) contains the 150-row `sklearn.datasets.load_iris` copy from scikit-learn 1.9.1; stable row IDs 0–149, four measurements in cm and species codes. The [provenance](drafts/clustering-evaluation-validation-silhouette-ari-nmi/data-provenance.md) records UCI DOI/license and corrected-loader distinction. DBSCAN's author agreed the same ordering, features, species map and coverage vocabulary; each lesson remains offline runnable.

Small content-stage calculations were executed with Python 3.12, NumPy 2.3.5, SciPy 1.18.1 and scikit-learn 1.9.1: six-point silhouette variants and CH/DB; five eight-ID partitions; all 70 balanced relabelings to establish the exact null shape; 16 Iris representation/k combinations; four small exact one-dimensional resample fits; rejection-population contrast. Results and scope are [author-calculations.json](drafts/clustering-evaluation-validation-silhouette-ari-nmi/author-calculations.json). These are calculations substantiating written claims, not an implementation/native/browser campaign. Manuscript programs are complete; their full stdout verification remains phase two unless explicitly identified as the same calculation.

## Canonical coverage check and research ledger

All links retrieved/inspected 12 September 2026. Reading means the named substantive passages, not every page of a whole book. No video playback is claimed.

| Source and inspected locator | Scope used and decision |
| --- | --- |
| Zaki & Meira, [book contents](https://dataminingbook.info/toc/), chapter 17, and [author slides](https://www.cs.rpi.edu/~zaki/DMML/slides/pdf/ychap17.pdf): external/internal/relative section structure; contingency/matching; Dunn; gap; stability | Canonical breadth map. Matching, pair and information families, internal geometry, tendency/relative selection and stability retained. Dunn and matching get a concise deeper comparison; graph cuts/modularity belong to Spectral Graph Theory. Cophenetic hierarchy fidelity receives a short deeper branch and exact three-point example in section 11; the existing hierarchy body was inspected and does not teach that evaluation measure, so it is not falsely routed as already covered. Inspect critical formulas independently: the slides themselves contain apparent direction/notation slips in graph-score summaries; not copied as authority. |
| [Scikit-learn clustering evaluation guide](https://scikit-learn.org/stable/modules/clustering.html#clustering-performance-evaluation), metric families and definitions | API/definition cross-check; no “one metric always wins.” Per-metric claims use corresponding API/source below. |
| [silhouette_score](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.silhouette_score.html), [silhouette_samples](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.silhouette_samples.html); actual installed `_unsupervised` functions | Cardinality/metric/subset and singleton contracts; source confirms chunked exact distances and subset-before-calculation. Use a fixed supported finite dissimilarity model. |
| [normalized_mutual_info_score](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.normalized_mutual_info_score.html); installed `_supervised.normalized_mutual_info_score` | Arithmetic normalizer, both-constant versus one-constant distinction. Briefly record other normalizers instead of mixing them. |
| Vinh/Epps/Bailey [2010 paper](https://jmlr.org/papers/volume11/vinh10a/vinh10a.pdf), §§3–4 and fixed-margin expectation discussion | Retain normalization versus chance distinction and VI connection; state the null, not “random means zero in each run.” Arithmetic AMI is default chosen here; no universal superiority or constant inflation number. |
| [AMI API](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.adjusted_mutual_info_score.html), parameters/return/notes | Upper bound, possible negative values, convention and extra computation; no invented tight global lower bound. |
| von Luxburg [Clustering Stability: An Overview](https://arxiv.org/pdf/1007.1075), contents and §§1–2, pp1–7 | Different perturbations, common observation comparison and trivial stability; asymptotic idealized-versus-Lloyd results are optional further reading, not necessary to compute the local experiment. |
| Ullmann et al., [validation framework](https://arxiv.org/html/2103.01281v2), introduction and §§2–3 discovery/validation distinction | Separate exploratory selection from frozen report; adapt terminology to this site's train/selection/report vocabulary. No biomedical efficacy claim. |
| [R cluster::clusGap manual](https://stat.ethz.ch/R-manual/R-devel/library/cluster/html/clusGap.html), null distribution, `d.power`, `Tibs2001SEmax`, SE formula | Gap deeper branch; explicit squared-distance dispersion, simulation factor and first qualifying k. Original [gap paper](https://hastie.su.domains/Papers/gap.pdf) located but scanned text extraction returned no content; do not claim full-paper reading. Maintainer manual resolves exact convention. |
| [UCI Iris](https://archive.ics.uci.edu/dataset/53/iris), variable table/license/source corrections; [load_iris](https://scikit-learn.org/stable/modules/generated/sklearn.datasets.load_iris.html), dimensions and version-0.20 correction note | Four cm-valued features, 150 observations/3 species, CC BY4; use corrected sklearn copy and disclose version. |
| Berkeley Data100 [lecture 24](https://ds100.org/sp21/lecture/lec24/), item 24.5 and linked [companion notebook](https://ds100.org/sp21/resources/assets/lectures/lec23/lec23.html), cells74–79 | Helpful video/companion alternative for silhouette and k-selection. Actual companion code/results inspected: two/three-cluster petal-space silhouette contrast and sorted bars. Adjacent old notebook dendrogram uses synthetic heights; resource annotation directs only to 24.5 and warns against reusing that earlier construction. Playlist URL read from official lecture page; no claimed playback/timestamps. |
| [SciPy cophenet](https://docs.scipy.org/doc/scipy/reference/generated/scipy.cluster.hierarchy.cophenet.html), definition/return values and single-linkage example | Whole-hierarchy distance fidelity, distinct from a selected cut; a new three-point local calculation explains the lost distinction and undefined constant-correlation case. |

## Incoming note and boundaries

The [destination note](topic-notes/clustering-evaluation-validation-silhouette-ari-nmi.md) is assessed and incorporated in this manuscript: fixed IDs before metrics, explicit chance model, minority inspection, same-reference stability, rejection coverage and no universal selection thresholds. It remains **open for implementation**, with content-stage disposition saved; “adapted” would imply implemented work and is not used yet. Hard partition metrics receive local limitations for overlapping/soft memberships. General soft/overlapping-membership evaluation and the complete DBCV density-validity derivation are beyond this hard-partition finish line; no existing lesson is claimed to cover them. DBCV's all-points-core distance and validity spanning-tree definitions require their own treatment, not the ordinary DBSCAN core graph. The DBSCAN author is preserving that rationale as an explicitly unimplemented specialist follow-up in its own destination note; this does not broaden the present manuscript. Mixture likelihood selection is a separate later GMM/EM mechanism, previewed rather than derived here. No broad catalogue audit or unrequested source change occurred.

## Author learning-experience review

This is the author's heuristic content/specification review, not an independent review or an observed beginner study. The full manuscript was reread after drafting. Parent read it and the specifications for cross-topic continuity; that did not start formal phase-two review.

| Checklist item | Concrete finding, action and remaining scope |
| --- | --- |
| Route | The opening gives an actual first-pass route with programs and investigations, not merely a contents list. Secondary indices, information-distance, null-reference and cost sections announce their deeper role. The final DBSCAN title is linked to the correct module route. |
| Cautions | One general evaluation-contract caution has a home in section 1; each specific limitation appears with its mechanism (geometry, null, rejection or final report). Removed repeated implementation-verification caveats from individual program passages and consolidated author status in the opening note/design. No displayed program prints cautionary prose. |
| Real question | Opens with actual Iris k2/k3 geometric-versus-species disagreement, returns with declared-purpose interpretation, then adds a separate frozen-report example. Licensed CSV, stable IDs, units and actual calculations accompany it. The museum and duplicate-record applications identify different decisions rather than decorate the text. |
| Labs as investigations | Every lab has an initially unset committed prediction and a meaningful editable input: point/membership, partition labels, binary fixed-margin arrangement, four weights or resample frequencies. Saved author probes establish the headline contrasts and nulls. Not every future control state has run; implementation must check its actual supported inputs and unbalanced/undefined states. Parent identified the optional L5 coordinate range excluding a required tripled point30; range now includes it at [−40,40]. |
| Figures | The inline pass identified seven separate hurdles: identity, point averages, shape preference, shared information, real-data disagreement, changing population and data-flow roles. Added calculated ring-versus-slice distributions and exact coordinate/label inputs rather than leaving a schematic quantitative claim. Each quantitative contract names baseline/units and narrow layout. Rendered desktop/phone perceptibility remains untested because rendering is outside this phase. |
| Connections | Pair enumeration equals contingency arithmetic; refinement relates entropy/NMI/homogeneity/completeness/VI; PCA geometry affects fixed-label silhouette; common scaling links fit cost, silhouette and unchanged pair agreement. Direct aligned-label comparison confirms scaled4/PCA2 k3 ARI1 instead of inferring identity from equal species ARI. Canonical chapter breadth is retained or explicitly bounded; hierarchy fidelity was added locally after checking its supposed prior owner. |
| Code | Six complete programs foreground averages, contingency counts, exact finite permutations, declared fits, exact weighted cuts and fit/selection/report ownership. Setup now has the full pip command and a concrete save/run command. No hardcoded answer is printed as an algorithm result. Expected outputs are calculations/derivations; the full displayed blocks have not been execution-verified in this phase. |
| Practice | Eight closing problems change distances, partitions, fragmentation, minority weighting, rejected populations, bootstrap identity, exact coordinates/units and application purpose; all have optional hints before explained acceptance. The earlier C-coordinate question provides another local changed input. Exact answers include s=.5, ARI−1/6, NMI.5 for singletons, .68 versus .2 weighting, and the [1,9]/cost4→[3,27]/cost36 transformation. |
| Screenshots | None created or claimed: the user authorized research/writing only. Specs explicitly require informative contrast, checked-prediction and narrow ordinary-reading captures in implementation. The current check concerns whether the representation explains the intended entities and operations, not whether an unbuilt interface looks correct. |

## Final author calculations and handoff

In addition to the first calculation record, [author-input-probes.py](drafts/clustering-evaluation-validation-silhouette-ari-nmi/author-input-probes.py) produced [visual-input-calculations.json](drafts/clustering-evaluation-validation-silhouette-ari-nmi/visual-input-calculations.json): directly compared aligned scaled4/PCA2 memberships (ARI1 and every pair identical), four frozen-weight rescores, and the explicit 32-point ring contrast. [report-author-calculation.py](drafts/clustering-evaluation-validation-silhouette-ari-nmi/report-author-calculation.py) produced [the prespecified report fixture](drafts/clustering-evaluation-validation-silhouette-ari-nmi/report-author-calculation.json): all three candidates eligible, selected k2, selection silhouette .5568016439, report distortion1.3751346768 versus baseline4.6732514472, report species ARI.6285226302. These were small author calculations; no full displayed-program execution, runtime test campaign, formal independent review or browser work occurred.

The final writing check parsed all six displayed Python blocks and both author-input scripts without executing the displayed blocks, loaded every authored JSON, checked the 150 CSV rows and contiguous IDs, confirmed local file links resolve, and balanced the 18 hint/answer disclosures. The original live topic hash remains the recorded baseline. All changed files are this design, this topic's destination note, and its draft directory. The draft's scripts are author inputs, not application/runtime support. No repository build is needed for these documentation-only changes. Parent owns final content checkpoints and any shared status updates.

## Status and continuation

Stages 1–2: complete authored content packet, subject to the parent's final cross-topic assessment/checkpoint. Implementation, full native verification, formal independent review, rendered visuals/browser/accessibility and integration: not started for this revision. No user approval is claimed. Next authorized phase, when requested, should consume this packet, implement the specified investigations and ordinary-reading figures, execute the exact programs with the pinned data, and record the actual supported-input and browser results before publication changes.

## Phase two — implementation, verification and integration, 12 September 2026

The finish request passed `--work finish` against the unchanged content checkpoint (`canFinish: true`). The complete manuscript, visual specifications, provenance and author calculations were consumed. The implementer is the primary agent, not an independent reviewer. A first implementation pass was interrupted by the user; a second agent took over from the working tree, reread every implemented file against the specifications, reran every check and repaired what the rendered screenshots exposed.

### What was built

- `src/learn/data/topics/clustering-evaluation-validation-silhouette-ari-nmi.jsx`: the twelve-section manuscript as reader components, with the first-pass route immediately after the introduction, one caution callout in section 1, six displayed programs where the manuscript placed them, the Iris CSV linked as a real download in section 1, eight practice tasks with hints before answers, and the reference list. The successor link names DBSCAN on the module route.
- `src/learn/data/clustering-evaluation-models.js`: pure models for silhouette a/b/s with the singleton s = 0 convention; pair contingency (S, A, B, M, TP/FN/FP/TN, RI, ARI with fixed-margin E[S] = AB/M); entropies, arithmetic and geometric NMI, homogeneity and completeness; AMI with the hypergeometric E[MI]; the exact enumeration of every fixed-margin assignment; the exact weighted contiguous-split solver; the 32-point ring contrast; Iris rescoring from stored labels.
- `src/learn/data/clustering-evaluation-data.js`: the 150 Iris rows at full precision, species IDs, the natively fitted standardization scaler and two-component PCA, the eight declared fits (labels, silhouette, ARI, AMI, negatives, sizes) and the frozen 90/30/30 report fixture.
- `src/learn/components/lesson-labs/ClusteringEvaluationLabs.jsx`: L1 silhouette lab (six editable locations and memberships, selected observation, doubling and move buttons), L2 pair lab (eight candidate labels, optional reference edit, rename/refine/cross buttons, 28 pair tiles), L3 chance lab (two binary labelings, exhaustive fixed-margin null histogram, attainable-value table), L4 Iris lab (eight declared fits or frozen rescoring under four editable weights), L5 resample lab (two multiplicity vectors, k, editable probe locations, exact split table). Every lab records a prediction before a draft is applied, compares it with the model, and invalidates it when the draft changes.
- `src/learn/components/lesson-labs/ClusteringEvaluationFigures.jsx`: V1 identity strips, V2 distance fan with genuine silhouette bars, V3 ring-versus-slice contrast, V4 information bars, V5 raw Iris two-versus-three snapshot, V6 rejection lanes, V7 report flow. All values are computed from the models or the embedded native record.
- `src/learn/components/lesson-labs/clustering-evaluation-labs.css`: `ce-` prefixed styles with explicit SVG text fill and size and 600 px stacking rules.
- `src/learn/data/clustering-evaluation-examples.js`, generated by `scripts/verify-clustering-evaluation-examples.py`, which executes the six displayed programs against the served CSV and asserts eleven manuscript oracles.
- `public/learn-assets/clustering-evaluation/iris.csv`: the phase-one CSV served as a download (SHA256 `13c9255444bce09fd9df60cccfec34d5b0a0eeebc06008fb4e2b836aa2caf78a`).
- `src/learn/data/curriculum/blueprints/clustering-evaluation-validation-silhouette-ari-nmi.js`: the blueprint file. Registration in the blueprint index is left to the integrating agent because that index is shared with concurrent topics.

Specification deviations, with reasons: V2 prints each point's location under its name instead of numbering every tick, because the tick numerals collided with the dashed foreign-group arcs at 320 px. V6 marks the rejected chip with a dashed border and ✕ plus screen-reader text rather than the word “rejected”, which overflowed a 25 px chip on phones. L4's weight fields are labelled “sepal length weight” and so on, with one caption stating that each weight multiplies that measurement's squared difference, because the longer labels wrapped to three lines and misaligned the four inputs. L4's mode option reads “Rescore frozen standardized k = 3” so it fits a native select at desktop width. Predictions are a select plus a compare button rather than radio groups; keyboard behaviour is identical. Dragging was not implemented anywhere; all coordinates are typed, which the specifications allow.

### Checks actually run

| Check | Result |
| --- | --- |
| `node scripts/verify-clustering-evaluation-models.mjs` | PASS: 26 grouped checks against independent oracles (six-point silhouettes, singleton convention, common-scale invariance, pair counts and ARI for rename/refine/cross, exact 70-assignment null with mean NMI 0.1148 and zero adjusted means, information measures for the strict refinement, ring contrast, exact split costs, Iris rescoring against the native fits). Evidence `evidence/clustering-evaluation-models.json`. |
| `scratch/lesson-tools/Scripts/python.exe scripts/verify-clustering-evaluation-examples.py` | PASS: 6 displayed programs executed, 11 oracle assertions; every output block equals the manuscript's. Evidence `evidence/clustering-evaluation-native.json`. |
| Production build to `dist-clustering-evaluation` | Passed after each change (about 25 s). |
| `node scripts/verify-clustering-evaluation-browser.cjs` (Edge 152, Playwright) | PASS: 12 cases: visible code and output for six programs, twelve anchors, seven figures, eight practice tasks, CSV download with 151 lines, module position 13 with PCA before and DBSCAN after, all five labs operated with compared and stale predictions, 390 px and 320 px layouts, lazy closure, completion persistence and successor route, import and render failure recovery. Evidence `evidence/clustering-evaluation-browser.json` and twelve screenshots. |
| `node scripts/verify-curriculum.mjs` | PASS: 28 modules, 1218 stable topics, 353 briefs, 7 paths. |

Screenshot inspection after the takeover found three rendering defects, all fixed and recaptured: tick numerals under the V2 number line were crossed by the dashed arcs at 320 px; the V6 “C rejected” chip text spilled outside its chip at 320 px; and the L4 weight labels wrapped unevenly so the four inputs sat at different heights while the mode option text was truncated by the select. A duplicate `headers` prop on the V5 table was removed. No numerical defect was found in the inherited implementation.

### Author learning-experience checklist (heuristic; not a learner study)

1. Route: stated after the introduction and rendered as the `ce-route` block; the deeper branches in section 11 name themselves as optional.
2. Cautions: exactly one callout, in section 1. Later limitations appear once each with their mechanism. No displayed program prints a cautionary sentence.
3. Real question: the raw Iris two-versus-three disagreement opens the lesson (V5), returns in L4 with declared fits and the frozen rescoring, and closes with the 90/30/30 report protocol (V7 and the report program). The CSV is downloadable with provenance in the references.
4. Labs: all five record a prediction, compare it, and invalidate it when the draft changes. Free controls beyond presets: any of six locations and memberships (L1), any eight candidate and reference labels (L2), any two binary labelings (L3), any four weights in [0.25, 4] and any specimen (L4), any multiplicities, k and strictly increasing locations within ±40 (L5). Fixture checks: negative s after a membership move and s unchanged under doubling; ARI unchanged under rename, −1/6 under crossing, 4/11 under refinement; AMI negative at the exactly independent table and a degenerate null for a constant candidate; silhouette moving while ARI and AMI stay fixed under frozen labels; k = 1 trivially stable with ARI 1. All asserted by the model verifier and exercised in the browser.
5. Figures: all twelve captures opened at desktop and phone widths; three defects found and fixed as recorded above. The V6 comparison table is wider than a 320 px screen and scrolls inside its own focusable region.
6. Connections: pairs equal contingency arithmetic (L2 and its caption); refinement links entropy, NMI, homogeneity and completeness (V4); PCA and standardization change silhouette without changing memberships (L4); common scaling links fit cost, silhouette and pair agreement (L1 and L5); the frozen report reuses the PCA lesson's train-only discipline.
7. Code: the six programs show the mechanism (averages, contingency counts, exhaustive permutations, declared fits, exact splits, forward-only report) with no guards or disclaimers beyond the manuscript's.
8. Practice: eight changed exercises; exact values such as s = 0.5, ARI −1/6, NMI 0.5, the 0.68 versus 0.2 weighting and the [1, 9]/cost 4 to [3, 27]/cost 36 transformation match the models, and the L5 lab reproduces the last one.
9. Screenshots: captured after compared predictions, the crossed partition, the exhaustive null, the rescored Iris workspace, the resample split, the ring contrast, the report flow and five figures at 320 px; all opened and inspected.

### Independent review disposition, 12 September 2026

The reviewer (a separate agent that authored none of the sources) recomputed every bounded value from definitions without importing the lesson's models, compared the CSV and stored fits with a fresh scikit-learn run, probed the split solver's tie comparator on random supported inputs, and ran the nine-item learning-experience checklist. Its report is `CLUSTERING-EVALUATION-INDEPENDENT-REVIEW.md`; the hashes recorded there identify the sources it read, which precede the fixes below. It found no material defects and five minor ones.

| Finding | Severity | Disposition |
| --- | --- | --- |
| m-1: Program 1 rendered “Before running: Before running:” because its question repeated the wrapper prefix | Minor | Fixed in the generator's question string; the examples module was regenerated by `verify-clustering-evaluation-examples.py --write` and the rendered page now carries the prefix once. |
| m-2: the split solver broke cost ties by string collation, which can keep centers that are not the numerically smaller ordered tuple on rare tied inputs | Minor | Fixed: ties within 1e-9 in cost are now broken by element-wise numeric comparison of the centers, matching Program 5's Python tuple ordering. The model verifier gained a “split tie-breaking” group: an independent enumeration with numeric tuple ordering on the tie `[−4, −2, 0]` (old comparator kept `[−3, 0]`, correct answer `[−4, −1]`), the symmetric tie `[0, 2, 4]`, and 3,000 seeded random supported inputs, asserting equal tie counts and centers. Every untied fixture is unchanged. |
| m-3: changing the selected observation, pair or specimen after a comparison relabelled the standing feedback instead of invalidating it | Minor | Fixed: the committed key in L1, L2, L4 and L5 now includes the selection, so the feedback goes stale and asks for a new prediction. The browser verifier now changes each selection after a comparison and asserts the stale message; L1 also asserts that restoring the selection restores the feedback. |
| m-4: the model evidence record's hashes for the lab, figure and CSS files predated the screenshot repair | Minor | Resolved by rerunning the model verifier after m-2; all five hashes in `evidence/clustering-evaluation-models.json` and all nine in the browser record now match the current files. |
| m-5: the manuscript's instruction to type a petal-width weight of one's own and use a common multiplier as the control had been dropped from section 8 | Minor | Restored verbatim in the paragraph before `CeIrisLab`. |
| Cosmetic notes: “H(U) = 1 bits”; the V7 caption's “90 / 30 / 30” wrapping | None | Pluralized by value; the split is now written with non-breaking spaces so it wraps as one token. The V6 table's horizontal scroll at 320 px was left as is; the means are also in the bar titles. |

After these changes the model verifier passes 27 grouped checks, the six displayed programs reproduce the manuscript's output blocks, and the browser verifier passes 12 cases against a rebuilt topic-only `dist-clustering-evaluation` on port 4175; the chance and report-flow captures were reopened and inspected.

### Remaining limitations

The blueprint has been registered in the shared index by the integrating agent; the manifest and the delivery ledger are theirs to update. The browser verifier ran against the topic-only build on port 4175 and must be rerun against the shared `dist` before publication.


## Prepared-content implementation audit — 14 September 2026

The user's PCA-through-GMM verification received a fresh complete comparison of this prepared manuscript/specification with the lesson, six displayed programs, pure models, figures and five investigations. [The PCA/Clustering audit](PCA-CLUSTERING-CONTENT-IMPLEMENTATION-AUDIT.md) retains the coverage map, separate correctness and learning-experience findings and inspected source hashes. The explanations, worked computations, eight independent exercises, annotated resources and real Iris protocol are preserved. Native program source and verifier hashes still match their recorded successful execution, so unchanged programs were not regenerated merely to repeat evidence.

Focused interaction corrections address defects missed by the original checks: “Match U exactly” now copies the learner's current reference, not the default partition; the chance table uses its actual number of nonempty candidate columns, including a single constant group; the highlighted cell uses the same first-U-group/smaller-V-label overlap as the null histogram even when V first appears as 1; checked predictions are frozen until an input changes or Reset clears them. The resampling caption now states that scaling coordinates by three multiplies cost by nine and distinguishes changing multiplicities from changing probe locations.

The existing browser verifier now contains a complementary regression case for the edited-reference copy, NMI=AMI=1, the corresponding highlighted overlap, a constant-candidate three-column table and immutable compared predictions. Root owns executing this browser case, inspecting its displayed states and reconciling the final source checkpoint. The metric models, fitted data, six learner programs, CSV and prepared packet were not changed by these repairs; previous source-bound numerical/native evidence is reused. Do not report the added browser assertions as executed until root records their actual result.

## Coordinator verification closure, 14 September 2026

The prepared-content comparison and subsequent repairs are closed in [the five-topic audit](PCA-THROUGH-GMM-IMPLEMENTATION-AUDIT.md). The coordinator ran 13 passing production browser cases against the corrected topic source, including changed-input and prediction regressions, narrow layouts, module navigation, loading and recovery. Selected informative screenshots were actually inspected; exact captures and the mathematical/native evidence retained for unchanged code are listed in that record. The saved manuscript/specifications and offline author inputs are unchanged by this audit. The appended design and repaired implementation are re-bound in the existing revision's phase ledger; earlier completion dates and independent-review attribution are preserved. User acceptance is separate.
