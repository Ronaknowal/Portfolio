export const optimalTransportExamples = {
  "originalScaling": {
    "title": "Preserved first scaling example: a tiny complete loop",
    "question": "Predict why the regularized plan keeps both marginals but moves a little mass along the more expensive routes.",
    "code": "import math\n\np = q = [0.5, 0.5]\nC = [[0, 1], [2, 1]]\nepsilon = 0.5\nK = [[math.exp(-C[i][j] / epsilon) for j in range(2)] for i in range(2)]\nu = v = [1.0, 1.0]\n\nfor _ in range(100):\n    u = [p[i] / sum(K[i][j] * v[j] for j in range(2)) for i in range(2)]\n    v = [q[j] / sum(K[i][j] * u[i] for i in range(2)) for j in range(2)]\n\nplan = [[u[i] * K[i][j] * v[j] for j in range(2)] for i in range(2)]\ncost = sum(plan[i][j] * C[i][j] for i in range(2) for j in range(2))\nprint([[round(x, 3) for x in row] for row in plan])\nprint(round(cost, 3))",
    "expected": "[[0.44, 0.06], [0.06, 0.44]]\n0.619"
  },
  "twoLocationCertificate": {
    "title": "Solve changed supplies and certify the endpoint",
    "question": "Source masses are .6 and .4; target masses are .3 and .7. Which feasible endpoint is cheapest, and can prices certify its cost?",
    "code": "from fractions import Fraction as F\n\na, b = F(3, 5), F(3, 10)\ncosts = [[0, 1], [2, 1]]\nlower, upper = max(F(0), a + b - 1), min(a, b)\n\n\ndef plan_at(t):\n    return [[t, a - t], [b - t, 1 - a - b + t]]\n\n\ndef cost(plan):\n    return sum(plan[i][j] * costs[i][j] for i in range(2) for j in range(2))\n\n\nt = min([lower, upper], key=lambda value: cost(plan_at(value)))\nplan = plan_at(t)\ncertificates = []\nfor f1 in [costs[1][j] - costs[0][j] for j in range(2)]:\n    f = [0, f1]\n    g = [min(costs[0][j], costs[1][j] - f1) for j in range(2)]\n    value = (1 - a) * f1 + b * g[0] + (1 - b) * g[1]\n    certificates.append((value, f, g))\ndual, f, g = max(certificates)\nslack = [[costs[i][j] - f[i] - g[j] for j in range(2)] for i in range(2)]\nassert all(value >= 0 for row in slack for value in row)\nassert cost(plan) == dual\nprint(\"feasible t:\", str(lower), \"to\", str(upper))\nprint(\"plan:\", [[str(value) for value in row] for row in plan])\nprint(\"source prices:\", f, \"target prices:\", g)\nprint(\"cost and lower bound:\", str(cost(plan)), str(dual))\nprint(\n    \"weighted slack:\", sum(plan[i][j] * slack[i][j] for i in range(2) for j in range(2))\n)",
    "expected": "feasible t: 0 to 3/10\nplan: [['3/10', '3/10'], ['0', '2/5']]\nsource prices: [0, 0] target prices: [0, 1]\ncost and lower bound: 7/10 7/10\nweighted slack: 0"
  },
  "weightedLine": {
    "title": "Match weighted quantiles and check the cumulative area",
    "question": "Two source atoms and three target atoms have unequal weights. Can a sorted sweep and a CDF-area calculation independently agree on W1?",
    "code": "from fractions import Fraction as F\nfrom math import sqrt\n\nsource = [(0, F(2, 5)), (3, F(3, 5))]\ntarget = [(1, F(1, 5)), (2, F(1, 2)), (5, F(3, 10))]\nleft = [mass for _, mass in source]\nright = [mass for _, mass in target]\nplan = [[F(0) for _ in target] for _ in source]\ni = j = 0\nwhile i < len(source) and j < len(target):\n    moved = min(left[i], right[j])\n    plan[i][j] += moved\n    left[i] -= moved\n    right[j] -= moved\n    if left[i] == 0:\n        i += 1\n    if right[j] == 0:\n        j += 1\ncosts = [\n    sum(\n        plan[i][j] * abs(x - y) ** order\n        for i, (x, _) in enumerate(source)\n        for j, (y, _) in enumerate(target)\n    )\n    for order in [1, 2]\n]\nknots = sorted({x for x, _ in source + target})\narea = F(0)\nfor lower, upper in zip(knots, knots[1:]):\n    fa = sum(mass for x, mass in source if x <= lower)\n    fb = sum(mass for x, mass in target if x <= lower)\n    contribution = abs(fa - fb) * (upper - lower)\n    area += contribution\n    print(\"gap\", lower, upper, \"contribution\", str(contribution))\nassert area == costs[0]\nprint(\"plan:\", [[str(mass) for mass in row] for row in plan])\nprint(\"W1:\", str(costs[0]), \"W2 squared:\", str(costs[1]))\nprint(\"W2:\", f\"{sqrt(costs[1]):.6f}\")",
    "expected": "gap 0 1 contribution 2/5\ngap 1 2 contribution 1/5\ngap 2 3 contribution 3/10\ngap 3 5 contribution 3/5\nplan: [['1/5', '1/5', '0'], ['0', '3/10', '3/10']]\nW1: 3/2 W2 squared: 5/2\nW2: 1.581139"
  },
  "linearProgram": {
    "title": "Solve a general cost matrix and inspect its certificate",
    "question": "The costs below are a declared route table, not necessarily a metric. Does a general linear-programming solver return matching primal and dual values?",
    "code": "import numpy as np\nfrom scipy.optimize import linprog\n\na = np.array([0.2, 0.5, 0.3])\nb = np.array([0.4, 0.1, 0.5])\ncosts = np.array([[0.0, 4.0, 1.0], [2.0, 1.0, 3.0], [3.0, 0.0, 2.0]])\nrows, columns = costs.shape\nconstraints = np.zeros((rows + columns, rows * columns))\nfor i in range(rows):\n    constraints[i, i * columns : (i + 1) * columns] = 1\nfor j in range(columns):\n    constraints[rows + j, j::columns] = 1\nresult = linprog(\n    costs.ravel(), A_eq=constraints, b_eq=np.r_[a, b], bounds=(0, None), method=\"highs\"\n)\nif not result.success:\n    raise RuntimeError(result.message)\nplan = result.x.reshape(rows, columns)\nf = result.eqlin.marginals[:rows]\ng = result.eqlin.marginals[rows:]\nslack = costs - f[:, None] - g[None, :]\ndual = a @ f + b @ g\nassert np.max(np.abs(plan.sum(axis=1) - a)) < 1e-10\nassert np.max(np.abs(plan.sum(axis=0) - b)) < 1e-10\nassert slack.min() >= -1e-10\nassert abs(result.fun - dual) < 1e-10\nprint(\"transport cost:\", f\"{result.fun:.6f}\")\nprint(\"dual lower bound:\", f\"{dual:.6f}\")\nprint(\"maximum weighted slack:\", f\"{np.max(np.abs(plan * slack)):.6f}\")\nprint(\"feasible:\", True)\n# An optimizer/certificate need not be unique. Check these invariants instead\n# of promising identical chosen entries across every solver version.",
    "expected": "transport cost: 1.700000\ndual lower bound: 1.700000\nmaximum weighted slack: 0.000000\nfeasible: True"
  },
  "stableSinkhorn": {
    "title": "Run log-domain scaling with honest stopping information",
    "question": "Can stable arithmetic handle a huge common cost offset, and does it also make a sharply concentrated comparison converge within the chosen budget?",
    "code": "import math\n\n\ndef logsumexp(values):\n    maximum = max(values)\n    return maximum + math.log(sum(math.exp(value - maximum) for value in values))\n\n\ndef solve(a, b, costs, epsilon, max_steps=5000, tolerance=1e-10):\n    if epsilon <= 0 or any(value <= 0 for value in a + b):\n        raise ValueError(\"This teaching solver requires epsilon>0 and positive weights\")\n    if not math.isclose(sum(a), 1) or not math.isclose(sum(b), 1):\n        raise ValueError(\"Both marginals must sum to1\")\n    log_kernel = [[-value / epsilon for value in row] for row in costs]\n    log_u, log_v = [0.0] * len(a), [0.0] * len(b)\n    for iteration in range(1, max_steps + 1):\n        log_u = [\n            math.log(a[i])\n            - logsumexp([log_kernel[i][j] + log_v[j] for j in range(len(b))])\n            for i in range(len(a))\n        ]\n        log_v = [\n            math.log(b[j])\n            - logsumexp([log_kernel[i][j] + log_u[i] for i in range(len(a))])\n            for j in range(len(b))\n        ]\n        gauge = sum(log_v) / len(log_v)\n        log_u = [value + gauge for value in log_u]\n        log_v = [value - gauge for value in log_v]\n        plan = [\n            [math.exp(log_u[i] + log_kernel[i][j] + log_v[j]) for j in range(len(b))]\n            for i in range(len(a))\n        ]\n        residual = max(\n            [abs(sum(plan[i]) - a[i]) for i in range(len(a))]\n            + [\n                abs(sum(plan[i][j] for i in range(len(a))) - b[j])\n                for j in range(len(b))\n            ]\n        )\n        if residual <= tolerance:\n            break\n    linear = sum(plan[i][j] * costs[i][j] for i in range(len(a)) for j in range(len(b)))\n    entropy_term = epsilon * sum(\n        value * (math.log(value) - 1) for row in plan for value in row if value > 0\n    )\n    return plan, residual, residual <= tolerance, linear, linear + entropy_term\n\n\na = b = [0.5, 0.5]\ncosts = [[0.0, 1.0], [2.0, 1.0]]\nshifted = [[value + 1000 for value in row] for row in costs]\nplain_kernel = [[math.exp(-value / 0.5) for value in row] for row in shifted]\noriginal = solve(a, b, costs, 0.5)\nstable = solve(a, b, shifted, 0.5)\nprint(\n    \"vanished kernel entries:\", sum(value == 0 for row in plain_kernel for value in row)\n)\nprint(\"stable plan:\", [[round(value, 6) for value in row] for row in stable[0]])\nprint(\"both converged:\", original[2] and stable[2])\nprint(\"added linear cost:\", f\"{stable[3] - original[3]:.6f}\")\n\n\ndef squared(left, right):\n    return [[(x - y) ** 2 for y in right] for x in left]\n\n\nsource, target = [0.0, 2.0], [0.0, 1.0]\ncross = solve(a, b, squared(source, target), 1.0)\nself_a = solve(a, a, squared(source, source), 1.0)\nself_b = solve(b, b, squared(target, target), 1.0)\ndivergence = cross[4] - (self_a[4] + self_b[4]) / 2\nprint(\"cross linear / full objective:\", f\"{cross[3]:.6f}\", f\"{cross[4]:.6f}\")\nprint(\"Sinkhorn divergence:\", f\"{divergence:.6f}\")\nprint(\"unregularized W2 squared:\", 0.5)\nsharp = solve(a, b, squared([0.0, 2.0], [1.0, 3.0]), 0.1)\nprint(\"sharp solve converged:\", sharp[2], \"residual:\", f\"{sharp[1]:.6f}\")\n# A finite iteration cap is a reportable result, not permission to label\n# the returned matrix feasible or its objective an exact transport value.",
    "expected": "vanished kernel entries: 4\nstable plan: [[0.440399, 0.059601], [0.059601, 0.440399]]\nboth converged: True\nadded linear cost: 1000.000000\ncross linear / full objective: 0.738406 -1.320075\nSinkhorn divergence: 0.538778\nunregularized W2 squared: 0.5\nsharp solve converged: False residual: 0.000050"
  },
  "projection": {
    "title": "Compute the distribution lost by a conditional-mean map",
    "question": "If each source splits equally to nearby destinations, what does replacing each row by its average destination discard?",
    "code": "from fractions import Fraction as F\n\nsource = [0, 2]\ntarget = [-1, 1, 3]\na = [F(1, 2), F(1, 2)]\nplan = [[F(1, 4), F(1, 4), F(0)], [F(0), F(1, 4), F(1, 4)]]\nb = [sum(row[j] for row in plan) for j in range(len(target))]\nmapped = [\n    sum(mass * y for mass, y in zip(row, target)) / weight\n    for row, weight in zip(plan, a)\n]\nmean = sum(weight * y for weight, y in zip(b, target))\ntarget_variance = sum(weight * (y - mean) ** 2 for weight, y in zip(b, target))\nmapped_variance = sum(weight * (y - mean) ** 2 for weight, y in zip(a, mapped))\nconditional_variance = sum(\n    plan[i][j] * (target[j] - mapped[i]) ** 2 for i in range(2) for j in range(3)\n)\nassert target_variance == mapped_variance + conditional_variance\nprint(\"target masses:\", [str(value) for value in b])\nprint(\"mean destinations:\", [str(value) for value in mapped])\nprint(\"same mean:\", str(mean))\nprint(\"target / mapped variance:\", str(target_variance), str(mapped_variance))\nprint(\"discarded conditional variance:\", str(conditional_variance))",
    "expected": "target masses: ['1/4', '1/2', '1/4']\nmean destinations: ['0', '2']\nsame mean: 1\ntarget / mapped variance: 2 1\ndiscarded conditional variance: 1"
  },
  "relaxedMass": {
    "title": "See exactly what a relaxed mass constraint buys",
    "question": "There is only one possible route, but the source amount is2 and the target amount is8. What transported amount minimizes a stated unbalanced objective?",
    "code": "import math\n\nsource_amount, target_amount = 2.0, 8.0\ncost, penalty = 1.0, 2.0\n\n\ndef generalized_kl(mass, reference):\n    return mass * math.log(mass / reference) - mass + reference if mass else reference\n\n\ndef objective(mass):\n    return cost * mass + penalty * (\n        generalized_kl(mass, source_amount) + generalized_kl(mass, target_amount)\n    )\n\n\n# Derivative = cost + penalty*log(mass/source_amount)\n#                   + penalty*log(mass/target_amount).\noptimum = math.sqrt(source_amount * target_amount) * math.exp(-cost / (2 * penalty))\nderivative = (\n    cost\n    + penalty * math.log(optimum / source_amount)\n    + penalty * math.log(optimum / target_amount)\n)\nprint(\"optimal transported amount:\", f\"{optimum:.6f}\")\nprint(\"objective:\", f\"{objective(optimum):.6f}\")\nprint(\"derivative near zero:\", abs(derivative) < 1e-12)\nprint(\"source marginal changed:\", not math.isclose(optimum, source_amount))\nprint(\"target marginal changed:\", not math.isclose(optimum, target_amount))\nprint(\"curvature is positive:\", 2 * penalty / optimum > 0)\n# Ordinary balanced transport has no feasible plan here. Normalizing both\n# original amounts to1 instead would deliberately discard their total difference.",
    "expected": "optimal transported amount: 3.115203\nobjective: 7.539187\nderivative near zero: True\nsource marginal changed: True\ntarget marginal changed: True\ncurvature is positive: True"
  },
  "slicedDirections": {
    "title": "Expose what a small set of projections can miss",
    "question": "Two point clouds have identical x and y marginals. Does that make their full two-dimensional distributions equal?",
    "code": "from itertools import permutations\nfrom math import sqrt\n\nsource = [(-1.0, -1.0), (1.0, 1.0)]\ntarget = [(-1.0, 1.0), (1.0, -1.0)]\ndirections = [(1.0, 0.0), (0.0, 1.0), (1 / sqrt(2), 1 / sqrt(2))]\nprojected_costs = []\nfor direction in directions:\n    left = sorted(sum(x * v for x, v in zip(point, direction)) for point in source)\n    right = sorted(sum(x * v for x, v in zip(point, direction)) for point in target)\n    squared = sum((x - y) ** 2 for x, y in zip(left, right)) / 2\n    projected_costs.append(squared)\nprint(\"projection W2 squared:\", [round(value, 6) for value in projected_costs])\nbest = min(\n    sum(\n        sum((x - y) ** 2 for x, y in zip(source[i], target[j]))\n        for i, j in enumerate(order)\n    )\n    / 2\n    for order in permutations(range(2))\n)\nprint(\"full W2:\", f\"{sqrt(best):.6f}\")\nprint(\"two-axis average:\", f\"{sum(projected_costs[:2]) / 2:.6f}\")\nprint(\"three-direction rooted average:\", f\"{sqrt(sum(projected_costs) / 3):.6f}\")\n# This finite direction average is the declared approximation, not the\n# exact integral over all unit directions defining sliced Wasserstein.",
    "expected": "projection W2 squared: [0.0, 0.0, 2.0]\nfull W2: 2.000000\ntwo-axis average: 0.000000\nthree-direction rooted average: 0.816497"
  }
};
