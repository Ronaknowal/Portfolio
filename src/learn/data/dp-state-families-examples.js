// Complete independently executed Python programs; regenerate with the semantic example script.
export const dpStateFamiliesExamples = {
  "matrix": {
    "title": "Plan a matrix chain and recover its operation order",
    "question": "Which grouping of the fixed matrix chain uses the fewest ordinary scalar multiplications, and which intermediate products realize it?",
    "code": "def matrix_chain(dimensions):\n    if len(dimensions) < 2 or any(type(value) is not int or value <= 0 for value in dimensions):\n        raise ValueError(\"Use at least two positive integer dimensions\")\n    size = len(dimensions) - 1\n    costs = [[None] * (size + 1) for _ in range(size)]\n    splits = [[None] * (size + 1) for _ in range(size)]\n    for length in range(1, size + 1):\n        for left in range(size - length + 1):\n            right = left + length\n            if length == 1:\n                costs[left][right] = 0\n            for split in range(left + 1, right):\n                candidate = (costs[left][split] + costs[split][right]\n                             + dimensions[left] * dimensions[split] * dimensions[right])\n                if costs[left][right] is None or candidate < costs[left][right]:\n                    costs[left][right], splits[left][right] = candidate, split\n\n    # Stack events emit one expression and child-before-parent operations.\n    tokens, operations = [], []\n    stack = [(\"visit\", 0, size)]\n    while stack:\n        kind, first, second = stack.pop()\n        if kind == \"text\":\n            tokens.append(first)\n        elif kind == \"merge\":\n            left, split, right = first\n            operations.append((left, split, right))\n        elif second == first + 1:\n            tokens.append(f\"A{first}\")\n        else:\n            split = splits[first][second]\n            stack.extend([(\"merge\", (first, split, second), None),\n                          (\"text\", \")\", None), (\"visit\", split, second),\n                          (\"text\", \" * \", None), (\"visit\", first, split),\n                          (\"text\", \"(\", None)])\n    return costs[0][size], \"\".join(tokens), operations\n\n\nfor dimensions in [[8, 2, 12, 3, 6], [3, 7, 2, 5], [2, 2, 2, 2], [4, 3]]:\n    value, expression, operations = matrix_chain(dimensions)\n    print(dimensions, \"=>\", value, expression)\n    print(\"child-before-parent intervals:\", operations)",
    "expected": "[8, 2, 12, 3, 6] => 204 (A0 * ((A1 * A2) * A3))\nchild-before-parent intervals: [(1, 2, 3), (1, 3, 4), (0, 1, 4)]\n[3, 7, 2, 5] => 72 ((A0 * A1) * A2)\nchild-before-parent intervals: [(0, 1, 2), (0, 2, 3)]\n[2, 2, 2, 2] => 16 (A0 * (A1 * A2))\nchild-before-parent intervals: [(1, 2, 3), (0, 1, 3)]\n[4, 3] => 0 A0\nchild-before-parent intervals: []"
  },
  "balloons": {
    "title": "Choose the last balloon, then recover the first removal",
    "question": "What is the maximum reward when every balloon must be removed, and does replaying the original-index witness earn that exact reward?",
    "code": "def balloon_plan(values):\n    if any(type(value) is not int or value < 0 for value in values):\n        raise ValueError(\"Use nonnegative integer values\")\n    padded = [1, *values, 1]\n    size = len(padded)\n    best = [[0] * size for _ in range(size)]\n    last_choice = [[None] * size for _ in range(size)]\n    for gap in range(2, size):\n        for left in range(size - gap):\n            right = left + gap\n            winner = None\n            for last in range(left + 1, right):\n                candidate = (best[left][last] + best[last][right]\n                             + padded[left] * padded[last] * padded[right])\n                if winner is None or candidate > winner:\n                    winner, last_choice[left][right] = candidate, last\n            best[left][right] = winner\n\n    order = []\n    stack = [(False, 0, size - 1)]\n    while stack:\n        emit, left, right = stack.pop()\n        if emit:\n            order.append(left - 1)  # Convert padded position to original ID.\n            continue\n        last = last_choice[left][right]\n        if last is not None:\n            stack.extend([(True, last, last), (False, last, right), (False, left, last)])\n    return best[0][-1], order\n\n\ndef replay(values, order):\n    if sorted(order) != list(range(len(values))):\n        raise ValueError(\"Remove every original object exactly once\")\n    live, earned = list(range(len(values))), []\n    for original in order:\n        position = live.index(original)\n        left = values[live[position - 1]] if position else 1\n        right = values[live[position + 1]] if position + 1 < len(live) else 1\n        earned.append(left * values[original] * right)\n        live.pop(position)\n    return earned\n\n\nfor values in [[2, 4, 3], [2, 0, 3], [1, 1, 1], []]:\n    value, order = balloon_plan(values)\n    scores = replay(values, order)\n    print(values, \"=>\", value, \"original IDs\", order, \"earned\", scores)\n    assert sum(scores) == value",
    "expected": "[2, 4, 3] => 33 original IDs [1, 0, 2] earned [24, 6, 3]\n[2, 0, 3] => 9 original IDs [1, 0, 2] earned [0, 6, 3]\n[1, 1, 1] => 3 original IDs [2, 1, 0] earned [1, 1, 1]\n[] => 0 original IDs [] earned []"
  },
  "tree": {
    "title": "Pass a parent boundary condition through a tree",
    "question": "Which nonadjacent original nodes maximize total weight, and how does an externally selected parent change the root answer?",
    "code": "def independent_tree(weights, edges, root=0, parent_selected=False):\n    if any(type(weight) is not int for weight in weights):\n        raise ValueError(\"Use integer weights; negative weights are permitted\")\n    size = len(weights)\n    if type(root) is not int or not 0 <= root < max(1, size) or type(parent_selected) is not bool:\n        raise ValueError(\"Use a valid root (0 for empty) and Boolean parent condition\")\n    if len(edges) != max(0, size - 1):\n        raise ValueError(\"Use n-1 edges, or zero for an empty tree\")\n    adjacency = [[] for _ in weights]\n    seen_edges = set()\n    for edge in edges:\n        if len(edge) != 2 or any(type(node) is not int or not 0 <= node < size for node in edge):\n            raise ValueError(\"Edges must name two existing node IDs\")\n        first, second = edge\n        key = (min(first, second), max(first, second))\n        if first == second or key in seen_edges:\n            raise ValueError(\"No self-loops or duplicate edges\")\n        seen_edges.add(key)\n        adjacency[first].append(second)\n        adjacency[second].append(first)\n    children = [[] for _ in weights]\n    parents, visited, order = [None] * size, set(), []\n    stack = [root] if size else []\n    while stack:\n        node = stack.pop()\n        if node in visited:\n            raise ValueError(\"A tree cannot contain a cycle\")\n        visited.add(node)\n        order.append(node)\n        for child in adjacency[node]:\n            if child == parents[node]:\n                continue\n            if child in visited:\n                raise ValueError(\"A tree cannot contain a cycle\")\n            parents[child] = node\n            children[node].append(child)\n            stack.append(child)\n    if len(visited) != size:\n        raise ValueError(\"A tree must be connected\")\n    free, blocked, take = [0] * size, [0] * size, [0] * size\n    for node in reversed(order):\n        blocked[node] = sum(free[child] for child in children[node])\n        take[node] = weights[node] + sum(blocked[child] for child in children[node])\n        free[node] = max(blocked[node], take[node])\n    selected = []\n    stack = [(root, parent_selected)] if size else []\n    while stack:\n        node, forbidden = stack.pop()\n        choose = not forbidden and take[node] > blocked[node]  # Skip ties.\n        if choose:\n            selected.append(node)\n        stack.extend((child, choose) for child in reversed(children[node]))\n    value = (blocked[root] if parent_selected else free[root]) if size else 0\n    return value, selected\n\n\nweights = [5, 9, 2, 4, 1, 6, 3]\nedges = [(0, 1), (0, 2), (1, 3), (1, 4), (2, 5), (2, 6)]\nprint(\"root free:\", independent_tree(weights, edges))\nprint(\"external parent selected:\", independent_tree(weights, edges, parent_selected=True))\nprint(\"negative chain:\", independent_tree([-2, -5, -1], [(0, 1), (1, 2)]))\nprint(\"empty:\", independent_tree([], []))",
    "expected": "root free: (19, [0, 3, 4, 5, 6])\nexternal parent selected: (18, [1, 5, 6])\nnegative chain: (0, [])\nempty: (0, [])"
  },
  "digits": {
    "title": "Count legal digit completions under an inclusive bound",
    "question": "How many positive integers have no repeated decimal digit up to the bound, and how do two prefix counts give an inclusive range answer?",
    "code": "from functools import cache\n\n\ndef count_distinct(bound):\n    if type(bound) is not int or not 0 <= bound <= 10**18:\n        raise ValueError(\"Use an integer bound from 0 through 10**18\")\n    digits = tuple(map(int, str(bound)))\n\n    @cache\n    def completions(position, tight, started, used):\n        if position == len(digits):\n            return int(started)  # The all-padding spelling is not positive.\n        limit = digits[position] if tight else 9\n        count = 0\n        for digit in range(limit + 1):\n            next_tight = tight and digit == digits[position]\n            if not started and digit == 0:\n                count += completions(position + 1, next_tight, False, used)\n            elif not used & (1 << digit):\n                count += completions(position + 1, next_tight, True, used | (1 << digit))\n        return count\n\n    return completions(0, True, False, 0)\n\n\ndef count_range(first, last):\n    if type(first) is not int or type(last) is not int or not 0 <= first <= last <= 10**18:\n        raise ValueError(\"Use an ordered nonnegative integer range through 10**18\")\n    before = count_distinct(first - 1) if first > 0 else 0\n    return count_distinct(last) - before  # Zero stays excluded.\n\n\nfor bound in [0, 9, 99, 100, 102, 213, 999, 10**18]:\n    print(bound, \"=>\", count_distinct(bound))\nprint(\"100 through 130:\", count_range(100, 130))\nprint(\"0 through 0:\", count_range(0, 0))",
    "expected": "0 => 0\n9 => 9\n99 => 90\n100 => 90\n102 => 91\n213 => 172\n999 => 738\n1000000000000000000 => 8877690\n100 through 130: 17\n0 through 0: 0"
  }
};
