import { professionalTopic } from "./professional-topic-plan.js";
import { professionalSources as source } from "./professional-curriculum-sources.js";

const networking = "Networking Foundations: Packets, Transport, DNS & Sockets";
const services = "Networked Services, HTTP Contracts & Identity Boundaries";
const failure = "Distributed Failure Semantics, Retries & Idempotency";
const framing = "System Design: Requirements, Constraints & Architecture Decisions";
const capacity = "Capacity Estimation, Workload Models & Performance Budgets";
const dataModel = "Data Modeling, Invariants & Schema Evolution";
const transactions = "Transactions, Isolation Levels & Concurrency Anomalies";
const consistency = "Distributed Consistency, Linearizability & Causal Guarantees";
const replication = "Replication, Quorums & Failover Semantics";
const consensus = "Consensus, Raft, Paxos & Replicated State Machines";
const queues = "Message Queues, Publish-Subscribe & Delivery Semantics";
const observability = "Observability: Metrics, Logs, Traces & Profiling";
const security = "System Threat Modeling, Trust Boundaries & Secure Defaults";
const plan = (title, level, prerequisites, sequence, visual, practice, misconception, sources = [source.distributed, source.sre]) =>
  professionalTopic(title, level, prerequisites, sequence, visual, practice, misconception, sources);

// Shared titles resolve to their original topic object in track-definitions.js.
// This creates another membership, never a second lesson or progress identity.
const shared = title => ({ sharedTitle: title });

export const systemDesignDefinition = {
  id: "system-design",
  title: "System Design & Distributed Systems Engineering",
  description: "Requirements, networking, storage, distributed correctness, APIs, security, reliability, infrastructure, architecture evolution and production design capstones",
  sections: [
    {
      name: "Design Foundations, Requirements & Tradeoffs",
      topics: [
        plan(framing, "foundation", [],
          ["Translate a user workflow into a system boundary", "Distinguish functional requirements, quality attributes and constraints", "State invariants, assumptions, alternatives and architecture decisions", "Defend a simple design and identify what evidence would change it"],
          ["context and decision map", "Which requirement justifies each component?", "Change a requirement and trace affected decisions"],
          ["Design a small reservation service before selecting technologies", "Specify measurable requirements, invariants and rejected alternatives"],
          "A collection of popular technologies is not a system design"),
        plan(capacity, "foundation", [framing],
          ["Describe a workload with requests, payloads and access distributions", "Estimate storage, bandwidth, CPU, memory and peak concurrency", "Apply latency budgets, queueing and Little's law with units", "Validate estimates against measurements and sensitivity ranges"],
          ["request budget and saturation curves", "Which resource saturates first as demand grows?", "Change payload size, fanout and peak factors"],
          ["Estimate resources for an upload-and-read service", "Units reconcile and average, peak and uncertainty are separated"],
          "Average requests per second do not describe burst or tail behavior", [source.sre, source.workbook]),
        plan("Architecture Styles: Modular Monoliths, Services & Event-Driven Systems", "foundation", [framing],
          ["Start with boundaries and a single deployable application", "Compare modular monoliths, service-oriented and microservice designs", "Distinguish synchronous requests, asynchronous events and batch work", "Evaluate coupling, team ownership and failure costs before distribution"],
          ["dependency and deployment boundary map", "Which boundary needs an independent deployment?", "Move one component across a network boundary"],
          ["Compare two architectures for the same requirements", "Justify distribution with operational and organizational costs"],
          "Microservices are not an automatic upgrade over a well-structured monolith"),
        plan("Domain Modeling, Bounded Contexts & Service Ownership", "intermediate", [framing],
          ["Model business concepts, events and invariants", "Distinguish entities, value objects, aggregates and bounded contexts", "Assign authoritative ownership and anti-corruption boundaries", "Prevent shared-database and dependency coupling across teams"],
          ["context map and invariant boundaries", "Who may change this fact?", "Trace an order across billing and fulfillment vocabularies"],
          ["Partition a commerce domain with explicit contracts", "Invariants have owners and cross-boundary coordination is justified"],
          "One service per noun rarely captures business consistency boundaries"),
        plan("Architecture Documentation, ADRs & Technical Design Reviews", "foundation", [framing],
          ["Represent context, containers, components and runtime sequences", "Write an architecture decision record with alternatives and consequences", "Review security, operations, cost and migration with stakeholders", "Update a design when evidence invalidates an assumption"],
          ["linked context and sequence diagrams", "Do the diagrams agree about where state lives?", "Follow a request and its failure path"],
          ["Write and critique a bounded design proposal", "Every major choice links to a requirement and testable assumption"],
          "Architecture diagrams should express contracts and behavior, not only icons"),
        plan("System Design Interviews: Clarification, Estimation & Deep Dives", "intermediate", [framing, capacity],
          ["Clarify the scope before drawing the architecture", "Estimate only quantities that affect a decision", "Develop APIs, data model, bottleneck and failure reasoning", "Adapt a proposal to a changed requirement and communicate limitations"],
          ["design decision progression", "Which uncertainty deserves the next five minutes?", "Introduce a scale or consistency change"],
          ["Complete a timed design and retrospective", "Explain reasoning and tradeoffs rather than reciting a memorized diagram"],
          "Interview diagrams omit many production obligations and are not deployment plans"),
        plan("Low-Level Design: Interfaces, Composition & Dependency Boundaries", "intermediate", ["Object-Oriented Programming in Python", "Domain Modeling, Bounded Contexts & Service Ownership"],
          ["Translate a service responsibility into a small executable model", "Design interfaces, composition, dependency inversion and explicit state ownership", "Apply SOLID principles as tradeoff questions rather than slogans", "Test substitutability, invariants and the cost of changing a requirement"],
          ["interface and state-ownership graph", "Which module must change when a provider changes?", "Replace an adapter without changing domain rules"],
          ["Design a pluggable reservation-pricing component", "Tests exercise domain behavior independently of network and persistence"],
          "More abstractions do not automatically mean a more maintainable design", [source.patterns]),
        plan("Design Patterns, State Machines & Maintainable Service Code", "intermediate", ["Low-Level Design: Interfaces, Composition & Dependency Boundaries"],
          ["Identify recurring variation, construction and coordination problems", "Compare strategy, factory, adapter, observer and command patterns", "Replace scattered state flags with explicit transitions and invariants", "Choose the smallest pattern that clarifies behavior and testing"],
          ["object collaboration and transition diagram", "Which transition is illegal even if every field is valid?", "Trigger events in an unexpected order"],
          ["Refactor a brittle order workflow", "Make invalid states unrepresentable or explicitly rejected"],
          "A pattern name does not justify adding indirection", [source.patterns]),
      ],
    },
    {
      name: "Operating Systems, Networking & Request Transport",
      topics: [
        shared(networking),
        plan("Operating-System Mechanisms for Service Design", "foundation", [framing],
          ["Trace a request through process, memory, file and socket boundaries", "Explain virtual memory, page cache, syscalls and scheduling", "Compare processes, threads and event loops", "Diagnose resource exhaustion with bounded queues and limits"],
          ["request and resource-lifetime diagram", "Where does a blocked request consume resources?", "Change I/O delay and worker count"],
          ["Explain a service failing despite low CPU usage", "Account for threads, descriptors, memory and waiting work"],
          "Low CPU utilization does not prove spare request capacity"),
        plan("Concurrency, Memory Models & Synchronization for Services", "intermediate", ["Operating-System Mechanisms for Service Design"],
          ["Identify shared state and concurrent operations", "Compare mutexes, atomics, semaphores and message passing", "Explain happens-before, deadlock, starvation and data races", "Choose synchronization around an invariant and test adverse schedules"],
          ["thread interleaving and state trace", "Which interleaving violates this invariant?", "Step two updates with and without synchronization"],
          ["Repair a concurrent resource allocator", "Demonstrate safety and progress under explicit scheduling assumptions"],
          "Thread-safe individual operations do not make a compound workflow atomic"),
        plan("TCP, UDP, QUIC, TLS & Connection Lifecycle Design", "intermediate", [networking],
          ["Trace connection establishment and encrypted transport", "Compare reliable streams, datagrams and QUIC multiplexing", "Explain congestion control, loss, head-of-line blocking and connection reuse", "Budget handshakes, timeouts and transport-level failure detection"],
          ["packet and connection timeline", "Which loss blocks which application message?", "Drop packets under different transport models"],
          ["Choose a transport for three stated workloads", "Justify reliability, latency, deployment and security requirements"],
          "A reliable transport does not provide exactly-once business effects", [source.http, source.timestamping]),
        plan("DNS, Anycast, Service Discovery & Traffic Steering", "intermediate", [networking],
          ["Resolve a public name through caches and authoritative servers", "Compare DNS, registry and mesh-based service discovery", "Explain anycast, regional steering and endpoint health", "Model TTLs, stale discovery and failover delays"],
          ["name-to-endpoint and cache timeline", "Why are clients still reaching a failed endpoint?", "Change TTL and resolver cache state"],
          ["Design discovery and failover for a regional service", "Account for cached answers and health-check limitations"],
          "Changing an authoritative DNS record does not instantly move all clients", [source.sre]),
        shared(services),
        plan("Load Balancers, Reverse Proxies & API Gateways", "intermediate", [services, capacity],
          ["Separate layer-four routing from layer-seven request handling", "Compare load-balancing policies, connection draining and health probes", "Place TLS termination, authentication and gateway policies", "Analyze sticky sessions, uneven work and proxy failure"],
          ["request distribution and queue view", "Why can equal request counts produce unequal load?", "Change request cost and routing policy"],
          ["Configure a design for long-lived and short requests", "Document health semantics, draining and bottlenecks"],
          "A load balancer does not make unhealthy downstream dependencies healthy"),
        plan("CDNs, Edge Caching & Geographic Content Delivery", "intermediate", [services, "DNS, Anycast, Service Discovery & Traffic Steering"],
          ["Trace cache hits, origin fetches and regional delivery", "Define cache keys, TTLs, invalidation and signed access", "Handle personalized content, stale assets and cache poisoning boundaries", "Measure origin protection, latency and regional consistency tradeoffs"],
          ["edge-origin cache hierarchy", "Can two users safely share this cached response?", "Change key dimensions and authorization headers"],
          ["Design static and private-media delivery", "Prevent cross-user leakage and specify invalidation behavior"],
          "A CDN cannot safely cache every authenticated response identically", [source.http, source.sre]),
      ],
    },
    {
      name: "APIs, Application State & Service Contracts",
      topics: [
        plan("API Design: REST, RPC, GraphQL & Compatibility", "intermediate", [services],
          ["Specify resources, operations and observable outcomes", "Compare REST, RPC and GraphQL against client access patterns", "Design errors, pagination, filtering, versioning and evolution", "Test compatibility and authorization at contract boundaries"],
          ["request-response contract comparison", "Which API shape reduces this client's work without hiding cost?", "Change client query and schema version"],
          ["Design and evolve a collection API", "Pagination and errors remain deterministic under concurrent changes"],
          "A schema alone does not define consistency or authorization semantics", [source.http]),
        shared(failure),
        plan("Timeouts, Deadlines, Retries & Exponential Backoff", "intermediate", [failure, capacity],
          ["Allocate an end-to-end deadline across nested calls", "Classify retryable outcomes and unknown effects", "Apply bounded retries, jitter and retry budgets", "Analyze retry amplification during overload"],
          ["nested deadline and retry timeline", "Can retries make recovery less likely?", "Change dependency delay and retry policies"],
          ["Repair a cascading retry policy", "Bound elapsed time, attempts and duplicate effects"],
          "A timeout reports missing knowledge, not proof that nothing happened"),
        plan("Idempotency Keys, Deduplication & Exactly-Once Effects", "intermediate", [failure, services],
          ["Separate repeated requests from repeated business effects", "Design idempotency-key scope, retention and parameter checks", "Coordinate durable deduplication with state changes", "Explain crash windows and the boundaries of exactly-once claims"],
          ["request and commit state machine", "What if the server crashes after committing but before replying?", "Retry at each persistence boundary"],
          ["Specify an idempotent payment-creation endpoint", "Duplicate requests cannot create extra effects within the stated guarantee"],
          "Exactly-once messaging does not automatically mean exactly-once external effects"),
        plan("Sessions, Stateless Services & Distributed Application State", "intermediate", [services, "Load Balancers, Reverse Proxies & API Gateways"],
          ["Locate session, workflow and durable business state", "Compare client tokens, server sessions and shared session stores", "Handle expiration, revocation, failover and sticky routing", "Scale workers without losing or leaking user context"],
          ["session ownership and failover map", "What state disappears if this worker is replaced?", "Restart one worker and invalidate a session"],
          ["Design session behavior across two regions", "Define revocation, security and availability tradeoffs"],
          "Stateless workers still depend on state somewhere in the system"),
        plan("WebSockets, Server-Sent Events, Webhooks & Realtime APIs", "intermediate", ["TCP, UDP, QUIC, TLS & Connection Lifecycle Design", "API Design: REST, RPC, GraphQL & Compatibility"],
          ["Choose push or polling from freshness and connectivity needs", "Compare bidirectional sockets, event streams and callbacks", "Design reconnection, resumable cursors, heartbeats and delivery retries", "Bound fanout, slow consumers and webhook authorization"],
          ["connection and resumption timeline", "What events did a reconnecting client miss?", "Interrupt a stream and resume from a cursor"],
          ["Design a resumable notification channel", "Define gaps, duplicates, retention and client backpressure"],
          "A persistent connection does not guarantee durable event delivery"),
        plan("Rate Limiting, Quotas & Admission Control", "intermediate", [capacity, failure],
          ["Separate fairness, protection and commercial quotas", "Compare fixed windows, sliding windows, token and leaky buckets", "Coordinate per-user and global limits across workers", "Handle burst tolerance, clock behavior and degraded dependencies"],
          ["bucket and request-admission trace", "Can a boundary burst exceed the intended limit?", "Replay a burst under different algorithms"],
          ["Design a multi-tenant API quota policy", "State accuracy, storage, fairness and fail-open or fail-closed behavior"],
          "A rate limiter is not sufficient protection against all overload patterns"),
        plan("Distributed IDs, Ordering & Uniqueness Guarantees", "intermediate", [failure],
          ["Distinguish uniqueness, sortability and causal order", "Compare random IDs, database sequences and time-worker counters", "Handle clock rollback, worker reuse and partitioned generation", "Assess information leakage and index-locality consequences"],
          ["ID field and clock-rollback trace", "Does a larger ID necessarily mean a later event?", "Roll a clock back and restart a generator"],
          ["Select an ID scheme for offline-capable clients", "State collision, ordering and coordination guarantees"],
          "Time-sortable identifiers are not a substitute for distributed causal order"),
        plan("Client Architecture: Rendering, State, Offline Data & API Boundaries", "intermediate", ["API Design: REST, RPC, GraphQL & Compatibility", "Sessions, Stateless Services & Distributed Application State"],
          ["Trace initial load, navigation and mutation from browser or mobile client", "Compare server rendering, client rendering and backend-for-frontend boundaries", "Separate server cache, local UI state and optimistic mutations", "Handle offline persistence, cancellation, stale responses and recovery"],
          ["client-server state and rendering timeline", "Can an older response overwrite a newer user action?", "Reorder responses and disconnect the client"],
          ["Design an offline-capable client workflow", "Define authoritative state, reconciliation and visible failure behavior"],
          "A successful optimistic UI update is not proof of a committed server effect", [source.webPerformance, source.http]),
        plan("User-Perceived Performance, Accessibility & Internationalization", "intermediate", ["Client Architecture: Rendering, State, Offline Data & API Boundaries"],
          ["Measure loading, responsiveness and layout stability on realistic devices", "Budget network, JavaScript, rendering and background work", "Include keyboard, assistive technology, localization and time-zone requirements", "Evaluate architectural choices against real user journeys and device constraints"],
          ["critical rendering and interaction timeline", "Is the backend fast while the interface still feels slow?", "Change device speed, bundle size and network delay"],
          ["Review a complete client journey under constrained conditions", "Demonstrate usable loading, error, keyboard and localized states"],
          "Server latency alone does not measure the user's experience", [source.webPerformance, source.accessibility]),
      ],
    },
    {
      name: "Storage Engines, Data Models & Transactions",
      topics: [
        plan(dataModel, "foundation", [framing],
          ["Derive entities and relationships from read and write workflows", "Express uniqueness, referential and business invariants", "Compare normalization, denormalization and access-oriented models", "Evolve schemas with compatibility, backfill and rollback"],
          ["entity and query-access map", "Which updates can leave this denormalized value stale?", "Change a source record and trace dependent copies"],
          ["Model a booking dataset and migrate one field", "Protect invariants during mixed-version reads and writes"],
          "Denormalization trades coordination work for read efficiency", [source.databases]),
        plan("Database Indexes, B-Trees, Hash Indexes & Query Planning", "intermediate", [dataModel, "Arrays, Strings & Hash Maps"],
          ["Trace a query from predicate to access path", "Compare ordered, hash, composite, covering and partial indexes", "Explain selectivity, statistics, join plans and write amplification", "Validate an index choice with representative query plans"],
          ["index traversal and row-access trace", "Why does this composite index miss the query's filter?", "Change key order and predicate ranges"],
          ["Choose indexes for a mixed read-write workload", "Explain plan, storage and maintenance tradeoffs"],
          "More indexes can slow writes and do not guarantee a better plan", [source.databases]),
        plan("Storage Internals: Pages, WAL, LSM Trees & Compaction", "intermediate", ["Operating-System Mechanisms for Service Design", dataModel],
          ["Trace a durable write through buffers, logs and files", "Compare page-oriented and log-structured engines", "Explain write-ahead logging, checkpoints, SSTables and compaction", "Measure read, write and space amplification under a workload"],
          ["write path and compaction layers", "When is an acknowledged write durable?", "Crash before and after log synchronization"],
          ["Design a tiny durable key-value write path", "State recovery invariants and amplification costs"],
          "Appending to a process buffer is not the same as durable storage", [source.databases]),
        plan("Probabilistic Data Structures: Bloom Filters, Sketches & Approximate Counts", "intermediate", ["Arrays, Strings & Hash Maps", "Database Indexes, B-Trees, Hash Indexes & Query Planning"],
          ["Choose approximation only after stating an acceptable error contract", "Compare Bloom filters, Count-Min sketches and HyperLogLog", "Compute memory, false-positive and estimation tradeoffs", "Handle mergeability, deletion and adversarial or skewed inputs"],
          ["bit-array membership and error view", "Can a positive filter answer prove that an item exists?", "Insert keys and vary filter size"],
          ["Choose an approximate structure for a telemetry workload", "State error direction, memory bounds and exact fallback behavior"],
          "Approximate membership and approximate cardinality solve different problems", [source.probabilistic, source.sketches]),
        plan("Bloom, Cuckoo & XOR Filters: Approximate Membership", "intermediate", ["Probabilistic Data Structures: Bloom Filters, Sketches & Approximate Counts"],
          ["Trace hashing into a Bloom filter and test present and absent keys", "Derive false-positive and memory tradeoffs under stated hash assumptions", "Compare counting Bloom, scalable Bloom, cuckoo and static XOR filters", "Reason about deletion, saturation, rebuilds and the exact-store fallback"],
          ["hash-to-bit and fingerprint bucket trace", "Why does a positive answer still require an exact lookup?", "Insert colliding keys, fill the filter and attempt a deletion"],
          ["Design a filter before an expensive storage lookup", "State false-positive behavior, update constraints and a safe deletion policy"],
          "Blindly clearing Bloom bits or deleting an absent fingerprint can introduce false negatives", [source.probabilistic, source.cuckooFilters, source.xorFilters]),
        plan("HyperLogLog & HLL++: Approximate Distinct Counting", "intermediate", ["Probabilistic Data Structures: Bloom Filters, Sketches & Approximate Counts"],
          ["Explain why a stream's number of events differs from its number of distinct keys", "Trace uniform hashes into register indices and leading-zero ranks", "Estimate cardinality and relate precision, relative error, bias correction and sparse representations", "Merge compatible sketches for unions and examine limits for deletion, intersections and time windows"],
          ["hash-bit register explorer and repeated-trial error distribution", "How can a small set of registers estimate millions of distinct users?", "Replay duplicates, increase precision and merge overlapping streams against an exact set"],
          ["Design daily and monthly distinct-user telemetry with HLL", "Verify unions on overlapping inputs, report measured error and state hash/precision compatibility and privacy limits"],
          "HLL neither stores membership nor supports arbitrary deletion; error is statistical and implementation-specific", [source.hyperloglog, source.hyperloglogPlus]),
        plan("Count-Min Sketch, Count Sketch & Streaming Heavy Hitters", "advanced", ["Probabilistic Data Structures: Bloom Filters, Sketches & Approximate Counts"],
          ["Distinguish counting one key from finding the most frequent keys", "Trace Count-Min counters and Count Sketch signed updates with their different assumptions", "Compare Misra-Gries and Space-Saving candidate tracking with sketch point queries", "Evaluate collisions, skew, bounded memory, merges and permitted update models"],
          ["counter collision trace and heavy-hitter candidate table", "Can an infrequent key look popular because of other keys?", "Change skew, width and update signs against an exact frequency table"],
          ["Detect popular keys in a bounded-memory event stream", "Separate candidate discovery from count estimation and state the error direction and update assumptions"],
          "A point-query sketch alone does not enumerate the top keys, and Count-Min guarantees depend on the update model", [source.sketches, source.probabilistic]),
        plan("Streaming Quantiles, KLL, t-Digest & Reservoir Sampling", "advanced", ["Probabilistic Data Structures: Bloom Filters, Sketches & Approximate Counts", "Probability Distributions & Bayes' Theorem"],
          ["Explain why averaging shard percentiles does not produce a global percentile", "Compare KLL rank error, t-digest tail behavior and DDSketch relative value error", "Trace reservoir and weighted sampling as different answers to a streaming-data question", "Evaluate mergeability, window expiry, weighting and extreme-tail uncertainty"],
          ["distribution, rank and sampled-item views", "Which error contract matters for a p99 latency objective?", "Merge unequal shards and compare estimates and samples with the exact stream"],
          ["Choose a latency summary and a diagnostic sampling policy", "Specify error units, memory, sampling inclusion probabilities and a validation workload"],
          "A percentile summary, a random sample and an average preserve different information", [source.sketches, source.ddsketch]),
        plan(transactions, "intermediate", [dataModel, "Concurrency, Memory Models & Synchronization for Services"],
          ["Specify which multi-record invariant a transaction protects", "Trace dirty reads, lost updates, skew and phantoms", "Compare isolation guarantees, MVCC, locking and serializable validation", "Handle deadlocks, retries and application-visible aborts"],
          ["transaction interleaving table", "Can both doctors go off call under this isolation level?", "Step reads and commits under alternative guarantees"],
          ["Repair a write-skew schedule", "Explain which guarantee blocks the bad history and its cost"],
          "Snapshot isolation is not generally serializability", [source.postgres, source.databases]),
        plan("Relational, Document, Key-Value, Wide-Column & Graph Databases", "intermediate", [dataModel, transactions],
          ["Describe query, relationship and update requirements first", "Compare database families by access path and consistency needs", "Analyze joins, aggregation, traversal and transactional boundaries", "Justify one or several stores with operational ownership"],
          ["same-workload data-model comparison", "Which store makes this access pattern expensive?", "Change the dominant query and required invariant"],
          ["Select stores for three concrete workloads", "Use measured requirements instead of SQL-versus-NoSQL slogans"],
          "A database category does not uniquely determine its consistency guarantees", [source.databases]),
        plan("Object Storage, Filesystems, Block Storage & Erasure Coding", "intermediate", ["Storage Internals: Pages, WAL, LSM Trees & Compaction"],
          ["Compare object, file and block interfaces", "Explain chunking, metadata, replication, checksums and erasure coding", "Design multipart upload, integrity checks and lifecycle policies", "Evaluate durability, repair traffic and failure-domain assumptions"],
          ["object shards and failure-domain placement", "How many failures can this placement actually tolerate?", "Lose disks and entire racks separately"],
          ["Design storage for large immutable objects", "Calculate redundancy costs and document repair and access semantics"],
          "High durability does not imply high availability or recoverability from deletion"),
        plan("Merkle Trees, Content Addressing & Data Integrity", "intermediate", ["Arrays, Strings & Hash Maps", "Object Storage, Filesystems, Block Storage & Erasure Coding"],
          ["Build a hash tree from canonically encoded data chunks", "Verify inclusion proofs and compare roots to locate differing subtrees", "Apply content addressing to deduplication, file sync and immutable artifacts", "Distinguish integrity from authenticity, freshness, availability and privacy"],
          ["chunk tree and proof-path explorer", "Which hashes must change when one chunk changes?", "Alter a leaf and verify a proof against a trusted root"],
          ["Specify a content-addressed backup with integrity verification", "Define chunk boundaries, hash/encoding choices, trusted roots and safe garbage collection"],
          "Matching an untrusted hash does not authenticate data, and a Merkle proof does not guarantee availability", [source.merkle, source.distributed]),
        plan("Time-Series Databases, Search Indexes & Specialized Retrieval", "intermediate", ["Database Indexes, B-Trees, Hash Indexes & Query Planning"],
          ["Compare timestamp ranges, inverted indexes and vector retrieval", "Design ingestion, retention, aggregation and index freshness", "Explain high cardinality, approximate retrieval and filtered search", "Choose specialized stores without duplicating the source of truth"],
          ["source-to-index freshness pipeline", "Which results are missing after a recent update?", "Delay indexing and change retention or filters"],
          ["Design metrics and search access for one dataset", "Specify ownership, freshness and approximation guarantees"],
          "A search index is not automatically an authoritative transactional database"),
        plan("Database Operations: Connections, Vacuum, Backups & Restore", "intermediate", [transactions, "Storage Internals: Pages, WAL, LSM Trees & Compaction"],
          ["Budget connection pools, locks and long-running transactions", "Explain maintenance, vacuuming, compaction and storage growth", "Plan backups, point-in-time recovery and restore rehearsals", "Diagnose replication lag and migration-induced outages"],
          ["transaction age and recovery timeline", "Can a successful backup still fail the recovery objective?", "Change log retention and restore throughput"],
          ["Write and exercise a database recovery plan", "Verify recovered data and measured recovery time"],
          "An untested backup is not demonstrated recovery capability", [source.postgres, source.sre]),
      ],
    },
    {
      name: "Distributed Correctness, Replication & Coordination",
      topics: [
        plan("Distributed Failure Models, Time & Impossibility Boundaries", "intermediate", [failure],
          ["Separate crash, omission, partition and Byzantine failure models", "Explain uncertain failure detection and physical-clock limits", "Use logical clocks and happens-before relations", "Interpret CAP and FLP under their actual assumptions"],
          ["message history with uncertain nodes", "Can silence distinguish a slow node from a failed one?", "Delay messages without revealing the node state"],
          ["Classify guarantees for several network histories", "Name timing and fault assumptions before drawing conclusions"],
          "CAP is not a universal choose-any-two checklist", [source.distributed, source.raft]),
        plan(consistency, "intermediate", ["Distributed Failure Models, Time & Impossibility Boundaries", transactions],
          ["Describe allowed read-write histories", "Compare linearizable, sequential, causal and eventual consistency", "Explain read-your-writes, monotonic reads and staleness bounds", "Select a guarantee from a business invariant and failure requirement"],
          ["operation-history checker", "Is there a legal instant when this write took effect?", "Reorder overlapping operations and inspect witnesses"],
          ["Classify small histories and repair a violating design", "Use precise guarantees rather than strong or weak labels"],
          "Database isolation and distributed consistency describe different properties", [source.distributed]),
        plan(replication, "intermediate", [consistency, "Storage Internals: Pages, WAL, LSM Trees & Compaction"],
          ["Compare leader-follower, multi-leader and leaderless replication", "Reason about acknowledgment, read and write quorums", "Handle lag, read repair, anti-entropy and failover", "Explain lost updates and stale reads under concrete failure histories"],
          ["replica acknowledgment and read timeline", "Which acknowledged write survives this failover?", "Crash replicas at different durability points"],
          ["Evaluate quorum claims with counterexample histories", "State membership, versioning and conflict-resolution assumptions"],
          "The inequality R plus W greater than N alone does not prove linearizability", [source.distributed]),
        plan(consensus, "advanced", [replication],
          ["Explain agreement, validity and termination for a replicated log", "Trace leader election, terms, log matching and commit rules", "Compare Raft and Paxos concepts and reconfiguration challenges", "Validate safety during leader changes and delayed messages"],
          ["term and replicated-log stepper", "Which entries are committed after a leader change?", "Partition, elect and reconnect nodes"],
          ["Trace and test a small consensus history", "Distinguish committed entries, uncommitted tails and liveness assumptions"],
          "Receiving an entry on one follower does not necessarily commit it", [source.raft, source.distributed]),
        plan("Leader Election, Leases, Distributed Locks & Fencing Tokens", "advanced", [consensus],
          ["Separate leader authority from evidence of liveness", "Explain leases, expiration and paused-process hazards", "Use monotonically checked fencing tokens at the protected resource", "Design ownership transfer across clock and network uncertainty"],
          ["lease expiry and stale-writer timeline", "Can the old owner still write after losing its lease?", "Pause a client through lease expiration"],
          ["Repair a distributed job-owner design", "The resource rejects stale owners rather than trusting a lock alone"],
          "Acquiring a new lock does not stop an old paused process from resuming", [source.distributed, source.raft]),
        plan("Sharding, Consistent Hashing, Hot Keys & Rebalancing", "intermediate", [replication, capacity],
          ["Choose hash, range or directory partitioning from access patterns", "Explain consistent and rendezvous hashing with skew", "Move data while preserving routing and write ownership", "Detect hot keys, cross-shard queries and rebalance amplification"],
          ["partition ownership and movement map", "Does adding a shard fix a single hot key?", "Change key popularity and node membership"],
          ["Design a live shard split", "Show reads and writes during movement and failure recovery"],
          "Even key counts do not guarantee even workload"),
        plan("Distributed Transactions, Two-Phase Commit & Atomic Commit", "advanced", [transactions, consensus, "Sharding, Consistent Hashing, Hot Keys & Rebalancing"],
          ["Specify atomicity across separately failing participants", "Trace prepare, commit and recovery in two-phase commit", "Distinguish consensus replication from transaction commit", "Assess blocking, coordinator recovery and alternative boundaries"],
          ["participant commit-state timeline", "What may a prepared participant do after coordinator failure?", "Crash the coordinator between protocol steps"],
          ["Analyze a cross-shard money transfer", "Preserve atomicity and state the availability cost"],
          "Consensus and two-phase commit solve related but different problems", [source.distributed, source.postgres]),
        plan("CRDTs, Conflict Resolution & Offline-First Synchronization", "advanced", [consistency],
          ["Represent concurrent updates without assuming a global order", "Compare state-based and operation-based convergence rules", "Explain causal metadata, tombstones and conflict-aware user workflows", "Identify invariants that require coordination beyond merging"],
          ["branch-and-merge replica history", "Can convergent replicas still violate a business rule?", "Edit offline and merge in different orders"],
          ["Specify an offline collaborative list", "State convergence, deletion and uniqueness semantics"],
          "Eventual convergence does not guarantee every application invariant", [source.distributed]),
        plan("Multi-Region Architecture, Data Residency & Disaster Recovery", "advanced", [replication, "Distributed Transactions, Two-Phase Commit & Atomic Commit"],
          ["Place reads, writes and authoritative data across regions", "Compare active-passive and active-active operation", "Budget RPO, RTO, latency, residency and inter-region cost", "Practice region loss, failback and split-brain prevention"],
          ["regional authority and recovery timeline", "Which data can be lost at this failover point?", "Partition regions and inspect acknowledged writes"],
          ["Design and rehearse a regional failover", "Document authority, allowable data loss and return-to-normal behavior"],
          "Multiple regions alone do not create a tested disaster-recovery plan"),
        plan("Distributed SQL, Logical Timestamps & Externally Consistent Transactions", "advanced", ["Distributed Transactions, Two-Phase Commit & Atomic Commit", "Multi-Region Architecture, Data Residency & Disaster Recovery"],
          ["Combine partitioned storage, replicated logs and transaction coordination", "Compare physical uncertainty, logical and hybrid logical timestamps", "Explain timestamp ordering, commit waits and external consistency assumptions", "Analyze cross-region transaction latency, locality and failure behavior"],
          ["timestamp uncertainty and commit timeline", "What must be true before a commit is ordered after an earlier response?", "Widen clock uncertainty and inspect waiting"],
          ["Compare two distributed SQL transaction designs", "Distinguish timestamp mechanism, replication and isolation guarantees"],
          "Synchronized clocks alone do not implement distributed transactions", [source.spanner, source.distributed]),
        plan("Membership Changes, Snapshots, Gossip & Anti-Entropy", "advanced", [consensus, "Sharding, Consistent Hashing, Hot Keys & Rebalancing"],
          ["Separate membership authority from gossip-based discovery", "Change replica configurations without incompatible quorums", "Install snapshots and compact logs while preserving recovery", "Use anti-entropy and failure detection with explicit convergence limits"],
          ["membership epochs and snapshot transfer timeline", "Can two different configurations both believe they own the data?", "Change membership during a partition"],
          ["Design a replica replacement and recovery procedure", "Preserve quorum intersection, ownership and snapshot consistency"],
          "Gossip spreads information but does not automatically provide consensus", [source.raft, source.distributed]),
      ],
    },
    {
      name: "Caching, Messaging & Durable Workflows",
      topics: [
        plan("Caching Strategies, Invalidation & Stampede Control", "intermediate", [consistency, capacity],
          ["Compare cache-aside, read-through, write-through and write-behind", "Define keys, eviction, TTL and invalidation contracts", "Handle stampedes, penetration, hot keys and stale reads", "Prevent cache failure from overwhelming the source of truth"],
          ["cache-database race timeline", "Can invalidation still leave an old value in the cache?", "Interleave cache fills with writes"],
          ["Design a cache for mutable account data", "State staleness bounds, failure behavior and consistency risks"],
          "Caching can change correctness, not only speed"),
        plan(queues, "intermediate", [failure, replication],
          ["Separate work queues from broadcast subscriptions", "Trace publish acknowledgment, delivery, acknowledgment and redelivery", "Compare at-most-once, at-least-once and scoped exactly-once guarantees", "Design retention, dead letters, poison-message handling and replay"],
          ["message ownership and acknowledgment trace", "What happens if a worker crashes after doing the work?", "Crash before and after acknowledgment"],
          ["Specify a durable background-job pipeline", "Account for duplicate effects, retries and permanently failing jobs"],
          "Acknowledgment timing must match the durable business effect", [source.kafka]),
        plan("Partitioned Logs, Consumer Groups & Event Ordering", "intermediate", [queues, "Sharding, Consistent Hashing, Hot Keys & Rebalancing"],
          ["Map event keys to log partitions and consumer ownership", "Explain offsets, retention, replay and consumer-group rebalances", "Distinguish partition order from global and causal order", "Handle hot partitions, duplicate consumption and changing keys"],
          ["partition log and consumer cursor view", "Which order survives a consumer rebalance?", "Move ownership while processing events"],
          ["Design a keyed event pipeline", "Specify offset commits, ordering scope and recovery"],
          "A distributed log does not generally provide total order across partitions", [source.kafka]),
        plan("Backpressure, Bounded Queues & Stream Flow Control", "intermediate", [queues, capacity],
          ["Model producer rate, service rate and queued work", "Compare pull demand, credit, blocking, dropping and load shedding", "Propagate pressure across asynchronous stages", "Bound memory and latency under sustained overload"],
          ["pipeline queue occupancy plot", "Where does unbounded work accumulate?", "Slow one consumer and compare admission policies"],
          ["Design a bounded ingestion pipeline", "State maximum backlog, drop policy and recovery behavior"],
          "A queue delays overload; it does not create processing capacity", [source.kafka, source.sre]),
        plan("Transactional Outbox, Inbox & Change Data Capture", "advanced", [transactions, queues],
          ["Identify the database-plus-message dual-write failure", "Persist events with business state through an outbox", "Compare polling and change-data-capture publication", "Handle duplicates, schema changes, ordering and cleanup"],
          ["database commit and publication timeline", "Can a state update occur without its event?", "Crash at each dual-write boundary"],
          ["Repair an order-created event pipeline", "Demonstrate atomic intent and idempotent downstream handling"],
          "An outbox does not remove duplicate delivery or cross-service coordination", [source.postgres, source.kafka]),
        plan("Sagas, Compensation & Durable Workflow Engines", "advanced", [queues, "Idempotency Keys, Deduplication & Exactly-Once Effects"],
          ["Represent a long-running workflow as durable states and actions", "Compare orchestration and choreography", "Design retries, timers, compensation and manual resolution", "Handle non-reversible effects and versioned workflow code"],
          ["workflow state and compensation graph", "What if refund succeeds but inventory release fails?", "Inject failure after each external effect"],
          ["Design a booking-payment-fulfillment saga", "Specify recovery and compensations without claiming ACID rollback"],
          "Compensation is a new business action and can also fail"),
        plan("Event Sourcing, CQRS & Projection Evolution", "advanced", ["Partitioned Logs, Consumer Groups & Event Ordering", dataModel],
          ["Distinguish event history from a mutable current-state store", "Separate command validation and read projections", "Handle event versioning, snapshots, replay and erasure obligations", "Evaluate when auditability benefits justify operational complexity"],
          ["event stream and two projections", "Can the same events build different read models safely?", "Replay an evolving projection from a snapshot"],
          ["Specify an event-sourced account aggregate", "Preserve invariants, version compatibility and rebuild semantics"],
          "Using a message broker does not by itself mean event sourcing"),
        plan("Job Scheduling, Timers & Distributed Task Execution", "intermediate", [queues, "Leader Election, Leases, Distributed Locks & Fencing Tokens"],
          ["Model scheduled, recurring and dependency-driven work", "Design claiming, leases, retries and cancellation", "Handle clock changes, missed schedules and duplicate triggers", "Provide fairness, resource quotas and observable task history"],
          ["schedule-claim-run-retry timeline", "Can two workers run the same overdue job?", "Expire a lease during a long task"],
          ["Design a durable scheduler with resumable workers", "Bound duplicates and protect side effects with fencing or idempotency"],
          "Exactly one scheduled trigger does not guarantee exactly one execution"),
      ],
    },
    {
      name: "Data Platforms, Analytics & ML System Interfaces",
      topics: [
        plan("Batch Data Platforms, Warehouses, Lakes & Lakehouses", "intermediate", ["Object Storage, Filesystems, Block Storage & Erasure Coding", dataModel],
          ["Separate operational data from analytical workloads", "Compare warehouse, lake and lakehouse responsibilities", "Explain columnar formats, partitioning, catalogs and table snapshots", "Plan incremental ingestion, compaction, lineage and access controls"],
          ["operational-to-analytical data lifecycle", "Which snapshot did this analysis actually read?", "Introduce a late backfill and inspect versioned tables"],
          ["Design an auditable analytical dataset", "Define ownership, quality, freshness and reproducible snapshots"],
          "Putting files in object storage does not provide transaction or catalog semantics", [source.databases, source.kafka]),
        plan("Distributed Query Engines, Joins, Shuffles & Analytical Execution", "advanced", ["Batch Data Platforms, Warehouses, Lakes & Lakehouses", "Database Indexes, B-Trees, Hash Indexes & Query Planning"],
          ["Turn a logical analytical query into partitioned physical operators", "Compare broadcast, shuffle, sort-merge and partitioned joins", "Handle skew, spill, pushdown, vectorization and partial aggregation", "Recover stages and measure data movement rather than only worker count"],
          ["query DAG and shuffle-volume map", "Why does adding workers fail to speed up this join?", "Change key skew and broadcast size"],
          ["Optimize a distributed analytical query plan", "Account for network, memory, skew and reproducible results"],
          "Parallel execution cannot eliminate the cost of moving and redistributing data", [source.databases]),
        plan("Stream Processing, Event Time, Watermarks & Windowing", "advanced", ["Partitioned Logs, Consumer Groups & Event Ordering", "Backpressure, Bounded Queues & Stream Flow Control"],
          ["Separate event time, ingest time and processing time", "Compare tumbling, sliding and session windows", "Explain watermarks, late events, retractions and state expiry", "Coordinate checkpoints and output effects across restart"],
          ["event-time versus arrival-time timeline", "When may this window result be considered final?", "Delay and reorder events around the watermark"],
          ["Compute a corrected aggregate after late arrivals", "State lateness, retention and output-update guarantees"],
          "A watermark is a policy or estimate, not proof that no earlier event exists", [source.kafka]),
        plan("Data Contracts, Schema Registries & Pipeline Quality", "intermediate", [dataModel, "Transactional Outbox, Inbox & Change Data Capture"],
          ["Define meaning, types, units and ownership at data boundaries", "Check backward and forward schema compatibility", "Validate freshness, completeness, uniqueness and lineage", "Handle backfills, quarantine, deletion and producer-consumer coordination"],
          ["schema-version compatibility matrix", "Can every deployed consumer read the new event?", "Add, remove and reinterpret fields"],
          ["Plan a safe event-schema migration", "Test old and new producers and consumers together"],
          "Type-compatible data can still be semantically wrong"),
        plan("Search System Architecture: Crawling, Indexing & Retrieval", "advanced", ["Time-Series Databases, Search Indexes & Specialized Retrieval", "Partitioned Logs, Consumer Groups & Event Ordering"],
          ["Trace documents from discovery through parsing and indexing", "Design inverted indexes, updates, ranking and query fanout", "Handle deduplication, freshness, access filtering and abuse", "Evaluate relevance, latency and index recovery separately"],
          ["document-to-query retrieval pipeline", "Why is a known document absent from results?", "Inspect crawl, index and permission stages"],
          ["Design an enterprise search system", "Define deletion propagation, authorization and measurable relevance"],
          "Fast approximate retrieval is not by itself high-quality search"),
        plan("ML Serving Architecture, Feature Freshness & Model Rollouts", "advanced", ["Data Contracts, Schema Registries & Pipeline Quality", "API Design: REST, RPC, GraphQL & Compatibility"],
          ["Separate offline training from online decision serving", "Define feature computation, freshness and model-version contracts", "Design batching, caches, fallbacks and staged model rollout", "Evaluate latency, quality, drift and rollback together"],
          ["feature-and-model version trace", "Did training and serving observe the same feature meaning?", "Change a feature pipeline version under a deployed model"],
          ["Design a bounded fraud-scoring serving service", "Document point-in-time features, fallback rules and model rollback"],
          "A model endpoint does not constitute a complete ML system"),
        plan("Vector Search, RAG & AI Application System Boundaries", "advanced", ["Search System Architecture: Crawling, Indexing & Retrieval", "ML Serving Architecture, Feature Freshness & Model Rollouts"],
          ["Separate retrieval, context assembly, generation and evaluation", "Design chunk and embedding versions, indexes and permission filters", "Handle deletion, prompt injection, tool permissions and fallback", "Budget latency, cost, freshness and evidence quality"],
          ["document-permission-to-answer provenance map", "Can a retrieved chunk bypass the user's access scope?", "Change document permissions and rebuild status"],
          ["Specify an auditable document-answering system", "Trace evidence and enforce access before generation"],
          "A vector database does not guarantee grounded or authorized answers"),
      ],
    },
    {
      name: "Reliability, Observability & Performance Engineering",
      topics: [
        plan("SLIs, SLOs, Error Budgets & Availability Modeling", "intermediate", [framing, capacity],
          ["Choose user-visible indicators and measurement populations", "Set objectives, windows and error-budget policies", "Model dependency availability and correlated failures", "Connect reliability targets to release and capacity decisions"],
          ["request outcome and error-budget burn chart", "Does the service meet its promise for this user group?", "Change aggregation window and regional failure"],
          ["Define an SLO for a critical workflow", "Specify valid events, objectives, exclusions and actionable burn alerts"],
          "Averages and aggregate uptime can conceal a failing user population", [source.sre, source.workbook]),
        plan(observability, "intermediate", [services],
          ["Instrument a request across service and data boundaries", "Compare metrics, structured logs, traces and profiles", "Manage sampling, cardinality, correlation and sensitive fields", "Use evidence to test a concrete failure hypothesis"],
          ["linked trace, metric and log timeline", "Which dependency explains the tail-latency spike?", "Inspect one slow request against aggregate distributions"],
          ["Diagnose a synthetic incident from telemetry", "Support the root-cause hypothesis and bound missing evidence"],
          "Collecting more telemetry is not automatically better observability", [source.sre, source.telemetry]),
        plan("Tail Latency, Queueing, Fanout & Performance Diagnosis", "advanced", [capacity, observability],
          ["Measure distributions rather than only means", "Explain fanout amplification, queueing and utilization near saturation", "Compare batching, caching, parallelism and hedged requests", "Identify bottlenecks with load-controlled experiments"],
          ["fanout latency and queue occupancy curves", "Why does one slow shard dominate the request?", "Increase fanout and correlate dependency delays"],
          ["Diagnose a tail-latency regression", "Separate service time, waiting, sampling artifacts and retries"],
          "Percentiles cannot generally be averaged into a global percentile", [source.sre]),
        plan("Circuit Breakers, Bulkheads, Load Shedding & Graceful Degradation", "intermediate", ["Timeouts, Deadlines, Retries & Exponential Backoff", "Backpressure, Bounded Queues & Stream Flow Control"],
          ["Bound the blast radius of a failing dependency", "Compare circuit breaking, concurrency limits and bulkheads", "Prioritize work and define safe degraded responses", "Test recovery, oscillation and overload admission"],
          ["dependency pressure and breaker state", "Does rejecting work early preserve the critical workflow?", "Overload a low-priority feature and inspect isolation"],
          ["Design a degradation policy for a checkout flow", "Protect invariants and distinguish reduced features from false success"],
          "A fallback must preserve the promise made to the user"),
        plan("Load Testing, Benchmark Validity & Capacity Experiments", "intermediate", [capacity, observability],
          ["Build workload distributions from stated access patterns", "Compare open and closed load generation", "Detect coordinated omission, cache bias and unrealistic data sizes", "Report saturation, error rates, percentiles and reproducible configurations"],
          ["offered versus completed load plot", "Did the test stop sending work when the service slowed?", "Compare fixed-arrival and response-paced load"],
          ["Design a reproducible service benchmark", "Include warmup, data shape, failure rates and load-generator limits"],
          "A high throughput number without workload and error context is not useful evidence"),
        plan("Incident Management, On-Call, Runbooks & Blameless Reviews", "intermediate", [observability, "SLIs, SLOs, Error Budgets & Availability Modeling"],
          ["Detect and triage a user-impacting incident", "Separate mitigation, diagnosis, communication and command roles", "Use runbooks, escalation and evidence preservation", "Produce a review with causal factors and verifiable follow-up actions"],
          ["incident decision and evidence timeline", "Which action reduced user impact before root cause was known?", "Reveal observations in time order"],
          ["Run a tabletop outage and write its review", "Record decisions, impact and actions without hindsight certainty"],
          "A blameless review still assigns clear ownership to improvements", [source.sre, source.workbook]),
        plan("Fault Injection, Chaos Experiments & Resilience Validation", "advanced", ["Incident Management, On-Call, Runbooks & Blameless Reviews", "Circuit Breakers, Bulkheads, Load Shedding & Graceful Degradation"],
          ["State a resilience hypothesis and measurable steady state", "Choose bounded faults, blast radius and abort criteria", "Inject process, network, dependency and resource failures", "Distinguish demonstrated behavior from untested failure combinations"],
          ["failure-domain and impact map", "Does this supposedly redundant path share the failed dependency?", "Remove one dependency within a controlled scenario"],
          ["Design a safe staging resilience experiment", "Include observability, stop conditions and recovery evidence"],
          "Randomly killing services is not a well-designed chaos experiment"),
        plan("Correctness Testing, Model Checking & Deterministic Simulation", "advanced", [consensus, transactions],
          ["Express safety and liveness properties before implementation", "Generate small concurrent histories and fault schedules", "Use property tests, state exploration and history checkers", "Build deterministic replay and minimize counterexamples"],
          ["state-space and failing-history explorer", "Which smallest schedule violates the invariant?", "Step a reduced counterexample"],
          ["Check a bounded replicated service model", "Separate tested bounds from a general proof and retain replay seeds"],
          "Passing many happy-path tests does not establish distributed correctness", [source.raft, source.distributed]),
      ],
    },
    {
      name: "Security, Privacy & Multi-Tenant Isolation",
      topics: [
        plan(security, "foundation", [services, dataModel],
          ["Identify assets, actors, trust boundaries and abuse cases", "Trace inputs and privileges through the architecture", "Apply least privilege, secure defaults and defense in depth", "Connect each threat to a control, test and residual risk"],
          ["data-flow and trust-boundary diagram", "Which untrusted input crosses a privileged boundary?", "Trace attacker-controlled fields through the design"],
          ["Threat-model a file-sharing system", "Protect assets through concrete controls and testable assumptions"],
          "Internal network placement is not a complete trust boundary"),
        plan("Authentication, OAuth, OpenID Connect & Session Security", "intermediate", [security, "Sessions, Stateless Services & Distributed Application State"],
          ["Separate identity proof, delegated access and application sessions", "Trace authorization-code flow, PKCE, token audiences and lifetimes", "Handle refresh, revocation, MFA and account recovery", "Protect cookies, redirects and browser request boundaries"],
          ["identity-provider and application sequence", "Which party is allowed to consume this token?", "Change issuer, audience and redirect destination"],
          ["Review a login and delegated-access design", "Reject token confusion and document revocation and recovery semantics"],
          "OAuth is an authorization framework, not by itself a user-authentication protocol", [source.oauth, source.openid]),
        plan("Authorization, RBAC, ABAC & Relationship-Based Permissions", "intermediate", [security, consistency],
          ["Model subjects, resources, actions and permission relationships", "Compare role, attribute and relationship-based decisions", "Propagate revocation consistently through caches and search", "Enforce object-level access at every relevant operation"],
          ["permission graph and revocation timeline", "Can stale authorization permit access after removal?", "Revoke membership during an in-flight request"],
          ["Design document-sharing permissions", "Address inheritance, revocation, audit and cache consistency"],
          "Authentication does not imply permission to every object"),
        plan("Encryption, Key Management, Secrets & Certificate Rotation", "intermediate", [security, "TCP, UDP, QUIC, TLS & Connection Lifecycle Design"],
          ["Separate confidentiality, integrity and authenticity guarantees", "Design envelope encryption and key hierarchy", "Manage secrets, certificate issuance, rotation and revocation", "Plan backups and recovery without exposing keys"],
          ["key hierarchy and rotation timeline", "Can old data still be recovered after this key change?", "Rotate a wrapping key and revoke a credential"],
          ["Design a service secret and encryption lifecycle", "Document access, audit, rotation and recovery failure modes"],
          "Encryption at rest does not protect data from an overprivileged running service"),
        plan("Multi-Tenant Architecture, Noisy Neighbors & Isolation", "advanced", [security, "Rate Limiting, Quotas & Admission Control", "Sharding, Consistent Hashing, Hot Keys & Rebalancing"],
          ["Compare shared tables, schemas, databases and dedicated deployments", "Enforce tenant identity in data, caches, jobs and telemetry", "Allocate fair resource budgets and limit blast radius", "Plan tenant migration, deletion and tier-specific guarantees"],
          ["tenant-resource isolation matrix", "Can one tenant's workload delay or expose another's data?", "Increase a tenant load and inspect shared bottlenecks"],
          ["Design a multi-tenant analytics service", "Demonstrate data isolation and resource fairness under adversarial load"],
          "A tenant ID column is not sufficient tenant isolation"),
        plan("Privacy, Retention, Deletion & Audit Evidence", "intermediate", [security, "Transactional Outbox, Inbox & Change Data Capture"],
          ["Classify data and minimize collection and access", "Trace retention and deletion through replicas, indexes, events and backups", "Separate operational audit evidence from unnecessary personal data", "Map jurisdiction-specific obligations to verified controls"],
          ["data-copy and deletion propagation graph", "Which copy survives a deletion request?", "Track deletion through async projections and restore"],
          ["Design verifiable deletion for a user dataset", "State scope, delay, backup policy and evidence without universal legal claims"],
          "Deleting the primary row does not necessarily delete all derived copies", [source.privacy]),
        plan("Abuse Prevention, DDoS, Fraud & Software Supply-Chain Security", "advanced", [security, "Rate Limiting, Quotas & Admission Control"],
          ["Distinguish volumetric attacks, expensive valid requests and business abuse", "Combine traffic controls, identity friction and risk signals", "Protect builds, dependencies, artifacts and production access", "Plan incident response with false-positive and availability costs"],
          ["attack path and control-layer map", "Can a small request trigger disproportionate work?", "Change request cost while keeping request rate fixed"],
          ["Review an expensive endpoint and its release pipeline", "Bound amplification and trace trusted artifact provenance"],
          "Rate limiting by IP alone is not complete abuse or DDoS protection"),
      ],
    },
    {
      name: "Cloud Infrastructure, Deployment & Architecture Evolution",
      topics: [
        plan("Virtual Machines, Containers, Isolation & Runtime Resources", "intermediate", ["Operating-System Mechanisms for Service Design", security],
          ["Compare VM and container isolation boundaries", "Explain images, namespaces, cgroups and runtime limits", "Budget memory, CPU, storage and network resources", "Diagnose OOM, throttling and shared-host effects"],
          ["host and workload isolation map", "Why is this container throttled while the host looks idle?", "Change quotas and shared workload pressure"],
          ["Specify runtime limits for a service", "Explain isolation, scheduling and failure consequences"],
          "Containers do not necessarily provide the same isolation as virtual machines", [source.kubernetes]),
        plan("Kubernetes Architecture, Scheduling & Service Operation", "advanced", ["Virtual Machines, Containers, Isolation & Runtime Resources", "DNS, Anycast, Service Discovery & Traffic Steering"],
          ["Separate control-plane desired state from running workloads", "Explain scheduling, services, probes, controllers and reconciliation", "Manage rollouts, disruption budgets, storage and secrets", "Diagnose control-plane, node and workload failure separately"],
          ["reconciliation loop and pod-placement map", "Why does a ready process still fail application requests?", "Change probes and dependency state"],
          ["Design a resilient service deployment", "Explain readiness, termination, disruption and resource policies"],
          "An orchestrator cannot infer every application correctness condition", [source.kubernetes]),
        plan("Serverless, Managed Services & Build-versus-Buy Decisions", "intermediate", [capacity, "Architecture Styles: Modular Monoliths, Services & Event-Driven Systems"],
          ["Define which operational responsibility a managed service absorbs", "Compare serverless, managed databases and self-operated systems", "Model cold starts, quotas, portability and failure visibility", "Estimate total lifecycle cost and exit constraints"],
          ["responsibility and cost boundary comparison", "Which responsibility remains with the application team?", "Change workload shape and vendor limits"],
          ["Write a build-versus-buy decision record", "Include operational burden, migration costs and failure constraints"],
          "Managed infrastructure does not remove application-level reliability work"),
        plan("Infrastructure as Code, Configuration & Environment Reproducibility", "intermediate", ["Virtual Machines, Containers, Isolation & Runtime Resources"],
          ["Represent desired infrastructure and configuration as versioned inputs", "Plan state management, drift detection and secret separation", "Test changes and isolate environments and permissions", "Recover from partial application and accidental configuration changes"],
          ["desired-versus-actual infrastructure diff", "Which unreviewed change caused environment drift?", "Modify a resource outside the declared configuration"],
          ["Design a reproducible staging environment", "Track dependencies, secrets, approvals and rollback limitations"],
          "Declarative configuration is not proof that applying it is risk-free"),
        plan("Cloud Networks, Service Meshes & Control-Plane Separation", "advanced", ["Kubernetes Architecture, Scheduling & Service Operation", security],
          ["Design subnets, routing, NAT, private access and network policy", "Separate traffic data planes from configuration and identity control planes", "Evaluate service meshes, mutual TLS and sidecar or proxy costs", "Diagnose policy propagation, shared control dependencies and route failures"],
          ["network and control-plane dependency diagram", "Does a control-plane outage stop existing traffic or only updates?", "Freeze configuration and interrupt identity refresh"],
          ["Design connectivity and policy for a private service estate", "Document egress, authorization, propagation delay and failure behavior"],
          "A service mesh adds capabilities and failure modes rather than removing application responsibilities", [source.kubernetes, source.sre]),
        plan("CI/CD, Canary Releases, Feature Flags & Safe Rollback", "intermediate", [observability, "Data Modeling, Invariants & Schema Evolution"],
          ["Separate build verification, deployment and feature release", "Compare rolling, blue-green and canary strategies", "Handle mixed-version protocols, schemas and feature flags", "Define automated promotion and rollback from user-impact evidence"],
          ["version rollout and compatibility timeline", "Can the old binary still read data written by the new one?", "Rollback after a schema-writing release"],
          ["Plan a safe stateful-service rollout", "Include compatibility windows, stop signals and irreversible-change handling"],
          "Rolling back a binary does not undo data mutations", [source.sre, source.workbook]),
        plan("Schema Migrations, Backfills & Zero-Downtime Evolution", "advanced", [dataModel, transactions, "CI/CD, Canary Releases, Feature Flags & Safe Rollback"],
          ["Expand a schema while old and new code coexist", "Backfill with bounded load, resumability and correctness checks", "Switch reads and writes through a verified compatibility window", "Contract obsolete state with a recovery plan"],
          ["expand-migrate-contract timeline", "Which application versions can safely coexist at this step?", "Interrupt a backfill and roll back a reader"],
          ["Migrate an indexed field on a busy service", "Bound locks, load, data divergence and rollback scope"],
          "Adding a nullable column is not the entire migration"),
        plan("Monolith Decomposition, Legacy Integration & Strangler Migrations", "advanced", ["Domain Modeling, Bounded Contexts & Service Ownership", "Schema Migrations, Backfills & Zero-Downtime Evolution"],
          ["Identify a migration goal and a bounded ownership seam", "Route traffic incrementally with anti-corruption adapters", "Separate data ownership without unsafe dual writes", "Measure parity and retire old paths only after reconciliation"],
          ["old-new traffic and data ownership map", "Which system is authoritative during the migration?", "Shift one workflow and compare shadow results"],
          ["Plan an incremental billing-service extraction", "Document authority, compatibility, cutover and rollback"],
          "Breaking a monolith into network calls can preserve or worsen its coupling"),
        plan("FinOps, Capacity Planning, Sustainability & Engineering Economics", "intermediate", [capacity, observability],
          ["Attribute cost to workloads, tenants and user outcomes", "Compare compute, storage, egress and operational labor", "Plan reserved, elastic and interruption-tolerant capacity", "Evaluate efficiency changes against reliability and quality budgets"],
          ["cost per useful operation and capacity curve", "Did the optimization merely move cost elsewhere?", "Change retention, replication and traffic geography"],
          ["Propose a measured cost-reduction plan", "Preserve declared reliability and correctness while stating energy-estimation limits"],
          "Lower cloud spend alone is not lower total engineering cost"),
      ],
    },
    {
      name: "End-to-End Product & Infrastructure Design Studios",
      topics: [
        plan("Design Studio: URL Shortener, Redirects & Abuse Controls", "intermediate", ["Distributed IDs, Ordering & Uniqueness Guarantees", "Caching Strategies, Invalidation & Stampede Control"],
          ["Specify creation, redirect, expiration and custom-alias workflows", "Choose identifiers, storage and cache behavior", "Separate redirect availability from analytics ingestion", "Handle malicious links, hot destinations and deletion propagation"],
          ["redirect critical path and analytics branch", "Should analytics failure block the redirect?", "Fail analytics and expire a cached link"],
          ["Design and benchmark a bounded redirect service", "Protect alias uniqueness and define cache and abuse behavior"],
          "A shortener's core challenge is more than generating a small string"),
        plan("Design Studio: Chat, Presence & Multi-Device Messaging", "advanced", ["WebSockets, Server-Sent Events, Webhooks & Realtime APIs", "Partitioned Logs, Consumer Groups & Event Ordering"],
          ["Define conversation membership and delivery-read semantics", "Design message IDs, ordering, history and offline synchronization", "Model presence, typing and fanout with weaker transient guarantees", "Handle reconnection, permission changes and multi-device duplicates"],
          ["conversation and device cursor timeline", "What does delivered mean across several devices?", "Disconnect one device and revoke a member"],
          ["Design a multi-device group messaging service", "Separate durable messages from transient presence and receipts"],
          "Online presence is a fallible estimate, not certain user availability"),
        plan("Design Studio: Social Feeds, Fanout & Notification Delivery", "advanced", [queues, "Caching Strategies, Invalidation & Stampede Control"],
          ["Specify feed freshness, privacy and ranking inputs", "Compare fanout-on-write, fanout-on-read and hybrid strategies", "Handle celebrity skew, edits, deletes and blocked relationships", "Design notification preferences, deduplication and quiet hours"],
          ["fanout and merge cost map", "Which users make precomputed feeds expensive?", "Change follower skew and posting frequency"],
          ["Design feed delivery with privacy changes", "Bound fanout and propagate deletion and permission changes"],
          "A feed cache must not bypass updated visibility rules"),
        plan("Design Studio: Payments, Ledgers & Financial Reconciliation", "advanced", [transactions, "Idempotency Keys, Deduplication & Exactly-Once Effects", "Sagas, Compensation & Durable Workflow Engines"],
          ["Specify monetary units, balances and double-entry invariants", "Separate payment intent, provider outcome and ledger posting", "Handle retries, refunds, chargebacks and uncertain external effects", "Reconcile internal books with independent provider statements"],
          ["payment state and balanced ledger trace", "Did a timed-out charge succeed at the provider?", "Retry and reconcile delayed provider records"],
          ["Build a simulated payment and reconciliation design", "Preserve balanced postings and never infer failure from timeout alone"],
          "A successful API response is not the whole payment settlement lifecycle"),
        plan("Design Studio: Inventory, Ticketing & Reservation Contention", "advanced", [transactions, "Leader Election, Leases, Distributed Locks & Fencing Tokens"],
          ["Define inventory conservation and reservation expiry", "Compare pessimistic, optimistic and queued allocation", "Coordinate payment and expiration without overselling", "Handle hot items, bots, fairness and canceled reservations"],
          ["inventory and reservation state machine", "Who owns the last seat when payment and expiry race?", "Interleave expiry, payment and confirmation"],
          ["Design a high-contention booking workflow", "Prove inventory invariants across retries and compensation"],
          "A cache lock alone is not durable inventory ownership"),
        plan("Design Studio: File Sync, Collaboration & Version Conflicts", "advanced", ["Object Storage, Filesystems, Block Storage & Erasure Coding", "CRDTs, Conflict Resolution & Offline-First Synchronization"],
          ["Separate file content, metadata, permissions and versions", "Design chunking, resumable transfer and change detection", "Handle concurrent edits, deletes and offline conflicts", "Propagate sharing revocation and recover previous versions"],
          ["device branches and chunk graph", "How should simultaneous rename and edit be merged?", "Reconnect devices after conflicting offline actions"],
          ["Design a conflict-aware sync service", "State data-loss, conflict resolution and permission guarantees"],
          "Content deduplication does not solve collaborative editing semantics"),
        plan("Design Studio: Video Streaming, Transcoding & Live Delivery", "advanced", ["CDNs, Edge Caching & Geographic Content Delivery", "Job Scheduling, Timers & Distributed Task Execution"],
          ["Trace upload or live ingest through encoding and packaging", "Design renditions, adaptive bitrate, storage and CDN delivery", "Budget startup delay, buffering, latency and bandwidth", "Handle failed transcodes, regional demand and access rights"],
          ["media pipeline and playback-buffer timeline", "Why does a fast network still produce playback stalls?", "Change bitrate, segment availability and bandwidth variability"],
          ["Design live and on-demand delivery for one service", "Separate latency, reliability, cost and quality tradeoffs"],
          "Live streaming and video on demand require different latency and recovery choices"),
        plan("Design Studio: Maps, Geospatial Search & Ride Dispatch", "advanced", ["Database Indexes, B-Trees, Hash Indexes & Query Planning", "Stream Processing, Event Time, Watermarks & Windowing"],
          ["Represent locations, spatial indexes and changing availability", "Compare proximity search with route and travel-time estimation", "Coordinate offers, assignment, cancellation and payment workflows", "Handle stale positions, regional partitions and fairness constraints"],
          ["spatial cells and assignment timeline", "Is the nearest available driver the fastest feasible match?", "Change road connectivity and location freshness"],
          ["Design dispatch with stale updates and competing requests", "Prevent conflicting assignment and state location-error limits"],
          "Geographic distance is not the same as travel time"),
        plan("Design Studio: Metrics, Logs & High-Cardinality Telemetry", "advanced", [observability, "Time-Series Databases, Search Indexes & Specialized Retrieval", "Stream Processing, Event Time, Watermarks & Windowing"],
          ["Specify ingest rate, query patterns and retention tiers", "Partition and aggregate metrics, logs and traces separately", "Control cardinality, sampling and tenant quotas", "Recover ingestion without misleading monitoring during outages"],
          ["telemetry pipeline and series-cardinality map", "Which label creates millions of new series?", "Add a user-ID label and inspect resource growth"],
          ["Design an observable telemetry backend", "Bound resource use and distinguish no data from healthy service"],
          "Missing telemetry must not automatically be interpreted as zero errors"),
        plan("Design Studio: Cloud Object Store & Metadata Service", "advanced", [consensus, "Object Storage, Filesystems, Block Storage & Erasure Coding", "Sharding, Consistent Hashing, Hot Keys & Rebalancing"],
          ["Specify object naming, versioning and consistency promises", "Separate metadata authority from bulk data placement", "Design multipart writes, checksums, repair and garbage collection", "Handle orphaned chunks, correlated loss and lifecycle deletion"],
          ["metadata commit and data-fragment placement", "Can metadata expose an incompletely stored object?", "Crash during a multipart commit"],
          ["Design an object store with recovery invariants", "Explain publication, durability and repair under stated failures"],
          "Replicated metadata does not guarantee the underlying bytes are safe"),
        plan("Design Studio: Workflow Platform, CI Runners & Build Cache", "advanced", ["Job Scheduling, Timers & Distributed Task Execution", "Multi-Tenant Architecture, Noisy Neighbors & Isolation"],
          ["Model dependency graphs, artifacts and execution environments", "Schedule workers with quotas, cancellation and durable progress", "Design content-addressed caches and reproducibility boundaries", "Isolate untrusted jobs and recover coordinator or worker loss"],
          ["DAG execution and artifact provenance graph", "Can a cache hit reuse an artifact from the wrong environment?", "Change dependency or toolchain versions"],
          ["Design a multi-tenant build platform", "Track cache validity, sandboxing and at-least-once execution effects"],
          "A matching source hash alone may not describe all build inputs"),
        plan("Design Studio: IoT Ingestion, Device Control & Edge Operation", "advanced", ["Message Queues, Publish-Subscribe & Delivery Semantics", "CRDTs, Conflict Resolution & Offline-First Synchronization", security],
          ["Model device identity, telemetry and desired versus reported state", "Design intermittent connectivity, buffering and command acknowledgment", "Handle firmware rollout, revocation and resource-constrained operation", "Separate cloud availability from local safety and control needs"],
          ["device-cloud synchronization timeline", "Was the command received, executed or merely queued?", "Disconnect the device and change desired state"],
          ["Design a fleet ingestion and update service", "Bound offline storage and define authorization and rollback"],
          "Cloud command acceptance does not prove physical actuation"),
        plan("Design Studio: Experimentation Platforms, Assignment & Metric Integrity", "advanced", ["Data Contracts, Schema Registries & Pipeline Quality", "CI/CD, Canary Releases, Feature Flags & Safe Rollback", "Hypothesis Testing & Confidence Intervals"],
          ["Separate feature delivery, treatment assignment, exposure and outcome measurement", "Design stable randomization units, namespaces, ramping and reproducible configuration", "Trace exposure logs, joins, late events and metric ownership through the platform", "Diagnose sample-ratio mismatch, interference and telemetry bias before interpreting effects"],
          ["assignment-to-metric lineage and missing-event trace", "Can correct assignment still produce a biased experiment result?", "Drop events from one variant and compare assignment, exposure and analysis populations"],
          ["Design a multi-team experiment service with an A/A validation", "Preserve assignment and exposure history, reconcile populations and link statistical assumptions to the analysis owner"],
          "A feature flag rollout is not automatically a valid randomized experiment", [source.experiments]),
      ],
    },
    {
      name: "Advanced Systems & Production Capstones",
      topics: [
        plan("Peer-to-Peer Systems, Byzantine Faults & Trustless Coordination", "advanced", [consensus, security],
          ["Contrast crash-fault consensus with malicious participants", "Explain identities, Sybil resistance and Byzantine quorum assumptions", "Compare permissioned BFT, peer discovery and decentralized ledgers", "Evaluate finality, partitions and incentive or trust boundaries"],
          ["faulty-participant message matrix", "Can conflicting messages create different committed decisions?", "Let one participant equivocate under a bounded model"],
          ["Compare trusted-cluster and adversarial-network designs", "State fault thresholds and identity assumptions precisely"],
          "Removing a central server does not remove trust assumptions", [source.distributed]),
        plan("Real-Time & Embedded System Design: Deadlines, Safety & Control Boundaries", "advanced", ["Operating-System Mechanisms for Service Design", "Tail Latency, Queueing, Fanout & Performance Diagnosis"],
          ["Distinguish hard deadlines from average low latency", "Budget worst-case execution, scheduling and communication", "Separate safety-critical local control from best-effort services", "Validate timing assumptions and degraded operation"],
          ["deadline and scheduling trace", "Can a low average latency still miss a safety deadline?", "Inject a worst-case blocking interval"],
          ["Design an edge-control and cloud-monitoring boundary", "State deadline evidence and keep unsupported safety claims explicit"],
          "A fast web service is not necessarily a hard real-time system"),
        plan("High-Performance Systems: Zero Copy, RDMA & Hardware-Aware Design", "advanced", ["Concurrency, Memory Models & Synchronization for Services", "Tail Latency, Queueing, Fanout & Performance Diagnosis"],
          ["Measure data movement and synchronization before optimizing", "Compare batching, zero copy, memory mapping and RDMA", "Account for NUMA, cache locality, ownership and device lifetimes", "Validate performance without weakening correctness or isolation"],
          ["data-copy and ownership path", "Which copy is actually on the critical path?", "Remove a copy and inspect changed buffer lifetimes"],
          ["Design a bounded high-throughput data path", "Measure bottlenecks and specify memory and failure semantics"],
          "Zero copy is a context-specific claim about a particular boundary", [source.dpdk, source.timestamping]),
        plan("System Design Capstone: Evolve a Service from One Node to Multiple Regions", "advanced", ["Multi-Region Architecture, Data Residency & Disaster Recovery", "CI/CD, Canary Releases, Feature Flags & Safe Rollback", "FinOps, Capacity Planning, Sustainability & Engineering Economics"],
          ["Start with explicit workflows and a working single-node baseline", "Introduce caching, partitioning and replication only for measured needs", "Migrate state and deploy regional operation with recovery exercises", "Deliver architecture decisions, load evidence, cost and operational ownership"],
          ["architecture evolution and evidence timeline", "Which measured limit justifies each new component?", "Advance workload stages and compare alternatives"],
          ["Produce a staged implementation and migration portfolio", "Preserve invariants and verify each scale transition"],
          "The most distributed final diagram is not automatically the best outcome"),
        plan("System Design Capstone: Build & Verify a Replicated Key-Value Service", "advanced", [consensus, "Correctness Testing, Model Checking & Deterministic Simulation", "Storage Internals: Pages, WAL, LSM Trees & Compaction"],
          ["Specify read, write, membership and durability guarantees", "Implement a bounded replicated log and recovery path", "Inject partitions, restarts, delayed messages and snapshot transitions", "Check histories independently and publish measured limits"],
          ["replicated log, client history and recovery view", "Can every acknowledged operation be explained by the promised history?", "Replay minimized failing schedules"],
          ["Deliver a reproducible correctness and performance report", "Distinguish safety evidence, liveness assumptions and tested scale"],
          "A teaching consensus implementation is not automatically production ready"),
        plan("System Design Capstone: Secure Multi-Tenant Event Platform", "advanced", ["Multi-Tenant Architecture, Noisy Neighbors & Isolation", "Partitioned Logs, Consumer Groups & Event Ordering", "Privacy, Retention, Deletion & Audit Evidence"],
          ["Define tenant isolation, delivery, ordering and retention contracts", "Implement ingestion, durable subscriptions and quotas", "Exercise tenant abuse, schema evolution, replay and deletion", "Deliver SLOs, threat model, runbooks and a cost model"],
          ["tenant streams and fault-isolation view", "Can one tenant exhaust or read another tenant's stream?", "Inject noisy-neighbor load and permission changes"],
          ["Build and review a bounded event-platform prototype", "Demonstrate isolation, recovery, retention and delivery guarantees"],
          "A working happy-path demo does not establish tenant isolation or recovery"),
      ],
    },
  ],
};
