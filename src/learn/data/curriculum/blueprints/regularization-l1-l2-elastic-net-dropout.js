export default {
  summary: 'State the preference a penalty adds, calculate the shrinkage and soft-threshold it produces, fit a small design one coordinate at a time, and read a real regularization path without mistaking a development selection score for independent evidence.',
  outcomes: [
    'Write the fitted objective with its 1/2n normalization, its mixing fraction and its unpenalized intercept',
    'Derive ridge shrinkage z/(1+λ) and the soft-threshold S(z, λρ)/[1+λ(1−ρ)], and say when a coefficient is exactly zero',
    'Explain why a unit change alters a raw coefficient penalty without changing any prediction',
    'Run a coordinate sweep from partial residuals, recover the intercept, and check the optimality conditions rather than a small step',
    'Distinguish a strict subthreshold association, which forces zero, from equality, which permits zero or nonzero',
    'Give two coefficient vectors with identical fitted values on duplicate columns, and say why solver order is not evidence',
    'Match Ridge, Lasso and ElasticNet alpha to one declared objective, including the sample-count factor',
    'Keep a learned scaler inside each validation fold, including when an internally cross-validated estimator is used',
    'Derive the exact dropout penalty (1−q)/(2q) Σ wⱼ²xⱼ² for independent masks and squared loss, and state where it stops holding',
    'Read ridge as a filter on singular directions, and separate it from early stopping and from decoupled weight decay',
    'Compute AIC, BIC and a two-part code length, and keep bits and log-likelihood units apart',
  ],
  prerequisites: ['Cross-Validation & Hyperparameter Tuning'],
  sequence: [
    'One objective: half the mean squared error plus a mixed penalty, with the intercept excluded',
    'Units change a penalty without changing a prediction',
    'Scalar shrinkage and soft-thresholding, then the two-dimensional constraint geometry at its actual contact point',
    'Centring, the ridge normal equations, coordinate descent and the KKT stopping rule',
    'Four constructed rows solved by hand, then the same program in NumPy',
    'Duplicate sensors: one prediction, a whole segment of minimizers',
    'Airfoil Self-Noise: twenty polynomial terms, three families, six strengths, three folds and a protected reserve',
    'One development row traced from five measurements to twenty signed contributions',
    'Dropout as an exact expectation over masks, and the penalty it equals in the linear squared-loss case',
    'Deeper: singular directions, L1 paths, solver choice, early stopping and AdamW',
    'Deeper: MAP priors, a parameterization that changes what L2 means, and generalized Tikhonov smoothness',
    'Deeper: AIC, BIC and a fully declared sixteen-bit code',
  ],
  visual: {
    type: 'A one-dimensional objective split into data cost and penalty with both decompositions labelled; six equal-scale constraint panels whose budget is the penalty value the penalised solution actually attained and whose contact point is the arithmetic\'s answer; a duplicate-coefficient plane with the whole lasso minimizer segment and the unique ridge and elastic-net points; measured per-fold and mean validation paths on a log-λ axis with a separately bounded zoom and a signed coefficient panel that marks exact zeros; analytic ridge direction filters; the ab = 1 hyperbola beside its reduced scalar problem; three denoising positions under two different penalties; and stacked AIC/BIC components beside an actual bit code.',
    question: "Is this coefficient negative, exactly zero or positive? What does one coordinate update do to the residual the next coordinate sees? Which way does this airfoil prediction move when one physical measurement changes? And does a mean-preserving mask leave the expected loss alone?",
    interaction: "Edit z, penalty and mixing fraction and follow the soft threshold and exact-zero interval live; edit rows and coordinate order and step actual residual updates; change physical airfoil measurements and inspect linked polynomial terms and fitted output; vary inputs, coefficients and keep probability and inspect all four dropout branches and expected loss immediately.",
  },
  practice: {
    task: 'Ten changed tasks: a negative-preference shrinkage under three families, a target shift that moves only the intercept, the Ridge/Lasso alpha conversion on two fold sizes, duplicate sensors at a new target, an exact dropout expectation at q = 0.75, a misreading of the real experiment, a factor penalty at a new strength, a sixteen-bit encoding, AIC against BIC on fifty observations, and a shifted smoothness problem answered without solving a new system.',
    success: 'A zero coefficient is said to be exactly zero and justified by the threshold; the intercept is recovered and never penalised; a library alpha is converted through the stated objective rather than by name; a duplicate-column answer is given as a set; every airfoil claim names the split, the predeclared grid and what the reserved rows were not used for; and the dropout difference is shown to depend on x, w and q but not on the target.',
  },
  misconceptions: [
    'A smaller penalised objective means a better predictor',
    'A penalty is only worth adding when it improves the training fit',
    'Standardizing is a rule rather than a choice about what one coefficient unit means',
    'L2 can never produce a zero coefficient',
    'Every L1 optimum sits on a diamond corner',
    'The same numerical lambda is the same constraint radius across families',
    'Elastic net always lands between separately tuned ridge and lasso',
    'A coefficient of zero proves the feature is irrelevant to the world',
    'Correlated columns always make the lasso solution nonunique',
    'The coefficient a solver returns on duplicate columns identifies the important sensor',
    'Ridge alpha and Lasso alpha mean the same strength',
    'Fitting a scaler before an internally cross-validated estimator is safe',
    'A regularization path must be U-shaped and have an interior optimum',
    'L1 guarantees a sparse final model',
    'Dropout is just L2 regularization',
    'Inverted dropout preserves the mean, so it leaves the loss unchanged',
    'no_grad() switches dropout into evaluation behaviour',
    'Early stopping is exactly ridge regression',
    'weight_decay is the same operation in every optimizer',
    'AIC, BIC and MDL are the same penalty with different constants',
    'A development-selected score is an independent performance claim',
  ],
  sources: [
    'https://scikit-learn.org/stable/modules/linear_model.html',
    'https://hastie.su.domains/Papers/B67.2%20%282005%29%20301-320%20Zou%20%26%20Hastie.pdf',
    'https://arxiv.org/pdf/1206.0313',
    'https://jmlr.org/papers/volume15/srivastava14a/srivastava14a.pdf',
    'https://nlp.stanford.edu/pubs/wager2013dropout.pdf',
    'https://arxiv.org/pdf/1711.05101',
    'https://homepages.cwi.nl/~pdg/ftp/mdlintro.pdf',
    'https://archive.ics.uci.edu/dataset/291/airfoil+self+noise',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/regularization-l1-l2-elastic-net-dropout/design.md',
  reviewFocus: 'The 1/2n convention and the nλ it forces in the ridge normal equations; the scalar table 1.5/2/(5/3) at z = 3 and the exact zero at z = 0.4 under lasso; the four-row objectives 2.29, 2.58 and 2.496667 coming from different penalties; λ_max = 3; the constraint budgets being attained penalty values rather than λ; the duplicate-column objectives 1.5, 2/3 and 1.1 with sums 1, 4/3 and 1.2; the recorded airfoil table with mean baseline 45.072768 and OLS 17.350268, all three families selecting 0.001, lasso keeping 9 terms per fold at λ = 0.1, zero at λ = 10 and all twenty at the selected refit; the traced prediction 124.465305 dB and 123.628646 dB after frequency 1250 → 1750; the dropout enumeration mean 1 with expected loss 2.5 matching (1−q)/(2q)(4+1); the ridge multipliers 16/17, 0.2, 0.8 and 0.058824 against nλ; early-stopping factors 0.1 and 0.4 needing λ = 9 and 6 separately; p* = max(1−2λ, 0) with total 0.375 at λ = 0.25; the denoising solutions (0.5, 1, 0.5) against (0, 1, 0) under the declared unaveraged objective; and AIC 306/302 against BIC 313.8155/315.0259.',
};
