export default {
  summary: 'Derive stochastic, momentum, coordinate-adaptive and layer-scaled updates, separate decay from the data gradient, and implement and diagnose a stateful optimizer under explicit conventions.',
  outcomes: ['Compute full and sampled gradients and state when the sample mean is unbiased', 'Explain and trace momentum, its convention and a Nesterov lookahead', 'Derive AdaGrad, RMSProp and Adam from their stored gradient histories', 'Calculate bias corrections and distinguish second raw moments from variance', 'Separate coupled L2 regularization from decoupled AdamW shrinkage', 'Calculate LARS and LAMB layer ratios with explicit zero and convention boundaries', 'Run a controlled complete optimization experiment and preserve state for exact continuation', 'Diagnose changes caused by sampling, mean reduction, step size, decay, memory and compute budget'],
  prerequisites: ['Vectors, Matrices & Tensor Operations', 'Multivariate Calculus & Gradients', 'Convex Optimization'],
  sequence: ['Write the empirical mean and inspect a sampled gradient', 'Trace a finite step and its noise or curvature limitations', 'Remember directions with momentum and lookahead', 'Adapt coordinates using accumulated or recent squared gradients', 'Derive Adam moments and initialization correction', 'Separate regularization from direct weight shrinkage', 'Scale explicit parameter blocks with LARS and LAMB', 'Run and resume a complete controlled native experiment', 'Solve changed calculations, failure diagnoses and stateful transfer tasks'],
  visual: { type: 'Batch-slope comparison, quadratic momentum trajectory, gradient-history arithmetic, decay paths and block-relative update geometry', question: 'Which information changes this update, and does its direction, magnitude and stored state match the stated rule?', interaction: 'Select observations; change a momentum rule and advance it; replay a sparse or alternating gradient history; compare coupled and decoupled decay; alter block norms and inspect trust ratios.' },
  practice: { task: 'Calculate an unseen stochastic/momentum/adaptive step, repair a bias or decay error, derive a layer-relative update and correct a microbatch or resumed-state experiment.', success: 'Explicit objective and sampling contract; correct intermediate states and shapes; complete native outputs; independent calculations; no universal optimizer or convergence claims.' },
  misconceptions: ['Every optimizer step opposes the current full gradient and lowers loss', 'SGD always means the same sampling and reduction rule', 'Momentum buffer and normalized EMA use the same learning-rate convention', 'Squared-gradient history is a Hessian or centered variance', 'Bias correction reconstructs the current true gradient under arbitrary drift', 'Adam and AdamW treat a penalty identically', 'A layer ratio guarantees equal relative motion under every momentum and fallback convention', 'Weights alone are enough to resume an optimizer exactly', 'One shared learning rate or one small synthetic curve proves a universal optimizer ranking'],
  sources: ['https://arxiv.org/pdf/1412.6980', 'https://jmlr.org/papers/v12/duchi11a.html', 'https://arxiv.org/pdf/1711.05101', 'https://arxiv.org/pdf/1708.03888', 'https://arxiv.org/pdf/1904.00962', 'https://docs.pytorch.org/docs/2.14/generated/torch.optim.AdamW.html'],
  depth: 'core',
  reviewFocus: 'Sampling conditionality; history and epsilon conventions; Adam correction versus raw state; decay placement; exact block normalization/fallback; trajectory provenance; optimizer restart state; runtime and generalization claims limited to actual evidence.',
  designRecord: 'docs/teaching/GRADIENT-DESCENT-VARIANTS-DESIGN.md',
};
