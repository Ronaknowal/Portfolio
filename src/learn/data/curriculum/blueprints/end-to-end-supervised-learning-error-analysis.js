export default {
  summary: 'Assemble the whole chain on one small real study: a contract before a model, a split whose three parts may change three different things, four candidates declared together, an improvement traced to the specimens it moved, and a held-out report that is not readable until the decision it reports on is frozen.',
  outcomes: [
    'Write an experiment contract that names the prediction unit, the available inputs, the primary measure and what evidence would justify a choice',
    'Say which of training, validation, selection and held-out a given number is, and why those four are not interchangeable',
    'Explain why fitting a scaler before splitting leaks, and why hiding the test target from fit does not prevent leakage through a researcher',
    'Fit a pipeline whose preprocessing is learned on the training rows only, and apply it unchanged to later specimens',
    'Choose baselines that answer stated doubts rather than a single strongest competitor',
    'Compute accuracy, balanced accuracy and log loss from their definitions and say which question each answers',
    'Change one factor at a time and attribute a difference to the factor that changed',
    'Trace an aggregate improvement to the specimens whose outcome moved, in both directions',
    'Show that a net gain of three correct predictions is four repairs minus one new error, and name all five',
    'Find a slice where the aggregate winner is worse, and report it rather than choosing a cut that flatters the result',
    'Distinguish a partition by label from a partition by a feature range, and explain why overlapping subsets need not move together',
    'Apply a declared selection rule, including its tie rule, and say why reading a validation score to choose changes what that number claims',
    'Refuse to report a held-out estimate for a selection the study never evaluated on the test rows',
    'Freeze a model, open a held-out set once, and read a test estimate that exceeds the validation one without treating either as a surprise',
    'Explain when refitting on all development rows is legitimate and why it produces a different artifact to report on',
    'Write a report whose claims are matched to the evidence that produced them',
    'Read a Wilson interval as a scale check under an independent-trial model rather than as an interval for balanced accuracy',
    'Explain why a paired comparison uses the same rows for both models and what treating the estimates as independent discards',
    'Compute coverage, conditional error among answered cases and total cost for an acceptance rule, and show that better conditional accuracy need not lower total cost',
    'Report a conditional error rate as undefined when nothing was answered, rather than as zero',
  ],
  prerequisites: ['Time-Series Validation & Forecasting Baselines', 'Cross-Validation & Hyperparameter Tuning'],
  sequence: [
    'The chain a classifier sits in, and why a notebook score does not tell you whether it answers the question',
    'The experiment contract: prediction unit, target, available inputs, primary measure, split sizes and the final protocol',
    'What this retrospective collection can and cannot support, stated once',
    'Training changes the fit, validation changes the researcher, testing estimates a choice already made',
    'The standardisation identity, and why a validation specimen uses its own x but not a newly fitted validation mean',
    'The information-lane figure: which specimens reach which fitted object, and the human-mediated leak as a labelled counterexample',
    'Table checks before training: identity, label meaning, units, duplicates, missing and impossible values',
    'Four candidates, each answering a stated doubt, declared together before the comparison',
    'Two comparisons that each change one thing: function class, and one measured feature',
    'The softmax, the predicted class, and the forest as a pooling of tree probabilities',
    'Accuracy, balanced accuracy and log loss refreshed from their definitions before anything depends on them',
    'The complete offline study, run verbatim against the served table',
    'The candidate comparison with the majority baseline kept in the frame',
    'A net gain of three contains four repairs and one new error; the paired strip names all five specimens',
    'Training against validation for each candidate, as a diagnostic about the fit and not an estimate',
    'Error slices, denominators, and the validation confusion matrix read as the same predictions differently aggregated',
    'The per-class recall figure: two cultivars reach every specimen correct while one goes backwards',
    'The colour-intensity slices, which reverse the direction of the improvement',
    'Investigation B: choose a cutoff and a side, predict the direction, then see the changed identifiers',
    'Reading an error pattern as a hypothesis with a discriminating next action',
    'Investigation D: choose the eligible candidates and the deciding measure, predict the winner, then freeze it',
    'Why no held-out estimate exists for a selection this study never evaluated on the test rows',
    'The held-out report, opened once, with a test estimate above the validation one',
    'Refitting on all development rows as a different artifact, specified in advance or not at all',
    'The compact final report, and the reproducibility fields that travel with it',
    'The experiment record, and reproducibility as regenerating a study rather than proving it generalises',
    'The inference contract a real application would need next',
    'Deeper: the Wilson interval as a scale check, with its sampling limits stated',
    'Deeper: pairing, the paired bootstrap, and the optimism a validation-selected winner carries',
    'Deeper: nested cross-validation estimates a procedure, not a handpicked fit',
    'Deeper: coverage, conditional error and total cost, with investigation C and its reversed-cost contrast',
  ],
  visual: {
    type: 'Every coordinate is computed in the verified model layer rather than placed by hand, because a cutoff line, a slice membership, a bar length and a repair link are all mathematical claims. Five inline figures and three investigations: an information-lane diagram whose three specimen lanes reach one fitted-pipeline box, with a closed gate on the test lane, a dashed human-mediated leak drawn as an explicit counterexample, and no edge from a label into a transform or from the test lane into the selection; a candidate bar chart that keeps the majority baseline in the frame so "does measuring anything help" has a visible answer; a paired before-and-after strip holding all 36 development specimens in identifier order, linked only where the outcome changed, with the four repairs and one new error named; a per-class recall chart showing two cultivars rising to every specimen correct while a third falls, which is the movement the mean hides; and a training-against-validation table read as a diagnostic about the fit rather than an estimate. The development explorer draws the 36 specimens at their measured alcohol and colour intensity with a movable cutoff, the added flavanoid measurement on its own one-dimensional strip rather than by displacing a point, and a linked per-specimen row. The acceptance queue puts ten constructed cases on a confidence rail and routes them to two queues under a movable threshold.',
    question: "Which candidate does the declared rule select, and what will the frozen model score on specimens it has never touched? Inside this slice of the development rows, will the added measurement make fewer, the same number of, or more errors than the reference — and which specimens moved? Will tightening the acceptance threshold lower the total cost, or move it the other way? Change the controls to inspect each calculation directly.",
    interaction: "Change comparisons, slice boundaries, selection criteria, acceptance threshold and costs. Show linked specimens, errors, coverage and workload. Opening the frozen held-out report is a protocol action with no prediction requirement; the test cannot select a new model. Results update directly on valid edits, with reset and explicit comparison snapshots; no learner prediction inputs or grading gates.",
  },
  practice: {
    task: 'Six changed tasks: reconstructing accuracy and balanced accuracy from a new confusion matrix and choosing which class to inspect, deciding whether a subgroup regression outweighs an aggregate gain and naming the question that settles it, diagnosing a test set that became development data through a researcher rather than through fit, a guided reaggregation by cultivar that deliberately does not match the feature-range slices, an acceptance policy whose better conditional accuracy leaves total cost unchanged, and an independent study that declares a new candidate before opening any new holdout outcomes.',
    success: 'A validation score is never reported as an independent estimate of the choice it made; a held-out number is never quoted for a selection that was never evaluated on the test rows; an aggregate improvement is reported with the subgroup it worsened; a slice is reported with its denominator; a conditional error rate over zero answered cases is called undefined rather than zero or perfect; an overlapping partition is not described as if it were the same partition; and a conclusion separates the evidence already consumed from the independent evidence the next claim would require.',
  },
  misconceptions: [
    'A good validation score is an estimate of how the chosen model will perform on new data',
    'Keeping test labels out of fit is enough to prevent leakage',
    'Scaling the whole table before splitting is harmless because no labels were used',
    'A higher balanced accuracy means every class improved',
    'Equal error counts under two models mean the two models get the same cases wrong',
    'A net gain of three correct predictions means three cases changed',
    'A test estimate above the validation estimate means something went wrong',
    'The confusion matrix and the score are alternative measurements rather than two aggregations of the same predictions',
    'A slice found by searching many cutoffs is as trustworthy as a predeclared one',
    'Grouping by a feature range and grouping by the label describe the same subsets',
    'Refitting on all development rows before the final evaluation is always wrong',
    'A fixed seed makes a result reproducible in the sense of stable across samples',
    'A binomial interval on accuracy is also an interval on balanced accuracy',
    'Two accuracy estimates from the same rows can be compared as independent quantities',
    'A confidence score is a calibrated probability',
    'Improving accuracy among answered cases improves the system',
    'A rule that answers nothing has perfect conditional accuracy',
    'Nested cross-validation is a way of retesting a favourite model more carefully',
    'A model card is a substitute for recording the split, the versions and the fitted transformations',
  ],
  sources: [
    'https://doi.org/10.24432/C5PC7J',
    'https://scikit-learn.org/stable/common_pitfalls.html',
    'https://scikit-learn.org/stable/modules/cross_validation.html',
    'https://scikit-learn.org/stable/modules/generated/sklearn.datasets.load_wine.html',
    'https://developers.google.com/machine-learning/crash-course/overfitting/dividing-datasets',
    'https://www.deeplearningbook.org/contents/guidelines.html',
    'https://www.itl.nist.gov/div898/handbook/prc/section2/prc241.htm',
    'https://arxiv.org/abs/1810.03993',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/end-to-end-supervised-learning-error-analysis/design.md',
  reviewFocus: 'That every score the page displays carries its role — training, validation, selection or held-out — and that no held-out quantity is reachable in the DOM before explicit inspection of the frozen declared decision it reports on, without a learner guess: the test balanced accuracy .966667, accuracy .972222, log loss .128708, the 35-of-36 confusion matrix, the four printed lines of the study program that open the test set, and the Wilson interval [.858300, .995080] that is computed from them. The validation balanced accuracies .792857, .820635, .888889 and .333333 against the training values .861240, .921373, .949945 and .333333; the paired repair and regression counts 4/1 for the three-feature linear model and 1/0 for the forest, taking 29 of 36 correct to 32 and 30; the colour slices 2 of 17 and 5 of 19 for the two-feature model against 3 of 17 and 1 of 19 for the three-feature model, whose lower and upper sides deliberately point in opposite directions; the cultivar-1 slice 1 of 14 against 0 of 14; and the per-class recalls 9/12, 13/14, 7/10 against 8/12, 14/14, 10/10, where cultivar 0 goes backwards while the mean rises. The acceptance fixture at threshold .60 giving 8 answered, 1 wrong, 2 deferred, coverage .8, conditional error .125 and cost 14, against .80 giving 4, 0, 6, .4, 0 and 12 — and the same threshold move reversing direction to 6 against 12 when a wrong answer costs 2 rather than 10; thresholds .61 and .64 giving the identical ledger; and threshold 1.01 giving coverage 0 with conditional error undefined rather than 0. Check separately that the slice rule the scatter draws is the rule the calculation applies at every one of the 1,401 cutoffs the control admits on both sides, including the one validation specimen whose colour intensity is exactly 4 and which belongs to the upper side; that a selection rule with no eligible candidate reports no winner rather than a default; that selecting a candidate the study never evaluated on the test rows refuses a held-out number instead of inventing one; and that the served Wine table is this lesson\'s own copy rather than a sibling lesson\'s.',
};
