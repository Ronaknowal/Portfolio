export default {
  summary: 'Learn conditional questions from labelled rows, see their exact regions, and evaluate differently fitted trees without confusing training fit, OOB evidence or probability quality.',
  outcomes: ['Route a new observation through fitted questions and interpret a mixed leaf', 'Calculate Gini/entropy and weighted gains while checking both child constraints', 'Distinguish an interaction a tree can represent from a greedy search that finds it', 'Derive regression leaf means, pruning costs and constant-leaf extrapolation limits', 'Build complete numeric trees and bootstrap forests with explicit ties and random state', 'Reconstruct OOB membership and distinguish probability averaging from hard voting', 'Explain the assumptions behind variance reduction and historical margin bounds', 'Evaluate a frozen pipeline with a baseline and interpret feature-reliance perturbations'],
  prerequisites: ['Linear & Logistic Regression'],
  sequence: ['Define labelled training and prediction-time inputs', 'Connect paths, rectangles and leaf counts', 'Calculate and compare candidate gains', 'Expose zero-gain interactions and midpoint behavior', 'Derive mean leaves and prune with actual subtree costs', 'Run complete tree/forest code', 'Track bootstrap omissions and aggregation', 'Validate a full workflow and feature schema', 'Investigate importance and a learned proximity representation', 'Practise changed cases before K-Nearest Neighbors'],
  visual: { type: 'Training/inference flow; query-linked regions; split row buckets; XOR partitions; exact subtree cost lines; bootstrap membership roster; aggregation bars; covariance-model curve; coherent and off-support feature perturbations', question: 'What changes in the fitted rule, eligible evidence and predicted probability when one training or inference choice changes?', interaction: 'Move a query, select legal split candidates, change growth policy, price leaves, inspect actual bootstrap draws and permute distinct information groups.' },
  practice: { task: 'Solve changed split, entropy, constraint, transformation, OOB, aggregation, variance, pruning and reliance cases, then independently select and report a held-out forest experiment.', success: 'Calculations and implementation contracts agree; unavailable evidence stays unavailable; test labels never select models; probability quality and feature reliance are interpreted with their assumptions.' },
  misconceptions: ['Trees cannot represent feature interactions', 'A nonlinear monotone transform preserves every unseen midpoint prediction', 'Parent size constraints ensure each child is large enough', 'A forest must beat one tree and automatically calibrates probabilities', 'Independent random seeds remove shared-data uncertainty', 'OOB makes every preprocessing step and repeated model selection independent', 'Permutation importance measures an intrinsic causal effect', 'A log-target transform makes constant leaves extrapolate', 'A family name implies a fixed runtime or memory cost'],
  sources: ['https://scikit-learn.org/stable/modules/tree.html','https://scikit-learn.org/stable/modules/ensemble.html#forest','https://scikit-learn.org/stable/modules/permutation_importance.html','https://www.stat.berkeley.edu/~breiman/randomforest2001.pdf','https://www.youtube.com/watch?v=J4Wdy0Wc_xQ'],
  depth: 'core',
  designRecord: 'docs/teaching/DECISION-TREES-RANDOM-FORESTS-DESIGN.md',
  reviewFocus: 'Same-data regions, exact weighted gains, complete bootstrap accounting, safe midpoint partitions, root-normalized pruning, explicit conditional variance assumptions, preserved original depth, executed changed examples, accessible topic-specific controls and annotated sources.',
};
