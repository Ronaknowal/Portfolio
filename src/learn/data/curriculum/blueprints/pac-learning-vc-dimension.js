export default {
  summary: 'Build the guarantee from its parts: a loss on a fresh example, a union bound over a class fixed before the data, and a count of how many label patterns a class can actually make. Then read a real VC bound, including one that says nothing, and finish on measured learning curves that do not measure capacity.',
  outcomes: [
    'Separate the two probability levels in "error at most ε with probability at least 1 − δ", and say which one a single fitted model is on',
    'State the realizable PAC contract with its quantifiers, and say what distribution-free does and does not mean',
    'Run the union-bound argument over K predeclared rules and derive the sufficient sample condition',
    'Explain why the argument needs independent observations but not independent rules',
    'Compute the exact failure probability in a four-input world and compare it with the general bound at the same n',
    'Recognise a valid bound that is numerically vacuous, and say what it does and does not imply',
    'Compute the simultaneous radius for K rules on a shared evaluation sample, and say why discarding candidates afterwards does not shrink it',
    'Derive the ERM comparison and say why the radius is paid twice',
    'Distinguish a gap bound from an excess-risk bound, and a sufficient sample size from a minimum one',
    'Decide whether a class shatters a given set, and produce the witness or name the obstruction',
    'Explain why one unshatterable configuration does not upper-bound a class VC dimension',
    'Count the growth function for thresholds and intervals, and read Sauer bound as an upper bound rather than an attained count',
    'Say why the pattern count observed on the training inputs may not be substituted for K, and what the ghost sample repairs',
    'Evaluate the explicit VC radius at stated d, n and δ, and the classical realizable sufficient size with its base-2 logarithms',
    'Fit the tight interval on a uniform input distribution and compute its exact population risk from the symmetric difference',
    'Identify an exact null: an observation that changes the picture and not the risk',
    'Read measured development curves without inferring a capacity ordering from them',
    'Explain why parameter count is not VC dimension, using the one-parameter sine class as the counterexample',
    'Separate statistical learnability from efficient computation, and name what finite VC dimension does not supply',
  ],
  prerequisites: ['Evaluation Metrics (Precision, Recall, F1, AUC-ROC, AP, R², MAE)', 'Concentration Inequalities (Hoeffding, Bernstein, Chernoff)'],
  sequence: [
    'Two probability levels: the error of a fitted rule on a fresh example, and the draw that produced the rule',
    'The realizable PAC contract, its quantifiers, and what distribution-free means',
    'Consistent learners and ERM: zero training error is a property of the fit, not the guarantee',
    'Statistical learnability against efficient learning, stated once and referred back to',
    'One bad fixed rule survives n draws with probability at most e^(−nε)',
    'The union bound over K predeclared rules, and why independence across rules is not needed',
    'The sufficient sample condition n ≥ (ln K + ln(1/δ))/ε, and 162 examples at K = 32, ε = .05, δ = .01',
    'An exact four-point world: failure probability 1/16 at n = 4 against a bound above 1',
    'The same class with an all-zero target never fails: class size is not task difficulty',
    'Agnostic learning: excess risk above the best member, not absolute error',
    'Hoeffding at δ/K, the simultaneous radius, and .083112906813 at K = 25, n = 500, δ = .05',
    'Why the protected event covers a member selected after the scores are seen',
    'Why keeping three finalists out of twenty-five does not entitle you to the K = 3 radius',
    'The three-step ERM comparison and the factor of two',
    'Seven interval patterns on three points, and the structural reason 101 is missing',
    'Shattering, VC dimension and its existential-then-universal quantifiers',
    'Thresholds, intervals and affine half-planes, with witnesses and the four-point impossibility',
    'The growth function, the interval closed form, and Sauer lemma with its d = 0 and n ≤ d edges',
    'Why the observed training pattern count may not be substituted for K, and what the ghost sample does',
    'The explicit conservative VC radius, its domain, and the vacuous region above 1',
    'The classical realizable sufficient sizes, in base-2 logarithms, on their own axis',
    'A fitted interval with an exactly calculable risk, and the two boundary strips',
    'A thousand repeated draws: exact per-run risks, estimated failure frequencies, and a bound that is neither',
    'Measured banknote development curves where a perfect training fit loses',
    'Deeper: capacity in model selection, structural risk minimization and the family-search budget',
    'Deeper: parameter count against VC dimension, and one real parameter with infinite VC dimension',
    'Deeper: Rademacher complexity, algorithmic stability, PAC-Bayes, pseudo-dimension and computation',
  ],
  visual: {
    type: 'Every coordinate is computed in the verified model layer rather than placed by hand, because a drawn bound curve and a drawn separator are mathematical claims. Eleven inline figures: four independently drawn training samples each with its own fresh-example error meter and an ε cutoff, so δ visibly counts rows rather than predictions; a steppable elimination of sixteen predeclared rules by their own mistakes, on a deliberately different target from the investigation below it so that stepping through it cannot answer that question; twenty-five simultaneous confidence bands on one evaluation sample, with the selected row revealed only after every band is drawn and a crossed-out K = 3 band beside them; eight half-plane panels carrying real separators with verified signed margins, plus a convex quadrilateral with its crossing diagonals, an interior point with its convex combination printed, and a collinear triple; pattern counts against n on a logarithmic axis where a linear one would flatten both structured classes; a ghost sample collapsing infinitely many intervals into finitely many rows; the explicit VC radius for three dimensions with the region above 1 shaded as vacuous and the raw values never clipped; the unit interval carrying target band, fitted band, hatched disagreement strips and the two ε/2 coverage strips; risk quantiles across a thousand retained draws with the mean, median and ε line as separate marks; measured development curves one procedure at a time with the exact counts beside them; and a binary fraction read back as four positions on a circle.',
    question: "Will the rule this learner returns meet the ε target, and what exactly will its population risk be? Can this class realize exactly these labels on exactly these points, and how many of the 2^n labelings can it realize altogether? Which way will the population risk move when this change is applied, and where will it land? Change the controls to inspect each calculation directly.",
    interaction: "Change sample observations, candidate rules, target error and witness coordinates. Update version spaces, shattering witnesses and interval risks live. Empirical outcomes and formal guarantees remain separately labelled. Results update directly on valid edits, with reset and explicit comparison snapshots; no learner prediction inputs or grading gates.",
  },
  practice: {
    task: 'Nine changed tasks: interpreting ε and δ against a claimed accuracy, the simultaneous radius at K = 12 and n = 800 with the independence question stated separately, the eleven interval patterns on four points and the five impossible ones, why a collinear counterexample does not bound the class, an axis-aligned box proof that uses the bounding box rather than the convex hull, an excess-risk ceiling with an optimization error added, repairing a report that confuses vacuity with unlearnability, a changed interval target with one edit that moves the risk and one that does not, and a measured learning-curve comparison with an explicit list of what stays undecided.',
    success: 'A sufficient sample size is never reported as a minimum; a radius above 1 is called vacuous rather than evidence of failure; an excess-risk bound is not reported as an absolute error; the union bound is applied without assuming independence across rules while keeping it across observations; an exterior negative observation is explained as an exact null rather than a rounding artefact; a feasible request is distinguished from shattering and both from a class-wide VC claim; and a measured development comparison is reported without inferring a VC ordering, a universal ranking or an untouched final estimate.',
  },
  misconceptions: [
    'δ is the fraction of predictions the fitted model gets wrong',
    'A PAC guarantee promises the stated confidence as an accuracy',
    'A sufficient sample bound is the minimum amount of data required',
    'Zero training error is itself evidence about unseen examples',
    'The union bound needs the candidate rules to be independent',
    'Evaluating many models on one shared sample is invalid because their errors are correlated',
    'Keeping only the finalists lets you pay for the smaller family',
    'A bound whose value exceeds 1 has been violated, or shows learning is impossible',
    'An agnostic excess-risk bound is a bound on the absolute error',
    'ERM pays the uniform radius once',
    'An infinite class cannot be learned because K is infinite',
    'Infinitely many parameter settings means infinitely many label patterns',
    'Showing one point set cannot be shattered bounds the class VC dimension',
    'A single rule must realize every labeling for a set to be shattered',
    'Sauer bound states the number of patterns a class actually realizes',
    'The pattern count observed on the training sample can be substituted for K',
    'The ghost sample is a second dataset the algorithm uses',
    'VC dimension equals the number of parameters',
    'A finite VC dimension supplies an efficient learning algorithm',
    'Finite VC dimension is required for any particular distribution to be learnable',
    'Length equals probability for any input distribution',
    'A model that fits every training label will generalize better than one that does not',
    'Learning curves measure the VC dimension of the model families',
    'A shared development set gives independent replicate estimates',
    'Pretraining exempts a representation from the selection analysis',
  ],
  sources: [
    'https://mwarmuth.bitbucket.io/pubs/J14.pdf',
    'https://web.uvic.ca/~nmehta/ml_theory_fall2021/lecture12.pdf',
    'https://www.stat.berkeley.edu/~bartlett/courses/2014spring-cs281bstat241b/lectures/04-notes.pdf',
    'https://vatsalsharan.github.io/fall23/lec3.pdf',
    'https://jmlr.org/papers/volume17/15-389/15-389.pdf',
    'https://proceedings.mlr.press/v195/larsen23a.html',
    'https://jmlr.org/papers/v20/17-612.html',
    'https://jmlr.org/papers/volume2/bousquet02a/bousquet02a.pdf',
    'https://arxiv.org/abs/1703.11008',
    'https://archive.ics.uci.edu/dataset/267/banknote+authentication',
    'https://work.caltech.edu/telecourse.html',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/pac-learning-vc-dimension/design.md',
  reviewFocus: 'The finite-family radius .0831129068134555 at K = 25, n = 500, δ = .05, which is sqrt(log(1000)/1000) and is the destination note anchor, against .060736146190830516 for a single fixed rule and .04155645340672775 at four times the sample; the practice radius .06656799548101218 at K = 12, n = 800, δ = .02; the finite realizable sufficient size 162 at K = 32, ε = .05, δ = .01; the exact four-input failure fractions 1/2, 1/4, 1/16, 1/256, 1/65536 and 1/16777216 at n = 1, 2, 4, 8, 16 and 24 against raw bounds 12.460813, 9.704491, 5.886071, 2.165365, .29305 and .03966, with the all-zero target giving exactly 0; the growth table 2, 4, 7, 11, 16, 22, 29, 37, 46, 56 for intervals against 2^n, the seven patterns on (.2, .5, .8) with 101 absent, the eleven on four points, and the Sauer sum 79,375,496 at n = 100, d = 5; the half-plane counts 8, 14, 6 and 14 for triangle, square, collinear triple and interior configuration, each recomputed by exact convex-hull intersection rather than by the linear program that produced the stored witnesses; the explicit VC radii 2.1835178827636925, .7900256200823507, .277759955057982 and .09585777862488472 at d = 2 and n = 100 to 100,000, every d at n = 100 being above 1; the realizable sufficient sizes 482, 1124 and 2568 at d = 2; the interval fits [.35, .65] at risk .1, [.31, .69] at .02, the exterior-negative null holding both fit and risk exactly, the empty rule at .4, and the changed target [.25, .75] giving .1, .06 and an unchanged .1; the retained simulation failure counts 743, 368, 46, 1 and 0 against two-strip bounds 1, .716971845, .153889951, .0118410584 and .0000701053; the banknote development counts, in particular the depth-5 tree fitting 80/80 training and 74/80 development while the RBF SVC fits 77/80 and reaches 79/80; and the sine witness r = 17/64 with cycles 17/64, 17/32, 1/16, 1/8 realizing [1, 0, 1, 1]. Check separately that valid controls update the connected calculation without a learner-answer requirement, that comparisons report unchanged if and only if the displayed output did not move, and that the served dataset is this lesson own copy rather than a sibling directory.',
};
