export default {
  summary: 'Separate what a predicted probability promises from what a prediction set promises: build a reliability diagram from ten cards you can edit and watch its error vanish without a single forecast changing, pool a monotone map by hand, calculate the exact finite rank that reserves a place for an example you have not seen, and then read two complete experiments on real data in which the best-covering method is not the most useful one.',
  outcomes: [
    'State what a probability forecast conditions on, and read a reliability diagram in the direction its axes declare',
    'Distinguish a class-1 reliability diagram from a top-confidence diagram, and give a two-group counterexample where only one of them notices the error',
    'Separate calibration from resolution, and price the difference under a stated cost so that two calibrated forecasts have different expected losses',
    'Report an empty bin as having no observed fraction rather than a fraction of zero',
    'Compute a binned ECE, and make it vanish by rebinning while the model is unchanged',
    'Fit a two-parameter sigmoid by minimising smoothed log loss, and say what its offset can change',
    'Run pool-adjacent-violators with equal scores grouped first and every merge count-weighted, and say why the average of two block means is the wrong answer',
    'Say what temperature scaling cannot change, and what it can',
    'Trace four separate label roles through one experiment and name the step at which a leak entered',
    'Compute k = ceil((n+1)(1 − alpha)) exactly, select the kth smallest score, and return infinity rather than clipping the rank',
    'Say why the rank denominator is n + 1, by the combined rank of the example you have not seen',
    'Form a class set under the weak comparison, including at exact equality, and read an empty set as a legitimate outcome',
    'Turn a dimensionless normalised threshold into a physical half-width, and say what a consistent change of units does not do',
    'Read a negative CQR adjustment, and recognise crossed endpoints as an empty set rather than a negative width',
    'Keep marginal coverage, coverage conditional on one calibration sample, and an observed assessment count apart, and never quote a coverage rate without its denominator',
    'Explain why marginal coverage protects no named group, and why keeping only singletons is a selection event',
    'Name what an acceptable uncertainty report contains, including the equality policy and what will trigger a new evaluation',
  ],
  prerequisites: ['PAC Learning & VC Dimension'],
  sequence: [
    'A probability is a statement about a conditioning population, not about one day',
    'The class-1 diagram and the top-confidence diagram are different questions: two groups at .2 and .8 whose rates are .3 and .9',
    'Calibration can discard information: two lines at .1 and .3 collapsed to .2, costing 16 against 14',
    'Brier loss .16 against .15 — a better proper score reflects resolution, not a calibration repair',
    'Ten forecast cards, two bins, and the signed gaps +.2 and −.2',
    'Binned ECE of .2, and the same observations in one bin giving exactly zero',
    'An empty bin has no observed fraction; a forecast of exactly 1 belongs in the last bin',
    'A raw score is not a probability: a sigmoid puts it in [0, 1] and nothing more',
    'Platt smoothing, the slope and the offset, and what a changed offset does under a .5 threshold',
    'Pool adjacent violators: group equal scores first, merge by combined counts, and get 1/3 rather than 1/4',
    'Fitted values are defined at the knots; interpolation between them is a separate library choice',
    'Temperature scaling preserves the winner and the ties, and still moves a threshold decision',
    'Four label roles: base fit, probability calibration, conformal calibration, assessment',
    'Why the third role is separate: probability calibration is itself training',
    'Frozen estimators, fold-specific ensembles, transformations inside the clone, and the current API',
    'A nonconformity score orders mistakes and need not be a probability',
    'k = ceil((n+1)(1 − alpha)) = 8 of 9, threshold .6, and the weak comparison that keeps equality',
    'Why n + 1: the unseen example is equally likely to occupy any of the n + 1 ranks',
    'The finite rank is not an interpolated percentile: .633333, .9 and .6 on the same nine scores',
    'A rank beyond the calibration set is infinity, and clipping it discards the requested protection',
    'Constant-width intervals for a future response, not confidence intervals for a mean',
    'A local scale makes the threshold dimensionless, so the half-width is q times u(x)',
    'Conformalised quantile regression, its signed score, and an empty set when shrunken endpoints cross',
    'Banknote: five declared procedures on one fixed split, with 88.75% coverage below the 90% target',
    'The constant baseline, its tied boundary, and one floating-point rounding unit that lost a whole class',
    'Airfoil: four interval procedures in decibels, where the narrower method covers fewer rows',
    'Marginal, conditional-on-calibration and observed coverage are three different quantities',
    'Marginal .9 from group coverages 1 and .5 — half of one group missed',
    'A singleton is a selection event; prevalence changes the meaning of a positive result',
    'Deeper: adaptive sets and their boundary conventions, full conformal, jackknife+ at 1 − 2 alpha, weighted conformal under covariate shift, risk control, and the library route',
  ],
  visual: {
    type: 'Every plotted coordinate is computed in the verified model layer rather than placed by hand, because a drawn reliability curve, interval band or coverage bar is a mathematical claim. Twelve inline figures: a two-panel conditioning fork drawing the same forecasts as class-1 points and as one merged confidence point on the diagonal; a decision fork pricing a coarse score against a resolved one; pooled-block rectangles whose width is the scores they span and whose height is the pooled probability, with the raw group means marked and the fitted sigmoid overlaid; stacked softmax bars at four temperatures with a movable decision line; four freeze-point lanes for the label roles; a score rail with the selected order statistic marked and infinity deliberately not drawn as a point; interval segments in response units under three score functions; all 80 measured probabilities as a reliability diagram beside all 80 conformal calibration scores on their own rail; all 120 airfoil intervals drawn to scale in decibels with misses marked, plus width against frequency on a base-ten logarithmic axis with the 2 kHz slice line; a three-row table separating marginal from conditional from observed coverage; a mosaic whose filled area is literally the marginal coverage; and cumulative APS mass with its direct and boundary-expanded sets kept apart. Sign is carried by shape as well as colour — a ring above the diagonal, a filled disc below — and every count strip distinguishes an empty bin from a bin with no positives.',
    question: "Will merging two bins change the model, or only the summary? Which adjacent pair must pool next, and to what — the combined counts, or the average of two block means? What does a candidate receive when its leading score lands exactly on the threshold, and how many of ten rotations cover their hidden card? Will doubling every calibration and query scale move the threshold, the physical width, or neither? Change the controls to inspect each calculation directly.",
    interaction: "Edit probability cards, isotonic blocks, calibration residuals, alpha and interval scales. Keep threshold ranks discontinuous where the mathematics is discontinuous; display exact nulls and infinity/empty-set boundaries rather than inventing smooth motion. Results update directly on valid edits, with reset and explicit comparison snapshots; no learner prediction inputs or grading gates.",
  },
  practice: {
    task: 'Eight changed tasks: a confidence diagram that sits on the diagonal while both class-1 points are .1 above it, a monotone fit on alternating labels where row order is provably irrelevant, a rank computed at two alphas with one of them exceeding the calibration set, a class set at two thresholds one of which turns on exact equality, a negative CQR adjustment that shrinks an interval, a four-step data-flow bug where the conformal observations already selected the model, a two-sentence interpretation of measured overall and subgroup coverage that avoids calling either a theorem, and a usefulness comparison between a system with perfect coverage and no discrimination and a selective one that misses thirty per cent.',
    success: 'The rank is computed from ceil((n+1)(1 − alpha)) and reported as infinity when it exceeds n, never clipped; the kth score is counted from the smallest and said to be so; equality is included in the set comparison and named; an empty bin is reported as having no observed fraction rather than zero; the pooled probability is the combined counts and the average of block means is identified as the wrong answer; a measured coverage fraction is described with its denominator and never called a guarantee; a subgroup fraction is not presented as a conditional guarantee; and a narrower interval is not called better without naming the task and the cost of an omission.',
  },
  misconceptions: [
    'A 70% forecast promises that seven of the next ten will happen',
    'A top-confidence reliability diagram and a class-1 reliability diagram are the same plot',
    'Accuracy below confidence puts a point above the diagonal',
    'A calibrated score contains all the information useful for a decision',
    'A lower Brier loss proves better calibration',
    'A zero binned ECE proves the model is calibrated',
    'An empty bin has an observed fraction of zero',
    'A forecast of exactly 1 can be excluded from the last bin without consequence',
    'Applying a sigmoid to a score makes it a probability',
    'A calibration map cannot change which class is predicted',
    'Isotonic regression is safe to fit on the same data that fitted the model',
    'Two adjacent blocks pool to the average of their two probabilities',
    'Isotonic fitted values form a staircase between every pair of distinct knots',
    'Temperature scaling cannot change any decision',
    'Every modern network is overconfident and improves under temperature scaling',
    'Holding out the classifier’s training data is enough to make a conformal threshold honest',
    'Probability calibration data can double as conformal calibration data',
    'Cross-validated calibration undoes hyperparameter selection that used the same labels',
    'The conformal threshold is a percentile of the calibration scores',
    'A rank larger than the calibration set should be clipped to the largest score',
    'The kth score is counted from the largest',
    'A prediction set can never be empty',
    'A singleton prediction set is a label with 1 − alpha probability of being correct',
    'Ties make the coverage guarantee fail',
    'Observed coverage below the target refutes the theorem',
    'Conformal intervals are confidence intervals for a mean',
    'Conformalising quantile estimates can only widen them',
    'Crossed conformalised endpoints mean a negative width',
    'A narrower interval procedure is better',
    'Marginal coverage implies coverage within each group',
    'Keeping only the singletons preserves the coverage guarantee',
    'Shuffling a time series before forecasting makes it exchangeable',
    'Jackknife+ inherits the split-conformal 1 − alpha bound',
    'Conformal prediction adds no measurable computational cost',
  ],
  sources: [
    'https://arxiv.org/html/2107.07511v6',
    'https://proceedings.mlr.press/v89/vaicenavicius19a/vaicenavicius19a.pdf',
    'https://proceedings.mlr.press/v70/guo17a/guo17a.pdf',
    'https://proceedings.neurips.cc/paper/2019/file/5103c3584b063c431bd1268e9b5e76fb-Paper.pdf',
    'https://stat.cmu.edu/~ryantibs/papers/jackknife.pdf',
    'https://proceedings.neurips.cc/paper/2019/file/8fb21ee7a2207526da55a679f0332de2-Paper.pdf',
    'https://scikit-learn.org/stable/modules/calibration.html',
    'https://scikit-learn.org/stable/modules/generated/sklearn.calibration.CalibratedClassifierCV.html',
    'https://scikit-learn.org/stable/modules/generated/sklearn.svm.SVC.html',
    'https://mapie.readthedocs.io/en/stable/api/classification/',
    'https://mapie.readthedocs.io/en/v1.5.0/content/getting-started/quick-start/',
    'https://stephenbates19.github.io/videos.html',
    'https://archive.ics.uci.edu/dataset/267/banknote+authentication',
    'https://archive.ics.uci.edu/dataset/291/airfoil+self+noise',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/calibration-conformal-prediction/design.md',
  reviewFocus: 'The two-group counterexample: forecasts .2 and .8 against rates .3 and .9 give class-1 points each .1 above the diagonal while top confidence is .8 for everyone and mean correctness is exactly .8, so the confidence-only reading sits on the diagonal; the practice variant .1/.9 against .2/1.0 does the same at .9. The resolution example: coarse .2 against rates .1 and .3, expected Brier .16 against .15, decision threshold .25, expected cost 16 against 14. The ten-card reliability fixture: bins of five with two and three positives, points (.2, .4) and (.8, .6), signed gaps +.2 and −.2, ECE .2 in two bins and exactly 0 in one, Brier .28 falling to .24 with AUC .6 unchanged both times, and the boundary fixture [0, .5, 1] with outcomes [0, 1, 1] giving counts 1 and 2 and ECE 1/6. The sigmoid fit a ≈ .2677017731 and b ≈ −.1338508865 with objective .6497069072; the browser’s damped-Newton solver must agree with the packet’s SciPy BFGS fit to within 1e-9. PAV on [−3…4] with labels [0,1,0,0,1,0,1,1] giving [0, 1/3, 1/3, 1/3, 1/2, 1/2, 1, 1] over exactly three merges; the tied fixture [−2,−2,0,1] with labels [1,0,0,1] giving [1/3, 1/3, 1] rather than [1/4, …]; the flipped-label variant giving [1/2, 1, 1]; practice giving [0, .5, .5, .5, .5, 1]. Softmax at T = .5, 1, 2 and 4 on logits [3, 1, 0] with the winner fixed throughout, and the fitted temperature 1.7526036667 moving the log loss from .9792860058 to .8904686059 without reaching its search boundary. The rank: k = 8 of 9 at alpha .2 giving q = .6 against NumPy’s linear .633333 and higher .9; k = 10 at alpha .05 giving infinity; k = 12 at n = 14 alpha .2 and k = 15 at alpha .02; rotation covering 8 of 10 distinct cards and 10 of 10 when all ten tie. Exact decimal arithmetic on alpha is load-bearing: naive floating point disagrees with the exact rank at 26 points of the enterable grid, including n = 24 at alpha .44. The sets {A}, {A, B} and empty at q = .6; the lab opens instead on an eighth card of .55 so its first question turns on equality. Adaptive intervals: absolute q = 6, normalised q = 2, queries (10, scale 1) and (20, scale 3) giving [8, 12] and [14, 26] against the absolute [4, 16] and [14, 26]; residuals changed to 12 and 16 giving q = 4 and widths 8 and 24; doubling every calibration and query scale halving q to 1 and leaving the physical intervals identical. CQR k = 4 of 5 at alpha .4 giving q = −2 and [12, 18], and the alternative scores giving q = 1 and [9, 21]. Measured banknote at alpha .1 with rank 73 of 80: correct 79 for all four SVC-derived methods and 51 for the prior constant, Brier .060271, .007839, .004766, .008521 and .235538, coverage 69, 71, 76, 69 and 80, mean set sizes .8625, .8875, .95, .8625 and 2, sigmoid holding 9 empty sets and 71 singletons, isotonic’s threshold exactly 0 from tied scores, and AUC exactly 1 for all four SVC-derived scores. The tied-boundary fixture: p = 103/240 makes 1 − p ≤ 1 − p true and p ≥ 1 − (1 − p) false, which is the whole difference between 51 and 80 correct. Measured airfoil at alpha .1 with rank 109 of 120: coverage 100, 108, 93 and 103 with mean widths 18.991917, 16.370437, 14.353792 and 15.886494 dB, q values 8.1852184543, .7663505895 and 9.4959583333, zero quantile crossings, point MAE 3.9390454396 against 5.7648652778, and ridge’s declared frequency slices 54/66 below 2 kHz and 54/54 at or above it. The group mosaic .8 × 1 + .2 × .5 = .9, and the prevalence shift taking a positive predictive value from .8 to .3076923077 with the test unchanged. Both offline programs must regenerate the packet’s checked-results.json and experiment-results.json byte for byte under NumPy 2.3.5, SciPy 1.18.1 and scikit-learn 1.9.1.',
};
