export default {
  summary: 'Replace the labels with coin flips and ask how well the best allowed rule can match them. That one number — exactly 2/3 for a threshold class on three points — becomes a risk bound, a geometric calculation on a norm ball, and a margin construction that makes a hard classification analysable. Then run it on real banknote measurements, where the classifier reaches 2 mistakes in 80 and every bound expression stays above 1 and therefore says nothing.',
  outcomes: [
    'Compute an exact empirical Rademacher complexity by enumerating every sign pattern, and say why the maximum is taken before the average',
    'State the convention in force — no absolute value, 1/n normalisation — and say what inserting an absolute value measures instead',
    'Reproduce the exact nulls: a duplicated row, a fixed translation, a convex average, a permutation, and the scaling that is not a null',
    'Separate empirical complexity, expected complexity and a high-probability bound, and say which is random over what',
    'Build a loss class from a predictor class, and use the exact one-half identity for sign-valued hypotheses without extending it to real-valued scores',
    'Say what 1 − δ ranges over, and what the word "simultaneously" does and does not license',
    'Follow the ghost-sample pair-swap argument and locate the single step where the factor 2 enters',
    'Derive the closed-form best response for a Euclidean norm ball, and identify the case where the maximiser does not exist uniquely',
    'Distinguish the exact quantity from the feature-energy bound, and name the directional information the bound discards',
    'Compute a kernel complexity from a Gram matrix alone, and say why infinite feature dimension does not imply an infinite bound',
    'Apply Massart and Sauer inside their stated ranges, including d = 0 and d > n',
    'Derive the ℓ1 best response and say why its bound carries a logarithm of the dimension',
    'Choose a valid Lipschitz constant for a named loss on a named domain, and not borrow the sigmoid\'s 1/4 for the logistic loss',
    'Assemble a margin bound term by term and show that scaling scores, budget and threshold together moves nothing',
    'Account for K predeclared comparisons with a union bound, and say why an unrestricted post-hoc sweep is not covered',
    'Correct a Monte-Carlo estimate with its own failure allowance, and say why more draws cannot repair an imperfect inner optimisation',
    'Apply a declared selection rule before any assessment number is visible, and say why the smallest bound expression need not be the selected candidate',
    'Report a vacuous bound honestly beside a model that measurably works, and say precisely what each does and does not establish',
  ],
  prerequisites: ['Calibration & Conformal Prediction'],
  sequence: [
    'Four prediction rows from infinitely many cutoffs: the restriction to a sample is what the calculation sees',
    'One sign pattern, one best row, and the exact value 1/3 for the pattern no threshold can match',
    'Eight patterns, eight maxima, and the average 2/3 — with averaging-first giving exactly zero instead',
    'Five nested classes on the same three inputs: 0, 1/2, 2/3, 5/6, 1, and why the comparison is justified only because they nest',
    'The definition, the three things held fixed, and what changes when the absolute value goes inside',
    'Empirical against expected complexity: more sign draws sharpen one and never touch the other',
    'The loss class, the [0,1] range assumption, and the empirical theorem with its coefficients 2 and 3',
    'The exact one-half identity for sign-valued hypotheses, and why hard-thresholding a score does not inherit it',
    'Ghost sample, pair swaps, and one shared supremum splitting into two independent ones',
    'Bounded differences twice over: where the 2 and the 3 come from, by the same argument applied to two events',
    'The norm-ball best response Bv/‖v‖, the v = 0 case, and why an artificial-label fit is unnecessary here',
    'Same lengths, same energy bound, different exact answers: duplicate against perpendicular unit vectors',
    'Kernels: the same calculation through inner products, with the trace bound that never moves with the similarity',
    'Massart, Sauer, and the ℓ1 diamond whose optimum sits on one coordinate',
    'Lipschitz constants with their domains stated, and the sigmoid-versus-logistic confusion the table exists to prevent',
    'The ramp φ_ρ, its two exact knots, and the margin bound that trades a training term against a 1/ρ complexity term',
    'Predeclared comparisons, the union bound, and why a regularization sweep is validation rather than a certificate',
    'Monte-Carlo estimation with a one-sided Hoeffding correction, and the inner-optimisation error that points the other way',
    'A real 480-row experiment: four disjoint roles, a frozen representation, five budgets and two thresholds declared first',
    'Validation selects B = 4 with 2 assessment mistakes in 80; every one of the ten bound expressions exceeds the trivial ceiling 1',
    'Deeper: convex hulls preserve score complexity, and what thresholding the average costs',
    'Deeper: covering numbers, chaining and local complexity',
    'Deeper: neural norm bounds, spectral margins, PAC-Bayes and algorithmic stability, compared by what each one constrains',
  ],
  visual: {
    type: 'Every drawn coordinate comes from the verified model layer, so a supporting point on a norm ball, a ramp curve, a stacked bound bar and a histogram edge are all asserted against the quantity they depict. Seventeen inline figures: a movable cutoff on three ordered inputs whose prediction row changes only when it crosses an input; a four-by-eight best-response matrix with the winner marked in each column and a toggle to the wrong order of operations; five nested rectangles carrying row counts and exact values; a predictor-to-mistake coordinate table; a three-term strip beside two separated randomness lanes; four ghost-sample pairs with individual swap controls; a norm ball with its signed sum and supporting point, including the degenerate v = 0 case; three small balls showing that direction moves the exact value while the energy bound does not; a Gram matrix with its four quadratic forms and the two feature vectors at the angle its similarity implies; a circle and a diamond sharing one signed sum; logistic and hinge losses on one axis beside the sigmoid on its own, because their ranges differ; the ramp with its two exact knots and four worked margins sitting on it; four independent Monte-Carlo intervals against an exact reference line, deliberately not drawn as one band; a proportional strip of the four data roles; five stacked bound bars drawn at full length past the trivial ceiling rather than clipped at it; a training-margin histogram with bin edges forced exactly onto 0 and ρ; and a convex hull whose interior mixture never out-projects its best vertex.',
    question: "Which row wins this sign pattern, and what is the average of all eight winners? Is the maximising coefficient unique — and what happens when the signed sum is zero? Will this expression come in under the trivial ceiling 1? Which budget does the declared rule select, and is it the one with the smallest bound expression? Change the controls to inspect each calculation directly.",
    interaction: "Edit function rows, signs, feature vectors, norm budget and margins. Recompute best responses, enumerated averages, feature geometry and bound terms together. Stages organize the explanation; none asks for a guessed winner/value. Results update directly on valid edits, with reset and explicit comparison snapshots; no learner prediction inputs or grading gates.",
  },
  practice: {
    task: 'Eight changed tasks: a two-input threshold class and the row that would complete it; two reported complexities for the same singleton under two conventions; a norm ball whose exact complexity is 5 and is not a bug; Massart on eight rules with a thousand duplicate copies added; a ramp loss beside a misapplied Lipschitz constant; a singleton class defined after seeing the data; the real experiment\'s best candidate against its worst bound expression; and a random-label neural fit presented as an upper bound.',
    success: 'The maximum is taken per pattern before averaging, never after; a value above 1 for unrestricted real scores is recognised as legitimate rather than a bug; duplicate rows are excluded from M and the exact quantity is distinguished from the bound; the sigmoid\'s 1/4 is not transferred to the logistic loss and unbounded losses are not fed to the [0,1] theorem; a class defined after seeing the sample is identified as outside the theorem, with two valid repairs named; a bound above 1 is called uninformative rather than contradicted, and the validation-selected candidate is not replaced by the smallest-expression one; and an achieved random-label correlation is identified as evidence that the supremum is at least that, never at most.',
  },
  misconceptions: [
    'Averaging each rule\'s correlation and then taking the maximum gives the same answer',
    'The complexity of a class can be compared across topics without naming the sample, outputs and constraints',
    'A low complexity means the class contains a good predictor',
    'A high complexity means the algorithm will overfit',
    'Restricting thresholds to observed values is enough to enumerate the class',
    'An absolute value inside the supremum is a formatting choice',
    'Empirical and expected complexity can be substituted into each other\'s theorem',
    'More sign draws on one sample estimate the expected complexity',
    'The complexity of a predictor class is the complexity of its loss class',
    'A bound holding with probability 1 − δ is a 95% confidence about an individual prediction',
    'A high-probability bound is a deterministic promise about the dataset in front of you',
    'You may define the class after seeing the data and use its singleton complexity',
    'Adding an observation must decrease the empirical complexity',
    'A freely chosen intercept is covered by the fixed-translation null',
    'The factor 2 should be applied again at the ghost-sample step',
    'The feature-energy bound captures the sample\'s geometry',
    'An infinite-dimensional feature map implies an infinite complexity',
    'A fitted kernel SVM\'s norm can be read off its support-vector count',
    'Counting duplicate hypotheses separately is harmless in Massart\'s bound',
    'Sauer\'s simplified expression applies for every VC dimension',
    'The logistic loss is 1/4-Lipschitz',
    'Squared loss is 2-Lipschitz without naming a domain',
    'Contraction means the fitted model\'s individual losses improve',
    'Multiplying every score by a constant improves a normalised guarantee',
    'A regularization sweep is a computed Rademacher certificate',
    'A Monte-Carlo average is an upper bound on the quantity it estimates',
    'A standard error on the sign simulation measures generalization uncertainty',
    'A random-label fit that reaches correlation .3 shows the class cannot exceed it',
    'A vacuous bound shows the model is bad',
    'The candidate with the smallest bound expression is the one to deploy',
    'Disjoint row identifiers mean distinct feature vectors',
    'Sampling without replacement from a fixed corpus is an iid sample from a population',
  ],
  sources: [
    'https://cs.nyu.edu/~mohri/mls/lecture_3.pdf',
    'https://www.cs.huji.ac.il/~shais/UnderstandingMachineLearning/understanding-machine-learning-theory-algorithms.pdf',
    'https://jmlr.org/papers/volume3/bartlett02a/bartlett02a.pdf',
    'https://www.stats.ox.ac.uk/~rebeschi/teaching/AFoL/22/',
    'https://www.stats.ox.ac.uk/~rebeschi/teaching/AFoL/22/material/lecture03.pdf',
    'https://arxiv.org/abs/math/0508275',
    'https://proceedings.mlr.press/v40/Neyshabur15.pdf',
    'https://papers.neurips.cc/paper/7204-spectrally-normalized-margin-bounds-for-neural-networks.pdf',
    'https://arxiv.org/pdf/1703.11008',
    'https://proceedings.mlr.press/v48/hardt16.pdf',
    'https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html',
    'https://archive.ics.uci.edu/dataset/267/banknote+authentication',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/rademacher-complexity-generalization-bounds/design.md',
  reviewFocus: "The five finite classes on inputs −1, 0, 1 giving exactly 0, 1/2, 2/3, 5/6 and 1, with the wrong order of operations giving exactly 0, and the mistake class exactly half the predictor class at 1/3 for every one of the eight label vectors; the absolute-value convention returning 1/2 for the singleton and, in general, the signed value of the negation-closed class; the exact nulls — duplicate row, fixed translation of 7, convex average, permutation, row reversal, negation — holding in rational arithmetic rather than to a tolerance, and scaling by c multiplying by exactly |c|; the Euclidean cases .5 for duplicate unit vectors against .707107 for perpendicular ones, both with energy bound .707107, with the bound tight exactly when every signed sum has the same length; the two-point kernel closed form B(√(2+2r) + √(2−2r))/4 matching enumeration at every similarity, the trace bound constant at .707107, and r against −r a null; Massart .235482 at M = 16, n = 100 and .144203 at M = 8, n = 200, with M = 1 giving exactly zero and no division by zero; Sauer exact at 166,751 for d = 3, n = 100 against the simplified 743,909, with d = 0 and d > n handled separately; the logistic margin loss approaching slope 1 while the sigmoid is exactly 1/4-Lipschitz; the ramp [1, .8, .2, 0] at ρ = .5 with mean .5, [1, .6, 0, 0] at ρ = .25, and [1, .5, 0] for practice 5, with exact knots at m = 0 and m = ρ and the drawn curve equal to the applied function at every sample of every threshold; the scale null holding across the whole factor grid; the recorded Monte-Carlo trajectory .437500, .406250, .531250, .519531 at 16, 64, 256 and 1,024 draws with corrections .305968, .152984, .076492, .038246 and endpoints .743468, .559234, .607742, .557777, each covering the exact .5 while the estimates do not approach it monotonically; the real experiment giving fit mistakes 52, 46, 36, 19, 8 of 240, validation 16, 14, 10, 6, 4 of 80 and assessment 11, 8, 6, 5, 2 of 80, with ρ = 1 expressions 1.293533, 1.258458, 1.198668, 1.129848 and 1.198469, every one above the trivial ceiling 1, energy factor .0799496, confidence .335172, the worked B = 2 sum .474878 + .319799 + .335172 = 1.129848, validation selecting B = 4 against a majority baseline of 29 of 80, and the smallest expression belonging to B = 2 at ρ = 1 and B = 1 at ρ = .5; the zero candidate making 134, 41 and 51 mistakes under the score ≥ 0 tie rule with every margin exactly zero; 476 distinct feature vectors among 480 rows with two repeated pairs crossing fitting and validation and none involving assessment; and every investigation updating its calculated quantities and drawn marks directly from valid inputs.",
};
