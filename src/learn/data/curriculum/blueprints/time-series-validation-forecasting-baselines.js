export default {
  summary: 'Reconstruct the moment a forecast was issued: what had been observed, how far ahead the request reached, and which simple rule it had to beat. Build rolling-origin evaluation from the one inequality that decides whether a training row may be used, then read a real measured comparison in which the selected model wins on aggregate and loses at three of seven horizons.',
  outcomes: [
    'Separate the three clocks a forecast carries: when the outcome occurs, when the forecast was issued, and when each observation became available',
    'Read and write the notation for a forecast made at one origin for a target h days later, and say why grouping by target date alone loses the distinction',
    'Produce the mean, naive, seasonal-naive and drift forecasts from one history, and name the observation each rule copies',
    'Apply the seasonal index T - m + ((h-1) mod m) past one full cycle without reading a future outcome to extend it',
    'Decide whether a training row may enter a fit at a given cutoff, from the single inequality s + h + d <= cutoff',
    'Explain why a reporting delay moves the historical feature snapshot backward as well as postponing the label',
    'Derive the index-splitter gap g >= h + d - 1 rather than copying a constant from a library example',
    'Distinguish a fixed-origin recursive multi-step forecast from an updated one-step forecast reissued each period',
    'Recognise that a protocol error need not improve a score, using a constructed pair with equal MAE',
    'Lay out a rolling-origin rehearsal with its per-horizon eligible maximum origin t - h and its own scaler fit',
    'Distinguish an expanding window from a sliding one and say what each trades away',
    'Distinguish a rolling assessment with a locked update policy from a frozen-model assessment',
    'Compute MAE and RMSE by horizon, and say why averaging per-horizon RMSE differs from pooling squared errors',
    'Report an error curve that is not monotone in horizon, and identify horizon confounded with weekday',
    'Select a procedure on development origins and assess it on later origins without reselecting on the later result',
    'Read an aggregate winner that loses at particular horizons, and explain an exact tie as a consequence of the rules',
    'Identify a future-outcome edit as one that changes a measured error but not an already issued forecast',
    'Compute a mean-absolute-scaled-error denominator from training history alone, and say why a zero scale leaves it undefined',
    'Separate overlapping labels entering training, shared information between tasks, and dependent assessment errors',
  ],
  prerequisites: ['ML Problem Formulation, Baselines & Data Leakage', 'Cross-Validation & Hyperparameter Tuning'],
  sequence: [
    'Three clocks: outcome time, issue time and availability time, extending the previous lesson contract',
    'Forecast notation y-hat at t+h given t, the horizon, and why origin and target must both be stored',
    'Nowcasting and retrospective reconstruction as different tasks whose scores answer a different question',
    'Four baseline rules on one six-day operating cycle, with the observation each carries forward',
    'The drift slope over T-1 intervals, and clipping a count-valued prediction at zero',
    'The seasonal index T - m + ((h-1) mod m), and repeating the observed block past one full cycle',
    'MAE on a constructed continuation: seasonal 0, mean and naive 5, drift 11',
    'A history edit that moves the seasonal and mean rules and leaves naive and drift untouched',
    'Naming a training row by its origin: lag features, the horizon label, and the two availability conditions',
    'A reporting delay moving the feature snapshot backward as well as postponing the label',
    'Eligible origins at cutoff 12: 6 and 7 at h=3 d=2, 6 to 9 at zero delay, none at cutoff 10',
    'The splitter convention g >= h + d - 1 and where its minus one comes from',
    'Origin-indexed against target-indexed rolling statistics, and the centred mean that is not causal',
    'A missing-date policy: reindex the intended calendar, and distinguish a missing report from a recorded zero',
    'Fixed-origin recursive against updated one-step, with equal MAE 10 on the first continuation',
    'A changed continuation where the updated sequence scores better, and the claim that is still wrong',
    'The five steps of a rolling-origin rehearsal, and saving the origin with every forecast',
    'Expanding against sliding windows, chosen on development forecasts rather than after the fact',
    'Per-horizon boundaries t - h, each with its own fit and its own scaler',
    'A rolling assessment with a locked update policy against a frozen-model assessment',
    'MAE and RMSE by horizon, pooling identities, and horizon confounded with weekday on a weekly schedule',
    'The declared bicycle contract: 52 Saturday origins, 36 development and 16 final, two days omitted',
    'Six candidates compared on development MAE, selecting expanding ridge at 772.33',
    'The final assessment: 1167.62 for the selected procedure against 1310.34 naive and 1390.91 seasonal',
    'The per-horizon table where seasonal wins at h=4 and h=5 and ties naive exactly at h=7',
    'Two real weeks in which the same weekly rule wins one and loses the other',
    'Deeper: direct, recursive and joint multi-step models and their different label-maturity conditions',
    'Deeper: overlapping targets, shared information and dependent assessment errors as three separate mechanisms',
    'Deeper: point forecasts against interval and path claims, and residuals against horizon errors',
    'Deeper: scaled errors, the lognormal mean against its median, and origin-available decomposition',
  ],
  visual: {
    type: 'Every coordinate is computed in the verified model layer rather than placed by hand, because a drawn split boundary is a mathematical claim about what was knowable when, and one drawn slightly wrong teaches the opposite of the lesson. Five inline figures: two fourteen-day calendar lanes ending on the same Saturday with only the shaded observed region differing, so the target square is visibly identical while the information is not, plus an event-against-arrival note; one training row drawn as a feature window, a target and a separate label-arrival journey against a cutoff, with the two availability decisions reported in their own rows because they fail separately; three forecast chains from the same last observation, one feeding its own predictions forward, one consuming a newly observed outcome at each advancing cutoff, and a third carrying the second chain numbers under the first chain issue date with its illegal arrows marked and counted by an audit rather than by a caption; a staircase of three rehearsals seven days apart with an expanding-against-sliding toggle, a per-horizon inspector showing that the last eligible training origin moves with h, an origin-step control that keeps previously issued forecasts, and a table whose final column is the largest observation index any of the seven fits reads; and the measured comparison at two resolutions, six development candidates as bars from a true zero and three final methods as horizon curves with the shared naive-seasonal point at h=7 ringed rather than hidden under whichever mark painted last.',
    question: "Which observation does this rule copy at this horizon, and what number does it give? Which of the offered training origins may enter a fit performed at this cutoff, and does the answer change when the label value changes? Will this weekly rule beat the naive rule on this particular week, and what will it forecast three days out? Change the controls to inspect each calculation directly.",
    interaction: "Change history, horizon, seasonal period, cutoff and label delay to update donor links, eligible rows and forecast/error tables. An exploratory edit does not recreate an unused historical test; keep the frozen recorded study distinct. Results update directly on valid edits, with reset and explicit comparison snapshots; no learner prediction inputs or grading gates.",
  },
  practice: {
    task: 'Eight changed tasks: eligible rows under a one-day reporting delay at two cutoffs, a five-horizon seasonal forecast at period 3 with the donor for horizon five identified and edited, repairing a report that calls three updated one-day forecasts a three-day forecast, two error profiles with equal MAE and different RMSE, whether a per-horizon hybrid chosen from the final table has established final performance, an irregular four-row calendar whose previous row is not yesterday, the latest eligible origin for a three-day summed target at cutoff 20 with the independence question stated separately, and a scaled-error denominator including the constant-history case where it is undefined.',
    success: 'A training row is admitted by the label-arrival inequality rather than by its feature date alone; a reporting delay is applied to the feature snapshot as well as the label; an updated one-step sequence is never reported as a multi-step forecast issued once, whether or not it scores better; an exact tie between two rules is explained by the rules rather than treated as a coincidence; a per-horizon winner chosen on the final table is called selection rather than assessment; a future-outcome edit is described as changing a measured error and not an issued forecast; a missing calendar day is distinguished from a recorded zero; and a scaled error with a zero denominator is called undefined rather than patched with a small constant.',
  },
  misconceptions: [
    'A date column is enough to make an experiment temporal',
    'A forecast is identified by its target date alone',
    'Every row in a downloaded CSV was available at every historical moment',
    'A reporting delay only postpones the label',
    'The gap between train and test is a constant copied from a library example',
    'Excluding recent rows is a convention rather than an information constraint',
    'A baseline is a meaningless constant rather than a complete prediction rule',
    'Seasonal means summer and winter rather than any repeating operating cycle',
    'A seasonal rule may extend itself past one cycle by reading future outcomes',
    'Drift continues the mean of the observations rather than the last level plus a slope',
    'Seven updated one-day forecasts are a seven-day forecast',
    'A protocol error must produce a better score to matter',
    'Rolling-origin evaluation refits one model and reuses it at every origin',
    'Every horizon in a direct multi-step experiment shares one training matrix',
    'A test set settles whether the model was refitted during the assessment',
    'Forecast error must increase at every horizon',
    'An aggregate winner wins at every horizon',
    'Two rules giving identical errors is a coincidence worth remarking on rather than a consequence',
    'Choosing a different model per horizon from the final table is assessment rather than selection',
    'Editing a future outcome changes the forecast that was already issued',
    'A scaled error below one proves a win over the naive forecasts on the assessment period',
    'Averaging per-horizon RMSE equals pooling squared errors first',
    'Adjacent weekly forecast errors are independent observations',
    'Exchangeability-based conformal guarantees transfer to a time series by relabelling the axis',
    'Exponentiating a mean prediction on the log scale gives an unbiased mean forecast',
  ],
  sources: [
    'https://otexts.com/fpp3/',
    'https://otexts.com/fpp3/simple-methods.html',
    'https://otexts.com/fpp3/tscv.html',
    'https://otexts.com/fpp3/accuracy.html',
    'https://otexts.com/fpp3/prediction-intervals.html',
    'https://otexts.com/fpp3/distaccuracy.html',
    'https://otexts.com/fpp3/ftransformations.html',
    'https://scikit-learn.org/stable/auto_examples/applications/plot_time_series_lagged_features.html',
    'https://archive.ics.uci.edu/dataset/275/bike+sharing+dataset',
    'https://doi.org/10.24432/C5W894',
  ],
  depth: 'core',
  designRecord: 'docs/teaching/drafts/time-series-validation-forecasting-baselines/design.md',
  reviewFocus: 'The information-set invariant first: no fitted window may contain an observation dated later than its own origin, asserted over every split the lesson draws and every split its controls can produce, with feature causality against the row own origin and label maturity against the fit origin checked as separate claims. Then the constructed fixtures: seasonal 12, 22, 12, 22 with MAE 0 against mean and naive 5 and drift 11 on the continuation 12, 22, 12, 22; the period-3 forecast 20, 12, 22, 20, 12, 22, 20, 12 through eight horizons, which must repeat the observed block and never read an outcome; the fifth-value edit 12 to 18 giving seasonal 18, 22, 18, 22 at MAE 3 while naive and drift do not move and the mean becomes 16.666667; the eligibility sets 6 and 7 at cutoff 12 h 3 d 2, 6 to 11 at h 1 d 0, 6 to 9 at h 3 d 0 and empty at cutoff 10, with the empty set reachable and reportable; the recursive 24, 26, 28, 30 against the updated 24, 14, 24, 14 both at MAE 10, then the changed continuation giving 2 against 0.5. Then the measured results: development MAE 2083.70, 1198.30, 987.32, 1203.00, 772.33 and 883.80, selecting expanding ridge; final 1167.62, 1310.34 and 1390.91; the final horizon curve 1134.02, 1118.55, 1530.54, 965.26, 931.64, 926.52, 1566.82 for ridge against naive 1313.69, 1549.19, 1750.13, 931.88, 1092.44, 1172.63, 1362.44 and seasonal 1686.56, 1698.63, 2377.69, 703.69, 890.56, 1016.81, 1362.44, with seasonal winning at h 4 and h 5 and the exact naive-seasonal equality at h 7 shown as a consequence of the seven-day donor being the origin own count. On the real series: origin 364 period 7 giving seasonal MAE 1042.571429 against naive 789.571429, origin 371 giving 978 against 1466.714286, origin 476 giving 1702.857143 at period 14 against 2449.714286 at period 7; the future-edit null at row 365 leaving the seasonal forecast array identical while the first actual becomes 3294; and the history edit 754 to 1754 at index 358 moving only the first forecast and dropping the MAE by exactly 1000/7. Check separately that valid edits update the forecasts and observed-error comparison directly with no learner-answer requirement, that every tie is reported with every method attaining it rather than the first one found, that an unscored horizon is reported as unscored and never as zero, and that the served dataset is this lesson own copy rather than a sibling directory.',
};
