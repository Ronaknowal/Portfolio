import { H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { ConvexHullLab, HullEnvelopeFigure, OrientationLab, PolygonQueryLab, PredicatePrecisionLab, SegmentIntersectionLab } from '../../components/lesson-labs/ComputationalGeometryLabs.jsx';
import { computationalGeometryExamples } from '../computational-geometry-examples.js';
import geometryPractice from '../practice/computational-geometry-robust-predicates-convex-hulls.js';
import GeometryLibraryBridge from '../../components/lesson-labs/GeometryLibraryBridge.jsx';
export default {
  title: 'Computational Geometry, Robust Predicates & Convex Hulls',
  readTime: '~75 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot computational-geometry-lesson">
    <LessonIntro prerequisites="Coordinate pairs, comparisons, loops, lists/stacks and sorting. We rebuild signed area, convexity and the needed numerical distinctions locally; a prior geometry course or geometry library is unnecessary." sections={[['1-make-a-geometric-decision', 'Coordinates and turns'], ['2-test-segments-with-a-boundary-contract', 'Segments and contact'], ['3-keep-the-sign-reliable', 'Precision and exactness'], ['4-wrap-a-point-set-with-a-convex-hull', 'Hull construction'], ['5-query-a-polygon-without-erasing-its-shape', 'Area and containment'], ['6-use-the-geometry-with-a-clear-contract', 'Applications and transfer'], ['7-practise-changed-geometric-cases', 'Independent practice'], ['guided-dsa-practice', 'LeetCode practice']]}>You have points from a floor plan. Which side of a wall is a point on? Do two segments meet? Which locations form the outer envelope? A reliable answer needs both a geometric rule and arithmetic that preserves its decisions. Build those rules from one small signed calculation, then combine them into complete algorithms.</LessonIntro>

    <H2>1. Make a geometric decision</H2>
    <Prose>A point A=(1,1) names a location. A displacement B−A names how to move from A to B. For B=(7,3), that displacement is (6,2): six units right, two up. We use ordinary Cartesian coordinates, with y increasing upward. A screen often increases y downward; our drawing flips screen y while keeping the mathematical data unchanged.</Prose>
    <Prose>Now add C=(4,5). Imagine walking from A toward B. C is on your left. This left/right/on-line decision is an <strong>orientation predicate</strong>. A predicate supplies a decision used by an algorithm; it need not return only true or false. Here its three results are positive, negative and zero.</Prose>
    <pre className="geometry-calculation" aria-label="Worked orientation calculation"><code>{`u = B − A = (6, 2)
v = C − A = (3, 4)
D(A,B,C) = u.x × v.y − u.y × v.x
         = 6 × 4 − 2 × 3 = 18`}</code></pre>
    <Prose>Why those products? With a horizontal baseline u=(width,0), width×v.y is signed base times height: positive above, negative below. For a tilted baseline, subtracting u.y×v.x removes the part explained by the baseline's rise. The result is the <strong>determinant</strong> of the two displacement columns. Its magnitude is parallelogram area, so triangle ABC has area |D|/2. Our example has area 9 square units.</Prose>
    <OrientationLab />
    <Prose>D&gt;0 means counterclockwise, or left of directed AB; D&lt;0 means clockwise/right; D=0 means collinear, including coincident points. Reversing A and B negates D without changing unsigned area. Translating all three points cancels out of their differences. Scaling both coordinate axes by a positive factor s multiplies D by s², preserving its sign. Reflecting one axis reverses the sign. These are useful tests of an implementation, not decorative identities.</Prose>
    <Prose>Do not divide slopes to compare directions. Vertical lines have a zero x difference, and near-equal slopes can round together. The determinant uses multiplication and subtraction, handles vertical baselines directly, and exposes the sign we actually need. If A=B, however, there is no unique baseline direction; D is zero for every C. A caller asking about a line must select two distinct points first.</Prose>
    <RunnableExample example={computationalGeometryExamples.turnAndArea} />
    <Prose>All programs here are complete Python 3.12 examples using the standard library. Save one block as <code>geometry.py</code> and run <code>python geometry.py</code>. Core routines have an integer-coordinate contract and exploit Python's arbitrary-size integers. The explicitly marked precision example deliberately converts to float to demonstrate failure; the rational-construction example deliberately extends arithmetic to fractions. The browser runs fixed geometric models, not these Python programs.</Prose>
    <Checkpoint prompt="C=(4,2) gives zero for the original A and B. Does that prove C is between them? What if C=(10,4)?"><Prose>Both points lie on the infinite line: C−A is a multiple of (6,2). Only (4,2) lies between the endpoints. Orientation says which line/side; a segment also needs coordinate bounds. A zero determinant alone does not test segment membership.</Prose></Checkpoint>

    <H2>2. Test segments with a boundary contract</H2>
    <Prose>A <strong>closed segment</strong> includes its two endpoints and everything between them. Our result distinguishes <em>disjoint</em>, <em>proper crossing</em> through both interiors, <em>touch</em> at a single point involving an endpoint, and <em>overlap</em> along a positive-length segment. A zero-length segment is a point. Declaring these cases first prevents an implementation from accidentally deciding that touching is either forbidden or allowed.</Prose>
    <Prose>First ask whether the x intervals and y intervals overlap. If either pair is disjoint, the segments cannot meet. This <strong>bounding-box test</strong> is a cheap rejection, not a complete intersection test: two diagonal segments can have overlapping boxes and still miss. For closed contact use inclusive comparisons. A task asking for positive rectangle overlap area instead needs positive overlap width in both axes.</Prose>
    <Prose>For noncollinear segments AB and CD, evaluate D(A,B,C), D(A,B,D), D(C,D,A), D(C,D,B). A strict sign change in the first pair places C and D on opposite sides of AB's line. A strict change in the second places A and B on opposite sides of CD's line. Both together give a proper crossing.</Prose>
    <Prose>The geometric reason is continuity along each straight segment. A point moving from C to D crosses AB's infinite line when its signed orientation changes sign. Requiring AB to straddle CD as well places that line intersection inside both finite segments. One straddle test alone could place the crossing on an extension beyond an endpoint.</Prose>
    <SegmentIntersectionLab />
    <H3>Zeros need a separate path</H3>
    <Prose>A point Q belongs to AB exactly when D(A,B,Q)=0 and Q's x and y coordinates both lie in the closed endpoint intervals. With A=B those bounds require Q=A, so the same membership rule handles a point segment. On a vertical segment the y interval does the useful work; on a horizontal segment the x interval does.</Prose>
    <Prose>The implementation gathers distinct endpoints belonging to both segments. Two or more give a collinear overlap, whose ends are the first and last in lexicographic order. One gives a touch. If there are none, perform the strict straddle test. This covers coincident/reversed segments, shared endpoints, point segments and collinear gaps without dividing by a slope or treating zero as an arbitrary sign.</Prose>
    <RunnableExample example={computationalGeometryExamples.segments} />
    <Prose>There are only four endpoint membership tests and four side values, so a pair takes O(1) arithmetic operations. Testing every pair among n segments takes O(n²) such tests. A box filter may reduce the work in particular data; it does not change that worst-case bound. A sweep-line algorithm or spatial index requires additional ordering and event invariants, which this pairwise routine does not secretly provide.</Prose>
    <Checkpoint prompt="A test returns true only when both orientation products are negative. Which of these does it miss: a proper cross, a shared endpoint, two overlapping collinear segments, a point on a segment?"><Prose>It recognizes the proper cross and misses all the others because at least one determinant is zero. Whether those cases should count depends on the task, but silently dropping them violates our closed-contact contract. Add boundary membership and overlap handling before applying strict straddles.</Prose></Checkpoint>

    <H2>3. Keep the sign reliable</H2>
    <Prose>A wrong distance in the final decimal place may produce a small metric error. A wrong orientation sign can choose a different branch: remove a hull point, miss a contact or create inconsistent connectivity. That is a change in <strong>topology</strong>—which parts connect or bound a region—not just a slightly different printed coordinate. A proof assuming exact signs no longer describes such an execution.</Prose>
    <Prose>Consider A=(0,0), B=(N+1,N), C=(N,N−1). The exact determinant is (N+1)(N−1)−N²=−1 for every integer N. At N=2²⁷, all input coordinates are exactly representable in binary64, yet its two large products round to the same value and their subtraction becomes zero. Exact input storage is necessary here, but insufficient.</Prose>
    <PredicatePrecisionLab />
    <Prose>The second example fails earlier. Binary64 cannot distinguish the integers 2⁵³ and 2⁵³+1. Converting both to floating point can turn two distinct locations into one. An exact predicate applied to those already rounded coordinates correctly answers a different question; it cannot recover the intended lost input. This is why “use more accurate arithmetic later” is not a complete data pipeline.</Prose>
    <H3>Choose what “exact” refers to</H3>
    <LessonTable caption="Different numerical contracts" headers={['Input and task', 'Useful policy', 'Limit that remains']} rows={[['Integer points; side/hull decisions', 'Arbitrary-size integer subtraction/products, or a proved sufficient fixed width.', 'Intermediate differences/products need room; large-integer operations are not constant-time in bit complexity.'], ['Intended decimal or rational geometry', 'Parse exact strings as rational values before rounding.', 'Input size, denominators and construction costs may grow.'], ['Already stored floating-point coordinates', 'A robust predicate can compute the correct sign for those represented values.', 'It does not recover unrecorded digits or physical measurement certainty.'], ['Noisy measurements or deliberately snapped coordinates', 'State a measurement/tolerance/snap model before constructing the geometry.', 'Changing equality or topology is a modeling choice, not a universal arithmetic fix.']]} />
    <Prose>An arbitrary rule such as “if |D|≤10⁻⁹, call it zero” is not a universal repair. The same positive triangle can have D=10⁻¹⁰ in one unit system and D=100 after scaling both axes by 10⁶. A fixed determinant epsilon changes its collinearity verdict. Even a distance-based tolerance changes the modeled geometry and needs consistent semantics; approximate equality may fail transitivity. A≈B and B≈C need not imply A≈C, making naive tolerance-based sorting or deduplication dangerous.</Prose>
    <Prose><strong>Adaptive or filtered predicates</strong> first attempt inexpensive arithmetic with a justified error bound. If that bound certifies a nonzero sign, they return it; otherwise they use enough extra precision/exact work to resolve the decision. This is different from guessing an epsilon. Shewchuk's orientation/incircle implementation and CGAL's exact-predicate kernels are established references, with their own arithmetic/platform contracts. We do not claim this lesson's BigInt illustration implements either library's adaptive algorithm.</Prose>
    <RunnableExample example={computationalGeometryExamples.precision} />
    <Prose><code>Fraction("0.1")</code> means the intended rational 1/10. <code>Fraction(0.1)</code> means the exact rational value of the already rounded binary float. Both are exact representations of different inputs. Likewise, integer correctness needs a bound on intermediate products: for |coordinate|≤M, each difference is at most 2M and |D|≤8M² is a safe bound. Python integers grow; a fixed-width implementation must prove that its intermediate arithmetic fits or use a robust alternative.</Prose>
    <H3>A predicate is not a construction</H3>
    <Prose>Knowing that segments cross does not give their crossing coordinates. Write a point on AB as A+t(B−A). For nonparallel direction vectors r=B−A and s=D−C, crossing with s gives t=cross(C−A,s)/cross(r,s); the term involving s vanishes because cross(s,s)=0. The numerator and denominator may be integers while t is a fraction.</Prose>
    <Prose>The diagonals from (0,0) to (3,3) and from (0,2) to (3,0) cross at (6/5,6/5). Rounding that constructed point and feeding it into later “on edge” predicates can create a new inconsistency. CGAL distinguishes exact-predicate/inexact-construction and exact-predicate/exact-construction kernels for this reason. Choose the contract needed by the whole algorithm.</Prose>
    <RunnableExample example={computationalGeometryExamples.rationalIntersection} />
    <Prose>This function intentionally accepts only a proper crossing: its denominator is nonzero and both parameters lie in the open interval. It raises for touching or parallel/overlap cases, whose result is already handled separately. The fractions preserve the exact crossing and pass both segment membership checks. Measurement uncertainty is still outside this purely mathematical construction.</Prose>

    <H2>4. Wrap a point set with a convex hull</H2>
    <Prose>A region is <strong>convex</strong> when every segment joining any two of its points stays in the region. The <strong>convex hull</strong> is the smallest convex region containing a point set. Imagine a taut band around pins: interior pins do not bend the band. This picture needs explicit degeneracies: no points give an empty hull, one distinct point gives a point, and collinear points give a segment rather than a two-dimensional polygon.</Prose>
    <HullEnvelopeFigure />
    <Prose>A hull's <strong>extreme vertices</strong> are its actual corners; points in the middle of straight boundary edges are not additional corners. A fence-record task may still require every input location on those edges. Our algorithm supports both policies. It returns distinct coordinates, starts at the lexicographically smallest point, proceeds counterclockwise for a nondegenerate hull, and does not repeat the starting point at the end. A caller needing a closed drawing connects the last vertex back to the first.</Prose>
    <H3>From a correct baseline to a changing chain</H3>
    <Prose>A simple baseline considers every directed pair of distinct points and tests whether every other point lies on its left or on its line. Supporting pairs mark the hull boundary, with extra work to reduce collinear pairs to the desired endpoints/order. There are O(n²) candidate pairs and O(n) side tests per pair: O(n³) predicate work. This slow idea is also a useful independent boundary oracle for small cases.</Prose>
    <Prose><strong>Andrew's monotone chain</strong> avoids trying every pair. Sort unique points by x, breaking ties by y. Scan them left to right to construct the lower boundary. Keep a stack of candidate boundary vertices. When a new point arrives, inspect the turn formed by the last two stack points and the candidate. A right turn bends upward into the region instead of along its lower convex boundary, so remove the last stack point and try again. For corners-only output remove a zero turn as well: the middle collinear point is redundant.</Prose>
    <Prose>For example, while scanning (1,1), (2,4), (3,2), the turn is 1×1−3×2=−5. The middle point (2,4) is above the segment from (1,1) to (3,2), so it cannot remain on this lower boundary. Popping it does <em>not</em> prove that it is absent from the complete hull: it may belong to the upper boundary. This is why the lab labels a pop as a removal from one chain.</Prose>
    <ConvexHullLab />
    <H3>Why the scan works</H3>
    <Prose>The invariant is that the stack describes the lower convex boundary of the processed sorted points, with every processed point on or above its directed edges. Initially one or two points satisfy the claim directly. The next candidate is at the right end of the processed ordering. Only the final part of the old boundary can become hidden by its arrival; earlier edges stay supporting until the candidate exposes a violating suffix.</Prose>
    <Prose>Each wrong turn identifies a final vertex above the chord joining its predecessor to the candidate, so that vertex is unnecessary on the new lower envelope. Removing it exposes the preceding edge, which must be checked again. Stop when the last turn satisfies the policy, then append the candidate. The surviving chain has the permitted turns and supports all processed points. Vertical ties are handled by the same lexicographic order and determinant, not by dividing by a slope.</Prose>
    <Prose>Repeat on the reversed sorted order to obtain the upper boundary. Omit the last point of each chain when joining so that the two endpoints occur once. For all-boundary output, keep zero turns; handle an all-collinear set separately, otherwise both scans would walk the same line and duplicate its middle locations. For a nondegenerate hull, lower and upper interior boundary points are distinct.</Prose>
    <RunnableExample example={computationalGeometryExamples.monotoneHull} />
    <Prose>Each point is pushed once and popped at most once per scan. This is a linear amount of stack work despite the nested <code>while</code>. Sorting costs O(n log n) comparisons; scanning costs O(n) predicate operations; storage is O(n), including the result. These are arithmetic-operation bounds. With B-bit integer coordinates, comparisons and multiplications have bit costs; do not interpret Python's growing integers as free. The browser additionally stores small snapshots for back/forward teaching, which can use quadratic total trace space. The native algorithm stores only its chains.</Prose>
    <Prose>Other hull methods make different tradeoffs. Jarvis's march selects each next supporting vertex by scanning all points, taking O(nh) work for h output corners. A divide-and-conquer hull combines two smaller hulls via supporting tangents. Those are useful alternatives; the detailed correctness and degeneracy contract established here belongs to monotone chain, not automatically to every hull implementation.</Prose>
    <Checkpoint prompt="On the fence preset, should enabling all boundary points add (3,3)? Should it add (3,1)? Why not keep every point that was ever pushed?"><Prose>(3,3) is interior, so neither policy returns it. (3,1) is on a straight bottom edge, so only all-boundary mode adds it. Being pushed means a point was a temporary candidate; later points can hide it. The final envelope invariant, not membership in a historical stack, decides the result.</Prose></Checkpoint>

    <H2>5. Query a polygon without erasing its shape</H2>
    <Prose>A polygon is an ordered boundary, not an unordered point set. Our query routine assumes a <strong>simple polygon</strong>: at least three vertices, edges joining consecutive vertices cyclically, no self-crossing, and no holes. The fixed courtyard, rectangle and triangle satisfy this contract. A hull is convex; a simple polygon may be concave. Replacing one with the other can fill a doorway or courtyard opening.</Prose>
    <H3>Signed area from boundary contributions</H3>
    <Prose>For vertices (xᵢ,yᵢ), sum xᵢyᵢ₊₁−yᵢxᵢ₊₁ around the boundary, including the last-to-first edge. Half of this <strong>shoelace sum</strong> is signed area: positive for counterclockwise order and negative after reversal. Each term is the doubled signed area of a triangle from the origin to an edge. Contributions outside a concave shape cancel; taking each term's absolute value separately would destroy that cancellation. Take the absolute value only after summing to get ordinary area.</Prose>
    <Prose>The courtyard's outer rectangle is 6×5, and its notch is 2×3, so its area is 30−6=24. The sum gives 48; reversing the vertex list gives −48 and the same unsigned area. On a self-intersecting boundary the same algebra produces an oriented sum, which is not automatically the physical union area. Validate the input or state a different winding/fill convention.</Prose>
    <H3>Locate a query with a boundary-first ray</H3>
    <Prose>First test whether Q is on any edge with the exact segment-membership rule. If so, return <em>boundary</em>. Otherwise cast a horizontal ray to the right. Starting far outside and crossing a simple boundary alternates between inside and outside. Thus an odd number of proper boundary crossings to the right means inside; an even number means outside.</Prose>
    <Prose>A ray passing through a vertex needs a consistent endpoint convention. An edge is eligible when exactly one endpoint is strictly above Q: <code>(a.y &gt; q.y) != (b.y &gt; q.y)</code>. This makes its vertical interval half-open. Horizontal edges are ineligible; the earlier boundary test still detects a query sitting on one. A ray continuing through a vertex is counted once, while a tangent at a local extremum contributes zero or two, leaving parity unchanged.</Prose>
    <Prose>We still need to know whether an eligible edge meets the ray to Q's right. For an upward edge, that happens when D(A,B,Q)&gt;0; for a downward edge, when D&lt;0. To see the sign, at Q's height write D=(b.y−a.y)×(x<sub>cross</sub>−q.x). The vertical direction supplies the sign reversal. No division is needed for the decision. The boundary test has already removed zero-on-segment cases.</Prose>
    <PolygonQueryLab />
    <RunnableExample example={computationalGeometryExamples.polygonQuery} />
    <Prose>There is one pass over m edges: O(m) predicate work and O(1) auxiliary state. The function assumes simplicity; it does not prove it. An O(m²) small-input validator can test nonadjacent edge pairs for intersections, reject repeated nonadjacent vertices and zero-length edges, and require adjacent edges to meet only at their shared endpoint. Production validation, holes, spherical longitude/latitude and self-intersection fill rules need their own explicit contracts.</Prose>
    <Checkpoint prompt="Q=(4,5) is inside the courtyard's bounding rectangle and inside its convex hull. Why is the polygon result still outside? Would reversing the boundary change membership?"><Prose>Q lies in the open notch, so the actual boundary produces two right crossings and outside parity. Both bounding objects deliberately fill that opening. Reversal flips every edge's orientation and upward/downward status together, preserving the crossing decisions and membership; signed area alone changes sign.</Prose></Checkpoint>

    <H2>6. Use the geometry with a clear contract</H2>
    <Prose>One useful application is a two-stage collision query. A bounding rectangle can quickly reject distant objects; a more detailed hull or original-shape test handles survivors. This is called a <strong>broad phase</strong> followed by a <strong>narrow phase</strong>. Overlapping bounding boxes mean “inspect further,” not “collision proven.” For a point moving along a segment, test the entire path against relevant boundaries, not only its endpoints. For a finite-size moving object, point-path tests alone omit its footprint; obstacle inflation and continuous collision belong to the motion-planning continuation.</Prose>
    <Prose>A less obvious use is a directional summary. Suppose a query asks for the largest score w.x×p.x+w.y×p.y over a fixed point set. Every point in its convex hull is a weighted average of original points with nonnegative weights summing to one. Its linear score is the same weighted average of their scores, so it cannot exceed the largest. A maximum is attained at a hull corner, though a whole edge may tie. You can discard interior points for any such linear direction query without changing the maximum.</Prose>
    <Prose>For the fence coordinates and w=(2,−1), (7,1) scores 13 and no other point scores higher. Switching to w=(−1,2) selects (1,6) with score 11. This underlies directional extent/support calculations: the same hull answers many directions. It does not preserve every statistic. Removing interior records changes an ordinary average, sample density and neighborhood relationships.</Prose>
    <RunnableExample example={computationalGeometryExamples.supportAndBounds} />
    <Prose>Scanning h hull corners takes O(h) per direction after O(n log n) preprocessing. For one query a direct O(n) scan may be preferable; a preprocessing benefit depends on the number of queries and h. Ordered-hull search and rotating-calipers methods can improve specialized sequences of queries but require additional monotonicity arguments. The four checked directions here are algebraic examples, not a speed benchmark.</Prose>
    <H3>Optional transfer: how many records share a line?</H3>
    <Prose>Fix an anchor point and group every other point by its line direction. A raw slope is a poor hash key: verticals divide by zero and rounded values can merge distinct lines. With integer coordinates, divide (dx,dy) by gcd(|dx|,|dy|), then choose one sign convention: dx positive, or dy positive if dx is zero. Thus (2,2), (−3,−3) and (1,1) all represent the same unoriented direction, while (0,5) becomes (0,1).</Prose>
    <Prose>A coincident point has zero displacement and no direction; keep duplicate multiplicity separately rather than divide by gcd(0,0). For each anchor, add its own multiplicity to the largest direction group's multiplicity. Trying every distinct anchor finds the best line. This transfers geometry into a canonical integer key, linking the earlier hashing and Euclid lessons.</Prose>
    <RunnableExample example={computationalGeometryExamples.lineGroups} />
    <Prose>For u distinct locations, the routine performs O(u²) pair/group operations and uses O(u) extra state per anchor, plus the input multiplicity map. GCD and large-integer hashing have additional bit costs; dictionary operations have their usual expected-cost assumptions. The official Max Points task has unique locations, while this version deliberately defines duplicate records and empty input as well.</Prose>
    <Prose>This lesson's finish line is reliable planar decisions, a verified hull and their segment/polygon uses. It is not a complete implementation of Delaunay triangulation, Voronoi diagrams, sweep-line arrangements, spatial indexes, spherical geometry or 3D hulls. Those need further predicates, representation and proofs. The robust-predicate distinction remains relevant when you learn them.</Prose>

    <GeometryLibraryBridge />
    <H2>7. Practise changed geometric cases</H2>
    <Prose>Work from the contracts before opening hints. For implementations, keep exact inputs, state the boundary policy and check adversarial cases as well as ordinary examples.</Prose>
    <div className="lesson-check"><p><strong>1. A new directed triangle.</strong> A=(−2,1), B=(4,4), C=(1,5). Compute the turn and area. Translate all points by (10,−7), then reverse A/B. Explain which values change.</p><details><summary>Hint</summary><Prose>Subtract A first; the displacements are (6,3) and (3,4).</Prose></details><details><summary>Worked solution</summary><Prose>D=6×4−3×3=15, a left turn with area 15/2. Translation preserves both displacement vectors, hence D and area. Reversal produces −15 and the same area. The location of the origin is irrelevant to this relative-side decision.</Prose></details></div>
    <div className="lesson-check"><p><strong>2. Decide what “intersect” means.</strong> Classify (0,0)→(4,0) against (2,0)→(6,0), then (4,0)→(4,3), then the point segment (5,0)→(5,0). Also classify (0,0)→(3,3) against (0,2)→(3,0), and construct that last result exactly.</p><details><summary>Hint</summary><Prose>Use shared endpoints and closed bounds before strict sign changes. A proper crossing needs a rational parameter for construction.</Prose></details><details><summary>Worked solution</summary><Prose>The first pair overlaps from (2,0) to (4,0); the second touches at (4,0); the point (5,0) is collinear but disjoint. The diagonals properly cross at (6/5,6/5). Reporting true/false alone would conceal whether the result is a point or a segment; returning the rounded crossing and demanding later exact membership would change the contract.</Prose></details></div>
    <div className="lesson-check"><p><strong>3. Repair a plausible numerical fix.</strong> Someone converts coordinates to float, then to an exact rational, and uses |D|≤10⁻⁹ as collinearity. Explain two distinct failures with the precision examples. Give an appropriate policy for intended integer data and for genuinely uncertain measurements.</p><details><summary>Hint</summary><Prose>Separate the source values, arithmetic error and physical/model uncertainty.</Prose></details><details><summary>Worked solution</summary><Prose>2⁵³+1 can already collapse to 2⁵³ at conversion; an exact rational afterward preserves the wrong intended coordinate. Separately, a determinant tolerance changes under squared unit scaling, classifying the same nonzero triangle differently. Preserve integer inputs and use sufficiently wide/exact products for integer geometry. For uncertain measurements, specify uncertainty or a justified snapping model and its topology policy; exact arithmetic cannot tell you which unknown physical location was true.</Prose></details></div>
    <div className="lesson-check"><p><strong>4. Reconstruct both hull contracts.</strong> Use (0,0),(2,0),(4,0),(4,3),(2,3),(0,3),(2,1),(0,0). Return corners and all boundary locations in the stated order. Then replace the input with (0,0),(1,1),(2,2),(1,1).</p><details><summary>Hint</summary><Prose>Deduplicate; the first set is a rectangle with extra edge/interior records. Handle the second set before running both polygon chains.</Prose></details><details><summary>Worked solution</summary><Prose>Corners are (0,0),(4,0),(4,3),(0,3). All-boundary order inserts (2,0) after the first and (2,3) after (4,3); (2,1) is interior. The line yields endpoints (0,0),(2,2), or the sorted list (0,0),(1,1),(2,2) when boundary records are requested. Repeating the start or the collinear middle would violate our unique-output contract.</Prose></details></div>
    <div className="lesson-check"><p><strong>5. A ray through vertices.</strong> Let the polygon be (0,0),(6,0),(6,4),(4,4),(4,2),(2,2),(2,4),(0,4). Find area and classify Q=(3,3), (1,3), (3,2), (6,4). Explain why counting every endpoint hit once is not a valid crossing rule.</p><details><summary>Hint</summary><Prose>Subtract the 2×2 notch from the 6×4 rectangle, and check boundary before parity.</Prose></details><details><summary>Worked solution</summary><Prose>Area is 24−4=20. The four results are outside, inside, boundary and boundary. At a vertex the ray can either pass through the boundary or only touch it. The half-open vertical rule distinguishes those events through zero/two versus one contributions; a uniform “one per vertex hit” rule can flip parity at a tangent. Reversing the polygon preserves membership and negates the signed area.</Prose></details></div>
    <div className="lesson-check"><p><strong>6. Build a small geometry report.</strong> From exercise4's input, produce both hulls, exact area, all four edge-contact classifications for a supplied path, and maxima in directions (1,1) and (−2,1). Compare support answers with scanning every original record. Include empty/collinear hull handling rather than passing them to a polygon routine.</p><details><summary>Hint</summary><Prose>Use (−1,1)→(5,1) as the first path; keep unique locations separate from input multiplicity.</Prose></details><details><summary>Example answer and acceptance checks</summary><Prose>The rectangle has area 12. Its edge order is bottom/right/top/left, so that path produces disjoint/proper crossing/disjoint/proper crossing. Support values are 7 at (4,3) for (1,1), and 3 at (0,3) for (−2,1), matching a full scan. An acceptable program composes the supplied routines, declares closed contacts and exact integers, checks those results, and adds a path through a corner plus a collinear edge overlap. It returns area zero for point/line envelopes and avoids claiming that all paths inside a hull stay inside an arbitrary original shape.</Prose></details></div>
    <Prose>Readiness means you can derive the sign rule, state and handle zero cases, justify each hull removal, preserve a boundary convention and diagnose a precision failure. Revisit a problem later with its usual method name hidden. Next in this module is <strong>Persistent Data Structures, Structural Sharing &amp; Versioned Queries</strong>: instead of keeping only a current structure, learn how updates can preserve earlier versions without copying everything.</Prose>
    <DsaPractice practice={geometryPractice} />

    <Sources alternatives={<Prose>For another explanation, <a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/resources/lecture-2-divide-conquer-convex-hull-median-finding/" target="_blank" rel="noreferrer">MIT 6.046 Lecture 2: Divide &amp; Conquer, Convex Hull</a> offers video, transcript and lecture notes. Use it after the core to compare a tangent-merge method with our stack scan. Its hull notes assume general position and use clockwise lists; our degeneracy and output contracts still need explicit handling. The official page, hull notes and relevant transcript explanation were inspected; full video playback was not.</Prose>}>
      <li><a href="https://www.cs.cmu.edu/~quake/robust.html" target="_blank" rel="noreferrer">Jonathan Shewchuk: Adaptive Precision and Robust Predicates</a> — author explanation, papers and implementation links for determinant-sign robustness. Read after the precision section; platform/arithmetic assumptions matter.</li>
      <li><a href="https://doc.cgal.org/latest/Kernel_23/index.html" target="_blank" rel="noreferrer">CGAL Kernel manual</a> — precise distinctions among predicates, constructions, coordinate types and intersection result variants. Version 6.2.1 documentation inspected; no CGAL execution is implied.</li>
      <li><a href="https://doc.cgal.org/latest/Convex_hull_2/index.html" target="_blank" rel="noreferrer">CGAL 2D Convex Hulls manual</a> — alternative algorithms, extreme-point output conventions and complexities. Check whether a library returns corners or all boundary records.</li>
      <li><a href="https://doc.cgal.org/latest/Polygon/index.html" target="_blank" rel="noreferrer">CGAL 2D Polygons manual</a> — simple-polygon, area and side-query contracts for moving beyond the local implementation.</li>
      <li><a href="https://docs.python.org/3/library/fractions.html" target="_blank" rel="noreferrer">Python fractions documentation</a> — construction from exact strings versus existing floats and rational arithmetic used by the complete intersection program.</li>
    </Sources>
  </div>
};
