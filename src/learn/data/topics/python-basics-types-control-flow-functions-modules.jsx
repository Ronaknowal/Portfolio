import { Code, H2, H3, Prose } from "../../components/content";
import { Checkpoint, LessonIntro, LessonTable, Sources } from "../../components/lesson-labs/LessonElements";
import PythonExample from "../../components/lesson-labs/PythonExample";
import { PythonReferencesLab, PythonFlowLab, PythonCallLab, PythonModuleDiagram } from "../../components/lesson-labs/python-foundations-labs";
import { ReferenceSetupFigure } from "../../components/lesson-labs/PythonReferenceFigure.jsx";
import { pythonFoundationsExamples } from "../python-foundations-examples";
import { pythonCoreExamples } from "../python-core-examples.js";

export default {
  title: "Python Basics: Types, Control Flow, Functions & Modules",
  readTime: "~45 min read + 75 min practice",
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot python-foundations">
    <LessonIntro prerequisites="No programming knowledge required. You need a Python 3.10+ interpreter only if you want to run the examples."
      sections={[["1-run-your-first-program", "Start here"], ["2-values-types-and-operations", "Values"], ["3-collections-and-references", "Collections"], ["5-functions-with-clear-contracts", "Functions"], ["7-build-a-two-file-program", "Build it"], ["8-practise-and-check", "Practise"]]}>
      A temperature log contains numbers, blanks and occasionally bad text. Build a program that turns it into a trustworthy report. Follow each decision, see how names refer to objects, and discover why a returned number is different from a printed message.
    </LessonIntro>
    <H2>1. Run your first program</H2>
    <Prose>Suppose two temperature measurements are 18 °C and 24 °C. Their average, or <strong>mean</strong>, is their total divided by how many measurements there are: (18 + 24) / 2 = 21 °C. A program records instructions that a computer can repeat when the measurements change.</Prose>
    <Prose>Python is a language for writing those instructions. An <strong>interpreter</strong> runs Python code. A <strong>script</strong> is a plain-text file containing that code. The terminal is where you ask the interpreter to run a file and see its output; the terminal and Python are different things.</Prose>
    <Prose>Use Python 3.10 or later. Save an example in a plain-text file named <Code>lesson.py</Code>, open a terminal in that folder, and run <Code>python lesson.py</Code>. On Windows, <Code>py lesson.py</Code> may be the available command; on some systems it is <Code>python3 lesson.py</Code>. These commands go in the terminal, not inside the Python file. No third-party packages are needed for this lesson.</Prose>
    <div className="pyf-program-flow" role="img" aria-label="Save instructions in lesson.py; run python lesson.py in a terminal; the interpreter prints the mean temperature"><div><strong>1 · Save instructions</strong><code>lesson.py</code></div><span aria-hidden="true">→</span><div><strong>2 · Run in a terminal</strong><code>python lesson.py</code></div><span aria-hidden="true">→</span><div><strong>3 · Read the output</strong><code>Mean temperature: 21.0 C</code></div></div>
    <PythonExample example={pythonFoundationsExamples.first}><Prose>Python evaluates the right-hand side before associating its result with the name on the left. First first refers to 18 and second to 24. Their addition produces 42; dividing by 2 produces 21.0. Print displays its arguments, separated by spaces by default. Execution proceeds down the file unless a branch, loop or function call redirects it.</Prose></PythonExample>
    <Prose>A calculation such as <Code>total / 2</Code> is an <strong>expression</strong>: it produces a value. An instruction such as an assignment is a <strong>statement</strong>. Parentheses after a function name, as in <Code>print(...)</Code>, call it with the supplied inputs. Quotes mark literal text: <Code>"mean"</Code> is the word, whereas <Code>mean</Code> asks for the value associated with that name.</Prose>
    <Checkpoint prompt="Change only second from 24 to 30. Predict total, mean and the printed line before running the script."><Prose>Total becomes 48 and mean becomes 24.0. The line is “Mean temperature: 24.0 C”. An assignment stores the result evaluated at that point; it does not create a spreadsheet formula that automatically recalculates when a name changes later.</Prose></Checkpoint>
    <Prose>A Python shell displays a bare expression's value, but a script generally needs <Code>print(...)</Code> to show it. Copy the code, not the expected-output block. Run standalone examples from a fresh script; the multi-file project explicitly names its required files. A notebook is also fine, but rerun from a fresh kernel to check that hidden state from an earlier cell is not making the program work accidentally.</Prose>
    <details className="pyf-deeper"><summary>Environment and recovery note</summary><Prose>These examples were independently run with Python 3.11.7; that is a tested snapshot, not a requirement to install that exact release. If no interpreter command exists, use the <a href="https://www.python.org/downloads/">official Python downloads and platform instructions</a>. If Python cannot open lesson.py, check the folder and filename first; some editors can accidentally save lesson.py.txt. The investigations here model fixed examples and do not execute arbitrary Python or change your files.</Prose></details>
    <Prose>Our running task is to read temperatures, ignore blanks and report their mean. We will first learn the pieces, then assemble the complete two-file program. Work through sections 1–4 in one sitting if you are new; continue with functions and the project when you can predict the smaller examples.</Prose>

    <H2>2. Values, types and operations</H2>
    <Prose>A value's type determines what it can do. <Code>"24"</Code> is text, not a number. Joining it to <Code>"1"</Code> gives <Code>"241"</Code>; converting it with <Code>int("24")</Code> allows numerical addition. Names are case-sensitive: <Code>reading</Code> and <Code>Reading</Code> are different. Use descriptive names and avoid replacing useful built-ins with assignments such as <Code>list = ...</Code>.</Prose>
    <LessonTable caption="The basic values you will encounter" headers={["Type", "Example", "Use and important limit"]} rows={[
      ["int", "24", "Whole-number counts; not restricted to a fixed 32-bit range."],
      ["float", "24.5", "Approximate real-number calculations; many decimals are not exact in binary."],
      ["bool", "True / False", "A yes/no result, usually from a comparison."],
      ["str", '"Pune"', "Immutable Unicode text; methods return new strings."],
      ["NoneType", "None", "An explicit missing/no-result marker, distinct from zero."],
    ]} />
    <Prose>Our raw reading moves through three stages: text with spaces → cleaned text → a numeric value. A <strong>method</strong> is an operation accessed through an object; the dot in <Code>raw.strip()</Code> selects the string's strip method, and the parentheses call it. Strip returns cleaned text; float then attempts the conversion.</Prose>
    <PythonExample example={pythonFoundationsExamples.conversion}><Prose>Repr makes quotes and spaces visible for diagnosis. Type(value).__name__ asks for the type's name; type(value) is also useful when exploring. Text addition joins characters to make 241, while numeric addition gives 25. An f-string inserts expressions inside braces, and :.1f requests one digit after the decimal point. Formatting changes display, not the stored value.</Prose></PythonExample>
    <Checkpoint prompt={'Predict "3" * 2 and 3 * 2. Which operation would double a count read as text?'}><Prose>The first repeats text and gives “33”; the second multiplies numbers and gives 6. Parse deliberately, such as int("3") * 2. Conversion does not establish that arbitrary data is meaningful for your task.</Prose></Checkpoint>
    <details className="pyf-deeper"><summary>Deeper: arithmetic and floating-point behavior</summary>
    <PythonExample example={pythonCoreExamples.basicsTypes} />
    <Prose><Code>/</Code> performs division; <Code>//</Code> is floor division, so a negative result rounds downward, not toward zero. <Code>%</Code> gives the remainder and <Code>**</Code> raises a power. Multiplication and division bind more tightly than addition; use parentheses when the intended grouping is not obvious. <Code>math.isclose</Code> compares approximate numbers using tolerances; choose tolerances meaningful for your units, particularly near zero.</Prose>
    <Prose>Binary floating-point cannot exactly represent many decimal fractions. The isclose check succeeds with its default tolerances, but appropriate tolerances depend on measurement units and required accuracy. Formatting to one decimal is a display decision, not an accuracy guarantee. Numerical stability receives its own later topic.</Prose></details>
    <H3>Text, conversion and input</H3>
    <Prose><Code>input("Temperature: ")</Code> prompts a terminal user and returns a string; convert and validate it before calculating. <Code>int("24.5")</Code> raises <Code>ValueError</Code>, while <Code>float("24.5")</Code> succeeds. We use fixed inputs here so examples are repeatable. Conversion is an explicit decision, not a guarantee that arbitrary input is acceptable.</Prose>
    <details className="pyf-deeper"><summary>Text tools for revision</summary><Prose><Code>len(text)</Code> counts Unicode code points, which need not match displayed glyphs for combined accents or emoji. <Code>text.lower()</Code> changes case, <Code>text.split(",")</Code> separates fields, and <Code>", ".join(words)</Code> combines strings. These return results without mutating the original string.</Prose></details>
    <Prose><Code>#</Code> starts a comment outside a string. Single and double quotes both create strings. Inside text, <Code>\n</Code> represents a newline; a backslash introduces such escape sequences. Use <Code>repr(value)</Code> when debugging invisible whitespace: it produces a representation that makes escapes and string quotes visible.</Prose>

    <H2>3. Collections and references</H2>
    <Prose>Several measurements should not require several separately named variables. A <strong>list</strong> holds an ordered sequence of object references. Square brackets create a list; commas separate its elements. An <strong>index</strong> selects one position, counting from zero. A negative index counts from the end. A <strong>slice</strong> selects a span, including its start but excluding its stop.</Prose>
    <LessonTable caption="Read the same list by position and span" headers={["Position", "0", "1", "2", "3"]} rows={[["Reading (°C)", "18", "21", "24", "27"], ["Negative index", "−4", "−3", "−2", "−1"], ["Included in [1:3]?", "No", "Yes", "Yes", "No"]]} />
    <PythonExample example={pythonFoundationsExamples.indexing}><Prose>Position 1 is replaced with 22; append adds 30 at the end. Len counts five elements and sum adds them to 121. Readings[1:3] contains positions 1 and 2, not three elements. Adjacent slices [:2] and [2:] cover the list without overlap.</Prose></PythonExample>
    <Prose>A <strong>dictionary</strong> maps named keys to values instead of positional slots. Our report will return <Code>{'{"count": 3, "mean": 21.0}'}</Code>. <Code>result["mean"]</Code> retrieves its named value, so a caller does not have to remember which position was the count and which was the mean.</Prose>
    <Checkpoint prompt="For readings = [18, 21, 24, 27], predict readings[1:1], readings[2:] and readings[4]. Which fails, and why?"><Prose>The empty span [1:1] gives []; [2:] gives [24, 27]. Index 4 raises IndexError because the last position is 3. A single index must exist, while a slice can describe an empty or clipped span.</Prose></Checkpoint>
    <details className="pyf-deeper"><summary>Deeper: choose among collections and inspect their operations</summary>
    <LessonTable caption="Choose a collection by what the task needs" headers={["Collection", "What it gives you", "Common operations"]} rows={[
      ["list", "Ordered, mutable sequence; duplicates allowed", "append, extend, pop, indexing, slicing"],
      ["tuple", "Ordered sequence whose element references cannot be reassigned", "unpacking, indexing, slicing"],
      ["dict", "Insertion-ordered mapping from unique hashable keys to values", "key lookup, get, items, update"],
      ["set", "Unique hashable elements; no positional indexing or promised display order", "add, membership, union, intersection"],
    ]} />
    <Prose>Hashable keys must have a stable hash/equality behaviour. Strings and integers make useful dictionary keys; a mutable list does not. <Code>{"{}"}</Code> makes an empty dictionary; use <Code>set()</Code> for an empty set. Tuples are immutable containers but can contain mutable objects—a tuple containing a list does not freeze that list.</Prose>
    <PythonExample example={pythonCoreExamples.basicsCollections}><Prose>Index 0 is the first element, and −1 is the last. Slice <Code>[1:3]</Code> includes positions 1 and 2, excluding 3. <Code>append</Code> adds one item; <Code>pop</Code> removes and returns the last item by default. <Code>items()</Code> provides key/value pairs that the loop unpacks. We sort the set before printing so the displayed result is deterministic.</Prose></PythonExample>
    <Prose>A missing dictionary key with <Code>record["humidity"]</Code> raises <Code>KeyError</Code>; <Code>get</Code> allows an explicit fallback. An out-of-range single index raises <Code>IndexError</Code>, whereas an oversized slice is clipped. <Code>sorted(values)</Code> returns a new list; <Code>values.sort()</Code> changes an existing list and returns <Code>None</Code>. Likewise, do not assign the result of <Code>append</Code> back to the list.</Prose>
    </details>
    <H3>Assignment is not copying</H3>
    <Prose>A name refers to an object. Assigning one name to another gives two references to that same object. Mutating the object changes what both names observe. Reassigning a name changes only its reference. This distinction explains many bugs that look like one variable changed “by itself”.</Prose>
    <Prose>Imagine two labels attached to one notebook. Adding a page changes what either label leads to; moving one label onto another notebook leaves the original intact. The analogy concerns references, not physical memory layout. In the explorer, mutation changes list slots and rebinding moves a name's arrow.</Prose>
    <ReferenceSetupFigure />
    <PythonReferencesLab />
    <PythonExample example={pythonCoreExamples.basicsAliasing}><Prose><Code>==</Code> asks about equality of values; <Code>is</Code> asks whether two references identify the very same object. Use equality for numbers and text, and <Code>is None</Code> for the missing-value marker. A shallow copy makes a new outer list but shares nested elements; copying a list of lists does not automatically copy every inner list.</Prose></PythonExample>
    <Checkpoint prompt="Right after c = a.copy(), before append, what would a == c and a is c return?">
      <Prose><Code>True</Code> and <Code>False</Code>. Their contents match, but they are different list objects. After appending to the shared a/b list, even the contents no longer match.</Prose>
    </Checkpoint>
    <details className="pyf-deeper"><summary>Deeper: a shallow copy leaves nested objects shared</summary><Prose>A list copy duplicates the outer sequence of references. If one element is another list, both outer containers initially point at the same inner list. Predict the difference between appending inside that list and replacing an outer slot.</Prose><PythonExample example={pythonFoundationsExamples.nested}><Prose>The outer lists have different identities but their first elements identify the same inner list. Appending appears through both outer lists. Replacing backup[1] moves only that slot. Choose a copying policy that matches the structure and intended sharing; an outer copy is not a recursive independence guarantee.</Prose></PythonExample></details>

    <H2>4. Decisions and repetition</H2>
    <Prose><Code>=</Code> assigns; <Code>==</Code> compares. Comparisons such as <Code>&gt;=</Code>, <Code>!=</Code> and <Code>in</Code> produce booleans. In an <Code>if / elif / else</Code> chain, Python takes the first true branch and skips the rest. The colon starts a block; indentation determines which statements belong to it. Use four spaces consistently.</Prose>
    <PythonExample example={pythonFoundationsExamples.decisions}><Prose>31 satisfies both “at least 30” and “at least 20”, but this chain chooses the first true branch only, so the label is hot. The final print is outside the indented blocks and runs after the chosen branch. An if tests at that moment; it does not keep watching a variable for later changes.</Prose></PythonExample>
    <H3>Visit each reading once</H3>
    <Prose>A for loop takes successive items from an <strong>iterable</strong>—something that supplies items one after another, such as a list. Each pass through its body is an <strong>iteration</strong>. The loop binds value to one item, runs its indented body, then asks for the next. A loop body can contain decisions of its own.</Prose>
    <Prose>Keep readings of at least 20 °C from [18, None, 25, 31, 0]. None marks missing data; zero is an actual measurement. Continue skips the rest of just this iteration. Break would end the entire loop. We need continue so later readings still get a chance.</Prose>
    <PythonFlowLab />
    <PythonExample example={pythonFoundationsExamples.filter}><Prose>25 and 31 reach selected. None is skipped before numeric comparison; 18 and 0 are below the threshold. The original list stays intact because we append to a separate result. Each iteration either moves to another input or finishes when input is exhausted.</Prose></PythonExample>
    <Checkpoint prompt="Replace continue with break in this example. What ends up in selected with the default threshold of 20?"><Prose>The result is empty: 18 is rejected, then None ends the whole loop before it can visit 25 or 31. Break is useful for ending a search after finding an answer; it is wrong when only one record should be skipped.</Prose></Checkpoint>
    <H3>Carry a result across iterations</H3>
    <Prose>An <strong>accumulator</strong> is a running result. Start total at zero, add each measurement and count how many have contributed. After any prefix of the input, total should equal that prefix's sum and count its length. This gives you a check on the loop's state before you reach the final answer.</Prose>
    <PythonExample example={pythonFoundationsExamples.mean}><Prose>The intermediate (total, count) pairs are (18, 1), (39, 2) and (63, 3). Dividing after the loop gives 21.0 °C. An empty list needs an explicit decision before division; our final function handles that case.</Prose></PythonExample>
    <Prose><Code>None</Code>, zero and empty collections are false in a condition; nonempty text such as <Code>"0"</Code> is true. <Code>and</Code> stops at a false operand, <Code>or</Code> at a true operand; both return an operand rather than necessarily a boolean. This makes <Code>value or fallback</Code> unsuitable when zero is a valid value. Use an explicit missing-value check instead. <Code>not</Code> negates truth.</Prose>
    <details className="pyf-deeper"><summary>Deeper: ranges, while loops, comprehensions and paired inputs</summary>
    <PythonExample example={pythonCoreExamples.basicsBranches}><Prose>Enumerate provides a counter and an item; start=1 changes its display counter, not list indexing. Range excludes stop, so range(1, 6, 2) supplies 1, 3 and 5. The loop unpacks each position/value pair into two names.</Prose></PythonExample>
    <PythonExample example={pythonCoreExamples.basicsLoops}><Prose>The while condition is checked before every iteration. Decrementing <Code>remaining</Code> moves toward termination; <Code>break</Code> exits early after attempt 2. A list comprehension reads “produce this expression for each value, optionally keeping values that satisfy this condition.” The filter comes after the loop clause. Understand the explicit loop before using its shorter form.</Prose></PythonExample>
    <Prose><Code>zip</Code> pairs corresponding items and, by default, stops when the shortest input ends. Use <Code>zip(..., strict=True)</Code> when unequal lengths should be an error. Prefer direct iteration over manually advancing indices. Avoid changing a collection's size while iterating it; build a filtered collection instead. Loop <Code>else</Code>, when used, runs only if the loop finishes without <Code>break</Code>; it is not the counterpart of an inner if.</Prose>

    </details>
    <H2>5. Functions with clear contracts</H2>
    <Prose><Code>def</Code> defines a function without running its body. Calling it supplies arguments, creates a local execution frame, runs the body and returns control to the caller. Parameters are names in the definition; arguments are the values supplied at the call. A function's contract says what inputs are accepted, what it returns and what happens for invalid input.</Prose>
    <Prose>Before returning to the report, package a tiny familiar calculation. Celsius converts to Fahrenheit by multiplying by 9/5 and adding 32: 20 °C becomes 68 °F. The numbers specify our example's unit conversion; they are not a Python rule. A local call frame holds the parameter and other names for one invocation while the caller waits.</Prose>
    <PythonExample example={pythonFoundationsExamples.functions}><Prose>Defining convert does not convert a temperature. Calling convert(20) binds celsius to 20, computes fahrenheit and returns 68.0. Only after the call finishes does the caller assign result. It can then calculate result + 1 because it received a reusable number.</Prose></PythonExample>
    <PythonCallLab />
    <PythonExample example={pythonFoundationsExamples.printReturn}><Prose>The first output line comes from inside the function. Reaching its end without return gives None to the caller, which prints that value on the second line. Assigning a call does not capture the text it displayed.</Prose></PythonExample>
    <Prose><Code>return</Code> sends a value to the caller and ends this invocation. <Code>print</Code> displays text and itself returns <Code>None</Code>. Reaching the end of a function without a return also returns None. Keep calculation separate from printing when another function needs to reuse the result.</Prose>
    <Checkpoint prompt="You need a returned number for another calculation, but your function only prints it. What should change, and where should display happen?"><Prose>Return the computed number from the function, then print it at the call site that actually needs display. The function can now also supply a chart, comparison or file writer without changing its calculation.</Prose></Checkpoint>
    <Prose>For our report, the contract will accept a list of strings, skip whitespace-only entries, require finite numbers and reject an empty result. It will return count and mean in Celsius and leave the original input unchanged. Stating those decisions first gives us useful cases to check.</Prose>
    <details className="pyf-deeper"><summary>Deeper: local names, mutable inputs, defaults and signatures</summary>
    <PythonExample example={pythonFoundationsExamples.mutateParameter}><Prose>The parameter is another name referring to the argument object. Append therefore changes the caller's list. Rebinding the local name to [0] does not move the caller's readings reference; return sends that new list to result. This is the first explorer's mechanism across a function boundary.</Prose></PythonExample>
    <PythonExample example={pythonCoreExamples.basicsFunctions}><Prose><Code>values</Code> is a required parameter. <Code>digits=1</Code> is a default; the standalone star makes <Code>digits</Code> keyword-only. <Code>mean(data, 2)</Code> is therefore not a valid call. The docstring describes the contract. Type hints communicate intent to people and checking tools; Python does not automatically enforce them. This example requires a nonempty list of suitable numbers.</Prose></PythonExample>
    <Prose>Python normally looks up names in local, enclosing-function, module-global, then built-in scopes. Assignment inside a function normally creates a local name. But a local parameter can refer to a caller's mutable object: <Code>readings.append(...)</Code> changes that object, while <Code>readings = []</Code> merely rebinds the local name. Prefer explicit inputs and returned results to changing globals.</Prose>
    <PythonExample example={pythonCoreExamples.basicsDefaults}><Prose>Default expressions are evaluated when the function is defined, not each time it is called. A default of <Code>readings=[]</Code> would share a list across calls. Using None and creating the list in the body gives independent defaults; passing <Code>shared</Code> deliberately shares it. <Code>*values</Code> collects extra positional arguments into a tuple; <Code>**options</Code> collects extra keyword arguments into a dictionary. Explicit parameters are clearer when the options are known.</Prose></PythonExample>
    <Prose>Functions are also values: they can be passed as arguments. For example, <Code>sorted(words, key=len)</Code> orders words by length. A <Code>lambda</Code> is a small anonymous function containing one expression; use a named def when the logic needs explanation, statements or reuse. Recursion and specialised function features can wait until the ordinary input/return model feels natural.</Prose>

    </details>
    <H2>6. Errors are information</H2>
    <Prose>An exception reports that an operation could not complete normally. Converting "twenty" to a float fails; silently calling it zero would invent a valid-looking measurement. Handle the failure where you can make a useful decision.</Prose>
    <PythonExample example={pythonFoundationsExamples.parse}><Prose>When conversion raises ValueError, Python skips to the matching except block. Else runs only if the try block completes normally. The final print runs after either handled outcome. Change raw to "0" to see a successful zero-valued result. Do not convert unexpected failures into success just to make the program finish.</Prose></PythonExample>
    <LessonTable caption="Read the last traceback line, then find the relevant source line" headers={["Error", "Typical cause", "Useful response"]} rows={[
      ["SyntaxError / IndentationError", "The code cannot be parsed", "Check colons, brackets and consistent indentation before data."],
      ["NameError", "A name has not been defined in this scope", "Check spelling and whether its assignment executed."],
      ["TypeError", "An operation received an unsuitable type", "Inspect the inputs rather than blindly converting everything."],
      ["ValueError", "The type is accepted, but the value is not", "Validate or report the invalid value."],
      ["KeyError / IndexError", "A requested key or position is absent", "Check the collection and decide how absence should be handled."],
    ]} />
    <Prose>A traceback is the path of calls that led to an exception. Start with its last line, then inspect the indicated line in your own code. <Code>try</Code> surrounds an operation that might fail; a matching <Code>except</Code> handles that exception. <Code>raise</Code> reports failure to the caller. Catch the exceptions you can actually handle, not every possible problem. Never silently turn malformed data into a valid-looking measurement.</Prose>
    <details className="pyf-deeper"><summary>Deeper: resource cleanup and development checks</summary><Prose>For resources such as files, <Code>with</Code> arranges cleanup even if an exception occurs; the iterator lesson uses this with a file-like object. <Code>assert</Code> is useful for development checks, but not for validating external input: optimised Python execution can remove assertions. Use explicit contract checks and meaningful errors where input may be invalid.</Prose></details>

    <H2>7. Build a two-file program</H2>
    <Prose>Put reusable parsing and summarising code in <Code>readings.py</Code>, and the command-line entry point in <Code>report.py</Code>. A module is an importable unit; a normal source module is a .py file, though Python also has built-in and extension modules. A package groups modules under a namespace. We only need two files in one folder here.</Prose>
    <Prose>From math import isfinite makes a standard-library function available. It rejects NaN and infinities: these can be parsed as floats but are not ordinary finite temperatures. A valid measurement can be zero; a blank is detected from its cleaned text before conversion. In the messages, !r applies repr to make the offending text visible.</Prose>
    <PythonModuleDiagram />
    <PythonExample example={pythonFoundationsExamples.project}><Prose>Save both files exactly as labeled in one folder and run report.py. Data passes through strip whitespace → skip blanks → parse and check finite numbers → return a summary → format a sentence. Three readings survive, totaling 63, so the mean is 21.0 °C. The count shows how much data supports that average. Parse_readings builds a new list; main decides how to display the result and demonstrates three failures.</Prose></PythonExample>
    <Prose>Raise ... from None suppresses the chained conversion traceback in favor of our clearer invalid-reading message; it does not mean “return None”. The empty-result check produces a useful error before division by zero. A function that returns a dictionary keeps count and mean reusable by another program.</Prose>
    <Prose><Code>from readings import summarize</Code> makes the function available by name. Importing executes a module's top-level statements on its first normal import in a process, then uses the cached module for later imports. Avoid network calls and other surprising work at import time. Do not name your files <Code>math.py</Code> or <Code>statistics.py</Code> if that would shadow a library you intend to import.</Prose>
    <Prose>When a file runs directly, its <Code>__name__</Code> is <Code>"__main__"</Code>. The guard calls main only in that case; importing report does not print a report. A plain <Code>import readings</Code> would instead require <Code>readings.summarize(...)</Code>. Avoid wildcard imports because the origin of names becomes unclear.</Prose>
    <Prose>Save this third file beside the two above and run it. Predict whether importing report prints its report before you check.</Prose>
    <PythonExample example={pythonFoundationsExamples.moduleCheck}><Prose>The guard prevents main from running during import. The new caller can reuse summarize with its own inputs; zero is retained, count is 2 and mean is 12.0. Import supplies the definition, calling it computes a value, and the caller chooses display.</Prose></PythonExample>
    <details className="pyf-deeper"><summary>Boundary of this small report</summary><Prose>The contract is a list of strings containing modest Celsius measurements. Parsed values must be finite, but extremely large finite inputs can still overflow during summation. Finite does not establish physical plausibility or sensor accuracy. Production ingestion needs unit, range, calibration, timestamp and numerical policies. This lesson does not claim to implement them. File formats, array dtype, numerical stability, dependency environments and automated tests get dedicated later topics.</Prose></details>

    <H2>8. Practise and check</H2>
    <section className="pyf-mission" aria-label="Temperature report investigation"><h3>Change the report before relying on it</h3><p>Keep readings.py unchanged. Build a new caller for <Code>["-6", "0", " ", "12"]</Code>. First predict the cleaned numeric list, count and mean. Then try blanks only. Explain why reporting zero as the mean of no readings would be misleading.</p><ol><li>Retain zero, skip whitespace and leave the input list unchanged.</li><li>Try "bad", "nan" and "inf" separately. Distinguish invalid text from parseable but nonfinite numbers.</li><li>Explain which function validates, which returns a result and which code prints it.</li></ol><details><summary>Hint: use the same stages with different data</summary><p>Only the blank disappears. Add −6, 0 and 12, then divide by three. No readings means no evidence for a mean, not evidence that the mean is zero.</p></details><details><summary>Complete changed-input caller and reasoning</summary><PythonExample example={pythonFoundationsExamples.projectChange} /><p>The retained list is [−6.0, 0.0, 12.0], totaling 6.0, count 3 and mean 2.0. The parser builds a fresh list and never assigns to the original input's elements. Blanks-only input raises no readings. If you wrongly skipped numeric zero after conversion, count would become 2 and mean 3.0; the missing-value policy would have changed the scientific result.</p></details></section>
    <section className="pyf-mission" aria-label="Independent Python application"><h3>Now solve a different problem</h3><p>Implement the winner-selection task below before opening its solution. Your function must return a name, preserve the input, reject an empty mapping and handle negative scores. After the supplied cases, try <Code>{'{"slow": -4, "steady": -1, "risky": -3}'}</Code>; steady must win. Explain why the stored candidate is always the best among entries already visited.</p><details><summary>Hint: carry a matching name and score</summary><p>Items() provides name/score pairs. Start without a winner, then update both stored values together. Use a strict improvement to preserve the first tie winner. Starting the best score at zero would incorrectly reject all-negative candidates.</p></details>
    <Checkpoint prompt="Write best_model(scores): return the name with the highest score. Reject an empty mapping, allow negative scores, and choose the first inserted name on a tie. Assume nonempty names and finite numeric scores.">
      <PythonExample example={pythonCoreExamples.basicsPractice} />
      <Prose>A strict greater-than comparison preserves the first tie winner. Starting with negative infinity avoids incorrectly assuming all scores are positive. The explicit loop exposes each decision; a concise max-based implementation can come after the logic is understood.</Prose>
    </Checkpoint>
    <details><summary>Transfer: keep the last inserted candidate on a tie</summary><p>Change the policy before changing the code. Which comparison must change? Test the negative tied scores and predict the new winner.</p><details><summary>Check the reasoning</summary><p>Use greater-than-or-equal when deciding to replace the stored pair. Equal scores now overwrite the earlier candidate, so second wins in the tied example. Empty-input handling and the returned type remain the same; the tie policy changes deliberately.</p></details></details></section>
    <Checkpoint prompt="Change the report input to blank strings only. Then try ['0', '24']. Predict both outcomes before running it.">
      <Prose>The first raises ValueError with the message “no readings”. The second returns count 2 and mean 12.0. Zero must not be discarded: we test whether the stripped text is empty, not whether its numeric value is true.</Prose>
    </Checkpoint>
    <Checkpoint prompt="A friend writes readings = readings.append(30). Why is the next len(readings) call broken?">
      <Prose>append mutates the original list and returns None. The assignment replaces the name's reference with None, so len receives an unsuitable object. Write the append statement without assignment.</Prose>
    </Checkpoint>
    <Checkpoint prompt="Explain the difference between changing a list, reassigning a name, returning a value and printing a value.">
      <Prose>Mutation changes an existing object seen by all its aliases. Rebinding changes which object one name refers to. Return passes a result to a caller; print sends a textual representation to the output stream. These are four different effects.</Prose>
    </Checkpoint>
    <H3>Ready for the next topic?</H3>
    <Prose>You are ready when you can run the two-file example from a clean start, explain the output without guessing, change its inputs safely, and describe each function's contract. Explain why a name can move without editing its former object, why zero survives missing-value filtering, and why a printed number is not automatically a call result. Revisit the relevant investigation if one answer is still uncertain.</Prose>
    <Prose>Next in this module, <a href="/learn/topic/object-oriented-programming-in-python">Object-Oriented Programming in Python</a> connects the state stored in objects with the operations that maintain it. Iterators, decorators and testing then develop your Python toolkit before NumPy introduces numerical arrays. Keep the same habit: identify inputs, trace the transformation, predict the result and test a changed case. On a focused path, use the reader's named Next link for its selected module topics.</Prose>
    <Sources>
      <li><a href="https://docs.python.org/3/tutorial/introduction.html">Python tutorial: numbers and text</a></li>
      <li><a href="https://docs.python.org/3/tutorial/datastructures.html">Python tutorial: collections and common operations</a></li>
      <li><a href="https://docs.python.org/3/tutorial/controlflow.html">Python tutorial: control flow and function parameters</a></li>
      <li><a href="https://docs.python.org/3/tutorial/errors.html">Python tutorial: errors and exceptions</a></li>
      <li><a href="https://docs.python.org/3/tutorial/modules.html">Python tutorial: modules and packages</a></li>
      <li><a href="https://docs.python.org/3/library/stdtypes.html">Python library: truth values, strings and built-in types</a></li>
      <li><a href="https://docs.python.org/3/reference/executionmodel.html#naming-and-binding">Python language reference: names and binding</a></li>
      <li><a href="https://docs.python.org/3/tutorial/floatingpoint.html">Python tutorial: floating-point limits</a></li>
      <li><a href="https://docs.python.org/3/library/math.html#math.isfinite">Python library: finite-number validation</a></li>
    </Sources>
  </div>,
};
