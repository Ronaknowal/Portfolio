import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { AmplificationFigure, ProbabilityTreeFigure, ReplayFigure, ReservoirLab, SampleBudgetLab, SelectionLab, ShuffleLab, SubsetCounterexampleFigure, UniformChoiceLab, VerificationLab, WeightedTicketsFigure } from '../../components/lesson-labs/RandomizedAlgorithmLabs.jsx';
import { randomizedAlgorithmExamples } from '../randomized-algorithm-examples.js';
import randomizedPractice from '../practice/randomized-algorithms-sampling-error-guarantees.js';
function Exercise({
  title,
  children,
  hint,
  solution
}) {
  return <section className="random-exercise"><H3>{title}</H3>{children}<details><summary>Optional hint</summary><Prose>{hint}</Prose></details><details><summary>Solution and reasoning</summary>{solution}</details></section>;
}
export default {
  title: 'Randomized Algorithms, Sampling & Error Guarantees',
  readTime: '~65 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot randomized-algorithms-lesson">
    <LessonIntro prerequisites="Use lists, loops, functions, zero-based indices and elementary fractions. Complexity introduced cost models; Hashing separated expected cost from amortized cost; String Matching distinguished fingerprints from identity. We introduce the probability and matrix operations needed here, so an unfinished probability prerequisite does not leave a gap." sections={[['1-name-what-randomness-is-allowed-to-change', 'Answers, work and probability'], ['2-build-an-unbiased-choice', 'Integer and weighted sampling'], ['3-shuffle-without-favoring-a-permutation', 'Fisher–Yates'], ['4-keep-a-fair-sample-from-a-growing-stream', 'Reservoir sampling'], ['5-find-an-exact-answer-with-random-work', 'Quickselect and tail guarantees'], ['6-verify-a-large-claim-with-a-small-random-probe', 'One-sided verification'], ['7-spend-an-error-budget-deliberately', 'Amplification and estimation'], ['8-replay-an-experiment-without-changing-its-claim', 'Reproducibility and adversaries'], ['9-reconstruct-the-guarantee-yourself', 'Independent practice'], ['guided-dsa-practice', 'Guided LeetCode practice']]}>A stream contains more records than you can keep. A selection routine sometimes takes many partitions. A computed matrix product needs a cheaper check. Randomness helps with all three, but it makes a different promise each time. Learn to design the random choices and explain precisely what can still go wrong.</LessonIntro>

    <H2>1. Name what randomness is allowed to change</H2>
    <Prose>A deterministic algorithm makes the same decisions when its input and starting state are the same. A <strong>randomized algorithm</strong> also consults random choices. To analyze it, first fix the input; then imagine running the algorithm over all possible random choices. We are averaging over its choices, not quietly assuming that real input data is random.</Prose>
    <LessonTable caption="Three different contracts for randomness" headers={['Task', 'What may vary?', 'What must hold?']} rows={[['Choose one of six records', 'The returned record', 'Every occurrence has probability1/6. There is no single preferred correct record.'], ['Find the median', 'Which pivots are used and how much work is done', 'The returned value has the requested sorted rank on every execution.'], ['Check a claimed product', 'Whether a randomly chosen probe detects an error', 'A wrong claim slips through with at most a declared probability; a detected mismatch is conclusive.']]} />
    <Prose>An exact-answer randomized method whose work may vary is commonly called <strong>Las Vegas</strong>. A method with a bounded resource budget that permits a bounded probability of a wrong answer is called <strong>Monte Carlo</strong>. Sampling has its own distributional output contract: returning A rather than B is not an error if both are eligible. An approximate estimate also needs both an accuracy tolerance and a probability of exceeding it; “randomized” alone supplies neither.</Prose>
    <H3>The probability bridge</H3>
    <Prose>An <strong>outcome</strong> is one result of a random experiment; an <strong>event</strong> is a collection of outcomes. For a finite uniform experiment, an event's probability is its number of outcomes divided by the total. “Uniform” means equally likely; it is an assumption to justify, not something inferred from calling a function named random.</Prose>
    <ProbabilityTreeFigure />
    <Prose>For two events E and F, the chance that both occur is P(E)×P(F given E). The second factor asks about F after restricting attention to outcomes in E. Events are <strong>independent</strong> when that restriction does not change F's probability, so the product becomes P(E)P(F). Reusing one fair bit twice produces only 00 or 11, each with probability 1/2. Both displayed bits are individually fair, but they are not independent.</Prose>
    <Prose>A <strong>random variable</strong> assigns a number to each outcome: number of records retained, comparisons performed, or 1 for failure and 0 for success. Its <strong>expectation</strong> is the probability-weighted average. If work is 1 with probability 3/4 and 5 with probability 1/4, expected work is 2, although no execution takes exactly 2. An event's 0/1 indicator has expectation equal to its probability. Expectations add even for dependent variables; multiplying probabilities requires the appropriate conditional or independence argument.</Prose>
    <Checkpoint prompt="A randomized median routine always returns the correct median, but one run is very slow. Does that refute an expected-linear claim? Does one successful matrix check prove an error probability of zero?"><Prose>No to both. An expectation is a weighted average, and a finite observation does not establish the full distribution. Test executions can expose bugs; a guarantee needs a model and a correctness/probability argument. Later we distinguish a theorem, an exact small enumeration and a sampled frequency.</Prose></Checkpoint>

    <H2>2. Build an unbiased choice</H2>
    <Prose>Suppose three records need equal chances, and the random source gives one of eight equally likely integers 0 through 7. Returning <Code>raw % 3</Code> looks plausible. But outputs 0 and 1 each have three preimages, while output 2 has two. Their probabilities are 3/8,3/8 and 2/8. Remainder arithmetic cannot make eight indivisible, equally weighted outcomes divide equally into three bins.</Prose>
    <UniformChoiceLab />
    <Prose><strong>Rejection sampling</strong> discards raw outcomes that would cause unequal bin sizes, then tries again with fresh independent randomness. Keep 0 through 5 here; the six accepted outcomes split two per output. Each attempt accepts with probability 6/8. Conditional on acceptance, every output has probability 2/6=1/3. Retrying does not change that conditional distribution.</Prose>
    <Prose>After a failed attempt you are back at the same task. If E is the expected number of attempts, E=1+(2/8)E, so E=4/3. The chance of needing more than r attempts is(2/8)<sup>r</sup>. It finishes with probability 1 in the ideal independent model, yet has no finite worst-case attempt count. If you impose a cap, return an explicit failure or use a correct fallback. Returning 0 on exhaustion adds bias.</Prose>
    <Prose>For a uniform source 0…M−1 and target 0…b−1 with b≤M, accept raw values below <Code>M - M % b</Code>, then take the remainder. Another implementation chooses just enough random bits to cover b and rejects candidates≥b. A single possible outcome needs zero random bits. The following version explains the construction; application code can use Python's <Code>rng.randrange(b)</Code>, which already implements uniform integer range selection.</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.rejection}><Prose>These two deterministic count tables establish the small mapping exactly. The seeded draws are only a reproducible demonstration of the implementation. With b&gt;1, a bit range of size 2<sup>ceil(log₂b)</sup> accepts more than half its candidates, so the expected number of attempts is less than 2. Each attempt still needs enough random bits and integer work for that range.</Prose></RunnableExample>
    <H3>Weighted choices are unequal by design</H3>
    <Prose>A task scheduler may allocate eligible jobs in proportions 1:3:2. Uniformity over jobs would be the wrong contract. Give each job a contiguous interval of integer tickets with length equal to its weight. Draw a ticket uniformly from 0 through totalWeight−1 and find its interval. A weight w receives exactly w tickets, hence probability w/totalWeight.</Prose>
    <WeightedTicketsFigure />
    <Prose>Store cumulative interval ends, then use binary search for the first end strictly greater than the ticket. Zero weights create empty intervals and must never be selected; negative weights and a zero total are invalid. Integers avoid ambiguous floating-point endpoints and rounding tiny probabilities to zero in this construction. The example builds prefix ends in O(n) time/storage and chooses in O(log n) comparisons, under a bounded-integer cost model. Updating many weights suggests the Fenwick tree already introduced in Range Queries.</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.weighted}><Prose>The complete ticket enumeration checks the interval boundaries, including zero-weight entries. This returns independent weighted draws with replacement when the random choices are independent. Sampling a weighted subset without replacement is a different distribution and is not obtained by silently forbidding a repeated result.</Prose></RunnableExample>

    <H2>3. Shuffle without favoring a permutation</H2>
    <Prose>A shuffle must choose each arrangement of <em>labeled occurrences</em> equally. For A,B,C there are 3×2×1=6 permutations. Swapping each position with any of the three positions uses 3³=27 equally likely choice paths. Because 27 is not divisible by 6, that scheme cannot assign the same number of paths to every permutation. Several random swaps are not a proof of a uniform shuffle.</Prose>
    <Prose><strong>Fisher–Yates</strong> fixes one final position at a time. Start with the last index i. Choose j uniformly from 0 through i, swap positions i and j, then decrement i. The suffix beyond i is already fixed and never participates again. Choosing j=i is allowed: the item already there may legitimately stay.</Prose>
    <ShuffleLab />
    <Prose>For any particular target permutation, its required final item has chance 1/n of being placed last. Conditional on that choice, its required next item has chance 1/(n−1), then 1/(n−2), and so on. Multiplying gives 1/n! for every labeled permutation. The process terminates after n−1 swaps. In-place Fisher–Yates uses O(1) extra storage; our non-mutating wrapper copies the input, so its additional storage is O(n).</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.shuffle}><Prose>Enumeration finds every three-item permutation once among six complete choice paths. This is stronger than checking that each element appears equally often in each position. Repeated values do not erase occurrence identity: two equal-valued records can still be separate records. Preserve an independent original snapshot if your API offers reset; returning the same mutable list as both the original and the shuffled state breaks that promise.</Prose></RunnableExample>

    <H2>4. Keep a fair sample from a growing stream</H2>
    <Prose>An audit can retain only two complete records from a long event stream, whose length is unknown until it ends. Keeping the first two favors early records. Replacing a retained record with probability 1/2 at every arrival favors late records. The changing denominator is the key: after t records, each occurrence should have the same chance of being retained.</Prose>
    <H3>Start with a single slot</H3>
    <Prose>Keep the first record. When the t-th record arrives, replace the slot with probability 1/t: draw uniformly from 0…t−1 and replace only if the draw is 0. Assume that after t−1 records every old record has probability 1/(t−1). An old record survives the new step with probability(t−1)/t, giving <strong>[1/(t−1)]×[(t−1)/t]=1/t</strong>. The new record also enters with probability 1/t. This proves the invariant by induction, beginning at t=1.</Prose>
    <Prose>Track record A in a four-record stream: it survives with probability(1/2)(2/3)(3/4)=1/4. Record C enters with probability 1/3 and survives D with probability 3/4, also 1/4. The cancellations explain why late arrivals get a smaller initial chance: earlier arrivals already faced more opportunities to be replaced.</Prose>
    <H3>Generalize to k slots, and prove the stronger promise</H3>
    <Prose>Fill the first k slots. For the t-th subsequent arrival, draw j uniformly from 0…t−1. If j&lt;k, replace slot j; otherwise discard the incoming record. The new record enters with probability k/t. An old retained record is evicted with probability 1/t because exactly one draw points to its slot. Its inclusion probability becomes [k/(t−1)]×[(t−1)/t]=k/t.</Prose>
    <SubsetCounterexampleFigure />
    <Prose>Equal inclusion probabilities are necessary but do not prove a uniform subset. A sample without replacement means every unordered set of k distinct occurrence positions has probability <strong>1/C(t,k)</strong>, where C(t,k), “t choose k,” counts those subsets. For example, C(4,2)=6. Reservoir sampling, also called Algorithm R, satisfies this stronger invariant.</Prose>
    <ReservoirLab />
    <details><summary>Why every k-subset has the same probability</summary><Prose>Assume every k-subset of the first t−1 records has probability 1/C(t−1,k). For a particular target subset S that excludes the newcomer, the prior sample must already be S and the newcomer must be discarded. Its probability is [1/C(t−1,k)]×[(t−k)/t]=1/C(t,k).</Prose><Prose>If S includes the newcomer, its other k−1 members must have been present together with one extra old record. There are t−k possible extra records. Each possible prior set has probability 1/C(t−1,k); the extra record's slot is replaced by exactly one of t draws. Summing these mutually exclusive ways gives(t−k)/[t C(t−1,k)]=1/C(t,k). This proves the step for t&gt;k. At t=k there is only one subset, and it is retained with certainty.</Prose></details>
    <RunnableExample example={randomizedAlgorithmExamples.reservoir}><Prose>The algorithm makes one pass with O(k) retained records and O(1) per-record updates under a unit-cost draw model. If fewer than k records arrive, this API returns all of them. An empty stream returns an empty sample. Capacity 0 returns immediately without consuming the stream; negative capacity is rejected. Slots need not be sorted or shuffled uniformly: the guarantee is an unordered subset. Shuffle the final sample if its order must itself be uniform.</Prose></RunnableExample>
    <Prose>Uniformity is over <strong>occurrences</strong>, not distinct values. If a participant emits nine records and another emits one, a uniform event sample selects the first participant with probability 9/10. It is not a fair participant sample. Decide the sampling unit before choosing the algorithm. Keeping only eligible events also changes the counter: increment it only when an eligible occurrence arrives.</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.filtered}><Prose>The matching counter advances only for the target. Counting every scanned position would undersample targets that occur late. A linked-list node can replace the integer index in the same single-slot invariant. Reservoir updates maintain a fair snapshot after every prefix; asking for independent samples at different prefixes does not follow—the maintained snapshots are correlated.</Prose></RunnableExample>
    <Checkpoint prompt="At t=5 with k=2, which draws accept the new record? What is one particular old record's eviction probability, conditional on currently being retained?"><Prose>Draws 0 and 1 accept it, so acceptance is 2/5. The particular old record occupies exactly one slot, so its eviction probability is 1/5, not 2/5. Confusing whole-reservoir acceptance with one-record eviction breaks the proof.</Prose></Checkpoint>

    <H2>5. Find an exact answer with random work</H2>
    <Prose>To find a median, sorting establishes more order than the question requires. <strong>Quickselect</strong> chooses a pivot occurrence and partitions the active values into less-than, equal-to and greater-than groups. Let L and E be the first two group sizes. For a zero-based requested rank r, keep the lower group if r&lt;L; return the pivot if L≤r&lt;L+E; otherwise keep the upper group and subtract L+E from r.</Prose>
    <Prose>The maintained claim is that the original answer equals the current active group's value at the current local rank. Partition order justifies discarding the irrelevant groups. A return from the equal group has exactly the required rank value, including duplicates. Every nonterminal step removes at least the pivot's equal group, so the active size strictly decreases. <strong>Correctness and termination do not require a lucky pivot.</strong> Randomness changes the amount of work.</Prose>
    <SelectionLab />
    <RunnableExample example={randomizedAlgorithmExamples.selection}><Prose>The copy-based implementation preserves input order outside the function and handles duplicates with one equal group. It requires consistently ordered finite values; NaN or an inconsistent comparison relation violates the partition contract. The counter records one visit per active element, even though an element may undergo two comparisons. Memory is O(n) for the active lists, and the iterative loop avoids a recursive call stack. An in-place partition can reduce auxiliary allocation but has a more delicate mutation invariant.</Prose></RunnableExample>
    <H3>Why the expectation is linear</H3>
    <Prose>Fix the data first. Each active occurrence is an equally likely pivot, using fresh choices conditional on the history. Roughly half the pivot ranks lie in the middle half; one of those leaves at most roughly three quarters of the values. Waiting for such a useful pivot takes at most a constant expected number of partitions. After each useful pivot the remaining sizes shrink geometrically, so the expected total scanned work is O(n). This is an average over pivot sequences for every fixed valid input, not an average over naturally shuffled input.</Prose>
    <details><summary>A precise bound and an exact small recurrence</summary><Prose>For n distinct values and pivot rank p, a continuing side has at most max(p,n−1−p) elements. Averaging that maximum over p=0…n−1 is at most 3 n/4. Let E(n,r) be expected scanned elements, counting a singleton partition as 1. Its recurrence is n plus the average expected work of the chosen surviving side; the matching pivot contributes 0 additional work. By strong induction, if every smaller state costs at most 4 times its size, this state costs at most n+4(3 n/4)=4 n. The base n=1 satisfies the bound. Duplicates only remove more of the equal group: label equal occurrences with arbitrary distinct ranks for this bound, while the actual algorithm skips the entire equal interval.</Prose><Prose>For each p&lt;r, the surviving state is(n−p−1,r−p−1); for each p&gt;r it is(p,r). These states and probability 1/n give an exact recurrence for distinct values. The native fraction program and the investigation use the same cost definition; their implementations are checked against an independently enumerated pivot-tree oracle.</Prose></details>
    <RunnableExample example={randomizedAlgorithmExamples.expectedWork}><Prose>The nine-value median has expected scanned work 6367/315≈20.213. One pivot sequence may finish in 9; searching for the maximum while always choosing the minimum scans 9+8+⋯+1=45. More generally the worst case is Θ(n²). Exact sorting is a good result oracle, not evidence of the randomized cost theorem.</Prose></RunnableExample>
    <H3>Convert an average into an explicit deadline claim</H3>
    <Prose>For nonnegative work T and threshold a, <strong>Markov's inequality</strong> says P(T≥a)≤E[T]/a. The reason is simple: outcomes with T≥a contribute at least a times their total probability to the average. Thus E[T]≤4 n gives P(T≥8 n)≤1/2. This is a valid but often loose tail bound. “Expected linear” does not automatically mean a particular small deadline is met with 99.9% probability.</Prose>
    <Prose>Independent restarts can turn that into a work budget. Give each attempt at most 8 n scanned elements on the same immutable input; return as soon as one exact attempt finishes. If all r attempts exhaust the budget, report “budget exhausted.” Under fresh independent choices, that event has probability at most 2<sup>−r</sup>, and total scanned work is capped by 8 nr. The returned answers are still exact; the new API permits no answer. Restarting from the same seed repeats the same path. Returning a guessed rank value on exhaustion would instead introduce answer error.</Prose>
    <Callout title="Name the resources being counted">The analysis treats ordering comparisons, list appends and uniform bounded draws as constant-cost primitives. A draw among n possibilities requires random bits; Python integers may grow; copies, garbage collection and I/O have real costs. A scanned-element budget is not a wall-clock service guarantee. Likewise, expected randomized work and amortized resizing are different averages, as in the earlier Hashing lesson.</Callout>

    <H2>6. Verify a large claim with a small random probe</H2>
    <Prose>Suppose another computation claims that multiplying square integer matrices A and B produced C. Recomputing every output entry by the usual three nested loops takes Θ(n³) arithmetic operations. A cheaper randomized verifier can look for a mistake in Θ(n²) operations per test. It is checking exact equality, not computing an approximate product.</Prose>
    <Prose>A matrix is a grid of numbers. Multiplying it by a vector r forms, for each row, the sum of row entries multiplied by matching vector entries. For a 0/1 vector, this sums the selected columns. Associativity gives <strong>(AB)r=A(Br)</strong>. Compute Br first, then A times that vector, and compare it with Cr. This requires three matrix–vector products and does not form AB.</Prose>
    <Prose>Take A=[[1,2],[0,1]] and B=[[2,0],[1,3]]. The true product is [[4,6],[1,3]]. The incorrect claim [[5,5],[1,3]] passes r=[1,1] because the errors in its first row cancel. Probe [1,0] isolates the first column and exposes a difference. Probe [0,0] always passes; including it is part of the uniform binary-vector experiment.</Prose>
    <VerificationLab />
    <H3>Why one test misses a wrong product with probability at most 1/2</H3>
    <Prose>Write D=AB−C, the error matrix; this is notation for the proof, not a matrix the verifier computes. If the claim is wrong, some row of D has a nonzero coefficient d at a position j. Fix every other coordinate of r. The row's dot product has the form s+d r [j]. Its two possible values are s and s+d. Since d≠0 over the exact integers, at most one can equal 0.</Prose>
    <Prose>The remaining bit is independently 0 or 1 with equal probability, so that row cancels with probability at most 1/2, even after fixing all other bits. Averaging over those other bits preserves the bound. Passing every row is a smaller event than passing this one row, so P(Dr=0)≤1/2. If C really equals AB, every probe passes. This is <strong>one-sided error</strong>: a mismatch proves the claim false; a pass may miss an error.</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.verification}><Prose>The implementation validates equally sized square integer matrices and a positive round count. Python's integers keep the arithmetic exact, but their bit lengths affect actual running time. The theorem here is about arithmetic operations. Arbitrarily reducing the input modulo a prime changes the equality being checked: an error divisible by that prime disappears. Adding a floating-point tolerance also creates a different numerical contract needing its own error analysis.</Prose></RunnableExample>
    <Prose>For a fixed wrong claim, independently repeating the test r times and accepting only if every probe passes gives false acceptance at most 2<sup>−r</sup>. The failure event is “all tests miss.” Reusing the same random vector repeats the same evidence, so its miss probability remains the single-test probability. For the cancellation fixture, the exact probabilities are 1/2 for one test and 1/8 for three independent tests.</Prose>
    <Prose>This can cheaply detect errors in exact batch arithmetic or an untrusted proposed result before a more expensive check. It is not a cryptographic proof system. If C is chosen after revealing a fixed probe, the other computation can construct a nonzero error with Dr=0. State when the claim is fixed and which random choices remain unknown.</Prose>

    <H2>7. Spend an error budget deliberately</H2>
    <H3>A probability bound is not a posterior belief</H3>
    <Prose>“A fixed wrong product passes with probability at most 2<sup>−r</sup>” conditions on the claim being wrong and averages over new probes. It does not say “given a pass, the probability this product is wrong is 2<sup>−r</sup>.” That reverse conditional would also require assumptions about how often wrong claims arise. Keep the experiment's direction visible.</Prose>
    <Prose>If M claims must collectively have false-acceptance probability at most δ, give each a budget δ/M. The <strong>union bound</strong> says the chance of at least one failure is at most the sum of individual failure probabilities. It needs no independence between claims. For 1000 claims and overall budget 0.001,20 independent probes per claim suffice because 1000/2²⁰≈0.000954. The per-claim theorem must still apply: reusing disclosed randomness for adaptively chosen claims can invalidate those individual bounds.</Prose>
    <H3>Two-sided decisions need a different combination rule</H3>
    <Prose>Some binary tests can be wrong on either answer. Suppose each independent run returns the correct yes/no answer with probability 3/4 for the fixed input. Take an odd number of votes and return the majority. For three runs, failure means exactly two or three wrong votes. There are three ways to locate two errors; the probability is 3(1/4)²(3/4)+(1/4)³=5/32. It is not(1/4)³.</Prose>
    <AmplificationFigure />
    <Prose>For r independent identically distributed runs with error p&lt;1/2, sum C(r,j)pʲ(1−p)<sup>r−j</sup> over j&gt;r/2. If each independent error probability is merely bounded above by p, that binomial tail is still an upper bound: making any individual error more likely cannot decrease majority failure. Pairwise independence alone is not the same as mutually independent votes. Repeatedly resetting to one seed gives perfectly correlated decisions.</Prose>
    <RunnableExample example={randomizedAlgorithmExamples.amplification}><Prose>The fraction outputs distinguish all-miss, majority and any-claim failure events. The last output is an upper bound, not a measured error rate. The correct repetition count comes from the requested failure budget and the proved per-run behavior, not a habitual choice of three or ten repetitions.</Prose></RunnableExample>
    <H3>Estimate an average with an accuracy and a failure budget</H3>
    <Prose>Suppose a large fixed population contains an unknown fraction μ of marked records. Draw records uniformly with replacement and record Xᵢ=1 for marked,0 otherwise. Independent draws give independent indicators with mean μ. Their average estimates μ. More samples reduce sampling variation; they do not correct a biased selection procedure or an incorrectly chosen population.</Prose>
    <Prose>A useful concentration theorem, <strong>Hoeffding's inequality</strong>, says that for s independent observations in [0,1] with common mean μ, the chance that their average differs from μ by at least ε is at most 2 exp(−2 sε²). The bound is meaningful only up to 1. Here ε is an <em>absolute</em> difference, such as 0.05 meaning five percentage points. δ is a permitted failure probability, not an error size.</Prose>
    <div className="random-formula">A sufficient sample count is s = ceil( ln(2/δ) / (2ε²) ).</div>
    <Prose>This follows by requiring 2 exp(−2 sε²)≤δ, taking natural logarithms and solving for s. With ε=0.1 and δ=0.05,185 samples suffice. Halving ε increases the unrounded requirement fourfold, to 738 after rounding. Making δ smaller increases the requirement logarithmically. The theorem gives a sufficient bound; it need not be tight for a particular population.</Prose>
    <SampleBudgetLab />
    <RunnableExample example={randomizedAlgorithmExamples.estimation}><Prose>The demonstration population has known marked fraction 0.3 so its estimate can be checked, but one run cannot validate a 95% guarantee. The count was chosen before sampling. Repeatedly inspecting the estimate and stopping when it looks favorable changes the procedure; this fixed-count bound does not automatically certify such optional stopping. A k-reservoir sample is without replacement and internally dependent, so do not cite the independent-draw theorem for it without a separate argument.</Prose></RunnableExample>
    <Prose>The full exponential-moment derivation and sharper bounds belong to <a href="/learn/path/full-curriculum/concentration-inequalities-hoeffding-bernstein-chernoff">Concentration Inequalities</a>. Correlated Markov-chain sampling belongs to <a href="/learn/path/full-curriculum/monte-carlo-methods-mcmc-metropolis-hastings-hmc-nuts">Monte Carlo Methods & MCMC</a>. The present finish line is to state the assumptions and derive a usable budget, not to pretend every random observation is independent.</Prose>

    <H2>8. Replay an experiment without changing its claim</H2>
    <Prose>The proofs use ideal uniform independent choices. A <strong>pseudo-random generator</strong> instead evolves deterministic internal state. A seed initializes that state. A good generator is useful for simulations and reproducible tests, but a fixed seed is one deterministic execution, not evidence for all seeds or all inputs. Use a local generator object so unrelated code does not accidentally consume the next draw.</Prose>
    <Prose>A finite-state generator can follow only finitely many distinct streams from its possible starting states. Meanwhile the number of labeled permutations grows as n!. For sufficiently long lists, a fixed finite-state generator cannot reach every permutation promised by the ideal independent-draw model. Python’s shuffle documentation calls out this limit. The Fisher–Yates argument proves the transformation under its random-source assumption; it does not prove that every practical generator realizes that assumption for arbitrary input sizes.</Prose>
    <ReplayFigure />
    <RunnableExample example={randomizedAlgorithmExamples.replay}><Prose>Saving and restoring generator state resumes the stream at a checkpoint. Constructing Random(17) for every call repeats the first draw and destroys the intended independent-trial model. Record the algorithm, input order, seed or checkpoint, generator/runtime version and call order. Concurrent tasks sharing a generator can consume draws in a different order even if its initial seed is unchanged. The shown outputs were checked with Python 3.12; arbitrary library methods are not promised to retain every sequence across versions.</Prose></RunnableExample>
    <Prose>For security-sensitive unpredictability, use the platform's cryptographic randomness facilities such as Python <Code>secrets</Code>, following that API's contract. Python <Code>random</Code> is for modeling and simulation, not secret tokens. Even an unpredictable draw does not repair an algorithm that applies a biased modulo mapping or a proof with the wrong adversary assumptions.</Prose>
    <H3>Connect the fingerprint lesson to the general guarantee</H3>
    <Prose>String Matching fixed two unequal strings, then chose a random polynomial base from a declared finite set. Its collision bound applied to that experiment. Independent new bases can reduce joint collision risk; a union bound handles many candidate windows without demanding that those windows be independent. Selecting the input after observing the base is a different experiment.</Prose>
    <Prose>With direct verification, a fingerprint collision causes more work rather than a false match. Without verification, it can cause a wrong answer. Verification also has a cost on true hits: an all-match detector that compares m symbols for each of z real occurrences can spend zm work even without a single false collision. Separating the answer contract, the random experiment and the resource model prevents a plausible slogan like “randomized means fast” from hiding those cases.</Prose>
    <LessonTable caption="Before relying on a randomized result" headers={['Question', 'Concrete answer to record']} rows={[['What is fixed before randomness?', 'Input population, claimed product, target rank or query; whether a later adversary sees probes.'], ['What is drawn, from where?', 'Uniform integer indices, independent binary coordinates, or a documented weighted distribution.'], ['What is failure?', 'Wrong answer, estimate beyond ε, no answer before a cap, or a sampling distribution that is not uniform.'], ['What is guaranteed?', 'Exactness, expected work, an explicit tail/error bound, and the cost model.'], ['What was actually checked?', 'Invariant/proof, independent small enumeration, native implementation, and repeated experiments with recorded state.']]} />

    <H2>9. Reconstruct the guarantee yourself</H2>
    <Prose>Close the worked code. Begin with an exact small oracle, then identify the maintained invariant or event being counted. A passing statistical test can miss a bias; a single surprising sample can occur under a correct distribution. Your explanation should distinguish both from a deterministic implementation bug.</Prose>
    <Exercise title="A. Repair a biased die" hint="Count the raw tickets assigned to every output. Reject a final incomplete block before taking a remainder." solution={<Prose>For raw 0…15 modulo 6, outputs 0…3 have three tickets and 4…5 have two. Accept 0…11, then use modulo 6; each output has two of 12 accepted tickets. Acceptance is 3/4, expected attempts 4/3, and more than r attempts has probability(1/4)ʳ under fresh independent draws. A cap followed by returning 0 would favor 0. With a cap of 3, returning a distinct failure has probability 1/64 and preserves conditional uniformity among successful results.</Prose>}><Prose>Build a fair six-sided result from four independent fair bits per attempt. Give exact probabilities, expected attempts and the contract of a three-attempt cap. Write an exhaustive raw-state test without random sampling.</Prose></Exercise>
    <Exercise title="B. Show why shuffled positions are not enough" hint="Examine all rotations of ABC and compare their occurrence marginals with the six permutations." solution={<Prose>Choose ABC, BCA or CAB with probability 1/3 each. Every occurrence visits each position with probability 1/3, but ACB, BAC and CBA never occur. Thus uniform position marginals do not establish uniform permutations. Fisher–Yates gives each target arrangement one path with probability 1/(3×2). A correct test can enumerate all six choice paths and compare complete outcomes.</Prose>}><Prose>Construct a three-item shuffler that places every item equally often in every position but fails to produce a uniform permutation. Explain the stronger property to test.</Prose></Exercise>
    <Exercise title="C. Change the sampling unit" hint="Track identities and counts separately. In a filtered reservoir, only eligible records increase the denominator." solution={<Prose>For values [A,A,B], an occurrence sample returns value A with probability 2/3, and B with 1/3. Sampling distinct labels uniformly requires deduplication or a different distinct-item data structure; the occurrence reservoir does not supply that. For k=2 from five labeled records, every pair must have probability 1/C(5,2)=1/10, and each record appears in four pairs, giving 2/5. Enumerate complete draw paths for t=3,4,5, assigning each path mass 1/(3×4×5); sort identities only when aggregating unordered subsets.</Prose>}><Prose>An event stream contains repeated participant IDs. Explain why uniform events differ from uniform participants. Independently enumerate a two-slot reservoir on five distinct records and check joint subset probabilities, not only marginal counts.</Prose></Exercise>
    <Exercise title="D. Audit the guarantee of a capped selector" hint="The Markov threshold uses the expectation bound, and a timeout is not an approximate rank answer." solution={<Prose>If E[T]≤4 n, P(T≥40 n)≤1/10. Four independent capped attempts have all-timeout probability at most 10⁻⁴ and total scanned work at most 160 n, allowing the cap to stop before it would be exceeded. A completed attempt returns an exact rank value. Returning no result is a separate failure status. Resetting to the same seed cannot justify the fourth power. Accounting must include any copying/draw costs if the claim changes from scanned elements to total operations.</Prose>}><Prose>Use the stated expected scanned-work bound to design four attempts with all-timeout probability at most 0.0001. State the result API, total scanned budget and assumptions.</Prose></Exercise>
    <Exercise title="E. Break a deterministic verifier" hint="Choose a nonzero row whose two entries sum to zero. Then change the probe to select only one column." solution={<Prose>Let D=[[2,−2],[0,0]]. Probe [1,1] gives zero and misses the error. Independent uniform binary probes [0,0] and [1,1] miss;[0,1] and [1,0] detect it. A single-test miss probability is 1/2; five independent tests all miss with probability 1/32. Reusing one vector five times remains 1/2. Reducing this D modulo 2 erases every entry, so it cannot verify exact integer equality under that reduction.</Prose>}><Prose>Construct an incorrect two-by-two product claim that always passes the fixed probe [1,1]. Enumerate all binary probes, compare five independent tests with one reused test, then explain the danger of reducing every entry modulo 2.</Prose></Exercise>
    <Exercise title="F. Budget estimation instead of guessing a trial count" hint="Use natural logarithms in the two-sided bounded-mean inequality. Keep percentage points separate from relative error." solution={<Prose>With ε=0.05 and δ=0.01, ceil(ln 200/0.005)=1060 independent samples suffice. The tolerance is five percentage points, not 5% of the unknown fraction. One repeated record copied 1060 times supplies one observation, not 1060 independent observations. If the true fraction is tiny, an additive 0.05 guarantee can be weak; a relative-error request needs a different theorem and assumptions rather than a relabeled ε.</Prose>}><Prose>Estimate a marked fraction within 0.05 with failure probability at most 0.01. Calculate the sufficient count, explain whether repeated copies of one sampled record qualify, and distinguish absolute from relative precision.</Prose></Exercise>
    <Exercise title="G. Combine the right event" hint="List which outcomes make the final rule wrong before applying independence or the union bound." solution={<Prose>With independent per-run error 1/3, a three-vote binary majority fails with 3(1/3)²(2/3)+(1/3)³=7/27, not 1/27. For 200 fixed wrong-product checks with a total false-acceptance budget 0.01, choose r so 200/2ʳ≤0.01: r=15, since 200/32768≈0.006104 whereas 14 gives≈0.012207. Independence is needed for repetitions inside a check under this argument; the union step across checks does not require it. Fresh hidden probes preserve the per-claim theorem for a claim fixed before its own probe choices.</Prose>}><Prose>A two-sided binary test errs with probability 1/3. Find the error of three independent majority votes. Separately budget product verification for 200 claims with overall false-acceptance probability at most 0.01, stating where independence enters.</Prose></Exercise>
    <Prose>You are ready to continue when you can reconstruct an unbiased draw or reservoir update, explain exactness separately from expected work, and write the failure event and random experiment before computing a probability. Mix a new sampling question with an earlier hash-map or binary-search question so that selecting the method is part of the practice. No finite set of examples guarantees every future interview problem.</Prose>
    <Prose>The next topic in this module is <a href="/learn/path/full-curriculum/network-flow-minimum-cuts-bipartite-matching?module=data-structures-algorithms">Network Flow, Minimum Cuts & Bipartite Matching</a>. It introduces capacity constraints and residual paths. Random contraction is an optional later connection once global cut versus source–sink cut is clear; it is not a reason to skip the decided sequence.</Prose>
    <DsaPractice practice={randomizedPractice} />
    <Sources alternatives={<><Prose><a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/resources/lecture-6-randomization-matrix-multiply-quicksort/" target="_blank" rel="noopener noreferrer">MIT 6.046 Lecture 6: Randomization—Matrix Multiply, Quicksort</a> offers a board-based video route into random work and random verification. Use it after sections 5–6; pause to state the fixed input and failure event. The official page and relevant transcript were inspected; this review did not watch the entire video.</Prose><Prose><a href="https://people.orie.cornell.edu/vc333/orie5270/lectures/04/" target="_blank" rel="noopener noreferrer">Cornell ORIE 5270: Randomized algorithms</a> connects selection with streams. Reconcile its stream indices with the number of records seen before translating formulas; our code consistently uses the one-based count t. The lesson above supplies the full subset proof and explicit memory contracts.</Prose></>}>
      <li><a href="https://docs.python.org/3/library/random.html" target="_blank" rel="noopener noreferrer">Python random documentation</a> — uniform integer APIs, shuffle/sample behavior, generator state and reproducibility limits. <a href="https://docs.python.org/3/library/secrets.html" target="_blank" rel="noopener noreferrer">Python secrets</a> describes the different security purpose. Native examples were run on Python 3.12; current documentation is newer.</li>
      <li><a href="https://www.cs.umd.edu/~samir/498/vitter.pdf" target="_blank" rel="noopener noreferrer">Vitter, Random Sampling with a Reservoir (1985)</a> — section 2 describes Algorithm R and its sample invariant, attributing that algorithm to Alan Waterman. Later skipping algorithms are a deeper optimization, not what the examples implement.</li>
      <li><a href="https://www.cs.utexas.edu/~ecprice/courses/randomized/fa21/scribe/lec11.pdf" target="_blank" rel="noopener noreferrer">UT Austin: matrix verification and polynomial identity testing</a> — a proof-oriented continuation into one-sided tests and fingerprints. Its field setup must be distinguished from exact integer equality; our proof and code declare the integer contract explicitly.</li>
      <li><a href="https://cs.uwaterloo.ca/~r5olivei/courses/2025-spring-cs466/lecture-notes/lecture3/" target="_blank" rel="noopener noreferrer">Waterloo: Concentration Inequalities</a> — the independent bounded-variable theorem and deeper exponential-moment derivation. Use after the sample-budget section; correlated or adaptively stopped samples require further reasoning.</li>
    </Sources>
  </div>
};
