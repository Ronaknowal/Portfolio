import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { AvoidedCrossingLab, CenteringRankFigure, CovarianceSpectrumLab, FiniteSpectrumLab, InverseGainFigure, NoiseSampleFigure, SpikeAlignmentLab, WignerSpectrumLab } from '../../components/lesson-labs/RandomMatrixLabs.jsx';
import { randomMatrixExamples } from '../random-matrix-examples.js';
function MatrixExample({
  example
}) {
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /><Prose>{example.explanation}</Prose></>;
}
function Practice({
  question,
  hint,
  children
}) {
  return <div className="rm-practice"><Prose><strong>Try independently.</strong> {question}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Show explained solution</summary>{children}</details></div>;
}
export default {
  title: 'Random Matrix Theory',
  readTime: '~80 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot random-matrix-lesson">
    <LessonIntro prerequisites="You can multiply a matrix by its transpose, interpret an eigenvector and an SVD, and use expectation, variance and independence. Those ideas are refreshed at their point of use. Matrix Decompositions, Eigenvalues & Eigenvectors, and Probability Distributions supply the longer prerequisites." sections={[['1-noise-can-have-principal-directions', 'A concrete noise sample'], ['2-choose-the-matrix-and-count-its-directions', 'Shape, scaling and centering'], ['3-why-high-dimensional-noise-does-not-collapse', 'A finite moment calculation'], ['4-read-the-whole-marchenko-pastur-law', 'The bulk, its scale and zero atom'], ['5-a-limiting-edge-is-not-a-finite-test', 'Finite bounds and matched nulls'], ['6-separate-a-population-spike-from-a-sample-component', 'Spikes and eigenvector alignment'], ['7-control-the-amplification-of-weak-directions', 'Inversion and regularization'], ['8-symmetry-produces-a-different-spectral-law', 'Wigner matrices and level gaps'], ['9-use-a-spectrum-to-make-a-better-decision', 'Applications and research branches'], ['10-practise-on-new-shapes-and-new-assumptions', 'Independent practice and what follows']]}>Learn to answer a practical question: if this matrix were generated by a defensible noise model, what would its spectrum look like? You will calculate actual small matrices, explain why noise eigenvalues spread, separate limiting laws from finite guarantees, and decide what further evidence an apparent signal needs.</LessonIntro>
    <Prose>The eleven complete programs use Python 3; the numerical ones use NumPy and one uses SciPy. Install them in a virtual environment with <Code>python -m pip install numpy scipy</Code>, save one program as <Code>example.py</Code>, then run <Code>python example.py</Code>. The displayed outputs were executed with Python 3.12, NumPy 2.3.5 and SciPy 1.18.1. A fixed seed makes a particular implementation repeatable; random-stream details and last printed digits can vary across versions. The browser computes its own bounded JavaScript models; its samples are not the Python samples.</Prose>

    <H2>1. Noise can have principal directions</H2>
    <Prose>Imagine measuring two unrelated sensors eight times. In the population model, each sensor independently returns −1 or +1 with equal probability, and observations are independent across time. Both population variances are 1 and their population covariance is 0. We would like a data-analysis method to avoid inventing a shared cause between them.</Prose>
    <Prose>Now suppose the observed pairs are three copies of (1,1), three of (−1,−1), one (1,−1), and one (−1,1). This is <strong>a possible sample under independent population coordinates</strong>. Its observed counts favor agreement, even though the generating model gives all four pairs equal probability. Independence is a property of that model; it is not a claim that the four observed frequencies factor exactly.</Prose>
    <Prose>Place each observation in a row of X. The first diagonal entry of XᵀX/8 is the average squared first reading: all eight squares equal 1. The other diagonal entry is also 1. Its off-diagonal entry is the average product: six agreements contribute +1 and two disagreements contribute −1, giving (6 − 2)/8 = 0.5. The resulting matrix has a preferred direction.</Prose>
    <NoiseSampleFigure />
    <MathBlock>{String.raw`\begin{gathered}S=\begin{bmatrix}1&0.5\\0.5&1\end{bmatrix},\\[.35em]
      S\frac{(1,1)^\mathsf T}{\sqrt2}
      =1.5\frac{(1,1)^\mathsf T}{\sqrt2}\end{gathered}`}</MathBlock>
    <Prose>Along the unit direction (1,1)/√2, the average squared projection is 1.5; along (1,−1)/√2 it is 0.5. These are the matrix's <strong>eigenvalues</strong>; their directions are eigenvectors. A PCA procedure will correctly report the larger sample direction. It has not thereby proved that the population has a common factor.</Prose>
    <Prose><strong>Random matrix theory</strong> studies matrices generated by specified random mechanisms, often as their dimensions grow. Its advantage is a baseline for the entire collection of eigenvalues, called the <strong>spectrum</strong>, rather than an intuition that every noise matrix should look like an identity matrix. Randomness in individual entries can produce predictable aggregate geometry.</Prose>
    <Checkpoint prompt="Does the sample eigenvalue 1.5 contradict a population covariance equal to the identity?"><Prose>No. The identity describes expected products under repeated sampling. This particular finite sample overrepresents matching signs. A statistic can differ from its expectation without contradicting its generating model.</Prose></Checkpoint>

    <H2>2. Choose the matrix and count its directions</H2>
    <Prose>Our main convention is X with n observations as rows and p features as columns. Initially the population mean is known to be zero, each entry has variance σ², and entries are independent. We compute S = XᵀX/n. Each entry Sij is an average of products of features i and j. Under this model, E[S] = σ²I. Individual sample means need not be zero.</Prose>
    <MathBlock>{String.raw`\begin{gathered}S=\frac{X^\mathsf TX}{n},\\[.35em]
      q^\mathsf TSq=\frac{\|Xq\|^2}{n}\geq0\end{gathered}`}</MathBlock>
    <Prose>A unit vector q selects a weighted combination of features. Its quadratic form is the average squared projection onto that combination. The equality proves that S is <strong>positive semidefinite</strong>: every eigenvalue is nonnegative. If X = UDVᵀ is an SVD, then S = V(DᵀD/n)Vᵀ. Its eigenvalues are squared singular values divided by n, with extra zeros as needed. A tall data matrix and a square covariance matrix tell the same story in different units.</Prose>
    <Prose>There is also an exact rank constraint. X maps p input coordinates into n outputs, so at most min(n,p) input directions can survive independently. Squaring via XᵀX does not change the nullspace: Xq = 0 if and only if qᵀXᵀXq = 0. Thus, when p exceeds n, S has at least p − n zero eigenvalues before invoking any probability theorem.</Prose>
    <H3>Subtracting an estimated mean uses a direction</H3>
    <Prose>If the population mean is unknown, subtract each observed column mean. Let H = I − 11ᵀ/n, where 1 is the n-vector of ones. H removes the component of each column along the constant vector, and the centered matrix is HX. The usual unbiased sample covariance divides its cross-products by n − 1:</Prose>
    <MathBlock>{String.raw`\begin{gathered}C=\frac{X^\mathsf THX}{n-1},\\[.35em]
      \operatorname{rank}(C)\leq\min(n-1,p)\end{gathered}`}</MathBlock>
    <Prose>Every centered column sums to zero, so it lies in the (n − 1)-dimensional subspace perpendicular to 1. That explains the rank loss. For independent observations with covariance Σ, E[XᵀHX] = (n − 1)Σ: the original centered-about-population sum contributes nΣ and the sample-mean correction contributes Σ. Dividing by n instead is a legitimate alternative convention, but changes the matrix's scale by (n − 1)/n.</Prose>
    <CenteringRankFigure />
    <Prose>For Gaussian entries there is a stronger fact. Rotate the observation coordinates so one axis is 1/√n. A standard Gaussian vector retains independent standard Gaussian coordinates under an orthogonal rotation. H removes that one axis; the remaining n − 1 rows have the same Gram matrix and are independent Gaussian rows. Therefore the centered covariance has an exact Gaussian representation with effective sample size n − 1. For sign-valued or other non-Gaussian entries, the rotated rows generally are not independent. The rank statement still holds exactly; the Gaussian distributional claim does not.</Prose>
    <MatrixExample example={randomMatrixExamples.gram} />
    <Prose>The program uses <Code>eigvalsh</Code> because the covariance is real symmetric. Eigenvectors returned by <Code>eigh</Code> are columns, ordered with the ascending eigenvalues. A numerical rank uses a tolerance; the exact rank argument above does not. Repeated eigenvalues determine an eigenspace rather than a unique eigenvector, and changing an eigenvector's sign changes no variance.</Prose>
    <LessonTable caption="Record the convention before comparing a spectrum" headers={['Choice', 'What it changes', 'A useful check']} rows={[['Known mean or estimated mean', 'X versus HX; denominator n versus n−1', 'Count the possible nonzero directions.'], ['Covariance or correlation', 'Original units versus feature-by-feature scaling', 'Estimate and apply scaling in the null in the same way.'], ['Rows independent or dependent', 'The effective information and spectral law', 'Check repeated subjects, time dependence and duplicated columns.'], ['Gram orientation', 'XᵀX is p×p; XXᵀ is n×n', 'Their positive eigenvalues agree before any differing normalization.']]} />

    <H2>3. Why high dimensional noise does not collapse</H2>
    <Prose>If p is fixed and n grows, averaging makes each sample covariance entry approach its population value. But if p grows alongside n, there are also more off-diagonal estimation errors. An error small in one cell can matter when thousands of cells act together.</Prose>
    <Prose>We can make that mechanism quantitative without starting from a difficult spectral theorem. For independent standard Gaussian entries, define the second spectral moment as the average squared eigenvalue, tr(S²)/p. Because S is symmetric, tr(S²) = Σij Sij²: square every matrix entry and add.</Prose>
    <Prose>For i ≠ j, Sij averages n independent products of two independent, mean-zero, variance-one variables. Its expectation is zero and its second moment is 1/n. For a diagonal entry, Sii averages n squared standard Gaussians. A standard Gaussian Z has E[Z²] = 1 and E[Z⁴] = 3, so Var(Z²) = 2. Hence E[Sii²] = 1 + 2/n. The Gaussian fourth moment follows by integration by parts using φ′(z) = −zφ(z): E[Z⁴] = 3E[Z²].</Prose>
    <MathBlock>{String.raw`\begin{gathered}\mathbb E\!\left[\frac{\operatorname{tr}(S^2)}p\right]
      \\ =\frac{p(1+2/n)+p(p-1)/n}{p}
      \\ =1+\frac{p+1}{n}\end{gathered}`}</MathBlock>
    <Prose>The average eigenvalue has expectation 1. If all eigenvalues really crowded arbitrarily close to 1, their average squared value would also crowd close to 1. Instead, when p/n approaches a positive number γ, the expression approaches 1 + γ. A persistent spread is natural. This expectation calculation explains the scale; by itself it is not a proof of convergence of the whole distribution.</Prose>
    <Prose>For n = 100 and p = 25, the exact expected second moment is 1.26. Doubling both dimensions makes it 1.255, not nearly 1. Keeping p = 25 and increasing n to 10,000 makes it 1.0026. These are different limits. The <strong>aspect ratio</strong> γ = p/n tells us which regime we are studying.</Prose>
    <MatrixExample example={randomMatrixExamples.moments} />
    <Checkpoint prompt="If entries instead take ±1 with equal probability, which part of the Gaussian second-moment derivation changes?"><Prose>Every diagonal Sii equals 1 exactly, so its square is 1 rather than having expectation 1 + 2/n. The off-diagonal calculation remains 1/n. The exact answer is 1 + (p − 1)/n, with the same limit 1 + γ. Shared limiting behavior does not mean identical finite moments.</Prose></Checkpoint>

    <H2>4. Read the whole Marchenko–Pastur law</H2>
    <Prose>The <strong>empirical spectral distribution</strong> assigns mass 1/p to each of the p eigenvalues. Its mass in an interval is simply the fraction of eigenvalues there. Under independent identically distributed, mean-zero entries with finite variance σ², as n and p grow with p/n → γ in (0,∞), this distribution converges to the Marchenko–Pastur (MP) law. The standard iid formulation is a global limiting statement, not a statement that every finite matrix has the same histogram.</Prose>
    <Prose>The continuous part lies between a and b. For λ strictly inside that interval and above zero, its density is:</Prose>
    <MathBlock>{String.raw`\begin{gathered}a=\sigma^2(1-\sqrt\gamma)^2,\\[.35em]
      b=\sigma^2(1+\sqrt\gamma)^2\end{gathered}`}</MathBlock>
    <MathBlock>{String.raw`\begin{gathered}f(\lambda)=
      \frac{\sqrt{(b-\lambda)(\lambda-a)}}{2\pi\gamma\sigma^2\lambda},
      \\[.35em] a<\lambda<b\end{gathered}`}</MathBlock>
    <Prose>A density is probability per unit eigenvalue, not probability at one exact positive eigenvalue. Integrate it across a bin to obtain a predicted fraction. At γ = 1, a = 0 and the density diverges as λ approaches zero, but its integral is finite. Drawing an arbitrarily tall sampled point at zero would misrepresent this endpoint; the investigation integrates each bin instead.</Prose>
    <Prose>There can also be an <strong>atom</strong>, a nonzero probability mass concentrated at the single point zero. The complete law is the continuous part plus mass max(1 − 1/γ, 0) at zero. For γ greater than 1, the continuous part has mass 1/γ, not 1. This matches the exact missing-rank fraction (p − n)/p. Although a becomes positive again when γ exceeds 1, the full matrix's smallest eigenvalue is still zero.</Prose>
    <LessonTable caption="Unit-variance limiting laws at three shapes" headers={['Aspect ratio', 'Continuous support', 'Mass at zero', 'Mean eigenvalue']} rows={[['γ = 0.25', '[0.25, 2.25]', '0', '1'], ['γ = 1', '[0, 4]', '0; no atom despite divergent density', '1'], ['γ = 4', '[1, 9]', '0.75', '1 across all eigenvalues']]} />
    <Prose>At ratio 4, the positive eigenvalues have conditional mean 4, because only one quarter of all eigenvalues are positive while the overall mean remains 1. A histogram normalized over only positive eigenvalues must rescale the continuous reference by 4. Quietly dropping zeros and overlaying the unmodified density mixes two different probability measures.</Prose>
    <MatrixExample example={randomMatrixExamples.original} />
    <CovarianceSpectrumLab />
    <H3>What supports the theorem, and what it does not say</H3>
    <Prose>The moment approach expands tr(Sᵏ) into products of entries. Independence and zero means make many terms vanish. Counting the index patterns that survive at leading order produces deterministic limiting moments; the second moment we computed is the first nontrivial example. Controlling fluctuations and showing those moments identify the limiting measure are additional mathematical steps. This lesson uses the MP theorem for that global conclusion; it does not present the entry-counting sketch as a complete proof.</Prose>
    <Prose>The complete measure has first moment σ² and second moment σ⁴(1 + γ). The next program checks both by numerical integration, keeping the atom separate. Quadrature approximates integrals; it is evidence that our normalization agrees, not a proof of a random-matrix limit.</Prose>
    <MatrixExample example={randomMatrixExamples.mass} />
    <Callout label="Bulk convergence does not trap every eigenvalue">One eigenvalue has empirical mass 1/p. Even if that one value behaves unusually, its mass can vanish in the global distribution. Additional assumptions control the extreme eigenvalues. For iid entries with a finite fourth moment, the familiar edge convergence is available under standard fixed-ratio conditions; the Gaussian case satisfies them. Finite variance alone should not be silently promoted into a claim about the largest eigenvalue.</Callout>
    <Prose>Multiplying every raw entry by 3 multiplies S and its eigenvalues by 9. A unit-variance MP curve cannot be compared to those eigenvalues unchanged. Standardizing by observed column variances produces a sample correlation matrix; it introduces dependence through random denominators. Related asymptotic results exist under appropriate conditions, but the exact Gaussian Gram representation no longer follows simply by renaming that matrix.</Prose>

    <H2>5. A limiting edge is not a finite test</H2>
    <Prose>An eigenvalue of 2.3 exceeds the reference edge 2.25 when γ = 0.25. That alone does not mean “statistically significant at 5%.” The limiting edge contains no selected failure probability, and finite null maxima fluctuate around it. Three questions need separate answers: where most eigenvalues accumulate, how extremes behave as dimensions grow, and how surprising a particular finite statistic is under a specified null.</Prose>
    <H3>A finite Gaussian guarantee</H3>
    <Prose>Let X have n by p independent standard Gaussian entries, with n ≥ p. For t ≥ 0, simultaneously, with probability at least 1 − 2exp(−t²/2), the largest singular value is at most √n + √p + t and the smallest is at least √n − √p − t. A negative lower bound carries no information beyond nonnegativity.</Prose>
    <Prose>The proof mechanism combines two facts. Gaussian comparison bounds place the expected extremes no farther out than √n ± √p. Each extreme singular value is 1-Lipschitz in the Frobenius norm: perturbing a matrix by E changes an extreme by at most ‖E‖₂ ≤ ‖E‖F. Gaussian concentration gives a one-sided tail exp(−t²/2) around the relevant expectation bound. Adding the two failure probabilities gives the simultaneous event. The comparison and concentration theorems are imported results; the conversion below is direct algebra.</Prose>
    <MathBlock>{String.raw`\begin{gathered}t=\sqrt{2\log(2/\delta)},\\[.35em]
      \mathbb P(\text{both bounds hold})\geq1-\delta\end{gathered}`}</MathBlock>
    <MathBlock>{String.raw`\begin{gathered}L=\max(0,\sqrt n-\sqrt p-t)\\
      U=\sqrt n+\sqrt p+t\\[.35em]
      \lambda_{\min}(S)\geq L^2/n\\
      \lambda_{\max}(S)\leq U^2/n\end{gathered}`}</MathBlock>
    <Prose>First clamp the lower singular-value bound at zero, then square. Squaring a negative lower bound would invent a positive guarantee. These bounds can be conservative; a wide valid interval may be less useful than a carefully calibrated simulation, but it answers a precisely stated finite question.</Prose>
    <MatrixExample example={randomMatrixExamples.bound} />
    <H3>Simulate the statistic you actually plan to use</H3>
    <Prose>For a fully specified null, generate independent synthetic datasets of the same shape, repeat the actual preprocessing, and compute the same statistic T, here the largest eigenvalue. With B null datasets and an observation exchangeable with them under the null, this rank score gives a conservative tail test when ties count as exceedances:</Prose>
    <MathBlock>{String.raw`r=\frac{1+\#\{b:T_b\geq T_{\rm obs}\}}{B+1}`}</MathBlock>
    <Prose>Why the extra one? Under a continuous exchangeable null, each rank among B + 1 statistics is equally likely. The observation itself occupies one rank, so the smallest possible score is 1/(B + 1), not zero. Ties make this upper-tail rule conservative. Fitting unknown null parameters on the observed data and then simulating from that fit is generally a parametric bootstrap approximation, not the same exact rank argument.</Prose>
    <FiniteSpectrumLab />
    <MatrixExample example={randomMatrixExamples.calibration} />
    <Prose>Duplicating a column preserves its marginal variance but violates feature independence. An iid null can treat that dependence as surprising; a null that deliberately includes the duplication asks a different question. Neither is automatically the scientifically correct baseline. Independently shuffling time-correlated measurements destroys temporal structure. A useful resampling scheme must preserve the null-relevant dependence and have a justified exchangeability or approximation argument.</Prose>
    <Prose>If you inspect many matrices, preprocessing choices and spectral statistics, then report only the smallest score, the single-test guarantee does not apply to the whole search. Predeclare a statistic or repeat the complete selection procedure inside each null replicate; use independent data for the final scientific or predictive claim.</Prose>

    <H2>6. Separate a population spike from a sample component</H2>
    <Prose>Now deliberately add a population direction. Let u be a fixed unit vector and let the population covariance be Σ = I + (ℓ − 1)uuᵀ, where ℓ is greater than 1. Variance is ℓ along u and 1 in every perpendicular direction. In the examples u is the first coordinate, so generating the model means multiplying the first Gaussian column by √ℓ. This is a <strong>spiked covariance model</strong>.</Prose>
    <Prose>We use the real Gaussian model with one fixed, simple population spike and n,p growing at fixed ratio 0 &lt; γ &lt; 1. Let v̂ be the leading sample eigenvector. Its squared overlap |uᵀv̂|² measures alignment: 1 means the same direction up to sign; 0 means perpendicular. An elevated sample variance is not itself a guarantee of a useful estimated direction.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\ell_{\rm crit}=1+\sqrt\gamma,\\[.35em] b=(1+\sqrt\gamma)^2\end{gathered}`}</MathBlock>
    <Prose>When 1 &lt; ℓ ≤ ℓcrit, the leading sample eigenvalue approaches b and its squared overlap with u approaches zero. When ℓ exceeds that threshold, the sample eigenvalue separates and both limits change:</Prose>
    <MathBlock>{String.raw`\widehat\lambda_{\max}\longrightarrow
      \ell\left(1+\frac{\gamma}{\ell-1}\right)`}</MathBlock>
    <MathBlock>{String.raw`|u^\mathsf T\widehat v|^2\longrightarrow
      \frac{1-\gamma/(\ell-1)^2}{1+\gamma/(\ell-1)}`}</MathBlock>
    <Prose>At γ = 0.25 the population threshold is 1.5. For ℓ = 2, the limiting sample eigenvalue is 2(1 + 0.25) = 2.5, while limiting squared overlap is 0.75/1.25 = 0.6. The estimated eigenvalue is biased upward relative to 2, and the estimated direction retains nonzero error. With a fixed ratio, collecting more observations while adding proportionally more features does not eliminate that error.</Prose>
    <SpikeAlignmentLab />
    <H3>Why a threshold appears</H3>
    <Prose>The needed block algebra has a small explanation. Write a covariance as blocks [[a, bᵀ], [b, C]], separating the first coordinate from the background C. An eigenvector has coordinates (t,w). Its lower equation is bt + Cw = ρw. If ρ lies outside C's spectrum, solve for w = (ρI − C)⁻¹bt. Substituting in the first equation gives the scalar relation below for an eigenvector with t ≠ 0:</Prose>
    <MathBlock>{String.raw`\rho = a + b^\mathsf T(\rho I-C)^{-1}b`}</MathBlock>
    <Prose>This operation is a Schur complement: eliminate one block to see its effect on the other. The resolvent (ρI − C)⁻¹ amplifies nearby background eigenvalues, so separating a spike depends on the whole noise spectrum. The probabilistic limit of this relation is the extra theorem, rather than an assumption that b is zero.</Prose>
    <Prose>Separate the spike coordinate from the other coordinates in a block covariance matrix. Eliminating the non-spike coordinates with a Schur complement gives a scalar equation for an eigenvalue outside the background spectrum. In the large Gaussian model, its admissible upper outlier branch simplifies to ρ(ℓ) = ℓ(1 + γ/(ℓ − 1)). Its derivative is 1 − γ/(ℓ − 1)². Its minimum occurs at ℓ = 1 + √γ, where ρ equals b. Below that point, blindly substituting into the outlier expression follows an inadmissible branch; the actual leading limit remains at the bulk edge. Establishing the limit and overlap requires the additional spectral arguments supplied by the cited spike theorem.</Prose>
    <Prose>For example, plugging ℓ = 1.1 and γ = 0.25 into the separated formula gives 3.85. That is wrong because the condition ℓ &gt; 1.5 fails. A formula's branch condition is part of the formula. The lab holds the limiting top value at b below the threshold instead of plotting this misleading branch.</Prose>
    <MatrixExample example={randomMatrixExamples.spike} />
    <Callout label="A PCA transition is a scoped claim">A vanishing overlap for this leading sample eigenvector is not a universal impossibility theorem for every estimator, prior, side-information source or data model. Repeated spikes can identify a subspace without identifying one preferred basis. Heavy tails, dependence and different asymptotic regimes require their own analysis. Treat an outlying component as a candidate to validate, not as proof of a cause.</Callout>

    <H2>7. Control the amplification of weak directions</H2>
    <Prose>Randomness matters at the small end of a spectrum too. To solve Sx = b in an eigenbasis, divide each component of b by the corresponding eigenvalue. A small eigenvalue produces a large inverse gain. If p exceeds the effective number of observations, some gains are not defined because the matrix is singular.</Prose>
    <Prose>Take S = diag(0.01, 2). A perturbation of 0.02 in each coordinate of b changes the solution by (2, 0.01). The weakly measured first direction turns a small input change into a large output change. This is a numerical sensitivity statement even before deciding whether b represents a portfolio constraint, a regression calculation or a sensor estimate.</Prose>
    <InverseGainFigure />
    <MathBlock>{String.raw`\begin{gathered}S=V\operatorname{diag}(\lambda_i)V^\mathsf T
      \\[.35em]
      (S+\alpha I)^{-1} \\
      =V\operatorname{diag}\!\left(\frac1{\lambda_i+\alpha}\right)V^\mathsf T\end{gathered}`}</MathBlock>
    <Prose>For α &gt; 0 and positive semidefinite S, adding αI makes all denominators positive and caps every gain by 1/α. The spectral condition number becomes (λmax + α)/(λmin + α). This explains why regularization stabilizes the solve. It also changes the answer: making α enormous would make every gain tiny, including gains in directions with useful signal. Choose regularization using a justified loss, validation data or an estimation criterion, not a condition number alone.</Prose>
    <MatrixExample example={randomMatrixExamples.ridge} />
    <Prose>Ridge shifts eigenvalues. A pseudoinverse sets gains of zero directions to zero while still inverting retained nonzero directions. Truncated SVD discards selected weak directions. Covariance shrinkage may pull eigenvalues toward a common scale. These operations impose different assumptions and trade bias against variance differently. Matrix Decompositions supplies the algebra; later PCA and estimation lessons own task-specific selection.</Prose>
    <Prose>For γ &lt; 1 under suitable noise assumptions, the asymptotic condition number suggested by the MP edges is [(1 + √γ)/(1 − √γ)]². It deteriorates as γ approaches 1. At γ &gt; 1 the full covariance is singular even though the positive spectrum's lower edge is above zero. A condition number computed only on the retained subspace answers a different question.</Prose>

    <H2>8. Symmetry produces a different spectral law</H2>
    <Prose>A covariance matrix is formed by multiplication, XᵀX. Suppose instead we create a symmetric matrix directly: draw independent entries above the diagonal and reflect them below it. Symmetry forces real eigenvalues, but does not force nonnegativity. This is the setting of <strong>Wigner matrices</strong>, useful as models of random symmetric interactions.</Prose>
    <Prose>For a d-by-d matrix, give each off-diagonal entry mean zero and variance 1/d. Why divide its unscaled value by √d? The average squared eigenvalue is tr(W²)/d = Σij Wij²/d. There are roughly d² off-diagonal terms. Variance 1/d makes their total divided by d remain of order one. Without that scaling, spectral width grows like √d.</Prose>
    <Prose>One Gaussian convention is W = (G + Gᵀ)/√(2d), with independent standard Gaussian entries in G. Off-diagonal entries then have variance 1/d and diagonal entries variance 2/d. This is the scaled <strong>Gaussian orthogonal ensemble</strong> (GOE). Alternatively, independent ±1/√d off-diagonal entries and a zero diagonal form a non-Gaussian Wigner example. The finite second moments differ: GOE has E[tr(W²)/d] = 1 + 1/d, while the sign example has exactly 1 − 1/d.</Prose>
    <MathBlock>{String.raw`\begin{gathered}f_{\rm sc}(\lambda)=\frac1{2\pi}\sqrt{4-\lambda^2},
      \\[.35em] -2<\lambda<2\end{gathered}`}</MathBlock>
    <Prose>For these ensembles, the empirical spectral distribution approaches the <strong>semicircle law</strong>. Its mean is zero and second moment is one. The Gaussian and bounded-sign examples satisfy stronger moment conditions than needed for the bulk theorem. More generally, a Wigner theorem requires appropriate independence, centering, variance and tail assumptions; “symmetric” alone does not imply a semicircle.</Prose>
    <WignerSpectrumLab />
    <MatrixExample example={randomMatrixExamples.wigner} />
    <Prose>The moment mechanism again exposes the structure. In tr(Wᵏ), terms correspond to closed index walks. An independently centered edge appearing only once makes a product's expectation vanish. At leading order, paired traversals survive; their counts give the semicircle moments (1, 0, 1, 0, 2, … starting at order zero). This explains why mirroring and scaling matter. Completing a theorem also requires controlling the other terms and fluctuations.</Prose>
    <H3>Optional depth: a two-level model of repulsion</H3>
    <Prose>Global density says little about the tiny gap between neighboring eigenvalues. A 2×2 symmetric matrix isolates a local mechanism. Let m be its overall level, d half the difference between diagonal entries, and c its coupling:</Prose>
    <MathBlock>{String.raw`\begin{gathered}A=\begin{bmatrix}m+d&c\\c&m-d\end{bmatrix},
      \\[.35em] \lambda_\pm=m\pm\sqrt{d^2+c^2}\end{gathered}`}</MathBlock>
    <Prose>Subtract λI and set the determinant to zero: (m − λ)² − d² − c² = 0. The gap is 2√(d² + c²). If c = 0, varying d through zero permits an exact crossing. If c is nonzero, the minimum gap is 2|c|. Coupling opens an <strong>avoided crossing</strong>.</Prose>
    <AvoidedCrossingLab />
    <Prose>There is an exact probabilistic calculation for the unscaled 2×2 GOE with diagonal variance 2 and off-diagonal variance 1. In this convention d and c are independent standard Gaussians. Their radius R = √(d² + c²) has radial density r exp(−r²/2), obtained by integrating the bivariate Gaussian density around a circle. The gap G = 2R therefore obeys:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\mathbb P(G\leq g)=1-\exp(-g^2/8),\\[.35em] g\geq0\end{gathered}`}</MathBlock>
    <Prose>For small g this probability is approximately g²/8. A small gap requires two coordinates to be small together, rather than only one diagonal difference. This is a concrete origin of level repulsion. It is exact for this two-level ensemble, not an exact nearest-neighbor gap formula for every matrix size. Comparing local gaps in a large matrix also requires accounting for the varying local eigenvalue density, often called unfolding.</Prose>
    <MatrixExample example={randomMatrixExamples.gap} />

    <H2>9. Use a spectrum to make a better decision</H2>
    <Prose>A usable spectral workflow starts by naming the matrix, its units, how observations were obtained, and the null hypothesis. It then separates exact linear algebra, finite computation and asymptotic approximation before drawing a practical conclusion.</Prose>
    <LessonTable caption="Applications with a specific follow-up decision" headers={['Situation', 'What the spectrum can reveal', 'What still needs checking']} rows={[['PCA or a recommender factor model', 'A candidate direction exceeds a matched noise baseline', 'Held-out reconstruction or task value; whether selection and missingness were reproduced in the null.'], ['Repeated sensor channels', 'A duplicated or coupled channel produces shared spectral structure', 'Whether the dependency is an instrument artifact or a real physical relation.'], ['Inverse covariance estimation', 'Weak directions amplify measurement errors', 'A justified shrinkage rule and independent performance on the intended loss.'], ['Random linear layers', 'Average norm preservation differs from worst-direction stretch', 'Nonlinearity, depth, aspect ratio and trained weights invalidate a simple iid linear interpretation.'], ['Random graph interactions', 'A centered, normalized adjacency matrix can have a Wigner-like bulk in suitable dense regimes', 'The mean component, sparse-degree effects and dependencies; raw adjacency is not automatically this ensemble.']]} />
    <H3>A fixed direction versus the worst direction</H3>
    <Prose>Let a square random linear layer W have independent mean-zero entries of variance 1/d, and choose a fixed input x independently of W. Expand each output square. Cross terms have expectation zero and diagonal terms contribute xj²/d; summing over d output coordinates gives E[‖Wx‖²] = ‖x‖². This motivates a variance-preserving scale.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\mathbb E\|Wx\|^2
      \\ =\sum_{i=1}^d\sum_{j=1}^d\frac{x_j^2}{d}
      =\|x\|^2\end{gathered}`}</MathBlock>
    <Prose>Now select x after seeing W: choose its top right singular vector. Its squared stretch is smax², not the fixed-direction average. In the square Gaussian case, the limiting squared singular-value support reaches 4 and touches 0. Average preservation can coexist with directions that stretch strongly or almost disappear. An orthogonal square matrix instead preserves every vector's norm exactly. Neither observation, alone, establishes which initialization trains a nonlinear deep network best.</Prose>
    <MatrixExample example={randomMatrixExamples.linear} />
    <Prose>For a practical PCA study, fit centering, scaling and component selection on training data. Record the entire selection procedure. Test reconstruction or downstream performance on independent data, and inspect subspace stability when eigenvalues are close. Stability is helpful evidence but is not causality. A shared nuisance variable can produce a stable, reproducible component with no value for the intended decision.</Prose>
    <H3>Where deeper random matrix theory goes</H3>
    <Prose>Local laws study finer spectral resolution; edge fluctuation results, including Tracy–Widom limits in suitable ensembles, calibrate scales beyond a coarse edge. Free probability supplies operations for certain asymptotically independent spectral objects. The circular law concerns eigenvalues of nonsymmetric random matrices in the complex plane, so a real-line covariance histogram cannot represent it. Structured, sparse, heavy-tailed and learned matrices each require explicit conditions. These are research branches, not results that follow merely from recognizing a familiar curve.</Prose>
    <Prose>You can now ask which ensemble, which observable, which scaling, which finite or limiting guarantee, and which validation task. The alternate resources develop full proofs and further ensembles without making this page depend on them.</Prose>

    <H2>10. Practise on new shapes and new assumptions</H2>
    <Prose>Work from the model and units before using a formula. The hints are optional; compare your reasoning with the explanation after making a prediction.</Prose>
    <Practice question="You have 300 observations and 600 features. Compare the rank limits and minimum zero counts for known-mean XᵀX/300 and empirically centered covariance with denominator 299. Propose a stable baseline and an independent evaluation." hint="Count observation directions before and after removing the constant direction; then distinguish a stable solve from a good estimator.">
      <Prose>The raw Gram matrix has rank at most 300 and at least 300 zeros. Centering leaves rank at most 299 and at least 301 zeros. Its effective Gaussian ratio is 600/299, not exactly 2. Both full matrices are singular. Adding αI with α &gt; 0 makes a positive-definite baseline, or truncated SVD can retain a selected subspace. Choose α or rank using a validation procedure fitted on training data and report held-out task loss. Stability alone does not establish accuracy.</Prose>
    </Practice>
    <Practice question="An iid covariance model has γ = 4 and entry variance 9. Compute the positive support, zero mass, overall first and second moments, and mean conditional on a positive eigenvalue." hint="Scale eigenvalues by the variance, keep the atom, and divide the first moment by the positive mass for the conditional mean.">
      <Prose>The support is [9,81], the atom is 3/4, the first moment is 9, and the second is 9²(1 + 4) = 405. Positive mass is 1/4, so its conditional mean is 36. The full matrix's minimum remains zero. Modify the integration program's variance and ratio to verify these values without deleting the atom.</Prose>
    </Practice>
    <Practice question="For Gaussian n = 80, p = 20, what is the exact expected second spectral moment? What changes for independent signs? Why do neither of these answers imply that one observed matrix has that exact moment?" hint="Separate the diagonal fourth moment from the off-diagonal product terms.">
      <Prose>Gaussian: 1 + 21/80 = 1.2625. Signs: 1 + 19/80 = 1.2375. Only the sign diagonal contribution is deterministic; off-diagonal sample correlations still vary. These are expectations across draws. Both approach 1.25 when both dimensions increase at fixed ratio 0.25, while their finite corrections differ.</Prose>
    </Practice>
    <Practice question="Repair this analysis: 'My 25×20 Gaussian matrix has minimum singular value at least 5 − √20 − √(2 log 40), so squaring that expression gives a positive 95% lower covariance bound. Its top eigenvalue exceeds the MP edge, which proves a 5% signal.'" hint="A negative lower singular-value bound is vacuous. Then identify which formula actually contains the selected failure probability.">
      <Prose>The expression is negative, so replace it by zero before squaring. The lower bound is zero. A finite Gaussian upper bound includes t = √(2 log 40); the MP edge does not. Exceeding the edge alone gives no 5% test. Use the valid finite upper bound or an appropriately calibrated finite null, and retain the distinction between statistical surprise and meaningful signal.</Prose>
    </Practice>
    <Practice question="At γ = 0.5, compare population spikes ℓ = 1.5 and ℓ = 3. Find limiting top eigenvalues and overlaps, explaining your branch choices." hint="The population threshold is 1 + √0.5. Use the separated expression only above it.">
      <Prose>The threshold is approximately 1.7071. At ℓ = 1.5, the top limit is (1 + √0.5)² ≈ 2.9142 and squared overlap tends to zero. At ℓ = 3, the sample limit is 3(1 + 0.5/2) = 3.75 and overlap is (1 − 0.5/4)/(1 + 0.5/2) = 0.7. A finite sample can differ from all four limits. Replace the dimensions and strengths in the spike program and inspect the difference, rather than treating limits as exact output assertions.</Prose>
    </Practice>
    <Practice question="For S = diag(0.04,4), compare inverse gains and the condition number before and after adding 0.2I. Does the improved condition number prove improved prediction?" hint="Compute both new eigenvalues. Ask what information the condition number contains about the target task.">
      <Prose>Original gains are 25 and 0.25, with condition number 100. New gains are 1/0.24 ≈ 4.1667 and 1/4.2 ≈ 0.2381; the condition number is 4.2/0.24 = 17.5. Worst-direction amplification falls, but the answer is biased toward a different solution. Prediction can improve or worsen depending on signal, noise and loss; it requires a separate evaluation.</Prose>
    </Practice>
    <Practice question="Modify the actual two-level program to use m = 3, d = −2, c = 1. Predict the eigenvalues and gap, then check with a symmetric eigensolver. Also find P(G ≤ 0.2) under the separately specified unscaled 2×2 GOE gap law." hint="The deterministic matrix is [[1,1],[1,5]]. For the probability, use the random d,c model rather than treating this fixed pair as random.">
      <Prose>The eigenvalues are 3 ± √5 ≈ 0.763932 and 5.236068; the gap is 2√5 ≈ 4.472136. NumPy's <Code>eigvalsh</Code> on [[1,1],[1,5]] confirms the pair. Under the GOE model, the probability is 1 − exp(−0.2²/8) ≈ 0.004987521. The probability describes new random matrices, not uncertainty about the fixed matrix's exactly determined gap.</Prose>
    </Practice>
    <Practice question="Design a spectral check for 200 time points from 40 sensors when neighboring readings are autocorrelated and two sensor columns may be duplicates. State what you would preserve, what you would test, and what could invalidate a rank score." hint="A null represents absence of the effect of interest, not absence of every structure. Independent entry shuffling is a substantive modeling choice.">
      <Prose>One starting question is whether cross-sensor structure exceeds a model that preserves each sensor's temporal dependence and includes known duplication artifacts. Fit or specify that null, state its estimation uncertainty, generate complete time series, repeat centering/scaling and compute a predeclared statistic. If generation relies on estimated parameters, explain why calibration is approximate or validate it separately; do not claim exact exchangeability automatically. If duplication is itself the target, including it in the null would remove the effect being tested. Use independent runs to evaluate whether selected components improve a real task. Different scientific questions justify different nulls; unrecorded model search invalidates a simple single-score guarantee.</Prose>
    </Practice>
    <Prose><strong>Readiness check:</strong> explain why an identity population can produce a broad sample spectrum; recover a zero count from rank; keep an atom separate from a density; convert a valid singular-value event into covariance bounds; choose the correct spike branch; and describe an independent check of a useful component. Revisit the worked matrix or investigation for any step you cannot yet explain.</Prose>
    <Prose>The next topic in this module is <a href="/learn/path/full-curriculum/queueing-theory-m-m-1-m-g-1-little-s-law?module=math-foundations">Queueing Theory</a>. It studies random arrivals, service and waiting rather than spectral limits. The connecting habit is to specify a stochastic model and its operating regime before using an average or guarantee. For a direct spectral application, return later to <a href="/learn/path/full-curriculum/pca-dimensionality-reduction">PCA & Dimensionality Reduction</a>.</Prose>
    <Sources alternatives={<div><h4>Another way to learn it</h4><p><a href="https://rolandspeicher.com/lectures/course-on-high-dimensional-analysis-random-matrices-and-machine-learning-summer-term-2023/" target="_blank" rel="noreferrer">Roland Speicher's recorded course</a> pairs lectures with notes. <a href="https://www.youtube.com/watch?v=isLIlb4GAss" target="_blank" rel="noreferrer">Lecture 11: the Marchenko–Pastur law</a> is a spoken route after section 4 at a higher mathematical pace. The creator's course page, lecture metadata and corresponding notes were checked; a full playback review is not claimed.</p><p><a href="https://rolandspeicher.com/wp-content/uploads/2023/08/hda_rmml.pdf" target="_blank" rel="noreferrer">Companion notes, section 4.2</a> compare histograms, moments and resolvents. Their observations-as-columns convention is the transpose of ours. <a href="https://lpetrov.cc/rmt25/" target="_blank" rel="noreferrer">Leonid Petrov's random matrix course</a> offers a graduate-level path and exercises for Gaussian ensembles, semicircle behavior and local questions; it is an optional next depth.</p></div>}>
      <li><a href="https://ocw.mit.edu/courses/18-s096-topics-in-mathematics-of-data-science-fall-2015/5f0f7205d1cf274e80d77345a7edbf2a_MIT18_S096F15_TenLec.pdf" target="_blank" rel="noreferrer">Afonso Bandeira, mathematics of data science notes</a>, sections 1.2–1.3: covariance regimes, noise spectra and spiked models. A useful written companion; historical open-problem claims are not imported.</li>
      <li><a href="https://www3.stat.sinica.edu.tw/statistica/oldpdf/A17n418.pdf" target="_blank" rel="noreferrer">Debashis Paul, 2007, spiked covariance asymptotics</a>, Theorems 1, 2 and 4: the qualified Gaussian eigenvalue and eigenvector limits. Read the ratio and simple-spike assumptions with the formulas.</li>
      <li><a href="https://arxiv.org/pdf/1011.3027" target="_blank" rel="noreferrer">Roman Vershynin, non-asymptotic random matrix tutorial</a>, section 5.3.1, especially Corollary 5.35: finite Gaussian singular-value bounds, their concentration mechanism and the distinction from asymptotic edges.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.eigh.html" target="_blank" rel="noreferrer">NumPy symmetric eigensolvers</a> and <a href="https://numpy.org/doc/stable/reference/random/generator.html" target="_blank" rel="noreferrer">random generators</a>: ordering, eigenvector columns, symmetry and reproducibility contracts. Current documentation was checked alongside the executed versions named above.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.quad.html" target="_blank" rel="noreferrer">SciPy quadrature</a>: numerical integration and error estimates used to check the complete MP measure. A numerical error estimate is not a proof of the limiting theorem.</li>
    </Sources>
  </div>
};
