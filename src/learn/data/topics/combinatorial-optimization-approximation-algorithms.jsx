import { H2, H3, Prose, Code } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { AssignmentMeaningFigure, BoundMeaningFigure, MatroidExchangeLab, KnapsackBoundLab, AssignmentResidualLab, CoverChoiceLab, VertexCoverBudgetLab, KnapsackScalingLab, MetricShortcutFigure } from '../../components/lesson-labs/CombinatorialOptimizationLabs.jsx';
import { combinatorialOptimizationExamples as examples } from '../combinatorial-optimization-examples.js';
function Example({
  example
}) {
  return <section><Prose><strong>Before running.</strong> {example.question}</Prose><RunnableExample example={example} /><Prose>{example.interpretation}</Prose></section>;
}
function Practice({
  title,
  prompt,
  hint,
  children
}) {
  return <section className="lesson-check"><h3>{title}</h3><Prose>{prompt}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Show explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Combinatorial Optimization & Approximation Algorithms',
  readTime: '~95 min read + 3–4 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot combinatorial-lesson">
    <LessonIntro prerequisites="Greedy choices and exchange arguments; graph paths, matching and residual flow; the idea of a linear relaxation and a dual bound. We rebuild the needed constraints and proofs locally. Complete Python programs provide an optional reproduction route." sections={[['1-choose-a-legal-combination', 'Model the decision'], ['2-read-a-quality-guarantee', 'Bounds and approximation'], ['3-find-the-structure-that-makes-greedy-exact', 'Matroids and exchanges'], ['4-search-with-an-optimistic-bound', 'Exact search and stopping'], ['5-undo-an-expensive-assignment', 'Minimum-cost matching and flow'], ['6-pay-for-newly-covered-requirements', 'Set cover and harmonic charges'], ['7-round-a-cover-and-build-its-certificate', 'LP rounding and primal-dual cover'], ['8-maximize-coverage-under-a-selection-budget', 'Diminishing returns'], ['9-trade-a-proved-error-for-smaller-states', 'A knapsack approximation scheme'], ['10-repair-a-route-without-losing-the-bound', 'Metric routing and parity'], ['11-check-the-decision-in-the-real-system', 'Model quality and independent practice']]}>A good individual choice can block a much better complete plan. Learn to model legal combinations, recognize when greedy decisions are exact, and measure how far a feasible answer could be from the best one. The central skill is being able to explain both the selected plan and the evidence behind its quality.</LessonIntro>

    <H2>1. Choose a legal combination</H2>
    <Prose>Two workers must fill two jobs. Worker A can do job 1 at cost 1 or job 2 at cost 100; worker B has the opposite costs. Taking the cheapest available worker independently for each job happens to work here. But selecting the cheapest worker independently for every task can assign one worker to several simultaneous tasks. A collection of individually attractive choices is not automatically a legal plan.</Prose>
    <AssignmentMeaningFigure />
    <Prose><strong>Combinatorial optimization</strong> means choosing the best permitted combination of discrete decisions. “Discrete” means choices such as yes/no, one route or another, or an integer number of machines. The challenge is how choices interact. If there are n yes/no decisions, there are 2ⁿ possible bit patterns before constraints remove any of them.</Prose>
    <Prose>Introduce a <strong>decision variable</strong> xᵢⱼ for worker i and job j: it is 1 when that assignment is used and 0 otherwise. For a complete one-to-one assignment of equally many workers and jobs, each row and each column must contain exactly one 1. Cost cᵢⱼ has declared units, such as staff hours or dollars; the objective adds the costs of selected pairs.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\min_x \sum_{i,j} c_{ij}x_{ij},\\
\sum_jx_{ij}=1\quad\text{for each worker }i,\\
\sum_ix_{ij}=1\quad\text{for each job }j,\\
x_{ij}\in\{0,1\}.
\end{gathered}`}</MathBlock>
    <Prose>The <strong>feasible set</strong> is the collection of plans satisfying every constraint. An <strong>objective</strong> ranks those legal plans. For a partial assignment of required size F, change row/column equalities to “at most one” and require the total sum of the variables to equal F. An unavailable assignment is forbidden, not represented by a cheap zero.</Prose>
    <Prose>This modeling distinction matters in applications. Finding one cheapest route is a shortest-path problem. Visiting every required location is a different problem. Adding several vehicles, capacities and delivery windows changes it again. The name “routing” does not identify an algorithm until the rules are specified.</Prose>
    <LessonTable caption="Structure, rather than the mere presence of discrete choices, determines what is tractable." headers={['Model', 'Useful exact structure', 'A change that needs new analysis']} rows={[['Minimum spanning tree', 'A forest admits a greedy exchange argument', 'Extra side constraints can invalidate that argument'], ['One-to-one weighted assignment', 'Integral flow/matching structure', 'Shared resource constraints across multiple assignments'], ['Whole-item knapsack', 'Value/weight dynamic programming; exact search', 'Large encoded capacities make capacity-index DP pseudopolynomial'], ['Set cover and general vertex cover', 'Small exact baselines and useful relaxations', 'No general polynomial exact algorithm is known'], ['Metric traveling salesperson', 'Triangle inequality enables approximations', 'Arbitrary costs destroy shortcut bounds']]} />
    <Prose>Some problems have polynomial-time exact algorithms; others are NP-hard. The latter classification is about a general family, not a prediction that each small or structured instance is difficult. An exact exponential search can still be the most useful independent oracle for a tiny example. We will label such oracles separately from scalable algorithms.</Prose>
    <Prose><strong>Run the examples.</strong> Save each complete program as its own Python file and run it with <Code>python filename.py</Code>. They use the standard library except the explicitly marked fractional-cover program, which also needs NumPy and SciPy. That program was executed with Python 3.12, NumPy 2.3.5 and SciPy 1.18.1. Install those packages in your own virtual environment if reproducing it. The browser investigations calculate bounded teaching models; they do not launch Python or a remote solver.</Prose>

    <H2>2. Read a quality guarantee</H2>
    <Prose>Write OPT for the unknown optimal objective and A for the objective of an actual feasible answer. For minimization, feasibility supplies an <em>upper</em> bound: the best plan cannot cost more than a plan we already have. A relaxation that permits more choices can supply a lower bound L. For maximization the directions reverse.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\text{minimize:}\quad L\le \mathrm{OPT}\le A,\\
\text{maximize:}\quad A\le \mathrm{OPT}\le U.
\end{gathered}`}</MathBlock>
    <BoundMeaningFigure />
    <Prose>A bound on one instance is a <strong>certificate</strong>. An <strong>approximation algorithm</strong> also gives a theorem over every input satisfying its assumptions, with a polynomial running-time guarantee in the encoded input length. Here, for nonnegative objectives, an α-approximation means α≥1 and the following inequality. Some maximization literature instead calls the reciprocal β=1/α the approximation fraction; always check the convention.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
A\le\alpha\,\mathrm{OPT}\quad\text{(minimization)},\\
A\ge\mathrm{OPT}/\alpha\quad\text{(maximization)}.
\end{gathered}`}</MathBlock>
    <Prose>A factor-2 guarantee does not predict that the answer will be twice optimal. The method may be optimal on your instance. Nor does a tiny exact solution prove that a heuristic has a factor-2 guarantee on all inputs. A <strong>heuristic</strong> can have excellent observed performance without that worst-case theorem; its runtime also needs its own description.</Prose>
    <Prose>Zero and signs deserve attention. For nonnegative minimization with OPT=0, any finite multiplicative guarantee requires A=0. Dividing A by OPT is still undefined. For a zero maximization optimum, the usual fraction statement can be vacuous. With negative objectives, these ratio formulas can behave perversely, so use a specifically stated signed-objective theorem or additive error. Adding a constant to make numbers positive changes ratios. Even if all plans have the same cardinality and the optimizer is preserved, the ratio is not generally preserved.</Prose>
    <Prose>A relative solver gap needs a stated denominator; packages use different conventions. If you know L≤OPT≤A for minimization, the additive gap A−L is unambiguous. If L is positive, A/L is a valid instance upper factor. Report whether the bounds and feasibility are exact, numerical within a tolerance, or estimates.</Prose>
    <Checkpoint prompt="A minimization run has a feasible cost 120 and a valid lower bound 100. Another run has feasible cost 110 and no bound. Which claims can you make?">
      <Prose>The second feasible answer is cheaper and therefore the better incumbent. The first certificate still applies to this same unchanged instance, so combining them gives 100≤OPT≤110 and a factor at most 1.1. Without transferring a valid bound from the same problem, cost 110 alone gives no such factor. An infeasible cost 90 would not be an improvement at all.</Prose>
    </Checkpoint>

    <H2>3. Find the structure that makes greedy exact</H2>
    <Prose>In the DSA greedy lesson, each correct algorithm needed an exchange argument. A <strong>matroid</strong> packages a particularly useful exchange structure. Let E be a finite set of candidate elements and let ℐ be its permitted, or <em>independent</em>, subsets. The word independent means “feasible under this set rule”; it need not refer to probabilistic independence.</Prose>
    <Prose>Three axioms define a matroid. The empty set is independent. Every subset of an independent set is independent: removing a selected element cannot break this rule. Finally, if independent sets I and J satisfy |I|&lt;|J|, at least one element of J outside I can be added to I while keeping it independent. This last <strong>augmentation axiom</strong> prevents a smaller feasible set from becoming permanently stuck while a larger one exists.</Prose>
    <Prose>“Choose at most two candidates” is a uniform matroid. The acyclic edge sets of an undirected graph form a graphic matroid. Why does augmentation hold for forests? A forest I divides vertices into components. If every edge of a larger forest J stayed inside those components, J could contain at most the sum of “component size minus one,” which equals |I|. Since it has more edges, some J edge must cross two I components. Adding that edge joins components without creating a cycle.</Prose>
    <Prose>Nonoverlapping intervals are hereditary but do not form a matroid in general. Take I containing one long interval [0,2), and J containing [0,1) and [1,2). Both are feasible and J is larger. Neither short interval can join the long one. We can assign the long interval value 3 and each short one value 2: decreasing-value greedy chooses 3 while the optimum gets 4. A special interval algorithm can still be correct for another objective, such as maximizing the number of intervals by earliest finish time; that does not create a general matroid weight theorem.</Prose>
    <MatroidExchangeLab />
    <H3>Why decreasing-weight greedy works on a matroid</H3>
    <Prose>For the unrestricted maximum-weight independent-set problem, ignore nonpositive weights and inspect the remaining elements in decreasing weight order. Add an element exactly when independence is preserved. At each prefix of this ordered list, the selected elements form a maximal independent subset of the prefix: every rejected element was already incompatible, and adding more elements cannot make it compatible again under heredity.</Prose>
    <Prose>Augmentation says that a maximal independent subset of a prefix has maximum possible cardinality there. Otherwise a larger independent subset could augment it. Therefore greedy contains at least as many elements from each high-weight prefix as any competing independent set. That prefix-count comparison also compares total weight.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
w_1\ge\cdots\ge w_m>0,\quad w_{m+1}=0,\\
N_k(S)=|S\cap\{1,\ldots,k\}|,\\
w(S)=\sum_{k=1}^{m}(w_k-w_{k+1})N_k(S).
\end{gathered}`}</MathBlock>
    <Prose>Each element's weight telescopes through the prefixes containing it. Every coefficient is nonnegative. Since greedy wins every prefix-count comparison, it wins their weighted sum. This proves optimality, rather than merely illustrating it on a few examples. Sorting costs O(m log m); the independence tests contribute their own runtime. A disjoint-set structure efficiently tests forest extensions.</Prose>
    <Prose>A <strong>base</strong> is a maximal independent set. Matroid bases all have the same size, by augmentation. If the problem requires a base, negative elements may be unavoidable; process all weights and extend to a base. One proof shifts every weight by the same large constant: all bases receive the same total shift, so their ordering is preserved. That argument is invalid for arbitrary independent sets of different sizes. A graph with one negative edge has an empty maximum-weight forest but a mandatory spanning tree containing that edge.</Prose>
    <Example example={examples.matroidForest} />

    <H2>4. Search with an optimistic bound</H2>
    <Prose>When structure does not justify greedy, an exact search need not blindly visit every complete choice. In <strong>branch-and-bound</strong>, a node is a partial decision. A branch fixes the next choice, such as include or exclude an item. A bound estimates the best objective that any completion of that partial decision could achieve. It must be optimistic in the correct direction.</Prose>
    <Prose>Consider whole-item knapsack: item i has positive weight wᵢ and nonnegative value vᵢ; selected weights may sum to at most capacity C. A partial selection is already a legal candidate if its current weight fits, because we may reject everything else. Such a candidate can improve the <strong>incumbent</strong>, the best feasible solution found so far.</Prose>
    <Prose>To upper-bound a subtree, allow fractions of the undecided items. Fill the remaining capacity in descending value-per-weight order. An exchange of a lower-density fraction for a higher-density fraction cannot reduce value, so this solves the fractional relaxation. Every whole-item completion is allowed by that relaxation; its optimum is therefore an upper bound on all of them.</Prose>
    <Prose>At capacity 10, items A=(weight 2,value 14), B=(3,18), C=(4,22), D=(5,25), E=(7,31) give a fractional root value of 59: take A, B, C and one fifth of D. The legal A+B+C selection is worth 54. The fractional number 59 is evidence about possibilities, not a legal whole-item answer. A+B+D later gives weight 10 and value 57.</Prose>
    <KnapsackBoundLab />
    <Prose>For maximization, prune a node only when its upper bound is at most the incumbent's value. That subtree cannot improve the answer. If the node is overweight, prune it for infeasibility. Otherwise branch and retain the alternatives. When stopping early, the largest remaining frontier bound, together with the incumbent value, bounds the unknown optimum. Losing the frontier and reporting only the last node's bound can falsely certify optimality.</Prose>
    <Example example={examples.boundedKnapsack} />
    <Prose>The displayed Python run uses exact rational bounds. At four heap removals it returns value 54 with upper bound 409/7≈58.429; it has not proved optimality. At completion both bounds are 57. Exponential worst-case search remains possible even when many branches prune. The browser's tiny oracle exhausts subsets to check the lesson model, while a real branch-and-bound solver seeks a certificate without needing that oracle.</Prose>
    <H3>A quick half guarantee before searching</H3>
    <Prose>There is also a simple whole-item approximation. First remove items whose individual weight exceeds C. Sort the remaining items by density, and take the consecutive whole-item prefix before the first item that would overflow. Compare that prefix's value P with the best feasible singleton value V. Return the better plan. If everything fits, return everything.</Prose>
    <Prose>If a fractional item occurs, the fractional optimum is P plus at most that next item's full value. The next item individually fits because of the filtering, so its value is at most V. Thus OPT≤P+V≤2 max(P,V). The returned value is at least OPT/2. Density prefix alone can be arbitrarily worse: with capacity 10, an item of weight 1/value 2 can block a weight 10/value 19 item, even though 2/1 is the larger density. The singleton comparison repairs that case. Later we will obtain arbitrarily tighter guarantees by spending more computation.</Prose>

    <H2>5. Undo an expensive assignment</H2>
    <Prose>A maximum-cardinality matching decides how many jobs can be filled, not their least total cost. For a weighted assignment, keep the flow network from the DSA lesson: source→worker edges and job→sink edges have capacity one and cost zero; each available worker→job edge has capacity one and its assignment cost. A flow of value F represents F distinct assignments.</Prose>
    <Prose>A selected edge has a reverse residual edge. Traversing it cancels one unit of the old assignment, so its cost must be the <strong>negative</strong> of the original cost. This is a refund. Without it, the algorithm can count jobs correctly while evaluating rearrangements incorrectly.</Prose>
    <Prose>Use costs [[1,2,8],[2,9,8],[8,8,1]]. First A takes job 1 at cost 1; then C takes job 3 at cost 1. To fill B cheaply, follow the residual path B→job 1→A→job 2. Its assignment changes cost 2−1+2=3. The reverse middle edge releases A's old job, so B gets job 1 while A moves to job 2. The complete cost is 2+3=5, not the cost 11 produced by leaving A fixed and assigning B job 2.</Prose>
    <AssignmentResidualLab />
    <Prose><strong>Successive shortest augmenting paths</strong> implement this idea. Begin with zero flow. Find a cheapest residual source–sink path; send one unit along it, updating both each forward residual capacity and its reverse. Repeat until the requested size is reached or no path exists. Bellman–Ford handles negative residual costs. This layered initial network has no directed cycle, even when some original costs are negative.</Prose>
    <H3>What certifies a minimum cost?</H3>
    <Prose>Fix the flow value. The difference between any two feasible flows of that value is a circulation: the same amount enters and leaves every vertex. It can be decomposed into residual cycles. If a negative-cost residual cycle exists, augmenting around it lowers cost without changing flow value. Conversely, if every residual cycle has nonnegative cost, no same-value alternative can be cheaper. This is the optimality criterion.</Prose>
    <Prose>A checkable way to rule out negative cycles uses a potential p(v) on every vertex. Define a <strong>reduced cost</strong> on a residual edge u→v as c(u,v)+p(u)−p(v). Potentials cancel around a cycle. If every positive-capacity residual edge has nonnegative reduced cost, every residual cycle has nonnegative original cost as well.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\bar c(u,v)=c(u,v)+p(u)-p(v),\\
\sum_{\text{cycle}}\bar c=\sum_{\text{cycle}}c.
\end{gathered}`}</MathBlock>
    <Prose>Shortest-path distances provide such potentials in a graph without negative cycles: distance(v)≤distance(u)+c(u,v). To include disconnected residual components, add an artificial source with a zero-cost edge to every vertex, or equivalently initialize every distance to zero in the certificate calculation. The investigation exposes the final potentials and every residual reduced cost.</Prose>
    <Prose>The shortest augmentation preserves the criterion. Within the reachable region, shortest-path distances make every usable residual reduced cost nonnegative; edges on the selected shortest path are tight. Reversing a tight path edge keeps its reduced cost zero. No previously unreachable vertex becomes reachable by merely adding reversals between path vertices. A new negative cycle would therefore need to appear inside a region whose reduced costs are nonnegative, which is impossible. Starting at the acyclic zero-flow graph, each attained cardinality is minimum-cost.</Prose>
    <Example example={examples.assignment} />
    <Prose>With F augmentations, V vertices and E directed residual edges, this straightforward implementation takes O(FVE) time for Bellman–Ford, plus O(V+E) working storage. Retaining the teaching trace adds storage proportional to the reported paths and matchings. It is not the optimized O(n³) Hungarian algorithm. The exhaustive comparison in the example is a tiny verification oracle and is deliberately more expensive.</Prose>
    <Prose>If the requested assignment size cannot be reached, the returned smaller matching is not a feasible answer to that request. It can still be a minimum-cost matching of its attained size, and the lack of a residual path certifies maximum attainable cardinality. These are different claims.</Prose>
    <H3>Where the exact structure extends—and where it changes</H3>
    <Prose>For a general prescribed-value min-cost flow, residual reversals and the no-negative-cycle criterion still apply. The simple initialization needs care: a general network can already contain a negative cycle at zero flow, so zero flow may not be its cheapest value-zero flow. Cancel negative cycles or obtain a suitable starting optimum before applying the same reasoning. Finite capacities limit how much can circulate; an uncapacitated negative cycle can instead make cost unbounded below. Multicommodity flows with shared capacities need their own formulation and do not inherit this assignment algorithm.</Prose>
    <Prose><strong>Weighted bipartite vertex cover</strong> has a separate exact cut reduction. Given left/right vertex classes and nonnegative vertex costs, put each left cost on source→left and each right cost on right→sink. For every original left–right edge, add a crossing arc of capacity M greater than the sum of all vertex costs. A minimum cut never pays M because selecting every vertex costs less. Select left vertices on the sink side and right vertices on the source side. Every original edge is covered, and the cut pays exactly the selected costs. Conversely, every cover gives such a finite cut. This proves equality of the two optima.</Prose>
    <Prose>General graph matching is still polynomial-time solvable, including its weighted form, but odd cycles require more machinery than bipartite alternating paths. On a triangle, setting every matching edge variable to .5 satisfies “at most one per vertex” and totals 1.5; an integral matching can use only one edge. An odd vertex set S needs at most (|S|−1)/2 internal matching edges. Blossom algorithms manage the corresponding odd-cycle obstruction by contraction and later expansion. This is a structural boundary, not evidence that general matching is NP-hard. The weighted vertex-cover problem on general graphs, which we study next, is a different optimization problem.</Prose>

    <H2>6. Pay for newly covered requirements</H2>
    <Prose>A regression suite contains candidate tests. Each covers a subset of required behaviors, and each has a nonnegative execution cost. <strong>Set cover</strong> chooses tests of minimum total cost that collectively cover every behavior. Sets may overlap; buying a second test does not earn credit twice for an already covered behavior.</Prose>
    <Prose>Start with the original six-element example. A covers &#123;1,2,3,4&#125;, B covers &#123;1,2,5&#125;, C covers &#123;3,4,6&#125;, and all costs are one. Greedy first buys the set with the most uncovered elements, A. It then needs both B and C for the two missing elements. The optimum buys only B and C.</Prose>
    <Example example={examples.originalCover} />
    <Prose>For unequal costs, choose minimum cost divided by the number of <em>newly</em> covered elements. Ignore candidates that add nothing. A zero-cost useful set has ratio zero and can be selected safely. If no candidate covers a remaining required element, the instance is infeasible; the original fixed-fixture loop assumes this cannot happen, whereas the weighted program detects it.</Prose>
    <CoverChoiceLab />
    <H3>Turn local prices into a global bound</H3>
    <Prose>When a selected set costs c and newly covers r elements, assign each new element a charge c/r. The charges sum to exactly the algorithm's total cost A. Now focus on any one candidate set S, whether greedy selected it or not. Suppose t of its elements remain uncovered when greedy covers the next one. Greedy could have bought S for cost per new element c(S)/t, so the chosen step's ratio—and that element's charge—is no greater.</Prose>
    <Prose>Order S's elements by when they become covered, breaking simultaneous ties arbitrarily. Before its jth element is covered, at least |S|−j+1 elements of S were uncovered at the start of that step. The successive charges are bounded by c(S)/|S|, c(S)/(|S|−1), …, c(S). Their sum is at most c(S)H_|S|, where H_r=1+1/2+…+1/r is a harmonic number.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\sum_{e\in S}\mathrm{charge}(e)\le c(S)H_{|S|},\\
H_r=\sum_{j=1}^{r}\frac1j\le1+\ln r
\quad(r\ge1).
\end{gathered}`}</MathBlock>
    <Prose>Take an optimal collection of sets. Every element appears in at least one of them, and every charge is nonnegative. Summing the candidate inequalities over that collection may count a charge several times, which only makes the sum larger. If d is the largest candidate size, A≤H_d OPT. For d=1, H₁=1: the theorem correctly gives exactness. Writing only “ln d” would fail at this endpoint. An empty universe has the valid empty cover and cost zero.</Prose>
    <Example example={examples.weightedCover} />
    <Prose>With costs 3,2,2, B and C each initially cost 2/3 per new requirement while A costs 3/4. Greedy now selects B and C and is optimal on this instance. The algorithm did not become exact on all weighted instances; this particular cost change removed the earlier trap.</Prose>

    <H2>7. Round a cover and build its certificate</H2>
    <Prose>In <strong>vertex cover</strong>, we pay for vertices so that every graph edge has at least one selected endpoint. One application is choosing monitoring points when every connection must touch a monitored endpoint. This is set cover with vertices as candidate sets and incident edges as the elements they cover, but its special “two endpoints per requirement” structure allows a better factor.</Prose>
    <Prose>Let cᵥ≥0 be a vertex cost. The binary program minimizes the sum cᵥxᵥ subject to xᵤ+xᵥ≥1 for every edge. Relax x to real numbers in [0,1]. Every binary cover remains feasible, so the LP optimum is a lower bound on the integer optimum. A fractional .5 means a mathematical half-purchase, not an actually monitored half-vertex.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\min\sum_v c_vx_v,\\
x_u+x_v\ge1\\
\text{for each edge }\{u,v\},\\
0\le x_v\le1.
\end{gathered}`}</MathBlock>
    <Prose>Round by selecting every vertex whose LP value is <em>at least</em> .5. Every edge remains covered: its two values cannot both be below .5 if their sum is at least one. For each selected vertex, 1≤2xᵥ. Multiplying by its nonnegative cost and summing proves that rounded cost is at most twice the LP value, which is at most twice OPT. This proof is specific to these constraints. Threshold rounding can violate knapsack capacity or assignment constraints.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
c(\mathrm{rounded})\le2\sum_vc_vx_v^*,\\
\mathrm{LP}\le\mathrm{OPT},\\
c(\mathrm{rounded})\le2\,\mathrm{OPT}.
\end{gathered}`}</MathBlock>
    <VertexCoverBudgetLab />
    <Example example={examples.fractionalCover} />
    <Prose>The triangle costs 2,3,4. A feasible fractional optimum is (.5,.5,.5), costing 4.5. Rounding buys all three for 9; the exact cover can buy the first two for 5. The bound allows this loss. On a unit-cost complete graph with n vertices, all-half costs n/2 while the integer optimum is n−1, so the relaxation's gap can approach 2. A small gap on one graph is not a proof that the relaxation is always tight.</Prose>
    <H3>A lower-bound certificate made of edge loads</H3>
    <Prose>We can construct a factor-2 cover without solving a general LP. Give each edge a nonnegative load yₑ. At a vertex, total incident load must not exceed its cost. Think of the cost as a budget shared by its incident edges. For any actual cover, every edge load is counted at least once among selected vertices' incident loads. Therefore the sum of all edge loads is a lower bound on every cover's cost.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
y_e\ge0,\qquad\sum_{e\ni v}y_e\le c_v,\\
\sum_e y_e\le\mathrm{OPT}.
\end{gathered}`}</MathBlock>
    <Prose>This is the LP dual when the redundant upper bounds xᵥ≤1 are dropped; nonnegative costs allow an optimum with xᵥ≤1. Start all loads at zero and select zero-cost vertices immediately. Pick an uncovered edge, increase its load until at least one endpoint's budget is exhausted, and select every endpoint that becomes tight. Repeat until no uncovered edge remains. A selected vertex never receives another increase, because future increased edges must have both endpoints unselected.</Prose>
    <Prose>At termination the selected vertices form a cover, and every selected vertex is tight. Its cost equals its total incident load. When we sum those costs, each edge load appears at most twice, once for each endpoint. Thus selected cost≤2∑yₑ≤2OPT. The edge loads are feasible throughout, but they need not be the <em>optimal</em> dual loads. On our triangle, the algorithm gets lower bound 3 and cover cost 5; the LP optimum is 4.5.</Prose>
    <Example example={examples.primalDualCover} />
    <details><summary>Deeper: why the small LP picture can search only 0, 1/2 and 1</summary>
      <Prose>The vertex-cover LP has a half-integral optimum. To see the key argument, consider an extreme feasible point with a fractional coordinate other than .5. Group coordinates strictly between 0 and .5 into L and those strictly between .5 and 1 into H. Any tight edge involving one of these coordinates joins L to H: its two values sum to one. Increase all L coordinates by a sufficiently small ε and decrease H by ε. Tight sums stay fixed; nontight inequalities and variable bounds have enough positive slack to remain feasible for small ε.</Prose>
      <Prose>The opposite perturbation is also feasible. Their midpoint is the original point, so it cannot be extreme. Hence an extreme optimum has coordinates only 0, .5 or 1. The browser enumerates that grid for at most four visible vertices to make the fractional picture independently inspectable. A 3ⁿ enumeration is still exponential; the theorem does not turn it into a scalable LP implementation.</Prose>
    </details>

    <H2>8. Maximize coverage under a selection budget</H2>
    <Prose>Suppose all six behaviors cannot be covered within your test-run budget, and you may choose at most two tests. Now uncovered behaviors are permitted. The objective is to maximize the number covered, rather than minimize cost subject to covering all. Greedy repeatedly chooses the largest <strong>marginal gain</strong>: the number of previously uncovered behaviors added by the next test. The same incidence picture serves this different question, so change its objective above.</Prose>
    <Prose>Coverage has <strong>diminishing returns</strong>. Adding a test to a small suite can reveal many new behaviors; adding the same test to a larger suite cannot reveal more new ones. Formally, for A⊆B and candidate e outside B, f(A∪&#123;e&#125;)−f(A)≥f(B∪&#123;e&#125;)−f(B). This property is called <strong>submodularity</strong>. Coverage also satisfies f(∅)=0, f≥0 and monotonicity: adding tests cannot reduce the set of covered behaviors.</Prose>
    <Prose>Let O be an optimal selection of at most k tests, and Gᵢ the i tests already selected greedily. The elements of O together can close the remaining gap OPT−f(Gᵢ): by monotonicity, f(Gᵢ∪O)≥OPT. Diminishing returns bounds that combined improvement by the sum of their individual marginal gains at Gᵢ. At least one of at most k candidates therefore has gain at least (OPT−f(Gᵢ))/k. Greedy gains at least that much.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
\Delta_i=\mathrm{OPT}-f(G_i),\\
\Delta_{i+1}\le(1-1/k)\Delta_i,\\
f(G_k)\ge[1-(1-1/k)^k]\,\mathrm{OPT}\\
\ge(1-1/e)\,\mathrm{OPT}\quad(k\ge1).
\end{gathered}`}</MathBlock>
    <Prose>For k=2, the finite-k fraction is 3/4. On A/B/C, greedy covers five behaviors with A and B, while B and C cover six. Its actual fraction 5/6 is better than the guaranteed 3/4. For k=0 the empty choice is the answer; do not substitute zero into 1/k.</Prose>
    <Example example={examples.maximumCoverage} />
    <Prose>The same proof works for any nonnegative, normalized, monotone submodular objective under a cardinality budget with exact marginal evaluations. Nonnegative weights on behaviors also preserve coverage's diminishing returns. But a dollar budget with unequal candidate costs is not this cardinality constraint; arbitrary nonmonotone rewards or complementarity need different algorithms. If two experiments are useful only when both run, their combined value can increase marginal gains, violating submodularity. Estimated gains from simulations also require an error analysis before inheriting the exact-oracle theorem.</Prose>

    <H2>9. Trade a proved error for smaller states</H2>
    <Prose>The DSA dynamic-programming lesson solves integer knapsack by capacity-indexed states. O(nC) operations can be practical for small C, but C may have only log₂C bits in the input. A number with 40 bits can be around a trillion, so polynomial in C is not polynomial in its encoded length. This is <strong>pseudopolynomial</strong> complexity.</Prose>
    <Prose>An <strong>approximation scheme</strong> takes an error tolerance as input. A PTAS runs in polynomial time for each fixed tolerance ε, but its exponent may depend on ε, for example n^(1/ε). An FPTAS is polynomial both in the encoded input length and in 1/ε. For this maximization problem, we use the convention returned value≥(1−ε)OPT, for 0&lt;ε&lt;1. If a source uses OPT/(1+δ), choose ε=δ/(1+δ) to translate the guarantee.</Prose>
    <H3>Store the lightest way to reach an integer value</H3>
    <Prose>First remove overweight items and zero-value items. If none remain, return the empty solution. Let n now count the remaining items, and let Vmax be their largest value. It individually fits, so Vmax≤OPT. Choose a value unit K=εVmax/n and round every value <em>down</em> to the integer v′ᵢ=floor(vᵢ/K). Keep each original weight unchanged.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
K=\frac{\varepsilon V_{\max}}n,\qquad
v_i'=\left\lfloor\frac{v_i}{K}\right\rfloor,\\
Kv_i'\le v_i<K(v_i'+1).
\end{gathered}`}</MathBlock>
    <Prose>Now define D[i,s] as the minimum weight needed to achieve rounded value exactly s using the first i items. Start with D[0,0]=0 and all other values unreachable. Either skip item i, or take it and come from the previous row at value s−v′ᵢ. Reading the previous row ensures the item cannot be reused.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
D[i,s]=\min\bigl\{D[i-1,s],\\
D[i-1,s-v_i']+w_i\bigr\}.
\end{gathered}`}</MathBlock>
    <Prose>Ignore the take expression when s&lt;v′ᵢ. Find the largest final score s with D[n,s]≤C and trace the chosen transitions backward. This produces a feasible subset for the original weights. Fractional scores never enter this DP: the scaling step made the scores integers, while the values you report afterward remain the original values.</Prose>
    <KnapsackScalingLab />
    <H3>Account for every unit of value lost</H3>
    <Prose>Let O be the true optimal subset and S the DP-selected subset. Each item's downward rounding loses less than K value; O contains at most n items. The DP maximizes rounded value among feasible subsets, so its rounded sum is at least O's. Translating back to original values gives:</Prose>
    <MathBlock>{String.raw`\begin{gathered}
v(S)\ge K v'(S)\ge K v'(O),\\
K v'(O)\ge v(O)-nK,\\
nK=\varepsilon V_{\max}\le\varepsilon\mathrm{OPT},\\
v(S)\ge(1-\varepsilon)\mathrm{OPT}.
\end{gathered}`}</MathBlock>
    <Prose>Every rounded value is at most n/ε, so the sum of rounded values is at most n²/ε. Updating n rows takes O(n³/ε) arithmetic operations. The displayed reconstruction stores the full table, using O(n³/ε) cells; computing only the optimum score can roll two rows for O(n²/ε) cells, but reconstruction then needs an additional strategy. Integer weight operations have bit costs polynomial in the input length. This is an FPTAS, despite its fairly large basic polynomial.</Prose>
    <Example example={examples.scaledKnapsack} />
    <Prose>The impossible value-100000 item in the program is intentional. Including it when selecting Vmax would invalidate Vmax≤OPT and can make all useful rounded values zero. Filtering is part of the proof, not an optional optimization. The program uses exact <Code>Fraction</Code> arithmetic to compute floors without a decimal rounding ambiguity. Smaller ε increases the state count; it does not force a better answer on every instance, because a coarser run can already be optimal.</Prose>
    <Prose>This scheme is consistent with knapsack's NP-hardness. To force exactness by demanding error smaller than one integer objective unit may require 1/ε proportional to a large numeric value. That can be exponential in its bit length. “Polynomial in 1/ε” does not mean polynomial for an exponentially precise request.</Prose>

    <H2>10. Repair a route without losing the bound</H2>
    <Prose>A traveling-salesperson tour visits every vertex once and returns to the start. Assume a complete undirected graph with nonnegative symmetric costs satisfying the <strong>triangle inequality</strong>: going directly from u to w costs no more than going through v. This is the metric version. Euclidean distances and shortest-path distances in an undirected nonnegative connected network are examples, though a tour in a shortest-path metric may revisit intermediate original-network vertices when expanded.</Prose>
    <Prose>Build a minimum spanning tree T. Deleting any edge from an optimal tour gives a spanning tree, so c(T)≤OPT. Double every tree edge. The resulting connected multigraph has even degree at every vertex, so it has an Euler circuit: a closed walk using each edge copy once. Its cost is 2c(T). Skip a previously visited vertex whenever it appears again. Triangle inequality ensures each shortcut cannot increase cost, yielding a tour of cost at most 2OPT.</Prose>
    <MetricShortcutFigure />
    <H3>Repair only the odd degrees</H3>
    <Prose>Christofides' algorithm improves the construction. Let O be the odd-degree vertices of the MST; their number is even because degrees sum to twice the edge count. Find a minimum-cost perfect matching on O and add those edges to the tree, keeping parallel copies if necessary. Each odd degree increases by one; all degrees are now even. Take an Euler circuit and shortcut repeated vertices as before.</Prose>
    <Prose>Why does the matching cost at most OPT/2? Follow an optimal tour and shortcut away vertices outside O. This yields a cycle through O of cost at most OPT. Its alternate edges form two perfect matchings; the cheaper has at most half the cycle's cost. The minimum perfect matching costs no more than that candidate. Consequently tree plus matching costs at most OPT+OPT/2, and shortcutting preserves the bound. With two odd vertices, the cycle is the two-edge closed walk between them; the same half-cost reasoning applies.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
c(T)\le\mathrm{OPT},\\
c(M)\le\mathrm{OPT}/2,\\
c(\mathrm{tour})\le c(T)+c(M)\\
\le\tfrac32\mathrm{OPT}.
\end{gathered}`}</MathBlock>
    <Example example={examples.metricRoutes} />
    <Prose>The five points use Manhattan distance. The MST costs 8; its odd-vertex matching costs 4. The unshortcutted repaired walk therefore costs 12, and its shortcut tour costs 10. The doubled-tree traversal happens to shortcut to cost 14. A tiny exact tour search confirms optimum 10. Different legal Euler traversals can produce different tours while preserving the proven bounds.</Prose>
    <Prose>The complete example enumerates perfect matchings only on its tiny odd set and enumerates tours only as a verification oracle. A polynomial-time Christofides implementation uses a polynomial general minimum-weight perfect-matching algorithm; swapping in exhaustive matching does not inherit that runtime. The mathematical guarantee applies to the optimal matching used by either method. Do not claim this example benchmarks large routing systems.</Prose>
    <Prose>Without metric structure, a shortcut can replace edges of costs 1 and 1 by an edge of cost 100. The proof fails exactly there. The difficulty is deeper than a broken drawing: for any fixed factor α≥1, take a graph, give existing edges cost 1 and missing edges cost greater than αn. A Hamiltonian cycle exists exactly when the completed instance has a tour of cost n. An α-approximation would then return cost at most αn in the yes case, while every no-case tour must contain an expensive missing edge. Such an algorithm would decide Hamiltonian cycle in polynomial time. This gap construction explains the hardness of a constant factor for unrestricted TSP unless P=NP; an ordinary decision reduction alone would not establish an approximation gap.</Prose>

    <H2>11. Check the decision in the real system</H2>
    <Prose>Choosing a method now becomes a series of concrete questions. Is the problem a structured exact family? Can a small exact solve establish a baseline? Does a valid relaxation provide a useful bound? Is there an approximation proof for the actual objective and constraints? If not, can an evaluated heuristic meet the application's needs with an independent feasibility checker?</Prose>
    <Prose>Mixed-integer linear programming combines integer decisions with linear constraints; practical solvers use methods including presolve, cuts and branching. Constraint programming can express detailed logical and scheduling rules. A heuristic such as local search changes a plan by a defined neighborhood—for example swapping two assigned jobs—and accepts improving moves. It can stop at a local optimum that is far from global. Simulated annealing sometimes accepts worsening moves; learned heuristics can propose choices or warm starts. None of those names supplies a quality theorem for a new application by itself.</Prose>
    <Prose>Consider an assignment selected using predicted travel times. If predicted costs are [[1,2],[2,1]], the diagonal plan costs 2 and is optimal. If realized costs are [[10,2],[2,10]], the same plan costs 20 while the off-diagonal costs 4. The optimization algorithm has not malfunctioned: the objective model changed. Validate predictions and realized outcomes separately from the optimizer's mathematical guarantee.</Prose>
    <LessonTable caption="Evidence to retain with an operational decision." headers={['Question', 'Concrete evidence']} rows={[['Is the plan legal?', 'Recompute every hard constraint from the returned choices, independently of the solver'], ['How good is its objective?', 'Return the feasible objective, valid bound, gap convention and termination reason'], ['How much work was required?', 'Measure elapsed time and memory on declared data and hardware; do not substitute asymptotic notation for a measurement'], ['Does randomness matter?', 'Report results across stated seeds and instances, including failures rather than only the best run'], ['Is the model useful?', 'Compare predicted and realized costs, service constraints and held-out changes'], ['What if a condition changes?', 'Recheck feasibility, reconsider assumptions and use a declared fallback or reoptimization policy']]} />
    <Prose>Hard constraints such as staffing legality, equipment capacity and mandatory coverage belong in the feasible set. If costs compete, such as hours versus service quality, specify their units and weights or retain a multiobjective tradeoff. A solver cannot decide which tradeoff the stakeholders meant. Confidence in a mathematical optimum is different from confidence that the model represents the real task.</Prose>

    <Practice title="1. Read an unfinished search" prompt="A maximizing search has incumbent value 72 and remaining subtree upper bounds 80, 69 and 77. Which subtree can be pruned, and what interval can you report if you stop?" hint="A subtree matters only if its optimistic value can beat a feasible incumbent.">
      <Prose>Prune the bound-69 subtree. The other two might improve the answer. Report 72≤OPT≤80 and maximum possible additive improvement 8. With positive upper bound 80, the candidate attains at least 72/80=.9 of OPT. The statement is an instance certificate, not a universal guarantee for your search policy.</Prose>
    </Practice>
    <Practice title="2. Separate a matroid from an interval rule" prompt="Give weights on [0,2), [0,1), [1,2) for which decreasing-weight feasible greedy fails. Then explain why an arbitrary feasible interval pair is not sufficient to prove the augmentation axiom." hint="The axiom quantifies over every pair of independent sets with different cardinalities.">
      <Prose>Weights 3,2,2 make greedy take the long interval for 3 while the two short intervals give 4. The smaller set containing only the long interval cannot accept either member of the larger two-short set. One successful pair would not prove a universal property; this one failed pair disproves it. An earliest-finish algorithm for maximum interval count solves a different, unweighted objective.</Prose>
    </Practice>
    <Practice title="3. Change a required assignment" prompt="Run the complete assignment helper on [[-4,2],[0,None]] with required size 1 and then 2. What should its optimal costs be, and why is the size-two path allowed to undo a negative-cost edge?" hint="List all legal full assignments. A refund is always the negation of the cost being removed.">
      <Prose>At size 1 choose A→job 1 for −4. At size 2, B must take job 1 and A must take job 2, for total 2. The augmenting assignment changes cost 0+4+2=6: removing the beneficial cost −4 raises cost by 4. A refund need not be negative when the original cost was negative. Expected stage costs are 0,−4,2; check both distinct-worker/job feasibility and exact size. The current optimum can become more expensive when a larger cardinality is required.</Prose>
    </Practice>
    <Practice title="4. Find an impossible coverage request" prompt="Adapt the weighted set-cover example so no set contains element 6. What should the full-cover helper report? What changes if the task instead allows two sets and maximizes coverage?" hint="Failure to cover a required element is not a very bad approximation of a feasible cover.">
      <Prose>The full-cover request is infeasible; the helper should raise its documented infeasibility error rather than loop or return a claimed full cover. The cardinality task is still feasible because uncovered elements are allowed. Its optimum is at most five covered elements. Evaluate the approximation against that changed optimum, not against a fictional six-element feasible cover.</Prose>
    </Practice>
    <Practice title="5. Test a weighted endpoint rule" prompt="A single edge joins vertices of costs 1 and 100. Why is 'select both endpoints of a maximal matching' not a factor-2 weighted-cover rule? What do LP rounding and the load-growing rule return?" hint="The unweighted proof charges numbers of vertices, not their arbitrary costs.">
      <Prose>Buying both costs 101 while OPT is 1, so the ratio is 101. The LP selects the cheap endpoint with x=1 and the expensive one with x=0, and rounding costs 1. Growing the edge load reaches 1, makes the cheap endpoint tight and selects it; lower bound and feasible cost both equal 1. The old cardinality proof cannot be transferred by replacing each vertex count with an unequal price.</Prose>
    </Practice>
    <Practice title="6. Inspect a dual certificate" prompt="On the triangle with costs 2,3,4, proposed edge loads on AB, BC, AC are 2,1,0. Check feasibility, give the lower bound, and decide whether the loads prove that the LP optimum equals 3." hint="A feasible dual is a lower bound. Optimality needs a matching upper certificate.">
      <Prose>Incident totals are A:2, B:3, C:1, all within the costs. Total edge load 3 is a valid lower bound. It does not prove LP optimality: the feasible fractional solution (.5,.5,.5) costs 4.5, and feasible loads AB=.5, BC=2.5, AC=1.5 total 4.5 and saturate all three budgets. Those equal primal/dual values certify LP optimum 4.5. The integer cover A+B costs 5.</Prose>
    </Practice>
    <Practice title="7. Prove a diminishing-return claim" prompt="Each test covers a fixed set of behaviors with nonnegative weights. Show that weighted coverage is submodular. Then give a two-test reward rule that is not submodular." hint="Write a test's marginal gain as the weights of behaviors it covers that the current suite does not.">
      <Prose>For a larger existing suite B, the still-uncovered behaviors in the new test form a subset of those still uncovered by A⊆B. Summing nonnegative weights over a subset cannot increase the sum, so the marginal gain decreases. For a counterexample, define reward 1 only when both tests p and q are selected and 0 otherwise. Adding q to the empty set gains 0, while adding it to &#123;p&#125; gains 1. Complementarity reverses the required inequality.</Prose>
    </Practice>
    <Practice title="8. Verify changed scaling contracts" prompt="Use items [(2,3),(3,4),(99,100000)] with capacity 3 and ε=1/4. Compute K and both useful rounded values. What should happen for zero capacity or ε=0?" hint="Remove the impossible item before calculating n and Vmax.">
      <Prose>The remaining n is 2 and Vmax=4, so K=.5. The rounded values are 6 and 8; the DP selects the second item, original value 4 and weight 3. Zero capacity removes both positive-weight items and returns the empty solution. ε=0 lies outside the approximation scheme's contract and is rejected; requesting exactness requires another algorithm or a finite precision argument. Test the actual helper's chosen indices, original capacity, guaranteed value and invalid-input behavior.</Prose>
    </Practice>
    <Practice title="9. Distinguish a PTAS from an FPTAS" prompt="Algorithm A uses n^(1/ε) operations. Algorithm B uses n³/ε operations on integers of polynomial bit length. Which is fully polynomial, and why does setting ε=2^(-n) still become expensive?" hint="Treat n and 1/ε as separate input parameters in the scheme definition.">
      <Prose>A is polynomial in n for any fixed ε, so this runtime fits a PTAS, but its exponent prevents a polynomial bound jointly in n and 1/ε. B has the FPTAS form. Setting ε=2^(−n) makes 1/ε exponential in n, so B's runtime also becomes exponential in n for that precision request. No contradiction with hard exact optimization follows.</Prose>
    </Practice>
    <Practice title="10. Repair the tour proof" prompt="A colleague doubles an MST in an arbitrary complete symmetric graph, shortcuts repeated vertices and claims a factor 2. Identify the missing assumption and construct a violated shortcut. Also say which step of Christofides needs general matching." hint="Completeness makes a direct edge available; it does not make it cheap.">
      <Prose>The missing assumption is the triangle inequality. Edges AB=1, BC=1 and AC=100 make shortcut A→C cost 100 instead of 2. Christofides requires a minimum-cost perfect matching on the MST's odd-degree vertex set; that set is not generally partitioned into prescribed left/right classes, so a bipartite assignment solver is insufficient. Exact enumeration can verify a tiny matching but does not supply the polynomial implementation.</Prose>
    </Practice>
    <Practice title="11. Make a scheduling model reviewable" prompt="Design a one-week staff schedule with coverage requirements, worker availability and a per-worker shift limit. State binary variables, constraints, an objective and one uncertainty source. Describe a small baseline and what you would require before using a larger solver result." hint="A valid answer makes feasibility independently checkable and separates the modeled cost from actual operational outcomes.">
      <Prose>Let xᵢₛ indicate worker i assigned to shift s. For every shift, require the sum over eligible workers to reach the declared staffing requirement; forbid unavailable assignments; bound each worker's total shifts and forbid overlapping shifts. Add required skill coverage as separate constraints when appropriate. Minimize declared wage/overtime costs, or state explicit weights for a service-quality tradeoff. An uncertainty source is a worker cancellation or a changed demand forecast.</Prose>
      <Prose>Enumerate a tiny instance and independently verify every returned schedule. Compare a larger exact/MILP or constraint-programming run with a simple feasible baseline and its valid objective bound, retaining termination reason, tolerances and measured runtime. Test cancellation scenarios, define when reoptimization is needed and specify a feasible fallback. Evaluate realized coverage and costs on held-out weeks. A solver's zero objective gap does not validate an inaccurate demand model.</Prose>
    </Practice>
    <Prose>You are ready to move on when you can define the actual feasible set, justify the direction of a bound, identify a structure that makes an algorithm exact, and reconstruct at least one approximation proof from its assumptions. Revisit the DSA lessons on <a href="/learn/path/full-curriculum/greedy-algorithms-exchange-arguments?module=data-structures-algorithms">greedy exchange arguments</a>, <a href="/learn/path/full-curriculum/dynamic-programming-states-transitions-optimization?module=data-structures-algorithms">dynamic programming</a> and <a href="/learn/path/full-curriculum/network-flow-minimum-cuts-bipartite-matching?module=data-structures-algorithms">flow and matching</a> for their guided implementation practice. The next topic in this module is <a href="/learn/path/full-curriculum/stochastic-processes-markov-chains-brownian-motion-poisson?module=math-foundations">Stochastic Processes</a>: it adds a language for evolving uncertainty, which a static deterministic objective does not represent.</Prose>
    <Sources alternatives={<p>For another teaching route, use <a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/resources/lecture-17-complexity-approximation-algorithms/" target="_blank" rel="noreferrer">MIT 6.046, lecture 17: Complexity and Approximation Algorithms</a>, also available as <a href="https://www.youtube.com/watch?v=MEz1J9wY2iM" target="_blank" rel="noreferrer">the official lecture video</a>. Its cover examples and approximation-scheme discussion are useful after the first two sections here. The official transcript provides a text route; use the precise PTAS/FPTAS definitions above when following the spoken discussion.</p>}>
      <li><a href="https://assets.cambridge.org/97805218/11514/sample/9780521811514ws.pdf" target="_blank" rel="noreferrer">Jon Lee, A First Course in Combinatorial Optimization, publisher sample</a> — the matroid and weighted-greedy sections develop augmentation, prefixes and bases. A deeper written route after the forest investigation.</li>
      <li><a href="https://theory.stanford.edu/~tim/w16/l/l5.pdf" target="_blank" rel="noreferrer">Tim Roughgarden, minimum-cost bipartite matching</a> and <a href="https://theory.stanford.edu/~tim/w16/l/l6.pdf" target="_blank" rel="noreferrer">generalizations to flow and general graphs</a> — alternating costs, potentials and the odd-cycle boundary; the optimized matching machinery goes beyond our straightforward Bellman–Ford implementation.</li>
      <li><a href="https://courses.csail.mit.edu/6.854/16/Notes/n10-mincostflow.html" target="_blank" rel="noreferrer">MIT 6.854, minimum-cost flow notes</a> — residual negative cycles, potentials and successive shortest paths. Read the starting-flow and reachability qualifications carefully when extending beyond an assignment network.</li>
      <li><a href="https://theory.stanford.edu/~tim/w16/l/l17.pdf" target="_blank" rel="noreferrer">Roughgarden, weighted set cover and duality</a> — the element-charge argument and its LP interpretation. Our statement retains exact harmonic numbers, including the singleton endpoint.</li>
      <li><a href="https://ocw.mit.edu/courses/6-854j-advanced-algorithms-fall-2008/a6b27d8a3d0ecda084f106f13b322676_lec16.pdf" target="_blank" rel="noreferrer">Michel Goemans, approximation lecture 16</a> — metric tours, weighted vertex-cover rounding and primal-dual loads. These older notes are used for the proofs, not as a claim about current best-known bounds.</li>
      <li><a href="https://theory.stanford.edu/~tim/w16/l/l15.pdf" target="_blank" rel="noreferrer">Roughgarden, approximation and maximum coverage</a> — the finite-k residual-gap proof and its monotone-submodular extension; useful for seeing exactly which assumptions support the guarantee.</li>
      <li><a href="https://www.cs.cornell.edu/courses/cs4820/2025sp/notes/31-Knapsack.pdf" target="_blank" rel="noreferrer">Cornell CS 4820, knapsack approximation notes</a> — handwritten value-scaling derivation. <a href="https://disco.ethz.ch/courses/hs22/coti/lecturenotes/script_compact.pdf" target="_blank" rel="noreferrer">Wattenhofer's computational-thinking text, section 2.10</a> offers a typed alternative with a different rounding convention; do not mix its upward-rounding proof with our downward-rounding code.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.linprog.html" target="_blank" rel="noreferrer">SciPy linprog documentation</a> — the ≤ inequality format, bounds, continuous variables and result status used by the LP program. Its numerical output supplements the exact proof; check the API for your installed version.</li>
    </Sources>
  </div>
};
