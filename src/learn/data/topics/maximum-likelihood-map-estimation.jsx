import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { BernoulliLikelihoodLab, LocationFitLab, BetaMapLab, SamplingEstimateLab, CoordinateModeFigure } from '../../components/lesson-labs/MaximumLikelihoodLabs.jsx';
import { maximumLikelihoodExamples as examples } from '../maximum-likelihood-examples.js';

function Practice({ prompt, hint, children }) {
  return <div className="lesson-check mle-practice"><p><strong>Try it independently.</strong> {prompt}</p><details><summary>Hint</summary>{hint}</details><details><summary>Show explained solution</summary>{children}</details></div>;
}

export default {
  title: 'Maximum Likelihood & MAP Estimation',
  readTime: '~60 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot maximum-likelihood-lesson">
    <LessonIntro prerequisites="Probabilities, probability densities, conditional independence and Bayes' rule from the preceding Probability lesson. We rebuild their roles here. Derivatives of logarithms and quadratics are needed for the short derivations; log-odds and the change-of-coordinate rule are introduced in the deeper section."
      sections={[
        ['1-fix-the-data-and-vary-the-explanation', 'Data, parameters and likelihood'],
        ['2-build-a-likelihood-you-can-actually-maximize', 'Log scores and boundary checks'],
        ['3-see-why-a-normal-fit-chooses-the-mean', 'Measurement geometry and variance'],
        ['4-add-a-prior-before-choosing-a-point', 'MAP, posterior mean and prediction'],
        ['5-connect-shrinkage-to-regularization', 'Normal priors and penalty scaling'],
        ['6-separate-an-estimate-from-its-uncertainty', 'Repeated samples and uncertainty'],
        ['7-look-deeper-a-mode-needs-a-coordinate', 'Optional MAP coordinate dependence'],
        ['8-check-whether-a-best-fit-even-exists', 'Support, identifiability and model limits'],
        ['9-practise-the-whole-estimation-workflow', 'Independent practice and next steps'],
      ]}>
      Four trial messages produce three successful deliveries and one failure. What success probability should you use for planning? The observed fraction is a candidate answer—but why that fraction, how should earlier knowledge influence it, and how much can four trials tell you? Build an estimation method from those questions, then transfer it to continuous measurements and machine-learning objectives.
    </LessonIntro>
    <Prose>The core route develops two methods: <strong>maximum likelihood estimation (MLE)</strong> chooses a parameter that best fits the observed data under a stated model; <strong>maximum a posteriori estimation (MAP)</strong> chooses a highest-density parameter after combining that evidence with a prior. A point estimate is the beginning of an analysis, not its uncertainty report. Section 7 is a deeper coordinate caveat. All seven Python programs are complete, use the standard library, and were checked with Python 3.12. Save one as <Code>example.py</Code> and run <Code>python example.py</Code>. Browser investigations evaluate bounded JavaScript models of the stated formulas.</Prose>

    <H2>1. Fix the data and vary the explanation</H2>
    <Prose>A <strong>parameter</strong> is a setting in a model: the success probability of a trial, the center of a measurement distribution, or the weights of a regression model. Write an unknown parameter as θ, pronounced “theta.” The parameter may be one number or a collection. Our first parameter is p, the chance of delivery success. Each observation is 1 for success or 0 for failure.</Prose>
    <Prose>Assume that, conditional on the same fixed p, the four trial outcomes are independent and each has the same success probability. This is an <strong>independent and identically distributed</strong>, or iid, Bernoulli model. The observed ordered data are <Code>[1, 1, 1, 0]</Code>. If p = 1/2, their probability is (1/2)³(1/2) = 1/16. If p = 3/4, it is (3/4)³(1/4) = 27/256, about .10547. The second candidate gives this sequence a larger probability.</Prose>
    <Prose>Once we hold the observations fixed and let p vary, this same probability formula becomes the <strong>likelihood function</strong>. It is a score for candidate parameter values based on their implications for the data. It does not say that p = .75 has probability .10547. Under this model, the sequence is 1.6875 times as likely at .75 as at .5; that ratio is evidence about fit, not posterior odds unless a prior comparison is also specified.</Prose>
    <MathBlock>{String.raw`\begin{gathered}L(p;D)=p^3(1-p),\quad 0\le p\le1.\\ \widehat p_{\mathrm{MLE}}\in\operatorname*{argmax}_{p\in[0,1]} L(p;D)\end{gathered}`}</MathBlock>
    <Prose><Code>argmax</Code> asks <em>which input attains the largest value</em>, rather than what the largest value is. The hat on p marks an estimate. We allow a set of answers because some datasets or models have several equally good maximizers.</Prose>
    <BernoulliLikelihoodLab />
    <Prose>There is a useful subtlety in the event selector. “Exactly three successes, in any order” includes four sequences. Its probability is four times that of our specific sequence. In general, the count of s successes among n trials has probability <Code>choose(n,s) pˢ(1−p)ⁿ⁻ˢ</Code>. The combinatorial factor counts which positions succeed. It changes the data event and its probability, but not the maximizing p, because that factor does not depend on p. Dropping such a factor is safe for finding this maximum or comparing candidate likelihood ratios; it is not safe if you report the full probability of a particular event.</Prose>
    <LessonTable caption="Three questions with different things held fixed" headers={['Question', 'Fixed', 'Varies']} rows={[
      ['What outcomes would this model produce?', 'Parameter p', 'Possible datasets; their probabilities sum to 1'],
      ['Which p best fits these observations?', 'Observed dataset', 'Candidate p; likelihood need not integrate to 1'],
      ['How uncertain are we about p after observing data?', 'Dataset and prior/model choices', 'Parameter under the posterior; density integrates to 1'],
    ]} />
    <Checkpoint prompt="Someone duplicates each of the four database rows ten times and multiplies all 40 Bernoulli factors. Is that forty trials of evidence?"><Prose>No. Copying a recorded outcome does not create a new trial. The copies are deterministically dependent. Treating them as independent raises the original likelihood to the tenth power and artificially concentrates the posterior. Repeated values can arise in independent trials, but independence comes from the observation process, not from how rows look.</Prose></Checkpoint>

    <H2>2. Build a likelihood you can actually maximize</H2>
    <Prose>Start with the joint probability mass or joint density of the actual observations. The iid model factors it into a product. If observations are dependent, use their joint model or an appropriate conditional factorization instead; the chain rule still works, but it does not let you discard dependence. For supervised learning with fixed inputs, one often models <Code>p(y₁,…,yₙ | x₁,…,xₙ,θ)</Code>, then states when the labels factor conditionally.</Prose>
    <MathBlock>{String.raw`\begin{aligned}L(\theta;D)&=\prod_{i=1}^{n} f(x_i\mid\theta),\\ \ell(\theta)&=\log L(\theta;D)\\ &=\sum_{i=1}^{n}\log f(x_i\mid\theta).\end{aligned}`}</MathBlock>
    <Prose>Here f is a probability mass function for discrete observations or a density for continuous ones. D denotes the full dataset; ℓ is a lowercase script ell for <strong>log-likelihood</strong>. A continuous density can exceed 1 and has measurement-dependent units. An exact continuous observation has probability zero, so a Gaussian likelihood compares densities, not the probability of hitting that exact real number.</Prose>
    <Prose>The natural logarithm is strictly increasing: among positive scores it preserves their order. Products become sums, and a product too tiny to represent can still have a manageable log score. Compute log terms directly; taking the logarithm after a product has already rounded to zero cannot recover the lost information. A genuinely impossible observation has likelihood zero and log-likelihood −∞.</Prose>
    <Prose>For s successes and f failures, ℓ(p) = s log p + f log(1−p). If both counts are positive, an interior maximum satisfies the derivative equation below. The second derivative is negative throughout (0,1), so this stationary point is the unique maximum there; the endpoints give zero likelihood.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\ell'(p)=\frac{s}{p}-\frac{f}{1-p}=0,\\ \widehat p=\frac{s}{s+f},\\ \ell''(p)=-\frac{s}{p^2}-\frac{f}{(1-p)^2}<0.\end{gathered}`}</MathBlock>
    <Prose>For our sequence the answer is 3/4. If f = 0 and s &gt; 0, the likelihood increases all the way to the allowed boundary p = 1. If s = 0 and f &gt; 0, its maximum is p = 0. With no data, the empty product is 1 for every p, and no unique MLE exists. A formula containing s/(s+f) must not silently divide by zero.</Prose>
    <Prose>The expression “0 log 0” in this calculation is defined by the zero count's contribution: no observations require that factor, so its log contribution is 0. A <em>positive</em> count multiplied by log 0 is −∞. The program separates these cases rather than letting floating-point arithmetic manufacture <Code>NaN</Code>.</Prose>
    <RunnableExample example={examples.bernoulli}><Prose>The .75 candidate maximizes the sequence and count likelihoods. The long example has 2,000 independent-model factors: direct multiplication underflows to 0, while the sum of logs remains about −1386.294. Logs solve this numerical problem; they do not establish independence or fix a wrong model.</Prose></RunnableExample>
    <Prose>This already gives a reusable workflow: specify observations and parameter support; write the appropriate joint mass/density; keep parameter-dependent terms; maximize the log score; check boundaries, existence and uniqueness; then interpret the result under the assumptions. For multi-parameter numerical optimization, inspect convergence and constraints and distinguish a local stationary fit from a proven global maximum. An optimizer's “success” flag is not a statistical adequacy test.</Prose>

    <H2>3. See why a normal fit chooses the mean</H2>
    <Prose>Now the data are measurements <Code>[2, 3, 4, 7]</Code>, in the same units. Suppose they are iid normal with unknown mean μ and a known positive variance σ². The Greek letter μ is the distribution's center; σ is its standard deviation, and σ² is its variance. A proposed center leaves a <strong>residual</strong> xᵢ−μ for each measurement.</Prose>
    <MathBlock>{String.raw`\begin{gathered}f(x_i\mid\mu,\sigma^2)\\ =\frac{1}{\sqrt{2\pi\sigma^2}}\exp\!\left(-\frac{(x_i-\mu)^2}{2\sigma^2}\right),\\ \ell(\mu)=-\frac n2\log(2\pi\sigma^2)\\ {}-\frac{1}{2\sigma^2}\sum_i(x_i-\mu)^2.\end{gathered}`}</MathBlock>
    <Prose>Because σ² is fixed, the first term is constant in μ. Maximizing this log-likelihood therefore minimizes the <strong>sum of squared errors</strong>, SSE = Σ(xᵢ−μ)². A residual twice as long contributes four times the squared cost. The measurement geometry below makes that tradeoff visible.</Prose>
    <LocationFitLab />
    <Prose>Differentiate SSE: its derivative is 2Σ(μ−xᵢ) = 2(nμ−Σxᵢ). It vanishes at the sample mean, written x̄ = Σxᵢ/n. Its second derivative is 2n &gt; 0 for n &gt; 0, so this is the unique minimizer. Our sample mean is 16/4 = 4; residuals are −2, −1, 0 and 3; their squares sum to 14. Notice the signed residuals sum to zero: positive and negative pulls balance at the mean.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\sum_i(x_i-\mu)^2&=\sum_i(x_i-\bar x)^2\\ &\quad+n(\mu-\bar x)^2.\end{aligned}`}</MathBlock>
    <Prose>This identity also proves the answer without relying only on a derivative test. The cross term vanishes because Σ(xᵢ−x̄) = 0. Every other center adds a nonnegative cost n(μ−x̄)².</Prose>
    <H3>Estimating variance uses a different denominator</H3>
    <Prose>If variance v = σ² is also unknown, with v &gt; 0, first choose μ = x̄. The remaining log score is −(n/2) log(2πv) − SSE/(2v). Its derivative in v is −n/(2v) + SSE/(2v²). For positive SSE, the maximum is v̂ = SSE/n. Here it is 14/4 = 3.5. This is the normal <em>variance MLE</em>.</Prose>
    <Prose>The familiar unbiased sample variance divides by n−1, giving 14/3 ≈ 4.667. It answers another design question: make the estimator's expectation equal the population variance over repeated iid samples. Estimating the mean from the same data has already reduced the residual sum of squares. In fact E[SSE] = (n−1)σ², so dividing by n is biased downward in finite samples. Being an MLE does not guarantee being unbiased.</Prose>
    <details><summary>Why the expected residual sum loses one variance</summary><Prose>For iid observations with finite variance, Σ(Xᵢ−X̄)² = Σ(Xᵢ−μ)² − n(X̄−μ)². The first sum has expectation nσ². Independence gives Var(X̄) = σ²/n, and X̄ has mean μ, so the subtracted term has expectation σ². What remains is (n−1)σ². Normality is needed for the normal-likelihood derivation, but not for this expectation identity.</Prose></details>
    <H3>A different noise model asks for a different center</H3>
    <Prose>A Laplace measurement model with fixed scale b &gt; 0 has density exp(−|x−μ|/b)/(2b). Its negative log-likelihood differs from Σ|xᵢ−μ|/b only by a constant. It therefore chooses a sample median. To see why, move a candidate slightly right: each point strictly to its left adds the movement to absolute cost, and each point strictly to its right subtracts it. A median balances those counts. For an even sample, every point between the two middle sorted observations minimizes the cost.</Prose>
    <Prose>For <Code>[2,3,4,7]</Code>, the entire interval [3,4] is optimal for absolute cost. Python's <Code>median</Code> returns its midpoint 3.5. Add 27: the mean moves to 8.6, whereas the median becomes 4. A very large residual grows quadratically under the normal model and only linearly under the Laplace model. That explains the mean's sensitivity without declaring one statistic universally better. If the target is expected load or expected financial loss, the mean may be the relevant quantity even for a skewed population; changing to a median changes the target as well as robustness.</Prose>
    <RunnableExample example={examples.location}><Prose>The outlier is an explicit changed dataset, not a hidden animation. Inspect provenance before discarding it: a sensor fault and a rare real demand spike require different responses. A model should explain the intended observation process and downstream decision.</Prose></RunnableExample>

    <H2>4. Add a prior before choosing a point</H2>
    <Prose>Four deliveries are a small basis for planning. Suppose previous knowledge makes success probabilities near the middle more plausible than values arbitrarily near 0 or 1. A <strong>prior density</strong> π(p) expresses that knowledge before these observations. Bayes' rule multiplies likelihood and prior, then divides by the total area to form the <strong>posterior density</strong>. We use π for a prior, reserving p for the success parameter.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\pi(p\mid D)=\frac{L(p;D)\pi(p)}{\int_0^1 L(u;D)\pi(u)\,du},\\ \widehat p_{\mathrm{MAP}}\in\operatorname*{argmax}_{p\in[0,1]}\pi(p\mid D).\end{gathered}`}</MathBlock>
    <Prose>u is just the integration variable. The denominator is one number once data and model are fixed, so finding a mode does not require calculating it. Reporting posterior probabilities does require a normalized posterior, explicitly or through a valid integration/sampling method. For positive densities, the MAP objective is log likelihood + log prior, up to a parameter-independent constant.</Prose>
    <Prose>A <strong>Beta(α,β)</strong> prior is convenient for a probability p. Its density is proportional to pᵅ⁻¹(1−p)ᵝ⁻¹ on [0,1], with α,β &gt; 0 controlling shape. Beta(1,1) is uniform in p. Beta(2,2) has density 6p(1−p), favoring the middle. Multiplying by s successes and f failures adds exponents, so the posterior is Beta(A,B) with A = α+s and B = β+f. A prior family that stays in the same family after updating is called <strong>conjugate</strong>; the later Bayesian lesson develops this more broadly.</Prose>
    <MathBlock>{String.raw`\begin{gathered}p^{s}(1-p)^f\,p^{\alpha-1}(1-p)^{\beta-1}\\ =p^{A-1}(1-p)^{B-1},\\ A=\alpha+s,\quad B=\beta+f.\end{gathered}`}</MathBlock>
    <Prose>Our three successes and one failure with Beta(2,2) give Beta(5,3), whose density is 105p⁴(1−p)². Differentiating its log density gives an interior mode (A−1)/(A+B−2) = 4/6 = 2/3. The MLE was .75; this prior pulls the mode toward .5. This interpretation is an explicit modeling choice, not evidence that the population truly centers on .5.</Prose>
    <BetaMapLab />
    <LessonTable caption="Three posterior summaries answer different questions" headers={['Summary for Beta(5,3)', 'Value', 'Interpretation']} rows={[
      ['Mode / MAP in p coordinates', '2/3', 'Where posterior density is highest per unit p'],
      ['Posterior mean E[p | D]', '5/8', 'Average p weighted by the posterior density'],
      ['Posterior predictive next success', '5/8', 'Integrate next-trial success chance p over the posterior'],
    ]} />
    <Prose>The predictive probability is ∫p π(p|D) dp, which equals the posterior mean A/(A+B). It is not generally the MAP. Prediction averages the possible parameter values' predictions; substituting one fitted parameter is a different approximation. Under squared error in p, the posterior mean also minimizes posterior expected loss: expanding E[(a−p)²|D] leaves Var(p|D) + (a−E[p|D])². Under absolute error it is a posterior median. MAP is a density summary and does not automatically solve either decision problem.</Prose>
    <Prose>The mode formula has conditions. For A,B &gt; 1 it is interior. With A = 1, B &gt; 1 it is at 0; with A &gt; 1, B = 1 it is at 1; with A = B = 1 every p has equal density. For example, four successes under a uniform prior give Beta(5,1): MAP = 1, but the next-success probability is 5/6. A continuous posterior still gives any single point probability zero, even when its density has a boundary maximum.</Prose>
    <details><summary>Deeper boundary case: shapes below one</summary><Prose>Positive noninteger Beta shapes are valid too. If A &lt; 1, the density diverges as p approaches 0; if B &lt; 1, it diverges toward 1. Such a density can be integrable while unbounded. When both are below one, both endpoints have singular peaks and the interior stationary point is a minimum. The elementary interior-mode formula is not a universal optimizer. State boundary/supremum conventions instead of replacing infinity by a plot cutoff and calling a grid point the MAP. The browser restricts prior shapes to positive integers so every displayed density is finite.</Prose></details>
    <RunnableExample example={examples.beta}><Prose>The exact fractions help keep three similar-looking expressions distinct. Prior “extra counts” are only a mnemonic with care: the exponents governing an interior mode are α−1 and β−1, while the posterior mean uses α and β. With fractional shapes, those exponents can even be negative; a prior is a density, not literally extra observations.</Prose></RunnableExample>
    <Checkpoint prompt="Two analysts use the same four observations and report MAP .75 and MAP 2/3. Must one have made an arithmetic error?"><Prose>No. A uniform Beta(1,1) prior gives the first answer, while Beta(2,2) gives the second. Ask for the likelihood, prior, parameter coordinate and optimization conditions before comparing estimates. If all of those agree, the numerical discrepancy then needs investigation.</Prose></Checkpoint>

    <H2>5. Connect shrinkage to regularization</H2>
    <Prose>Return to continuous measurements, now with known measurement variance σ² = 4. Give the unknown mean μ a normal prior with mean m₀ = 0 and variance τ² = 1. Two sources of information pull on μ: the sample's average and the prior's center. The reciprocal of a variance is called <strong>precision</strong>; smaller variance means stronger concentration.</Prose>
    <Prose>The μ-dependent negative log posterior is Σ(xᵢ−μ)²/(2σ²) + (μ−m₀)²/(2τ²). Expanding its two quadratics and completing the square shows the posterior is normal. Its precision is the sum of the data precision n/σ² and the prior precision 1/τ²; its mean is their precision-weighted compromise. Because a normal distribution is symmetric and unimodal, that mean is also its MAP.</Prose>
    <MathBlock>{String.raw`\begin{gathered}v_{\mathrm{post}}=\left(\frac n{\sigma^2}+\frac1{\tau^2}\right)^{-1},\\ m_{\mathrm{post}}=v_{\mathrm{post}}\left(\frac n{\sigma^2}\bar x+\frac1{\tau^2}m_0\right).\end{gathered}`}</MathBlock>
    <Prose>Our n = 4 measurements have x̄ = 4. Data precision is 4/4 = 1 and prior precision is 1/1 = 1, so the posterior mean/MAP is (1·4 + 1·0)/2 = 2, with variance 1/2. This is <strong>shrinkage</strong> toward the prior center. It does not mean individual measurements are replaced by 2. The posterior concerns the unknown shared μ.</Prose>
    <Prose>Hold this prior and measurement variance fixed and increase the number of genuinely informative iid observations: n/σ² grows, so the sample's weight increases. In other models, “more data washes out the prior” requires appropriate identifiability and regularity; duplicated observations, a changing parameter, or a prior that excludes relevant values can break the simple story.</Prose>
    <H3>Why L2 and L1 penalties appear</H3>
    <Prose>For regression, replace μ by predictions determined by weights w. With iid normal residuals of known variance σ² and an independent zero-centered normal prior of variance τ² on each penalized weight, MAP minimizes the following objective. The notation ‖w‖² means the sum of squared components.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\frac{\mathrm{SSE}(w)}{2\sigma^2}+\frac{\lVert w\rVert_2^2}{2\tau^2}\\ \Updownarrow\\ \mathrm{SSE}(w)+\frac{\sigma^2}{\tau^2}\lVert w\rVert_2^2\\ \Updownarrow\\ \frac{\mathrm{SSE}(w)}n+\frac{\sigma^2}{n\tau^2}\lVert w\rVert_2^2.\end{gathered}`}</MathBlock>
    <Prose>Multiplication by a positive constant preserves the optimizer. But switching from <em>sum</em> loss to <em>mean</em> loss without changing the penalty coefficient changes the implied prior strength. Also state whether an intercept is penalized, the units/scales of features, and whether σ² is fixed or estimated. A zero-centered prior is not a universal definition of “simple”: rescaling a feature changes the weight units and hence the prior expressed by the same numerical coefficient.</Prose>
    <Prose>A zero-centered Laplace prior on a scalar weight has density proportional to exp(−|w|/b). Its negative log contributes |w|/b, an L1 penalty. In the scalar normal-mean case, the data term becomes n(μ−x̄)²/(2σ²) plus a constant. Differentiating separately for positive and negative μ, and checking the corner at zero, gives <strong>soft thresholding</strong>: move x̄ toward zero by t = σ²/(nb), stopping at zero. This explains how a MAP can be exactly zero even though a continuous prior and posterior put no point mass there.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\widehat\mu_{\mathrm{MAP,Laplace}}\\ =\operatorname{sign}(\bar x)\max(|\bar x|-t,0),\\ t=\frac{\sigma^2}{nb}.\end{gathered}`}</MathBlock>
    <RunnableExample example={examples.shrinkage}><Prose>The program first verifies the normal posterior with τ² = 1, then deliberately uses τ² = 4 for the penalty comparison. The Gaussian-prior MAP is 3.2; a Laplace prior with b = 1 gives 3. For small enough |x̄|, the latter becomes exactly zero. General correlated-design L1 regression does not reduce to applying this scalar formula independently to arbitrary coefficients.</Prose></RunnableExample>
    <Prose><strong>Objective-level L2 is not interchangeable with every optimizer's “weight decay.”</strong> Adding an L2 term to the loss changes its gradient. Adam's adaptive moments and scaling act on that coupled gradient. AdamW applies a separate shrinkage update and generally follows a different trajectory. The <a href="/learn/path/full-curriculum/gradient-descent-variants-sgd-adam-adagrad-rmsprop-lamb-lars?module=math-foundations#6-separate-decay-from-the-data-gradient">earlier Gradient Variants investigation</a> makes that distinction concrete. A Gaussian-prior interpretation belongs to the specified objective and parameterization; it does not make a point-fit training run a complete Bayesian uncertainty analysis. <a href="https://arxiv.org/abs/1711.05101">The original AdamW paper</a> establishes the adaptive-optimizer distinction.</Prose>

    <H2>6. Separate an estimate from its uncertainty</H2>
    <Prose>After observing three successes in four trials, the MLE is the fixed number .75. Before running a new study, the rule “count successes and divide by n” is an <strong>estimator</strong>: a function of random data. Its output varies across possible studies. An estimator's <strong>sampling distribution</strong> describes that variation under a specified population/model.</Prose>
    <SamplingEstimateLab />
    <Prose>Let Xᵢ be independent Bernoulli(p) outcomes. Each has mean p and variance p(1−p). Their sample proportion is p̂ = ΣXᵢ/n. Linearity of expectation gives E[p̂] = p; independence makes variances add, giving Var(p̂) = p(1−p)/n. Thus this estimator is unbiased, and its standard deviation shrinks as 1/√n when p is fixed. “Unbiased” describes an average over repeated studies; it does not mean each realized estimate is correct.</Prose>
    <RunnableExample example={examples.sampling}><Prose>When true p = .5 and n = 4, getting p̂ = 1 still has probability 1/16. The point estimate 1 from that dataset does not prove future failure impossible. This calculation conditions on a stated true p; the Beta posterior instead conditions on data and a prior. Both are useful, but they are different distributions over different objects.</Prose></RunnableExample>
    <LessonTable caption="What remains after fitting a point" headers={['Need', 'Relevant object', 'What a point fit omits']} rows={[
      ['Repeat-study reliability', 'Sampling distribution, bias, standard error', 'How the fitted value varies with new data'],
      ['Parameter uncertainty under a prior', 'Posterior distribution and credible regions', 'Spread, asymmetry and multiple plausible regions'],
      ['Next-outcome uncertainty', 'Predictive distribution', 'Both outcome noise and, when integrated, parameter uncertainty'],
      ['Model adequacy', 'Residual/model checks and held-out predictions', 'Whether the chosen family describes the intended process'],
    ]} />
    <Prose>A sharply peaked likelihood can suggest strong information <em>within the model</em>. It cannot certify the independence assumption or rule out measurement bias. In regular identifiable models, suitably behaving MLEs often become approximately normal after centering and scaling as sample size increases. This is an asymptotic result with conditions, not a guarantee for every MLE, every boundary parameter, or a particular “large” n. The likelihood's curvature can then help approximate standard errors; the next lesson develops confidence intervals and testing rather than treating a peak width as an automatically calibrated interval.</Prose>
    <details><summary>A local curvature calculation, with its limits</summary><Prose>For an interior Bernoulli parameter, expected negative log-likelihood curvature is n/[p(1−p)], called Fisher information. Its reciprocal matches the exact sampling variance above. A common plug-in standard error is √[p̂(1−p̂)/n]. But at an all-success fit p̂ = 1 it returns zero even though a finite study has not established certainty. This is one reason to examine interval methods and their boundary behavior instead of using a normal approximation mechanically. Expected information, observed curvature and actual finite-sample coverage are related concepts, not interchangeable claims.</Prose></details>

    <H2>7. Look deeper: a mode needs a coordinate</H2>
    <Prose>Probability is often represented by <strong>log-odds</strong>, η = log[p/(1−p)], which maps p in (0,1) to any real number. Its inverse is p = 1/(1+exp(−η)). For likelihood maximization this is a one-to-one relabeling of candidate models: maximize L(p(η);D), and an interior MLE maps to the same fitted probability. No parameter Jacobian appears, because we have not changed the measure used to describe the observed data.</Prose>
    <Prose>A posterior <em>density</em> is different. Density means probability mass per unit of the chosen coordinate. Equal-width intervals in η correspond to different widths in p. To preserve the probability of corresponding intervals, the transformed density must include the absolute derivative |dp/dη| = p(1−p). That derivative is the one-dimensional <strong>Jacobian factor</strong>.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\pi_\eta(\eta\mid D)=\pi_p(p(\eta)\mid D)\left|\frac{dp}{d\eta}\right|,\\ \frac{dp}{d\eta}=p(\eta)(1-p(\eta)).\end{gathered}`}</MathBlock>
    <CoordinateModeFigure />
    <Prose>For Beta(5,3), the p-density is proportional to p⁴(1−p)² and peaks at 2/3. The η-density is proportional to p⁵(1−p)³, so its mode occurs at the η whose inverse probability is 5/8. This is the same posterior probability model described using a different density coordinate. Merely changing tick labels on the first curve would miss the required vertical adjustment.</Prose>
    <RunnableExample example={examples.coordinate}><Prose>The posterior probability of p between 1/2 and 3/4 is 8681/16384, about .52985. It is the same probability as η between 0 and log 3. Density modes can move while corresponding-event probabilities remain unchanged. This example's inverse-transformed η-mode happens to equal the p-posterior mean; that coincidence in this Beta/log-odds calculation is not a general identity for transformations or expectations.</Prose></RunnableExample>
    <Prose>State the parameter coordinate when reporting MAP, especially for scales, probabilities or constrained parameters. Choosing a flat prior in p and choosing a flat prior in η are also different prior choices. An unconstrained numerical search can still optimize the original p-density if that is its intended objective; including a Jacobian switches the density whose mode is sought. The <a href="https://mc-stan.org/docs/reference-manual/optimization.html">Stan optimization reference</a> explicitly distinguishes these modes. Bayesian inference is not broken by reparameterization; an isolated density maximum is a coordinate-dependent summary.</Prose>

    <H2>8. Check whether a best fit even exists</H2>
    <Prose>The neat derivative examples can create an unhelpful reflex: differentiate, set to zero, report a number. Four different problems can defeat that recipe, even before we consider whether a model predicts well.</Prose>
    <LessonTable caption="Different reasons a maximum needs more care" headers={['Example', 'What the likelihood does', 'Correct conclusion']} rows={[
      ['Bernoulli, all successes', 'Increases to p = 1', 'Finite boundary MLE, if [0,1] is allowed'],
      ['Normal mean and positive variance, all measurements identical', 'Grows without bound as variance tends to zero at that mean', 'No finite positive-variance MLE'],
      ['Normal mean represented as a + b', 'Same distribution for every pair with the same sum', 'Components are not identifiable from these observations'],
      ['Separated scalar logistic data', 'Approaches its supremum as weight grows without bound', 'No finite unregularized weight MLE'],
    ]} />
    <H3>Support can determine the answer</H3>
    <Prose>Suppose measurements are iid uniform on [0,θ], θ &gt; 0, using the density 1/θ on that closed interval and zero outside. For nonnegative observations with positive maximum m, the likelihood is θ⁻ⁿ when θ ≥ m and zero otherwise. It decreases on the allowed region, so its maximum is θ̂ = m. For <Code>[.2,.7,.4]</Code> this is .7: a smaller endpoint excludes an observed value; a larger one spreads density more thinly. There is no interior zero-derivative solution. The support condition is part of the model, not a detail to drop from the objective.</Prose>
    <H3>A ridge is not evidence for its individual coordinates</H3>
    <Prose>Imagine the normal mean is a sensor offset a plus a calibration offset b. Data likelihood depends only on a+b. Pairs (0,4), (1,3) and (2,2) all give exactly the same distribution and SSE = 14 for our sample. A parameter is <strong>identifiable</strong> when different allowed values lead to distinguishable data distributions. Here the sum is identifiable, but the two components separately are not. More observations of this same type cannot resolve the decomposition.</Prose>
    <Prose>A prior or a constraint can choose among such pairs, but the data have not thereby learned which physical component caused the offset. For example, among pairs whose sum is fixed at 4, equal L2 penalties favor (2,2). In a full penalized fit the optimal sum may shrink too. To identify the physical components, add information such as a separate reference measurement, or change the parameterization to the identifiable sum if that is all the task needs.</Prose>
    <H3>An optimizer can chase infinity</H3>
    <Prose>For four identical normal measurements, centering at that value makes SSE zero. The log-likelihood becomes −2 log(2πσ²), which increases without bound as σ² approaches zero. A zero variance is excluded from the positive-variance normal-density model. Calling it a valid Gaussian variance MLE hides the failure of existence.</Prose>
    <Prose>For a second example, use a binary-response model with success probability sigmoid(wx), where sigmoid(z) = 1/(1+exp(−z)). This is scalar logistic regression without an intercept. Observe x = −1 with label 0 and x = 1 with label 1. Larger positive w makes both observed labels more likely: their joint likelihood is sigmoid(w)². It approaches 1 as w tends to infinity, but no finite w attains 1. The data are perfectly separated by the sign of x.</Prose>
    <Prose>A N(0,1) weight prior adds −w²/2 to this log score. Its derivative is 2/(1+exp(w))−w, strictly decreasing, positive at 0 and negative at 2. Bisection on that bracket therefore finds its unique finite zero, approximately .674832. Bisection preserves a root bracket by testing its midpoint; it is justified here by continuity and strict monotonicity, not merely by trying a grid of weights.</Prose>
    <RunnableExample example={examples.failures}><Prose>The normal and logistic calculations demonstrate different nonexistence mechanisms: unbounded density versus a bounded supremum never attained. The offset pairs demonstrate nonuniqueness. These outcomes are meaningful statistical diagnoses, not reasons to silently clamp a parameter and report the clamp as the mathematical MLE.</Prose></RunnableExample>
    <H3>Fit the model to the intended question</H3>
    <Prose>MLE compares parameters <em>within</em> a specified family. A very flexible model can memorize noise and improve training likelihood while predicting new observations poorly. A carefully chosen prior can regularize it; a poor prior can distort it. Neither method corrects unmodeled dependence, selective sampling, measurement error or distribution shift automatically. Use held-out prediction checks, residual structure and relevant domain knowledge; distinguish model selection from parameter fitting.</Prose>
    <Prose>Applications include estimating a sensor's calibration center and noise scale, pooling sparse delivery outcomes with prior experience, fitting regression weights, and learning conditional token probabilities in a language model. In each case, write down what is conditioned on. A language model commonly factors a sequence into next-token conditional probabilities; tokens are not assumed mutually independent just because their log probabilities are summed. The chain-rule factorization and an iid factorization are different reasons a sum of log terms can arise.</Prose>

    <H2>9. Practise the whole estimation workflow</H2>
    <Prose>Work each problem before opening its hint. A complete answer states the model, allowed parameter values, derivation or numerical check, and what the result does not tell you. Change the provided programs and use the expected cases below to check your reasoning.</Prose>
    <Practice prompt="Five independent Bernoulli trials produce [1,0,1,0,1]. Compare p=.5 and p=.6, derive the MLE, and explain what changes if only the total count was recorded." hint={<Prose>Write p³(1−p)². The count event contains choose(5,3) sequences.</Prose>}><Prose>The ordered likelihoods are .03125 at .5 and .03456 at .6, so .6 fits better. The interior derivative gives 3/5; endpoints are impossible because both outcomes occurred. Count likelihood multiplies each score by 10, producing .3125 and .3456 without changing the argmax or ratio. Neither number is the probability that the parameter equals its candidate.</Prose></Practice>
    <Practice prompt="Under a Beta(3,2) prior, observe one success and three failures. Compute the posterior, MAP, mean and next-success probability. Then remove all observations and use a uniform prior: which answers still exist?" hint={<Prose>Add counts to shapes. Check the conditions before using the mode formula.</Prose>}><Prose>The posterior is Beta(4,5), with MAP 3/7 and mean/predictive probability 4/9. With no data and Beta(1,1), the posterior remains uniform. Every p is a mode, while its mean and predictive probability are both 1/2. There is no data-driven unique MLE.</Prose></Practice>
    <Practice prompt="For [1,2,6], derive the normal mean and variance MLE and the fixed-scale Laplace location MLE. Check the squared-cost identity at candidate center 4." hint={<Prose>The sample mean is 3. Keep normal variance MLE and unbiased variance separate.</Prose>}><Prose>The normal mean is 3; SSE = 4+1+9 = 14; variance MLE = 14/3 and unbiased variance = 7. The Laplace location is median 2. At center 4, squared cost is 9+4+4 = 17, equal to 14 + 3(4−3)². These are model-specific estimates, not competing arithmetic definitions of one center.</Prose></Practice>
    <Practice prompt="A training objective changes from SSE + 2‖w‖² to SSE/100 + 2‖w‖² for the same 100 examples. Did it preserve the MAP objective? Repair it, and explain what additional assumption is needed before calling this a Gaussian-prior fit." hint={<Prose>Scale the entire old objective by the same positive constant.</Prose>}><Prose>No. Keeping coefficient 2 makes the penalty 100 times stronger relative to the data. Use SSE/100 + .02‖w‖² to preserve the minimizer. The Gaussian-prior interpretation also needs the stated normal residual model/variance, prior scale, feature and parameter units, and which weights are penalized. An arbitrary adaptive-decay update is not automatically minimization of that objective.</Prose></Practice>
    <Practice prompt="For true p=.2 and n=10 iid Bernoulli trials, find the expected sample proportion and its variance. Explain why observing all failures does not justify a zero uncertainty report." hint={<Prose>Use p(1−p)/n and consider the probability of that dataset under p=.2.</Prose>}><Prose>The expectation is .2 and variance .016, with standard deviation about .12649. All failures have probability .8¹⁰ ≈ .10737 even at true p=.2. That dataset's MLE is 0, but a plug-in normal standard error of zero at the boundary is not reliable evidence of certainty. An interval procedure with appropriate boundary behavior or a stated posterior analysis is needed.</Prose></Practice>
    <Practice prompt="For a Beta(3,4) posterior, compare the p-density mode with the probability corresponding to the log-odds-density mode. Must interval probabilities change too?" hint={<Prose>The Jacobian adds one to each power of p and 1−p.</Prose>}><Prose>The p-density mode is (3−1)/(3+4−2) = 2/5. The log-odds-density is proportional to p³(1−p)⁴, so its mode maps back to p = 3/7. Corresponding interval probabilities are unchanged when the Jacobian is included; density maxima are different summaries in different coordinates.</Prose></Practice>
    <Practice prompt="An analyst fits a normal model with unknown positive variance to [5,5,5] and reports μ=5, variance=0 as a valid maximizer. Another fits mean a+b and reports a=2,b=3 as uniquely learned. Diagnose both claims." hint={<Prose>Separate existence in the allowed parameter space from identifiability of a decomposition.</Prose>}><Prose>The first likelihood grows without bound as positive variance tends to zero; zero lies outside the normal-density parameter domain, so there is no finite valid variance MLE. The second data model sees only a+b. Every pair with the same sum has the same likelihood. A prior or arbitrary optimizer choice may select one, but it is not uniquely identified by these observations.</Prose></Practice>
    <Practice prompt="Extend the stable Bernoulli program to return all of: success/failure counts, log score at a requested p, MLE or an explicit nonunique result, and Beta posterior summaries for integer prior shapes. Test invalid input and boundary cases before trying a larger dataset." hint={<Prose>Keep the likelihood's no-data case separate from a prior that may still have a mode. Do not turn −∞ into a finite clipped score.</Prose>}><Prose>Required acceptance cases: <Code>[1,0]</Code> at p=.5 has log score −2 log 2 and MLE .5; <Code>[1,1]</Code> at p=1 has log score 0 and MLE 1; <Code>[1,0]</Code> at p=1 is impossible; empty data has a flat likelihood; empty data with Beta(2,3) still has posterior mode 1/3 and mean 2/5. Reject nonbinary observations, out-of-range p, negative counts and invalid prior shapes. Check that replacing a sequence score with a count score adds log choose(n,s) while leaving the MLE unchanged.</Prose></Practice>
    <Prose>You are ready to move on when you can distinguish likelihood, posterior and sampling distribution without relying on their similar-looking curves; derive the two basic MLEs; explain the prior and scale behind a MAP penalty; and recognize a boundary, a ridge or an unattained optimum. The next module topic is <a href="/learn/path/full-curriculum/hypothesis-testing-confidence-intervals?module=math-foundations">Hypothesis Testing &amp; Confidence Intervals</a>, where point estimates become part of calibrated statements about uncertainty and evidence. After that, <a href="/learn/path/full-curriculum/bayesian-inference-conjugate-priors?module=math-foundations">Bayesian Inference &amp; Conjugate Priors</a> develops full posterior reasoning beyond MAP.</Prose>
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/res-6-012-introduction-to-probability-spring-2018/resources/maximum-likelihood-estimation/">MIT OpenCourseWare: 20.9 Maximum Likelihood Estimation</a> — John Tsitsiklis's official video page offers a spoken introduction to the likelihood viewpoint. Useful after sections 1–2; the page and description were reviewed, not the full video playback.</li>
      <li><a href="https://web.stanford.edu/class/archive/cs/cs109/cs109.1226/lectures/21_mle_annotated.pdf">Stanford CS109: Maximum Likelihood Estimation, 2022 lecture slides</a> — another worked route through Bernoulli, uniform-support and normal estimates. Use this lesson's explicit existence and regularity qualifications when reading compact slide summaries.</li>
    </ul>}>
      <li><a href="https://www.cs.cornell.edu/courses/cs4780/2018fa/lectures/lecturenote04.html">Cornell CS4780: MLE and MAP estimation</a> — primary course notes connecting coin likelihoods, Beta updating and predictive averaging. The boundary and no-data cases here make the scope of the interior formulas explicit.</li>
      <li><a href="https://www.stat.umn.edu/geyer/f21/5421/notes/likelihood.html">Charles Geyer, University of Minnesota: Likelihood Inference</a> — deeper notes on likelihood ratios, local versus global maxima and the conditions behind asymptotic inference. Appropriate after the core route.</li>
      <li><a href="https://mc-stan.org/docs/reference-manual/optimization.html">Stan Reference Manual: Optimization</a> — the constrained/unconstrained mode distinction motivates the coordinate warning. Current documentation was reviewed; this lesson's example uses a self-contained analytic transformation rather than requiring Stan.</li>
      <li><a href="https://arxiv.org/abs/1711.05101">Loshchilov and Hutter: Decoupled Weight Decay Regularization</a> — original paper distinguishing adaptive weight decay from coupled L2 regularization. The abstract and the earlier locally verified optimizer lesson support the objective/update qualification; no performance benchmark is reproduced here.</li>
    </Sources>
  </div>,
};
