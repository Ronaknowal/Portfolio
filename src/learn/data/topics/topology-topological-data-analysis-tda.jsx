import { H2, H3, Prose, Code, Callout } from '../../components/content';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram';
import libraryOutput from '../topology-tda-library-output';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { ShapeEquivalenceFigure, ComplexBoundaryLab, RipsFiltrationLab, CechRipsFigure, BoundaryReductionLab, DiagramMatchingLab, PixelFiltrationLab, DelayEmbeddingFigure, PersistenceRepresentationLab, MapperLab } from '../../components/lesson-labs/TopologyTdaLabs.jsx';
import { topologyTdaExamples as examples } from '../topology-tda-examples.js';
function Example({
  example
}) {
  return <section><Prose><strong>Before running.</strong> {example.question}</Prose><RunnableExample example={example} /><Prose>{example.interpretation}</Prose></section>;
}
function Practice({
  title,
  prompt,
  hint,
  children
}) {
  return <section className="lesson-check"><h3>{title}</h3><Prose>{prompt}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Show explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Topology & Topological Data Analysis (TDA)',
  readTime: '~110 min read + 4–5 hours practice',
  content: () => <div className="topology-tda-lesson">
    <LessonIntro prerequisites="Be comfortable with sets of vertices, graph connectivity, vectors and matrix rank. Set inclusion and arithmetic over the two-element field are introduced here. Python is useful for reproducing programs; no prior algebraic topology course is assumed." sections={[['1-decide-what-counts-as-the-same-shape', 'Shapes and continuity'], ['2-build-a-space-from-observations', 'Cells, Rips and Čech'], ['3-distinguish-a-cycle-from-a-hole', 'Chains, boundaries and homology'], ['4-follow-a-feature-through-scale', 'Filtrations and persistence'], ['5-compute-the-pairing', 'A complete finite algorithm'], ['6-compare-diagrams-and-bound-change', 'Matching and stability'], ['7-let-pixels-build-their-own-space', 'Cubical image filtrations'], ['8-turn-time-into-coordinates', 'Delay embedding'], ['9-build-usable-topological-features', 'Landscapes and images'], ['10-test-the-claim-against-data', 'A reproducible evaluation'], ['11-summarize-overlap-with-mapper', 'Lens, clusters and nerve'], ['12-know-what-the-summary-leaves-out', 'Deeper branches'], ['13-practise-calculation-and-judgment', 'Independent practice']]}>Can a collection of measurements reveal a loop—and can we tell whether that loop is meaningful? Start with four points, build the object being analyzed, and follow every step from boundary cancellation to a practical data experiment. Read in several sittings: understand shapes and homology first, then persistence and computation, then applications and practice.</LessonIntro>

    <H2>1. Decide what counts as the same shape</H2>
    <Prose>Imagine a sensor recording two coordinates of a repeating motion. The points appear to surround an empty region. Is the process following a cycle? Did the sampling leave a gap by chance? Did a plotting method create the apparent ring? These are different questions. <strong>Topology</strong> studies properties of spaces that survive appropriate notions of continuous equivalence. <strong>Topological data analysis</strong> builds explicitly defined spaces and summaries from observations, then asks what those summaries tell us.</Prose>
    <Prose>Before counting holes, choose the object. A circle means its circumference; a disk includes its interior. A drawing of an unfilled triangle means three edges unless a face is explicitly included. A finite collection of distinct points, with the usual distances, is a discrete space: each point has a sufficiently small neighborhood containing no other point. The observed points do not already contain the connecting lines that we draw. Those connections are a modeling decision.</Prose>
    <ShapeEquivalenceFigure />
    <H3>A continuous map need not be reversible</H3>
    <Prose>A map is <strong>continuous</strong> when points sufficiently close to a given input map sufficiently close to its output. More generally, the inverse image of every open set must be open. A <strong>homeomorphism</strong> is a continuous bijection with a continuous inverse. It pairs the spaces without tearing, identifying different points, or losing the neighborhood structure. Merely saying “continuous deformation” can conceal the crucial inverse requirement.</Prose>
    <Prose>For a&gt;0, the plane map (x,y) ↦ (x,ay) takes a circle to an ellipse. Its inverse is (u,v) ↦ (u,v/a). It preserves the topological type while changing distances. At a=0 the upper and lower circle points with the same x-coordinate collapse together. The map remains continuous, but is no longer a homeomorphism. Thus a topological equivalence does not preserve every distance-based persistence number, and a continuous map alone does not preserve even the existence of a loop.</Prose>
    <H3>What “open” and “nearby” mean</H3>
    <Prose>A metric d supplies open balls B(x,r)=&#123;y:d(x,y)&lt;r&#125;. A set is open if each of its points has an open ball staying inside that set. A <strong>topology</strong> generalizes this: specify a collection of open subsets containing the empty set and the whole space, closed under arbitrary unions and finite intersections. “Closed” means its complement is open. A set can be both open and closed; these are not opposites in the sense of an exclusive choice.</Prose>
    <Prose>The <strong>interior</strong> of A is the largest open set inside A. Its <strong>closure</strong> contains A and all points whose every neighborhood meets A. Its boundary is closure(A) minus interior(A). In the real line, (0,1) has closure [0,1] and boundary &#123;0,1&#125;; the singleton &#123;0&#125; has empty interior and is its own boundary. In a different ambient space, the answers can change—as the relative interval in the figure shows.</Prose>
    <Prose>A space is <strong>connected</strong> if it cannot be separated into two nonempty disjoint relatively open pieces. It is <strong>path-connected</strong> if any two points can be joined by a continuous path within the space. Path-connected implies connected; the converse fails for some general spaces. In the finite complexes used below, their components agree, so graph-style reachability is appropriate. That convenient finite setting is part of the calculation.</Prose>
    <Prose><strong>Compactness</strong> means that every open cover has a finite subcover. In Euclidean space, compact sets are exactly closed and bounded sets. This shortcut is not a universal definition in every space. Finite geometric complexes are compact, and a continuous real function on a compact space attains a finite minimum and maximum. These facts help make finite-scale constructions manageable.</Prose>
    <H3>A weaker equivalence: allow a contraction</H3>
    <Prose>A <strong>homotopy</strong> is a continuous family of maps, indexed by t from 0 to 1. A disk contracts to its center by H(x,t)=(1−t)x. The intermediate maps stay in the disk. The disk is therefore homotopy-equivalent to a point, although it cannot be homeomorphic to one: a bijection would already fail. A circle cannot be contracted to a point while remaining in that circle. Allowing its surrounding disk would change the space in the question.</Prose>
    <Prose>More precisely, spaces X and Y are homotopy-equivalent if maps f:X→Y and g:Y→X have compositions homotopic to the relevant identity maps. A homeomorphism supplies this immediately; a homotopy equivalence allows more information to be forgotten. Homology, introduced shortly, respects homotopy equivalence. Equal homology does not conversely guarantee either kind of equivalence.</Prose>
    <Checkpoint prompt="You shrink a disk to its center. Have you found a homeomorphism because every intermediate map is continuous?"><Prose>No. The final collapse identifies different disk points and has no inverse. You have exhibited a contraction, which establishes homotopy equivalence with a point. Continuity, homeomorphism and homotopy equivalence make different claims.</Prose></Checkpoint>
    <Prose><strong>Run the examples.</strong> Save each complete program in a separate Python file. The mechanism examples use only the standard library and were executed with Python 3.12.14. The GUDHI bridge below additionally uses NumPy and GUDHI and supplies its own install command. Browser investigations use their bounded JavaScript models; they do not run the displayed Python. Coordinates and thresholds in the toy point examples are dimensionless unless an application supplies units. The construction and units must travel together when real data replaces the toy data.</Prose>

    <H2>2. Build a space from observations</H2>
    <Prose>A <strong>simplex</strong> is the basic building block: one vertex is a 0-simplex, two joined vertices form an edge or 1-simplex, three form a filled triangle or 2-simplex, and four form a filled tetrahedron or 3-simplex. Abstractly, record a simplex by its vertex set. Every nonempty subset must also be present as a face. The collection is a <strong>simplicial complex</strong>. A triangle without its edges is invalid, while three edges without the filled triangle are perfectly valid.</Prose>
    <Prose>The dimension counts how many independent directions the simplex contains, not how many coordinates we used to display it. A tetrahedron can be projected onto a screen, but overlapping triangle drawings do not make its interior a planar region. Likewise, crossing edge strokes do not create a new vertex unless the complex declares one. Data structure and drawing must describe the same intended object.</Prose>
    <H3>Vietoris–Rips: fill every sufficiently close clique</H3>
    <Prose>Choose a distance and a threshold ε. The <strong>Vietoris–Rips complex</strong> contains every vertex set whose largest pairwise distance is at most ε. Thus an edge appears when its two endpoints are close enough; a triangle appears when all three of its edges qualify. There is no extra “fill triangle” parameter. We use the <strong>diameter ≤ ε</strong> convention throughout this lesson. Some sources instead use edge distance ≤2r and call r the scale.</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
D(\sigma)&=\max_{x,y\in\sigma}d(x,y),\\
\sigma\in\operatorname{Rips}_{\varepsilon}(P)
&\Longleftrightarrow D(\sigma)\le\varepsilon.
\end{aligned}
`}</MathBlock>
    <Prose>Each simplex enters at its diameter; a vertex enters at zero. Every face has no larger diameter than its coface, so the construction automatically respects the face rule. Raising ε only adds cells. Uniformly multiplying coordinates by c&gt;0 multiplies all finite entry values by c. Changing the relative scale of just one coordinate can change them nonuniformly. Mixing meters with milliseconds without a justified metric would change the answer before topology even enters the calculation.</Prose>
    <H3>Čech: ask for one point common to all the balls</H3>
    <Prose>Another construction places a closed Euclidean ball of radius ε/2 at each sample. The <strong>Čech complex</strong> contains a simplex when the corresponding balls have a common intersection. This is the <strong>nerve</strong> of the ball collection: sets become vertices, and nonempty common intersections become simplices. Every common intersection gives pairwise intersections, but the converse fails.</Prose>
    <CechRipsFigure />
    <Prose>For an equilateral triangle of side 2, its altitude is √3 and its center lies two-thirds of that altitude from each vertex, at distance 2/√3. Pairwise unit balls already touch when ε=2. A point common to all three first exists when their radius reaches 2/√3, so Čech inserts the face at ε=4/√3. Rips filled it earlier, using only the three pairwise tests.</Prose>
    <Prose>Why does a nerve help? In the finite convex-ball setting, a nerve theorem identifies the homotopy type of the union with the nerve. Convex nonempty intersections are contractible, supplying the needed condition. An arbitrary collection of sets or arbitrary clustered data does not automatically meet such conditions. The construction is useful because it replaces a continuous union with combinatorial cells under stated assumptions.</Prose>
    <details><summary>Choosing another complex: alpha, graph and dissimilarity inputs</summary><Prose>In Euclidean geometry, an alpha complex uses ball regions restricted to their Voronoi cells—the locations at least as close to that site's sample as to the others—and takes the corresponding nerve. This can provide a smaller geometric representation. Do not define every alpha face's entry time solely by its own circumradius: cofaces and restricted intersections matter. Libraries may report radius or squared radius, so inspect that convention before comparing scales.</Prose><Prose>A graph can supply edges with a distance-like or affinity value, followed by a clique construction; weighted graph filtrations need a declared direction and face-entry rule. A symmetric dissimilarity that violates the triangle inequality can still define a valid clique filtration through maximum pair values. It does not thereby become a metric, and metric perturbation theorems cannot be invoked without checking their assumptions. Choose the construction for the measured object rather than forcing every dataset into a Euclidean point-cloud story.</Prose></details>

    <H2>3. Distinguish a cycle from a hole</H2>
    <Prose>Walking around three edges returns you to your start. If their triangle is filled, the same walk is the edge of a filled region; if it is empty, it surrounds a missing region. Counting graph cycles alone cannot distinguish these cases. We need an operation that knows which higher-dimensional cells can fill a cycle.</Prose>
    <H3>First learn the arithmetic of cancellation</H3>
    <Prose>A <strong>k-chain</strong> selects k-dimensional cells with coefficients in a chosen field. Here use F₂=&#123;0,1&#125;: a cell is absent or present, and 1+1=0. Adding chains is symmetric difference—cells occurring in both cancel. We choose this field to remove orientation signs while keeping exact algebra. It is not ordinary arithmetic rounded to 0 or 1.</Prose>
    <Prose>The <strong>boundary</strong> ∂ of an edge &#123;0,1&#125; is its endpoints &#123;0&#125;+&#123;1&#125;. The boundary of triangle &#123;0,1,2&#125; is its three edges &#123;0,1&#125;+&#123;0,2&#125;+&#123;1,2&#125;. Apply the operation linearly to a chain. For edges &#123;0,1&#125;+&#123;1,2&#125;, vertex 1 appears twice and cancels, leaving vertices 0 and 2. For the whole triangular edge cycle, every vertex appears twice and the result is zero.</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
\partial\{0,1,2\}&=\{0,1\}\\
&\quad+\{0,2\}+\{1,2\},\\[4pt]
\partial^2\{0,1,2\}&=(\{0\}+\{1\})\\
&\quad+(\{0\}+\{2\})\\
&\quad+(\{1\}+\{2\})=0.
\end{aligned}
`}</MathBlock>
    <Prose>This is not special to one triangle. Each codimension-two face of a simplex arises twice when removing two different vertices in the two possible orders. The copies cancel over F₂. Therefore <strong>the boundary of a boundary is always zero</strong>: ∂ₖ∂ₖ₊₁=0. Over other fields, consistent orientations and alternating signs give the corresponding cancellation.</Prose>
    <ComplexBoundaryLab />
    <H3>Cycles, boundaries and classes</H3>
    <Prose>A <strong>cycle</strong> has zero boundary. A <strong>boundary</strong> is the boundary of a chain one dimension higher. Since ∂²=0, every boundary is a cycle. The reverse need not hold; that gap is precisely what homology measures. Two cycles represent the same class if their difference is a boundary. Over F₂ their difference equals their sum.</Prose>
    <Prose>For the unfilled triangle, all three edges form a nonzero cycle and no face can fill it. After adding the face, those exact edges still form a cycle, but now equal its boundary. The resulting homology class is zero. With a triangulated annular region, two loops running along different edges can represent the same class: the strip of triangles between them has their sum as its boundary. A representative is not a unique geometric outline.</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
Z_k&=\ker\partial_k,\\
B_k&=\operatorname{im}\partial_{k+1},\\
H_k&=Z_k/B_k.
\end{aligned}
`}</MathBlock>
    <Prose>The kernel is the vector space of chains whose boundary is zero. The image contains boundaries produced by higher chains. The quotient groups cycles together when they differ by such a boundary. Since Bₖ lies inside Zₖ, this quotient is well-defined. Its dimension over the chosen field is the <strong>Betti number</strong> βₖ: the number of independent classes, not the number of every possible walk or every drawn empty patch.</Prose>
    <Prose>With nₖ k-simplices, the chain space has dimension nₖ. Rank–nullity gives dim ker∂ₖ=nₖ−rank∂ₖ. Quotienting removes rank∂ₖ₊₁ independent boundary directions. Therefore:</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
r_k&=\operatorname{rank}_{\mathbb F_2}\partial_k,\\
\beta_k&=n_k-r_k-r_{k+1}.
\end{aligned}
`}</MathBlock>
    <LessonTable caption="Read the count only after choosing the cells and field" headers={['Complex', '(β₀, β₁, β₂)', 'Reason']} rows={[['Three triangle edges', '(1, 1, 0)', 'One component, one independent unfilled edge cycle.'], ['Filled triangle', '(1, 0, 0)', 'Its face makes the same cycle a boundary.'], ['Tetrahedron shell: four faces', '(1, 0, 1)', 'All four faces form a 2-cycle; there is no 3-cell filling it.'], ['Filled tetrahedron', '(1, 0, 0)', 'The 3-cell makes the shell a boundary.']]} />
    <Prose>For a graph without faces, rank∂₁=V−c, where c is its component count. Hence β₁=E−V+c. Once faces are present, their independent boundary rank must also be subtracted. Likewise, computing Hₖ generally needs cells through dimension k+1. A tetrahedral shell is the 2-skeleton of a filled tetrahedron. Omitting the 3-cell creates an H₂ class that would not exist in the full simplex.</Prose>
    <Prose>The <strong>Euler characteristic</strong> χ is the alternating sum of cell counts, and also of Betti numbers: V−E+F−…=β₀−β₁+β₂−…. This is a useful consistency check, but one alternating sum cannot recover several unknown Betti numbers. In the planar image example later, an additional fact—H₂=0—will make the formula especially useful.</Prose>
    <Example example={examples.boundaries} />
    <Checkpoint prompt="A square has its four perimeter edges and a diagonal, with no faces. What is β₁? What if you then insert one of its two triangular faces?"><Prose>The graph has V=4, E=5 and c=1, so β₁=2. One face contributes one nonzero independent boundary column, reducing β₁ to 1. The filled triangular side is no longer an independent hole; the remaining cycle survives. Adding the second face reduces β₁ to 0.</Prose></Checkpoint>

    <H2>4. Follow a feature through scale</H2>
    <Prose>A single distance threshold can be arbitrary. A <strong>filtration</strong> studies a nested sequence of spaces: Kₐ is contained in Kᵦ whenever a≤b. For Rips, increasing ε creates exactly such a sequence. Inclusions carry earlier cycles into later spaces. A cycle may continue to represent a nonzero class, merge with another class, or become a boundary after new cells arrive.</Prose>
    <Prose><strong>Persistent homology</strong> studies those induced maps between homology groups. It asks which classes survive, not only how many exist at each snapshot. This matters even when the counts are identical. Suppose K₀ has one triangular edge loop plus three isolated vertices. At K₁, fill that old loop and connect the other three vertices into a new triangular edge loop. β₁ is 1 at both times, but the old class maps to zero. The new class did not exist at K₀.</Prose>
    <H3>The square gives a complete hand calculation</H3>
    <Prose>Take corners (−1,−1), (1,−1), (1,1), (−1,1). Adjacent distances are 2; diagonal distances are 2√2. Below ε=2, the four vertices are separate. At ε=2, all four perimeter edges arrive: they connect the vertices and produce one unfilled loop. At ε=2√2, both diagonals and all qualifying faces arrive. The loop becomes a boundary.</Prose>
    <RipsFiltrationLab />
    <Prose>A <strong>barcode</strong> records each persistent class as an interval [birth,death). Half-open means the class is present at birth but absent at death, matching the sublevel convention. The square has three finite H₀ bars [0,2), one H₀ bar [0,∞), and one positive H₁ bar [2,2√2). H₀ tracks components: three independent differences between initially separate components disappear when the graph connects. Which tied vertex is called the survivor is a basis choice.</Prose>
    <Prose>A <strong>persistence diagram</strong> plots each finite interval at (birth,death), above the diagonal. Its vertical separation death−birth is the interval's persistence. Multiple identical intervals remain multiple points, even if plotted at the same coordinates. A diagram is a multiset, not a set that removes duplicate entries.</Prose>
    <Prose>Equal-distance cells arrive at the same scale. An implementation still needs an order for processing them, with every face before its cofaces. Different legal tie orders can change representatives and produce different zero-length bookkeeping pairs. They do not change the positive-duration persistence of the tied filtration. Do not present every internal column operation as a new physical scale.</Prose>
    <Callout label="Infinite versus not yet dead">An essential bar survives the full filtration being defined. A bar still alive when you stop computation at an arbitrary cutoff is only known to survive that cutoff. Without information about the continuation, an “infinite” output placeholder may mean right-censored, not mathematically essential. Here the bounded Rips examples explicitly compute their whole finite H₀/H₁ filtration.</Callout>
    <Prose>The sampled filled-region fixture in the lab can contain short loops between its grid points, even though the intended continuous region has no hole. Those loops belong to a constructed finite complex at certain scales. They illustrate why “a loop appears” is not yet a conclusion about the underlying population. Sampling density, metric, dimensional truncation and noise belong in the interpretation.</Prose>

    <H2>5. Compute the pairing</H2>
    <H3>Start with components and a familiar graph algorithm</H3>
    <Prose>For H₀, join nearby vertices and maintain components with Union-Find. The preserved program starts from four points on a line. Two short gaps create pairs; the larger gap between the pairs delays their final connection. This is the same chaining behavior used by single-linkage clustering.</Prose>
    <Example example={examples.original} />
    <Prose>If all vertices are born at zero, the finite H₀ death values of the complete weighted graph are the edge weights accepted by a minimum spanning tree, with multiplicity. Kruskal's algorithm adds an edge precisely when it joins previously separate components. A sparse graph may instead produce a spanning forest and several essential components. This links persistence to the earlier Union-Find and spanning-tree lessons, but H₁ additionally needs cycles and face boundaries.</Prose>
    <H3>Reduce the boundary matrix while respecting time</H3>
    <Prose>List simplices by increasing filtration value, then dimension, then deterministic vertex order. Build one boundary column per simplex; the rows use that same list. For an edge column, two rows contain 1; for a face, three edge rows contain 1. Entries above the column's own position express the fact that faces precede cofaces.</Prose>
    <Prose>Process columns from left to right. For a nonzero column, call the greatest row index containing 1 its <strong>pivot</strong>. If an earlier reduced column already has that pivot, XOR the earlier column into the current one. The equal pivot entries cancel. Repeat until the column becomes zero or has a pivot no earlier reduced column owns.</Prose>
    <Prose>The operation terminates because each cancellation strictly lowers the greatest remaining row; a nonnegative row index cannot descend forever. Only earlier columns of the same boundary dimension are added, preserving the span of available boundaries at each processed prefix. Keep the actual combination of original columns too: it is needed to show a representative chain and to check that the displayed geometric boundary matches the algebra.</Prose>
    <BoundaryReductionLab />
    <Prose>A zero reduced column creates a homology class in its simplex dimension. A nonzero column with pivot i pairs creator i with this column's entry value as death, one dimension lower. Creators never paired with a destructor remain essential in the defined filtration. Why does this pairing represent persistence, rather than merely matrix rank? The reduction respects each filtration prefix, so it records exactly when a new boundary relation removes an earlier independent class. The general pairing theorem makes this correspondence precise; the lab exposes its cancellation invariant and a complete small example rather than claiming to reprove the full decomposition theorem.</Prose>
    <Prose>In the triangle, the last edge column reduces to zero: its boundary can be cancelled using the earlier edges, creating a cycle. The face's boundary later pairs with that creator. When all seven cells share entry value zero, the pair has zero length. In the square's distance filtration, creation and filling occur at different values, giving its positive H₁ bar.</Prose>
    <Example example={examples.persistence} />
    <Prose>For S simplices, this straightforward reduction has an O(S³) worst-case field-operation bound and O(S²) storage. Sparse sets often reduce work on a particular input, but do not make the worst case linear. More fundamentally, a full complex on n vertices can have 2ⁿ−1 nonempty simplices. Even stopping at triangles leaves n+binomial(n,2)+binomial(n,3) cells. A fast implementation cannot make an arbitrarily large complex free.</Prose>
    <Prose>The browser therefore uses at most 14 points and displays only the H₀/H₁ contract, while the standalone teaching program allows up to 30. Production libraries use additional algorithmic improvements and compressed representations. Choose the required homology dimension, memory budget, cutoff and approximation deliberately. Subsampling or sparsifying changes the analyzed object; name that change and investigate its effect.</Prose>
    <Checkpoint prompt="You build a square's complete graph at a large threshold but never create any faces. The program reports three persistent H₁ classes. Is the persistence reducer necessarily broken?"><Prose>No. The complete graph on four vertices has six edges and four vertices, so its graph β₁ is 6−4+1=3. Those cycles cannot become boundaries without faces. The reducer may be correct for the wrong represented complex. H₁ of the intended Rips filtration needs its 2-simplices.</Prose></Checkpoint>

    <H3>Give GUDHI exactly the filtration you reduced by hand</H3>
    <Prose>The boundary reducer explains the pairing. For ordinary work, GUDHI supplies maintained complex construction and persistence routines. This complete bridge compares its <Code>RipsComplex</Code> and <Code>SimplexTree.persistence</Code> with the same F₂ column-reduction mechanism above, adding a declared distance cutoff. Distances are Euclidean and filtration values are maximum edge lengths, not ball radii. The small reference caches pair distances once before enumerating simplices.</Prose>
    <LessonTable caption="Match the persistence contract before comparing bars" headers={['Choice', 'Setting in the bridge', 'Why it matters']} rows={[
      ['Build enough cells', 'create_simplex_tree(max_dimension=2)', 'Edges create H₁; triangles can kill it.'],
      ['Choose coefficients', 'homology_coeff_field=2', 'Matches the XOR arithmetic of the manual reducer.'],
      ['Select reported intervals', 'min_persistence=0; keep H₀/H₁', 'Remove zero-length bars in both outputs.'],
      ['Include graph-only H₁ when requested', 'persistence_dim_max=True', 'Do not silently drop the highest built dimension.'],
    ]} />
    <MechanismProgram source="/learn-assets/topology-tda/gudhi_rips.py" title="Open the complete F₂ reduction / GUDHI comparison" output={libraryOutput} />
    <Prose>The executed GUDHI 3.13.0 run compares sorted interval <em>multisets</em>, preserving repeated bars and infinite endpoints. Representative cycles and reduction order may differ without changing persistence. For the square with side length 2 and cutoff 3, both routes return H₁=[2,√8). The rectangle with side lengths 3 and 1 gives [3,√10). Duplicate points, a singleton and invalid coordinates also have explicit checks.</Prose>
    <Prose>Now change only the square's cutoff to 2.1: one H₁ interval is still alive at the computation boundary. Its returned death is infinity in that truncated filtration; this is a <em>censored</em> observation about the intended full Rips filtration, not evidence that its hole survives forever. At cutoff 3 but with maximum dimension one, there are three unfilled H₁ cycles because the complete graph has no faces. These are two different reasons for infinite endpoints.</Prose>
    <Prose>This explicit oracle is limited to thirty planar points. Enumerating triangles can already require O(n³) cells, and column reduction has the larger worst-case costs described above. GUDHI improves the implementation but cannot make an unrestricted complex small. Cutoffs, sparse Rips approximations and landmark choices must be justified as part of the analyzed object. Keep dimensions, metric units, coefficient field and cutoff with stored diagrams so later feature construction does not mix incompatible contracts.</Prose>
    <Checkpoint prompt="A GUDHI result reports an infinite H₁ interval. Which two settings must you inspect before concluding the data contains a permanent hole?">
      <Prose>Inspect the filtration cutoff and whether 2-simplices were built. The executed square examples distinguish a cutoff before the diagonal arrives from a graph-only complex that can never fill cycles. Recompute the declared full Rips filtration through triangles to recover [2,√8). A different representative cycle is harmless; deleting duplicate intervals or interpreting censored infinity as a full-filtration conclusion is not.</Prose>
    </Checkpoint>

    <H2>6. Compare diagrams and bound change</H2>
    <Prose>A feature can shift slightly between datasets, appear, or disappear. Diagram distance must allow all three situations. Match some finite points one-to-one between diagrams; unmatched points in either diagram are sent to the diagonal. Sending a point to the diagonal represents creating or removing a zero-persistence feature.</Prose>
    <Prose>With L∞ ground distance, the cost of matching (b,d) to (b′,d′) is max(|b−b′|,|d−d′|). For (1,4) and (1.2,3.8), the cost is 0.2. The cheapest diagonal point for (b,d) is its midpoint ((b+d)/2,(b+d)/2), with cost (d−b)/2. To see why, the two coordinate displacements must together span d−b, so their maximum is at least half; the midpoint attains that bound.</Prose>
    <MathBlock>{String.raw`
d_B(D,E)=\min_M\max_i c_i(M).
`}</MathBlock>
    <Prose>Here M ranges over partial matchings, and cᵢ(M) lists every matched-point and unmatched-to-diagonal cost described above. An empty list has maximum zero. The <strong>bottleneck distance</strong> minimizes that largest cost over such matchings. A p-Wasserstein diagram distance instead takes the pth root of the sum of pth powers of all costs, for p≥1, then minimizes. State the ground norm: this lesson retains L∞ for both. These are diagram matchings with diagonal copies, not transport between normalized point-cloud probability measures.</Prose>
    <DiagramMatchingLab />
    <Example example={examples.matching} />
    <H3>What stability guarantees</H3>
    <Prose>Consider a fixed finite complex K with two valid simplex-entry functions f and g. Suppose |f(σ)−g(σ)|≤η for every simplex σ. If a simplex is present under f at threshold t, it must be present under g by t+η. The reverse relation also holds. Hence:</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
K_t^f&\subseteq K_{t+\eta}^g\subseteq K_{t+2\eta}^f,\\
K_t^g&\subseteq K_{t+\eta}^f\subseteq K_{t+2\eta}^g.
\end{aligned}
`}</MathBlock>
    <Prose>These shifted inclusions commute with the ordinary inclusions as t changes. Applying homology gives the corresponding shifted linear maps, called an <strong>interleaving</strong>. For these finite one-parameter persistence modules, the bottleneck stability theorem yields d_B≤η, with compatible treatment of essential bars. The existence of an appropriate matching is a substantive theorem; drawing two inclusions alone is not its full proof. Tame continuous-function versions extend the setting under their own assumptions.</Prose>
    <Prose>For paired Euclidean observations pᵢ and qᵢ with each movement at most δ, triangle inequality gives |d(pᵢ,pⱼ)−d(qᵢ,qⱼ)|≤2δ. Taking the maximum over the same vertex subset preserves this bound, so their diameter-based Rips entry functions differ by at most 2δ. Comparing the complete corresponding finite filtrations therefore gives the matching bound 2δ. The correspondence, distance convention, homology field and represented dimensions must agree.</Prose>
    <Prose>If a fraction of observations are arbitrarily far outliers, the fraction may be small while this displacement bound is huge or unavailable. A claim of “robustness to noise” must state what kind of perturbation is small. Likewise, a long bar is not automatically statistically significant, useful for prediction, causal evidence, or a sensor-coverage certificate. Stability describes the behavior of a summary under a specified change; statistical inference needs a sampling model and uncertainty analysis.</Prose>

    <H2>7. Let pixels build their own space</H2>
    <Prose>An image already has a grid. Turning every pixel into a point cloud may discard its natural adjacency or mix up spatial and intensity distances. A <strong>cubical complex</strong> instead uses grid cells. Here each pixel is a closed square with a scalar intensity, and it enters when the threshold reaches that intensity. Its boundary edges and vertices must enter no later; equivalently, each shared boundary cell enters at the minimum value of its incident pixels.</Prose>
    <Prose>For a low-valued ring with a high-valued center, the ring appears first and its center fills later. Raising the threshold forms a <strong>sublevel filtration</strong>. If bright structures are the foreground of interest, a superlevel filtration may be more natural; one can negate intensities and explicitly translate the scale convention. The choice determines what “birth” means physically.</Prose>
    <PixelFiltrationLab />
    <Prose>At threshold 1, the eight-pixel ring has V=16 vertices, E=24 edges and F=8 squares. Its Euler characteristic is zero. Since it is connected and has no H₂ as a finite planar union, β₁=β₀−χ=1. Filling the center makes F=9 and χ=1, so β₁=0. Removing the top-middle ring pixel instead leaves V=16, E=22, F=7: χ=1 and no enclosed hole. The two operations remove the hole for different geometric reasons.</Prose>
    <Example example={examples.pixels} />
    <Prose>Two closed pixels meeting at a corner share a vertex and are connected under this convention. A four-neighbor foreground labeling would treat them differently. Similarly, identifying opposite image edges makes a periodic domain; it changes the underlying space, potentially adding classes associated with the domain itself. Document foreground/background, cell convention, units and boundary conditions before comparing outputs.</Prose>
    <Prose>This construction supports practical questions about connected structures and enclosed regions in segmented or grayscale images. It does not by itself establish that an apparent cavity is a defect, a biological structure or a meaningful material pore. The threshold policy, imaging process, resolution and independent domain evidence determine that interpretation.</Prose>

    <H2>8. Turn time into coordinates</H2>
    <Prose>Sometimes the interesting space is not directly measured. A single periodic signal s(t)=cos t moves back and forth on a line. Record a delayed pair (s(t),s(t−τ)) instead. Time now generates a curve in two coordinates. The trigonometric identity supplies an exact mechanism:</Prose>
    <MathBlock>{String.raw`
\begin{gathered}
\begin{bmatrix}s(t)\\s(t-\tau)\end{bmatrix}
=A\begin{bmatrix}\cos t\\\sin t\end{bmatrix},\\[4pt]
A=\begin{bmatrix}1&0\\\cos\tau&\sin\tau\end{bmatrix},\\
\det A=\sin\tau.
\end{gathered}
`}</MathBlock>
    <DelayEmbeddingFigure />
    <Prose>At τ=π/2 the delayed pair is (cos t,sin t), a circle. For sin τ≠0, the invertible linear map produces an ellipse. At τ=0 or π it collapses to a segment. Thus a poor delay can erase the visible loop even though the original oscillation is unchanged. A near-zero determinant also makes the ellipse thin, changing its metric scale and its finite persistence under noise.</Prose>
    <Example example={examples.delay} />
    <Prose>For sampled data, delay windows must respect the sampling interval and time ordering. Missing samples, interpolation and normalization affect the resulting cloud. Ordinary point-cloud persistence then forgets the order in which those points were traversed: reversing the point sequence only relabels the distance matrix. An application needing direction or causal timing needs additional information.</Prose>
    <Prose>This exact cosine calculation is an understandable reason to investigate periodic measurements with delay coordinates. General delay-embedding theorems require assumptions about the dynamical system, observation function and embedding dimension. The plotted ellipse is not a proof of those hypotheses, nor evidence that an arbitrary biological or mechanical signal has a single underlying oscillator.</Prose>

    <H2>9. Build usable topological features</H2>
    <Prose>Different samples can produce different numbers of intervals. Many statistical or machine-learning methods expect vectors of equal length, so a raw diagram needs another representation. Choose what information to retain and how to compare it. This is a modeling step, not a free conversion that preserves everything.</Prose>
    <H3>Landscapes: turn intervals into ranked tents</H3>
    <Prose>For a finite interval [b,d), draw a triangular tent: height rises at slope 1 from birth, reaches (d−b)/2 at the midpoint, then falls at slope −1 to death. Outside the interval, its height is zero. At coordinate t, λ₁(t) is the largest tent height, λ₂(t) the second largest, and so on. Missing levels are zero.</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
m(t)&=\min(t-b,d-t),\\
h_{b,d}(t)&=\max(0,m(t)).
\end{aligned}
`}</MathBlock>
    <Prose>For [0,3) and [1,4), at t=1.5 the heights are 1.5 and 0.5. At t=2 both are 1. Keeping only their maximum would discard the fact that two tents contribute there. A sampled landscape vector approximates these functions on a fixed grid; a coarse grid can miss narrow peaks. Separate essential bars before applying a finite-interval tent definition.</Prose>
    <Prose>The previous Functional Analysis lesson gives a useful connection: a collection of functions can be averaged or compared in a chosen function norm, subject to its integrability conditions. Finite diagrams with finite bars give bounded, compactly supported landscapes. This does not make every grid or finite collection of levels an information-preserving encoding.</Prose>
    <H3>Persistence images: integrate weighted density-like surfaces</H3>
    <Prose>First change coordinates from (birth,death) to (birth,persistence), where persistence=death−birth. Center a normalized two-dimensional Gaussian of bandwidth σ at each point. Multiply its contribution by a specified continuous weight that vanishes on persistence=0; here w(p)=min(p,1) for p≥0. Sum the surfaces, then integrate over each pixel of a fixed grid.</Prose>
    <Prose>For a pixel [x₀,x₁]×[y₀,y₁], a bar with birth b and persistence p contributes a product of two one-dimensional normal probabilities. Φ is the standard normal cumulative distribution function. The product follows because the Gaussian uses independent coordinate directions. Let uⱼ and vⱼ be the standardized pixel endpoints, Pₓ and Pᵧ the two interval probabilities, and I this bar's weighted pixel contribution:</Prose>
    <MathBlock>{String.raw`
\begin{aligned}
u_j&=(x_j-b)/\sigma,\\
v_j&=(y_j-p)/\sigma,\qquad j=0,1,\\
P_x&=\Phi(u_1)-\Phi(u_0),\\
P_y&=\Phi(v_1)-\Phi(v_0),\\
I&=w(p)\,P_xP_y.
\end{aligned}
`}</MathBlock>
    <PersistenceRepresentationLab />
    <Prose>A Gaussian value at the pixel center is not this integral. Center sampling might approximate an integral after accounting for pixel area and an error criterion, but must not be mislabeled. The full surface integrates to the sum of weights. A finite image window loses tails, as the lab reports; silently renormalizing it would produce a different feature.</Prose>
    <Prose>The two bars in the default example each have persistence 3 and weight 1. With σ=0.5, the selected pixel [0,1]×[2,3] receives about 0.227767 from each bar. The whole [0,4]² image captures about 1.443642 of the total weight 2. Its left boundary cuts through the Gaussian centered at birth 0, so losing substantial mass is expected—not a numerical bug.</Prose>
    <Example example={examples.features} />
    <Prose>Bandwidth, weight, window and pixel resolution determine what differences are smoothed or emphasized. If chosen from data, fit those choices on the training portion or select them using validation; do not use the final test diagrams to set a convenient common grid. A persistence image can be stable under specified assumptions and still be useless for a particular prediction task. Its pixels are numerical features, not posterior probabilities that real holes exist.</Prose>

    <H2>10. Test the claim against data</H2>
    <Prose>Here is a complete, deliberately small experiment. Each independent sample is a cloud of 14 points. Class 1 samples a noisy-radius ring at roughly even angles; class 0 samples a disk with area-uniform radial distribution, using radius √U for U uniform on [0,1]. Both classes get random translations and a common scale range. Those sampling rules make this an easy synthetic distinction; they are not a model of every dataset containing loops.</Prose>
    <Prose>Compute the longest finite H₁ lifetime divided by cloud diameter, or the sum of finite H₁ lifetimes divided by diameter. This normalization removes uniform coordinate scale in exact arithmetic. As a simple geometric baseline, use one minus the coefficient of variation of distances to the cloud's centroid. A roughly evenly sampled ring has similar radii about its centroid; random disk points vary more. This baseline does not need persistent homology.</Prose>
    <Prose>For each scalar feature, training data chooses a threshold. Validation chooses between the two predeclared topological descriptors. The untouched test split is evaluated after that selection. Finally, new diagnostic samples alter angular sampling, anisotropic scale or noise without retuning. These are additional stress tests, not extra validation candidates quietly used to change the reported model.</Prose>
    <Example example={examples.validation} />
    <Prose>In this actual seeded run, both the chosen topological feature and the radial baseline score 24/24 on the simple test split. That is a result about 24 synthetic clouds, not proof of perfect generalization. Uneven ring sampling lowers the scores to 15/24 and 17/24; anisotropic scaling lowers both to 12/24; added coordinate noise gives 13/24 and 21/24. The baseline's advantage on that last diagnostic is useful information, not something to hide to make topology look impressive.</Prose>
    <Prose>Why can stretching hurt if a circle and an ellipse have the same topology? The classifier uses <em>metric lifetimes relative to diameter</em>, not just an abstract homeomorphism class. Squeezing one axis shortens the loop's persistence relative to its remaining diameter. Uneven sampling can leave a wide gap that delays loop creation, and noise can bridge or fill the central region earlier. Topological invariance and the stability of a chosen data feature are distinct ideas.</Prose>
    <LessonTable caption="A defensible topology report connects every claim to its object" headers={['Report item', 'Concrete question']} rows={[['Observation and unit', 'What is an independent sample, and what do coordinates or scalar values measure?'], ['Representation', 'Point cloud, image cells, graph or delay windows? What information was discarded?'], ['Metric and filtration', 'How were units normalized, what enters at each threshold, and which scale convention is used?'], ['Algebra and computation', 'Which coefficient field, cell dimensions, cutoff and approximation were used?'], ['Sensitivity', 'What changes under resampling, noise, parameter variation or a plausible measurement error?'], ['Task evidence', 'What baseline, validation policy, held-out unit and uncertainty assessment support the intended use?'], ['Limit', 'Which observed summary is established, and which population or causal claim remains unsupported?']]} />
    <Prose>For trajectories or repeated measurements, randomly splitting overlapping windows can put near-duplicates in training and test. The independent unit may be a person, device, experiment or time block. Split at that level and account for temporal dependence. A short-lived bar may still help a task; a long-lived bar may reflect a nuisance variable. Let the task and evidence decide rather than imposing a “long equals meaningful” rule.</Prose>

    <H2>11. Summarize overlap with Mapper</H2>
    <Prose>Persistence is not the only TDA construction. <strong>Mapper</strong> builds an exploratory summary of how overlapping local groups connect. Start with a scalar <strong>lens</strong>—a chosen function of each observation. It might be one coordinate, a score, or another deliberately selected quantity. Cover its range with overlapping intervals. Pull each interval back to the observations whose lens values lie inside it.</Prose>
    <Prose>Within each pullback, cluster using a stated rule on the original observations. In this lesson the rule is connected components of a distance-threshold graph, not a mysterious black box. Each resulting cluster becomes a Mapper node. Join two nodes when they share at least one observation ID. More generally, a group of nodes forms a nerve simplex when they all share an observation. Overlap lets one source point belong to multiple nodes; Mapper nodes do not partition the sample.</Prose>
    <MapperLab />
    <Prose>The default twelve-point circle uses the x-coordinate lens, four overlapping intervals and a neighbor threshold 0.6. Its pullbacks produce six clusters and six overlap edges, forming a cycle. At threshold 1.8, the top and bottom portions within middle bands connect; the graph becomes a four-node chain. The observations are unchanged. The within-band clustering decision changed the summary.</Prose>
    <Example example={examples.mapper} />
    <Prose>There is a further subtlety: cover intervals can overlap in the real line while their overlap contains no sampled lens value. For this fixture, three intervals with overlap fraction 0.4 produce no shared cluster members. The disconnected summary reflects the finite sample and chosen cover. Increasing overlap can also create triple intersections; drawing only a graph then omits the nerve's filled triangles. Do not count graph cycles as the complete nerve's homology without those cells.</Prose>
    <Prose>A <strong>Reeb graph</strong> identifies points in the same connected component of the same exact lens level set. Mapper uses a finite cover and local clustering to obtain a related summary. Recovering a particular underlying topology needs assumptions about the space, lens, sampling and cover. Good intervals in the lens range do not imply that every pulled-back cluster intersection is contractible. A useful exploratory Mapper picture is not automatically a homotopy-equivalence certificate.</Prose>
    <Prose>This makes Mapper useful for examining branching, transitions or groups in high-dimensional measurements when the local memberships can be inspected. Its graph layout is a display choice. Nearby node drawings are not necessarily similar source observations, and long rendered edges are not measured distances. Pair the graph with actual members, meaningful variables and sensitivity checks.</Prose>

    <H2>12. Know what the summary leaves out</H2>
    <Prose>Homology is powerful partly because it intentionally forgets information. It records additive cycle classes in each dimension over a selected coefficient system. It does not retain every length, angle, local geometry, time direction or way loops interact. A complete end-to-end analysis includes recognizing when the chosen summary cannot answer the question.</Prose>
    <details><summary>Coefficient fields, orientation and torsion</summary><Prose>The same boundary matrix can have different rank over different fields. The program's three-by-three matrix has determinant −2 over the reals and rank 3 there, but its rows sum to zero over F₂ and its F₂ rank is 2. This concrete algebraic difference explains why the field belongs in the result. General oriented simplicial chains use signed boundary coefficients; reducing signs modulo 2 removes them.</Prose><Prose>Integer homology can retain torsion: a nonzero class may become zero after multiplying by an integer. For example, a standard cell structure for the real projective plane has one cell in each of dimensions 0, 1 and 2, with ∂₂ multiplication by 2 and ∂₁=0. Over the integers, H₁ is Z/2Z. Over F₂ that multiplication becomes zero, so H₁ and H₂ each have dimension 1; over a field of characteristic other than 2, multiplication by 2 is invertible and both dimensions are zero. This is an additional cellular example, not the three-by-three triangle-incidence matrix from the program. Understanding why that space has this attachment map belongs to a fuller topology course.</Prose></details>
    <details><summary>Fundamental group and cohomology ask richer questions</summary><Prose>The fundamental group records based loops up to continuous deformation and composes them by following one after another. That operation can be noncommutative, information ordinary H₁ does not retain. Cohomology uses functions on chains and, with additional structure such as the cup product, can describe how classes interact.</Prose><Prose>For a concrete limit of counts, the torus and the wedge of two circles with a 2-sphere both have Betti numbers (1,2,1), yet they are not homotopy-equivalent. The torus's fundamental group is Z², which is commutative; the wedge's is the noncommutative free group on two generators. Equal Betti counts—and even equal additive integer homology in this example—do not classify the spaces. This is a reason to study richer invariants when the task requires them, not a reason to dismiss homology's useful summaries.</Prose></details>
    <details><summary>Beyond one increasing parameter</summary><Prose>A usual barcode relies on a one-dimensional ordered parameter with forward maps. A time-varying complex that adds and removes cells does not directly fit that model. Zigzag persistence permits maps pointing in both directions, providing a way to track such changes. Extended persistence joins ordinary and relative constructions to obtain additional pairings; it changes the question and conventions rather than merely relabeling infinity.</Prose><Prose>If both distance and density thresholds vary independently, neither parameter setting may contain the other. General multiparameter persistence has no single interval-barcode classification analogous to the finite one-parameter case. Slices, rank invariants and other summaries retain different information. A two-slider visualization alone does not solve this classification problem.</Prose></details>
    <details><summary>When inference and learned topology need more theory</summary><Prose>Inferring the topology of an unknown space from samples requires assumptions such as sampling density, noise control and geometric regularity. Confidence sets or hypothesis tests need a stated stochastic model and a validated procedure. Bottleneck stability is an ingredient, not a replacement for those assumptions.</Prose><Prose>A fixed persistence image fed to a classifier is also different from learning the filtration, differentiating a topological objective, or operating directly on simplicial/cellular signals. Learned filtrations must preserve face relations; gradients can change at ties; oriented chain operators must respect their algebra. The planned Geometric &amp; Topological Deep Learning topic owns those architectures and learning mechanisms. Here the finite computations remain an interpretable foundation.</Prose></details>
    <Prose>The next topic, Category Theory, gives a language for one relationship already used: each inclusion of complexes induces a linear map on homology, and those maps respect identities and composition. An injective inclusion of spaces need not induce an injective map on H₁—the triangular loop included into its filled triangle maps its nonzero class to zero. “Preserves composition” is a precise promise; it is not “preserves every property.”</Prose>

    <H2>13. Practise calculation and judgment</H2>
    <Prose>Attempt each task before opening its hint. Show the represented object, intermediate operations and assumptions, not only a final number. For code work, change the inputs deliberately and check the acceptance cases against the independently derived answer.</Prose>
    <Practice title="1. Repair the equivalence claim" prompt="A colleague says a disk and a point have the same topology because the disk continuously shrinks to its center. Identify what is true, what is too strong, and an explicit formula supporting the true claim. Is [0,1/3) open in the space [0,1]?" hint="Separate a reversible map from a family of maps; then use an intersection with an ambient open interval."><Prose>The contraction H(x,t)=(1−t)x stays within the disk, so it is homotopy-equivalent to a point. It is not homeomorphic to one: no bijection exists from all disk points to a singleton. The interval [0,1/3) is relatively open because it equals [0,1]∩(−1/3,1/3). It is not open as a subset of the whole real line. Each statement depends on its precise equivalence or ambient space.</Prose></Practice>
    <Practice title="2. Calculate a changed boundary and filling" prompt="Use a square with diagonal {0,2}, no faces. Select {0,1}+{1,2}+{0,2}. Compute its boundary and the complex's β₁. Add face {0,1,2}; explain what happens to that selected class and to the perimeter class." hint="Count each endpoint modulo two, then distinguish a boundary column from the number of all graph cycles."><Prose>Every selected vertex occurs twice, so the chain is a cycle. The graph has five edges, four vertices and one component: β₁=2. Adding the face makes the selected triangle its boundary, so that class becomes zero and β₁ becomes 1. The outer perimeter remains nonzero. Adding the other triangular face would fill it too. In the changed standalone boundary program, closure of facets [(0,1,2),(0,3),(2,3)] must return (1,1,0).</Prose></Practice>
    <Practice title="3. Derive a rectangle barcode without running code" prompt="For corners (0,0),(4,0),(4,3),(0,3), give every positive H₀/H₁ interval under diameter-based Rips. Then double both axes, and finally explain why squeezing only the vertical axis is different." hint="The vertical edges enter at 3, horizontal edges at 4, and diagonals at 5."><Prose>At 3 the two vertical pairs form, killing two H₀ classes. At 4 the graph connects and creates the perimeter loop. At 5 the diagonals and faces fill it. Thus H₀ has two [0,3), one [0,4), and one [0,∞); H₁ has [4,5). Doubling coordinates gives finite endpoints 6,8,10. If only the height changes to h&lt;4, the H₁ interval is [4,√(16+h²)), whose lifetime tends to zero as h tends to zero. Uniform scaling preserves endpoint ratios; anisotropic scaling need not.</Prose></Practice>
    <Practice title="4. Explain an apparently inconsistent reduction" prompt="The last edge of a filled triangle reduces to zero, but the final triangle has β₁=0. Explain the two statements. What would go wrong if a face were sorted before one of its edges?" hint="Distinguish a birth column from its eventual pairing, and an internal tie order from a distinct filtration scale."><Prose>The last edge creates a class at its processing prefix. The face subsequently pairs with and kills it. If both have the same entry value, the resulting interval has zero duration and does not contribute at any complete scale snapshot. A face before its edge breaks the face-before-coface filtration order required by this reduction; the boundary row would refer to unavailable structure. Repair the sort or invalid filtration, rather than hiding the row or accepting the output as a persistence diagram.</Prose></Practice>
    <Practice title="5. Choose a better diagram matching" prompt="For A={(2,10),(4,10)} and B={(3,10),(0,10)}, calculate the greedy first-to-first bottleneck cost, an optimal bottleneck cost, and the optimal 2-Wasserstein cost using L∞ ground distance. What if a diagram has two copies of (1,4) and the other has one?" hint="Swapping partners gives costs 2 and 1. A leftover copy still pays to reach the diagonal."><Prose>The greedy pairing gives costs 1 and 4, bottleneck 4. Swapping gives 2 and 1, bottleneck 2 and 2-Wasserstein √5≈2.236068. Sending these long bars to the diagonal cannot improve that maximum; each relevant diagonal cost is at least 3, and leaving B=(0,10) unmatched costs 5. For two copies versus one, one copy matches at zero and the other costs 1.5 to reach the diagonal. Removing duplicates as if the diagram were an ordinary set would incorrectly return zero.</Prose></Practice>
    <Practice title="6. State the actual perturbation bound" prompt="Corresponding sample points move by at most 0.03 in Euclidean norm. Give a valid bound for their diameter-filtration bottleneck distance. Does replacing 1% of points with arbitrary outliers satisfy the same premise?" hint="Bound one changed pair distance first; the percentage of modified samples is not a displacement bound."><Prose>For each pair, the distance changes by at most 0.03+0.03=0.06. The maximum pair distance of every corresponding simplex therefore changes by at most 0.06. Under the finite corresponding-filtration conditions, bottleneck stability gives d_B≤0.06. Arbitrarily distant outliers need not satisfy the movement bound at all. The statement is mathematical sensitivity, not a p-value or proof that a feature is useful.</Prose></Practice>
    <Practice title="7. Audit a pixel convention" prompt="At threshold 1, compare the eight-pixel ring, the same ring with its top-middle pixel raised to 3, and two low pixels meeting only at a corner. Give (V,E,F), (β₀,β₁), and explain why a four-neighbor labeling may disagree." hint="Shared cells count once. Use the planar Euler identity only after explaining why H₂ is zero."><Prose>The ring has (16,24,8) and (1,1). The gap case has (16,22,7) and (1,0). The corner pair has (7,8,2) and (1,0): its shared vertex connects the closed squares. A four-neighbor foreground algorithm declines that corner connection and would count two foreground components, answering a different construction's question. Periodic boundary identification would also require a different homology calculation, because the planar H₂ premise no longer follows.</Prose></Practice>
    <Practice title="8. Change the delay and the summary" prompt="For s(t)=cos t, derive the delayed-pair map at τ=π/6 and give its determinant. Would reversing the order of a fixed set of delayed points change its ordinary Rips persistence? Would multiplying just one coordinate by 0.1 preserve all persistence endpoints?" hint="Use the displayed two-by-two map, then separate time ordering, topology and metric."><Prose>The map is [[1,0],[√3/2,1/2]] with determinant 1/2, so the continuous curve is an ellipse homeomorphic to a circle. Reversing the order of already constructed points merely relabels distances, leaving ordinary Rips persistence unchanged. Scaling one coordinate by 0.1 is still invertible, but generally changes distances and persistence endpoints nonuniformly. If instead the delay windows are reconstructed from a changed time series, first check what new points were actually generated.</Prose></Practice>
    <Practice title="9. Compute a feature rather than naming it" prompt="For bars [0,3) and [1,4), calculate λ₁ and λ₂ at t=1.5 and t=2. With σ=0.5 and weight min(p,1), compute the contribution of each bar to pixel [0,1]×[2,3]. Explain why the full displayed image has total weight below 2." hint="Both persistences are 3. The two birth intervals have symmetric normal probabilities around their respective centers."><Prose>At t=1.5 the landscape levels are 1.5 and 0.5; at t=2 they are both 1. For each bar the selected pixel contributes [Φ(2)−Φ(0)]·[Φ(0)−Φ(−2)]≈0.227767436. The sum is about 0.455534873. The finite [0,4]² image excludes Gaussian tails, especially to the left of birth zero. It captures about 1.443642; omitted weight is about 0.556358. Renormalizing would change the feature definition.</Prose></Practice>
    <Practice title="10. Trace a Mapper edge to the data" prompt="In the default twelve-point example, which nodes contain point 3 and why are they connected? Change the clustering threshold to 1.8. Then explain why three overlapping cover intervals can still yield no overlap edges." hint="Use actual member IDs, not the distances between drawn nodes."><Prose>At threshold 0.6 with four intervals, point 3 belongs to nodes 1 and 3, producing their overlap edge. At threshold 1.8 the two components in each middle band merge, leaving four nodes and three edges. With three intervals and overlap fraction 0.4, the overlapping real intervals contain none of the sampled x-values in this fixture; different pullback clusters share no observation, so the nerve graph has no edges. A cover of lens space and a sampled overlap relation are different objects.</Prose></Practice>
    <Practice title="11. Reproduce a useful failure" prompt="Run the synthetic evaluation, then inspect its frozen anisotropic diagnostic. Explain why a circle remains topologically equivalent to its squeezed ellipse while the classifier can fail. Propose one improvement and a valid way to evaluate it." hint="The feature divides metric lifetime by diameter; it does not output a homeomorphism class."><Prose>The thin ellipse retains a continuous loop, but its finite Rips lifetime relative to diameter shrinks. The unchanged threshold can now reject ring clouds. A proposed improvement might model anisotropic variation in the training distribution, use a justified coordinate normalization, or add domain-relevant features. Select its parameters on new training/validation data and evaluate on a genuinely held-out set of independent experimental units. Do not tune repeatedly on the printed test split and still call it untouched. A correct failure explanation and honest negative result meet the exercise's goal.</Prose></Practice>
    <Practice title="12. Write a claim you can defend" prompt="A dashboard shows a long persistence bar and a dramatic Mapper branch. Its author claims a statistically significant biological subtype. Write the missing evidence plan and one narrower claim already justified by the calculation." hint="Separate correctness of the finite computation from interpretation of the population and the measurement process."><Prose>A defensible immediate claim is that the declared finite data representation and parameters produce the reported bar and overlapping clusters, if the code and input were verified. A subtype claim additionally needs justified measurement preprocessing and metric, independent sampling units, sensitivity to plausible noise and parameters, a suitable inferential or validation procedure with multiplicity considerations, and external domain validation. Test whether the branch tracks a nuisance variable or acquisition artifact. Neither long persistence nor a compelling layout supplies significance or biological meaning by itself.</Prose></Practice>
    <Prose><strong>Readiness check.</strong> You can now build and describe a small complex; explain why a cycle becomes a boundary; compute Betti numbers and a finite barcode; check a diagram matching and a perturbation premise; choose an image or point construction; and test a feature without confusing topological, numerical and statistical claims. If a plotted hole cannot be traced back to its cells, scale and operation, revisit that mechanism.</Prose>
    <Prose><strong>Next in this module:</strong> <a href="/learn/path/full-curriculum/category-theory-emerging-use-in-ml?module=math-foundations">Category Theory (Emerging Use in ML)</a> organizes structure-preserving maps and their composition. The complex-inclusion → homology-map relationship provides a concrete starting example. Differential Geometry follows it and adds smooth and metric structure to questions about spaces. These are the actual next module topics, independent of publication status.</Prose>
    <Sources alternatives={<p>For a spoken alternative after the first chain calculation, use <a href="https://youtu.be/sAZT-hYUs1A?si=DZgM7fwzt2zv48dL" target="_blank" rel="noreferrer">Steve Oudot: Introduction to Persistent Homology</a> and <a href="https://youtu.be/3JR4eqFjdvg?si=B4VwTnhiSgM3FRLd" target="_blank" rel="noreferrer">Henry Adams: Applied Persistent Homology</a>, linked by the <a href="https://www.cmi.ac.in/~pdeshpande/tda_omws_2026.html" target="_blank" rel="noreferrer">CMI 2026 school schedule</a>. They are graduate-oriented companions. The official recording identities and direct links were checked; full playback was not reviewed. The <a href="https://www.enseignement.polytechnique.fr/informatique/INF556/" target="_blank" rel="noreferrer">INF556 course</a> also pairs homology/persistence lectures with notes and tutorials.</p>}>
      <li><a href="https://pi.math.cornell.edu/~hatcher/AT/AT.pdf" target="_blank" rel="noreferrer">Hatcher, Algebraic Topology</a> — chapter 0 and section 2.1 for homotopy, chains, boundary cancellation and quotient homology; examples 1.13, 1.21 and 2.42 support the optional fundamental-group and coefficient comparisons. These selected passages were reviewed, not the whole graduate text. Integer orientations there extend the explicitly taught F₂ route here.</li>
      <li><a href="https://ti.inf.ethz.ch/ew/courses/TDA25/Chapter4.pdf" target="_blank" rel="noreferrer">ETH: persistent homology and matrix reduction</a> — sections 4.2–4.3 for the filtration-respecting pairing algorithm. Pair this with the executable trace. The lesson states a worst-case bound rather than treating informal fast-runtime observations as guarantees.</li>
      <li><a href="https://ti.inf.ethz.ch/ew/courses/TDA25/Chapter5.pdf" target="_blank" rel="noreferrer">ETH: point-cloud complexes</a> and <a href="https://ti.inf.ethz.ch/ew/courses/TDA25/Chapter7.pdf" target="_blank" rel="noreferrer">Mapper</a> — selected sections 5.1–5.2 and 7.3. The point-cloud notes use a radius convention; translate it before comparing thresholds. Restricted intersections, not bare face circumradii, define the alpha construction.</li>
      <li><a href="https://math.uchicago.edu/~shmuel/AAT-readings/Data%20Analysis%20/Edelsbrunner,%20Harer,%20Stability.pdf" target="_blank" rel="noreferrer">Cohen-Steiner, Edelsbrunner &amp; Harer: Stability of Persistence Diagrams</a> — the mathematical bottleneck stability result. Definitions and the opening stability argument were inspected; read the full proof only after understanding the finite shifted inclusions.</li>
      <li><a href="https://jmlr.org/papers/v18/16-337.html" target="_blank" rel="noreferrer">Adams et al.: Persistence Images</a> — section 4 gives weighted surfaces and integrated pixels. The linked erratum was also checked. This lesson derives its own simple pixel example and does not borrow the paper's performance claims.</li>
      <li><a href="https://www.frontiersin.org/journals/artificial-intelligence/articles/10.3389/frai.2021.667963/full" target="_blank" rel="noreferrer">Chazal &amp; Michel: An Introduction to Topological Data Analysis</a> — selected persistence, stability, descriptor and statistical-learning sections for a broader route after the concrete mechanisms. It separates mathematical summaries from statistical inference.</li>
      <li><a href="https://gudhi.inria.fr/python/latest/rips_complex_user.html" target="_blank" rel="noreferrer">GUDHI Rips documentation</a> and <a href="https://gudhi.inria.fr/python/latest/cubical_complex_user.html" target="_blank" rel="noreferrer">cubical-complex documentation</a> — current construction, dimension and boundary conventions. The bridge now executes GUDHI 3.13.0 for the Rips comparison; the earlier standalone reduction and cubical examples remain their explicit teaching implementations.</li>
    </Sources>
  </div>
};
