import { H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { BPlusContrastFigure, BPlusRangeLab, BTreePageLab, ExternalMergeLab, PageBufferLab, ShadowCommitLab } from '../../components/lesson-labs/ExternalMemoryLabs.jsx';
import { externalMemoryExamples } from '../external-memory-examples.js';
import externalMemoryPractice from '../practice/external-memory-algorithms-b-trees-i-o-complexity.js';
import ExternalMemoryLibraryBridge, { StreamingSortImplementation } from '../../components/lesson-labs/ExternalMemoryLibraryBridge.jsx';
export default {
  title: 'External-Memory Algorithms, B-Trees & I/O Complexity',
  readTime: '~80 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot external-memory-lesson">
    <LessonIntro prerequisites="Arrays, maps, linked lists, heaps, sorted merging, search trees and asymptotic reasoning. We rebuild the page/cache model and durability distinction here; SQL and the previous Persistent Data Structures lesson provide useful context." sections={[['1-count-the-data-that-move', 'Pages, buffers and writes'], ['2-pack-search-decisions-into-a-page', 'Wide search trees'], ['3-keep-the-page-tree-balanced', 'Splits, borrowing and merging'], ['4-follow-records-through-b-leaves', 'B+ ranges and layout'], ['5-sort-more-than-memory-can-hold', 'Runs and merge budgets'], ['6-publish-a-root-that-can-survive', 'Durable root publication'], ['7-choose-for-the-workload', 'Applications and limits'], ['8-practise-a-changed-storage-contract', 'Independent practice'], ['guided-dsa-practice', 'LeetCode transfer']]}>A sensor archive is larger than RAM. Reading one record may fetch an entire page, and two algorithms performing the same number of comparisons may move very different amounts of data. Learn to organize searches, updates and sorting around those transfers. Then connect the previous lesson's copied roots to the additional promises needed after a crash.</LessonIntro>

    <H2>1. Count the data that move</H2>
    <Prose>Imagine a file containing fixed-size records numbered from zero. Storage transfers a group of records at a time: a <strong>block</strong> or <strong>page</strong>. A <strong>buffer frame</strong> is a place in RAM holding one page. Reading a resident record uses that frame; reading a nonresident page first brings it into memory. That miss costs one page read in our model, even if the algorithm wanted only one record.</Prose>
    <pre className="external-calculation" aria-label="Page address and memory units"><code>{`N = 16 records
B = 4 records per page
M = 8 records; P = M/B = 2 page frames
page(address 6) = floor(6/4) = page 1`}</code></pre>
    <Prose>N is the input size, B the transfer size in records, and M the usable record capacity of fast memory. We take M to be a multiple of B when budgeting frames. The two-level <strong>external-memory model</strong> counts transfers between fast and slow memory separately from computation on resident data. “External” means outside the modeled fast memory, not necessarily a removable drive. The same locality question appears between CPU caches and RAM, although real hierarchies add more levels and policies.</Prose>
    <Prose>An aligned scan of N contiguous records needs ceil(N/B) page reads from a cold buffer. An unaligned contiguous slice can touch one extra page. Random or strided record access can revisit pages after eviction and cost as much as one read per access. Neither statement says every O(N) CPU algorithm necessarily transfers N/B pages: its access pattern and layout decide that.</Prose>
    <PageBufferLab />
    <Prose>With B=4 and two frames, addresses 0,1,…,15 cost four reads. The column-first ordering 0,4,8,12,1,5,… visits the same records but rotates through four pages. A two-frame <strong>least recently used</strong> buffer evicts the page whose last use is oldest; each of the sixteen requests misses. With four frames, both orders cost four cold reads. More memory helps here because it holds the working set, not because it reduces the number of requested records.</Prose>
    <RunnableExample example={externalMemoryExamples.locality} />
    <Prose>The programs are complete Python 3.12 standard-library examples. Save one block as <code>external_memory.py</code> and run <code>python external_memory.py</code>. The browser executes small JavaScript models; it does not execute the displayed Python. Except for the explicitly identified temporary-file sorter, their “pages” are in-memory objects with counted logical transfers.</Prose>
    <H3>A dirty page owes a write</H3>
    <Prose>A write can modify an already resident page without immediately transferring it back. Mark that page <strong>dirty</strong>. A writeback cache writes a dirty page when evicting it or when explicitly flushing it; evicting a clean page needs no write. Our write-allocate policy reads a missing page before modifying it, preserving the rest of its contents. A system overwriting an entire page might avoid that read, but that is a different contract.</Prose>
    <Prose>Several updates to one resident page can share a later write. The lab therefore keeps read, write and hit counters separate, and includes the final dirty flush as explicit steps. Its flush counts a storage-model write; it is not proof of a real device's durable completion. Section 6 adds that distinct requirement.</Prose>
    <RunnableExample example={externalMemoryExamples.writeback} />
    <Checkpoint prompt="Two requests modify addresses 0 and 1 with B=4 and an initially empty one-frame buffer. What is owed before and after a final flush?"><Prose>Both use page 0. The first incurs one read, the second hits, and the page is dirty. Before the flush there are zero writes and one pending dirty page; after it there is one write. Counting two record updates as two page writes would miss the buffer's effect.</Prose></Checkpoint>

    <H2>2. Pack search decisions into a page</H2>
    <Prose>A binary search tree chooses between two children at each node. If every node occupies an unrelated page, a root-to-leaf search can fetch many mostly unused pages. A <strong>B-tree</strong> packs many sorted keys and child references into each page. One page read then supplies many comparisons and selects among many children. The aim is a shallow, ordered, balanced page tree.</Prose>
    <Prose>For keys [10,20], the three children contain keys below 10, strictly between 10 and 20, and above 20. In this textbook B-tree, 10 and 20 are themselves stored records; equality can finish in the internal page. We implement a set of unique integer keys. A map would attach a value, and duplicates require an explicit policy such as a composite key or a list of matching records.</Prose>
    <LessonTable caption="Our B-tree invariant, with minimum degree t≥2" headers={['Part', 'Required property']} rows={[['Keys and subtrees', 'Keys are strictly sorted; child intervals are disjoint and respect every ancestor separator.'], ['Nonroot occupancy', 'Every nonroot page has t−1 through 2t−1 keys. An internal page with k keys has k+1 children.'], ['Root exception', 'A nonempty internal root has at least one key and two children. An empty tree is one empty leaf root.'], ['Balance', 'Every leaf has the same depth. Pages may contain different numbers of keys.'], ['Storage interpretation', 'One node is one abstract page. Degree t is a key/child occupancy parameter; B is a transfer size in records.']]} />
    <Prose>Search finds the first key at least as large as the target. Equality succeeds; a leaf without equality fails; otherwise follow the corresponding child interval. Those cases preserve the statement “if present, the target lies in this subtree.” Binary search within each page takes O(log t) comparisons. A simple scan takes O(t); both use the same page once it is resident. The native implementation uses binary search for this within-page decision; the tiny browser trace uses a linear scan.</Prose>
    <H3>Derive the height instead of memorizing it</H3>
    <Prose>Let h count edges from root to leaf. At height h≥1, the sparsest legal tree has one root key, at least two children, then at least t children per internal nonroot page. Every nonroot page contributes at least t−1 keys. Summing levels gives N ≥ 1 + 2(t−1)(1 + t + … + t<sup>h−1</sup>) = 2t<sup>h</sup> − 1. Taking a base-t logarithm gives h ≤ floor(log<sub>t</sub>((N+1)/2)); a cold search visits at most h+1 pages. For a nonempty leaf root h=0; handle N=0 separately.</Prose>
    <Prose>The page-visit bound is O(1 + log<sub>t</sub>(N+1)). If fixed-size keys/references give t proportional to B, write O(1 + log<sub>B</sub>(N+1)), with B&gt;1. The leading 1 matters for tiny trees. A cached root or upper levels reduce actual reads below the page visits. The height proof is about occupancy, not a claim that every database chooses this exact page format.</Prose>
    <Prose>For a hypothetical 4096-byte B+ internal page with a 64-byte header, 8-byte separator keys and 8-byte child references, c children require 64+8c+8(c−1) bytes. The largest c is 252: 4088 bytes fit, whereas 253 children need 4104. Real headers, slots, variable keys, compression and fill policies change the result. This calculation deliberately describes an internal separator page, not storage for all payload records.</Prose>
    <RunnableExample example={externalMemoryExamples.fanout} />
    <Prose>The accompanying height comparison separately applies the textbook minimum-degree bound to one million keys. Its t=126 case corresponds to at most 252 children in that abstract occupancy model, giving at most two edges and three cold page visits. It is an explanatory parameter calculation, not a measured database search time or a claim about a particular product.</Prose>

    <H2>3. Keep the page tree balanced</H2>
    <Prose>Insertion cannot simply append to a full leaf. Our <strong>top-down</strong> method first ensures that the page we descend into is not full. A full page contains 2t−1 keys. Split it into two t−1-key pages, move the middle key into its parent, and partition its child references t to each side if it is internal. The parent must have space; maintaining that condition before descent avoids an uncontrolled upward overflow.</Prose>
    <Prose>With t=2, splitting [5,10,20] leaves [5] and [20] below separator 10. A new key 6 routes left and becomes [5,6]. If the root was full, create a new root above it and split: every leaf gets one additional ancestor, so equal leaf depth remains true. A nonfull leaf accepts the key in sorted order.</Prose>
    <BTreePageLab />
    <Prose>Why is splitting safe? Keys on each side of the median remain in the same ordered intervals, and the promoted median separates them. Internal child intervals divide with those keys. Both new pages satisfy minimum occupancy. Splitting a nonroot page does not change leaf depth; root splitting changes all depths together. These observations prove the structural repair, while the search interval invariant proves that the insertion enters the correct subtree.</Prose>
    <H3>Deletion needs a reserve before descent</H3>
    <Prose>Removing from a t−1-key nonroot leaf would violate minimum occupancy. Before descending into such a minimal child, give it a reserve. If an adjacent sibling has at least t keys, <strong>borrow through the parent</strong>: move the parent's separating key into the child, move the sibling's boundary key up into the parent, and move the corresponding child reference too when the pages are internal. Moving a sibling key directly across without repairing the separator can break search intervals.</Prose>
    <Prose>If neither adjacent sibling can spare a key, <strong>merge</strong> the child, a minimal sibling and their parent separator. The new page has (t−1)+1+(t−1)=2t−1 keys, so it fits. The parent loses a key and a child reference. Our descent reserve protects a nonroot parent from underflow; an empty internal root is replaced by its sole child, reducing every leaf's depth by one.</Prose>
    <Prose>If the requested key is internal, choose its predecessor from the left subtree when that child has at least t keys, replace the internal key, then recursively remove that predecessor. The successor on the right works symmetrically. If both children are minimal, merge them with the key and delete inside the merged page. Replacement must remove the old occurrence; otherwise the set gains a duplicate.</Prose>
    <Prose>The lab shows primitive repairs, including a temporary duplicated replacement key or an empty root awaiting promotion. “Complete” events are the public states where the full set/occupancy invariant is restored. This is not an animation of independently committed database operations. Switch to deletion, follow the parent and child key changes, and search a final tree to see the visited page path.</Prose>
    <RunnableExample example={externalMemoryExamples.btreeUpdates} />
    <Prose>The full program includes search, splitting, borrowing both ways, predecessor/successor replacement, merging and root contraction. It prechecks duplicate insertion and missing deletion so those operations leave the structure unchanged. That extra search adds a path traversal; it does not make the operation free. The public result is compared with a unique sorted set, not just with a picture that looks balanced.</Prose>
    <Prose>Search and update traverse O(h) levels. With the involved parent/sibling pages buffered, each level needs only a constant number of page accesses or writes, so textbook updates take O(1 + log<sub>t</sub>(N+1)) I/Os. Array shifts and copies within a page can take O(t), giving O(t(h+1)) CPU work for this simple implementation; search with binary search takes O((h+1)log t) comparisons. The visual trace additionally stores snapshots and is not the algorithm's space bound.</Prose>
    <Checkpoint prompt="For t=2, the root is [10] with leaf children [5] and [20]. Delete 5. Can either child lend a key?"><Prose>No: both are minimal one-key pages. Merge them with separator 10 to get [5,10,20], delete 5, and promote the sole child when the old root becomes empty. The new leaf root is [10,20]. No key other than 5 was removed, and the height drops from one edge to zero.</Prose></Checkpoint>

    <H2>4. Follow records through B+ leaves</H2>
    <Prose>A <strong>B+ tree</strong> uses internal pages only as guides; its leaf pages contain the records or record references. Internal separators are copies under a declared routing convention. Here each separator is the smallest key in its right child's subtree, so equality goes right. Leaves link to their neighbors in sorted order. A search for a separator value must continue to a leaf rather than return an internal “record.”</Prose>
    <BPlusContrastFigure />
    <Prose>To answer an inclusive range [low,high], descend once using low. Examine the destination leaf, retain qualifying records, and follow next-leaf links until the range is passed or leaves end. The first leaf may have no match if low lies in a gap; a final additional leaf may establish that there are no more qualifying records. These constant boundary costs belong in the bound.</Prose>
    <BPlusRangeLab />
    <Prose>The experiment bulk-builds its tree from sorted unique records: divide them into balanced leaf groups up to the selected capacity, link leaves, then group child pages upward and copy separator minima. This is a static bulk-layout model, with its own maximum leaf capacity/fanout parameters. It does not implement dynamic B+ split/delete rules or silently equate those parameters with the earlier minimum degree t.</Prose>
    <RunnableExample example={externalMemoryExamples.leafRanges} />
    <Prose>With page occupancy proportional to B, a range returning z leaf records costs O(1 + log<sub>B</sub>(N+1) + z/B) cold page reads. A sequential walk can reuse already loaded pages; an index root often stays cached. Next-leaf links imply logical order, not adjacent physical addresses. Fragmentation may affect latency even when the page count remains the same.</Prose>
    <Prose>The output term also depends on what a leaf contains. If it holds complete requested records, or a <strong>covering index</strong> supplies all requested fields, scanning leaf pages may suffice. If it holds row identifiers pointing to scattered payload pages, fetching z rows can require up to z additional reads. A <strong>clustered</strong> layout keeps payload records in the index's order, improving locality for that order; it cannot simultaneously cluster unrelated orders equally well. State the payload layout before claiming an index makes every range cheap.</Prose>
    <Checkpoint prompt="Why does equality at separator 20 continue right in this B+ model, while a textbook B-tree can stop at internal key 20?"><Prose>The B+ separator is a copy describing a child interval; the actual record belongs to the right subtree's leaf. In the textbook B-tree, the internal key itself is a stored set element and is absent from its children. Mixing the conventions can miss records or count them twice.</Prose></Checkpoint>

    <H2>5. Sort more than memory can hold</H2>
    <Prose>A file larger than RAM cannot be loaded into one ordinary list and sorted there under the intended budget. <strong>External merge sort</strong> first reads up to M records, sorts that chunk in memory and writes a sorted <strong>run</strong>. Repeat to obtain R=ceil(N/M) runs. Their global order is not yet sorted, but each run is.</Prose>
    <Prose>Merge runs using one page buffer for each input and one for output. With P=M/B record-buffer pages, at most k=P−1 input runs can be merged at once. Keep one current head per nonempty run in a min-heap. Emit the smallest head and advance only its run; refill that input's buffer when empty and write the output buffer when full. Flush the last partial output page too.</Prose>
    <Prose>Why is the minimum head safe? Every unconsumed value in a sorted run is at least that run's head. The minimum across heads is therefore the minimum across all unconsumed records. After emitting it and replacing its head, the statement is restored. Equal values must be emitted with their multiplicity. A heap uses O(log k) comparisons per emitted record; a linear scan of k heads works for a tiny illustration but changes CPU cost.</Prose>
    <ExternalMergeLab />
    <Prose>For N=32, B=4 and P=3, M=12 records and k=2. Initial runs contain 12,12,8 records. The first merge pass produces lengths 24,8; the second produces 32. Each materialized stage reads eight pages and writes eight pages. Including run formation, the total is 3×(8+8)=48 transfers. The lab deliberately rewrites even a singleton group; carrying its file forward or streaming the final result would change the exact ledger.</Prose>
    <H3>Keep ceilings and memory overhead visible</H3>
    <Prose>For N&gt;0, the number of merge passes after run formation is ceil(log_k R), with zero passes when R=1. Each run's page count is ceil(run length/B), so count partial pages per run rather than round a combined length blindly. For example, N=33 with B=4, P=3 gives runs 12,12,9, then 24,9, then 33. Each stage transfers nine pages in and nine out, for 54 transfers.</Prose>
    <pre className="external-calculation" aria-label="External sorting transfer bound"><code>{`P = M/B ≥ 3
k = P − 1 input runs
R = ceil(N/M) initial runs
merge passes = ceil(log_k R)
stages = 1 + merge passes
scan = ceil(N/B)
scale = max(1,
  log base (M/B) of (N/B))
sort I/O = O(scan × scale)`}</code></pre>
    <Prose>These formulas apply to N&gt;0; log_k denotes the logarithm with base k. The clamped logarithm retains the scan cost when the input fits in memory; the empty input costs zero in our implementation. This comparison-based, fixed-record model separates I/O from in-memory sorting work. The familiar external sorting lower bound has matching asymptotic form under its comparison/indivisibility assumptions; special integer structure, compression, cached data or a different output contract can change the problem. It is not a universal wall-clock formula.</Prose>
    <Prose>The buffer picture budgets records. An implementation also needs heap entries, cursors, file metadata and language/runtime space. Reserve that overhead before choosing a practical merge fan-in. The Python program below uses actual temporary binary files and bounded chunks/merge frontiers, but Python lists, tuples, sorting temporaries and buffered streams mean that P×B is <em>not</em> a strict process-RAM cap. It is a transparent teaching implementation, not a production memory limiter.</Prose>
    <RunnableExample example={externalMemoryExamples.externalSort} />
    <Prose>Each nonempty read of at most B signed 64-bit records counts one logical read; each corresponding output block counts one write. The initial input-file creation and the final small result readback for display are outside the reported sort ledger. Temporary files are created in an owned temporary directory and cleaned up automatically. Operating-system caching can service these calls without physical disk access, so the counters are reproducible algorithmic I/O, not measured device traffic or latency.</Prose>
    <Prose>For already sorted inputs, a merge can stream output to a join or aggregator and avoid materializing a final file. A top-z request can stop early. Those optimizations require a different accounting boundary; document it rather than subtract writes from a diagram while still claiming the fully materialized contract. Likewise, a stable sort needs tie ordering preserved through run creation and merging; this integer-only example establishes sorted multiplicities, not a labeled-record stability API.</Prose>

    <StreamingSortImplementation />
    <H2>6. Publish a root that can survive</H2>
    <Prose>The previous topic's persistent tree keeps old versions by copying changed nodes and sharing unchanged nodes. An in-memory root reference makes a version reachable while the process lives. <strong>Durability</strong> adds a separate question: after losing volatile memory, does the stored root reach complete stored pages? Copying an object and writing a page are different operations, and neither alone proves a committed transaction.</Prose>
    <Prose>Consider an abstract <strong>shadow-page</strong> update. Retain an immutable old root and leaf containing value 5. Prepare a new leaf containing 9 and a new root pointing to it. Make both new pages durable; only then publish the new root in durable metadata. Unchanged branches can remain shared. A copied path of h+1 pages may require h+1 page writes even though only one logical value changed, plus the root-publication mechanism.</Prose>
    <ShadowCommitLab />
    <Prose>This model assumes a single writer, retained immutable old pages, correctly completed durable page writes and an <strong>atomic, durable root-metadata publication</strong>. Those are protocol assumptions, not properties supplied automatically by an ordinary file write or a “4 KB page” label. Before root publication, recovery follows the old root and returns 5. After publication, every reachable new page is durable, so it returns 9. Publishing the new root first exposes a reference to a missing page.</Prose>
    <Prose>The safe ordering proof is about reachability at every possible interruption. Old metadata always reaches old durable pages. The metadata changes only after the new root's entire reachable graph is durable. If the switch is atomic, recovery sees one complete graph or the other. If the root update can tear, or a supposedly durable write can be lost, that proof's premises fail and an actual protocol needs additional protection.</Prose>
    <RunnableExample example={externalMemoryExamples.crashRoots} />
    <Prose>A crash after publication but before the client receives acknowledgement leaves value 9 committed while the client is uncertain. “No reply” does not prove “no commit.” An application may need an idempotent request identifier or a status query to retry safely. The model also retains obsolete pages: safe reclamation requires knowing that no retained root or reader needs them. Concurrent writers, garbage collection, torn-page protection and recovery metadata deserve their own designs.</Prose>
    <Prose>CMU's shadow-paging material explains the page-copy/root-switch idea and its fragmentation/reclamation costs. SQLite's official atomic-commit article describes a different, rollback-journal protocol: preserve original pages and make the journal safely available before overwriting database pages, with explicit recovery and flush assumptions. That article is evidence about why ordering matters, not evidence that SQLite implements this lab's shadow-root algorithm. We do not simulate real fsync, power loss or an entire transaction engine.</Prose>

    <H2>7. Choose for the workload</H2>
    <Prose>A sensor archive provides a useful combination. An individual device/time lookup benefits from a B+ index. A time interval can follow ordered leaves, with payload locality deciding the extra reads. A new unsorted import may be cheaper to externally sort and bulk-load than to insert every record through separate cold root-to-leaf paths. That conclusion depends on batch size, caching, existing index state and update requirements; count the alternatives for the actual workload.</Prose>
    <H3>A sorted merge can connect datasets</H3>
    <Prose>Suppose one sorted stream contains device measurements and another contains calibration records. Compare their current device IDs. If one is smaller, advance that stream; if equal, output the joined record and advance both. The smaller unmatched ID cannot match a later ID in the other sorted stream. With unique keys this gives a complete streaming join using two input buffers and an output buffer.</Prose>
    <RunnableExample example={externalMemoryExamples.batchJoin} />
    <Prose>The example explicitly assumes sorted unique keys. Duplicates require gathering equal-key groups and emitting all required pairs, which can expand output dramatically and require spilling a large group. With sorted inputs of lengths N and K and output length Z, the ideal buffered unique-key join uses O(ceil(N/B)+ceil(K/B)+ceil(Z/B)) transfers. If the inputs are unsorted, include the cost of arranging them first.</Prose>
    <Prose>A less obvious application is preparing machine-learning data. Grouping examples by source shard can reduce page or remote-object reads, while globally random example order may destroy locality. Block shuffling and prefetching can improve throughput, but they change ordering and possibly the training distribution seen over short windows. The storage model suggests what to measure; it does not prove that a new sampler preserves every statistical guarantee from the Randomized Algorithms lesson.</Prose>
    <LessonTable caption="Match the layout and accounting boundary to the work" headers={['Workload', 'Useful mechanism', 'Question to retain']} rows={[['Repeated point lookups', 'Wide ordered tree and cached upper pages.', 'Which page visits actually miss? What payload must be fetched?'], ['Large ordered range', 'Linked leaves and clustered/covering records.', 'How many output pages and scattered payload reads are required?'], ['Unsorted bulk import', 'Run formation, multiway merging and bulk layout.', 'How many materialized passes fit the available buffer budget?'], ['Frequent small updates', 'Dirty-page coalescing or a separately designed buffered structure.', 'What are write amplification, latency and durability requirements?'], ['Remote or multi-level storage', 'Batching, prefetching and measured workload-aware layout.', 'Which latency, bandwidth, concurrency and pricing costs does simple I/O counting omit?']]} />
    <Prose>Sequential and random I/O differ on rotating disks; SSDs remove mechanical seeks but still have access-size, parallelism and write-management effects. CPU time, serialization, network delay and device queues can dominate in different situations. No fabricated speed chart can rank implementations independently of those conditions. Count transfers to understand an algorithm, then benchmark the real system with stated data, cache state, hardware and versions.</Prose>
    <Prose>There are deeper branches. Cache-oblivious algorithms seek locality across unknown B and M using recursive layout without explicitly choosing a page size. Buffer trees batch deferred updates down a search structure; LSM designs merge sorted components and trade update cost against reads and compaction. These are distinct mechanisms, not synonyms for a cached B-tree. This lesson establishes the page/search/merge contracts needed to study them, without claiming to implement their complete designs.</Prose>

    <ExternalMemoryLibraryBridge />
    <H2>8. Practise a changed storage contract</H2>
    <Prose>Work these with the examples closed. Draw the pages or runs first, state the invariant and counting boundary, then use a program to check the answer. Reattempt a changed case later and mix it with earlier hashing, heap, proof and persistence problems.</Prose>
    <section className="lesson-check external-exercise"><h3>1 · Count dirty transfers, not updates</h3><p>Use B=2, one cold LRU frame and write allocation. Perform write 0, write 1, read 2, read 0, then flush. Give reads, hits, eviction writes and final-flush writes.</p><details><summary>Show explanation</summary><p>The first write reads page 0 and marks it dirty; write 1 hits. Read 2 evicts dirty page 0, writing it once, and loads page 1. Read 0 evicts clean page 1 and loads clean page 0. Totals: three reads, one hit, one eviction write and zero final-flush writes. If an implementation forgets the eviction write it has lost the update, not discovered a free optimization.</p></details></section>
    <section className="lesson-check external-exercise"><h3>2 · Separate bytes from height parameters</h3><p>A hypothetical internal page has 1024 bytes, a 32-byte header, 8-byte keys and 8-byte child references. Find its maximum children. Separately, bound the edge height of a textbook t=3 B-tree with 53 keys.</p><details><summary>Show explanation</summary><p>c children occupy 32+8(c−1)+8c=24+16c bytes. c=62 needs 1016 bytes; c=63 needs 1032, so 62 is maximal. For the separate occupancy model, 53≥2·3^h−1 gives 3^h≤27, hence h≤3 and at most four cold page visits. The two questions have different parameters; the byte calculation alone does not specify a complete tree's minimum fill policy.</p></details></section>
    <section className="lesson-check external-exercise"><h3>3 · Repair the separator during deletion</h3><p>With t=2, root [10] has leaf children [2,5] and [12]. Delete 12 using top-down preparation. Then repeat with left child [5].</p><details><summary>Show explanation</summary><p>First borrow from the left through the root: 10 moves down to the right leaf, 5 moves up, and the left leaf becomes [2]. The right leaf temporarily holds [10,12], then deletion leaves [10]. Root [5] separates [2] and [10], storing exactly {(2, 5, 10)}. With left [5], neither sibling can lend; merge [5,10,12], delete 12 and promote the sole leaf root [5,10]. Copying 5 directly into the right child without replacing the root separator would break the search intervals.</p></details></section>
    <section className="lesson-check external-exercise"><h3>4 · Query gaps and separator equality</h3><p>Use the B+ lab's default capacity 3 and fanout 3. Trace [20,20], [36,40] and reversed [9,3]. Count cold reads and returned records.</p><details><summary>Show explanation</summary><p>[20,20] follows I6→I5→L2, reading three pages and returning 20. Equality at the root separator routes right. [36,40] follows I6→I5→L3, also reading three pages but returning none; L3 is the final leaf. The reversed range is rejected as empty before reading a page, so it costs zero. A zero-result normal query need not cost zero I/O.</p></details></section>
    <section className="lesson-check external-exercise"><h3>5 · A small memory change can remove a whole pass</h3><p>Sort N=35 records with B=4 and P=4 using the materialized policy, including singleton rewrites. List run lengths and total transfers. Repeat with P=3.</p><details><summary>Show explanation</summary><p>With P=4, M=16 and fan-in 3, initial lengths are 16,16,3 and one merge produces 35. Both stages read and write 4+4+1=9 pages, so total transfers are 36. With P=3, initial lengths are 12,12,11, then 24,11, then 35. Each stage reads and writes nine pages, giving 54. The improvement comes from the pass schedule, not from fewer records or an invented device speed.</p></details></section>
    <section className="lesson-check external-exercise"><h3>6 · Crash after a page write</h3><p>The new leaf is durable but the new root is not. What does recovery see under the safe and early-root protocols? What if both pages and the root switch are durable, but the reply to the client is lost?</p><details><summary>Show explanation</summary><p>The safe protocol still publishes oldRoot, whose durable graph returns 5. The early protocol publishes newRoot before that page exists durably, so recovery encounters a missing page; a durable new leaf cannot repair the missing link. After a completed root switch, recovery returns 9 even if the reply was lost. The client is uncertain, not entitled to assume the transaction failed.</p></details></section>
    <section className="lesson-check external-exercise"><h3>7 · Rebuild the pipeline with changed data</h3><p>Run the temporary-file sorter on [9,1,7,1,4,8,2,6,5,3,0,9,2] with B=2 and P=3. Predict run lengths, output and transfers before execution. Insert the distinct result keys into the B-tree, delete 1,5,9, and check both sorted membership and page occupancy. Finally explain what extra protocol would be needed to publish that changed tree durably.</p><details><summary>Show explanation</summary><p>The run lengths are 6,6,1, then 12,1, then 13. Each of the three stages reads and writes seven pages: 42 transfers. Sorted output is [0,1,1,2,2,3,4,5,6,7,8,9,9]; duplicates survive sorting. The unique B-tree starts with 0 through 9 and finishes [0,2,3,4,6,7,8]. A sorted result alone does not establish valid occupancy or equal leaf depth; inspect every page and child interval. The in-memory tree code has no durable commit API: writing new reachable pages followed by a safely implemented root publication requires the additional storage assumptions from section 6.</p></details></section>
    <Prose>This completes the DSA module's current sequence: algorithms now have explicit CPU, space, probability, correctness and transfer contracts. In the Complete Curriculum, the next module begins with <strong>Vectors, Matrices &amp; Tensor Operations</strong>, moving into mathematical foundations. Use the route's Next control for your selected path and module context; it preserves the declared learning sequence.</Prose>
    <DsaPractice practice={externalMemoryPractice} />
    <Sources>{[{
        label: 'MIT 6.851 · Lecture 7: Memory Hierarchies',
        href: 'https://courses.csail.mit.edu/6.851/spring12/lectures/L07.html',
        note: 'Official lecture page with video, slides and scribe notes. An advanced alternate explanation of the M/B model, B-trees and external sorting. The lecture page and relevant scribe sections were inspected; full video playback was not reviewed. Historical hardware tables are not current specifications.'
      }, {
        label: 'Open Data Structures · B-Trees',
        href: 'https://opendatastructures.org/ods-python/14_2_B_Trees.html',
        note: 'Open textbook treatment of search, height, splitting and deletion. Useful for a second written derivation. Its degree notation and bottom-up repair convention differ from our top-down implementation; translate the invariants rather than combine half of each algorithm.'
      }, {
        label: 'Cornell CS4320 · External Sorting',
        href: 'https://www.cs.cornell.edu/courses/cs4320/2008fa/slides/20081008_ExtSort.pdf',
        note: 'Worked multiway-merge and buffer/pass calculations. Its N is often a page count, whereas this lesson uses N records. Compare units and rounding before substituting numbers. Relevant merge and pass sections were inspected.'
      }, {
        label: 'CMU 15-415/615 · Crash Recovery, Part 1',
        href: 'https://15415.courses.cs.cmu.edu/spring2014/slides/24Recovery1.pdf',
        note: 'Shadow-paging/root-switch discussion and its copying, fragmentation and reclamation tradeoffs; PDF pages 7–10 were inspected. The lab states an abstract atomic durable-root assumption explicitly rather than claiming a hardware implementation.'
      }, {
        label: 'SQLite · Atomic Commit',
        href: 'https://www.sqlite.org/atomiccommit.html',
        note: 'Official explanation of rollback journaling, buffered/reordered writes, flush assumptions and recovery. Introduction, hardware assumptions and journal/data commit staging were inspected. This is a contrasting real protocol, not the implementation of our shadow-page lab.'
      }].map(source => <li key={source.href}><a href={source.href} target="_blank" rel="noreferrer">{source.label}</a> — {source.note}</li>)}</Sources>
  </div>
};
