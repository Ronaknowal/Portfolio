import { Code, H2, H3, Prose } from '../../components/content';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram';
import libraryOutput from '../spectral-graph-library-output';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import SpectralLab from '../../components/lesson-labs/SpectralLab';
import { LaplacianRowFigure } from '../../components/lesson-labs/InferenceMechanismFigures';
import { SpectralAnchorFigure, SpectralCutLab, SpectralEmbeddingLab, SpectralFilterLab, SpectralCoordinateFigure, SpectralWalkFigure, SpectralResistanceFigure } from '../../components/lesson-labs/SpectralGraphLabs';
import { spectralGraphExamples } from '../spectral-graph-examples.js';
function Example({
  id,
  children
}) {
  const example = spectralGraphExamples.find(item => item.id === id);
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="lesson-check"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Spectral Graph Theory',
  readTime: '~70 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot spectral-lesson">
    <LessonIntro exampleKind="Optional NumPy" prerequisites="Read weighted adjacency and degree matrices, multiply a matrix by a vector, and interpret an orthonormal eigenbasis. Graph Fundamentals and Eigenvalues develop those skills; we refresh the relevant identities. Calculus is used only in the diffusion and optional variational arguments. No prior clustering library or Fourier course is required." sections={[['1-start-with-values-on-a-graph', 'Values & disagreement'], ['2-find-the-graph-s-slow-patterns', 'Modes & the bridge'], ['3-turn-a-continuous-vector-into-a-cut', 'Cut objectives'], ['4-normalize-degrees-and-sweep-for-a-bottleneck', 'Normalization & sweep'], ['5-embed-nodes-and-form-several-groups', 'Complete clustering'], ['6-transform-filter-and-reconstruct-a-signal', 'Graph filters'], ['7-predict-diffusion-and-recognize-periodicity', 'Limits & basis choices'], ['8-measure-a-connection-s-electrical-resistance', 'Current & resistance'], ['9-make-the-computation-answer-the-right-question', 'Practical decisions'], ['10-practise-on-changed-graphs', 'Practice & next']]}>Six documents have many links within two groups and just one weak link between them. How can we discover the groups without trying every partition? The same mathematics also explains how a measurement spreads through a network. We will follow weighted differences into eigenvectors, then turn those vectors into decisions and signals.</LessonIntro>

    <H2>1. Start with values on a graph</H2>
    <Prose>A <strong>node</strong> is an object and an <strong>edge weight</strong> says how strongly two objects are related. Here documents A, B and C form one triangle; D, E and F form another. Every triangle edge has weight 1. The C–D bridge has weight 0.2. These are invented similarities for an inspectable example, not measured document scores. The graph is finite, undirected, has nonnegative weights and no self-loops.</Prose>
    <Prose>A <strong>graph signal</strong> puts one number at every node. It might be a temperature, a score or a temporary coordinate used to discover a partition. Signal x=[1,1,1,−1,−1,−1] is constant within each triangle. Its only disagreement is across the weak bridge. Thus “smooth” on this graph can include a large change between weakly connected groups; it does not mean a smooth curve across the screen.</Prose>
    <SpectralAnchorFigure />
    <Prose>Let A be the symmetric adjacency matrix, dᵢ the sum of row i, and D the diagonal matrix of degrees. The <strong>combinatorial Laplacian</strong> L=D−A computes weighted disagreement with neighbors. Matrix L has one row and column per node; multiplying it by x produces one signed difference total per node.</Prose>
    <MathBlock>{String.raw`\begin{aligned}(Lx)_i&=d_i x_i-\sum_j A_{ij}x_j\\&=\sum_j A_{ij}(x_i-x_j).\end{aligned}`}</MathBlock>
    <Prose>Node C has degree 1+1+0.2=2.2. In A–F order its row is [−1,−1,2.2,−0.2,0,0]. Substituting our signal gives (Lx)C=(1−1)+(1−1)+0.2(1−(−1))=0.4. E and F do not occur because C has no edges to them. A signed row value may be negative; it is not yet an energy.</Prose>
    <LaplacianRowFigure />
    <Prose>Multiply each row result by its own signal value and add. Pair the i→j and j→i terms. Each undirected edge contributes Aᵢⱼ(xᵢ²−2xᵢxⱼ+xⱼ²), a nonnegative weighted square:</Prose>
    <MathBlock>{String.raw`\begin{aligned}x^\mathsf TLx&=\sum_{\{i,j\}\in E} A_{ij}(x_i-x_j)^2\\&=\tfrac12\sum_{i,j}A_{ij}(x_i-x_j)^2\ge0.\end{aligned}`}</MathBlock>
    <Prose>The first sum counts each undirected edge once; the double sum counts it twice. This <strong>graph energy</strong> is 0.2×2²=0.8 for our two-group signal. It differs from its squared length xᵀx=6. If x has temperature units and weights are dimensionless, this quadratic expression has squared-temperature units; calling it energy does not automatically make it joules.</Prose>
    <Checkpoint prompt="If every node has value 7, do high-degree nodes have a larger Laplacian output?">
      <Prose>No. Every neighbor difference is zero, so Lx=0 everywhere, even when degrees differ. The diagonal degree term exactly cancels the weighted neighbor sum. Degree alone does not measure signal disagreement.</Prose>
    </Checkpoint>
    <Prose><strong>Running the examples:</strong> save each NumPy mechanism program below as a separate Python file and run it with <Code>python filename.py</Code> in an environment with NumPy installed. Those examples were executed with Python 3.12.14 and NumPy 2.3.5. The maintained-library bridge later on this page lists its additional SciPy/scikit-learn dependencies. Displayed decimal results are rounded where the code says so; eigenvector signs and repeated-eigenspace bases can differ across valid solvers. The browser labs compute bounded examples, not arbitrary Python.</Prose>
    <Example id="energy"><Prose>The equal first pair spends no energy. The other edges contribute 0.5×3²=4.5 and 3×(−2)²=12, giving 16.5. Dividing by squared length 10 gives 1.65; this quotient is a property of that chosen signal, not automatically an eigenvalue.</Prose></Example>

    <H2>2. Find the graph’s slow patterns</H2>
    <Prose>An eigenvector u satisfies Lu=λu: weighted neighbor differences reproduce the same pattern, scaled by λ. Because L is real symmetric, choose an orthonormal basis u₁,…,uₙ. Order the eigenvalues increasingly. Nonnegative energy forces λ≥0, and L1=0 gives a constant zero mode. For a unit eigenvector, uᵀLu=λ; for any nonzero scaling, it is λ‖u‖².</Prose>
    <Prose>Zero energy means every positive-weight edge joins equal values. Following a path propagates that equality through a component. Conversely, any signal constant separately on each component has zero energy. Therefore the number of independent zero modes equals the number of connected components, including isolated vertices. This is an exact statement about positive edges, not about rounded eigenvalues.</Prose>
    <SpectralLab />
    <Prose>When the graph is connected, the constant unit vector is u₁=1/√n. To find a nonconstant smooth pattern, remove its direction and keep the remaining signal at unit length. Otherwise the zero vector would minimize energy. Write x=Σₖcₖuₖ. Orthogonality gives ‖x‖²=Σcₖ² and xᵀLx=Σλₖcₖ². Requiring x⊥1 removes c₁, so the smallest attainable unit energy is λ₂:</Prose>
    <MathBlock>{String.raw`\lambda_2=\min_{\substack{x\ne0\\x^\mathsf T\mathbf1=0}}\frac{x^\mathsf TLx}{x^\mathsf Tx}.`}</MathBlock>
    <Prose>A minimizing direction is a <strong>Fiedler vector</strong>; λ₂ is the <strong>algebraic connectivity</strong>. Our λ₂≈0.122 has positive entries on ABC and negative ones on DEF. C and D are nearer zero than their partners because the bridge makes their disagreement costly. This is a continuous low-energy coordinate, not yet a proof of the best discrete grouping.</Prose>
    <Example id="original"><Prose>The residual check confirms the computed vector satisfies its eigen-equation within tolerance; unit length fixes its otherwise arbitrary scale. A global sign flip swaps the two names of a sign partition. The triple eigenvalue 3 permits rotations within a three-dimensional eigenspace, so different solvers need not print the same higher-mode columns.</Prose></Example>
    <Checkpoint prompt="Set the bridge to zero. Why is it misleading to call the returned second vector the unique group boundary?">
      <Prose>The kernel now has dimension two: one independent constant per triangle. Any orthonormal rotation of those two component indicators is a valid zero-mode basis. Components remain unambiguous, but a solver’s particular second column need not be the positive-versus-negative pattern you expected.</Prose>
    </Checkpoint>
    <details><summary>Why adding edge weight cannot lower this λ₂</summary><Prose>Increasing edge a–b by δ≥0 adds δ(eₐ−eᵦ)(eₐ−eᵦ)ᵀ to L. For every x, its Rayleigh numerator increases by δ(xₐ−xᵦ)². Minimizing over the same unit vectors perpendicular to 1 cannot lower the minimum. This argument is for the combinatorial L with fixed nodes; normalized eigenvalues need not obey the same comparison because D changes too. Multiplying every weight by c&gt;0 gives cL, so eigenvalues scale by c and the eigenspaces can stay the same.</Prose></details>

    <H2>3. Turn a continuous vector into a cut</H2>
    <Prose>A <strong>cut</strong> chooses a nonempty proper node set S and its complement T. Its weight c(S,T) is the sum of crossing edge weights, once each. Merely minimizing this weight may peel off a weakly attached singleton. If a useful grouping needs substantial groups, specify what “substantial” means before choosing an algorithm.</Prose>
    <LessonTable caption="Three ways to charge the same crossing edges" headers={['Quantity', 'Definition', 'What the denominator measures']} rows={[['RatioCut', 'c(S,T)(1/|S|+1/|T|)', 'Number of nodes; discourages a very small side.'], ['Normalized cut (Ncut)', 'c(S,T)(1/vol(S)+1/vol(T))', 'Total incident weight on each side.'], ['Conductance φ(S)', 'c(S,T)/min(vol(S),vol(T))', 'Crossing weight relative to the smaller-volume side.']]} />
    <Prose>Here vol(S)=Σᵢ∈S dᵢ: an internal edge contributes twice to its side’s volume; a crossing edge contributes once to each side. For ABC versus DEF, the crossing weight is 0.2, each size is 3, and each volume is 6.2. RatioCut is 0.2(1/3+1/3)=0.13333; Ncut is 0.2(1/6.2+1/6.2)=0.064516; conductance is 0.2/6.2=0.032258. These definitions have no extra half in the two-way formulas used here; check conventions when comparing references.</Prose>
    <Prose>Why does an eigenvector enter? For a fixed cut, put one positive constant on S and one negative constant on T. Choose the constants so the vector is centered and unit length. Write s=|S|, r=|T| and n=s+r:</Prose>
    <MathBlock>{String.raw`x_i=\begin{cases}\sqrt{r/(ns)},&i\in S,\\-\sqrt{s/(nr)},&i\in T.\end{cases}`}</MathBlock>
    <Prose>Its sum is zero, its squared length is 1, and only crossing edges see a difference. The squared difference is n/(sr)=1/s+1/r. Thus xᵀLx equals RatioCut exactly. Searching such two-valued vectors is a discrete partition problem. Allowing <em>any</em> centered unit vector enlarges the feasible set and gives the Fiedler minimization. Therefore λ₂ lower-bounds the optimum RatioCut; it does not promise that thresholding its vector reaches that optimum.</Prose>
    <Prose>For the balanced triangle split, the centered unit vector is [1,1,1,−1,−1,−1]/√6. Its quotient is 0.8/6≈0.13333, above λ₂≈0.122. The continuous optimum saves some energy by making C and D less extreme. Rounding back to a partition removes that flexibility.</Prose>
    <Prose>A zero sign threshold is a simple candidate. A <strong>sweep</strong> sorts the node coordinates and tests thresholds between successive distinct values. Every such threshold has an explicit crossing weight and balance measure. Equal coordinates stay together when a true scalar threshold cannot separate them; arbitrary tie breaking changes the rounding rule.</Prose>

    <H2>4. Normalize degrees and sweep for a bottleneck</H2>
    <Prose>When degrees differ greatly, equal node counts and equal incident weight are different goals. With every dᵢ&gt;0, define the symmetric normalized Laplacian 𝓛=D⁻¹/²LD⁻¹/². Its zero vector direction is √d, not generally the constant vector. To interpret its eigenvector v as a per-node threshold coordinate, set y=D⁻¹/²v. Then Ly=νDy and the weighted mean Σdᵢyᵢ is zero for a nonzero normalized mode.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathcal L&=D^{-1/2}LD^{-1/2}\\&=I-D^{-1/2}AD^{-1/2},\\\frac{v^\mathsf T\mathcal Lv}{v^\mathsf Tv}&=\frac{y^\mathsf TLy}{y^\mathsf TDy},\\v&=D^{1/2}y.\end{aligned}`}</MathBlock>
    <SpectralCoordinateFigure />
    <Prose>For volume a=vol(S), b=vol(T), set yᵢ=√(b/[a(a+b)]) on S and −√(a/[b(a+b)]) on T. Direct substitution gives yᵀD1=0, yᵀDy=1 and yᵀLy=c(S,T)(1/a+1/b)=Ncut. Relaxing its two allowed values gives ν₂, the second eigenvalue of 𝓛. Use <strong>y=v₂/√d</strong> for the normalized cut sweep below, not the untransformed v₂ coordinates.</Prose>
    <SpectralCutLab />
    <Prose>A nonempty set can still have volume zero if it consists of isolates. We define D⁻¹/² as zero at zero degree and use the <em>product</em> D⁻¹/²LD⁻¹/². This gives an isolated node a zero row and diagonal. The blind formula I−D⁻¹/²AD⁻¹/² instead puts a 1 there and is different. Handle isolates separately before a positive-volume cut/clustering algorithm; do not silently divide by zero.</Prose>
    <Example id="isolates"><Prose>The third node contributes another zero mode under the stated product convention. On the two positive-degree nodes both formulas agree. This is a convention difference to resolve, not numerical noise.</Prose></Example>
    <H3>What a small normalized eigenvalue actually guarantees</H3>
    <Prose>For a connected undirected nonnegative weighted graph with at least two nodes and positive degrees, let φ* be the minimum conductance over nonempty proper sets. The normalized spectrum lies in [0,2]. One reason is (a−b)²≤2(a²+b²): apply it edge by edge to the normalized energy. <strong>Cheeger’s inequality</strong> links a continuous low-energy mode to a discrete bottleneck:</Prose>
    <MathBlock>{String.raw`\frac{\nu_2}{2}\le\phi_*\le\sqrt{2\nu_2}.`}</MathBlock>
    <Prose>For the lower bound, every cut’s Ncut is at least ν₂ by the relaxation above. If a≤b, Ncut=c(1/a+1/b)≤2c/a=2φ(S). Thus ν₂≤2φ(S) for every cut. The upper bound says that a suitable threshold of the normalized coordinate y reaches conductance at most √(2ν₂). It does not say every threshold, especially zero, has that quality. The tiny example has ν₂≈0.059358 and φ*=0.032258; the bound’s upper end ≈0.34455 is valid but loose.</Prose>
    <details><summary>Deeper derivation: why some sweep threshold has low conductance</summary>
      <Prose>Start with yᵀD1=0 and quotient ρ=yᵀLy/(yᵀDy). Subtract a degree-weighted median m, obtaining z=y−m1. Each strict side of zero has at most half the volume. Shifting leaves edge differences unchanged. Because y was weighted-mean zero, Σdᵢ(yᵢ−m)²=Σdᵢyᵢ²+m²Σdᵢ, so the quotient cannot increase.</Prose>
      <Prose>Scale the nonconstant z so zmin²+zmax²=1. Zero lies between these extremes; one side may contain only zero-valued nodes. Choose a threshold t between the extremes with density 2|t|. This integrates to 1. A node of value zᵢ belongs to the smaller side for a threshold interval of probability zᵢ². Consequently the expected smaller-side volume is Σdᵢzᵢ².</Prose>
      <Prose>An edge is cut precisely when t lies between its endpoint values. Its crossing probability is at most |zᵢ−zⱼ|(|zᵢ|+|zⱼ|). Multiply by its weight, sum once per edge, and apply Cauchy–Schwarz. The first square-sum is zᵀLz; the second is at most 2Σdᵢzᵢ². Hence:</Prose>
      <MathBlock>{String.raw`\begin{aligned}V_t&=\min(\operatorname{vol}S_t,\operatorname{vol}T_t),\\\mathbb E[c(S_t,T_t)]&\le\sqrt{2z^\mathsf TLz\;z^\mathsf TDz}\\&\le\sqrt{2\rho}\;\mathbb E[V_t].\end{aligned}`}</MathBlock>
      <Prose>If every nontrivial threshold had ratio strictly above √(2ρ), its weighted expectation would contradict this inequality. So one threshold works. Sorting preserves the same family of thresholds after shifting/scaling. Set ρ=ν₂ for the exact second normalized mode. A numerical sweep is checked with its actual values; finite residuals and a tie tolerance do not constitute an exact theorem proof.</Prose>
    </details>
    <Example id="sweep"><Prose>The sweep and exhaustive enumeration agree on this six-node fixture. Enumeration checks all 62 nontrivial subsets, including complementary duplicates. It is feasible here only because the graph is tiny; the result is not a proof that a spectral sweep is globally optimal for all graphs.</Prose></Example>

    <H2>5. Embed nodes and form several groups</H2>
    <Prose>For more than two groups, one coordinate is restrictive. Choose k, compute the k smallest eigenvectors of 𝓛, and put them in the <em>columns</em> of an n×k matrix U. Then node i becomes the <em>row</em> Uᵢ,:—its values across all k modes. These are coordinates describing one node, not k different documents. The symmetric-normalized algorithm used here divides each nonzero row by its Euclidean length before clustering.</Prose>
    <Prose>The exact disconnected case explains that row step. Suppose the graph has k positive-volume components. Choose normalized zero modes vⱼ=D¹/²1Cⱼ/√vol(Cⱼ). A node i in component j then has a row with one nonzero entry √(dᵢ/vol(Cⱼ)). Its length depends on degree, but normalizing the row leaves the same unit coordinate eⱼ for every node in that component. Any orthonormal rotation of all k zero modes rotates these k centers together and preserves their distances. Weak connections perturb this clean picture.</Prose>
    <Prose>Now group these row points using <strong>k-means</strong>. Fix k centers and assign each point to its nearest center in squared Euclidean distance. Then hold assignments fixed and move each center to the mean of its assigned points. These two steps cannot increase their respective squared-distance objective. Repeating can reach a local fixed point; different starts can reach different groupings. An empty group needs an explicit policy.</Prose>
    <SpectralEmbeddingLab />
    <Prose>The point coordinates stay fixed during assignment and center updates. Changing the graph recomputes them, so the lab restarts its trace. It retains and flags an empty center to make that failure visible. The complete program below instead discards a restart with an empty group or unfinished iteration limit, then selects the lowest-loss converged run with all k groups occupied. Those are explicit teaching choices, not universal k-means defaults.</Prose>
    <Example id="clusters"><Prose>The nine-node fixture yields ABC, DEF and GHI. Sorting group names only stabilizes the printed labels; it does not use those names to decide membership. NumPy’s generator seed makes the restart list reproducible. The spread start guarantees a useful candidate for this fixture, but neither restarts nor a low k-means loss prove that the input graph represents meaningful real-world classes.</Prose></Example>
    <Prose>The gap νₖ₊₁−νₖ can suggest a low-dimensional subspace separated from higher modes. Choosing k still needs a question, component inspection and stability under justified changes to the affinity. A conspicuous gap is not a theorem about the true number of document categories. If more components exist than retained modes, some rows may be zero or arbitrary mixtures; handle the components rather than force a normalization of zero.</Prose>
    <LessonTable caption="Keep each spectral clustering convention together" headers={['Method', 'Coordinates before clustering', 'Row step']} rows={[['Combinatorial', 'k low eigenvectors of L', 'No required row normalization in this standard version.'], ['Random-walk / generalized', 'Ly=νDy eigenvectors', 'Different degree geometry; do not copy the symmetric row step blindly.'], ['Symmetric normalized, used here', 'k low eigenvectors of 𝓛', 'Normalize each nonzero node row, then cluster.']]} />

    <H2>6. Transform, filter and reconstruct a signal</H2>
    <Prose>Use the combinatorial L again. Its orthonormal eigenvectors form coordinates for any node signal x. A <strong>graph Fourier coefficient</strong> cₖ=uₖᵀx is the signed amount of mode k in the signal. Full reconstruction adds those amounts back. No information is lost by this change of basis:</Prose>
    <MathBlock>{String.raw`\begin{aligned}c&=U^\mathsf Tx,\qquad x=Uc,\\\|x\|^2&=\sum_k c_k^2,\\x^\mathsf TLx&=\sum_k\lambda_kc_k^2.\end{aligned}`}</MathBlock>
    <Prose>The last two quantities answer different questions: squared signal magnitude and weighted roughness. A large constant signal has large magnitude but zero roughness. “Frequency” here orders graph variation through λ, not cycles per second or the number of color changes in a drawing. An abrupt change across a strong edge is expensive; the graph determines the meaning of roughness.</Prose>
    <Prose>A <strong>spectral filter</strong> chooses a gain h(λₖ) for each mode, multiplies cₖ by that gain, and reconstructs. Keeping the first m coefficients gives the orthogonal projection onto that selected subspace. Its squared reconstruction error is exactly Σₖ&gt;ₘ cₖ². This is the best approximation <em>within that subspace</em>, not automatically the best estimate of an unknown true signal.</Prose>
    <MathBlock>{String.raw`x_{\rm out}=U\operatorname{diag}(h(\lambda_k))U^\mathsf Tx.`}</MathBlock>
    <LessonTable caption="Different goals produce different gains" headers={['Filter', 'Gain', 'Interpretation']} rows={[['First m modes', '1 for k≤m, otherwise 0', 'Project onto a chosen low-energy subspace. Mind repeated eigenvalues at the boundary.'], ['Heat at time t≥0', 'exp(−tλ)', 'Let neighbor differences relax continuously.'], ['Ridge smoothing, α≥0', '1/(1+αλ)', 'Balance closeness to the observed signal against graph roughness.']]} />
    <SpectralFilterLab />
    <H3>A complete hand calculation on three nodes</H3>
    <Prose>Take the unit-weight path A–B–C. Its L has rows [1,−1,0], [−1,2,−1], [0,−1,1]. A convenient ordered eigenbasis is u₁=[1,1,1]/√3, u₂=[1,0,−1]/√2, u₃=[1,−2,1]/√6, with eigenvalues 0,1,3. Substitution verifies each pair. For the impulse x=[1,0,0], the coefficients are 1/√3,1/√2,1/√6.</Prose>
    <Prose>At ridge penalty α=1, the gains are 1,1/2,1/4. Reconstruct one node at a time. A gets 1/3+1/4+1/24=5/8; B gets 1/3−2/24=1/4; C gets 1/3−1/4+1/24=1/8. The impulse spreads, while the total stays 1. This gives a useful check on a library result before dealing with a bigger graph.</Prose>
    <Prose>Where did ridge’s gain come from? Minimize ‖z−x‖²+αzᵀLz over z. Differentiating gives (I+αL)z=x. Its eigenvalues are 1+αλ&gt;0, so the minimizer exists and is unique for α≥0. In the eigenbasis, the equation is simply (1+αλₖ)ẑₖ=cₖ. We can solve the linear system without explicitly forming an inverse or computing every eigenvector.</Prose>
    <Example id="filters"><Prose>The three outputs differ because their gains differ. The reported projection change equals its discarded coefficient energy. Heat reduces roughness from 5.672 to about 0.567868 in this example, but it also changes the group contrast. Calling that change “denoising” requires a justified belief that the removed variation was noise.</Prose></Example>
    <Prose>A temperature network might legitimately have smooth neighboring values. A boundary between two materials, opposing political groups, or an intentional difference between connected biological populations may be the signal of interest. Strong smoothing can erase it. Compare plausible graphs and held-out measurement errors when there is a prediction task; mathematical smoothness alone is not validation.</Prose>

    <H2>7. Predict diffusion and recognize periodicity</H2>
    <Prose>Suppose each node’s value changes at the negative of its current weighted neighbor disagreement: x′(t)=−Lx(t). This is a <strong>continuous-time diffusion</strong> equation with equal node capacities. In mode coordinates c′ₖ=−λₖcₖ, whose solution is cₖ(t)=exp(−tλₖ)cₖ(0). That is the heat filter above. If weights are inverse-time rates, tλ is dimensionless; for abstract similarities, t is a chosen mathematical smoothing parameter.</Prose>
    <Prose>The sum stays fixed because 1ᵀL=0. Every positive mode decays, leaving the projection onto the kernel. A connected graph therefore approaches the ordinary mean at every node. A disconnected graph approaches the mean <em>within each component</em>; no amount of time moves information across a missing edge. Unequal physical capacities lead to a different weighted dynamics, so this arithmetic-mean conclusion needs the stated model.</Prose>
    <MathBlock>{String.raw`\begin{aligned}e(t)&=x(t)-\bar x\mathbf1,\\\|e(t)\|&\le e^{-t\lambda_2}\|e(0)\|.\end{aligned}`}</MathBlock>
    <Prose>This follows by squaring the nonconstant coefficients: exp(−2tλₖ)≤exp(−2tλ₂). λ₂ controls a worst-case decay scale, while a particular signal may have no component along u₂ and decay faster. A single small residual or one plotted curve does not establish a universal empirical mixing time.</Prose>
    <H3>Why a connected discrete walk can fail to settle</H3>
    <Prose>With positive degrees, the ordinary transition matrix is P=D⁻¹A. A row probability distribution updates by pnext=pP, while a column node-value function averages by P times that function. Its stationary distribution is πᵢ=dᵢ/Σdⱼ. Do not confuse this degree-weighted probability with the equal-capacity heat mean. The normalized Laplacian eigenvalues ν correspond to transition eigenvalues 1−ν, which can be negative.</Prose>
    <SpectralWalkFigure />
    <Prose>A connected bipartite graph can have νmax=2, hence a transition mode −1 that alternates forever. A <strong>lazy walk</strong> uses (I+P)/2: stay with probability one-half, otherwise take an ordinary step. Its corresponding eigenvalues are 1−ν/2 in [0,1]; in a finite connected graph the nonconstant modes decay. Continuous heat uses exponential decay and does not have this discrete alternation.</Prose>
    <Example id="walk"><Prose>On this two-node fixture the lazy walk reaches [0.5,0.5] in one step. Larger graphs usually require more. Connectivity identifies the unique stationary distribution here; absence of periodicity is an additional condition for convergence from every initial distribution.</Prose></Example>
    <H3>A repeated eigenvalue is one subspace, not unique arrows</H3>
    <Prose>If B contains an orthonormal basis of an eigenspace and R is an orthogonal rotation, BR is another basis. Its projector is BR(BR)ᵀ=BBᵀ. A scalar filter gives the same h(λ) to that entire eigenspace, so its contribution h(λ)BBᵀ is basis invariant. Retaining only one vector from a repeated block can choose a different projector when the solver rotates that block.</Prose>
    <Example id="rotation"><Prose>The unit triangle’s eigenvalue 3 has multiplicity two. The whole two-dimensional projector and its heat gain survive a basis rotation; an arbitrary single direction does not. Likewise, if a clustering embedding ends inside a repeated eigenvalue, its selected subspace may not be unique. An eigengap at the chosen boundary matters more than fixing arbitrary vector signs.</Prose></Example>
    <Prose>We can also filter locally without an eigenbasis. If h is a polynomial of degree r, h(L)x combines x,Lx,…,Lʳx. Each multiplication can move influence across at most one edge, so the result at a node depends only on nodes within r graph hops. This is a concrete connection between spectral filters and local message passing. An arbitrary filter such as exact heat need not have a finite-hop boundary; polynomial approximation introduces an approximation error to control.</Prose>

    <H2>8. Measure a connection’s electrical resistance</H2>
    <Prose>Here is a different use of the same matrix. Reinterpret each positive weight as an electrical <strong>conductance</strong> in siemens. For node potentials v in volts, the current from i to j is Aᵢⱼ(vᵢ−vⱼ) amperes. Kirchhoff’s current law says Lv=b, where b records externally injected current. Positive entries inject, negative entries extract. A connected isolated network needs Σbᵢ=0, since summing Lv always gives zero.</Prose>
    <Prose>Inject one ampere at node a and extract it at b. The resulting voltage difference vₐ−vᵦ is the <strong>effective resistance</strong> in ohms. Only differences matter: adding a constant to all potentials changes no current. We can fix one potential to zero and solve a reduced system, or choose the unique mean-zero potential.</Prose>
    <SpectralResistanceFigure />
    <Prose>For a connected graph, the nonconstant eigenvalues are positive. Expand the unit injection eₐ−eᵦ in those modes and divide each coefficient by its eigenvalue to solve Lv=b. Taking the same endpoint difference gives:</Prose>
    <MathBlock>{String.raw`R_{ab}=\sum_{k=2}^n\frac{(u_k(a)-u_k(b))^2}{\lambda_k}.`}</MathBlock>
    <Prose>This is also (eₐ−eᵦ)ᵀL⁺(eₐ−eᵦ), where L⁺ inverts the positive modes and leaves the constant mode zero. Small positive eigenvalues can make a large contribution when their endpoint values differ. For C–D the sole bridge forces RCD=1/0.2=5. For A–B, the direct 1-ohm edge is in parallel with the two-edge 2-ohm route through C: 1/(1+1/2)=2/3 ohm. The bridge to an otherwise uninjected component carries no current in that second experiment.</Prose>
    <Example id="resistance"><Prose>The independent grounded solve agrees with the spectral sum. A–F additionally crosses each triangle from a nonbridge node to its bridge node, giving 2/3+5+2/3=19/3 ohm. This is a useful way to distinguish one indispensable bridge from an edge with alternate routes. It is an exact finite circuit calculation, not a measured failure-probability model.</Prose></Example>
    <Prose>If a and b are in different components, no finite-voltage steady solution can carry that unit current: the effective resistance is infinite. Blindly evaluating a pseudoinverse quadratic form across disconnected components does not produce that physical answer. Check connectivity first. Multiplying all conductances by c multiplies L by c and divides resistance by c, providing another units check.</Prose>

    <H2>9. Make the computation answer the right question</H2>
    <Prose>Start with what an edge means. A distance grows when points become less similar; a clustering affinity should normally grow when they become more similar. For a Gaussian affinity exp(−‖zᵢ−zⱼ‖²/(2σ²)), feature units and bandwidth σ change the graph. A k-nearest-neighbor rule is initially directed: union and mutual-neighbor symmetrization are different graphs. Document that choice, remove or explicitly model self-loops, inspect components and degrees, and test plausible settings instead of treating preprocessing as invisible.</Prose>
    <LessonTable caption="A practical spectral workflow" headers={['Decision', 'Check before interpreting output']} rows={[['Choose graph and task', 'Record node meaning, weight units, missing edges, feature scaling and whether the goal is cuts, grouping or signal processing.'], ['Choose operator', 'Use L, 𝓛 or the generalized pair consistently; state zero-degree and self-loop conventions.'], ['Choose requested subspace', 'Count components; decide k from the task and inspect its boundary gap. A repeated block can make individual columns arbitrary.'], ['Compute and check', 'Check symmetry, eigenvalue ordering, residuals, orthogonality and null-space dimension. Residual alone does not certify that you found the desired lowest modes.'], ['Round or filter', 'Record threshold objective, row normalization, assignment method, initialization/restarts or filter gain.'], ['Evaluate the application', 'Compare meaningful baselines and justified graph changes. Validate on information available at prediction time.']]} />
    <Prose>For an n-node dense graph, merely storing A requires order n² numbers; a full dense symmetric eigendecomposition typically costs order n³ arithmetic. A sparse graph with m edges permits a matrix-vector product in order n+m work. Sparse iterative methods compute a requested small subspace, with cost depending on iterations, spectral separation and solver settings—not a universal linear-time promise.</Prose>
    <Prose>For symmetric inputs, <Code>numpy.linalg.eigh</Code> returns ascending eigenvalues and eigenvectors in columns; it assumes the supplied triangle represents a Hermitian matrix, so validate symmetry yourself. For sparse matrices, SciPy <Code>eigsh</Code> requires k&lt;n and offers spectral-selection and shift-invert options. A shift at zero can require factoring a singular Laplacian. Handle convergence failures and inspect the returned pairs instead of treating any returned array as a successful low-mode solve.</Prose>
    <Prose>Scikit-learn’s <Code>SpectralClustering</Code> with <Code>affinity="precomputed"</Code> expects similarities. Its supported label-assignment strategies include k-means, discretization and cluster-QR; their behavior and randomness differ. Our explicit NumPy pipeline teaches one symmetric-normalized variant and is not a byte-for-byte copy of that library. Pin the version and chosen settings in an experiment.</Prose>
    <Prose>For a prediction application, avoid building affinities from future measurements or target labels unavailable at use time. A transductive task may deliberately use unlabeled test-node features; an inductive task may forbid that. State which task you evaluate. Document clusters can aid browsing; graph filtering can stabilize compatible sensor measurements; resistance can reveal alternate current routes. Each conclusion depends on its stated graph and task, not only an attractive spectral plot.</Prose>

    <H3>Move from explicit eigenvectors to sparse tools and a clustering API</H3>
    <Prose>The download reuses our nine-node graph: three unit-weight triangles connected by .08 bridges. Both eigensolvers receive the same symmetric normalized Laplacian, with no self-loops and positive degrees. Dense <Code>eigh</Code> supplies a small exact reference; SciPy <Code>eigsh</Code> requests the three smallest-magnitude modes from a sparse matrix, with a fixed starting vector. It checks ascending eigenvalues, residuals and the whole subspace projector instead of signs of individual columns.</Prose>
    <Prose>The first eigenvalues are approximately [0,.012599,.038124]; dense/sparse residual and projector errors are below 1e−10. An eigengap separates this selected block. If the cut ended inside a repeated eigenvalue, a solver could correctly return a different partial subspace; forcing column equality would misdiagnose the ambiguity. The repeated-eigenvalue example above owns that mechanism.</Prose>
    <MechanismProgram source="/learn-assets/spectral-graph/spectral_library.py" title="Open the complete sparse-solver / SpectralClustering comparison" output={libraryOutput} />
    <Prose>The maintained clustering route uses scikit-learn 1.9.1 with <Code>affinity="precomputed"</Code>, three clusters/components, ARPACK, k-means assignment and twenty starts. A meaningful convention differs: the earlier Ng–Jordan–Weiss route normalizes each embedding row to unit length, whereas this library's route uses the generalized-eigenvector scaling D⁻¹ᐟ²U. The bridge constructs that scaling explicitly and also runs the earlier row-normalized variant. All three give ABC / DEF / GHI on this fixture, but that agreement is not a theorem that their pipelines are identical.</Prose>
    <Prose>Labels are arbitrary integers. The check compares whether every pair of vertices belongs together, then compares normalized-cut value .052290 under the original affinity. Matching a renamed label such as cluster “0” would be meaningless. This is a transductive grouping of these nine vertices; <Code>fit_predict</Code> does not define an out-of-sample assignment rule for an unseen document.</Prose>
    <Prose>A sparse affinity uses O(n+m) storage and matrix–vector work for m edges; iterative eigensolvers add a retained basis and convergence-dependent work. This small verifier deliberately forms a dense reference and n×n projectors; avoid those allocations on large graphs and use residual/orthogonality checks with a justified spectral selection. Smallest modes can be difficult for ARPACK. Inspect nonconvergence; shift-invert can help but introduces a factorization and a zero shift is singular for a Laplacian. Do not silently accept whichever pairs happened to converge.</Prose>
    <Checkpoint prompt="Change the requested cluster count to one, then add a completely isolated tenth vertex. What changes are a valid modeling choice rather than a solver repair?">
      <Prose>The executed k=1 case assigns all nine vertices together and has normalized cut zero; a low cut alone cannot select the number of groups. The bridge explicitly rejects the isolated vertex because its degree-based scaling is undefined. Decide whether to report that vertex separately or choose a documented zero-degree convention and rerun both routes under it. Adding arbitrary weak edges would change the graph, not merely fix a numerical error.</Prose>
    </Checkpoint>

    <H2>10. Practise on changed graphs</H2>
    <Prose>Attempt each task before opening its hint. You should be able to name the operator, calculate the relevant quantity and explain why its conclusion follows. Decimal agreement alone is not enough. These tasks reuse the mechanisms with changed assumptions or inputs; the original pilot’s scaling, block-energy and disconnected-limit checks remain part of the route.</Prose>
    <Practice title="Scale every connection" question="Multiply every edge weight by ten. Predict L, its eigenvalues, its eigenspaces, the triangle sign split, 𝓛 and effective resistance. Does heat at the same t remain unchanged?" hint="Track each factor of D and A. Heat depends on the product tλ.">
      <Prose>L and all its eigenvalues multiply by ten; eigenspaces can stay fixed, although a numerical solver may choose different signs or bases. The sign partition can stay the same. On positive degrees, the normalization cancels the scale, so 𝓛 is unchanged. Resistance divides by ten. Heat at the same t decays ten times as far along the old time parameter: exp(−t·10L)=exp(−(10t)L). Divide t by ten to reproduce the previous heat result.</Prose>
    </Practice>
    <Practice title="Read the block signal before dividing" question="For x=[1,1,1,−1,−1,−1], compute graph energy at bridge 0.2 and then bridge 0.5. Is either number itself λ₂?" hint="Only one edge crosses different values; the signal is not unit length.">
      <Prose>The squared difference is 4, so the energies are 0.8 and 2. Their Rayleigh quotients are 0.8/6≈0.133333 and 2/6≈0.333333. These bound λ₂ from above because the signal is centered, but it is not generally an eigenvector: the bridge endpoints have different Laplacian outputs from the other nodes. The exact λ₂ values are about 0.122027 and 0.267949, respectively.</Prose>
    </Practice>
    <Practice title="Change the group values and disconnect" question="Set the bridge to zero and start heat with [4,1,1,−2,−2,1]. What is the limiting signal? Would a connected bridge change that answer?" hint="Compute the mean in each connected component before taking the long-time limit.">
      <Prose>The disconnected means are 2 and−1, so the limit is [2,2,2,−1,−1,−1]. With any fixed positive bridge, the connected graph’s long-time limit is the overall mean 0.5 at all six nodes. A very weak bridge can take a long time to equalize the groups. Setting the bridge exactly to zero and taking infinite time in the connected family are different limiting operations.</Prose>
    </Practice>
    <Practice title="Compute cut balance on unequal degrees" question="Take the weighted path A—B—C—D with weights 2,1,3. Compare S={A} and S={A,B}: crossing weight, volumes, RatioCut, Ncut and conductance." hint="Degrees are [2,3,4,3]; volumes count incident weights, including internal edges twice.">
      <Prose>For the singleton A, c=2, volumes are 2 and 10, and sizes are 1 and 3. RatioCut=8/3, Ncut=2(1/2+1/10)=1.2, φ=1. For the pair A,B, c=1, volumes are 5 and 7, and sizes are 2 and 2. RatioCut=1, Ncut=12/35≈0.342857, φ=1/5. The second cut is better by all three balance measures here. This calculation evaluates two candidates, not every partition of an arbitrary larger graph.</Prose>
    </Practice>
    <Practice title="Repair a normalized coordinate mistake" question="A triangle has edge weights A–B=2, A–C=2, B–C=7, hence degrees [4,9,9]. For a trial normalized coordinate v=[1,−1,1], what per-node coordinate belongs in y=D⁻¹/²v? Does this v qualify as the second normalized eigenvector just because it has both signs?" hint="Divide by square roots of degree, and test the required orthogonality before any eigen-equation.">
      <Prose>y=[1/2,−1/3,1/3]. The weighted sum yᵀd=2−3+3=2, equivalently vᵀ√d=2−3+3=2, so this vector is not orthogonal to the normalized zero mode. It cannot be a nonzero-eigenvalue mode of that connected normalized operator. Signs alone do not certify an eigenvector. An additional isolated node would need separate handling instead of dividing its coordinate by zero.</Prose>
    </Practice>
    <Practice title="Trace points and centers" question="Two row-normalized component points are [1,0] and[0,1]. A third raw row is [0,3]. After row normalization, which of those two centers is nearest? If a center has assigned points[1,0] and[0,1], where does its mean move? Must that center remain unit length?" hint="The unit-length constraint applies to input row points, not Lloyd’s centers.">
      <Prose>[0,3] becomes [0,1], so it coincides with the second center. The mean of [1,0] and[0,1] is [1/2,1/2], whose length is 1/√2. Ordinary Euclidean k-means does not renormalize the centers to the unit circle. Doing so defines a different update rule and objective.</Prose>
    </Practice>
    <Practice title="Solve a changed ridge filter" question="On the three-node unit path, smooth x=[0,1,0] with α=1. Solve the three normal equations and check total value and energy." hint="Symmetry gives equal endpoint values a; write the middle value b.">
      <Prose>The endpoint equations give 2a−b=0. The middle equation is 3b−2a=1. Thus a=1/4 and b=1/2, giving [1/4,1/2,1/4]. The total remains 1. Input energy is 2; output energy is(−1/4)²+(1/4)²=1/8. The lower energy is a verified property of this filter, not evidence that the middle impulse was an erroneous measurement.</Prose>
    </Practice>
    <Practice title="Choose an invariant filter boundary" question="A unit-weight square cycle has eigenvalues 0,2,2,4. Is retaining exactly the first two eigenvectors a uniquely defined low-pass operator? Give two basis-invariant alternatives." hint="The retained boundary cuts through a two-dimensional equal-eigenvalue block.">
      <Prose>No. The selected one-dimensional portion of the eigenvalue 2 eigenspace depends on the solver’s basis. Retain the entire 0-and 2 eigenspaces, giving three modes, or use a scalar gain such as exp(−tλ) or 1/(1+αλ) that treats both λ=2 directions equally. Changing a basis within a fully included eigenspace cannot change its projector.</Prose>
    </Practice>
    <Practice title="Change conductance, not just the drawing" question="Each triangle edge now has conductance 2 siemens and the sole bridge has conductance 0.5 siemens. What are RCD, RAB and RAF? What if the bridge is removed?" hint="A triangle has a direct resistor in parallel with a two-resistor path; the cross-group path includes the bridge in series.">
      <Prose>Each internal edge is 1/2 ohm, so RAB=(1/2) parallel 1=1/3 ohm. RCD=1/0.5=2 ohms. RAF=1/3+2+1/3=8/3 ohms. Removing the bridge makes cross-component resistance infinite; within-triangle RAB remains 1/3. A finite pseudoinverse expression across disconnected endpoints would not solve this injection experiment.</Prose>
    </Practice>
    <Practice title="Design a defensible application" question="You want document clusters from a nearest-neighbor graph and report a large eigengap. Specify what else you would record or test before claiming useful clusters." hint="Separate the affinity model, numerical solution, rounding method and external purpose.">
      <Prose>A defensible answer records text features and scaling, distance-to-affinity conversion, neighborhood count and symmetrization, component/isolate handling, selected Laplacian, k and its reason, eigenpair residuals and orthogonality, row/assignment convention and restarts. Compare justified graph settings and evaluate whether the resulting groups improve a concrete task such as browsing or retrieval. State what information was available at evaluation time. An eigengap supports inspecting a candidate subspace; it cannot by itself establish true semantic classes.</Prose>
    </Practice>
    <Prose><strong>Ready to continue:</strong> you can compute a graph energy, identify which zero modes are possible, explain a cut relaxation, execute a normalized sweep, trace a row-embedding workflow and interpret a filter or circuit result under its assumptions. Next is <a href="/learn/path/full-curriculum/combinatorial-optimization-approximation-algorithms">Combinatorial Optimization &amp; Approximation Algorithms</a>: it develops how to choose discrete solutions and what an approximation guarantee actually promises. Later stochastic-process material expands the random-walk connection.</Prose>
    <Sources alternatives={<><p><a href="https://www.youtube.com/watch?v=cxTmmasBiC8">Gilbert Strang, MIT: Finding Clusters in Graphs</a> gives a spoken undergraduate route through centroid updates, Laplacians and Fiedler vectors. The <a href="https://ocw.mit.edu/courses/18-065-matrix-methods-in-data-analysis-signal-processing-and-machine-learning-spring-2018/resources/lecture-35-finding-clusters-in-graphs-second-project-handwriting/">official course page</a> includes a transcript and triangle/square exercise. Use it after sections 2–3; notation differs. The transcript was reviewed, not the full recording.</p><p><a href="https://arxiv.org/pdf/0711.0189">Ulrike von Luxburg’s spectral clustering tutorial</a> is a deeper written companion: sections 3–5 compare operators and cut relaxations; section 7 discusses perturbations. Its 2007 theory is useful, while historical claims about the state of the field should be read in that context.</p></>}>
      <li><a href="https://www.cs.yale.edu/homes/spielman/561/lect06-15.pdf">Spielman, Conductance, the Normalized Laplacian, and Cheeger’s Inequality</a> — threshold proof and generalized quotient; the notes’ normalized eigenvalues use their own notation.</li>
      <li><a href="https://arxiv.org/pdf/1211.0053">Shuman et al., The Emerging Field of Signal Processing on Graphs</a> — sections II.B–F develop graph Fourier coordinates and graph-relative variation; a research-level extension of sections 6–7.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.eigh.html">NumPy eigh</a> — symmetric/Hermitian contract, ascending eigenvalues and column eigenvectors. Local examples were tested on NumPy 2.3.5; the current documentation snapshot was 2.5.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.sparse.linalg.eigsh.html">SciPy eigsh</a> — partial sparse symmetric solves, selection, shift-invert and convergence behavior; technical reference after the dense examples.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.cluster.SpectralClustering.html">Scikit-learn SpectralClustering</a> — documented affinity and label-assignment options, including randomness. Documentation 1.9.1 was checked; no claim that our NumPy implementation reproduces every library convention.</li>
    </Sources>
  </div>
};
