import { Code, H2, H3, Prose } from "../../components/content";
import { Checkpoint, LessonIntro, LessonTable, Sources } from "../../components/lesson-labs/LessonElements";
import { RunnableExample } from "../../components/lesson-labs/RunnableExample";
import { TreeAnatomyFigure, TreeBoundsFigure, TreeRotationFigure, TreeSearchLab, TreeTraversalLab, TreeDeletionLab } from "../../components/lesson-labs/TreeLabs";
import { treeExamples } from "../tree-examples.js";
import { DsaPractice } from "../../components/lesson-labs/DsaPractice.jsx";
import treePractice from "../practice/trees-binary-search-trees.js";
import ExpressionTreeFigure from "../../components/lesson-labs/ExpressionTreeFigure.jsx";
import TreeLibraryBridge from "../../components/lesson-labs/TreeLibraryBridge.jsx";

export default {
  title: "Trees & Binary Search Trees",
  readTime: "~60 min read + 2–3 hours practice",
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot tree-lesson">
    <LessonIntro prerequisites="Python functions, classes and references; the preceding stack/queue lesson. A short recursion and cost-model bridge is included below; the later Complexity Analysis & Recursion topic develops it further."
      sections={[["1-when-one-next-reference-is-not-enough","Read a tree"],["2-search-by-ruling-out-a-whole-subtree","Search and insert"],["3-follow-calls-without-losing-your-place","Recursion and traversals"],["4-delete-a-key-keep-the-other-relationships","Deletion"],["5-the-rule-comes-from-every-ancestor","Validation and ordered queries"],["6-what-height-costs-and-how-balance-helps","Height and balance"],["7-use-the-shape-to-compute-an-answer","Applications and deeper patterns"],["8-practise-explain-and-transfer","Independent practice"],["guided-dsa-practice","Guided LeetCode practice"]]}>
      Organize branching relationships, keep a changing set of keys in order, and explain what happens to every reference during a search or edit. Then use the same shape for expression evaluation and computations that combine answers from subtrees. Work through the first six sections before choosing a deeper branch.
    </LessonIntro>

    <H2>1. When one next reference is not enough</H2>
    <Prose>A linked list gives each item one next item. A folder can contain several folders, and an arithmetic operation can depend on two smaller expressions. These relationships branch. A <strong>tree</strong> represents a hierarchy of nodes connected by edges. In a rooted tree, one node is the starting point, the <strong>root</strong>; every other node has exactly one parent, and following child links never leads back to an ancestor.</Prose>
    <Prose>A node's children start smaller trees called its <strong>subtrees</strong>. A node without children is a <strong>leaf</strong>. An ancestor lies on the path from the root to a node; a descendant lies below it. A <strong>binary tree</strong> has at most two child positions per node, named left and right. Either position can be empty. “Binary” describes these positions; it does not say that the values are 0 and 1, that every node has two children, or that the values are sorted.</Prose>
    <TreeAnatomyFigure />
    <Prose>Our continuing example inserts the keys <Code>[8, 3, 10, 1, 6, 14, 4, 7, 13]</Code> into an initially empty ordered tree. The key is a value used for comparison, while the node is an object holding that key and two references. Empty child references are <Code>None</Code> in Python. The connecting lines describe references, not physical memory layout or a distance between values.</Prose>
    <LessonTable caption="Read shape before learning algorithms" headers={["Term", "Convention used here", "Example"]} rows={[
      ["Depth", "Number of edges from the root", "8 has depth 0; 6 has depth 2; 4 has depth 3."],
      ["Height", "Longest downward path to a leaf, measured in edges", "A leaf has height 0; this tree has height 3; an empty tree has height −1."],
      ["Size", "Number of stored nodes", "Nine keys occupy nine nodes in this set implementation."],
      ["Subtree", "A node together with all its descendants", "The subtree rooted at 6 contains 6, 4 and 7."],
      ["Full / complete / perfect", "Full: every node has 0 or 2 children. Complete: levels fill left-to-right, with only the last possibly partial. Perfect: all internal nodes have 2 children and all leaves share a depth.", "These are shape properties, separate from key ordering. Complete shape will matter for heaps next."],
    ]} />
    <Prose>Some sources and interview questions measure height in nodes instead. A nonempty tree's node-height is its edge-height plus one. State the convention before answering a depth or diameter question. None of our code assumes that a key identifies a unique memory address. The examples use distinct integer keys as a set; other designs can store counts or values associated with keys.</Prose>
    <Checkpoint prompt="A root stores 8, its left child stores 20 and its right child stores 3. Is it a binary tree? Is it an ordered search tree?">
      <Prose>It is a binary tree because each node has at most two child positions. It violates the search ordering we introduce next. Shape and order are different contracts; traversals can still visit this tree, but an ordered search can discard the wrong branch.</Prose>
    </Checkpoint>

    <H2>2. Search by ruling out a whole subtree</H2>
    <Prose>Suppose an application keeps numeric release versions and asks for a particular version, the next available version or all versions in an interval. An unsorted list can answer by scanning every item. A sorted array supports fast binary search, but keeping its order during arbitrary insertion may require shifting many items. A <strong>binary search tree</strong>, or BST, uses branching references and an ordering rule to support these changing ordered collections.</Prose>
    <Prose>For every node, <strong>every key in its left subtree is smaller, and every key in its right subtree is larger</strong>. This must be true for all descendants, not just immediate children. Equality means the key is already present: our set ignores another insertion of it. A map could instead update the value associated with that key; a multiset could maintain a count. Mixing duplicate policies makes validation and deletion ambiguous.</Prose>
    <Prose>To find 7, compare with 8 and take its left link. At 3 take the right link. At 6 take the right link. At 7 stop. The comparisons did more than choose a direction: the first excluded every key above 8, and the next established that the answer must be above 3. By the time you reach 6, the whole current subtree must fit between 3 and 8.</Prose>
    <TreeSearchLab />
    <H3>The search invariant</H3>
    <Prose>At the start of each iteration, if the target exists, it lies in the current subtree. This is true initially because that subtree is the whole tree. Comparing with its root either finds the target or excludes one entire side by the BST rule. Following the remaining child preserves the fact. Each move descends an edge, so a finite tree eventually yields a match or an empty child. An empty child means absence, because all other regions were already excluded.</Prose>
    <Prose>Insertion follows the same unsuccessful-search path, then replaces the empty child reference with a new node. Inserting 5 follows 8 → 3 → 6 → 4 and attaches to 4's right. The bounds inherited along that path are exactly why the new key belongs there. Inserting into an empty tree returns a new root; remember to keep that returned root.</Prose>
    <RunnableExample example={treeExamples.search}><Prose>Each block is a complete standalone Python 3.12+ program using the standard library. Save it as a .py file and run it with Python. In this insertion implementation, side is the string “left” or “right”: getattr reads that named child attribute and setattr updates it. The search result deliberately includes the comparison path so you can match code to the drawing. That diagnostic list uses extra space; the underlying iterative search needs only its current reference.</Prose></RunnableExample>
    <Checkpoint prompt="Without running the lab, where would 12 be inserted? Why can't it go directly under 10 as a left child?">
      <Prose>Follow 8 → 10 → 14 → 13, then attach 12 as 13's left child. The key is above 10, so it must remain in 10's right subtree. A locally convenient empty position is not necessarily globally valid.</Prose>
    </Checkpoint>

    <H2>3. Follow calls without losing your place</H2>
    <Prose>Search visits one selected path. Sometimes you need to visit <em>every</em> node, for example to display all keys or compute a result for every folder. A <strong>traversal</strong> specifies that visiting order. The useful insight is that each subtree has the same shape of problem as the whole tree.</Prose>
    <H3>A recursion bridge you can use now</H3>
    <Prose>A recursive function calls itself on a smaller input. Each call has its own local variables and a place to resume after the child call finishes. Those unfinished calls form the call stack. The base case handles an empty subtree without making more calls. Because every child subtree contains fewer nodes than its parent subtree, repeatedly descending reaches that base case in a proper finite tree.</Prose>
    <Prose>“Visit the left subtree, emit the root, visit the right subtree” is <strong>inorder</strong>. For the sample, the calls first descend 8 → 3 → 1 → empty. The empty call returns. The call for 1 emits 1 and finishes its empty right subtree. Only then does the suspended call for 3 emit 3. A node on the call stack is pending work; it is not necessarily already part of the output.</Prose>
    <TreeTraversalLab />
    <LessonTable caption="The placement of one action changes the traversal" headers={["Order", "Work at each node", "What it helps explain"]} rows={[
      ["Preorder", "Emit root, traverse left, traverse right", "Describe a parent before its descendants; encode a shape when missing children are recorded."],
      ["Inorder", "Traverse left, emit root, traverse right", "List BST keys in increasing order. On an arbitrary binary tree this is not a sort."],
      ["Postorder", "Traverse left, traverse right, emit root", "Combine completed child results, such as expression values or subtree heights."],
      ["Level order", "Use a FIFO queue to visit successive depths", "Collect a level at a time; a bridge to graph breadth-first search."],
    ]} />
    <RunnableExample example={treeExamples.traversals}><Prose>For recursive traversals, only the placement of result.append changes. The iterative inorder helper in the setup represents the same unfinished work with an explicit list stack: descend left while saving nodes, pop and emit, then enter the right subtree. The level-order loop takes len(queue) once at the start of each level, so newly appended children belong to the next level.</Prose></RunnableExample>
    <Prose>Each traversal processes each of n nodes once: O(n) time under a constant-cost per-node model. The recursive depth-first traversal and explicit inorder stack require O(h) auxiliary space for a tree of height h (more precisely O(h + 1) including the empty/singleton boundary). The returned output list adds O(n). The breadth-first queue uses O(w), where w is the maximum level width, up to a constant factor as adjacent levels overlap in the queue; w can itself be O(n).</Prose>
    <Prose>Python limits recursion depth. A long chain can reach that limit even when the algorithm is logically correct. Increasing the limit does not remove stack consumption. For deep or untrusted input, use an explicit stack or a shape with a justified height bound. Our small examples expose the mechanism, not a promise that recursion accepts every input size.</Prose>
    <Checkpoint prompt="If you take inorder traversal of an arbitrary binary tree, is the output sorted? Does an explicit stack eliminate all auxiliary storage?">
      <Prose>Sorted output follows from the BST ordering rule, not the name of the traversal. An explicit stack replaces call frames with an object you manage; it still stores pending ancestors and can grow to O(n) on a chain.</Prose>
    </Checkpoint>

    <H2>4. Delete a key, keep the other relationships</H2>
    <Prose>Deletion starts with search. Once the target is found, its parent has one child reference pointing at it. What can replace that reference while keeping all remaining keys reachable and ordered? The answer depends on how many children the target has.</Prose>
    <LessonTable caption="Three structural cases, including the root" headers={["Case", "Replacement", "Reason"]} rows={[
      ["No children", "Replace the incoming link with None", "No subtree remains to preserve."],
      ["One child", "Replace the incoming link with that child", "Every key below it already satisfies the target's ancestor bounds."],
      ["Two children", "Use the smallest key in the right subtree, then remove its former occurrence", "That successor is above all left-subtree keys and below the remaining right-subtree keys."],
    ]} />
    <Prose>The successor cannot have a left child, because then it would not be the smallest node in that right subtree. It <em>can</em> have a right child. Removing the successor therefore falls into the simpler zero/one-child cases; forgetting its right child loses valid data.</Prose>
    <TreeDeletionLab />
    <Prose>This implementation copies the successor's key into the target object, then unlinks the successor object. Deleting root key 8 leaves the original root object holding 10; its right reference now reaches 14. In a map, copy the associated value as well. Some implementations physically transplant the successor node instead. Both can preserve set contents, but their node-identity behavior differs. Do not retain a node handle and assume that the same key stays in it after a key-copy deletion.</Prose>
    <RunnableExample example={treeExamples.deletion}><Prose>Each delete call returns the root of the surviving subtree. The caller reconnects that return value through root.left or root.right. At the top level, root = delete(root, key) is just as necessary: deleting a singleton returns None, and deleting a root with one child returns that child. A missing key leaves the set unchanged.</Prose></RunnableExample>
    <Prose>Correctness follows by the cases. The zero/one-child replacement retains the ancestor restrictions. In the two-child case, the successor preserves order at the target and its removal is safe in the right subtree. Calls on the search path then reconnect valid subtrees. All recursive paths descend, so termination follows from finite height. Finding the target and successor takes O(h); key-copy deletion does not make the whole operation O(1). Recursive frames use O(h) additional space.</Prose>
    <Checkpoint prompt="Delete 20 from the tree built by [20, 10, 40, 30, 50, 35]. Which key replaces 20, and where must 35 remain?">
      <Prose>30 replaces 20. The old 30 node had right child 35, so 40's left link must now point to 35. The final inorder sequence is [10, 30, 35, 40, 50]. Setting that link to None would silently drop 35.</Prose>
    </Checkpoint>

    <H2>5. The rule comes from every ancestor</H2>
    <Prose>A tempting validator checks only whether the left child is smaller and the right child larger. Consider root 10, its left child 5 and 5's right child 12. Both immediate comparisons pass, but 12 lies in 10's left subtree, where every key must be below 10.</Prose>
    <TreeBoundsFigure />
    <Prose>Pass an allowed interval into each recursive call. The root starts without bounds. Going left tightens the upper bound to the current key; going right tightens the lower bound. Both restrictions from earlier ancestors remain in effect. Use strict inequalities for our unique-key policy. This validates the ordering of an already well-formed tree; it is not a cycle detector or a validator for arbitrary aliased object graphs.</Prose>
    <RunnableExample example={treeExamples.validation}><Prose>The function short-circuits as soon as it finds a violation. A successful full validation inspects every node, O(n) time and O(h) stack space. An empty tree is a valid BST. A sorted inorder check is another possible strategy with a consistent duplicate policy, but checking bounds makes the ancestor requirement visible.</Prose></RunnableExample>
    <H3>Use order for more than exact equality</H3>
    <Prose>The minimum key is reached by following left links until none remains; the maximum follows right links. A <strong>floor</strong> query asks for the greatest stored key no larger than a target, while <strong>ceiling</strong> asks for the smallest stored key no smaller than it. These include equality. A predecessor/successor query may instead demand strict inequality: specify which contract is intended.</Prose>
    <Prose>In an original version-selection scenario, available numeric releases are the sample keys and a client supports at most version 5. Scanning works, but ordered search can keep a best candidate. At 8, move left because 8 is too new. At 3, remember 3 and try its right subtree for a larger compatible key. At 6, move left. At 4, update the candidate, then find no larger candidate. Return 4. This models a simple numeric compatibility rule, not the full semantics of software-version compatibility.</Prose>
    <RunnableExample example={treeExamples.orderedQueries}><Prose>The floor invariant is that candidate is the largest suitable key seen so far, and the remaining subtree is the only place that might improve it. The inclusive range walk skips branches that cannot overlap the requested interval, emits results in sorted order and takes O(h + k) time for k returned keys. The result itself requires O(k) space in addition to traversal frames. A dictionary's fast exact lookup does not by itself supply these ordered queries.</Prose></RunnableExample>

    <H2>6. What height costs, and how balance helps</H2>
    <Prose>O(n) describes work growing linearly with the number of nodes; O(log n) describes the number of repeated roughly halving steps; O(h) describes work along a tree-height path. These are growth statements under a cost model, not milliseconds. We treat a key comparison and a reference operation as constant cost. Comparing very long strings or very large integers can add a nonconstant comparison cost.</Prose>
    <Prose>A BST does not guarantee that either child receives half the keys. Insert increasing values and every new key goes to the right: the tree becomes a linked chain of height n − 1. Searching its last key takes n comparisons. A shape with height O(log n) avoids that degeneration. There are at most 1 + 2 + 4 + … + 2ʰ = 2ʰ⁺¹ − 1 nodes through depth h; consequently even the best binary-tree shape needs height growing logarithmically with its size.</Prose>
    <LessonTable caption="State the structure's guarantee before quoting its cost" headers={["Operation", "Ordinary BST", "With a maintained logarithmic-height guarantee"]} rows={[
      ["Search, insert, delete, floor/ceiling", "O(h); O(n) worst case", "O(log n) worst case for the corresponding balanced structure"],
      ["Traverse all keys", "O(n), regardless of shape", "O(n): every returned key still needs to be visited"],
      ["Range query returning k keys", "O(h + k)", "O(log n + k)"],
      ["Storage for n key nodes", "O(n)", "O(n), possibly with extra balance/size metadata per node"],
    ]} />
    <H3>Rebuilding and rotating solve different problems</H3>
    <Prose>If you already have sorted distinct keys and can build a new tree, choose the middle key as root and repeat on the two index ranges. Each key becomes one node, so construction takes O(n) time without copying slices, with O(log n) recursive depth. If you start from an unsorted list, obtaining sorted keys is additional work. Rebuilding after every small edit is also additional work; this is not a free dynamic balancing strategy.</Prose>
    <TreeRotationFigure />
    <Prose>A <strong>rotation</strong> changes local shape while preserving inorder. In the pictured right rotation, 20 moves above 30. The middle subtree rooted at 25 transfers from 20's right to 30's left because its keys remain between 20 and 30. Keeping this middle subtree is the essential reference update. The operation changes only a few references, but the incoming parent link or root reference must also be updated.</Prose>
    <RunnableExample example={treeExamples.balance}><Prose>The seven sorted keys produce a chain of height 6 by ordinary insertion and a rebuilt tree of height 2. The separate five-key rotation example checks that the inorder sequence stays equal and that 25 is retained. The rotation function does not decide when a rotation is needed; that decision belongs to a balancing algorithm.</Prose></RunnableExample>
    <details className="lesson-deeper"><summary>Deeper: AVL, red-black and augmented trees</summary>
      <Prose>An AVL tree maintains a height difference of at most one between each node's two child subtrees. After insertion or deletion, it updates heights while returning up the changed path and repairs violations with single or double rotations. A left-right configuration first rotates the left child left, then the unbalanced node right; the mirror handles right-left. Insertion can stop height repair after the relevant balancing fix, but deletion can require repairs at several ancestors. The rotations are local; the height and balance invariants make the global guarantee.</Prose>
      <Prose>Why does this prevent chains? If N(h) is the fewest nodes in an AVL tree of height h, its smallest permitted subtrees have heights h − 1 and h − 2. Thus N(h) = 1 + N(h − 1) + N(h − 2), starting at N(−1) = 0 and N(0) = 1. The minimum size grows exponentially with height, so height grows only logarithmically with size. Red-black trees use color and path constraints for another logarithmic-height guarantee; they do not demand exactly equal subtree heights. A production balanced container requires all its update cases, not just the rotation shown here.</Prose>
      <Prose>A node can also store a subtree count: size(node) = 1 + size(left) + size(right). For zero-based select(k), let s be the left count. If k is below s, recurse left; if equal, return the current key; otherwise recurse right with k − s − 1. Rank similarly accumulates skipped left subtrees. Updating every affected count during edits and rotations is part of correctness. Without stored counts, counting the subtree afresh can erase the desired O(h) query cost.</Prose>
      <Prose>Persistent trees additionally share unchanged structure between versions; B-trees organize many keys/children around storage blocks. Their detailed mechanisms belong to <a href="/learn/topic/persistent-data-structures-structural-sharing-versioned-queries">Persistent Data Structures</a> and <a href="/learn/topic/external-memory-algorithms-b-trees-i-o-complexity">External-Memory Algorithms & B-Trees</a>. Neither is simply a renamed binary search tree.</Prose>
    </details>

    <TreeLibraryBridge />
    <H2>7. Use the shape to compute an answer</H2>
    <H3>An expression tree explains why parentheses matter</H3>
    <Prose>The expression (2 + 3) × (9 − 4) has multiplication at the root, with addition and subtraction as its children. Each operation needs its child answers first. Evaluating postorder gives 5 on the left, 5 on the right, then 25. This tree encodes syntactic grouping rather than sorted search keys; placing “+” left of “*” is not a key-comparison decision.</Prose>
    <ExpressionTreeFigure />
    <RunnableExample example={treeExamples.expression}><Prose>Evaluate returns one completed subtree value to its caller. The example supports only numeric leaves and +, −, ×; it does not parse arbitrary text or call eval. This is a small original model of the kind of structure used to represent expressions. A real language needs parsing, types, operator semantics and error handling beyond these three operations.</Prose></RunnableExample>
    <H3>A reusable question: what must a subtree return?</H3>
    <Prose>Height needs the two child heights. A tree's <strong>diameter</strong> is the greatest number of edges on any simple path between its nodes; it need not pass through the overall root. At one node, the best path is either entirely in its left subtree, entirely in its right subtree, or joins their deepest paths through that node. The last candidate has left_height + right_height + 2 edges. Return both height and diameter so each parent combines answers without recomputing them.</Prose>
    <Prose>The <strong>lowest common ancestor</strong> of two nodes is their deepest shared ancestor, allowing a node to be its own ancestor. In a general binary tree, search both subtrees: if each side returns one target or their ancestor, the current node is where the branches join. If only one side returns a result, pass it upward. The following function assumes both node identities really are present. For possibly missing targets, first check membership or return found-target counts too. In a BST with present distinct keys, ordering can instead identify the first split on one search path.</Prose>
    <RunnableExample example={treeExamples.summaries}><Prose>The sample's height is 3, but its diameter is 6: one longest path runs from 4 or 7 across 8 to 13. Both general-tree functions inspect O(n) nodes and use O(h) stack space; summarize performs constant additional work at each node. Recomputing an entire height for every node would instead take O(n²) on a chain.</Prose></RunnableExample>
    <details className="lesson-deeper"><summary>Deeper: preserve shape when saving a tree</summary>
      <Prose>Values alone are not always enough to reconstruct a binary tree. Preorder [2, 1] could describe 2 with a left child 1, or 2 with a right child 1. Record an explicit marker for each empty child to remove that ambiguity. The decoder consumes one root token, then recursively consumes its left and right descriptions. A list index cursor avoids repeatedly removing the first list element. None is reserved as the empty marker, so stored keys must not be None in this format.</Prose>
      <RunnableExample example={treeExamples.serialization}><Prose>The round-trip test compares shape as well as values. A tree with n real nodes has n + 1 empty child links, so this preorder format contains 2n + 1 tokens. Encoding and decoding take O(n) time plus output/new-tree storage, and O(h) recursion depth. A robust external format also needs input-size bounds, versioning and appropriate data validation.</Prose></RunnableExample>
    </details>

    <H2>8. Practise, explain and transfer</H2>
    <Prose>Try these before opening solutions. For each, state the input/output contract, sketch a small example, name the fact that remains true, and test empty, singleton, equality and skewed cases. Then explain time and auxiliary space separately from returned output.</Prose>
    <H3>Independent task A: find a ceiling</H3>
    <Prose>Implement ceiling_key(root, target), returning the smallest stored key at least target, or None. For the sample, queries 0, 5, 6 and 15 should return 1, 6, 6 and None. Do not flatten or sort the tree. Explain how your candidate changes and why discarded branches cannot improve it.</Prose>
    <details><summary>Hint: reverse the floor candidate rule</summary><Prose>A key above the target is a valid candidate, but its left subtree might contain a smaller valid one. A key below the target cannot help, nor can its left subtree.</Prose></details>
    <details><summary>Complete solution and boundary cases</summary><RunnableExample example={treeExamples.ceilingPractice}><Prose>Candidate is the smallest suitable key examined so far. Search descends one path, so it takes O(h) time and O(1) auxiliary space. Changing the comparison to strictly greater produces an upper-bound/successor-style query instead; equality in the contract matters.</Prose></RunnableExample></details>
    <H3>Independent task B: diagnose a tempting validator</H3>
    <Prose>A function accepts a node if its immediate children have the right comparison signs, then repeats on the children. Construct the smallest tree it incorrectly accepts. Repair the function without sorting all keys. Explain why empty bounds are different from using a magic maximum integer.</Prose>
    <details><summary>Reasoning and repair</summary><Prose>Three nodes suffice: 10 with left child 5, whose right child is 12. Pass lower and upper bounds exactly as in the global validator. None means unbounded; a chosen integer sentinel can incorrectly reject a valid extreme key. Separately check object shape if the input may contain cycles or shared children.</Prose></details>
    <H3>Independent task C: a path need not use the root</H3>
    <Prose>Build root 20 with a left subtree whose root has two chains of length 3 beneath it, and leave 20's right side empty. Predict the diameter before computing it. Explain why returning only the path through the overall root is wrong. Then implement the one-pass subtree summary from memory.</Prose>
    <details><summary>Hint</summary><Prose>Compare the best path inside each child subtree with the path that crosses the current node. A subtree must return enough information for both kinds of candidate.</Prose></details>
    <details><summary>Solution reasoning</summary><Prose>The two length-3 chains join at the left-subtree root to form a six-edge path. The longest path through 20 reaches a deepest descendant in four edges: one edge to that subtree root plus three along its chain. The six-edge internal path wins. With height measured in edges, the child subtree has height 3 and 20 has height 4. Return (height, best diameter) as in summarize; the max over both child diameters and the through-node candidate yields 6. Construct the tree with distinct objects; its values need not obey BST ordering for this computation.</Prose></details>
    <H3>Readiness check</H3>
    <Prose>Can you explain why search discards a whole subtree, trace a traversal without confusing stack and output, reconnect a deleted successor's right child, reject a global ordering violation, and qualify a logarithmic-time claim? Then practise an unfamiliar tree problem without choosing a template from its title. If you cannot explain why a step is safe, return to its invariant before adding more problems.</Prose>
    <Prose>Next in module order is <a href="/learn/topic/heaps-priority-queues-tries">Heaps, Priority Queues & Tries</a>. A heap makes the next-priority item easy to access without fully sorting siblings; a trie follows key parts instead of whole-key comparisons. Comparing their weaker/different rules with the BST rule will help you choose the right structure.</Prose>
    <DsaPractice practice={treePractice} />
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-5-binary-search-trees-bst-sort/" target="_blank" rel="noreferrer">MIT OCW: Binary Search Trees, BST Sort</a> — lecture video and linked notes for search, insertion and traversal. Use after sections 2–3; older course code may need Python syntax updates.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-6-avl-trees-avl-sort/" target="_blank" rel="noreferrer">MIT OCW: AVL Trees, AVL Sort</a> — video plus notes for the optional balance branch. Review rotations and height reasoning before attempting a full balanced implementation.</li>
      <li><a href="https://visualgo.net/en/bst" target="_blank" rel="noreferrer">VisuAlgo: BST and AVL exploration</a> — another interactive way to compare ordinary and balanced shape and trace updates; check its displayed conventions against ours.</li>
    </ul>}>
      <li><a href="https://algs4.cs.princeton.edu/32bst/" target="_blank" rel="noreferrer">Princeton Algorithms: Binary Search Trees</a> — ordering, ordered operations, successor deletion and height-dependent costs; Java implementations supply a useful second formulation.</li>
      <li><a href="https://docs.python.org/3/library/sys.html#sys.setrecursionlimit" target="_blank" rel="noreferrer">Python documentation: recursion limits</a> — explains why logically correct recursive code still has runtime depth constraints.</li>
    </Sources>
  </div>,
};
