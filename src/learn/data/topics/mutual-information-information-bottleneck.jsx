import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { JointInformationLab, NonlinearInformationFigure, ConditionalInformationLab, ProcessingFigure, InformationBottleneckLab, BottleneckIterationLab, VariationalInformationLab, GaussianInformationFigure, InformationEstimationLab } from '../../components/lesson-labs/MutualInformationLabs.jsx';
import { mutualInformationExamples as examples } from '../mutual-information-examples.js';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram.jsx';
import { mechanismProgram } from '../mutual-information-mechanism-program.js';

function PromptedExample({ example }) {
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /></>;
}

function Practice({ prompt, hint, children }) {
  return <div className="lesson-check mi-practice"><p><strong>Try it independently.</strong> {prompt}</p><details><summary>Hint</summary>{hint}</details><details><summary>Show explained solution</summary>{children}</details></div>;
}

export default {
  title: 'Mutual Information & Information Bottleneck',
  readTime: '~85 min read + 1–2 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot mutual-information-lesson">
    <LessonIntro prerequisites="Use joint and conditional probabilities, weighted sums and logarithms from Probability and Entropy. We reconnect those ideas locally. The later optimization derivation uses a derivative with a normalization constraint; the neural bridge builds on Variational Inference."
      sections={[
        ['1-measure-the-value-of-an-observation', 'A joint table and uncertainty removed'],
        ['2-compare-the-pairing-with-independence', 'Log ratios, units and exact calculation'],
        ['3-find-information-that-single-features-miss', 'Nonlinear dependence and conditional information'],
        ['4-follow-information-through-a-processing-step', 'Data processing and its equality condition'],
        ['5-keep-the-signal-without-the-nuisance', 'Representations and the bottleneck objective'],
        ['6-compute-a-finite-bottleneck', 'Actual encoder updates and local solutions'],
        ['7-distinguish-a-training-bound-from-information', 'Variational bounds and neural encoders'],
        ['8-handle-continuous-values-and-measurement-noise', 'Gaussian information and infinite limits'],
        ['9-check-what-a-sample-can-support', 'Estimation, leakage and contrastive bounds'],
        ['10-practise-and-connect-the-next-question', 'Independent transfer and rate-distortion'],
      ]}>
      Two sensors both produce a long stream of readings. One reveals a fault; the other mostly records an irrelevant lighting change. Which stream is useful, and what should a smaller representation preserve? We will answer with distributions, then build an encoder that trades retained detail against predictive information.
    </LessonIntro>
    <Prose>The preceding lesson measured uncertainty with entropy. Here we ask how much that uncertainty changes when another variable is revealed. Then we turn the question around: design what to reveal. Nine complete Python programs use only the standard library and have verified output. Save a program as <Code>example.py</Code> and run <Code>python example.py</Code>. The browser models calculate finite sums or explicitly identified simulated samples; their graphs are computed from those quantities.</Prose>

    <H2>1. Measure the value of an observation</H2>
    <Prose>Let Y be a binary label: zero or one, equally likely. Before seeing anything else, its entropy is one bit. Now observe a binary feature X that agrees with Y on 80% of cases. After X=0, assign probabilities (.8,.2) to the two labels; after X=1, assign (.2,.8). Both conditional distributions have entropy about .722 bits. The average uncertainty removed is therefore <Code>1 − .722 = .278 bits</Code>.</Prose>
    <Prose>That average reduction is <strong>mutual information</strong>, or MI. It is a property of the joint probability law of X and Y. It is not the accuracy of one particular classifier, the number of features, or a claim that X caused Y. A classifier can fail to use information that its input contains.</Prose>
    <MathBlock>{String.raw`I(X;Y)=H(Y)-H(Y\mid X).`}</MathBlock>
    <Prose>Read the vertical bar as “given.” The conditional entropy averages the uncertainty after each possible observation, weighting it by how often that observation occurs. We write h₂(p)=−p log₂p−(1−p)log₂(1−p) for the entropy of a binary probability pair (p,1−p), with zero terms omitted. If one X value is rare, its conditional entropy contributes little to the average. The colored bars below show label probabilities; entropy is calculated from their shape, not from total bar length.</Prose>
    <JointInformationLab />
    <Checkpoint prompt="Set the flip probability to 1. Does a feature that is always wrong contain no information?">
      <Prose>No. It determines the label exactly by inversion. With balanced X, the remaining uncertainty is zero and MI is one bit. A fixed predictor that copies X has zero accuracy, while a predictor that inverts X is perfect. Information concerns what could be inferred from the distribution, rather than whether a particular decision rule is sensible.</Prose>
    </Checkpoint>
    <Prose>“Observations reduce uncertainty” is an average statement. Suppose two independent fair bits U and V determine Y=U OR V. Initially P(Y=1)=.75, with entropy about .811 bits. Seeing U=0 leaves the fair bit V, so this particular observation raises uncertainty to one bit. Seeing U=1 determines Y. Their average remaining entropy is .5 bits, which is lower than .811.</Prose>

    <H2>2. Compare the pairing with independence</H2>
    <Prose>The first joint table has .4 on each agreement cell and .1 on each disagreement cell. Its row totals and column totals are both (.5,.5). If these same marginals were independent, every cell would be .25. Dependence is visible in the <em>pairing</em>: some combinations happen more often than independence predicts, and others less often.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\imath(x;y)=\log_2\frac{p(x,y)}{p(x)p(y)},\\ I(X;Y)=\sum_{x,y}p(x,y)\,\imath(x;y).\end{gathered}`}</MathBlock>
    <Prose>The lowercase quantity, <strong>pointwise mutual information</strong>, evaluates one pair with positive probability. For an agreement cell it is log₂(.4/.25)≈.678 bits; for a disagreement cell it is log₂(.1/.25)≈−1.322 bits. Negative pointwise information means this pair is less likely than under the independent reference. Weight all four contributions by their actual probabilities to get a nonnegative average .278 bits.</Prose>
    <Prose>A zero-probability cell contributes zero to the expectation; do not evaluate log(0) and multiply an infinity by zero in code. Whenever the joint probability is positive, both associated marginals are positive. A conditional distribution for a row of total probability zero is not determined by the joint and should not be displayed as an observed fact.</Prose>
    <H3>Why this equals uncertainty removed</H3>
    <Prose>Factor p(x,y)=p(x)p(y|x). The ratio becomes p(y|x)/p(y), and its logarithm becomes a difference. Averaging −log₂p(y) gives H(Y); averaging −log₂p(y|x) gives H(Y|X). Thus MI is also the improvement in expected log loss obtained by using the true conditional label distribution instead of the true marginal label distribution.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\begin{aligned}I(X;Y)&=\mathbb E\log_2p(Y\mid X)\\&\quad-\mathbb E\log_2p(Y),\end{aligned}\\I(X;Y)=D_{\rm KL}(P_{XY}\Vert P_XP_Y).\end{gathered}`}</MathBlock>
    <Prose>KL from the previous lesson is nonnegative, with equality exactly when its two distributions coincide. Consequently MI is zero exactly when X and Y are independent. Swapping X and Y leaves the joint-versus-product expression unchanged, so MI is symmetric even though predicting X from Y can be a very different practical task from predicting Y from X.</Prose>
    <LessonTable caption="Finite discrete laws: facts and their meanings" headers={['Fact', 'Reason or interpretation']} rows={[
      ['0 ≤ I(X;Y) ≤ min(H(X),H(Y))', 'The conditional entropies are nonnegative. You cannot remove more finite label uncertainty than existed.'],
      ['I(X;X)=H(X)', 'Seeing the exact discrete value removes all its uncertainty.'],
      ['If Y=f(X), I(X;Y)=H(Y)', 'The output is determined; X may still contain information unrelated to Y.'],
      ['Bits use log₂; nats use ln', 'Multiply bits by ln 2 to get nats. Do not compare numbers without checking the log base.'],
      ['Renaming categories does not change MI', 'Invertible relabeling changes neither probabilities nor the pairing structure.'],
    ]} />
    <PromptedExample example={examples.original} />
    <Prose>This retains the original lesson’s exact table and output. To make it your own, change the joint probabilities while preserving total mass one, then independently compute the row and column totals. Reusing the old marginals after changing the table would calculate the wrong reference distribution.</Prose>

    <H2>3. Find information that single features miss</H2>
    <Prose>Pearson correlation measures linear association. Consider a centered sensor reading X equally likely to be −1, 0 or 1, and a label Y=X² that identifies a nonzero magnitude. Positive and negative readings cancel in the covariance: E[XY]=E[X]=0. Yet X determines Y exactly, giving MI=H(Y)=h₂(1/3)≈.918 bits. A sensor threshold on magnitude preserves the label; a linear trend can miss it.</Prose>
    <NonlinearInformationFigure />
    <PromptedExample example={examples.nonlinear} />
    <Prose>Information can also live in a <em>combination</em> of inputs. Let A and B be independent fair bits and let Y=A XOR B: one exactly when the bits differ. Either bit alone leaves the label fair, so I(A;Y)=I(B;Y)=0. The pair determines Y, giving I((A,B);Y)=1 bit. Rejecting every feature with zero individual MI would discard both useful ingredients.</Prose>
    <ConditionalInformationLab />
    <H3>Ask what is added after something is already known</H3>
    <Prose><strong>Conditional mutual information</strong>, I(A;Y|B), computes dependence separately inside each B slice, then averages using p(b). Write P_b for the conditional joint p(a,y|b) and Q_b for the product p(a|b)p(y|b). In each XOR slice, knowing A determines Y. Therefore I(A;Y|B)=1 bit even though unconditional I(A;Y)=0 for fair B.</Prose>
    <MathBlock>{String.raw`\begin{gathered}I(A;Y\mid B)\\=\sum_b p(b)D_{\rm KL}(P_b\Vert Q_b),\\ I((A,B);Y)\\=I(B;Y)+I(A;Y\mid B).\end{gathered}`}</MathBlock>
    <Prose>The second line is the <strong>chain rule</strong>: revealing B first, then A, removes the same total uncertainty as revealing the pair. It follows by adding and subtracting H(Y|B). The incremental term is nonnegative, but it need not equal I(A;Y). Conditioning may reveal a dependence, as in XOR, or remove one: if X=Y=C for a fair common bit C, then I(X;Y)=1 but I(X;Y|C)=0. After fixing C, both values are constants.</Prose>
    <PromptedExample example={examples.conditional} />
    <Prose>This program changes P(B=1) to .25. A now has about .189 bits about Y, while B alone still has zero; the pair still has one. Information overlaps and interactions cannot generally be represented as three ordinary, nonnegative overlapping circles. Conditional information is a safer calculation than assuming a Venn picture implies all interactions are positive.</Prose>
    <Prose>These ideas matter when selecting sensor pairs, genetic markers or complementary image channels. Evaluate an added feature conditional on what the system already uses, and validate the resulting predictor on held-out data. A high MI can arise through confounding or leakage; an observational dependence is not an intervention effect.</Prose>

    <H2>4. Follow information through a processing step</H2>
    <Prose>Now produce a representation Z from X using an encoder q(z|x). The encoder can be deterministic or randomized. Its defining restriction is that once X is known, the generation of Z does not additionally consult Y. The joint law factors as p(x,y)q(z|x). This is the Markov condition Y–X–Z; the arrows in the figure identify conditional distributions, not causal evidence.</Prose>
    <ProcessingFigure />
    <Prose>The <strong>data-processing inequality</strong> says I(Y;Z)≤I(Y;X). The processing step can make a pattern easier for a limited predictor to use, but cannot create population information about Y beyond the information available in X under this factorization.</Prose>
    <MathBlock>{String.raw`\begin{gathered}I(Y;(X,Z))\\=I(Y;X)+I(Y;Z\mid X)\\=I(Y;X),\\I(Y;(X,Z))\\=I(Y;Z)+I(Y;X\mid Z).\end{gathered}`}</MathBlock>
    <Prose>The first conditional term vanishes by the Markov condition; the second is nonnegative. Subtracting yields the exact loss I(Y;X)−I(Y;Z)=I(Y;X|Z). Equality holds precisely when Z leaves no additional label information in X. Such a representation is sufficient for predicting Y under this joint law; it need not reconstruct X.</Prose>
    <PromptedExample example={examples.processing} />
    <Prose>Here Y flips a fair X with probability .1 and Z independently flips X with probability .2. The original label information is .531004 bits; the encoded information is .173254 bits. The difference .357751 is calculated again as conditional MI. Noise is introduced in a specified channel, so the claim can be checked from the full joint law.</Prose>
    <Checkpoint prompt="A programmer appends the true label to Z during evaluation, then reports that I(Y;Z) exceeded I(Y;X). Did the inequality fail?">
      <Prose>No. The construction now depends on Y beyond X, violating the factorization. For a learned encoder, discuss fresh evaluation pairs with fitted parameters held fixed, or explicitly include the training data and parameter randomness in the probability model. Treating a label-trained map and its training samples as if all variables were an independent fresh pair can conceal leakage.</Prose>
    </Checkpoint>

    <H2>5. Keep the signal without the nuisance</H2>
    <Prose>Let the input be X=(S,N), two independent fair bits. S is a useful signal and N is a nuisance, such as an irrelevant acquisition setting. The label Y is S flipped with probability .1. The input contains two bits in total, but only 1−h₂(.1)≈.531 bits about the noisy label. Encoding S alone retains all of that label information. Encoding N alone retains one bit of input detail and no label information.</Prose>
    <Prose>The <strong>information bottleneck</strong> balances two quantities: the <strong>rate</strong> R=I(X;Z), measuring how much the representation retains about the input, and the <strong>relevance</strong> T=I(Y;Z), measuring what it retains about the chosen task. We minimize the following objective over allowed encoders.</Prose>
    <MathBlock>{String.raw`\begin{gathered}J(q)=I(X;Z)-\beta I(Y;Z),\\\beta\geq0,\\ p(x,y,z)=p(x,y)\,q(z\mid x).\end{gathered}`}</MathBlock>
    <Prose>β, pronounced “beta,” is the relevance weight. Larger β rewards task information more strongly relative to retained input information. This lesson uses this convention consistently; some papers place β on the compression term instead. The variables and the objective identify the convention, not the letter alone.</Prose>
    <InformationBottleneckLab />
    <Prose>The dotted family in the information plane contains encoders that flip S with different probabilities. It is a computed candidate family, not a claim that these points solve every possible bottleneck optimization. At β=3, storing S gives J≈−.593, storing both bits gives J≈.407, and storing no input information gives J=0. The extra nuisance bit has a measurable cost without a relevance benefit.</Prose>
    <PromptedExample example={examples.representations} />
    <H3>Two limits expose what the objective asks for</H3>
    <Prose>For 0≤β≤1, data processing also gives I(Y;Z)≤I(X;Z). Hence J≥(1−β)I(X;Z)≥0. An input-independent Z achieves zero, so a trivial representation is a global optimum in that regime. At β=1, informative equality cases can tie; do not infer that the optimum is always uniquely constant. With larger β, an informative encoder may beat zero, as the example demonstrates.</Prose>
    <Prose>Even very large β does not require memorizing all of X. It prioritizes relevance, while the positive rate term still distinguishes equally relevant candidates. If the target changes from S to the nuisance N, the same representation preferences should change. “Useful information” is always relative to a target, distribution and permitted encoder family.</Prose>
    <Prose>For a deterministic finite encoder, I(X;Z)=H(Z), because H(Z|X)=0. For a randomized encoder, I(X;Z)=H(Z)−H(Z|X). Thus a random codeword can have high entropy but no information about X. Neither mutual information nor latent dimension alone equals serialized file size, memory traffic or the length of one physical message. Operational coding questions require additional assumptions; the next lesson develops that connection.</Prose>

    <H2>6. Compute a finite bottleneck</H2>
    <Prose>An objective is only useful if we can compute an encoder. Start with four possible inputs and two codewords. Each row q(z|x) gives the probabilities of assigning one input to the two codewords. Two distributions are implied by those assignments: how often each codeword occurs, and which labels occur within it.</Prose>
    <MathBlock>{String.raw`\begin{gathered}p(z)=\sum_xp(x)q(z\mid x),\\p(x\mid z)=\frac{p(x)q(z\mid x)}{p(z)},\\p(y\mid z)=\sum_xp(x\mid z)p(y\mid x).\end{gathered}`}</MathBlock>
    <Prose>For p(z)&gt;0, the last line is a weighted average of input-specific label distributions. The denominator in p(x|z) matters: it converts the assigned joint mass into the conditional weights used by that average. The browser uses strictly positive initial probabilities and bounded settings, so both codewords remain available in its finite updates.</Prose>
    <Prose>Now compare an input’s label distribution p(y|x) with a codeword’s current label distribution p(y|z). Their KL divergence is the <strong>predictive distortion</strong>: assigning x to a codeword with the wrong label mix loses task detail. Prefer common codewords, but penalize predictive mismatch. Form unnormalized assignment weights a_z(x), then divide by their sum over codewords.</Prose>
    <MathBlock>{String.raw`\begin{gathered}d(x,z)\\=D_{\rm KL}(p(y\mid x)\Vert p(y\mid z)),\\a_z(x)=p(z)\exp[-\beta d(x,z)],\\q_{\rm new}(z\mid x)=\frac{a_z(x)}{\sum_j a_j(x)}.\end{gathered}`}</MathBlock>
    <Prose>In this exponential formula d uses natural logarithms, so it is in nats. If d is in bits, use 2 raised to −βd instead of exp(−βd). Changing the log base for both rate and relevance scales J by a common positive constant and does not change its optimizer.</Prose>
    <BottleneckIterationLab />
    <Prose>Each complete update uses the previous marginal and decoder to form distortions, normalizes every new encoder row, then recomputes the marginal and decoder. The “distortions used” table deliberately refers to the previous decoder. The current assignment bars and objective refer to the new state. There is no hidden animation or prewritten trajectory.</Prose>
    <H3>Where the update comes from, and what convergence means</H3>
    <Prose>Introduce an auxiliary codeword distribution r(z) and auxiliary label decoder w(y|z). Consider the following free energy, with expectations taken under p(x)q(z|x). Keep all logarithms in one base; use nats for the differentiation below.</Prose>
    <MathBlock>{String.raw`\begin{gathered}D_w(x,z)\\=D_{\rm KL}(p(y\mid x)\Vert w(y\mid z)),\\F(q,r,w)\\=\mathbb E_{x,z}\log\frac{q(z\mid x)}{r(z)}\\+\beta\mathbb E_{x,z}D_w(x,z).\end{gathered}`}</MathBlock>
    <Prose>For fixed q, a minimizing r is its marginal p(z), and a minimizing w is its label conditional p(y|z) on positive-mass codewords. To see both claims, add and subtract the logs of these two true distributions. The extra terms are nonnegative KL divergences. At β=0 the decoder term is absent, so its minimizer is not unique.</Prose>
    <MathBlock>{String.raw`\begin{gathered}F(q,r,w)\\=J(q)+\beta I(X;Y)\\+D_{\rm KL}(p(z)\Vert r(z))\\+\beta\mathbb E_zD_{\rm KL}(p(y\mid z)\Vert w(y\mid z)).\end{gathered}`}</MathBlock>
    <Prose>For fixed r,w, each encoder row minimizes a weighted log ratio plus a linear distortion cost, subject to summing to one. Its derivative is log q(z|x)−log r(z)+1+βd(x,z)+λₓ. Set that derivative to zero and normalize: this gives the exponential row update. These are exact minimizations of separate blocks, not a heuristic rule to assign every input to its closest codeword.</Prose>
    <Prose>Each exact block update cannot increase F. After updating r and w, F=J+βI(X;Y), with the last term fixed by the original joint law. Thus the displayed J cannot increase, apart from floating-point roundoff. This is not a proof of a unique or global optimum: the blocks are not jointly convex in all variables. A bounded monotone sequence of objective values can stabilize while the encoder sits at an inferior stationary point.</Prose>
    <PromptedExample example={examples.iteration} />
    <Prose>At β=3, the informative start reaches J≈−.600835 after 40 updates, slightly better than the deterministic signal encoder’s −.593013. A little assignment noise reduces rate enough to compensate for the small relevance loss. A perfectly symmetric start keeps both codewords identical and remains at J=0. Try that start explicitly. “Nothing changed” is not evidence that no better encoder exists. Multiple starts, feasible comparison encoders and downstream checks are needed.</Prose>
    <Prose>For K codewords, A input values and B label values, a direct dense update computes weighted decoder sums and predictive distortions with O(ABK) arithmetic and stores the corresponding tables. The small browser model is practical because A=4,B=2,K=2. Estimating these laws or representing a huge input alphabet introduces a different problem; we turn to the variational approach next.</Prose>

    <H2>7. Distinguish a training bound from information</H2>
    <Prose>For a high-dimensional input, the marginal p(z)=Eₓq(z|x) and true label conditional p(y|z) may be difficult to evaluate. A variational method replaces them with tractable approximations while tracking which direction the replacement changes the objective. The encoder itself is still q(z|x); the reference r(z) and predictive decoder s(y|z) have separate roles.</Prose>
    <MathBlock>{String.raw`\begin{aligned}U&=\mathbb E_xD_{\rm KL}(q(z\mid x)\Vert r(z))\\ &=I(X;Z)+D_{\rm KL}(p(z)\Vert r(z))\\ &\geq I(X;Z).\end{aligned}`}</MathBlock>
    <Prose>The rate replacement U is an <strong>upper bound</strong>. Add and subtract log p(z) to obtain the exact gap. Merely making this bound smaller could mean tightening the reference mismatch, reducing actual input information, or both. They are distinguishable in the finite lab.</Prose>
    <MathBlock>{String.raw`\begin{gathered}C=\mathbb E_{y,z}[-\log s(y\mid z)],\\G_s=\mathbb E_zD_{\rm KL}(p(y\mid z)\Vert s(y\mid z)),\\L=H(Y)-C\\=I(Y;Z)-G_s\leq I(Y;Z).\end{gathered}`}</MathBlock>
    <Prose>C is the decoder’s expected cross-entropy, and G_s names its nonnegative conditional KL gap. The relevance replacement L is a <strong>lower bound</strong>. A poor decoder can make L negative, even though actual MI cannot be negative. A zero decoder probability for an event with positive true mass gives infinite cross-entropy; support choices matter.</Prose>
    <VariationalInformationLab />
    <Prose>The encoder stays fixed in this investigation. Moving the reference and decoder therefore cannot change its true information. It changes only the two gaps. Matching r to the true marginal and s to the true conditional closes both gaps. In a real model, those exact choices may be unavailable.</Prose>
    <MathBlock>{String.raw`\begin{gathered}J=I(X;Z)-\beta I(Y;Z)\\ \leq U+\beta C-\beta H(Y).\end{gathered}`}</MathBlock>
    <Prose>For a fixed data law, H(Y) is constant. Minimize U+βC, or, when β&gt;0, divide by β and minimize C+(1/β)U. This explains the reciprocal parameter convention in a loss that writes prediction plus a compression penalty. At β=0, that division is invalid; the objective reduces to rate minimization.</Prose>
    <PromptedExample example={examples.bounds} />
    <H3>Connect the finite calculation to neural training</H3>
    <Prose>A neural encoder can output the mean μ(x) and positive standard deviation σ(x) of a diagonal Gaussian. With r(z) a standard normal, its per-input KL has a closed form. Draw independent standard-normal ε and set z=μ(x)+σ(x)⊙ε, where ⊙ is elementwise multiplication. The pathwise derivative from Variational Inference lets gradients pass through μ and σ while holding the sampled ε fixed. For one fixed input, abbreviate μ_j=μ_j(x) and v_j=σ_j(x)² in the KL formula below.</Prose>
    <MathBlock>{String.raw`\begin{gathered}D_{\rm KL}(q(z\mid x)\Vert\mathcal N(0,I))\\=\frac12\sum_j\left(\mu_j^2+v_j-1-\log v_j\right).\end{gathered}`}</MathBlock>
    <Prose>This Gaussian formula is in nats. Use a decoder cross-entropy in nats as well. Average the analytic KL and sampled label loss over training examples; optimize encoder and decoder parameters, then evaluate task performance, calibration and the same declared surrogate on fresh data. The analytic KL removes sampling noise from that term, but does not turn it into exact I(X;Z): the marginal mismatch gap remains.</Prose>
    <Prose>A variational autoencoder instead reconstructs X under a generative model. A supervised bottleneck explicitly rewards information about Y. Their stochastic encoders and KL calculations can look similar while their learning objectives ask for different information. An information-plane plot does not by itself demonstrate generalization, fairness, privacy or robustness. For example, exact population I(Z;A)=0 implies marginal independence from attribute A, but does not establish conditional independence after another variable is revealed; an estimated value near zero is weaker still.</Prose>

    <H2>8. Handle continuous values and measurement noise</H2>
    <Prose>The joint-versus-product definition extends beyond finite tables: MI is KL between the joint probability measure and the product of its marginals. It remains nonnegative and may be infinite. Differential entropy can be negative and changes with coordinate units, so it must not be substituted into discrete entropy intuition without checking assumptions.</Prose>
    <MathBlock>{String.raw`I(X;Z)=D_{\rm KL}(P_{XZ}\Vert P_XP_Z).`}</MathBlock>
    <Prose>When joint densities exist and the needed differential entropies are finite, one may write I(X;Z)=h(X)+h(Z)−h(X,Z), or h(Z)−h(Z|X). Do not subtract two undefined or infinite differential entropies. For jointly Gaussian scalar variables with correlation ρ and |ρ|&lt;1, the finite result is −½log(1−ρ²).</Prose>
    <Prose>Take X and E to be independent standard normals and observe Z=X+σE with σ&gt;0. The observation variance is 1+σ²; its conditional noise variance is σ². Their Gaussian entropy difference gives a particularly useful measurement example.</Prose>
    <MathBlock>{String.raw`I(X;Z)=\frac12\log_2\left(1+\frac{1}{\sigma^2}\right).`}</MathBlock>
    <GaussianInformationFigure />
    <PromptedExample example={examples.continuous} />
    <Prose>Halving a noise standard deviation is not halving its variance: the variance becomes a quarter. This is why the squared scale appears in the formula. Multiplying the entire observation Z by 10 changes its numerical units but preserves information, because it is invertible. Adding fresh noise or quantizing to bins is a processing operation and cannot increase population MI under the stated channel.</Prose>
    <Prose>At σ=0, the observation is the exact real value X and the MI is infinite. The joint law is concentrated on the diagonal Z=X, while two independent continuous draws hit that diagonal with probability zero. KL is therefore infinite. More generally, a deterministic Z=f(X) with a non-atomic output law has infinite MI with X; an exact non-atomic value encodes arbitrarily fine distinctions. A deterministic finite-valued Z instead has MI=H(Z), which can be finite.</Prose>
    <Prose>This distinction matters when someone plots a finite “MI of a deterministic neural layer.” They must specify quantization, injected noise, an estimator, or another modeling choice. A finite estimate does not refute the infinite exact-value result. A quantized sensor’s MI refers to the bins actually retained, not to an inaccessible infinitely precise reading.</Prose>

    <H2>9. Check what a sample can support</H2>
    <Prose>So far we knew the probability law and evaluated it. With observed data, a simple <strong>plug-in estimator</strong> replaces probabilities by cell counts divided by sample size, then computes the same MI formula. Its output is the MI of the empirical table. That is not automatically the MI of the population that generated the table.</Prose>
    <InformationEstimationLab />
    <Prose>Under the independent simulator, true population MI is zero. Random counts will usually not factor into exactly independent marginals, so empirical MI is often positive. Sparse tables make the problem visible: an 8×8 table has many more cells to estimate than a 2×2 table with the same number of observations. For general dependent laws, an individual estimation error can be positive or negative; do not turn this independence example into a universal sign rule.</Prose>
    <Prose>The shuffled results retain the same observed X and Y values and change their pairing. They illustrate how much apparent dependence can arise with those observed marginals after association is broken. These twenty shuffles are a diagnostic display, not a confidence interval or a calibrated significance test. A formal permutation test needs an exchangeability argument and a specified statistic and decision rule; temporal, grouped or repeated measurements may not permit unrestricted row shuffling.</Prose>
    <PromptedExample example={examples.estimation} />
    <Prose>The last calculation assigns every training row a unique identifier. The empirical label entropy is one bit, and the identifier determines every observed label, so its empirical MI is also one bit. This does not establish usefulness for a new identifier. Fit feature selection, binning and estimator tuning inside training folds, then evaluate the complete fitted procedure on held-out data. Splitting after selecting features using all labels leaks evaluation information.</Prose>
    <H3>A contrastive objective is another bound with assumptions</H3>
    <Prose>InfoNCE turns paired-data learning into identifying the positive item among K candidates. Under its population sampling setup, one candidate is drawn conditionally on the context, and the other K−1 are independent draws from the target marginal. For a fixed positive scoring function, its expected cross-entropy Lₖ gives the bound I≥log K−Lₖ in the same log units. Since this loss is nonnegative, the displayed bound is at most log K. This is not a statement that true MI is at most log K.</Prose>
    <Prose>An observed minibatch loss is a random estimate of that expectation, and training the critic on the same data adds another selection issue. Negatives chosen from a different distribution change the argument. The dedicated contrastive-learning topic develops the objective and its sampling choices; do not label its training loss as an exact MI measurement. Distribution-free, high-confidence lower bounds from finite samples also have formal limits of order log(sample size); this does not forbid useful estimation under additional model assumptions or imply that all MI estimators obey one universal ceiling.</Prose>
    <LessonTable caption="Before trusting an information claim" headers={['Ask', 'Why the answer changes the claim']} rows={[
      ['Which variables and population?', 'A target, acquisition condition or deployment distribution change can change what is relevant.'],
      ['Exact law, fitted bound or sample estimate?', 'These have different gaps and different uncertainty.'],
      ['Which units and measurement resolution?', 'Bits versus nats rescale the number; quantization can change the random variables themselves.'],
      ['Which factorization and support?', 'Data processing needs the Markov condition; KL and cross-entropy need compatible positive mass.'],
      ['Which validation separates fitting from evaluation?', 'A critic, feature selector or binning rule can overfit the same observations used to score it.'],
    ]} />

    <section id="mutual-information-code-route" aria-label="Finite mutual information and scikit-learn">
      <H3>Keep counts, probabilities and units distinct in the API</H3>
      <Prose>The complete bridge computes plug-in MI from nonnegative integer counts, masking empty cells before taking logs. It compares the result with <Code>mutual_info_score(contingency=counts)</Code> and with <Code>mutual_info_classif</Code> on the expanded rows using <Code>discrete_features=True</Code>. Both library results are in nats; divide by log(2) to match the lesson's bits. Pass actual counts to the contingency API, not a normalized probability table. Install NumPy 2.3.5 and scikit-learn 1.9.1; run <Code>python mutual-information-library-bridge.py</Code>.</Prose>
      <MechanismProgram {...mechanismProgram} title="Complete finite MI and discrete feature-estimator comparison" />
      <Prose>The table [[30,10],[10,30]] gives .130812036 nats or .188721876 bits. Duplicating every row and relabeling categories preserve this plug-in value. A unique identifier gives one empirical bit for these balanced labels, without establishing population relevance. The dense table calculation costs O(rc) time and storage for r-by-c categories; expanding N observations is only for this small API comparison. Large sparse count tables should stay sparse.</Prose>
      <Prose>The finite information-bottleneck iteration above already owns its self-consistent update, initialization sensitivity and objective trace. A feature-selection MI score is not an IB optimizer. For fitted-data selection, reuse <a href="/learn/topic/feature-selection-importance-shap-permutation-mutual-info">Feature Selection &amp; Importance</a>, which owns train-only selection, held-out evaluation and leakage controls. Continuous nearest-neighbor MI uses different assumptions and tuning; changing the discrete flag does not merely change storage.</Prose>
      <details><summary>Implementation practice: preserve and destroy pairing</summary><Prose>Relabel both categories, duplicate rows, then replace the table by [[2,4],[3,6]]. Add an all-zero category. State what the estimator should do before interpreting feature relevance.</Prose><details><summary>Solution and checks</summary><Prose>Relabeling and duplication preserve the first value. The replacement factors into its row and column marginals, so MI is zero up to roundoff; adding a zero-count category changes nothing. These invariances test the implementation, while held-out relevance and estimator bias remain separate statistical questions.</Prose></details></details>
      <Prose><a href="https://scikit-learn.org/stable/modules/generated/sklearn.metrics.mutual_info_score.html" target="_blank" rel="noreferrer">mutual_info_score</a> and <a href="https://scikit-learn.org/stable/modules/generated/sklearn.feature_selection.mutual_info_classif.html" target="_blank" rel="noreferrer">mutual_info_classif</a> document the units and discrete-feature contract used here.</Prose>
    </section>
    <H2>10. Practise and connect the next question</H2>
    <Practice prompt="A joint table has rows (.45,.15) and (.10,.30). Compute both marginals, I(X;Y) and H(Y|X). Identify one negative pointwise information value." hint={<Prose>Use the new row totals (.6,.4) and column totals (.55,.45). Weight log ratios by joint probabilities.</Prose>}>
      <Prose>H(Y)=h₂(.45)≈.992774 bits. Each row, after normalization, has probabilities (.75,.25) or (.25,.75), so H(Y|X)=h₂(.25)≈.811278. MI≈.181496 bits. For cell (0,1), log₂(.15/(.6·.45))=log₂(5/9)≈−.847997 bits; its weighted contribution is about −.127200. Negative local contributions do not make the total negative.</Prose>
    </Practice>
    <Practice prompt="Let A be fair and B independent with P(B=1)=.1. For Y=A XOR B, find I(A;Y), I(B;Y), I((A,B);Y) and I(A;Y|B). Would adding the first two numbers recover the pair’s information?" hint={<Prose>Y is fair because A is fair. Given A, the only remaining uncertainty is B; given B, A still determines Y.</Prose>}>
      <Prose>The answers are 1−h₂(.1)≈.531004, zero, one and one bit. Adding individual MI gives only .531004. The correct chain decomposition is I(B;Y)+I(A;Y|B)=0+1. The extra value of A after B is known differs from its value alone.</Prose>
    </Practice>
    <Practice prompt="In the three-valued sensor example, compare Z₁=|X| and Z₂=1[X&gt;0]. Which is sufficient for Y=X², and what conditional information is lost by Z₂?" hint={<Prose>For Z₂=0, the two equally likely possibilities are X=−1 and X=0. For Z₂=1, Y is fixed.</Prose>}>
      <Prose>Z₁ determines Y, so I(Y;Z₁)=H(Y)≈.918296. For Z₂, H(Y|Z₂)=(2/3)·1+(1/3)·0=2/3 bit. Hence I(Y;Z₂)≈.251629 and I(Y;X|Z₂)=2/3. Z₁ is sufficient for this label; Z₂ loses magnitude information that still remains in X.</Prose>
    </Practice>
    <Practice prompt="For the signal/nuisance example, compare constant, signal and both at β=.5 and β=4. Then change the target to the noiseless label Y=N. What representation should be preferred at β=4?" hint={<Prose>Use (rate,relevance)=(0,0),(1,.531004),(2,.531004). Recompute relevance after changing the label.</Prose>}>
      <Prose>At β=.5 the three objectives are 0,.734498,1.734498. At β=4 they are 0,−1.124018,−.124018. The signal beats those two alternatives at β=4, without proving it is best among all stochastic encoders. With Y=N, storing N has objective 1−4=−3, storing S has 1, and storing both has 2−4=−2. A variable’s nuisance status belongs to the task, not to its name.</Prose>
    </Practice>
    <Practice prompt="A two-codeword update has r=(.25,.75), β=2 and predictive distortions (0,ln 2) nats for one input. Calculate the new encoder row. Why would using the displayed bit value 1 inside exp give a different answer?" hint={<Prose>Form unnormalized weights r(z)exp(−βd) before normalizing.</Prose>}>
      <Prose>The weights are (.25,.75/4)=(.25,.1875); normalization gives (4/7,3/7). Using exp(−2·1) incorrectly treats one bit as one nat. The equivalent bit calculation is .75·2⁻². Units inside an exponent affect the actual encoder, not merely its displayed number.</Prose>
    </Practice>
    <Practice prompt="Suppose true rate=.6 and relevance=.4 bits, reference mismatch=.2 bits and decoder mismatch=.1 bits. Find both variational bounds and the objective gap at β=3. Does minimizing the upper objective guarantee that actual MI decreased?" hint={<Prose>The rate gap adds to rate; the predictive gap subtracts from relevance. Combine them with the same β as J.</Prose>}>
      <Prose>The rate upper bound is .8 and relevance lower bound .3. True J=.6−3·.4=−.6; its upper bound is .8−3·.3=−.1, a gap .5=.2+3·.1. Reducing a mismatch can tighten the bound while the encoder and true MI remain fixed, so a lower bound-objective value alone does not identify which information changed.</Prose>
    </Practice>
    <Practice prompt="X is standard normal. Compare observations X+.5E and 10(X+.5E), where E is independent standard normal. Then remove the noise. What happens if the noiseless observation is instead quantized to its sign?" hint={<Prose>Separate an invertible change of units, an exact real-valued observation and a finite-valued observation.</Prose>}>
      <Prose>The noisy observations both have ½log₂5≈1.160964 bits. Exact X has infinite MI with itself. Its sign is a deterministic fair binary variable (the zero event has probability zero), giving exactly one bit. A one-dimensional output can therefore have finite or infinite information depending on noise and precision; dimension alone does not resolve the question.</Prose>
    </Practice>
    <Practice prompt="Run the categorical experiment with eight categories and twenty observations under independence, then increase to two thousand. What should be recorded, and what conclusion would a single positive estimate fail to justify?" hint={<Prose>Record the declared population, seed, empirical table, estimator and sample size. The known population reference is zero; a trajectory need not decrease at every step.</Prose>}>
      <Prose>Record those settings and the two empirical MI values, along with shuffled diagnostics if used. Under this independent law the true MI remains zero for both sample sizes. Sparse empirical pairings often produce positive apparent information; a particular larger sample can fluctuate either way, so no exact monotonic numerical answer is promised. A positive estimate alone is not proof of useful population dependence, a causal effect or a calibrated rejection of independence.</Prose>
    </Practice>
    <Practice prompt="Design a feature-selection check for a neural sensor model with repeated recordings from each participant. It chooses channels by estimated MI with an intended movement label. Name a plausible leakage route and a validation change that addresses it." hint={<Prose>Repeated rows from one participant are not automatically independent evidence about deployment to a new participant. Feature selection itself uses labels.</Prose>}>
      <Prose>A channel can reveal participant or session identity correlated with training labels, and global MI selection can leak validation labels. If the intended use is new participants, split by participant first; fit preprocessing, binning and channel selection only on training participants within each fold. Evaluate the fitted pipeline on held-out participants, report task performance and uncertainty, and inspect changed acquisition conditions. A low-dimensional code or low estimated MI with an identity variable does not replace this evaluation.</Prose>
    </Practice>
    <Practice prompt="Build a complete changed-task experiment from the finite bottleneck program. Keep X=(S,N) fair and independent, but increase the label flip probability to .2 and use β=4. Compare constant, signal and both; run 40 updates from informative and symmetric starts. State what the resulting evidence establishes." hint={<Prose>Change LABEL to two rows (.8,.2) followed by two rows (.2,.8), and change each optimization call’s beta to 4. Validate row sums, recompute the joint laws, and check objective descent. Compare candidate values before interpreting the optimizer.</Prose>}>
      <Prose>The constant, signal and both objectives are 0, −.112288 and .887712. The informative start after 40 updates gives rate .698380, relevance .218212 and J=−.174466. Its first two encoder rows have P(Z=1|X)≈.053630 and the last two ≈.946370. The symmetric start remains at rate=relevance=J=0. These are independently checked results for the declared initialization and finite run; verify them to about 10⁻⁵, row sums within 10⁻¹², and no objective increase beyond roundoff. They demonstrate an improved feasible stochastic encoder and an inferior stationary initialization. They do not establish the global optimum of every encoder family or performance on a different deployment distribution.</Prose>
    </Practice>
    <Prose>You can now distinguish information present in a joint law, information retained by an encoder, a variational bound used to train it, and an estimate computed from observations. The next topic in this module is <a href="/learn/path/full-curriculum/rate-distortion-theory?module=math-foundations">Rate-Distortion Theory</a>. It asks a complementary operational question: how much communication rate is needed when a specified amount of reconstruction distortion is acceptable? Here the distortion emerged from disagreement between predictive label distributions; there we choose a distortion criterion and study coding tradeoffs.</Prose>
    <Sources alternatives={<ul>
      <li><a href="https://comsm0075.github.io/2020-21.html">Conor Houghton’s University of Bristol course: information theory videos and notes</a> — watch the linked <a href="https://www.youtube.com/watch?v=vYPR5_docCE">mutual information lecture</a>, <a href="https://www.youtube.com/watch?v=6s1JpK0PbnA">data-processing lecture</a> or <a href="https://www.youtube.com/watch?v=fM7RFNwtJuI">continuous MI lecture</a> alongside the course slides. Begin with lecture 3 if conditional entropy needs refreshing. The course’s video index and lecture descriptions were checked; these are optional alternate explanations, not required watching.</li>
      <li><a href="https://ocw.mit.edu/courses/6-441-information-theory-spring-2016/184197ca5d5418da2415d37e929860b9_MIT6_441S16_chapter_2.pdf">MIT 6.441, chapter 2: Mutual Information</a> — a written mathematical route for joint/product KL, conditional information, data processing and the continuous-value distinctions. Best after the finite investigations; the notes use measure-theoretic language for the general case.</li>
    </ul>}>
      <li><a href="https://arxiv.org/html/physics/0004057">Tishby, Pereira and Bialek: The Information Bottleneck Method</a> — original relevance/compression objective, predictive KL distortion and alternating updates. The finite derivation here keeps normalization and log units explicit and distinguishes block descent from a global optimum.</li>
      <li><a href="https://arxiv.org/html/1612.00410v7">Alemi et al.: Deep Variational Information Bottleneck</a> — section 3 develops tractable encoder/decoder bounds and stochastic neural training. Its compression-weight convention is reciprocal to the relevance-weight convention used here after rescaling the objective.</li>
      <li><a href="https://arxiv.org/html/1807.03748v2">van den Oord, Li and Vinyals: Contrastive Predictive Coding</a> — section 2.3 gives the positive-versus-marginal-negative InfoNCE setup and population lower-bound connection. A sampled loss and a population expectation have different evidential roles.</li>
      <li><a href="https://proceedings.mlr.press/v108/mcallester20a/mcallester20a.pdf">McAllester and Stratos: Formal Limitations on the Measurement of Mutual Information</a> — a precise limit for distribution-free, high-confidence lower bounds based on finite samples. Its assumptions are essential; it is not a ban on estimation with a correctly specified model.</li>
    </Sources>
  </div>,
};
