import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { StackSharingFigure, PathCopyFigure, PathCopyLab, HistoryLookupLab, PrefixRankLab } from '../../components/lesson-labs/PersistentStructuresLabs.jsx';
import { persistentStructuresExamples as examples } from '../persistent-structures-examples.js';
import practice from '../practice/persistent-data-structures-structural-sharing-versioned-queries.js';

function PracticeTask({ prompt, hint, children }) {
  return <div className="lesson-check persistent-practice-task">
    <p><strong>Try it independently.</strong> {prompt}</p>
    <details><summary>Hint</summary>{hint}</details>
    <details><summary>Show explained solution</summary>{children}</details>
  </div>;
}

export default {
  title: 'Persistent Data Structures, Structural Sharing & Versioned Queries',
  readTime: '~65 min read + 3–4 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot persistent-structures-lesson">
    <LessonIntro prerequisites="Tree nodes, references and recursive descent; correctness invariants; the idea of combining interval summaries. Range Queries supplies the segment-tree background, which we briefly rebuild here. Binary search is needed for the compact-history alternative. Optional order statistics introduces its own frequency/rank notation."
      sections={[
        ['1-keep-an-old-answer-available', 'Versions and accidental aliases'],
        ['2-share-what-did-not-change', 'Immutable tails and branching'],
        ['3-copy-the-path-to-a-changed-value', 'Point updates and historical sums'],
        ['4-prove-preservation-and-count-the-cost', 'Correctness, allocation and ownership'],
        ['5-use-a-timeline-when-branching-is-unnecessary', 'Compact snapshot histories'],
        ['6-turn-prefix-versions-into-order-statistics', 'Optional subarray ranks'],
        ['7-preserve-lazy-updates-without-mutating-children', 'Optional versioned range additions'],
        ['8-choose-persistence-for-the-right-reason', 'Applications and deeper limits'],
        ['9-practise-version-ownership-and-queries', 'Independent practice'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      A planning tool should let you try a change, compare it with yesterday's plan, and start a second experiment from yesterday without losing the first. Learn how to keep those versions while copying only the parts that change. Then answer historical range queries and use two carefully related histories to find a ranked value.
    </LessonIntro>
    <Prose>Every displayed program is complete Python 3.12+ using the standard library. Save one as <Code>example.py</Code> and run <Code>python example.py</Code>; its exact output is shown below it. Browser investigations use bounded integer models and do not execute the Python. The core route ends with a working versioned array and a reasoned choice between branching roots and timestamp histories. Sections 6 and 7 are deeper extensions, each with its own complete program.</Prose>

    <H2>1. Keep an old answer available</H2>
    <Prose>Suppose five days have planned demand <Code>[2, 1, 4, 3, 5]</Code>. Yesterday's total for days at indices 1, 2 and 3 was 8. Today you revise index 2 from 4 to 9, so that same interval now totals 13. You need both answers, each tied to its own plan. Tomorrow someone may start a different experiment from yesterday's plan.</Prose>
    <Prose>A <strong>persistent data structure</strong> keeps earlier logical versions available after updates. “Update version v” means create a result derived from v; it does not mean secretly change v's past. An ordinary in-place structure is called <strong>ephemeral</strong> when updates replace the state it previously represented. These names describe supported access to history, not whether bytes survive a power failure.</Prose>
    <Prose>The tempting first attempt is <Code>old = current</Code>. In Python that gives another name to the same object. If a nested list is later changed, both names see the change. Even copying the outer dictionary leaves its nested list shared. The following deliberately broken snapshot demonstrates the difference between copying a container and copying the objects it references.</Prose>
    <RunnableExample example={examples.alias}><Prose>The shallow copy has a different outer identity, yet its <Code>counts</Code> field reaches the original inner list. The deep copy preserves these integer-list values independently. For this small nested value, a full copy is a reasonable baseline; it is not the efficient solution when thousands of large versions differ at one location.</Prose></RunnableExample>
    <LessonTable caption="Three ways to retain a previous array value" headers={['Method', 'What happens on an update?', 'What must be true?']} rows={[
      ['Keep another reference', 'Both names observe the same mutable object', 'Safe as a snapshot only if reachable state will not change'],
      ['Copy the entire value', 'Create independent data before changing it', 'Copy the relevant descendants too; account for O(n) copied elements'],
      ['Structural sharing', 'Create changed nodes; reuse unchanged immutable substructures', 'No update may mutate a node or payload that an old version can still observe'],
    ]} />
    <Prose><strong>Structural sharing</strong> means different logical versions reference some of the same physical objects. Sharing is useful only with an ownership rule. The examples below use immutable node fields and integer payloads. Replacing an integer is a new node operation; mutating an externally shared list hidden inside a node would break the value-history promise even if the node's own fields were frozen. Python's <Code>frozen=True</Code> blocks ordinary field assignment; it does not recursively freeze arbitrary referenced objects or form a security boundary.</Prose>
    <Checkpoint prompt="A frozen node contains a mutable list called payload. An old and a new root both reach that node. Does payload.append(9) preserve the old value?"><Prose>No. The field still points to the same list, but the list's contents change for both roots. Use immutable payloads, defensively copy them at an appropriate boundary, or define a narrower promise that explicitly excludes their changing contents. Freezing only the surrounding record is insufficient.</Prose></Checkpoint>

    <H2>2. Share what did not change</H2>
    <Prose>A stack gives the smallest useful example. Its root is a reference to its top node; each node contains one value and a reference to the remaining stack, called its <strong>tail</strong>. Starting from the tail 3 → 1 → empty, pushing 9 allocates one new node whose tail points at 3. Pushing 7 onto the same old tail allocates a different head. Neither operation needs to copy 3 or 1.</Prose>
    <StackSharingFigure />
    <Prose>Notice two kinds of identity. Root A and root B represent different stacks, while n2 is one actual object shared by both. Reading through either reference gives the same immutable value 3. Popping A returns its value 9 and its tail reference. If A is still retained, it continues to represent the full stack beginning with 9.</Prose>
    <RunnableExample example={examples.stack}><Prose><Code>None</Code> represents an empty stack. Push allocates exactly one <Code>Link</Code>; pop allocates no new link. The returned Python pair is constant-size bookkeeping. Printing all values takes linear time, which is separate from the constant-time push/pop operations. Popping an empty stack raises <Code>IndexError</Code>.</Prose></RunnableExample>
    <LessonTable caption="Which histories does the interface permit?" headers={['Persistence model', 'Read older versions?', 'Where may a new update start?']} rows={[
      ['Partial', 'Yes', 'Only the latest version; updates form one timeline'],
      ['Full', 'Yes', 'Any existing version; updates form a branching version tree'],
      ['Confluent', 'Yes', 'An operation can combine multiple versions; version ancestry can become a DAG'],
    ]} />
    <Prose>A <strong>DAG</strong> is a directed acyclic graph: following its arrows never returns to the same vertex. Keep two graphs separate. The <em>version tree</em> records which version an update started from. The <em>physical node graph</em> records references between stored objects and is already a DAG when unchanged subtrees are shared—even without confluent operations. A node can have several incoming references while every individual version still looks like a tree.</Prose>
    <Prose>Our stack and the next array support full persistence: you choose any old root and receive a new root. They do not implement confluent merging. If two plans disagree about the same index, deciding how a merge should resolve that conflict is an application policy, not something shared pointers can decide.</Prose>

    <H2>3. Copy the path to a changed value</H2>
    <Prose>A stack changes only its head. An indexed array update may change a value deep inside a structure. Represent the array using a balanced interval tree: a leaf owns one index; an internal node owns an interval and stores the sum of its two children. We use <strong>half-open intervals</strong>: [l,r) contains indices l through r−1. Its length is r−l; [l,l) is empty. The root for five values owns [0,5).</Prose>
    <Prose>Split an interval at <Code>middle = (low + high) // 2</Code>. The left child owns [low,middle), the right child [middle,high). For the initial array, the root's sum is 15, the left child [0,2) stores 3, and the right child [2,5) stores 12. This shape comes from index boundaries, so sorted or repeated values cannot make it skewed. Five need not be a power of two.</Prose>
    <Prose>Now assign 9 at index 2. Follow the intervals [0,5) → [2,5) → [2,3). Build a new leaf containing 9, then build a new [2,5) parent containing 9+8=17, then a new root containing 3+17=20. Each new parent points to its new changed child and to its <em>existing</em> unchanged child. The other nodes remain physically identical. This is <strong>path copying</strong>.</Prose>
    <PathCopyFigure />
    <ol>
      <li>Read the old node; never overwrite its fields.</li>
      <li>Recursively obtain a replacement for the one child that contains the assigned index.</li>
      <li>Reuse the other child reference unchanged.</li>
      <li>Return a new parent with the two chosen references and their combined sum.</li>
      <li>Save the returned root under a new version handle.</li>
    </ol>
    <PathCopyLab />
    <Prose>The default graph contains v0 and v1 simultaneously. A node ID occurs once even when several arrows reach it. Query v0 on [1,4) and then v1 on [1,4): the answers are 8 and 13. Next create an assignment at index 0 starting from <em>v0</em>. This sibling branch must still contain 4 at index 2, because it did not start from v1.</Prose>
    <Prose>A historical range query does not replay earlier edits. Select its root and use the ordinary segment-tree rule: a disjoint node contributes 0; a fully covered node contributes its stored sum; a partially covered node asks its children and adds their answers. These selected intervals are a disjoint cover of the requested range. Empty queries return 0 without traversing the tree.</Prose>
    <RunnableExample example={examples.point}><Prose>The program creates v1 by changing index 2 and v2 by changing index 0, both from v0. The <Code>is</Code> check proves actual left-child sharing between v0 and v1; equal values alone would not prove sharing. The <Code>identities</Code> helper uses identity equality via <Code>eq=False</Code> so two distinct equal-valued nodes still count separately. It is an inspection helper, not part of logarithmic update/query work.</Prose></RunnableExample>
    <Prose>This implementation preserves the original root for a no-op assignment. When the requested leaf already has the value, it returns that leaf. Each ancestor sees both child references unchanged and returns itself. The caller may still create a new <em>version handle</em> referring to the same root; logical version IDs and physical allocations need not increase together.</Prose>
    <Checkpoint prompt="Without using the lab, assign −2 at index 0 of v0. Which nodes must be new, what is the new root sum, and is the right child shared with v0?"><Prose>The new intervals are [0,1), [0,2) and [0,5). Their sums are −2, −1 and 11. The whole right child [2,5) remains the same object with sum 12. The other branch v1 still sums to 20 and still contains 9 at index 2.</Prose></Checkpoint>

    <H2>4. Prove preservation and count the cost</H2>
    <H3>One invariant serves every retained root</H3>
    <Prose>For a node owning [l,r), its total equals the sum of that version's values in [l,r). Its children, if present, partition the interval at its midpoint and satisfy the same invariant. To prove an assignment, use induction on interval length. A leaf is either returned unchanged or replaced by the requested integer. At an internal node, induction makes the changed child correct; the unmodified child already represents the required unchanged values. A new parent adding their totals therefore represents exactly the assigned array.</Prose>
    <Prose>That proves the new value. Persistence needs one more argument: the operation never assigns to an existing node's fields, and the payloads are immutable integers. Every path reachable from an old root therefore still reaches the same objects with the same fields. This remains true when several branches share those objects. Query correctness follows from the interval partition: disjoint pieces contribute nothing and covered pieces contribute their exact sums.</Prose>
    <Prose>Termination follows because each recursive update enters one strictly shorter nonempty interval until its length is 1. A query recurses only on shorter intervals. The empty array has root <Code>None</Code>; its only valid sum range is [0,0), and every assignment is invalid. Negative Python indices are explicitly rejected rather than interpreted as indexing from the end.</Prose>
    <H3>Count physical objects, not pictures of logical arrays</H3>
    <Prose>With n≥1 leaves, this full binary tree has n−1 internal nodes: each internal node has two child edges, while a tree with N total nodes has N−1 edges; solving 2I=I+n−1 gives I=n−1. Building it therefore allocates exactly 2n−1 nodes. Its longest root-to-leaf path has at most ⌈log₂ n⌉ edges. A changed point assignment allocates one node per node on that path, at most ⌈log₂ n⌉+1. The exact count can differ between indices when n is not a power of two.</Prose>
    <LessonTable caption="Costs of the supplied point-assignment tree" headers={['Operation', 'Time in node/integer operations', 'New persistent nodes']} rows={[
      ['Build n values', 'O(n)', '2n−1 for n≥1; zero for empty'],
      ['Retain or copy a root handle', 'O(1), excluding growth of the handle container', 'Zero'],
      ['Assign one index', 'O(1+log n)', 'O(1+log n), zero for an identical assignment'],
      ['Sum one interval', 'O(1+log n) worst case; empty/full range O(1)', 'Zero'],
      ['Expand/print a whole version', 'O(n)', 'Output storage O(n) if collected into an array'],
      ['Inspect the union of reachable identities', 'O(R+U) for R handles and U reached objects', 'Temporary visited-set storage O(U)'],
    ]} />
    <Prose>For an interval query, at a given depth only nodes intersecting its two boundaries can remain partially covered. Fully covered siblings terminate immediately, so a constant number of frontier pieces per depth accounts for the logarithmic bound. The query need not read every leaf inside the interval. These bounds count constant-size node operations and arithmetic; Python integers use exact arbitrary precision, so addition and storage also depend on the numbers' bit lengths. The browser caps values and versions to keep every displayed integer exact.</Prose>
    <Prose>After m changed assignments with all roots retained, node storage is O(n+m(1+log n)), plus O(m) root handles and any application metadata. It is not O(n) just because subtrees are shared. In the five-value program, v0 uses nine objects; the two branches allocate three each, producing 15 unique objects across all three versions, rather than 27. Keeping only v1 needs nine of those objects.</Prose>
    <Prose>Releasing a root handle does not automatically free every node reachable from it. Another root may still reach some or all of them. An object becomes reclaimable only when no retained root or other live reference reaches it. Open the ownership experiment in the lab and release v0 while keeping v1. Objects shared with v1 remain needed; objects unique to v0 become logically unreachable. The demo retains an inspection arena, so its readout is a reachability calculation, not an observed garbage-collector event.</Prose>
    <Prose>Persistence also differs from <strong>rollback</strong>. An undo log can restore earlier state by reversing changes, often efficiently in a last-in-first-out order. It normally does not make arbitrary old and new states simultaneously readable without restoring or replaying something. Choose the interface you actually need. If only one state is live and historical access is rare, copying, replay or rollback can be simpler than a persistent structure.</Prose>

    <H2>5. Use a timeline when branching is unnecessary</H2>
    <Prose>Suppose updates always change today's working array, and saved snapshots need only <Code>get(index, snapshot)</Code>. A full aggregate tree can solve it, but there is a simpler partial-history design: keep a sorted list of writes for each index. Each record says “starting at snapshot s, this index has value x.” Saving a snapshot advances an ID; it does not copy every array entry.</Prose>
    <Prose>For index 0, use records (0,0), (1,5), (3,9). At snapshot 2, the answer is 5: time 1 is the latest recorded change no later than 2. At snapshot 4, the answer is 9. Index 2 has only (0,0), so every saved snapshot sees zero there. The initial record supplies the before-first-write value.</Prose>
    <HistoryLookupLab />
    <Prose>Binary search finds the first record whose time is greater than the requested snapshot. The preceding record is the latest eligible one. In the search interval [low,high), if the middle time is eligible, that record and everything earlier cannot be a <em>later</em> answer, so move low after it. If it is too late, discard it and everything later by moving high to it. When the bounds meet, they locate the boundary between eligible and too-late records.</Prose>
    <Prose>The current working ID has not been saved yet. Multiple writes at the same index in that working ID replace its last record, because no saved snapshot can observe the earlier value from that ID. Once <Code>snap()</Code> returns that ID and advances the counter, a later write must append a new record instead of replacing the saved one.</Prose>
    <RunnableExample example={examples.history}><Prose>The official Snapshot Array problem uses this interface. This implementation additionally defines a zero-length array and permits negative integer values; neither broadening changes the history logic. Only already saved snapshot IDs are queryable. <Code>bisect_right</Code> receives an integer target and extracts only the timestamp from each record; it does not compare values as a tie-breaker.</Prose></RunnableExample>
    <Prose>Initialization takes O(n) records. Set takes O(1) amortized list-append work, snapshot O(1), and get O(log h) comparisons for h records at that index. Total records are O(n+u), where u counts writes that append rather than replace; retaining fewer records than calls is intentional. For a string-keyed history, as in Time Based Key-Value Store, first locate that key's history, then apply the same predecessor search. Appending assumes the write timestamps arrive in the required order.</Prose>
    <Checkpoint prompt="v1 changes index 0 to 5. v2 branches from v0 and changes index 1 to 7. Why is choosing the last write with version ID ≤2 wrong for index 0 in v2?"><Prose>It would select v1's value 5, although v1 is a sibling rather than an ancestor of v2. v2 should still contain v0's value 0 at index 0. Increasing IDs record creation order, not ancestry. Root-based path copying encodes each branch's actual values; a history method for full persistence needs additional ancestry-aware machinery.</Prose></Checkpoint>

    <H2>6. Turn prefix versions into order statistics</H2>
    <Prose>This optional application changes what the tree stores. Given <Code>[5, 1, 4, 1, 3, 5]</Code>, find the third-smallest <em>occurrence</em> in subarray [1,5). Its values are [1,4,1,3], whose sorted order is [1,1,3,4], so the answer is 3. Duplicates occupy separate ranks. Sorting each requested subarray would repeat substantial work.</Prose>
    <Prose>First sort the distinct values to obtain <Code>[1, 3, 4, 5]</Code>. Their positions are <strong>compressed coordinates</strong>: original value 1 becomes coordinate 0, 3 becomes 1, and so on. Sorting preserves value order while replacing a potentially enormous numeric range with σ occupied buckets, where σ is the number of distinct values. These coordinates are value buckets, not positions in the original array.</Prose>
    <Prose>Create a tree of zero counts over these σ buckets. Root P₀ represents the empty prefix. To obtain Pᵢ₊₁ from Pᵢ, add one to the bucket for array element i, using a persistent point assignment. Root Pⱼ now describes frequencies in array positions [0,j). The frequency of any bucket in [l,r) is its count in Pᵣ minus its count in Pₗ: the earlier prefix cancels from the later prefix.</Prose>
    <PrefixRankLab />
    <Prose>For [1,5), P₅ has counts [2,1,1,1], P₁ has [0,0,0,1], and their difference is [2,1,1,0]. At the root, the left half contains values 1 and 3, with 3 occurrences. Because rank 3 is within those 3, descend left without changing the rank. There, the left bucket 1 has 2 occurrences. Skip them, descend right with local rank 3−2=1, and reach value 3.</Prose>
    <Prose>The invariant is that the requested occurrence lies within the current value interval and has local rank k between 1 and that interval's total count. Let c be its left-half count, obtained by subtracting the earlier left-node total from the later left-node total. If k≤c, the occurrence is in the left half with rank k. Otherwise, all c left occurrences precede it, so it is in the right half with rank k−c. At a single bucket, that bucket's value is the answer.</Prose>
    <RunnableExample example={examples.rank}><Prose>The program materializes an iterable once, compresses values and builds the prefix roots. Its update first reads the bucket's current count and then assigns count+1; both operations are logarithmic. It does not expand a histogram during a query. The browser expands tiny histograms deliberately to expose the subtraction and rank decision.</Prose></RunnableExample>
    <Prose>Preprocessing includes O(n) expected hashing work for Python's set/dictionary operations, O(σ log σ) sorting comparisons, and O(n(1+log σ)) tree work. The usual simpler bound is O(n log n) expected preprocessing under these hash assumptions. Each rank query uses O(1+log σ) node operations. Initial count-tree storage is O(σ), new nodes O(n(1+log σ)), and roots O(n). The array may be empty at construction, but no rank query is then valid.</Prose>
    <Prose>Why insist on <em>prefix</em> roots? Pₗ's multiset is contained in Pᵣ for l≤r, so every bucket difference is nonnegative and the total is r−l. Two arbitrary branch roots do not have this containment guarantee. Histograms [2,0] and [0,2] differ by [−2,2]; a negative “number of occurrences” cannot support the rank invariant. Subtracting version sums can still report an arithmetic difference, but selecting a kth occurrence needs stronger semantics.</Prose>

    <H2>7. Preserve lazy updates without mutating children</H2>
    <Prose>The point tree changes one leaf. A versioned interval increment should avoid visiting every leaf in the interval. The mutable lazy tree from Range Queries postpones work using tags, but copying its root and then <em>pushing into shared children</em> would corrupt older versions. Here is a different fully functional design for <strong>range addition and range sum only</strong>. It keeps tags at their nodes and carries their contribution during queries.</Prose>
    <Prose>Each node has a stored total and an <Code>add</Code> tag. The total includes that node's own tag, but its children do not include that tag. For an internal node of width w, maintain <Code>total = left.total + right.total + add × w</Code>. Children may have their own tags. Nothing in this design means that a child can be interpreted as a complete standalone array version without its ancestors' contributions.</Prose>
    <LessonTable caption="A full-array +10 update leaves both children physically unchanged" headers={['Object', 'Stored total', 'Own add tag', 'Meaning']} rows={[
      ['Old root [0,5)', '15', '0', '3 + 12 + 0×5'],
      ['New root [0,5)', '65', '10', '3 + 12 + 10×5'],
      ['Shared left child [0,2)', '3', '0', 'Querying through the new root inherits +10 per element'],
      ['Shared right child [2,5)', '12', '0', 'Still represents its local subtotal before the parent’s +10'],
    ]} />
    <Prose>For a fully covered update by d, allocate a node with total increased by d×w and add increased by d; retain both child references. For a partial update, recursively update intersected children and rebuild the current node with its <em>unchanged</em> own tag. Its new total is the new child totals plus that tag times its width. A disjoint interval or a zero increment returns the old node.</Prose>
    <Prose>A query carries an <Code>inherited</Code> increment from strict ancestors. At a fully covered node, return its stored total plus inherited×width. When descending, pass inherited+node.add to each child. Do not add the node's tag again at the covered-node return: its total already includes it. In the full-array +10 version, a point query for original value 1 reaches a leaf whose stored total is still 1, but its inherited increment is 10, giving 11.</Prose>
    <RunnableExample example={examples.lazy}><Prose>v2 adds −3 to [1,4) of v1, leaving the root's +10 tag in place. v3 branches from v0 and adds 2 to [2,5), so it inherits none of v1's +10. The displayed leaf values come from queries with the correct carries. Inspecting raw leaf totals alone would produce incorrect answers for v1 and v2.</Prose></RunnableExample>
    <Prose>Correctness follows the tag invariant. A full-cover update adds exactly d once per element of that interval. A partial update preserves the ancestor contribution and changes only the children's covered elements. A query's inherited carry accounts for precisely the strict ancestors whose tags are absent from its stored total. Old nodes are never modified, so the persistence proof is the same ownership argument as before. A contiguous update or query visits only O(1+log n) boundary/cover nodes, with that many new nodes for an update and none for a query.</Prose>
    <Prose>This no-push algebra relies on addition commuting with other additions. Do not extend it by simply adding an assignment tag: “set to 4, then add 2” differs from “add 2, then set to 4,” and an old parent assignment can invalidate descendant summaries. A mixed set/add persistent tree needs a carefully composed operation order and immutable child transformations. The supplied code intentionally promises only range addition and sum.</Prose>

    <H2>8. Choose persistence for the right reason</H2>
    <LessonTable caption="Applications need an access pattern, not just a desire for history" headers={['Situation', 'Why sharing helps', 'What remains outside this data structure']} rows={[
      ['Undo and branching experiments', 'Keep a root for each checkpoint; return to it or branch from it without deep-copying everything', 'Decide which checkpoints to retain, how redo branches behave, and whether results must survive restart'],
      ['Repeated historical analysis', 'Answer the same interval under different plans by selecting different roots', 'Version labels, provenance, access control and display of differences'],
      ['Speculative search or an interpreter environment', 'Child computations reuse unchanged state while changing a few bindings', 'Use a suitable persistent map; our fixed-index sum tree is not a name dictionary'],
      ['Stable snapshots for readers', 'A reader holding an immutable root sees a consistent structure while another root is constructed', 'Coordinate publication of the chosen root, concurrent writers, mutable payloads and reclamation'],
      ['Subarray order statistics', 'Nested prefix roots share most of their frequency trees', 'The original array is fixed during this preprocessing; arbitrary later edits require another design'],
    ]} />
    <Prose>Persistence is a data-structure property. It does not automatically provide disk durability, database isolation or thread-safe publication of shared application state. Two writers can both compute correct new roots from the same old root and still lose an intended update if publication simply overwrites one result with the other. The concurrency lesson's coordination rules and the SQL lesson's transaction semantics remain relevant.</Prose>
    <details>
      <summary>Deeper: path copying is a technique, not a universal cost theorem</summary>
      <Prose>A persistent binary search map can similarly copy the search path, rebuilding aggregates and any rebalancing changes. Its cost depends on actual height h: an unbalanced chain costs O(n), while a correctly maintained balanced tree can keep h logarithmic. Tree rotations must allocate/reuse nodes without modifying shared ones; a mutable implementation cannot be converted just by changing its return type.</Prose>
      <Prose>Wide branching persistent vectors and hash tries apply sharing to different navigation schemes, and production libraries engineer node layout, collisions, small collections and batched changes. Clojure's official collection documentation is one concrete API reference. Our binary range tree teaches the ownership mechanism, not a claim to match a production vector's locality or constants.</Prose>
      <Prose>Another family stores modification records inside nodes, sometimes called fat-node methods. A query interprets the appropriate record for a version; record growth, lookup and full-version ancestry must be handled. The original persistence paper proves stronger overhead results under specified pointer-machine assumptions using more machinery than our simple path copy. Its constant amortized overhead is not a theorem that every Python tree update allocates only one object. Our actual measured allocation contract is the copied path.</Prose>
    </details>
    <details>
      <summary>Deeper: a branching history can invalidate an amortized argument</summary>
      <Prose>A common mutable queue keeps a front list and a rear list. When the front becomes empty, it reverses the rear once; along one sequence of destructive operations, each element can be charged for that move. But if an old queue with an empty front remains available, several branches can each reverse the same rear again.</Prose>
      <RunnableExample example={examples.queue}><Prose>Three independent dequeues of the same four-element rear perform 12 reversal visits. Generalizing to m branches and n rear elements gives mn visits. The tuple representation adds its own copying costs; this example deliberately counts only the repeated reversal that already defeats the claimed shared credit.</Prose></RunnableExample>
      <Prose>The failure is in reusing a one-future amortization argument for many futures. It does not mean efficient persistent queues are impossible. Shared memoized work or scheduled incremental rebuilding can support appropriate bounds, with their own proof obligations. Okasaki's chapters on persistence and queues are the advanced follow-on; the constant-time immutable stack does not require this machinery.</Prose>
    </details>
    <LessonTable caption="Symptoms that expose an incorrect persistence implementation" headers={['Symptom', 'Likely cause', 'A decisive check']} rows={[
      ['An old query changes after a new update', 'A shared node or mutable payload was modified', 'Save all old point/range answers; recheck after every branch'],
      ['Updates allocate nearly a whole tree', 'Both children are copied, including untouched subtrees', 'Count identity-new objects and compare with the changed path'],
      ['Sibling branches contain each other’s writes', 'Creation-time order was mistaken for ancestry', 'Create two updates from the same old root at different indices'],
      ['Memory grows despite discarding a version name', 'Other handles, caches or inspection structures retain references', 'Compute the union of all actual root reachability'],
      ['A kth query descends using negative counts', 'The two histories do not have prefix containment', 'Check every bucket difference and total population'],
      ['A lazy query is off by tag×width', 'A tag was omitted or counted twice', 'Compare all small intervals with an eagerly updated array'],
    ]} />

    <H2>9. Practise version ownership and queries</H2>
    <Prose>Write predictions before opening the explanations. For implementation tasks, create your own small tests before borrowing the examples. A passing latest-version query is insufficient: old versions and sibling branches are the important witnesses.</Prose>
    <PracticeTask prompt="1. Build a tree for seven zeros. Assign 6 at index 6. Predict the intervals copied, the total initial nodes and the new allocation count." hint={<Prose>Use floor midpoints and follow only the right child when index 6 is at or above the midpoint.</Prose>}><Prose>The path is [0,7), [3,7), [5,7), [6,7). The initial tree has 13 nodes; the changed assignment adds four. Repeating the same assignment through that new root allocates zero nodes in this implementation. Four is the ceiling-logarithmic bound plus one for this size.</Prose></PracticeTask>
    <PracticeTask prompt="2. Implement a version manager with fork(version), assign(version,index,value) and sum(version,l,r). A fork returns a distinct handle but performs no tree copy. Branch twice from an older version and show that all three answers remain available." hint={<Prose>The list of handles is mutable application bookkeeping. Its entries reference immutable roots. A new assignment should append a root, not overwrite the source entry.</Prose>}><Prose>Use a roots list; fork appends roots[version], assign appends the returned updated root, and sum chooses roots[version]. Maintain copied arrays as an independent test oracle and check every [l,r) after each operation. Assert that fork returns a different handle with the same root identity. Include negative values, empty sums, repeated assignments and non-power-of-two sizes.</Prose></PracticeTask>
    <PracticeTask prompt="3. Retain v0 and v1 from the default five-value update. How many unique objects are needed? Release v0, then release v1. Why can the first release not reclaim all nine of v0's objects?" hint={<Prose>v1 adds three nodes and shares the other six nodes from v0.</Prose>}><Prose>Twelve objects are reachable while both roots are retained. Keeping only v1 leaves nine; v0's three superseded path nodes become unreachable. Releasing the last root leaves zero reachable objects if no other references exist. The shared six remain necessary after the first release. A browser arena or debugger may itself be another reference owner.</Prose></PracticeTask>
    <PracticeTask prompt="4. An index is set to 4 and then 8 before snapshot 0 is saved; snapshot 1 is saved without another write. What records and query answers are necessary?" hint={<Prose>No saved snapshot observed the intermediate 4.</Prose>}><Prose>One record (0,8) suffices at that index. Both get(index,0) and get(index,1) return 8. Saving an unchanged snapshot creates a new valid ID without duplicating per-index data. After saving, replacing the old time-0 record would break history, so the next write uses the current newer ID.</Prose></PracticeTask>
    <PracticeTask prompt="5. Replace range sum with range minimum. What changes in the persistent point tree, and can the prefix-frequency kth algorithm simply use minimum differences?" hint={<Prose>Separate the associative range aggregate from a count's arithmetic meaning.</Prose>}><Prose>Store a node's minimum, combine children with min and use positive infinity for a disjoint query. The path-copy ownership proof is unchanged. But minima do not subtract to produce a subarray minimum or a population count; kth descent needs additive frequencies and nonnegative populations. Structural sharing applies broadly; subtraction is an extra algebraic property.</Prose></PracticeTask>
    <PracticeTask prompt="6. For [4,2,4,1], derive prefix frequency roots and answer rank 2 in [1,4). Then explain whether these roots alone remain valid if the original element at index 0 is edited later." hint={<Prose>The sorted distinct buckets are [1,2,4]; [1,4) contains [2,4,1].</Prose>}><Prose>P1 is [0,0,1], P4 is [1,1,2], so their difference is [1,1,1] and rank 2 returns 2. The saved prefix roots continue to represent the original array. Editing the source array does not magically update later prefixes; rebuilding or a different dynamic order-statistics structure is needed for queries over the edited array.</Prose></PracticeTask>
    <PracticeTask prompt="7. In the lazy program, start at [2,1,4,3,5], add 10 to all positions, then add −3 to [1,4). Derive the value at index 2 and explain which ancestor contribution cannot be lost." hint={<Prose>The root still has add=10 after the second update. Descendant updates represent the extra −3 in their local coordinates.</Prose>}><Prose>The answer is 4+10−3=11. The leaf or a lower ancestor represents the −3, while the query must inherit the root's +10. The root's stored total is 56, already including its own tag. Adding +10 again to that full-root total would double-count 50.</Prose></PracticeTask>
    <PracticeTask prompt="8. An API claims O(1) amortized persistent queue dequeue because each rear element is reversed only once. Construct a family of valid operations that tests the missing assumption." hint={<Prose>Keep a reference to a version whose next dequeue forces a length-n reversal. Do not feed each result into the next operation.</Prose>}><Prose>Call dequeue on that same retained version m times. A naive implementation reverses n elements independently on each branch, doing Θ(mn) reversal work. The old one-timeline accounting spends the same credit repeatedly. A repaired claim must identify shared memoized work or a different schedule and prove its cost over branching executions.</Prose></PracticeTask>
    <DsaPractice practice={practice} />
    <Prose>For a direct full-branching application, try <a href="https://cses.fi/problemset/task/1737/" target="_blank" rel="noopener noreferrer">CSES: Range Queries and Copies (opens in a new tab)</a>. Its array handles may be copied and later edited independently. A copy can initially share a root, and an edit replaces only the selected handle with a newly built root. Translate its one-based inclusive [a,b] into this lesson's zero-based [a−1,b). The official time and memory limits are a separate implementation challenge; the clear Python object implementation here is not a claim of an accepted submission at maximum constraints.</Prose>
    <Prose>You are ready to continue when you can draw both the version ancestry and the physical sharing graph, implement a historical query without replay, and explain every newly allocated node after a branch. The next module topic is <strong>External-Memory Algorithms, B-Trees & I/O Complexity</strong>. It changes the cost model from individual memory operations to movement of blocks between memory levels. Keeping a version cheaply and making that version durable on disk are related engineering questions with different correctness requirements.</Prose>
    <Sources alternatives={<>
      <p><strong>Another route through the ideas.</strong> <a href="https://courses.csail.mit.edu/6.851/spring12/lectures/L01.html" target="_blank" rel="noopener noreferrer">MIT 6.851 Lecture 1: persistence (official video and notes)</a> develops the version models and more advanced pointer-machine constructions. Read sections 1–5 here first; its 2012 open-problem remarks are historical, not a current survey. The linked official description/notes were reviewed; the video was not watched end to end.</p>
      <p><a href="https://www.cs.cmu.edu/~rwh/students/okasaki.pdf" target="_blank" rel="noopener noreferrer">Chris Okasaki, Purely Functional Data Structures, chapters 3–4</a> is the deeper written route into why ordinary amortization can fail across branches and how functional queues address it. It uses functional-language notation and is more advanced than this lesson's Python examples.</p>
    </>}>
      <li><a href="https://www.cs.cmu.edu/~sleator/papers/making-data-structures-persistent.pdf" target="_blank" rel="noopener noreferrer">Driscoll, Sarnak, Sleator and Tarjan: Making Data Structures Persistent</a> — original partial/full definitions and model-dependent transformation results. Its stronger overhead bounds require the paper's machinery; they are not substituted for our path-allocation proof.</li>
      <li><a href="https://docs.python.org/3/library/copy.html" target="_blank" rel="noopener noreferrer">Python copy documentation</a> — assignment, shallow copying and deep copying; no use of newer copy.replace APIs is required by the Python 3.12 programs.</li>
      <li><a href="https://docs.python.org/3/library/dataclasses.html" target="_blank" rel="noopener noreferrer">Python dataclasses documentation</a> — frozen field behavior and equality options. Payload immutability remains an explicit design contract.</li>
      <li><a href="https://clojure.org/reference/data_structures" target="_blank" rel="noopener noreferrer">Clojure's official collection reference</a> — a real persistent collection interface to compare after mastering references and versions; this lesson does not benchmark that implementation.</li>
      <li>Official public practice statements checked on 10 September 2026: <a href="https://leetcode.com/problems/snapshot-array/description/" target="_blank" rel="noopener noreferrer">Snapshot Array</a>, <a href="https://leetcode.com/problems/time-based-key-value-store/description/" target="_blank" rel="noopener noreferrer">Time Based Key-Value Store</a>, and the CSES task above. The hints and transfer tasks here are original, not reproduced editorials.</li>
    </Sources>
  </div>,
};
