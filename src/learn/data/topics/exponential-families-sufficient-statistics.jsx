import { H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CanonicalFeatureFigure, FamilyMomentLab, FamilyNormalizerLab, GaussianSummaryFigure, PriorCoordinateLab, SufficiencyFiberFigure, SufficiencyModelLab } from '../../components/lesson-labs/ExponentialFamilyLabs.jsx';
import { exponentialFamilyExamples } from '../exponential-family-examples.js';
function FamilyExample({
  example
}) {
  return <section className="family-example"><Prose><strong>Question to investigate:</strong> {example.question}</Prose><RunnableExample example={example} /></section>;
}
function FamilyExercise({
  prompt,
  hint,
  children
}) {
  return <section className="lesson-check family-exercise"><p><strong>Try it first.</strong> {prompt}</p><details><summary>Show a hint</summary><Prose>{hint}</Prose></details><details><summary>Show the solution</summary><div>{children}</div></details></section>;
}
export default {
  title: 'Exponential Families & Sufficient Statistics',
  readTime: '~55 min read + 90–120 min exploration and practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot exponential-family-lesson">
    <LessonIntro prerequisites="Conditional probability, expectation, variance and likelihood from Probability Distributions & Bayes’ Theorem and Maximum Likelihood & MAP Estimation. We rebuild the relevant notation here. The derivative sections use basic gradients; the density-coordinate section explains its change-of-variable step." sections={[['1-what-can-you-forget-without-changing-the-inference', 'What a sufficient summary preserves'], ['2-factor-the-likelihood-and-test-the-model', 'Factorization and model changes'], ['3-build-the-family-from-weights', 'Natural parameters and normalization'], ['4-differentiate-the-normalizer', 'Means, covariance and exponential tilting'], ['5-fit-moments-and-recognize-boundaries', 'Moment matching and geometry'], ['6-keep-the-right-gaussian-summary', 'Spread and mergeable summaries'], ['7-connect-the-model-to-features', 'GLMs and feature-aware statistics'], ['8-update-a-prior-with-the-right-units', 'Conjugacy, exposure and coordinates'], ['9-practise-changing-the-inference-contract', 'Independent practice'], ['10-connect-the-ideas-and-continue', 'Readiness and the next foundation']]}>You receive eight binary outcomes. Can you replace them with “six successes in eight trials” and still make exactly the same inference? Sometimes. This lesson shows what “sometimes” means, why many familiar probability models have compact summaries, and how one normalization function produces their moments, fitting equations and Bayesian updates.</LessonIntro>

    <H2>1. What can you forget without changing the inference?</H2>
    <Prose>Suppose eight independent trials share one unknown success probability p. Record a success as 1 and a failure as 0. The sequence 1,0,1,1,0,1,1,1 has six successes. Its probability under that model is p⁶(1−p)². Every rearrangement with six successes has the same expression. When comparing candidate values of this one p, rearranging the observations changes nothing.</Prose>
    <Prose>A <strong>statistic</strong> is a function of the data that does not use the unknown parameter: a count, mean, maximum or even the whole dataset. A statistic is <strong>sufficient</strong> for a specified parameter under a specified model when, conditional on that statistic, the remaining distribution of the data does not depend on the parameter. “Conditional on” means we restrict attention to datasets with that summary and ask how probability is distributed among them.</Prose>
    <Prose>Make the definition concrete with four trials and S=2 successes. There are six compatible ordered datasets. Each has probability p²(1−p)²; their total probability is six times that. Dividing one dataset’s probability by the group’s probability gives 1/6, independently of p, for0&lt;p&lt;1.</Prose>
    <SufficiencyFiberFigure />
    <MathBlock>{String.raw`\begin{gathered}\Pr_p(X=x\mid S=2)\\ =\frac{p^2(1-p)^2}{6p^2(1-p)^2}=\frac16\end{gathered}`}</MathBlock>
    <Prose>Once you know S=2, a random choice among those six strings reconstructs a dataset with the correct conditional law without knowing p. You cannot reconstruct the actual original string. Sufficiency preserves the information used for parameter inference under the model, not the physical record itself. At p=0 or 1, the event S=2 has probability zero; this conditional fraction is undefined, rather than evidence against the interior calculation.</Prose>
    <Prose>Programs use Python 3 and its standard library: copy a whole program to a file and run <code>python example.py</code> or <code>py example.py</code>. Printed values are rounded. Labs execute here; Python runs in your environment.</Prose>
    <FamilyExample example={exponentialFamilyExamples.conditional} />
    <Prose>The exact fractions show that changing p changes how often the group occurs but not its internal relative probabilities. Change the condition to S=1: four datasets remain, each with conditional mass 1/4. Known sample size n belongs to the setup. If you store only a success count and later lose n, you have lost essential context.</Prose>
    <Checkpoint prompt="Is the whole ordered dataset sufficient? Does sufficiency automatically mean that a summary is small?"><Prose>Yes, the full dataset is sufficient: after conditioning on it, nothing remains random. No, sufficiency alone does not imply compression. A smaller sufficient statistic can preserve the same parameter information with less storage or simpler computation.</Prose></Checkpoint>

    <H2>2. Factor the likelihood and test the model</H2>
    <Prose>Conditional enumeration becomes awkward for large or continuous datasets. The <strong>factorization theorem</strong> gives a practical criterion for models with a common dominating measure, such as mass functions on the same discrete space or densities integrated against ordinary length. A statistic S is sufficient when the joint density or mass factors into a parameter-dependent part that sees the data only through S, and a remaining part independent of the parameter.</Prose>
    <MathBlock>{String.raw`p_\theta(x)=g_\theta(S(x))\,h_0(x)`}</MathBlock>
    <Prose>Here x denotes the entire dataset, θ the unknown parameter, and h₀ can depend on all observations. In the discrete case the reason is direct: within a group S=s, the same gθ(s) appears in every probability. It cancels when conditioning, leaving h₀(x) divided by the sum of h₀ over the group. That conditional distribution has no θ. For continuous data the corresponding theorem uses conditional distributions, not division by the probability of an exact continuous value.</Prose>
    <Prose>For independent, identically distributed Bernoulli trials, S=Σxᵢ, gₚ(S)=pˢ(1−p)ⁿ⁻ˢ and h₀=1 on binary datasets. Modeling the <em>count</em> S as one Binomial(n,p) observation introduces C(n,S), the number of compatible strings. That factor changes the probability of the event being described but does not depend on p. An ordered string and a count event are different sample-space objects.</Prose>
    <H3>Equal information need not mean equal dataset probability</H3>
    <Prose>For three independent Poisson counts with one positive rate λ, datasets (0,3,1) and (2,1,1) both total 4. Their likelihoods contain the same λ⁴e⁻³λ, but the factorial denominators are 6 and 2. The first dataset is one third as probable at every λ. Both produce the same relative comparison between candidate λ values because their likelihood functions differ by a parameter-independent constant.</Prose>
    <FamilyExample example={exponentialFamilyExamples.poissonRatio} />
    <Prose>You may drop a parameter-independent term while optimizing that one parameter or normalizing its posterior. You cannot automatically drop it when computing an absolute data probability, comparing sample spaces, or comparing models whose omitted terms differ.</Prose>
    <H3>A richer model can need a richer summary</H3>
    <Prose>Suppose the first four binary observations come from group A and the next four from B, with two unknown probabilities. Group counts (4,2) and (3,3) both total 6, but their probabilities involve different powers of pA and pB. One total no longer retains all information about that parameter pair; the two group counts do.</Prose>
    <SufficiencyModelLab />
    <Prose>At pA=0.8 and pB=0.3, moving a success from A to B changes current/reference likelihood to (0.3/0.7)/(0.8/0.2)=3/28≈0.107143. Under a common p the ratio stays 1. The summary was sufficient for the original model and insufficient for the expanded one. Event totals can support a constant-rate fit yet erase evidence about drift, weekday effects or user segments.</Prose>
    <Prose>Aggregation is not a privacy guarantee: sufficient statistics can still reveal sensitive information. Raw records may also be needed for audits or future model checks. Retention is a broader decision than sufficiency.</Prose>

    <H2>3. Build the family from weights</H2>
    <Prose>An <strong>exponential family</strong> is a structured collection of distributions. Each outcome gets a nonnegative base weight h(x). A vector of statistics T(x) describes aspects of that outcome the parameter can reward or penalize. A vector η, pronounced “eta,” controls the rewards through the dot product ηᵀT(x). Exponentiating gives a positive weight. Divide by the sum or integral of weights to obtain a probability mass or density.</Prose>
    <MathBlock>{String.raw`\begin{gathered}p_\eta(x)=h(x)e^{\eta^\mathsf{T}T(x)-A(\eta)},\\ A(\eta)=\log\!\int h(x)e^{\eta^\mathsf{T}T(x)}\,d\nu(x)\end{gathered}`}</MathBlock>
    <Prose>The integral is a sum for discrete outcomes; ν describes the counting or integration rule. We keep h explicit rather than absorbing it into ν. The <strong>natural parameter</strong> η is the coordinate multiplying T. The <strong>log-partition function</strong>, or log-normalizer, is A: eᴬ is the total unnormalized weight. Subtracting A in the exponent divides by that total.</Prose>
    <Prose>The <strong>natural parameter space</strong> contains η values for which the total is finite and positive. The base weight and support are fixed, independent of η. With finite η and positive h, an outcome cannot abruptly become impossible. Some familiar distribution families include degenerate endpoints as limits; those need not have finite natural parameters. A full family uses the entire natural parameter space. A regular full family has an open natural parameter space. We state interior conditions where differentiation requires them.</Prose>
    <H3>Derive Bernoulli rather than memorize its row</H3>
    <Prose>For x=0 or 1, log p(x)=x log p+(1−x)log(1−p). Collect the coefficient of x: log[p/(1−p)]. Set η to that coefficient. Solving for p gives the sigmoid. The remaining term is−log(1+eη). Thus T=x, h=1 on {'{0,1}'}, and A=log(1+eη). Every real finite η represents a probability strictly between zero and one.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\eta=\log\frac{p}{1-p},\qquad p=\frac{e^\eta}{1+e^\eta},\\ A(\eta)=\log(1+e^\eta)\end{gathered}`}</MathBlock>
    <Prose>For Poisson, expanding λˣe⁻λ/x! gives T=x, η=logλ, h=1/x! on nonnegative integers and A=eη. For an Exponential waiting time with rate λ&gt;0, density λe⁻λx on x≥0 gives η=−λ&lt;0, T=x, h=1 and A=−log(−η). Here η≥0 is invalid because the integral diverges. “Exponential family” is the broad structure; “the Exponential distribution” is one member.</Prose>
    <LessonTable caption="Concrete representations: identify support before using a formula" headers={['Model', 'Statistic / natural coordinate', 'Normalizer and domain']} rows={[['Bernoulli, x=0 or 1', 'T=x; η=log[p/(1−p)]', 'A=log(1+exp η); η real; h=1'], ['Poisson, x=0,1,…', 'T=x; η=log λ', 'A=exp η; η real; h=1/x!'], ['Exponential time, x≥0', 'T=x; η=−λ', 'A=−log(−η); η<0; h=1'], ['Categorical, K outcomes', 'T=K one-hot indicators; η=unrestricted logits', 'A=log Σ exp ηk; h=1; one redundant coordinate'], ['Gaussian, unknown mean and variance', 'T=(x,x²); η=(µ/σ², −1/(2σ²))', 'η₂<0; derive A in section6']]} />
    <FamilyNormalizerLab />
    <Prose>The middle base weight here is 2. Therefore A(0)=log 4, not log 3. The unweighted three-outcome family used later has h=(1,1,1) and A(0,0)=log 3. Declaring base weights prevents a similar-looking diagram from silently changing the model.</Prose>
    <Prose>For stable computation, set m=max log-weight, exponentiate log-weight−m, and divide by the shifted sum. The largest shifted exponent is zero, preventing overflow. Recover A as m+log(shifted sum). Very small probabilities can still underflow in finite precision; use log probabilities for rare-event work. Avoid taking the log of a probability already rounded to 0 or 1.</Prose>

    <H2>4. Differentiate the normalizer</H2>
    <Prose>Why does the form help inference? Differentiate A=logZ. The derivative of log contributes 1/Z; differentiating exp(ηᵀT) contributes T. The remaining normalized weight is exactly pη. Component by component, the derivative is the expected statistic.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\frac{\partial A}{\partial\eta_j}=\frac{\int T_j(x)h(x)e^{\eta^\mathsf{T}T(x)}d\nu(x)}{Z(\eta)}\\ =\mathbb E_\eta[T_j(X)]\end{gathered}`}</MathBlock>
    <Prose>This holds at interior natural-parameter points where the standard differentiation identities apply. Finite support makes it a finite sum. For unbounded support, finiteness in a neighborhood justifies the required moment and differentiation steps; a boundary point where A happens to be finite does not automatically have those properties.</Prose>
    <Prose>Next differentiate the normalized density. The numerator’s derivative contributes Tₖ; the normalizer’s derivative subtracts E[Tₖ]. Multiplying by Tⱼ and averaging gives E[TⱼTₖ]−E[Tⱼ]E[Tₖ], a covariance entry. The <strong>Hessian</strong> collects all second derivatives.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\nabla_\eta\log p_\eta(x)=T(x)-\mathbb E_\eta[T(X)],\\ \nabla A(\eta)=\mathbb E_\eta[T(X)],\\ \nabla^2 A(\eta)=\operatorname{Cov}_\eta[T(X)]\end{gathered}`}</MathBlock>
    <Prose>Covariance has nonnegative quadratic forms: vᵀCov(T)v=Var(vᵀT)≥0. Thus A is convex. Its curvature measures how much the statistic varies and how sensitively its mean changes when tilted. For Bernoulli, A′=p and A″=p(1−p). These are derivatives with respect to η, not p. Differentiating A(logit p)=−log(1−p) with respect to p gives 1/(1−p), not the mean; a change of coordinates requires the chain rule.</Prose>
    <H3>The same shift produces a moment-generating function</H3>
    <Prose>Concentration bounds used E[exp(uT)] to control tails. Multiplying the density by exp(uᵀT) changes η to η+u before renormalizing. Provided the shifted parameter is valid, the log moment-generating function is a difference of log-normalizers.</Prose>
    <MathBlock>{String.raw`\log\mathbb E_\eta[e^{u^\mathsf{T}T(X)}]=A(\eta+u)-A(\eta)`}</MathBlock>
    <Prose>For Poisson this is λ(eᵘ−1), the expression behind its Chernoff calculation. For Exponential waiting time, shifting η=−λ requires u&lt;λ; ignoring the domain can create an alleged bound from an infinite expectation. Derivatives with respect to u at zero recover cumulants, including mean and covariance.</Prose>
    <FamilyExample example={exponentialFamilyExamples.normalizer} />
    <Prose>At η=0.7, the mean is about 0.336376 and variance 0.443426. Central finite differences agree to printed precision on this fixture; they check the implementation, not replace the proof. At η=1000 the shifted A remains finite, while negligible probabilities round to zero in machine arithmetic. The mathematical distribution still has positive mass at each supported outcome.</Prose>
    <H2>5. Fit moments and recognize boundaries</H2>
    <Prose>For n independent observations from the same family, multiplying densities adds exponents. Let S=ΣT(xᵢ). All parameter dependence enters through S and known n; the product of base factors stays independent of η. This proves sufficiency by factorization and makes the fitting objective compact.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\ell(\eta)=\eta^\mathsf{T}S-nA(\eta)+C(x),\\ \nabla\ell=S-n\mathbb E_\eta[T],\\ \nabla^2\ell=-n\operatorname{Cov}_\eta[T]\end{gathered}`}</MathBlock>
    <Prose>At a <strong>finite interior maximum</strong> in unrestricted natural coordinates, the score is zero: the model’s expected statistic equals S/n. This is moment matching. Concavity makes a stationary point a global maximum on the convex natural domain. It does not by itself prove that a finite maximizer exists, nor that parameters are uniquely identified.</Prose>
    <FamilyExample example={exponentialFamilyExamples.bernoulli} />
    <Prose>This retains the earlier six-of-eight example. Both outcomes occur, so p=0.75 is interior and η≈1.098612 is finite. Expected successes are np=6. Change all eight observations to 1: the MLE over closed p∈[0,1] is p=1, but no finite log-odds attains it. The original program would divide by zero; that boundary requires a branch, not an arbitrary tiny failure presented as the same MLE. All-zero Poisson data similarly yield a boundary rate of zero, with η tending to minus infinity.</Prose>
    <H3>See two sufficient moments at once</H3>
    <Prose>Take X∈{'{−1,0,1}'}, T(X)=(X,X²), h=1 and η=(η₁,η₂). Outcome −1 has weight exp(−η₁+η₂), zero has weight 1, and +1 has weight exp(η₁+η₂). η₁ favors one sign; η₂ favors nonzero outcomes relative to zero. The statistic values form three triangle vertices. Averaging them with probabilities places the mean inside their convex hull.</Prose>
    <MathBlock>{String.raw`A(\eta)=\log\!\left(1+e^{-\eta_1+\eta_2}+e^{\eta_1+\eta_2}\right)`}</MathBlock>
    <FamilyMomentLab />
    <Prose>Write the probabilities q−, q₀, q+. Their ratios to q₀ are the two nonzero weights. Taking logs and adding or subtracting gives the inverse mapping below. When all observed counts are positive, substitute their proportions to obtain this family’s unique finite MLE.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\hat\eta_1=\tfrac12\log\frac{q_+}{q_-},\\ \hat\eta_2=\tfrac12\log\frac{q_+q_-}{q_0^2}\end{gathered}`}</MathBlock>
    <Prose>Counts (2,5,3) give q=(0.2,0.5,0.3), observed mean vector (0.1,0.5), and η≈(0.202733,−0.713558). If q₀=0, the target lies on the upper triangle edge. Sending η₂ toward positive infinity suppresses zero’s normalized probability; finite η₂ never makes it exactly zero. This explicitly solved finite example illustrates existence and boundary geometry. General unbounded families can require additional conditions for global mean-parameter results.</Prose>
    <FamilyExample example={exponentialFamilyExamples.moments} />
    <H3>Remove redundant coordinates; distinguish a constrained family</H3>
    <Prose>For K categories, K one-hot indicators always sum to 1. Adding the same constant to every logit changes A by that constant and changes no probability. The covariance has a zero direction corresponding to this common shift. Fix one reference logit to zero and use K−1 log-ratios to it; A becomes log(1+Σexp ηₖ). Do not set A=0 for unrestricted logits. Zero A is compatible with already-normalized log probabilities only on their constrained surface.</Prose>
    <Prose>A <strong>minimal exponential-family representation</strong> has no nonzero linear combination of statistic coordinates equal to a constant almost surely under the base support. In a regular full family this removes affine redundancies and makes covariance positive definite. A <strong>minimal sufficient statistic</strong> is a different concept: it is sufficient and recoverable from every other sufficient statistic, up to usual null-set qualifications. Invertibly recoding a sufficient statistic preserves sufficiency; appending unused coordinates can preserve sufficiency while making a representation redundant.</Prose>
    <Prose>Restricting η to a curve η(θ) also changes fitting equations. Its score is the Jacobian transpose times S−nE[T]. A zero projected score need not make every residual component zero. Do not apply unrestricted moment matching to a curved or constrained model without re-deriving its parameter-space conditions. Fisher information per observation in natural coordinates is Cov(T); independent n observations multiply it by n. Inverse-information uncertainty formulas need identifiable interior parameters and their asymptotic assumptions, not merely a matrix inversion.</Prose>

    <H2>6. Keep the right Gaussian summary</H2>
    <Prose>Which Gaussian quantities must you keep? Expand the log density instead of guessing. For unknown mean µ and variance σ²&gt;0, the data-dependent terms involving parameters are xµ/σ² and −x²/(2σ²). Thus T=(x,x²), η₁=µ/σ² and η₂=−1/(2σ²)&lt;0. Completing the square gives A below when h=1 on the real line.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\eta_1=\mu/\sigma^2,\qquad\eta_2=-1/(2\sigma^2),\\ A(\eta)=-\frac{\eta_1^2}{4\eta_2}+\frac12\log\frac{-\pi}{\eta_2}\end{gathered}`}</MathBlock>
    <Prose>Indeed, η₁x+η₂x²=η₂(x+η₁/(2η₂))²−η₁²/(4η₂). Integrating the negative-quadratic exponential contributes √(−π/η₂). Its log plus the remaining constant yields A. Converting back gives A=µ²/(2σ²)+½log(2πσ²). This checks the sign and squared mean; confusing variance with precision, its reciprocal, changes the family.</Prose>
    <GaussianSummaryFigure />
    <Prose>For fixed n, retain Σx and Σx² when both parameters are unknown. With known σ², the term involving Σx² is parameter independent, so Σx alone is sufficient for µ. In the picture, A=(1,2,3) and B=(0,2,4) share sum 6 but have squared sums 14 and 20. Their Gaussian likelihood ratio depends on σ². That dependence is irrelevant to inference about µ when σ² is fixed.</Prose>
    <H3>A useful application: merge summaries from separate data partitions</H3>
    <Prose>Raw sums of squares can lose precision when values have a large common offset: subtracting huge nearly equal numbers to recover a small variance is fragile. An equivalent summary is (n,m,M₂), where m is the sample mean and M₂=Σ(xᵢ−m)². In exact arithmetic, Σx=nm and Σx²=M₂+nm². It supports a numerically preferable one-pass update and a merge without storing every observation.</Prose>
    <Prose>For nonempty partitions A and B, set d=mB−mA and n=nA+nB. The merged mean shifts mA toward mB in proportion to B’s size. Total squared deviation includes both within-partition deviations and a between-partition correction.</Prose>
    <MathBlock>{String.raw`\begin{gathered}m=m_A+d\,n_B/n,\\ M_2=M_{2,A}+M_{2,B}+d^2n_An_B/n\end{gathered}`}</MathBlock>
    <Prose>Why the extra term? Expand each group’s x−m as (x−mgroup)+(mgroup−m). Cross terms sum to zero because centered deviations sum to zero. The squared mean offsets simplify to d²nAnB/n. Adding only the two within-group M₂ values would pretend the group means were equal.</Prose>
    <FamilyExample example={exponentialFamilyExamples.gaussianMerge} />
    <Prose>The separate means 2 and 5.5 are substantially apart. Their within-group M₂ values sum to 2.5; the between-group correction 14.7 produces 17.2. Gaussian MLE variance is M₂/n=3.44. Dividing by n−1 gives the usual unbiased sample variance 4.3 under iid sampling; those are different estimators. If M₂=0 and variance is unknown, the positive-variance Gaussian likelihood can become unbounded as variance approaches zero. That is another boundary rather than a finite ordinary fit.</Prose>
    <Prose>Distributed aggregation, streaming sensor calibration and summary-based posterior updates can use these identities. Mathematical merging is order independent; floating-point computations need not be bitwise associative. This summary cannot by itself diagnose a changing mean over time or multimodality.</Prose>

    <H2>7. Connect the model to features</H2>
    <Prose>A constant probability is often too restrictive. A <strong>generalized linear model</strong>, or GLM, combines a response distribution, a linear predictor from known features, and a link connecting the conditional mean to that predictor. The <strong>canonical link</strong> chooses the distribution’s canonical parameter as the linear predictor. Other valid links exist; a GLM does not require that canonical choice.</Prose>
    <CanonicalFeatureFigure />
    <Prose>For Bernoulli responses yᵢ, use known feature vectors zᵢ and shared coefficients β. The canonical model sets ηᵢ=zᵢᵀβ, giving pᵢ=sigmoid(ηᵢ). Let X contain feature vectors as rows. The independent joint log likelihood is βᵀXᵀy−ΣA(zᵢᵀβ). Holding X fixed, the response enters through Xᵀy. An intercept-only column retains Σy; additional columns retain feature-weighted outcome sums.</Prose>
    <MathBlock>{String.raw`\begin{gathered}L(\beta)=\sum_i\left[A(z_i^\mathsf{T}\beta)-y_i z_i^\mathsf{T}\beta\right],\\ \nabla L=X^\mathsf{T}(p-y),\\ \nabla^2L=X^\mathsf{T}\operatorname{diag}(p_i(1-p_i))X\end{gathered}`}</MathBlock>
    <Prose>L is negative log likelihood. Its gradient compares model expectations with observed outcomes, weighted by each feature. The Hessian propagates Bernoulli variances through the design matrix. With fixed full-column-rank X and finite β it is positive definite; separated binary data can nevertheless push the best fit toward infinite coefficients. Positive curvature at finite points alone does not establish finite existence.</Prose>
    <FamilyExample example={exponentialFamilyExamples.features} />
    <Prose>The statistic is (2,1), not simply “two successes.” With the same feature rows, changing outcomes from (0,1,1) to (1,1,0) retains the total 2 but changes the second component to −1. A slope model distinguishes those datasets. The code evaluates a declared coefficient vector; it does not claim that vector is optimal.</Prose>
    <Prose>Poisson regression commonly uses log conditional mean as its canonical link. Categorical softmax uses category log-ratios with a reference or an explicit redundant-logit convention. Gaussian GLMs commonly separate dispersion σ² and use an identity link for µ. Our density absorbed 1/σ² into η=µ/σ²; with known variance that constant rescaling can be absorbed into coefficients. Keep the parameter and dispersion convention explicit.</Prose>
    <Prose>The outcome type narrows plausible likelihoods but does not certify one. Poisson assumes conditional variance equal to its mean; unexplained overdispersion, dependence or zero inflation can make it unsuitable. Bernoulli calibration can fail even while predictions stay in [0,1]. Examine residuals and held-out predictions; retain context for diagnosis. Later model-specific lessons develop those fitting and diagnostic questions.</Prose>

    <H2>8. Update a prior with the right units</H2>
    <Prose>Bayesian updating multiplies prior by likelihood. An iid exponential-family likelihood contributes exp(ηᵀS−nA). A prior density with the same algebraic form keeps that structure after updating. With respect to <em>ordinary volume dη in natural-parameter space</em>, write its kernel using hyperparameters χ and ν. Hyperparameters specify the prior; they are not the unknown likelihood parameter. Here ν is a scalar prior weight, a different object from the integration measure denoted by ν earlier.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\pi(\eta\mid\chi,\nu)\propto e^{\chi^\mathsf{T}\eta-\nu A(\eta)},\\ (\chi,\nu)\longmapsto(\chi+S,\nu+n)\end{gathered}`}</MathBlock>
    <Prose>A <strong>conjugate prior</strong> belongs to a family preserved by that update. The kernel still needs a finite normalizing integral on its domain. Similar-looking exponents do not prove properness, and conjugacy is a computational property rather than a reason that a prior suits an application. We use positive-parameter Beta and Gamma cases whose normalization is established.</Prose>
    <H3>The same prior is not the same density formula in every coordinate</H3>
    <Prose>A Beta(α,β) density for p is proportional to pᵅ⁻¹(1−p)ᵝ⁻¹ for α,β&gt;0. Under η=logit p, an infinitesimal interval dp has width p(1−p)dη. Probability mass is density times width, so the density with respect to dη must include dp/dη=p(1−p), the <strong>Jacobian</strong>.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\pi_\eta(\eta)=\pi_p(p(\eta))\,p(\eta)(1-p(\eta)),\\ \pi_\eta(\eta)\propto\exp\!\left(\alpha\eta-(\alpha+\beta)A(\eta)\right)\end{gathered}`}</MathBlock>
    <Prose>Thus χ=α and ν=α+β in η coordinates. Using α−1 and α+β−2 would omit the Jacobian and describe another prior. The ordinary posterior in p remains Beta(α+S,β+n−S). Starting with Beta(2,2) and observing six of eight gives Beta(8,4), with posterior mean 8/12=2/3. That differs from MLE 3/4 because the prior contributes information.</Prose>
    <PriorCoordinateLab />
    <FamilyExample example={exponentialFamilyExamples.coordinates} />
    <Prose>At p=0.5, a Beta(2,3) density has height 1.5 per p unit. The corresponding η=0 density has height 0.375 per log-odds unit. Neither height is itself the probability of that exact point. Integrating corresponding intervals gives equal mass. A uniform prior on p is not a uniform proper prior on the unbounded η line. Maximum density locations, and hence MAP coordinates, can also change under transformation; probabilities of corresponding events do not.</Prose>
    <H3>Count events together with the time they could occur</H3>
    <Prose>Unequal exposure is a useful practical trap. Independently observed counts xᵢ follow Poisson(λtᵢ), with one unknown rate λ per hour and positive known durations tᵢ. Expanding the joint likelihood leaves λˢe⁻λE, where S=Σxᵢ and E=Σtᵢ. The remaining product tᵢˣⁱ/xᵢ! is parameter independent. Known E is total exposure, not number of rows.</Prose>
    <Prose>A Gamma prior with shape α and <em>rate</em> β has density proportional to λᵅ⁻¹e⁻βλ. Multiplication gives posterior shape α+S and rate β+E. We use rate, whose mean is shape/rate, not the alternative scale convention. Counts (3,1,8) over (1,0.5,2) hours with Gamma(2,rate1) produce Gamma(14,rate4.5), giving mean rate about 3.111111 events/hour.</Prose>
    <FamilyExample example={exponentialFamilyExamples.exposure} />
    <Prose>Updating each partition once or combining their S and E once gives the same posterior. Duplicating a batch counts its evidence twice. Units matter: rate per minute is rate per hour divided by 60; the Gamma rate hyperparameter in minutes multiplies by 60. A unit mismatch can survive algebraically correct code.</Prose>
    <Prose>For categorical counts, a Dirichlet update adds each category count to its own positive prior parameter. The vector matters; a total alone cannot identify proportions. These exact conjugate computations complement Bayesian and Variational Inference: they are convenient where the model supports them; many other posteriors require approximation.</Prose>
    <H2>9. Practise changing the inference contract</H2>
    <Prose>Reason before opening the answers. These tasks change data or assumptions so that matching a displayed output is insufficient.</Prose>
    <FamilyExercise prompt="For five iid Bernoulli trials with one unknown interior p, keep only whether the success count is even or odd. Is parity sufficient? Give a conditional or likelihood-ratio counterexample." hint="Compare datasets in the even group with totals 0 and 2. Their ratio must be parameter independent if their distinction becomes irrelevant after conditioning."><Prose>Compare 00000 and 11000. Their probabilities are (1−p)⁵ and p²(1−p)³. The second/first ratio is [p/(1−p)]², which depends on p. Both have even parity, yet their relative conditional probabilities vary with the unknown parameter. Parity is insufficient. A many-to-one recoding of a sufficient statistic need not remain sufficient.</Prose></FamilyExercise>
    <FamilyExercise prompt="For outcomes 0,1,2, base weights (1,3,2), statistic T(x)=x and η=log 2, compute the masses, A, E[X] and Var(X). Which derivative coordinate produces the mean?" hint="Multiply each base weight by 2 raised to its outcome, normalize, then compute a centered variance or E[X²]−E[X]²."><Prose>Weights (1,6,8) total 15. Probabilities are (1/15,6/15,8/15), and A=log 15≈2.708050. E[X]=22/15; E[X²]=38/15. Variance is 38/15−(22/15)²=86/225≈0.382222. Derivatives with respect to η give mean and variance. If r=expη, the chain rule gives dA/dr=E[X]/r, not E[X]. Omitting base weights changes the model.</Prose></FamilyExercise>
    <FamilyExercise prompt="Fit the triangle family to counts (1,8,1) and (4,2,4). Find η₁, η₂ and E[X²]. Why does matching their common mean X=0 not suffice?" hint="Symmetry gives η₁=0. Compare the probability of a nonzero outcome to that of zero to recover η₂."><Prose>For (1,8,1), q=(0.1,0.8,0.1), η₁=0, η₂=log(1/8)≈−2.079442 and E[X²]=0.2. For (4,2,4), q=(0.4,0.2,0.4), η₁=0, η₂=log 2≈0.693147 and E[X²]=0.8. A zero mean does not distinguish mass at zero from symmetric mass on the extremes. The second statistic is an independent model direction. The earlier normalizer lab used different base weights; do not reuse its one-parameter formula.</Prose></FamilyExercise>
    <FamilyExercise prompt="Two Gaussian partitions have summaries (n,mean,M₂)=(2,1,2) and (2,5,2). Find the merged summary, MLE variance and unbiased sample variance. What changes if variance is known?" hint="The mean difference is 4. Include its squared contribution times nA nB/(nA+nB)."><Prose>Combined n=4 and mean 3. Between-group correction 16 makes M₂=2+2+16=20. MLE variance is 5; unbiased sample variance is 20/3≈6.666667. Adding only within-group deviations would give the wrong M₂=4. If variance is fixed and only µ is unknown, n and sum 12 suffice for that parameter. Spread may still be useful for checking the assumed variance or model.</Prose></FamilyExercise>
    <FamilyExercise prompt="Repair two claims without silently changing the model: (a) concavity ensures every all-success Bernoulli sample has a finite natural-parameter MLE; (b) three softmax logits uniquely identify three class probabilities." hint="Consider a limit toward probability 1, then add the same constant to every logit. Separate existence from redundancy."><Prose>(a) The closed-p maximum is p=1, but over finite η the likelihood approaches its supremum as η→∞. Concavity does not ensure finite attainment. A prior or regularizer may produce a finite alternative, but changes the objective. (b) A common logit shift changes no probability. Fix a reference logit or another identifying constraint. Removing redundancy still does not guarantee finite MLE existence with a missing observed category.</Prose></FamilyExercise>
    <FamilyExercise prompt="Revisit daily website visits: first use one Poisson rate with equal one-day exposure, then log-rate=β₀+β₁·weekend with fixed labels. Which sufficient response summaries and known context should you retain?" hint="Expand β₀Σy+β₁Σ(weekend·y). The parameter-dependent normalizer also needs the number of each type of day."><Prose>For the constant model, keep total visits and known observation days. For the feature model, keep total and weekend visits, equivalently separate weekday/weekend totals, plus each group’s exposure. Equal grand totals with different allocation now carry different parameter information. The usual canonical link is log conditional mean. Unequal durations require known exposures and a log-exposure offset so λ represents visits per unit time. A trend, holidays or other features can require new weighted summaries and known design context.</Prose></FamilyExercise>
    <FamilyExercise prompt="Counts (2,4) over (0.5,1.5) hours update Gamma(shape 3, rate 2 hours) on events/hour. Find the posterior and mean, then express the same prior and posterior for events/minute." hint="Add counts to shape and hours to the rate hyperparameter. Transform the random rate λminute=λhour/60, rather than changing only its label."><Prose>Six events in two hours give Gamma(9,rate4 hours), mean 9/4=2.25 events/hour. Scaling the random rate by 1/60 multiplies the Gamma rate hyperparameter by 60: prior Gamma(3,rate 120 minutes), posterior Gamma(9,rate 240 minutes), mean 0.0375 events/minute. Number of rows happens to equal total hours here; that coincidence is not the rule. Change the second exposure to 2.5 hours: posterior rate becomes 5 hours and mean 1.8/hour.</Prose></FamilyExercise>
    <FamilyExercise prompt="A uniform prior on p∈(0,1) is updated by three successes and one failure. Write the posterior in p and its kernel in log-odds η. Why is a flat η-density not the same prior? Finally explain why max(X) is sufficient for iid Uniform(0,θ), despite moving support." hint="Uniform p is Beta(1,1); include dp/dη. For the last part, factor the joint density using the largest observation and retain its parameter-dependent support indicator."><Prose>The posterior is Beta(4,2), with p-density proportional to p³(1−p). Its η-kernel is exp(4η−6A). Uniform p has η-density p(1−p), not a constant. A positive constant over the real line cannot normalize. For Uniform(0,θ), the joint density is θ⁻ⁿ·1{'{max xᵢ≤θ}'}·1{'{min xᵢ≥0}'}, ignoring immaterial continuous endpoint conventions. It factors through the maximum, which is sufficient. But the support changes with θ, preventing this from being a fixed-support finite-natural-parameter family of the form used above. Sufficiency is broader; a converse needs a separate theorem and hypotheses.</Prose></FamilyExercise>

    <H2>10. Connect the ideas and continue</H2>
    <Prose>The complete chain is now visible: declare a sampling model and unknown parameter; factor its interaction with observations; identify the statistic and known context; normalize exponential weights; derive moments and curvature; solve or diagnose a fit; update a prior in declared coordinates and units. This explains familiar Bernoulli classification, categorical softmax, Poisson counts and Gaussian summaries without making their assumptions interchangeable.</Prose>
    <FamilyExercise prompt="A monitoring service first fits a constant event rate, then compares device groups, then investigates drift. Propose summaries for each stage and state what aggregation prevents you from recovering." hint="For every stage, name the likelihood or diagnostic question before deciding which observation distinctions to discard."><Prose>A defensible design retains total count and exposure for a constant rate, separate count/exposure per fixed group for group rates, and time-indexed records or suitably fine summaries for drift. Preserve sampling design, units and duplicate-batch protection. Group totals cannot reconstruct within-group event times, and fixed time bins cannot recover arbitrary timing below bin resolution. “One total is enough because Poisson is exponential-family” fails when the likelihood or diagnostic question changes. A satisfactory answer explicitly states those lost distinctions and explains which future queries remain supported.</Prose></FamilyExercise>
    <Prose>You are ready to continue when you can prove a simple sufficiency claim, refute one with a ratio, derive a normalizer and moments, explain a boundary in the triangle, merge spread correctly and transform a prior while preserving event probabilities. If uncertain, return to the corresponding investigation and change its inputs before rereading the answer.</Prose>
    <Prose>Next in the full Mathematical & Statistical Foundations sequence is <strong>Measure Theory & Probability Spaces</strong>. It develops the common language behind sums, integrals, densities and conditioning, including why a density depends on a reference measure. Follow the reader’s Next link for the selected path and module context. Completeness, refined minimal-sufficiency theory and general boundary geometry are deeper branches, not conclusions guaranteed by these finite examples.</Prose>
    <Sources alternatives={<Prose><a href="https://ocw.mit.edu/courses/18-650-statistics-for-applications-fall-2016/resources/lecture-21-video/" target="_blank" rel="noreferrer">MIT18.650, Philippe Rigollet: Lecture21, Generalized Linear Models</a> is a video/lecture alternative after the core. It motivates response distributions and links before decomposing the family. The official page and relevant transcript examples were reviewed, not full video playback. Translate its notation and normalization conventions explicitly.</Prose>}>
      <li><a href="https://ocw.mit.edu/courses/18-655-mathematical-statistics-spring-2016/resources/mit18_655s16_lecnote6/" target="_blank" rel="noreferrer">MIT18.655: Sufficiency</a> — graduate notes for conditional sufficiency and factorization, useful after the binary groups.</li>
      <li><a href="https://www.stat.berkeley.edu/~wfithian/courses/stat210a/exponential-families.html" target="_blank" rel="noreferrer">Berkeley Stat210A: Exponential Families</a> — deeper natural-space and log-MGF treatment. Check the independently expanded Gaussian formula here; the linked page contains transcription inconsistencies.</li>
      <li><a href="https://slinderman.github.io/stats305b/lectures/04_expfam.html" target="_blank" rel="noreferrer">Stanford STATS305B: Exponential Families</a> — moments, fitting and reference-category parameterization. Keep unrestricted logits distinct from normalized log probabilities, and qualify finite-MLE existence.</li>
      <li><a href="https://www.stat.umn.edu/geyer/f22/5421/notes/prior.html" target="_blank" rel="noreferrer">Charles Geyer: Proper Conjugate Priors</a> — advanced normalization conditions and hyperparameter updates. Mathematical properness and the author’s philosophical preference for conjugacy are separate.</li>
    </Sources>
  </div>
};
