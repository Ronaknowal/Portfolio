import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DijkstraFrontierLab, BellmanFordPassLab, SpanningForestLab, TopologicalDependencyLab, RouteVersusNetworkFigure, CriticalPathFigure } from '../../components/lesson-labs/WeightedGraphLabs.jsx';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { weightedGraphExamples } from '../weighted-graph-examples.js';
import graphPractice from '../practice/shortest-paths-spanning-trees-topological-ordering.js';

export default {
  title: 'Shortest Paths, Spanning Trees & Topological Ordering',
  readTime: '~90 min read + 3–5 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot weighted-graphs-lesson">
    <LessonIntro prerequisites="Graphs' vertices, adjacency lists, directed edges and equal-cost BFS; Heaps' priorities and stale entries; Union-Find's component joins; Greedy's exchange reasoning; DP's states and transitions. The needed invariants, edge budgets and path witnesses are derived here."
      sections={[
        ['1-ask-which-graph-question', 'Separate three graph objectives'],
        ['2-relax-routes-and-finalize-safe-candidates', 'Dijkstra: candidates, stale entries and proof'],
        ['3-allow-negative-edges-with-an-edge-budget', 'Bellman–Ford and negative-cycle influence'],
        ['4-specialize-the-route-contract', '0/1 costs, all pairs and reweighting'],
        ['5-connect-a-network-at-minimum-total-cost', 'Kruskal, Prim and the cut argument'],
        ['6-emit-only-ready-dependencies', 'Topological order and actual cycle witnesses'],
        ['7-use-dependency-order-to-solve-paths-and-schedules', 'Signed DAG routes and critical paths'],
        ['8-practice-the-changed-question', 'Independent proofs, witnesses and transfer'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      A graph describes possible connections. An algorithm also needs a question: what is the cheapest route, which connections should be installed, or what may happen before what? Learn to choose from those questions, watch each algorithm's state acquire meaning, and justify the choices it makes.
    </LessonIntro>

    <H2>1. Ask which graph question</H2>
    <Prose>A team has six services. A request can travel between some services, cables could connect some pairs, and build jobs depend on other jobs. All three situations can be drawn with dots and lines. They have different outputs and different notions of success.</Prose>
    <LessonTable caption="Specify the objective before naming an algorithm" headers={['Question', 'Input meaning', 'Required output']} rows={[
      ['Cheapest route from A', 'Directed or undirected edge weights add along a route', 'A best cost and, when finite, an actual route to each reachable target'],
      ['Cheapest connecting network', 'One undirected installation cost per chosen edge', 'An acyclic connecting edge set with minimum total weight in each component'],
      ['A legal dependency order', 'u→v means u must precede v', 'Every vertex exactly once with all arrows pointing forward, or a cycle proving impossibility'],
    ]} />
    <RouteVersusNetworkFigure />
    <Prose>A <strong>weight</strong> is a number attached to an edge: travel seconds, an exact abstract penalty, or an installation cost. The drawing's line length is not the weight unless the problem explicitly defines that relationship. Use compatible units before adding costs. Direction is also part of the input: A→B does not create B→A. An undirected edge can be represented by two directed adjacency entries for routing, but remains one selectable occurrence for a spanning forest.</Prose>
    <Prose>A <strong>walk</strong> may revisit vertices and edges. Here a <strong>simple path</strong> visits each vertex at most once. We seek minimum-cost walks; whenever the answer is finite, a simple path can achieve it. Remove a repeated cycle whose cost is nonnegative and the remaining walk is no more expensive. A reachable negative cycle changes this reasoning because repeating it can keep reducing the cost. The empty walk from a vertex to itself costs zero unless a relevant negative cycle makes costs unbounded below.</Prose>
    <Prose>Earlier BFS minimized the number of edges. Consider A→B with cost 10, A→C with cost 1, and C→B with cost 1. BFS discovers B in one hop. The cheapest weighted route has two hops and costs 2. Multiplying every edge count by the same positive cost preserves BFS's answer; unequal costs remove that guarantee.</Prose>
    <Prose>All standalone programs use standard-library Python 3.10 or newer and exact integer weights; displayed output was checked in Python 3.12. Save any complete program as <Code>graph_example.py</Code> and run <Code>python graph_example.py</Code>. Vertices in code are 0…n−1; the labs label those positions A…F. Native adjacency preserves parallel edge occurrence IDs and self-loops. The bounded editor deliberately excludes those cases and states its limits. Labs are computed teaching models, not a Python interpreter or runtime benchmark.</Prose>
    <Checkpoint prompt="A graph has no edges and six vertices. Should a result omit five of them?">
      <Prose>No. For routes from A, A has distance 0 and the others are unreachable. Its minimum spanning forest has six one-vertex components and total 0. Every permutation of the six vertices is a valid dependency order. The vertex set exists independently of the edge list.</Prose>
    </Checkpoint>

    <H2>2. Relax routes and finalize safe candidates</H2>
    <Prose>Imagine keeping the cheapest route you have actually found to each place on a board. Initially the source is 0 and other entries are <Code>∞</Code>, meaning “no known route.” If your best route to u costs 4 and an edge u→v costs 3, you have found a candidate route to v costing 7. Replace v's old entry only if 7 is smaller. This operation is called <strong>relaxation</strong>: <Code>distance[v] = min(distance[v], distance[u] + weight)</Code>.</Prose>
    <Prose>A finite tentative distance is an <strong>upper bound</strong> on the optimum: it is the cost of a real discovered walk, so the optimum can be no larger. Relaxing improves a candidate; it does not by itself prove that no unseen route is better. Store the predecessor vertex and edge occurrence when a strict improvement occurs. A scalar distance alone cannot tell the learner or caller how to travel.</Prose>
    <DijkstraFrontierLab />
    <H3>What the priority queue promises</H3>
    <Prose><strong>Dijkstra's algorithm</strong> requires nonnegative edge weights. Repeatedly remove the queued candidate with smallest cost. If its cost differs from the vertex's current best distance, discard that obsolete entry. Otherwise this extraction <strong>finalizes</strong> the vertex: the distance is optimal. Relax its outgoing edges and push a new entry for each strict improvement.</Prose>
    <Prose>In the default graph, B first receives 10 and later 2. Both physical entries may coexist. The array contains one current best, 2; the heap still contains an old work request for 10. When B10 is eventually removed, the stale check prevents reprocessing its outgoing edges. This is the same lazy invalidation idea introduced with priority queues, now attached to a correctness invariant. A ticket breaks equal-cost ties without requiring vertex payloads to be comparable; the tie rule makes traces deterministic, not mathematically optimal.</Prose>
    <H3>Why a non-stale minimum is safe</H3>
    <Prose>Suppose the algorithm is about to finalize u but some cheaper source→u path exists. On that path, let y be its first not-yet-finalized vertex and x the preceding finalized vertex. If y is the source, its initial 0 entry handles the boundary. Otherwise, when x was finalized, relaxing x→y established a candidate no greater than that path's cost up to y. Nonnegative remaining edges make that prefix no more expensive than the complete cheaper path to u. Therefore y has a queued current candidate cheaper than u. That contradicts selecting u as the minimum current candidate.</Prose>
    <Prose>The proof needs both ingredients: relaxation preserves a real candidate route, and the remaining path cannot decrease its cost. Zero weights are allowed. A zero-cost cycle may create multiple optimal walks, but strict improvements avoid endlessly re-enqueuing equal distances. Parent pointers point from a newly improved destination to an already finalized predecessor; reconstructing a chosen finite path terminates. With equal optima, this implementation preserves the first chosen witness.</Prose>
    <RunnableExample example={weightedGraphExamples.dijkstra}><Prose>To E, the recovered vertices are 0→2→1→3→4 with original edge IDs [1,2,3,5], cost 1+1+2+3=7. F returns None because it is unreachable. The final stale list includes D9, B10 and E21, even though those vertices already have correct final distances.</Prose></RunnableExample>
    <Prose>For a single target, early return is safe when that target is removed as a <em>non-stale minimum</em>. First discovery is too early. Returning then also means other vertices may still have tentative or unknown entries; do not label the entire distance array final. The displayed implementation runs to an empty queue to return complete source results and expose stale entries.</Prose>
    <Prose>With adjacency lists, initializing vertices costs O(V), outgoing edges are scanned once from each reached vertex, and there are at most E successful relaxations. Each creates a heap entry; pushes and pops cost O(log(E+1)) under this lazy implementation. Total work is O(V+E log(E+1)), with O(V+E) stored graph, distances, parents and queue. An indexed decrease-key heap keeps at most V live entries and has a different bound; do not borrow its memory bound for this duplicate-entry implementation. Costs count bounded-cost arithmetic/comparisons; very large Python integers also have bit costs.</Prose>
    <Checkpoint prompt="Can you mark a vertex visited when it is first pushed, as in equal-cost BFS?">
      <Prose>Not for this weighted rule. Pushing B10 would block the later A→C→B route of cost 2. Discovery, current candidate and finalization are three different events. A priority data structure becomes a correct algorithm only when the event at which you trust its answer has been justified.</Prose>
    </Checkpoint>

    <H2>3. Allow negative edges with an edge budget</H2>
    <Prose>Now consider A→B2, A→C5, C→B−4 and B→D2. The best B route costs 1 through C. A finalized-once Dijkstra variant could trust B2 before seeing C5. The proof fails at its nonnegative-suffix step: a later negative edge can make an expensive prefix lead to a cheaper total. This is why the program rejects negative weights. A different algorithm that repeatedly reopens vertices has different termination and cost obligations; removing the validation does not establish them.</Prose>
    <Prose>A more systematic question is: what is the cheapest route using <em>at most k edges</em>? Let row k store that answer for every destination. Row 0 is 0 at the source and ∞ elsewhere. To compute row k, either keep the old route with at most k−1 edges, or extend such a route to u by one edge u→v. Every candidate reads row k−1, while every minimum is written to a new row. This is a dynamic-programming state with the edge budget as its extra coordinate.</Prose>
    <BellmanFordPassLab />
    <Prose>When a finite optimum exists, a simple optimal path uses at most V−1 edges. Thus V−1 rounds suffice for every finite optimum not influenced by a negative cycle. <strong>Bellman–Ford</strong> uses repeated relaxation to obtain those answers. The version here deliberately copies each old row so that “at most k” has an exact meaning. The common in-place version can propagate through several edges in one pass; its guarantee is that k passes include all improvements obtainable with at most k edges, not that it represents only those routes.</Prose>
    <H3>What a negative cycle actually invalidates</H3>
    <Prose>If an additional pass improves some vertex, that improvement requires a walk longer than a finite optimal simple path could need. Such a walk contains a reachable negative cycle. Repeating the cycle can reduce costs arbitrarily. Mark the improved destinations as detection seeds, then traverse forward from them: every reachable destination is also unbounded below. Conversely, finite optima outside the downstream region have already been found. This classifies source-target answers; it does not assert that every seed or affected descendant is itself on the cycle.</Prose>
    <LessonTable caption="Three answers with different meanings" headers={['Result', 'Meaning', 'Can you return one optimal route?']} rows={[
      ['+∞', 'The source cannot reach this target', 'No route exists'],
      ['A finite integer', 'At least one minimum-cost route exists', 'Yes; retain or reconstruct its edges'],
      ['−∞', 'A reachable negative cycle can also reach this target', 'No minimum is attained; walks can be made cheaper repeatedly'],
    ]} />
    <Prose>In the lab, B→C→B costs −3. Source A can reach it, and the cycle can reach D, so B,C,D receive −∞. The E↔F cycle is unreachable from A and changes none of those source-A answers. A itself stays 0 because the reachable negative cycle cannot return to A. Switching the source changes which cycles matter. An undirected negative edge also creates a negative walk by crossing it and returning; this matters for unrestricted shortest walks, even though a spanning forest never repeats that edge.</Prose>
    <RunnableExample example={weightedGraphExamples.bellman}><Prose>After computing costs, the code reconstructs finite witnesses with a small BFS over <strong>tight edges</strong>: edges satisfying distance[u]+weight=distance[v], with both endpoints finite. Every finite optimum has such a path. A visited set prevents zero-cycle parent loops. Call route only for a finite destination; −∞ is not a route cost. Edge IDs keep parallel occurrences unambiguous.</Prose></RunnableExample>
    <Prose>The displayed layered implementation performs V−1 copies of a V-cell row and scans E edges each round, followed by a detection scan and reachability/reconstruction. Its general bound is O(V(V+E)) time and O(V+E) working storage, including adjacency. The familiar O(VE) description usually refers to edge-scan work or a conventional in-place variant; copying all vertices is material when many are isolated. The browser retains every small row for teaching and therefore uses extra trace storage absent from the native two-row recurrence.</Prose>
    <H3>An edge budget is also a useful new problem</H3>
    <Prose>Suppose a trip may use at most two edges. In A→B4, A→C1, C→B1, B→D1, the cheapest unrestricted B route uses both edges already. The more expensive direct route to B leaves one edge for D. Remembering only “B costs 2” and discarding how that state was reached loses a feasible route to D costing 5. Row copying preserves the budget: D costs 5 with two edges and 3 with three edges. A stop limit k normally means k intermediate stops and therefore at most k+1 flight edges; establish that convention from the actual task.</Prose>
    <RunnableExample example={weightedGraphExamples.budget}><Prose>For limit L this code takes O(V+E+L(V+E)) time, including validation and row copying, and O(V+E) storage including the temporary validation graph. The result is a cost vector only. Returning a budgeted witness needs parent information indexed by the budget generation, not a single predecessor overwritten across incompatible rows.</Prose></RunnableExample>

    <H2>4. Specialize the route contract</H2>
    <Prose>The following branches are useful after the core source-route mechanism is clear. They change the state order or the set of questions answered, rather than replacing the meaning of relaxation. You can continue to network connection first and return to these complete examples later.</Prose>
    <details><summary>Optional branch · zero/one costs need only two priority levels</summary>
      <Prose>If every edge costs 0 or 1, a deque can maintain candidate entries in nondecreasing distance order. Removing cost d produces only candidates d or d+1. Put new d entries at the front and new d+1 entries at the back. All existing queue entries lie at those two levels relative to the front; stale entries are discarded on removal. This gives the same safe nonnegative minimum extraction without a general comparison heap.</Prose>
      <RunnableExample example={weightedGraphExamples.zeroOne} />
      <Prose>Each vertex is processed once at its final non-stale extraction, each outgoing edge is scanned once, and each strict improvement creates at most one extra entry. Deque endpoint operations give O(V+E) work and O(V+E) graph/queue storage. Replacing cost 1 by arbitrary cost 7 breaks the two-level order: append-to-back no longer implements minimum priority.</Prose>
      <Prose>An interesting application is a grid of preferred direction signs. Moving in a cell's preferred direction costs 0; choosing another legal direction costs one sign change. The objective is number of changed signs, not step count. An optimal simple route suffices with nonnegative costs, so changing each visited cell once matches the route cost. Boundary signs may point outside the grid; those moves are simply absent. This supplies the mapping used in the optional grid practice.</Prose>
    </details>
    <details><summary>Optional branch · all pairs with Floyd–Warshall</summary>
      <Prose>For repeated questions between arbitrary endpoints, store a V×V matrix. Initialize diagonal 0 for empty walks, direct-edge cells to their minimum occurrence weight, and absent connections to ∞. At stage k, allow vertex k as an intermediate. A best route either avoids k or joins u→k and k→v. Update <Code>D[u][v] = min(D[u][v], D[u][k] + D[k][v])</Code>, with k as the outermost loop.</Prose>
      <LessonTable caption="A route can become cheaper when an intermediate is allowed" headers={['Permitted connection', 'A→C best', 'A→D best']} rows={[
        ['Direct edges: A→B3, B→C−2, A→C8, C→D4', '8', '∞'],
        ['Allow B internally', '3−2 = 1', '∞'],
        ['Then allow C internally', '1', '1+4 = 5'],
      ]} />
      <Prose>When there is no negative cycle, an optimal simple route uses the newly allowed intermediate at most once. Its two pieces use only previously allowed intermediates. In-place updates are safe because D[k][k]=0 cannot improve its own pivot row or column at that stage. With negative cycles, intermediate numeric cells need not mean finite shortest costs; the final classification handles that separately. A negative diagonal identifies a vertex influenced by a negative closed walk. Mark pair (u,v) unbounded only when u can reach such a vertex and it can reach v.</Prose>
      <RunnableExample example={weightedGraphExamples.floyd}><Prose>The final classification writes to a separate matrix while reading the completed reachability/cost matrix. It never adds +∞ to −∞, which could produce NaN. The fourth displayed row shows that a directed graph does not automatically provide reverse routes.</Prose></RunnableExample>
      <Prose>The triple loop and pair classification take O(V³) time; two V×V matrices take O(V²) space. The native validation also reads E edge occurrences, so include O(E) input/temporary adjacency cost if a multigraph contains many parallel entries. This version returns costs. To return routes as well, store next-hop or split witnesses consistently and refuse reconstruction for unbounded pairs. A matrix of finite numbers alone is not a route witness.</Prose>
    </details>
    <details><summary>Optional branch · Johnson reweighting for sparse signed graphs</summary>
      <Prose>Adding the same positive constant to every edge can make weights nonnegative, but also penalizes a route once per hop, so it can change the winner. Instead attach a number h[v], called a <strong>potential</strong>, to each vertex. Replace edge weight w(u,v) by w(u,v)+h[u]−h[v]. Along A→B→C, the two terms involving h[B] cancel. Every route with fixed endpoints s,t changes by the same h[s]−h[t], regardless of its edge count. Their relative costs are preserved.</Prose>
      <Prose>To obtain nonnegative adjusted edges, add a new source with zero-cost edges to all original vertices and compute its Bellman–Ford distances h. If any negative cycle exists anywhere, it is now reachable and this Johnson implementation rejects the graph. Otherwise all h values are finite, and the shortest-distance inequality h[v]≤h[u]+w(u,v) implies adjusted weight≥0. Run Dijkstra from each original source and convert back with distance=adjustedDistance−h[source]+h[target].</Prose>
      <RunnableExample example={weightedGraphExamples.johnson} />
      <Prose>For the four-edge signed example, the original-vertex potentials are [0,−4,0,−2]. Adjusted weights in input order are [6,5,0,0]. A→C→B has adjusted cost 5; converting back gives 5−0+(−4)=1. No physical edge changed: this is a computational transformation. The program returns O(V²) distance output and uses O(V²+V+E) storage. With these layered Bellman–Ford and lazy Dijkstra variants, work is O(V(V+E)+V(V+E log(E+1))). Faster textbook bounds may use different heap operations. On sparse graphs this avoids Floyd–Warshall's cubic scan, but negative-cycle handling is deliberately a global rejection rather than Floyd's per-pair classification.</Prose>
    </details>
    <LessonTable caption="Select by assumptions, output and actual implementation" headers={['Contract', 'Method', 'Core reason']} rows={[
      ['Equal positive edge cost', 'BFS', 'Fewest edges is also least cost'],
      ['Weights only 0/1', 'Deque shortest paths', 'Two ordered priority levels suffice'],
      ['Nonnegative weights', 'Dijkstra', 'Minimum current candidate can be finalized'],
      ['Signed weights', 'Bellman–Ford', 'Edge-budget relaxation and negative-cycle influence'],
      ['A directed acyclic graph', 'Topological relaxation', 'All predecessors are processed before use; see section 7'],
      ['All pairs, moderate vertex count', 'Floyd–Warshall', 'Reuse permitted-intermediate subproblems'],
      ['Sparse all pairs, signed but no negative cycle', 'Johnson', 'Reweight safely, then reuse Dijkstra'],
    ]} />

    <H2>5. Connect a network at minimum total cost</H2>
    <Prose>Now each undirected edge is a possible cable installation. Pay once for every selected edge and connect all vertices with no cycles. A connected graph has a <strong>minimum spanning tree</strong> (MST); a disconnected graph has a <strong>minimum spanning forest</strong> (MSF), one tree per component. A forest covering V vertices in c components contains V−c edges. This is not a source-route objective, and no privileged source is part of the answer.</Prose>
    <Prose>Zero, tied and negative installation weights are valid. The requested object is a tree/forest. If the different problem allowed arbitrary connected subgraphs with negative edges, adding a negative cycle could further reduce its total, so “cheapest connected subgraph” and “minimum spanning tree” would no longer be interchangeable. State the feasible output before using an algorithm.</Prose>
    <H3>A cut is a boundary with a proof</H3>
    <Prose>A <strong>cut</strong> partitions the vertices into two sides. An edge crosses it when its endpoints lie on opposite sides. Suppose the edges already accepted form a forest contained in some optimal completion, and none of those accepted edges crosses the chosen cut. Select a lightest available crossing edge e. If an optimal completion already contains e, there is nothing to prove. Otherwise add e to that completion: it creates a cycle. The cycle has another crossing edge f, because it must return to e's starting side.</Prose>
    <Prose>The cut was not crossed by any previously accepted edge, so removing f does not undo an accepted choice. Also weight(e)≤weight(f). Replacing f by e leaves a spanning tree of no larger weight that retains the old forest and includes e. Since the original completion was optimal, the replacement is optimal too. Thus e is <strong>safe for some optimal completion consistent with our choices</strong>. With ties, this does not mean every MST must contain e.</Prose>
    <SpanningForestLab />
    <H3>Kruskal: grow a forest in global edge order</H3>
    <Prose>Sort edge occurrences by weight. For each, use Union-Find to ask whether its endpoints are in different accepted-edge components. If so, accept it and merge those components; otherwise reject the cycle. Why is an accepted edge lightest for a safe cut? Take the cut around one of its current components. A lighter crossing edge could not have been skipped earlier: if its endpoints were already connected then, accepted connectivity would keep them connected now. Therefore the chosen edge is a lightest crossing candidate and the exchange proof applies.</Prose>
    <RunnableExample example={weightedGraphExamples.kruskal}><Prose>The main fixture accepts edge IDs [2,1,6,8,3], total 13. The second input includes two parallel 0—1 occurrences and a negative self-loop at 2. The cheaper parallel edge is selected; the loop cannot belong to any tree and is rejected. Vertex 3 still contributes an isolated component.</Prose></RunnableExample>
    <Prose>Sorting E occurrences costs O(E log(E+1)); union-by-size with path compression contributes an amortized O(E α(V)) term, with O(V) initialization. Total is O(V+E log(E+1)+E α(V)); graph validation and sorting storage are O(V+E). Here α is the extremely slowly growing function from Union-Find's amortized analysis, not a claim that an individual find is literally one operation.</Prose>
    <H3>Prim: grow one tree across its current boundary</H3>
    <Prose>Start at any vertex in a component. Queue its incident crossing edges by their <em>own edge weight</em>. Remove the minimum and accept it if its outside endpoint is still unvisited; then visit that endpoint and add its new crossing edges. Some queued edges become internal when other connections are accepted. Leave them physically queued and discard them when popped. Each accepted edge is a lightest crossing edge of the visited/unvisited cut, so the same exchange proof works. Restart from the next unvisited vertex to include disconnected components.</Prose>
    <RunnableExample example={weightedGraphExamples.prim}><Prose>From A, Prim's default acceptance order differs from Kruskal's, but both install total 13. Dijkstra would prioritize accumulated source cost; Prim prioritizes a single installation edge. Their similar priority-queue code does not make their keys, invariants or output objectives interchangeable.</Prose></RunnableExample>
    <Prose>This lazy Prim implementation inserts each undirected edge occurrence at most once, scans adjacency in O(V+E), and uses O(E) possible heap entries. Time is O(V+E log(E+1)); graph plus working storage is O(V+E). A dense complete graph can make heap/sorted-edge storage unnecessarily large.</Prose>
    <details><summary>Practical branch · connect points without materializing all pairs</summary>
      <Prose>If the task defines cost between points (x,y) and (a,b) as |x−a|+|y−b|, every pair has an implicit Manhattan-weight edge. Keep the best crossing edge for each unvisited point. Repeatedly scan to find the smallest key and update remaining keys by computing the new point's distances on demand. There are n scans of n points, giving O(n²) arithmetic/comparison work and O(n) extra arrays, without storing the Θ(n²) edge list. This is Prim with a different representation and priority-selection implementation.</Prose>
      <RunnableExample example={weightedGraphExamples.points} />
      <Prose>The function assumes each input item is a pair of exact integer coordinates; duplicate points simply create possible zero-cost connections. Its empty input returns total 0 and no edges. This explicit geometric cost formula is different from the arbitrary SVG layout used by the labs.</Prose>
    </details>
    <H3>A useful property beyond installation cost</H3>
    <Prose>An MST also supplies a <strong>minimum bottleneck route</strong> between every pair in a component: among all connecting routes, it minimizes the largest edge encountered. Let e be a largest edge on the tree's a→b path. Removing e separates a and b. Any other route must cross that cut; a crossing edge lighter than e would replace e and make the tree cheaper, contradicting optimality. So no route can have every edge lighter than e. This is useful when the worst link, elevation threshold or maximum required capability matters. It does not say the MST minimizes the sum along each route; the opening triangle already disproved that.</Prose>

    <H2>6. Emit only ready dependencies</H2>
    <Prose>Drop installation weights and interpret u→v as “u must happen before v.” The <strong>indegree</strong> of v counts incoming edge occurrences. As completed vertices are removed, the remaining indegree counts prerequisites still blocking v. A vertex with zero remaining indegree is <strong>ready</strong>. Kahn's algorithm repeatedly emits a ready vertex and decrements its outgoing neighbors' counts.</Prose>
    <TopologicalDependencyLab />
    <Prose>Every emitted vertex has no un-emitted predecessor, so appending it preserves a valid order. Any finite nonempty DAG has a zero-indegree vertex: if every vertex had a predecessor, repeatedly following predecessors would revisit one and create a cycle. Therefore a DAG never gets stuck early. If the ready queue empties while vertices remain, that residual graph contains a cycle and no complete order exists. It can also contain acyclic descendants blocked by that cycle.</Prose>
    <Prose>The example initially permits A or B. Choosing either can lead to a valid order. A FIFO queue makes one deterministic order given input adjacency order; it does not promise the lexicographically smallest order. Use a min-heap of ready labels if that is the required contract, paying the additional priority costs. A topological order is unique exactly when there is one ready choice at every step of Kahn's process: two simultaneous ready vertices can be swapped at that point to obtain distinct valid completions in a DAG.</Prose>
    <H3>DFS supplies an actual cycle witness</H3>
    <Prose>DFS marks vertices unseen, active in the current call chain, or finished. An edge to an active vertex closes a directed cycle with the active suffix. An edge to a finished vertex does not: two dependency paths may merge into a previously explored descendant. In a DAG, when u finishes, every outgoing v has already finished; otherwise v would be active and form a cycle. Reversing finish order therefore puts u before every outgoing neighbor.</Prose>
    <RunnableExample example={weightedGraphExamples.topological}><Prose>The explicit DFS frames retain the next neighbor index, matching suspended recursive calls without depending on Python's recursion limit. Its active positions reconstruct C→E→C when E→C is added. The same failed Kahn run also leaves F waiting, but F is not in that witness. Both functions include isolated vertices and support repeated dependency occurrences by counting and removing each occurrence consistently.</Prose></RunnableExample>
    <Prose>Kahn's FIFO version and the explicit-frame DFS both take O(V+E) time and O(V+E) storage including adjacency. Each vertex enters/exits once and each adjacency entry is inspected once. DFS returns either a complete order or a concrete vertex cycle; Kahn raises on impossibility. Choose the API deliberately rather than confusing a partial emitted prefix with a full answer.</Prose>

    <H2>7. Use dependency order to solve paths and schedules</H2>
    <Prose>A <strong>directed acyclic graph</strong> has no way to return to an earlier prerequisite. That means we can process each vertex after every possible predecessor and relax outgoing edges once. Signed weights are safe: a later vertex cannot send a surprise route back into an earlier one. This is a DP over topological order, not Dijkstra's priority proof.</Prose>
    <RunnableExample example={weightedGraphExamples.dag}><Prose>The signed example again gives B distance 1 through C, even though C's source distance 5 is larger than B's earlier candidate 2. Topological order processes C before B because C→B exists. Both graph construction and order validation cost O(V+E); relaxation adds one edge scan. Unreachable vertices are skipped and keep ∞.</Prose></RunnableExample>
    <H3>Critical paths answer “when can everything finish?”</H3>
    <Prose>For build jobs, each vertex has a nonnegative duration. Assume all needed capacity is available, there are no release times or setup delays, and dependencies are the only reason to wait. A job can start only after <em>all</em> predecessors finish. Hence start[v] is the maximum predecessor finish, or 0 when none exist; finish[v]=start[v]+duration[v]. This uses max rather than the min of a cheapest-route recurrence.</Prose>
    <CriticalPathFigure />
    <Prose>Any legal schedule must take at least the total duration of every directed dependency chain, because those jobs cannot overlap. The earliest-start construction attains the longest such chain length: each finish follows the max recurrence, and the predecessor causing that max reconstructs a chain whose duration equals the makespan. This matching feasible schedule and lower bound prove optimality under the stated unlimited-capacity assumptions.</Prose>
    <RunnableExample example={weightedGraphExamples.schedule}><Prose>Durations [3,2,4,6,2,1] produce starts [0,0,3,2,8,10], finishes [3,2,7,8,10,11], and critical chain B→D→E→F. Tied critical chains may exist; the function returns one using first strict improvement. Zero-duration jobs are legal. Empty input has makespan 0 and an empty chain.</Prose></RunnableExample>
    <Prose>The recurrence and witness reconstruction cost O(V+E) time and storage including the graph. A single worker would forbid A and B from overlapping, so 11 would only be a dependency lower bound, not the complete answer. With multiple limited resources, deadlines or setup rules, choose a scheduling model that actually represents those constraints. A topological ordering alone is not a resource schedule.</Prose>

    <H2>8. Practice the changed question</H2>
    <Prose>Before reopening a program, state the exact objective, graph direction, allowed weights, output contract and event that makes an answer trustworthy. Draw an adversarial example, propose a simple oracle, then justify the optimized method. The following tasks change the problem rather than asking you to reproduce the default trace.</Prose>
    <H3>Exercise: distinguish labels from a proof of a route</H3>
    <Prose>For A→B6, A→C2, C→B1, B→D2, C→D7, predict every final source-A distance and one route to D. Explain why returning when D is first discovered is wrong. Then say which invariant and return contract change if only one target is requested.</Prose>
    <details><summary>Hint · distinguish discovery and extraction</summary><Prose>C discovers D at 9 before B's better route through cost 3 is processed. Track both predecessor identity and priority cost.</Prose></details>
    <details><summary>Show the answer and acceptance criteria</summary><Prose>Final distances are A0,C2,B3,D5. The D witness is A→C→B→D. D9 may remain as a stale physical entry after D5 is queued. Single-target return is safe at D5's non-stale minimum extraction; it says nothing final about unprocessed vertices. Verify the witness follows actual directed edges and its weights sum to 5, then compare all small source-target simple paths. A scalar 5 without any valid path is incomplete when a witness is required.</Prose></details>
    <H3>Exercise: classify a negative cycle by destination</H3>
    <Prose>Add B→C−4 to A→B1, C→B1, C→D2, A→E8; F is isolated. Classify A…F from A. Would adding D→A0 change A's answer? Is D itself necessarily a cycle vertex before that addition?</Prose>
    <details><summary>Hint · separate reaching the cycle from leaving it</summary><Prose>Find a repeated loop of negative total, then mark only destinations reachable after leaving that loop. The existence of an A→cycle route does not create a cycle→A route.</Prose></details>
    <details><summary>Show the reasoning</summary><Prose>Before adding D→A, answers are [0,−∞,−∞,−∞,8,+∞]. B→C→B has cost −3; D is downstream but not on any directed cycle in this graph. After adding D→A, the negative loop can reach A and then E, so A through E are all unbounded below while F stays unreachable. The empty A→A walk of cost 0 still exists, but it is no longer a minimum.</Prose></details>
    <H3>Exercise: ties and a changed edge transformation</H3>
    <Prose>A triangle has all weights 2. Is every edge in every MST? Now add the same constant 10 to every edge of an arbitrary fixed graph: does this preserve its minimum spanning forest choices, and does it preserve all shortest routes?</Prose>
    <details><summary>Hint · count selected edges versus route edges</summary><Prose>Every spanning forest of the original components has V−c edges. Competing routes can have different numbers of edges.</Prose></details>
    <details><summary>Show the proof and counterexample</summary><Prose>Each two-edge selection in the triangle is optimal, so no edge is mandatory. The cut theorem promises a compatible optimum, not inclusion in all optima. Adding 10 changes every spanning forest's total by the same 10(V−c), preserving their order. It changes a route by 10 times its hop count. For A→B3 versus A→C1→B1, the original cheaper route costs 2; after the transformation the direct edge costs 13 and the two-edge route costs 22, reversing the choice. Johnson's vertex-potential cancellation avoids this hop bias.</Prose></details>
    <H3>Exercise: a cycle edge and a bottleneck certificate</H3>
    <Prose>Prove that an edge strictly heavier than every other edge on some cycle cannot occur in any MST. Explain why replacing “strictly heavier” with “tied for heaviest” fails. Then design a direct oracle for the minimum-bottleneck claim on four vertices.</Prose>
    <details><summary>Hint · remove the proposed tree edge first</summary><Prose>If a tree contains the heavy edge, removing it defines a cut. The rest of the original cycle contains a route across that cut.</Prose></details>
    <details><summary>Show a proof and verification plan</summary><Prose>Some other edge on the cycle crosses the cut. It is strictly lighter, so swapping it in yields a cheaper spanning tree, a contradiction. In an all-equal triangle, an edge tied for heaviest can appear in an MST. For bottlenecks, enumerate all simple paths between each pair, take the maximum edge weight on each path, then minimize those maxima. Compare that result with the maximum on the MST's unique path. Check ties and negative weights; for identical endpoints the empty-path convention must be defined separately rather than inventing an edge.</Prose></details>
    <H3>Exercise: ordering is not completion time</H3>
    <Prose>Jobs P and Q take 5 and 2 time units; R takes 4 and depends on both. List valid topological orders, derive the unlimited-capacity makespan and one critical chain, then explain what changes with one worker. Add R→P and identify both a cycle and any blocked vertices.</Prose>
    <details><summary>Hint · readiness and running are different states</summary><Prose>Topological output is a sequence of labels. Unlimited-capacity jobs whose prerequisites are finished can run at the same physical time.</Prose></details>
    <details><summary>Show the complete explanation</summary><Prose>P,Q,R and Q,P,R are valid orders. P and Q may start together; R starts at max(5,2)=5 and finishes at 9. P→R certifies the 9-unit lower bound. One worker takes at least 5+2+4=11 with these nonpreemptive jobs and no other delays, attainable by either legal order. Adding R→P creates P→R→P; Q can still be emitted before Kahn gets stuck with P,R. There is no feasible dependency schedule regardless of worker count.</Prose></details>
    <DsaPractice practice={graphPractice} />
    <Prose>The next topic in this module is <a href="/learn/topic/string-matching-prefix-functions-rolling-hashes">String Matching, Prefix Functions &amp; Rolling Hashes</a>. For a later spatial application, <a href="/learn/topic/motion-planning-path-planning-rrt-a-trajectory-optimization">Motion Planning &amp; Path Planning</a> owns A* heuristics, collision geometry and trajectory constraints. This lesson establishes exact weighted graph routes; a drawing or an optimistic heuristic alone does not establish a physically feasible motion plan.</Prose>
    <Sources alternatives={<>
      <p><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-16-dijkstra/" target="_blank" rel="noopener noreferrer">MIT OCW · Lecture 16: Dijkstra</a> provides a lecture-video route through DAG paths, relaxation and nonnegative priorities. The original page and all seven pages of its linked lecture notes were inspected; full video playback was not reviewed. Its decrease-key discussion uses a different priority representation from the lazy Python heap here, so compare invariants before comparing bounds.</p>
      <p><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/resources/mit6_006s20_lec14/" target="_blank" rel="noopener noreferrer">MIT OCW · Johnson's Algorithm notes</a> give a second derivation of potential cancellation and the added source. All five pages were inspected. The notes' heap cost assumptions differ from this lesson's implementation; the transformation itself is independent of that choice.</p>
      <p><a href="https://algs4.cs.princeton.edu/43mst/" target="_blank" rel="noopener noreferrer">Princeton · Minimum Spanning Trees</a> supplies illustrated Prim/Kruskal growth and Java implementations. Its initial distinct-weight simplification is useful for a first picture; revisit this lesson's tied-weight exchange quantifier before treating an edge as mandatory.</p>
    </>}>
      <li><a href="https://algs4.cs.princeton.edu/44sp/" target="_blank" rel="noopener noreferrer">Princeton · Shortest Paths</a> — route APIs, relaxation, DAG applications and precise source-target negative-cycle conditions. Distinguish its indexed and lazy priority variants.</li>
      <li><a href="https://algs4.cs.princeton.edu/42digraph/" target="_blank" rel="noopener noreferrer">Princeton · Directed Graphs</a> — active-cycle reasoning, reverse finish order and indegree processing, with graph APIs in Java.</li>
      <li><a href="https://docs.python.org/3/library/heapq.html#priority-queue-implementation-notes" target="_blank" rel="noopener noreferrer">Python · Priority queue implementation notes</a> — ticket ties and stale-entry management. The current docs include newer APIs; this lesson uses longstanding min-heap operations available in Python 3.10.</li>
      <li><a href="https://docs.python.org/3/library/collections.html#collections.deque" target="_blank" rel="noopener noreferrer">Python · deque</a> — efficient endpoint operations used by 0/1 search and Kahn's ready queue.</li>
    </Sources>
  </div>,
};
