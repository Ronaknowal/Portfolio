import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { CollisionLayoutFigure, DenseSetLab, HashCostAxesFigure, HashFamilyLab, ProbeChainLab, RehashLocationsFigure, ResizeAccountingLab } from '../../components/lesson-labs/HashingAmortizedLabs.jsx';
import { hashingAmortizedExamples } from '../hashing-amortized-examples.js';
import hashingPractice from '../practice/hashing-collision-resolution-amortized-analysis.js';
function Exercise({
  title,
  children,
  hint,
  solution
}) {
  return <section className="hash-exercise"><H3>{title}</H3>{children}
    <details><summary>Optional hint</summary><Prose>{hint}</Prose></details>
    <details><summary>Solution and reasoning</summary>{solution}</details>
  </section>;
}
export default {
  title: 'Hashing, Collision Resolution & Amortized Analysis',
  readTime: '~60 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot hashing-lesson">
    <LessonIntro prerequisites="Use Python lists, dictionaries, functions and loops. Arrays/Hash Maps introduced the map contract and collisions; Complexity introduced asymptotic costs. Correctness supplied invariants and termination. We introduce the probability and accounting needed here locally." sections={[['1-start-with-identity-and-a-correct-baseline', 'Identity and the baseline'], ['2-give-collisions-a-place-to-go', 'Chains and probe paths'], ['3-delete-without-erasing-search-evidence', 'Deletion and replacement'], ['4-rebuild-the-mapping-not-the-old-positions', 'Rehashing and occupancy'], ['5-say-what-the-expectation-is-over', 'Expected collision work'], ['6-pay-for-expensive-updates-across-a-sequence', 'Amortized growth'], ['7-keep-growth-and-shrinkage-apart', 'Mixed updates'], ['8-combine-structures-with-a-shared-invariant', 'Composite applications'], ['9-choose-the-contract-and-the-cost-model', 'Practical decisions'], ['10-build-and-explain-it-independently', 'Independent practice'], ['guided-dsa-practice', 'Guided LeetCode practice']]}>
      A map promises that a key retrieves its value even when many keys land in the same place. A fast map adds a second promise about work. Learn how to keep the first promise through deletion and resizing, and how to state the second precisely enough to trust it.
    </LessonIntro>

    <H2>1. Start with identity and a correct baseline</H2>
    <Prose>A monitoring service receives an update for sensor 17. It must replace sensor 17's old reading, preserve every other sensor's reading and later distinguish “sensor absent” from “sensor present with no reading yet.” A hash value helps locate candidates. It does not replace the identity of the sensor.</Prose>
    <Prose>A <strong>map</strong> stores at most one association for each equal key. Inserting a new key increases its size; assigning an existing key replaces a value. Removing an absent key follows an explicit contract, such as returning false. Here <Code>find</Code> returns a pair: a presence flag and a value. Thus <Code>(True, None)</Code> and <Code>(False, None)</Code> mean different things. A private sentinel or an exception is another valid API choice.</Prose>
    <RunnableExample example={hashingAmortizedExamples.listMap}><Prose>The list implementation is deliberately simple. It compares full keys and preserves the mapping. Finding or rejecting a key can examine every entry, so lookup and replacement are O(n) in the number of entries. A correct baseline is useful as an oracle even after you improve the representation.</Prose></RunnableExample>
    <Prose>If every possible key is an integer in 0…U−1, a <strong>direct-address table</strong> can reserve U positions and use the key itself as an index. Under constant-time indexing, lookup takes O(1), but storage is Θ(U), even when only n keys occur. Sparse event identifiers make that tradeoff expensive. Hashing maps a large key universe into a smaller range of m homes, numbered 0…m−1.</Prose>
    <Prose>Throughout the small implementations, keys are integers and the teaching home is <Code>key % capacity</Code>. Two different keys with the same home <strong>collide</strong>. Keys 1, 9 and 17 all have home 1 when m=8. This is intentional: a collision-heavy input makes the hidden mechanism visible. Deterministic remainder hashing alone supplies no randomized constant-time guarantee.</Prose>
    <Callout title="Declare the unit of work">Our first cost model treats an array access, a small key hash, an equality comparison and moving one stored reference as bounded work. Hashing a fresh L-character string can itself require Θ(L) character work; comparing long equal keys can also be nonconstant. We return to these costs before applying the bounds to real programs.</Callout>

    <H2>2. Give collisions a place to go</H2>
    <Prose><strong>Separate chaining</strong> gives each home a collection of candidates. A lookup calculates one home, then compares full keys inside that bucket. The collection could be a linked list or a small array; our native example uses a Python list. It never treats “same bucket” as “same key.”</Prose>
    <Prose><strong>Open addressing</strong> stores entries in the slot array itself. If the home is occupied by an unequal key, a <strong>probe sequence</strong> specifies other slots to inspect. In <strong>linear probing</strong>, the sequence is h, h+1, h+2 and so on, wrapping modulo m. There are only m distinct slots, so every scan must stop after at most m probes even if none is empty.</Prose>
    <CollisionLayoutFigure />
    <Prose>The contiguous run of occupied slots is a <strong>cluster</strong>. Keys with different homes can join the same cluster, so a short distance from one home does not mean every lookup will be short. A new entry that lands in or before a cluster may extend it. This correlation is why reasoning “each next slot is independently empty with a fixed probability” is unsafe for linear probing.</Prose>
    <LessonTable caption="Same abstract map, different physical work" headers={['Representation', 'Where equal keys are checked', 'Worst lookup under this implementation']} rows={[['List of entries', 'All entries until equal key or end', 'Θ(n)'], ['Separate chaining', 'Only the home bucket', 'Θ(n) if all keys collide'], ['Linear probing', 'Successive positions until equal key, EMPTY or a full cycle', 'Θ(m); with m proportional to n, Θ(n)'], ['Direct addressing', 'One reserved position for each possible key', 'O(1) indexing, with Θ(U) storage']]} />
    <Prose>Linear probing with step 1 reaches all m positions. If you instead step by d, it reaches only m/gcd(d,m) positions. For m=8 and d=2, starting at 1 visits 1,3,5,7 and repeats, leaving four usable positions unreachable. Step 3 visits all eight because gcd(3,8)=1. The proof is modular: a return to the start occurs when m divides the number of steps times d. Dividing both by their greatest common divisor gives the stated cycle length.</Prose>
    <Checkpoint prompt="Could a table be unable to insert even though unused slots exist? Give two different reasons.">
      <Prose>An invalid probe step can cycle through only a subset of slots. Separately, an implementation may reject insertion deliberately at its chosen load threshold before physical exhaustion. The first is a coverage bug; the second can be a performance policy if its behavior is documented and it grows or reports failure safely.</Prose>
    </Checkpoint>

    <H2>3. Delete without erasing search evidence</H2>
    <Prose>Insert 1, 9 and 17 into an empty eight-slot linear-probe table. They occupy slots 1, 2 and 3. Now remove key 1. If slot 1 becomes an ordinary EMPTY slot, a lookup for 17 stops there and falsely reports absence. The value is still physically present; the evidence needed to reach it has been erased.</Prose>
    <Prose>Use three distinct states. <strong>OCCUPIED</strong> stores a key/value entry. <strong>EMPTY</strong> means the slot has not been occupied since this table was built. <strong>DELETED</strong>, also called a <strong>tombstone</strong>, means an entry was removed but searches must keep going. These meanings belong to this deletion policy; other correct schemes repair or move the cluster instead.</Prose>
    <H3>The invariant that licenses stopping</H3>
    <Prose>For every live key, its probe path from its home to its actual position contains no earlier EMPTY slot. Each equal key appears at most once. A search can therefore stop at EMPTY: if the requested key were stored later, the first invariant would be violated. An unequal occupied slot or a tombstone supplies no such absence certificate. After m different positions have been checked, the finite table has been exhausted.</Prose>
    <Prose>Insertion preserves the invariant by using the first available destination on a valid search path. Deletion to a tombstone preserves it because it introduces no EMPTY hole. Replacement changes a value without changing key identity or placement. These are local preservation arguments from the previous lesson, now tied to a useful data structure.</Prose>
    <H3>A tombstone is reusable, but not an immediate insertion command</H3>
    <Prose>After deleting 1, inserting <Code>17 → 171</Code> must update the existing key at slot 3. If insertion immediately writes into the tombstone at slot 1, the table contains two entries for 17. Remember the first tombstone, continue searching for an equal key, and only use the remembered slot after reaching EMPTY or completing the whole cycle. If every slot is live and unequal, insertion must fail or rebuild; it must not loop forever.</Prose>
    <ProbeChainLab />
    <Prose>Start with the default script. Before advancing past <Code>del 1</Code>, predict whether <Code>get 17</Code> can still reach slot 3. Then enable the faulty erase rule and compare the first broken invariant with the later wrong answer. The green checks in the correct trace are concrete diagnostics on these bounded states; the preservation argument is what covers arbitrary permitted table states.</Prose>
    <RunnableExample example={hashingAmortizedExamples.deletionFailure}><Prose>This small failure fixture isolates deletion and duplicate insertion. Its step-2 and step-3 paths also distinguish probe coverage from collision handling. Reading the wrong output is useful: you can identify precisely which assertion the shortcut destroys.</Prose></RunnableExample>
    <RunnableExample example={hashingAmortizedExamples.probeMap}><Prose>The complete fixed-capacity map uses distinct object sentinels and a bounded scan. It allows any value, including None; rejects a full insertion without overwriting another key; and can rebuild explicitly. Integers are its key contract. The teaching map does not emulate Python dict's internal layout, insertion order, automatic growth or concurrency behavior.</Prose></RunnableExample>

    <H2>4. Rebuild the mapping, not the old positions</H2>
    <Prose>Capacity changes the home calculation. Key 9 has home 1 modulo 8 and home 9 modulo 16. Copying its old physical slot into a larger array while switching the lookup rule can make it unreachable. A <strong>rehash</strong> or <strong>rebuild</strong> creates an empty destination and places each live key using the destination's home and collision rules. The invariant is about the abstract mapping: exactly the same keys have exactly the same values afterward.</Prose>
    <RehashLocationsFigure />
    <Prose>A same-capacity rebuild can also be useful: it removes tombstones without increasing the number of slots. The fixed probe example rejects a destination smaller than its live-entry count before changing the table. It builds the replacement separately and swaps it in once complete. A production design also has to specify allocation failure, concurrent readers and iterator invalidation; those are additional contracts rather than properties supplied by this sequential example.</Prose>
    <H3>Count live entries and search obstacles separately</H3>
    <Prose>The <strong>load factor</strong> α=n/m is the number of live entries divided by capacity. Chaining permits α greater than 1. In open addressing n cannot exceed m. With tombstones, also track used occupancy β=(n+t)/m, where t is the number of deleted markers. A table with one live entry and seven tombstones has low live load but may require eight probes to reject a missing key. A policy based only on α misses this state.</Prose>
    <Prose>Good policies rebuild or grow before probe paths become too expensive, and occasionally clean tombstones after enough updates have accumulated to pay for that work. There is no single threshold forced by the map contract. The exact time guarantee depends on the hash/probe scheme, the gap below full used occupancy and the accounting for cleaning. Calling a full rebuild after every deletion would cost Θ(m) per deletion in the simple scan-and-allocate model, even if the resulting lookups look short.</Prose>
    <RunnableExample example={hashingAmortizedExamples.chainedMap}><Prose>This second complete implementation makes resizing explicit in a simpler collision scheme. It starts with four buckets, doubles before inserting a new key into a table with n=m, and halves after removal when n≤m/4, never below four buckets. It searches for replacement before deciding to grow. Thus assigning an existing key at a threshold does not increase size or trigger a needless expansion.</Prose></RunnableExample>
    <Prose>The chained rebuild scans the old buckets and appends known-unique entries directly into new buckets. It does not search for duplicates that have already been ruled out by the representation invariant. Consequently its migration costs O(m+n+new capacity) including array initialization, even when all keys collide, under bounded hash/reference costs. The ordinary key lookup can still be Θ(n).</Prose>
    <Callout title="Two rebuilds need different cost statements">The fixed probe example reinserts by probing. If many keys collide, reinserting n keys can require 1+2+…+n probes, which is Θ(n²). Do not transfer the chained migration's worst-case linear claim to it. Expected-linear probe rebuilding requires suitable hashing and load assumptions; the tiny deterministic modulo example supplies correctness and observable counts, not that guarantee.</Callout>

    <H2>5. Say what the expectation is over</H2>
    <Prose>A workload that happens to spread keys evenly is evidence about that workload. A randomized performance argument is different: fix the keys and the query first, then choose a hash function randomly from a stated family. Different choices produce different collision patterns. <strong>Expected cost</strong> is the probability-weighted average of those costs. It does not promise that each choice or operation is cheap.</Prose>
    <HashCostAxesFigure />
    <H3>Count collisions one other key at a time</H3>
    <Prose>Consider a stored query q and n distinct keys. Its candidate bucket contains q plus every other key whose home equals q's home. For another key x, define an indicator I(x): it is 1 if x collides with q, and 0 otherwise. The bucket length is 1 plus the sum of these indicators. The expected value of an indicator is its event's probability, and expectation distributes over a sum even when the indicators are not independent.</Prose>
    <Prose>Suppose the family guarantees collision probability at most 1/m for every distinct fixed pair. Then the expected candidate-bucket length is at most <strong>1+(n−1)/m</strong>. For a fixed absent query, there is no stored self-entry, so the expected number of candidates is at most n/m. These bound equality comparisons for chaining, with an additional constant home calculation under our model. Keeping α=n/m bounded gives expected O(1) lookup.</Prose>
    <Prose>A family with this pairwise bound is commonly called <strong>universal</strong>. The statement is about fixed inputs independent of the random choice. An adversary that learns the chosen hash and then selects colliding keys is a different model. Also, a pairwise collision bound for chains does not by itself establish the same performance theorem for linear probing's clusters.</Prose>
    <HashFamilyLab />
    <H3>An exact example you can check without simulation luck</H3>
    <Prose>The lab uses keys from 0…16 and four buckets. Choose a uniformly from 1…16 and b uniformly from 0…16. Calculate <Code>h(k)=((a*k+b) % 17) % 4</Code>. There are 272 equally likely functions in this experiment. Holding keys [1,5,9,13] fixed, query 9 has candidate length 1 under 144 choices, length 2 under 98, length 3 under 20, and length 4 under 10. The mean is (144+196+60+40)/272 = 55/34, about 1.618. The bound is 1+3/4 = 1.75.</Prose>
    <details><summary>Why this tiny family has the required pair bound</summary>
      <Prose>For distinct x and y modulo the prime 17, multiplying by nonzero a and adding b maps the ordered pair to a uniformly chosen pair of distinct residues. To see uniqueness, subtract the two image equations: a is determined by their nonzero difference and the inverse of x−y modulo 17; then b is determined. Every permitted image pair therefore corresponds to exactly one (a,b).</Prose>
      <Prose>Reduction of residues 0…16 modulo 4 creates bucket sizes 5,4,4,4. The number of ordered distinct residue pairs in one final bucket is 5×4 + 3×4×3 = 56, out of 17×16 = 272. Thus each distinct input pair collides with probability 56/272 = 7/34, below 1/4. Summing three such probabilities gives the exact mean 1+21/34 = 55/34. This proof is for the displayed universe and family; replacing prime 17 or allowing congruent keys outside the universe requires reassessing it.</Prose>
    </details>
    <RunnableExample example={hashingAmortizedExamples.hashFamily}><Prose>Complete enumeration confirms the exact distribution with rational arithmetic. The code is a finite experiment and a useful independent check of the worked numbers. The indicator argument explains the larger idea; neither a balanced-looking selected table nor one random sample proves an expected bound.</Prose></RunnableExample>
    <Checkpoint prompt="If the selected hash puts all four keys together, has the expected bound failed? What if the keys were chosen after observing the hash?">
      <Prose>No: some functions in the fixed experiment produce a bucket of length 4. The guarantee averages over all choices. Choosing the input after seeing the function violates the fixed-input assumption, so this argument no longer supplies the claimed bound for that adaptive process.</Prose>
    </Checkpoint>

    <H2>6. Pay for expensive updates across a sequence</H2>
    <Prose>Even with no collisions, an append to a full array must sometimes move many existing entries. <strong>Amortized analysis</strong> adds the costs of an entire permitted operation sequence and bounds the total. It does not average over random inputs. One update may still have linear latency; a constant amortized bound means many updates together require only linear total work.</Prose>
    <Prose>Start empty with capacity 1. Appending to a full allocation first doubles capacity, copies all existing references, then writes the new reference. Count one unit per write and per copied existing entry. Allocation and initialization are excluded from the exact totals in this investigation; including Θ(capacity) allocation work changes constants but preserves the geometric argument.</Prose>
    <ResizeAccountingLab />
    <Prose>For 24 appends, capacities grow through 1,2,4,8,16,32. Existing entries copied total 1+2+4+8+16=31; the 24 new writes bring the total to 55. In general, the copied capacities form a geometric sum smaller than 2t after t positive appends, so total work is below 3t in this model. Each prefix is covered, even one ending immediately after a large resize.</Prose>
    <Prose>Growing by just one slot gives a different series: 1+2+…+(t−1)=t(t−1)/2 copies. At t=24 it copies 276 existing entries and performs 300 total counted units. Every individual allocation is “only one slot larger,” yet the repeated migration makes the sequence quadratic.</Prose>
    <H3>A precise way to store analysis credit</H3>
    <Prose>A <strong>potential</strong> assigns a nonnegative amount of analytical credit to a state. Charge an operation its actual cost plus the increase in potential. In a sum over operations, intermediate potentials cancel, leaving actual total plus final potential minus initial potential. A nonnegative final potential prevents the charged total from underpaying the actual total when initial potential is zero.</Prose>
    <Prose>For this append-only doubling sequence, use Φ=2n−C+1, where n is length and C is capacity. Initially n=0,C=1, so Φ=0. In every reachable append-only state, Φ≥0. A nonresizing append costs 1 and increases Φ by 2, so charge 3. When a full capacity-m array grows, the actual cost is m+1; potential changes from m+1 to 3. Actual cost plus potential change is (m+1)+(2−m)=3 again.</Prose>
    <Prose>After t appends, actual total is exactly 3t−Φ. At t=24, Φ=48−32+1=17, so 72−17=55. The lab's credit is an accounting device; it is not stored bytes, money or unused processor time. This particular potential is for the append-only states. Arbitrary deletions can make it negative, so they need a new argument.</Prose>
    <Checkpoint prompt="Does O(1) amortized append meet a requirement that every individual update finish within ten counted steps?">
      <Prose>No. Appending to capacity 16 already costs 17 counted units. Worst-case response time is a separate requirement. Incremental migration, reserved capacity, bounded blocks or a different structure may be appropriate, each with its own invariants and resource costs.</Prose>
    </Checkpoint>

    <H2>7. Keep growth and shrinkage apart</H2>
    <Prose>Suppose capacity 8 is full. An append grows it to 16 and leaves 9 items. If the next pop shrinks whenever n≤C/2, it returns to 8 items in capacity 8. Another append immediately grows again. Alternating those two ordinary updates copies eight retained entries on nearly every operation. The expensive events are no longer separated by many cheap ones.</Prose>
    <Prose>Leave a gap between the thresholds. Keep doubling before insertion into a full allocation, but halve only after a removal leaves n≤C/4. Never go below a minimum capacity. With capacity 16 and length 8, the next pop leaves 7 and does not shrink. A shrink at length 4 creates capacity 8 at half occupancy; several future additions are needed before it grows again. This separation is called <strong>hysteresis</strong>.</Prose>
    <Prose>Switch the investigation to mixed updates. Both policies first fill 8 slots and then perform eight append/pop pairs. With minimum capacity 1, counting each successful append/pop plus retained copies, the half-threshold policy costs 159 units across 24 operations; the quarter-threshold policy costs 39. The latter grows once during the alternating phase and retains capacity 16. That spare room is precisely what avoids the repeated migration.</Prose>
    <H3>Why the gap supports a sequence bound</H3>
    <Prose>Immediately after a grow or a shrink, occupancy is near one half. Reaching full occupancy again requires a number of insertions proportional to capacity; reaching one quarter requires a number of deletions proportional to capacity. Those intervening updates can pay for a migration proportional to capacity. Do not claim a constant charge merely because a threshold looks reasonable: check both directions, initial conditions and the small minimum-capacity cases.</Prose>
    <details><summary>Deeper: a potential for both append and pop</summary>
      <Prose>For power-of-two capacity C≥1 and the quarter-threshold policy, use Φ=2n−C when n≥C/2, and Φ=C/2−n when n&lt;C/2. Both pieces are nonnegative and meet at zero. Starting empty with C=1 gives initial potential 1/2, a harmless additive constant in the total bound.</Prose>
      <Prose>Without resizing, an append increases potential by at most 2, so its amortized cost is at most 3; a successful pop increases it by at most 1, so its amortized cost is at most 2. At growth from a full capacity C, copying C entries plus the new write costs C+1; the potential change is 2−C, again giving 3.</Prose>
      <Prose>For C≥4, a shrink is triggered by a pop from n=C/4+1 to n=C/4. It copies C/4 retained entries and halves capacity. For C≥8, potential falls from C/4−1 to 0, so amortized cost is (1+C/4)−(C/4−1)=2. At C=4 the same direct calculation gives 2. At C=2 the final pop can shrink to C=1, costs 1 and leaves potential 1/2, still at most 2. Empty pops are rejected/no-ops in the model and cannot create unbounded unpaid migration. Thus total work over t permitted updates is at most 3t+1/2 in this counted model.</Prose>
    </details>
    <RunnableExample example={hashingAmortizedExamples.resizeAccounting}><Prose>This complete simulation states the exact inequalities and counts copies, not timing. Its append-only and boundary outputs reproduce the worked totals. The drain sequence tests returning to the minimum allocation. These policies illustrate a design choice; they are not claims about CPython list or dict thresholds.</Prose></RunnableExample>
    <Prose>For a randomized chained map with bounded load, expected collision scans and amortized linear rebuilds combine into <strong>expected amortized O(1)</strong> updates under the stated hash/input assumptions. An adversarial deterministic modulo bucket can still make every key search linear. Amortizing resize work does not remove that cost. Likewise, tombstone cleaning needs an accounting argument of its own; the dynamic-array proof alone does not establish a complete open-addressing performance theorem.</Prose>

    <H2>8. Combine structures with a shared invariant</H2>
    <H3>Sample a uniformly chosen member while supporting deletion</H3>
    <Prose>An experiment needs to select one currently eligible participant uniformly, while participants arrive and leave. A map supports membership and deletion, but its iteration order does not give indexed random access. A dense array supplies random indexing, but deleting from its middle normally shifts the suffix. Combine them: store every value once in an array, and store its array index in a map.</Prose>
    <Prose>The shared invariant is <Code>positions[items[i]] == i</Code> for every array index i, with exactly the same values in both structures. To remove value x at index i, move the last array value into position i, update that moved value's reverse index, pop the last slot, then remove x's map entry. If x is already last, the same sequence still works. An absent x returns false without mutation.</Prose>
    <DenseSetLab />
    <Prose>After the operation finishes there is a bijection: each of n values occupies exactly one of n indices. A uniform index therefore selects each value with probability 1/n. This is a proof from the sampling contract, not a claim established by observing a balanced histogram. A map's hash randomness and the random index generator are separate mechanisms.</Prose>
    <RunnableExample example={hashingAmortizedExamples.denseRandomSet}><Prose>The implementation uses a local seeded generator to make the demonstration reproducible, and explicitly rejects sampling an empty set. Insert/delete combine expected map operations with amortized array updates. Sampling requires a nonempty array and uniform index selection. The data structure gives up stable order and does not make intermediate mutation states safe for concurrent readers.</Prose></RunnableExample>
    <H3>Discover an integer run without repeatedly walking it</H3>
    <Prose>Given [11,4,3,2,4,10], the longest consecutive run is 2,3,4, of length 3. Input order is irrelevant and the duplicate 4 does not extend the run. Put the distinct values in a set. Start scanning from a value only when its predecessor is absent. Thus 2 starts one run and 10 starts another; 3,4,11 do not restart those scans.</Prose>
    <Prose>Let s be the number of distinct values and r the number of runs. The outer loop checks s predecessors. Across all inner scans, each distinct value is visited once, followed by one failed membership check for each run: s+r checks. Total membership checks are 2s+r≤3s. The nested loops therefore perform O(s) membership operations, plus O(n) input insertions, rather than O(n²). With expected constant-time hash operations the total is expected O(n), using O(s) extra storage. If the outer loop iterated the original duplicate-filled list, duplicate run starts could repeat long scans and break this argument.</Prose>
    <RunnableExample example={hashingAmortizedExamples.consecutiveRuns}><Prose>The counter exposes the aggregate reasoning independently of elapsed time. For the six-element example, s=5 and r=2, so there are 12 membership checks in the scan. A sorted-unique implementation is a useful independent answer oracle and offers a different O(n log n) comparison-based tradeoff.</Prose></RunnableExample>
    <H3>Join groups by a complementary pair total</H3>
    <Prose>Suppose you need the number of four-way choices from groups A,B,C,D whose total is zero. Listing every quadruple takes a×b×c×d combinations. Count how many index pairs from A and B produce each sum. For a pair from C and D with sum t, add the stored count for −t. Every valid quadruple belongs to exactly one left pair and one right pair, so it is counted exactly once.</Prose>
    <Prose>The map value must be a frequency, not just a presence flag. If all four groups contain two zeroes, there are 2⁴=16 index quadruples even though there is only one distinct value quadruple. With expected map operations the method costs O(ab+cd) time, and space proportional to the number of distinct stored pair sums, at most ab. This time/space exchange also appears in matching complementary measurements or joining records by a derived condition.</Prose>
    <RunnableExample example={hashingAmortizedExamples.pairCounts}><Prose>The complete function accepts unequal group lengths and a target, handles empty groups and retains multiplicities. For imbalanced group sizes, the choice of which groups to pair affects both time and storage; derive those products before allocating a large table.</Prose></RunnableExample>

    <H2>9. Choose the contract and the cost model</H2>
    <Prose>A practical key often has several fields. If event identity is (source, sequence number), use that pair as the identity. A naive concatenation can make different field pairs identical before hashing: ('ab','c') and ('a','bc') both produce 'abc'. No better hash can recover information already discarded by the encoding. Use a structured immutable key or an unambiguous serialization.</Prose>
    <RunnableExample example={hashingAmortizedExamples.keyContracts}><Prose>The frozen dataclass makes equality and hashing depend on the same immutable fields. Recreating an equal key replaces the previous mapping. Python rejects a mutable list as a key. Freezing the outer object is insufficient if its equality fields themselves are mutable/unhashable; the contract must hold for the full identity.</Prose></RunnableExample>
    <Prose>Equal keys must have equal hashes. Unequal keys may share a hash and must still be compared correctly. Fields that affect a stored key's equality/hash must not change while it is in the table. Otherwise the current lookup path may no longer lead to its stored location. For large keys, include hash and equality work: a bound such as O(hash cost + candidate comparisons × equality cost) exposes what the word-sized model hides.</Prose>
    <Prose>Python salts the hashes of str and bytes by default: they remain stable within a process but need not match another run. Do not use built-in <Code>hash</Code> as a persistent content identifier or an interoperable file checksum. Those require a specified stable encoding and digest contract. Randomized or keyed hash-table defenses also address deliberately chosen collisions; correctness under collisions and resistance to a hostile workload are distinct requirements. The references explain the language contract and historical motivation without assuming an old implementation is today's default.</Prose>
    <LessonTable caption="Choose based on the operation you must guarantee" headers={['Need', 'Useful direction', 'Question to settle']} rows={[['Sparse exact-key lookup', 'Collision-resolving map', 'What are the equality, load and adversarial-input assumptions?'], ['Sorted traversal, predecessor or range queries', 'Balanced search tree or sorted representation', 'Do you need dynamic updates or can you sort once?'], ['Small bounded integer universe', 'Direct addressing or a bit representation', 'Can storage proportional to the universe be afforded?'], ['Tight per-operation latency', 'Reserve capacity, incremental migration or a structure with the required worst-case bound', 'What work can be deferred, and where are entries found during migration?'], ['Prefix matching', 'Trie or an appropriate string index', 'Exact-key equality alone does not answer prefix queries.']]} />
    <details><summary>Deeper directions: different probing rules and different problems</summary>
      <Prose><strong>Double hashing</strong> uses a second key-derived step, with a full-cycle condition such as a step relatively prime to capacity. <strong>Quadratic probing</strong> uses a nonlinear offset; its coverage depends on the precise capacity and offset rule. Neither name alone guarantees that every slot is reachable. A new probe scheme requires revisiting both the stopping invariant and its distribution analysis.</Prose>
      <Prose>Other schemes such as Robin Hood hashing move entries according to probe distance, while cuckoo hashing may displace entries between candidate homes. They change insertion/deletion invariants and latency tradeoffs, so their theorems cannot simply be attached to this linear-probe code. A focused implementation of those schemes would deserve its own careful derivation.</Prose>
      <Prose>Rolling fingerprints in string matching, probabilistic membership filters, cryptographic digests and consistent hashing across machines use related words for different contracts. A probabilistic filter can allow false positives; our exact map cannot. A distributed placement rule must handle node membership changes; this table only changes local capacity. Follow the relevant later string, security or distributed-systems material before applying those techniques. Here the core finish line is an exact local map and a defensible analysis of its updates.</Prose>
    </details>
    <Prose>The next lesson in this module is <strong>Shortest Paths, Spanning Trees & Topological Ordering</strong>. You will use maps and sets as supporting structures while proving properties of graph-wide choices. Carry forward the habit of naming both the representation invariant and the assumptions behind each cost.</Prose>

    <H2>10. Build and explain it independently</H2>
    <Prose>Close the implementations before starting. Write a small correct baseline, a representation invariant and at least one counterexample to a plausible shortcut. Test against an independently structured oracle; do not rely only on copying the example's expected output.</Prose>
    <Exercise title="A. A wrapped cluster and a replacement" hint="Trace from the query's home, not from the lowest array index. A deleted marker is not an absence certificate." solution={<Prose>With capacity 4, keys 3,7,11 occupy slots 3,0,1. Delete 7 by marking slot 0 DELETED. Looking up 11 visits 3,0,1 and succeeds. Replacing 11 remembers slot 0 but reaches the equal key at 1 and updates there; live size stays 2. Inserting 15 later visits the same path, reaches EMPTY slot 2, then uses the remembered slot 0. Erasing slot 0 instead would hide 11.</Prose>}>
      <Prose>Use capacity 4. Insert 3,7,11 with distinct values; delete 7; look up 11; replace 11's value; then insert 15. Draw every slot and give the live size after replacement. Explain why immediate tombstone insertion would be wrong.</Prose>
    </Exercise>
    <Exercise title="B. Rebuild with no spare live slot" hint="A scan needs a numerical bound even when the stopping marker does not exist." solution={<Prose>A full table can still update an existing key after at most m probes. It can reject an absent get after the same bound. A new unequal key has no destination, so it must fail or grow. After deleting a key to a tombstone, a new insertion can scan all m slots, rule out duplication and reuse the remembered slot even though no EMPTY exists. An inadequate rebuild must leave the original associations unchanged.</Prose>}>
      <Prose>Fill a four-slot map. Specify get, replacement, new insertion, deletion and reinsertion with no EMPTY slots. Implement these cases and compare every completed operation against a dict. Also request a rebuild smaller than the live size and verify the original mapping survives rejection.</Prose>
    </Exercise>
    <Exercise title="C. A different append prefix" hint="List the old capacities copied, then reconcile the result with the potential rather than treating the 24-operation total as a formula." solution={<Prose>For 17 appends, copies are 1+2+4+8+16=31 and writes are 17, giving 48. The last operation costs 17. Final n=17,C=32 gives Φ=3; 3×17−3=48. A large last operation and a linear prefix total are compatible.</Prose>}>
      <Prose>Derive the exact counted work for 17 appends starting from capacity 1. What does the last append cost? Verify both the geometric sum and Φ=2n−C+1.</Prose>
    </Exercise>
    <Exercise title="D. Design a nonthrashing shrink policy" hint="Check the state immediately after each resize. How many updates are needed to reach either threshold again?" solution={<Prose>With growth at full and shrink at n≤C/2, a doubled full table is already at about half occupancy, so one deletion can undo growth. Shrinking at n≤C/4 leaves a halved table at half occupancy and separates future expensive events by Θ(C) updates. Specify minimum capacity, whether the inequality is strict and whether shrink is tested before or after deletion; then simulate boundary lengths. Other separated constant thresholds can work, but each requires its own bound rather than reusing the exact displayed totals.</Prose>}>
      <Prose>Starting with eight items in eight slots, construct ten operations that defeat shrinking at half occupancy. Propose an exact replacement policy and explain why its copy cost can be charged to intervening updates. Include the empty/minimum-capacity case.</Prose>
    </Exercise>
    <Exercise title="E. Separate three cost claims" hint="Name the set over which each average is taken. The map's key comparison still happens during every update." solution={<Prose>A fixed all-colliding modulo map can have Θ(n) lookup regardless of geometric capacity growth. A universal chained hash family supplies expected candidate bounds for inputs independent of its random choice, with bounded load and key costs. Doubling separately pays linear rebuild work across many updates. Combining the latter two under their assumptions gives expected amortized updates; it does not provide constant worst-case latency or protect an adaptive adversary automatically.</Prose>}>
      <Prose>A developer says “the table doubles, so every operation is O(1), even when all keys collide.” Identify each missing assumption and replace the statement with precise alternatives.</Prose>
    </Exercise>
    <Exercise title="F. Change the composite contract" hint="A single value-to-index mapping loses occurrence identity. Ask what exactly must be sampled uniformly." solution={<Prose>The dense set stores a value once and samples distinct values uniformly. If duplicates are allowed and occurrences must be uniform, retain every occurrence in the array and track all positions for each value, updating the moved occurrence's position during swap-delete. If distinct values must remain uniform instead, maintain a separate dense collection of distinct values plus multiplicities. These are different contracts. Neither follows from changing a boolean return alone.</Prose>}>
      <Prose>Allow repeated values in the random-access collection. Explain two meanings of “uniform”: uniform over occurrences and uniform over distinct values. Sketch the additional state for each before attempting code.</Prose>
    </Exercise>
    <Exercise title="G. Preserve multiplicity and choose a pairing" hint="Compare ab+cd for the three partitions into pairs; memory depends on the side whose frequencies you store." solution={<Prose>For lengths 2,3,100,100, pairing (2×3)+(100×100) requires 10006 pair iterations. Pairing (2×100)+(3×100) requires 500, and the smaller stored side has at most 200 distinct sums. Equal totals can reduce distinct-sum space but do not eliminate pair enumeration. Validate the count on tiny arrays against four nested loops, including duplicates and a nonzero target.</Prose>}>
      <Prose>For group lengths 2,3,100,100, compare pairing choices and choose which side to store. Explain why replacing sum frequencies by a set changes the answer. Implement a tiny exhaustive oracle.</Prose>
    </Exercise>
    <DsaPractice practice={hashingPractice} />
    <Sources alternatives={<>
      <Prose><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/resources/lecture-4-hashing/" target="_blank" rel="noopener noreferrer">MIT 6.006: Lecture 4 — Hashing (video and notes)</a> offers a board-based second route from direct access to randomized chaining. Use it after section 5 and reconstruct the indicator argument with different keys. The official page and accompanying written notes were inspected; full video playback was not part of this lesson's verification.</Prose>
      <Prose><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-10-open-addressing-cryptographic-hashing/" target="_blank" rel="noopener noreferrer">MIT 6.006: Lecture 10 — Open Addressing, Cryptographic Hashing</a> is an optional video route for comparing probe rules. Its cryptographic extension has a different purpose from exact map lookup. The official resource page and notes identify the relevant sections; historical performance and implementation details require separate current verification.</Prose>
      <Prose><a href="https://opendatastructures.org/ods-python/5_2_LinearHashTable_Linear_.html" target="_blank" rel="noopener noreferrer">Open Data Structures: LinearHashTable</a> gives a longer written analysis of used occupancy and clusters. Read its assumptions as carefully as its formulas, and compare its policy with this lesson's explicit fixed-capacity probe map.</Prose>
    </>}>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/ce9e94705b914598ce78a00a70a1f734_MIT6_006S20_lec4.pdf" target="_blank" rel="noopener noreferrer">MIT Lecture 4 notes</a> — direct addressing, chaining and universal-family collision expectation. The finite keys, outcome lattice and complete programs here are original worked examples.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/79a07dc1cb47d76dae2ffedc701e3d2b_MIT6_006S20_lec2.pdf" target="_blank" rel="noopener noreferrer">MIT Lecture 2 notes</a> — dynamic arrays and separated deletion thresholds. Our plotted values are calculated reference-copy counts under the declared policy, not measured runtime.</li>
      <li><a href="https://opendatastructures.org/ods-python/5_1_ChainedHashTable_Hashin.html" target="_blank" rel="noopener noreferrer">Open Data Structures: ChainedHashTable</a> — adjacent chaining analysis and resizing context.</li>
      <li><a href="https://15445.courses.cs.cmu.edu/fall2022/notes/07-hashtables.pdf" target="_blank" rel="noopener noreferrer">CMU database systems: Hash Tables</a> — sections 4.2–4.3 explain the Robin Hood and cuckoo displacement mechanisms mentioned as deeper directions. Their other historical implementation/performance claims are not needed for our examples.</li>
      <li><a href="https://docs.python.org/3/reference/datamodel.html#object.__hash__" target="_blank" rel="noopener noreferrer">Python data model: object.__hash__</a> — equality, immutable hash identity and default str/bytes randomization. <a href="https://peps.python.org/pep-0456/" target="_blank" rel="noopener noreferrer">PEP 456</a> explains historical collision-attack motivation, not a current benchmark or default-algorithm claim.</li>
      <li><a href="https://docs.python.org/3/library/random.html#random.choice" target="_blank" rel="noopener noreferrer">Python random.choice</a> — sampling from a nonempty sequence. The demonstration generator is for reproducible learning, not cryptographic selection.</li>
    </Sources>
  </div>
};
