import { H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { ConcentrationTailLab, ExponentialWitnessLab, ConcentrationBudgetLab, ZeroVarianceFigure, SamplingDependenceLab, SimultaneousBudgetLab } from '../../components/lesson-labs/ConcentrationInequalityLabs.jsx';
import { concentrationInequalityExamples } from '../concentration-inequalities-examples.js';
function ConcentrationExercise({
  prompt,
  hint,
  children
}) {
  return <section className="lesson-check concentration-exercise">
    <p><strong>Try it first.</strong> {prompt}</p>
    <details className="concentration-hint"><summary>Show a hint</summary><Prose>{hint}</Prose></details>
    <details className="concentration-solution"><summary>Show the solution</summary><div>{children}</div></details>
  </section>;
}
function ConcentrationExample({
  example,
  question
}) {
  return <section className="concentration-program">
    <Prose><strong>Question to investigate:</strong> {question}</Prose>
    <RunnableExample example={example} />
  </section>;
}
export default {
  title: 'Concentration Inequalities (Hoeffding, Bernstein, Chernoff)',
  readTime: '~65 min read + 90–120 min exploration and practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot concentration-lesson">
    <LessonIntro prerequisites="Expectation, variance, independent events and the binomial distribution from Probability Distributions & Bayes’ Theorem. We rebuild sample-average notation, the exponential argument and error-budget reasoning here. The optional curvature proof uses derivatives and integration." sections={[['1-state-the-event-before-bounding-it', 'Events and actual probabilities'], ['2-turn-an-event-into-an-expectation', 'Markov, moments and exponentials'], ['3-derive-a-bound-from-a-known-range', 'Hoeffding and sample budgets'], ['4-use-variance-with-a-valid-contract', 'Bernstein and its limitations'], ['5-count-rare-events-with-chernoff', 'Relative and exact count tails'], ['6-check-what-independent-evidence-means', 'Copies and finite populations'], ['7-budget-for-selection-and-stopping', 'Many checks and monitoring'], ['8-practise-changing-the-contract', 'Independent practice']]}>A classifier scores 83% on a test set. A service sees 30 failures when its model predicts 20. How much could sampling alone explain? Turn a precisely stated randomness model into a finite-sample guarantee, see where it loses information, and recognize situations where its assumptions do not support the conclusion.</LessonIntro>

    <H2>1. State the event before bounding it</H2>
    <Prose>A different sample can produce a different observed average. A <strong>concentration inequality</strong> bounds the probability that a random quantity lies far from its expectation. It does not estimate the expectation for us, prove a predictor is correct, or establish that the sampled population matches tomorrow’s users. Its promise is conditional: under specified assumptions, a specified large deviation is sufficiently unlikely.</Prose>
    <Prose>For binary observations, let Xᵢ be 1 for a success and 0 otherwise. With n observations, S is the success count and X̄=S/n is the success fraction. If each observation has success probability p, then E[S]=np and E[X̄]=p. Expectation, written E, averages over repeated outcomes under the probability model, not just the one sample we observed.</Prose>
    <MathBlock>{String.raw`S=\sum_{i=1}^{n}X_i,\qquad \bar X=S/n`}</MathBlock>
    <Prose>Use <strong>µ</strong> for a population mean, <strong>m</strong> for an expected count, <strong>ε</strong> for an absolute error tolerance and <strong>δ</strong> for a failure-probability budget. These are different jobs. An accuracy error of ε=0.10 means ten percentage points: 0.83 versus 0.73 differs by 0.10. A relative error of 10% of 0.83 would instead be 0.083.</Prose>
    <MathBlock>{String.raw`\Pr\!\left(|\bar X-\mu|\geq\varepsilon\right)\leq\delta`}</MathBlock>
    <Prose>The bars mean absolute value, so the event includes both sides. The probability concerns a procedure applied to random data before seeing its outcome. After observing a dataset, this frequentist statement does not become “the fixed µ has probability 95% of being in this particular interval.” The previous inference lessons distinguish this from a posterior probability.</Prose>
    <Prose>Independent binary observations with the same p give a binomial count. We can calculate P(S≥k) by adding masses at counts k,k+1,…,n. The inequality ≥ is inclusive. A threshold k=n+1 is impossible; k=0 is certain. Above the mean, an upper-tail bound is useful; below it, the same positive-deviation formula cannot be used by squaring a negative deviation.</Prose>
    <ConcentrationTailLab />
    <Prose>The bars come from a calculated binomial law, not a simulation. The highlighted mass is the actual event probability under that law. Each ledger entry is an upper bound using different information. A bound of 0.2 does not assert that the actual probability is 0.2. A raw expression above 1 gives only the trivial probability bound 1. At deterministic endpoints the lab uses the known support directly.</Prose>
    <Checkpoint prompt="Four independent fair coin flips produce a count S. What does P(S≥3) include, and what is its exact value?"><Prose>It includes exactly three and exactly four heads: four outcomes plus one among sixteen equally likely outcomes, giving 5/16. P(S&gt;3) would include only four heads and equal 1/16. A concentration formula cannot repair an incorrectly specified event.</Prose></Checkpoint>

    <H2>2. Turn an event into an expectation</H2>
    <H3>Markov: rare large values consume the average</H3>
    <Prose>If Z is nonnegative with finite expectation, every outcome Z≥a contributes at least a to Z, for a&gt;0. The indicator 1{'{'}Z≥a{'}'} equals 1 when the event occurs and 0 otherwise. Pointwise, a times this indicator is at most Z. Taking expectations gives <strong>Markov’s inequality</strong>: P(Z≥a)≤E[Z]/a. Nonnegativity matters: negative values could otherwise offset large positive contributions.</Prose>
    <Prose>Apply Markov to the nonnegative square (Y−E[Y])². Its expectation is the variance, Var(Y), giving <strong>Chebyshev’s inequality</strong>: P(|Y−E[Y]|≥t)≤Var(Y)/t² for t&gt;0 and finite variance. No independence is needed for this statement about one Y. Independence enters when we add variances of observations; without it, covariance terms can remain.</Prose>
    <H3>An exponential can cover the event more efficiently</H3>
    <Prose>For λ&gt;0, an outcome S≥k has exp(λ(S−k))≥1. On other outcomes the event indicator is zero and the exponential stays positive. Thus the exponential covers the indicator pointwise. Averaging that cover creates a bound; λ trades excess weight below the threshold against large weight far above it.</Prose>
    <figure className="concentration-inline concentration-proof-flow" aria-label="Exponential proof chain">
      <ol><li><strong>Specify the event</strong><span>S≥k; its indicator is 0 or 1.</span></li><li><strong>Cover every outcome</strong><span>1{'{'}S≥k{'}'} ≤ exp(λ(S−k)), λ&gt;0.</span></li><li><strong>Average the cover</strong><span>P(S≥k) ≤ exp(−λk) E[exp(λS)].</span></li><li><strong>Use independence</strong><span>E[exp(λΣXᵢ)] = ∏ E[exp(λXᵢ)].</span></li><li><strong>Choose λ</strong><span>Minimize the bound you can justify.</span></li></ol>
      <figcaption>Every arrow is a mathematical operation, not a change to the population. Without independence, the product step needs a different argument.</figcaption>
    </figure>
    <Prose>The function M<sub>Y</sub>(λ)=E[exp(λY)] is the <strong>moment-generating function</strong> (MGF), where finite. Its logarithm is often easier to use: products become sums, and tiny probabilities can be represented without numerical underflow. Exponential Markov followed by choosing λ is the <strong>Chernoff method</strong>. It works beyond binary variables whenever we can control the relevant MGF.</Prose>
    <Prose>Take n=20 independent Bernoulli(1/4) observations and k=10. For one observation, the exponential expectation is (3/4)+(1/4)exp(λ). For the count it is that quantity raised to the twentieth power. The actual event remains fixed as λ changes.</Prose>
    <ExponentialWitnessLab />
    <Prose>At λ=0 the cover is identically 1, giving no information. At λ=log 3 the bound is about 0.05631, versus actual probability 0.01386. Raising λ to 2 worsens the bound to about 0.40219: aggressively weighting large counts can add more excess than it removes. The second curve anticipates Hoeffding’s simpler MGF envelope. It can be easier to compute while losing information about this particular law.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.exponentialWitness} question="Does summing the exponential contributions over every count agree with the factored MGF? Compare several weights, then locate the best bound while the event remains fixed." />
    <Prose>All nine programs run independently with Python 3.12 and its standard library. Save one complete block as <code>concentration.py</code>, then run <code>python concentration.py</code>. Fraction arithmetic calculates finite probabilities exactly where specified; displayed decimals and exponential functions are rounded. The browser runs separately checked JavaScript models, not the Python blocks.</Prose>

    <H2>3. Derive a bound from a known range</H2>
    <Prose>Suppose independent Xᵢ each lie in a known interval [aᵢ,bᵢ]. Their distributions and means may differ. Center each observation: Yᵢ=Xᵢ−E[Xᵢ]. The centered sum T=ΣYᵢ has mean zero. Write wᵢ=bᵢ−aᵢ and D=Σwᵢ². D summarizes squared widths; it is not the actual variance of the sum.</Prose>
    <Prose><strong>Hoeffding’s lemma</strong> bounds each centered log-MGF by λ²wᵢ²/8. Independence lets us add them, so exponential Markov gives P(T≥t)≤exp(−λt+λ²D/8). For t&gt;0 and D&gt;0, this quadratic is minimized at λ=4t/D.</Prose>
    <MathBlock>{String.raw`\Pr(T\geq t)\leq e^{-2t^2/D}`}</MathBlock>
    <Prose>Apply the same argument to −T and add the tail bounds. Addition is the union bound: the probability of at least one event is at most the sum of their probabilities. No independence between those events is needed. Thus P(|T|≥t)≤min(1,2exp(−2t²/D)). If D=0, all observations are constant almost surely and T=0; handle that case directly instead of dividing by zero.</Prose>
    <details className="concentration-proof"><summary>Derive the bounded log-MGF lemma</summary>
      <Prose>For bounded X, let A(λ)=log E[exp(λX)]. Boundedness makes this finite and permits differentiating the expectation. Reweight the original law by exp(λX)/E[exp(λX)]; these positive weights integrate to 1 and define a tilted probability law. Differentiation gives A′(λ)=E<sub>λ</sub>[X] and A″(λ)=Var<sub>λ</sub>(X). This tilted law is a proof device, not a claim that collected data changed.</Prose>
      <Prose>Any law on [a,b] has variance at most (b−a)²/4. Set c=(a+b)/2. The identity Var(X)=E[(X−c)²]−(E[X]−c)² is at most E[(X−c)²], which is at most (b−a)²/4 pointwise. The tilted law has the same support, so its variance obeys the same bound.</Prose>
      <Prose>Now ψ(λ)=A(λ)−λE[X] is the centered log-MGF. It satisfies ψ(0)=ψ′(0)=0 and ψ″(λ)≤(b−a)²/4. For λ≥0, integrating the curvature twice gives ψ(λ)≤λ²(b−a)²/8. Apply the same reasoning to −X for negative λ. Exponentiating gives the lemma. The range supplies a uniform bound over all tilted laws; using only the original variance would not justify this proof.</Prose>
    </details>
    <Prose>For a common interval of width w and average error ε, set t=nε and D=nw². If means differ, the target is the average of their means; if all share µ, it is simply µ. The two-sided form and sample-budget inversion are:</Prose>
    <MathBlock>{String.raw`\Pr(|\bar X-\mu|\geq\varepsilon)\leq 2e^{-2n\varepsilon^2/w^2}`}</MathBlock>
    <MathBlock>{String.raw`n\geq\frac{w^2\log(2/\delta)}{2\varepsilon^2}`}</MathBlock>
    <Prose>Use natural logarithms and round the sample count upward. Binary outcomes with n=100 and ε=0.10 give a bound about 0.271. A failure budget δ=0.05 needs 185 observations according to this guarantee. This is sufficient under the assumptions; it is not a theorem that 184 observations can never work.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.hoeffdingBudget} question="How do the same tolerance and failure budget produce both a tail guarantee for a fixed sample count and a sufficient count for a planned experiment? Identify why rounding must be upward." />
    <Prose>Alternatively, the radius is r=w√(log(2/δ)/(2n)). Repeated samples produce intervals [X̄−r,X̄+r] covering the common mean with probability at least 1−δ. If µ lies in [a,b], intersect with that interval. Halving the radius requires four times as many observations under this formula. Reducing δ changes a logarithm, so the two kinds of precision have different costs.</Prose>
    <ConcentrationBudgetLab />
    <Prose>The variance curves are developed next. First vary the range: multiplying w by ten while holding absolute ε fixed multiplies this sufficient sample budget by one hundred. Merely converting the same problem from fractions to percentages multiplies both w and ε by one hundred, leaving n unchanged. Units belong to both sides of the error contract.</Prose>
    <H3>Different ranges and fixed weights</H3>
    <Prose>Three independent fair choices from {'{'}0,1{'}'}, {'{'}0,2{'}'} and {'{'}−1,1{'}'} have sum expectation 3/2 and squared-width sum D=1+4+4=9. Exactly two of eight equally likely outcomes deviate from 3/2 by at least 2, so the true two-sided probability is 1/4. Hoeffding gives about 0.8222: correct, but loose.</Prose>
    <Prose>For a weighted sum ΣαᵢXᵢ with fixed coefficients, scale each support width to |αᵢ|wᵢ. Then D=Σαᵢ²wᵢ² and the target is ΣαᵢE[Xᵢ]. Coefficients selected after inspecting these observations need a selection argument. Negative coefficients are allowed; the absolute width handles their reversed intervals.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.unequalRanges} question="When three observations have different supports, which expectation and squared-width sum control their centered sum? Enumerate the outcomes, then repeat the calculation with fixed weights." />

    <H2>4. Use variance with a valid contract</H2>
    <Prose>A bounded population whose values mostly stay near their mean can be easier to estimate than the worst distribution allowed by its range. <strong>Bernstein’s inequality</strong> exploits a justified variance bound as well as a bound on each centered deviation. Let independent centered Yᵢ satisfy |Yᵢ|≤c, let v be the sum of their variances or a known upper bound, and let T=ΣYᵢ. For t&gt;0:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\Pr(|T|\geq t)\\\leq 2\exp\!\left(-\frac{t^2}{2(v+ct/3)}\right)\end{gathered}`}</MathBlock>
    <Prose>Cap a probability bound at 1. When v dominates ct/3, the exponent behaves like −t²/(2v), a quadratic penalty. When ct/3 dominates, it behaves like −3t/(2c), a linear penalty. This transition preserves protection against rare large increments; small variance alone cannot forbid them.</Prose>
    <details className="concentration-proof"><summary>See where the Bernstein denominator comes from</summary>
      <Prose>For centered bounded Y and integer k≥2, E[|Y|ᵏ]≤cᵏ⁻²E[Y²]. Expand E[exp(λY)] into its power series; the linear term vanishes. The elementary bound k!≥2·3ᵏ⁻² sums the remaining terms as a geometric series when 0≤λ&lt;3/c. Using log(1+u)≤u gives a centered log-MGF bound λ²Var(Y)/(2(1−λc/3)).</Prose>
      <Prose>Add these using independence and apply exponential Markov. For v&gt;0, choosing λ=t/(v+ct/3) lies below 3/c and gives the upper-tail exponent; repeat for −T and add. If v=0 is a genuine variance bound, all centered increments vanish almost surely, so positive deviations are impossible. The generic relaxed formula need not expose that stronger degenerate conclusion. This λ is a convenient valid choice, not a claimed exact minimizer of every MGF envelope.</Prose>
    </details>
    <Prose>For observations in an interval of width w, take c=w. Suppose each variance is at most known σ². Set v=nσ², t=nr and L=log(2/δ). Solving the quadratic for a nonnegative radius gives the first expression; the second is a convenient larger relaxation.</Prose>
    <MathBlock>{String.raw`\begin{aligned}a&=\frac{cL}{3n},\quad L=\log(2/\delta),\\r_{\mathrm{root}}&=a+\sqrt{2\sigma^2L/n+a^2},\\r_{\mathrm{easy}}&=\sqrt{2\sigma^2L/n}+2a.\end{aligned}`}</MathBlock>
    <Prose>The relaxation uses √(u+a²)≤√u+a for u,a≥0. With n=100, δ=0.05, c=1 and σ²=0.09, the root radius is about 0.09470. The easier form is about 0.10608; Hoeffding gives 0.13581. At variance bound 0.25, Hoeffding can instead be tighter. Variance information does not guarantee winning every comparison.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.varianceRadii} question="For the same data count and failure budget, how much changes when a valid variance bound is available? Keep the interval width and variance information separate." />
    <H3>An observed variance is not automatically a variance bound</H3>
    <Prose>The theorem needs population variance or a justified upper bound. A sample can miss rare outcomes and report variance zero. Substituting that estimate changes the procedure without proving its coverage. This counterexample establishes actual miscoverage, not merely one failure that a 95% procedure may legitimately have.</Prose>
    <ZeroVarianceFigure />
    <Prose>Take n=100 independent Bernoulli(1/40) observations. On the all-zero sample, either common sample-variance convention gives zero. Substitution yields radius 2log(40)/300≈0.02459253, smaller than actual error 1/40=0.025. This outcome alone occurs with probability (39/40)¹⁰⁰≈0.07951729, already above δ=0.05. Thus the proposed procedure cannot have its claimed 95% coverage for this population.</Prose>
    <Prose>An <strong>empirical Bernstein</strong> theorem can account for variance estimation using its own corrections and assumptions. It is not the population-variance theorem with a guessed number. If only [0,1] boundedness is known, σ²≤1/4 is justified universally, and Hoeffding is often simpler.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.bernsteinInversion} question="Does the quadratic radius actually solve the target tail equation? Then compute both the probability and the size of the error on an all-zero sample to test the unjustified plug-in procedure." />

    <H2>5. Count rare events with Chernoff</H2>
    <Prose>Let S sum independent Bernoulli observations with probabilities pᵢ, not necessarily identical. Its expected count is m=Σpᵢ. One exponential expectation is 1+pᵢ(exp(λ)−1). Applying 1+x≤exp(x), then multiplying using independence, bounds the count MGF by exp(m(exp(λ)−1)). Compressing the probabilities into their sum can lose information.</Prose>
    <Prose>For m&gt;0 and relative increase r&gt;0, exponential Markov at threshold (1+r)m is optimized for this envelope by λ=log(1+r). The mean-only bound is valid for every r&gt;0; its common quadratic simplification needs r≤1.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\Pr(S\geq(1+r)m)\leq e^{-m h(r)},\\h(r)=(1+r)\log(1+r)-r.\end{gathered}`}</MathBlock>
    <MathBlock>{String.raw`0<r\leq1:\qquad e^{-mh(r)}\leq e^{-mr^2/3}`}</MathBlock>
    <Prose>For the lower tail, use a negative exponential parameter: for 0&lt;r&lt;1, P(S≤(1−r)m)≤exp(−mr²/2). The less simplified exponent is −m[r+(1−r)log(1−r)]. Integer counts make noninteger thresholds unambiguous: round an upper threshold up or a lower threshold down when evaluating the exact event. If m=0, every Bernoulli probability is zero and S=0 almost surely; do not divide by m.</Prose>
    <Prose>A stable model expects 20 errors in 1,000 independent requests. Observing 30 is a 50% relative increase, r=0.5, but only a one-percentage-point increase in the sample mean. The mean-only upper bound is about 0.115. It is neither the exact probability nor evidence that independent requests adequately model every service outage.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.rareCount} question="How does a change from an expected count of 20 to a threshold of 30 determine the relative increase? Evaluate the unsimplified guarantee without confusing it with the exact tail." />
    <H3>Keep the full binomial MGF when its model is known</H3>
    <Prose>When all Bernoulli probabilities equal p, keep the exact MGF (1−p+p exp(λ))ⁿ. For target fraction q=k/n with p&lt;q&lt;1, minimizing its log bound gives λ=log(q(1−p)/(p(1−q))). Substitution gives exp(−nD(q‖p)), where the binary relative entropy is:</Prose>
    <MathBlock>{String.raw`\begin{gathered}D(q\Vert p)=q\log(q/p)\\{}+(1-q)\log((1-q)/(1-p)).\end{gathered}`}</MathBlock>
    <Prose>This measures the log-likelihood cost of a changed success fraction; broader information theory comes later. At q=1, use the limit 0log0=0, giving the exact all-success probability pⁿ. Handle p=0 or 1 from the support. This is an upper-tail bound when q&gt;p; inserting q&lt;p into that upper-tail conclusion is incorrect.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.exactBinomial} question="For one fixed inclusive event, how different are the exact probability and four valid guarantees? Verify the small rational case and the impossible threshold as well as the rare-count scenario." />
    <Prose>At n=1000, p=0.02 and k=30, the actual tail is about 0.02069652. One-sided Hoeffding gives 0.81873075; bounded Bernstein gives 0.11301524, mean-only Chernoff 0.11487011, and binomial KL 0.10913735. These are calculated from the stated model, not universal rankings. Choose a valid useful bound, and compute a distribution-specific tail when that distribution is genuinely available.</Prose>
    <H3>Why rare means change the sample-size question</H3>
    <Prose>Absolute tolerance 0.10 tells us little about estimating mean 0.02 relatively accurately. Relative error r means absolute ε=rp. For independent identical Bernoulli observations, combining the simplified tails for 0&lt;r&lt;1 gives sufficient n≥3log(2/δ)/(r²p). Inserting ε=rp into Hoeffding instead scales as 1/p². The count-sensitive bound uses extra Bernoulli information to obtain a 1/p dependence.</Prose>
    <Prose>This planning expression contains unknown p. A known positive lower bound p≥p₀ gives a uniform sufficient budget using p₀; an optimistic pilot estimate does not automatically qualify. Without a lower-mean assumption, no fixed n guarantees useful relative precision uniformly over arbitrarily small positive p: an all-zero sample remains likely.</Prose>
    <Prose>For an algorithmic application, consider the load of one fixed hash bucket. Under independent uniform assignment of N keys to B buckets, indicators “key i lands in bucket j” are independent across keys and sum to a count of mean N/B. Chernoff bounds its overload. Protect all B buckets by summing their overload bounds; their loads need not be independent for that union step. Indicators that different buckets are occupied are generally dependent and do not inherit the elementary independent-count proof.</Prose>

    <H2>6. Check what independent evidence means</H2>
    <Prose>Equal observation means do not tell us how much averaging helps. Sum variance includes individual variances plus twice each distinct covariance. Independence makes the covariances zero, yielding Var(X̄)=σ²/n when variances equal σ². Zero covariance suffices for this variance identity, but not automatically for the MGF factorization above.</Prose>
    <Prose>Compare three ways to obtain n binary values of marginal mean 1/4. Draw independently with replacement from twenty labelled records, five marked. Or draw once and copy it n times. Or choose a uniform n-record subset without replacement. Their marginal means match; their joint laws differ.</Prose>
    <SamplingDependenceLab />
    <Prose>At n=20 and ε=0.20, the independent mean has actual error-event probability about 0.06524. The copied mean is either 0 or 1, always at least 0.20 from 1/4, so the event probability is 1. Treating those copies as twenty independent observations gives about 0.40379 and is false for that design. With a uniform subset, n=20 samples the entire population: the mean is exactly 1/4 and the event impossible.</Prose>
    <Prose>The without-replacement count is <strong>hypergeometric</strong>: mass at k is C(5,k)C(15,n−k)/C(20,n), with impossible combinations contributing zero. Its mean variance is (p(1−p)/n)·(20−n)/19. That finite-population factor describes uniform sampling from a fixed set; it is not a correction for arbitrary duplicated, clustered or Markov-chain data.</Prose>
    <H3>A useful extension requires its own theorem</H3>
    <Prose>Without-replacement draws are dependent, yet the same bounded Hoeffding expression holds for a uniform sample from a fixed finite population. A separate comparison theorem explains why: for every convex function, its expected value at the without-replacement sum is no larger than at the sum of the same number of independent draws with replacement from that population. Exponentials of either sign are convex. Compare their expectations first, then apply the independent-draw MGF bound. This proves the extension without pretending the draws are independent.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.samplingLaws} question="Do identical marginal means produce identical error events under three sampling designs? Compare the exact distributions at a partial sample and at the full population size." />
    <Prose>The finite population is the target. A biased subset does not become uniform merely because it has no duplicates. Likewise, a million consecutive MCMC states need not supply a million independent pieces of evidence. The next topic develops those chains. Boundedness, finite variance, dependence, stationarity and population shift are separate questions; bootstrap or permutation machinery is not an automatic repair for all of them.</Prose>

    <H2>7. Budget for selection and stopping</H2>
    <H3>Many fixed checks</H3>
    <Prose>A 95% guarantee for one fixed metric does not remain a 95% guarantee after inspecting one hundred metrics and reporting the worst surprise. If failure event Eⱼ has probability at most δⱼ, the union bound gives P(any Eⱼ)≤Σδⱼ. Allocate δ/K to each of K fixed checks to keep the total at most δ. Their failure events need not be independent.</Prose>
    <SimultaneousBudgetLab />
    <Prose>The exact family curves are explicit toys: either independent collections of Bernoulli(1/2) samples, or copies of one identical check. They are not measurements of one hundred trained models. At n=100, K=100 and δ=0.05, the naive per-check radius is about 0.13581; independent failures accumulate to actual family probability about 0.48620. Budgeting δ/K expands the radius to about 0.20364 and gives actual family failure about 0.00321 in this conservative example. For identical checks, repetition does not change the actual event, but the union guarantee remains valid.</Prose>
    <Prose>This supports selecting among a <strong>predeclared finite class</strong> of predictors: apply a bounded-loss guarantee to each member on independent evaluation examples, then union-bound. On the simultaneous good event, even the member selected using those results satisfies its own bound. Member errors may be strongly dependent because they share a test set. Inventing an unrestricted family after seeing evaluation data lies outside this finite-class argument; sample separation or suitable uniform/generalization tools must address that dependence.</Prose>
    <H3>Monitoring repeatedly</H3>
    <Prose>Fixed-time validity does not alone give validity at an adaptively selected stopping time. For a conservative repair, assume an independent bounded stream with one fixed mean and known width w. Allocate δₜ=δ/[t(t+1)] to time t≥1. Since 1/[t(t+1)]=1/t−1/(t+1), these budgets sum to δ over all times.</Prose>
    <MathBlock>{String.raw`r_t=w\sqrt{\frac{\log(2t(t+1)/\delta)}{2t}}`}</MathBlock>
    <Prose>At each fixed time Hoeffding bounds failure by δₜ. A countable union bound makes the probability of <em>any</em> failure at any time at most δ. Thus the intervals remain valid at a time chosen from observed data, on paths where that stopping time is finite. Their overlapping samples do not invalidate the union step. This conservative <strong>confidence sequence</strong> is wider than a fixed-time interval because it protects every time together.</Prose>
    <Prose>For δ=0.05 and w=1, radii are about 0.25406 at time 100 and 0.09356 at time 1000. These come from our elementary time-budget construction. Sharper martingale, mixture and empirical-variance confidence sequences exist with their own proofs. Replacing t with an informally estimated “effective sample size” does not establish these assumptions for a dependent stream.</Prose>
    <ConcentrationExample example={concentrationInequalityExamples.simultaneousBudget} question="How do per-check and global budgets differ numerically? Calculate family failures, verify a telescoping allocation over time, and solve the changed sample-size tasks." />
    <LessonTable caption="Choose the guarantee by its contract" headers={['Known structure', 'Useful starting point', 'Keep explicit']} rows={[['Nonnegative quantity / finite variance', 'Markov / Chebyshev', 'Moment existence and target; no independence needed for the one-variable statement.'], ['Independent known ranges', 'Hoeffding', 'Support bounds, squared widths, fixed coefficients and one-sided versus two-sided event.'], ['Independent centered bound and variance bound', 'Bounded Bernstein', 'Population variance, maximum increment c and total v.'], ['Independent Bernoulli count', 'Multiplicative Chernoff; binomial tail/KL for identical probabilities', 'Count mean m, relative increase r and support endpoints.'], ['Uniform finite-population sample', 'Without-replacement comparison', 'Uniform design and fixed population; a separate theorem supplies the extension.'], ['Selection / repeated monitoring', 'Finite or countable budget allocation', 'The entire protected family and assumptions at each check.']]} />
    <Prose>Start with the population and independent sampling unit, then state the event and tolerated error. Check range, moments and joint law before choosing a theorem. Invert it for sample count or radius, including selection and monitoring in the budget. An unhelpfully loose bound can motivate more data or a better-justified model; it does not justify an unproved narrower interval.</Prose>

    <H2>8. Practise changing the contract</H2>
    <Prose>Work these before opening the explanations. State the probability event and assumptions alongside each number. Recognize when a changed design, target or stopping rule needs a changed argument.</Prose>
    <ConcentrationExercise hint="Solve the two-sided Hoeffding expression once for its radius and once for n. Separate failure of the independence assumption from a mismatch between the sampled and claimed populations." prompt="1. Evaluate a fixed binary classifier on 400 independent examples. Find its two-sided Hoeffding radius at δ=.05. Then find the sufficient count for ε=.05 and δ=.01, and identify two failures of the application assumptions."><Prose>The radius √(log40/800)=0.06790508 is about 6.79 percentage points. The second count is ceil(log200/(2·.05²))=1060. Multiple records from one user can violate independence; evaluation from a different population does not establish the deployment target. The sampled-population guarantee might remain true in the latter case while being irrelevant to deployment.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Count outcomes with six, seven and eight heads. For each inequality, write its random variable, expectation or variance, and the deviation from the mean before substituting." prompt="2. Flip a fair coin eight times. Bound P(S≥6) by exact enumeration, Markov, two-sided Chebyshev used on the upper tail, and one-sided Hoeffding. Which values are only guarantees?"><Prose>The exact value is (28+8+1)/256=37/256≈0.14453125. Markov gives 4/6=2/3. Sum variance is 2 and deviation t=2, so Chebyshev gives 2/4=1/2. Hoeffding gives exp(−1)≈0.36787944. Only enumeration gives this model’s actual probability. Chebyshev protects a larger two-sided event and still bounds the selected upper event.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Square each support width and add. A fixed coefficient scales its interval width by the coefficient’s absolute value, even when the coefficient is negative." prompt="3. Independent bounded variables have widths 2, 1 and 3. Bound a centered-sum deviation of at least 4 on either side. Then use fixed coefficients 1, −2 and 1/2, retaining that deviation threshold."><Prose>Initially D=4+1+9=14, giving min(1,2exp(−32/14))≈0.20340278. After weighting, widths are 2,2,1.5, so D=10.25 and the bound is about 0.08814218. Center around the weighted expectation. The negative coefficient reverses an interval but contributes a positive squared width.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Set L=log(2/δ), form the positive root, and substitute it back into the exponent. For the plug-in question, distinguish a sample statistic from a population guarantee." prompt="4. With n=200, δ=.05, centered cap c=1 and known variance upper bound .04 per observation, calculate the Bernstein root radius. Why can replacing that variance bound with an observed zero invalidate coverage?"><Prose>Let L=log40 and a=L/600. The radius a+√(.08L/200+a²) is about 0.04504995. Its two-sided Bernstein expression equals .05 up to rounding. An observed zero is compatible with an unseen rare population outcome. The 100-observation Bernoulli(1/40) construction proves the naive plug-in can fail more than 5% of the time; estimation needs its own correction.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Relative tolerance r corresponds to absolute tolerance rp. Keep p in the count-sensitive formula, and ask what information makes that unknown quantity usable for planning." prompt="5. Independent Bernoulli trials have p=.01. Plan for relative error at most one half with failure probability .05 using the two-tail simplified Chernoff budget. Contrast additive Hoeffding. What if p is unknown?"><Prose>Chernoff gives ceil(3log40/(.5²·.01))=4427. Additive ε=.005 gives ceil(log40/(2·.005²))=73778. These are sufficient counts, not exact minima. A justified lower bound p≥.01 supports the first budget; an unsupported estimate does not. No fixed count handles arbitrarily small positive p uniformly at this relative precision.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Use variance of a sum, including covariance, or the exact finite law for each design. The uniform-subset correction depends on the fraction of the fixed population observed." prompt="6. Sample ten of twenty labelled records, five marked. Compare independent draws, a uniform subset, and ten copies of one draw. Find their mean variances and decide which use of Hoeffding is justified."><Prose>The variances are .1875/10=.01875, (.1875/10)(10/19)≈.00986842, and .1875. The independent theorem applies to the first. A separate without-replacement comparison gives the same conservative bounded expression for the second. Treating copies as ten independent observations is unjustified. Equal marginals or absence of duplicate records does not prove a sampling design.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Budget the probability of any predictor failing, rather than requiring predictor failures to be independent. Repeated times add an additional index to that family." prompt="7. Evaluate 25 predictors fixed before seeing 500 independent validation cases; each loss lies in [0,1]. Give a simultaneous radius at δ=.05, even though predictors share cases. Why does checking repeatedly until a desirable result appears need another budget?"><Prose>Allocate .002 per predictor. Radius √(log1000/1000)≈.08311291 protects all 25 simultaneously, including one selected afterward. Independence across predictors is unnecessary. Repeated checks add another family: protect (predictor,time) pairs with summable budgets such as δ/[25t(t+1)] when every fixed-time observation model is valid. The finite-predictor radius alone does not establish optional-stopping validity.</Prose></ConcentrationExercise>
    <ConcentrationExercise hint="Write three separate targets and sampling designs. Ask which information was fixed before evaluation, and whether a finite family or an all-time procedure is being protected." prompt="8. Design a protocol for a bounded error metric using a finite archive, daily new arrivals and a detector you may change after seeing results. Identify a defensible guarantee for each phase."><Prose>For the archive, target its mean and use a uniform labelled subset with a without-replacement theorem, or measure all records. For arrivals, first justify an independent stable-mean model; if valid, a confidence sequence can protect repeated checks. Clustered or drifting observations need a different analysis. For detector selection, freeze a finite candidate family and budget simultaneously, or reserve fresh independent evaluation data. Arbitrarily creating and retesting candidates on the same archive requires an adaptive/uniform analysis. Name population, unit, event, budget and limitation for each phase.</Prose></ConcentrationExercise>
    <Prose>You are ready to proceed when you can derive a bound from the event, identify the step using independence, and reject an attractive number whose assumptions are absent. The next topic in this module is <strong>Monte Carlo Methods & MCMC (Metropolis-Hastings, HMC, NUTS)</strong>. It turns sampling into a computational tool and introduces dependent chains, making sample count versus independent information especially consequential.</Prose>
    <Sources alternatives={<p>For a spoken undergraduate explanation, <a href="https://ocw.mit.edu/courses/18-200-principles-of-discrete-applied-mathematics-spring-2024/resources/18200-lecture-9-version-2_mp4/" target="_blank" rel="noreferrer">MIT 18.200 Lecture 9: Chernoff Bounds</a> focuses on the independence-based proof. The official page and description were checked; the full recording was not reviewed. Try it after the exponential-witness section, then derive the bound without the video.</p>}>
      <li><a href="https://cs.uwaterloo.ca/~r5olivei/courses/2025-spring-cs466/lecture-notes/lecture3/" target="_blank" rel="noreferrer">Waterloo CS466 Lecture 3</a> — written Markov, Chebyshev and Chernoff-Hoeffding reasoning. A second pass through moment assumptions and factorization; its deviation notation differs from this lesson’s failure budget δ.</li>
      <li><a href="https://www.stat.berkeley.edu/~bartlett/courses/2014spring-cs281bstat241b/lectures/04-notes.pdf" target="_blank" rel="noreferrer">Peter Bartlett’s concentration lecture</a> — deeper MGF, bounded-curvature and sub-Gaussian arguments, plus finite-family guarantees. Relevant pages 4, 8–20 and 23–25 were inspected; derivatives support the lemma proof.</li>
      <li><a href="https://www.stat.berkeley.edu/~stark/Teach/S240/Notes/ch1.pdf" target="_blank" rel="noreferrer">Philip Stark’s STAT240 notes, §1.17</a> — bounded Bernstein and the convex-function comparison for sampling without replacement. This lesson uses explicit total variance v to avoid a notation inconsistency in the source.</li>
      <li><a href="https://arxiv.org/html/1810.08240" target="_blank" rel="noreferrer">Howard et al., Time-uniform confidence sequences</a> — advanced simultaneous coverage and stopping. The definition, introduction and stopping equivalence were inspected. Our elementary summable-budget boundary is not an implementation of the paper’s sharper mixture or stitching methods.</li>
    </Sources>
  </div>
};
