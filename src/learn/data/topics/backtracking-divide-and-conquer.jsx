import { Code, CodeBlock, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { SubsetChoicesLab, QueensSearchLab, SubarraySummaryLab, InversionBoundaryFigure } from '../../components/lesson-labs/BacktrackingDivideLabs';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { backtrackingDivideExamples } from '../backtracking-divide-examples.js';
import backtrackingPractice from '../practice/backtracking-divide-and-conquer.js';

export default {
  title: 'Backtracking & Divide-and-Conquer',
  readTime: '~50 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot backtracking-divide-lesson">
    <LessonIntro prerequisites="Recursion, call frames, progress measures and cost models from Complexity Analysis & Recursion; ordered merging from Binary Search, Sorting & Two-Pointer Patterns. Earlier lists, sets, dictionaries and graph paths supply the state containers. No bit tricks or advanced combinatorics are required."
      sections={[["1-two-reasons-to-branch", "Choose the kind of decomposition"], ["2-choose-explore-and-restore", "Build a search without losing state"], ["3-prune-only-with-a-proof", "Reject impossible futures"], ["4-change-the-choice-and-identity-rules", "Permutations and combinations"], ["5-constraints-can-live-on-a-board", "Queens and path-local search"], ["6-divide-the-input-and-count-across-the-boundary", "Combine inversion counts"], ["7-return-enough-information-to-combine", "Build richer summaries"], ["8-practise-a-new-contract", "Independent practice"], ["guided-dsa-practice", "Guided LeetCode practice"]]}>
      Design two useful kinds of recursive algorithms. Backtracking explores candidate choices and restores state before trying another branch. Divide-and-conquer solves smaller input regions and combines their answers. Learn how to define the state, prove completeness, prune safely, and return exactly the information the caller needs.
    </LessonIntro>

    <H2>1. Two reasons to branch</H2>
    <Prose>Suppose a small device can enable a red indicator, a blue indicator, both or neither. You want to list every configuration. At the red decision, there are two possible futures: include red or exclude it. Each future still has a blue decision. The branches represent <strong>alternative candidate answers</strong>.</Prose>
    <Prose>Now suppose you want to count out-of-order pairs in a sequence of readings. Split the sequence into two contiguous halves. Solve the left half and the right half, then count pairs that cross the split. Here the branches represent <strong>pieces of one input whose results are combined</strong>. You need both answers even if the first one looks good.</Prose>
    <LessonTable caption="Recursion is the control mechanism; the design question differs" headers={["Approach", "What does a child mean?", "What does the parent do afterward?"]} rows={[
      ["Backtracking", "A candidate extension of the current partial answer", "Restore the prior state, then explore another choice; possibly stop after a witness"],
      ["Divide-and-conquer", "A smaller subproblem under a stated return contract", "Combine returned information into the answer for its own region"],
      ["Binary search, recalled", "The one remaining region that can contain the answer", "Continue in that region; discarded regions need not be solved"],
    ]} />
    <Prose>The terminology is not a rigid taxonomy: binary search is often included in the broader divide-and-conquer family, and a search algorithm can also use a divide-and-conquer subroutine. The useful distinction is the reasoning you must supply. What is one state or subproblem? Which children are required? What information survives a return? Why is no needed answer lost?</Prose>
    <Prose>The previous recursion lesson already explained waiting frames and base cases. Here we will use that machinery to design algorithms. Python 3 and its standard library are sufficient for every complete program. Save an example as <Code>search_design.py</Code> and run <Code>python search_design.py</Code>. The browser investigations use small bounded inputs so individual state changes remain inspectable.</Prose>

    <H2>2. Choose, explore and restore</H2>
    <H3>A subset is a sequence of yes/no decisions</H3>
    <Prose>For a list of n distinct items, deciding whether to include each item defines one subset. A call <Code>search(index)</Code> means: “All earlier positions have been decided; explore both possibilities for the remaining positions.” A shared <Code>path</Code> list contains exactly the selected earlier items. When index equals n, there are no decisions left, so copy that path into the answer collection.</Prose>
    <Prose>To explore inclusion, append the current item, recurse on the next index, then pop the item. The pop <strong>undoes</strong> this call's choice. Now the same path can explore exclusion. This pattern is often called <strong>choose, explore, unchoose</strong>. Backtracking means returning to an earlier decision with its state restored so a different continuation can be tried.</Prose>
    <RunnableExample example={backtrackingDivideExamples.subsets}><Prose>The ordering is include-first: red+blue, red, blue, empty. A different branch order can produce the same valid answer set in a different order. There is exactly one subset of an empty set—the empty subset—so the result for [] is [[]], not []. This differs from reporting “there are no valid answers.”</Prose></RunnableExample>
    <Prose><strong>Copy the answer, not the reference to the working list.</strong> If the leaf uses <Code>answers.append(path)</Code>, every answer refers to the same mutable object. Later pop operations change what all those entries appear to contain; when the search ends they may all look empty. <Code>path.copy()</Code> preserves each answer's current sequence. It is a shallow copy: if the elements themselves are mutable objects, their payloads are still shared unless another copying policy is specified.</Prose>
    <H3>The proof follows the choice tree</H3>
    <Prose>At entry to search(i), the path describes selected positions strictly before i. The include branch explores every subset containing position i; the exclude branch explores every subset omitting it. These cases do not overlap, and together they cover every possible answer. By applying the same argument to the remaining positions, each subset is reached once. Each child increases i, so the nonnegative measure n−i decreases until the base case. The append/pop pair restores the entry path before the call returns.</Prose>
    <Prose>There are 2ⁿ leaves, since each of n positions has two choices. For n ≥ 1, explicitly copying every subset costs Θ(n2ⁿ) total element references: across all subsets, each item occurs in exactly half of them. The result list also stores 2ⁿ list objects. Auxiliary path and recursive stack use O(n) space, excluding returned answers. Small code is not the same as small output.</Prose>
    <Prose>The code treats input positions as choices. If values repeat, two different position subsets can produce identical value lists. That is correct only when occurrences are distinct under the task's contract. “All subsets of distinct values,” “all subsets of positions” and “all unique value-multisets” are different requests. We will handle duplicate-valued permutations explicitly rather than hiding duplicate output with an unexplained final set.</Prose>
    <Checkpoint prompt="Why is an undo after a recursive call different from clearing the whole path? What goes wrong if you pop before the recursive call?">
      <Prose>This call owns only the item it appended; older items belong to earlier choices still being explored. Clearing the path erases that prefix. Popping before the child call erases the current choice before its consequences are explored. Undo should restore exactly the state that existed before this call's choice.</Prose>
    </Checkpoint>

    <H2>3. Prune only with a proof</H2>
    <Prose>Now attach positive costs [2,4,5] to three items and ask for subsets totaling 5. Plain enumeration lists all eight subsets and checks their sums. During search, carry a running sum instead of recalculating it at each leaf. If the sum already exceeds 5, adding more positive values can never repair it. The entire subtree is impossible and can be skipped: this is <strong>pruning</strong>.</Prose>
    <Prose>There is another safe rejection. Precompute the sum of values remaining at each index. If current_sum + remaining_sum is below the target, even taking everything left is insufficient. This rejects a whole family of completions using one bound. Both rules need the positive-value contract; a negative value could bring an overshoot back down.</Prose>
    <SubsetChoicesLab />
    <Prose>With [2,4,5] and target 5, the branch taking 2 then 4 reaches sum 6 before deciding about 5. Pruning removes both last-choice completions. The entered-call count falls from 15 to 13, while the sole answer remains position [2], value [5]. These are exact counts for this input and this instrumentation, not an asymptotic speedup theorem. On another input the bounds may reject little or nothing.</Prose>
    <RunnableExample example={backtrackingDivideExamples.targetSubsets}><Prose>The result stores positions so repeated equal values remain distinguishable. Target zero has one solution, the empty subset, under strictly positive values. The function rejects zero/negative values rather than silently applying an unsound or differently interpreted pruning rule. The suffix sums cost O(n) to prepare.</Prose></RunnableExample>
    <H3>What is preserved when a branch is cut?</H3>
    <Prose>A pruning test must establish that <em>every completion</em> of this partial state fails the required condition. “This looks unpromising” is not enough. The unpruned include/exclude tree establishes coverage; removing only impossible subtrees preserves all valid answers. A heuristic such as trying the smallest candidate first can change when an answer is found, but does not justify discarding other branches.</Prose>
    <Prose>Worst-case search still explores exponentially many states. A safe upper bound is O(2ⁿ + nK) arithmetic/list work after O(n) preprocessing, where K is the number of copied answers and nK bounds their total length. This can also be written O(n2ⁿ) when output is large. The O(n) active path/stack bound excludes answers. The browser's snapshots and full tiny tree are additional diagnostic storage; the actual Python search does not retain that whole tree.</Prose>
    <Prose>With signed values, the unpruned finite include/exclude algorithm still terminates because index increases. But its safe pruning bounds need redesign: for example, compute the smallest and largest sum obtainable from the remaining values by taking all negative or all positive values respectively. The interval may be loose, yet rejecting a target outside it is safe. A correct weak bound is preferable to a fast rule that erases real solutions.</Prose>
    <Checkpoint prompt="The partial sum is 7, target is 5, and an undecided item has value −2. Is overshoot pruning valid? Does sorting the candidates make it valid?">
      <Prose>No. Taking −2 reaches 5. Sorting changes exploration order, not the existence of that completion. The proof must use the actual remaining-value constraints; it cannot be replaced by a sorting step.</Prose>
    </Checkpoint>

    <H2>4. Change the choice and identity rules</H2>
    <H3>Permutations: each next position needs an unused item</H3>
    <Prose>A subset decides whether items participate; a permutation decides their order. For n distinct items, the first output position has n choices, the next n−1, and so on. Their product is n!, read “n factorial.” A direct algorithm tracks which input positions are already used, chooses an unused position, recurses, then restores its used flag. Copy a path only when its length reaches n.</Prose>
    <Prose>Repeated values complicate that position-based search. With [1,1,2], treating the two ones as separately labeled produces different decision paths that spell the same permutation. If the task wants unique value sequences, keep a <strong>remaining frequency</strong> for each distinct value. Choosing a 1 decreases its count; it does not choose between two indistinguishable copies.</Prose>
    <RunnableExample example={backtrackingDivideExamples.permutations}><Prose>The frequency table avoids duplicate branches at their source. Its invariant is “remaining counts plus path counts equal the original counts.” A complete path therefore uses exactly the required multiplicities. Conversely, any valid next value with a positive remaining count is considered, so no unique permutation is omitted. Sorting the distinct keys makes output order deterministic; uniqueness does not depend on that order.</Prose></RunnableExample>
    <Prose>If multiplicities are c₁, c₂, …, the number of distinct full permutations is P = n!/(c₁!c₂!…). Copying them costs Θ(nP) output work for n ≥ 1. This particular implementation additionally scans u distinct keys at every non-leaf call, including keys whose remaining count is zero. Its precise search accounting is O(uS + nP), where S is the number of entered non-leaf states, plus input counting/key sorting. Do not quote only the output bound as the entire cost of every implementation. Active state is O(n + u), excluding output.</Prose>
    <H3>Combinations with reuse need an ordering policy and a progress measure</H3>
    <Prose>Suppose positive denominations [2,3,5] may each be used any number of times to total 8. The answer [2,3,3] should appear once; [3,2,3] is the same combination. Sort the distinct candidates and let search(start, remaining) use only candidate indices at least start. A chosen candidate can be reused by recurring with its own index. Every output is nondecreasing, so permutations of the same combination are never generated.</Prose>
    <RunnableExample example={backtrackingDivideExamples.combinations}><Prose>Changing <Code>search(index, ...)</Code> to <Code>search(index + 1, ...)</Code> permits each candidate at most once. That is a different contract, not an interchangeable optimization. The sorted order makes a break safe when a candidate exceeds remaining: every later candidate is at least as large.</Prose></RunnableExample>
    <Prose>Termination now depends on remaining decreasing, not on index increasing. Because every candidate is positive, each choice reduces remaining by at least the smallest candidate c. Maximum path length is floor(target/c) when candidates are nonempty. Allowing unlimited zero would fail to decrease it; positive and negative values together can create infinitely many representations. The validation deliberately excludes both cases.</Prose>
    <Prose>For u candidates and maximum depth d, a coarse worst-case upper bound counts a u-branching tree through depth d, plus copying all returned combinations. A sharper bound depends on which sums and nondecreasing paths are actually reached. State the visited-state and output costs instead of calling the procedure polynomial merely because target is a numeric argument. If target is encoded in binary, its magnitude can be much larger than its bit length.</Prose>
    <Checkpoint prompt="You may use each input occurrence once, values may repeat, and the output must contain unique combinations. Is the current reusable-candidate function already correct for that task?">
      <Prose>No. Reuse must stop after a position is chosen, while equal-valued alternatives at the same decision depth need deduplication. One approach sorts occurrences, recurs from index+1 and skips equal candidates at the same depth, while still allowing a later equal occurrence after choosing the earlier one. Explain both rules; dropping duplicates from the input would lose valid combinations requiring multiple equal occurrences.</Prose>
    </Checkpoint>

    <H2>5. Constraints can live on a board</H2>
    <H3>N-Queens: make the rejection visible</H3>
    <Prose>Place n queens on an n×n board so no two share a row, column or diagonal. Choose one row at a time; this handles the row rule by construction. The path stores the selected column for each previous row. For candidate (r,c), reject if its column is already occupied, if r−c matches an occupied descending diagonal, or if r+c matches an occupied ascending diagonal.</Prose>
    <Prose>Why those diagonal keys? Moving one row down and one column right adds one to both coordinates, leaving r−c unchanged. Moving one row down and one column left keeps r+c unchanged. Equal keys therefore identify a shared diagonal without scanning every square. The board investigation draws the actual attacking relationship so these expressions have a geometric meaning.</Prose>
    <QueensSearchLab />
    <RunnableExample example={backtrackingDivideExamples.queens}><Prose>The two four-queen answers are column sequences [1,3,0,2] and [2,0,3,1]. They are mirror images but distinct board placements; this solver does not identify symmetric boards as one solution. No solution exists for n=2. The local API allows n=0 and returns one empty placement; the external N-Queens practice statement starts at n=1.</Prose></RunnableExample>
    <Prose>At entry to row r, exactly r mutually nonattacking queens occupy rows 0 through r−1, and the three sets describe precisely those queens. A permitted placement preserves the invariant. If row reaches n, the board is complete and valid. Every valid solution chooses one allowed column in each row, so it is reached by some branch. Every return removes exactly the column and diagonal keys just added, restoring the parent state. Depth is at most n.</Prose>
    <Prose>Column uniqueness bounds the number of partial placements by prefixes of permutations, and diagonals prune more. This code still loops over all n columns at each non-leaf state. Under expected constant-time set operations, a safe bound is O(n·n! + nK) time for n ≥ 1, where K is the number of copied solutions; it is intentionally loose. O(n) auxiliary state excludes output. Quoting O(n!) without accounting for the candidate loop, or treating a five-row visualization as a runtime benchmark, hides part of this implementation's work.</Prose>
    <H3>Word search needs membership local to the current path</H3>
    <Prose>A letter grid creates another choice tree. Starting at a matching cell, select a side-adjacent cell for the next letter; the same cell cannot be reused within one word. The state needs the current cell, the next character position, and the cells used on this candidate path. A global “visited once” set from ordinary reachability is insufficient: another prefix may need the same cell in a different candidate route.</Prose>
    <RunnableExample example={backtrackingDivideExamples.wordSearch}><Prose>The returned witness uses coordinates (0,0)→(0,1)→(1,1) for ABA. Extending it to ABAB would revisit the only B cell, (0,1), so that query fails. None means no witness; [] is a valid empty-word witness. Checking <Code>if answer</Code> would confuse those two cases. The function leaves the caller's board untouched.</Prose></RunnableExample>
    <Prose>The <Code>finally</Code> block restores path and used even when a deeper call finds a word and returns early. That matters whenever temporary state survives outside a recursive frame. An alternative can mark cells in the board and restore them, but its mutation contract and cleanup on every exit must be equally clear. If you deliberately leave a solved board in place, document that different behavior.</Prose>
    <Prose>For R×C cells and a word of length L≥1, a coarse upper bound is O(RC·4ᴸ) search work; after the first step, immediate reuse forbids at least the previous cell, giving a tighter branching argument in many formulations. Auxiliary path/used/stack size is O(min(L,RC)), and rectangular-input validation costs O(R). This solver seeks one witness and returns early; enumerating every word path changes the output size and stopping rule.</Prose>
    <H3>Decide what the search is supposed to return</H3>
    <LessonTable caption="Related searches need different stopping and state policies" headers={["Goal", "At a valid completion", "Extra reasoning"]} rows={[
      ["Existence / one witness", "Return True or a copied witness and stop", "Still restore temporary state on the successful unwind"],
      ["Count solutions", "Increment a counter and continue", "Do not need to retain all complete outputs"],
      ["Enumerate solutions", "Copy/yield the answer and continue", "Charge total output length and avoid duplicate paths"],
      ["Optimize a score", "Update the best known answer", "A bound must prove a branch cannot improve the incumbent"],
    ]} />
    <Prose><strong>Branch-and-bound</strong> uses a best complete answer already found—the incumbent—and a proven bound on how good a partial branch could become. For minimization, a lower bound at least as large as the incumbent cannot improve its value. If every tied optimum must be enumerated, pruning equality may be wrong. This differs from feasibility pruning, which proves no completion is valid at all.</Prose>
    <Prose><strong>Memoization</strong> reuses a result when different paths reach the same complete subproblem state. The key must include every fact that affects allowed future choices. For a word path, (cell,index) alone omits which cells are already used. For reusable combinations, (start,remaining) captures the remaining search contract, but caching full outputs has different costs from caching a Boolean or a count. <a href="/learn/topic/dynamic-programming-states-transitions-optimization">Dynamic Programming: States, Transitions & Optimization</a> develops this distinction next in the algorithmic sequence after Greedy.</Prose>
    <Prose>Bitmasks can compactly encode a finite used-set, but that changes representation rather than proving the search correct. This lesson uses explicit sets first. Before adopting mask-based search or DP, learn membership bits, bitwise versus Boolean operations, width/sign/shift semantics and the state key's meaning. An unexplained XOR or shift trick should not replace the constraint invariant.</Prose>

    <H2>6. Divide the input and count across the boundary</H2>
    <Prose>An <strong>inversion</strong> is a pair of original indices i &lt; j with values[i] &gt; values[j]. It measures a kind of disagreement between input order and value order. In [3,1,2,1], the four inversions are index pairs (0,1), (0,2), (0,3) and (2,3). Equal values are not strict inversions.</Prose>
    <Prose>A correct baseline checks every pair in Θ(n²) time. To improve it, split the contiguous region in half. Every inversion is wholly left, wholly right or crosses from left to right. Those categories are disjoint and exhaustive. Recursive calls count the first two; a modified merge counts the crossing pairs.</Prose>
    <InversionBoundaryFigure />
    <Prose>Ask each recursive call for two results: its region must become sorted, and it must return that region's inversion count. When merging, if the left head is at most the right head, emit the left head; it creates no inversion with the remaining right values. If the right head is smaller, it is smaller than <em>every remaining left value</em>, because the left half is sorted. Add middle−left_index, emit the right head, and continue.</Prose>
    <RunnableExample example={backtrackingDivideExamples.inversions}><Prose>The code copies the caller's values once, uses one reusable buffer, and works with half-open index ranges [low,high). The sorted output is part of the return contract, so comparisons at the next level are justified. Using &lt; instead of &lt;= when deciding to emit from the left would incorrectly count equal values in the right branch.</Prose></RunnableExample>
    <Prose>Each crossing pair is counted exactly when its right element is emitted ahead of its still-unmerged left partner. Pairs already inside a half were counted by that half's recursive call and are never counted as crossing at this split. Base regions of length zero or one have no pair. Both halves are strictly shorter for a longer region, so recursion terminates.</Prose>
    <Prose>The merge and copy-back cost Θ(n) for a region of n elements. The recurrence is T(n)=T(floor(n/2))+T(ceil(n/2))+Θ(n), giving Θ(n log n) for n≥2: each level processes a total of n elements across Θ(log n) levels. Working arrays require Θ(n) storage; recursive depth is O(log(n+1)). The previous sorting lesson supplies the merge mechanics; the new idea is strengthening its returned information and partitioning the pairs being counted.</Prose>
    <Prose>A useful application compares two rankings of the same distinct items. Translate one ranking into positions in the other, then count inversions: each inversion is a pair whose order disagrees. Tied ranks need an explicit policy or a different statistic; do not treat equal scores as distinct ordered preferences by accident. Inversions are also the minimum number of adjacent swaps needed to sort a sequence under the corresponding total-order/strict-inversion convention: one adjacent inversion swap removes one inversion, while each adjacent swap changes the count by at most one.</Prose>
    <Checkpoint prompt="Both halves contain a value 4. Why does emitting the left 4 first matter for strict inversion counting? Is sorting each half enough to finish the whole task?">
      <Prose>Equal values do not satisfy greater-than, so a left-first tie avoids entering the branch that adds inversions. Sorting/counting inside each half alone misses every crossing pair. Divide-and-conquer is complete only after proving that the combine step accounts for those interactions exactly once.</Prose>
    </Checkpoint>

    <H2>7. Return enough information to combine</H2>
    <H3>The best answer may straddle the split</H3>
    <Prose>A <strong>subarray</strong> is a contiguous interval, not an arbitrary subset. Find the largest sum of a nonempty subarray in [-2,4,-1,3,-5,2]. The answer is 6 from [4,-1,3]. Split after the first three values: the left half's best sum is 4 and the right half's best is 3. Taking max(4,3) misses the answer crossing the boundary.</Prose>
    <Prose>A crossing subarray consists of a suffix of the left half followed by a prefix of the right half. A prefix starts at its region's first position; a suffix ends at its last position. The left suffix [4,−1] sums to 3, and the right prefix [3] sums to 3, giving 6 together. Returning only “best anywhere” from each child loses exactly the boundary information the parent needs.</Prose>
    <SubarraySummaryLab />
    <H3>Strengthen the return contract</H3>
    <Prose>For each nonempty region, return four values: total sum, maximum nonempty prefix sum, maximum nonempty suffix sum, and maximum nonempty subarray sum anywhere. The parent's total is left.total + right.total. Its best prefix either stays in the left half or includes the whole left half plus a right prefix. Its best suffix is the symmetric case. Its best subarray lies left, lies right, or crosses.</Prose>
    <LessonTable caption="Every candidate is justified by where an interval can lie" headers={["Returned field", "Combine rule", "Why the cases cover the answer"]} rows={[
      ["total", "L.total + R.total", "The two regions partition the input"],
      ["prefix", "max(L.prefix, L.total + R.prefix)", "A prefix stops on the left or crosses using all of the left"],
      ["suffix", "max(R.suffix, L.suffix + R.total)", "A suffix starts on the right or crosses using all of the right"],
      ["best", "max(L.best, R.best, L.suffix + R.prefix)", "An interval stays in one child or touches both"],
    ]} />
    <RunnableExample example={backtrackingDivideExamples.summary}><Prose>At a singleton with value x, all four fields equal x. For [-8,−3,−6], the best nonempty answer is −3, not zero. Initializing best to zero would silently allow an empty answer and change the problem. This API rejects empty input; another API could permit empty subarrays, but would need different base cases and stated output semantics.</Prose></RunnableExample>
    <Prose>The proof is structural induction: leaves have correct summaries, and the exhaustive interval cases show that combining two correct child summaries produces a correct parent summary. The children are strictly shorter, so the induction and termination follow the same decreasing interval length. The native code returns only sums; the browser also carries witness intervals and resolves equal sums by earliest start then earliest end so highlights are deterministic.</Prose>
    <Prose>A simpler divide-and-conquer version scans outward from the split to find crossing prefix/suffix sums at every level. That costs Θ(n log n). Returning the richer summary makes each combine constant arithmetic work, yielding T(n)=T(floor(n/2)) + T(ceil(n/2)) + Θ(1)=Θ(n). For n input elements there are n leaves and n−1 combine nodes. The sequential implementation has O(log(n+1)) active frames with constant-sized summaries and O(1) returned fields. Retaining the full tree for the lab adds O(n) diagnostic storage.</Prose>
    <Prose>These bounds count fixed-size arithmetic/comparisons. Python exact integers may grow with the input's magnitude, so bit costs differ. No slices are constructed by the summary function. The preceding complexity lesson explains why hidden copies and large numbers must be charged separately. A linear scan such as Kadane's algorithm is another approach; the point here is that a stronger child contract can remove repeated work from a divide-and-conquer design.</Prose>
    <H3>A reusable boundary summary is useful beyond one recursion</H3>
    <Prose>Imagine a long signed signal split across files or workers. Each ordered chunk can produce these four values, then neighboring summaries combine without shipping every sample to the final stage. The operation preserves sequence order: swapping left and right can change prefixes, suffixes and the answer, so it is not an unordered reduction. Under exact arithmetic, grouping adjacent chunks differently produces the same semantic summary; floating-point rounding adds a separate reproducibility question.</Prose>
    <Prose>The same summary can be stored in nodes of a <a href="/learn/topic/segment-trees-fenwick-trees-range-queries">segment tree</a> so updates recompute only affected ancestors. That later topic adds query decomposition and update mechanics. Ordinary recursive Python code runs its children sequentially; potential parallel independence does not by itself provide a parallel speedup, remove communication cost or override task-granularity limits.</Prose>
    <Checkpoint prompt="Why is a child's total required if the final caller asks only for the best sum? Can you drop it and still combine prefixes correctly?">
      <Prose>No. A parent prefix that crosses the split must include every value in the left region, including negative values. L.best is not a substitute for L.total because it may omit some of that region. The intermediate return contract can be richer than the final requested answer; that extra information is what permits a correct constant-work combine.</Prose>
    </Checkpoint>

    <H2>8. Practise a new contract</H2>
    <H3>Exercise A · enumerate balanced parentheses</H3>
    <Prose>For n pairs, generate each balanced parenthesis string once. At any prefix, the number closed must not exceed the number opened, and neither can exceed n. Derive allowed next characters from this invariant rather than generating every length-2n string and filtering at the end. Include n=0.</Prose>
    <details className="lesson-deeper"><summary>Hint for parenthesis prefixes</summary><Prose>An opening parenthesis is allowed while some opening quota remains. A closing parenthesis needs an unmatched opening already in the prefix. The remaining length gives a decreasing progress measure.</Prose></details>
    <Checkpoint prompt="Explain soundness, completeness, restoration and the cost in terms of explored prefixes and output.">
      <Prose>Maintain 0≤closed≤opened≤n. Add '(' only if opened&lt;n and ')' only if closed&lt;opened. A state with closed=n necessarily has opened=n, so it is balanced and complete. Every valid string's next character satisfies those rules, so no valid string is pruned. Each append is followed by a pop, and each step consumes one of the remaining 2n positions. Time is O(S+nK), where S is explored prefix work and K is the number of outputs; O(n) auxiliary state excludes the Θ(nK) output characters for n≥1. The combinatorics topic studies the resulting Catalan counts; you do not need to memorize them to write a correct solver.</Prose>
      <RunnableExample example={backtrackingDivideExamples.parentheses} />
    </Checkpoint>
    <H3>Exercise B · return a maximum-subarray witness</H3>
    <Prose>Extend the four-field Python summary to return the winning interval as well as its sum. Use half-open coordinates, require nonempty answers and choose earliest start then earliest end on ties. Test [-2,4,-1,3,-5,2], an all-negative input and [0,0].</Prose>
    <details className="lesson-deeper"><summary>Hint for witness intervals</summary><Prose>Every candidate already comes from either a child interval or concatenating a left suffix with a right prefix. Carry each candidate's start and end along with its sum; use one tie comparator everywhere.</Prose></details>
    <Checkpoint prompt="Give expected witnesses and justify that the richer return value still has constant size per field.">
      <Prose>The first result is sum 6 on [1,4). For [-8,−3,−6], choose −3 on [1,2). For [0,0], choose 0 on [0,1). A crossing candidate starts where the left suffix starts and ends where the right prefix ends. Total still needs only its sum; each other field is a constant-size (sum,start,end) record. Apply the same comparator to prefix, suffix and best candidates. Brute-force every nonempty interval for small arrays to verify sums and ties independently; the browser model provides a worked implementation to inspect afterward.</Prose>
    </Checkpoint>
    <H3>Exercise C · count disagreements without counting ties</H3>
    <Prose>Two reviewers rank the same four distinct items: first [A,B,C,D], second [B,D,A,C]. Convert the second ranking to first-ranking positions and count disagreements. Then decide what must change if reviewers are allowed to tie items.</Prose>
    <details className="lesson-deeper"><summary>Hint for ranking disagreement</summary><Prose>Replace each item with its position in the first ranking. An out-of-order pair in this new list corresponds to one pair on which the reviewers disagree.</Prose></details>
    <Checkpoint prompt="Show the transformed sequence and counted pairs, then state the boundary of the reduction.">
      <Prose>The transformed sequence is [1,3,0,2], with three inversions: values (1,0), (3,0) and (3,2). These correspond to the pairs B/A, D/A and D/C. Every item must occur exactly once in both total rankings for this direct mapping. Ties require a definition of disagreement and normalization before applying or adapting the counting method; breaking them arbitrarily creates preferences the reviewer did not express.</Prose>
    </Checkpoint>
    <H3>Exercise D · construct a bad memoization key</H3>
    <Prose>For a path that cannot reuse cells, explain why the same (cell,word_index) may admit different futures when reached through different used-cell sets. Draw a small cyclic grid and two partial paths. Then propose a complete key for a Boolean “can this state finish?” cache and explain its potential size.</Prose>
    <details className="lesson-deeper"><summary>Hint for cached path search</summary><Prose>Let the next required letter lie in a neighboring cell used by one prefix but not the other. The visible endpoint and character index agree, yet that move is legal in only one state.</Prose></details>
    <Checkpoint prompt="Explain the necessary state and why adding a cache does not guarantee a small algorithm.">
      <Prose>For a concrete counterexample, use grid rows [A,B,X] and [B,A,Y] with the word ABABX. Both prefixes (0,0)→(0,1)→(1,1) and (0,0)→(1,0)→(1,1) spell ABA and end at the same cell and word index. The first prefix has already used the upper B, so it cannot finish with B→X; its other B neighbor has no X neighbor. The second prefix can finish through (0,1)→(0,2). Thus one state fails and the other succeeds despite sharing (cell,index).</Prose>
      <Prose>The key must distinguish the used-cell subset as well as the current cell and word position, for example (cell,index,frozenset(used)). Two states with different forbidden cells cannot generally share a cached result. For N cells there can be up to 2ᴺ subsets, although a particular word/search reaches far fewer. Building and hashing a frozenset also costs work. Memoization helps when complete states repeat enough to justify the storage; it does not turn every backtracking tree into polynomial time. A later bitmask representation can make a bounded subset key compact, but cannot eliminate the number of possible subsets.</Prose>
    </Checkpoint>
    <H3>Ready to continue</H3>
    <Prose>For a new recursive problem, you should be able to name the state/return contract, identify a decreasing measure, prove that the children cover the required cases, and explain how state is restored or results are combined. Demonstrate an invalid pruning rule with a counterexample, separate temporary storage from output, and compare your optimized code with an independent brute-force oracle on small cases.</Prose>
    <DsaPractice practice={backtrackingPractice} />
    <Prose>The next topic in the module is <a href="/learn/topic/greedy-algorithms-exchange-arguments">Greedy Algorithms & Exchange Arguments</a>. Backtracking keeps alternatives available; a greedy algorithm commits to a choice. The next lesson asks what proof can make that commitment safe. Dynamic programming then revisits repeated subproblem states.</Prose>

    <Sources alternatives={<><p><a href="https://see.stanford.edu/Course/CS106B/147" target="_blank" rel="noopener noreferrer">Stanford CS106B · Lecture 10</a> offers a video route through permutations, duplicate choices, subsets and N-Queens. The official page and <a href="https://see.stanford.edu/materials/icspacs106b/transcripts/ProgrammingAbstractions-Lecture10.html" target="_blank" rel="noopener noreferrer">transcript</a> were reviewed on 10 September 2026; video playback is not claimed as watched. The C++ examples use different container conventions, so map each choice and restoration to the Python state here.</p><p><a href="https://web.stanford.edu/class/archive/cs/cs106b/cs106b.1196/notes/lecture11.html" target="_blank" rel="noopener noreferrer">Stanford · written backtracking notes</a> are useful for revisiting the choose/explore/unchoose reasoning. <a href="https://www.cs.cmu.edu/afs/cs/academic/class/15210-f14/www/lectures/dandc.pdf" target="_blank" rel="noopener noreferrer">CMU 15-210 · divide-and-conquer notes</a> provide a deeper treatment of maximum contiguous sums and richer returned summaries, including a work/span model beyond this lesson's sequential Python implementation.</p></>}>
      <li><a href="https://web.stanford.edu/class/archive/cs/cs161/cs161.1168/lecture3.pdf" target="_blank" rel="noopener noreferrer">Stanford CS161 · recurrences and divide-and-conquer</a> — a written reference for the split/combine approach and recurrence arguments; distinguish its crossing-scan implementation from the constant-size summary optimization here.</li>
      <li><a href="https://www.cs.cmu.edu/afs/cs/academic/class/15210-f14/www/lectures/dandc.pdf" target="_blank" rel="noopener noreferrer">CMU divide-and-conquer chapter</a> — primary teaching reference for total/prefix/suffix/best summaries. Its sequence-operation cost model differs from direct Python index access; costs must be transferred with that distinction intact.</li>
    </Sources>
  </div>,
};
