import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { AssignmentHistoryFigure, CompactionInvariantLab, DescentFigure, EuclidTerminationLab, PartitionInvariantLab, ProofBoundaryFigure, SearchInvariantLab, SortedContractFigure } from '../../components/lesson-labs/AlgorithmCorrectnessLabs.jsx';
import { algorithmCorrectnessExamples } from '../algorithm-correctness-examples.js';
import correctnessPractice from '../practice/algorithm-correctness-loop-invariants-termination.js';
function Exercise({
  title,
  children,
  hint,
  solution
}) {
  return <section className="correctness-practice">
    <H3>{title}</H3>{children}
    <details><summary>Hint: start the argument</summary><Prose>{hint}</Prose></details>
    <details><summary>Solution and reasoning</summary>{solution}</details>
  </section>;
}
export default {
  title: 'Algorithm Correctness, Loop Invariants & Termination',
  readTime: '~55 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot correctness-lesson">
    <LessonIntro prerequisites="Read Python assignments, lists, if/while, functions and returns. Earlier search, recursion, Greedy and DP lessons have already used small correctness arguments; we connect them here. We introduce the needed logic and induction locally, so a later formal proof-techniques page is not required before this core route." sections={[['1-decide-what-correct-means', 'State the contract'], ['2-prove-what-the-loop-knows', 'Prove the loop'], ['3-find-and-repair-an-invariant', 'Develop the proof'], ['4-protect-information-during-mutation', 'Safe in-place work'], ['5-classify-every-unknown-occurrence', 'Four-region partition'], ['6-prove-the-computation-will-stop', 'Termination'], ['7-justify-recursive-and-algebraic-steps', 'Recursion and conserved work'], ['8-connect-the-local-proofs', 'Compose arguments'], ['9-test-the-argument-and-its-assumptions', 'Evidence and limits'], ['10-build-a-proof-for-a-new-contract', 'Independent practice'], ['guided-dsa-practice', 'Guided LeetCode practice']]}>
      A program can pass its examples and still skip a case. It can also stop reliably while returning the wrong answer. Learn to explain what each state means, why every allowed step preserves that meaning, and why the computation must eventually produce the promised result.
    </LessonIntro>

    <H2>1. Decide what correct means</H2>
    <Prose>Imagine a list of event codes [4,9,2,9]. You need the position of the <em>first</em> 9 so a report starts at the earliest matching event. Returning 3 finds a match but fails the request. Returning 1 succeeds. If no 9 exists, return −1. A claim such as “the function searches the list” is too vague to distinguish these outcomes.</Prose>
    <Prose>A <strong>specification</strong> states the actual problem. Its <strong>precondition</strong> says which starting states are permitted; its <strong>postcondition</strong> says what must be true on return. These are conditions about values and relationships, not necessarily lines of executable validation. A caller must establish the precondition, or the implementation must explicitly handle its violation under a broader contract.</Prose>
    <LessonTable caption="The first-occurrence contract" headers={['Part', 'Precise requirement', 'Why it matters']} rows={[['Input', 'A finite list of integers and an integer target; no other code changes the list during the scan.', 'Comparisons and the length refer to one stable input.'], ['Successful result r', '0 ≤ r < n, a[r] equals the target, and every index k with 0 ≤ k < r has a different value.', 'A later duplicate is not sufficient.'], ['Absent result', 'r = −1 exactly when every input value differs from the target.', 'An empty list must return −1 without indexing it.'], ['Mutation', 'The input list remains unchanged.', 'Returning a correct number would not excuse rearranging event order.'], ['Completion', 'The function returns for every permitted input.', 'A correct answer that is never returned does not meet the task.']]} />
    <Prose>Here n is the list length; indices start at 0. “Every k in a range has property P” is a <strong>universal statement</strong>. An empty range has no counterexample, so it satisfies such a statement. This small logical fact makes the empty prefix a useful starting point. We use [low,high) for indices at least low and strictly less than high; its length is high−low.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.search}><Prose>Each failure to match eliminates one position. The code never assigns into values, so the mutation part is immediate in this sequential setting. The nontrivial part is explaining why the discarded positions cannot hide an earlier match.</Prose></RunnableExample>
    <Checkpoint prompt="If the contract instead asks for any matching position, would returning 3 be wrong on [4,9,2,9]? Would returning −1 be allowed?">
      <Prose>Returning 3 would satisfy the weaker successful-result contract. Returning −1 would still fail because a match exists. Changing the specification changes what a proof must establish; it does not change the meaning of the code you already wrote.</Prose>
    </Checkpoint>

    <H2>2. Prove what the loop knows</H2>
    <Prose>Just before the loop tests <Code>index &lt; len(values)</Code>, divide the list at index. Everything before the boundary has already been rejected; everything at or after it still needs consideration. The useful claim is: <strong>0 ≤ index ≤ n, and no position before index contains the target.</strong> The list still equals the original input.</Prose>
    <Prose>This is a <strong>loop invariant</strong>: a statement preserved at a specified program point. The variable index changes; the relationship describing the processed prefix remains true. We place the claim at the loop boundary before each guard test. It need not hold between every pair of statements inside the body.</Prose>
    <ProofBoundaryFigure />
    <H3>Four obligations, including both exits</H3>
    <Prose><strong>Initialize.</strong> Before the first test, index=0. The bounds hold, and the prefix [0,0) is empty. Therefore it contains no match. The untouched-list clause also holds.</Prose>
    <Prose><strong>Preserve.</strong> Assume the invariant holds and the guard is true. “Assume P, show Q” means proving that whenever P holds, Q follows; it does not assume the desired answer. The bounds and guard give 0≤index&lt;n, so the read is valid. If the current value is not the target, the body advances index by 1. Every previously rejected position remains a nonmatch, and the one newly included position is a nonmatch because of the comparison. The new boundary remains at most n. Thus the invariant is restored on the continuing edge.</Prose>
    <Prose><strong>Justify each exit.</strong> If the current value matches, the invariant excludes every earlier position, so returning index gives the first match. If the guard is false, index≥n; combine this with index≤n to obtain index=n. The rejected prefix is now the entire list, so returning −1 is justified. A proof that mentions only the loop's bottom exit misses the early return.</Prose>
    <Prose><strong>Terminate.</strong> At every continuing step, n−index is a nonnegative integer and drops by 1. There cannot be infinitely many such decreases. The loop either returns on a match or reaches the false guard after at most n comparisons. This gives a termination argument as well as a worst-case linear comparison count.</Prose>
    <SearchInvariantLab />
    <Prose>The first three obligations establish <strong>partial correctness</strong>: if execution returns normally from a permitted start, the promised result holds. Add termination, including termination of operations called by the body, to obtain <strong>total correctness</strong>. Both claims are about the stated computational model. Here finite integer comparisons and list reads finish; a loop waiting on an external service needs additional assumptions.</Prose>
    <Prose>Why does checking one preservation step cover any number of iterations? This is induction on the number of continuing iterations. Initialization handles zero iterations. If the statement holds after k iterations, the preservation argument makes it hold after k+1. No particular value of k was assumed. This general step is what a trace of four values alone cannot establish.</Prose>

    <H2>3. Find and repair an invariant</H2>
    <Prose>Start from the desired result and describe a partially completed version of it. “No match anywhere” becomes “no match in the prefix already examined.” Replace the fixed final boundary n by a moving boundary index, then add the bounds needed to keep operations safe. For a sum, the analogous claim is “total equals the sum of the processed prefix.” For a search interval, it may be “any remaining solution lies in this region.”</Prose>
    <Prose>An invariant can be <strong>too weak</strong>: the bounds 0≤index≤n are easy to preserve, but at index=n they say nothing about the values. They cannot justify−1. Strengthening a claim supplies more useful facts, but those extra facts must themselves be initialized and preserved. “The target is absent from the entire list” is too strong for the permitted input [4,9,2,9]: it is false before the first iteration.</Prose>
    <Prose>The investigation checks hypothetical boundary states allowed by a candidate claim, not only the states observed in the current run. This matters: an inductive preservation proof must work for <em>any state satisfying its assumptions</em>. A weak claim may admit an unreachable bad state; then it is insufficient for that proof even if the algorithm happens to be correct. Add the missing relationship instead of pretending the hypothetical state is impossible without justification.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.assertions}><Prose>The assertion fails as soon as the faulty jump moves past a match. It checks the explanation on this execution. The debug scan rescans/copies a prefix on each boundary, so it can take quadratic work; that overhead is not part of the original linear scan's bound. Python can omit assert statements under optimization, so use explicit validation for conditions that must be enforced in every run.</Prose></RunnableExample>
    <H3>Work backward through an assignment</H3>
    <Prose>Suppose the next instruction is x=x+3 and you want new x≥10 afterward. Replace the future x in that condition by the expression being assigned: old x+3≥10, or old x≥7. This is backward reasoning. For sequential assignments, work backward one instruction at a time; later expressions read values that earlier instructions have already changed.</Prose>
    <AssignmentHistoryFigure />
    <RunnableExample example={algorithmCorrectnessExamples.assignments}><Prose>The temporary preserves the old value that the second assignment needs. Python's tuple assignment evaluates the right-hand side before assigning to the left-hand targets, so it also swaps these two variables. In a larger mutation, aliasing and overlapping array positions still need deliberate reasoning.</Prose></RunnableExample>
    <details><summary>Deeper: a compact notation for local proofs</summary>
      <Prose>A Hoare triple, written {`{P} command {Q}`}, expresses partial correctness: a normal terminating execution starting with P ends with Q. For assignment x=E, establish the condition obtained by replacing the new x in Q with E evaluated in the old state. For a sequence C1;C2, find an intermediate assertion R such that C1 establishes what C2 requires. For an if statement, prove the then branch under P and its guard, and the else branch under P and the negated guard. A loop uses the invariant to connect arbitrarily many such local steps.</Prose>
      <Prose>We use this notation informally, with mathematical integers and explicit memory assumptions. A real proof assistant gives commands a formal semantics and checks rules in that system. Neither annotations in a source file nor green browser indicators mean that a proof assistant has verified this Python program.</Prose>
    </details>
    <Checkpoint prompt="During a prefix-sum loop, total is updated before index advances. Must total equal the sum of values[:index] immediately after the addition?">
      <Prose>No. At that internal point it includes the next value too. The chosen invariant holds again after index advances. You can annotate the internal point with a different condition, but requiring the boundary assertion after every instruction would reject a correct update sequence.</Prose>
    </Checkpoint>

    <H2>4. Protect information during mutation</H2>
    <Prose>Now remove every 0 from a list while retaining the order of the other occurrences. Return the number kept; the prefix of that length is the result. The physical list need not shrink. This is useful when a caller owns a fixed buffer and consumes only the reported prefix. It also makes mutation part of the proof rather than a detail hidden behind the return value.</Prose>
    <Prose>Use read for the next original position to inspect and write for the next output position. Let A₀ mean the original sequence, held fixed in the argument. It is a mathematical reference, not an instruction to copy the list. At each boundary require:</Prose>
    <ul>
      <li>0≤write≤read≤n.</li>
      <li>The current prefix a[0:write] is exactly A₀[0:read] with all 0s removed, including its order and repeated occurrences.</li>
      <li>The unread suffix a[read:n] still equals A₀[read:n]. The intervening gap a[write:read] has no promised contents.</li>
    </ul>
    <Prose>Initially both boundaries are 0, so the result prefix is empty and the unread suffix is the entire original list. If a[read] is 0, reject it and advance read; the retained sequence is unchanged. Otherwise copy it to a[write], advance write, then advance read. Because write≤read, the destination is either the current source itself or a position already processed. It cannot destroy a later unread value. Appending this occurrence extends the retained sequence in its original order.</Prose>
    <CompactionInvariantLab />
    <RunnableExample example={algorithmCorrectnessExamples.compaction}><Prose>When read=n, the retained prefix describes the whole original input. The variant n−read drops by 1 each iteration. Under constant-cost integer comparisons and list accesses, the function uses Θ(n+1) time and O(1) auxiliary machine-word state. The copied slices used to display the result, the visual history and an optional proof-only original copy are separate allocations.</Prose></RunnableExample>
    <Prose>The original-input relationship is a <strong>conservation</strong> condition: no retained occurrence is invented, lost or reordered. The untouched-suffix clause is a <strong>frame condition</strong>: it tells us what a write leaves unaffected. Such conditions are especially useful when equal values hide different identities or a caller retains an alias to the same array.</Prose>
    <H3>A stronger result can need a second proof phase</H3>
    <Prose>“Move zeroes to the end” promises a complete list: retained nonzero values in order, followed by exactly enough zeroes to preserve the length. Compaction alone does not promise that tail. After it returns write, fill a[write:n] with 0. At each fill boundary f, the retained prefix remains fixed and a[write:f] contains only zeroes. Initialization gives an empty filled range; each write extends it; f=n establishes the whole tail. The first loop's postcondition supplies the second loop's precondition.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.zeroFill} />
    <Checkpoint prompt="Can a stable filter write a kept item to read+1 and still use this proof?">
      <Prose>No. That destination may contain an unread original item. The untouched-suffix clause fails, even if a few example lists happen to survive. The proof tells you why direction matters; a backwards merge into spare capacity needs a different suffix invariant, as the guided merge problem asks you to derive.</Prose>
    </Checkpoint>

    <H2>5. Classify every unknown occurrence</H2>
    <Prose>Suppose a buffer contains only category codes 0,1,2 and must group them in that order. A frequency count followed by rewriting is a simple correct baseline for bare numbers. A swap-based method retains each existing occurrence and needs only three boundaries. It is also the three-way partition used when separating less-than, equal-to and greater-than groups around a pivot, after defining the category comparison.</Prose>
    <Prose>Let low, middle and high divide the array into four regions: known 0s in [0,low), known 1s in [low,middle), unknown values in [middle,high), and known 2s in [high,n). Keep 0≤low≤middle≤high≤n and preserve the original multiset of occurrences. An <strong>invariant is a conjunction</strong>: all these clauses must hold together. Sorted regions alone would allow an algorithm that silently deletes an occurrence.</Prose>
    <Prose>Initialize low=middle=0 and high=n. The three known regions are empty; all values are unknown. While middle&lt;high, classify the value at middle:</Prose>
    <LessonTable caption="Derive the update from what the destination contains" headers={['Current category', 'Update', 'Why the regions remain true']} rows={[['0', 'Swap a[low] and a[middle]; advance both low and middle.', 'If low<middle, the displaced occurrence is a known 1. If low=middle, the swap is with itself. The new 0 joins the first region and the known 1 region remains valid.'], ['1', 'Advance middle.', 'The unknown occurrence is now classified as 1; no swap is needed.'], ['2', 'Decrement high; swap a[middle] with a[high]; keep middle fixed.', 'The 2 joins the final region. The incoming occurrence came from the unknown region, so it still needs classification.']]} />
    <PartitionInvariantLab />
    <Prose>Every update reduces high−middle by 1 in the correct algorithm. A 0 or 1 increases middle; a 2 decreases high. All swaps preserve occurrences. On exit, middle=high, so no unknown region remains. The three classified regions cover the array in order. Their sizes can be zero, so empty, all-equal and missing-category inputs require no special loop branch.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.partition}><Prose>Validation occurs before the first mutation, so an invalid category leaves the input untouched. Including that scan, time remains Θ(n+1); the classification loop performs exactly n iterations and uses O(1) auxiliary word state. The swaps need not preserve order <em>within</em> a category: this is not a stable partition of arbitrary records.</Prose></RunnableExample>
    <Prose>On [1,2,0], the faulty rule first advances middle past the 1. It then swaps 2 with the last 0 and advances middle again, ending with [1,0,2]. The loop stops and preserves all occurrences, but its claimed 1 region contains 0. The precise failure is preservation; changing the termination argument cannot repair it.</Prose>

    <H2>6. Prove the computation will stop</H2>
    <Prose>A <strong>variant</strong>, also called a ranking or decreasing measure, tracks remaining work in an order that cannot descend forever. A common sufficient rule is a nonnegative integer at loop boundaries that strictly decreases on every continuing iteration. The invariant records a preserved fact; the variant records progress. They serve different purposes, although the invariant often establishes the measure's bounds.</Prose>
    <Prose>“The variable changes” is too weak: x=1−x can alternate forever. “It never increases” is too weak: x=x makes no progress. Even “strictly decreases and stays positive” is insufficient if the measure is an exact real number. The domain and ordering must rule out an infinite descending chain; this property is called <strong>well-foundedness</strong>.</Prose>
    <DescentFigure />
    <Prose>For a nonnegative integer measure initially m, strict descent consumes at least 1 each time, so there are at most m such steps. Some valid measures are more sophisticated or can cross below their bound on the final exit. Our simple convention is sufficient, not a claim that every terminating program has an obvious variable that behaves this way.</Prose>
    <H3>Euclid: the invariant can be a mathematical relationship</H3>
    <Prose>The greatest common divisor of two nonnegative integers is their largest common positive divisor, unless both are 0, for which we explicitly use the conventional result 0. For 84 and 30 the answer is 6. Instead of trying all divisors, use division with remainder: a=q·b+r with 0≤r&lt;b when b&gt;0. Replace (a,b) by (b,r).</Prose>
    <Prose>Why preserve the answer? Any common divisor of a and b also divides r=a−q·b. Conversely, any common divisor of b and r divides a=q·b+r. Thus the set of common divisors is unchanged. This is the invariant. The next second coordinate is r, a strictly smaller nonnegative integer than b. This is the variant. When b=0 and a&gt;0, every divisor of a divides 0 and the greatest is a, so return a. Starting at (0,0) immediately returns the stated convention without dividing.</Prose>
    <EuclidTerminationLab />
    <RunnableExample example={algorithmCorrectnessExamples.gcd}><Prose>The simultaneous assignment uses the old a and b to compute the remainder. Reversing the inputs does not break the proof; if a&lt;b, the first remainder is simply a. Nonnegative bounds and the nonzero guard make every modulo operation valid.</Prose></RunnableExample>
    <details><summary>Deeper: a termination proof can yield a sharper work bound</summary>
      <Prose>A remainder r is smaller than b, but it need not be at most b/2. If it exceeds b/2, the next remainder is b−r, which is below b/2. Therefore within two remainder steps the positive second coordinate at least halves. This gives O(log(max(a,b)+1)) division steps under an arithmetic-operation model. It is not a constant-cost bit-complexity bound for arbitrarily large integer division.</Prose>
    </details>
    <H3>Progress when an inner counter resets</H3>
    <Prose>Imagine visiting a rectangle row by row. The number of columns remaining resets when a row finishes, so it cannot decrease throughout the whole program. Use the pair (rows remaining, columns remaining), comparing the first coordinate before the second. This is <strong>lexicographic order</strong>, like comparing words at their first differing letter.</Prose>
    <Prose>At a normal cell step, the first coordinate is unchanged and the second drops. At a row transition, the first drops, so the second is allowed to increase. A pair of nonnegative integers cannot descend infinitely in this order: the first can drop only finitely many times; between those drops, the second can drop only finitely many times too.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.lexicographic}><Prose>The visited list and saved measures are teaching outputs, not constant-space counters. With R rows and C columns this particular loop has RC visits plus R row transitions, including when C=0. “Both nested counters always decrease” would be false; the ordered pair explains the reset correctly.</Prose></RunnableExample>
    <Checkpoint prompt="A loop uses while x > 0: x = x // 2 + 1 on nonnegative integers. Is x a valid decreasing measure?">
      <Prose>No: x=2 maps to 2 and x=1 maps to 1. Starting there never reaches a false guard. Either the update or the stopping condition must change to fit the intended postcondition. An attractive logarithmic formula cannot replace checking actual progress at the boundary.</Prose>
    </Checkpoint>

    <H2>7. Justify recursive and algebraic steps</H2>
    <Prose>A recursive proof works with a function's whole return contract. To compute an integer base raised to a nonnegative integer exponent e, handle e=0 by returning 1. Otherwise compute the power for h=floor(e/2). When e=2h, square that result; when e=2h+1, multiply the square by one more base.</Prose>
    <Prose>Use <strong>strong induction</strong>: assume the function meets its contract for every exponent smaller than e. The recursive argument h is nonnegative and strictly smaller when e&gt;0, so its result is baseʰ. Squaring gives base²ʰ; the odd branch adds exactly one factor. Together with the base case, this proves the returned value for every permitted exponent. The decreasing exponent also proves termination in the abstract model. Assuming the same call at e is correct would be circular; relying on a smaller call with a stated contract is not.</Prose>
    <H3>An iterative version transfers unfinished work</H3>
    <Prose>Let B and E be the original base and exponent, fixed throughout the proof. Keep result, factor and remaining with invariant <strong>result × factor<sup>remaining</sup> = B<sup>E</sup></strong> and remaining≥0. Initially result=1, factor=B and remaining=E. If remaining=2h, leave result unchanged, square factor and set remaining=h. If remaining=2h+1, first multiply result by factor, then square factor and set remaining=h. In both cases the represented product is unchanged.</Prose>
    <Prose>When remaining reaches 0, factor⁰=1 and result equals the required power. A positive remaining strictly decreases under integer halving. The loop does not guess the answer from a pattern: it moves factors from the unfinished part into the accumulated part while preserving an equation.</Prose>
    <RunnableExample example={algorithmCorrectnessExamples.powers}><Prose>This contract uses exact integer arithmetic and treats exponent 0 as the empty product 1, including base 0. Both implementations use O(log(e+1)) arithmetic steps; the recursive version keeps O(log(e+1)) frames. Integer multiplication and storage are not constant in bit length, and an exact result can itself contain Θ(e log|base|) bits for |base|≥2. Python recursion limits and available memory constrain real executions despite abstract termination.</Prose></RunnableExample>
    <details><summary>Useful transfer: keep only a remainder after repeated multiplication</summary>
      <Prose>If only the remainder modulo positive m is needed, numbers differing by a multiple of m are interchangeable for later additions and multiplications modulo m. Replace the equality invariant by congruence: result × factor<sup>remaining</sup> has the same remainder as the requested power. Reducing after each product preserves that relation while keeping result and factor below m.</Prose>
      <RunnableExample example={algorithmCorrectnessExamples.modularPower} />
      <Prose>For 3¹⁰⁰ mod 7, the result is 4. The function never constructs 3¹⁰⁰. When m=1 the only remainder is 0, which explains initializing result with 1%m. This teaches arithmetic-state reduction; it is not a constant-time cryptographic implementation. Its runtime branches on the exponent and its multiplication cost depends on operand sizes.</Prose>
    </details>

    <H2>8. Connect the local proofs</H2>
    <Prose>Correctness is often assembled from smaller contracts. Before using a helper's guarantee, establish its precondition. After it returns, rely on what it actually guarantees, including any mutation and aliasing effects. The helper's name or a passing unit test is not an extra theorem. If its implementation is recursive, the induction hypothesis must match this full contract.</Prose>
    <LessonTable caption="The earlier techniques use related proof structures" headers={['Earlier method', 'What must be justified', 'What does not follow automatically']} rows={[['Divide and conquer', 'Base cases; every child meets the smaller-task precondition; the combine step produces the parent contract.', 'A correct child sum may not include enough information for a parent optimum; Backtracking’s four-field summaries show why.'], ['Backtracking', 'Every emitted answer is valid, every valid answer has an explored branch, pruning is sound, and local mutations are restored.', 'Finding one answer does not prove completeness or uniqueness.'], ['Greedy', 'A local choice can be incorporated into an optimum, or a comparison proves it stays ahead; the remaining problem still meets its assumptions.', 'Feasibility of a chosen schedule does not prove best total value.'], ['Dynamic programming', 'A state contains sufficient information; transitions cover the legal choices; dependencies have been solved; bases and reconstruction match the objective.', 'Caching an incomplete state cannot make two different futures equivalent.'], ['Range queries', 'An aggregate has its stated identity/combine rules, an update preserves representation, and query regions cover exactly the requested range.', 'A correct sum query does not establish that a different aggregate or noncommutative order works.']]} />
    <Prose>A proof can include <strong>soundness</strong> (every produced answer is valid), <strong>completeness</strong> (every required answer or possibility is represented), and <strong>optimality</strong> (no feasible answer is better). Which of these you need depends on the task. A single-witness search, all-solutions enumeration, approximate optimizer and exact optimizer have different obligations. State the guarantee before selecting a familiar proof technique.</Prose>
    <Prose>For structures, use structural induction. To compare two finite binary trees, first handle the empty cases, then compare root values and both corresponding child pairs. Correct child comparisons plus matching roots establish both structure and values. Equal traversal value lists alone may lose the locations of missing children. For a cyclic graph, “recurse on a child” does not automatically make the problem smaller; visited-state or another well-founded measure is needed.</Prose>
    <Checkpoint prompt="A sorted-array helper promises a sorted permutation of its input but may change the order of equal records. Can a caller infer that it preserves earlier tie-breaking order?">
      <Prose>No. Stable ordering is a stronger contract. A proof using that property must choose a stable helper or establish another explicit tie rule. Treating an unstated guarantee as a fact creates a gap between individually correct components.</Prose>
    </Checkpoint>

    <H2>9. Test the argument and its assumptions</H2>
    <Prose>Tests and proofs support each other. An example can expose an omitted condition or mistaken transition. A proof explains why the mechanism generalizes over its specified inputs. Executing a few examples cannot justify an unbounded claim; conversely, a correct mathematical recurrence does not show that the typed implementation matches it.</Prose>
    <SortedContractFigure />
    <RunnableExample example={algorithmCorrectnessExamples.certificate}><Prose>This checks a particular candidate's full sorted-permutation contract using independent facts: adjacent order and occurrence counts. With integers and expected constant-time hash-table operations, its work is expected linear in the two list lengths; Counter storage depends on the number of distinct values. The candidate slice used by zip also allocates linear storage here. The verifier is a clear reference, not a proof of a sorting implementation's constant-space bound.</Prose></RunnableExample>
    <Prose>An output checker is useful even when it does not produce the answer. Its success certifies <em>that candidate under that checker's assumptions</em>, not every result the solver could ever return. Later reductions and complexity theory distinguish finding a witness from checking one; a fast checker by itself does not settle the difficulty of finding an answer.</Prose>
    <H3>Choose tests from the proof obligations</H3>
    <ul>
      <li>Initialization suggests empty inputs, neutral values and boundary positions.</li>
      <li>Preservation suggests every branch, equal values, aliasing and the smallest case that crosses a region boundary.</li>
      <li>Exit arguments suggest no solution, the earliest/latest solution, duplicate solutions and stronger output contracts.</li>
      <li>Termination suggests stalled updates, signed values, zero divisors, reset counters and the smallest positive measure.</li>
      <li>The arithmetic model suggests large integers, overflow/rounding where relevant and the difference between exact and approximate equality.</li>
    </ul>
    <RunnableExample example={algorithmCorrectnessExamples.counterexample}><Prose>The first discovered failure is the two-element input (0,1): the jump skips the only match. The enumeration order makes it a smallest-length failure in this declared binary-value search. If no failure appeared through length 4, that would establish only the tested finite coverage, not correctness for arbitrary-length integer lists.</Prose></RunnableExample>
    <Prose>Complete enumeration can establish a result for a fully specified finite domain if the model, checker and coverage are trustworthy. The important question is what domain was actually covered. Property-based generation, randomized tests and mutation tests can probe much larger spaces, but “no counterexample found” must retain its scope. Useful transformations—such as adding a nonmatching suffix without changing an existing first match—also test relationships, yet a buggy implementation may satisfy several such properties.</Prose>
    <Prose>A proof assumes a language and environment. The same algebra may fail after a fixed-width integer overflows; a floating-point rewrite can change rounding; a second thread can invalidate a claim about an untouched array. A sequential proof does not establish race freedom, fairness or a network response deadline. Those require additional specifications and models. No amount of proof of the wrong specification establishes that it matches the user's actual need.</Prose>

    <H2>10. Build a proof for a new contract</H2>
    <Exercise title="Find an integer square root without rounding" hint="Keep one nonnegative boundary whose square is small enough and another whose square is too large. Your exit should make them consecutive integers." solution={<>
        <Prose>Choose low=0 and high=n+1. The invariant is 0≤low&lt;high and low²≤n&lt;high². It holds for every nonnegative n, including 0. When high−low&gt;1, the floor midpoint lies strictly inside the two boundaries. If its square is at most n, replace low; otherwise replace high. Each update preserves the two inequalities and strictly reduces the positive integer gap.</Prose>
        <Prose>At exit, the positive gap is 1, so high=low+1 and low²≤n&lt;(low+1)². This is exactly the postcondition. Using multiplication avoids floating-point rounding. For fixed-width arithmetic, a division-based comparison or sufficiently wide type is needed to avoid overflowing the square.</Prose>
        <RunnableExample example={algorithmCorrectnessExamples.squareRoot} />
        <Prose>The gap is approximately halved each iteration, giving O(log(n+2)) comparisons/multiplications under an arithmetic-operation model and constant many integer variables. This is not O(log n) bit time when n is arbitrarily large. The returned result is checked against the defining inequalities, not just a decimal approximation.</Prose>
      </>}>
      <Prose>Given a nonnegative integer n, return r with r≥0 and r²≤n&lt;(r+1)². Do not use a floating-point square root. Write the precondition, an invariant, each branch's preservation argument, a termination measure and tests for 0,1, a perfect square and its neighbors. Explain the result for n=15 before opening the solution.</Prose>
    </Exercise>
    <Exercise title="Repair a proof that forgot the output's meaning" hint="A lower bound on the maximum is not enough. State that the current answer comes from the processed input as well as dominating it." solution={<Prose>For a nonempty list, initialize best=a[0], index=1. At each boundary, 1≤index≤n, best is an element of a[:index], and best≥every element of that prefix. Updating best with a[index] only when it is larger preserves membership and dominance. At index=n, these two facts say it is the maximum. Initializing best=0 would fail membership and returns an incorrect answer for a wholly negative list. Empty input needs a separate stated convention or a rejected precondition.</Prose>}>
      <Prose>A maximum routine initializes best=0 and claims: “best is at least every value seen so far.” It works on some positive lists. Construct a failing permitted input if arbitrary nonempty integer lists are allowed, strengthen the invariant and choose a correct initialization.</Prose>
    </Exercise>
    <Exercise title="Explain an early exit and a broken variant" hint="A break does not imply the guard became false. A continue still needs the progress required by the proof." solution={<Prose>If a scan breaks on a match, establish the found-result condition at that break; you cannot conclude index=n from it. If its nonmatching branch continues before incrementing index, the same mismatch repeats forever. The invariant can remain true while n−index does not decrease. Move the progress update onto every continuing path, or change the control flow while preserving the contract.</Prose>}>
      <Prose>A colleague replaces a matching return with break, then reasons that the loop's guard must be false afterward. In a second version, a nonmatch runs continue before index is advanced. Identify the different missing obligation in each version.</Prose>
    </Exercise>
    <Prose>Ready to continue when you can state a contract without pointing at the code, draw the processed and remaining regions, justify every branch and exit, name a well-founded measure, and produce a counterexample to a plausible faulty update. Close a solution after using it and reconstruct the argument later on a changed input. An accepted answer or a green checklist alone does not establish those skills.</Prose>
    <Prose>The next module topic, <a href="./hashing-collision-resolution-amortized-analysis?module=data-structures-algorithms">Hashing, Collision Resolution &amp; Amortized Analysis</a>, studies a representation whose correctness must survive collisions and resizing. The invariant and preservation habits here let you separate “the table still represents the same mapping” from its expected or amortized performance.</Prose>
    <DsaPractice practice={correctnessPractice} />
    <Sources alternatives={<>
      <h4>Another route through the ideas</h4><ul>
        <li><a href="https://www.cs.cornell.edu/courses/JavaAndDS/loops/30aloop.html" target="_blank" rel="noopener noreferrer">David Gries, Cornell — Finding loop invariants</a>: short videos with written companions. The array-diagram generalization segment is useful after sections 2–4; its <a href="https://www.cs.cornell.edu/courses/JavaAndDS/loops/32genReplaceA.pdf" target="_blank" rel="noopener noreferrer">two-page script</a> gives a visual alternative. The page and script were reviewed; full playback was not. Java notation and endpoint conventions differ from this lesson.</li>
        <li><a href="https://courses.cis.cornell.edu/courses/cs2110/2026fa/lectures/lec04/" target="_blank" rel="noopener noreferrer">Cornell CS2110 — Loop Invariants</a>: a written progression from array regions to working loops. Use it to practise constructing an invariant, rather than copying one after an algorithm is finished.</li>
        <li><a href="https://softwarefoundations.cis.upenn.edu/plf-current/Hoare2.html" target="_blank" rel="noopener noreferrer">Software Foundations — Hoare Logic, Part II</a>: a deeper written and proof-assistant route through decorated programs and invariant discovery. Start after section 3; learning Rocq and the book's small formal language is an additional undertaking. No proof-assistant execution is needed for this lesson.</li>
      </ul>
    </>}>
      <li><a href="https://www.cs.cornell.edu/courses/cs2112/2019fa/lectures/loopinv/" target="_blank" rel="noopener noreferrer">Cornell CS2112 — Loop invariants</a>: initialization, preservation, postconditions, termination and exponentiation. Its binary search assumes a present target; compare its contract with our absent/duplicate cases instead of transplanting its bounds.</li>
      <li><a href="https://dafny.org/latest/OnlineTutorial/Termination" target="_blank" rel="noopener noreferrer">Dafny — Termination</a>: integer and lexicographic decreasing measures, with tool-specific details and limits. An optional bridge from our handwritten arguments to a verifier; this page's examples were read, not executed here.</li>
      <li><a href="https://docs.python.org/3/reference/simple_stmts.html#the-assert-statement" target="_blank" rel="noopener noreferrer">Python — Simple statements</a>: assignment and assert semantics, including optimized execution. The current 3.14 documentation was checked; complete programs here were executed on the recorded 3.12 runtime.</li>
      <li><a href="https://docs.python.org/3/library/math.html#math.gcd" target="_blank" rel="noopener noreferrer">Python — gcd and isqrt</a>: exact integer reference contracts used in independent checks, including the all-zero gcd convention. These references are checkers, not substitutes for the derivations above.</li>
    </Sources>
  </div>
};
