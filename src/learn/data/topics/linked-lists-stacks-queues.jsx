import { Code, H2, H3, Prose } from "../../components/content";
import { Checkpoint, LessonIntro, LessonTable, Sources } from "../../components/lesson-labs/LessonElements";
import { LearningResources } from "../../components/lesson-labs/LessonInvestigation.jsx";
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import linkedPractice from '../practice/linked-lists-stacks-queues.js';
import { LinkedReversalLab, BracketStackLab, CircularQueueLab } from "../../components/lesson-labs/LinkedFoundationsLabs";
import PythonExample from '../../components/lesson-labs/PythonExample';
import { linkedExamples } from "../linked-foundations-examples";
import { LinkedCycleLab, LinkedMiddleLab } from '../../components/lesson-labs/LinkedTraversalLabs.jsx';
import { NextGreaterLab, HistogramBoundaryLab } from '../../components/lesson-labs/MonotonicStackLabs.jsx';
import { linkedTraversalExamples } from '../linked-traversal-examples.js';
import { monotonicStackExamples } from '../monotonic-stack-examples.js';

export default {
  title:'Linked Lists, Stacks & Queues',readTime:'~70 min read + extended practice',hasIntegratedGuide:true,
  content:()=> <div className="lesson-pilot">
    <LessonIntro prerequisites="Arrays, Strings & Hash Maps for sequence operations and growth costs; Python functions and OOP for the small Node and RingQueue classes. No pointer arithmetic or recursion is required." sections={[["1-separate-the-rule-from-the-storage","Behavior versus storage"],["2-follow-references-instead-of-positions","Linked lists"],["3-reverse-links-without-losing-the-rest","Reversal"],["4-a-stack-remembers-what-must-finish-first","Stacks"],["5-a-queue-preserves-arrival-order","Queues"],["6-combine-lookup-with-recency","LRU application"],["7-remove-one-node-with-a-clear-contract","Removal practice"],["8-maintain-a-recent-window-of-readings","Recent window"],["9-detect-a-cycle-without-remembering-every-node","Cycle entry"],["10-define-the-middle-before-splitting-a-list","Middle and split"],["11-use-a-stack-to-settle-unresolved-future-questions","Monotonic stack"],["12-transfer-the-stack-to-smaller-boundaries-and-rectangles","Boundary geometry"],["13-rebuild-the-reasoning-on-changed-inputs","Changed practice"],["guided-dsa-practice","Guided LeetCode practice"]]}>
      Follow node references, preserve the unprocessed part while reversing links, detect incorrectly nested brackets, and operate a bounded queue across wraparound. Then use these ideas for recency-based caching and a moving window of readings. The deeper branches derive constant-space cycle entry, explicit middle/splitting policies and monotonic stacks for future distances and rectangle boundaries.
    </LessonIntro>
    <H2>1. Separate the rule from the storage</H2>
    <Prose>You undo the most recent edit first, but serve the oldest waiting print job first. Both store items, yet they promise different removal orders. A <strong>stack</strong> removes the last item added; a <strong>queue</strong> removes the first. These describe behavior. A <strong>linked list</strong> describes a storage organization: each node holds a value and references to other nodes.</Prose>
    <LessonTable caption="An interface says what happens; an implementation says how" headers={['Idea','Removal/access rule','Possible implementation']} rows={[
      ['Stack: last in, first out (LIFO)','Push at the top; pop the newest item.','End of a dynamic array, or head of a singly linked list.'],
      ['Queue: first in, first out (FIFO)','Enqueue at the back; dequeue from the front.','Linked head/tail nodes, circular array or deque.'],
      ['Linked sequence','Follow references to reach the next element.','Nodes allocated independently, with next and possibly previous links.'],
    ]}/>
    <Prose>Choosing a stack does not require choosing linked nodes. Python's list is useful as a stack; collections.deque supports efficient operations at both ends. We will implement small structures to understand their invariants, then use library structures for applications. All complete programs use only Python 3.12+ and its standard library.</Prose>

    <H2>2. Follow references instead of positions</H2>
    <Prose>In a singly linked list, a <strong>node</strong> contains a value and one next reference. The <strong>head</strong> reference identifies the first node; None means no node. The last node's next is None. Nodes do not have to be adjacent in memory. A reference leads to the next node wherever it is stored.</Prose>
    <div className="nt-flow"><span>head → A<small>value 4 · next B</small></span><span>Node B<small>value 7 · next C</small></span><span>Node C<small>value 9 · next None</small></span></div>
    <Prose>A, B and C are node identities in our drawings, not array indices or actual memory addresses. Changing A.next changes one relationship; it does not move B's stored value into A. Two nodes can hold equal values and still be different nodes. As in OOP, two references can also reach the same object, so a mutation may be visible through both names.</Prose>
    <H3>What does a local edit require?</H3>
    <Prose>To insert X after a known node A, first set X.next to A.next, then set A.next to X. This preserves the old suffix. To remove the node after a known predecessor A, set A.next to A.next.next, after checking that the node exists. Removing the first node instead updates head. These local changes are constant in number, but finding A by value or position can still require walking the whole list.</Prose>
    <LessonTable caption="State the condition behind each cost" headers={['Operation on a singly linked list','Cost','Condition']} rows={[
      ['Read element at position i','O(i + 1)','Start at head and follow links; there is no slot-address formula.'],
      ['Find a value','O(n) worst case','Equality has bounded cost; target may be absent.'],
      ['Insert after a known node','O(1) link changes','Already have the node; count allocation separately if relevant.'],
      ['Remove after a known predecessor','O(1) link changes','Already have the predecessor and check its successor exists.'],
      ['Append using a maintained tail','O(1) link changes','Maintain both head and tail correctly, including the empty case.'],
      ['Remove the last node','O(n) in general','A tail reference alone does not identify its predecessor.'],
    ]}/>
    <Prose>A <strong>doubly linked list</strong> also stores previous links, enabling direct unlinking of a known node when both directions and endpoints are maintained. A <strong>circular list</strong> links the end back into the sequence, so traversal needs a termination rule other than “until None.” These are useful variants, not automatic upgrades: extra references and independently allocated nodes cost memory, and following links can have worse locality than visiting consecutive array slots. Measure real workloads before claiming one is faster.</Prose>
    <Checkpoint prompt="A singly linked list stores a million items, and you only know a target's value. Is deletion O(1)?">
      <Prose>The local relink can be O(1), but discovering the matching node and its predecessor may take O(n). A claim about one step is not a claim about the complete operation. Also define whether you remove the first match or every match.</Prose>
    </Checkpoint>

    <H2>3. Reverse links without losing the rest</H2>
    <Prose>Reversing [4, 7, 9] in place should make the same nodes reachable in the order [9, 7, 4]. No new value nodes are needed. The trap is overwriting a next reference before remembering where the remaining sequence starts. The lab displays all original nodes for diagnosis, including nodes no longer reachable from head.</Prose>
    <LinkedReversalLab/>
    <H3>Keep a fact true after every iteration</H3>
    <Prose>An <strong>invariant</strong> is a fact that remains true as an algorithm progresses. Here previous identifies the already reversed prefix, and current identifies the untouched suffix. At the loop boundary, these two chains account for every original node exactly once. Initially the reversed prefix is empty and current is the original head.</Prose>
    <div className="nt-flow"><span>Save following<small>Remember the remaining suffix</small></span><span>Reverse current.next<small>Attach this node to the prefix</small></span><span>Advance both references<small>Prefix grows; suffix shrinks</small></span></div>
    <Prose>When current becomes None, the suffix is empty and previous reaches the whole reversed list. That explains both why the loop stops and why it returns previous. Testing examples helps; the invariant explains the general mechanism.</Prose>
    <PythonExample example={linkedExamples.reverse}><Prose>Node stores a value and next reference. Build prepends input values in reverse order to construct their original order. Values walks nodes and tracks identities to diagnose accidental cycles. Reverse itself uses only a few references: O(n) time and O(1) additional working space. The display helper's result list and diagnostic set do use O(n) extra space; do not attribute their cost to the reversing function.</Prose></PythonExample>
    <Checkpoint prompt="What happens if the caller runs reverse(head) but ignores the returned reference? What if you set current.next = previous before saving the old next?">
      <Prose>The caller's old head still reaches the old first node, which is now the last; it does not become the new head automatically. Overwriting next before saving it can lose the remaining suffix. A one-node test might hide both mistakes, so test a multi-node case and check every original node remains reachable exactly once.</Prose>
    </Checkpoint>

    <H2>4. A stack remembers what must finish first</H2>
    <Prose>In ([…]), the square bracket must close before the surrounding parenthesis can close. The most recently opened unfinished group is the next one that needs attention: exactly the stack rule. <strong>Push</strong> adds a new unfinished opening; <strong>pop</strong> removes and returns the top. <strong>Peek</strong> inspects the top without removing it. Popping an empty stack needs an explicit error or rejection policy.</Prose>
    <BracketStackLab/>
    <Prose>The invariant is that the stack contains precisely the unmatched openings in the processed prefix, with the newest on top. On an opening, push it. On a closing, require a nonempty stack whose top is the matching opening; then pop. At the end, require an empty stack. Merely counting equal numbers of opening and closing symbols cannot detect the crossed nesting in ([)].</Prose>
    <PythonExample example={linkedExamples.brackets}><Prose>List.append and pop at the end implement the stack. The partner dictionary maps each closing symbol to the required opening. Python's short-circuit or avoids popping when the stack is empty. Characters outside these six bracket symbols are ignored. Each symbol is visited once; time is O(n) with amortized list-end operations, and extra space is O(d) for maximum nesting depth d, which can reach n.</Prose></PythonExample>
    <Prose>This is a bracket recognizer, not a complete Python parser: quotes, comments and escape sequences change whether a visible bracket is syntax. For actual code, tokenize or parse under the language's rules first. The same newest-unfinished-first idea also appears in function-call return state and undo histories. An undo system usually needs stored inverse actions or earlier state, not merely a stack of action names.</Prose>
    <Checkpoint prompt="Why is an empty input balanced, but (() is not? Would a queue of openings correctly accept ([]) ?">
      <Prose>Empty input has no unmatched obligations. (() leaves an opening on the stack at the end. A FIFO queue would compare the ] with the oldest opening (, rejecting a valid nesting; the required matching order is last in, first out.</Prose>
    </Checkpoint>

    <H2>5. A queue preserves arrival order</H2>
    <Prose>A work queue accepts A, then B, then C. Serving A should leave B before C. Enqueue adds at the back and dequeue removes the front. A Python list can express this with append and pop(0), but each front removal shifts remaining references. A deque avoids that repeated movement for end operations. Deque is short for “double-ended queue,” so it can also support stack behavior.</Prose>
    <H3>Reuse fixed slots with a circular buffer</H3>
    <Prose>A bounded queue can reserve an array once. Store head, the physical index of the oldest live item, and size, the number of live items. The next insertion index is <Code>(head + size) % capacity</Code>. Modulo gives the remainder: for capacity 4, index 4 wraps to 0 and index 5 wraps to 1. Logical FIFO order can cross the end of the physical array without moving stored items.</Prose>
    <CircularQueueLab/>
    <Prose>The invariants are 0 ≤ size ≤ capacity and: logical item i occupies (head + i) modulo capacity. Empty means size is zero; full means size equals capacity. In both cases the next insertion index equals head, so indices alone cannot distinguish them under this representation. Size carries that distinction.</Prose>
    <PythonExample example={linkedExamples.ring}><Prose>Put refuses a full queue before changing state. Get saves the oldest value, clears its slot to release the stored reference, advances head with wraparound, and reduces size. None may be a legitimate queued value because emptiness is determined by size, not slot contents. Construction allocates O(capacity) slots; each put/get makes a bounded number of changes, O(1), without resizing.</Prose></PythonExample>
    <Prose>The correct full-queue policy depends on what is stored. A job processor might reject or wait for space; a live plot might intentionally discard its oldest reading. Python deque(maxlen=k) automatically discards an item from the opposite end on a full append, which is useful for recent-history buffers but can silently lose jobs if you assume this lab's rejection policy. A queue's storage alone does not coordinate concurrent producers and consumers; blocking, locking and process communication are separate contracts.</Prose>
    <Checkpoint prompt="Capacity is 3, head is 2 and size is 2. Which slots hold the live items, and where does the next insertion go? Does a priority queue promise this same removal order?">
      <Prose>The live items are in slots 2 then 0; the insertion goes into slot (2 + 2) % 3 = 1. A priority queue removes by priority rather than arrival order; its tie policy needs definition. Heaps develop that different contract later.</Prose>
    </Checkpoint>

    <H2>6. Combine lookup with recency</H2>
    <Prose>A cache with room for two results receives requests a, b, a, c. When c arrives, removing b keeps the more recently used a. This is <strong>least recently used (LRU)</strong> eviction. A map can find an entry by key quickly, while a doubly linked recency list can move a known entry to the newest end without searching for its predecessor. Together they support lookup and recency updates; neither structure alone provides both capabilities efficiently.</Prose>
    <PythonExample example={linkedExamples.lru}><Prose>OrderedDict supplies efficient reordering operations, so we use it instead of writing production cache internals. A hit moves the existing key to the newest end. A miss inserts a value and, if capacity is exceeded, removes the oldest. Requesting a again protects it from the next eviction; frequency and recency are different policies. Unknown source keys raise KeyError before a result is inserted.</Prose></PythonExample>
    <Prose>Recency is useful when recently used data is likely to be needed again, but a one-time scan can evict otherwise valuable entries. This toy source is fixed: eviction does not address stale data, expiration, synchronization or failures while computing a value. Those belong in the later <a href="/learn/topic/caching-strategies-semantic-exact-kv-cache-sharing">caching strategies</a> topic. The example's purpose is to show why a lookup map and an ordered structure complement each other.</Prose>

    <H2>7. Remove one node with a clear contract</H2>
    <Prose><strong>Independent task:</strong> remove only the first node whose value equals a target, preserving every other node and its order. Return the possibly changed head. Input is an acyclic singly linked list. Test [4, 7, 7, 9] with target 7, then remove 4 from the result; also test an absent target and an empty list. Do not convert the input to an array and rebuild it: the task is to preserve remaining node identities.</Prose>
    <details><summary>Hint: make the head case look like every other case</summary><Prose>A temporary sentinel node can point to the real head. It is not a data item in the returned list. Starting previous at the sentinel means even removing the first real node can use previous.next = current.next. Otherwise you may write a separate head case; both approaches are valid when their contracts are checked.</Prose></details>
    <details><summary>Complete solution and invariant</summary><PythonExample example={linkedExamples.remove}/><Prose>Before each comparison, previous.next is current and no earlier examined node matched the target. If current matches, changing one link removes exactly that node from the returned chain, and immediate return protects later equal values. If the loop ends, nothing matched and the original sequence remains. The sentinel is omitted by returning sentinel.next. Search is O(n), with O(1) additional space for references and one sentinel.</Prose></details>
    <Checkpoint prompt="Which original node identities should remain after removing 7 from [4, 7, 7, 9]? Does removing a node from one list guarantee no other variable can still access it?">
      <Prose>The original first, third and fourth nodes remain, in that order. The second node is no longer reachable through the returned list, but another reference can still reach it. Unlinking and making an object unreachable everywhere are different events.</Prose>
    </Checkpoint>

    <H2>8. Maintain a recent window of readings</H2>
    <Prose><strong>Transfer task:</strong> for readings [2, 4, 8, 10], report the mean of at most the most recent three values after each arrival. Early windows use the available count, so the first two means are 2 and 3. Keep a deque and a running total; when a full window receives another reading, remove the oldest contribution before adding the new one. This pattern supports a sensor dashboard without retaining its entire history.</Prose>
    <details><summary>Hint: keep the sum consistent with membership</summary><Prose>After every arrival, total must equal the sum of the live window. Eviction changes both membership and total. Divide by the current count, not always the maximum width. For empty input, no mean exists, so return no outputs.</Prose></details>
    <details><summary>Complete solution and changed-window check</summary><PythonExample example={linkedExamples.window}/><Prose>When 10 arrives at width 3, discard 2: the new window [4, 8, 10] has sum 22 and mean 22/3. Width 2 instead ends with [8, 10], mean 9. The task assumes finite numeric readings; missing/nonfinite values need an explicit policy. Floating-point running totals can accumulate rounding error in long streams, so this is not a numerical-accuracy guarantee for every dataset. Update work is O(1) per item; window storage is O(width), while this batch helper also stores O(n) returned means.</Prose></details>
    <H2>9. Detect a cycle without remembering every node</H2>
    <Prose>A traversal that waits for None never finishes if the last link leads back into the chain. The earlier diagnostic remembers visited node identities. That is a correct baseline: if an identity repeats, the repeated object is the cycle's entry. Equal values alone do not establish a cycle. Two different nodes can both store 7 while their chain still ends normally.</Prose>
    <Prose>Now change the resource contract: return the same entry object, or None, while using only a constant number of working references and leaving every link unchanged. Assume a finite, unchanging reachable chain in which every next is a node or None. With one successor per node, the walk either ends or follows a nonrepeating prefix into a cycle. This is not an algorithm for arbitrary branching graphs or for links being changed by another thread.</Prose>
    <H3>Let two references cover different distances</H3>
    <Prose>Start slow and fast at head. On each round, advance slow one link and fast two links. First check that fast and fast.next are not None, so both hops are safe. Compare the resulting references using Python's <Code>is</Code>, which asks whether they identify the same object. Their equality before any movement tells you nothing: both were deliberately initialized to head.</Prose>
    <LinkedCycleLab/>
    <Prose>In the seven-node fixture, the tail points to n2. The prefix has two edges before entering the cycle; the loop contains five nodes. We call these lengths μ (mu) and λ (lambda). The identities n2, n3, n4, n5 and n6 form the loop even when all seven values are equal.</Prose>
    <LessonTable caption="Detection rounds for μ=2 and λ=5" headers={['Round t','Slow: t hops','Fast: 2t hops','Conclusion']} rows={[
      [0,'n0','n0','Initialization only; do not report a cycle.'],
      [1,'n1','n2','Different identities.'],
      [2,'n2','n4','Both are inside the loop.'],
      [3,'n3','n6','The two-hop route crosses two actual links.'],
      [4,'n4','n3','Fast has wrapped past the tail.'],
      [5,'n5','n5','Positive meeting; entry is still n2, not n5.'],
    ]}/>
    <H3>Why a meeting must occur</H3>
    <Prose>After t rounds, slow has followed t edges and fast 2t edges from the same start. Once slow enters the cycle, fast is already there. Measured from the entry, their offsets are t−μ and 2t−μ, with full loops ignored. Their difference is t. Thus they meet exactly when t is a multiple of λ after both are inside. “Modulo λ” is just this idea of ignoring complete λ-edge laps.</Prose>
    <Prose>As rounds advance, the relative offset advances by one position around the loop. Within at most one complete cycle of offsets it is zero. Reaching the loop takes at most μ rounds, so detection uses O(μ+λ) rounds. On an acyclic chain, fast instead reaches a failed two-hop guard in O(n) time. A self-loop is a genuine cycle of length one; it is detected after one positive round.</Prose>
    <H3>Why resetting one reference finds the entrance</H3>
    <Prose>Suppose the positive meeting occurred after t rounds. We just proved that λ divides t. Put slow back at head and leave fast at the meeting. Now move both one edge per round. After μ rounds, slow reaches the entry. The meeting node is also the node reached after slow's t hops from head. Following μ more links therefore reaches the node at distance t+μ, whose offset from the entry is t—a whole number of laps. It reaches the entry too.</Prose>
    <Prose>They cannot meet earlier during this reset walk: slow would still be in the noncyclic prefix, while fast stays in the cycle. If the head is already the entry, μ=0 and the reset references agree immediately. In our fixture the reset pairs are (n0,n5), then (n1,n6), then (n2,n2). Meeting and entry answer different questions.</Prose>
    <Prose><strong>Before running:</strong> {linkedTraversalExamples.cycle.question}</Prose>
    <PythonExample example={linkedTraversalExamples.cycle}><Prose>The algorithm never receives the fixture's tail index. It follows real Node references and returns an existing object. The visited-set method remains a useful correctness baseline; with ordinary identity hashing it takes expected O(n) time and O(n) stored references. The fast/slow finder uses O(n) time and O(1) additional working references. Construction, printing, the saved-link audit and the set baseline do allocate more memory; those are separate from the finder. The cycle-size helper makes one lap starting at a verified entry.</Prose></PythonExample>
    <Checkpoint prompt="Could you use a three-hop fast pointer and keep the same reset-to-head proof? Try a four-node loop whose entry is the head.">
      <Prose>With speeds one and three, the first positive meeting is after two rounds at node n2. Resetting one reference to n0 leaves them two positions apart; moving both one step preserves that gap forever. The detection meeting equation changed to 2t being a multiple of four; it no longer proves that t itself is a multiple of the cycle length. A changed speed needs a changed proof, not a blind substitution.</Prose>
    </Checkpoint>

    <H2>10. Define the middle before splitting a list</H2>
    <Prose>Fast/slow references can also answer an acyclic length question without first counting all nodes. For an even-length list, however, “the middle” is ambiguous. In n0→n1→n2→n3, the first middle is n1 and the second is n2. Returning a middle reference is a read-only query; cutting the list into two parts is a separate mutation with two returned heads.</Prose>
    <LinkedMiddleLab/>
    <LessonTable caption="Two guards encode two explicit middle contracts" headers={['Query','Continue while','Result index when n>0']} rows={[
      ['Second middle on even lengths','fast exists and fast.next exists','floor(n/2)'],
      ['First middle on even lengths','fast.next and fast.next.next exist, after handling empty input','floor((n−1)/2)'],
    ]}/>
    <Prose>Here floor means round down to an integer. In both queries, after k rounds slow is at index k and fast at index 2k when that index exists. For the second-middle guard, an even n=2m allows m rounds and leaves fast at None; slow is at m. An odd n=2m+1 also allows m rounds and leaves fast on the last node; slow is again at m. For the first-middle guard, an even chain stops one round earlier at m−1, whereas the odd chain still stops at m. These outcomes follow from the guard, not from the names “slow” and “fast.”</Prose>
    <Prose>A <strong>left-heavy split</strong> gives the left part the extra node when the length is odd. Cut after the first-middle-policy result: an even length four becomes two plus two; an odd length five becomes three plus two. Save the right head before replacing the boundary node's next with None. The remaining nodes keep their identities and order. An empty list returns two None references; a singleton remains entirely on the left.</Prose>
    <Prose><strong>Before running:</strong> {linkedTraversalExamples.middle.question}</Prose>
    <PythonExample example={linkedTraversalExamples.middle}><Prose>The middle helper assumes an acyclic chain. The public split checks that precondition using the constant-space entry finder and rejects a cycle before touching any link. That extra scan preserves the O(n) total-time and O(1) working-space bounds. The label display allocates a result list and is not part of those bounds. Another variable may still refer to the old boundary or right suffix after the cut: splitting does not erase aliases.</Prose></PythonExample>
    <Prose>This boundary is useful before merging separately processed halves or rearranging two runs. Those algorithms need their own invariants; locating a middle does not by itself implement merge sort or reorder a list. In particular, cutting <em>after</em> the second middle of a four-node list would give three nodes and one, contradicting our balanced split contract.</Prose>

    <H2>11. Use a stack to settle unresolved future questions</H2>
    <Prose>For each reading, how long must you wait for the first strictly larger reading? For [6,6,4,7,5,8], index 0 waits until index 3, a distance of three positions. A later equal 6 does not qualify. If no later value qualifies, return zero. This models the distance to a warmer day in a recorded series or a later threshold exceedance; it does not predict an unknown future.</Prose>
    <Prose>A correct baseline searches forward separately from every index. On decreasing input, no search succeeds, so it performs (n−1)+(n−2)+…+1 comparisons: O(n²). We can avoid rechecking the same failed comparisons by remembering unresolved indices in a stack.</Prose>
    <NextGreaterLab/>
    <H3>What the stack means</H3>
    <Prose>Before an arrival at index j, the stack contains exactly those earlier indices that have not yet encountered a qualifying later reading. Positions increase from bottom to top, while their values are nonincreasing. Keep the positions, not just values: two equal readings can have different wait lengths.</Prose>
    <Prose>If the new value exceeds the top's value, pop that earlier index i and set its answer to j−i. This is its <em>first</em> qualifying future reading because otherwise i would already have been removed. Continue popping as long as the same new value qualifies. When the top is at least the new value, stop: every hidden value below it is at least as large as the top, so none can be answered now. Push j as a new unresolved index.</Prose>
    <Prose>That preserves both parts of the invariant: all remaining earlier indices are still unresolved, and the newly pushed value is no larger than the previous top. Initially the empty stack satisfies it. At the end, remaining indices have no qualifying future value, so their zero answers are final. Sorting the readings would destroy their original time order and cannot replace this scan.</Prose>
    <LessonTable caption="One arrival answers three questions" headers={['Before index 3 arrives','Operation for new value 7','Distance written']} rows={[
      ['Stack indices [0,1,2], values [6,6,4]','Pop index 2 because 7>4','3−2=1'],
      ['Indices [0,1], values [6,6]','Pop index 1 because 7>6','3−1=2'],
      ['Index [0], value [6]','Pop index 0 because 7>6','3−0=3'],
      ['Empty stack','Push index 3; its future is still unresolved','No answer for index 3 yet'],
    ]}/>
    <H3>Equal values and total work</H3>
    <Prose>For [5,5,6], the strict answers are [2,1,0]. Popping on greater-or-equal instead would give [1,1,0], which answers a different question. In the strict version equal values stay on the stack; the ordered stack is nonincreasing, not necessarily strictly decreasing. The lab's comparison selector deliberately changes the contract and resets its trace.</Prose>
    <Prose>Although one arrival can pop many entries, each index is pushed once and popped at most once across the <em>whole</em> scan. There are at most 2n such modifications, plus a linear number of stopping comparisons. This charging argument proves O(n) total time under bounded-cost comparisons and amortized constant-cost list-end operations. It does not promise constant time for every arrival. The unresolved stack can hold n indices, and the returned answer has n entries: each uses O(n) space.</Prose>
    <Prose><strong>Before running:</strong> {monotonicStackExamples.greater.question}</Prose>
    <PythonExample example={monotonicStackExamples.greater}><Prose>The full strict result is [3,2,1,2,1,0]. Values are integers; negatives are allowed in this general reading contract and an empty input gives []. Pop removes only a saved index, leaving the original readings unchanged. Python's arbitrary-size integers have magnitude-dependent arithmetic costs; the linear count concerns comparisons and stack operations, with bounded-size readings as in the lab.</Prose></PythonExample>
    <Prose>A monotonic stack keeps an order among unresolved questions or useful candidates. A <a href="/learn/topic/segment-trees-fenwick-trees-range-queries">monotone deque for a sliding window</a> additionally removes expired indices at its front and represents a different task. The two structures are related by reasoning about discarded work, not interchangeable recipes.</Prose>

    <H2>12. Transfer the stack to smaller boundaries and rectangles</H2>
    <Prose>This deeper branch asks a different question: for each bar, where is the nearest <em>strictly smaller</em> height to its left and right? A histogram has adjacent unit-width bars with nonnegative heights. A rectangle under them must fit below every included bar. Finding its blockers turns local stack reasoning into a geometric width.</Prose>
    <H3>Discard a candidate that can never be the nearest answer</H3>
    <Prose>Scan left to right for left boundaries. Keep candidate indices whose heights strictly increase from bottom to top. Before answering the current bar i, pop while the top height is greater than or equal to h[i]. The remaining top, if present, is its nearest strictly smaller left index; otherwise use −1 to denote no blocker. Then push i.</Prose>
    <Prose>Why may a popped index j be forgotten? The new index i is nearer to every future position and has height at most h[j]. If j could be a strictly smaller answer for a future bar, i would also qualify and would be nearer. If j would not qualify, keeping it would not help. This is permanent candidate dominance. Unlike the next-greater scan, popping here does not fill an earlier index's answer; it removes an obsolete candidate for future queries.</Prose>
    <Prose>Any discarded qualifying index has a nearer surviving replacement, or a chain of such replacements. Thus, after the pops, no hidden discarded index can beat the remaining top. The top is the nearest valid one. Reverse the scan direction for right boundaries and use n when there is no blocker on the right. Both scans pop equal heights: equality is not a strictly smaller boundary. Their stacks are strictly increasing by height in their own scan order.</Prose>
    <HistogramBoundaryLab/>
    <H3>The boundary formula and why it reaches a maximum</H3>
    <Prose>Let L and R be bar i's smaller-boundary indices. Every bar from L+1 through R−1 is at least h[i]. The two blockers are excluded, so the width is <Code>R − L − 1</Code> and area is <Code>h[i] × (R − L − 1)</Code>. If a boundary is outside the array, the same formula still works. Equal-height bars may share the same maximal span; that is legitimate, not a reason to stop at the first equal bar.</Prose>
    <Prose>To see that taking the largest candidate area finds a global maximum, consider any rectangle under a nonempty interval of bars. Choose a bar of minimum height in that interval. Raising the rectangle to that minimum cannot reduce its area. The nearest strictly smaller blockers of that bar lie outside the interval, so the bar's computed rectangle extends at least as far and is still valid. Therefore every possible rectangle is matched or exceeded by one of our candidates. Conversely every computed candidate fits, completing both directions of the argument.</Prose>
    <Prose>For [2,2,1,4,4,3], the two height-4 bars both extend from index 3 through 4, giving area 8. The height-3 bar extends from index 3 through 5, giving area 9. The lowest height-1 bar spans all six bars but gives only area 6. “Choose the tallest bar” and “choose the widest span” are both incomplete rules: area needs height and width together.</Prose>
    <Prose><strong>Before running:</strong> {monotonicStackExamples.histogram.question}</Prose>
    <PythonExample example={monotonicStackExamples.histogram}><Prose>The program makes two O(n) stack scans and one O(n) candidate pass, with O(n) boundary/stack storage. Its witness is (start index, exclusive end index, height); the platform's histogram problem asks only for area. On empty or all-zero input it returns area zero and no positive rectangle witness. Among positive ties it retains the first candidate encountered, which is not a uniqueness claim.</Prose></PythonExample>
    <Prose>Changing widths changes the formula: unequal-width bars need cumulative horizontal coordinates, so width becomes a coordinate difference, not a count of indices. For these unit-width nonnegative bars, the exact candidate geometry is enough. A similar boundary calculation can describe a contiguous region that supports a minimum clearance, but the physical measurements and units must justify the model before its maximum is useful.</Prose>

    <H2>13. Rebuild the reasoning on changed inputs</H2>
    <Prose>Try these before revealing feedback. Keep the older removal and recent-window exercises: the new tasks test different resource, equality and boundary contracts.</Prose>
    <section className="linked-extension-practice"><H3>A. Find the entry, not merely a meeting</H3><Prose>Build seven nodes n0 through n6, with n6.next=n4 and all values equal to 7. Predict μ, λ, the first positive meeting, the reset path and which links the finder should change.</Prose><details><summary>Hint: count distances from the same head</summary><Prose>The cycle has three nodes. Find the first positive multiple of three after slow has reached index four.</Prose></details><details><summary>Explained cycle solution</summary><Prose>μ=4 and λ=3. The first meeting is t=6 at n6. The reset pairs are (n0,n6), (n1,n4), (n2,n5), (n3,n6), (n4,n4), so the returned object is n4. No next link changes. Repeating the value 7 at every node does not affect the argument. Changing only the tail to None makes the chain acyclic even though all values still match.</Prose></details></section>
    <section className="linked-extension-practice"><H3>B. Keep both parts after a cut</H3><Prose>For an acyclic six-node chain, return both middle choices and perform the left-heavy split. Then repeat on seven nodes. A variable already points to n3; explain what it can still reach afterward.</Prose><details><summary>Hint: write the boundary index before changing next</summary><Prose>Use floor((n−1)/2) for the cut boundary, save its successor, and examine the old n3 alias separately from head.</Prose></details><details><summary>Explained split solution</summary><Prose>At length six, first middle=n2 and second=n3; cut n2.next, returning n0→n1→n2 and n3→n4→n5. The existing n3 reference reaches the entire right part. At length seven both middles are n3; cut n3.next, returning four nodes on the left and three on the right. The existing n3 reference now ends immediately, while the saved right head is n4. These are original nodes, not copied values; both lengths and aliases must be checked.</Prose></details></section>
    <section className="linked-extension-practice"><H3>C. Change equality and account for the burst</H3><Prose>Find strict next-greater distances for [4,4,2,5,5,3,6]. Change the requirement to greater-or-equal. Separately count pushes and pops for [9,8,7,6,10], and explain whether its last arrival takes constant work.</Prose><details><summary>Hint: equal-valued indices can have different distances</summary><Prose>Keep original positions; draw each stack immediately before the first 5 and before the final 6. Charge a pop to the index that leaves.</Prose></details><details><summary>Explained unresolved-index solution</summary><Prose>Strict distances are [3,2,1,3,2,1,0]. Greater-or-equal gives [1,2,1,1,2,1,0]: the next 4 and next 5 now qualify. For [9,8,7,6,10], all four earlier indices are popped by the final 10; there are five pushes and four pops overall. That last arrival has four pops, so an individual arrival is not constant work in general. The whole scan is linear because no index can be popped twice.</Prose></details></section>
    <section className="linked-extension-practice"><H3>D. Explain two different maximum rectangles</H3><Prose>Find both strictly smaller boundary arrays for [3,1,3,3,2]. Give two distinct maximum-area rectangles. Show how treating an equal bar as a blocker fails on the separate input [2,2].</Prose><details><summary>Hint: missing boundaries are −1 and n</summary><Prose>Use the two height-3 bars as one candidate plateau, then let height 2 extend farther. Equal values should be removed before reporting strictly smaller boundaries.</Prose></details><details><summary>Explained boundary solution</summary><Prose>L=[−1,−1,1,1,1] and R=[1,5,4,4,5]. Height 3 over indices 2–3 has area 3×2=6; height 2 over indices 2–4 has area 2×3=6. Both are optimal, so a correct witness need not be unique. On [2,2], true boundaries are L=[−1,−1], R=[2,2], giving area 4. If both directional scans incorrectly stop at equality, each bar sees width one and reports only area 2. State the strict boundary contract before choosing a comparison.</Prose></details></section>
    <Prose>You are ready to continue when you can explain which references an edit changes, state a loop invariant, choose LIFO versus FIFO by the task, and distinguish a bounded queue's full/empty states. Next in this module, <a href="/learn/topic/trees-binary-search-trees">Trees &amp; Binary Search Trees</a> extends linked relationships into branching structures. <a href="/learn/topic/complexity-analysis-recursion">Complexity Analysis &amp; Recursion</a> later makes operation-growth analysis more systematic. The reader's Next link includes planned lessons and follows your selected module order.</Prose>
    <DsaPractice practice={linkedPractice}/>
    <Sources alternatives={<LearningResources>
      <li><a href="https://www.youtube.com/watch?v=aV8LlSmd1E8">CS50x 2025, Lecture 5: Data Structures — YouTube lecture</a> with <a href="https://cs50.harvard.edu/x/2025/notes/5/">illustrated lecture notes</a> and <a href="https://cs50.harvard.edu/x/2025/weeks/5/">transcript and alternate player</a>. A visual second pass through nodes, lists, queues and stacks. It uses C pointers and allocation; focus on the diagrams first if C is unfamiliar. Python's reference and memory-management rules remain the ones used in this lesson.</li>
      <li><a href="https://docs.python.org/3/tutorial/datastructures.html#using-lists-as-stacks">Python tutorial: stacks and queues</a>. Short runnable alternatives using list and deque. Compare why the recommended end operations differ.</li>
      <li><a href="https://docs.python.org/3/library/collections.html#deque-recipes">Python deque recipes</a>. Continue the recent-window investigation with additional queue-based patterns; some recipes require iterator concepts introduced later.</li>
    </LearningResources>}>
      <li><a href="https://docs.python.org/3/library/collections.html#collections.deque">Python deque reference</a> — end-operation behavior, bounded eviction and indexing limitations.</li>
      <li><a href="https://docs.python.org/3/library/collections.html#collections.OrderedDict">OrderedDict reference and LRU recipes</a> — move_to_end, oldest removal and the distinction from ordinary insertion-ordered dict.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/resources/lecture-2-data-structures-and-dynamic-arrays/">MIT 6.006: data-structure interfaces</a> — sequence representations and why an operation's implementation matters.</li>
      <li><a href="https://cses.fi/book/book.pdf">Antti Laaksonen — Competitive Programmer's Handbook</a>, sections 8.2 and 16.4 (printed pages 79–80 and 155–156). The inspected 2018 draft supplies a concise written second pass through nearest-smaller stack charging and successor-cycle meeting/reset reasoning. Its successor graph always has a next node; our linked implementation additionally guards None. C++ notation differs from our complete Python programs. Read the local proofs first, then compare the contracts.</li>
    </Sources>
  </div>,
};
