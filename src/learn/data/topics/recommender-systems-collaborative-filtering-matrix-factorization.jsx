import { Callout, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { FactorRotationLab, FactorUpdateLab, FeedbackEvidenceLab, ImplicitConfidenceLab, LoggedPolicyLab, NeighborhoodEvidenceLab, PairwiseRankingLab, RecommendationSlateLab } from '../../components/lesson-labs/RecommenderLabs.jsx';
import { AmbiguousCompletionFigure, RecommendationPipelineFigure, RecommendationTimelineFigure } from '../../components/lesson-labs/RecommenderFigures.jsx';
import { recommenderExamples } from '../recommender-examples.js';
const headings = ['1. Start with a request and the evidence you actually have', '2. Put time, eligibility and baselines before the model', '3. Borrow evidence from people or items with shared history', '4. Replace a huge score table with shared factors', '5. Learn factors one observed rating at a time', '6. Hold one side fixed and solve the other', '7. Turn implicit events into targets and confidence', '8. Learn which of two items should come first', '9. Evaluate the whole recommendation list', '10. Use current libraries without changing the problem accidentally', '11. Handle new items and connect scores to a serving system', '12. Optional: distinguish logged rewards from a new policy', '13. Complete a small experiment with an honest report', '14. Practise, diagnose and choose the next step'];
function Program({
  example
}) {
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /></>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="rec-practice"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Get a hint</summary><Prose>{hint}</Prose></details><details><summary>Show explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Recommender Systems (Collaborative Filtering, Matrix Factorization)',
  readTime: '~100 min read + investigations and independent practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot rec-lesson">
    <LessonIntro prerequisites="Linear & Logistic Regression; Vectors, Matrices & Tensor Operations. We refresh dot products, gradients, linear solves and evaluation conventions where they are used." sections={headings.map(heading => [heading.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''), heading.replace(/^\d+\. /, '')])}>
      A recommender chooses useful items from many possibilities using incomplete evidence about people, items and their interactions. You will follow one small catalogue from its missing ratings through neighbors, learned factors and ranked lists, then test what the result does and does not establish.
    </LessonIntro>
    <Prose><strong>First pass:</strong> follow sections 1–5, the list investigation in section 9 and the complete experiment in section 13. Then return to ALS, implicit feedback and pairwise learning as alternative objectives and solvers. The optional policy section is a self-contained extension. This is a multi-session lesson: the goal is to explain and implement the choices, not memorize every formula at once.</Prose>
    <Callout type="insight" title="Every score answers a particular question">
      A predicted star rating, an implicit-feedback factor score, a pairwise preference gap and a click probability are different quantities. None becomes a universal measure of usefulness because it can be sorted. All numbers and catalogues in our worked examples are declared synthetic inputs or actual outputs of the shown programs; they are not performance benchmarks.
    </Callout>

    <H2>{headings[0]}</H2>
    <Prose>Imagine a catalogue of workshop recordings. A learner opens the site, and only three suggestions fit on the screen. The site knows some recordings they rated or played, but knows nothing explicit about most learner–recording pairs. The problem is to choose an eligible list that is likely to help this learner now. A list of globally popular recordings is already a recommender; personalization must earn its additional complexity.</Prose>
    <Prose>A <strong>user</strong> is the entity receiving a suggestion, an <strong>item</strong> is something that can be suggested, and a <strong>request</strong> is a particular decision at a particular time. An <strong>interaction</strong> is an event such as a rating, play or purchase. An <strong>impression</strong> records that an item was actually shown. These data units are not interchangeable: no play may mean lack of interest, no opportunity to see the item, or an unavailable item. A missing rating is not a low rating.</Prose>
    <Prose>We retain a small catalogue with five users, numbered 0–4, and seven items, numbered 0–6. The rating matrix has 20 observed entries and 15 missing entries. The observed mean is 3.1. User 0 rated items 0, 1, 3 and 5 as 5, 3, 1 and 4; items 2, 4 and 6 are unseen in this miniature example. The numerical IDs are arbitrary labels, not meaningful distances.</Prose>
    <FeedbackEvidenceLab />
    <Prose>An <strong>observation mask</strong> stores whether a rating exists separately from its numeric value. Write Ω for the set of observed user–item pairs. An explicit-rating objective sums errors only over Ω. It can represent a legitimate observed zero if the rating scale allows one. Our original 1–5 catalogue has no observed zero, but using <code>rating != 0</code> as a general mask would silently break that broader contract.</Prose>
    <MathBlock>{String.raw`\operatorname{SSE}_{\Omega}=\sum_{(u,i)\in\Omega}(r_{ui}-s_{ui})^2.`}</MathBlock>
    <Prose>Here r is an observed rating and s is the model's proposed score for that pair. The summation means “add one term for each observed pair.” Squaring makes both overprediction and underprediction costly, with larger misses receiving more weight. If every observed score were 3, this catalogue's sum of squared errors would be 36. There is no missing-cell contribution hidden in that number.</Prose>
    <Prose><strong>Run the examples:</strong> every block is a complete Python 3 script. Save one as <code>recommendation_example.py</code>, then run <code>python recommendation_example.py</code> or <code>py recommendation_example.py</code> on Windows. The main numerical route uses NumPy; the exact probability and ranking programs use the standard library. Install NumPy with <code>python -m pip install numpy</code>. The optional library section gives its extra dependencies. No prior block, notebook state, account or dataset download is required. Browser investigations compute locally; the Python blocks execute in your own environment.</Prose>
    <Program example={recommenderExamples.evidence} />
    <Practice title="A missing observation is not an error label" question="A learner was never shown item 6. Someone fills its missing rating with zero before calculating RMSE. What question has changed?" hint="Compare the set of error terms before and after filling.">
      <Prose>The new objective trains or evaluates toward an invented zero rating for that pair. It now treats an absence of evidence as a numerical target. The original explicit-rating question asks how accurately the model predicts observed ratings. An implicit-feedback method may deliberately use weak zero targets, but must declare that different objective and its weights; filling does not discover a true dislike.</Prose>
    </Practice>

    <H2>{headings[1]}</H2>
    <Prose>Before selecting a model, define what it is allowed to know and what it is allowed to recommend. For the final experiment we create an explicit event order: 20 training events, five later validation events and six still later test events. They are a teaching fixture, not a random sample from a real service.</Prose>
    <RecommendationTimelineFigure />
    <Prose><strong>Training</strong> fits parameters. <strong>Validation</strong> compares choices such as rank and regularization. <strong>Test</strong> estimates performance of the frozen choice on a new declared sample. Selecting a setting because it looked best on the test set turns that set into more validation. Likewise, computing an item's popularity using all events before splitting lets future behavior influence an earlier prediction.</Prose>
    <Prose>All learned quantities follow the same boundary: user/item means, co-rating similarities, factor vectors, metadata preprocessing and candidate-popularity tables. For temporal evaluation, no event later than the simulated request may enter its history. A global cutoff is a simple coherent policy. Splitting each user's events separately can be useful for a different task, but it can put another user's future event into a supposedly historical model unless the intended information boundary is stated.</Prose>
    <Prose><strong>Eligibility</strong> is separate from preference. A recording released tomorrow cannot appear in today's list. Access restrictions and unavailable items are hard filters. This lesson's final toy code uses seven items released at time zero and recommends previously unseen items. A music or grocery service may deliberately recommend repeats; never remove past interactions by habit when repeat use is the actual task.</Prose>
    <LessonTable caption="Three baselines and the precise evidence they need" headers={['Baseline', 'Calculation from training only', 'Purpose and limitation']} rows={[['Global mean', 'Average all observed ratings', 'A rating fallback with no personalization'], ['Shrunk item mean', '(sum of item ratings + s × global mean) / (item count + s)', 'Balances item evidence with a prior mean; s is a chosen pseudo-count'], ['Popularity ranking', 'Count eligible items’ training interactions; break ties by item ID', 'A list baseline; counts mix exposure and preference']]} />
    <Prose>Shrinkage keeps an item with one high rating from automatically outranking an item with extensive support. If an item's two training ratings are 5 and 1, the global mean is 3.1 and s=3, its shrunk mean is (6+9.3)/5=3.06. With no item ratings it returns 3.1. The pseudo-count is a modeling choice, not three secretly observed ratings. It belongs in validation if it is tuned.</Prose>
    <Prose>A <strong>warm</strong> user or item has training history; a <strong>cold</strong> one does not. A random interaction split usually measures another event for mostly familiar entities. A held-out-user or held-out-item split asks a harder, different question. Report which one you tested and what fallback covered the cold cases. Do not silently discard unknown IDs and then describe the result as covering every user.</Prose>

    <H2>{headings[2]}</H2>
    <Prose><strong>Collaborative filtering</strong> uses patterns of interaction across users and items. It does not require item descriptions. User-based filtering asks: “Which other people have rated a shared subset similarly, and what did they think of this item?” Item-based filtering asks: “Which items have similar rating patterns, and what did this user think of those?” These are two ways of reusing evidence, not two names for content similarity.</Prose>
    <Prose>For item i and neighbor j, let Sᵢⱼ contain users who rated both. We compare only their aligned observations. Their cosine similarity divides a dot product by the product of vector lengths. The dot product adds corresponding products; the lengths remove an overall magnitude scale. A zero length means the normalized comparison is undefined, so our implementation does not use that neighbor.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      C_{ij}&=\sum_{v\in S_{ij}}a_{vi}a_{vj},\\
      N_i&=\sqrt{\sum_{v\in S_{ij}}a_{vi}^2},\\
      N_j&=\sqrt{\sum_{v\in S_{ij}}a_{vj}^2},\\
      \operatorname{cos}(i,j)&=C_{ij}/(N_iN_j).
    \end{aligned}`}</MathBlock>
    <Prose>All three sums use the same shared set Sᵢⱼ, so the lengths Nᵢ and Nⱼ belong to this particular pair. For raw cosine, aᵥᵢ=rᵥᵢ. For <strong>adjusted cosine</strong>, subtract that co-rater's mean over their observed training ratings: aᵥᵢ=rᵥᵢ−r̄ᵥ. This asks whether both items sit above or below the person's usual rating level. The centering mean uses all that person's training ratings, while the cosine numerator and lengths use the shared subset. Different centering conventions define different estimators; state the one you implement.</Prose>
    <Prose>With only one co-rater and two positive raw ratings, cosine is exactly 1: ab/(|a||b|)=1. That does not mean the items are known to be interchangeable. A minimum overlap excludes unsupported comparisons. A shrinkage weight n/(n+s), with n shared users, reduces small-sample similarities. Neither operation creates missing evidence or supplies a statistical confidence interval.</Prose>
    <NeighborhoodEvidenceLab />
    <Prose>For a centered prediction, start at this user's baseline and add a weighted average of their deviations on chosen neighbors. Using an absolute-weight denominator avoids cancellation in the denominator when signed similarities are allowed:</Prose>
    <MathBlock>{String.raw`\hat r_{ui}=\bar r_u+\frac{\sum_{j\in N_{ui}}w_{ij}(r_{uj}-\bar r_u)}{\sum_{j\in N_{ui}}|w_{ij}|}.`}</MathBlock>
    <Prose>Nᵤᵢ contains supported neighbors the target user actually rated. Our default takes the three largest absolute weights after the support and sign rules, breaking ties by item ID. If the denominator is zero, use a fallback: this user's mean, then the training global mean, then a declared prior if the entire training dataset is empty. Do not divide by an empty set or quietly call the fallback a personalized estimate.</Prose>
    <Prose>The raw variant averages ratings without the residual baseline. In our original matrix, raw cosine with at least one co-rater and no shrinkage predicts user 0's item 2 at approximately 3.062518. Centering and requiring two co-raters leaves no positive usable neighbor for that target, so the honest result is the fallback 3.25. A more conservative method need not produce a more elaborate-looking answer.</Prose>
    <Program example={recommenderExamples.neighbors} />
    <Prose>Positive normalized weights keep an uncentered prediction between the contributing ratings. Signed residual estimates have no such bound; the lab can produce 5.5 on a 1–5 scale. You may clip a final rating prediction, but must evaluate that clipped predictor consistently. For a ranked list, monotone clipping can also create additional ties.</Prose>
    <Prose><strong>Cost depends on the operation.</strong> Given k cached usable neighbor weights, evaluating one pair takes O(k) arithmetic. Building similarities requires finding shared histories, and ranking every item requires many pair evaluations or an inverted candidate process. User-based versus item-based performance depends on sparsity, update frequency and catalogue size; neither has one universally cheaper total complexity.</Prose>

    <H2>{headings[3]}</H2>
    <Prose>A score table with a million users and a million items would contain a trillion entries. Matrix factorization instead gives each user a short vector pᵤ and each item a short vector qᵢ. Their dot product adds d products. The same user vector helps predict many items, and the same item vector learns from many users. This shared structure is what allows observations to influence missing-pair predictions.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      s_{ui}&=\mu+b_u+b_i+p_u^\top q_i,\\
      p_u^\top q_i&=\sum_{f=1}^{d}p_{uf}q_{if}.
    \end{aligned}`}</MathBlock>
    <Prose>μ is a global baseline, bᵤ and bᵢ are user and item offsets, and d is the number of latent coordinates. With P shaped U×d and Q shaped I×d, the product PQᵀ is U×I. “Latent” means the coordinates are learned rather than supplied as measured features. A user's tendency to rate high and an item's general appeal need not consume the same coordinates as their specific interaction.</Prose>
    <Prose>For μ=3, bᵤ=.1, bᵢ=−.1, p=(.4,−.2) and q=(.5,.3), the dot product is .20−.06=.14 and the score is 3.14. Each coordinate contributes a product. Negative coordinates are allowed; their sign by itself is not dislike. The complete dot product and objective determine the score's meaning.</Prose>
    <H3>Sharing structure does not identify the missing truth</H3>
    <AmbiguousCompletionFigure />
    <Prose>Both possible completions agree on every observation, so no training error criterion using only those two cells can distinguish them. More observations, regularization or content assumptions may help, but generalization is an empirical and modeling question. A low training error is not proof that the hidden entries were recovered.</Prose>
    <Prose>Even after predicted scores are fixed, factor coordinates need not be unique. If every vector undergoes the same orthogonal rotation R, then (Rp)ᵀ(Rq)=pᵀRᵀRq=pᵀq because RᵀR=I. Orthogonal transformations also preserve squared vector lengths, so an L2 penalty cannot choose a unique orientation. Naming a learned coordinate “science taste” requires evidence beyond its axis number.</Prose>
    <FactorRotationLab />
    <Prose>More generally P→PA and Q→QA⁻ᵀ preserves PQᵀ for any invertible A, but usually changes an L2 penalty. The dot-product part has rank at most d; adding user/item bias matrices can raise the rank of the whole score matrix. Parameter count is roughly (U+I)d plus biases, rather than UI stored scores. This saves representation space, but calculating every request's score for every item still costs O(Id) without a retrieval shortcut.</Prose>
    <Callout type="warning" title="The name SVD does not settle the objective">
      The truncated SVD of a fully specified matrix solves a particular squared-error approximation problem. Filling missing ratings with zero and taking that decomposition includes those invented zeros in its objective. Observed-entry regularized MF solves a different problem. Some recommender libraries call the latter “SVD” for historical naming reasons; inspect the equation and algorithm rather than the label.
    </Callout>

    <H2>{headings[4]}</H2>
    <Prose>To learn factors, compare a prediction with an observed rating and move the parameters to reduce a declared loss. For one record, define error e=r−s. We use a local loss with a one-half factor, which cancels the 2 when differentiating a square:</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      \ell_{ui}&=\tfrac12 e_{ui}^2+\tfrac\lambda2(\|p_u\|^2+\|q_i\|^2)\\
      &\quad+\tfrac\lambda2(b_u^2+b_i^2).
    \end{aligned}`}</MathBlock>
    <Prose>The full training objective in the next SGD program is the sum of these local terms over observed records. This means the penalty for a user with nᵤ records is counted nᵤ times. We choose that convention explicitly; it is not the same objective as penalizing every user and item only once. μ remains the observed training mean in this program.</Prose>
    <Prose>The gradient tells us the instantaneous change of the loss as a parameter changes. Since ∂e/∂p=−q, the chain rule gives ∇ₚℓ=−eq+λp. Subtracting a step size η times that gradient yields the familiar update. The item and bias equations follow by the same argument:</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      p_u^{new}&=p_u+\eta(e q_i-\lambda p_u),\\
      q_i^{new}&=q_i+\eta(e p_u-\lambda q_i),\\
      b_u^{new}&=b_u+\eta(e-\lambda b_u),\\
      b_i^{new}&=b_i+\eta(e-\lambda b_i).
    \end{aligned}`}</MathBlock>
    <Prose>Every right-hand side uses the same <strong>old</strong> values and old error. In mutable code, copy the old p and q before changing either. Using the newly updated p in the q expression performs a different update; it is not the simultaneous gradient step just derived. That alternative needs its own analysis rather than a claim that it “always converges faster.”</Prose>
    <FactorUpdateLab />
    <Prose>For the .4,−.2 / .5,.3 fixture above, r=5 gives e=1.86. At λ=.1 and η=.05, the new user vector is (.4445,−.1711), the new item vector is (.5347,.2799), and the local loss decreases from 1.7578 to about 1.350450. A rate of 1 makes that same one-step loss about 5.698610. A downhill derivative describes sufficiently small movement, not every finite step.</Prose>
    <Program example={recommenderExamples.gradient} />
    <Prose>An <strong>epoch</strong> is one traversal of the observed records. We shuffle their order with a fixed pseudorandom seed and take one local update at a time. A rolling error accumulated during the epoch uses changing parameter values. An end-of-epoch training RMSE recomputes every prediction using the final fixed parameters. Held-out RMSE uses different records that did not determine those parameters. These three quantities answer different questions.</Prose>
    <MathBlock>{String.raw`\operatorname{RMSE}=\sqrt{\frac1n\sum_{j=1}^n(r_j-s_j)^2}.`}</MathBlock>
    <Prose>The square root returns the error to rating units. The regularized objective additionally includes penalty terms; it is not RMSE. Our full-matrix mechanism run prints the rolling value, fixed training RMSE and objective separately, and deliberately prints no test claim.</Prose>
    <Program example={recommenderExamples.explicitFit} />
    <Prose>Initialize factors with small nonzero random values. If both sides start exactly at zero, their dot-product gradients vanish; bias updates alone do not unstick the factors. Larger rank permits more interactions, and positive regularization discourages large parameters, but neither proves recovery or prevents every form of overfitting. Tune them against the intended validation task, inspect variation across seeds, and stop or lower the learning rate when training becomes unstable.</Prose>
    <details><summary>Deeper: reconcile two regularization conventions</summary>
      <Prose>Suppose J is one-half the observed squared-error sum plus λ/2 times each user's and item's squared norm once. To distribute its penalty across local record updates exactly, the pᵤ penalty in each of nᵤ records can use λ/nᵤ, and the qᵢ penalty can use λ/nᵢ. Alternatively define the degree-weighted objective intentionally, as our program does. Bias penalties follow the same counting issue. Sampling triples or records nonuniformly adds another weighting choice; the stochastic gradient's expectation must match the stated objective if you claim unbiased gradient estimation.</Prose>
      <Prose>A sum and an average loss also change the effective penalty unless λ is rescaled. Therefore equal numeric λ values across libraries or SGD/ALS implementations need not mean equally strong regularization. Compare equations and evaluation, not parameter names alone.</Prose>
    </details>

    <H2>{headings[5]}</H2>
    <Prose>With item vectors fixed, learning one user vector is a much simpler problem: ordinary regularized least squares. <strong>Alternating least squares</strong> (ALS) solves all user blocks with items fixed, then all item blocks with users fixed, and repeats. The overall factor problem is not jointly convex, but each positive-regularized block is a strictly convex quadratic.</Prose>
    <Prose>For clarity, this program omits biases and uses a once-per-entity penalty. Let Qᵤ contain the item vectors for items that user u actually rated, and rᵤ the matching ratings. Its block objective is ‖rᵤ−Qᵤpᵤ‖²+λ‖pᵤ‖². Differentiating, setting the gradient to zero and regrouping gives:</Prose>
    <MathBlock>{String.raw`(Q_u^\top Q_u+\lambda I)p_u=Q_u^\top r_u.`}</MathBlock>
    <Prose>I is the identity matrix: it leaves a vector unchanged. For any nonzero vector v, vᵀ(QᵤᵀQᵤ+λI)v=‖Qᵤv‖²+λ‖v‖² is positive when λ&gt;0. This proves the normal matrix is positive definite, hence invertible and the block minimum unique. Use a linear-system solver rather than explicitly forming an inverse. If the user's history is empty, the right side is zero and the positive penalty gives pᵤ=0.</Prose>
    <Prose>The item equation swaps the roles of P and Q. Updating one exact block cannot increase this declared objective because it replaces the previous feasible vector with that block's minimizer. Repeating gives nonincreasing objective values in exact arithmetic. It does not prove a globally best factorization, uniquely interpretable factors, or universally faster optimization. Floating-point roundoff and inexact iterative solves require their own tolerances.</Prose>
    <Program example={recommenderExamples.observedAls} />
    <Prose>To include fixed biases in a factor solve, replace rᵤ by rᵤ−μ−bᵤ−bᵢ on the corresponding records. Biases can then receive their own scalar block updates. To match the earlier degree-weighted penalty, the user normal system needs λnᵤI rather than λI. These are straightforward modifications once the objective is explicit; silently mixing them would compare different problems.</Prose>

    <H2>{headings[6]}</H2>
    <Prose>Most services collect many more plays, views or purchases than star ratings. These are <strong>implicit feedback</strong>: behavior may suggest interest without directly stating a preference score. Repeating a workshop 15 times might indicate interest, confusion, a shared account or repeated background playback. The count is evidence whose meaning depends on the logging process.</Prose>
    <Prose>A useful classical model separates a binary target from its confidence weight. Let nᵤᵢ be an event count during a declared window, zᵤᵢ=1 when the count is positive and 0 otherwise, and cᵤᵢ=1+αnᵤᵢ. An observed pair has target 1 with stronger weight; an unobserved pair has target 0 with baseline weight 1. That weak zero is a deliberate modeling assumption, not a discovered negative rating.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      E&=\sum_{u,i}c_{ui}(z_{ui}-p_u^\top q_i)^2,\\
      J&=E+\lambda\sum_u\|p_u\|^2\\
      &\quad+\lambda\sum_i\|q_i\|^2.
    \end{aligned}`}</MathBlock>
    <Prose>E is the data-error sum, and J adds the penalties. This sum includes <strong>all user–item pairs</strong>. The penalty is once per entity. Larger α makes recorded events more influential relative to the many missing pairs; it does not transform counts into probabilities. Scores remain unconstrained dot products and can be negative or exceed 1. Calling a negative score “a probabilistic dislike” would add a meaning the objective does not provide.</Prose>
    <Prose>With Q fixed, use diagonal Cᵤ containing that user's weights and vector zᵤ containing all its targets. The normal system is (QᵀCᵤQ+λI)pᵤ=QᵀCᵤzᵤ. In the following two-coordinate example, item factors are (1,0), (0,1), (1,1), counts are (2,0,1), α=2 and λ=1. Therefore weights are (5,1,3), the normal matrix is [[9,3],[3,5]], and the right side is (8,3).</Prose>
    <ImplicitConfidenceLab />
    <Prose>Solving gives p=(31/36,1/12), approximately (.861111,.083333). The unobserved second item's score .083333 is not exactly zero because the same p must balance several terms. Removing its zero-target term changes the objective and the optimum to (23/27,1/9). It does not merely save work on an equivalent problem.</Prose>
    <H3>Keep all missing terms without iterating over all of them</H3>
    <Prose>Since missing weights are 1, split Cᵤ into I+(Cᵤ−I). The first part is a Gram matrix shared by every user, and only observed entries contribute to the correction:</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      Q^\top C_uQ&=Q^\top Q\\
      &\quad+\sum_{i:n_{ui}>0}(c_{ui}-1)q_iq_i^\top,\\
      Q^\top C_uz_u&=\sum_{i:n_{ui}>0}c_{ui}q_i.
    \end{aligned}`}</MathBlock>
    <Prose>A Gram matrix QᵀQ adds qᵢqᵢᵀ over all items once. Each outer product qᵢqᵢᵀ contains every pair of that item's factor coordinates. The correction increases the baseline weight only where events occurred. The right side uses only positive targets. This identity is exact for the declared weights; dropping the Gram term would remove the missing-pair losses.</Prose>
    <Program example={recommenderExamples.implicitBlock} />
    <Prose>Computing the shared Gram costs O(Id²) for I items and d factors. For a user with nᵤ observed items, assembling the correction costs O(nᵤd²); a dense direct factor solve costs O(d³). Iterative solvers have different iteration-dependent costs. The identity avoids an O(UI) pass over all missing targets in each user update, while still accounting for their baseline quadratic contribution.</Prose>
    <Program example={recommenderExamples.implicitFit} />
    <Prose>The preserved count matrix's miniature fit can give negative unseen scores; the program labels them as scores. An empty user gets zero factors under this fixed-Q, positive-penalty objective, so every item ties before any additional bias, content or popularity policy. Confidence strength, transformations such as log counts, rank and regularization must be compared under a held-out task. There is no rule that implicit ALS always beats an explicit model or that one α fits every event type.</Prose>

    <H2>{headings[7]}</H2>
    <Prose>A list may need the right relative order more than accurate star ratings. <strong>Bayesian personalized ranking</strong> (BPR) trains comparisons: for a user u, an interacted item i is assumed preferable to a sampled unobserved item j. The triple (u,i,j) is the training unit. A missing item can be useful but undiscovered, so this is a supervision assumption, not ground-truth preference for every pair.</Prose>
    <Prose>Let Δ=sᵤᵢ−sᵤⱼ. Positive Δ ranks i first. The logistic sigmoid σ(Δ)=1/(1+exp(−Δ)) turns the score gap into the model's pairwise preference probability. The negative log of that probability is softplus(−Δ)=log(1+exp(−Δ)). It is small for a strongly correct order and large for a strongly reversed order.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      \ell_{uij}&=\log(1+e^{-\Delta})\\
      &\quad+\tfrac\lambda2(\|p_u\|^2+\|q_i\|^2)\\
      &\quad+\tfrac\lambda2\|q_j\|^2.
    \end{aligned}`}</MathBlock>
    <Prose>This local teaching program counts the displayed penalty for each sampled triple. Write g=σ(−Δ). Differentiating the data term with respect to Δ gives −g. For Δ=pᵤᵀ(qᵢ−qⱼ), the old-value updates are:</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      p_u^{new}&=p_u+\eta[g(q_i-q_j)-\lambda p_u],\\
      q_i^{new}&=q_i+\eta[g p_u-\lambda q_i],\\
      q_j^{new}&=q_j+\eta[-g p_u-\lambda q_j].
    \end{aligned}`}</MathBlock>
    <Prose>At a tie, g=.5: the data term still has a learning signal. When i is already far ahead, the signal shrinks. User bias and global mean cancel from the score difference, while distinct item biases would not. The probability above concerns the model's pair comparison; it is not automatically a calibrated probability that a user will click either item when shown a slate.</Prose>
    <PairwiseRankingLab />
    <Program example={recommenderExamples.bpr} />
    <Prose>Use a stable softplus calculation, such as NumPy's <code>logaddexp(0, -gap)</code>, rather than overflowing <code>exp</code> for extreme gaps. Our finite-difference check differentiates the complete actual loss. Training a recommender would draw many valid triples, not repeatedly optimize just this demonstration pair.</Prose>
    <details><summary>Deeper: the sampler is part of the statistical question</summary>
      <Prose>Uniform sampling over all valid triples gives more total weight to a user with more positive–unobserved combinations. Sampling a user uniformly, then a positive uniformly and an unobserved item uniformly, instead gives each user equal expected weight. Popularity-weighted or hard-negative sampling changes which comparisons receive attention again. These choices need not have the same optimum. To estimate the gradient of a fixed target-weighted objective using a different sampling law, importance ratios are required wherever the target assigns mass; missing support cannot be corrected by a weight.</Prose>
      <Prose>Uniformly choosing j from “not interacted before this request” must respect item availability and the time boundary. A future positive should not be exposed as a known positive while simulating past training. If you sample from shown-but-unclicked impressions, you get more direct exposure evidence, but position, context and delayed outcomes still matter. The classical BPR likelihood's pairwise independence assumptions are modeling conveniences; observed comparisons can share users and items. It is a smooth ranking surrogate, not an exact optimizer of every top-K metric or a universal winner.</Prose>
    </details>

    <H2>{headings[8]}</H2>
    <Prose>Rating RMSE asks whether numeric estimates match ratings. It can improve while a top-three list gets worse, because its errors are averaged over different items and do not directly reward position. To evaluate recommendations, first freeze the eligible catalogue, candidate set, relevance labels, cutoff K, treatment of ties and user cohort.</Prose>
    <Prose>A <strong>candidate set</strong> is the subset retrieved for scoring. The <strong>slate</strong> is its ordered list. Our lab uses distinct item IDs, rejects repeats and lets you move entries directly. Its evaluation labels are declared knowledge for a toy evaluator; they do not pretend every missing real-world rating is a known negative.</Prose>
    <RecommendationSlateLab />
    <LessonTable caption="Our explicit per-request metric conventions" headers={['Metric', 'Calculation', 'Question']} rows={[['Precision@K', 'Relevant items in first K slots / K; unfilled slots count as misses', 'How much of the requested visible space is useful?'], ['Recall@K', 'Relevant retrieved items in first K / eligible relevant items', 'How much labeled relevant material did we find?'], ['Reciprocal rank@K', '1 / first relevant rank, or 0 if none appears in K', 'How soon does the first success appear?'], ['Average precision@K', 'Sum precision at relevant ranks ≤K, divided by min(K, R)', 'How consistently do relevant items occur early?'], ['NDCG@K', 'Discounted gain / ideal discounted gain for the eligible labels', 'How good is the order relative to its stated ideal?']]} />
    <Prose>R denotes the number of eligible labeled relevant items. AP@K has multiple conventions in practice; some divide by R rather than min(K,R). We state and implement the latter. With no eligible relevant labels, recall, AP, reciprocal rank and NDCG are undefined in this lesson; report the excluded count when averaging those metrics. Precision and fill rate remain defined for K&gt;0. This is a cohort convention, not a claim that an unlabeled request has no useful recommendations.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      \operatorname{DCG}@K&=\sum_{k=1}^{K}\frac{2^{g_k}-1}{\log_2(k+1)},\\
      \operatorname{NDCG}@K&=\frac{\operatorname{DCG}@K}{\operatorname{IDCG}@K}.
    \end{aligned}`}</MathBlock>
    <Prose>gₖ is a declared relevance grade at rank k; nonexistent slots have zero gain. The denominator log₂(k+1) discounts later positions, and 2ᵍ−1 chooses an exponential gain convention. For binary labels the gain is simply 0 or 1. IDCG sorts the eligible label grades ideally, not merely the candidate subset that happened to survive retrieval. If IDCG=0, division is undefined.</Prose>
    <Prose>Initially the list [2,0,4,1,3] has relevant items 0 and 1 and K=3. Only rank 2 is relevant: precision=1/3, recall=1/2, reciprocal rank=1/2 and AP=(1/2)/2=1/4. DCG=1/log₂3≈.630930; the ideal is 1+1/log₂3≈1.630930, so NDCG≈.386853. Moving item 0 to rank 1 improves position-sensitive metrics without changing how many relevant items occupy the first three slots.</Prose>
    <Program example={recommenderExamples.ranking} />
    <Prose>If retrieval omits one of two relevant items, even perfect reranking cannot return both. More generally, its best possible recall at K is min(K, number of relevant candidates)/R. Changing the candidate pool also changes difficulty: testing against 99 sampled negatives is not directly comparable to ranking against the entire eligible catalogue. A sampled-negative metric may answer a useful bounded question, but must name its sampling procedure.</Prose>
    <Prose>Average per-user metrics to give each included user equal weight, or weight requests/events if that is the intended deployment unit. Report the choice. Repeated events from one user are not independent evidence about many users; uncertainty estimation should respect the sampling unit and temporal dependencies. For a real evaluation, include warm/cold and activity cohorts, candidate coverage, fill rate, latency/resource constraints and a task-appropriate outcome. Good offline agreement alone does not establish that a deployed policy causes better learning, satisfaction or retention.</Prose>

    <H2>{headings[9]}</H2>
    <Prose>The following routes were executed with Python 3.12, NumPy 2.3.5, SciPy 1.18.1, <code>implicit 0.7.3</code> and <code>scikit-surprise 1.1.5</code>. They are optional API demonstrations after the mathematical implementations. Package APIs and binary compatibility can change; inspect the linked current documentation and verify installed versions when adapting them.</Prose>
    <Prose>Install the additional packages with <code>python -m pip install scipy pandas implicit==0.7.3 scikit-surprise==1.1.5</code>. The examples use CPU execution, fixed seeds and one implicit worker thread. The lesson supplies all data. Small floating-point differences across numerical-library builds can change last digits or near-tied orders; actual output here is from the stated environment.</Prose>
    <H3>implicit: user rows and confidence entries</H3>
    <Prose>The current CPU ALS <code>fit</code> method accepts a <strong>user×item CSR matrix</strong>: rows are users, columns are items. Its stored positive entries are confidence weights for target 1. Absent entries mean target 0 with baseline confidence 1. Our counts therefore become sparse values 1+αn on the observed entries, with model <code>alpha=1</code>. Passing raw counts and hoping the library adds the baseline would define another confidence rule.</Prose>
    <Prose>The implementation may internally transpose for its item update; that does not reverse the public input contract. The example asserts factor shapes (5,3) and (7,3). It also recalculates one user against the final item factors and checks the result against a directly assembled normal system. The last alternating item sweep changes Q, so the stored user vector from the preceding user sweep need not already minimize the block for final Q.</Prose>
    <Program example={recommenderExamples.implicitLibrary} />
    <Prose>Recommendations filter the actual historical interactions, not all low scores. Its item 4 score can exceed 1; this is entirely compatible with the unconstrained factor objective. A default iterative approximation, a different factor count, alpha placement or stale factor cache can change results. Validate those settings before attributing a discrepancy to an unexplained algorithmic mystery.</Prose>
    <H3>Surprise: explicit-rating MF and cold-ID behavior</H3>
    <Prose>Surprise's biased <code>SVD</code> implements regularized factor learning on rating triplets, rather than a zero-filled matrix decomposition. Its raw user/item identifiers are mapped into an internal training index. The program uses explicit string IDs and retrieves predictions through the public interface, avoiding accidental confusion between raw and internal IDs.</Prose>
    <Program example={recommenderExamples.surpriseLibrary} />
    <Prose>We request <code>clip=False</code> to inspect the mathematical prediction; the default prediction interface clips to the declared rating scale. In this biased model an unknown user loses its user bias and factor interaction, while a known item's bias can remain. If both are unknown, the prediction is the training global mean, here 3.1. Those are fallbacks, not learned personalization for an unseen entity.</Prose>
    <Prose>The two packages solve different objectives with different conventions. Their toy ranks are not a competition. The original workflow's data and useful algorithms are retained, but each shown output now comes from that actual program rather than an approximate result borrowed from another implementation.</Prose>

    <H2>{headings[10]}</H2>
    <Prose>Pure collaborative filtering needs interaction evidence. If a new workshop has no ratings, a factor table alone cannot tell what it contains. A <strong>content-based</strong> method uses attributes such as subject, duration, language or measured text features. A <strong>hybrid</strong> combines content with collaborative history, so new items can receive a provisional representation and familiar items can also use interaction evidence.</Prose>
    <Prose>One transparent hybrid fits a ridge map from training-item metadata X to already learned item factors Q. With rows aligned to the same items, choose W to minimize ‖XW−Q‖²<sub>F</sub>+λ‖W‖²<sub>F</sub>. The Frobenius square means add the squares of all entries. Differentiating each output column gives (XᵀX+λI)W=XᵀQ. New metadata x then produces q̂=xW and score pᵤᵀq̂.</Prose>
    <Program example={recommenderExamples.coldItem} />
    <Prose>This is an original synthetic metadata example, not an inferred genre map from the rating toy. It assumes new items follow a relationship learned from old ones. Novel languages, missing features, changed item types or biased training coverage can break that transfer. A new user needs a different fallback: declared interests, a short onboarding choice, context, popularity or deliberate exploration. It is not enough to invent a random personalized factor and present it confidently.</Prose>
    <RecommendationPipelineFigure />
    <Prose>Factor scores provide a useful <strong>candidate-generation</strong> interface: retrieve items with large inner product, then apply a richer ranking model or list policy to a much smaller set. For one user, global and user biases do not change item order, but item bias does. An exact augmented dot product can include it: concatenate 1 to pᵤ and bᵢ to qᵢ. Replacing inner product with cosine normalization changes the ranking unless vector norms are handled deliberately.</Prose>
    <Prose>For example p=(1,0), item A=(2,0) and B=(1,1) yield dot scores 2 and 1. Cosines are 1 and 1/√2 here, but consider A=(2,2) and B=(1.5,0): dot ranking favors A, while cosine ranking favors B. An index configured for the wrong similarity can therefore change the recommender before the final ranker runs.</Prose>
    <Prose>Exact scoring costs O(Id). Approximate nearest-neighbor or maximum-inner-product retrieval trades resources against candidate recall; it has no universal logarithmic runtime or hardware-independent speed multiplier. Filtering after retrieving too few candidates can leave a short slate, so oversampling, eligibility-aware retrieval and fill-rate monitoring matter. The existing Recommendation System Design topic owns index updates, feature stores, deployment and detailed candidate/ranking/reranking architecture.</Prose>
    <details><summary>Optional architecture bridge: two towers, context and sequences</summary>
      <Prose>A two-tower model replaces lookup vectors with learned functions p=f(user features, history, context) and q=g(item features). Their dot product retains a retrieval-friendly interface if the item representation can be precomputed for the serving context. A general network that mixes every user feature with every item feature may model richer interactions, but usually cannot precompute a single reusable item vector for exact dot-product retrieval. Candidate generation and final ranking can therefore use different models.</Prose>
      <Prose>Sequence models can distinguish the same set of plays in different orders; a bag of counts cannot. “Played an introductory lesson, then a project tutorial” differs from the reverse when predicting the next activity. Time-aware features must be computed from the prefix before the request, and a feature such as eventual completion must not enter an earlier prediction. The historical 2016 YouTube paper describes separate candidate-generation and ranking networks; it is a useful architecture example, not evidence that today's whole service or every domain uses the same design.</Prose>
      <Prose>Neural interaction models, session architectures and retrieval-system operation belong to their existing specialist topics. Their role here is to show how the factor interface can be extended and where its precomputation assumptions matter, not to claim a few network names complete those fields.</Prose>
    </details>
    <Prose>A useful application beyond entertainment is <strong>matching maintenance engineers with relevant troubleshooting reports</strong>. Items can have equipment and fault metadata, while collaborative evidence comes from prior successful use. A cold report can enter through metadata, and a familiar engineer's history can personalize ranking. The outcome should distinguish a clicked report from a resolved fault; availability, equipment compatibility and access restrictions belong before or alongside ranking. This is a proposed design example, not a claimed deployment or measured benefit.</Prose>
    <Prose>For workshop recommendations, a list of nearly identical advanced lectures might score highly but fail a novice. Reranking may enforce prerequisites, reduce redundancy or balance breadth. State the rule and measure its intended outcome: pairwise dissimilarity is not automatically learning value, and high click-through is not automatically mastery. Different stakeholder goals can require explicit constraints and tradeoffs rather than a single unexplained “engagement” number.</Prose>

    <H2>{headings[11]}</H2>
    <Prose>This optional section answers a narrower causal question under fully declared assumptions. Suppose each request randomly shows exactly one of two items. The logger chooses A with probability .8 and B with probability .2, independently of the request's potential rewards. A clicks with probability .4, B with .8 in this stationary synthetic environment. The expected logged click rate is .8×.4+.2×.8=.48.</Prose>
    <Prose>A proposed policy chooses A and B equally, so its expected click rate is .5×.4+.5×.8=.6. The raw logged mean targets the logger's mixture, not the proposed mixture. If the assignment probabilities are known and every target action has positive logging probability, an <strong>inverse-propensity</strong> or importance-weighted reward rebalances the observations.</Prose>
    <MathBlock>{String.raw`\widehat V=\frac1n\sum_{t=1}^n\frac{\pi_{target}(A_t)}{\pi_{log}(A_t)}Y_t.`}</MathBlock>
    <Prose>Aₜ is the action actually shown and Yₜ its observed reward. An A click receives weight .5/.8=.625; a B click receives .5/.2=2.5. These propensities are <strong>assignment probabilities</strong>, not click probabilities. In the finite model, taking the expectation cancels the logging mass: summing π<sub>log</sub>(a)×π<sub>target</sub>(a)/π<sub>log</sub>(a)×E[Y|a] leaves the target mixture. That is the entire unbiasedness argument here.</Prose>
    <LoggedPolicyLab />
    <Program example={recommenderExamples.policy} />
    <Prose>Expectation is not a guarantee for one sample. The declared 20-request sample has nine clicks, raw rate .45 and weighted estimate .5625; neither must equal its population expectation. For the original probabilities, the one-request weighted reward has second moment 1.125 and variance 1.125−.6²=.765. Under independent requests, the mean's standard error is √(.765/n). Rare actions can create large weights and high variance even with unbiasedness.</Prose>
    <Prose>If B is never shown while the target sometimes selects B, its reward mean is not identified by those logs. Two worlds with different B rewards generate the same A-only observations. Filling B's missing rewards, clipping large weights or self-normalizing weights does not prove the original unbiased target; such estimators require their own bias/variance and support analysis.</Prose>
    <Prose>Real policies often condition on user and context, so ratios and support are conditional too. Observational exposure influenced by unlogged preferences can confound a comparison. Slates introduce positions and interactions, while changing a policy can change future histories. Known random assignment, correct reward observation, an appropriate unit and a stable declared target are substantive assumptions. A high offline score on arbitrary clicks cannot establish the causal benefit of a new recommender. More general causal inference and online experimentation are separate topics.</Prose>

    <H2>{headings[12]}</H2>
    <Prose>Now connect the pieces in a complete small workflow. We preserve the 20 original ratings as training events 1–20, add five explicit later validation ratings, and then six test ratings. The last test event belongs to a new user. Every event is specified in the program's literal data, and every item is declared released at time zero.</Prose>
    <Prose>The experiment compares ranks 1, 2 and 3 with penalties .05 and .2 on validation RMSE. It then refits the chosen setting on events 1–25, computes a shrunk-item baseline from that same prefix, and evaluates both on the six untouched test events. It reports all, warm-user and cold-user cohorts. The cold-user MF fallback is the global mean plus the known item bias, as explicitly implemented.</Prose>
    <Program example={recommenderExamples.evaluation} />
    <Prose>The first split chooses rank 2 and penalty .05, with validation RMSE about 1.337272. On six test events, the shrunk-item baseline has RMSE about 1.111143 and MF about 1.229386. The factor model therefore loses this comparison. A sophisticated method does not deserve a rewritten result. Six synthetic events also do not establish that either method wins in a real population.</Prose>
    <Prose>User 0's eligible unseen items after refitting are 4 and 6. Popularity orders them [4,6], while the chosen factor model orders them [6,4]. That list comparison is an additional diagnostic, not an RMSE ranking theorem. We do not label every unobserved catalogue item as a negative to fabricate a complete relevance test.</Prose>
    <Prose>For the changed exercise, moving the initial cutoff to 18 leaves seven validation events and the same six test events. The chosen penalty becomes .2 and final MF test RMSE about 1.128322, still above the same item baseline. Treat this second run as a predeclared teaching comparison. Repeatedly inspecting these test results to choose a cutoff would consume the test set; a final deployment claim would require fresh evaluation.</Prose>
    <LessonTable caption="A reproducible report names the decision behind each number" headers={['Report field', 'This tiny experiment', 'What a real study still needs']} rows={[['Target and unit', 'An observed rating event; squared error', 'A justified outcome and observation process'], ['Information boundary', 'Integer time cutoffs; all features fitted on the prefix', 'Versioned timestamps, availability and feature replay'], ['Baselines and selection', 'Shrunk item mean; six MF configurations selected on validation', 'Declared search budget and model-specific baselines'], ['Evaluation cohort', 'Six test events, including one cold user', 'Adequate users/time periods, uncertainty and subgroup reporting'], ['List behavior', 'Unseen eligible items; explicit deterministic tie rule', 'Candidate recall, fill rate and actual ranking labels'], ['Claim', 'Executable mechanism comparison on invented data', 'Independent evidence before generalizing or claiming causal value']]} />

    <H2>{headings[13]}</H2>
    <Prose>Try these with the explanations closed. A correct answer should identify its evidence, target, denominator or objective before producing a number. Use the programs to test changed inputs, then explain why the result follows. The first exercises test reasoning, the later ones combine mechanisms.</Prose>
    <Practice title="A. Audit a past request" question="At time 20, a learner asks for recommendations. A candidate was released at 23, similarities were computed from events through 30, and a validation setting was chosen using events 21–25. Which parts may serve the time-20 request? How would you repair the replay?" hint="A valid configuration choice and a valid historical information set are separate constraints.">
      <Prose>The unreleased candidate is ineligible. Similarities through 30 leak future interactions. A setting selected using events 21–25 was also unavailable at time 20 unless it had been chosen independently in an earlier experiment. To replay time 20, use only configuration decisions and fitted artifacts available then, and filter releases at or before 20. Alternatively move the evaluation request after validation, refit the allowed prefix, and keep later outcomes untouched. Name that different experiment honestly.</Prose>
    </Practice>
    <Practice title="B. Explain a signed neighbor prediction" question="A user's baseline is 3. Two usable neighbors have ratings 1 and 5, with signed weights −.5 and .25. Calculate the centered prediction. What happens if both neighbors fail the support rule?" hint="Subtract the same target-user baseline, and divide by the sum of absolute weights.">
      <Prose>Residuals are −2 and 2. Their weighted contributions are 1 and .5, so the residual prediction is 1.5/.75=2 and the full prediction is 5. If both are rejected, no weighted prediction remains and the declared fallback is 3. A negative similarity reverses a residual's direction; it is not a negative confidence count.</Prose>
    </Practice>
    <Practice title="C. Give a different rank-one completion" question="Only the two diagonal entries of a 2×2 matrix are observed, both equal to 1. Construct a completion different from either figure and prove its rank. Does equal validation performance at ranks 2 and 4 prove the population has intrinsic rank 2?" hint="Choose nonzero off-diagonal entries whose product is 1. Separate an algebraic rank statement from finite validation evidence.">
      <Prose>[[1,3],[1/3,1]] has determinant 1−1=0 and is nonzero, so its rank is one. Its second row is one-third its first. It fits the observations and disagrees on missing entries. Similar validation at ranks 2 and 4 only says that the chosen finite evaluation did not distinguish those trained settings. Optimization, regularization, sample size and noise can all mask differences; it is not a proof of intrinsic rank.</Prose>
    </Practice>
    <Practice title="D. Recompute a simultaneous gradient step" question="Change the gradient program to p=(.2,.4), q=(−.3,.5), user bias .05, item bias .1, rating 2, mean 3, λ=.2 and η=.1. Predict the error's sign, compute all new values, and keep the finite-difference assertion." hint="The old score is 3.29. Use its single old error in every update before recomputing any score.">
      <Prose>The error is −1.29. The gradients for (p₁,p₂,q₁,q₂,bᵤ,bᵢ) are (−.347,.725,.198,.616,1.3,1.31), giving (.2347,.3275,−.3198,.4384,−.08,−.031). The new score is about 2.957519 and the local objective falls from .8873 to .504838. The finite-difference assertion passes for the complete changed loss. Reusing the new p in q's update would no longer equal these simultaneous gradients.</Prose>
    </Practice>
    <Practice title="E. Match the penalty to the equation" question="One user has four observed records. The SGD objective counts λ‖p‖²/2 inside every record, while an ALS derivation counts it only once. What diagonal penalty belongs in the ALS user normal system to match that SGD objective? What if the user has no observations?" hint="Collect all copies of the same user's penalty before differentiating.">
      <Prose>Four copies produce an effective 4λ penalty, so add 4λI to QᵤᵀQᵤ. With no records, the degree-weighted objective contains no term for this user's p at all, so it does not identify a unique vector. A zero-vector fallback is a chosen convention. In contrast, a once-per-entity positive penalty uniquely minimizes the empty-history block at zero. The distinction explains why multiplying a penalty by degree affects more than a parameter label.</Prose>
    </Practice>
    <Practice title="F. Solve a changed implicit block" question="Keep item factors (1,0), (0,1), (1,1), but change counts to (0,3,1), α=1 and λ=2. Form targets, weights, A and b, solve for p, and verify with the augmented least-squares check." hint="The missing first item still has weight 1. The two observed items contribute to b.">
      <Prose>Targets are (0,1,1), weights (1,4,2), A=[[5,2],[2,8]] and b=(2,6). The determinant is 36, yielding p=(1/9,13/18). Scores are (1/9,13/18,5/6), approximately (.111111,.722222,.833333), and the block objective is 13/9≈1.444444. The augmented least-squares solution agrees. If all counts become zero, b=0 and positive λ again gives p=0.</Prose>
    </Practice>
    <Practice title="G. Diagnose a reversed pair" question="In the BPR program keep p=(1,.5) and positive item (.5,1), but change the sampled item's vector to (1.5,0). At η=.1 and λ=.1, calculate the old gap and one new gap. Does the first update have to make the order correct?" hint="The old gap is negative; g=σ(−gap). Copy all three old vectors before changing them.">
      <Prose>The old scores are 1 and 1.5, so Δ=−.5 and g≈.622459. The new user is approximately (.927754,.557246), positive item (.557246,1.021123), and sampled item (1.422754,−.031123). The gap becomes about −.216619 and the local loss about 1.034794, down from 1.211577. The comparison improves but is still reversed. An update need not finish the ranking task, and one sampled pair cannot establish whole-list quality.</Prose>
    </Practice>
    <Practice title="H. Keep the evaluator's denominator fixed" question="With relevant items {0,1}, list [4,1,2,0,3] and K=2, calculate precision, recall, reciprocal rank, AP and NDCG. Move item 1 to the first slot. Then suppose retrieval entirely omits item 0: can reranking ever reach recall 1?" hint="The ideal contains both eligible relevant items even when the candidate generator misses one.">
      <Prose>Initially precision=.5, recall=.5, RR=.5, AP=.25 and NDCG≈.386853. After moving item 1 first, precision and recall stay .5, RR becomes 1, AP=.5 and NDCG≈.613147. If item 0 is not a candidate, at most one of the two relevant items can be returned, so the recall ceiling is .5. Shrinking the denominator to the surviving relevant candidates would hide that retrieval failure.</Prose>
    </Practice>
    <Practice title="I. Explain a new item's limits" question="The metadata map gives a new item a factor vector. A new user has no history and no declared preferences. Can the dot product provide justified individual personalization? What would a defensible first list and evaluation look like?" hint="Cold-item and cold-user problems are different missing inputs.">
      <Prose>The item map solves only the missing item representation. A random or arbitrary user vector is not evidence of that person's taste. Use a declared nonpersonalized/contextual fallback or ask a lightweight preference question, then state what data support the choice. Evaluate on held-out new users and new items with metadata available at request time; include fallback coverage. A plausible design is an eligible popular list with some subject diversity and optional onboarding, judged on an outcome beyond merely having produced scores. Other choices can be sound if their information and objective are explicit.</Prose>
    </Practice>
    <Practice title="J. Change the logging mixture" question="Keep reward probabilities .4 for A and .8 for B. Change logging to half A/half B and the target to one-quarter A/three-quarters B. Calculate raw expectation, IPS expectation and one-request variance. Why does setting logging B=0 break the inference?" hint="Weights become .5 for an A reward and 1.5 for a B reward. Enumerate action and reward before subtracting the squared mean.">
      <Prose>The raw expectation is .6 and the target/IPS expectation is .7. The weighted second moment is .5×.4×.5²+.5×.8×1.5²=.95, giving variance .95−.7²=.46. If B has zero logging probability while the target assigns it .75, the logs cannot identify B's reward distribution. A declaration of the toy truth is not evidence that would be present in an actual A-only dataset.</Prose>
    </Practice>
    <Practice title="K. Produce a changed experiment report" question="Run the full experiment with cutoff 18 while preserving test_start=26. Report all split sizes, chosen settings, baseline and MF test error, and cold-user count. Explain what remains unproved, then propose a fresh evaluation for a newly released item." hint="The program already includes this declared changed run. Read its report as an experiment, not as a search for a favorable test number.">
      <Prose>The split is 18/7/6; validation chooses rank 2 and penalty .2 with validation RMSE≈1.180979. Test RMSE is about 1.111143 for the shrunk-item baseline and 1.128322 for MF. There are five warm-user events and one cold-user event; the latter MF error is about .671405. No population superiority, robust cold-start performance or causal benefit follows from this tiny synthetic test.</Prose>
      <Prose>For a new-item evaluation, reserve items released after the training cutoff, retain only metadata known before each request, and use a declared content/hybrid fallback. Keep their interactions out of factor fitting and feature construction until the chosen observation boundary allows them. Compare against an eligible nonpersonalized/content baseline, report new-item coverage and the intended ranking or outcome metric, and preserve a later untouched period. Do not smuggle the new items' future rating means into their initial features.</Prose>
    </Practice>
    <Prose><strong>Readiness:</strong> you should now be able to point to the actual feedback target, derive an update or block solve, explain a list metric's denominator, construct a leakage-free small comparison, and state a cold-start or support limitation. Neighbors, explicit MF, implicit ALS and BPR are alternatives whose suitability depends on evidence and task. No finite method list or exercise set guarantees every future recommendation problem.</Prose>
    <Prose>The next topic in this module is <strong>Multi-Label & Multi-Output Learning</strong>. It generalizes the idea that one input can require several coordinated predictions. The current route's Next control preserves the curriculum sequence; follow it in order rather than skipping to whichever later page happens to be published.</Prose>
    <Sources alternatives={<><Prose><strong>Another way to learn:</strong> <a href="https://developers.google.com/machine-learning/recommendation/collaborative/matrix" target="_blank" rel="noreferrer">Google's matrix-factorization lesson</a> is a compact diagram-led explanation of shared user/item representations. Compare its weighted objectives with the explicit mask and confidence distinction here.</Prose><Prose><a href="https://snap.stanford.edu/class/cs246-videos-2019/" target="_blank" rel="noreferrer">Stanford's public CS246 lecture archive</a> lists Recommender Systems I (January 29) and II (January 31), a longer spoken alternative. The public listing and lecture titles were verified; no claim is made that every minute was reviewed. <a href="https://snap.stanford.edu/class/cs246-2025/WWW/slides/07-recsys1.pdf" target="_blank" rel="noreferrer">The course's recommendation slides</a> provide worked content/collaborative similarity explanations; the inspected co-rating and centering sections are useful alongside section 3. They are conceptual resources, not a current package tutorial.</Prose></>}>
      <li><a href="https://yifanhu.net/PUB/cf.pdf" target="_blank" rel="noreferrer">Hu, Koren and Volinsky — Collaborative Filtering for Implicit Feedback Datasets</a>. Read sections 3–4 and equations 3–5 for the confidence-weighted all-pair objective and cached Gram solve. The historical experimental comparison does not establish a universal winner.</li>
      <li><a href="https://arxiv.org/pdf/1205.2618" target="_blank" rel="noreferrer">Rendle et al. — BPR: Bayesian Personalized Ranking from Implicit Feedback</a>. Sections 3–4 explain the assumed preference triples, smooth objective, sampling and factor derivatives. Our stable loss and finite checks derive their signs explicitly.</li>
      <li><a href="https://benfred.github.io/implicit/api/models/cpu/als.html" target="_blank" rel="noreferrer">implicit CPU ALS API</a> and <a href="https://pypi.org/project/implicit/" target="_blank" rel="noreferrer">release record</a>. Used for user×item input, confidence semantics, filtering, recalculation and actual 0.7.3 execution.</li>
      <li><a href="https://surprise.readthedocs.io/en/stable/matrix_factorization.html" target="_blank" rel="noreferrer">Surprise matrix-factorization documentation</a> and <a href="https://pypi.org/project/scikit-surprise/" target="_blank" rel="noreferrer">release record</a>. Used for its own biased MF model, settings and current 1.1.5 environment; native predictions are shown above.</li>
      <li><a href="https://proceedings.mlr.press/v48/schnabel16.pdf" target="_blank" rel="noreferrer">Schnabel et al. — Recommendations as Treatments</a>. Sections 3.1–3.3 distinguish observation selection, prediction risk and propensity corrections. Useful deeper context for the optional finite-policy section; the estimands must not be conflated.</li>
      <li><a href="https://research.google/pubs/deep-neural-networks-for-youtube-recommendations/" target="_blank" rel="noreferrer">Covington, Adams and Sargin — Deep Neural Networks for YouTube Recommendations</a>. The official 2016 abstract explicitly describes separate candidate-generation and ranking models. Linked for that historical architecture distinction, not an assertion about today's entire system.</li>
    </Sources>
  </div>
};
