import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { NumericalEvidenceFigure, RoundingCellsLab, CancellationLab, MeasurementSensitivityLab, BackwardErrorLab, SummationLab, ErrorPropagationLab, DefectResidualFigure } from '../../components/lesson-labs/ConditioningStabilityLabs.jsx';
import { conditioningStabilityExamples } from '../conditioning-stability-examples.js';
function Example({
  id,
  children
}) {
  const example = conditioningStabilityExamples.find(item => item.id === id);
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="lesson-check"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Conditioning, Stability & Numerical Analysis',
  readTime: '~105 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot conditioning-lesson">
    <LessonIntro prerequisites="Review matrix-vector multiplication in Vectors, Matrices & Tensor Operations, and approximations, derivatives and refinement in Numerical Methods. We introduce norms, rounding and the local recurrence arguments here. Matrix Decompositions is useful for the optional factorization branch. Python is optional; it is not needed to operate the investigations." sections={[['1-ask-which-answer-you-are-checking', 'The problem and its stored input'], ['2-see-the-gaps-between-stored-numbers', 'Rounding and precision'], ['3-follow-the-arithmetic-not-just-the-formula', 'Equivalent expressions'], ['4-measure-how-much-the-data-matter', 'Conditioning and measurement geometry'], ['5-interpret-a-residual-through-a-nearby-problem', 'Backward error and scaling'], ['6-watch-small-contributions-disappear', 'Summation and stability'], ['7-repair-a-solve-without-changing-its-target', 'Refinement and numerical rank'], ['8-connect-local-defects-to-global-error', 'Propagation and convergence'], ['9-write-an-accuracy-report-that-can-be-checked', 'Tolerances and a complete report'], ['10-practise-with-changed-data-and-methods', 'Independent practice']]}>Two instruments measure almost the same mixture of two ingredients. A program solves the resulting equations and prints twelve decimal places. How many of those digits deserve your trust? We will separate sensitivity in the measurements, information lost while storing or combining numbers, and errors amplified by a numerical method. Each cause calls for a different response.</LessonIntro>

    <H2>1. Ask which answer you are checking</H2>
    <Prose>Suppose x₁ and x₂ are two unknown amounts in the same unit. A calibrated channel measures x₁+x₂; a second measures x₁+(1+ε)x₂. The small positive coefficient ε makes the channels slightly different. The readings are b₁ and b₂. Writing both equations together gives Ax=b, where each row of A describes one mixture. When ε is tiny, the channels tell us nearly the same thing. Their difference is the only evidence that separates the two amounts.</Prose>
    <Prose>A numerical calculation has several layers. First there is a physical question and a mathematical model. Next there are recorded data, their representation in the computer, and a method that produces an approximation. An exact solve of the recorded equations can still disagree with reality if the model or measurements are wrong. Conversely, perfectly specified mathematical data can be processed inaccurately.</Prose>
    <NumericalEvidenceFigure />
    <LessonTable caption="Name the comparison before measuring it" headers={['Quantity', 'What is being compared?']} rows={[['Input or representation error', 'Intended data versus the values actually stored'], ['Forward error', 'Computed answer versus the exact answer for the stated problem/data'], ['Residual', 'What remains when the computed answer is substituted into the equation'], ['Backward error', 'The smallest allowed data change that makes the computed answer exact'], ['Model or discretization error', 'The mathematical/discrete target versus the physical/continuous target']]} />
    <H3>Give an error a size and a unit</H3>
    <Prose>If a reference length is 2 metres and an approximation is 2.01 metres, the absolute error is 0.01 metres. Dividing by the reference gives relative error 0.005, or 0.5%. Relative error is useful across scales, but division by a zero reference is undefined. If the target is zero volts, an absolute voltage budget is more meaningful than “within one percent of zero.”</Prose>
    <Prose>For a vector, choose what “size” means. The infinity norm ‖v‖∞ is the largest absolute component: ‖(−3,2)‖∞=3. Thus an error bound of 0.01 in that norm means every component is within 0.01 of its reference. Components need compatible units or an explicit scaling; taking an unqualified maximum of metres and seconds is not a physical error criterion.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\|v\|_\infty&=\max_i|v_i|,\\\|A\|_\infty&=\max_i\sum_j|a_{ij}|,\\|(Av)_i|&\leq\sum_j|a_{ij}|\,|v_j|\\&\leq\left(\sum_j|a_{ij}|\right)\|v\|_\infty.\end{aligned}`}</MathBlock>
    <Prose>The matrix infinity norm is the largest absolute row sum. The displayed inequality shows that ‖Av‖∞≤‖A‖∞‖v‖∞. Equality is possible by choosing the signs of v to match a row attaining the maximum, so this is the induced norm: the largest amplification over inputs of size one. For A with rows (1,−2) and (0,4), the row sums are 3 and 4; its infinity norm is 4. We will state the norm each bound uses.</Prose>
    <Prose><strong>Optional Python setup.</strong> Save each complete program in a separate file, such as <Code>accuracy_check.py</Code>, and run <Code>python accuracy_check.py</Code>. The early programs use Python 3.12's standard library. Section 7 introduces NumPy and SciPy explicitly. These programs create synthetic values in memory; they do not change a measurement file. Browser readouts are rounded unless they are explicitly shown as exact fractions.</Prose>

    <H2>2. See the gaps between stored numbers</H2>
    <Prose>A floating-point format stores a finite set of numbers, using a significant part and an exponent. Think of a movable ruler whose gaps grow as its scale grows. A simple binary format with four significant bits stores 1, 1.125, 1.25, …, 1.875 in [1,2). In [2,4), its gaps double: 2, 2.25, 2.5, …, 3.75. It does not store an equally fine grid across all magnitudes.</Prose>
    <Prose>Under <strong>round to nearest, ties to even</strong>, a value halfway between neighbors chooses the neighbor with an even final retained significand bit. For example, 17/16=1.0625 lies between 1 and 1.125 and rounds to 1. But 19/16=1.1875 lies between 1.125 and 1.25 and rounds upward to 1.25. “Even” refers to that binary significand, not whether the decimal input looks even.</Prose>
    <RoundingCellsLab />
    <Prose>The common binary64 format has 53 significant bits. Its gap from 1 to the next larger number is 2⁻⁵². We call half that gap the <strong>unit roundoff</strong>, u=2⁻⁵³, for nearest rounding. Some texts use “machine epsilon” for the gap and others for unit roundoff, so check the convention before comparing constants.</Prose>
    <Prose>Precision and range are different. The smallest positive <em>normal</em> binary64 value is 2⁻¹⁰²². Smaller positive <em>subnormal</em> values use a constant absolute gap, down to 2⁻¹⁰⁷⁴, with fewer significant bits relative to their size. Underflow can round a nonzero result to zero; overflow can leave the finite range. Infinity and NaN are not successful finite answers to quietly include in an error plot.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\operatorname{fl}(a\mathbin{\circ}b)=(a\mathbin{\circ}b)(1+\delta),\\|\delta|\leq u.\end{gathered}`}</MathBlock>
    <Prose>This standard model describes a relevant basic operation rounded to nearest when its exact result is nonzero and in the normal finite range. Exact zero can be handled directly. Do not silently extend the relative bound through arbitrary underflow or overflow. Transcendental implementations, compiler reassociation and device-specific modes need their own contracts. We will measure actual Python/JavaScript results and use this model only where its assumptions are appropriate.</Prose>
    <Example id="rounding"><Prose>The two Decimal constructions answer different questions. <Code>Decimal(intended)</Code> preserves the supplied decimal string; <Code>Decimal.from_float(stored)</Code> describes the already rounded binary value exactly. Converting the second one to a wider format cannot recover the missing one.</Prose></Example>
    <Checkpoint prompt="A program prints twenty decimal places from a binary64 value. Did it gain twenty decimal digits of information?"><Prose>No. Formatting changes how the stored value is displayed. It does not add information lost in input conversion or earlier arithmetic. More precision can help only when the calculation or data conversion is actually performed with that precision and an appropriate method.</Prose></Checkpoint>

    <H2>3. Follow the arithmetic, not just the formula</H2>
    <Prose>Consider f(x)=√(1+x)−1 for x≥−1. For a tiny positive x, the square root is very close to 1. If it is rounded to 1 before subtraction, the computed answer is zero. We can change the evaluation path without changing the exact function: multiply numerator and denominator by √(1+x)+1.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\sqrt{1+x}-1\\=\frac{(\sqrt{1+x}-1)(\sqrt{1+x}+1)}{\sqrt{1+x}+1}\\=\frac{x}{\sqrt{1+x}+1}.\end{gathered}`}</MathBlock>
    <Prose>The new numerator keeps x directly. The denominator is near 2, so a small relative error there does not wipe out the result. At x=2⁻⁵⁴, the direct binary64 calculation returns zero. The rationalized form returns about 2.7755575615628914×10⁻¹⁷. This is a change in arithmetic, not evidence that one algebraic identity is more true.</Prose>
    <H3>Is the function itself sensitive near zero?</H3>
    <Prose>A derivative describes a small change: f(x+Δx)−f(x)≈f′(x)Δx. Dividing output change by |f(x)| and input change by |x| gives the local relative sensitivity |xf′(x)/f(x)|, when x and f(x) are nonzero and f is differentiable. This is a local first-order comparison, not an exact finite-error bound for every Δx.</Prose>
    <MathBlock>{String.raw`\begin{aligned}f'(x)&=\frac{1}{2\sqrt{1+x}},\\\kappa_f(x)&=\left|\frac{xf'(x)}{f(x)}\right|\\&=\frac{\sqrt{1+x}+1}{2\sqrt{1+x}},\\&\quad x>-1,\ x\ne0.\end{aligned}`}</MathBlock>
    <Prose>As x approaches zero, this expression approaches one. A one-percent relative input change produces about a one-percent relative output change locally. The enormous loss in the direct calculation is therefore not explained by the conditioning of the function. At x=0 the exact output is zero and relative output error is undefined; use absolute error. At x=−1 the function is defined but the derivative is unbounded, so the displayed derivative formula is not a finite local certificate there.</Prose>
    <CancellationLab />
    <Callout title="Cancellation can expose an earlier error">Subtracting two nearby exactly stored values can itself be exact. The danger is often that their earlier rounding errors remain after the leading digits cancel. Inspect the whole evaluation path; “subtraction is always unstable” is not a correct rule.</Callout>
    <H3>Build a reference for the same input</H3>
    <Prose>A trustworthy comparison starts with an unambiguous input. The dyadic values ±2⁻ᵏ in this experiment are exactly representable. For the reference, multiply 1+x by 2³²⁰, take the integer square root m, and divide back by 2¹⁶⁰. The exact square root lies between m/2¹⁶⁰ and (m+1)/2¹⁶⁰; if the integer is a perfect square, both endpoints coincide. Subtracting one as rational arithmetic gives an exact interval for f(x), of width at most 2⁻¹⁶⁰. This proves containment for these inputs; it is not an arbitrary-purpose interval package.</Prose>
    <Example id="cancellation"><Prose>The separate Decimal calculation checks the reference by another arithmetic route. The browser compares stored answers with the rational interval, then rounds the error-bound display. A rounded answer need not lie inside an extremely narrow interval around the exact answer.</Prose></Example>
    <Prose>The same need arises in growth rates and small relative changes. Computing exp(x)−1 near zero can lose x after exp(x) rounds near one; <Code>math.expm1(x)</Code> evaluates that change directly. <Code>math.log1p(x)</Code> similarly avoids first discarding x in 1+x. These functions have their own domains and library accuracy limits.</Prose>
    <Example id="small-changes"><Prose>This matters when converting a small continuously compounded rate into its fractional change, or taking the logarithm of a small relative increase. The special function changes the evaluation method, not the quantity being requested.</Prose></Example>

    <H2>4. Measure how much the data matter</H2>
    <Prose>Return to the two channels. Choose readings b₁=2 and b₂=2+ε, so the exact amounts are (1,1). Now change only the second reading by δ. Subtract the first equation from the second: εx₂=ε+δ. Therefore x₂=1+δ/ε and x₁=1−δ/ε. The same small reading change causes a larger amount change when ε is smaller.</Prose>
    <MathBlock>{String.raw`\begin{gathered}A_\varepsilon=\begin{bmatrix}1&1\\1&1+\varepsilon\end{bmatrix},\quad b=\begin{bmatrix}2\\2+\varepsilon\end{bmatrix},\\\Delta b=\begin{bmatrix}0\\\delta\end{bmatrix},\qquad\Delta x=\begin{bmatrix}-\delta/\varepsilon\\\delta/\varepsilon\end{bmatrix}.\end{gathered}`}</MathBlock>
    <Prose>For ε=1/16 and δ=1/256, the inferred amounts become (15/16,17/16). The relative infinity-norm output change is 1/16, since the original answer has norm one. The uncertainty comes from almost redundant measurements; a perfect solver cannot remove it.</Prose>
    <MeasurementSensitivityLab />
    <H3>Turn the geometry into a bound</H3>
    <Prose>For a nonsingular square A, compare Ax=b and A(x+Δx)=b+Δb. Subtraction gives Δx=A⁻¹Δb. Thus ‖Δx‖≤‖A⁻¹‖‖Δb‖. To express this relatively, use ‖b‖=‖Ax‖≤‖A‖‖x‖, assuming the reference x and b are nonzero.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\frac{\|\Delta x\|}{\|x\|}\leq\kappa(A)\frac{\|\Delta b\|}{\|b\|},\\\kappa(A)=\|A\|\,\|A^{-1}\|.\end{gathered}`}</MathBlock>
    <Prose>The condition number is tied to a map and a norm. Here it bounds the relative response of a linear solve to right-hand-side changes. For our positive ε, the inverse is ε⁻¹ times the matrix with rows (1+ε,−1) and (−1,1). Its largest absolute row sum is (2+ε)/ε. The original matrix has largest row sum 2+ε, giving κ∞=(2+ε)²/ε.</Prose>
    <Prose>For the worked ε=1/16, δ=1/256, the bound is 33/256, while the actual relative error is 1/16. A worst-case bound need not be attained by this particular perturbation. A large κ also does not force error when Δb=0. “κ=10ᵏ loses exactly k digits” is only a rough worst-case scale heuristic, not a law about every answer.</Prose>
    <H3>What if the coefficients also change?</H3>
    <Prose>Let the changed problem be (A+ΔA)(x+Δx)=b+Δb. Rearrange it as AΔx=Δb−ΔA x−ΔAΔx. Taking norms gives a term proportional to ‖Δx‖ on both sides. Moving that term to the left is why the finite bound has a denominator:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\varepsilon_A=\frac{\|\Delta A\|}{\|A\|},\quad\varepsilon_b=\frac{\|\Delta b\|}{\|b\|},\\\frac{\|\Delta x\|}{\|x\|}\leq\frac{\kappa(A)(\varepsilon_A+\varepsilon_b)}{1-\kappa(A)\varepsilon_A},\\\text{provided }\kappa(A)\varepsilon_A<1.\end{gathered}`}</MathBlock>
    <Prose>The condition also ensures A+ΔA remains nonsingular: the operator I+A⁻¹ΔA has an inverse given by a convergent geometric series when its perturbation norm is below one. The reference norms must be nonzero for the stated relative parameters. If the hypothesis fails, this bound supplies no certificate; it does not by itself prove that the changed problem has no solution.</Prose>
    <details><summary>Deeper connection: singular directions and the meaning of “ill-posed”</summary><Prose>In the Euclidean norm, a singular value tells how much A stretches a particular input direction. The largest and smallest stretches give κ₂(A)=σmax/σmin for nonsingular A. A weakly observed direction has small σmin; applying A⁻¹ amplifies data components in the corresponding output direction. Matrix Decompositions derives this geometry in detail.</Prose><Prose>An ill-conditioned problem still has a well-defined solution but is sensitive under a stated perturbation model. At ε=0, our two equations either coincide, giving infinitely many answers, or disagree, giving no answer. That is a failure of existence or uniqueness, not merely a large finite condition number. Different scalings or allowed perturbations can change a reported condition measure; always name the question it measures.</Prose></details>
    <Example id="sensitivity"><Prose>The exact Fraction solve provides both the changed answers and the bound. It rejects ε=0 before dividing, rather than presenting a fabricated finite inverse.</Prose></Example>

    <H2>5. Interpret a residual through a nearby problem</H2>
    <Prose>Consider A=diag(1,10⁻⁶), b=(1,10⁻⁶), and the proposed answer x̂=(1,0). The true answer is (1,1). Substituting x̂ gives the residual r=b−Ax̂=(0,10⁻⁶). Its infinity norm looks small, while the forward error is one. The missing second component is weakly weighted by the equation.</Prose>
    <MathBlock>{String.raw`\begin{gathered}A(\hat x-x)=-r,\\\|\hat x-x\|\leq\|A^{-1}\|\,\|r\|.\end{gathered}`}</MathBlock>
    <Prose>Here ‖A⁻¹‖∞=10⁶, so multiplying the small residual by the inverse bound gives one. A residual becomes solution-error evidence only through an appropriate conversion. In a real computation even r=b−Ax̂ is rounded; a small computed residual may require a more accurate residual calculation before this reasoning is convincing.</Prose>
    <H3>One norm for all entries</H3>
    <Prose>Ask for the smallest η allowing ‖ΔA‖∞≤η‖A‖∞ and ‖Δb‖∞≤η‖b‖∞ such that (A+ΔA)x̂=b+Δb. We allow unstructured changes to the coefficient matrix and the RHS. Since ΔA x̂−Δb=r, the triangle inequality gives ‖r‖∞≤η(‖A‖∞‖x̂‖∞+‖b‖∞). For this perturbation model the lower bound is attainable:</Prose>
    <MathBlock>{String.raw`\eta_\infty=\frac{\|r\|_\infty}{\|A\|_\infty\|\hat x\|_\infty+\|b\|_\infty}.`}</MathBlock>
    <Prose>In our example the denominator is 2, so η∞=5×10⁻⁷. One attaining change adds 5×10⁻⁷ to A₂₁, which was zero, and subtracts 5×10⁻⁷ from b₂. The second equation then reads (5×10⁻⁷)x₁+(10⁻⁶)x₂=5×10⁻⁷, which (1,0) satisfies exactly. That is a concrete nearby problem explaining the answer.</Prose>
    <details><summary>Why the normwise formula is attainable in general</summary><Prose>Write d=‖A‖∞‖x̂‖∞+‖b‖∞. If x̂ is nonzero, choose a component j with |x̂ⱼ|=‖x̂‖∞. In each row set ΔAᵢⱼ=(rᵢ/d)‖A‖∞sign(x̂ⱼ), set every other matrix change to zero, and set Δbᵢ=−(rᵢ/d)‖b‖∞. Substitution gives ΔA x̂−Δb=r, and both norm bounds hold with η=‖r‖∞/d. If x̂=0 and b≠0, changing b to zero gives η=1. If the denominator is zero, the residual is also zero and η=0 suffices. This proof permits arbitrary entry changes, not a requirement to preserve symmetry or another structure.</Prose></details>
    <H3>Relative permission for each entry</H3>
    <Prose>A different rule allows |ΔAᵢⱼ|≤η|Aᵢⱼ| and |Δbᵢ|≤η|bᵢ| separately. Now an original zero entry cannot change at all. Apply the same substitution row by row. The amount available to explain residual rᵢ is η times dᵢ=Σⱼ|Aᵢⱼ||x̂ⱼ|+|bᵢ|, giving the componentwise relative backward error:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\eta_c=\max_i\frac{|r_i|}{d_i},\\d_i=(|A||\hat x|+|b|)_i.\end{gathered}`}</MathBlock>
    <Prose>When dᵢ=0, exact arithmetic also gives rᵢ=0, and that row contributes zero. For dᵢ positive, choosing ΔAᵢⱼ=rᵢ|Aᵢⱼ|sign(x̂ⱼ)/dᵢ and Δbᵢ=−rᵢ|bᵢ|/dᵢ attains the row's bound. These changes preserve exact zeros but need not preserve symmetry or positive definiteness. In our second row d₂=10⁻⁶ and |r₂|=10⁻⁶, so ηc=1. The supposedly tiny backward error depends on what changes were allowed.</Prose>
    <BackwardErrorLab />
    <H3>Scaling helps representation; it does not create information</H3>
    <Prose>Multiply the second equation by 10⁶. It becomes x₂=1, the matrix becomes the identity, and κ∞ becomes one. The same x̂ now has normwise backward error 1/2 and componentwise error one. If the original second reading had uncertainty 10⁻⁸, its uncertainty in the scaled units is 0.01. In either representation the amount uncertainty is 0.01. Comparing condition numbers after changing units while forgetting to change the data-error model is misleading.</Prose>
    <Prose>For a joint normwise backward error η, applying the finite perturbation bound gives relative forward error at most 2κη/(1−κη) when κη&lt;1. In this example κη=1/2 and the bound is 2. This conservative bound is valid under its model; writing “forward error equals κη” would not be. Estimated condition numbers and residuals at working precision generally give numerical estimates unless their own uncertainty is also bounded.</Prose>
    <Example id="backward"><Prose>All arithmetic here is rational, so the displayed witness is exact. A practical LAPACK error-estimation routine additionally guards small denominators and computed residuals; this transparent example does not claim to reproduce every driver safeguard.</Prose></Example>
    <Checkpoint prompt="A least-squares solver has a nonzero data residual. Does that prove its algorithm failed?"><Prose>No. If the equations are inconsistent, even the exact least-squares minimizer has a nonzero residual. Its optimality is related to Aᵀr=0 under the usual unconstrained least-squares model. The square nonsingular-system residual-to-solution formula cannot be applied unchanged to every least-squares problem.</Prose></Checkpoint>

    <H2>6. Watch small contributions disappear</H2>
    <Prose>The exact sum of 10¹⁶, 1 and −10¹⁶ is one. A binary64 left fold first computes 10¹⁶+1, which rounds to 10¹⁶; the last addition produces zero. Reordering as 10¹⁶, −10¹⁶, 1 first cancels the large terms exactly and returns one. Addition remains associative in real arithmetic; the sequence of rounded operations does not.</Prose>
    <Prose>A balanced tree groups nearby pairs and has a shorter maximum path from a leaf to the result. That can improve a worst-case accumulation bound, but it does not promise a better answer on every ordering. The investigation shows an order where the left fold returns one and this particular balanced tree returns zero.</Prose>
    <SummationLab />
    <H3>The problem can be sensitive even when the sum is computed stably</H3>
    <Prose>Suppose each stored input may independently change by at most a small relative amount η. Then the sum changes by at most ηΣ|xᵢ|. Relative to a nonzero exact sum s, its componentwise relative condition is κsum=Σ|xᵢ|/|s|. Opposite-sign terms can make this huge. If s=0, relative output error is undefined; an absolute bound is still useful.</Prose>
    <Prose>Now analyze the algorithm. In a naive accumulation, each addition rounds by a factor (1+δ), with |δ|≤u under the earlier arithmetic assumptions. Expanding the nested recurrence expresses the final result as a sum of each original xᵢ multiplied by a product of rounding factors. Each path contains at most n−1 factors. These products differ from one by at most γₙ₋₁:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\gamma_m=\frac{mu}{1-mu},\qquad mu<1,\\|\hat s-s|\leq\gamma_{n-1}\sum_i|x_i|,\\\frac{|\hat s-s|}{|s|}\leq\gamma_{n-1}\kappa_{\rm sum}\quad(s\ne0).\end{gathered}`}</MathBlock>
    <Prose>For example, the upper product bound follows by comparing the binomial expansion of (1+u)ᵐ with the geometric series 1/(1−mu); the lower product is at least (1−u)ᵐ≥1−mu. Thus each input can be regarded as having received a small componentwise relative perturbation. This is a backward-stability statement with a dimension-dependent constant, not a promise of a small forward error for an arbitrarily cancellation-sensitive sum.</Prose>
    <Prose>In contrast, all-positive inputs have κsum=1. Even then an error can be visible: adding four ones individually to 2⁵³ loses all four in the naive loop. The absolute error four is still tiny relative to the large total and fits the bound. A diagnosis needs both absolute and relative scales.</Prose>
    <H3>Keep track of a discarded low part</H3>
    <Prose>Compensated methods keep an additional correction. If t is the rounded sum of a subtotal s and a new value x, the expression (s−t)+x can recover a low part when |s|≥|x|; the reversed form (x−t)+s handles the opposite magnitude ordering. Neumaier accumulation adds that correction separately, then combines it with the running total at the end. The correction arithmetic and final combination are themselves rounded.</Prose>
    <Prose>Classic Kahan uses a related but different recurrence: subtract the previous correction from the next input, add that adjusted value, then estimate the new rounding error. It is useful, but for [10¹⁶,1,−10¹⁶] this version returns zero while Neumaier and <Code>math.fsum</Code> return one. Do not memorize “compensation makes every sum exact.” Python's built-in <Code>sum</Code> changed its floating-point strategy in version 3.12; our naive baseline is an explicit loop.</Prose>
    <Example id="summation"><Prose>The reference sums the exact rational values of the stored inputs. It does not pretend that a decimal measurement contained more information than was supplied. The fsum documentation also names arithmetic/platform caveats; the printed outcomes are local observations, not a theorem about all builds and sequences.</Prose></Example>
    <details><summary>“Stable” does not always mean “backward stable”</summary><Prose>Backward stability is a precise and valuable property: the computed result exactly solves a nearby permitted problem, with a small bound uniform over inputs in the declared class. Other notions allow small forward error, or small changes to both input and output. They are not identical.</Prose><Prose>For f(x)=1+x with tiny nonzero x, the rounded result can be one with very small relative forward error. If only x may change and the result must become exactly one, the required new input is zero, giving relative backward error one. This does not make the forward answer poor. Name the stability criterion and permitted perturbations instead of using one slogan for every problem.</Prose></details>

    <H2>7. Repair a solve without changing its target</H2>
    <Prose>Several repairs are now distinguishable. Reformulation changes the arithmetic path. Better accumulation changes how contributions are combined. Scaling changes the representation and needs matching units. A higher-precision computation reduces some arithmetic errors if it starts from the relevant data. A better measurement changes the information available. Regularization deliberately changes the mathematical problem, trading sensitivity against bias; it is not merely a more accurate evaluation of the original inverse.</Prose>
    <H3>Use the residual to correct an existing answer</H3>
    <Prose>Suppose x̂ is an approximate solution of Ax=b. Its residual is r=b−Ax̂. If we could solve Ad=r exactly, then A(x̂+d)=b and the corrected answer would be exact. <strong>Iterative refinement</strong> uses this idea repeatedly, usually reusing factors of A rather than rebuilding a solve from nothing.</Prose>
    <Prose>A factorization rewrites a matrix into pieces that are easier to solve with. In pivoted LU, a row permutation and lower/upper triangular factors permit a sequence of substitutions. We let SciPy compute those factors once in float32, reuse them to solve each correction, but store the iterate and compute the residual with the original float64 data. Matrix Decompositions supplies the full LU derivation. The important distinction here is which precision owns each operation.</Prose>
    <Prose><strong>Setup for this branch:</strong> use an environment containing NumPy and SciPy; install them there with <Code>python -m pip install numpy scipy</Code> if needed. These examples were executed with Python 3.12.14, NumPy 2.3.5 and SciPy 1.18.1. Their complete programs check actual dtype roles. A numerical library or BLAS implementation can alter the final low bits.</Prose>
    <Example id="refinement"><Prose>At separation 2⁻²⁰, the initial low-precision answer is (0.375,0.625). Two correction steps recover this fixture's exact stored-input solution. It is close to (1/3,2/3), but those intended construction values are not the exact solution of the rounded RHS. At 2⁻²⁴, the float32 matrix becomes singular: the program rejects the factor instead of continuing with NaNs.</Prose></Example>
    <H3>Why correction can converge</H3>
    <Prose>Imagine an exact correction solve with some nonsingular approximation M: d=M⁻¹(b−Ax̂). If e=x̂−x is the error before correction, then e_new=(I−M⁻¹A)e. Therefore a bound ‖I−M⁻¹A‖≤q&lt;1 is sufficient to contract the error by at most q each step. Rounded residuals and correction solves add further error terms. Their size and propagation matter; there is no universal rule that any low-precision factor plus two iterations yields a full-precision answer.</Prose>
    <Prose>The familiar heuristic involving κ and unit roundoff depends on the factorization error, dimension, scaling, residual precision and correction method. The exact local experiment establishes its observed behavior, not a hardware speedup or a general mixed-precision convergence theorem. A failed factorization is a different state from a successful solve with a nonzero residual.</Prose>
    <details><summary>Deeper experiment: a rounded Gram matrix can lose a direction</summary><Prose>Let A have rows (1,1), (1,1+ε) and (1,1−ε), with ε=2⁻²⁷, and b=(0,−ε,ε). The exact solution is (1,−1). The columns are independent, and the lower-right entry of AᵀA is 3+2ε²=3+2⁻⁵³. Binary64 Gram formation rounds that entry to 3, making all four displayed entries equal. The rounded normal system is singular even though the original A has full column rank.</Prose><Example id="least-squares"><Prose>QR and lstsq preserve the weak direction in this fixture without explicitly forming AᵀA. For full column rank, κ₂(AᵀA)=κ₂(A)². This explains a potential loss mechanism, not a guarantee that QR will cure uncertainty already present in A or b. A numerical rank threshold is a computational decision, distinct from exact mathematical rank.</Prose></Example></details>

    <H2>8. Connect local defects to global error</H2>
    <Prose>So far we have studied a fixed calculation. A numerical method that takes many steps introduces another question: what happens to a small error after it is repeatedly reused? Start with the scalar recurrence eₖ₊₁=q eₖ+δₖ. The multiplier q acts on the existing error, and δₖ is a new disturbance. Substitution gives e₂=q²e₀+qδ₀+δ₁. Continuing makes each earlier disturbance acquire more powers of q.</Prose>
    <MathBlock>{String.raw`\begin{gathered}|\delta_k|\leq\rho\quad\Longrightarrow\\|e_n|\leq |q|^n|e_0|+\rho\sum_{j=0}^{n-1}|q|^j.\end{gathered}`}</MathBlock>
    <Prose>If |q|&lt;1, the repeated-disturbance contribution is at most ρ/(1−|q|). At |q|=1 it can grow as nρ. If |q|&gt;1 it can grow exponentially in the number of steps. Actual signed disturbances can cancel, so this envelope is not always attained. A negative multiplier can flip a disturbance's sign before the next addition.</Prose>
    <ErrorPropagationLab />
    <Example id="propagation"><Prose>The Fraction arithmetic separates the recurrence from floating simulation error. The final two-coordinate example also shows why stable eigenvalues alone do not prevent large finite-time growth.</Prose></Example>
    <H3>Hold the final time fixed when discussing convergence</H3>
    <Prose>Suppose a one-step approximation has local solution defect at most C hᵖ⁺¹ and propagates a previous error by at most 1+Lh, with L≥0. Take N=T/h steps over a fixed interval. Summing the propagated local defects gives a geometric series. Since (1+Lh)ᴺ≤exp(LT), one sufficient bound is:</Prose>
    <MathBlock>{String.raw`\begin{gathered}L>0:\quad\|e_N\|\\\leq e^{LT}\|e_0\|+C h^p\frac{e^{LT}-1}{L},\\L=0:\quad\|e_N\|\\\leq\|e_0\|+CTh^p.\end{gathered}`}</MathBlock>
    <Prose>The constants and hypotheses must hold uniformly on that interval as h shrinks. The local solution defect here has order hᵖ⁺¹; some texts divide it by h and call that quantity the local truncation error of order hᵖ. State the convention. A fixed rounding disturbance per step does not automatically shrink like hᵖ⁺¹, so more steps can eventually increase its contribution.</Prose>
    <H3>A consistent formula that does not converge</H3>
    <Prose>Consistency means that substituting a smooth exact solution into the numerical formula produces a defect tending to zero in the stated normalization. It does not say the formula controls how such defects or initial perturbations propagate. Consider this derivative approximation, found by expanding u(t+h) and u(t+2h) in Taylor series:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\frac{3u(t+h)-2u(t)-u(t+2h)}{h}\\=u'(t)-\frac h2u''(t)+O(h^2).\end{gathered}`}</MathBlock>
    <Prose>The constant terms cancel; the u′ coefficient is 3−2=1; the u″ coefficient is (3/2−2)h=−h/2. Thus the derivative approximation is first-order consistent for a sufficiently smooth solution. Replacing u′(t) by f(t,u) and rearranging gives yₙ₊₂=3yₙ₊₁−2yₙ−h f(tₙ,yₙ).</Prose>
    <Prose>Apply it to the simplest problem u′=0, u(0)=1, whose solution is constantly one. Start with y₀=1 and y₁=1+h². The error satisfies eₙ₊₂=3eₙ₊₁−2eₙ. Try eₙ=rⁿ: the equation is r²−3r+2=(r−1)(r−2)=0. Matching e₀=0 and e₁=h² gives eₙ=(2ⁿ−1)h². The unwanted mode with root two amplifies the small starting disturbance.</Prose>
    <MathBlock>{String.raw`\begin{gathered}T=1,\quad h=1/N,\\e_N=\frac{2^N-1}{N^2}.\end{gathered}`}</MathBlock>
    <Prose>At N=4, the final error is 15/16. At N=8, it is 255/64. At N=16, it is 65535/256. The start error shrinks while the fixed-time answer becomes worse. This supplies an exact counterexample to “smaller local defect therefore better final answer.” It is not a model of physical growth; the exact solution stays one.</Prose>
    <Example id="consistency"><Prose>The recurrence and its closed form agree in exact arithmetic. An actual error graph for one method does not prove convergence of a different one. Numerical ODE and PDE methods require their own stability and consistency assumptions.</Prose></Example>
    <details><summary>Deeper caution: eventual decay and transient growth differ</summary><Prose>The matrix B with rows (1/2,10) and (0,1/2) has both eigenvalues 1/2. Yet from e₀=(0,1), direct multiplication gives Bⁿe₀=(10n(1/2)ⁿ⁻¹,(1/2)ⁿ). The first component is 10 after one step, 10 after two, 5 after four and 0.625 after eight. It eventually decays, but a small input disturbance can first be strongly amplified. The nonorthogonal coupling between coordinates matters. A norm contraction is sufficient for immediate control; an eigenvalue statement alone does not supply that finite-time bound.</Prose></details>
    <H3>Separate the discrete equation from solving it</H3>
    <Prose>For a finite linear discretization, let uₕ be the exact continuous solution restricted to the discrete locations. Let τₕ=bₕ−Aₕuₕ be its consistency defect, and let rₕ=bₕ−Aₕx̂ₕ be the residual of a computed solution. Subtract the two equations:</Prose>
    <DefectResidualFigure />
    <MathBlock>{String.raw`\begin{gathered}\|\hat x_h-u_h\|\\\leq\|A_h^{-1}\|\bigl(\|\tau_h\|+\|r_h\|\bigr).\end{gathered}`}</MathBlock>
    <Prose>This requires an invertible discrete operator and compatible stated norms. A uniform inverse bound and vanishing appropriately normalized defect and residual give this sufficient convergence result. Multiplying every equation by a small constant also multiplies τₕ and rₕ by that constant, while increasing the inverse norm; it cannot make the solution magically more accurate. Comparing with a continuous field rather than just its restriction may require another interpolation/reconstruction error. Numerical PDEs will establish these ingredients for actual grids, boundary conditions and schemes.</Prose>

    <H2>9. Write an accuracy report that can be checked</H2>
    <Prose>An error tolerance belongs to a question. Suppose two voltages are 10⁻⁹ V and 2×10⁻⁹ V. Their difference is 10⁻⁹ V: tiny in one absolute context and a 100% relative difference from the first value. NumPy's default <Code>isclose</Code> accepts them because of its absolute tolerance. That does not establish that they are interchangeable for your instrument.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\texttt{math.isclose}:\\|a-b|\leq\max\{r\max(|a|,|b|),a_0\},\\\texttt{numpy.isclose}:\\|a-b|\leq a_0+r|b|.\end{gathered}`}</MathBlock>
    <Prose>Here r denotes a relative tolerance and a₀ an absolute tolerance, not a data value. The first rule is symmetric in a and b; the second treats b as the reference. For these voltages, <Code>math.isclose</Code> with <Code>rel_tol=1e-6</Code> and <Code>abs_tol=1e-12</Code> rejects the comparison. If you express the same voltages in millivolts, multiply the absolute tolerance by 1000 too. A unit conversion should not change a consistent acceptance decision.</Prose>
    <H3>A complete changed measurement-and-solve report</H3>
    <Prose>Use the two-channel matrix with ε=2⁻¹⁶. The stored central readings are (1,1.0000101725260415). Only the second reading is uncertain, within 2⁻²⁴ of that stored center; the matrix and first reading are treated as exact for this scenario. Require an absolute error at most 10⁻⁴ in each inferred amount. These are declared assumptions for a teaching measurement model, not an empirical calibration claim.</Prose>
    <Prose>A float32 solve gives (0.3359375, 0.6640625). Two refinement steps recover the exact solution of the stored central readings: (11453246123/34359738368, 22906492245/34359738368). The original thirds used to construct the synthetic readings are not substituted as the exact stored-input reference.</Prose>
    <Prose>The arithmetic repair succeeds, but the second-reading uncertainty allows each amount to move by 2⁻²⁴/2⁻¹⁶=1/256≈0.00390625. That exceeds the 0.0001 requirement. We cannot certify that requirement over all the allowed readings simply by improving the solve. This is a worst-case statement: the unknown actual measurement error need not equal its bound. Reducing the reading uncertainty to 2⁻³⁰ gives amount radius 1/16384≈0.0000610352, which fits the requirement here after accounting for the arithmetic error.</Prose>
    <Example id="report"><Prose><strong>Accepted report:</strong> the target is the pair of amounts under the stated calibrated linear model. Reference data are the exact stored float64 A and central b; forward error is measured in the infinity norm against a rational solve. Factors/corrections use float32 and iterates/residuals use float64. The first uncertainty bound fails the requested guarantee despite zero exact central-solve error in this fixture; the improved uncertainty bound passes. No conclusion is made about omitted calibration, coefficient or model error.</Prose></Example>
    <LessonTable caption="What a reproducible accuracy report contains" headers={['Record', 'Why it changes the interpretation']} rows={[['Target, units and input provenance', 'The intended decimal, measured interval and exact stored float are different references'], ['Dtype, method and evaluation order', 'They determine rounding paths and failure states'], ['Norm and allowed perturbations', 'A small aggregate error may hide a large error in one component'], ['Reference, residual and sensitivity evidence', 'A reference comparison, estimate and rigorous enclosure support different strengths of claim'], ['Tolerance and unresolved uncertainty', 'An arithmetic pass cannot remove assumptions or uncertainty from the data/model']]} />
    <Prose>To continue the module, <strong>Decision Theory, Risk & Cost-Sensitive Decisions</strong> asks how to choose an action when outcomes and consequences differ. Numerical analysis helps establish the reliability and limits of the numbers supplied to that decision; the cost of an action requires additional reasoning.</Prose>

    <H2>10. Practise with changed data and methods</H2>
    <Prose>Try a prediction or derivation before opening a hint. A correct conclusion should state which reference, norm and assumptions make it valid.</Prose>
    <Practice title="Rounding at a new scale" question="In the four-significant-bit toy format on [2,4), round 17/8 and 19/8 to nearest-even. Explain why both choices need not point downward." hint="The spacing is 1/4. Express each candidate in units of that spacing."><Prose>17/8=2.125 is halfway between 2 and 2.25, whose significand integers are 8 and 9; choose 2. Next, 19/8=2.375 is halfway between 2.25 and 2.5, whose integers are 9 and 10; choose 2.5. The retained even significand determines the direction. In binary64, 1+u rounds to 1, while 1+3u rounds to 1+4u under the same tie principle.</Prose></Practice>
    <Practice title="Choose the intended reference" question="A decimal sensor file contains 10000000000000001. You first load it as a float and then convert it to Decimal. Which of the original and stored numbers is appropriate when checking the solver alone, and which comparison measures the input conversion?" hint="Keep the input conversion separate from the later algorithm."><Prose>The stored float is exactly 10000000000000000. To isolate the later solver's error, compare its output with an exact or trustworthy solve using that stored value. To assess the whole pipeline relative to the file, also account for the one-unit input difference. Reading the original string directly into Decimal can preserve it; converting the already rounded float cannot.</Prose></Practice>
    <Practice title="Change the measurement direction" question="Use ε=1/128 and δ=−1/256. Find the inferred amounts, relative infinity error and the stated matrix condition bound. What changes if δ=0?" hint="Subtract the two row equations; the difference in amounts is controlled by δ/ε."><Prose>δ/ε=−1/2, so x=(3/2,1/2). The relative infinity error is 1/2. The bound is (2+ε)|δ|/ε=(257/128)(1/2)=257/256. The bound is larger because this particular RHS direction need not attain the worst case. If δ=0, the exact answer remains (1,1) despite the large condition number.</Prose></Practice>
    <Practice title="Explain the new nearby problem" question="For A=diag(1,10^-3), b=(1,10^-3) and x̂=(1,0), compute the two backward errors and exhibit an attaining normwise perturbation." hint="The normwise denominator is two. The componentwise denominator in the second row is only 10^-3."><Prose>Residual is (0,10⁻³), forward error is one, η∞=1/2000 and ηc=1. Set ΔA₂₁=1/2000 and Δb₂=−1/2000. The changed second equation becomes x₁/2000+x₂/1000=1/2000, satisfied by (1,0). Componentwise relative perturbations cannot change the original A₂₁=0, which explains the different answer. Scaling the second equation to x₂=1 gives η∞=1/2 and ηc=1.</Prose></Practice>
    <Practice title="Repair without making an exactness promise" question="Predict the naive, balanced, classic Kahan and Neumaier results for [1e16,3,-1e16]. Then explain what relative error means for the exact-zero input [1,-1]." hint="At 1e16, the gap is two; adding three reaches a halfway rounding case."><Prose>The exact stored-input sum is three. On the checked nearest-even binary64 runtime, naive, this balanced tree and classic Kahan return four; Neumaier and fsum return three. Re-run the complete summation program to see the actual order. For [1,−1], the exact answer and all listed computed results are zero. Absolute error is zero, but relative output error has a zero denominator and is undefined. Do not replace it with a fabricated relative percentage.</Prose></Practice>
    <Practice title="Distinguish repair from a failed factor" question="Why can refinement help at separation 2^-12 yet be invalid after the float32 matrix collapses at 2^-24? Does an accurate central solve settle measurement uncertainty?" hint="The correction needs a usable factorization, and the input interval is separate from arithmetic."><Prose>At 2⁻¹² the factor is nonsingular and the accurate residual exposes a correction the low-precision solve can apply. At 2⁻²⁴, 1+ε rounds to 1 in float32, making the rows identical; the factor is singular, so a correction solve is not defined by this procedure. Even successful refinement solves the central recorded problem. It does not reduce an allowed interval of measurement values unless the data collection or uncertainty model changes.</Prose></Practice>
    <Practice title="Derive a disturbance plateau" question="For e₀=0, q=1/2 and disturbance +1/100 every step, derive eₙ and its limiting value. What if the disturbance alternates, beginning negative?" hint="Sum the powers of 1/2. For alternating signs, try a particular solution proportional to (−1)^n."><Prose>The constant case is eₙ=(1/100)Σⱼ₌₀ⁿ⁻¹2⁻ʲ=(1/50)(1−2⁻ⁿ), tending to 1/50. For alternating signs, eₙ=(1/150)((−1)ⁿ−2⁻ⁿ). At n=8 it is (1/150)(255/256)=17/2560, while the envelope is 51/2560. The same bound permits disturbances to align; the actual alternating pattern does not attain it.</Prose></Practice>
    <Practice title="A smaller starting error is not enough" question="For the consistent constant-trajectory method, use N=8 and starting error 1/64. Derive the final error without relying on a graph." hint="Solve eₙ₊₂=3eₙ₊₁−2eₙ with e₀=0 and e₁=1/64."><Prose>The roots are 1 and 2, so eₙ=c₁+c₂2ⁿ. From e₀=0, c₁=−c₂; from e₁=1/64, c₂=1/64. Hence e₈=(256−1)/64=255/64. The root-two mode amplifies the starting error. Taylor consistency says the normalized local defect tends to zero; it does not suppress that mode.</Prose></Practice>
    <Practice title="Keep a tolerance consistent under unit conversion" question="Compare 10^-9 V and 2×10^-9 V using absolute budget 10^-12 V and relative budget 10^-6. Convert the entire comparison to millivolts." hint="Multiply both data values and the absolute budget by 1000; leave the relative budget unchanged."><Prose>The voltage difference 10⁻⁹ V exceeds the absolute budget; the relative term is smaller still. In millivolts the values are 10⁻⁶ and 2×10⁻⁶, and the absolute budget is 10⁻⁹ mV. The difference 10⁻⁶ mV still fails. Keeping the number 10⁻¹² as the millivolt absolute budget would describe a different physical tolerance.</Prose></Practice>
    <Practice title="Complete the changed measurement report" question="Change the section 9 report to ε=2^-18 and replace its smaller second-reading radius with 2^-34; keep the larger radius 2^-24 and the amount budget 1/10000. Run both cases and explain the pass/fail decisions, exact central reference, precision roles and what the worst-case bound does not say." hint="Change the exponent in epsilon and the second Fraction in the uncertainty list. Convert each allowed second-reading change through δx₂=δb₂/ε, then add the central arithmetic error."><Prose>The new stored readings are (1, 1.0000025431315103). Their exact central solution is (2863311531/8589934592, 5726623061/8589934592). Initial float32 output is (0.34375, 0.65625); two corrections using the reused float32 factor and float64 residual/iterate give zero exact forward error for this particular central system. With reading radius 2⁻²⁴, the total amount bound is 1/64 and fails 1/10000. With radius 2⁻³⁴ it is 1/65536 and passes. The larger bound does not assert that the unknown actual error equals 1/64. The report treats the matrix and first reading as exact and makes no claim about omitted calibration/model error. If those assumptions change, the error budget must change too.</Prose></Practice>

    <Sources alternatives={<ul><li><a href="https://fncbook.com/conditioning/" target="_blank" rel="noreferrer">Driscoll & Braun — Problems and conditioning</a>: an accessible written route through input perturbations and local sensitivity; follow with their <a href="https://fncbook.com/stability/" target="_blank" rel="noreferrer">stability chapter</a> for evaluation paths and backward error.</li><li><a href="https://www.youtube.com/watch?v=gv-AB35V2k8" target="_blank" rel="noreferrer">MIT / Gilbert Strang — Difference Methods for Ordinary Differential Equations</a>: a graduate-level video alternative for repeated multipliers and step stability. The <a href="https://ocw.mit.edu/courses/18-086-mathematical-methods-for-engineers-ii-spring-2006/resources/lecture-1-difference-methods-for-ordinary-differential-equations/" target="_blank" rel="noreferrer">official page and transcript</a> support following the scalar stability argument after section 8. This is a 2006 lecture; historical MATLAB defaults are not current Python guidance.</li><li><a href="https://ocw.mit.edu/courses/18-335j-introduction-to-numerical-methods-spring-2019/pages/week-2/" target="_blank" rel="noreferrer">MIT 18.335 — Week 2 notes</a>: deeper written material on summation, uniform backward stability and norms.</li></ul>}>
      <li><a href="https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html" target="_blank" rel="noreferrer">Goldberg — Floating-point arithmetic</a>: a classic deeper treatment of representation and rounding; keep its historical architecture context separate from present devices.</li>
      <li><a href="https://nhigham.com/2020/03/25/what-is-backward-error/" target="_blank" rel="noreferrer">Higham — What Is Backward Error?</a>: a concise expert explanation of the nearby-problem viewpoint and first-order conditioning relation.</li>
      <li><a href="https://www.netlib.org/lapack/explore-html/d0/df7/dla__lin__berr_8f_source.html" target="_blank" rel="noreferrer">LAPACK — Componentwise backward error</a>: the denominator and numerical safeguards behind a practical residual diagnostic; <a href="https://www.netlib.org/lapack/lug/node79.html" target="_blank" rel="noreferrer">the users' guide</a> explains why small entries and zeros matter.</li>
      <li><a href="https://nhigham.com/2017/07/" target="_blank" rel="noreferrer">Carson & Higham — Iterative refinement in three precisions</a>: an advanced application of residual correction and distinct precision roles. Its performance examples are not benchmarks for this lesson.</li>
      <li><a href="https://docs.python.org/3.12/library/math.html" target="_blank" rel="noreferrer">Python 3.12 math reference</a>: fsum, isclose, expm1, log1p and ulp contracts; consult the <a href="https://docs.python.org/3.12/library/functions.html#sum" target="_blank" rel="noreferrer">sum version note</a> before choosing a naive baseline.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.linalg.lu_factor.html" target="_blank" rel="noreferrer">SciPy LU factorization</a>: factor storage, pivots and finite-input checks for the reusable solve.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.isclose.html" target="_blank" rel="noreferrer">NumPy isclose</a>: reference asymmetry and near-zero tolerance caveats.</li>
      <li><a href="https://fncbook.com/zerostability/" target="_blank" rel="noreferrer">Driscoll & Braun — Zero-stability</a>: a deeper continuation on unwanted recurrence modes and why consistency alone is insufficient.</li>
    </Sources>
  </div>
};
