import { Code, CodeBlock, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { UnionFindEquivalenceFigure, UnionFindLab, PathCompressionLab, IslandUnionLab } from '../../components/lesson-labs/UnionFindLabs';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { unionFindExamples } from '../union-find-examples.js';
import unionFindPractice from '../practice/disjoint-sets-union-find.js';

export default {
  title: 'Disjoint Sets & Union-Find',
  readTime: '~45 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot union-find-lesson">
    <LessonIntro prerequisites="Python lists, dictionaries, sets and classes; parent pointers from Trees; connected components and BFS/DFS from the preceding Graphs lesson. The needed logarithmic and amortized cost intuition is explained here before the next topic develops it further."
      sections={[["1-track-groups-as-links-arrive", "Start with connected groups"], ["2-a-forest-can-remember-a-partition", "Understand parent pointers"], ["3-implement-the-contract-and-prove-it", "Build a correct implementation"], ["4-make-long-paths-unnecessary", "Weight and compress"], ["5-turn-real-questions-into-merges", "Model practical applications"], ["6-open-cells-and-count-islands", "Investigate active elements"], ["7-know-what-the-structure-forgets", "Limits and rollback"], ["8-practise-with-a-changed-contract", "Independent practice"], ["guided-dsa-practice", "Guided LeetCode practice"]]}>
      Maintain changing groups without repeatedly searching the whole network. Learn what a representative means, why a merge must link roots, and how weighting and path compression make repeated queries efficient. Then use those ideas to detect redundant connections, reconcile shared identifiers and count growing regions.
    </LessonIntro>

    <H2>1. Track groups as links arrive</H2>
    <Prose>Six devices begin with no links. A cable joins 0 and 1; another joins 2 and 3. They form two pairs, while 4 and 5 remain alone. Now join 1 and 2. You have connected the entire group {'{0, 1}'} to the entire group {'{2, 3}'}. Device 0 can reach 3 even though nobody installed a direct cable between them.</Prose>
    <Prose>The previous graph lesson can answer “Are 0 and 3 connected?” with BFS or DFS. For a fixed network, one traversal can label every component in O(n + m) time, where n is the number of vertices and m the number of edges. But suppose links keep arriving and connectivity queries are frequent. Searching the network again after each change repeats work. We need to remember which groups have merged.</Prose>
    <LessonTable caption="A merge changes a partition, not just two individual labels" headers={["New link", "Groups after the link", "Component count"]} rows={[
      ["None", "{0}, {1}, {2}, {3}, {4}, {5}", "6"],
      ["0—1", "{0,1}, {2}, {3}, {4}, {5}", "5"],
      ["2—3", "{0,1}, {2,3}, {4}, {5}", "4"],
      ["1—2", "{0,1,2,3}, {4}, {5}", "3"],
      ["0—3", "{0,1,2,3}, {4}, {5}", "3: already connected"],
    ]} />
    <Prose><strong>Disjoint sets</strong> are groups that do not overlap. A <strong>partition</strong> divides all known elements into such groups: every element belongs to exactly one. A <strong>disjoint-set data structure</strong>, abbreviated DSU and also called <strong>Union-Find</strong>, maintains a partition while groups merge. It is not Python's ordinary set type, and its union operation does not return a newly allocated collection of every member.</Prose>
    <Prose>Each group has a <strong>representative</strong>, an element used as its current name. If <Code>find(a) == find(b)</Code>, a and b belong to the same group. The name need not be the smallest element, first record, oldest device or most important member. It is an implementation choice, and a union may change it.</Prose>
    <LessonTable caption="The small interface behind the mechanism" headers={["Operation", "What the caller asks", "What must remain true"]} rows={[
      ["Initialize / make singleton", "Give each known element its own group", "No element is omitted or shared between groups"],
      ["find(x)", "Return a representative of x's group", "Equal groups have equal representatives"],
      ["union(a, b)", "Merge the groups containing a and b", "All old within-group connections remain"],
      ["connected(a, b)", "Do the representatives match?", "Self-connectivity is true, even with no edge"],
      ["count / component_size(x)", "How many groups / members are there?", "A redundant union changes neither answer"],
    ]} />
    <Prose>Undirected connectivity works because it is an <strong>equivalence relation</strong>: it is reflexive (a connects to itself), symmetric (if a reaches b, b reaches a) and transitive (if a reaches b and b reaches c, a reaches c). A DSU group captures exactly that kind of “belongs together” relationship. One-way reachability is not symmetric. Merging A and B for a directed edge A→B would incorrectly imply B can reach A.</Prose>
    <Checkpoint prompt="After union(0,1), union(1,2), then union(0,2), how many successful merges occurred? Does find(0) have to return 0?">
      <Prose>Two merges. The last call connects elements already in one group, so it does not reduce the component count. Any one representative shared by 0, 1 and 2 is valid; the interface does not require that representative to be 0.</Prose>
    </Checkpoint>

    <H2>2. A forest can remember a partition</H2>
    <H3>First build a correct, simple baseline</H3>
    <Prose>Store one group label per element. Initially <Code>labels = [0, 1, 2, 3, 4]</Code>. To merge 0 and 1, change every entry carrying 0's old label to 1's label. To merge bigger groups, use the same scan. A find is just one array lookup. This approach is called <strong>quick-find</strong>: it eagerly updates every member so later queries are immediate.</Prose>
    <RunnableExample example={unionFindExamples.eager}><Prose>The example assumes valid integer indices. Capture the two original labels before scanning: changing labels[a] partway through the loop must not change which old label the remaining comparisons search for. A successful merge scans n entries and costs O(n); queries cost O(1). A sequence of many merges can therefore be expensive even though a single query is cheap.</Prose></RunnableExample>
    <H3>Delay the relabeling by following parents</H3>
    <Prose>Instead of keeping every label final, let each element point to a <strong>parent</strong>. A root points to itself. To find a representative, follow parents until reaching that self-parent root. Several trees form a <strong>forest</strong>; each tree is one group. To merge two groups, redirect one root's parent to the other root. All descendants now lead to that surviving representative without being individually relabeled.</Prose>
    <UnionFindEquivalenceFigure />
    <Prose>This is <strong>quick-union</strong>. The forest is an index over group membership. It does not preserve the network's original shape, distances or individual edges. In the figure, a parent pointer may connect two elements that were never direct neighbors in the input. Following it answers “Which group?”; it does not produce a physical route.</Prose>
    <Prose><strong>Link roots, not arbitrary endpoints.</strong> Suppose 0 and 1 belong together, with parent[1] = 0, and 2 is alone. Setting parent[1] = 2 moves only the subtree under 1 away from 0. The old connection between 0 and 1 is lost. Setting parent[find(1)] = find(2) instead moves the whole tree. And if the roots are already equal, stop: joining nodes inside one tree carelessly can create a parent cycle and make find loop forever.</Prose>
    <UnionFindLab />
    <Prose>Try joining 0 and 1, then 2 and 3, then 1 and 2. Inspect the exact table after the third merge. Two elements can have different immediate parents yet share a representative. Comparing <Code>parent[a] == parent[b]</Code> is therefore not a valid general connectivity test. The traversal to the root is essential.</Prose>

    <H2>3. Implement the contract and prove it</H2>
    <Prose>The class below uses fixed integer IDs 0 through n−1. Every element begins as a singleton, so parent[i] = i, size[i] = 1 and count = n. The size at a root records its entire component's member count. A union attaches the smaller component under the larger one; ties keep the first argument's root. Find uses an iterative two-pass compression routine, which the next section will unpack visually.</Prose>
    <Prose>Each complete program uses Python 3 with no third-party packages. Copy one into <Code>union_find.py</Code> and run <Code>python union_find.py</Code> in a terminal. Later program disclosures include the class again so each runs independently; their explanations and worked reasoning stay in the main reading flow.</Prose>
    <RunnableExample example={unionFindExamples.basic}><Prose>The output's three groups include isolated elements 4 and 5. An empty structure is allowed and has zero groups; finding any element in it is invalid. Python accepts negative list indices, so explicit validation prevents find(−1) from silently referring to the last element. This teaching API also rejects booleans as IDs even though Python treats bool as a subclass of int.</Prose></RunnableExample>
    <H3>Why this stays a partition</H3>
    <Prose><strong>Initialization:</strong> each element leads to its own root, so there are n separate trees. <strong>Find:</strong> each first-pass step moves to a strict ancestor until a root is reached. In a valid finite forest, that process terminates. The second pass redirects elements only to that same root, so no group splits, no group merges and no representative changes.</Prose>
    <Prose><strong>Union:</strong> if the roots match, both elements already belong to one tree. Otherwise, the roots belong to separate trees, so connecting one root below the other cannot create a parent cycle. Every member of both trees now reaches the surviving root. No member outside those two trees changes group. Adding their sizes and subtracting one from count preserves the metadata. These facts establish the same-root connectivity test after every operation.</Prose>
    <Prose>A non-root size entry is left in the array as an old value. It is no longer authoritative and must not be interpreted as the current subtree size or component size. Always read <Code>size[find(x)]</Code>. The browser table shows a dash at non-roots to reinforce that contract, while the Python implementation keeps the unused storage simple.</Prose>
    <Prose>The class's arrays are implementation state. External writes can break its invariant; callers should use the methods. A production API could make them private or expose read-only snapshots. Find mutates parent pointers when it compresses, even though it leaves the mathematical partition unchanged. Code sharing a DSU concurrently would need an explicit synchronization design; ordinary Python code here does not establish a concurrent DSU algorithm.</Prose>
    <Checkpoint prompt="A successful merge combines components of sizes 3 and 5. What changes? What happens if the same endpoints are passed again?">
      <Prose>The smaller root becomes a child of the larger root, the surviving root's size becomes 8, and component count falls by one. A repeated call finds the same root and returns False; adding the sizes again would double-count members.</Prose>
    </Checkpoint>

    <H2>4. Make long paths unnecessary</H2>
    <H3>Weighting prevents avoidable depth</H3>
    <Prose>A correct unweighted union can still be slow. Always attach the first root below the second, then process (0,1), (1,2), …, (6,7). The forest becomes 0→1→2→3→4→5→6→7. Finding 0 needs seven parent hops. Extend the sequence to n elements and one find can require n−1 hops.</Prose>
    <Prose>In the first lab, choose “First root below second,” then load consecutive links. Switch to “Smaller below larger” and load the same links again. The second construction keeps one large component and adds each singleton directly below its root. The maximum depth is now one. The input connectivity did not change; only the work needed to look up its groups did.</Prose>
    <Prose>Why does size weighting give a general bound, beyond that friendly example? A particular element's depth increases only when its whole component is attached below another root. Its old component has size s; the other component has size at least s. After the attachment, its component has at least 2s members. Every depth increase therefore accompanies at least a doubling: 1, 2, 4, 8, …. There are at most floor(log₂ n) doublings before reaching n members. Thus a size-weighted forest without compression has height at most floor(log₂ n).</Prose>
    <H3>Compression improves the routes you actually use</H3>
    <Prose>Weighting chooses a good attachment when groups merge. <strong>Path compression</strong> improves a path during find. First discover the root. Then point every element on that original path directly at the root. In the balanced eight-element seed, 7→6→4→0 becomes 7→0 and 6→0; 4 already pointed at 0. The root remains 0 and the group still contains all eight elements.</Prose>
    <PathCompressionLab />
    <Prose>Finish the trace, then find again on its result. The first lookup used three upward hops; the next uses one. A different branch, 5→4→0, still has two hops because find(7) did not visit 5. Compression is local to a searched route; it does not scan and flatten the entire forest after every query.</Prose>
    <details className="lesson-deeper"><summary>Run the complete compression trace in Python</summary><RunnableExample example={unionFindExamples.compression} /></details>
    <Prose>The second loop saves <Code>next_item</Code> before overwriting a pointer. Otherwise the code can jump straight to the root and skip the remaining old path. <strong>Full compression</strong> rewrites every path element. <strong>Path halving</strong> is another strategy that repeatedly points a visited element to its grandparent during the walk. They need different traces but preserve the same root invariant. Our Python and browser examples use full compression; Princeton's referenced UF class uses halving.</Prose>
    <H3>Rank is a different kind of metadata</H3>
    <Prose><strong>Union by rank</strong> is an alternative to union by size. Give each singleton rank 0. Attach the lower-rank root under the higher-rank root. If their ranks tie at r, choose one root and increase its rank to r + 1. Without compression, this rank equals the resulting tree height. With compression, paths get shorter but ranks are not decreased: rank remains a historical upper bound used for future attachments.</Prose>
    <LessonTable caption="Three numbers that should not be confused" headers={["Quantity", "What it measures", "Effect of compression"]} rows={[
      ["Component size", "Number of members; authoritative at the root", "Unchanged"],
      ["Current height", "Longest current path to the root, measured in edges", "Can decrease"],
      ["Rank in union by rank", "A balancing bound, increased only when equal-rank roots merge", "Kept unchanged; not recomputed height"],
    ]} />
    <Prose>A root reaches rank r only after enough equal-rank merges to contain at least 2ʳ elements. Thus ranks remain logarithmically bounded. Size weighting and rank weighting are alternative balancing rules; you do not need both. This lesson uses size because it also answers component-size queries directly. Do not replace the size comparison with an arbitrary ID comparison and expect the same guarantee.</Prose>
    <H3>Understand the cost claim you are making</H3>
    <Prose>Size or rank weighting combined with compression has a famous <strong>amortized</strong> bound: over m union/find operations on n initialized elements, the total cost is O(n + m α(n)). The initial n term pays for creating the arrays. The inverse Ackermann function α grows extraordinarily slowly, so this is often called “almost constant amortized time.” It is a sequence guarantee, not a promise that every individual find follows one pointer.</Prose>
    <Prose>Amortized analysis counts all the work across a sequence, including occasional longer operations and the benefit they create for later operations. It is not an average over random inputs. The eight-element compression trace already shows different costs for two consecutive queries. Deriving the inverse Ackermann theorem requires a more advanced charging analysis than the simple doubling argument; treat it as an established bound here, not as something proved merely by observing a small lab.</Prose>
    <LessonTable caption="Array-index model: initialization costs O(n), storage costs O(n)" headers={["Representation", "Find / connectivity query", "Union"]} rows={[
      ["Eager labels (quick-find)", "O(1)", "O(n) worst case"],
      ["Unweighted parent forest", "O(n) worst case", "O(n), including its root searches"],
      ["Size or rank weighting, no compression", "O(log(n + 1)) worst case", "O(log(n + 1)) worst case"],
      ["Weighting plus compression", "O(α(n)) amortized; O(log(n + 1)) individual upper bound", "Same bound, including both finds"],
    ]} />
    <Prose>Count is stored, so reading it is O(1). Component size needs a find. Enumerating every group scans every element and produces n member IDs: <Code>groups()</Code> costs O(n α(n)) under the sequence bound and O(n) output/temporary storage. Ordering or sorting that output adds its own work. The browser also records snapshots, enumerates groups and draws SVG; its display cost is not the data structure's operation bound.</Prose>
    <Checkpoint prompt="Does a failed union necessarily leave the parent array byte-for-byte unchanged? Does compression change the component count?">
      <Prose>A failed union returns False because no groups merged, but its two finds may still compress paths. Thus the parent array can change while the partition and count remain identical. Compression itself never changes the component count.</Prose>
    </Checkpoint>

    <H2>5. Turn real questions into merges</H2>
    <H3>Redundant links reveal a connecting forest</H3>
    <Prose>Process undirected edges in arrival order. If their endpoints have different roots, keep the edge and merge. If the roots already match, a route already connects the endpoints, so adding a new simple edge closes a cycle. The kept edges never close a cycle; they form a <strong>spanning forest</strong> connecting every component of the input.</Prose>
    <Prose>For six vertices and links (0,1), (1,2), (0,2), (3,4), (2,4), the link (0,2) is redundant. Four kept edges connect five vertices; vertex 5 remains alone. There are two components. Starting with n components, each kept edge subtracts one, so there are exactly n − c kept edges when c components remain.</Prose>
    <details className="lesson-deeper"><summary>Run the complete link-classification program</summary><RunnableExample example={unionFindExamples.redundant} /></details>
    <Prose>Joining c nonempty components into one needs at least c−1 new links because one link can reduce the count by at most one. It is sufficient: connect one chosen component to each of the others. If redundant cables can be removed and freely reconnected, at least c−1 spare cables are needed. The kept forest remains intact while those extra cables are moved. This argument assumes undirected links and unrestricted relocation; real installation constraints can change the problem.</Prose>
    <Prose>A self-loop or a repeated edge also produces a failed union. Whether that counts as a cycle depends on the graph's loop/multiedge convention. For the ordinary simple-graph argument above, edges have distinct endpoints and duplicate edges are absent. DSU is reliable about connectivity; the surrounding application must define how duplicate observations are interpreted.</Prose>
    <Prose>If you sort weighted edges from cheapest to most expensive and retain only successful unions, you obtain the structure used by Kruskal's spanning-tree algorithm. DSU prevents cycles, but that alone does not prove minimum total weight: the sorted order and a greedy correctness argument are essential. Continue that application in <a href="/learn/topic/shortest-paths-spanning-trees-topological-ordering">Shortest Paths, Spanning Trees & Topological Ordering</a>.</Prose>
    <H3>Equality constraints need two passes</H3>
    <Prose>Suppose x must equal y and y must equal z. Those declarations imply x equals z even if that pair was never stated. Merge all required equalities. Then an inequality a ≠ b is impossible exactly when find(a) equals find(b). If every inequality crosses between different components, assign a distinct integer to each component; that assignment satisfies all equalities and inequalities. This gives both a rejection argument and a constructive reason for acceptance.</Prose>
    <Prose>Checking inequalities too early fails. In the order x ≠ z, x = y, y = z, x and z are separate when the first statement arrives but must become equal later. Collect all equalities before checking the inequalities, or maintain additional constraint machinery. Ordinary DSU does not automatically revisit earlier rejected relationships.</Prose>
    <details className="lesson-deeper"><summary>Run the complete equality-consistency program</summary><RunnableExample example={unionFindExamples.equations}><Prose>The function accepts arbitrary string names and equality/inequality triples. With q statements and u names, mapping and scanning cost O(u + q α(u)) under hash-table assumptions, plus O(u log u) for the code's explicit sorting of names. Sorting makes ID assignment deterministic and can be omitted if IDs need not follow lexical order. The relation x ≠ x fails immediately; an empty list is satisfiable.</Prose></RunnableExample></details>
    <H3>Shared identifiers can connect records indirectly</H3>
    <Prose>Consider laboratory records with identifier sets: record 0 contains cal-17 and camera-a; record 1 contains camera-a and session-9; record 2 contains session-9. If the policy says any shared identifier links records into a group, all three belong together, even though records 0 and 2 share no identifier directly. A dictionary from identifier to its first record lets later occurrences union with that record instead of comparing every pair of records.</Prose>
    <details className="lesson-deeper"><summary>Run the complete shared-identifier program</summary><RunnableExample example={unionFindExamples.identifiers}><Prose>One vertex represents a record, not an identifier. Records with no identifiers still exist as singleton groups. For R records and T identifier occurrences, the DSU work is O(R + T α(R)), plus enumeration, assuming expected constant-time dictionary operations and ignoring string-length costs. The dictionary stores at most the number of distinct identifiers; the output contains R record indices.</Prose></RunnableExample></details>
    <Prose>The structure executes your equivalence policy; it does not validate it. A reused camera identifier may connect many sessions that you wanted to keep separate. A shared account email may be an intended identity rule, while a shared display name is not enough. Describe what a merge means before applying transitive closure. Otherwise a perfectly implemented DSU can produce an unjustified real-world grouping.</Prose>
    <H3>Keep useful labels as metadata, not forced tree shapes</H3>
    <Prose>Suppose users need the smallest member ID as a stable label for a component. Forcing that ID to become the parent root can conflict with size weighting and build long paths. Instead store a minimum at each root. When two different components merge, set the surviving root's minimum to the minimum of their previous values. A query reads <Code>minimum[find(x)]</Code>. The representative handles efficient storage; the metadata handles application meaning.</Prose>
    <details className="lesson-deeper"><summary>Run the complete minimum-label extension</summary><RunnableExample example={unionFindExamples.metadata}><Prose>The component containing 0, 3 and 4 has representative 3 but minimum label 0. Compression preserves both. A repeated merge must not combine metadata as if it had found new members. Sums, sizes, minima or maxima can be maintained similarly when their merge operation correctly combines disjoint groups; an arbitrary statistic such as a median requires more state and a cost analysis.</Prose></RunnableExample></details>

    <H2>6. Open cells and count islands</H2>
    <Prose>Begin with a grid of closed cells. Opening a cell creates a new land cell, initially an island by itself. For each side-adjacent open neighbor, try a union. Each successful merge reduces the number of islands by one. Diagonal neighbors do not connect under this model. Encode (row, column) as <Code>row * columns + column</Code> to map coordinates into the DSU's integer IDs.</Prose>
    <IslandUnionLab />
    <Prose>Load the ring, then open its center. All four neighbors are already part of one island. The center creates one new component; the first successful union joins it to the ring. The remaining three unions find the same root and do nothing. The island count stays one. Subtracting once per open neighbor would incorrectly report a negative count.</Prose>
    <Prose>The grid has two kinds of state: whether a cell is active, and which active group it belongs to. Allocating a DSU for all R × C possible positions does not make closed cells into islands. Keep a separate active set and active-island count. A repeated activation must be detected before adding one to that count.</Prose>
    <details className="lesson-deeper"><summary>Run the complete incremental-island program</summary><RunnableExample example={unionFindExamples.islands}><Prose>For K activation events, each new cell attempts at most four unions. The dense implementation costs O(RC + K α(RC)) time and O(RC) state, with expected constant-time active-set operations. Coordinate validation happens before use. For an enormous mostly empty grid, a dictionary-based DSU that creates IDs only for activated cells can avoid dense initialization; it still needs coordinate keys and their mapping costs.</Prose></RunnableExample></details>
    <Prose>This same mechanism can follow connected patches as a threshold admits more pixels or voxels, provided the rule only adds vertices/edges. In a percolation model, cells represent open pores and side-neighbor links represent permitted movement; you can ask when a top-to-bottom connection first exists. That is a simplified connectivity experiment, not a full fluid simulator or evidence for a material's physical flow rate.</Prose>
    <Checkpoint prompt="To test top-to-bottom connectivity, someone joins all open top cells to a virtual top node and all open bottom cells to a virtual bottom node. Can that same structure safely answer whether every bottom cell has its own open path to the top after the grid percolates?">
      <Prose>No. Once the virtual nodes connect, an unrelated open bottom cell joined to the virtual bottom may appear connected to the top through that artificial node. This is often called backwash. Keep a separate top-connectivity structure without the virtual bottom node, or maintain top-touch/bottom-touch flags at actual component roots. Virtual edges solve the question they were designed for; they can distort a different query.</Prose>
    </Checkpoint>

    <H2>7. Know what the structure forgets</H2>
    <LessonTable caption="Choose the structure from the question, not its reputation for speed" headers={["Question", "Ordinary DSU?", "What is missing or different?"]} rows={[
      ["Connectivity under added undirected links", "Yes", "Partition only grows by merging"],
      ["Actual route or shortest distance", "No", "Keep the original graph and use traversal / appropriate path algorithm"],
      ["Directed reachability or strong components", "No direct substitution", "One-way edges are not equivalence merges"],
      ["Remove a link or close a cell", "Not supported by this class", "A component may split; discarded edge information is needed"],
      ["List a component's members", "Possible by scanning / added membership state", "Output and enumeration are not constant-time find"],
      ["Preserve previous versions", "Needs another design", "Find itself can mutate paths; rollback or persistence is explicit"],
    ]} />
    <Prose>Removing the last input edge is not the inverse of setting one parent pointer back. The parent edge may never have been an input edge, and later merges or compression may depend on that structure. Even knowing which union originally joined two groups does not reveal whether another remaining input route keeps them connected. Recompute components from retained edges for a simple correct baseline, or study an algorithm designed for the particular dynamic update model.</Prose>
    <details className="lesson-deeper"><summary>Deeper: reversible unions with a rollback log</summary>
      <Prose>If you need to explore a temporary batch of merges and then return to an earlier state, use a stack of changes. Size weighting keeps find logarithmic without compression. Each successful union logs the attached root, its new parent and that parent's old size. Undoing those records in reverse order restores parent, size and count. A redundant union records nothing because a snapshot is the number of actual structural changes, not the number of calls.</Prose>
      <RunnableExample example={unionFindExamples.rollback} />
      <Prose>Find costs O(log(n + 1)); union adds O(1) logged data after its finds. Taking a snapshot costs O(1); rolling back k successful merges costs O(k). History storage is proportional to retained successful merges, at most n−1 between the singleton state and the current state. The checkpoint is a depth in the current history, not a permanent version ID: after rollback and a different branch, an old numeric token may refer to different changes.</Prose>
      <Prose>Compression is deliberately absent. Adding it without logging every pointer rewrite makes rollback incomplete. This data structure can support backtracking or offline algorithms that arrange temporary edge lifetimes into a suitable nested schedule. It does not by itself support deleting an arbitrary old edge while retaining every later edge. <a href="/learn/topic/persistent-data-structures-structural-sharing-versioned-queries">Persistent Data Structures, Structural Sharing & Versioned Queries</a> develops the broader versioning problem if that destination is part of your route.</Prose>
    </details>
    <details className="lesson-deeper"><summary>Deeper: when a parent link must also carry a relationship</summary>
      <Prose>Some tasks ask more than group membership. If variables have known differences, a parent edge may store the difference between an element's value and its parent's value. Following the path accumulates those differences; compression must update both the parent and the accumulated difference. Parity constraints use the same idea with a two-valued relation. Ordinary size or rank metadata is not enough: it describes group structure, not a relation between values.</Prose>
      <Prose>These are weighted or potential DSU variants. Inconsistent cycles require checking whether an already connected pair's implied relationship agrees with a new constraint. Derive the sign/composition convention before implementing them; copying the ordinary parent assignment silently loses the extra information. They are further variants, not capabilities promised by this lesson's class.</Prose>
    </details>

    <H2>8. Practise with a changed contract</H2>
    <Prose>Attempt these before opening the hints. Draw the state, write an invariant and predict boundary behavior. Passing one sample is insufficient: compare your result with a simpler independent method on small cases, then explain why your optimization preserves that method's answer.</Prose>
    <H3>Exercise A · repair an incorrect merge</H3>
    <Prose>A programmer writes the function below for an existing parent forest. Find a concrete input where it loses an old connection, then repair it. State whether comparing the immediate parents of two elements is enough to decide whether the merge is redundant.</Prose>
    <CodeBlock language="python">{`def broken_union(parent, a, b):
    parent[a] = b`}</CodeBlock>
    <details className="lesson-deeper"><summary>Hint for the incorrect merge</summary><Prose>Make a a non-root in a component with another member above it. Move a to a different component and trace where both old members now end.</Prose></details>
    <Checkpoint prompt="Explain your counterexample and the corrected invariant.">
      <Prose>Take parent = [0,0,2] and call broken_union(parent,1,2). The result [0,2,2] separates 0 and 1 even though they used to belong together. Find both roots and, only if they differ, link one root to the other. With size weighting, attach the smaller component and update only the surviving root's size and count. Immediate parents are insufficient: in [0,0,1], 0 and 2 have different parents but the same root. Verify the repaired function against explicit set merging, including same-element and repeated-union calls.</Prose>
    </Checkpoint>
    <H3>Exercise B · count pairs that still cannot communicate</H3>
    <Prose>For n devices and a sequence of undirected links, return the number of unordered pairs in different components after each arrival. With five devices and links (0,1), (2,3), (1,2), (0,3), (3,4), predict the entire answer before coding. Repeated links are allowed.</Prose>
    <details className="lesson-deeper"><summary>Hint for disconnected pairs</summary><Prose>Initially choose any two distinct devices. When components of sizes a and b merge, how many previously separated cross-component pairs become connected? Does a failed union change that number?</Prose></details>
    <Checkpoint prompt="Show the running count and explain why the update counts every newly connected pair exactly once.">
      <Prose>Initially there are n(n−1)/2 = 10 unordered pairs. Merging sizes a and b connects exactly a × b cross pairs; old pairs within either component were already connected. The outputs are [9,8,4,4,0]. The repeated internal link leaves 4 unchanged. Read both root sizes before merging, subtract their product only when the roots differ, and retain the running count. This gives O(n + m α(n)) time and O(n) DSU state plus O(m) output. An independent oracle can enumerate all pairs after a BFS component calculation on each small prefix.</Prose>
      <RunnableExample example={unionFindExamples.exercise} />
    </Checkpoint>
    <H3>Exercise C · distinguish membership, size and representative</H3>
    <Prose>Build the balanced eight-element seed, save each element's representative and the component count, then find(7). List exactly which parent entries change. Does find(5) become a one-hop lookup? Can a root's stored size or the group's minimum label change merely because find compressed a path?</Prose>
    <details className="lesson-deeper"><summary>Hint for compression invariants</summary><Prose>Write the original path of 7 and the separate path of 5 before inspecting any compressed output. Only elements on the first path are candidates for a rewrite.</Prose></details>
    <Checkpoint prompt="Compare the structural edits with the mathematical group.">
      <Prose>Before find(7), the path is 7→6→4→0. parent[7] changes from 6 to 0 and parent[6] from 4 to 0; parent[4] already equals 0. Element 5 still follows 5→4→0. Every representative remains 0, count remains 1, size at root 0 remains 8 and the minimum remains 0. Size-weighted construction, actual height and rank metadata are different concepts; a rank-based implementation would keep its stored rank after compression.</Prose>
    </Checkpoint>
    <H3>Exercise D · an island's boundary touches itself</H3>
    <Prose>In the grid lab, open the ring and then its center. Implement an alternative update that first collects the distinct roots of open neighbors before merging. Compare it with the existing successful-union counting method. Explain what fails if you decrement for each neighbor, or if you count every allocated closed cell as an island.</Prose>
    <details className="lesson-deeper"><summary>Hint for the island boundary</summary><Prose>The number of neighboring cells and the number of neighboring components need not match. Take the set of neighbor roots before any merge changes them.</Prose></details>
    <Checkpoint prompt="State the equivalent update and test it on diagonals and repeated openings.">
      <Prose>Before the activation's merges, collect roots of active side neighbors. Add the new cell, increase count by one, then union it with each collected component and subtract the number of distinct old roots. For the ring, there is one old root, so the change is +1−1 = 0. The existing method reaches the same result by subtracting only when union returns True, without requiring a separate set. Diagonally touching cells remain separate; opening the same cell twice leaves everything unchanged. Dense allocation reserves IDs but does not activate them. Compare every small grid state with a flood-fill count.</Prose>
    </Checkpoint>
    <H3>Ready to move on</H3>
    <Prose>You should be able to reconstruct the ordinary DSU, explain why only roots merge, distinguish rank from size and height, predict compression edits, justify the logarithmic doubling argument and state the amortized theorem accurately. Given a new application, identify its elements and equivalence relation, then decide whether a partition is enough or whether paths, deletions or directed information are required.</Prose>
    <DsaPractice practice={unionFindPractice} />
    <Prose>The next topic in this module's sequence is <a href="/learn/topic/complexity-analysis-recursion">Complexity Analysis & Recursion</a>. It develops the language for comparing total work and analyzing recursive algorithms. Keep the doubling argument and repeated-find trace as concrete examples while learning that broader framework.</Prose>

    <Sources alternatives={<><p><a href="https://www.coursera.org/learn/algorithms-part1" target="_blank" rel="noopener noreferrer">Princeton · Algorithms, Part I, Union-Find module</a> offers a video route from dynamic connectivity through the implementation variants and applications. Start with Dynamic Connectivity; revisit Quick-Union Improvements after the weighting/compression lab. The public course listing was reviewed on 10 September 2026; watching or participating may require a Coursera account. The examples use Java, while this lesson's runnable programs use Python.</p><p><a href="https://algs4.cs.princeton.edu/15uf/" target="_blank" rel="noopener noreferrer">Princeton · written Union-Find case study</a> is useful if you prefer diagrams, implementation comparisons and further exercises. Compare its API conventions with this lesson's explicit root-size and error contracts rather than assuming every implementation stores the same metadata.</p></>}>
      <li><a href="https://algs4.cs.princeton.edu/code/javadoc/edu/princeton/cs/algs4/UF.html" target="_blank" rel="noopener noreferrer">Princeton UF API</a> — primary implementation reference for rank weighting, path halving, representative behavior and the amortized operation bound. Its compression strategy differs from the full-compression trace here.</li>
      <li><a href="https://algs4.cs.princeton.edu/15uf/" target="_blank" rel="noopener noreferrer">Algorithms, 4th edition companion, §1.5</a> — quick-find/quick-union comparison, the size-weighting depth argument, connectivity applications and the distinction between incremental and fully dynamic updates.</li>
      <li><a href="https://coursera.cs.princeton.edu/algs4/assignments/percolation/specification.php" target="_blank" rel="noopener noreferrer">Princeton percolation assignment</a> and <a href="https://coursera.cs.princeton.edu/algs4/assignments/percolation/faq.php" target="_blank" rel="noopener noreferrer">its FAQ</a> — the open-cell connectivity model and the backwash pitfall. Useful further project work; its Java API uses one-based coordinates, unlike the zero-based Python/grid examples here.</li>
    </Sources>
  </div>,
};
