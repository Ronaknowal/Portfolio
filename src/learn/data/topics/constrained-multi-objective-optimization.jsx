import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CoupledProjectionLab, ConstraintPenaltyLab, ConsensusAdmmLab, ParetoDecisionLab, ContinuousTradeoffLab, UnitConversionFigure, CommonDescentFigure } from '../../components/lesson-labs/ConstrainedMultiobjectiveLabs.jsx';
import { constrainedExamples as examples } from '../constrained-multiobjective-examples.js';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram.jsx';
import { mechanismProgram } from '../constrained-optimization-mechanism-program.js';
function Practice({
  title,
  children,
  hint,
  solution
}) {
  return <section className="constrained-practice"><H3>{title}</H3>{children}<details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Show reasoning and acceptance checks</summary>{solution}</details></section>;
}
export default {
  title: 'Constrained & Multi-Objective Optimization',
  readTime: '~90 min read + 3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot constrained-lesson">
    <LessonIntro prerequisites="Gradients and convex feasible sets from Multivariate Calculus and Convex Optimization; multipliers and feasibility from Convex Duality & Lagrangian Methods. Non-Convex Optimization Landscape explains why local evidence need not certify a global answer. We recap projection, define the splitting variables, and introduce Pareto comparisons here." sections={[['1-decide-what-is-allowed-and-what-is-better', 'Allowed versus better'], ['2-project-onto-the-whole-feasible-set', 'Coupled projection'], ['3-make-a-feasible-descent-step', 'Feasible descent'], ['4-distinguish-penalties-and-barriers', 'Penalties and barriers'], ['5-split-a-problem-without-losing-agreement', 'Copies and agreement'], ['6-read-the-pareto-frontier', 'Pareto comparisons'], ['7-express-a-preference-without-hiding-it', 'Weights, bounds and priorities'], ['8-check-local-evidence-and-measurement', 'Evidence and measurement'], ['9-practise-and-connect', 'Practise and connect']]}>A model can have the smallest error and still be unusable because it exceeds a memory or latency limit. Several usable models can also be incomparable: one is faster, another more accurate. We will learn how to keep a decision feasible, solve a coupled problem with explicit checks, and choose among competing goals without pretending that the mathematics supplied our preferences.</LessonIntro>

    <H2>1. Decide what is allowed and what is better</H2>
    <><Prose>A <strong>decision</strong> is the thing you can choose: a model architecture, a vector of actuator commands, or an allocation across machines. A <strong>constraint</strong> says which decisions are allowed. An <strong>objective</strong> says what to improve among those allowed decisions. The same measured quantity can play either role: “latency at most 25 ms” is a constraint; “make latency smaller” is an objective. Using it in both roles is also meaningful.</Prose><Prose>Consider three hypothetical deployment models. Small has error fraction 0.12 and latency 8 ms; Medium has 0.08 and 18 ms; Large has 0.07 and 70 ms. Error fraction 0.12 means 12% of evaluated predictions are wrong. These are teaching measurements, not claims about real products.</Prose></>
    <LessonTable caption="Start with the decision contract" headers={['Part', 'Concrete statement', 'Consequence']} rows={[['Decision set', 'Choose exactly one of Small, Medium or Large', 'A halfway point between models is not an available decision'], ['Hard limit', 'Latency ≤ 25 ms', 'Large is excluded, despite its lower error'], ['Objective', 'Minimize error fraction', 'Medium wins among the remaining models'], ['Tie rule', 'If error is equal, prefer lower latency', 'A hidden preference becomes reproducible']]} />
    <RunnableExample example={examples.selection}><><Prose>Every program on this page is complete. Save one as a <Code>.py</Code> file and run <Code>python filename.py</Code>. Programs importing NumPy need <Code>python -m pip install numpy</Code> in the chosen Python environment. Outputs were executed with Python 3.12 and NumPy 2.3.5; floating-point last digits may vary. The browser investigations run independently of your local Python.</Prose><Prose>Returning <Code>None</Code> for an empty feasible set is informative. Picking the least-violating model instead would answer a different question and needs an explicit policy.</Prose></></RunnableExample>
    <MathBlock>{String.raw`\begin{gathered}\mathcal F=\{x:g_i(x)\le0,\ h_j(x)=0\},\\ \min_{x\in\mathcal F}f(x).\end{gathered}`}</MathBlock>
    <><Prose>The <strong>feasible set</strong> ℱ contains decisions satisfying <em>all</em> inequalities and equalities simultaneously. For latency ℓ(x)≤25, write g(x)=ℓ(x)−25. For a fixed allocation total x₁+x₂=B, write h(x)=x₁+x₂−B. An inequality may have slack; an equality must match its target. A mathematical hard limit is exact; numerical tolerances approximate its evaluation and should be stated in the quantity’s units.</Prose><Prose>Feasibility and existence are separate. The requirements x≥2 and x≤1 are contradictory. A nonempty feasible set can still have no attained minimum: minimizing x over x&gt;0 has infimum 0 but no allowed minimizer. For a continuous objective on a nonempty compact feasible set, a minimum is attained. Convexity supplies other useful structure; it does not by itself make inconsistent requirements consistent.</Prose></>
    <Checkpoint prompt="If two models have equal error and latency but different memory use, are they equivalent?"><Prose>They are equivalent for a problem whose only objectives are error and latency, provided both satisfy its memory limit. If lower memory matters even below that limit, introduce it as another objective or tie rule. A decision problem only expresses the quantities you include.</Prose></Checkpoint>

    <H2>2. Project onto the whole feasible set</H2>
    <><Prose>A proposed update can leave the allowed region. One repair is a <strong>Euclidean projection</strong>: choose the allowed point closest to the proposal. Here coordinates have the same units and distance is ordinary Euclidean distance. If coordinates have different physical units or importance, first choose a justified scaling or metric; “closest” depends on that choice.</Prose></>
    <MathBlock>{String.raw`P_{\mathcal F}(v)=\arg\min_{x\in\mathcal F}\tfrac12\|x-v\|_2^2.`}</MathBlock>
    <><Prose>For a nonempty closed convex set in finite-dimensional Euclidean space, this projection exists and is unique. Existence follows because a sufficiently large ball contains every candidate closer than any fixed feasible reference point; intersecting that ball with the closed set gives a compact search region. Strict convexity of squared distance gives uniqueness on a convex set. Without convexity, projecting 0 onto &#123;−1,1&#125; has two equally close answers. An open set may not attain the closest distance.</Prose><Prose>Now allocate a total amount B between two nonnegative coordinates. Let C=&#123;x:x₁≥0,x₂≥0&#125;, and D=&#123;x:x₁+x₂=B&#125;. C is a quadrant; D is a line. Their intersection is the line segment from (0,B) to (B,0), a two-coordinate simplex.</Prose><Prose>Each separate projection is easy. P<sub>C</sub> clips each negative coordinate to zero. P<sub>D</sub> subtracts the same correction (v₁+v₂−B)/2 from both coordinates; this moves perpendicular to the line. But a correction that fixes one rule can break the other.</Prose></>
    <CoupledProjectionLab />
    <><Prose>With v=(2,−0.6) and B=1, clamping gives (2,0). Projecting that point onto the sum line gives (1.5,−0.5): its sum is 1, but its second coordinate is negative. Reversing the order gives (1.8,−0.8), then (1.8,0), whose sum is 1.8. Neither composition is the required projection.</Prose><Prose>We can solve the joint problem exactly. Substitute x₂=B−x₁ and write t=x₁. Feasibility becomes 0≤t≤B. Differentiate the squared distance in this single variable:</Prose></>
    <MathBlock>{String.raw`\begin{gathered}\begin{aligned}q(t)&=\tfrac12(t-v_1)^2\\&\quad+\tfrac12(B-t-v_2)^2,\end{aligned}\\q^{\prime}(t)=2t-v_1+v_2-B,\\t_*=\operatorname{clip}\!\left(\frac{v_1-v_2+B}{2},0,B\right),\\P_{C\cap D}(v)=(t_*,B-t_*).\end{gathered}`}</MathBlock>
    <><Prose>Before clipping, the example gives t=(2+0.6+1)/2=1.8. Clipping it to [0,1] gives the nearest feasible point (1,0). Its squared distance to the original target is 1²+0.6²=1.36. The sequential result (1.5,−0.5) is closer—squared distance 0.26—but that comparison cannot make an infeasible point acceptable.</Prose></>
    <RunnableExample example={examples.projection}><><Prose>The implementation accepts any positive finite budget and finite two-coordinate target. Both separate orders and the joint projection are printed with independent sum and nonnegativity checks. Repeating individual projections can be a useful algorithm under suitable assumptions, but one pass is not an intersection formula; finding a point in an intersection and finding its closest point are also different tasks.</Prose></></RunnableExample>

    <H2>3. Make a feasible descent step</H2>
    <><Prose><strong>Projected gradient descent</strong> first proposes an ordinary gradient step, then projects that proposal onto the whole feasible set. It is useful when evaluating the gradient and computing the projection are both practical. Use a positive step size η.</Prose></>
    <MathBlock>{String.raw`\begin{aligned}x^+&=P_{\mathcal F}(x-\eta\nabla f(x)),\\G_\eta(x)&=\frac{x-P_{\mathcal F}(x-\eta\nabla f(x))}{\eta}.\end{aligned}`}</MathBlock>
    <><Prose>The <strong>gradient mapping</strong> G<sub>η</sub> measures the actual projected step per unit step size. At a constrained optimum, the ordinary gradient need not vanish: the direction it requests may be forbidden.</Prose><Prose>Use the segment x₁+x₂=1, x≥0 and objective f(x)=½[(x₁−2)²+4(x₂−0.5)²]. Substituting x=(t,1−t) gives derivative (t−2)+4(t−0.5)=5t−4. Its zero is t=0.8, inside [0,1], so x*=(0.8,0.2). The full gradient there is (−1.2,−1.2), pointing equally in both coordinates. Moving along −∇f increases their sum and leaves the segment; projecting that proposal returns x*.</Prose><Prose>Starting at (0.5,0.5) with η=0.2 gives gradient (−1.5,0), proposal (0.8,0.5), then projection (0.65,0.35). In this example the feasible-coordinate recurrence is t⁺=0.5t+0.4, so the error t−0.8 halves each update.</Prose></>
    <RunnableExample example={examples.projectedGradient}><><Prose>The program prints a nonzero ordinary gradient together with a tiny gradient mapping and original-constraint checks. Stopping only when the ordinary gradient norm is small would miss this valid boundary-constrained solution.</Prose></></RunnableExample>
    <details><summary>Why projection supports a descent guarantee</summary><><Prose>For a nonempty closed convex ℱ, p=P<sub>ℱ</sub>(v) obeys (v−p)ᵀ(y−p)≤0 for every y∈ℱ. To derive this, differentiate squared distance along the feasible segment from p toward y; its one-sided derivative at the minimizer cannot be negative. Set v=x−η∇f(x), p=x⁺ and y=x. Rearranging gives ∇f(x)ᵀ(x⁺−x)≤−‖x⁺−x‖²/η.</Prose><Prose>If f has an L-Lipschitz gradient on a region containing the relevant segments, the smoothness bound then gives:</Prose></><MathBlock>{String.raw`\begin{gathered}h=x^+-x,\\\begin{aligned}f(x^+)&\le f(x)+\nabla f(x)^\top h\\&\quad+\tfrac L2\|h\|^2\\&\le f(x)-\left(\tfrac1\eta-\tfrac L2\right)\|h\|^2.\end{aligned}\end{gathered}`}</MathBlock><><Prose>For η≤1/L the right side gives a definite decrease whenever the step is nonzero. Here the Hessian is diag(1,4), so L=4 and η=0.2 satisfies the bound. A zero mapping is equivalent to ∇f(x)ᵀ(y−x)≥0 for all feasible y. For convex differentiable f this certifies a global optimum, as in Convex Optimization. For a nonconvex f it is only a first-order condition; a local maximum can also have zero mapping. These statements assume exact projection and gradient evaluation.</Prose></></details>

    <H2>4. Distinguish penalties and barriers</H2>
    <><Prose>Another approach changes the objective so unwanted decisions become expensive. That is not automatically equivalent to enforcing a hard constraint. A one-dimensional example makes the difference calculable: minimize f(x)=½(x−3)² subject to x≤1. The unconstrained favorite is 3; the hard-constrained optimum is 1 with cost 2.</Prose><Prose>A <strong>squared violation penalty</strong> adds μ·max(x−1,0)²/2, μ≥0. On the violating side x&gt;1, its derivative is (x−3)+μ(x−1). Setting this to zero gives:</Prose></>
    <MathBlock>{String.raw`\begin{gathered}x_\mu=1+\frac{2}{1+\mu},\\x_\mu-1=\frac{2}{1+\mu}>0.\end{gathered}`}</MathBlock>
    <><Prose>Every finite μ leaves positive violation. To make it smaller than 0.1 requires μ&gt;19. Increasing μ improves approximate feasibility but also increases the violating-side curvature from 1 to 1+μ, which can make numerical optimization more demanding. This is one reason “just use a huge number” is not a complete solver design.</Prose><Prose>A <strong>hinge penalty</strong> instead adds ρ·max(x−1,0). On x&gt;1 its derivative is x−3+ρ. The optimum is max(1,3−ρ), so ρ≥2 makes this example exact. At x=1 the hinge has subgradient interval [0,ρ]; it can balance the original derivative −2 precisely when that interval contains 2. This agrees with the hard problem’s multiplier magnitude, but exact-penalty conclusions in other problems require their own regularity and threshold analysis.</Prose><Prose>A <strong>log barrier</strong> adds −τ log(1−x), with τ&gt;0 and domain x&lt;1. It prevents boundary crossing by excluding the boundary and violating side from its domain. Its derivative is x−3+τ/(1−x). Write d=1−x&gt;0. Then d(2+d)=τ, giving:</Prose></>
    <MathBlock>{String.raw`\begin{aligned}d&=\sqrt{1+\tau}-1=\frac{\tau}{\sqrt{1+\tau}+1},\\x_\tau&=1-d<1.\end{aligned}`}</MathBlock>
    <><Prose>The rationalized expression avoids subtracting nearly equal numbers for small τ. For every positive τ the minimizer is strictly inside; as τ tends to zero it approaches the boundary. Setting τ=0 while retaining x&lt;1 does not magically include the boundary—it leaves an unattained infimum. A barrier also needs a strictly feasible domain; equality constraints or a feasible set with no strict interior need appropriate reformulation or another method.</Prose></>
    <ConstraintPenaltyLab />
    <RunnableExample example={examples.penalties}><><Prose>This complete scalar solver also accepts a changed center and bound, under the stated center&gt;bound assumption. It prints quadratic, hinge and barrier optima in that order. Equal numerical strengths belong to different modified objectives; they are not interchangeable convergence parameters.</Prose></></RunnableExample>
    <Checkpoint prompt="A finite penalty returns x=1.001 for a hard x≤1 requirement. Can a small original objective justify accepting it?"><Prose>No. First report the violation and the numerical tolerance or operational policy that could make an approximation acceptable. A penalty value measures the modified problem; it does not rewrite the original requirement by itself.</Prose></Checkpoint>

    <H2>5. Split a problem without losing agreement</H2>
    <><Prose>Projection onto a complex intersection may be expensive even when individual pieces are easy. <strong>Splitting</strong> gives different pieces separate copies of the decision and then enforces agreement. The extra copies are computational variables, not permission to deliver contradictory answers.</Prose><Prose>We will solve the same nearest-point problem as before, so its exact answer provides a check. Use x for the nonnegative copy and z for the copy whose coordinates sum to B. The indicator I<sub>C</sub>(x) is 0 on C and +∞ outside; it encodes a hard domain, not a binary 0/1 penalty.</Prose></>
    <MathBlock>{String.raw`\begin{gathered}\min_{x,z}\;f(x)+g(z),\\\text{subject to }x=z,\\f(x)=\tfrac12\|x-c\|^2+I_C(x),\\g(z)=I_D(z).\end{gathered}`}</MathBlock>
    <><Prose>When x=z, this is exactly the original problem. The <strong>alternating direction method of multipliers</strong>, ADMM, combines a multiplier for disagreement with a squared disagreement penalty. For fixed ρ&gt;0, use augmented Lagrangian f(x)+g(z)+yᵀ(x−z)+ρ‖x−z‖²/2 and scaled multiplier u=y/ρ. Completing the square replaces the last two terms by ρ‖x−z+u‖²/2 plus a term independent of x and z.</Prose></>
    <MathBlock>{String.raw`\begin{aligned}x^{k+1}&=\max\!\left(0,\frac{c+\rho(z^k-u^k)}{1+\rho}\right),\\z^{k+1}&=P_D(x^{k+1}+u^k),\\u^{k+1}&=u^k+x^{k+1}-z^{k+1}.\end{aligned}`}</MathBlock>
    <><Prose>Each line has a specific job. In the x update, combine the two quadratics, whose total curvature is 1+ρ; their unconstrained minimizer is the displayed fraction, then clip it onto C. The z update projects x+u onto D. The u update accumulates disagreement so the next round accounts for the mismatch. Crucially, the z update uses the <em>new</em> x and the old u.</Prose><Prose>Set c=(2,−0.6), B=1, ρ=1, x⁰=z⁰=(0.5,0.5), u⁰=0. Then x¹=max(0,(c+z⁰)/2)=(1.25,0). Its sum is 1.25. Projecting onto D subtracts 0.125 from both coordinates, so z¹=(1.125,−0.125). Finally u¹=(0.125,0.125). x¹ satisfies C and z¹ satisfies D, but neither copy is yet a feasible full decision.</Prose></>
    <ConsensusAdmmLab />
    <H3>Two residuals answer different questions</H3>
    <><Prose>The <strong>primal residual</strong> r=x−z measures disagreement. The <strong>dual residual</strong> for this split is s=−ρ(z<sup>k+1</sup>−z<sup>k</sup>). Its norm measures the stationarity mismatch created because the x subproblem used the previous z. The sign does not change its norm; it does matter if writing the vector optimality equations.</Prose><Prose>For the first update, r=(0.125,0.125), giving ‖r‖≈0.176777. The z change is (0.625,−0.625), so s=(−0.625,0.625) and ‖s‖≈0.883883. Initial r=0 is not enough: the identical initial copies have not optimized the objective, and there is no completed dual-residual test yet.</Prose></>
    <MathBlock>{String.raw`\begin{aligned}\epsilon_{\rm pri}&=\sqrt2\,\epsilon_{\rm abs}\\&\quad+\epsilon_{\rm rel}\max(\|x\|,\|z\|),\\\epsilon_{\rm dual}&=\sqrt2\,\epsilon_{\rm abs}+\epsilon_{\rm rel}\|\rho u\|.\end{aligned}`}</MathBlock>
    <><Prose>The lab uses ε<sub>abs</sub>=10⁻⁴ and ε<sub>rel</sub>=10⁻³ and requires both norms to be below their corresponding tolerances. The √2 factor reflects two consensus coordinates. The consensus equation is x−z=0; its right side is zero, not the allocation budget B. In physical applications, primal and dual quantities can have different units, so tolerance scales must be chosen accordingly; the toy coordinates and costs here are dimensionless.</Prose><Prose>The lab also projects z onto the intersection to produce a fully feasible repair for <em>this</em> simple problem and compares its original cost with the known optimum cost. That repair is a diagnostic convenience, not a general free operation for every large split problem. Displayed values are rounded numerical evidence; neither one small residual nor a solver’s “success” label proves exact feasibility or exact optimality.</Prose></>
    <RunnableExample example={examples.admm}><><Prose>The loop starts its stopping checks after a complete update, uses both residuals, and retains copies of every state. If it reaches its iteration limit, the last boolean still says whether the tests passed. Change ρ, the target or the budget and recheck the original constraints; do not assume an identical iteration count.</Prose></></RunnableExample>
    <details><summary>What the convergence statement assumes—and what it does not say</summary><><Prose>The standard two-block convex result uses closed proper convex f and g, a saddle point of the unaugmented Lagrangian, exact well-defined subproblem solves, and a fixed positive penalty. Under these conditions the primal residual tends to zero, objective values approach the optimum, and the dual sequence converges to a dual solution. General primal variable sequences need not themselves converge without additional assumptions. Our quadratic-plus-polyhedral example has explicit subproblem minimizers and a unique full optimal decision.</Prose><Prose>Do not transfer this guarantee automatically to three or more naive blocks, arbitrary nonconvex objectives, inexact subproblem solves, or an arbitrary changing-ρ policy. If ρ is changed while preserving the unscaled multiplier y, u=y/ρ must also be rescaled. Those variants need their own analysis. Splitting can coordinate computations across machines or facilities, but the mathematical method alone does not provide data privacy.</Prose><Prose>This makes a useful engineering pattern: a local subsystem handles a cheap constraint or cost, a coordinating step enforces shared compatibility, and explicit residuals reveal unfinished agreement. Examples include coordinating energy allocations or separating image-fitting and regularity terms. A complete application must still specify those terms and validate its measurements.</Prose></></details>

    <H2>6. Read the Pareto frontier</H2>
    <><Prose>So far we have minimized one cost under several rules. Suppose we want both lower error <em>and</em> lower latency. An objective vector records both, F(x)=(error(x), latency(x)). There may be no decision that is best on both coordinates.</Prose><Prose>For minimization, decision a <strong>dominates</strong> decision b if it is no worse on every objective and strictly better on at least one. If each is better on a different objective, the choices are incomparable. Equal objective vectors are equivalent under the stated objectives. A feasible decision is <strong>Pareto optimal</strong> if no feasible decision dominates it. Its objective vector belongs to the <strong>Pareto frontier</strong>. Several different decisions can share one frontier vector.</Prose></>
    <MathBlock>{String.raw`\begin{gathered}a\prec b\quad\Longleftrightarrow\\f_i(a)\le f_i(b)\quad\text{for all }i,\\\text{and }f_j(a)<f_j(b)\quad\text{for some }j.\end{gathered}`}</MathBlock>
    <><Prose>Add Compact with error 11%, latency 15 ms and memory 48 MB; add Legacy with 13%, 22 ms and 128 MB. Small, Medium and Large use 32, 96 and 384 MB respectively. Without budget filtering, Legacy is dominated, for example by Medium: 8%&lt;13% and 18&lt;22. Compact is not dominated: Small is faster but less accurate; Medium is more accurate but slower.</Prose></>
    <ParetoDecisionLab />
    <><Prose>A Pareto filter removes choices with a clear improvement available under the stated metrics. It does not choose the final trade-off. With more objectives, dominance becomes harder: memory might convert a formerly dominated choice into an incomparable one if memory becomes an objective. In the lab memory stays a constraint, and the frontier is recomputed after filtering by both budgets.</Prose><Prose>Equal vectors do not strictly dominate one another. A duplicate Medium implementation with the same measured error and latency remains nondominated even if it has a different name. Other distinctions—maintainability, uncertainty, compatibility—must be added if they should affect the decision.</Prose></>
    <RunnableExample example={examples.pareto}><><Prose>This all-pairs implementation uses O(n²m) comparisons for n candidates and m objectives. It retains duplicate objective vectors and handles an empty candidate list. It is a transparent baseline for a small deployment table, not a claim of optimal performance for very large frontiers.</Prose></></RunnableExample>
    <Checkpoint prompt="Does 'Pareto optimal' mean that no other model has a lower error?"><Prose>No. Another model may lower error while increasing latency. Pareto optimality forbids an improvement that is no worse on all the included objectives; it does not forbid trade-offs.</Prose></Checkpoint>

    <H2>7. Express a preference without hiding it</H2>
    <H3>A weighted score states an exchange rate</H3>
    <><Prose>A <strong>weighted sum</strong> turns the objective vector into one number: w₁f₁+⋯+wₘfₘ. For our table, use error in percentage points plus p times latency in milliseconds. The price p has units of error percentage points per millisecond. With p=0.2, accepting one extra millisecond needs a reduction of more than 0.2 error percentage points to lower the score. This is a preference, not a scientific constant.</Prose><Prose>If every weight is strictly positive, every <em>global</em> minimizer of the weighted sum is Pareto optimal. The proof is direct: a dominating decision weakly reduces every weighted term and strictly reduces at least one, contradicting minimality. A local optimizer in a nonconvex problem does not supply that global premise.</Prose><Prose>Zero weights need care. Minimizing only latency can tie two models with equal latency but different error, including a dominated one. A secondary error comparison can resolve that tie. A nonzero vector of nonnegative weights guarantees only <strong>weak Pareto optimality</strong>: no feasible decision can improve every objective strictly. Weak Pareto permits improvements that leave a zero-weight objective unchanged.</Prose></>
    <UnitConversionFigure />
    <RunnableExample example={examples.units}><><Prose>Changing milliseconds to seconds divides the latency number by 1000, so preserving the same score multiplies its coefficient by 1000. Normalizing objectives by meaningful reference scales is another valid approach. Normalization is not mandatory when weights carry the correct units, and normalizing alone does not decide the preferences. Sample-dependent min–max scales can also change when new candidates arrive.</Prose></></RunnableExample>
    <H3>Some useful points cannot win any positive weighted score</H3>
    <><Prose>Compact is Pareto optimal, yet a weighted score can miss it. To beat Small, 11+15p≤12+8p requires p≤1/7. To beat Medium, 11+15p≤8+18p requires p≥1. No price satisfies both. Dividing any weighted sum with a positive error weight by that weight reduces it to this price form, so merely trying more such weights cannot recover Compact.</Prose></>
    <RunnableExample example={examples.unsupported}><><Prose>The exact fraction calculation proves incompatibility for the whole price range, rather than relying on a finite slider sweep. Such an <strong>unsupported Pareto point</strong> can occur for discrete or nonconvex attainable objective sets. Geometrically, no supporting straight score line touches it first.</Prose></></RunnableExample>
    <><Prose>An <strong>epsilon-constraint</strong> formulation keeps one objective and turns others into bounds: minimize error subject to latency≤15 ms. That selects Compact over Small. “Epsilon” is just the conventional name for a chosen bound; it need not be tiny. Bounds have meaningful units and can describe requirements directly.</Prose><Prose>Every Pareto decision can be recovered among global solutions by setting the other objective bounds to its values and minimizing the remaining objective: a strictly better feasible primary value would dominate the proposed Pareto decision. With those exact Pareto-target bounds, every tied optimum must match the target on all objectives: any strict improvement would dominate that target. For general chosen bounds, however, primary-objective ties can include dominated alternatives. Compare the remaining objectives as secondary criteria, or check the resulting frontier.</Prose><Prose><strong>Lexicographic optimization</strong> gives exact priorities: first minimize one objective, then minimize the next while retaining the earlier optimum value, and continue. For a finite table, sorting by an objective tuple implements this directly. If all objectives are eventually compared, a selected decision is Pareto optimal. An “almost best first objective” tolerance instead permits sacrificing some priority; that may be useful, but it defines a different preference.</Prose></>
    <LessonTable caption="Choose a formulation that expresses the actual decision" headers={['Method', 'What the learner must specify', 'Failure to avoid']} rows={[['Weighted sum', 'Unit-aware exchange rates and tie handling', 'Assuming all Pareto points are supported'], ['Epsilon constraints', 'A primary objective and explicit bounds', 'Ignoring infeasibility or dominated primary ties'], ['Lexicographic priorities', 'Exact order, or explicit permitted degradation', 'Hiding a trade-off inside an unexplained tolerance'], ['Present a frontier', 'Which objectives, candidate set and measurements define it', 'Claiming that nondominance itself chooses a winner']]} />
    <H3>A continuous trade-off we can solve end to end</H3>
    <><Prose>Let a decision x range over [0,2]. Define f₁=x², its squared distance from target 0, and f₂=(x−2)², its squared distance from target 2. On this interval, moving right increases the first objective and decreases the second. Consequently every x in [0,2] is Pareto optimal.</Prose><Prose>For weights α and 1−α, α∈[0,1], differentiate αx²+(1−α)(x−2)². The derivative is 2x−4(1−α), and second derivative is 2, so the unique optimum is x*=2(1−α). Alternatively, f₂≤ε means |x−2|≤√ε. On [0,2], minimizing x² chooses the smallest feasible x: max(0,2−√ε). We use 0≤ε≤4.</Prose></>
    <ContinuousTradeoffLab />
    <RunnableExample example={examples.continuous}><><Prose>At α=0.25, x*=1.5 gives objectives (2.25,0.25). The bound ε=0.25 selects the same point through a different statement: achieve second-objective cost at most 0.25, then minimize the first. The distinction matters when preferences change or measurements are uncertain.</Prose></></RunnableExample>
    <details><summary>Why convexity helps weighted sums, with an endpoint caveat</summary><><Prose>For convex objectives on a convex feasible set, the set of attainable objective vectors plus all coordinatewise worse vectors is convex, even when the attainable vectors alone form a curved boundary. Supporting-hyperplane separation connects a Pareto point to a nonzero nonnegative weight vector. Some endpoints require zero weights; not every minimizer with those weights is strongly Pareto optimal. Thus “weighted sums recover the convex frontier” needs both the convex formulation and the nonnegative-weight/tie qualification. The finite Compact example violates the convex-setting premise because choosing exactly one listed model gives a discrete decision set.</Prose></></details>

    <H2>8. Check local evidence and measurement</H2>
    <H3>Can a small step improve every objective?</H3>
    <><Prose>For differentiable objectives, a direction d has first-order change ∇fᵢ(x)ᵀd. If all these rates are strictly negative, a sufficiently small positive step improves all objectives, provided it remains feasible. In an unconstrained open neighborhood this gives a direct local test. For the next local comparison, extend the squared-distance formulas to the whole real line: x=3 is outside the earlier interval and is not an admissible decision in that earlier constrained problem.</Prose></>
    <CommonDescentFigure />
    <details><summary>Deeper: the minimum-norm gradient mixture and its limits</summary><><Prose>Let v be the point of smallest Euclidean norm in the convex hull of the gradients g₁,…,gₘ: v=Σαᵢgᵢ, αᵢ≥0, Σαᵢ=1. Projecting zero onto this convex hull gives (gᵢ−v)ᵀv≥0 for every i. Therefore, if v≠0, d=−v has gᵢᵀd≤−‖v‖²&lt;0 for all i. If v=0, any direction with all gᵢᵀd&lt;0 would give vᵀd=Σαᵢgᵢᵀd&lt;0, impossible. This is a first-order obstruction to improving every objective strictly.</Prose><Prose>For two gradients, write v=g₂+α(g₁−g₂), α∈[0,1]. Differentiating ‖v‖² gives α=clip(−g₂ᵀ(g₁−g₂)/‖g₁−g₂‖²,0,1); identical gradients need no division. This small quadratic problem supplies the complete program below.</Prose><Prose>A zero mixture is not sufficient for nonconvex Pareto optimality: f₁=f₂=−x² at x=0 has zero gradients but is dominated by nearby points. Even with convex objectives, it need not imply <em>strong</em> Pareto optimality when zero mixture weights are allowed. For f₁(x)=0 and f₂(x)=x on the real line, every x admits the zero first gradient as a mixture; reducing x improves the second objective without changing the first. No decision is strongly Pareto optimal, although none can improve both objectives strictly. At a constrained boundary, feasible directions and normal-cone conditions must also be included; do not apply an unconstrained mixture rule while ignoring constraints.</Prose></><RunnableExample example={examples.commonDescent}><><Prose>For gradients (2,0) and (0,2), the mixture is (1,1), so d=(−1,−1) gives rate −2 for both. For opposite scalar gradients 2 and −2, the direction is zero. These are local rate calculations, not a replacement for checking the full objective after a finite update.</Prose></></RunnableExample></details>
    <H3>The best measured decision may solve the wrong question</H3>
    <><Prose>A latency constraint must say what was measured: mean, median, a tail percentile, a workload distribution, hardware and concurrency conditions. Suppose 90 requests take 10 ms and 10 take 100 ms. Their mean is 19 ms, passing a 25 ms mean limit. Their nearest-rank 95th percentile is 100 ms, failing the same numerical limit interpreted as p95. Neither calculation is inconsistent; the requirements differ.</Prose><Prose>Aggregate accuracy can similarly hide a subgroup trade-off. For equally sized groups, system A with accuracies (0.95,0.55) averages 0.75, while B with (0.80,0.68) averages 0.74. A wins on mean accuracy; B wins on worst-group accuracy. Those two summaries answer different questions. One metric alone is not a complete fairness definition, and changing the objective may redistribute errors. State the populations, measurement uncertainty and intended criterion.</Prose></>
    <RunnableExample example={examples.measurement}><><Prose>The percentile convention here is explicit: sort n observations and select rank ⌈pn⌉, starting ranks at 1. Other interpolation conventions can differ on small datasets. This toy calculation describes observed requests; it does not establish the future latency distribution.</Prose></></RunnableExample>
    <><Prose>Other useful applications follow the same formulation discipline. A robot trajectory may minimize energy and tracking error while respecting actuator limits. Image reconstruction may trade data fit against roughness while enforcing nonnegative intensities. A compiler may offer a discrete frontier of program size and execution time. In each case, first name the decision and attainable choices, then the constraints, objective units, trade-off rule and validation data. A sampled frontier from a heuristic is an approximation over explored candidates, not proof that the global frontier has been found.</Prose></>

    <section id="constrained-code-route" aria-label="Simplex projection and a constrained solver">
      <H3>Extend the projection, then control a general solver</H3>
      <Prose>For any positive budget s, the simplex is the set of nonnegative vectors whose entries sum to s. The KKT equations give xᵢ=max(vᵢ−τ,0), with one shared threshold τ. Sort v, examine cumulative sums, and select the last positive active coordinate. This O(d log d) projection preserves the coupled constraint; independently clipping coordinates and renormalizing is generally a different operation.</Prose>
      <Prose>The complete program uses that projection inside gradient descent for ½Σhᵢ(xᵢ−cᵢ)², with strictly positive h. Step size 1/max(h) respects the curvature bound. It stops on the infinity norm of the projected-gradient mapping or returns an explicit exhausted-budget state. SciPy SLSQP receives the identical objective, analytic gradient, nonnegative bounds and sum equality. Its success flag is checked alongside feasibility, the objective and our independently recomputed mapping. Install NumPy 2.3.5 and SciPy 1.18.1; run <Code>python simplex-solver-bridge.py</Code>.</Prose>
      <MechanismProgram {...mechanismProgram} title="Complete simplex projection, descent and SLSQP comparison" />
      <Prose>The two-coordinate case reproduces (.8,.2). The changed four-coordinate case has an inactive second coordinate and solution (6/7,0,3/7,3/14) at budget 1.5. A one-step budget reports failure to converge. This specialized descent uses O(Kd log d) work and O(d) working storage; SLSQP supplies a more general constrained workflow with different stopping and scaling behavior. Keep the full ADMM implementation above for split objectives, and the Pareto routines for actual multiple-objective decisions: scalar SLSQP does not choose a preference for you.</Prose>
      <details><summary>Implementation practice: change the feasible budget</summary><Prose>Set c=(2,−1), h=(1,1), and s=.5. Compare projection, projected descent and SLSQP. Then give SLSQP an equality that contradicts its bounds.</Prose><details><summary>Solution and checks</summary><Prose>The feasible optimum is (.5,0). The mapping is zero although the ordinary gradient is not. A contradictory equality must produce a failure or infeasible residual; never report only the returned coordinates as a successful solution. Check sum, nonnegativity and the original objective after every changed constraint.</Prose></details></details>
      <Prose><a href="https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html" target="_blank" rel="noreferrer">SciPy SLSQP</a> documents its stopping controls. Its ftol is not the same quantity as this program's projected-gradient tolerance.</Prose>
    </section>
    <H2>9. Practise and connect</H2>
    <><Prose>Work these on paper first, then alter a complete program to check the result. State the feasible set, objective and numerical tolerance before accepting output.</Prose></>
    <Practice title="Repair a changed coupled projection" hint="Use t=(v₁−v₂+B)/2 before clipping. Then calculate both individual projection orders." solution={<><Prose>With v=(0.5,3), B=2, the unconstrained t is −0.25, so the joint projection is (0,2), with squared distance 0.25+1=1.25. Clamping first changes nothing; line projection then yields (−0.25,2.25), violating nonnegativity. Line projection then clamping yields (0,2.25), violating the sum. Acceptance: the joint result has nonnegative coordinates, exact sum 2, and no other t∈[0,2] lowers the convex one-variable distance.</Prose></>}><Prose>Project v=(0.5,3) onto x≥0, x₁+x₂=2. Compare both separate orders and compute the exact minimum squared distance.</Prose></Practice>
    <Practice title="Change the penalty problem, not just the slider" hint="Replace the unconstrained center 3 by 4. The derivative of the original cost at the bound is now −3." solution={<><Prose>For ½(x−4)² with x≤1, the hard optimum is x=1 with cost 4.5. The squared-penalty optimum is 1+3/(1+μ); violation≤0.1 requires μ≥29. The hinge becomes exact for ρ≥3. With barrier τ=4, write d=1−x; d²+3d=4 gives d=1 and x=0. That is strictly feasible with original cost 8, not the hard optimum. Check all values against the modified objective derivative or hinge subgradient.</Prose></>}><Prose>Move the center to 4, keeping the bound 1. Find a squared penalty giving violation at most 0.1, the exact hinge threshold, and the barrier optimum at τ=4.</Prose></Practice>
    <Practice title="Trace a different consensus update" hint="Start with x⁰=z⁰=(0.5,0.5), u⁰=0. Remember that z¹ uses the new x¹." solution={<><Prose>For c=(1.5,−0.5), ρ=1, B=1, x¹=(1,0). Its sum is already 1, so z¹=(1,0) and u¹=(0,0). Primal norm is zero, but the dual vector is −(z¹−z⁰)=(−0.5,0.5), with norm √0.5≈0.707107. One small residual is insufficient. The exact projection is (1,0); this special run has reached the primal optimum at its first update while its standard dual stopping test has not yet passed. At the next update x becomes (1.25,0) and z becomes (1.125,−0.125): the copies move away from that first primal answer while the multiplier adjusts. Agreement and primal location alone did not establish the full optimality conditions.</Prose></>}><Prose>Use c=(1.5,−0.5), B=1 and ρ=1. Calculate the first x, z, u and both residuals. Explain why a correct primal answer and a failed numerical stopping test can coexist at this frame.</Prose></Practice>
    <Practice title="Separate duplicate, dominated and unsupported choices" hint="Use the strict part of dominance. Recover Compact by a latency bound, then apply the memory bound independently." solution={<><Prose>With all five original candidates feasible, S,C,M,L are nondominated and G is dominated. A second implementation with the same metrics as M is also nondominated; it does not strictly dominate M. Under latency≤15 ms and memory≤40 MB, Compact fails memory and Small is selected. Raising memory to 48 MB makes Compact feasible and it wins on error. Compact’s weight inequalities p≤1/7 and p≥1 remain incompatible without those filters; feasibility changes the comparison set.</Prose></>}><Prose>Add an M-copy with identical metrics. Identify the frontier, then choose minimum error subject to latency≤15 ms and memory≤40 MB. What changes if memory rises to 48 MB?</Prose></Practice>
    <Practice title="Keep an exchange rate consistent across two unit changes" hint="Changing error from percentage points to a fraction divides the whole score by 100; changing latency to seconds multiplies its numerical coefficient by 1000." solution={<><Prose>The original score is E<sub>points</sub>+0.2L<sub>ms</sub>. Dividing by 100 gives E<sub>fraction</sub>+0.002L<sub>ms</sub>. Since L<sub>ms</sub>=1000L<sub>s</sub>, the equivalent fractional-error score is E<sub>fraction</sub>+2L<sub>s</sub>. Medium has 0.08+2·0.018=0.116, the original 11.6 divided by 100. Acceptance: all models’ scores transform by the same positive factor and their ranking is unchanged.</Prose></>}><Prose>Rewrite the price 0.2 error percentage points per millisecond using error fractions and latency in seconds. Verify Medium’s score and ranking.</Prose></Practice>
    <Practice title="Recover one point using two preference languages" hint="The weighted optimum is 2(1−α); the epsilon optimum is 2−√ε on the stated range." solution={<><Prose>For x=0.5, α=0.75 and ε=(0.5−2)²=2.25. Both select objectives (0.25,2.25). The weight expresses an exchange between objectives; the bound expresses a requirement on the second. Every x∈[0,2] is Pareto because a strict decrease in x improves x² and worsens (x−2)², and a strict increase reverses that. A finite sampling plot alone would not prove the statement between samples.</Prose></>}><Prose>Choose x=0.5 in the continuous example using a weight and an epsilon bound. Explain why every point on that curve is Pareto.</Prose></Practice>
    <Practice title="Challenge a first-order certificate and a metric claim" hint="A zero gradient can hide a maximum. A percentile is a rank of observations, not their arithmetic average." solution={<><Prose>For both objectives −x² at 0, both gradients vanish. Moving to x=0.1 gives both values −0.01 instead of 0, so the point is dominated. For 98 latencies of 10 ms and two of 200 ms, the mean is (980+400)/100=13.8 ms and nearest-rank p95 is 10 ms; nearest-rank p99 is 200 ms. A p95≤25 statement permits this p99 failure and does not guarantee that every request is below 25 ms. Report the exact metric and sample scope.</Prose></>}><Prose>Disprove “a zero gradient mixture proves Pareto optimality.” Then compute mean, nearest-rank p95 and p99 for 98 requests at 10 ms and two at 200 ms.</Prose></Practice>
    <><Prose>You can now separate a feasible decision from a promising but invalid proposal, trace a split solver’s unfinished agreement, and state why a trade-off was selected. Keep the original constraints and measurements visible alongside any solver output.</Prose><Prose>The next module topic is <strong>Probability Distributions &amp; Bayes’ Theorem</strong>. Our deployment tables treated error and latency as known numbers. Probability supplies the next layer: how variable observations, uncertain quantities and new evidence should be represented before they become optimization inputs. </Prose></>
    <Sources alternatives={<ul>
      <li><a href="https://www.youtube.com/watch?v=VxQ8VHm1Ci4&t=1643s" target="_blank" rel="noreferrer">Stephen Boyd, EE364A Lecture 7: vector and multicriterion optimization</a> — begin around 27:23 for objective vectors, 33:10 for Pareto comparisons, and 54:48 for scalarization. The <a href="https://see.stanford.edu/Course/EE364A/89" target="_blank" rel="noreferrer">official lecture page</a> provides bookmarks and a transcript. The relevant transcript and bookmarks were reviewed for these concepts; the full recording was not watched. This is an optional university-level visual explanation after the concrete examples above.</li>
      <li><a href="https://web.stanford.edu/~boyd/papers/pdf/springer_15_lect2.pdf" target="_blank" rel="noreferrer">Boyd, distributed optimization lecture slides</a> — a diagram-oriented extension of local copies, consensus and exchange problems. Use it after tracing the three ADMM updates. Relevant consensus/exchange slides were inspected; historical application timings are not current performance guidance.</li>
    </ul>}>
      <li><a href="https://web.stanford.edu/~boyd/cvxbook/bv_cvxbook.pdf" target="_blank" rel="noreferrer">Boyd &amp; Vandenberghe, Convex Optimization</a> — §4.7 develops vector objectives, Pareto points and the positive-versus-nonnegative weight distinction; §11.2 introduces the logarithmic barrier. These support the assumptions and endpoint cautions above.</li>
      <li><a href="https://web.stanford.edu/~boyd/papers/pdf/admm_distr_stats.pdf" target="_blank" rel="noreferrer">Boyd and colleagues, Distributed Optimization and Statistical Learning via ADMM</a> — §§3.1–3.3 define scaled updates, convex convergence assumptions, residuals and stopping thresholds. Read after the complete two-coordinate derivation; do not transfer its theorem to an arbitrary nonconvex or multi-block variant.</li>
    </Sources>
  </div>
};
