import { Callout, Code, CodeBlock, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { AdaptiveLab, BracketLab, CalibrationLab, DifferenceLab, NewtonLab, PumpConnectionFigure, QuadratureLab } from '../../components/lesson-labs/NumericalMethodsLabs.jsx';
import { numericalMethodsExamples } from '../numerical-methods-examples.js';
function NumericalExample({
  example
}) {
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /><Prose>{example.explanation}</Prose></>;
}
function Practice({
  question,
  hint,
  children
}) {
  return <div className="nm-practice"><Prose><strong>Try independently.</strong> {question}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Show explained solution</summary>{children}</details></div>;
}
export default {
  title: 'Numerical Methods (Finite Differences, Quadrature, Root Finding)',
  readTime: '~75 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot numerical-methods-lesson">
    <LessonIntro prerequisites="You can use functions and arithmetic. Slope, area and Taylor approximation are refreshed here; Single-Variable Calculus: Limits, Derivatives & Integrals supplies the longer foundation when available. Python loops/functions are useful for the runnable examples; the visual investigations need no installation." sections={[['1-an-answer-and-a-reason-to-trust-it', 'A rate, a total and a target'], ['2-keep-a-root-inside-an-interval', 'Bisection and what it guarantees'], ['3-follow-a-tangent-with-a-safeguard', 'Newton, failures and safeguards'], ['4-estimate-a-derivative-without-losing-the-signal', 'Stencils and competing errors'], ['5-turn-local-shapes-into-total-area', 'Trapezoids, Simpson and samples'], ['6-refine-where-the-function-needs-attention', 'Adaptivity and its blind spots'], ['7-choose-nodes-and-compose-error-budgets', 'Gaussian nodes and calibration'], ['8-transfer-the-error-discipline', 'Linear solves, AD and time evolution'], ['9-practise-with-changed-problems', 'Independent practice and answers'], ['10-next-connections-and-learning-resources', 'Next connections and resources']]}>Learn to compute an approximate answer and explain what supports its accuracy. Follow one small calculation before using a library, then challenge the method with a case where its usual story breaks.</LessonIntro>

    <H2>1. An answer and a reason to trust it</H2>
    <Prose>A pump delivers liquid at a changing rate. You want to know how quickly the rate is changing now, how much liquid has arrived so far, and when the total will reach a target. These become three different numerical questions: estimate a <strong>derivative</strong>, estimate an <strong>integral</strong>, and find a <strong>root</strong>. A numerical method answers such questions using a finite collection of arithmetic operations and function evaluations.</Prose>
    <Prose>We will use a synthetic rate r(t) = t exp(−t), with the numerical value t measured in minutes and the numerical rate measured in litres/minute. In a dimensional formula, the exponent is t divided by one minute, and the prefactor carries the rate scale. This is a deliberately simple model whose exact answers are available for checking. It is not a measured pump calibration.</Prose>
    <PumpConnectionFigure />
    <Prose>Near a time t, the slope tells us how much the rate changes per extra minute. Area under the rate curve adds small amounts “rate × time” into litres. A target time solves V(T) − target = 0, where V(T) is the accumulated volume. Zero is simply a convenient way to express equality; root finding is not limited to equations whose original application mentions roots.</Prose>
    <Prose>The exact quantity is our mathematical target. The difference between that quantity and a computed result is the <strong>error</strong>. Usually we cannot calculate the error directly because we do not know the exact answer. Instead we may have a proven bound under assumptions, an estimator built from two approximations, or a comparison with an independent method. These are different kinds of evidence.</Prose>
    <LessonTable caption="Four statements that should not be confused" headers={['Statement', 'Meaning']} rows={[['Requested tolerance: 0.01 minutes', 'The accuracy we want; a request is not evidence it was achieved.'], ['Error estimate: 0.003 minutes', 'A computed diagnostic whose reliability has assumptions.'], ['Conditional bound: at most 0.008 minutes', 'A mathematical conclusion when its hypotheses and error model hold.'], ['Actual error on a known fixture: 0.005 minutes', 'A comparison with this fixture’s reference, not a universal guarantee.']]} />
    <Prose>An absolute error carries the answer's units. Relative error divides by an appropriate reference scale, and becomes awkward when the true value is zero or very small. A useful mixed target often has the form absolute tolerance + relative tolerance × scale. State the exact convention: some library routines use a maximum instead of a sum.</Prose>
    <Prose>Also separate three sources of difficulty. A model or measurement can be uncertain before any computation begins. A discretization replaces a continuous operation by finitely many samples. Floating-point evaluation rounds arithmetic and can lose information. Finally, <strong>conditioning</strong> describes how sensitive the mathematical answer is to changed input; algorithmic <strong>stability</strong> concerns how the computation handles errors. A well-written method cannot make an intrinsically sensitive problem insensitive.</Prose>
    <Callout label="A residual needs a scale">For f(x) = 10⁻⁶(x − 1), x = 0 has residual |f(x)| = 10⁻⁶ but is one whole unit from the root. The function is so flat that a small vertical error corresponds to a large horizontal error. We will make this conversion explicit in the pump calculation.</Callout>
    <Prose><strong>Run the examples:</strong> use Python 3, save a complete block as a .py file and run <Code>python filename.py</Code> in your terminal. The following examples need only the standard library until the final SciPy comparison, where installation is shown. Each printed reference is explained; experiments in the page run independently in JavaScript.</Prose>
    <NumericalExample example={numericalMethodsExamples.original} />

    <H2>2. Keep a root inside an interval</H2>
    <Prose>Suppose a continuous curve is below zero at one endpoint and above zero at the other. It must pass through zero somewhere between them. This is the intermediate value theorem. The endpoints form a <strong>bracket</strong>: they do not reveal the exact root, but they retain a place where at least one root exists.</Prose>
    <Prose>For f(x) = x² − 2, begin with [0, 2]. The midpoint is 1, where f(1) = −1. Keep [1, 2], whose signs are still opposite. Its midpoint is 1.5, where f(1.5) = 0.25, so keep [1, 1.5]. Next, f(1.25) = −0.4375, so keep [1.25, 1.5]. The operation is called <strong>bisection</strong>: evaluate the middle and retain the half with opposite signs.</Prose>
    <MathBlock>{String.raw`\begin{gathered}w_k=\frac{b_0-a_0}{2^k},\\ m_k=a_k+\frac{b_k-a_k}{2},\\ |m_k-r|\leq\frac{w_k}{2}.\end{gathered}`}</MathBlock>
    <Prose>Here k counts completed interval halvings, wₖ is the current width, mₖ is its midpoint, and r is a root in that bracket. The width statement concerns exact arithmetic; actual floating-point updates eventually stop resolving distinct points. If the initial width is 2, ten halvings leave width 1/512 and midpoint radius 1/1024 ≈ 0.0009766. Do not mix this convention with a formula that counts the initial midpoint as iteration one.</Prose>
    <BracketLab />
    <H3>What the interval does and does not prove</H3>
    <Prose>Continuity matters. The function 1/x is negative at −1 and positive at 1 but has no root between them; it is undefined at zero. Conversely, (x − 1)² touches zero while staying nonnegative, so endpoints 0 and 2 do not have opposite signs. Bisection is a method for an established bracket, not a complete procedure for discovering every root. Opposite signs also establish existence, not uniqueness; monotonicity can provide the latter.</Prose>
    <Prose>Check endpoint roots before requiring opposite signs. Compare signs directly rather than multiplying f(a)f(b): multiplying two huge finite values may overflow even when comparing their signs is easy. Stop explicitly on a nonfinite evaluation, exhausted work limit or midpoint equal to an endpoint. Reporting failure preserves information that a silently returned plausible decimal would hide.</Prose>
    <Prose>A computed zero deserves similar care. If a function contains cancellation or an approximate inner computation, its evaluated value can round to zero even when the mathematical function does not. For a known continuous function with reliably evaluated signs, the bracket is informative; an uncertain sign cannot safely discard half an interval. Section 7 makes that issue visible.</Prose>
    <NumericalExample example={numericalMethodsExamples.bracket} />

    <H2>3. Follow a tangent with a safeguard</H2>
    <Prose>Bisection uses signs and ignores the curve's shape. Newton's method uses a local straight-line approximation. At a current point x, the tangent has equation f(x) + f′(x)(z − x). Set that line to zero and solve for its horizontal intercept z:</Prose>
    <MathBlock>{String.raw`x_{k+1}=x_k-\frac{f(x_k)}{f'(x_k)}.`}</MathBlock>
    <Prose>For x² − 2 at x = 1.5, the value is 0.25 and the slope is 3. The next point is 1.5 − 0.25/3 ≈ 1.41667, already near √2. This works because the tangent is a good local description. With a twice continuously differentiable function, a nonzero derivative at the root, and a sufficiently close starting point, Newton's error eventually shrinks quadratically: roughly a constant times the square of its previous error. The “sufficiently close” condition is part of the result.</Prose>
    <Prose>A tangent is not a global map. For x³ − 2x + 2, starting at zero gives f = 2 and slope −2, so the next point is 1. At 1, f = 1 and slope 1, so the next point is 0. It cycles forever in exact arithmetic. A zero derivative prevents this formula; a tiny derivative can produce a huge move. Domain restrictions can make the proposed point invalid.</Prose>
    <NewtonLab />
    <Prose>A multiple root changes the convergence story. For f(x) = (x − 1)², Newton gives x_next = (x + 1)/2 away from the root, so the error halves. The derivative at the root is zero; the simple-root quadratic theorem does not apply. Also, a small update does not by itself guarantee a small residual, and a small residual needs a sensitivity argument to guarantee a small location error.</Prose>
    <Prose>A <strong>safeguard</strong> combines local speed with retained interval information. Our teaching method keeps opposite-sign endpoints. It accepts a finite Newton proposal only inside the central 80% of that bracket; otherwise it bisects. The accepted point is at least 10% of the width from either endpoint, so the new retained width is at most 90% of the old width in exact arithmetic. This is a concrete safeguard, not an implementation of Brent's algorithm.</Prose>
    <NumericalExample example={numericalMethodsExamples.newton} />
    <Prose>If derivatives are inconvenient, the <strong>secant method</strong> estimates a slope from two previous points. It can also leave a safe domain or divide by a tiny difference of function values. Production <Code>scipy.optimize.brentq</Code> instead combines bracketing with interpolation under its documented assumptions. Choosing a library does not remove the need for continuity, meaningful signs and inspection of its status.</Prose>
    <H2>4. Estimate a derivative without losing the signal</H2>
    <Prose>A derivative is a limiting rate of change. With only nearby function values available, a forward difference uses the slope between x and x + h. A central difference uses points on either side. To understand their errors, use a Taylor expansion: locally, a smooth function is its value plus slope × displacement, plus curvature and higher-order corrections.</Prose>
    <MathBlock>{String.raw`\begin{aligned}f(x+h)&=f(x)+hf'(x)\\ &\quad+\tfrac12h^2f''(x)+\cdots,\\ D_+(h)&=\frac{f(x+h)-f(x)}h,\\ D_0(h)&=\frac{f(x+h)-f(x-h)}{2h}.\end{aligned}`}</MathBlock>
    <Prose>The forward subtraction cancels the original value but leaves a curvature term proportional to h. The central subtraction cancels the even powers as well, leaving a leading derivative error proportional to h². Under suitable bounded second and third derivatives, respectively, their truncation errors are at most M₂h/2 and M₃h²/6. “First order” and “second order” describe those powers; they do not describe which derivative is being estimated.</Prose>
    <LessonTable caption="A stencil is a weighted pattern of sample locations" headers={['Target', 'Locations relative to x', 'Weighted result']} rows={[['First derivative, forward', '0, +h', '(−f(x) + f(x+h)) / h'], ['First derivative, central', '−h, +h', '(−f(x−h) + f(x+h)) / (2h)'], ['Second derivative, central', '−h, 0, +h', '(f(x−h) − 2f(x) + f(x+h)) / h²'], ['First derivative, one-sided second order', '0, +h, +2h', '(−3f(x) + 4f(x+h) − f(x+2h)) / (2h)']]} />
    <Prose>The central second derivative cancels constants and linear terms, then divides the remaining quadratic-scale change by h². With a bounded fourth derivative it has second-order truncation error. At a boundary you may not be allowed to sample x − h; the last row avoids that point. Its weights come from requiring constants to disappear, linear functions to differentiate correctly, and the quadratic error term to vanish. More sample points are useful only when their information and the function's smoothness justify them.</Prose>
    <NumericalExample example={numericalMethodsExamples.stencil} />
    <H3>Smaller spacing amplifies errors in the values</H3>
    <Prose>Suppose each sampled value differs from its exact value by at most η. Subtracting two such values can introduce error at most 2η; division by h then amplifies it. For a central difference the denominator is 2h. Combining this sample-error model with truncation gives:</Prose>
    <MathBlock>{String.raw`\begin{aligned}|D_+-f'(x)|&\leq M_2h/2+2\eta/h,\\ |D_0-f'(x)|&\leq M_3h^2/6+\eta/h.\end{aligned}`}</MathBlock>
    <Prose>These inequalities assume exact subsequent arithmetic on the perturbed samples. Real subtraction, division and coordinate formation add rounding too. The two terms explain the competing tendencies: decreasing h improves the local approximation but magnifies value error. When the leading constants are nonzero and this model applies, balancing terms gives a step scale proportional to √η for forward differences or ∛η for central differences. The constants depend on the function and its units; this is not a universal recipe to use one fixed h everywhere.</Prose>
    <DifferenceLab />
    <Prose>For f(x) = 10¹² + x at x = 1, h = 10⁻⁵ is too small to change the rounded function value in binary64. The computed central difference becomes zero although the true derivative is one. Even earlier, coordinates can coincide: at x = 1, adding 10⁻²⁰ rounds back to x. An algorithm must not report an apparently precise derivative from two identical sample coordinates.</Prose>
    <NumericalExample example={numericalMethodsExamples.differences} />
    <Prose>A measurement-noise term may be much larger than machine rounding. Repeated measurements, smoothing or a model fit can help, but introduce their own assumptions; shrinking the difference interval does not magically remove noise. For a deterministic gradient check, keep the input state and random draws controlled while perturbing one coordinate or direction. Compare absolute error near zero and relative error only against a meaningful scale. A passing set of checks supports those tested cases, not every possible computation graph.</Prose>

    <H2>5. Turn local shapes into total area</H2>
    <Prose>An integral adds continuously varying contributions. Replace each short section of a curve by a shape whose area is easy to calculate. The trapezoid rule joins neighboring samples with a line. A strip of width h and endpoint heights u and v has area h(u + v)/2. Interior samples belong to two neighboring strips, explaining their doubled contribution to a composite sum.</Prose>
    <MathBlock>{String.raw`\begin{aligned}T_n&=\tfrac h2[f(a)+f(b)]\\ &\quad+h\sum_{i=1}^{n-1}f(a+ih),\\ h&=(b-a)/n.\end{aligned}`}</MathBlock>
    <Prose>For x² on [0,1] with two panels, the sampled values are 0, 1/4 and 1. The trapezoid result is (1/2)(0/2 + 1/4 + 1/2) = 3/8, while the true area is 1/3. The chords sit above the convex curve, so the sign of the error has a geometric explanation. With |f″| bounded by M₂ on the interval, the mathematical absolute error is at most (b − a)h²M₂/12.</Prose>
    <Prose>Simpson's rule replaces a pair of neighboring panels by one quadratic interpolant. On the local coordinate u ∈ [−1,1], the three interpolation basis functions are u(u−1)/2, 1−u² and u(u+1)/2. Their integrals are 1/3, 4/3 and 1/3. Scaling back to equally spaced points gives the weights 1, 4, 1:</Prose>
    <MathBlock>{String.raw`\begin{gathered}Q=f(a)+4f(m)+f(b),\\ m=(a+b)/2,\\ S_{[a,b]}=(b-a)Q/6.\end{gathered}`}</MathBlock>
    <Prose>Although the fitted curve is quadratic, the symmetric integral also handles the odd cubic component exactly. Classic composite Simpson groups an even number of subintervals into pairs; its global weights are 1, 4, 2, 4, …, 2, 4, 1, multiplied by h/3. For a continuous fourth derivative bounded by M₄, the usual absolute error bound is (b − a)h⁴M₄/180. Those derivative hypotheses matter.</Prose>
    <QuadratureLab />
    <Prose>There is an important input distinction. A callable function lets you request new values anywhere in its domain. A measurement record supplies only its existing samples. For unequal times tᵢ, a piecewise-linear estimate sums (tᵢ₊₁ − tᵢ)(yᵢ + yᵢ₊₁)/2. Reordering time/value pairs or pretending the spacing is constant changes the problem. That estimate says what the linearly interpolated record integrates to, not what an unknown physical curve must have done between observations.</Prose>
    <NumericalExample example={numericalMethodsExamples.quadrature} />
    <Prose>The SciPy sampled-data Simpson API has its own handling of sample counts and unequal spacing. Its familiar cubic exactness assumes equally spaced samples with an odd number of sample points; for arbitrary spacing the documented polynomial exactness is lower. Do not silently transfer a theorem about classic composite Simpson to every branch of a library API.</Prose>

    <H2>6. Refine where the function needs attention</H2>
    <Prose>A uniform grid spends equal work on flat and rapidly changing regions. Adaptive integration spends additional evaluations where its local comparison suggests a problem. But first we need to understand what the comparison estimates.</Prose>
    <Prose>Suppose an approximation has an expansion A(h) = I + c hᵖ + smaller terms. Repeating with h/2 gives A(h/2) ≈ I + c hᵖ/2ᵖ. Subtract these equations and solve for I: the improved estimate is A(h/2) + [A(h/2) − A(h)]/(2ᵖ − 1). This is Richardson extrapolation. It relies on a leading-error regime; two accidentally matching approximations do not establish that regime.</Prose>
    <Prose>For Simpson, compare one panel-pair result S with two half-interval results whose sum is S₂. Under the smooth leading-error model, the refined error is approximately |S₂ − S|/15, because 2⁴ − 1 = 15. The corrected value is S₂ + (S₂ − S)/15. We now have an estimator that can guide refinement, not a universal upper bound.</Prose>
    <Prose>Give an interval a positive absolute budget ε. Evaluate its endpoints, midpoint and quarter points. If the estimate is within ε, accept it. Otherwise split the interval and give each child ε/2. Reuse old samples, add the child estimates, and sum their estimated errors. The child budgets add up to the original budget. Passing the full original tolerance independently to every child would not preserve that simple accounting.</Prose>
    <AdaptiveLab />
    <NumericalExample example={numericalMethodsExamples.adaptive} />
    <H3>A zero estimate can miss a positive integral</H3>
    <Prose>Consider g(x) = [x(x − 1/4)(x − 1/2)(x − 3/4)(x − 1)]² on [0,1]. It is a smooth nonnegative polynomial. The five initial adaptive Simpson samples are exactly its five roots. Both the coarse and refined estimates are zero, so their estimated discrepancy is zero. Yet g is positive between the roots and its integral is 5/1419264 ≈ 3.52295 × 10⁻⁶.</Prose>
    <Prose>Switch the investigation to this blind spot. The drawing uses additional points to show the curve, but the integration algorithm sees only its own blue sample points. A dense display is not evidence that the algorithm sampled densely. More permitted recursion depth cannot help if the initial estimator never requests a split. Additional assumptions, strategically supplied points or a different diagnostic are needed.</Prose>
    <NumericalExample example={numericalMethodsExamples.blind} />
    <H3>Change a difficult problem before increasing the work</H3>
    <Prose>The integral of x⁻¹ᐟ² from zero to one is finite, but its endpoint value is infinite. Omitting the interval [0,δ] discards 2√δ; even δ = 10⁻⁶ loses 0.002 of area. An arbitrarily tiny-looking cutoff can violate an accuracy request by a wide margin.</Prose>
    <Prose>Set x = u². Then dx = 2u du and x⁻¹ᐟ² dx = 2 du for u positive; the transformed integrand has the continuous extension 2 at u = 0. The integral is simply two. This is a change of variables, not a numerical trick that invents the missing endpoint. Known discontinuities can be split explicitly; rapidly oscillating or singular integrals may need specialized algorithms. More subdivisions cannot replace a valid mathematical setup.</Prose>
    <Prose>Our complete program reports a work-limit failure explicitly. It does not replace a failed region with zero or return a partial sum as a completed integral. In SciPy, <Code>quad</Code> returns an estimated absolute error and exposes diagnostics; some full-output configurations return messages instead of displaying warnings. Read the return contract you actually use. QUADPACK's methods are more sophisticated than this teaching Simpson recursion, but an error estimate remains an estimate.</Prose>
    <H2>7. Choose nodes and compose error budgets</H2>
    <H3>Optional depth: two well-placed evaluations</H3>
    <Prose>If evaluation locations are free, equal spacing is not compulsory. Seek a symmetric rule for the integral of f on [−1,1] with two nodes ±c and equal weights w. Exactness for the constant function gives 2w = 2. Exactness for x² gives 2wc² = 2/3. Therefore w = 1 and c = 1/√3. Symmetry handles x and x³ automatically.</Prose>
    <MathBlock>{String.raw`\begin{gathered}c=1/\sqrt3,\\ \int_{-1}^1 f(x)\,dx\ \approx\ f(-c)+f(c).\end{gathered}`}</MathBlock>
    <Prose>This is the two-node Gauss–Legendre rule. Its nodes depend on the integration interval and weight, not on this particular integrand. On [a,b], put m = (a+b)/2 and d = (b−a)/2: evaluate at m ± d/√3 and multiply the sum by d. General Gaussian rules use a related orthogonal-polynomial construction. Here the small moment calculation explains why node placement matters. You cannot use it to request values that were never measured in a fixed dataset.</Prose>
    <NumericalExample example={numericalMethodsExamples.gaussian} />
    <H3>A volume approximation inside a time calculation</H3>
    <Prose>Return to the pump. Integrating r(t) = t exp(−t) gives V(T) = 1 − (1 + T)exp(−T). This exact primitive is our independent reference; suppose the computational pipeline normally supplies a numerical integral instead. We want V(T) = 0.8 litres. V(2) ≈ 0.593994 and V(4) ≈ 0.908422, so the root is in [2,4]. Since r is positive there, it is unique.</Prose>
    <Prose>Let Î(T) approximate V(T), with known integral error at most εᵢ under its stated model. If Î(T) − εᵢ exceeds the target, the true volume is above target. If Î(T) + εᵢ is below target, it is below. If the interval contains the target, the sign is unresolved: refine the integral or stop with that uncertainty. Choosing a half-bracket from the central estimate alone would discard the very guarantee that made bisection useful.</Prose>
    <Prose>There is also a useful error conversion. On [2,4], V′(t) = t exp(−t) is decreasing and at least m = 4exp(−4) ≈ 0.0732626 litres/minute. By the mean value theorem, changing time by ΔT changes volume by at least m|ΔT| as long as both times stay in this interval. If T* is the true crossing, then:</Prose>
    <MathBlock>{String.raw`\begin{gathered}|T-T^*|\leq\frac{|V(T)-0.8|}{m}\\ \leq\frac{|\widehat I(T)-0.8|+\varepsilon_I}{m}.\end{gathered}`}</MathBlock>
    <Prose>The numerator is litres and the denominator litres/minute, so the answer is minutes. For our trapezoid estimate on [0,T], r″(t) = (t−2)exp(−t) has absolute value at most 2. The discretization error is therefore at most T³/(6n²) with n equal panels. This is a mathematical quadrature bound, not a complete outward-rounded enclosure of every floating-point evaluation. Strict machine-certified bounds need that extra arithmetic treatment.</Prose>
    <CalibrationLab />
    <NumericalExample example={numericalMethodsExamples.calibration} />
    <Prose>The exact crossing is about 2.994308347 minutes. At T = 3, accurate quadrature can resolve that the target has been exceeded, but it cannot make three minutes equal the crossing time. The outer residual and the inner integral error both contribute. A flat cumulative curve would make the same volume uncertainty correspond to much greater time uncertainty.</Prose>
    <Prose>A complete search can now connect the pieces. Take the current bracket midpoint, integrate there, and first ask whether its conditional time-error bound already meets the goal. Otherwise retain a half only if the volume-error interval resolves the sign. If it does not, increase the number of integral panels. Exhausting that inner budget means “unresolved,” not permission to guess a sign. A sufficiently narrow retained bracket can also finish the search. The next program implements that control flow and reports both success and a deliberately insufficient budget.</Prose>
    <NumericalExample example={numericalMethodsExamples.nestedCalibration} />

    <H2>8. Transfer the error discipline</H2>
    <Prose>The same distinction between approximation, sensitivity and verification appears outside these three scalar operations. For Ax = b, use a factorization or an appropriate iterative solver rather than explicitly computing A⁻¹ just to multiply by b. Matrix Decompositions explains LU, QR, Cholesky and SVD. Conjugate gradients is suitable for symmetric positive-definite systems under its standard assumptions; a useful preconditioner improves the effective problem seen by the iteration. An algorithm's name is not a substitute for checking the matrix properties.</Prose>
    <Prose>Take A = diag(1,10⁻⁶), b = (1,10⁻⁶) and approximate x = (1,0). The true answer is (1,1). The residual has norm 10⁻⁶, but the solution error has norm one. That weakly scaled second direction explains the mismatch. Scaling, residual interpretation and backward/forward error are developed more fully in Conditioning, Stability & Numerical Analysis.</Prose>
    <Prose>Automatic differentiation applies derivative rules along the actual computation graph. It does not obtain derivatives by shrinking h, so it avoids this finite-difference truncation tradeoff. It still uses finite arithmetic and the chosen primitive/branch rules. At a nondifferentiable point, a framework may use a convention that is not the limit a symmetric finite difference appears to suggest. Use analytical identities, varied numerical checks and tested primitive contracts together.</Prose>
    <H3>A numerical update is a different evolution rule</H3>
    <Prose>For cooling x′ = −kx with x(0) = 1, the exact state at time t is exp(−kt). Forward Euler approximates one short change by current slope × h: x_next = (1−kh)x. After n steps it gives (1−kh)ⁿ. At a fixed horizon T = nh, compare this with exp(−kT), rather than compare runs after the same number of different-sized steps.</Prose>
    <Prose>The exact one-step multiplier is exp(−kh) = 1−kh + k²h²/2 + …. Thus the one-step defect is of order h² for this smooth problem. There are about T/h steps, and propagated defects accumulate; a first-order global error over a fixed interval is compatible with that second-order local defect. The propagation itself matters: |1−kh| greater than one makes Euler amplify the cooling state even though the real system decays. At kh = 2 it alternates without decay.</Prose>
    <Prose>The Dynamics lesson's oscillator makes a complementary point. Forward Euler on q′ = p, p′ = −q multiplies q²+p² by 1+h². Kick-then-drift symplectic Euler preserves q²+p²−hqp, a different positive quadratic when |h| &lt; 2. Neither statement says that physical energy is numerically exact at every time. Accuracy order, stability and geometric fidelity answer different questions.</Prose>
    <Prose>For the following optional library comparison, install NumPy and SciPy in your own environment with <Code>python -m pip install numpy scipy</Code>. The programs were checked with Python 3.12.14 and SciPy 1.18.1. Version-dependent last digits or diagnostics should be checked in the environment you actually use.</Prose>
    <NumericalExample example={numericalMethodsExamples.transfer} />
    <Prose>When using <Code>solve_ivp</Code>, requested local error tolerances do not universally bound the final trajectory error. <Code>t_eval</Code> selects reported times, while the solver can take a different sequence of internal steps. Event detection can miss multiple zero crossings within one accepted step. The existing ODE and Numerical PDE owners develop full time integration, stiffness, grids and boundary conditions; these bridges explain why their assumptions matter.</Prose>

    <H2>9. Practise with changed problems</H2>
    <Prose>Close the worked examples first. Write a prediction and its assumptions, then check the hint only if needed. For coding tasks, record the input, actual output and failure behavior. Return later with a different scale or function; knowing a method includes recognizing when its result is uninformative.</Prose>
    <Practice question="A continuous function has opposite signs on [−1,3]. How many interval halvings ensure the current midpoint is within 0.01 of some root? Does the same argument work for 1/x on [−1,3]?" hint="The midpoint radius is the initial width divided by 2^(k+1). Continuity is a separate premise."><Prose>The radius is 2/2ᵏ. Seven halvings leave 0.015625, which is too large; eight leave 0.0078125, which meets the target. For 1/x the function is discontinuous and undefined at zero; the opposite signs do not establish a root. If the function were continuous with several roots, the bracket argument would establish proximity to a root in that interval, not a selected root elsewhere.</Prose></Practice>
    <Practice question="For f(x)=(x−2)², start Newton at x=6. Give three updates and the error pattern. Why is a sign bracket [0,4] unavailable?" hint="Simplify x−(x−2)²/[2(x−2)] before substituting values."><Prose>The update is (x+2)/2 away from x=2, giving 4, 3, 2.5. Errors 4, 2, 1, 0.5 halve. The root is multiple and f′(2)=0, so the simple-root quadratic result does not apply. Both endpoint values are 4; absence of opposite signs does not rule out the touching root.</Prose></Practice>
    <Practice question="Derive the one-sided first-derivative weights at x, x+h and x+2h. Then check it on f(x)=x³ at x=1 with h=1/2 and h=1/4." hint="For weights a,b,c divided by h, require a+b+c=0, b+2c=1 and b+4c=0."><Prose>Subtracting the last two equations gives c=−1/2, then b=2 and a=−3/2. The weighted cubic gives 3−2h², hence 5/2 and 23/8, with signed errors −1/2 and −1/8. Halving h divides this truncation error by four. The complete Fraction program earlier executes this changed-function check exactly; it does not rely on subtracting rounded decimal references.</Prose></Practice>
    <Practice question="For a central difference, suppose |f'''|≤6 and each sample has error at most 10⁻⁶. Compare the model bounds at h=0.1, 0.01 and 0.001. What does adding a huge constant to f change?" hint="The bound becomes h²+10⁻⁶/h. A constant leaves the derivative unchanged but can change the magnitude of floating-point evaluation error."><Prose>The three bounds are 0.01001, 0.0002 and 0.001001. Among these choices, 0.01 has the smallest bound. This is a sample-error model, not a prediction that the actual error equals the bound. A huge constant can enlarge rounding in the sampled values; the fixed η assumption must then be reconsidered. At a true derivative of zero, report absolute error rather than divide by zero to form relative error.</Prose></Practice>
    <Practice question="A sensor records times [0,1,3] seconds and rates [1,3,1] units/second. Estimate the accumulated amount with piecewise-linear interpolation. Why is the equally spaced Simpson formula inappropriate?" hint="Compute each width separately before averaging its endpoint heights."><Prose>The first interval contributes 1×(1+3)/2=2 units. The second contributes 2×(3+1)/2=4 units. Total: 6 units. The sample positions are not equally spaced, so the classic equal-spacing Simpson weights do not describe the interpolant. Even 6 is an estimate based on the line-between-samples assumption; unobserved behavior could change the physical total.</Prose></Practice>
    <Practice question="An adaptive routine reports zero error on the blind polynomial. Scale that polynomial by 1000: what do its five initial samples and its true integral become? Then explain what increasing only the recursion limit achieves." hint="Scaling preserves the roots. If the initial estimator already accepts, a larger permitted recursion depth does not request any extra samples."><Prose>All five samples remain zero, and both Simpson estimates still vanish. The true integral becomes 625/177408 ≈ 0.00352295. Increasing only the recursion limit changes nothing in this path. You need information that changes sampling or supplies a valid function-class bound; a larger work allowance does not force exploration. The earlier exact-coefficient program can be changed by multiplying each final coefficient by 1000.</Prose></Practice>
    <Practice question="Use the two-node Gauss–Legendre rule on x⁴ over [−1,1]. Compute its signed error. What additional factor appears if you map a rule from [−1,1] to [a,b]?" hint="Each fourth power is 1/9. The substitution x=(a+b)/2+(b−a)u/2 also changes dx."><Prose>The estimate is 2/9, the exact integral is 2/5, and estimate minus exact is −8/45. Exactness through degree three does not extend to degree four. The general-interval sum needs the multiplicative factor (b−a)/2 as well as the relocated nodes; moving the nodes alone loses the units and interval length.</Prose></Practice>
    <Practice question="At T=3, suppose a volume estimate is 0.801 litres with error at most 0.0002 litres. The target is 0.8 and the minimum slope is 0.073 litres/minute. Is the sign resolved? Is time accuracy 0.01 minutes established?" hint="Check the whole volume interval, then divide residual plus volume error by the lower slope."><Prose>The volume interval [0.8008,0.8012] is entirely above 0.8, so the sign is resolved under the bound's assumptions. The time bound is (0.001+0.0002)/0.073 ≈ 0.01644 minutes, so it does not establish 0.01 minutes. The actual time error might be smaller; failing to certify a requested tolerance is not proof of exceeding it. Reducing the integral error alone leaves a residual contribution, so improving the candidate can be necessary.</Prose></Practice>
    <Practice question="Write a changed calibration run for target 0.7 litres, candidate 2.44 minutes and 32 trapezoid panels. Compute the signed residual, integral bound and conditional time bound. Then compare with the analytic crossing." hint="Reuse the three helper functions in the calibration program. Replace the target/candidate and use the same derivative lower bound on [2,4]."><NumericalExample example={numericalMethodsExamples.changedCalibration} /></Practice>
    <Practice question="For cooling x'=−2x at a fixed time of three seconds, compare Euler with h=1 and h=1/2. Explain why this does not contradict its local second-order defect." hint="There are three and six steps respectively. Use (1−2h)^n and compare with exp(−6)."><Prose>With h=1, the multiplier is −1 and the three-step result is −1. With h=1/2, the first update is zero and the final result remains zero. The exact state is exp(−6) ≈ 0.00247875. A local asymptotic error expansion describes sufficiently small h; it does not promise useful accuracy at every large step. Over a fixed time interval many local errors propagate, so a global first-order result and a local second-order defect are compatible.</Prose></Practice>

    <H2>10. Next connections and learning resources</H2>
    <Prose>Before moving on, choose a new function and explain its target, units, method assumptions, error evidence and failure behavior without copying the example's justification. A useful numerical report includes all five. Refinement, independent calculations and known fixtures support confidence in a computation; none can silently certify every function or arithmetic range.</Prose>
    <Prose>The next topic in this module is <strong>Functional Analysis & RKHS</strong>. A derivative or integral is an operation on a function; choosing a function space and a norm makes questions about sensitivity and convergence precise. That is a natural mathematical extension. For more numerical practice, the related Conditioning, ODE and Numerical PDE topics deepen different branches; follow their stated readiness requirements when they are available.</Prose>
    <Sources alternatives={<ul>
      <li><a href="https://fncbook.com/python/newton/" target="_blank" rel="noreferrer">Driscoll & Braun: Newton's method</a> — an alternate written explanation of tangent iteration and local convergence. Read after the first root investigations.</li>
      <li><a href="https://fncbook.com/python/fd-converge/" target="_blank" rel="noreferrer">Fundamentals of Numerical Computation: finite-difference convergence</a> — Taylor expansions and cancellation with worked calculations, useful when your step sweep stops improving.</li>
      <li><a href="https://fncbook.com/python/adaptive/" target="_blank" rel="noreferrer">Fundamentals of Numerical Computation: adaptive integration</a> — another derivation of refinement and estimated error; compare its implementation choices with our explicit child-budget accounting.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-12-square-roots-newtons-method/" target="_blank" rel="noreferrer">MIT OCW, Srini Devadas: Square Roots, Newton's Method</a> — a video alternative for iterative refinement, with <a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/d16678659cb7c98d611f12e7c1f7298d_MIT6_006F11_lec12.pdf" target="_blank" rel="noreferrer">accompanying lecture notes</a>. High-precision arithmetic analysis is optional depth. The page and notes were reviewed; full video viewing is not claimed.</li>
    </ul>}>
      <li><a href="https://dlmf.nist.gov/3.4" target="_blank" rel="noreferrer">NIST DLMF: Numerical Differentiation</a> and <a href="https://dlmf.nist.gov/3.5" target="_blank" rel="noreferrer">Quadrature</a> — precise formulas and hypotheses for differentiation, Newton–Cotes and Gaussian rules.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.brentq.html" target="_blank" rel="noreferrer">SciPy brentq</a> and <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.newton.html" target="_blank" rel="noreferrer">newton</a> — bracket requirements, tolerances, stopping criteria and result status.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.quad.html" target="_blank" rel="noreferrer">SciPy quad</a> and <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.simpson.html" target="_blank" rel="noreferrer">simpson</a> — callable versus sampled integration, estimated errors and diagnostic details.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.solve_ivp.html" target="_blank" rel="noreferrer">SciPy solve_ivp</a> — time-step control, output times and event-detection qualifications for the ODE bridge.</li>
    </Sources>
  </div>
};
