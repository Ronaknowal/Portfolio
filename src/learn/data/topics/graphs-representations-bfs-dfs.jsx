import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { GraphRepresentationLab, GraphSearchLab, GridWavefrontLab, DirectedCycleFigure } from '../../components/lesson-labs/GraphTraversalLabs';
import GraphCloneFigure from '../../components/lesson-labs/GraphCloneFigure.jsx';
import { graphTraversalExamples } from '../graph-traversal-examples.js';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import graphPractice from '../practice/graphs-representations-bfs-dfs.js';
import GraphTraversalLibraryBridge from '../../components/lesson-labs/GraphTraversalLibraryBridge.jsx';

export default {
  title: 'Graphs: Representations, BFS & DFS',
  readTime: '~60 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot graph-traversal-lesson">
    <LessonIntro prerequisites="Python lists, dictionaries, sets and references; queues and stacks from the earlier DSA lessons. Trees introduced branching and traversal. Set/edge notation and the needed correctness arguments are explained locally; the formal Sets, Logic, Relations & Proof Techniques topic supplies later depth."
      sections={[["1-when-relationships-reconnect", "Model the graph"],["2-one-graph-several-representations", "Choose storage"],["3-bfs-explore-in-increasing-distance", "BFS and shortest routes"],["4-dfs-keep-track-of-unfinished-work", "DFS entry and finish"],["5-explore-all-components-not-just-one-start", "Disconnected regions"],["6-the-graph-can-be-hidden-in-a-grid", "Implicit and multi-source search"],["7-the-answer-determines-the-state", "Identity and richer constraints"],["8-practise-a-changed-contract", "Independent practice"],["guided-dsa-practice", "Guided LeetCode practice"]]}>
      Turn a relationship into vertices and edges, expose its neighbors, and explore it without looping forever or losing the result you need. Learn why a queue gives shortest equal-cost routes, what a depth-first frame remembers, and how the same mechanisms apply to images, grids and shared objects.
    </LessonIntro>

    <H2>1. When relationships reconnect</H2>
    <Prose>In a rooted tree, each non-root node has one parent. Real relationships often reconnect: two pages link to the same page, roads form loops, or several tasks use the same dependency. A <strong>graph</strong> represents these relationships as <strong>vertices</strong>, also called nodes, and <strong>edges</strong> connecting them. A vertex can have several incoming relationships, and following edges may return to where you started.</Prose>
    <Prose>Write G = (V, E) to mean “the graph has vertex set V and edge set E.” When discussing costs, we will use n for the number of vertices and m for the number of edges. A label such as A identifies a vertex in our examples; its position on the screen is just layout. Crossing lines do not create another vertex unless one is explicitly drawn there.</Prose>
    <Prose>An <strong>undirected</strong> edge A—B permits traversal both ways. A <strong>directed</strong> edge A→B permits the stated direction; it does not imply B→A. A weight adds a cost, duration or other numeric quantity to an edge. The core algorithms here count edges, so every permitted move has equal cost. Choosing what one vertex, one edge and one unit of distance mean is the first algorithmic decision.</Prose>
    <LessonTable caption="Vocabulary tied to an actual question" headers={["Term", "Meaning here", "Question it helps answer"]} rows={[
      ["Adjacent / neighbor", "Joined by an edge; for directed traversal, an outgoing edge", "Where can the next step go?"],
      ["Walk / path", "A walk may repeat vertices; here a path has no repeated vertices", "Can we connect these endpoints without useless loops?"],
      ["Reachable", "There is an allowed route from the source, including zero steps to itself", "Can this vertex be found from this start?"],
      ["Cycle", "A nonempty closed route using distinct edges, with no repeated internal vertices in the simple-cycle case", "Can following these relationships return to earlier work?"],
      ["Connected component", "A maximal mutually connected region of an undirected graph", "How many separate regions exist?"],
      ["Self-loop / parallel edges", "An edge to the same vertex / multiple edges joining the same pair", "Does the data model preserve or collapse these relationships?"],
    ]} />
    <Prose>Walking out and back on the same undirected edge is not a cycle under this convention: it reuses that edge. We will use eight vertices A–H with edges A—B, A—C, B—D, C—D, D—E and F—G. The first four edges form a diamond with a cycle. F and G form a second region; H is isolated. This intentionally contains more than a tree: there are two routes from A to D and vertices that a search from A cannot reach.</Prose>

    <H2>2. One graph, several representations</H2>
    <Prose>An <strong>edge list</strong> stores pairs such as (A, B). An <strong>adjacency list</strong> stores a neighbor collection for each vertex: A has [B, C]. An <strong>adjacency matrix</strong> stores one entry per ordered pair: row A, column B contains 1 when that edge exists. These are different views of the same relation, with different costs for answering “Who are A's neighbors?”</Prose>
    <GraphRepresentationLab />
    <Prose>Toggle direction and inspect the same edge in every view. An undirected off-diagonal edge appears in two adjacency lists and in two symmetric matrix cells. A directed edge appears only in its source's outgoing list and corresponding matrix cell unless the reverse edge is separately present. H must retain an empty list and a zero row, even though it never occurs in the edge list.</Prose>
    <RunnableExample example={graphTraversalExamples.representations}><Prose>The builder declares all vertices before reading edges, rejects unknown endpoints and duplicate vertex labels, and collapses repeated edges. An undirected self-loop appears once in its own neighbor collection. That membership count is not the conventional graph-theoretic degree of a loop, which counts twice. The 0/1 matrix represents presence, so it cannot retain parallel-edge multiplicity or weights without another encoding.</Prose></RunnableExample>
    <Prose>The function preserves the first encounter order of neighbors. Our sample happens to give ascending neighbor lists; the browser sorts its small labels for repeatable traces. BFS/DFS outputs can change when neighbor order changes, even when reachability and shortest edge-count distances remain correct. Sorting neighbors is extra work, not a hidden part of the O(n + m) traversal bound.</Prose>
    <LessonTable caption="Choose storage for the operations you actually perform" headers={["Representation", "Storage", "Enumerate one vertex's neighbors", "Test one edge"]} rows={[
      ["Edge list plus vertex collection", "O(n + m)", "O(m) scan unless an index is added", "O(m) scan"],
      ["Adjacency lists", "O(n + m)", "O(number of neighbors)", "O(number of neighbors) for a plain list"],
      ["Adjacency sets", "O(n + m), with set overhead", "O(number of neighbors)", "Expected O(1) under hash-table assumptions"],
      ["Dense adjacency matrix", "O(n²)", "O(n) to scan a row", "O(1) with known indices"],
    ]} />
    <Prose>A sparse graph has relatively few edges compared with n², so adjacency lists avoid many absent matrix entries. A matrix can be reasonable for dense graphs or frequent constant-time edge tests. An implicit graph generates neighbors on demand, as a grid will below. It avoids storing all edges, but the input, search state and neighbor computation still have costs.</Prose>
    <Checkpoint prompt="If you build vertices only from the endpoints in the six sample edges, what answer becomes wrong before the search even starts?">
      <Prose>H disappears, so you report only two components instead of three. Traversal cannot recover a vertex omitted by the representation. Keep the declared vertex set, including isolated vertices; a correct algorithm on the wrong model answers the wrong question.</Prose>
    </Checkpoint>

    <H2>3. BFS: explore in increasing distance</H2>
    <H3>Separate discovery, pending work and the answer</H3>
    <Prose>Start at A. Vertices one edge away are B and C. A vertex two edges away is D; E is three edges away. <strong>Breadth-first search</strong>, or BFS, explores these increasing-distance layers. A FIFO queue holds discovered vertices whose neighbors still need inspection. The distance map records which vertices have already been discovered and their shortest known edge count; the parent map records how they were first reached.</Prose>
    <Prose>Initialize A with distance 0 and enqueue it. Remove A from the front; discover B then C at distance 1 and enqueue them. The queue is [B, C]. Remove B; A is already discovered, so skip it, then discover D at distance 2. The queue is [C, D]. Remove C; D is already discovered, so it must not be enqueued again. The diamond makes this timing decision visible.</Prose>
    <GraphSearchLab />
    <Prose><strong>Mark a neighbor when it is enqueued.</strong> If you wait until removing it, both B and C can enqueue D before either copy is processed. More elaborate implementations can manage duplicate candidates, but that changes the invariant and work bound. This implementation reserves each vertex at discovery, once, and fixes its parent then. A processed vertex and a queued vertex are both discovered; “visited” alone can obscure that difference.</Prose>
    <RunnableExample example={graphTraversalExamples.breadthFirst}><Prose>The sample returns one route A→B→D→E with three edges. A→C→D→E is equally short; the B route wins only because of neighbor order. The route to A is [A], zero edges. H has no parent entry, so path_to returns None. This helper also returns None for a target absent from its discovery map; validate membership in the graph separately if an unknown target must be distinguished from a known but unreachable one. An unknown source raises KeyError.</Prose></RunnableExample>
    <H3>Why the first discovery gives a shortest route</H3>
    <Prose>The queue processes vertices in nondecreasing distance. Initially it contains only distance 0. Processing a distance-d vertex appends only newly discovered vertices at distance d + 1 behind the already queued work. Thus every distance-d vertex is processed before any later layer could discover a shorter route through it. If a newly discovered vertex had a route shorter than d + 1, the predecessor on that shorter route would have been processed earlier and would already have discovered it. That contradiction proves the distance is minimal.</Prose>
    <Prose>Each discovered vertex gets one parent edge from a vertex one layer nearer the source. Following parents therefore decreases distance by one, cannot cycle, and eventually reaches the source. The parent edges form a <strong>search tree</strong> inside the original reachable region. They do not say the original graph was a tree or that its other edges vanished. Path reconstruction reverses the backwards chain and costs time proportional to the number of returned vertices.</Prose>
    <Prose>Every vertex enters the queue at most once, and each processed adjacency list is scanned once. A complete adjacency-list traversal costs O(n + m) time and O(n) additional state, excluding the stored graph. For just one reachable region, only its reached vertices and outgoing adjacency entries need processing; the displayed all-vertex distance table adds a scan of the declared vertices. With a matrix, scanning n positions per reached row can instead cost O(n²). Use deque.popleft rather than list.pop(0), which shifts the remaining list.</Prose>
    <Checkpoint prompt="A→B costs 10, while A→C and C→B each cost 1. Does ordinary BFS find the cheapest route to B?">
      <Prose>No. BFS correctly finds the route with the fewest edges: A→B uses one. The cheapest total cost is A→C→B, cost 2. The queue's layer argument assumes equal edge costs. Weighted algorithms require a different update/finalization argument; swapping the queue for a heap without defining that argument is not enough.</Prose>
    </Checkpoint>

    <H2>4. DFS: keep track of unfinished work</H2>
    <Prose><strong>Depth-first search</strong>, or DFS, follows one newly discovered branch as far as it can, then returns to unfinished neighbor choices. At A, choose B. At B, choose D. At D, skip discovered B and choose C. At C, both A and D are already discovered, so finish C and return to D, which still has E to inspect. The order of <em>finishing</em> work is different from the order of entering vertices.</Prose>
    <Prose>Switch the preceding exploration to DFS and inspect the frames. A frame stores a vertex and the next neighbor it still needs to inspect. The top frame owns the current work; frames below it are suspended. When no neighbor remains, pop that frame and record a finish event. The explicit stack performs the same job as recursive calls while making the suspended neighbor iterator visible.</Prose>
    <RunnableExample example={graphTraversalExamples.depthFirst}><Prose>The entry order is A, B, D, C, E. The finish order is C, E, D, B, A. In this DFS parent tree, C's parent is D, so the tree route from A to C takes three edges even though A—C is a direct edge in the original graph. DFS finds a route and reachability; it does not generally minimize edge count.</Prose></RunnableExample>
    <H3>Why a frame is more than a vertex waiting in a pile</H3>
    <Prose>When a child frame finishes, its parent's iterator continues where it stopped. Marking a vertex on entry prevents a cycle from creating another frame for the same vertex. At every point, the stack is the current ancestor chain of this depth-first exploration. A frame finishes only after all its neighbors have been examined and every newly entered child has finished.</Prose>
    <Prose>A simpler iterative reachability routine can push many neighbors at once and still find the reachable set. However, marking all siblings immediately can produce a different parent tree from recursive DFS. Pending siblings are not automatically active ancestors. If the task depends on finish times or detecting back edges to the current recursion path, use actual frames or another carefully justified entry/exit scheme.</Prose>
    <Prose>DFS also scans each reached adjacency list once, so its full time is O(n + m). The discovered map and explicit stack can each need O(n) space; a long chain can put n frames on the stack. Recursive Python code additionally encounters an interpreter recursion limit. Iterative code avoids that call-depth limit, but not the need to store unfinished work. BFS can also require O(n) queued state in a broad layer—neither is universally the smaller-memory choice.</Prose>
    <Checkpoint prompt="Can BFS and DFS return different orders while both being correct? Which properties should your tests compare instead of one arbitrary order?">
      <Prose>Yes. Compare the reachable set, valid parent edges, discovery-at-most-once and termination. For BFS, independently compare shortest distances; for a specified DFS neighbor order, compare entry/finish behavior. If a problem accepts any valid route or ordering, a different valid answer is not automatically a bug.</Prose>
    </Checkpoint>

    <H2>5. Explore all components, not just one start</H2>
    <Prose>Starting at A cannot visit F, G or H. To cover an entire undirected graph, scan its vertices and start another traversal whenever the next vertex is still undiscovered. Each such start begins a new connected component. Mark that entire region with one component ID before continuing the outer scan. The IDs name groups; their numeric order is not a property of the graph.</Prose>
    <RunnableExample example={graphTraversalExamples.components}><Prose>The sample produces {"{A, B, C, D, E}"}, {"{F, G}"} and {"{H}"}. Every vertex receives exactly one label. After this preprocessing, two vertices are connected exactly when their component labels match. Across all starts, each vertex and adjacency entry is still processed once, so the total remains O(n + m), not n separate full traversals. An empty graph has zero components.</Prose></RunnableExample>
    <Prose>For directed graphs, ordinary reachability is not symmetric. If A→B exists without B→A, B is reachable from A but A is not reachable from B. Running this undirected component-labeling routine on outgoing directed edges does not compute strongly connected components. A <strong>strongly connected component</strong> requires mutual reachability; a weak component ignores direction. State which question is intended before applying a component algorithm.</Prose>
    <Prose>For a graph that changes by adding edges and asks many “Are these vertices connected?” queries, recomputing all components after each addition can be wasteful. The next module topic, <a href="/learn/topic/disjoint-sets-union-find">Disjoint Sets & Union-Find</a>, maintains merging groups. It answers a different set of questions from recovering a BFS route or directed reachability.</Prose>

    <GraphTraversalLibraryBridge />
    <H2>6. The graph can be hidden in a grid</H2>
    <H3>Vertices are cells; edges are permitted moves</H3>
    <Prose>A maze does not need a separately allocated graph node for every square. Let a vertex be an open coordinate (row, column), and let neighbors be open side-adjacent coordinates inside the grid. Four candidate coordinate changes generate the edges when needed. A wall removes a vertex from the traversable region. This model permits up, left, right and down; diagonal contact does not connect cells.</Prose>
    <GridWavefrontLab />
    <Prose>Place or remove a wall and rerun the traversal to inspect which distances change. A detour can lengthen a route or make a cell unreachable. The numbers count moves, not straight-line distance: source (0, 0) has distance 0. Our example's target (4, 4) has shortest distance 8. Many equally short routes can exist; the parent map retains one under the declared neighbor order.</Prose>
    <RunnableExample example={graphTraversalExamples.gridWavefront}><Prose>The grid is preserved. A distance of −1 means no distance was assigned; look at the original grid to distinguish a wall from an open but unreachable cell. The distance array and parent map require O(RC) state for R rows and C columns. Each cell generates at most four candidate moves, so full traversal is O(RC), including distance-array initialization. “Implicit” removes explicit edge storage, not search memory.</Prose></RunnableExample>
    <H3>Several sources can share one wavefront</H3>
    <Prose>Suppose you need each cell's distance to the nearest of several starting locations. Put <em>all</em> sources in the queue at distance 0 before processing any of them. The next layer contains cells one move from some source, then two moves from the nearest source, and so on. This <strong>multi-source BFS</strong> computes the minimum distance over sources in one traversal. A parent chain ends at the source that first reached that cell; a tie may be resolved either way.</Prose>
    <Prose>With sources (0, 0) and (4, 4), the lower-right region is reached from (4, 4) instead of waiting for the first source to cross the whole grid. Completing one source's traversal, then treating its distances as final while starting another, would give incorrect nearest-source distances. Multiple simultaneous starts are an initialization change; edge costs must still be equal.</Prose>
    <H3>Flood fill is a connected-component query with a visible result</H3>
    <Prose>An image editor's fill operation begins with a pixel's original color, follows side-neighbor pixels with that color, and changes the reached region. The color rule defines allowed vertices. A pixel of the same color across a gap or only diagonally touching need not belong to the same component. The example below deliberately leaves the lower-right 1 unchanged.</Prose>
    <RunnableExample example={graphTraversalExamples.floodFill}><Prose>This version copies each row, then uses recoloring as its discovery mark. It must return early when new_color equals old_color; otherwise recoloring would not distinguish discovered cells and a cycle could keep adding them. Copying rows makes the total time and output memory O(RC), even if the reached component is tiny. If modifying the caller's image is allowed, the copy can be omitted and traversal work can follow just the reached region. State that mutation contract explicitly.</Prose></RunnableExample>
    <details className="lesson-deeper"><summary>Deeper: a location may not be a complete search state</summary>
      <Prose>In a puzzle with a key and a locked door, reaching the same coordinate before and after collecting the key is not the same situation. A vertex may need to be (row, column, has_key). Otherwise, marking only the coordinate as discovered can discard a later route that unlocks the goal. With one binary key flag there are at most twice as many cell states; with k independent flags, combinations can grow as 2ᵏ. The search cost follows the expanded state graph, not just the picture's cell count.</Prose>
      <Prose>This is a useful modeling test: if two visits to the same proposed vertex allow different future actions, the state probably omits relevant information. Keep only information that changes future possibilities; adding the entire route to every state can create unnecessary or infinite distinctions. Backtracking and dynamic programming will revisit the same question of sufficient state.</Prose>
    </details>

    <H2>7. The answer determines the state</H2>
    <H3>Copying a graph must preserve shared identity</H3>
    <Prose>Suppose A points to both B and C, B also points to C, and C points back to A. Recursively making a fresh copy every time an edge is followed either duplicates the shared C or loops through the cycle. Instead keep a dictionary mapping each original node object to exactly one new node. Allocate and register a copy when the original is first discovered, before exploring its neighbors.</Prose>
    <GraphCloneFigure />
    <RunnableExample example={graphTraversalExamples.cloneGraph}><Prose>The checks establish three separate facts: the root is a new object, the two routes to C still share one copy, and the cycle returns to the new A rather than the original. The mapping keys are object identities, not their displayed values; different nodes can carry the same value. This code copies the reachable topology and node objects while reusing payload references; its labels are immutable strings. Deep-copying arbitrary mutable payloads requires an additional policy. An unreachable component is not copied from this single starting reference.</Prose></RunnableExample>
    <Prose>Each reachable original is allocated once and each outgoing adjacency entry is reproduced once, giving O(n + m) time and O(n) mapping/queue state for the reached subgraph, plus O(n + m) output. Storing the original-to-copy association before following an edge is the invariant that handles both sharing and cycles.</Prose>
    <H3>A directed revisit is not automatically a cycle</H3>
    <Prose>In a directed diamond A→B→D and A→C→D, both branches reach D, but there is no route back to an ancestor. A plain “seen D before” test falsely declares a cycle. The distinction is whether D is still <strong>active on the current DFS path</strong> or has already finished.</Prose>
    <DirectedCycleFigure />
    <Prose>Use three states: unseen, active and finished. An edge to an active vertex closes a directed cycle because the active stack already contains a directed route from that ancestor to the current vertex. An edge to a finished vertex alone proves no such return route. When a frame exhausts its neighbors, mark it finished and remove it from the active stack. Restart at unseen vertices so a disconnected cycle is also detected.</Prose>
    <details className="lesson-deeper"><summary>Complete directed-cycle program and the topological-order bridge</summary>
      <RunnableExample example={graphTraversalExamples.directedCycle}><Prose>The diamond returns False; adding D→A returns True. A directed self-loop is a cycle by itself. The outer scan and all frames take O(n + m) time and O(n) state. For an undirected graph, the edge back to the parent is expected; cycle detection needs a different parent-edge rule. In a multigraph, edge identities matter because a second parallel edge is not the same parent edge.</Prose></RunnableExample>
      <Prose>A directed acyclic graph, or DAG, has no directed cycle. Reversing a complete DFS finish order gives a topological order when there is no cycle: every edge goes from an earlier to a later vertex. An active-neighbor edge must cause rejection, not a plausible-looking output. For prerequisite scheduling, first define whether A→B means A must precede B or the reverse. A separate indegree-removal strategy also exists; fuller scheduling, weighted paths and spanning-tree objectives belong to <a href="/learn/topic/shortest-paths-spanning-trees-topological-ordering">Shortest Paths, Spanning Trees & Topological Ordering</a>.</Prose>
    </details>
    <details className="lesson-deeper"><summary>Deeper: can every constraint cross between two groups?</summary>
      <Prose>A bipartition divides vertices into two groups so every edge crosses between them. For example, an original seating model can connect pairs that must be assigned to different tables. Give a start group 0, give each undiscovered neighbor group 1, and alternate. Encountering an edge whose endpoints already have the same group is a contradiction. An odd cycle eventually requires its starting vertex to be in both groups; an even cycle can alternate consistently.</Prose>
      <RunnableExample example={graphTraversalExamples.bipartition}><Prose>The four-cycle succeeds; the triangle fails. Restarting across components matters because the first component may satisfy the rule while a later one does not. An isolated vertex can use either group. None indicates failure; an empty dictionary is a valid coloring of an empty graph, so do not confuse “empty result” with “no valid answer.” Each edge also needs a consistency check after discovery.</Prose></RunnableExample>
    </details>
    <details className="lesson-deeper"><summary>Deeper: one route, every route and a global visited set</summary>
      <Prose>A→B→D→E and A→C→D→E are two different routes in a DAG. If enumerating every route, a global “D was already visited” rule can erase the second answer. Keep the current path, extend it for one branch, record a copy at the destination, and restore it when returning. For graphs with cycles, simple-path enumeration needs path-local membership to avoid repeating vertices in the same route. That differs from reachability, where one discovery per vertex is enough.</Prose>
      <Prose>The number of routes can be exponential even in a DAG. Printing them cannot take only O(n + m) time when the output itself is larger. Memoizing a count for a DAG is another task again; it does not print the paths. The optional all-paths problem below belongs after this state/output distinction and the later backtracking lesson.</Prose>
    </details>

    <H2>8. Practise a changed contract</H2>
    <H3>Independent task A: diagonals count, and distance counts cells</H3>
    <Prose>A rectangular grid uses 0 for open and 1 for blocked. Travel from the top-left to the bottom-right using eight-direction moves, including diagonals. Diagonal movement is allowed even when the two touching side cells are blocked. Return the number of <em>cells</em> in a shortest route, or −1 if none exists. A one-cell open grid returns 1. Do not modify the grid.</Prose>
    <details><summary>Hint: change the model and the initial distance</summary><Prose>Generate all row/column offsets in {"{−1, 0, 1}"} except (0, 0). An allowed move still costs one, so BFS still works. Count the starting cell as 1 rather than using an edge distance of 0. Reject blocked endpoints first.</Prose></details>
    <details><summary>Complete solution with boundary cases</summary><RunnableExample example={graphTraversalExamples.diagonalPractice}><Prose>The 2×2 diagonal example returns 2 even though its side cells are blocked; a physical navigation system might forbid such corner crossing, but this contract allows it. The same discovery and shortest-layer proof applies after changing adjacency. Worst-case time and extra state are O(RC); at most eight constant-time candidate moves are tested per reached cell. Empty and blocked inputs return −1, and malformed rectangular/symbol inputs are explicitly checked.</Prose></RunnableExample></details>
    <H3>Independent task B: find a missing component</H3>
    <Prose>A program starts a BFS at the first vertex, calls its reached count “the graph size,” and reports the graph connected. Use the sample to disprove it. Repair the test for an undirected graph and define what your API means by connected for an empty graph.</Prose>
    <details><summary>Solution reasoning</summary><Prose>A reaches five of eight declared vertices, so it cannot establish whole-graph connectivity. For a nonempty undirected graph, compare the reached count with the declared vertex count, or compute all components and require one. Whether an empty graph is called connected varies by convention; state the chosen contract rather than indexing its nonexistent first vertex. The component function above consistently returns zero groups for empty input.</Prose></details>
    <H3>Independent task C: distinguish unfinished from finished</H3>
    <Prose>Trace DFS on directed edges A→B, A→C, B→D, C→D in ascending neighbor order. When C examines D, what is D's state? Add one edge that creates a cycle and explain the exact moment the active-state rule detects it.</Prose>
    <details><summary>Hint</summary><Prose>Follow the B branch fully, including D's finish, before returning to A and entering C. A second route into finished work is different from a return to unfinished work.</Prose></details>
    <details><summary>Solution reasoning</summary><Prose>D is finished before C examines it, so that edge does not establish a cycle. Adding D→A creates A→B→D→A. While D examines the new edge, A is still active at the bottom of the DFS stack, so the active-neighbor test succeeds. The complete directed-cycle program supplies both fixtures; changing a visited set to three labels only helps when those labels follow the actual frame lifetime.</Prose></details>
    <H3>Readiness check</H3>
    <Prose>Before choosing a traversal, state the vertices, legal edges, direction, distance units and required output. Then explain what discovery reserves, what the frontier still owes, and why the state is sufficient for the answer. Test isolated vertices, a cycle, a shared descendant, a blocked route and a source equal to its target. Finally solve a new graph-shaped task whose title does not mention BFS or DFS.</Prose>
    <Prose>Next in module order is <a href="/learn/topic/disjoint-sets-union-find">Disjoint Sets & Union-Find</a>. It will maintain which groups have merged as relationships are added. Keep the distinction between knowing two vertices are connected and recovering the actual route between them.</Prose>
    <DsaPractice practice={graphPractice} />
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-13-breadth-first-search-bfs/" target="_blank" rel="noreferrer">MIT OCW: Breadth-First Search</a> — lecture video and typed notes on representations, layers and parent paths. Useful after the BFS investigation; examples use older Python notation.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-14-depth-first-search-dfs-topological-sort/" target="_blank" rel="noreferrer">MIT OCW: Depth-First Search and Topological Sort</a> — video with notes for entry/finish, edge classification and the optional directed-cycle branch. Review frame state before the advanced applications.</li>
    </ul>}>
      <li><a href="https://algs4.cs.princeton.edu/41graph/" target="_blank" rel="noreferrer">Princeton: Undirected Graphs</a> — representations, paths, BFS/DFS and connected components; Java code and some terminology conventions differ from ours.</li>
      <li><a href="https://algs4.cs.princeton.edu/42digraph/" target="_blank" rel="noreferrer">Princeton: Directed Graphs</a> — directed reachability, cycle detection, topological and strongly connected structure.</li>
      <li><a href="https://algs4.cs.princeton.edu/44sp/" target="_blank" rel="noreferrer">Princeton: Shortest Paths</a> — a later reading on weighted edges, relaxation and algorithm assumptions; use after the equal-cost/weighted counterexample.</li>
    </Sources>
  </div>,
};
