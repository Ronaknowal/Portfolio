import { Callout, H2, H3, Prose, Code, CodeBlock } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { LessonIntro, LessonTable, Checkpoint, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { LloydLab, FeatureGeometryLab, SeedingLab, HierarchyLab, PaletteLab } from '../../components/lesson-labs/KMeansHierarchicalLabs.jsx';
import { FaithfulScatterFigure, FaithfulDendrogramFigure, MeanRepresentativeFigure, LinkageDefinitionFigure, ClusteringShapeFigure, ClusteringDiagnosticsFigure } from '../../components/lesson-labs/KMeansHierarchicalFigures.jsx';
import { clusteringExamples } from '../k-means-hierarchical-examples.js';

const headings = [
  '1. Decide what a useful group means',
  '2. Let points choose representatives',
  '3. Make the geometry match the question',
  '4. Derive the updates and choose the seeds',
  '5. Implement a complete fit with an honest stopping rule',
  '6. Build a hierarchy one merge at a time',
  '7. Calculate Ward cost and read the height correctly',
  '8. Use library fits without losing the contract',
  '9. Choose a resolution and evaluate the result',
  '10. Turn clusters into a color palette',
  '11. Scale the computation and route a search',
  '12. Recognize when another objective is needed',
  '13. Practise, diagnose and transfer',
  '14. Complete a reproducible clustering report'
];
const headingId = heading => heading.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');

function Program({ example, children }) {
  return <section><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></section>;
}

function Deeper({ title, children }) {
  return <details className="cluster-deeper"><summary>{title}</summary>{children}</details>;
}

function Practice({ title, question, hint, children }) {
  return <section className="cluster-practice"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Get a hint</summary><Prose>{hint}</Prose></details><details><summary>Show the explained solution</summary>{children}</details></section>;
}

const kMeansHierarchicalContent = {
  title: 'K-Means & Hierarchical Clustering',
  readTime: '~60 min first pass · ~95 min complete read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot clustering-lesson">
    <LessonIntro prerequisites={<>Review <a href="/learn/path/full-curriculum/k-nearest-neighbors-knn?module=classical-ml">K-Nearest Neighbors</a> for vectors, distances and fitting transformations on training data. We refresh means and squared distance here. The complete Python programs use NumPy arrays with rows as observations and columns as features; the mathematical deeper branches are optional on a first pass.</>} sections={headings.map(heading => [headingId(heading), heading.replace(/^\d+\. /, '')])}>
      Learn to build groups when you have measurements but no supplied group labels. You will move representatives through a point cloud, construct and cut a merge tree, fit a real geyser dataset, turn pixel colors into a palette, and decide what evidence supports a useful result. Each lab lets you change inputs, inspect the current result and follow the process step by step; the exercises change the numbers so you cannot copy the demonstration.
    </LessonIntro>
    <Prose><strong>Your first pass:</strong> read sections 1 through 9, trying their four labs and four programs as you meet them, then finish with the practice in 13. Section 10 adds the fifth lab and a color-palette application; section 14 supplies a complete held-out report. Return to these applications when you are ready to transfer the methods. Sections 11 and 12 are deeper branches about scale and alternative objectives. You should finish able to trace an assignment and mean update by hand, read a dendrogram height under its convention, explain why a real dataset splits where it does, and say what a low error does and does not tell you.</Prose>

    <H2>{headings[0]}</H2>
    <Prose>Imagine advising a visitor who has just watched Old Faithful erupt: how long might they wait for the next eruption? The R <Code>faithful</Code> dataset gives us 272 historical observations from this Yellowstone geyser. Each row records an eruption’s duration and the waiting time to the next eruption, both in minutes. There are no supplied “short type” or “long type” group labels. We will explore whether a small number of groups summarizes these paired measurements, then ask what else a waiting-time recommendation would require.</Prose>
    <FaithfulScatterFigure />
    <Prose>Two dense regions are visible, but a picture is not a decision. <strong>Clustering</strong> turns a chosen notion of similarity into groups. Before choosing an algorithm, fix three things: what one observation is, which measurements describe it, and what decision the groups will support. Here an observation is one eruption and the measurements are two durations in minutes. Our immediate task is to summarize their joint pattern; the visitor’s future-wait question is a separate test of whether that pattern helps. Change any of these choices and you have a different problem on the same file.</Prose>
    <Callout title="The one caution that covers the whole lesson">
      The methods here can partition pure noise as readily as observations from distinct regimes. They follow their fitting rules in the geometry you supply. Everything that follows separates two questions: did the fitting procedure satisfy its stated objective and stopping rules, and are the resulting groups useful for the decision? A low objective value is evidence about representation error; usefulness needs evidence matched to the intended action. Keep this distinction in mind when reading each result.
    </Callout>
    <LessonTable caption="Two ways to answer a grouping question" headers={['question', 'k-means', 'agglomerative hierarchical clustering']} rows={[
      ['What is built?', 'k representative vectors called centers or centroids, plus point assignments', 'A nested tree of merges, from individual points to one group'],
      ['What must be chosen?', 'Representation, k, initialization and fitting settings', 'Representation, distance/linkage and a rule for reading or cutting the tree'],
      ['Can a point change its group?', 'Yes, on the next assignment step', 'A completed merge is not undone in the same run'],
      ['How does a new row enter?', 'Transform it and assign it to its nearest fitted center', 'The fitted merge tree has no inherent new-row prediction rule']
    ]} />
    <Prose>The k in <strong>k-means</strong> counts representatives. In KNN it counted neighbors consulted for a prediction. K-means never looks at supplied class labels; it learns centers from the feature vectors alone. You still choose k. A hierarchy postpones the choice of a flat group count, but it does not remove the choices of distance, linkage and resolution.</Prose>
    <Prose>To calculate by hand we need something smaller than 272 rows. We will carry six constructed points through the center and merge investigations, then return to the geyser once the mechanisms are clear. P0 through P5 are observation IDs; a cluster label is a separate number.</Prose>
    <LessonTable caption="The six points used in the center and merge investigations" headers={['observation', 'feature 1', 'feature 2']} rows={[["P0", 1, 0], ["P1", 1.5, .5], ["P2", 3, 2], ["P3", 3.5, 2], ["P4", 7, 5], ["P5", 7.5, 5.5]]} />
    <Prose>We will ask two things of these points: which two centers represent them well, and what sequence of groups appears if we merge the closest pair again and again. Two inspection teams call for two representatives; exploring several resolutions calls for a hierarchy.</Prose>

    <H2>{headings[1]}</H2>
    <Prose>Start with two tentative centers at P0=(1,0) and P4=(7,5). For each point, calculate its squared distance to each center and choose the smaller. Squaring avoids a square root without changing which distance is smallest. P2=(3,2) has squared distances (3−1)²+(2−0)²=8 and (3−7)²+(2−5)²=25, so it chooses the first center.</Prose>
    <Prose>After every point has chosen, move each center to the coordinate-wise mean of its assigned points. The first four points choose the first center, whose new position is ((1+1.5+3+3.5)/4, (0+.5+2+2)/4)=(2.25, 1.125). The other two produce (7.25, 5.25). Now repeat: new centers may change which center is closest. This alternation is <strong>Lloyd’s algorithm</strong>.</Prose>
    <Prose>The quantity being reduced is the sum of squared distances from each observation to its assigned center: <strong>squared error</strong>, <strong>within-cluster sum of squares</strong> or <strong>inertia</strong>. The initial assignment costs 19.25. Moving to the two means reduces it to 7.6875. Keep this number in mind; it returns in section 7 by a completely different route.</Prose>
    <LloydLab />
    <Prose>Two things to notice in the lab. First, the pale boundary line is the set of points equally far from both centers. Assignment simply asks which side of that line a point is on, so labels can only change when a center moves and drags the line across a point. Second, a fixed point is reached when an assignment leaves every label unchanged; the objective then has nowhere to go. That is the algorithm finishing its job, and it is a different statement from “these are the right groups.”</Prose>
    <MeanRepresentativeFigure />
    <Prose>The mean is a representative vector, not necessarily an observed item. For values 1, 2, 9 the mean is 4 even though nobody supplied a 4. If the task requires selecting an existing photograph, document or machine as an exemplar, that constraint calls for a different representative, such as a medoid. Section 12 returns to that choice.</Prose>
    <Checkpoint prompt="With current centers at 0 and 6, where does the point 3 go? Would odd k solve this?">
      <Prose>It is equally distant from both. Specify a deterministic tie rule, such as choosing the lower-index center. Odd k does not prevent a nearest-center tie; that idea concerns binary voting ties in KNN. The label chosen at the boundary does not change this point’s squared error.</Prose>
    </Checkpoint>

    <H2>{headings[2]}</H2>
    <Prose>Distance adds differences across columns, so units matter before any center is learned. Suppose two customers differ by 1,000 dollars and 1 visit. Raw squared distance is 1,000,001. Writing the same money difference in thousands of dollars makes it 1²+1²=2. No customer changed; the numerical meaning of “near” did.</Prose>
    <Prose>The geyser shows the same effect on real data. Waiting times range over about 50 minutes and eruption durations over about 3.5 minutes. In raw units the waiting column has much greater overall spread, so the checked two-center fit splits almost entirely on waiting time. This does not mean it dominates every pair: two eruptions with equal waiting times differ only in duration. After standardizing both columns, each has equal variance in the distance calculation. The two partitions agree on most eruptions but not all: 4 of the 272 rows change side, and both centers move. The library program in section 8 prints both fits.</Prose>
    <Prose>Choose a meaningful scale for each feature, then express a difference relative to that scale. If a difference of 1,000 dollars should matter as much as one visit, divide dollars by 1,000 before using Euclidean distance. Alternatively, state weights explicitly. A nonnegative weight w_r multiplies the squared difference in column r. To implement that weight by transforming coordinates, multiply the column by √w_r, not by w_r.</Prose>
    <MathBlock>{'\\begin{gathered}d_w(x,y)^2=\\sum_{r=1}^{d}w_r(x_r-y_r)^2,\\\\ z_r=\\sqrt{w_r}\\,x_r.\\end{gathered}'}</MathBlock>
    <FeatureGeometryLab />
    <Prose>Converting every coordinate from meters to centimeters multiplies all squared distances by 10,000 and leaves nearest-center choices unchanged, provided the centers are converted too. Changing one column’s scale changes that column’s relative importance. To preserve an existing distance when x_r becomes a·x_r, divide its squared-distance weight by a².</Prose>
    <Prose><strong>Standardization</strong> subtracts a training mean and divides by a training standard deviation, making a one-standard-deviation difference equally costly in every column. It is a sensible default and a modeling choice: a nearly constant noisy column or a rare binary flag can gain large influence. Centering alone does not change pairwise Euclidean distances; scaling does. A constant feature carries no distance information and needs safe handling rather than division by zero.</Prose>
    <Prose>Missing values are not zeros. Nominal category IDs such as red=1, blue=2, green=3 do not supply meaningful Euclidean intervals. Counts may benefit from a justified transform when relative differences matter. Write these choices down before looking for attractive clusters.</Prose>
    <H3>Run the examples in your own environment</H3>
    <Prose>The visual labs run on this page. The Python blocks are complete programs with recorded outputs; they do not execute in the browser. In a terminal with Python 3.12 available, create a project folder, change into it and run <Code>python -m venv .venv</Code>. Use <Code>py -3.12</Code> on Windows or <Code>python3.12</Code> on macOS/Linux instead of <Code>python</Code> if that is your installation’s command.</Prose>
    <Prose>Activate the environment with <Code>.venv\Scripts\activate.bat</Code> in Windows Command Prompt, or <Code>source .venv/bin/activate</Code> in a macOS/Linux shell. Then install the versions used for the recorded results:</Prose>
    <CodeBlock language="bash">{'python -m pip install numpy==2.3.5 scipy==1.18.1 scikit-learn==1.9.1'}</CodeBlock>
    <Prose>Save the next complete Python block as <Code>clustering_scaling.py</Code> in that folder and run <Code>python clustering_scaling.py</Code>. Save each later block in its own descriptively named file; each contains its own imports and inputs. If activation is unavailable, invoke the environment’s Python directly: <Code>.venv\Scripts\python.exe</Code> on Windows or <Code>.venv/bin/python</Code> on macOS/Linux. Compare the printed result with the recorded output before changing a fixture.</Prose>
    <Program example={clusteringExamples.scaling}><Prose>Read the two comparisons separately: a pure change of units with compensating weights preserves a distance; a change of feature importance can change the partition. The held-out transformation uses scales learned on training rows only, so the distant final device still receives a label. A label is an assignment, not a certificate that the device resembles its group.</Prose></Program>

    <H2>{headings[3]}</H2>
    <H3>Write the two unknowns separately</H3>
    <Prose>Let X contain n row vectors x_i in d dimensions. Let μ_j be center j and a_i the index assigned to row i. We choose k centers and assignments to make the following sum small. The norm squared means “sum of squared coordinate differences,” and J is the total error.</Prose>
    <MathBlock>{'\\begin{gathered}J(a,\\mu)=\\sum_{i=1}^{n}\\|x_i-\\mu_{a_i}\\|^2,\\\\ a_i\\in\\{1,\\ldots,k\\}.\\end{gathered}'}</MathBlock>
    <Prose>For fixed centers, each row contributes its own term, and choosing the nearest center minimizes that term independently. For fixed assignments, each center influences only its own group, and the mean minimizes that group’s squared error. These two easy subproblems make alternating updates practical even though the joint problem is hard. Finding the global minimum is NP-hard even with k=2 when the dimension can grow, and even in the plane when k can grow. These are separate hardness results: they do not say that planar k=2 is hard. NP-hardness concerns general worst-case instances, not the impossibility of solving a small or structured instance exactly. Lloyd’s algorithm is a practical heuristic; the seed affects which candidate it finds.</Prose>
    <MathBlock>{'\\begin{gathered}a_i\\leftarrow\\operatorname*{argmin}_{j}\\|x_i-\\mu_j\\|^2,\\\\ \\mu_j\\leftarrow\\frac{1}{|C_j|}\\sum_{i\\in C_j}x_i\\quad (|C_j|>0).\\end{gathered}'}</MathBlock>
    <Prose>C_j is the set of observation indices currently assigned to center j. The mean requires a nonempty group. Ties and empty groups need explicit rules in code.</Prose>
    <Deeper title="Why the mean is optimal, and what weights change">
      <Prose>For a group C with mean m, write x−c=(x−m)+(m−c) and expand the square. Summing eliminates the cross term because Σ(x−m)=0. The remaining identity gives both the minimizer and exactly how much a misplaced center costs.</Prose>
      <MathBlock>{'\\begin{aligned}\\sum_{x\\in C}\\|x-c\\|^2&=\\sum_{x\\in C}\\|x-m\\|^2\\\\ &\\quad+|C|\\|m-c\\|^2.\\end{aligned}'}</MathBlock>
      <Prose>The second term is nonnegative and is zero exactly at c=m. No calculus is needed, though differentiating gives the same answer: the gradient in c is 2|C|c−2Σx. With nonnegative observation weights v_i and positive total group weight, replace the mean by Σv_ix_i/Σv_i and the objective by Σv_i‖x_i−μ_a_i‖². A weight of three has the same objective contribution as three identical copies of a row. Observation weights and feature weights solve different problems.</Prose>
      <Prose>For fixed nonnegative feature weights shared by all centers, the coordinate-wise weighted metric still has the ordinary observation mean as an optimum in each positively weighted coordinate. A zero-weight coordinate does not affect the objective and should not be read as a learned discriminating feature.</Prose>
    </Deeper>
    <H3>Initialization changes the route through the objective</H3>
    <Prose>A good first center does not make the second one obvious. Randomly chosen rows can fall in the same region. <strong>K-means++</strong> spreads the starting opportunities by giving each row a probability proportional to its current squared distance D_i² to its nearest selected center. First choose one row uniformly, then repeatedly sample from these probabilities and update the nearest-center distances.</Prose>
    <MathBlock>{'\\begin{gathered}P(\\text{next center}=x_i)=\\frac{D_i^2}{\\sum_r D_r^2},\\\\ D_i^2=\\min_{c\\in\\text{selected}}\\|x_i-c\\|^2.\\end{gathered}'}</MathBlock>
    <Prose>For one-dimensional rows 0, 2, 5 with 0 already selected, squared distances are 0, 4, 25. The next probabilities are 0, 4/29, 25/29. The farthest point is likely, but point 2 still has a chance. Choosing the farthest every time is a different rule. With P0 as the first center of our six-point fixture, the farthest row has less than half of the probability mass. Change the selected centers in the lab and watch how the remaining opportunities change.</Prose>
    <SeedingLab />
    <Prose>When every row already coincides with a selected center, all D_i² are zero and there is no distribution to sample from. Extra centers cannot reduce an error that is already zero. The lab exposes this stopping condition; the runnable implementation shows how to honor a requested k anyway by choosing an unused row, which can create a duplicate center and leave fewer than k occupied groups.</Prose>
    <Deeper title="What the k-means++ theorem does and does not guarantee">
      <Prose>For the original unweighted Euclidean D²-sampling algorithm, Arthur and Vassilvitskii prove E[J_seed]≤8(ln k+2)J*, where J* is the globally optimal k-center squared error and the expectation is over the random initialization. Non-increasing Lloyd updates cannot worsen that bound. It is a guarantee about expected objective value, not a promise that every run places one center in each visually intended group.</Prose>
      <Prose>The theorem is for vanilla D² sampling. Current scikit-learn KMeans uses a greedy k-means++ variant that considers several candidate centers per selection and keeps the best. Sharing the name does not transfer the theorem. Multiple initializations compare candidate fits by the same objective; they improve the odds without certifying the global optimum.</Prose>
    </Deeper>

    <H2>{headings[4]}</H2>
    <Prose>Every exact assignment and mean-update half-step is <em>non-increasing</em> in J. It may leave J unchanged because the best choice was already in place or a point is tied. Being bounded below by zero is not by itself a proof that an iterative algorithm stops: the positive sequence 1, 1/2, 1/4, … never reaches zero in finitely many terms.</Prose>
    <Prose>In exact arithmetic, the finite-data argument needs one more ingredient. Fix a deterministic tie rule and retain an empty center at its previous location. Each occupied center is then the mean of some subset; an empty center retains either one of those means or its initial location. Only finitely many such configurations exist. Whenever a nonempty center actually moves to its mean, the identity above gives a strict decrease in J, so those decreases cannot continue forever. Once the centers no longer move, deterministic reassignment also stops changing the labels. Floating-point implementations add an iteration limit and usually a tolerance.</Prose>
    <Prose>A finite finish need not be quick. Vattani constructed planar instances with growing k on which Lloyd’s algorithm needs 2^Ω(n) iterations. Smoothed analysis offers a more favorable, explicitly modeled result: for inputs in a bounded box perturbed independently by Gaussian noise with standard deviation σ, Arthur, Manthey and Röglin bound the expected running time by a polynomial in n and 1/σ. The dependence on 1/σ matters: arbitrarily tiny noise is not a fixed practical speed guarantee. This helps explain why worst-case constructions need not describe typical behavior, but it does not prove that any real measurement noise eliminates slow cases. Record observed iterations and keep a stopping limit.</Prose>
    <Prose>A stopped loop is not necessarily a fixed point. If an iteration limit interrupts the updates, reassigning points to the returned centers may change labels, and those centers may no longer equal the means of their returned groups. Report that status instead of printing “converged” because the loop ended. At an ordinary fixed point both conditional choices are optimal; ties and empty centers complicate stronger local-optimum statements, and there is no global-optimum guarantee.</Prose>
    <Prose>The rectangle in the center investigation makes the distinction concrete. Four points (0,0), (0,1), (3,0), (3,1) with k=2 can stabilize as a bottom and top group with centers (1.5,0) and (1.5,1) and total squared error 9. Left and right groups have centers (0,.5) and (3,.5) and error 1. Both are fixed points under their own centers. Stability alone does not identify the better one.</Prose>
    <Program example={clusteringExamples.lloyd}><Prose>Follow the central operations: a distance matrix, an argmin, a per-cluster mean, and a check that reassignment changes nothing. Basic input checks support the small examples; this is not a full numerical library for arbitrary coordinate magnitudes. The returned inertia is recalculated from the returned labels and centers. Look at the deliberately short run and the duplicate fixture as carefully as the favorable blob example: matching a library’s error on one dataset is useful verification, not proof of optimality.</Prose></Program>
    <Prose>The scratch program is for learning and small controlled checks. Production fitting needs optimized distance kernels, careful numerical behavior, stopping diagnostics and input validation. Do not replace a mature implementation with this one because the sample output agrees.</Prose>

    <H2>{headings[5]}</H2>
    <Prose>Instead of moving centers and allowing memberships to change, begin with six singleton groups and repeatedly join two groups. After the first merge there are five groups; after five merges there is one. The whole merge history is a <strong>dendrogram</strong>, a tree whose leaves are observations and whose internal nodes are merges. Agglomerative means building upward by joining; divisive methods build downward by splitting.</Prose>
    <Prose>To choose the next merge, define how far apart two existing groups are. Four common linkage rules use the same input points but summarize their cross-group relationships differently. Ordinary Euclidean distance has the coordinate unit; squared error has its square.</Prose>
    <LessonTable caption="What a candidate pair of groups is scored by" headers={['linkage', 'score for A and B', 'what to watch for']} rows={[
      ['Single', 'Smallest distance between one member of A and one of B', 'A chain of nearby points can join groups whose endpoints are far apart'],
      ['Complete', 'Largest cross-group pair distance', 'Requires all cross-pairs to be close; an extreme point can delay a merge'],
      ['Average', 'Mean of all |A|·|B| cross-pair distances', 'Uses every cross-pair; it is not generally the distance between the two means'],
      ['Ward', 'Increase Δ in total within-group squared error after merging', 'Balances center separation with group sizes; requires Euclidean geometry for this interpretation']
    ]} />
    <LinkageDefinitionFigure />
    <Prose>For our six points, Ward first joins P2 and P3. They are 0.5 apart; their new mean is (3.25, 2). P0/P1 and P4/P5 then form two more pairs. On this fixture all four linkages happen to produce the same tree shape, differing only in heights, because the three pairs are so much tighter than anything between them. The lab therefore offers a second configuration: a chain of six evenly spaced points plus a compact triple. Switch linkages and inspect which keep that chain together when their trees are cut to two groups. A sequence of close neighbors and a small maximum distance across a group are different requirements; the new fixture lets you investigate the consequence.</Prose>
    <Prose>Each tree leaf shows its observation ID above its current cluster number: P4 above 2 means observation P4 currently belongs to group 2. Group numbers only distinguish the current groups; they are neither quantities nor a ranking.</Prose>
    <HierarchyLab />
    <Prose><strong>Read the axes before the colors.</strong> In the scatter view, horizontal and vertical position are feature coordinates. In the dendrogram, vertical height is the merge score under its stated convention, and horizontal leaf placement is chosen only to draw the tree legibly. Rotating the two children of any branch changes the left-to-right order while preserving the hierarchy. Adjacent leaves are not necessarily each other’s nearest observations.</Prose>
    <Prose>A horizontal threshold includes merges at or below that height. Branches connected below the line form a cluster. Alternatively, apply exactly a chosen number of recorded merges to obtain a chosen count. With equal merge heights these are different policies: a threshold includes all tied merges together, while a merge-count prefix can stop partway through the tied operations.</Prose>
    <Prose>Our Ward tree has two successive merges at √.5≈.707107. Just below that height there are five groups; at that height both tied pairs are joined, leaving three. No horizontal threshold produces exactly four groups on this tree. A four-group merge prefix depends on the declared order of the tied merges, so reporting the cut convention is part of reporting the result.</Prose>
    <FaithfulDendrogramFigure />
    <Prose>A tree with dozens of leaves is read differently from a tree with six. Begin by looking for a tall empty stretch between consecutive merge heights: every cut inside that stretch gives the same partition. On this geyser subsample the final merge is about five times higher than the preceding one, making two groups a plausible resolution to inspect under this geometry. Lower gaps are smaller, so a smaller change in cut height can cross a merge. Membership still changes only when the threshold crosses an actual merge height; row identities remain useful when investigating a particular branch.</Prose>
    <Checkpoint prompt="If you rotate the left and right children of the final dendrogram node, do observations switch clusters at a fixed height?">
      <Prose>No. All ancestor relationships and merge heights remain the same. Only the drawing’s leaf order changes. A dendrogram is a record of the selected algorithm’s merges; it is not automatically a family tree, an evolutionary model or a causal explanation.</Prose>
    </Checkpoint>

    <H2>{headings[6]}</H2>
    <Prose>Ward asks a particularly useful question: how much extra squared error must we pay if A and B are represented by one mean instead of two? Let their sizes be a and b and their means m_A and m_B. The combined mean is (a m_A+b m_B)/(a+b). Moving A’s representative to that mean adds a‖m_A−m_AB‖²; moving B’s adds b‖m_B−m_AB‖². The scatter already inside each group appears on both sides of the comparison and cancels.</Prose>
    <MathBlock>{'\\begin{aligned}\\Delta&=\\operatorname{SSE}(A\\cup B)\\\\ &\\quad-\\operatorname{SSE}(A)-\\operatorname{SSE}(B),\\\\ &=\\frac{ab}{a+b}\\|m_A-m_B\\|^2.\\end{aligned}'}</MathBlock>
    <Prose>Two groups with the same center separation can have different Ward costs. Merging two singletons separated by 2 costs (1/2)·4=2. Merging two groups of 100 with that separation costs (10,000/200)·4=200. Ward penalizes giving many observations a worse representative; it is not nearest-centroid linkage.</Prose>
    <Callout title="A height convention that changes the numbers">
      SciPy’s Euclidean Ward linkage height is h=√(2Δ). Thus h²=2Δ and Δ=h²/2. The height has distance units; Δ has squared-distance units. For two singletons, h equals their Euclidean distance. For larger groups it generally does not equal the distance between their means. Murtagh and Legendre distinguish two software recurrences called Ward1 and Ward2: applying both to the same unsquared distance matrix can produce different trees. With matched tie handling, they agree on the Ward criterion and tree when Ward1 receives squared distances and Ward2 receives distances, with the corresponding square-root conversion of Ward1 heights. The input convention and height convention must be matched, not just the method’s name.
    </Callout>
    <LessonTable caption="Calculated Ward merges for the six-point fixture; ties use the stated stable merge order" headers={['new group', 'Δ squared error', 'h = √(2Δ)']} rows={[
      ['{P2,P3}', '.125', '.500000'],
      ['{P0,P1}', '.250', '.707107'],
      ['{P4,P5}', '.250', '.707107'],
      ['{P0,P1,P2,P3}', '7.0625', '3.758324'],
      ['all six points', '56.020833…', '10.585…']
    ]} />
    <Prose>Once the three pairs have formed, their internal squared errors sum to .125+.25+.25=.625. Merging the first two pairs adds 7.0625, giving 7.6875 for the two-group partition. Adding the final cost gives 63.708333… for the single group. Summing all Ward increments recovers the total one-group scatter, a telescoping identity that gives an independent check on any merge implementation.</Prose>
    <Callout title="Two routes, one number">
      The two-group Ward cut costs 7.6875. Lloyd’s algorithm in section 2 stopped at 7.6875. Both use within-group squared error, and here they arrive at the same partition {'{P0,P1,P2,P3}'} versus {'{P4,P5}'}. The shared objective does not force them to agree on other data. Lloyd can reassign individual points but depends on its starting centers; Ward permanently commits each greedy merge and needs a tie rule rather than a random start. Compare both membership and cost: different partitions can even have equal error. A useful hybrid initializes Lloyd at the means of a Ward cut. Subsequent exact Lloyd steps cannot increase that cut’s error and may improve it by moving points across branches, though a stable result still need not be globally optimal.
    </Callout>
    <Deeper title="Derive the size factor and connect the tree to pairwise geometry">
      <Prose>Because m_A−m_AB=b(m_A−m_B)/(a+b) and m_B−m_AB=−a(m_A−m_B)/(a+b), the two movement penalties sum to [ab²+ba²]/(a+b)² times ‖m_A−m_B‖². Cancelling gives ab/(a+b). The same parallel-axis identity proved the k-means mean update: the methods share an error measure while taking different routes through possible partitions.</Prose>
      <Prose>Greedily minimizing the next Ward increment does not minimize the total error for every eventual k. Earlier merges cannot be undone. Nor does Ward guarantee spherical or equal-cardinality groups; its score favors compact solutions in the chosen Euclidean geometry without enforcing those conditions.</Prose>
      <Prose>Single linkage has a different useful connection: at a distance threshold t, make a graph connecting observations whose distance is at most t. Its connected components are the single-linkage clusters at t. A minimum spanning tree preserves those threshold connections, so cutting its edges above t yields the same groups. This explains both efficient single-linkage constructions and the chaining effect: connectivity through a path does not require close endpoints.</Prose>
      <Prose>All four linkages taught here produce monotone trees: each merge is at least as high as the merges below it. Centroid and median linkage do not have this property. Merging two groups can move their new centroid closer to a third group than either was before, producing an <em>inversion</em> where a parent sits below its child and the height axis stops being readable as a threshold. The MIT lecture linked below uses centroid linkage in its example, so keep that caveat in mind when watching it.</Prose>
    </Deeper>
    <Program example={clusteringExamples.ward}><Prose>The scratch merge implementation computes each candidate cost from actual group sizes and means and compares the result to SciPy. The output distinguishes a linkage row’s children, height and merged observation count. Compare pair membership rather than raw label numbers, and inspect the at-most-k behavior of <Code>fcluster(..., criterion='maxclust')</Code> at tied heights.</Prose></Program>
    <Prose>A SciPy linkage input can be an n×d observation matrix or a one-dimensional <em>condensed</em> distance vector containing n(n−1)/2 distinct pair distances. A square n×n distance matrix is not silently equivalent: passed as observations, its rows would themselves become feature vectors. Use the documented conversion for precomputed distances. Ward must receive Euclidean geometry; already-squared distances are also not the correct input for its usual height convention.</Prose>

    <H2>{headings[7]}</H2>
    <Prose>Before running a library example, identify its input, learned state and prediction rule. KMeans fits centers and exposes training <Code>labels_</Code> and <Code>inertia_</Code>. <Code>predict</Code> assigns new transformed rows to centers. Cluster 0 is not “low,” cluster 1 is not “high,” and a large distance does not produce a rejected or noise label.</Prose>
    <Prose>The native examples below were executed with Python 3.12.14, NumPy 2.3.5, SciPy 1.18.1 and scikit-learn 1.9.1. Set <Code>random_state</Code>, initialization and <Code>n_init</Code> explicitly, and record versions rather than expecting identical defaults forever. Small floating-point differences and tied label numbering can still vary across environments.</Prose>
    <FaithfulScatterFigure grouped />
    <Program example={clusteringExamples.production}><Prose>The geyser data are embedded in the program so it runs offline. Read the standardized and raw-unit fits side by side: the same algorithm, the same k, and centers that differ because the geometry differs. Then read the k sweep with section 9 in mind. The blob comparison at the end is retained from the earlier lesson: three estimators agree perfectly on generated data, which tells you the data are easy, not that the estimators are interchangeable.</Prose></Program>
    <Prose>In the checked KMeans API, <Code>n_init='auto'</Code> uses one initialization for k-means++ and ten for random or callable initialization. Explicit repeated starts make the experimental choice clear. KMeans chooses its best fit among runs. MiniBatchKMeans uses its initialization count to choose a starting configuration before one mini-batch optimization run, which is a different contract. Read the parameter documentation rather than transferring the interpretation by name.</Prose>
    <Prose>Library stopping may use small center movement rather than exact assignment stabilization. The KMeans documentation explains that early stopping can leave centers inconsistent with the means of the final labels; it reassigns labels to agree with prediction on the returned centers. That is why the scratch example distinguishes a complete fixed point from a coherent but early-stopped return.</Prose>
    <Prose><Code>AgglomerativeClustering</Code> provides a flat partition for a selected cluster count or a distance threshold. For a complete inspectable tree, SciPy’s linkage matrix is convenient. Ward requires Euclidean distance. Single, complete and average can use other dissimilarities, but their results then answer those different distance questions. The agglomerative estimator has no <Code>predict</Code> method: attaching a new point to a nearest old center is an extra rule, not a reconstruction of the merge procedure.</Prose>

    <H2>{headings[8]}</H2>
    <Prose>More representatives can describe more detail. At the global optimum, allowing another center cannot increase the minimum squared error: the old solution can be kept while the extra center does no work, or a group can be split. With one center per distinct observation the error is zero. Minimizing training inertia across k therefore selects the largest k you try, unless something else is paying for complexity.</Prose>
    <ClusteringDiagnosticsFigure />
    <Prose>An <strong>elbow</strong> plot looks for a change in the rate of improvement. On a linear axis the large drop from one center to two compresses the later differences. The logarithmic axis above turns equal proportional improvements into equal vertical drops: the median recorded inertia falls from 544 to about 79.58 at k=2, then to about 56.83 at k=3, a further 29% reduction. Later decreases are smaller than the first drop but do not diminish uniformly. Separately fitted local solutions can even show a small increase as k grows because the optimizer found a worse candidate; retry before treating that as a property of the objective.</Prose>
    <Prose>The <strong>silhouette</strong> compares a point’s average distance to other members of its own group, a(i), with the smallest average distance to another group, b(i). For a nonsingleton group and a positive denominator, s(i)=(b(i)−a(i))/max(a(i),b(i)). A score near 1 means within-group neighbors are closer under this distance; near 0 means the averages are similar; a negative score means another group has the smaller average distance.</Prose>
    <MathBlock>{'\\begin{gathered}s(i)=\\frac{b(i)-a(i)}{\\max(a(i),b(i))},\\\\ -1\\le s(i)\\le1.\\end{gathered}'}</MathBlock>
    <Prose>For a hand calculation, put 0 and 1 in group A and 4 and 6 in group B. For the point 1, its within-group mean distance is a=|1−0|=1. Its average distance to B is b=(|1−4|+|1−6|)/2=4. Its silhouette is (4−1)/4=.75. Notice that b averages over the other group, not just its closest point or its center.</Prose>
    <Prose>The usual implementation convention gives a singleton silhouette of zero, and scikit-learn’s overall score requires between 2 and n−1 distinct labels, which is why the figure shows k=1 as undefined. A mean silhouette can hide one poor group, so inspect group sizes and individual values. Here the figure takes the median of five runs’ <em>mean</em> silhouette scores; it is not a median over individual eruptions. That summary peaks at k=2. The five starts also have the same recorded inertia there; at each tested k from 3 through 8 they include different objective values. Separation, initialization sensitivity and the subsample’s dendrogram gap are complementary diagnostics, but they reuse related observations and the same geometry. Their agreement is not independent evidence that the grouping serves the visitor’s decision.</Prose>
    <LessonTable caption="Different kinds of evidence answer different questions" headers={['evidence', 'what it can establish', 'what it cannot establish alone']} rows={[
      ['Inertia / held-out distortion', 'How well centers represent rows in this geometry', 'A best k without a complexity or utility decision'],
      ['Silhouette', 'Within-versus-between separation under the selected distance', 'Validity of a nonconvex, unequal-density or task-specific grouping'],
      ['Repeat seeds', 'Sensitivity to optimization initialization', 'Sensitivity to future populations or a flawed representation'],
      ['Resample rows / perturb features', 'Stability under a declared perturbation', 'Usefulness; a consistently irrelevant split can be stable'],
      ['Reference-label agreement', 'Agreement with that external partition, using label-invariant comparison', 'That its categories are the only useful target'],
      ['Domain and downstream checks', 'Whether the groups support the intended action', 'Causality or fairness from an appealing label']
    ]} />
    <Prose>Relabeling [0,0,1,1] as [7,7,3,3] leaves the partition unchanged. Comparing raw labels row by row would call every row different. Compare which pairs share a group, or use a permutation-invariant measure. The adjusted Rand index (ARI) accounts for chance agreement under its reference model; it is not an accuracy and may be negative. The later <a href="/learn/path/full-curriculum/clustering-evaluation-validation-silhouette-ari-nmi?module=classical-ml">Clustering Evaluation &amp; Validation</a> lesson develops these metrics and their assumptions.</Prose>
    <Prose>A sensible workflow chooses a small plausible range of k, compares repeated fits and profiles, checks sensitivity to justified representations, then freezes a decision before its evaluation. For future-row quantization, hold out rows and report average nearest-center squared error with the same training-owned transform. For an exploratory description of one fixed collection, fitting all rows can be appropriate; say so, rather than implying a generalization test was performed.</Prose>
    <Prose>Return to the visitor’s question. The two fitted profiles suggest candidate waits of roughly 55 and 80 minutes, and summarizing each group separately reduces this sample’s waiting-time scatter. But these groups were fitted using <em>both</em> eruption duration and the later waiting time. A visitor cannot supply that future wait to select a group. Treating the resulting within-group error as forecast accuracy would leak the answer into the prediction.</Prose>
    <Prose>To test a recommendation, set aside later eruptions before fitting. On development observations, define an assignment rule using only information available when the visitor asks—for example, two centers learned from eruption duration alone—and estimate a mean wait for each assigned group. Freeze that rule, then use each held-out eruption’s duration to choose its predicted wait. Compare held-out RMSE, in minutes, with a baseline that always predicts the development mean wait. The two-dimensional clustering here supplies a descriptive pattern and a hypothesis to test; it does not supply that forecast evaluation. The capstone below demonstrates the separate held-out protocol for a representation task.</Prose>
    <Deeper title="Gap statistics and why stability still needs a question">
      <Prose>The gap statistic compares observed within-cluster dispersion with dispersion from simulated reference datasets intended to have no cluster structure. One common form subtracts observed log dispersion from its expected log value under the reference. Its recommendation depends on the null generator, Monte Carlo variability and selection rule. A uniform reference in a bounding box is a modeling choice, not a universal definition of “no clusters.”</Prose>
      <Prose>For resampling stability, compare partitions on a common set of observations. A KMeans fit from each resample can predict a fixed probe set. A hierarchy needs an explicitly chosen comparison on shared sampled observations or another justified out-of-sample rule. Comparing unrelated bootstrap row positions compares different objects, even if both label vectors have the same length.</Prose>
    </Deeper>

    <H2>{headings[9]}</H2>
    <Prose>An image provides a concrete use for representatives. Treat each pixel’s three color channels as a vector x_i=(R,G,B). Fit k centers, then replace each pixel by its assigned center’s color. The result is <strong>vector quantization</strong>: representing many possible vectors with a finite codebook. Here “cluster label” becomes a palette index, and squared error becomes a measurable reconstruction cost.</Prose>
    <PaletteLab />
    <Prose>The three images are constructed in code so that their data, pixel counts and calculations are inspectable. Increasing k preserves more color detail but does not recover spatial boundaries: the clustering sees colors, not pixel coordinates. Two distant pixels with identical RGB values contribute identically. That is exactly right for a global color palette and insufficient for segmenting connected objects.</Prose>
    <Prose>Repeated colors offer an exact reduction. If a color occurs 40 times, store it once with observation weight 40. Weighted squared error over unique colors equals squared error over every original pixel for the same centers. Unweighted unique-color clustering would give a once-occurring color the same total influence as a frequently repeated color. In the sky image, pixel counts therefore matter alongside color separation: for equal per-pixel error, misrepresenting a small accent costs less than misrepresenting a large region. The gradient gives a useful comparison because every count is one, so weighting cannot change its objective.</Prose>
    <Program example={clusteringExamples.quantization}><Prose>Separate the floating-point centers from the byte-valued palette actually stored. Rounding channels changes both the reconstruction error and, after reassignment, the nearest palette choice. The output checks the equivalent weighted and repeated objectives and scores the stored reconstruction rather than a palette that was never written.</Prose></Program>
    <Prose>For N pixels in a raw 8-bit RGB representation, the color payload is 24N bits. A toy indexed representation with k≤256 byte RGB palette entries takes 24k palette bits plus N⌈log₂k⌉ index bits, ignoring dimensions, headers, padding and coding details. This is bookkeeping for a specified format, not a prediction of PNG or JPEG file sizes, which also exploit spatial structure.</Prose>
    <Prose>Squared distance in encoded sRGB channel values is a teaching objective, not a model of perceived color difference. A perceptually motivated color space changes the problem and has its own conversion details. Choose the metric for the visual task.</Prose>
    <Prose>This connection explains a historical name. Stuart Lloyd worked out the alternating assignment-and-mean procedure in 1957 as a Bell Labs technical note on least-squares quantization for pulse-code modulation, the digitization of telephone signals. It circulated privately for a quarter of a century and was finally published in 1982, by which time the same idea had been rediscovered several times under other names. Representatives need not be categories with human names; they can be codebook values that trade storage against distortion. The same center-assignment mechanism can also organize a search index, as the next section shows.</Prose>

    <H2>{headings[10]}</H2>
    <Prose>A direct dense Lloyd assignment compares n rows with k centers across d coordinates, costing O(nkd) work per iteration; mean accumulation adds O(nd). With I iterations and r complete starts, the main bound is O(rInkd), excluding initialization and input processing. Chunking distance calculations avoids materializing every row-center-coordinate difference at once.</Prose>
    <Prose>Keeping the dense input costs O(nd) storage, centers O(kd) and labels O(n). A broadcasted NumPy demonstration may additionally materialize O(nkd) differences, while a chunked production implementation avoids that temporary. Elkan-style acceleration uses triangle-inequality bounds to skip some distance computations at the cost of extra O(nk) memory; it changes the speed, not the objective.</Prose>
    <H3>Update representatives from small batches</H3>
    <Prose>Mini-batch k-means updates from a small subset rather than recomputing every full-data mean each step. The incremental-mean identity explains its basic update: if a center represents t assigned observations with mean m and a new assigned point is x, the combined mean is (tm+x)/(t+1)=m+(x−m)/(t+1). The new point’s influence shrinks as the count grows.</Prose>
    <MathBlock>{'m_{t+1}=m_t+\\frac{x-m_t}{t+1}.'}</MathBlock>
    <Prose>This running summary reflects assignments made while centers were changing; it is not the mean of a freshly reassigned full dataset, and individual mini-batch steps need not decrease full-data inertia. A count-based learning rate also adapts slowly to genuine drift; forgetting old data is a separate modeling decision.</Prose>
    <H3>A merge tree can be expensive before it is drawn</H3>
    <Prose>There are n(n−1)/2 distinct pair distances. For n=10,000, storing just those values as float64 needs 399,960,000 bytes, about 381.4 MiB, before data and algorithm overhead. At n=100,000 it is about 40 billion bytes. A small dendrogram view does not reduce the computation needed to build a full dense hierarchy.</Prose>
    <Prose>The scratch Ward implementation repeatedly considers active pairs for clarity. SciPy’s documented implementations for single, complete, average and Ward use O(n²) time for the clustering stage and O(n²) memory, with method-specific algorithms; computing dense high-dimensional input distances adds its own work. A sparse connectivity constraint reduces candidate merges but changes which merges are admissible, so it changes the problem as well as the resource use.</Prose>
    <Deeper title="BIRCH summaries preserve a useful identity, not every original partition">
      <Prose>BIRCH builds a compact tree of subcluster summaries. For a subcluster, store its count N, vector sum LS and sum of squared norms SS. Its mean is LS/N and its within-group squared error is SS−‖LS‖²/N. This follows by expanding every ‖x−mean‖²; the summary can be updated without retaining every row.</Prose>
      <Prose>If a later center c must represent that whole subcluster, its cost is the subcluster’s internal error plus N‖mean−c‖². Weighted clustering of subcluster means therefore reproduces the objective for that <em>restricted</em> assignment problem, up to a constant. It cannot recover a better original partition that would split a compressed subcluster. The checked scikit-learn BIRCH global-clustering path does not pass subcluster counts as sample weights, so do not attribute this exact weighted objective to that API without implementing it.</Prose>
      <Prose>The identity has a numerical caveat: SS−‖LS‖²/N subtracts similar large quantities when data have a large offset and tiny variance. Stable centered or incremental calculations matter in floating point.</Prose>
    </Deeper>
    <H3>Use a codebook to route a nearest-neighbor search</H3>
    <Prose>In an inverted-file vector index, first learn coarse centers. Assign each database vector to a center and store its ID in that center’s list. At query time, find a few close centers and search their lists. For a toy database split across four lists, probing one list avoids examining the other three. It can also miss the true nearest vector near a list boundary.</Prose>
    <Prose>The cluster assignment is now a <em>routing</em> decision. More lists or fewer probed lists change work and recall together. Faiss’s IVF indexes implement this family of ideas. Measure recall against exact search on representative held-out queries and record index size and latency under actual conditions; a low training inertia says nothing about retrieval quality.</Prose>

    <H2>{headings[11]}</H2>
    <Prose>Every point assigned to one of two distinct Euclidean centers is separated from the other center’s points by a straight equal-distance boundary, the line you watched in the first lab. With more centers, each nearest-center region is an intersection of half-spaces: a convex Voronoi cell. That geometry suits compact regions and cannot represent every intended shape.</Prose>
    <ClusteringShapeFigure />
    <Prose>A large, spread-out group may receive several centers while a small nearby group shares one. Unequal density, imbalance and outliers change squared-error contributions; none is diagnosed by k alone. Because the loss is squared, moving an outlier from distance 10 to distance 100 multiplies its contribution from 100 to 10,000, and k-means will still assign it to a center. A separate distance threshold needs its own data and validation before it becomes an anomaly detector.</Prose>
    <LessonTable caption="Change the objective when the needed notion of a group changes" headers={['need', 'candidate direction', 'qualification']} rows={[
      ['An observed representative under a chosen dissimilarity', 'k-medoids: minimize distances to selected observations', 'Less freedom than a mean; robustness and search cost depend on loss, data and algorithm'],
      ['Directions rather than vector magnitudes', 'Spherical k-means / normalized directional representations', 'Renormalizing centers is part of the directional objective; ordinary k-means on normalized rows is not identical'],
      ['Density-connected shapes and a noise label', 'DBSCAN / HDBSCAN', 'Density scale and sampling matter; these build different structures from a Ward tree'],
      ['Soft membership and ellipsoidal density components', 'Gaussian mixture model', 'Covariance, mixture weights and model checking matter; posterior responsibilities are not calibrated real-world categories'],
      ['Neighborhood or graph structure', 'Spectral or kernel-based clustering', 'The affinity/kernel defines the geometry; dense matrices and an unsuitable similarity create new problems'],
      ['A nested segmentation constrained by spatial adjacency', 'Connectivity-constrained agglomeration', 'Adjacency changes admissible merges; it does not establish semantic validity']
    ]} />
    <Deeper title="Cosine geometry, kernels and the small-variance mixture connection">
      <Prose>For unit vectors x and y, ‖x−y‖²=2−2xᵀy. Nearest <em>unit</em> centers therefore maximize cosine similarity. But the mean of normalized rows need not have unit length. Ordinary k-means retains that mean, whereas spherical k-means renormalizes a nonzero group sum and uses a directional assignment objective. Zero vectors or cancelling group sums need explicit treatment. Normalizing the input alone does not make those procedures equivalent.</Prose>
      <Prose>Kernel k-means uses the same mean-squared-error idea after an implicit mapping φ. For a group C, distance to its feature-space mean expands to K(x,x)−(2/|C|)Σ_y∈C K(x,y)+(1/|C|²)Σ_y,z∈C K(y,z). A valid positive-semidefinite kernel makes this an inner-product geometry. The formula can model curved input-space boundaries without constructing φ, but pairwise kernel storage and the kernel choice introduce real costs and assumptions.</Prose>
      <Prose>For an equal-weight Gaussian mixture with common covariance σ²I, responsibilities are proportional to exp(−‖x−μ_j‖²/(2σ²)). As σ² approaches zero, a uniquely nearest component receives limiting responsibility one, and the weighted mean update approaches the hard-assignment mean. Exact ties do not automatically choose one component; finite-variance EM remains soft, and unequal priors shift its comparisons. This limiting connection is useful, but ordinary k-means is not a Gaussian mixture fit with learned covariances.</Prose>
    </Deeper>
    <Prose>In a high-dimensional dataset, adding many weak or noisy coordinates dilutes meaningful distance contrasts, and redundant columns count the same variation repeatedly. The effect depends on the distribution; there is no universal dimension at which Euclidean distance stops working. Feature selection, justified weighting and dimensionality reduction can help when they preserve information relevant to the grouping question.</Prose>
    <Prose>For example, grouping patients by expression profiles requires a defensible measurement and normalization model, batch-effect checks and a decision about absolute levels versus profile shape. Five hundred patients require only 124,750 distinct pair distances, but computing them across many genes, processing the measurements and validating the result still take work. PCA does not guarantee that discarded low-variance directions were irrelevant, and UMAP or t-SNE islands are not independent confirmation of clusters. Any clinical claim requires validation beyond this lesson.</Prose>
    <Prose>The next module topic, <a href="/learn/path/full-curriculum/pca-dimensionality-reduction?module=classical-ml">PCA &amp; Dimensionality Reduction</a>, develops what a projection preserves and loses. Later <a href="/learn/path/full-curriculum/dbscan-density-based-clustering?module=classical-ml">DBSCAN &amp; Density-Based Clustering</a> and <a href="/learn/path/full-curriculum/gaussian-mixture-models-gmm-em-algorithm?module=classical-ml">Gaussian Mixture Models &amp; EM</a> give the alternative objectives their full treatment.</Prose>

    <H2>{headings[12]}</H2>
    <Prose>Attempt each changed case before opening its hint. Explain the reasoning and convention, not just the final number. A useful readiness check is whether you can explain how a control changed the mechanism and result, then choose a suitable setting for a new case.</Prose>
    <Practice title="1. Move means on a new dataset" question="In one dimension, use rows 0,2,3,9 with k=2 and initial centers 0,3. Assign ties to the lower-index center. Calculate the first assignment, its error, the mean update and the next assignment. Is the first mean update already a fixed point?" hint="Compare squared distances for each row. Keep the assignments fixed while computing each new mean.">
      <Prose>The first groups are [0] and [2,3,9], with error 0+1+0+36=37. Their new means are 0 and 14/3. With these memberships the error is 86/3≈28.6667. The new midpoint is 7/3, so row 2 now chooses the first center. The first mean update was not a fixed point. The next groups [0,2] and [3,9] have means 1 and 6 and error 20. Another assignment moves row 3 left; eventually [0,2,3] has mean 5/3 and [9] has mean 9, with error 14/3. An apparently improved intermediate answer need not be finished.</Prose>
    </Practice>
    <Practice title="2. Prove why an extra center is not evidence" question="A colleague gets training inertia 2,500 for k=3 and 2,490 for k=4 and declares four real populations. What can you conclude? What if the fitted inertia for k=4 had been 2,510?" hint="Separate the globally best objective allowed by k, the result of this optimizer and the usefulness of a partition.">
      <Prose>The observed reduction is 10/2,500=.4%, but its practical meaning depends on units and the task. Lower error by itself cannot identify real populations. Inspect the range of k, fit stability, group profiles, the selected metric and an independent decision criterion. If a k=4 run returns 2,510, it is a worse local fit or a changed setup; the globally optimal k=4 objective still cannot exceed the k=3 optimum. Do not infer a violation of that property from two imperfect optimization runs.</Prose>
    </Practice>
    <Practice title="3. Predict a seeding draw" question="Rows are 0,1,4, and the first center is 0. What are the vanilla D² probabilities? Which row is selected by a uniform draw u=.05 if intervals follow row order? What changes if all rows are 0?" hint="The cumulative interval for row 1 has length 1/17. Division by a zero total is not a probability rule.">
      <Prose>The probabilities are 0, 1/17, 16/17. Since .05{"<"}1/17≈.0588, row 1 is selected even though 4 is farther away. With all rows at zero, every row is already represented exactly. Stop with zero error or use a declared duplicate-center fallback if an API must return k centers. Neither option justifies silently generating NaNs.</Prose>
    </Practice>
    <Practice title="4. Change the units without changing the question" question="A distance uses spend in dollars and a squared-distance weight of 1/1,000,000 on that column. You convert spend to thousands of dollars. What should the new weight be? Would independently standardizing every column preserve the same question?" hint="A coordinate multiplier a requires dividing its old weight by a².">
      <Prose>The coordinate multiplier is .001, so the new weight is (1/1,000,000)/(.001²)=1. This preserves that column’s distance contribution. Standardization instead uses data-dependent inverse-variance weights. It might be appropriate, but it need not preserve the original distance or objective. Explain why its relative weights serve the application.</Prose>
    </Practice>
    <Practice title="5. Derive a changed Ward merge" question="Group A has 3 observations with mean 1; group B has 2 with mean 6. Their internal squared errors are 2 and .5. Find the merged mean, the extra error, the new total error and the SciPy-style Ward height." hint="The means are five units apart. Keep the old scatter separate from the penalty for moving representatives.">
      <Prose>The mean is (3·1+2·6)/5=3. The increment is (3·2/5)·25=30. The merged error is 2+.5+30=32.5. The Ward height is √60≈7.74597, not 30 and not the centroid separation 5. Checking directly gives movement penalties 3·(1−3)²+2·(6−3)²=12+18=30.</Prose>
    </Practice>
    <Practice title="6. Read a tree with tied heights" question="A six-leaf tree has successive monotone merge heights .5,.7,.7,3,8. Under a cut that includes all merges at or below t, can any horizontal threshold produce exactly four groups? What do the two .7 operations imply for a four-group merge-count prefix?" hint="Start with six groups and count how many joins enter together at each distinct height.">
      <Prose>No. Below .5 there are six groups; from .5 to below .7 there are five; at .7 there are three. A merge-count prefix can stop after two merges and leave four groups, but it must choose which tied .7 merge was second. That is a tie-order decision, not a height difference represented in the data. Rotating the drawing does not resolve it.</Prose>
    </Practice>
    <Practice title="7. Diagnose a persuasive but misleading dashboard" question="A dashboard plots two UMAP islands, reports silhouette .82, names them low-risk and high-risk, and compares [0,0,1,1] with [1,1,0,0] as 0% agreement. Identify at least four separate issues." hint="Inspect geometry, the meaning of a metric, external labels and label permutation before looking at formatting.">
      <Prose>The projection can alter distances and create apparent separation; state whether silhouette uses original or projected coordinates and why. A favorable score does not validate a risk label; such names need independent outcome evidence and an evaluation protocol. The two label vectors describe the same partition, so raw label accuracy is wrong. Also ask whether preprocessing and model selection used held-out information and whether the result is stable across justified representations and populations.</Prose>
    </Practice>
    <Practice title="8. Count colors, not just distinct color names" question="A grayscale image has nine pixels of value 0 and one of value 100. For a single unconstrained representative, compare the correct mean/error with clustering the two distinct values without weights. Why can the better floating-point mean still require a separate stored-palette check?" hint="The original image has ten observations. A repeated value must contribute repeatedly to the objective.">
      <Prose>The correct mean is 10 and error is 9·10²+90²=9,000. Treating 0 and 100 equally gives mean 50, whose error on the original ten pixels is 10·50²=25,000. Counts preserve the intended objective. In another example the learned mean may not be an integer channel value, so rounding the stored palette changes the reconstruction and should be scored separately. Here 10 is already integral.</Prose>
    </Practice>
    <Practice title="9. Change the geyser’s units" question="Suppose Old Faithful waiting times are expressed in seconds while eruption durations stay in minutes. Predict how this changes raw squared distances. Must it change the fitted partition? Compare the raw-seconds, raw-minutes and standardized two-center fits, and explain which measurements would be available for a future-wait recommendation." hint="Multiplying one column by 60 multiplies its squared differences by 3,600. A changed objective need not move any observation across the final assignment boundary. Standardization cancels a positive unit conversion when the fitted scales are converted consistently.">
      <Prose>The raw objective puts 3,600 times as much weight on a given waiting-time difference, relative to an unchanged duration difference. Yet a changed objective need not change the best candidate found. In the checked fit with the lesson’s settings, raw seconds and raw minutes give exactly the same memberships; both differ from the standardized fit on 4 of 272 observations after matching group labels. The raw-seconds centers are approximately (2.09433 minutes, 3,285 seconds) and (4.29793 minutes, 4,817.093 seconds). Divide the second coordinate by 60 to recover the raw-minute centers (2.09433, 54.75) and (4.29793, 80.28488). There is no further drift in the duration coordinates in this case.</Prose>
      <Prose>Standardizing each column with consistently converted training means and standard deviations makes this positive unit change cancel. Equal variance is a defensible exploratory choice, not the only geometry. Clustering on waiting time alone can describe historical waits, but a visitor asking about a future wait cannot supply that outcome. A recommendation needs an assignment rule based on already observed information, such as eruption duration, and the separate held-out check described in section 9.</Prose>
    </Practice>
    <Practice title="10. Design a scientifically defensible grouping" question="You receive expression measurements for 500 patients across 20,000 genes and are asked for patient subgroups. Propose an analysis plan, then give one reason a technically correct Ward tree could still mislead." hint="Specify the observation and measurement model before choosing a normalization, feature reduction or clinical name.">
      <Prose>An acceptable plan states patient-level sampling, measurement and batch provenance, missingness and the intended use; chooses and justifies normalization and the meaning of similarity; checks sensitivity to feature selection and representation; explores suitable linkages and resolutions; and evaluates stability and external associations on separated evidence. Dense pair storage for 500 rows is only 124,750 values, about 1 MB of float64 distances, but the feature matrix and validation costs remain. A tree may primarily recover batch effects or irrelevant variation. There is no universal “use Ward, standardize everything and reduce to 50 dimensions” answer.</Prose>
    </Practice>

    <H2>{headings[13]}</H2>
    <Prose>The capstone treats clustering as a reproducible engineering decision: learn representatives on development rows, choose a resolution using development evidence, freeze the chosen transform and fit, then report distortion on untouched rows. The synthetic generator makes the experiment rerunnable; the devices are constructed, so the result is about the protocol, not about real machines.</Prose>
    <Prose>The example declares its selection rule before evaluating the test set: each training group must have at least 20 rows and the median bootstrap ARI on common validation rows must be at least .8; among eligible k values, select the highest validation silhouette. These are illustrative operating requirements, not universal thresholds. Bootstrap means sampling training rows with replacement. Each refit predicts the same validation rows so the ARI compares the same objects. KMeans <Code>transform</Code> returns distances to its fitted centers; after the pipeline’s scaler, the smallest squared distance per row is the row’s representation error.</Prose>
    <Program example={clusteringExamples.capstone}><Prose>Check that no test rows are used to learn scales, choose k or select a seed. Read cluster sizes and center profiles alongside error. The label-invariant stability check compares predictions on the same development reference rows. The held-out result estimates the stated representation task on this constructed sampling process.</Prose></Program>
    <Prose><strong>Your independent variation:</strong> the report function takes the two seeds as arguments, so the first variation is one line. Add it below the program and run again.</Prose>
    <CodeBlock language="python">{'changed = report(data_seed=29, split_seed=31)'}</CodeBlock>
    <Prose>With those seeds the declared rule again selects k=3, the test mean squared standardized distance is 0.253428 against a one-center baseline of 2.043875, and the three held-out profiles have 18, 17 and 25 rows. If your numbers differ, compare versions before comparing reasoning. Then make a variation that is yours: add a low-information feature to the generated matrix, and compare one justified rescaling with the original representation. Choose the comparisons and all settings on the development portion, then evaluate each frozen procedure once on its untouched test rows. If a previous test result inspired another change, reserve fresh final evaluation rows rather than treating the old test as untouched. Include the one-center baseline for distortion and explain why its silhouette is undefined.</Prose>
    <LessonTable caption="What a complete submission should make inspectable" headers={['part', 'success check']} rows={[
      ['Task and representation', 'One row, feature meanings, transformations and the intended decision are explicit'],
      ['Fitting protocol', 'Splits, versions, seeds, initialization count, k candidates and stopping settings are reproducible'],
      ['Mechanism', 'A changed hand calculation agrees with code; tie, empty and early-stop behavior is explained'],
      ['Evidence', 'Distortion units, cluster sizes, per-group profiles and a label-invariant stability check are reported with their limits'],
      ['Hierarchy', 'Construct the six-point tree under another linkage and explain one changed merge and one valid cut'],
      ['Transfer', 'Explain why the added feature or rescaling changed, or did not change, the useful partition'],
      ['Limits', 'One counterexample and one alternative objective are justified; no claim of a universal true k']
    ]} />
    <Prose>A strong explanation can say “these centers represent this distribution under these weights with this error; the grouping was stable under these changes, but a ring-shaped target would need another objective.” If you can derive an update, explain a tree height, reproduce a fit and diagnose a changed representation, you are ready for <a href="/learn/path/full-curriculum/pca-dimensionality-reduction?module=classical-ml">PCA &amp; Dimensionality Reduction</a>, the next topic in this module’s sequence.</Prose>
    <Sources alternatives={<><Prose>For a second explanation, use the annotated resources below after attempting the local examples. Older teaching material is useful for the mathematics; use current documentation for API defaults and verify a video’s conventions against the calculations here.</Prose><ul>
      <li><a href="https://hastie.su.domains/ElemStatLearn/download.html">Hastie, Tibshirani &amp; Friedman, <em>The Elements of Statistical Learning</em>, 2nd ed., §14.3</a> — the standard graduate treatment; the authors’ download page offers the corrected twelfth printing as a free PDF with Springer’s agreement. §14.3.6 derives k-means from the within-cluster scatter exactly as section 4 does; §14.3.9 covers vector quantization; §14.3.12 treats the linkages and the monotonicity that centroid linkage lacks. Dense but precise.</li>
      <li><a href="https://www.statlearning.com/">James, Witten, Hastie &amp; Tibshirani, <em>An Introduction to Statistical Learning</em>, 2nd ed., §12.4</a> — the gentler sibling, also free. The authors’ <a href="https://web.stanford.edu/~hastie/ISLR2/Slides/Ch12_Unsupervised_Learning.pdf">chapter 12 slides</a> offer illustrated assignment updates, tree cuts, linkage comparisons and practical choices; use their clustering portion as a second explanation after sections 2 and 6 here. Keep this lesson’s explicit tie and stopping conventions when interpreting simplified lecture statements.</li>
      <li><a href="https://ocw.mit.edu/courses/15-071-the-analytics-edge-spring-2017/resources/video-5-hierarchical-clustering-0/">MIT OpenCourseWare: Video 5, Hierarchical Clustering</a> — a short narrated five-point merge and tree introduction with an official transcript. The transcript was reviewed; its example uses centroid linkage, which is not taught here and can produce inversions, and its large-gap cut suggestion is a heuristic. <a href="https://www.youtube.com/watch?v=fuUC0WVeKsg">Watch on YouTube</a>.</li>
      <li><a href="https://scikit-learn.org/stable/auto_examples/cluster/plot_kmeans_assumptions.html">Scikit-learn: Demonstration of k-means assumptions</a> — executable figures comparing anisotropy, unequal variance and uneven sizes; useful after the geometric counterexamples in section 12.</li>
      <li><a href="https://scikit-learn.org/stable/auto_examples/cluster/plot_agglomerative_dendrogram.html">Scikit-learn: Plot Hierarchical Clustering Dendrogram</a> — a practical bridge from a fitted estimator’s merge structure to a tree; check its height convention.</li>
    </ul></>}>
      <li><a href="https://search.r-project.org/R/refmans/datasets/html/faithful.html">R datasets: Old Faithful Geyser Data</a> — documentation of the 272-row version used here (Härdle 1991), with the note that Azzalini and Bowman (1990, <em>Applied Statistics</em> 39:357) describe a fuller 299-row record. The values were retrieved from the Rdatasets mirror on 12 September 2026.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.cluster.KMeans.html">KMeans API</a> and <a href="https://scikit-learn.org/stable/modules/generated/sklearn.cluster.MiniBatchKMeans.html">MiniBatchKMeans API</a> — checked initialization, stopping, weighting and output contracts; see the versions recorded above.</li>
      <li><a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.cluster.hierarchy.linkage.html">SciPy linkage</a>, <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.cluster.hierarchy.fcluster.html">flat cuts</a> and <a href="https://scikit-learn.org/stable/modules/generated/sklearn.cluster.AgglomerativeClustering.html">AgglomerativeClustering</a> — input shapes, linkage definitions, tie and cut semantics and algorithm-specific resource costs.</li>
      <li><a href="https://theory.stanford.edu/~sergei/papers/kMeansPP-soda.pdf">Arthur &amp; Vassilvitskii: k-means++ (SODA 2007)</a> — the original D²-sampling algorithm and expected approximation theorem; advanced proof reading.</li>
      <li><a href="https://link.springer.com/article/10.1007/s10994-009-5103-0">Aloise, Deshpande, Hansen &amp; Popat: NP-hardness of Euclidean sum-of-squares clustering (<em>Machine Learning</em> 75, 2009)</a> and <a href="https://repository.ias.ac.in/128048/1/1-s2.0-S0304397510003269-main.pdf">Mahajan, Nimbhorkar &amp; Varadarajan: The planar k-means problem is NP-hard (<em>TCS</em>, 2012; free paper)</a> — separate worst-case hardness results with growing dimension or growing k; they do not rule out exact solutions of particular instances.</li>
      <li><a href="https://link.springer.com/article/10.1007/s00454-011-9340-1">Vattani: k-means requires exponentially many iterations even in the plane (<em>DCG</em>, 2011)</a> and <a href="https://dl.acm.org/doi/10.1145/2027216.2027217">Arthur, Manthey &amp; Röglin: Smoothed analysis of the k-means method (<em>JACM</em> 58, 2011)</a> — the worst-case and smoothed iteration bounds quoted in section 5.</li>
      <li><a href="https://www.numericalecology.com/Reprints/Murtagh_Legendre_J_Class_2014.pdf">Murtagh &amp; Legendre: Ward’s hierarchical agglomerative clustering method: which algorithms implement Ward’s criterion? (<em>Journal of Classification</em> 31, 2014; authors’ free paper)</a> — how input squaring and output heights reconcile Ward1 and Ward2, and why mixing conventions can change the tree.</li>
      <li><a href="https://doi.org/10.1109/TIT.1982.1056489">Lloyd: Least Squares Quantization in PCM (<em>IEEE Trans. Inf. Theory</em> 28, 1982; Bell Labs note 1957)</a> — the classical representative and quantization connection; the paper’s setting is more specific than every modern clustering application.</li>
      <li><a href="https://scikit-learn.org/stable/modules/clustering.html">Scikit-learn clustering guide</a> — method comparisons, BIRCH summaries, mini-batches and diagnostics.</li>
      <li><a href="https://github.com/facebookresearch/faiss/wiki/Faiss-indexes">Faiss index descriptions</a> — inverted lists, coarse quantization and search controls; a deeper application of learned representatives to candidate routing.</li>
    </Sources>
  </div>
};

export default kMeansHierarchicalContent;
