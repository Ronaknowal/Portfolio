import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { BinarySurpriseLab, PrefixTreeFigure, PrefixCodeLab, MismatchLab, ConditionalEntropyLab, LogitLossLab, ContinuousEntropyLab, MaximumEntropyLab, ClassifierLossFigure } from '../../components/lesson-labs/EntropyInformationLabs';
import { entropyInformationExamples } from '../entropy-information-examples';
function Example({
  name,
  children
}) {
  const example = entropyInformationExamples[name];
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></>;
}
function Practice({
  title,
  children,
  hint,
  solution
}) {
  return <section className="entropy-practice"><H3>{title}</H3>{children}<details><summary>Optional hint</summary><Prose>{hint}</Prose></details><details><summary>Worked solution and reasoning</summary>{solution}</details></section>;
}
export default {
  title: 'Entropy, Cross-Entropy & KL Divergence',
  readTime: '~78 min read + 100 min investigations and practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot entropy-lesson">
    <LessonIntro exampleKind="Optional Python 3; one separate PyTorch example" prerequisites="Probabilities that sum to one, weighted averages and logarithms. We refresh those operations before using them. Conditional probability is reviewed locally. Integrals and density changes enter a clearly marked continuous branch; Measure Theory supplies further background." sections={[['1-start-with-a-message-and-its-surprise', 'Surprise and units'], ['2-average-over-what-actually-happens', 'Entropy'], ['3-turn-information-into-actual-binary-words', 'Coding and decoding'], ['4-let-one-distribution-supply-outcomes-and-another-supply-prices', 'Cross-entropy and KL'], ['5-prove-the-bound-and-respect-the-direction', 'Proof and support'], ['6-predict-with-the-information-you-have', 'Conditional prediction'], ['7-compute-and-interpret-log-loss', 'Stable computation and evaluation'], ['8-change-continuous-units-without-changing-the-experiment', 'Densities and coordinates'], ['9-use-kl-to-certify-a-maximum-entropy-model', 'Maximum entropy'], ['10-practise-with-new-distributions-and-decisions', 'Independent practice']]}>
      A device sends a stream of status symbols. Some occur often; others rarely. Can shorter messages exploit that pattern? Now imagine a model predicting those symbols: how much does it cost to believe the wrong pattern? The same logarithmic accounting explains compression, probabilistic prediction and the information lost by a mismatched model.
    </LessonIntro>

    <H2>1. Start with a message and its surprise</H2>
    <Prose>A <strong>distribution</strong> lists the possible outcomes and their probabilities. Write P(x) for the probability of outcome x under a source model P. For our status stream, the outcomes are A, B, C and D, with probabilities 1/2, 1/4, 1/8 and 1/8. The letters are labels; A is not numerically closer to B than to D.</Prose>
    <Prose>Receiving a likely outcome tells us less than receiving an unlikely one. Information theory turns this intuition into <strong>surprisal</strong>, or information content: −log₂ P(x). A logarithm answers “to what power must the base be raised?” Because 2⁻³=1/8, an outcome with probability 1/8 has three bits of surprise. Here a <strong>bit</strong> is an information unit, not yet a promise about the number of physical binary digits sent.</Prose>
    <LessonTable caption="Probability becomes an additive information price" headers={['Outcome', 'P(x)', '−log₂ P(x)', 'Read it as']} rows={[['A', '1/2', '1 bit', 'One equally weighted binary choice'], ['B', '1/4', '2 bits', 'Two equally weighted binary choices'], ['C', '1/8', '3 bits', 'Three equally weighted binary choices'], ['D', '1/8', '3 bits', 'Three equally weighted binary choices']]} />
    <Prose>Why a logarithm? Independent events multiply probabilities, but we want their information to add. If an independent event has probability 1/2 and another has probability 1/4, their joint probability is 1/8. Their surprises are 1+2=3 bits. The negative sign makes the information nonnegative because probabilities are at most one.</Prose>
    <MathBlock>{String.raw`\begin{aligned}I_P(x)&=-\log_b P(x),\\
      -\log_b(pq)&=-\log_b p-\log_b q.
    \end{aligned}`}</MathBlock>
    <Prose>The second line is always a logarithm identity; using pq as a joint probability requires independence. For dependent events, use a conditional probability: P(x,y)=P(x)P(y|x). We will return to that distinction when a prediction has useful context.</Prose>
    <Prose>Base 2 gives bits; the natural logarithm ln, with base e, gives <strong>nats</strong>. Divide a value in nats by ln 2 to convert it to bits. One bit equals ln 2≈0.693147 nats. Keep the log base consistent across a sum or comparison. “Surprising” here means improbable under the stated model; it does not measure emotional significance, usefulness, truth or whether a string looks random.</Prose>
    <Checkpoint prompt="A model assigns probability 1 to A. What is the information in seeing A? What would seeing B reveal if P(B)=0?"><Prose>A has −log₂1=0 bits: it was completely predicted. B has infinite model surprise; its occurrence contradicts the model's exclusion of B. That does not prove B is impossible in reality. It exposes a model-support error.</Prose></Checkpoint>

    <H2>2. Average over what actually happens</H2>
    <Prose>One rare event's surprise is different from average uncertainty before a draw. To form an expectation, multiply each outcome's value by how often it occurs and add. <strong>Shannon entropy</strong> is the expected surprisal when the probabilities supplying the frequencies also supply the prices.</Prose>
    <MathBlock>{String.raw`\begin{aligned}H_b(P)&=\mathbb E_{X\sim P}[-\log_b P(X)]\\
      &=-\sum_x P(x)\log_b P(x).
    \end{aligned}`}</MathBlock>
    <Prose>For our four status symbols, A contributes (1/2)×1=0.5 bits per symbol, B contributes (1/4)×2=0.5, and C and D each contribute (1/8)×3=0.375. Total entropy is <strong>1.75 bits per symbol</strong>. The expectation weights the rare symbols less often; simply averaging the four surprise values would incorrectly give 2.25.</Prose>
    <Prose>A fair coin has one bit of entropy. A coin that lands heads with probability 0.9 has H=−0.9log₂0.9−0.1log₂0.1≈0.468996 bits. A rare tail has about 3.322 bits of surprise, yet its average contribution is only 0.3322. The common head contributes the remaining 0.1368.</Prose>
    <BinarySurpriseLab />
    <Prose>We define <Code>0·log 0 = 0</Code> inside the entropy sum by the limit p log p→0 as p→0⁺. This convention does not assert that log 0 is a finite number. A zero-probability outcome is omitted from this expectation. A distribution concentrated on one outcome has entropy 0; a uniform distribution on m possible outcomes has entropy log_b m. We will prove that the uniform distribution is maximal after introducing KL.</Prose>
    <Prose>The optional code uses Python 3's standard library unless explicitly marked PyTorch. Save one complete block as <Code>information_check.py</Code> and run <Code>python information_check.py</Code> in your Python environment. Each block has its own imports and data. The examples were executed with Python 3.12; printed decimals are rounded, not symbolic exact values.</Prose>
    <Example name="coin"><Prose>Swap 0.9 and 0.1 to check that renaming heads and tails does not change the uncertainty. This is a distribution property; inspecting one short observed string cannot establish its generating entropy.</Prose></Example>
    <Prose>Entropy is also <strong>concave</strong> in a finite probability vector: mixing two distributions cannot produce less entropy than their weighted average entropy. One way to see the mechanism is that −p ln p has second derivative −1/p for p&gt;0; summing preserves concavity, and the continuous endpoint convention handles zeros. Mixing two certain but different outcomes can create uncertainty even though each component alone had none.</Prose>

    <H2>3. Turn information into actual binary words</H2>
    <Prose>A binary <strong>codeword</strong> is the string of zeros and ones assigned to one symbol. Fixed two-bit words can represent four outcomes. We can do better on average for our uneven source by giving A the word 0, B the word 10, C the word 110 and D the word 111. The receiver knows the same mapping.</Prose>
    <Prose>This is a <strong>prefix-free code</strong>: no complete codeword begins another complete codeword. Starting at the root of the tree, each received bit chooses an edge. Reaching a leaf identifies a symbol immediately, so decoding resets to the root for the next symbol. Without that property, a bit might be either a finished word or the start of a longer one.</Prose>
    <PrefixTreeFigure />
    <Prose>The expected word length is (1/2)×1+(1/4)×2+(1/8)×3+(1/8)×3=1.75 bits per symbol. It equals H(P) here because each probability is exactly 2 raised to a negative integer and the lengths match −log₂ P. The message <Code>AAAABBCD</Code> contains the proportions of this source exactly, so its eight symbols use 14 bits instead of 16.</Prose>
    <PrefixCodeLab />
    <Example name="prefix"><Prose>The decoder accumulates an unfinished prefix and emits a symbol only when it reaches a complete word. It explicitly rejects an incomplete ending. The example is a small exact codec for A–D, not a general file-compression format.</Prose></Example>
    <Prose>For probabilities such as 0.9, −log₂0.9≈0.152 is not an integer word length. A nontrivial two-symbol prefix code still needs at least one binary digit per symbol when coding each separately. For a finite alphabet, the optimal average prefix-code length L* obeys the following bound, allowing the empty word for a deterministic single-outcome source:</Prose>
    <MathBlock>{String.raw`H_2(P)\le L^*<H_2(P)+1.`}</MathBlock>
    <Prose>The upper bound comes from rounding each ideal length upward: ℓ_x=⌈−log₂ P(x)⌉ for positive-probability symbols. Those lengths satisfy the prefix-code budget Σ2⁻ˡ≤1, so a prefix code with them exists. Rounding adds less than one bit to each ideal length. This construction supplies a bound; it need not be the optimal code.</Prose>
    <Prose>To construct an optimal binary prefix tree from arbitrary finite symbol frequencies, use Huffman’s algorithm. The <a href="/learn/path/full-curriculum/greedy-algorithms-exchange-arguments#6-merge-small-parts-into-a-code">Greedy Algorithms lesson</a> already develops its sibling-exchange proof, complete encoder/decoder and framing limits. Here the entropy bound explains what that optimum can and cannot achieve.</Prose>
    <details><summary>Deeper: why every prefix code pays at least the entropy</summary>
      <Prose>At a common depth d in a binary tree, a word of length ℓ occupies 2^(d−ℓ) descendant slots. Prefix-free words occupy disjoint slots, giving K=Σ2⁻ˡ≤1 after division by 2^d. This is the Kraft inequality. Normalize their slot weights to R(x)=2⁻ˡˣ/K. Then −log₂ R(x)=ℓ_x+log₂ K, so L−H(P)=KL(P∥R)−log₂ K≥0. The KL proof appears in section 5. The count extends to arbitrary finite word lengths by taking d at least their maximum.</Prose>
      <Prose>Grouping n independent draws creates an alphabet of whole blocks with entropy nH(P). Apply the same bound to block words: nH≤L_n&lt;nH+1, hence H≤L_n/n&lt;H+1/n. The overhead per draw can approach zero. This does not promise that a short individual message beats fixed-width encoding, nor does it include metadata, finite-precision coder costs or unknown-source estimation. Dependent sequences require their joint/conditional distribution; independence cannot be assumed just to multiply single-symbol entropies.</Prose>
      <Example name="blocks"><Prose>The program constructs feasible ceiling lengths and verifies their Kraft budget with exact rational probabilities. Its n=1 rate 1.3 is an upper-bound construction, worse than the optimal one-bit binary code. The 8-draw rate is closer to entropy; monotonic improvement for every particular block size is not required by the bound.</Prose></Example>
    </details>

    <H2>4. Let one distribution supply outcomes and another supply prices</H2>
    <Prose>Suppose the device still emits according to P=(1/2,1/4,1/8,1/8), but the prediction model Q believes (1/8,1/8,1/4,1/2). It treats common A as rare and rare D as common. Keep the actual frequencies P; charge each event the model's surprise −log Q. This expected cost is <strong>cross-entropy</strong>, written H(P,Q).</Prose>
    <MathBlock>{String.raw`\begin{aligned}H_b(P,Q)&=\mathbb E_{X\sim P}[-\log_b Q(X)]\\
      &=-\sum_x P(x)\log_b Q(x).
    \end{aligned}`}</MathBlock>
    <Prose>The prices in bits are now 3, 3, 2, 1. Cross-entropy is 0.5×3 + 0.25×3 + 0.125×2 + 0.125×1 = <strong>2.625 bits per symbol</strong>. The distributions use the same labeled outcomes; comparing unrelated coordinates would not define this intended score. In this dyadic example those model prices are actual codeword lengths. For general Q they are ideal logarithmic costs, approached by suitable block coding with rounding overhead.</Prose>
    <Prose>Subtract the source's 1.75-bit entropy. The extra 0.875 bits per symbol is the <strong>Kullback–Leibler divergence</strong>, or KL. Its first argument tells us which distribution supplies the expectation.</Prose>
    <MathBlock>{String.raw`\begin{aligned}D_{\rm KL}(P\Vert Q)
      &=\sum_x P(x)\log_b\frac{P(x)}{Q(x)}\\
      &=H_b(P,Q)-H_b(P).
    \end{aligned}`}</MathBlock>
    <Prose>Each contribution compares two prices for the same outcome. For A it is 0.5×log₂4=1; for D it is 0.125×log₂(1/4)=−0.25. The other contributions are 0.25 and −0.125. Some rows are negative because Q prices those outcomes more cheaply. Their weighted total is nonnegative. KL is not a sum of independent “mistake probabilities.”</Prose>
    <MismatchLab />
    <Example name="bernoulli"><Prose>This two-outcome example uses nats, while the four-status calculation used bits. The values 0.611,0.765 and 0.154 are respectively entropy, cross-entropy and the extra cost. Multiplying every value by 1/ln 2 changes the unit without changing the relation.</Prose></Example>
    <LessonTable caption="Zero support is a mathematical case, not a numerical nuisance to hide" headers={['At one outcome', 'Entropy contribution', 'Cross-entropy / KL contribution']} rows={[['P=0; Q≥0', '0 by the limiting convention', '0: this expectation never charges the outcome'], ['P>0; Q=0', 'Finite for a fixed positive P', '+∞: possible source outcome excluded by model'], ['P>0; Q>0', 'Ordinary formula', 'Ordinary formula; KL term can have either sign']]} />
    <Prose>The <strong>support</strong> of a finite distribution is its positive-probability outcomes. Finite KL requires the support of P to lie inside the support of Q. Replacing zero probabilities with a small floor changes the prediction and score. If smoothing is part of the intended model, define and normalize that model explicitly. If clipping is merely a reporting choice, disclose the threshold and changed metric; it is not the original infinite loss.</Prose>
    <Example name="support"><Prose>The helper skips a P=0 row before taking logarithms. It returns infinity when Q excludes a P-positive outcome and rejects malformed distributions rather than silently normalizing them. The browser, by contrast, explicitly accepts weights and normalizes them; those are two different input contracts.</Prose></Example>

    <H2>5. Prove the bound and respect the direction</H2>
    <Prose>Why can cheaper outcomes never win enough to make total KL negative? For t&gt;0, ln t≤t−1, with equality only at t=1. To verify this inequality, the difference t−1−ln t has derivative 1−1/t, decreases until t=1, increases afterwards and equals zero there.</Prose>
    <Prose>Assume Q is positive wherever P is positive; otherwise KL is already infinite. Use t=Q(x)/P(x), multiply by P(x), and sum only over S, the support of P. In nats:</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      D_{\rm KL}(P\Vert Q)
      &=-\sum_{x\in S}P(x)\ln\frac{Q(x)}{P(x)}\\
      &\ge\sum_{x\in S}\bigl(P(x)-Q(x)\bigr)\\
      &=1-Q(S)\ge0.
    \end{aligned}`}</MathBlock>
    <Prose>Equality requires Q(x)=P(x) for every x∈S and no leftover Q mass outside S. Thus KL=0 exactly when the entire finite distributions agree. It follows that H(P,Q)≥H(P): reporting the actual distribution uniquely minimizes expected logarithmic loss over all distributions. This is the <strong>strictly proper scoring</strong> property. It is a population statement about an expectation, not a promise that the honest model wins on every finite sample.</Prose>
    <Prose>Set Q to the uniform distribution U on m labels. Since −log_b U(x)=log_b m for every outcome, H(P,U)=log_b m. Therefore H(P)=log_b m−KL(P∥U)≤log_b m, with equality exactly at uniformity. This proves the earlier entropy maximum without assuming that “more spread” is a sufficient argument.</Prose>
    <Prose>Changing the direction changes the question. KL(P∥Q) weights omissions using P. KL(Q∥P) weights them using Q and is infinite if Q puts mass where P assigns zero. In a restricted variational family, minimizing the reverse direction can favor concentration in one region of a multimodal target. The result depends on the target, family and optimization; reverse KL does not invariably pick one mode. <a href="/learn/path/full-curriculum/variational-inference">Variational Inference</a> examines that approximation problem.</Prose>
    <Prose>KL is not a metric. It is generally asymmetric and can fail the triangle inequality. For Bernoulli probabilities 0.1,0.5,0.9, in nats, D(0.1∥0.9)≈1.757780 while D(0.1∥0.5)+D(0.5∥0.9)≈0.878890. Relabeling categories leaves KL unchanged; moving mass between numerically near or far labels has no built-in transport cost. The preceding <a href="/learn/path/full-curriculum/optimal-transport-wasserstein-distance-sinkhorn">Optimal Transport</a> lesson explains geometry-aware comparisons; <a href="/learn/path/full-curriculum/f-divergences-integral-probability-metrics">f-divergences and integral probability metrics</a> compares other choices.</Prose>
    <Prose>A symmetric finite alternative is Jensen–Shannon divergence: set M=(P+Q)/2 and average KL(P∥M) and KL(Q∥M). The mixture covers both supports, avoiding their direct support-mismatch infinities. This still does not insert geometric distances between labels; choose the comparison for the question being asked.</Prose>
    <Checkpoint prompt="P=(1,0) and Q=(1/2,1/2). Someone says their supports differ, so both KL directions must be infinite. Repair the claim."><Prose>KL(P∥Q)=log₂2=1 bit: Q assigns positive probability to P's only possible outcome. KL(Q∥P)=∞ because Q sometimes draws the second outcome that P excludes. Different supports alone do not settle finiteness; the direction of the inclusion matters.</Prose></Checkpoint>
    <details><summary>Deeper: finite entropy versus an undefined subtraction</summary><Prose>A countably infinite source can have H(P)=∞. If Q=P, the ratio definition still gives KL=0, but the expression H(P,Q)−H(P) becomes ∞−∞ and cannot be used as a numerical identity. For example, normalize p_k proportional to 1/[k(ln k)²] for k≥2. The probability series converges by the integral test, while its entropy diverges like Σ1/(k ln k). For general spaces define KL as the expected log density ratio when P is absolutely continuous with respect to Q—meaning every Q-null measurable set is also P-null—and infinity otherwise. The ratio definition, with its integrability conventions, is primary; subtracting two separately divergent information quantities is not.</Prose></details>

    <H2>6. Predict with the information you have</H2>
    <Prose>In classification, the model sees features X before predicting a label Y. It provides Q(y|x), a distribution over labels for each observed feature value. For an observed correct label y, the <strong>one-hot</strong> target distribution puts all its mass on y. Its cross-entropy is just −log Q(y|x); no sum over unobserved correct classes remains.</Prose>
    <Prose>The population target is the average of that cost over the actual joint distribution P(X,Y). For every context x, split its cross-entropy into entropy plus KL, then average over how often x occurs. This gives the conditional version of the earlier decomposition:</Prose>
    <MathBlock>{String.raw`\begin{gathered}
      \mathbb E_{(X,Y)\sim P}[-\log Q(Y|X)]\\
      =H_P(Y|X)\\
      +\mathbb E_{X\sim P_X}
      [D_{\rm KL}(P(\cdot|X)\Vert Q(\cdot|X))].
    \end{gathered}`}</MathBlock>
    <Prose><strong>Conditional entropy</strong> H(Y|X) is the remaining label uncertainty averaged over contexts. It is not generally the entropy of the overall label histogram. If X is a fair bit and Y always equals X, then Y is balanced overall with H(Y)=1, but someone who sees X knows Y, so H(Y|X)=0. A perfect context-aware predictor can legitimately beat the marginal one-bit baseline.</Prose>
    <ConditionalEntropyLab />
    <Prose>With 10% independent label flips, the remaining uncertainty is h₂(0.1)≈0.468996 bits, while H(Y) remains one bit. A model that says Y=X with probability 0.8 has conditional cross-entropy −0.9log₂0.8−0.1log₂0.2≈0.521928 bits. Its extra cost is about 0.052933 bits. That gap comes from probability mismatch, not from the label imbalance.</Prose>
    <Example name="conditional"><Prose>The code visits all four joint outcomes and weights their losses by joint probability. It does not accidentally count each context once when contexts have unequal frequencies; for a changed problem, replace the joint weights accordingly.</Prose></Example>
    <Prose>The same multiplication rule explains the entropy <strong>chain rule</strong>. Take −log of P(x,y)=P(x)P(y|x), then average over the joint distribution. The cost of identifying the pair is the cost of identifying X plus the average remaining cost of identifying Y after X.</Prose>
    <MathBlock>{String.raw`H(X,Y)=H(X)+H(Y|X).`}</MathBlock>
    <Prose>For a finite discrete model, conditioning reduces entropy <em>on average</em>. One particular rare context can be more uncertain than the marginal distribution. The next lesson makes the average reduction H(Y)−H(Y|X) precise as mutual information. An empirical conditional loss can be below the population conditional entropy on a lucky finite sample; the inequality above concerns the full expected risk.</Prose>
    <Prose>For observations (x_i,y_i) modeled as conditionally independent given the inputs and shared parameters θ, the conditional likelihood is ∏Q_θ(y_i|x_i). Taking its negative logarithm turns a product into a sum. Dividing by the fixed positive sample count n changes the scale but not the minimizer:</Prose>
    <MathBlock>{String.raw`\begin{gathered}
      \underset{\theta}{\operatorname{argmax}}
      \prod_{i=1}^{n}Q_\theta(y_i|x_i)\\
      =\underset{\theta}{\operatorname{argmin}}
      \frac1n\sum_{i=1}^{n}-\log Q_\theta(y_i|x_i).
    \end{gathered}`}</MathBlock>
    <Prose>This connects cross-entropy to <a href="/learn/path/full-curriculum/maximum-likelihood-map-estimation">maximum likelihood</a>. For an autoregressive sequence, dependence is handled by Q(y_t|earlier context) and the joint chain factorization; tokens do not need to be independent. An objective that omits dependence without modeling it has a different statistical meaning.</Prose>

    <H2>7. Compute and interpret log loss</H2>
    <Prose>A neural classifier often outputs <strong>logits</strong>: real-valued scores z_j, one for each of C classes. Softmax converts them into probabilities Q_j=e^(z_j)/Σe^(z_k). Adding the same constant to every score multiplies numerator and denominator by the same factor, so probabilities do not change. We can exploit that invariance to avoid exponentiating huge positive values.</Prose>
    <Prose>Let m=max_j z_j. Compute shifted scores z_j−m≤0 and s=Σexp(z_j−m). Then log Q_j=(z_j−m)−ln s. For observed class y, the loss is (m−z_y)+ln s. Keep the subtraction grouped this way; forming a huge common log normalizer and subtracting it later can lose precision.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      m&=\max_j z_j,\quad s=\sum_j e^{z_j-m},\\
      \log Q_j&=(z_j-m)-\ln s,\\
      \ell(z,y)&=(m-z_y)+\ln s.
    \end{aligned}`}</MathBlock>
    <LogitLossLab />
    <Example name="logits"><Prose>The first two score vectors differ only by a common 1000; their log probabilities agree. The final example has an underflowed probability for the third class, yet its finite log loss is preserved. Stable arithmetic does not repair NaN inputs, unbounded floating-point differences or a logically excluded class.</Prose></Example>
    <Prose>Use the library's documented loss contract. In PyTorch's <Code>cross_entropy</Code>, input is unnormalized logits, usually shape (N,C) for N examples and C classes. Integer targets have shape (N,) and dtype <Code>torch.long</Code>. A valid soft-target distribution instead has the same (N,C) shape and floating dtype; each row is nonnegative and sums to one. The library does not fully enforce those probability constraints for you. No manual softmax should be applied first.</Prose>
    <Prose>The following optional program uses PyTorch 2.14 CPU, the tested environment. Use a Python environment with PyTorch installed according to its <a href="https://pytorch.org/get-started/locally/" target="_blank" rel="noreferrer">official platform instructions</a>; all other programs here need only the standard library. This example uses no class weights, ignored labels or smoothing, so “mean” is the arithmetic mean over its two examples. Those options change the scoring/reduction contract; inspect their documentation before transferring a hand calculation.</Prose>
    <Example name="torch"><Prose>Soft targets are distributions to score, not a second set of logits. The same formula is linear in their probabilities. Smoothing or distillation can define such targets, but the resulting training objective is then relative to that target distribution. For binary one-logit models, the corresponding stable PyTorch interface is <Code>binary_cross_entropy_with_logits</Code>; do not silently interchange the input conventions of different losses or frameworks.</Prose></Example>
    <H3>Confidence changes what accuracy cannot see</H3>
    <Prose>Use one shared binary test set of ten observations. Model A assigns the correct label probability 0.6 on cases 1–8 and 0.4 on cases 9–10. Model B assigns 0.99 on cases 1–8 and 0.01 on cases 9–10. With threshold 0.5 both make eight correct predictions and two errors: 80% accuracy. Every observation has an explicit probability under both models.</Prose>
    <ClassifierLossFigure />
    <Prose>A's mean log loss is −[8ln0.6+2ln0.4]/10≈0.591919 nats. B's is −[8ln0.99+2ln0.01]/10≈0.929074 nats. B saves a little on each already-correct answer but pays about 4.605 nats for each confidently wrong answer. Accuracy hides those prices; it only checks which probability is larger. This example compares these ten outcomes, not every possible future test set.</Prose>
    <Example name="evaluation"><Prose>The helper's accuracy calculation is appropriate for this binary setup with no ties. In multiclass problems, use the predicted argmax label, not whether the correct-label probability exceeds 0.5.</Prose></Example>
    <Prose><strong>Perplexity</strong> is b raised to the average base-b log loss. If the loss is in nats, use exp; if in bits, use 2^loss. It equals the reciprocal of the geometric mean probability assigned to the actual outcomes. For a uniform m-way model it equals m. A general value 20 does not mean that every context has exactly 20 candidates; it gives the equivalent uniform cost.</Prose>
    <MathBlock>{String.raw`\begin{aligned}
      \operatorname{PP}
      &=\exp\!\left(-\frac1n\sum_i\ln Q(y_i|x_i)\right)\\
      &=\left(\prod_i Q(y_i|x_i)\right)^{-1/n}.
    \end{aligned}`}</MathBlock>
    <Prose>Language-model perplexity requires like-for-like tokenization, text, context rules and averaging. A shorter token sequence and a longer token sequence change the unit “per token”; their raw perplexities are not directly comparable. Aggregate total negative log likelihood and total scored-token count when a token-weighted mean is intended, rather than averaging sentence perplexities. Zero-probability observed outcomes make perplexity infinite.</Prose>
    <Prose>Lower held-out cross-entropy means a higher geometric mean probability on the same observed outcomes. It does not alone guarantee better accuracy, calibration in each subgroup, fairness, robustness under shift, or a better decision under asymmetric costs. Model selection on that held-out set also changes its role; an untouched final evaluation remains valuable. A log-loss anomaly score is not automatically a p-value or evidence of a particular cause.</Prose>
    <Prose>Another useful connection is sequential evidence. If two models give likelihoods P and Q to a sequence, the log likelihood ratio is the sum of log[P(event|past)/Q(event|past)]. In an independent finite source model P, its expected value after n draws is n KL(P∥Q). A long-run cost gap can therefore accumulate as evidence between specified models. A realized sum can be negative; expectation is not a guarantee of a monotone trace or a license to ignore the sampling design.</Prose>

    <H2>8. Change continuous units without changing the experiment</H2>
    <Prose><strong>Continuous branch.</strong> A density f(x) is probability per unit coordinate, not the probability of a single value. Interval probability is ∫f(x)dx. A density may exceed 1; a singleton can have probability zero even where its density is positive. The <a href="/learn/path/full-curriculum/measure-theory-probability-spaces">Measure Theory</a> lesson develops these distinctions and mixed/singular laws.</Prose>
    <Prose>For a distribution with a density relative to the chosen Lebesgue coordinate measure, <strong>differential entropy</strong> is h(f)=−∫f(x)log f(x)dx when this integral is defined. We use lower-case h to distinguish it from the discrete bin-label entropy H. Differential entropy can be negative, infinite, or undefined; the discrete 0≤H≤log m bounds do not transfer unchanged.</Prose>
    <Prose>For X uniform on [0,w], the density is 1/w. Thus h₂(X)=log₂ w. If w=0.25 metres, h₂=−2 in that coordinate convention. This is not “negative binary digits to send an exact real number.” It is a density-based quantity relative to the coordinate unit.</Prose>
    <ContinuousEntropyLab />
    <Prose>Write Y=aX with a&gt;0. A small interval dy corresponds to dx=dy/a, so probability preservation requires f_Y(y)=f_X(y/a)/a. Substitute that density into the expectation of its negative logarithm: h(Y)=h(X)+log a, when the involved integrals exist with the needed finiteness. For an invertible smooth transformation, the analogous correction is the expected log absolute Jacobian determinant, under valid change-of-variable and integrability hypotheses.</Prose>
    <Prose>In our example a=100 changes metres to centimetres. The interval grows numerically from 0.25 to 25, density falls from 4 per metre to 0.04 per centimetre, and h₂ grows from −2 to about 4.643856. The middle-half event still has probability 1/2. Writing the density and its reference unit together makes the apparently different entropy values understandable.</Prose>
    <Prose>For aligned equal bins of width Δ=w/N in this uniform interval, each bin has probability 1/N, so the discrete entropy of the bin index is log₂N=h₂(X)+log₂(1/Δ). That identity is exact here. For smooth enough densities and sufficiently fine bins, a related approximation often holds; it is not a universal identity for arbitrary densities, atoms, uneven bins or divergent entropies.</Prose>
    <MathBlock>{String.raw`\begin{gathered}
      D_{\rm KL}(P\Vert Q)\\
      =\int f(x)\ln\frac{f(x)}{g(x)}\,dx.
    \end{gathered}`}</MathBlock>
    <Prose>KL compares density ratios relative to a common reference, so both Jacobian factors cancel when <em>both</em> distributions undergo the same invertible change of coordinates. It remains nonnegative and coordinate invariant under those conditions. Differential cross-entropy and differential entropy separately change. A zero of a density at one isolated point is not by itself a support failure: the issue is a Q-null measurable set receiving positive P probability.</Prose>
    <Example name="continuous"><Prose>The quantized entropy remains 2 bits in both units. For P=Normal(0,1²) and Q=Normal(1,2²), KL is about 0.443147 nats before and after scaling both means and standard deviations by 100. Scaling only one model would change the comparison.</Prose></Example>
    <details><summary>Deeper: derive Normal KL and avoid the empirical-distribution trap</summary>
      <Prose>For nondegenerate univariate Normal laws, expand the log density ratio. Under P, E[(X−μ_P)²]=σ_P² and E[(X−μ_Q)²]=σ_P²+(μ_P−μ_Q)². The normalizing constants supply ln(σ_Q/σ_P); the two expected quadratic terms give the rest:</Prose>
      <MathBlock>{String.raw`\begin{aligned}
        D_{\rm KL}(P\Vert Q)
        &=\ln\frac{\sigma_Q}{\sigma_P}-\frac12\\
        &+\frac{\sigma_P^2+(\mu_P-\mu_Q)^2}{2\sigma_Q^2}.
      \end{aligned}`}</MathBlock>
      <Prose>This derivation explains both the unit cancellation and why standard deviations must be strictly positive. It does not cover degenerate point masses by plugging σ=0 into the formula.</Prose>
      <Prose>For continuous density models, empirical average negative log density is still a useful likelihood objective. But do not call it finite KL from the empirical point-mass measure to a continuous model. The finite observed set has empirical probability 1 and continuous-model probability 0, so that measure-theoretic KL is infinite. A fixed common binning or smoothing changes the model and can make a discrete comparison meaningful. For an underlying continuous density P with appropriate finite entropy/cross-entropy, its <em>population</em> expected log density still has the valid entropy-plus-KL decomposition.</Prose>
    </details>

    <H2>9. Use KL to certify a maximum-entropy model</H2>
    <Prose>Suppose a count X can be 0,1 or 2, and only its mean is known: E[X]=10/7. Many distributions meet that constraint. Let t=q(2). Normalization and the mean then force q(1)=10/7−2t and q(0)=t−3/7. Nonnegativity permits t from 3/7 to 5/7. We have a real choice left; the constraints do not already determine all three probabilities.</Prose>
    <Prose>The <strong>maximum-entropy</strong> rule chooses the feasible distribution with the largest Shannon entropy on this specified finite alphabet. It is a modeling principle relative to the chosen outcomes and constraints, not a proof that the selected law is the true physical process. Try p*=(1,2,4)/7. Its probabilities sum to one and its mean is (2+8)/7=10/7.</Prose>
    <MaximumEntropyLab />
    <Prose>Here is a global certificate. For x=0,1,2, p*(x)=2^x/7, so log₂p*(x)=x−log₂7. Every feasible q has the same E_q[X]=10/7. Therefore its cross-entropy against p* is fixed at log₂7−10/7≈1.378783 bits. Subtract H(q):</Prose>
    <MathBlock>{String.raw`\begin{gathered}
      D_{\rm KL}(q\Vert p^*)\\
      =\log_2 7-\frac{10}{7}-H_2(q)\\
      =H_2(p^*)-H_2(q)\ge0.
    \end{gathered}`}</MathBlock>
    <Prose>This proves p* maximizes entropy over <em>every</em> feasible q, not just the plotted samples or stationary points. Equality occurs only at p*. At t=3/7, q=(0,4/7,3/7) has entropy about 0.985228; at t=5/7, q=(2/7,0,5/7) has about 0.863121. Both satisfy the same mean and both lose entropy relative to p*.</Prose>
    <Prose>The <a href="/learn/path/full-curriculum/exponential-families-sufficient-statistics">Exponential Families</a> lesson introduced p_η(x)=exp(ηT(x)−A(η)) on a fixed finite support with base weights 1. If a positive member has the desired statistic mean m, then E_q[log p_η]=ηm−A(η) is fixed across every q with that mean. The same KL argument certifies its entropy maximum. The finite example above uses T(x)=x and η=ln 2.</Prose>
    <Prose>For the three-count model, interior means 0&lt;m&lt;2 have positive maximizers. The means 0 and 2 force point masses at the endpoints and require limits η→−∞ or +∞; no finite η gives them. On a nonempty finite feasible simplex slice, continuity of entropy and compactness guarantee an attained maximum, and strict concavity gives uniqueness. This argument does not establish that every unbounded continuous moment problem has an attained finite optimizer.</Prose>
    <Example name="maximum"><Prose>The positive KL gaps match the entropy deficits. The boundary cleanup in this one numerical fixture removes floating subtraction noise near an exact zero; it does not replace the mathematical feasibility or support rules for arbitrary distributions.</Prose></Example>
    <details><summary>Deeper: nonuniform base weights change the objective</summary>
      <Prose>If p_η(x)=h(x)exp(ηT(x)−A(η)) with positive nonuniform h, then log p contains an extra log h term. For q with the required moment, KL(q∥p_η)=A−ηm−[H(q)+E_q log h]. Thus the certified objective is H(q)+E_q log h, entropy relative to those base weights. With r=h/Σh, this equals −KL(q∥r)+logΣh. It is generally not ordinary Shannon entropy.</Prose>
      <Prose>For outcomes 0,1,2 and base h=(1,2,1), the η=0 tilt is p=(1/4,1/2,1/4), with mean 1 and entropy 1.5 bits. The uniform distribution has the same mean and entropy log₂3≈1.584963, so p does not maximize ordinary H. But H(p)+E_p log₂ h=1.5+0.5=2, while uniform gives log₂3+1/3≈1.918296. The base-relative objective is the one the tilt maximizes.</Prose>
    </details>
    <Prose>This connects information theory to constrained modeling: a moment condition fixes the expected log price of an exponential-family candidate, and nonnegative KL turns that fact into an optimality certificate. No additional claim about “least assumptions” is needed for the mathematical conclusion.</Prose>

    <H2>10. Practise with new distributions and decisions</H2>
    <Prose>Try these without changing a worked example line by line. State the alphabet, the distribution that supplies each expectation, the log base and any required support or sampling assumptions. Open hints only when needed; the solutions give reasoning as well as numbers.</Prose>
    <Practice title="Information and code length are different quantities" hint="Use the five masses 1/2,1/8,1/8,1/8,1/8. A code may finish at one leaf while the other branch contains a complete two-level subtree." solution={<><Prose>The entropy is (1/2)×1+4×(1/8)×3=2 bits per symbol. One optimal prefix code is A=0, B=100, C=101, D=110, E=111, with average 2. Fixed-width coding needs three bits for five labels. Message AEEE uses 1+3+3+3=10 bits, or 2.5 per symbol, exceeding the source entropy on that message. Entropy predicts a source-weighted average, not a per-message guarantee.</Prose></>}>
      <Prose>A source has five labels with probabilities (1/2,1/8,1/8,1/8,1/8). Calculate its entropy and construct a prefix code attaining it. Compare fixed-width coding and the actual message AEEE. Explain why a 2.5-bit message average would not contradict the 2-bit source entropy.</Prose>
    </Practice>
    <Practice title="Compute a changed mismatch and diagnose the signed terms" hint="The uniform model charges one bit per outcome. Keep P=(3/4,1/4) as the weighting distribution." solution={<><Prose>Compute H₂(P) as −(3/4)log₂(3/4) − (1/4)log₂(1/4), obtaining about 0.811278 bits. H₂(P,Q)=1, so KL≈0.188722 bits. The terms are (3/4)log₂(3/2)≈0.438722 and (1/4)log₂(1/2)=−0.25. The negative term says the rarer outcome is priced more cheaply by Q; it is not a negative divergence on its own. Reversing gives about 0.207519 bits, so the direction matters.</Prose></>}>
      <Prose>For P=(3/4,1/4) and Q=(1/2,1/2), compute entropy, cross-entropy, both signed KL contributions and the reverse KL. Explain why taking the absolute value of each contribution would define a different quantity.</Prose>
    </Practice>
    <Practice title="Decode a changed stream and reject an ambiguous code" hint="Start again at the root each time 0,10,110 or 111 is complete. For the proposed code, ask whether 00 could mean more than one message." solution={<><Prose>Using A=0, B=10, C=110, D=111, the stream 110010111 decodes C A B D: 110|0|10|111. The alternative A=0, B=00, C=1 is not prefix-free and is not uniquely decodable: 00 could mean B or AA. A decoder that simply chooses the shortest match loses B. A correct changed implementation must reject that code and also reject a final unfinished 11 under the original code.</Prose></>}>
      <Prose>Decode 110010111 using the lesson's matched code. Then inspect A=0, B=00, C=1. Give a concrete ambiguous bitstream and explain why “always choose the shortest matching word” is not a repair.</Prose>
    </Practice>
    <Practice title="Repair a support and clipping claim" hint="Check the support inclusion separately in each direction. A probability floor must come with a defined normalized model." solution={<><Prose>For P=(0.8,0.2,0) and Q=(1,0,0), KL(P∥Q)=∞ because the second outcome occurs under P but is excluded by Q. KL(Q∥P)=−log₂0.8≈0.321928 bits. Reporting a finite clipped loss does not establish finite KL for the original Q. One explicit replacement is Q_ε=(1−2ε,ε,ε), 0&lt;ε&lt;1/2; it is a new normalized distribution, and its score should be identified as such. No contribution is charged for P's zero third coordinate.</Prose></>}>
      <Prose>For P=(0.8,0.2,0) and Q=(1,0,0), classify both KL directions. A colleague replaces log 0 with log 10⁻⁶ and reports “the exact original KL.” Explain the error and give one valid explicitly smoothed model.</Prose>
    </Practice>
    <Practice title="Use unequal context frequencies" hint="Average the two conditional entropies with 0.75 and 0.25. First calculate the marginal probability of Y=1." solution={<><Prose>P(Y=1)=0.75×0.2+0.25×0.8=0.35, so H(Y)=h₂(0.35)≈0.934068 bits. Both contexts have entropy h₂(0.2)=h₂(0.8)≈0.721928, hence H(Y|X)=0.721928. A model that uses the true conditional achieves that expected loss. A model using the true marginal in both contexts has expected loss 0.934068. The context's average reduction is about 0.212140 bits. Weighting the contexts equally would generally be wrong, even though this symmetric pair happens to have equal conditional entropies.</Prose></>}>
      <Prose>P(X=0)=0.75, P(Y=1|X=0)=0.2 and P(Y=1|X=1)=0.8. Calculate H(Y), H(Y|X), and the ideal expected losses with and without X. Explain which probabilities would need changing if the context frequencies shifted.</Prose>
    </Practice>
    <Practice title="Evaluate confidence with a consistent denominator" hint="Use all six correct-label probabilities. Model C has four probabilities above 0.5, while D has five." solution={<><Prose>C has accuracy 4/6 and mean loss −[4ln0.9+2ln0.4]/6≈0.375671 nats. D has accuracy 5/6 and mean loss −[5ln0.6+ln0.01]/6≈1.193216 nats. D wins accuracy but loses log loss because its lone error is extremely confident. The difference does not certify fairness, calibration or performance after distribution shift. A defensible comparison states the same six cases, binary threshold/no ties, arithmetic mean over cases and the decision objective.</Prose></>}>
      <Prose>On six shared binary test cases, C's correct-label probabilities are (0.9,0.9,0.9,0.9,0.4,0.4). D's are (0.6,0.6,0.6,0.6,0.6,0.01). Calculate accuracy and mean log loss. Which wins each measure, and what additional claim would this small test not justify?</Prose>
    </Practice>
    <Practice title="Compute an extreme but finite log loss" hint="Subtract the maximum first. The normalizer's log is approximately zero when the remaining exponentials underflow." solution={<><Prose>For scores (800,0,−800), shifted scores are (0,−800,−1600). The observed last-class loss is 1600+ln(1+e⁻⁸⁰⁰+e⁻¹⁶⁰⁰), numerically 1600 nats in the displayed precision. Adding −700 to every score leaves it unchanged. Taking log of a separately underflowed zero probability gives ∞ and destroys a finite-logit result. At ordinary scores (1,0,−1), direct and stable calculations should agree within tolerance; that is a useful independent implementation check.</Prose></>}>
      <Prose>Scores are (800,0,−800), and the last class occurs. Calculate the stable loss and explain why adding −700 to every score changes nothing. Give both an ordinary-score acceptance case and an underflow case for an implementation test.</Prose>
    </Practice>
    <Practice title="Preserve the physical experiment when changing units" hint="Uniform width w has differential entropy log₂w. An aligned bin label has entropy log₂N." solution={<><Prose>For a uniform interval of width 2 metres, h₂=1. In millimetres the numerical width is 2000, so h₂=log₂2000≈10.965784; the change is log₂1000. Four equal bins have H=2 bits in both units. Their widths are 0.5 m and 500 mm, so h−log₂Δ=2 either way. The probability of the first physical metre is 1/2 in both coordinates. For a continuous Q, the empirical finite set remains Q-null despite positive density at observed points; empirical-to-Q measure KL is ∞, while average negative log density can be finite.</Prose></>}>
      <Prose>X is uniform on [0,2] metres. Express its differential entropy in metres and millimetres; quantize it into four equal bins and compare bin-label entropy. Then explain why finite negative log density on observed samples does not establish finite KL from their empirical point-mass distribution.</Prose>
    </Practice>
    <Practice title="Certify a new constrained maximum" hint="For mean 1/2, q=(1/2+t,1/2−2t,t), 0≤t≤1/4. Put candidate probabilities proportional to 1,r,r² and solve their mean equation." solution={<><Prose>The equation (r+2r²)/(1+r+r²)=1/2 gives 3r²+r−1=0, with positive root r=(√13−1)/6. Normalize (1,r,r²) to get approximately (0.616204,0.267592,0.116204). Its mean is 1/2. The candidate's log probability is x ln r−ln(1+r+r²); every feasible q has the same expected log price. Thus KL(q∥p*)=H(p*)−H(q)≥0 and equality requires q=p*. Checking only the derivative or only a sampled plot would not by itself supply this global certificate. A base h=(1,2,1) would add E_q log h to the certified objective.</Prose></>}>
      <Prose>On outcomes 0,1,2, replace the required mean with 1/2. Derive the feasible slice, find the positive exponential-family candidate and prove it maximizes entropy. State what changes if the base weights become (1,2,1).</Prose>
    </Practice>
    <Prose>You are ready to move on when you can distinguish an outcome's surprise from a distribution's average, identify which distribution supplies a cross-entropy expectation, explain support infinities and code overhead, and choose a valid conditional or continuous interpretation. The next lesson, <a href="/learn/path/full-curriculum/mutual-information-information-bottleneck">Mutual Information &amp; Information Bottleneck</a>, asks how much one variable tells us about another and what useful information survives a compressed representation.</Prose>

    <Sources alternatives={<><Prose>For a slower spoken route into the coding idea, try <a href="https://www.youtube.com/watch?v=BtaVq2g17G0" target="_blank" rel="noreferrer">MIT 6.02 — George Verghese, “Overview: information and entropy”</a>. Its <a href="https://ocw.mit.edu/courses/6-02-introduction-to-eecs-ii-digital-communication-systems-fall-2012/resources/lecture-1-overview-information-and-entropy/" target="_blank" rel="noreferrer">official lesson page</a> supplies the recording and companion materials. This undergraduate lecture develops surprise, binary entropy and coding before Huffman construction; the accompanying slides were reviewed for fit, not the entire recording. It is an alternate explanation, not required homework.</Prose><Prose>For a proof-oriented written route, <a href="https://ocw.mit.edu/courses/6-441-information-theory-spring-2016/2243edffb30f57181ed97dcb77691580_MIT6_441S16_chapter_1.pdf" target="_blank" rel="noreferrer">MIT 6.441 Chapter 1: entropy and divergence</a> develops finite and general-measure definitions. It is graduate-level and more compressed than this lesson; use its sections 1.1–1.3 after the worked examples.</Prose></>}>
      <li><a href="https://ocw.mit.edu/courses/6-441-information-theory-spring-2016/67d8e68cd8fda55366e3f9f0a9465c17_MIT6_441S16_chapter_6.pdf" target="_blank" rel="noreferrer">MIT 6.441 Chapter 6: variable-length lossless compression</a> — Kraft inequality, prefix-code bounds and Huffman construction. Our ceiling-length program checks a feasible bound; it is not a Huffman implementation.</li>
      <li><a href="https://cs229.stanford.edu/notes-spring2019/MaxEnt.pdf" target="_blank" rel="noreferrer">Stanford CS229: Maximum Entropy and Exponential Families</a> — finite-support moment constraints and interior exponential form. Read with the positivity and boundary conditions in mind; our KL certificate supplies a global optimum proof rather than relying only on stationarity.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.nn.CrossEntropyLoss.html" target="_blank" rel="noreferrer">PyTorch 2.14 CrossEntropyLoss</a> — logits, class-index and soft-target shapes, reduction, weights, ignored classes and smoothing. The included CPU program was executed against this version; other frameworks may use different defaults.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.nn.BCEWithLogitsLoss.html" target="_blank" rel="noreferrer">PyTorch 2.14 BCEWithLogitsLoss</a> — the stable single-logit binary interface, useful when transferring the log-sum-exp idea to binary targets.</li>
    </Sources>
  </div>
};
