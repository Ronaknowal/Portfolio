import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { BorderEvidenceFigure, ChunkMatcherLab, KmpAlignmentLab, OverlapFigure, PalindromicPrefixFigure, PrefixBorderLab, PrefixCancellationFigure, RollingFingerprintLab, UnicodeCoordinatesFigure, ZReuseFigure } from '../../components/lesson-labs/StringMatchingLabs.jsx';
import { stringMatchingExamples } from '../string-matching-examples.js';
import stringMatchingPractice from '../practice/string-matching-prefix-functions-rolling-hashes.js';
import StringSearchLibraryBridge from '../../components/lesson-labs/StringSearchLibraryBridge.jsx';
function Exercise({
  title,
  children,
  hint,
  solution
}) {
  return <section className="string-exercise"><H3>{title}</H3>{children}<details><summary>Optional hint</summary><Prose>{hint}</Prose></details><details><summary>Solution and reasoning</summary>{solution}</details></section>;
}
export default {
  title: 'String Matching, Prefix Functions & Rolling Hashes',
  readTime: '~75 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot string-matching-lesson">
    <LessonIntro prerequisites="Use Python strings, lists, loops and functions. Arrays introduced indexing; Complexity and Correctness introduced cost and invariant arguments; Hashing separated candidate locations from identity. We introduce borders, rolling arithmetic and text units here." sections={[['1-decide-what-a-match-means', 'The matching contract'], ['2-see-a-prefix-inside-a-suffix', 'Reusable borders'], ['3-build-the-prefix-table', 'Prefix construction'], ['4-search-without-forgetting-evidence', 'KMP and overlap'], ['5-carry-state-across-chunks', 'Streaming matches'], ['6-use-borders-to-find-structure', 'Periods and palindromes'], ['7-roll-a-candidate-fingerprint', 'Rolling hashes'], ['8-query-fingerprints-and-state-their-guarantees', 'Substring queries and guarantees'], ['9-choose-the-symbols-you-are-matching', 'Unicode and exact DNA encoding'], ['10-another-exact-view-the-z-array', 'The Z algorithm'], ['11-build-and-explain-it-independently', 'Independent practice'], ['guided-dsa-practice', 'Guided LeetCode practice']]}>A log arrives in pieces. A short alarm signature may begin in one piece and end in the next, and its occurrences may overlap. Learn to recognize every occurrence without repeatedly comparing evidence you already have—and learn when a compact fingerprint is only a reason to look more closely.</LessonIntro>

    <H2>1. Decide what a match means</H2>
    <Prose>The <strong>text</strong> is the sequence you search; the <strong>pattern</strong> is the sequence you want to find. A <strong>substring</strong> occupies consecutive positions. Finding abc in axbyc is a different task: that is a subsequence, which may skip positions. Exact matching also differs from approximate matching, where a misspelling, insertion or deletion may be allowed.</Prose>
    <Prose>Write n for the text length and m for the pattern length. We use zero-based indices and half-open intervals: text[start:start+m] includes start and stops before start+m. A match is a start for which this interval equals the entire pattern. Unless a section says otherwise, return <strong>all starts in increasing order, including overlaps</strong>. This contract is more demanding than returning only the first match.</Prose>
    <OverlapFigure />
    <Prose>For text ababa and pattern aba, the starts are 0 and 2. The two occurrences share the a at index 2. If you jump three positions after the first match, you miss the second. A result is a position, not a command to delete or reserve the matched text.</Prose>
    <Prose>The empty pattern needs an explicit policy. Here it matches at all n+1 boundaries, from 0 through n. Thus searching ab for the empty pattern returns [0,1,2], and searching an empty text for an empty pattern returns [0]. If m&gt;n and m is nonzero, there is no complete window. Other APIs can choose different empty behavior; document the choice before comparing implementations.</Prose>
    <H3>A baseline you can trust</H3>
    <Prose>Try every possible start, then compare left to right until a mismatch or a complete match. The final possible start is n−m, so a Python range must stop at n−m+1. The loop's else runs when its inner loop finishes without break, including a zero-length pattern. This makes the boundary policy visible rather than accidental.</Prose>
    <RunnableExample example={stringMatchingExamples.baseline}><Prose>The first search makes seven comparisons. In aaaaaaaa / aaab, all five windows match three a symbols and fail on the fourth, totaling 20 comparisons. The repeated prefix is exactly the information the baseline throws away. A pattern longer than the text creates no valid starts and no comparisons.</Prose></RunnableExample>
    <Prose>For nonempty m≤n, the worst-case comparison count is (n−m+1)m, attained by a repeated near-match such as this one. This is O(nm), with O(1) working state beyond the input and returned starts. The baseline remains useful for small inputs, tests and unusual equality contracts. A language's optimized built-in search may outperform a teaching implementation; asymptotic reasoning is not a substitute for measuring your actual workload.</Prose>
    <Callout title="Declare the unit before counting">Python str indexing counts Unicode code points. The browser investigations use Array.from to use the same unit. One code point is not necessarily one visible character or one byte. Our basic cost model treats indexing and comparing individual code points as O(1). We revisit encodings and transformed offsets in section 9.</Callout>

    <H2>2. See a prefix inside a suffix</H2>
    <Prose>Suppose the matched part of a pattern is ababa, but its next symbol fails. Could a new match already have begun inside those five symbols? Yes: the last aba is also the first aba. We can retain three matched symbols without asking the text to prove them again. We can also retain a single a if the three-symbol candidate later fails.</Prose>
    <Prose>A <strong>prefix</strong> starts at position 0; a <strong>suffix</strong> ends at the sequence's end. A <strong>proper border</strong> is a prefix that is also a suffix, excluding the entire sequence. The empty sequence is a border of length 0. Prefix and suffix regions are allowed to overlap. For ababa the nonempty border lengths are 3 and 1; length 5 is excluded because it would leave the same failed alignment in place.</Prose>
    <LessonTable caption="Borders are equality evidence, measured in lengths" headers={['Sequence', 'Longest proper border', 'Length', 'What the next candidate retains']} rows={[['a', 'empty', '0', 'No nonempty overlap'], ['abab', 'ab', '2', 'Its final ab already equals its initial ab'], ['ababa', 'aba', '3', 'The last three symbols can start a new candidate'], ['aaaa', 'aaa', '3', 'Overlapping regions are still valid evidence'], ['abc', 'empty', '0', 'No nonempty prefix also ends the sequence']]} />
    <BorderEvidenceFigure />
    <Prose>The <strong>prefix function</strong>, written π, stores this information for every prefix of one sequence. <Code>π[i]</Code> is the length of the longest proper border of the prefix ending at i. For pattern ababaca, the full table is [0,0,1,2,3,0,1]. The entry π[4]=3 describes ababa, not the entire pattern and not a position in the text.</Prose>
    <Prose>Why are shorter candidates found by another table lookup? If b is a border of a sequence and a shorter k is also a border, the first k symbols equal the final k symbols inside that border b. Therefore k is a border of b. Starting at the longest border, <Code>b = π[b−1]</Code> visits the next longest, then the next, until 0. This nested chain accounts for the possible reused alignments without testing every length.</Prose>
    <Checkpoint prompt="Why is π[0] zero even if the first symbol occurs many times later?"><Prose>π[0] describes only the one-symbol prefix. Its full length is excluded, leaving only the empty proper border. Later occurrences have no role in that entry.</Prose></Checkpoint>

    <H2>3. Build the prefix table</H2>
    <Prose>Build the table from left to right. At position i, the earlier entries are known. Let j=π[i−1], the longest border of the preceding prefix. To extend it, compare pattern[i] with pattern[j]. If they are equal, the border grows to j+1. If they differ and j&gt;0, use π[j−1] to try the next shorter border against the <em>same</em> pattern[i]. If j reaches 0, compare with the first pattern symbol; equality gives 1 and inequality gives 0.</Prose>
    <Prose>For ababaca, the prefix ababa has j=3. To append c at i=5, compare it with pattern[3]=b: mismatch. Fall to π[2]=1 and compare with pattern[1]=b: mismatch. Fall to π[0]=0 and compare with pattern[0]=a: mismatch. Store π[5]=0. The next a matches pattern[0], giving π[6]=1. No candidate border was silently skipped.</Prose>
    <PrefixBorderLab />
    <RunnableExample example={stringMatchingExamples.prefix}><Prose>The table records lengths even though its storage is indexed by positions. Access π[j−1] only when j&gt;0; otherwise Python's negative indexing would quietly access the wrong entry. The sequence can be a string or a list of comparable tokens, which will become useful for unique separators.</Prose></RunnableExample>
    <H3>Why the nested loop is linear</H3>
    <Prose>Initialization of each iteration carries over the previous border length. Successful extensions increase j by at most one per new position. Every fallback strictly decreases j, and j never becomes negative. Across the whole construction, total decreases cannot exceed the accumulated increases. There are at most m−1 extensions and O(m) fallback steps, plus one final comparison per position. This is O(m) time and O(m) table space under the symbol-cost model.</Prose>
    <Prose>The argument counts progress across the entire execution, as amortized analysis did earlier; it does not assume that each individual position needs one comparison. A single difficult c can cause several fallbacks, paid for by earlier extensions. The table construction is deterministic: there is no average-case input or random hash assumption.</Prose>

    <H2>4. Search without forgetting evidence</H2>
    <Prose>Precompute π for the pattern. During the text scan, keep q, the number of initial pattern symbols currently matched by a suffix of the consumed text. Before the next character is compared, q&lt;m: a complete match has already been reported and replaced by its proper border. Text before this suffix need not be reread.</Prose>
    <Prose>Compare the next text symbol with pattern[q]. On equality, consume the symbol and increase q. On a mismatch with q&gt;0, replace q with π[q−1] and retry the <strong>same unread symbol</strong>. On a mismatch with q=0, consume that symbol; it cannot start the pattern. This is the prefix-function form of the Knuth–Morris–Pratt algorithm, usually called <strong>KMP</strong>.</Prose>
    <Prose>When q reaches m just after consuming index i, the start is i−m+1. Report it, then set q=π[m−1]. For aaa, that leaves q=2, because the last two a symbols of a completed match may begin another one. Resetting q to zero is correct for some non-overlap contracts but wrong for the all-overlap contract here.</Prose>
    <KmpAlignmentLab />
    <RunnableExample example={stringMatchingExamples.kmp}><Prose>The near-match before ababaca is handled without moving the text scan backward; the default text produces start 2. The repetitive aaa case reports [0,1,2], and the emoji example reports code-point positions [1,3]. The empty case is handled before indexing pattern[q].</Prose></RunnableExample>
    <H3>What licenses the skipped comparisons?</H3>
    <Prose>Maintain two facts. Every reported start really matches, and q describes the longest proper pattern prefix that is a suffix of the text consumed so far after any full match has been reported. If the next symbol fails, a shorter surviving candidate must be a border of the already matched prefix. The π chain lists these in decreasing length. All longer alternatives have been excluded; retrying the same symbol makes the next possibility explicit. An equal symbol extends the surviving evidence by one.</Prose>
    <Prose>These facts give both soundness and completeness. Soundness follows because a report occurs only after m equal symbols. Completeness follows because no viable suffix-prefix candidate is discarded without a mismatch or an already known border restriction. Each mismatch fallback makes q strictly smaller, so retrying a character terminates.</Prose>
    <H3>Account for work and outputs separately</H3>
    <Prose>The scan consumes n symbols. Each successful extension raises q by one, while fallbacks and post-report retention only lower it. Thus total fallback work is O(n), and preprocessing adds O(m). Reporting z start indices costs O(z); total time is O(n+m+z), and working space is O(m) plus O(z) if results are stored. For nonempty patterns z≤n, but displaying z copied substrings of length m would change the output cost to Θ(zm). State what the result contains before claiming a bound.</Prose>
    <Callout title="Different tables can implement correct KMP variants">You may see failure arrays, next arrays, −1 sentinels or finite-state transition tables. Their meanings and indexing can differ. An optimized next table can skip some comparisons that this simpler π version performs. Translate the invariant before reusing code; mixing a one-based next table with zero-based border lengths is not a harmless renaming.</Callout>

    <H2>5. Carry state across chunks</H2>
    <Prose>A file reader, socket or streaming parser chooses chunk sizes for transport and buffering. Those boundaries are unrelated to the pattern. Suppose pattern aba arrives inside decoded chunks xxa, an empty chunk, b, and aba. The state after xxa is q=1; after b it is q=2. The first a in the last chunk completes the occurrence beginning at global offset 2. Its final aba also completes a second occurrence beginning at 4.</Prose>
    <Prose>Keep the pattern, its prefix table, q and the number of symbols consumed globally. A new chunk continues the same loop; it does not reset q. A match ending after the global count becomes offset has start offset−m. The existing invariant applies across any chunk partition because processing a concatenation performs the same sequence of symbol transitions.</Prose>
    <ChunkMatcherLab />
    <RunnableExample example={stringMatchingExamples.stream}><Prose>feed returns only starts newly discovered by that delivery. Empty chunks return no duplicate report. For an empty pattern, the first feed emits boundary 0 once, and later consumed symbols each add their new boundary; this API choice avoids duplicating seam boundaries. The lab displays initial boundary 0 immediately as an initial-state annotation.</Prose></RunnableExample>
    <Prose>This implementation retains O(m) pattern state plus each call's returned results, not the complete text. The overall work remains O(n+m+z) over all chunks. KMP's per-symbol while loop can still have several fallbacks on one delivery; linear total work is not a hard real-time latency promise for every symbol.</Prose>
    <Prose>The input here is already decoded. A UTF-8 byte sequence for an emoji can be split between network packets. Decode with a stateful incremental decoder before feeding code points, or deliberately match bytes and report byte offsets. Similarly, do not normalize each chunk independently and assume the result equals normalizing the entire stream: a combining mark in the next chunk can alter the preceding letter. Matching state and decoding/normalization state are different responsibilities.</Prose>
    <Checkpoint prompt="Could keeping only the last m−1 text symbols across chunks also work?"><Prose>For nonempty m, keeping that suffix and searching suffix+new chunk can recover seam-crossing matches, provided you translate offsets and suppress old reports. It is a valid alternative. A naive rescan can repeat work across many small chunks; KMP retains the sufficient prefix state and a proven total comparison bound. Neither approach should invent a separator at the seam.</Prose></Checkpoint>

    <H2>6. Use borders to find structure</H2>
    <H3>A period can end in a partial copy</H3>
    <Prose>A positive p is a <strong>period</strong> of a nonempty length-m string if symbol[i]=symbol[i+p] whenever both positions exist. Equivalently, its prefix and suffix of length m−p are equal. The longest proper border therefore gives the smallest period: p=m−π[m−1]. This equivalence proves minimality; any smaller period would create a longer border.</Prose>
    <Prose>For ababa, the longest border is aba, so p=5−3=2. It repeats the ab rhythm but ends halfway through the last copy. Being made from two or more <em>whole</em> copies requires p&lt;m and m divisible by p. ababab passes; ababa does not. The empty string has no smallest positive period under this lesson's returned contract, so the function returns None and false.</Prose>
    <RunnableExample example={stringMatchingExamples.structure}><Prose>The border chain lists every nonempty proper border in decreasing order. Repetition detects a compact repeating schedule or motif candidate; it does not prove that a repeated DNA word has biological importance or that a periodic log is caused by a particular system.</Prose></RunnableExample>
    <H3>A useful reduction: prepend the fewest symbols to make a palindrome</H3>
    <Prose>A palindrome equals its reversal. If text is a palindromic prefix followed by a remaining suffix, prepend the reversal of that suffix. For abac, the prefix aba is already palindromic; moving a mirrored c in front gives cabac. To minimize the number added, retain the <strong>longest palindromic prefix</strong>, not the longest palindrome somewhere in the middle.</Prose>
    <PalindromicPrefixFigure />
    <Prose>Let R be the reversed text. In the sequence text + separator + R, a border of length k≤m means that the first k symbols of text equal the last k symbols of R. Those last symbols are the reverse of the first k symbols of text, so that prefix is palindromic. A separator unequal to every text symbol prevents a proper border from crossing it and producing k&gt;m. Use a unique object in a list, rather than assuming # cannot occur in real input.</Prose>
    <Prose>Why is the construction shortest? A solution with t prepended symbols leaves the first m−t symbols of the original text paired with each other in the final palindrome, so that retained prefix must be palindromic. The longest such prefix has length k, requiring t≥m−k. Prepending the reverse of text[k:] achieves exactly m−k, meeting the bound. Taking the whole reversed text is valid but generally longer than necessary.</Prose>
    <RunnableExample example={stringMatchingExamples.palindrome}><Prose>The table is built over a list containing strings and one unique sentinel object. The a#a fixture checks that a literal punctuation character is safe. This code reverses code points; it is not a user-visible grapheme palindrome service. Its total time and extra space are O(m), including the constructed output.</Prose></RunnableExample>

    <H2>7. Roll a candidate fingerprint</H2>
    <Prose>KMP keeps exact equality evidence. A <strong>fingerprint</strong> instead summarizes a window by a number. Unequal fingerprints rule out equal strings, provided the same encoding and parameters were used. Equal fingerprints only identify a candidate: different strings may collide. The identity lesson from hash tables applies again.</Prose>
    <Prose>Give each code point its integer value c. For a length-m window, a polynomial fingerprint with base B and modulus Q is computed left to right by <Code>h = (h*B+c) % Q</Code>, beginning at 0. This represents c₀B^(m−1)+c₁B^(m−2)+…+cₘ₋₁ modulo Q. The leftmost symbol has the largest weight. Reducing after every step preserves the final remainder without storing the enormous full integer.</Prose>
    <H3>Remove, shift, append</H3>
    <Prose>Adjacent windows share m−1 symbols. Precompute w=B^(m−1) mod Q. Subtract the outgoing code point times w, multiply the remaining polynomial by B to shift its positions, then append the incoming code point. Reduce modulo Q: <Code>next = ((h − outgoing*w)*B + incoming) % Q</Code>. Python's remainder with positive Q is nonnegative; languages with signed remainders may need an explicit positive-modulo normalization.</Prose>
    <Prose>With B=3 and Q=7, ad has fingerprint (97×3+100) mod 7=6. ba has (98×3+97) mod 7=6 too. Their first symbols differ. This tiny collision is a feature of the lesson: it exposes why accepting a fingerprint as a match changes the correctness contract.</Prose>
    <RollingFingerprintLab />
    <RunnableExample example={stringMatchingExamples.rolling}><Prose>The audit lists only hash-equal candidates, with an exact-equality result and the number of symbol checks. adbaad / ba reports only start 2 despite four candidate hits. The implementation checks in place and stops at the first unequal symbol instead of allocating each candidate substring. Its larger default modulus still needs verification; a familiar large prime does not turn a fingerprint into identity.</Prose></RunnableExample>
    <H3>Exact answers can still have expensive verification</H3>
    <Prose>Let C be the number of windows with an equal hash. Rolling arithmetic and initialization cost O(n+m) for fixed-size modular arithmetic, while verification costs at most O(Cm). Thus this all-match implementation is O(n+m+Cm), worst-case O(nm). False collisions can make C large, but so can <em>true</em> matches. In aaaaa / aaa, all three windows really match and all nine symbol checks are required by this implementation.</Prose>
    <Prose>If you stop at the first true occurrence, only one successful full verification is needed. With suitable randomized hashing assumptions and bounded false-candidate work, that can yield expected-linear search. It does not justify claiming unconditional linear work for verifying every true hit. KMP already supplies deterministic O(n+m+z) all-start search without rereading each full occurrence.</Prose>
    <Callout title="Correctness and probability are separate decisions">Checking actual symbols on every hash hit gives an exact-answer algorithm: collisions affect work, not the reported set. Skipping the check gives a fingerprint-based answer that can be wrong. The terms Las Vegas and Monte Carlo are often used for randomized versions of these choices. Our fixed toy parameters are deterministic, so they provide no randomness-based error guarantee.</Callout>

    <H2>8. Query fingerprints and state their guarantees</H2>
    <Prose>A rolling window is useful when adjacent equal-length regions are queried in order. For many arbitrary substring queries on one fixed text, precompute prefix fingerprints H and powers. Let H[0]=0 and H[i+1]=(H[i]B+cᵢ) mod Q. The fingerprint of [l,r) is <Code>(H[r] − H[l]*B^(r−l)) % Q</Code>: subtract the older prefix after shifting its weights to the positions it occupies inside H[r].</Prose>
    <Prose>The shift matters. Simply subtracting H[l] leaves the earlier symbols at the wrong powers. Return the substring length with its fingerprint, since different lengths have different identity contracts. Equal length and equal fingerprint still need verification if the answer must be exact. This resembles prefix-sum cancellation from range queries, but the preceding contribution has a multiplicative positional weight.</Prose>
    <PrefixCancellationFigure />
    <RunnableExample example={stringMatchingExamples.substring}><Prose>The two ab regions both have signature (2,3105). The ad/ba collision has the same length and hash but unequal contents. Preprocessing takes O(n) time and space; each bounded-arithmetic fingerprint query takes O(1). A subsequent exact substring comparison is O(length) in the worst case and may also allocate slices in the shown demonstration.</Prose></RunnableExample>
    <H3>What a random-base guarantee actually assumes</H3>
    <Prose>For an optional probabilistic argument, fix two unequal strings of the same length m <em>before</em> choosing a base. Choose a prime Q larger than every encoded symbol and choose B uniformly from 2…Q−1. The difference of their hash polynomials is a nonzero polynomial over arithmetic modulo Q, of degree at most m−1. It is nonzero because at least one pair of symbols differs by a nonzero residue.</Prose>
    <Prose>A nonzero degree-d polynomial over a field has at most d roots: a root supplies one linear factor, and repeatedly factoring cannot remove more roots than the degree. Hence at most m−1 of the Q−2 available bases cause a collision. The probability is at most min(1,(m−1)/(Q−2)). This is an upper bound for this precise random experiment, not a measured collision rate for the lab.</Prose>
    <Prose>For W fixed unequal window comparisons, the probability of any collision is at most the sum of their individual bounds, capped at 1. Independence between window events is unnecessary for that union bound. Two independently chosen fingerprints can reduce a fixed pair's collision probability by multiplying their bounds, but two arbitrary constants are not evidence of independence. Inputs chosen after learning the base require a different adversarial analysis.</Prose>
    <Prose>The toy Q=7 is intentionally smaller than many code points, so it does not satisfy this random-base setup. The complete exact verifier remains correct anyway. Fingerprints are also not cryptographic authentication: these algebraic summaries are designed for efficient comparisons, not resistance to a malicious author creating a chosen collision.</Prose>
    <H3>Choose the method for the query</H3>
    <LessonTable caption="Same text, different queries and guarantees" headers={['Need', 'Useful starting point', 'Qualification']} rows={[['One exact pattern, all overlaps or a stream', 'Prefix-function KMP', 'O(n+m+z) symbol work; retains O(m) pattern state'], ['Many substring-equality candidates in one static text', 'Prefix fingerprints plus exact checks', 'O(n) preprocessing; O(1) signatures, not O(1) certified equality'], ['Words sharing a dictionary prefix', 'Trie from the earlier lesson', 'Indexes a set of words; a different query from one pattern inside a stream'], ['Many patterns in one stream', 'A multiple-pattern automaton such as Aho–Corasick', 'Combines trie states with suffix fallback; requires its own construction and output accounting'], ['Many queries against a fixed long text', 'Suffix-based text indexes', 'Preprocess the text rather than each separate pattern'], ['Misspellings, insertions or deletions', 'Alignment/edit-distance states', 'Changes equality into an optimization or threshold problem; revisit dynamic programming']]} />
    <Prose>These last directions name changed problems, not completed implementations. The concrete tools developed here let you identify the missing state and choose the next specialization rather than hoping a hash or prefix array solves every string task.</Prose>

    <H2>9. Choose the symbols you are matching</H2>
    <Prose>The same visible text can occupy different coordinate systems. In A🙂e followed by a combining acute accent, Python sees four code points. UTF-16 uses five code units; UTF-8 uses eight bytes. A UI selection API, a file offset and a Python slice can therefore point to different places when given the same integer.</Prose>
    <UnicodeCoordinatesFigure />
    <Prose>Canonical normalization can make differently encoded but canonically equivalent text comparable. For this example, NFC combines e plus the accent into é. Searching the raw code-point sequence for the single precomposed é fails; searching the NFC text succeeds at code-point index 2. That result belongs to the <strong>normalized text</strong>. A general highlighting tool needs a deliberate mapping back to original spans because normalization can combine or reorder code points.</Prose>
    <Prose>Case folding can expand a symbol: Straße has six code points, while its case-folded form strasse has seven. Visible-character matching adds another layer: an emoji sequence or a letter plus marks may be one user-perceived grapheme made from several code points. Choose and document the unit, transformation and offset space before reusing a string algorithm. None of KMP's correctness reasoning silently supplies locale-sensitive matching or grapheme segmentation.</Prose>
    <RunnableExample example={stringMatchingExamples.unicode}><Prose>The final pair of booleans demonstrates a seam error independently of KMP: normalizing e and its following combining accent together gives é, while normalizing each chunk and then concatenating does not. The full code uses Python's standard Unicode implementation; no hand-maintained normalization table is embedded in the lesson.</Prose></RunnableExample>
    <H3>An exact compact encoding for a restricted alphabet</H3>
    <Prose>For fixed-length DNA words drawn only from A,C,G,T, use digits 0,1,2,3 in base 4. A width-k word has exactly k digits and at most 4^k possibilities, so the integer in 0…4^k−1 uniquely identifies it. Leading A symbols are safe because the width is fixed. Comparing variable-length words would require retaining their lengths too.</Prose>
    <Prose>To slide by one symbol, keep the last k−1 digits using <Code>code % 4**(k−1)</Code>, multiply by 4 and add the new digit. Unlike the small-modulus general fingerprint, this discards only the symbol leaving the window; it retains every digit of the current fixed-width word. For k=3, ACG is 0×16+1×4+2=6. Sliding to CGT gives (6 mod 16)×4+3=27, an exact new code.</Prose>
    <RunnableExample example={stringMatchingExamples.dna}><Prose>seen records words encountered before; emitted ensures that a word appearing three times is returned only once. The function rejects symbols outside its declared alphabet instead of treating an unknown nucleotide as an arbitrary digit. A biological motif search may need ambiguous bases, reverse complements or approximate matches; those are additional policies, not conclusions supplied by repeated exact words.</Prose></RunnableExample>
    <Prose>For the platform's fixed k=10, the code uses 20 bits, so bounded word arithmetic and expected set lookup give O(n) scan work plus returned strings. For arbitrary k, Python's integer width grows with k, so arithmetic is no longer O(1) independent of k. Copying r distinct reported words costs O(rk), and stored codes and results require space. A collision-free representation does not remove the cost of storing and returning its information.</Prose>

    <H2>10. Another exact view: the Z array</H2>
    <Prose>π[i] asks how much of the prefix also <em>ends</em> at i. A Z array asks how much of the prefix matches a substring that <em>starts</em> at i. These are different coordinates for reusable evidence. This optional branch is useful when a task asks for common-prefix lengths at many positions, or when you want a second exact matching algorithm to compare with KMP.</Prose>
    <details className="string-advanced"><summary>Build the Z algorithm and its evidence box</summary>
      <Prose>Define Z[i] as the longest length such that S[0:Z[i]] equals S[i:i+Z[i]], for i&gt;0. We set Z[0]=0 by convention; another source may choose the full length. Remember the previously found match box [L,R) whose right boundary reaches farthest. By construction S[L:R] equals the prefix S[0:R−L].</Prose>
      <Prose>If i≥R, the box supplies no reusable symbols: compare from length 0. If i&lt;R, i corresponds to mirror position k=i−L in the prefix. The box proves at least <Code>min(Z[k], R−i)</Code> matching symbols. If Z[k]&lt;R−i, its known mismatch lies inside the box too, so Z[i]=Z[k] exactly. If it reaches the boundary, the box says nothing about what follows R; compare additional symbols there until mismatch or end.</Prose>
      <ZReuseFigure />
      <Prose>After computing Z[i], replace [L,R) with [i,i+Z[i]) if it extends farther right. The implementation below makes one harmless explicit mismatch check even in the strictly-inside case. Successful comparisons beyond existing evidence advance the farthest boundary, which advances at most n positions overall. At most one failed comparison ends each iteration. Thus total work is O(n), not O(n²), even though the code contains a nested while loop.</Prose>
      <Prose>To find a pattern, construct pattern + a unique sentinel + text as a sequence. At any position in the text portion, Z equals m exactly when a full pattern starts there; the sentinel prevents a match from extending through the boundary between the pattern prefix and its separator. Subtract m+1 from that combined-sequence index to recover the original text start.</Prose>
      <RunnableExample example={stringMatchingExamples.z}><Prose>The # example works because the sentinel is an object that cannot equal an input character. Z search takes O(n+m+z) time and O(n+m) extra array/sequence space here. KMP is more convenient when the text cannot be retained and only streaming prefix state is desired. The two methods preserve different shapes of evidence while solving the same exact occurrence contract.</Prose></RunnableExample>
    </details>

    <StringSearchLibraryBridge />
    <H2>11. Build and explain it independently</H2>
    <Prose>Run each program in a fresh Python file with Python 3 and no third-party packages. For example, save the complete KMP program as string_match.py and run <Code>python -X utf8 string_match.py</Code>. Run it, inspect its state changes, then compare its output with the displayed result. The investigations run in this page; the Python examples run in your own environment. The checks below require an explanation as well as a result.</Prose>
    <Exercise title="1. Build a table that falls back more than once" hint="At i=5, compare with pattern[j] before increasing j. For the changed final c, write every shorter length tested against that same c." solution={<Prose>For aabaaab the table is [0,1,0,1,2,2,3]. At i=5 (the third a in the later run), candidate j=2 expects b and falls to π[1]=1, which expects a and extends to 2. At the final b, j=2 extends directly to 3. The tempting assumption that repeated a symbols always extend the longest border fails at i=5; trace the indices, not the visual rhythm. Changing the final b to c tests lengths 2, 1 and 0, all unsuccessfully, so the last entry becomes 0. Each fallback reuses established equality and keeps the new symbol at the same i.</Prose>}><Prose>Compute π for aabaaab without running code. Identify a position that falls back before extending, and explain why decreasing i would be unnecessary. Then change the final symbol to c and list the fallback lengths at that position.</Prose></Exercise>
    <Exercise title="2. Diagnose a lost overlap and a lost seam" hint="Distinguish a completed occurrence from the proper suffix that may start another occurrence." solution={<><Prose>aaaaa / aaa must report [0,1,2]. Clearing q after the first report retains no evidence and misses starts 1 and 2. Keeping q=π[2]=2 allows each additional a to complete the next occurrence.</Prose><Prose>For chunks [ab,a] and pattern aba, the first chunk ends with q=2; the next a reports start 0. Resetting at the seam loses it. Verify all partitions of a short text, including empty chunks, against a single whole-text baseline. A seam is not a mismatch.</Prose></>}><Prose>A detector sets q=0 after a match and at the start of each chunk. Give one input exposing each error independently, repair both rules and state what q means after your repair.</Prose></Exercise>
    <Exercise title="3. A short period is not always a repeating block" hint="Relate a shift to a border, then check whether it tiles the full length." solution={<Prose>For abcab, the longest border is ab, so the shortest period is 5−2=3. It is not a concatenation of whole copies because 3 does not divide 5. Appending c produces abcabc, whose shortest repeating block is abc with two copies. A test that only checks π[-1]&gt;0 would incorrectly classify abcab as whole-copy repetition.</Prose>}><Prose>Find the shortest period of abcab, decide whether it consists of whole copies, and append as few symbols as possible to complete two copies of its shortest-period block.</Prose></Exercise>
    <Exercise title="4. Separate hash collisions from verification work" hint="A true match costs a full verification even when no false collision ever occurs." solution={<><Prose>Under B=3,Q=7, ad and ba both hash to 6 but are unequal; the exact verifier rejects ad. This disproves “same hash means same string.” For text a repeated n times and pattern a repeated m times, there are n−m+1 true hits and each verification reads m symbols, giving (n−m+1)m checks. Randomizing the hash cannot make an equal string fail its own hash.</Prose><Prose>Use KMP when deterministic all-occurrence scan work is the priority. If retaining hashes for other queries, document verification work separately and do not report a fingerprint collision rate as an exact matching guarantee.</Prose></>}><Prose>Give a concrete false-candidate example, then an input with no false candidates where verified rolling search still does Θ(nm) work for m around n/2. Explain which algorithm from this lesson avoids that repeated verification.</Prose></Exercise>
    <Exercise title="5. Design a highlighting contract" hint="An offset describes a representation. Transforming the text can change the number and order of code points." solution={<Prose>Searching NFC(A🙂e◌́) for é returns normalized code-point start 2, with the transformed match covering one code point. In the raw string the corresponding e plus accent covers code-point interval [2,4); its UTF-8 bytes begin after A's one byte and the emoji's four bytes, so the raw byte interval is [5,8). A highlight API must say which of these spaces it expects and retain a transformation-to-source span map if searching transformed text. Independently normalizing chunks does not generally produce the same text.</Prose>}><Prose>A server searches normalized text but a viewer highlights raw UTF-8 input. Explain why forwarding only the normalized integer start is insufficient. Work through the displayed A🙂e plus combining-accent example and specify the raw span.</Prose></Exercise>
    <Exercise title="6. Explain the palindrome construction and the separator" hint="A proper border cannot contain the unique separator in two different positions. The remaining center must equal its reverse." solution={<Prose>For cabca the longest palindromic prefix is c, length 1, so reverse the remaining abca to acba and prepend: acbacabca. Any shorter prepend would require a longer palindromic prefix, which direct checks rule out. A literal # can occur in arbitrary input; a fresh object inside a sequence cannot equal any string symbol. This proves a coordinate bound on the border rather than hoping the separator is rare.</Prose>}><Prose>Find the shortest prepend-only palindrome for cabca. Prove minimality from retained-prefix length and explain why your implementation remains correct if the input contains #.</Prose></Exercise>
    <Exercise title="7. Capstone: an exact detector that survives delivery changes" hint="Start with a slicing oracle over the complete decoded input. Treat each chunk as a delivery of more symbols, not a new search." solution={<><Prose>A successful submission exposes feed(chunk), returns new global starts, documents code-point offsets and empty behavior, and retains pattern/π/q/offset. Its combined reports must equal all valid starts from a direct slice comparison. Include empty text/pattern, m&gt;n, repeated overlaps, a match split at every internal position and a non-ASCII code point. For each length-N short text, enumerate cut choices between symbols; insert empty chunks too.</Prose><Prose>Explain q's suffix-prefix invariant and O(n+m+z) total work. Demonstrate that clearing q at a seam fails your test suite. A second implementation using the last m−1 symbols can also be accepted if its duplicate-report and cost policies are correct. Extension: feed copies of abcd while searching for cdabcdab; the minimum is three copies. Any possible start can be shifted into the first copy, so ceil(m/len(block))+1 copies suffice to rule out all candidates. Test the ceiling alone and the extra copy, rather than extending forever.</Prose></>}><Prose>Implement the streaming API from a blank file. Build an independent oracle and adversarial partitions. Add one intentionally incorrect implementation and show which case detects it. Then solve the repeated-block extension without allocating an unbounded text.</Prose></Exercise>
    <Prose>Revisit one exercise later without notes, change its input contract and compare an alternative. Mastery is the ability to explain which evidence survives a change, not the ability to reproduce one prefix loop. The next module topic, <strong>Reductions, P, NP & Computational Intractability</strong>, asks a broader version of the same design question: what can solving one problem tell us about another, and what does that imply about feasible computation?</Prose>
    <DsaPractice practice={stringMatchingPractice} />
    <Sources alternatives={<div><p><strong>Another route through the ideas:</strong></p><ul>
      <li><a href="https://users.encs.concordia.ca/~chvatal/notes/kmp.html" target="_blank" rel="noopener noreferrer">Vašek Chvátal: KMP lecture notes</a> — a written geometric derivation of mismatch shifts and linear comparison work. Read after the border lab; its one-based optimized next array uses a different convention from our π lengths.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-9-table-doubling-karp-rabin/" target="_blank" rel="noopener noreferrer">MIT OpenCourseWare: Lecture 9, Table Doubling and Karp–Rabin</a> — Erik Demaine's video route connects the previous hashing lesson to rolling search. The first portion revisits resizing. The resource page and matching written notes were checked; the video was not watched for this lesson.</li>
      <li><a href="https://www.cs.cmu.edu/~ckingsf/bioinfo-lectures/zalg.pdf" target="_blank" rel="noopener noreferrer">CMU: String Matching Z slides</a> — compact diagrams for prefix-at-start evidence and box reuse, useful after the optional Z section. Translate its inclusive, one-based boxes to this lesson's half-open indices.</li>
    </ul></div>}>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/160b3b5f9da2e03815ca1e6ee0dba62a_MIT6_006F11_lec09.pdf" target="_blank" rel="noopener noreferrer">MIT 6.006 lecture 9 typed notes</a> — pages 3–5 explain candidate verification and the rolling remove/append operations. Our complete code makes the final window, empty policy and all-hit verification cost explicit.</li>
      <li><a href="https://algs4.cs.princeton.edu/53substring/" target="_blank" rel="noopener noreferrer">Sedgewick and Wayne: Substring Search</a> — Java implementations and further border, period and all-match exercises. The page identifies itself as under construction; use it for additional perspectives, not as a replacement for the proofs here.</li>
      <li><a href="https://docs.python.org/3/howto/unicode.html" target="_blank" rel="noopener noreferrer">Python Unicode HOWTO</a> — code points, encodings, normalization and case folding. The current documentation was checked on 10 September 2026; examples here were verified on Python 3.12.14.</li>
    </Sources>
  </div>
};
