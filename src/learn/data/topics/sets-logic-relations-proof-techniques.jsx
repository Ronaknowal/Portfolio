import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { DiagonalSubsetLab, FunctionImageFigure, ImplicationFigure, OddSquareLab, PartialOrderFigure, ProofScopeFigure, QuantifierWitnessLab, RelationPropertiesLab, SetMembershipFigure, SetRegionsLab, TruthArgumentLab } from '../../components/lesson-labs/SetsLogicLabs.jsx';
import { setsLogicExamples } from '../sets-logic-examples.js';
function LogicExample({
  example
}) {
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /></>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="sets-logic-practice"><H3>{title}</H3><Prose>{question}</Prose>
    <details><summary>Get a hint</summary><Prose>{hint}</Prose></details>
    <details><summary>Show explained solution</summary>{children}</details>
  </section>;
}
export default {
  title: 'Sets, Logic, Relations & Proof Techniques',
  readTime: '~85 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot sets-logic-lesson">
    <LessonIntro prerequisites="Basic arithmetic is enough. We introduce the symbols, functions and proof conventions locally. Algebra, Functions, Exponentials & Logarithms gives more practice with equation transformations. Python examples are optional; the investigations work here without installation." sections={[['1-say-exactly-which-objects-you-mean', 'Sets and their elements'], ['2-build-conditions-with-set-operations', 'Regions, products and subsets'], ['3-read-the-direction-of-a-claim', 'Logic and countermodels'], ['4-follow-the-order-of-the-witnesses', 'Quantifiers and empty domains'], ['5-build-a-proof-one-justified-step-at-a-time', 'Proof methods and scope'], ['6-move-between-sets-and-functions', 'Element proofs, images and preimages'], ['7-decide-when-two-objects-count-as-equivalent', 'Relations and well-defined classes'], ['8-order-without-forcing-every-pair-to-compare', 'Partial orders and Hasse diagrams'], ['9-prove-the-next-case-for-an-arbitrary-size', 'Induction and finite testing'], ['10-a-subset-that-escapes-every-proposed-row', 'Optional depth: diagonal reasoning'], ['11-practise-with-changed-claims', 'Independent practice'], ['12-carry-the-method-into-the-next-topic', 'Connections and learning resources']]}>Learn to turn a sentence into a precise claim, find exactly what would make it fail, and construct an argument that covers every case it promises. Sets name the objects; logic connects the conditions; proof explains why the conclusion follows.</LessonIntro>

    <H2>1. Say exactly which objects you mean</H2>
    <Prose>Imagine a small workshop with four people: Ada, Bo, Cam and Dee. Ada and Bo have completed training. Bo and Cam have badges. Someone says, “Everyone who is trained has a badge.” You can settle this claim by finding Ada: she is trained and lacks a badge. One carefully chosen person is enough to refute “everyone.” Bo satisfying both conditions does not rescue it.</Prose>
    <Prose>This invented roster will let us see mathematical language before meeting more abstract objects. A <strong>set</strong> is a collection whose membership is definite. Write our universe of people as U = {'{Ada, Bo, Cam, Dee}'}, the trained set as T = {'{Ada, Bo}'}, and the badged set as B = {'{Bo, Cam}'}. A universe specifies which objects are currently under discussion. “Not trained” within this workshop means Cam and Dee, not every person in the world.</Prose>
    <LessonTable caption="Read the symbol as a complete sentence" headers={['Notation', 'Meaning in this roster']} rows={[['Ada ∈ T', 'Ada is an element of the trained set.'], ['Cam ∉ T', 'Cam is not an element of the trained set.'], ['T ⊆ B', 'Every element of T is also an element of B; false here because of Ada.'], ['T = B', 'The two sets have exactly the same elements; false here.'], ['|T| = 2', 'T has two distinct elements. The bars denote cardinality here.']]} />
    <Prose>A set has no repeated elements and no intrinsic order. {'{Ada, Bo, Ada}'} is the same set as {'{Bo, Ada}'}. A sequence or list, by contrast, can retain order and repetitions. A <strong>proper subset</strong> A ⊊ B satisfies A ⊆ B and A ≠ B. We use ⊆ for containment that permits equality, because the bare symbol ⊂ has different conventions in different books.</Prose>
    <Prose>The empty set ∅ has no elements. The set {'{∅}'} has one element: the empty set itself. Membership asks whether a particular object occurs at the current level. Containment asks whether every element of one set occurs in the other. These are different questions.</Prose>
    <SetMembershipFigure />
    <Prose>The empty set is a subset of every set A. A failure of ∅ ⊆ A would require an element of ∅ missing from A, and there is no element to supply. But ∅ ∈ A holds only if A actually contains the empty set as an element. In the workshop roster it does not.</Prose>
    <Prose>A <strong>set-builder description</strong> gives a membership rule rather than a roster. For example, {'{n ∈ ℤ : −2 ≤ n ≤ 2}'} means the integers from −2 through 2. The colon reads “such that.” Here ℤ denotes the integers, ℝ the real numbers, and ℕ = {'{0, 1, 2, …}'} in this lesson. Some sources start ℕ at 1; a proof's starting value must make its convention clear. Not every informal rule safely defines a set of absolutely everything; our constructions work inside stated sets, and axiomatic set theory is a later subject.</Prose>
    <Prose><strong>Run the examples:</strong> use Python 3, save a complete block as a .py file, and run <Code>python filename.py</Code>. All examples here need only the standard library. Read the question first, predict the result, then inspect the displayed reference. Browser investigations use separate bounded JavaScript models. A Python <Code>set</Code> is unordered, so the examples sort displayed values; a nested immutable <Code>frozenset</Code> can itself be a set element.</Prose>
    <LogicExample example={setsLogicExamples.sets} />

    <H2>2. Build conditions with set operations</H2>
    <Prose>Now ask who is both trained and badged. Keep a person exactly when both membership tests pass. The result T ∩ B = {'{Bo}'} is an <strong>intersection</strong>. To find everyone with at least one credential, keep either membership; the <strong>union</strong> T ∪ B = {'{Ada, Bo, Cam}'} includes Bo only once.</Prose>
    <LessonTable caption="An operation is a membership condition" headers={['Operation', 'Keep x when', 'Original result']} rows={[['T ∩ B — intersection', 'x belongs to T and to B', '{Bo}'], ['T ∪ B — union', 'x belongs to T or to B, including both', '{Ada, Bo, Cam}'], ['T ∖ B — difference', 'x belongs to T and not B', '{Ada}'], ['U ∖ T — relative complement', 'x belongs to U and not T', '{Cam, Dee}'], ['T △ B — symmetric difference', 'x belongs to exactly one of T and B', '{Ada, Cam}']]} />
    <Prose>Difference has a direction: B ∖ T = {'{Cam}'} is different from T ∖ B. A complement always has an ambient universe, even when its notation Aᶜ suppresses that universe. The circle sizes below are schematic; they do not encode proportions or probabilities. The names and selected memberships carry the data.</Prose>
    <SetRegionsLab />
    <Prose><strong>Try a prediction:</strong> give Dee both credentials. Dee moves to the overlap and belongs to union and intersection, but disappears from symmetric difference. Next make both sets empty: their union and intersection are empty, while U ∖ T becomes the entire roster. The rules are stable even when a region has no members.</Prose>
    <H3>Pairs, subsets and a useful binary representation</H3>
    <Prose>If jobs J = {'{Scan, Label}'} and reviewers R = {'{Ada, Bo}'}, the <strong>Cartesian product</strong> J × R contains all four ordered pairs: (Scan, Ada), (Scan, Bo), (Label, Ada), (Label, Bo). Each pair has a first role and a second role. An assignment relation selects some of these pairs. If either set is empty, there are no such pairs. In finite sets, |J × R| = |J||R| because each first choice has exactly |R| second choices.</Prose>
    <Prose>The <strong>power set</strong> P(A) is the set of all subsets of A. For A = {'{Ada, Bo}'}, it is {'{∅, {Ada}, {Bo}, {Ada, Bo}}'}. For every one of n distinct elements, decide independently whether it is included. This gives 2ⁿ subsets. In particular P(∅) = {'{∅}'} has 2⁰ = 1 element, because “include nothing” is one valid choice.</Prose>
    <Prose>Fix the roster order Ada, Bo, Cam, Dee. Then T becomes the membership vector (1, 1, 0, 0), and B becomes (0, 1, 1, 0). Positionwise AND gives the intersection (0, 1, 0, 0); OR gives the union (1, 1, 1, 0); XOR gives symmetric difference (1, 0, 1, 0). This is why sets of enabled features or permissions can be stored as bits. A complemented bit pattern must still be restricted to the chosen universe: unused positions do not become new people.</Prose>
    <Prose>For a family of sets Aᵢ indexed by i in I, ⋃ᵢ Aᵢ contains an element if it belongs to at least one member of the family; ⋂ᵢ Aᵢ requires membership in every member. We will soon name these “there exists” and “for all.” Within a fixed universe U, an empty union is ∅ and an empty intersection is U. These conventions follow the membership conditions; an unqualified empty intersection without an ambient universe needs care.</Prose>

    <H2>3. Read the direction of a claim</H2>
    <Prose>A <strong>proposition</strong> is a statement with a truth value, such as “Bo is trained.” A <strong>predicate</strong> is a condition with an input, such as “x is trained.” Its truth depends on which x is supplied. This lesson uses classical two-valued logic: a well-defined statement is true or false. Missing database values, uncertain measurements and probabilities need additional models; they are not extra truth values silently built into these tables.</Prose>
    <Prose>Use P and Q as placeholders for propositions. Negation ¬P reverses a truth value. P ∧ Q requires both; P ∨ Q requires at least one. Mathematical “or” is inclusive unless “exactly one” is stated. The implication P ⇒ Q says that a true P must not accompany a false Q. That one combination is its entire failure condition.</Prose>
    <LessonTable caption="The four truth assignments" headers={['P', 'Q', 'P ⇒ Q', 'Q ⇒ P', 'P ⇔ Q']} rows={[['false', 'false', 'true', 'true', 'true'], ['false', 'true', 'true', 'false', 'false'], ['true', 'false', 'false', 'true', 'false'], ['true', 'true', 'true', 'true', 'true']]} />
    <Prose>Why is an implication true when its premise is false? It forbids a particular failure, and that failure has not occurred. A rule “if trained, then badged” places no requirement on an untrained person. A false premise does not supply evidence for Q itself; it only means this implication is not violated by that assignment. An implication also says nothing by itself about cause, time or an explanatory mechanism.</Prose>
    <Prose>The <strong>converse</strong> Q ⇒ P reverses the arrow and needs a separate argument. The <strong>contrapositive</strong> ¬Q ⇒ ¬P has the same truth conditions as P ⇒ Q: each fails exactly at P true and Q false. An <strong>equivalence</strong> P ⇔ Q requires both directions, or equivalently equal truth values. Calling P “sufficient for Q” means P ⇒ Q. Calling Q “necessary for P” expresses the same arrow. “P only if Q” also means P ⇒ Q; “P if Q” means Q ⇒ P.</Prose>
    <ImplicationFigure />
    <Prose>Algebra makes the direction tangible. For a real x, x = −2 implies x² = 4. The converse fails at x = 2. Subtracting the same quantity on both sides is reversible. Dividing by a known nonzero quantity is reversible; multiplying both sides by zero discards the original constraint. Likewise, (x² − 1)/(x − 1) equals x + 1 only on the original domain x ≠ 1. Simplifying a formula does not restore an excluded input. A solution method may generate candidates with one-way steps, then check those candidates in the original statement.</Prose>
    <H3>An argument must preserve truth under its premises</H3>
    <Prose>An argument is <strong>valid</strong> when every assignment making all its premises true also makes its conclusion true. From P ⇒ Q and P, you may conclude Q: this is modus ponens. From P ⇒ Q and ¬Q, you may conclude ¬P: modus tollens. From P ⇒ Q and Q, concluding P is invalid; P false and Q true is an admitted countermodel. Each claim concerns the entire argument, not whether its conclusion happens to be true in one observed situation.</Prose>
    <TruthArgumentLab />
    <Prose>Try the inconsistent-premises preset. P and ¬P cannot both hold, so no world is admitted. Under classical semantics every conclusion follows from such premises. The checker therefore separates <strong>validity</strong> from <strong>consistency</strong>. For a useful sound argument, the reasoning must be valid and its premises must actually be true. A truth-table checker verifies this finite propositional form, not whether a real organization's records or assumptions are correct.</Prose>
    <LogicExample example={setsLogicExamples.truth} />

    <H2>4. Follow the order of the witnesses</H2>
    <Prose>The symbol ∀ means “for every,” and ∃ means “there exists at least one.” They bind a variable to a domain. Thus ∀x ∈ T: x ∈ B is our original trained-person claim. To prove it, cover an arbitrary trained person; to refute it, provide one trained person missing a badge. An existential claim ∃x ∈ T: x ∈ B needs just one <strong>witness</strong>, such as Bo.</Prose>
    <Prose>Let A(j, r) mean “reviewer r is approved for job j.” Compare two staffing requirements. In the first, a reviewer may be chosen after the job is known. In the second, one reviewer must be chosen before the job is known:</Prose>
    <MathBlock>{String.raw`\begin{aligned}&\forall j\in J\;\exists r\in R:\ A(j,r),\\&\exists r\in R\;\forall j\in J:\ A(j,r).\end{aligned}`}</MathBlock>
    <Prose>If Scan has Ada, Label has Bo, and Audit has Cam, every row has a witness, but no reviewer covers every row. One common reviewer would satisfy each job, so the second requirement implies the first. The first does not generally imply the second. Adjacent universal quantifiers can exchange order, as can adjacent existential quantifiers; exchanging different kinds needs justification.</Prose>
    <QuantifierWitnessLab />
    <Prose>Think of the first line as “given any job j, produce r(j).” The notation r(j) records a possible dependence on j; it does not assert a particular numerical formula. In an existence proof, name a witness and verify every required condition. In a universal proof, do not choose a specially convenient input. Over the integers, ∀n ∃m: m &gt; n is true using m = n + 1. The reversed ∃m ∀n: m &gt; n is false: given a proposed m, choose n = m.</Prose>
    <H3>Negation changes the task</H3>
    <Prose>To deny “every job has someone,” find a job with nobody. To deny “someone covers every job,” show that each candidate misses at least one job. Push a negation through one quantifier at a time, changing ∀ to ∃ or ∃ to ∀, and negate the final predicate:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\neg(\forall j\,\exists r\ A(j,r))\\ \iff\exists j\,\forall r\ \neg A(j,r),\\[.7em]\neg(\exists r\,\forall j\ A(j,r))\\ \iff\forall r\,\exists j\ \neg A(j,r).\end{gathered}`}</MathBlock>
    <Prose>The omitted domains are still J and R. “Not every” means at least one failure, not “none.” For example, the negation of ∀x ∈ ℝ: x² &gt; 1 is ∃x ∈ ℝ: x² ≤ 1. The equality boundary belongs to the negation. A free variable remains an input until bound or assigned; parentheses show exactly how much of a formula a quantifier controls.</Prose>
    <LessonTable caption="Empty domains still follow the same definitions" headers={['Jobs J', 'Reviewers R', 'Every job has someone', 'Someone covers every job']} rows={[['empty', 'nonempty', 'true: no job fails', 'true: choose any reviewer'], ['nonempty', 'empty', 'false: a job has no witness', 'false: no reviewer exists'], ['empty', 'empty', 'true: no job fails', 'false: no reviewer exists']]} />
    <Prose>These are <strong>vacuous</strong> truths, not exceptions added to the rules. A universal claim has no counterexample in an empty domain; an existential claim has no witness. A practical requirement may also demand “there is at least one job.” Write that condition separately. The notation ∃!x P(x) means exactly one witness: existence plus the statement that any two witnesses are equal. It is stronger than ∃.</Prose>
    <LogicExample example={setsLogicExamples.quantifiers} />

    <H2>5. Build a proof one justified step at a time</H2>
    <Prose>A proof is an explanation whose steps follow from definitions, stated assumptions and established results. Start by writing the exact domain, hypotheses and conclusion. Then unpack the definitions until the needed object or relation becomes concrete. An integer is even if it equals 2k for some integer k, and odd if it equals 2k + 1. Every integer has exactly one of these forms by division with remainder 0 or 1, including negative integers.</Prose>
    <H3>Direct proof: expose the witness</H3>
    <Prose><strong>Claim:</strong> for every integer n, if n is odd then n² is odd. Let n be an arbitrary odd integer. By definition, there is an integer k with n = 2k + 1. Expanding gives n² = 4k² + 4k + 1 = 2(2k² + 2k) + 1. The quantity 2k² + 2k is an integer, so it is the witness required by the definition of oddness for n². Since we used nothing special about n beyond its stated hypothesis, the conclusion covers every odd integer.</Prose>
    <ProofScopeFigure />
    <Prose>The assumption “n is odd” is local: we are proving a conditional. We are not assuming that n² is odd. Once the reasoning inside that assumption is complete, we conclude “if n is odd, then n² is odd.” This is called discharging the assumption. The witness k may depend on n; giving it the same letter in a written proof does not make it a fixed integer for all n.</Prose>
    <LogicExample example={setsLogicExamples.parity} />
    <Prose>The displayed negative inputs make the witness construction less easy to mistake for a positive-only pattern. The general proof is the algebra with arbitrary integer k. Printing four or four million valid cases cannot replace that argument.</Prose>
    <H3>Choose a proof method that matches the conclusion</H3>
    <LessonTable caption="What to establish, and a sensible starting move" headers={['Goal', 'Starting move', 'Obligation']} rows={[['P ⇒ Q', 'Assume P temporarily', 'Derive Q using permitted facts.'], ['P ⇔ Q', 'Separate the two arrows', 'Prove P ⇒ Q and Q ⇒ P.'], ['∀x ∈ A: P(x)', 'Take an arbitrary x in A', 'Avoid extra unstated restrictions on x.'], ['∃x ∈ A: P(x)', 'Construct or establish a witness', 'Check both x ∈ A and P(x).'], ['P and Q', 'Separate the requirements', 'Establish both, possibly by different arguments.'], ['A statement is false', 'Negate it precisely', 'Prove the negation or exhibit a valid counterexample.']]} />
    <Prose>A <strong>proof by cases</strong> divides the allowed domain into exhaustive possibilities. To prove n(n + 1) is even for every integer n: if n = 2k, the product is 2[k(n + 1)]; if n = 2k + 1, then n + 1 = 2(k + 1), so the product is 2[n(k + 1)]. Both brackets contain integers. The even/odd cases cover all integers. Examples for n = 2 and n = 3 do not constitute these cases; each case still handles an arbitrary integer with that property.</Prose>
    <Prose>A <strong>contrapositive proof</strong> establishes ¬Q ⇒ ¬P instead of P ⇒ Q. To prove “if n² is even, then n is even,” suppose n is not even. It is odd, and the direct proof above makes n² odd, hence not even. This proves the contrapositive. Notice the dependence on the integer domain; parity is not being applied to arbitrary real numbers.</Prose>
    <Prose>A <strong>proof by contradiction</strong> temporarily assumes the negation of the desired claim and derives an impossibility. For a conditional P ⇒ Q, this means assuming P and ¬Q together. The contradiction must follow from those assumptions and established facts, not from an unsupported assertion that the conclusion “obviously” holds.</Prose>
    <Prose>For a complete example, suppose √2 were rational, meaning √2 = a/b for integers a and positive b. Choose such a representation with the smallest positive denominator; positive integers have a least element in every nonempty subset. Then a² = 2b², so a² is even and the contrapositive result makes a even. Write a = 2c. Substitution gives 4c² = 2b², hence b² = 2c², so b is even too. Dividing a and b by 2 gives the same rational value with a smaller positive denominator, a contradiction. Therefore √2 is irrational. The argument used the least-positive-integer principle and the parity result explicitly; it did not infer irrationality from a long decimal expansion.</Prose>
    <Callout label="An error to diagnose, not imitate">Starting from the desired equality and rearranging until reaching a true equality can help discover a proof. It establishes the claim only if the steps can be reversed under the stated assumptions, or if the finished argument runs forward from known facts. Squaring or division by a possibly zero expression can silently break that reversibility.</Callout>

    <H2>6. Move between sets and functions</H2>
    <Prose>To show A ⊆ B, take an arbitrary x ∈ A and prove x ∈ B. To show A = B, prove both A ⊆ B and B ⊆ A. This <strong>double inclusion</strong> method works because sets are equal exactly when their memberships agree. A diagram suggests a statement, while element reasoning explains why it holds even for sets that cannot be drawn as two tidy circles.</Prose>
    <H3>A set identity is a logical identity about membership</H3>
    <Prose>Take subsets A and B of U. We will prove U ∖ (A ∪ B) = (U ∖ A) ∩ (U ∖ B), a De Morgan law. If x is on the left, x lies in U and is in neither A nor B, so x belongs to both complements and therefore their intersection. Conversely, if x is in the right-hand intersection, x is in U, outside A and outside B; it cannot be in A ∪ B, so it is on the left. Both directions use an arbitrary element and preserve the universe condition.</Prose>
    <Prose>The partner law U ∖ (A ∩ B) = (U ∖ A) ∪ (U ∖ B) says that failing “both” requires failing at least one condition. This also explains Boolean filter transformations: excluding records matching either rule is the same as requiring both exclusions. Set logic applies to a precise two-valued record model; SQL NULL and duplicate-row behavior must be treated separately before translating it into an actual query.</Prose>
    <H3>A function assigns exactly one output to every input</H3>
    <Prose>A relation from A to B is a subset of A × B: a selection of ordered pairs. A <strong>function</strong> f: A → B, in our convention, is such a relation with exactly one output in B for each input in A. A is the domain, B the codomain, and the outputs actually attained form the image f(A). A partial function may omit some inputs; when needed, we explicitly restrict its domain or call it partial. Merely writing a formula does not establish that it is defined on the announced domain.</Prose>
    <Prose>A function is <strong>injective</strong> when f(x) = f(y) forces x = y: distinct inputs never collide. It is <strong>surjective onto B</strong> when every b ∈ B has an input mapping to it. A <strong>bijection</strong> has both properties. The rule x ↦ x² on domain {'{−2, −1, 0, 1, 2}'} reaches exactly {'{0, 1, 4}'}. It is onto that codomain but not onto ℝ, and it is not injective because −2 and 2 collide. Codomain is part of the surjectivity claim.</Prose>
    <FunctionImageFigure />
    <Prose>For S ⊆ A, the <strong>image</strong> f(S) contains outputs reached from some input in S. For C ⊆ B, the <strong>preimage</strong> f⁻¹(C) contains inputs whose outputs lie in C. The superscript −1 here names a set operation; it does not require f to have an inverse function.</Prose>
    <MathBlock>{String.raw`\begin{aligned}f(S)&=\{f(x):x\in S\},\\ f^{-1}(C)&=\{x\in A:f(x)\in C\}.\end{aligned}`}</MathBlock>
    <Prose>Let S = {'{−2}'} and T = {'{2}'}. Their intersection is empty, so f(S ∩ T) is empty. But f(S) ∩ f(T) = {'{4}'}. Images need not preserve intersections because different inputs may reach the same output. We always have f(S ∩ T) ⊆ f(S) ∩ f(T): a shared input supplies an output to both sides. If f is injective, any two inputs producing a shared output must coincide, which also gives the reverse inclusion.</Prose>
    <Prose>Preimages do preserve intersections. For an arbitrary x in the domain, x ∈ f⁻¹(C ∩ D) exactly when f(x) belongs to both C and D, exactly when x belongs to both f⁻¹(C) and f⁻¹(D). Thus f⁻¹(C ∩ D) = f⁻¹(C) ∩ f⁻¹(D). The same membership method proves preservation of unions and relative complements. A preimage of an impossible output may be empty; f⁻¹(B) = A for our total function.</Prose>
    <LogicExample example={setsLogicExamples.images} />
    <Prose>Composition (g ∘ f)(x) means first apply f, then g; f's outputs must lie in g's domain. A bijection has an inverse function taking each output to its unique input. Existence and uniqueness are separate obligations: for 2x + 3 = 7 over ℝ, x = 2 demonstrates existence. If a and b both satisfy it, subtracting their equations gives 2(a − b) = 0; division by nonzero 2 gives a = b. That is uniqueness. By contrast, x² = 4 has witnesses but no unique real witness.</Prose>

    <H2>7. Decide when two objects count as equivalent</H2>
    <Prose>A relation on A selects pairs from A × A. Write xRy when the pair (x, y) is selected. Sometimes the relation means “same remainder,” sometimes “is a prerequisite of,” and sometimes “is within a distance.” The same pair representation does not give these meanings the same mathematical properties.</Prose>
    <LessonTable caption="Properties must hold for every permitted element or tuple" headers={['Property', 'Requirement', 'A failure needs']} rows={[['Reflexive', 'xRx for every x', 'One missing self-pair.'], ['Symmetric', 'xRy implies yRx', 'A present pair with missing reverse.'], ['Transitive', 'xRy and yRz imply xRz', 'A two-step chain with missing shortcut.'], ['Antisymmetric', 'xRy and yRx imply x = y', 'Two distinct elements related in both directions.']]} />
    <Prose>An <strong>equivalence relation</strong> is reflexive, symmetric and transitive. On integers, let a ~ b mean that a − b is divisible by 3, written a ≡ b (mod 3). Reflexivity follows because a − a = 3·0. If a − b = 3k, then b − a = 3(−k), proving symmetry. If a − b = 3k and b − c = 3m, then a − c = 3(k + m), proving transitivity. These are proofs for all integers, not just the six displayed by a preset.</Prose>
    <Prose>By comparison, define aRb by |a − b| ≤ 1. Every number is within one of itself, and distance is symmetric, but 0R1 and 1R2 do not imply 0R2. Treating “similar enough” as though it were already an equivalence relation can create contradictory grouping expectations. The investigation displays the exact missing pair before offering any classes.</Prose>
    <RelationPropertiesLab />
    <H3>Why equivalence classes form a partition</H3>
    <Prose>The class [a] contains all x with a ~ x. Every a belongs to its own class by reflexivity, so the classes cover A and are nonempty. If two classes [a] and [b] share c, then a ~ c and b ~ c. Symmetry gives c ~ b; transitivity gives a ~ b. For any x ∈ [a], symmetry and transitivity then give b ~ x, so [a] ⊆ [b]. Reversing the argument gives equality. Classes therefore cannot partly overlap: they are equal or disjoint. A collection of nonempty, disjoint blocks covering a set is a <strong>partition</strong>.</Prose>
    <Prose>Conversely, membership in the same block of a partition is an equivalence relation. Each element is in its own block, sharing a block is symmetric, and the unique block containing a middle element makes a two-step relation transitive. Thus grouping into exact classes and defining an equivalence relation describe the same structure. The empty set has the empty partition; it does not need an artificial empty block.</Prose>
    <LogicExample example={setsLogicExamples.relations} />
    <Prose>For integers modulo 3 there are three classes, represented by 0, 1 and 2. A <strong>representative</strong> is one member used to name its entire class; [1] also contains 4 and −2. The quotient set ℤ/~ contains the classes themselves, not just the three selected integer labels.</Prose>
    <H3>A calculation on classes must ignore the choice of representative</H3>
    <Prose>Define [a] + [b] to be [a + b]. Is that well-defined? Suppose a′ is another representative of [a] and b′ another of [b]. Then a′ − a = 3k and b′ − b = 3m, so (a′ + b′) − (a + b) = 3(k + m). The output classes agree. By contrast, “return the integer a from [a]” is not a well-defined rule: [1] = [4], but it would return two different integers.</Prose>
    <Prose>This question matters when different encodings describe the same object. If records are grouped by a canonical identifier, a later operation intended to depend only on identity must give the same result for every encoding in that class. A deliberate canonicalization rule may choose one representation, but its existence and behavior must be specified; equivalence alone does not automatically select one.</Prose>

    <H2>8. Order without forcing every pair to compare</H2>
    <Prose>A <strong>partial order</strong> is reflexive, antisymmetric and transitive. It permits incomparable elements. For positive integers, a ≼ b can mean “a divides b,” so b = ak for some integer k. Each positive integer divides itself, and divisibility composes. If positive a divides b and positive b divides a, their positive integer factors multiply to 1, hence both are 1 and a = b. Positivity matters: on all nonzero integers, 2 and −2 divide one another without being equal.</Prose>
    <Prose>Within {'{2, 3, 6}'}, 2 and 3 each divide 6, but neither divides the other. A <strong>Hasse diagram</strong> draws only covers: place b above a when a ≺ b and no allowed c lies strictly between them. Omit self-loops and edges already implied by an upward chain. The relation contains all those inferred pairs even though the picture omits them. Covers depend on the chosen set; removing an intermediate element can create a new cover.</Prose>
    <PartialOrderFigure />
    <Prose>A <strong>minimal</strong> element has no strictly smaller element in the set. A <strong>least</strong> element is below every element in the set. Here both 2 and 3 are minimal, but there is no least element. Six is greatest because every element divides it. Maximal and greatest are the reversed notions. If a least element exists it is unique: two candidates must be below each other and antisymmetry makes them equal. On this finite nonempty set minimal elements exist; infinite partial orders may have none, as ℤ with its usual ≤ demonstrates.</Prose>
    <Prose>Antisymmetric does not mean “not symmetric.” Equality is both: its only pairs are self-pairs. A <strong>strict order</strong> writes x ≺ y for x ≼ y and x ≠ y; it is irreflexive and transitive. A <strong>total order</strong> additionally compares every pair. Task dependencies are naturally partial: two independent preparations may both precede assembly without needing an order between themselves. A topological sorting algorithm chooses one compatible total sequence; it does not show that the original requirements were total.</Prose>
    <LogicExample example={setsLogicExamples.orders} />

    <H2>9. Prove the next case for an arbitrary size</H2>
    <Prose><strong>Mathematical induction</strong> is a way to prove a claim P(n) for every integer n at or above a starting value. Establish an actual base case P(n₀). Then let n be arbitrary with n ≥ n₀, assume P(n) temporarily, and derive P(n + 1). Starting at the established base, this step covers every finite number of successors. The induction hypothesis is local to a proof of an implication; it is not an assertion that the whole theorem is already true.</Prose>
    <Prose>We will prove that the first n positive odd numbers sum to n². For n = 0, the empty sum is 0, equal to 0². Assume the sum of the first n odd numbers is n². The next odd number is 2n + 1, so the next sum is n² + 2n + 1 = (n + 1)². This proves the arbitrary step. Induction gives the identity for every n ∈ ℕ.</Prose>
    <MathBlock>{String.raw`\begin{aligned}S_n&=\sum_{k=0}^{n-1}(2k+1),\\S_0&=0,\\S_{n+1}&=S_n+(2n+1)\\&=n^2+2n+1=(n+1)^2.\end{aligned}`}</MathBlock>
    <Prose>The sigma symbol adds the indicated terms as k runs from 0 to n − 1. When there are no terms, the sum is zero. Geometrically, an n-by-n square gains one row of n + 1 cells and one column of n remaining cells. Their total is 2n + 1, with the corner counted once. The diagram exposes the same arbitrary construction used by the algebra.</Prose>
    <OddSquareLab />
    <LogicExample example={setsLogicExamples.induction} />
    <Prose>A missing base is fatal. The false claim “2ⁿ is divisible by 3” has a perfectly valid conditional step: if 2ⁿ were divisible by 3, so would 2ⁿ⁺¹ be. It never begins. Conversely, a true base does not excuse a broken step. For “all natural numbers equal zero,” the base n = 0 works, but assuming n = 0 gives n + 1 = 1, not zero. The desired conclusion cannot simply be inserted as the next line.</Prose>
    <H3>Strong and structural induction</H3>
    <Prose>Strong induction allows the step to use all earlier cases from the base through n. For example, every integer m ≥ 2 is a product of primes. A prime is an integer at least 2 with no positive divisors other than 1 and itself. For m = 2 the claim holds. For a later m, either m is prime, or it factors as ab with 2 ≤ a, b &lt; m. By the earlier-case hypotheses, each factor is a product of primes, so combining them gives a product for m. The factors being strictly smaller explains why the argument progresses. Ordinary induction can express the same proof by applying it to “all cases through n are true.”</Prose>
    <Prose>Structural induction follows the constructors of an object. Define a binary expression tree recursively: it is either one leaf, or an internal node with exactly two such trees as children. A leaf has L = 1 leaves and I = 0 internal nodes. If its children satisfy L₁ = I₁ + 1 and L₂ = I₂ + 1, joining them gives L = L₁ + L₂ and I = I₁ + I₂ + 1, hence L = I + 1. Because these are all the constructors, every finite tree of this defined kind satisfies the identity. A one-child node would violate the definition, so the theorem does not automatically cover it.</Prose>
    <Prose>The Counting, Combinatorics & Mathematical Induction topic develops more counting identities and induction applications. Algorithm Correctness, Loop Invariants & Termination applies related reasoning to program states and decreasing measures. Here the essential habit is identifying a complete base, an allowed hypothesis and a step that actually covers the next object.</Prose>
    <H3>What a finite check can establish</H3>
    <Prose>On an explicitly finite domain, exhaustively checking all cases can be a proof, provided the checker and representation are correct. On an infinite domain, a finite sample cannot establish a universal statement by itself. For any chosen last test N, define q(n) as the product of n − k for k = 0 through N. It is zero at each tested integer 0, …, N because one factor is zero. At n = N + 1, every factor is positive, so the product is (N + 1)! and is not zero. The exclamation mark means the product of positive integers through that value.</Prose>
    <LogicExample example={setsLogicExamples.finiteTesting} />
    <Prose>With N = 3, the first four outputs are zero and the next is 24. Extending the last test to N = 6 moves the first failure to 7, where the value is 5040. The point is not that every tested pattern is wrong. It is that no predetermined finite run rules out all untested counterexamples without additional structure. A polynomial degree bound, for example, could supply such additional structure, but must be part of the argument.</Prose>

    <H2>10. A subset that escapes every proposed row</H2>
    <Prose><strong>Optional depth.</strong> The core proof habits above are enough to proceed to practice. This section combines them into an unusual application: proving that some collections cannot be completely listed by another collection.</Prose>
    <Prose>Two sets have the same cardinality when a bijection matches their elements. This makes sense even for infinite sets, where counting to a last element is impossible. The map n ↦ 2n is a bijection from our ℕ to the nonnegative even integers: it never collides, and every nonnegative even integer has exactly one input n. A proper subset of an infinite set can therefore have the same cardinality as the whole set. That does not imply that all infinite sets have the same cardinality.</Prose>
    <Prose>For a finite illustration, imagine trying to list every subset of four elements using only four proposed rows. Represent each proposed subset by its four membership bits. Build a new subset D by reversing the diagonal entries: element i belongs to D exactly when it does not belong to proposed row i. D differs from row 0 at element 0, from row 1 at element 1, and so on. It cannot be any proposed row, even if some rows were duplicated.</Prose>
    <DiagonalSubsetLab />
    <Prose>The general argument, Cantor's theorem, does not depend on a four-row limit. Take any function f: A → P(A); f(a) is a proposed subset indexed by a. Define D = {'{a ∈ A : a ∉ f(a)}'}. This is a subset of A, so it is a candidate output in P(A). For any a ∈ A, membership of a in D is the opposite of membership of a in f(a). Thus D ≠ f(a). Since a was arbitrary, no input maps to D and f is not surjective.</Prose>
    <Prose>There is an injection A → P(A) sending a to {'{a}'}, but no surjection from A onto P(A). In this precise sense the power set has strictly greater cardinality. The empty case also works: P(∅) contains ∅, while a map with empty domain has no output that reaches it. We did not need to assume an element exists to finish that case.</Prose>
    <LogicExample example={setsLogicExamples.diagonal} />
    <Prose>The finite board helps you find the mechanism; the arbitrary-a proof establishes the unrestricted theorem. Sets in bijection with ℕ are countably infinite, and “countable” here includes finite sets as well. P(ℕ) is uncountable. A full treatment of cardinal arithmetic, set-theoretic axioms and independence results is beyond this foundation. The valuable transfer is the construction: disagree with the i-th proposed object at a deliberately selected i-th feature.</Prose>

    <H2>11. Practise with changed claims</H2>
    <Prose>Write the domains and assumptions before opening a hint. For a false statement, provide a counterexample that satisfies its hypotheses. For a true universal statement, explain why the reasoning covers an arbitrary allowed object. Repeat selected problems later without their original wording, and invent a nearby claim whose answer changes.</Prose>
    <Practice title="A. Keep the levels and the universe straight" question="Let U = {1, 2, 3, 4}, A = {1, 3}, B = {3, 4}, and C = {1, {2}}. Find A △ B and U ∖ (A ∪ B). Decide whether 2 ∈ C, {2} ∈ C, {2} ⊆ C and ∅ ⊆ C. If U gains 5, which computed results change?" hint="For a subset test, inspect the elements of the set on the left. For a complement, enumerate the ambient universe.">
      <Prose>A △ B = {'{1, 4}'}, because 3 is shared. A ∪ B = {'{1, 3, 4}'}, so its complement within U is {'{2}'}. In C, 2 is not an element, but {'{2}'} is. The subset assertion {'{2}'} ⊆ C is false because its only element 2 is absent from C. The empty set is a subset of C. Adding 5 to U changes the complement to {'{2, 5}'}; the symmetric difference and C assertions do not change.</Prose>
    </Practice>
    <Practice title="B. Diagnose a convincing-sounding argument" question="A service promises: if the cache misses, it reads storage. You observe a storage read and conclude there was a cache miss. Is this valid? State a countermodel if necessary, then give a conclusion supported by observing no storage read under the promise." hint="Let P mean a miss and Q mean a storage read. Compare Q ⇒ P with ¬Q ⇒ ¬P.">
      <Prose>The first argument is invalid: P can be false and Q true. A background refresh could read storage even with a cache hit; the promise does not exclude this. If no storage read occurs and the promise is true, the contrapositive gives no cache miss. This conclusion depends on the promised implication actually describing the service and the observation being accurate. The example does not prove a causal model of the implementation.</Prose>
    </Practice>
    <Practice title="C. Quantifier order, negation and a boundary" question="Over the integers, compare ∀x ∃y: x + y = 0 with ∃y ∀x: x + y = 0. Prove or refute each and negate each. Then evaluate the two staffing requirements when there are no jobs and two reviewers." hint="The witness in the first formula may depend on x. For the second, test two different x values against one fixed y.">
      <Prose>The first is true: choose y = −x for any integer x. The second is false: x = 0 forces y = 0, but then x = 1 fails. Their negations are respectively ∃x ∀y: x + y ≠ 0, and ∀y ∃x: x + y ≠ 0. The latter is true with x = 1 − y. With no jobs and two reviewers, every job has someone is vacuously true, and someone covers every job is also true: choose either reviewer and there is no failing job.</Prose>
    </Practice>
    <Practice title="D. Repair the proof, not just the conclusion" question="A student says: if ab is even, then a and b are both even, because a = 2r and b = 2s gives ab = 4rs. Diagnose the direction error. State a correct related claim and prove it for arbitrary integers." hint="The displayed calculation assumes both factors even. Try a = 2, b = 3. For a useful true reverse statement, use the contrapositive and two odd factors.">
      <Prose>The calculation proves only “both factors even ⇒ product even.” It does not prove its converse, which fails at a = 2, b = 3. A correct claim is “if ab is even, at least one factor is even.” Suppose neither is even; then a = 2r + 1 and b = 2s + 1 for integers r, s. Their product is 2(2rs + r + s) + 1, which is odd. This proves the contrapositive. The conclusion “at least one” includes the case where both are even.</Prose>
    </Practice>
    <Practice title="E. Prove an identity and refute a nearby one" question="For arbitrary subsets A, B, C of U, prove A ∖ (B ∪ C) = (A ∖ B) ∩ (A ∖ C). Is A ∖ (B ∩ C) = (A ∖ B) ∩ (A ∖ C) also always true?" hint="Translate membership of an arbitrary x. To refute the second formula, let x belong to A and B but not C.">
      <Prose>An x lies in the first left side exactly when x ∈ A, x ∉ B and x ∉ C. Those are precisely the conditions for x to belong to both A ∖ B and A ∖ C, proving equality in both directions. For the second claim, choose A = B = {'{1}'} and C = ∅. Its left side is {'{1}'}, but its proposed right side is empty. The correct right side is (A ∖ B) ∪ (A ∖ C), since failing a conjunction means failing at least one part.</Prose>
    </Practice>
    <Practice title="F. Pull back a changed output condition" question="Let f: ℤ → ℤ send n to 2n + 1. Find f⁻¹({−1, 0, 5}) and explain whether an inverse function on all of ℤ exists. Prove that f⁻¹(C ∪ D) = f⁻¹(C) ∪ f⁻¹(D) for arbitrary C, D ⊆ ℤ." hint="Solve 2n + 1 = y for each target, retaining integer inputs only. A preimage operation can exist even when an inverse function does not.">
      <Prose>The preimage is {'{−1, 2}'}. Targets −1 and 5 give those inputs, while 0 would require n = −1/2, outside the domain. The function is injective but misses every even integer, so it has no inverse function defined on all of its codomain ℤ. It does have an inverse on the odd integers. For the identity, an arbitrary integer n belongs to f⁻¹(C ∪ D) exactly when f(n) is in C or D, exactly when n is in f⁻¹(C) or f⁻¹(D). This proves both inclusions.</Prose>
    </Practice>
    <Practice title="G. Equivalence and representative independence" question="On ℤ, define a ~ b when a and b have the same parity. Prove it is an equivalence relation. Is F([a]) = a² mod 2 a well-defined function to {0, 1}? Is G([a]) = a² a well-defined function to ℤ?" hint="Use a − b = 2k. Representatives in the same class can be 1 and 3.">
      <Prose>Reflexivity uses difference 0; symmetry negates an even difference; transitivity adds two even differences. These give an equivalence relation with even and odd classes. If a − b is even, a² − b² = (a − b)(a + b) is even, so their square remainders agree: F is well-defined. G is not: 1 and 3 are equivalent, but their squares are 1 and 9. A correct output must be a property of the entire class, not an unannounced representative.</Prose>
    </Practice>
    <Practice title="H. A changed divisibility order" question="Use divisibility on {4, 6, 12, 24}. List the covers, minimal and maximal elements, and any least or greatest element. Why is 4 ≼ 24 absent from a Hasse drawing? What changes when 12 is removed?" hint="A cover has no intermediate element from the selected set, even if other integers exist between the labels numerically.">
      <Prose>The covers are 4 ≺ 12, 6 ≺ 12 and 12 ≺ 24. The minimal elements are 4 and 6; 24 is maximal and greatest. There is no least element because neither 4 nor 6 divides the other. The pair 4 ≼ 24 remains in the relation but is implied by a two-edge upward chain. Removing 12 produces covers 4 ≺ 24 and 6 ≺ 24. The numeric spacing is irrelevant; only divisibility and permitted intermediates matter.</Prose>
    </Practice>
    <Practice title="I. Build a new induction argument" question="Prove 1 + 2 + … + n = n(n + 1)/2 for all n ∈ ℕ, with the empty sum convention. Explain why checking n through 100 does not establish the induction step. Then identify what fails in trying to prove 2ⁿ divisible by 3 using a valid multiplication-by-2 step." hint="Add n + 1 to the assumed formula and factor the result. Separate the base from the conditional step.">
      <Prose>At n = 0 both sides are zero. Assume the identity at arbitrary n ≥ 0. Adding n + 1 gives n(n + 1)/2 + (n + 1) = (n + 1)(n + 2)/2, the required formula at n + 1. The arbitrary step, together with the base, completes induction. A finite test checks only its finite inputs; it does not establish that conditional for arbitrary n. For the divisibility claim, the conditional step is fine but the base fails: 2⁰ = 1 is not divisible by 3.</Prose>
    </Practice>
    <Practice title="J. Change a proposed list" question="Optional: let A = {0, 1, 2}, with f(0) = {1}, f(1) = {0, 1}, and f(2) = ∅. Construct D from the reversed diagonal and identify one feature distinguishing it from each row. If row 1 is changed to {0, 2}, does the old D still have to escape?" hint="Recompute membership i ∈ D from i ∉ f(i). The construction depends on the proposed function.">
      <Prose>The original diagonal is false, true, false, so D = {'{0, 2}'}. It differs from f(0) at 0, from f(1) at 1, and from f(2) at 2. After row 1 becomes {'{0, 2}'}, the old D is literally that row and no longer escapes. Recompute: the diagonal is now false, false, false, yielding D′ = {'{0, 1, 2}'}. The theorem says every proposed function has a missing subset constructed from it; it does not promise one fixed subset is missing from every function.</Prose>
    </Practice>
    <Practice title="K. Audit a specification and repair the right condition" question="Use the workshop roster again, with qualified reviewers T ∩ B. Initial job assignments are Scan→{Ada}, Label→{Bo}, Audit→{Bo, Cam}. Check: (1) every job has a qualified assigned reviewer, (2) every assigned reviewer is qualified, and (3) some qualified reviewer is assigned to every job. Add Bo to every job; which conditions now pass? Then remove every unqualified assignment and check again." hint="Keep the three quantified requirements separate. Adding one good witness does not remove existing bad assignments.">
      <Prose>Only Bo is qualified. Initially (1) fails at Scan, (2) fails at Ada on Scan and Cam on Audit, and (3) fails because Bo is absent from Scan. Adding Bo to every job repairs (1) and (3), but leaves both unqualified assignments, so (2) remains false. Filtering each job to qualified reviewers leaves Bo alone on all three, satisfying all conditions. The repair depends on Bo being a permitted assignee for each job; a real policy must include any additional authorization constraints.</Prose>
      <Prose>For a changed-input check, remove Bo's badge. Then T ∩ B becomes empty, so (1) and (3) are impossible for these nonempty jobs, even if every assignment is filtered away. Condition (2) becomes vacuously true after filtering. A robust report should distinguish “no unqualified assignment exists” from “the jobs are actually staffed.”</Prose>
    </Practice>
    <LogicExample example={setsLogicExamples.policy} />
    <Prose>Try the changed badge condition in a copy of the program by removing Bo from <Code>badged</Code>. The expected final filtered report has all three jobs uncovered, no unqualified assignments and no common qualified reviewer. This checks the empty-witness boundary rather than only the successful original repair.</Prose>

    <H2>12. Carry the method into the next topic</H2>
    <Prose>You should now be able to read a quantified statement, name a counterexample without violating its assumptions, and build a short proof whose local hypotheses are visible. If a problem still feels opaque, write one allowed object, one forbidden object and the exact condition separating them. Then replace the particular object with an arbitrary one and see which steps remain valid.</Prose>
    <Prose>The next topic in this module is <strong>Geometry, Trigonometry & Coordinate Reasoning</strong>. The same habits will distinguish a diagram that suggests a geometric claim from assumptions and deductions that establish it. Later, Counting, Combinatorics & Mathematical Induction develops richer finite constructions; Real Analysis, Sequences & Modes of Convergence uses witness order to distinguish pointwise and uniform statements. Equivalence classes recur in modular arithmetic, geometric identification and state abstraction. These are connections, not prerequisites for understanding the current lesson.</Prose>
    <Sources alternatives={<>
      <Prose><strong>Another written route:</strong> <a href="https://richardhammack.github.io/BookOfProof/" target="_blank" rel="noreferrer">Richard Hammack's free Book of Proof</a> offers a slower exercise-driven progression. Start with Chapters 1–2 for notation, Chapters 4–8 for proof methods, and Chapter 11 for relations. Our natural-number convention starts at zero; check a source's convention before borrowing its base case.</Prose>
      <Prose><strong>Video route:</strong> MIT's <a href="https://ocw.mit.edu/courses/6-042j-mathematics-for-computer-science-fall-2010/resources/lecture-1-introduction-and-proofs/" target="_blank" rel="noreferrer">Lecture 1: Introduction and Proofs</a> supplies a spoken introduction to propositions and proof. Continue with <a href="https://ocw.mit.edu/courses/6-042j-mathematics-for-computer-science-fall-2010/resources/lecture-2-induction/index.html" target="_blank" rel="noreferrer">Lecture 2: Induction</a> after section 9. Pause to write the exact hypothesis and conclusion before following the lecturer's next step. These official course pages provide the videos and related course materials; the lesson's verification inspected the page descriptions and written sources, not an entire video viewing.</Prose>
    </>}>
      <li><a href="https://ocw.mit.edu/courses/6-042j-mathematics-for-computer-science-spring-2015/pages/readings/" target="_blank" rel="noreferrer">MIT Mathematics for Computer Science, 2015 readings</a> — sessions on proofs, logical formulas, predicate formulas, sets, binary relations and induction. Useful for comparing formal definitions and short exercises with this lesson's roster examples.</li>
      <li><a href="https://ocw.mit.edu/courses/6-042j-mathematics-for-computer-science-spring-2015/mit6_042js15_session5.pdf" target="_blank" rel="noreferrer">MIT §3.6: Predicate Formulas</a> — mixed quantifiers, witness order, negation and a countermodel. The explicitly empty restricted domains in our board extend the usual nonempty domain-of-discourse convention carefully.</li>
      <li><a href="https://ocw.mit.edu/courses/6-042j-mathematics-for-computer-science-fall-2010/efac321fdc8d0b27586ca35b04aab808_MIT6_042JF10_chap07.pdf" target="_blank" rel="noreferrer">MIT Chapter 7: Relations and Partial Orders</a> — relation properties, equivalence classes, partitions and order structure; a mathematical continuation beyond simply storing relation pairs.</li>
      <li><a href="https://richardhammack.github.io/BookOfProof/Main.pdf" target="_blank" rel="noreferrer">Book of Proof, selected chapters</a> — element proofs, functions, equivalence relations and Chapter 14 cardinality. Theorem 14.7 gives the general power-set comparison behind the optional diagonal investigation.</li>
    </Sources>
  </div>
};
