import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CovarianceFactorLab, DecompositionMatrix, EliminationLab, QrProjectionLab, SingularDirectionsLab, TriangularDependencyFigure } from '../../components/lesson-labs/MatrixDecompositionLabs';
import { matrixDecompositionExamples } from '../matrix-decomposition-examples.js';
export default {
  title: 'Matrix Decompositions (SVD, QR, Cholesky, LU)',
  readTime: '~55 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot matrix-decomposition-lesson">
    <LessonIntro prerequisites="Read a matrix–vector product, transpose, dot product and vector length from the preceding Vectors & Matrices lesson. We introduce triangular structure, orthogonality, rank and positive definiteness here. Eigenvalues are the next topic; their optional SVD connection is explained locally." sections={[["1-one-matrix-several-simpler-jobs", "Read the factors"], ["2-lu-eliminate-store-and-reuse", "LU and pivoting"], ["3-qr-separate-directions-and-fit-data", "QR and least squares"], ["4-cholesky-build-correlated-quantities", "Cholesky and covariance"], ["5-svd-follow-independent-stretches", "SVD and approximation"], ["6-numerical-answers-need-more-than-a-small-residual", "Numerical diagnosis"], ["7-practise-and-explain-your-choice", "Independent practice"], ["8-connect-the-factors-to-the-next-idea", "Readiness and next steps"]]}>
      Recover unknown quantities, fit measurements that do not agree exactly, construct correlated variation, and decide what a low-rank approximation loses. Learn what the factors do before asking a library to compute them. Each method has its own worked calculation, visual investigation and practical checks.
    </LessonIntro>

    <H2>1. One matrix, several simpler jobs</H2>
    <Prose>Two mixtures contain unknown amounts of ingredients. Three sensors give inconsistent measurements of the same process. A simulation needs two errors to vary together. These sound different, but each can be expressed using a matrix: a rectangular collection of coefficients that maps input quantities to output quantities. The hard part is often working backward from an output, or deciding which combinations of inputs the measurements can distinguish.</Prose>
    <Prose>A <strong>factorization</strong>, also called a decomposition, expresses a matrix as a product of matrices with simpler structure. The product must reproduce the original transformation. If A = BC, applying A to a column vector x means first computing Cx, then B(Cx). The factors are not pieces cut out of the original table; they are successive operations whose combined effect is the same.</Prose>
    <Prose>A <strong>linear system</strong> Ax = b asks for unknown x, given the coefficients A and target b. Here A has m rows of equations and n columns of unknowns, so x has n entries and b has m. A factorization depends on A. Solving additionally depends on b. Reuse matters when the coefficients stay fixed while many target vectors change.</Prose>
    <LessonTable caption="The structures that make a factor useful" headers={["Structure", "Plain meaning", "What becomes easier"]} rows={[['Triangular', 'All entries below, or above, the main diagonal are zero.', 'One equation exposes a single remaining unknown; solve in dependency order.'], ['Orthonormal columns', 'Each column has length 1 and distinct columns have dot product 0.', 'Read independent coordinates with dot products; preserve lengths within the represented coordinates.'], ['Diagonal', 'Only the main diagonal can be nonzero.', 'Each coordinate is scaled independently; zero scales reveal lost directions.'], ['Permutation', 'Reorders coordinates or equations without changing their values.', 'Move a usable pivot into place and remember how targets must move too.']]} />
    <TriangularDependencyFigure />
    <Prose>For an orthonormal matrix with columns Q, the identity QᵀQ = I says that taking dot products with those columns recovers the coordinates you used to combine them. If Q is tall rather than square, QQᵀ is generally a projection, not the identity on every possible output vector. This distinction is central to least squares.</Prose>
    <LessonTable caption="Start from the question; then check the assumptions" headers={["Question", "Useful factorization", "Condition to check first"]} rows={[['Solve a square system repeatedly', 'LU with pivoting', 'Square, nonsingular system for a unique solve.'], ['Fit inconsistent observations', 'QR; SVD when rank needs attention', 'Tall, independent columns for the simple QR triangular solve below.'], ['Exploit symmetric positive-definite structure', 'Cholesky', 'Symmetry and strict positivity, not merely nonnegative diagonal entries.'], ['Inspect rank, weak directions or a rank budget', 'SVD', 'Exists for every real rectangular matrix; interpretation depends on the data and error measure.']]} />
    <Prose>These are useful starting choices, not exclusive uses: QR can solve square systems, SVD can solve least-squares problems, and Cholesky can solve positive-definite equations that have nothing to do with probability. This lesson uses dense real matrices. Sparse, structured, complex and very large problems may require specialized implementations.</Prose>
    <Checkpoint prompt="Does an invertible A guarantee that its top-left entry is a usable first pivot? Try A = [[0, 2], [1, 3]].">
      <Prose>No. The first entry is zero, but the equations can have a unique solution: the first equation determines x₂ and the second then determines x₁. Reordering the equations supplies a nonzero first pivot. An entry is not the same thing as the matrix's overall invertibility.</Prose>
    </Checkpoint>

    <H2>2. LU: eliminate, store and reuse</H2>
    <Prose>Start with 2x₁ + x₂ = 5 and 4x₁ + 3x₂ = 11. Subtract twice the first equation from the second. The second equation becomes x₂ = 1; substituting back gives x₁ = 2. We removed an unknown from an equation without changing which x satisfies the pair. A <strong>pivot</strong> is the coefficient used to eliminate entries beneath it.</Prose>
    <Prose>The resulting upper-triangular matrix is U = [[2,1],[0,1]]. We can reconstruct the old second row by adding twice the first new row to the second new row. Store that multiplier in L = [[1,0],[2,1]]. Multiplying L by U recovers A exactly. Thus LU stores elimination as a reusable factorization, rather than forgetting how the rows changed.</Prose>
    <div className="decomposition-matrix-row"><DecompositionMatrix values={[[2, 1], [4, 3]]} label="A" /><span aria-hidden="true">=</span><DecompositionMatrix values={[[1, 0], [2, 1]]} label="L" /><span aria-hidden="true">×</span><DecompositionMatrix values={[[2, 1], [0, 1]]} label="U" /></div>
    <Prose>This hand calculation works, but numerical implementations usually choose the largest-magnitude available entry in the current column as the pivot: <strong>partial pivoting</strong>. In our example they swap the rows first because 4 is larger than 2. That produces different valid L and U, together with a permutation P that records the row order. Follow the swapped version below.</Prose>
    <EliminationLab />
    <H3>Why the two solves give the original answer</H3>
    <MathBlock>{String.raw`PA=LU,\qquad Ly=Pb,\qquad Ux=y`}</MathBlock>
    <Prose>First reorder b using the same P as A. Solve Ly = Pb from the top row downward: L is lower triangular, so each row depends only on already computed entries. Then solve Ux = y from the bottom upward. Substituting the second equation into the first gives LUx = Pb. Since LU = PA, this is PAx = Pb. Undoing the row reordering gives Ax = b. This proves the chain preserves the intended system.</Prose>
    <Prose>In the default swapped example, P sends [5,11] to [11,5], L stores 1/2, and forward substitution gives y = [11,−1/2]. The bottom equation in U is −x₂/2 = −1/2, hence x₂ = 1. The top is 4x₁ + 3x₂ = 11, hence x₁ = 2. The intermediate y is a transformed target, not the final solution.</Prose>
    <RunnableExample example={matrixDecompositionExamples.squareSolve}><Prose>Every code block is a complete program. Use Python with NumPy installed, save the code as a .py file and run it. These outputs were checked with Python 3.12.14 and NumPy 2.3.5. NumPy's solve uses a general LAPACK system solver; it does not inspect this example and automatically choose Cholesky because A happens to be symmetric.</Prose></RunnableExample>
    <Prose>For a solve, ask for the solution directly. Forming an entire inverse and then multiplying it by b adds unnecessary work and is usually a poorer numerical route. “Exact system” describes the mathematical task; floating-point solutions still have rounding error.</Prose>
    <RunnableExample example={matrixDecompositionExamples.pivotedLu}><Prose>This readable implementation exposes the row-order array, earlier multiplier swaps, and the two substitution loops. It deliberately supports one real, finite, square matrix and one target vector per solve. It detects an exactly zero available pivot; that is not a robust numerical-rank test for every ill-conditioned input. Use maintained numerical libraries for production workloads. Notice that the second target reuses the same factors.</Prose></RunnableExample>
    <H3>Costs, singular cases and conventions</H3>
    <Prose>For ordinary dense n×n elimination, the trailing updates add up to cubic arithmetic work, while each triangular solve sums a triangular number of products and uses quadratic work. Factor storage is quadratic. Those are arithmetic counts, not promised seconds or fixed speedups; sparsity, layout and hardware change practical costs. Reusing a factor avoids repeating the cubic stage.</Prose>
    <Prose>Partial pivoting avoids division by a needlessly small available pivot, but does not cure sensitivity already present in the problem or guarantee small element growth for every matrix. A singular system can be inconsistent, or have multiple solutions: the two singular presets show the difference. LU factors may still exist even when a unique solve does not.</Prose>
    <Prose>Read a library's permutation convention carefully. This lesson uses PA = LU. SciPy's <Code>scipy.linalg.lu</Code> returns factors satisfying A = PₛLU, so our P is Pₛᵀ. A single row swap is its own inverse and can hide this difference; a three-row permutation need not be. Verify the documented product instead of guessing from a variable called P.</Prose>
    <Checkpoint prompt="The factors above were computed for A. A colleague changes only b and suggests recomputing LU. What work can you reuse, and what must still change?">
      <Prose>Reuse P, L and U because the coefficient matrix is unchanged. Reorder the new target, perform new forward substitution, then new back substitution. Reusing the old x would generally be wrong. If A changes, the old factorization is no longer automatically valid.</Prose>
    </Checkpoint>

    <H2>3. QR: separate directions and fit data</H2>
    <Prose>Suppose several measurements should lie near a line, but noise prevents one line from passing through all of them. Now Ax = b may have no solution. Instead, choose x to make the residual r = b − Ax as small as possible. <strong>Least squares</strong> minimizes the sum of the squared residual entries. This is a modeling choice: it penalizes large discrepancies strongly and does not make outliers harmless.</Prose>
    <Prose>The columns of A describe available prediction directions. Their <strong>span</strong> is every vector you can create by combining them. If one column partly repeats another, its coefficients are harder to reason about. QR expresses the same columns using perpendicular unit directions Q and a triangular coordinate table R. The decomposition changes the description of available predictions, not which predictions are possible.</Prose>
    <H3>Build two perpendicular directions by hand</H3>
    <Prose>Take a₁ = [1,1] and a₂ = [1,0]. Normalize a₁ by its length √2 to get q₁ = [1,1]/√2. The dot product q₁·a₂ = 1/√2 measures a₂'s coordinate along q₁. That parallel piece is (q₁·a₂)q₁ = [1/2,1/2]. Subtract it from a₂: the remainder is [1/2,−1/2], perpendicular to q₁. Divide by its length 1/√2 to obtain q₂ = [1,−1]/√2.</Prose>
    <QrProjectionLab />
    <Prose>Reading those steps backward gives a₁ = √2q₁ and a₂ = (1/√2)q₁ + (1/√2)q₂. These coordinates form the columns of R. Its lower-left entry is zero because constructing a₁ needed no q₂. This is the basic idea of <strong>Gram-Schmidt orthogonalization</strong>: subtract already represented directions, then normalize what remains. Repeating it on independent columns constructs QR.</Prose>
    <Prose>If the remainder is zero, that column adds no new direction. Do not divide by its length. A rectangular QR factorization still exists, but a square R needed by the simple unique coefficient solve can be singular. Pivoted QR or SVD helps diagnose such cases; blindly proceeding with the full-rank formula does not.</Prose>
    <H3>Why projection solves least squares</H3>
    <Prose>For a tall m×n A with independent columns, reduced Q has shape m×n and R has shape n×n. Q's columns form an orthonormal basis for the prediction space. Taking c = Qᵀb extracts b's coordinates in that space. The closest available prediction is p = Qc. Solve Rx = c to express that same p using A's original columns.</Prose>
    <MathBlock>{String.raw`c=Q^Tb,\quad Rx=c,\quad p=Ax=QQ^Tb`}</MathBlock>
    <Prose>Here is the reason it is closest. The leftover r = b − p is perpendicular to every column of Q, so it is perpendicular to every change Aδ in the prediction. For a different coefficient vector x + δ, the new residual is r − Aδ. Perpendicular components satisfy the Pythagorean rule: its squared length is ‖r‖² + ‖Aδ‖², which cannot be smaller than ‖r‖². With independent columns, only δ = 0 leaves the prediction unchanged, giving a unique coefficient vector.</Prose>
    <Prose>This explains a useful diagnostic: Aᵀr should be close to zero at a computed least-squares solution. The residual itself need not be zero. Calling a nonzero residual a failed solve confuses an inconsistent measurement model with an incorrectly minimized error.</Prose>
    <RunnableExample example={matrixDecompositionExamples.qrFit}><Prose>The first column contains times and the second contains ones, so the coefficient order is slope then intercept. The fitted line is 0.96t + 0.11. At t = 2 it predicts 2.03 against the observed 2.2, leaving residual 0.17. The squared residuals add to 0.042. Units follow the data: slope is output units per time unit; intercept has output units. NumPy's general <Code>lstsq</Code> is a convenient independent solution, not a claim that it calls this explicit QR sequence internally.</Prose></RunnableExample>
    <Prose>The normal equations AᵀAx = Aᵀb express the same orthogonality in exact arithmetic. Forming AᵀA can magnify conditioning: for full-column-rank A, its 2-norm condition number is the square of A's. QR avoids that explicit product. This improves the computational route; it cannot make indistinguishable measurements suddenly informative.</Prose>
    <details><summary>Deeper: Householder reflections and full versus reduced Q</summary>
      <Prose>A reflection across a plane preserves lengths just as a rotation does, but reverses orientation. With a unit normal v, H = I − 2vvᵀ flips the component along v and leaves perpendicular components unchanged. Because HᵀH = I, transforming a least-squares residual by H preserves its length. Choose such reflections to zero the entries below each pivot in turn; their accumulated orthogonal transformation gives a numerically dependable QR construction.</Prose>
      <RunnableExample example={matrixDecompositionExamples.householder}><Prose>The column [3,4] has length 5. Its image [−5,0] has the same length and a zero lower entry. The minus sign is allowed: QR factors are not uniquely signed. Flipping a column of Q and the matching row of R leaves QR unchanged. Validate the product and orthogonality instead of demanding one particular signed printout.</Prose></RunnableExample>
      <Prose>Full Q has m columns and includes directions perpendicular to the column space; reduced Q keeps only n columns for a tall full-rank matrix. Full R has extra zero rows. In NumPy, the reduced dimension is min(m,n). A wide matrix therefore has a different triangular shape from this tall-system derivation. Do not copy <Code>solve(R, Q.T @ b)</Code> to a wide or rank-deficient problem without reconsidering the contract.</Prose>
    </details>
    <Checkpoint prompt="In the four-point fit, should the residual be perpendicular to [0,1,2,3], to [1,1,1,1], to both, or to neither? What would adding 1 to every measurement do?">
      <Prose>It must be perpendicular to both design columns: each direction is an allowed prediction change. Adding 1 to every measurement adds exactly the all-ones direction, so the intercept rises by 1, the slope is unchanged, and the residual stays the same. This conclusion follows from the model's structure before any code runs.</Prose>
    </Checkpoint>

    <H2>4. Cholesky: build correlated quantities</H2>
    <Prose>A covariance matrix records how random quantities vary: diagonal entries are variances; an off-diagonal entry is their covariance. Covariance is symmetric. It is <strong>positive semidefinite</strong> because the variance of every linear combination is nonnegative. It is <strong>positive definite</strong> when that variance is strictly positive for every nonzero combination. A perfectly repeated or redundant variable can make a valid covariance matrix semidefinite without making it definite.</Prose>
    <MathBlock>{String.raw`\operatorname{Var}(w^TX)=w^TCw\geq 0`}</MathBlock>
    <Prose>Here X is the random vector, C is its covariance, and w chooses a linear combination. For example, if X₂ = X₁ exactly, choosing w = [1,−1] produces a constant zero difference and therefore zero variance. Nonnegative diagonal entries alone do not guarantee a valid covariance: [[1,2],[2,1]] gives negative quadratic value −2 for this same w.</Prose>
    <H3>Compute the factor instead of memorizing its name</H3>
    <Prose>For real symmetric positive-definite C, Cholesky writes C = LLᵀ with lower-triangular L and positive diagonal entries. Consider C = [[4,2],[2,2]]. Write L = [[a,0],[b,c]]. Multiplying the factors yields entries a², ab and b²+c². Match them one at a time: a = 2, b = 2/a = 1, and c = √(2−b²) = 1. No eigenvalue calculation is needed.</Prose>
    <div className="decomposition-matrix-row"><DecompositionMatrix values={[[4, 2], [2, 2]]} label="C" /><span aria-hidden="true">=</span><DecompositionMatrix values={[[2, 0], [1, 1]]} label="L" /><span aria-hidden="true">×</span><DecompositionMatrix values={[[2, 1], [0, 1]]} label="Lᵀ" /></div>
    <Prose>For a larger matrix, repeat the same accounting: a new diagonal square is the original diagonal minus contributions already supplied by earlier columns of L; each lower entry is the remaining covariance divided by the current diagonal factor. Positive definiteness makes the required square roots strictly positive in exact arithmetic. Zero or negative remainders require diagnosis, not an unexplained square root of an absolute value.</Prose>
    <Prose>Now let z₁ and z₂ be independent, zero-mean, unit-variance random variables. With x = Lz, the example gives x₁ = 2z₁ and x₂ = z₁ + z₂. Their variances are 4 and 2; their covariance is 2 because they share z₁. More generally Cov(Lz) = L Cov(z)Lᵀ = LLᵀ = C. Independence is sufficient, but zero covariance and unit variances are all this covariance identity needs. Joint Gaussianity matters if you also want a particular multivariate Gaussian distribution.</Prose>
    <CovarianceFactorLab />
    <Prose>The investigation uses a related family with fixed variances 4 and 1, letting you change correlation separately. Correlation divides covariance by the product of standard deviations, so its off-diagonal covariance is 2ρ. At |ρ| = 1, one independent contribution disappears. The displayed singular algebraic square root still reconstructs C, but standard Cholesky's strict positive-definite contract is no longer satisfied.</Prose>
    <RunnableExample example={matrixDecompositionExamples.covariance}><Prose>This preserves the original correlation-0.8 example. Rows store samples, so <Code>independent @ L.T</Code> implements x = Lz for each sample. The covariance printed from 10,000 samples is an observed seeded estimate, not exactly C; changing the seed or sample size changes it. Whitening solves Lz = x and reverses the transformation here. For unknown real data, whitening can decorrelate the modeled covariance without proving independence or Gaussianity.</Prose></RunnableExample>
    <H3>Validation belongs before trusting a factor</H3>
    <Prose>NumPy's Cholesky routine assumes symmetry and reads the chosen triangle; it does not compare both halves for you. Therefore successful execution alone does not certify that the matrix you supplied was symmetric. Check the input's meaning, finite values, symmetry under a justified tolerance, and reconstruction. An empirical covariance can be singular when variables repeat or there are too few independent observations.</Prose>
    <RunnableExample example={matrixDecompositionExamples.covarianceBoundary}><Prose>Symmetrizing or adding λI can be intentional modeling choices, but they change the matrix. They are not universal repairs for invalid measurements. In the covariance setting, positive λ adds independent variance along the chosen coordinate axes; record that assumption instead of silently changing a failed input until the solver succeeds.</Prose></RunnableExample>
    <details><summary>Deeper: solves, log determinants and a shared geometry</summary>
      <Prose>Cholesky also solves Cx = b: first Ly = b, then Lᵀx = y. The determinant is the product of squared diagonal entries of L, so log det(C) = 2Σ log Lᵢᵢ. Summing logarithms avoids first forming a potentially extreme product. The code above obtains −1.021651 for the original covariance because det(C) = 0.36.</Prose>
      <Prose>A distance that accounts for C is xᵀC⁻¹x. If Ly = x, this becomes yᵀy: transform into coordinates with unit covariance, then compute ordinary squared length. For the hand example C = [[4,2],[2,2]] and x = [2,2], solving gives y = [1,1], hence adjusted squared distance 2. The ordinary squared length was 8. This illustrates why a large variation along a high-variance direction should not automatically count as unusual. Statistical interpretation still needs an appropriate data model.</Prose>
      <Prose>Both LU and Cholesky have cubic dense factorization work and quadratic triangular solves. Cholesky exploits symmetry and uses one triangular factor, reducing the leading arithmetic and storage constants. It is not interchangeable with LU on an indefinite matrix. For complex data, replace transpose with conjugate transpose and symmetry with Hermitian symmetry.</Prose>
    </details>
    <Checkpoint prompt="If both variances are 1, can covariance 1.5 describe a real two-variable distribution? Use the variance of X₁ − X₂ to decide.">
      <Prose>No: Var(X₁ − X₂) = 1 + 1 − 2(1.5) = −1. A variance cannot be negative. The problem is the proposed covariance model, not a missing Cholesky option. At covariance 1, the difference has zero variance; that boundary is possible but singular.</Prose>
    </Checkpoint>
    <H2>5. SVD: follow independent stretches</H2>
    <Prose>LU reorganizes equations, QR gives perpendicular prediction directions, and Cholesky exploits strict positive definiteness. The <strong>singular value decomposition</strong>, or SVD, asks another question: which perpendicular input directions does A stretch into perpendicular output directions, and by how much? It works even when A is rectangular, singular or zero.</Prose>
    <Prose>For a real m×n matrix, write A = UΣVᵀ. In a full factorization, U is m×m and V is n×n, both orthogonal; Σ is an m×n diagonal-shaped array with nonnegative entries σ₁ ≥ σ₂ ≥ … ≥ 0 along its main diagonal. Orthogonal factors may rotate or reflect. The rectangular middle factor may also discard input coordinates or insert zero output coordinates.</Prose>
    <MathBlock>{String.raw`Av_i=\sigma_i u_i,\qquad 1\le i\le r=\min(m,n)`}</MathBlock>
    <Prose>For each of these r pairs, the column vᵢ of V is a unit input direction; uᵢ is the corresponding unit output direction. Input vᵢ emerges as σᵢuᵢ, with length σᵢ. These lengths are the <strong>singular values</strong>. They measure the action of A under the chosen coordinate scaling and Euclidean lengths. Calling a direction “important” requires a task and appropriate units: changing a feature from metres to millimetres can change the spectrum.</Prose>
    <H3>A complete two-direction calculation</H3>
    <Prose>Take A = [[3,1],[1,3]]. The perpendicular unit directions v₁ = [1,1]/√2 and v₂ = [1,−1]/√2 give Av₁ = 4v₁ and Av₂ = 2v₂. Thus one valid SVD uses u₁ = v₁, u₂ = v₂ and singular values 4, 2. The input x = [1,0] has coordinates [1/√2,1/√2] in the v basis. Scale them to [4/√2,2/√2], then recombine the u directions: the output is [3,1], exactly the first column of A.</Prose>
    <Prose>The input and output bases happen to agree in this symmetric positive-definite example. For a general matrix they need not. The next investigation lets those bases vary independently. It constructs a matrix with known factors so that every displayed step can be checked; it does not guess the factors of an arbitrary user-supplied matrix.</Prose>
    <SingularDirectionsLab />
    <H3>Rank is the number of directions that survive</H3>
    <Prose>A zero singular value means the corresponding input direction is sent to zero. That direction lies in the <strong>null space</strong>. The number of strictly positive singular values is the exact rank: the dimension of the reachable output space. A wide matrix has extra columns of V beyond the r pairs: A sends these input directions to zero too. For example, A = [[1,0]] sends [1,0] to 1 and [0,1] to 0, but its one-dimensional output space has only one basis vector. A tall matrix instead has extra columns of U; these output directions are perpendicular to every reachable output, and Aᵀ sends them to zero. The number of axes of an array and the linear-algebra rank of a matrix are different uses of the word “rank.”</Prose>
    <Prose>A tiny nonzero value is different from an exact zero. It describes a direction that is weakly observed or strongly contracted, not necessarily a mathematical redundancy. Numerical rank uses a tolerance relative to the problem's scale, arithmetic and measurement uncertainty. A software default is a useful convention to inspect, not a universal scientific threshold.</Prose>
    <Prose>SVD factors are not generally unique. Changing the signs of both uᵢ and vᵢ leaves their contribution unchanged. When singular values repeat, different orthonormal bases within the tied subspace can describe the same transformation. Compare reconstructed matrices, singular values and subspaces rather than treating one library's individual signed vectors as the only answer.</Prose>
    <H3>A matrix is a sum of simple directional contributions</H3>
    <Prose>The outer product uᵢvᵢᵀ first measures an input's coordinate along vᵢ, then places that amount along uᵢ. Scaling by σᵢ supplies its stretch. Adding these contributions reconstructs A. Keeping the first k gives an approximation Aₖ of rank at most k. In the hand example, the first contribution is [[2,2],[2,2]], and the second is [[1,−1],[−1,1]]. Their sum is [[3,1],[1,3]].</Prose>
    <div className="decomposition-matrix-row"><DecompositionMatrix values={[[3, 1], [1, 3]]} label="A" /><span aria-hidden="true">=</span><DecompositionMatrix values={[[2, 2], [2, 2]]} label="σ₁u₁v₁ᵀ" /><span aria-hidden="true">+</span><DecompositionMatrix values={[[1, -1], [-1, 1]]} label="σ₂u₂v₂ᵀ" /></div>
    <Prose>Removing the second contribution preserves all inputs along v₁, but loses every output along v₂. For x = [1,0], A₁x = [2,2] instead of [3,1]. The error is [1,−1], with length √2. A worst-case unit input is v₂ itself: its output error has length 2, the discarded singular value.</Prose>
    <MathBlock>{String.raw`\|A-A_k\|_2=\sigma_{k+1},\qquad \|A-A_k\|_F^2=\sum_{i>k}\sigma_i^2`}</MathBlock>
    <Prose>The spectral norm on the left of the first identity is the largest output error over unit-length inputs. The Frobenius norm squares every matrix-entry error, sums them, then takes the square root. Their meanings differ, though both are minimized by a truncated SVD among matrices of rank at most k: the Eckart–Young–Mirsky result. Equal singular values can create ties. At a full rank budget the discarded error is zero.</Prose>
    <RunnableExample example={matrixDecompositionExamples.ratingsSvd}><Prose>This retains the original dense ratings fixture and makes the rounding explicit. NumPy returns singular values as a one-dimensional array; <Code>U[:, :k] * s[:k]</Code> scales the selected columns before multiplying by the selected rows of Vᵀ. Reduced factors have shapes m×r, r and r×n, where r = min(m,n), including any zero singular values. Reduced is not the same as truncated to a smaller useful rank.</Prose></RunnableExample>
    <Prose>Two cautions make the example more useful. First, treating a zero rating as a missing observation would change the problem: ordinary SVD fits that zero as a value. Missing-data recommendation needs an appropriate observed-entry objective. Second, storing two factors and two singular values for this tiny 3×3 matrix takes 14 scalar entries, more than the original 9. Low rank enables compression only when k(m+n+1) is smaller than mn under this storage convention. A 100×100 matrix with k = 5 would use 1,005 entries rather than 10,000, before metadata or encoding overhead.</Prose>
    <Prose>Discarding small singular directions is not automatically denoising. It is useful when the signal is well represented by a low-rank model and the discarded content is less useful under the task's loss. A small but meaningful signal can also live in a weak direction. Select the rank with a justified error budget or validation, and distinguish approximation of observed data from recovery of unknown truth.</Prose>
    <details><summary>Deeper: why the approximation bound holds, and how eigenvalues connect</summary>
      <Prose>For the spectral-norm lower bound, take any candidate B of rank at most k. Among the first k+1 input singular directions, there is a unit vector v that B maps to zero: B cannot independently preserve all k+1 directions in that subspace. A stretches every such unit vector by at least σₖ₊₁. Therefore ‖(A−B)v‖ = ‖Av‖ ≥ σₖ₊₁. Truncation attains this lower bound by dropping exactly the remaining directions. That proves its optimal worst-case error. The Frobenius result aggregates squared directional errors and requires the corresponding rank-constrained theorem, not an assumption that small entrywise errors are independently selectable.</Prose>
      <Prose>Multiply A = UΣVᵀ by its transpose: AᵀA = VΣᵀΣVᵀ. Each vᵢ is an eigenvector of AᵀA: its eigenvalue is σᵢ² for the first r pairs, and zero for any additional right-basis directions of a wide matrix. An eigenvector is a direction that a square transformation scales without moving it off its line; the next topic develops this idea. This identity explains the SVD mathematically. Explicitly forming AᵀA to compute an SVD can lose accuracy in small directions, so it is not the recommended numerical recipe.</Prose>
      <Prose>For a centered data matrix with rows as observations, V's leading columns describe principal directions and the squared singular values determine the corresponding sample variances after dividing by the appropriate sample-size denominator. Centering and feature scaling are part of that interpretation. The later PCA and randomized-linear-algebra lessons develop statistical selection and large-matrix computation.</Prose>
    </details>
    <Checkpoint prompt="If the singular values are 5, 3 and 1, what are the spectral and Frobenius errors of keeping one direction? Would the answer change if singular vectors flipped signs?">
      <Prose>The largest discarded value is 3, giving spectral error 3. The squared discarded values sum to 3² + 1² = 10, giving Frobenius error √10. Paired sign flips do not change the matrix or these errors. Keeping two directions gives both errors equal to 1.</Prose>
    </Checkpoint>

    <H2>6. Numerical answers need more than a small residual</H2>
    <Prose>Factor reconstruction, solve residual and parameter accuracy answer different questions. A small ‖A−QR‖ says the factors reproduce the supplied matrix. A small ‖b−Ax‖ says x nearly satisfies the supplied equations. Neither alone tells you whether x is close to the unknown physical quantities when the measurements are uncertain or A is sensitive.</Prose>
    <Prose>For an invertible square matrix, its 2-norm <strong>condition number</strong> is κ₂(A) = σmax/σmin. A direction that is strongly contracted must be strongly amplified when solving backward. A large ratio signals that small changes can have large relative consequences for some inputs. It is a worst-case sensitivity measure, not a promise that every target will realize the bound.</Prose>
    <RunnableExample example={matrixDecompositionExamples.sensitivity}><Prose>The second equation is 10⁻⁸x₂ = b₂. Changing b₂ from 10⁻⁸ to 2×10⁻⁸ doubles x₂. Each solution satisfies its own target almost perfectly; the measurement change is what makes the answer vary. This is the problem's conditioning, not proof that the solver is broken. The normal matrix has singular-value ratio 10¹⁶, exposing the squared-condition-number issue directly.</Prose></RunnableExample>
    <H3>What happens when several answers fit equally well?</H3>
    <Prose>If two columns of A are identical, only their combined coefficient is observable. For A with rows [1,1], [2,2], [3,3] and b = [2,4,6], every pair satisfying x₁+x₂ = 2 is exact. The smallest-length pair is [1,1]: write the pair as [1+t,1−t], whose squared length is 2+2t², minimized at t = 0.</Prose>
    <RunnableExample example={matrixDecompositionExamples.minimumNorm}><Prose>NumPy's least-squares routine chooses the minimum-norm solution when the minimizing coefficients are not unique under its numerical-rank convention. An empty returned residual summary does not imply a zero residual; that field is empty for rank-deficient or not-overdetermined systems. Compute <Code>b - A @ x</Code> explicitly when diagnosing the fit.</Prose></RunnableExample>
    <details><summary>Deeper: the pseudoinverse and a deliberate cutoff</summary>
      <Prose>The pseudoinverse uses the SVD to reverse only the surviving singular directions: A⁺ = VΣ⁺Uᵀ, where Σ⁺ reciprocates positive singular values and leaves zero ones at zero. For the least-squares task, A⁺b gives the smallest-norm coefficients among minimizers. A component of b perpendicular to the column space cannot be reproduced and remains in the residual.</Prose>
      <Prose>In floating point, replacing a tiny singular value by zero is an intentional numerical decision. Reversing 10⁻¹² multiplies that component by 10¹² and may amplify noise. A cutoff can trade fit for stability, but changes the effective problem. NumPy's <Code>lstsq</Code> uses a relative rank threshold; its default changed in NumPy 2.0. Inspect the singular values, returned rank and actual residual, then relate any chosen cutoff to measurement accuracy. Do not silently claim discarded information was mathematically absent.</Prose>
      <Prose>The least-squares coefficient problem can also be sensitive when b is almost perpendicular to the prediction space: a small projection can have a large relative change even when b changes little. QR or SVD provides a good computational representation; neither guarantees a well-identified scientific model. The later Conditioning, Stability & Numerical Analysis lesson gives more precise error bounds and diagnostic methods.</Prose>
    </details>
    <LessonTable caption="Check the property you actually need" headers={["Check", "What it establishes", "What it does not establish"]} rows={[['PA ≈ LU or A ≈ QR / LLᵀ / UΣVᵀ', 'The computed factors reconstruct the supplied input within tolerance.', 'That the measurements or model are valid.'], ['QᵀQ ≈ I', 'The computed columns are approximately orthonormal.', 'QQᵀ = I for a tall reduced Q.'], ['Aᵀ(b−Ax) ≈ 0', 'Residual orthogonality for least squares, assessed with suitable scaling.', 'Zero residual, unique coefficients, or low prediction error on new data.'], ['Small normalized residual', 'The supplied equations are nearly satisfied under the chosen scaling.', 'Small forward error in an ill-conditioned problem.'], ['Singular values and rank tolerance', 'Which directions are retained under a stated numerical rule.', 'A universal threshold for meaningful signal.']]} />

    <H2>7. Practise and explain your choice</H2>
    <Prose>For each task, state the shapes, assumptions and intended result before choosing a method. Attempt the hand calculation before opening a hint. Then use code to check the mathematics and try the changed condition; a correct library call without an explanation is not the finish line.</Prose>
    <H3>Calibration with a changed dataset</H3>
    <Prose>A sensor is tested at input levels −1, 0 and 1, producing outputs 1, 1 and 4. Fit output ≈ slope × input + intercept. Compute the fitted values, residual and squared error. Explain why a QR solve is valid and how to check optimality. Then predict what happens if every output increases by 2.</Prose>
    <details><summary>Hint: the two design columns are already perpendicular</summary><Prose>The columns [−1,0,1] and [1,1,1] have dot product zero. Their squared lengths are 2 and 3. Compute the target's dot product with each, then divide by the corresponding squared length to find the two coefficients.</Prose></details>
    <details><summary>Solution and complete reproducible check</summary>
      <Prose>The first dot product is −1+4 = 3, so the slope is 3/2. The second is 6, so the intercept is 2. Predictions are [0.5,2,3.5] and residuals are [0.5,−1,0.5]. The squared residual is 1.5; it is perpendicular to both columns, which proves least-squares optimality. Increasing all outputs by 2 changes the intercept to 4, leaving the slope and residual unchanged. The columns are independent, so the coefficients are unique.</Prose>
      <RunnableExample example={matrixDecompositionExamples.independentFit} />
    </details>
    <H3>Repair an elimination mistake</H3>
    <Prose>For 2x₁+x₂ = 5 and 4x₁+3x₂ = 11, someone subtracts twice the first row from the left side of the second row, but keeps the target 11. They report x₂ = 11. Identify the invariant they broke, repair the calculation, and give an original-equation check that rejects their result.</Prose>
    <details><summary>Hint: an equation has two sides</summary><Prose>Subtracting another equation means subtracting its left and right sides together. The set of solutions stays unchanged only when both operations agree.</Prose></details>
    <details><summary>Explanation</summary><Prose>The second target must become 11−2(5) = 1. Then x₂ = 1 and x₁ = 2, which give targets 5 and 11 in the original equations. Keeping x₂ = 11 and using the original first equation gives x₁ = −3; the second left side becomes 21, not 11. A residual check catches the invalid transformation.</Prose></details>
    <H3>Build the uncertainty you intended</H3>
    <Prose>Construct a lower-triangular factor for C = [[9,3],[3,2]]. Express two outputs in terms of independent zero-mean unit-variance z₁,z₂. Then replace the off-diagonal entries by 5 and determine whether the resulting matrix is a valid covariance.</Prose>
    <details><summary>Hint: match the three distinct entries of LLᵀ</summary><Prose>Start with the positive square root of 9. Use the covariance to obtain the lower-left entry, then account for how much of the second variance remains.</Prose></details>
    <details><summary>Explanation</summary><Prose>L = [[3,0],[1,1]] gives x₁ = 3z₁ and x₂ = z₁+z₂. Their covariance is 3. With covariance 5, the determinant would be 9·2−25 = −7. Equivalently, the last squared factor would be 2−(5/3)², which is negative. This proposed covariance is invalid; do not replace the negative remainder by its absolute value.</Prose></details>
    <H3>Choose a rank from an explicit error budget</H3>
    <Prose>A 20×10 matrix has singular values 8, 3, 1 and seven zeros. Choose the smallest rank budget giving Frobenius error at most 1.5. Count entries for Uₖ, the singular values and Vₖᵀ separately. Would an error budget of 0.5 change the answer?</Prose>
    <details><summary>Hint: add squared discarded values</summary><Prose>For each candidate k, add the squares of the singular values that will be discarded, then take a square root. Compare that error with the specified budget rather than with the percentage of values retained.</Prose></details>
    <details><summary>Explanation</summary><Prose>Keeping one direction gives √(3²+1²) = √10, which is too large. Keeping two gives error 1 and is sufficient. Store 20·2 + 2 + 2·10 = 62 scalars instead of 200, before metadata. With budget 0.5, retain three directions for zero error, using 60+3+30 = 93 scalars. These counts assume ordinary dense factor storage; they do not establish compressed file sizes or a denoising benefit.</Prose></details>
    <Checkpoint prompt="A report says: 'Cholesky succeeded, therefore our covariance is symmetric, valid and accurately estimated.' Which parts of that inference need additional evidence?">
      <Prose>Check both triangles for symmetry because the routine need not do so. Reconstruction checks consistency with the supplied matrix. Positive-definiteness and tolerance need careful interpretation near a boundary. Accurate estimation of a population covariance also depends on data quality, sampling, measurement and model assumptions; factorization success cannot establish it.</Prose>
    </Checkpoint>

    <H2>8. Connect the factors to the next idea</H2>
    <Prose>You are ready to continue when you can trace one elimination, explain why a projection residual is perpendicular, derive a small Cholesky factor, and follow an input through SVD factors. You should also be able to choose a method for a new task, state its assumptions, and distinguish a good computation from a well-conditioned or statistically justified model.</Prose>
    <Prose>The next module topic is <a href="./eigenvalues-eigenvectors?module=math-foundations">Eigenvalues & Eigenvectors</a>. It studies directions a square transformation preserves, then connects them to repeated transformations, stability and spectral descriptions. The SVD relationship AᵀA = VΣᵀΣVᵀ gives you a concrete reason to care about those directions. Later randomized linear algebra and numerical analysis build on the rank and sensitivity distinctions established here.</Prose>
    <Sources alternatives={<div><p><strong>Another explanation, with practice:</strong></p><ul>
      <li><a href="https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/pages/positive-definite-matrices-and-applications/singular-value-decomposition/" target="_blank" rel="noopener noreferrer">MIT OCW: Singular Value Decomposition</a> — Strang's lecture, recitation and worked exercises. Use after the directional walkthrough; the companion summary develops singular bases and the eigenvalue connection. The session and substantive notes were reviewed; this is not a claim of watching the full video.</li>
      <li><a href="https://www.cs.cornell.edu/courses/cs4220/2026sp/lec/2026-02-20.html" target="_blank" rel="noopener noreferrer">David Bindel: Introduction to least squares</a> — a written geometric and numerical treatment of projection, factor choices and sensitivity. Best after the core QR section; notation and code use a more advanced numerical-analysis setting.</li>
      <li><a href="https://www.cs.cornell.edu/courses/cs6241/2025sp/lec/2025-02-13.html" target="_blank" rel="noopener noreferrer">Cornell: SVD and other low-rank decompositions</a> — a deeper written account of approximation guarantees and the choice of representation. Read after the rank-budget exercise.</li>
    </ul></div>}>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.solve.html" target="_blank" rel="noopener noreferrer">NumPy solve</a> and <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.linalg.lu.html" target="_blank" rel="noopener noreferrer">SciPy LU</a> — system shapes, implementation notes and the precise permutation convention. No SciPy installation is required for this lesson's examples.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.qr.html" target="_blank" rel="noopener noreferrer">NumPy QR</a> and <a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.lstsq.html" target="_blank" rel="noopener noreferrer">least squares</a> — reduced/full factors, numerical-rank thresholds and return-value contracts.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.cholesky.html" target="_blank" rel="noopener noreferrer">NumPy Cholesky</a> — positive-definite and Hermitian assumptions, selected-triangle behavior and solve identities.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html" target="_blank" rel="noopener noreferrer">NumPy SVD</a> — factor shapes, sorted values, complex-data conventions and stacked operation. Online documentation was checked on 10 September 2026; examples use the stated local NumPy 2.3.5 snapshot.</li>
    </Sources>
  </div>
};
