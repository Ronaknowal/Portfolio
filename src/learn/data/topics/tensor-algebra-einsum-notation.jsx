import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { AttentionContractionLab, BatchPairingFigure, ContractionOrderLab, DiagonalReductionFigure, IndexContractionLab, TensorBasisLab } from '../../components/lesson-labs/EinsumLabs';
import { einsumExamples } from '../einsum-examples.js';
function Practice({
  title,
  children,
  hint,
  solution
}) {
  return <section className="einsum-practice"><H3>{title}</H3>{children}<details><summary>Hint: a place to start</summary><Prose>{hint}</Prose></details><details><summary>Solution and reasoning</summary>{solution}</details></section>;
}
export default {
  title: 'Tensor Algebra & Einsum Notation',
  readTime: '~55 min + practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot einsum-lesson">
    <LessonIntro exampleKind="NumPy" prerequisites="Read vector/matrix entries, follow a finite sum, and multiply a row by a column. Vectors, Matrices & Tensor Operations supplies those skills. No physics or neural-network knowledge is needed for the core; we explain the small applications as we reach them." sections={[['1-give-each-axis-a-job', 'Name the axes'], ['2-compute-one-output-entry', 'Follow the products'], ['3-separate-diagonals-from-reductions', 'Read the exact rules'], ['4-keep-batches-and-broadcast-groups-straight', 'Batches & broadcasting'], ['5-compute-relationships-in-data', 'Covariance'], ['6-compare-then-combine-with-attention', 'Attention'], ['7-choose-what-to-compute-first', 'Contraction order'], ['8-separate-an-object-from-its-coordinates', 'Tensor meaning'], ['9-check-the-program-you-actually-wrote', 'Numerical checks'], ['10-practise-with-new-questions', 'Practice & next']]}>A tensor expression is a recipe for selecting numbers, multiplying them, and deciding which indices to keep. We will make that recipe visible, translate it into complete programs, and learn when an array calculation also represents a geometric object.</LessonIntro>

    <Prose>Each code block is a complete program using Python 3 and NumPy; the displayed outputs were checked with NumPy 2.3.5. Save a block as a .py file and run it in your NumPy environment. The browser investigations run independently and do not execute pasted Python.</Prose>
    <H2>1. Give each axis a job</H2>
    <Prose>Imagine measurements from several experiments. Each experiment contains several observations, and each observation records several features. An array X with shape (2,3,4) could mean 2 experiments × 3 observations × 4 features. X[1,0,2] selects one number: experiment 1, observation 0, feature 2. Array indices here count from zero.</Prose>
    <Prose>An <strong>axis</strong> is a position in the indexing scheme. Its <strong>length</strong> is how many choices it allows. Three axes make this an order-three numerical tensor in array-library language. That does not mean a three-dimensional geometric space or matrix rank 3. Shape tells you how values are arranged; names and units tell you what those values mean.</Prose>
    <LessonTable caption="Names attach meaning to axis positions" headers={['Object', 'Shape and labels', 'One entry']} rows={[['Observations X', '(B,N,D) → b n d', 'X[b,n,d]: feature d in observation n of batch b.'], ['Linear map W', '(D,F) → d f', 'W[d,f]: contribution from input feature d to output feature f.'], ['Transformed Y', '(B,N,F) → b n f', 'Y[b,n,f]: one output feature for the same observation.']]} />
    <Prose>Upper-case B,N,D,F denote axis lengths; lower-case b,n,d,f denote index choices. The characters are not reserved words: b means batch only because we assigned that role. NumPy does not check that the b-axis actually contains the same experiments in two operands. Equal lengths alone do not establish alignment.</Prose>
    <MathBlock>{String.raw`Y_{bnf}=\sum_{d=0}^{D-1}X_{bnd}W_{df}`}</MathBlock>
    <Prose>Read it aloud: “For each experiment b, observation n and output feature f, multiply matching input-feature entries and add over d.” The equivalent expression is <Code>np.einsum("bnd,df-&gt;bnf", X, W)</Code>. The comma separates input arrays; the arrow introduces the output's axes, in order. This is a description of the computation, not a command to physically reorder the input first.</Prose>

    <H2>2. Compute one output entry</H2>
    <Prose>Start smaller. A=[[1,2],[3,4]], B=[[5,6],[7,8]], and C=A B. To find C[0,1], hold output indices i=0 and j=1 fixed. Vary k from 0 to 1. The paired input entries are A[0,0] with B[0,1], then A[0,1] with B[1,1].</Prose>
    <MathBlock>{String.raw`\begin{aligned}C_{ij}&=\sum_{k=0}^{1}A_{ik}B_{kj},\\ C_{01}&=1\cdot6+2\cdot8=22.\end{aligned}`}</MathBlock>
    <Prose>The output indices i,j are <strong>free indices</strong>: each choice identifies a different output cell. The summation index k is a <strong>dummy index</strong>: it runs through the inner calculation and disappears from the output. Summing over paired axes in a product is a <strong>tensor contraction</strong>; matrix multiplication is one example. Consistently renaming k to r changes no arithmetic. Reusing i for it would tie together previously independent choices and change the question.</Prose>
    <IndexContractionLab />
    <Prose>The remaining cells are C[0,0]=1×5+2×7=19, C[1,0]=3×5+4×7=43 and C[1,1]=3×6+4×8=50. Each output cell gets its own sum. For A of shape (M,K) and B of shape (K,N), the result has shape (M,N), even when M and N differ.</Prose>
    <RunnableExample example={einsumExamples.loops}><Prose>For the changed non-square fixture, C[1,1] uses products −1,0,−2, giving −3. Ordinary loops and <Code>@</Code> supply two independent ways to check the contraction. Prefer <Code>@</Code> for an obvious matrix product; use einsum when naming more axes clarifies the intent.</Prose></RunnableExample>
    <Checkpoint prompt="In ik,kj->ikj, k appears in both inputs. Is it summed? What changes if you then sum the middle output axis?"><Prose>No. The explicit output retains k, so each output stores one A[i,k]B[k,j] product. Summing the middle axis adds those products and gives ordinary matrix multiplication. Repetition alone does not settle the answer.</Prose></Checkpoint>

    <H2>3. Separate diagonals from reductions</H2>
    <Prose>In <strong>explicit mode</strong>, read the whole arrow. First associate each input axis with its label. The same label means the same index choice, including two axes within one operand. Multiply the selected entries across operands. Then sum over each input label absent from the output. Finally arrange surviving axes in exactly the written output order.</Prose>
    <DiagonalReductionFigure />
    <Prose>A repeated label <em>inside one operand</em> imposes equality between those axis indices. Thus <Code>ii</Code> visits only the diagonal, not every row/column pair. Those two axes must have the same length. <Code>ii-&gt;i</Code> keeps the selected values, while <Code>ii-&gt;</Code> sums them. A label need not appear twice to be reduced: <Code>ij-&gt;j</Code> sums over i, and <Code>i-&gt;</Code> sums a vector.</Prose>
    <LessonTable caption="The output contract changes the operation" headers={['Expression', 'Computation', 'Output']} rows={[['i,i->', 'Multiply matching vector entries and sum i.', 'Scalar dot product for real vectors.'], ['i,j->ij', 'Multiply every independent i/j pair.', 'Outer-product matrix.'], ['ij,ij->ij', 'Multiply matching cells; both labels survive.', 'Elementwise product, not matrix multiplication.'], ['ij->ji', 'Keep every value, reverse the order of the two axes.', 'Transpose.'], ['ii->i / ii->', 'Select equal-axis entries; keep i / sum i.', 'Diagonal vector / scalar trace.'], ['ij->j', 'Add across observation/row choices i.', 'One total per column j.']]} />
    <RunnableExample example={einsumExamples.patterns}><Prose>The result of <Code>ji-&gt;ji</Code> is the original array: the first input axis was named j and remains first. In contrast, implicit <Code>ji</Code> sorts surviving labels into alphabetical i,j order and transposes it. Labels name axes; they do not inherently mean row or column.</Prose></RunnableExample>
    <H3>Why implicit notation looks different</H3>
    <Prose>Without an arrow, NumPy keeps labels that occur exactly once and sorts those output labels alphabetically; repeated labels are reduced. Thus implicit <Code>ii</Code> is a trace. In traditional Einstein notation, paired repeated indices imply summation under a stated convention, usually one upper and one lower index in general tensor algebra. NumPy's explicit form extends that convention: it can keep shared labels or force a sum over a label used only once. We will use explicit output throughout the main examples.</Prose>
    <Prose>An output label must occur in an input and can occur only once in the output. <Code>ij-&gt;ii</Code> is invalid. Einsum also cannot express an arbitrary nonlinear function, softmax, or a new flattened axis just by renaming letters. Compose it with ordinary array operations when the calculation needs them.</Prose>

    <H2>4. Keep batches and broadcast groups straight</H2>
    <Prose>A <strong>batch</strong> collects independent cases. In <Code>bi,bi-&gt;b</Code>, b appears twice but survives: compare row b of X with row b of Y and sum over i. Changing the second b to c creates a separate index. Now every X row is compared with every Y row. This is often a useful similarity table; it is wrong when you meant only corresponding cases.</Prose>
    <BatchPairingFigure />
    <Prose>For batched matrix multiplication, <Code>bik,bkj-&gt;bij</Code> preserves the batch and sums k. A shared matrix uses <Code>bik,kj-&gt;bij</Code>; the second operand has no batch axis, so the same matrix participates in every case. Neither operation mixes data between batches.</Prose>
    <H3>Ellipsis means a group of axes, with broadcast rules</H3>
    <Prose><Code>...</Code> stands for the extra axes not explicitly named. In <Code>...i,...i-&gt;...</Code>, pair the last i-axis and reduce it; broadcast the leading axis groups by aligning them from the right. Each aligned pair of lengths must match or one must be1. Missing leading axes behave as length 1. This is a compatibility rule, not averaging or adding independent batches.</Prose>
    <Prose>For shapes (2,1,3) and (4,3), i has length 3. The leading groups (2,1) and (4) align as (2,1) and (1,4), producing (2,4). Each length-one group is reused where needed. Explicit named axes across different operands can also broadcast a singleton length: a b-axis of1 can pair with a b-axis of2. Repeated axes <em>within one operand</em> still require exact equal lengths for diagonal selection. Do not reduce this to a blanket rule that all shared lengths must be identical.</Prose>
    <RunnableExample example={einsumExamples.batches}><Prose>Check the batch identities as well as the resulting sizes. For two arrays of shape (2,3), <Code>...i,...i-&gt;...</Code> returns two paired dot products; add a separate singleton leading axis to each if you intend all pairs. Einsum aligns positions and sizes, not labels from a database.</Prose></RunnableExample>
    <details><summary>Deeper: contract several axes and compare tensordot</summary><Prose>In <Code>ijk,jil-&gt;kl</Code>, fix output k,l and sum both i and j. A has axes (i,j,k), while B has (j,i,l), so the matching contracted axis positions differ. The corresponding <Code>tensordot</Code> explicitly pairs A axes [0,1] with B axes [1,0]. It returns the uncontracted A axes followed by the uncontracted B axes.</Prose><RunnableExample example={einsumExamples.higher} /></details>

    <H2>5. Compute relationships in data</H2>
    <Prose>A <strong>Gram matrix</strong> contains dot products between columns: <Code>ni,nj-&gt;ij</Code> for an observation-by-feature array. To make those dot products describe how features vary around their means, center each feature first. For ordinary sample covariance, divide the centered cross-products by N−1, with at least two observations.</Prose>
    <MathBlock>{String.raw`\begin{aligned}Z_{bni}&=X_{bni}-\frac1N\sum_{r=0}^{N-1}X_{bri},\\ C_{bij}&=\frac1{N-1}\sum_{n=0}^{N-1}Z_{bni}Z_{bnj}.\end{aligned}`}</MathBlock>
    <Prose>b identifies the batch, n the observation, and i,j two independent feature choices. Distinct labels i,j let the result compare every feature with every feature. For observations [1,2],[3,0],[5,4], the mean is [3,2] and centered rows are [−2,0],[0,−2],[2,2]. Their feature cross-product is −2×0+0×−2+2×2=4; dividing by 2 gives covariance 2. Both diagonal entries are 4, so the covariance matrix is [[4,2],[2,4]].</Prose>
    <RunnableExample example={einsumExamples.covariance}><Prose><Code>bni,bnj-&gt;bij</Code> gives a (B,D,D) result: the output cannot use the same literal label twice just because both feature axes have length D. The uncentered second moment is a different quantity. In an independent identically distributed sampling model with finite second moments, the N−1 estimator is unbiased for population covariance; that statement does not automatically apply to arbitrary dependent time-series observations. Feature i/j covariance has the product of their units.</Prose></RunnableExample>
    <Checkpoint prompt="What would bni,bni->bi compute on the centered data? Is it the complete covariance?"><Prose>It sums squared values over observations separately for each feature. After division by N−1 it gives the covariance diagonal (variances), not the off-diagonal relationships. The two feature axes were tied to the same index i.</Prose></Checkpoint>

    <H2>6. Compare then combine with attention</H2>
    <Prose>Suppose a query asks which stored entries are relevant. Each entry has a <strong>key</strong> for comparison and a <strong>value</strong> to retrieve. A query and a key share a feature space so we can take their dot product. Values may have a different feature dimension. This mechanism is attention; our example demonstrates its arithmetic without requiring a trained language model.</Prose>
    <LessonTable caption="Keep the roles separate" headers={['Array', 'Shape', 'Meaning']} rows={[['Q', '(B,T,D)', 'Batch, query position, comparison feature.'], ['K', '(B,S,D)', 'Batch, key position, same comparison feature.'], ['V', '(B,S,F)', 'Batch, key position, value feature. F need not equal D.'], ['Scores / weights', '(B,T,S)', 'One query-to-key comparison or normalized weight.'], ['Context', '(B,T,F)', 'One weighted value vector per query.']]} />
    <Prose>For each query, Z is the sum of exponentials of its allowed scores. The piecewise rule below assigns an explicit zero to a blocked key. At least one key must be allowed for Z to define a nonzero normalizer.</Prose>
    <MathBlock>{String.raw`\begin{aligned}R_{bts}&=\frac1{\sqrt D}\sum_d Q_{btd}K_{bsd},\\ Z_{bt}&=\sum_{r\ \mathrm{allowed}}\exp(R_{btr}),\\ W_{bts}&=\begin{cases}\frac{\exp(R_{bts})}{Z_{bt}}&\text{allowed }s,\\0&\text{blocked }s,\end{cases}\\ O_{btf}&=\sum_s W_{bts}V_{bsf}.\end{aligned}`}</MathBlock>
    <Prose>Compute scores with <Code>btd,bsd-&gt;bts</Code>, divide by √D, then normalize across <em>key positions</em> with softmax. Set blocked-key weights to zero. Finally use <Code>bts,bsf-&gt;btf</Code> to sum weighted values. Einsum performs the products and sums; division, masking and softmax are separate operations.</Prose>
    <Prose>For query [1,0] and keys [1,0],[0,1],[1,1], raw dot products are 1,0,1. The scaled scores are approximately 0.7071,0,0.7071. If only keys 0 and 2 are allowed, equal scores give weights 0.5,0,0.5. With values [2,0],[0,4],[2,2], the context is0.5[2,0]+0.5[2,2]=[2,1]. Each intermediate has a different meaning.</Prose>
    <AttentionContractionLab />
    <RunnableExample example={einsumExamples.attention}><Prose>Subtracting the maximum allowed score before exponentiation leaves the normalized weights unchanged. An all-masked query has no distribution to normalize; this implementation raises a clear error. The examples use small finite inputs. Production code also needs suitable dtype/range handling and a deliberate masking policy; finite inputs alone do not guarantee every floating-point product stays finite.</Prose></RunnableExample>
    <details><summary>Deeper: why the scale and why contraction order cannot remove softmax</summary><Prose>If query/key components are independent, zero-mean and unit-variance, their D-term dot product has variance D. Dividing by √D keeps that variance at1 under those assumptions. Learned features need not exactly satisfy that model; it explains the design motivation, not a theorem about every trained network.</Prose><Prose>Without the nonlinear normalization, (QKᵀ)V can be regrouped as Q(KᵀV). With softmax between the products, that regrouping generally changes the computation. A contraction planner may rearrange products/sums within an einsum expression; it cannot move a nonlinear operation through them. Later attention lessons develop training, heads, masking and architectural alternatives.</Prose></details>

    <H2>7. Choose what to compute first</H2>
    <Prose><Code>ab,bc,cd-&gt;ad</Code> describes A B C but does not mandate which pair to multiply first. Over exact arithmetic, associativity gives the same result. Intermediate sizes can differ dramatically. A conventional (a,b)×(b,c) multiply uses abc scalar multiplications: ac output entries, each with b products.</Prose>
    <MathBlock>{String.raw`\begin{aligned}(AB)C &: abc+acd,\\ &\text{first temporary }(a,c),\\ A(BC) &: bcd+abd,\\ &\text{first temporary }(b,d).\end{aligned}`}</MathBlock>
    <Prose>With a=5,b=40,c=2,d=30, left-first costs 400+300=700 multiplications and forms 10 temporary entries. Right-first costs 2400+6000=8400 and forms 1200 entries. This comparison counts multiplications, not additions, bytes transferred, threads, kernel startup or wall-clock seconds.</Prose>
    <ContractionOrderLab />
    <RunnableExample example={einsumExamples.order}><Prose><Code>einsum</Code> defaults to <Code>optimize=False</Code>; <Code>optimize=True</Code> selects a greedy path. <Code>einsum_path</Code> returns a reusable plan and a diagnostic report. Its indices refer to the current operand list, with each computed result appended before the next step. Do not read them as permanently identifying the original array positions.</Prose></RunnableExample>
    <Prose>Greedy search is a heuristic, not a guarantee of global optimality. Exhaustive <Code>"optimal"</Code> search becomes expensive as the number of operands grows. A low arithmetic count is not necessarily the fastest hardware execution, and a path chosen for one set of shapes may be unsuitable after the shapes change. Measure relevant workloads with the actual dtype, layout and memory constraints; use dedicated matrix kernels when they make the operation clearer or faster. Floating-point reassociation can change low-order digits, so compare approximate results with a justified tolerance.</Prose>
    <details><summary>Deeper: a contraction can track physical units</summary><Prose>For a body of mass m moving with Cartesian velocity v, classical kinetic energy is ½m‖v‖². Suppose velocity has axes body n, time t, component d. To keep one total energy per time, use <Code>n,ntd,ntd-&gt;t</Code>: mass is shared across times/components, velocity is squared entrywise, and n,d are summed. The units become kg·(m/s)², or joules. The coordinate components here use an orthonormal Cartesian basis; an arbitrary oblique basis requires a metric, as the next section explains.</Prose><RunnableExample example={einsumExamples.energy}><Prose>This computes translational kinetic energy from supplied masses and velocities. It does not simulate forces, include internal/rotational energy of extended bodies, or validate measured data. At time 0, the two contributions are 1 and 6 joules, totaling 7.</Prose></RunnableExample></details>

    <H2>8. Separate an object from its coordinates</H2>
    <Prose>In a numerical library, “tensor” commonly means a multidimensional array. In mathematics, tensor structure includes how components represent a multilinear object and change with the basis. A photograph's (height,width,color) shape alone does not tell you a vector-space transformation law. You can endow arrays with such an interpretation, but it is extra structure, not something <Code>einsum</Code> discovers.</Prose>
    <H3>One vector, two coordinate lists</H3>
    <Prose>Work in a real plane. Let the columns of invertible S be the <em>new basis vectors expressed in old coordinates</em>. If v′ is the new coordinate list, then v=S v′: multiplying by S assembles the same geometric vector. Therefore v′=S⁻¹v. For S=[[1,1],[0,1]] and v=[3,2], the new list is[1,2], since 1[1,0]+2[1,1]=[3,2]. Changing basis is different from actively moving the vector while leaving the basis fixed.</Prose>
    <TensorBasisLab />
    <H3>A measurement transforms in the opposite way</H3>
    <Prose>A <strong>linear functional</strong>, or covector, takes a vector and returns one scalar. For example ℓ(v)=2v₁−v₂ has coefficient column f=[2,−1], with ℓ(v)=fᵀv. Substitute v=S v′: ℓ(v)=fᵀS v′=(Sᵀf)ᵀv′. Thus f′=Sᵀf. The vector's coordinates and the functional's coefficients follow different rules so their pairing stays unchanged. Here f′=[2,1] and both measurements equal 4.</Prose>
    <Prose>The same substitution determines the matrix of a linear map. If w=A v, then S w′=A S v′, so A′=S⁻¹A S. A bilinear form such as squared length uses two vector slots instead: substituting into vᵀGv gives G′=SᵀG S. These two “two-index” arrays obey different laws because their roles differ.</Prose>
    <MathBlock>{String.raw`\begin{aligned}v'&=S^{-1}v,& f'&=S^Tf,\\ A'&=S^{-1}AS,&G'&=S^TGS.\end{aligned}`}</MathBlock>
    <Prose>If the old basis is orthonormal, G=I. The shear gives G′=[[1,1],[1,2]]. The naive new-coordinate square sum 1²+2²=5 is not the original squared length 13. Including the metric gives 1²+2×1×2+2×2²=13. Orthogonal basis changes are the special case where SᵀS=I and an ordinary dot product still works unchanged.</Prose>
    <RunnableExample example={einsumExamples.basis}><Prose><Code>solve</Code> implements the inverse action without constructing an inverse explicitly. Check preserved quantities, not equality of coordinate lists: the numbers should change while the represented vector, map action and measurement remain the same.</Prose></RunnableExample>
    <details><summary>Deeper: multilinearity, upper/lower indices and tensor products</summary><Prose>A bilinear map is linear in each argument separately while the other argument is fixed. For example B(u,v)=uᵀGv satisfies B(αu+βw,v)=αB(u,v)+βB(w,v), and the corresponding rule in its second slot. More generally a tensor of type(r,s) in a finite-dimensional real vector space can be viewed as a multilinear scalar-valued map taking r covectors and s vectors. Its components acquire one transformation factor per slot.</Prose><Prose>Upper indices conventionally mark vector-type components and lower indices covector-type components. With our S convention, each upper index contributes an S⁻¹ factor, and each lower index an S factor. The pairing fᵢvⁱ sums one of each. NumPy strings cannot encode upper versus lower variance; the author supplies the correct arrays and any metric. The word “covariant” here describes a transformation law, not statistical covariance.</Prose><MathBlock>{String.raw`\begin{aligned}(u\otimes v)^{ij}&=u^iv^j,\\ T'&=S^{-1}T(S^{-1})^T.\end{aligned}`}</MathBlock><Prose>An outer product <Code>i,j-&gt;ij</Code> stores the components of u⊗v after choosing bases. It has two vector-type slots, unlike the linear map's one-of-each type or the metric's two covector-type slots. This is why shape (2,2) cannot select the transformation formula. Tensor fields and derivatives in changing local bases require further geometry; continue with the dedicated differential-geometry topic when ready.</Prose></details>

    <H2>9. Check the program you actually wrote</H2>
    <Prose>A successful call proves only that an expression is computationally allowed. Before trusting it, write one output entry as a sum with index ranges, check physical/data identities, and compare a tiny non-square case against loops or another library operation. Unequal axis lengths make accidental swaps harder to hide. Test a mask boundary, an all-zero input, singleton axes and a changed batch count when those are relevant.</Prose>
    <RunnableExample example={einsumExamples.numerical}><Prose>A diagonal or pure permutation of one operand can be a writeable view. Editing it may edit the original, so use <Code>.copy()</Code> when independence matters. Einsum also does not automatically conjugate complex values: <Code>i,i-&gt;</Code> is a bilinear sum. A Hermitian squared length uses the conjugate of one factor. Both programs can run successfully while answering different questions.</Prose></RunnableExample>
    <LessonTable caption="Diagnose the meaning before optimizing" headers={['Symptom or tempting claim', 'What to check', 'Useful repair']} rows={[['Correct shape, wrong batch result', 'Did independent batch labels create all pairs?', 'Check stable case identities and one off-diagonal pair.'], ['Feature covariance is too large', 'Were features centered, and was the intended denominator used?', 'Compare np.cov on one batch; state the sampling convention.'], ['An integer total wraps or a float becomes nonfinite', 'Accumulation dtype, ranges and operation ordering.', 'Choose a sufficient dtype, bound inputs and test representative extremes.'], ['Attention weights do not sum to 1', 'Normalization axis and allowed-key policy.', 'Sum along key positions and explicitly handle no allowed keys.'], ['A transpose unexpectedly changes the source', 'View aliasing rather than independent storage.', 'Use np.shares_memory and an intentional copy.'], ['A longer expression must be faster', 'Operation/intermediate counts and actual hardware execution.', 'Inspect a path, then measure an equivalent workload.']]} />

    <H2>10. Practise with new questions</H2>
    <Practice title="Derive a weighted total and a diagonal" hint="A weight has the feature index, and the output keeps only the row index. Diagonal selection is a separate condition." solution={<Prose><Code>ij,j-&gt;i</Code> gives [2×2−1×3,0×2+3×3]=[1,9]. <Code>ii-&gt;i</Code> on X gives[2,3]. The weighting expression sums j; the diagonal expression ties row/column indices and keeps i. They select different input entries.</Prose>}>
      <Prose>Let X=[[2,−1],[0,3]] and w=[2,3]. Write and compute one weighted sum per row. Separately extract X's diagonal without summing it. Explain the role of every label.</Prose>
    </Practice>
    <Practice title="Repair a valid all-pairs mistake" hint="Check whether the equation uses one batch choice or two independent choices." solution={<Prose>Use <Code>bi,bi-&gt;b</Code>, giving [1×2+0×5,0×7+1×11]=[2,11]. The mistaken <Code>bi,ci-&gt;bc</Code> gives[[2,7],[5,11]]; the off-diagonal cells compare different cases. A unit test should assert both the output shape and corresponding data identities.</Prose>}>
      <Prose>X=[[1,0],[0,1]] and Y=[[2,5],[7,11]] represent aligned cases. A colleague uses <Code>bi,ci-&gt;bc</Code> to compare each case with its matching case. Give the intended expression/result and explain the extra cells in the colleague's result.</Prose>
    </Practice>
    <Practice title="Center before comparing features" hint="Compute both means, then subtract them before taking the cross-product." solution={<Prose>Means are[1,2]. Centered rows[−1,−1],[1,1] give cross-products[[2,2],[2,2]]. N−1=1, so that is also the sample covariance. Without centering, the Gram matrix is[[4,6],[6,10]], a different result. Shifting the second feature by 100 leaves centered covariance unchanged.</Prose>}>
      <Prose>For observations [0,1] and [2,3], calculate the sample covariance and the uncentered Gram matrix. Predict what adding 100 to every second-feature value changes.</Prose>
    </Practice>
    <Practice title="Choose an order with a different shape" hint="Count output entries times inner length for each of the two products." solution={<Prose>Left-first:2×20×3+2×3×4=144 multiplications, with 6 first-temporary entries. Right-first:20×3×4+2×20×4=400, with 80 entries. This establishes a count advantage under the stated dense algorithm, not an observed wall-clock ratio.</Prose>}>
      <Prose>A is(2,20), B is(20,3), C is(3,4). Which parenthesization uses fewer scalar multiplications? Report the first intermediate shape and explain what the counts cannot establish.</Prose>
    </Practice>
    <Practice title="Preserve a measurement through a basis change" hint="New basis columns are in old coordinates: solve S v′=v, then transform functional coefficients with Sᵀ." solution={<Prose>For S=[[1,1],[0,1]], v=[4,1] gives v′=[3,1]. f=[1,2] becomes f′=[1,3]. Both measurements are 6:4+2×1 and 3+3×1. Naively retaining f would give 5 in the new coordinates. The new-coordinate square sum 10 also differs from the actual squared length 17; G′=[[1,1],[1,2]] restores 9+6+2=17.</Prose>}>
      <Prose>In the same sheared basis, express v=[4,1] and the functional ℓ(v)=v₁+2v₂. Verify the same scalar before and after changing coordinates. Explain why keeping the old coefficients fails.</Prose>
    </Practice>
    <Practice title="Explain a masked retrieval without a library" hint="Equal allowed scores receive equal normalized weights; blocked values do not contribute." solution={<Prose>The two allowed weights are½ and½, so the context is½[4,−2]+½[0,6]=[2,2]. If both entries become blocked there is no allowed total to normalize. Raise an error or define an application-specific fallback explicitly; quietly applying softmax to all negative infinity produces no valid distribution.</Prose>}>
      <Prose>A query has two equally scoring allowed keys with values [4,−2] and[0,6]; every other key is blocked. Calculate the context. Then specify what should happen if both keys are blocked too.</Prose>
    </Practice>
    <Prose>You are ready to continue when you can name each axis, derive one output cell independently, explain why an index survives or disappears, diagnose a shape-valid mistake and compare equivalent execution plans without changing the mathematical question. Revisit a new expression after a delay and write its loops before running it.</Prose>
    <Callout accent="green" label="Next in this module"><a href="/learn/path/full-curriculum/randomized-linear-algebra">Randomized Linear Algebra</a> asks when we can avoid computing an entire large exact matrix result. The axis and cost reasoning here lets you describe what an approximation keeps, how its work changes and what must be checked.</Callout>
    <Sources alternatives={<><h4>Another explanation or deeper route</h4><ul>
      <li><a href="https://labs.acme.byu.edu/SupplementalLabs/AdvancedNumpy/AdvancedNumpy.html#einsteinian-summation-notation">BYU ACME — Einsteinian summation and NumPy examples</a>. A written route from sums to explicit expressions. Use the notation/common-operation sections after sections 2–4 here; their explanations were reviewed. This recommendation does not adopt the page's broad performance claims.</li>
      <li><a href="https://www.youtube.com/watch?v=H6eR3sG524M">MIT 8.962 — Tensors Continued, Scott Hughes (video)</a>, with its <a href="https://ocw.mit.edu/courses/8-962-general-relativity-spring-2020/resources/lecture-3-tensors-continued/">official course entry</a>. A graduate-level continuation of section 8 on multilinearity, covectors and invariant contractions in relativity. Transcript portions on those concepts were reviewed; the full recording was not watched. It is optional and assumes substantially more physics.</li>
    </ul></>}>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.einsum.html">NumPy — einsum reference</a>. Precise explicit/implicit, diagonal, ellipsis, view and dtype contracts. The current reference was checked; complete examples here were executed with NumPy 2.3.5.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.einsum_path.html">NumPy — einsum_path</a>. Planning controls, temporary sizes and how to interpret path entries; useful when extending section 7. Its diagnostic cost report is not a hardware benchmark.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.cov.html">NumPy — covariance reference</a>. Observation/variable orientation, centering and denominator choices used to check section 5.</li>
      <li><a href="https://openstax.org/books/university-physics-volume-1/pages/7-2-kinetic-energy">OpenStax — Kinetic Energy</a>. Classical particle/system energy and units behind the optional mass–velocity contraction. The supplied numerical fixture is invented.</li>
      <li><a href="https://arxiv.org/html/1706.03762v7#S3.SS2.SSS1">Vaswani et al. — Attention Is All You Need, section 3.2.1</a>. Original scaled dot-product definition and normalization; read after the complete arithmetic here. The paper's architecture and reported task results are beyond this example.</li>
      <li><a href="https://ocw.mit.edu/courses/8-962-general-relativity-spring-2020/e76be907216aa182691ac751d957c2c5_H6eR3sG524M.pdf">MIT tensor lecture transcript</a>. Pages 1–8 and 14–15 discuss multilinear slots, basis components, covectors and contraction. These portions support the advanced resource selection; the lesson's shear example uses ordinary Euclidean space, not spacetime.</li>
    </Sources>
  </div>
};
