import { Code, H2, H3, Prose } from '../../components/content';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram';
import libraryOutput from '../variational-inference-library-output';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { ElboBalanceLab, GaussianFamilyLab, CoordinateAscentLab, MixtureProjectionLab, VariationalGradientLab, AmortizationFigure } from '../../components/lesson-labs/VariationalInferenceLabs.jsx';
import { variationalExamples as examples } from '../variational-inference-examples.js';

function Practice({ prompt, hint, children }) {
  return <div className="lesson-check vi-practice"><p><strong>Try it independently.</strong> {prompt}</p><details><summary>Hint</summary>{hint}</details><details><summary>Show explained solution</summary>{children}</details></div>;
}

export default {
  title: 'Variational Inference',
  readTime: '~80 min read + 1–2 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot variational-inference-lesson">
    <LessonIntro prerequisites="Bayesian Inference supplies posterior distributions and prediction; Monte Carlo/MCMC supplies expectations, sampling error and diagnostic limits. We refresh those roles here. Ordinary derivatives and gradients are needed for coordinate and stochastic optimization. KL, entropy and amortization are introduced locally."
      sections={[
        ['1-fit-a-distribution-not-just-a-number', 'Unknowns, distributions and adjustable parameters'],
        ['2-build-an-objective-without-the-evidence', 'ELBO, support and a complete finite calculation'],
        ['3-see-what-a-restricted-family-loses', 'Correlation and decision uncertainty'],
        ['4-optimize-one-factor-at-a-time', 'Coordinate ascent with actual updates'],
        ['5-test-a-posterior-with-two-explanations', 'Reverse KL and missing modes'],
        ['6-differentiate-through-an-expectation', 'Pathwise/score gradients and a complete optimizer'],
        ['7-scale-data-work-and-share-inference', 'Minibatches and amortized posteriors'],
        ['8-validate-the-answer-you-will-use', 'Three errors, predictions and richer families'],
        ['9-practise-and-connect-the-next-ideas', 'Independent reasoning and next steps'],
      ]}>
      Two uncertain measurement corrections move together. Your downstream system needs their sum for one decision and their difference for another. A convenient approximation might preserve both individual means yet get those two uncertainties wrong in different directions. Variational inference gives us a way to fit an approximate distribution—and a reason to check what that fitted shape actually preserves.
    </LessonIntro>
    <Prose>In the previous topic, Monte Carlo methods estimated expectations from samples. Here we optimize the <em>distribution from which we can easily obtain answers</em>. Sampling may still be used inside that optimization. Begin with an exactly solvable target so every approximation has a reference, then build the methods used when the exact posterior is unavailable. Seven complete Python programs use the standard library and were checked with Python 3.12. Save a program as <Code>example.py</Code> and run <Code>python example.py</Code>; the browser investigations are bounded numerical models of the stated mathematics.</Prose>

    <H2>1. Fit a distribution, not just a number</H2>
    <Prose>Suppose θ is an unknown measurement correction and D is the observed data. A Bayesian model gives a posterior <Code>p(θ|D)</Code>: a distribution describing which corrections remain plausible. Its mean answers one question, but a tail probability, a correlation or a prediction requires more of its shape. MLE and MAP choose points; <strong>variational inference</strong>, or VI, chooses an approximate distribution.</Prose>
    <Prose>Choose a family of manageable distributions called q. If q is Gaussian, we might adjust its mean m and standard deviation s. Those settings are the <strong>variational parameters</strong>, collectively λ, pronounced “lambda.” The unknown correction θ is not the same thing as λ. θ is what we are uncertain about; λ controls our representation of that uncertainty.</Prose>
    <MathBlock>{String.raw`\begin{gathered}q_\lambda(\theta)=\mathcal N(\theta;m,s^2),\\ \lambda=(m,\log s).\end{gathered}`}</MathBlock>
    <Prose>We use <Code>N(mean, variance)</Code> throughout. Writing s=exp(a), where a=log s, lets an unconstrained real parameter a represent any positive standard deviation. Optimizing m alone while fixing s would search a smaller family. Optimizing both still cannot turn a single Gaussian into a two-peaked distribution.</Prose>
    <LessonTable caption="Three separate choices determine the result" headers={['Choice', 'Question', 'Example']} rows={[
      ['Model and target', 'Which distribution should the data and assumptions imply?', 'The posterior over two correlated corrections'],
      ['Approximation family', 'Which distributions can q represent?', 'Independent Gaussians versus a full covariance Gaussian'],
      ['Objective and optimizer', 'Which member is preferred, and how will we find it?', 'Maximize an ELBO by coordinate updates or stochastic gradients'],
    ]} />
    <Prose>VI is especially useful when evaluating or sampling the fitted q is convenient, or when many related inference problems can share computation. That does not guarantee that VI is faster or more accurate for every target. Its computational cost depends on the family, objective estimates, optimizer and required accuracy. A difficult multimodal target can frustrate either VI or MCMC for different reasons.</Prose>
    <Prose>Our smallest target has three possibilities A,B,C. Give each prior probability 1/3. An observed event has likelihood .24 under A, .60 under B and .36 under C. Multiplying prior by likelihood gives joint weights .08,.20,.12. Their total—the <strong>evidence</strong>—is .40. The posterior is therefore (.20,.50,.30). We know it exactly, so we can assess q without confusing a numerical result with truth.</Prose>

    <H2>2. Build an objective without the evidence</H2>
    <Prose>A distribution discrepancy must care about where probability is placed. <strong>Kullback–Leibler divergence</strong>, or KL, compares log density ratios averaged under a chosen distribution. This lesson uses KL(q‖p), often called <strong>reverse KL</strong> in this context. The order matters: the average is under q, the proposed approximation. Logs are natural logs, so these values are in nats.</Prose>
    <MathBlock>{String.raw`\operatorname{KL}(q\Vert p)=\mathbb E_q\!\left[\log\frac{q(\theta)}{p(\theta\mid D)}\right].`}</MathBlock>
    <Prose>For a finite target, the expectation is a sum of q values times their log ratios. For a continuous target it is an integral of densities. A particular contribution can be negative where q&lt;p, but the total KL is nonnegative. It is zero exactly when the two probability distributions agree, apart from zero-probability differences. KL is not a symmetric distance and does not satisfy all the usual distance rules.</Prose>
    <Prose>Bayes' rule writes the posterior as the joint model divided by evidence. Substitute that ratio inside the logarithm. The evidence depends on the fixed data and model, but not on λ. We can therefore drop it when finding the best λ even if we cannot evaluate it.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\log p(\theta\mid D)&=\log p(D,\theta)\\ &\quad-\log p(D),\\ \mathcal L(q)&=\mathbb E_q[\log p(D,\theta)]\\ &\quad-\mathbb E_q[\log q(\theta)].\end{aligned}`}</MathBlock>
    <MathBlock>{String.raw`\log p(D)=\mathcal L(q)+\operatorname{KL}(q\Vert p).`}</MathBlock>
    <Prose>ℒ is the <strong>evidence lower bound</strong>, or ELBO. Maximizing it minimizes the reverse KL for this fixed target. The unknown evidence prevents calculating the absolute KL in general; it does not prevent optimizing an equivalent objective or its gradient. Computing the ELBO itself may still require approximate expectations. “No evidence integral” is not “no numerical work.”</Prose>
    <Prose>The term −E_q log q is <strong>entropy</strong>. In a finite distribution it rewards spreading mass instead of collapsing everything onto a high joint-weight state. The expected log joint rewards mass in model-supported regions. More explicitly, separate prior and likelihood:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathcal L(q)&=\mathbb E_q[\log p(D\mid\theta)]\\ &\quad-\operatorname{KL}(q\Vert p(\theta)).\end{aligned}`}</MathBlock>
    <Prose>The rightmost p is the <em>prior</em>, not the posterior. This form expresses likelihood fit balanced against deviation from the prior; it is more precise than an unexplained “complexity penalty.” Continuous differential entropy can be negative and changes with coordinates. The whole objective must use compatible densities and change-of-variable terms; entropy by itself is not a coordinate-free accuracy score.</Prose>
    <ElboBalanceLab />
    <Prose>For our three-state target, log evidence≈−.916291. The uniform q has ELBO≈−.986531 and gap≈.070240. If we demand qB=qC, the target is unreachable because its B and C probabilities differ. Write the restricted distribution as <Code>(a,(1−a)/2,(1−a)/2)</Code>. Its best a is not simply the original posterior mass .2: the reverse-KL optimum shifts the whole allocation slightly.</Prose>
    <MathBlock>{String.raw`a^*=\frac{p_A}{p_A+2\sqrt{p_Bp_C}}\approx0.205213.`}</MathBlock>
    <Prose>To obtain that result, differentiate the restricted KL with respect to a. Its derivative is log(a/pA) minus half the sum of log((1−a)/(2pB)) and log((1−a)/(2pC)). Setting it to zero gives a/(1−a)=pA/(2√(pBpC)). The second derivative 1/a+1/(1−a) is positive inside(0,1), so this is the unique minimum there; check endpoints as well. Its residual gap is .025732. A better optimizer cannot remove this restriction.</Prose>
    <RunnableExample example={examples.finite}><Prose>The program computes every quantity from the same joint weights and checks ELBO+KL=log evidence. Releasing the restriction allows q to equal the posterior exactly. This is a controlled example of family error versus optimization error; no Monte Carlo noise is present.</Prose></RunnableExample>
    <H3>Support and normalization are part of the contract</H3>
    <Prose>We assume a positive finite evidence and normalized q. If q assigns positive probability to a region where the target is zero, reverse KL is infinite and the ELBO is −∞. If q assigns zero mass where p is positive, reverse KL does not automatically become infinite: the average never visits that missing region. That asymmetry will matter for separated modes. A zero-q discrete term contributes 0 by its limit; positive q times log 0 does not.</Prose>
    <Prose>An unnormalized joint can be used for optimization when its omitted factor is independent of λ. But dropping that constant changes the reported ELBO value. Exact ELBOs from different families are directly comparable for the <em>same fully specified joint and data</em>: higher means smaller KL. Comparing different omitted constants, noisy estimates without accounting for their precision, or different models/data is a different problem. An ELBO is a lower bound, not an exact marginal likelihood for ranking arbitrary models.</Prose>
    <Checkpoint prompt="You multiply every joint weight by10. Does the best q change? Do the displayed ELBO and KL both increase?"><Prose>The posterior and best q stay the same because the normalization cancels 10. Every ELBO increases by log 10, and log evidence does too. KL remains unchanged. A raw objective value is interpretable only with its model, data and retained constants.</Prose></Checkpoint>

    <H2>3. See what a restricted family loses</H2>
    <Prose><strong>Mean-field</strong> inference factors the approximation into distributions over coordinates or blocks: q(θ)=∏q_j(θ_j). The factors need not be Gaussian, and a block can contain several dependent coordinates. Independence is a restriction on q; it is not evidence that the actual posterior coordinates are independent. Coordinate-wise independent Gaussian factors have a diagonal covariance matrix and cannot encode a tilted posterior cloud.</Prose>
    <Prose>Consider an exactly Gaussian posterior with zero means, marginal variances 1 and correlation ρ. Its covariance is Σ with diagonal entries 1 and off-diagonal entriesρ. Positive correlation means that positive values of one correction tend to accompany positive values of the other. A negative correlation means opposing deviations are favored.</Prose>
    <Prose>The ellipse below joins locations with the same standardized distance from the center, accounting for the covariance. This is called <strong>Mahalanobis distance</strong>. A tilted ellipse shows a direction in which joint deviations are less surprising; a round contour has no preferred direction. The chosen radius is a fixed comparison level, not the probability of an exact point.</Prose>
    <GaussianFamilyLab />
    <Prose>For this target, the best independent Gaussian under reverse KL has the correct means but each variance is 1−ρ². At ρ=.8, each is .36. The product of the <em>true marginal distributions</em> would instead use variance 1. Both candidates discard dependence, but they answer different optimization questions: the product of true marginals is the best independent approximation under forward KL(p‖q), not generally reverse KL(q‖p). Under forward KL, the terms involving log q_j are averaged against the true marginal for coordinate j. Matching each of those marginals minimizes its contribution, even though their product still loses the joint dependence.</Prose>
    <Prose>Here is why reverse KL chooses .36. The target's quadratic log density uses the <strong>precision</strong> matrix P=Σ⁻¹. Precision measures how strongly departures in each direction are penalized. For an independent Gaussian q with variances v_j, the variance-dependent part of KL is half the sum of P_jj v_j−log v_j. Differentiating gives P_jj−1/v_j=0, so v_j=1/P_jj. For our covariance, P_jj=1/(1−ρ²).</Prose>
    <MathBlock>{String.raw`\begin{gathered}P=\Sigma^{-1},\qquad v_j^*=1/P_{jj},\\ \operatorname{KL}_{\rm best}=-\tfrac12\log(1-\rho^2).\end{gathered}`}</MathBlock>
    <Prose>At ρ=.8 the best family gap is .510826. Increasing optimization iterations cannot make a diagonal covariance tilt. A full covariance Gaussian can recover this particular target exactly; that is a property of the target and family, not a promise for arbitrary posteriors.</Prose>
    <Prose>Now return to the two decisions. Variance of a sum includes covariance twice; variance of a difference subtracts it twice. Under the true target, Var(θ₁+θ₂)=3.6 and Var(θ₁−θ₂)=.4. Under the best mean-field q, both are .72. The sum is too certain; the difference is <em>too uncertain</em>. Even though both marginal variances are underestimated, it would be wrong to say every uncertainty is underestimated.</Prose>
    <RunnableExample example={examples.covariance}><Prose>The double sum computes wᵀΣw for each requested linear quantity. In a calibration application, two correlated corrections can jointly amplify a total offset while largely canceling in a difference. The required approximation quality depends on which of those quantities drives the action.</Prose></RunnableExample>
    <details><summary>Deeper: derive and check the full Gaussian discrepancy</summary><Prose>For q=N(m,C) and p=N(μ,Σ) in d dimensions, take the expectation of their quadratic log-density difference. E_q[(θ−μ)(θ−μ)ᵀ]=C+(m−μ)(m−μ)ᵀ supplies the trace and mean-error terms. The log normalizers supply the determinant ratio.</Prose><MathBlock>{String.raw`\begin{gathered}\delta=m-\mu,\quad P=\Sigma^{-1},\\ 2\operatorname{KL}(q\Vert p)=\operatorname{tr}(PC)\\ \quad+\delta^\top P\delta-d+\log\frac{\det\Sigma}{\det C}.\end{gathered}`}</MathBlock><Prose>Both covariances must be positive definite for this formula. If C=Σ and m=μ, the trace is d and the other terms vanish. If C is diagonal, the earlier variance derivatives follow. Mean-field results depend on coordinate choice: rotating or whitening a Gaussian can remove its correlation, but a nonlinear or multimodal posterior is not necessarily fixed by a linear rotation.</Prose></details>

    <H2>4. Optimize one factor at a time</H2>
    <Prose>Choosing the family defines the search space. <strong>Coordinate-ascent variational inference</strong>, or CAVI, improves one factor while holding the others fixed. “Coordinate” here means an entire distribution factor, not just one sampled value. Its update has a useful exact form when the required expectation and normalizer are tractable.</Prose>
    <Prose>Focus on factor j. Average the log joint over all other factors, leaving θ_j as the variable. Call the resulting function f_j. Exponentiate it and normalize to obtain the new factor. This uses the expected <em>log</em> joint; averaging the joint first generally gives a different answer.</Prose>
    <MathBlock>{String.raw`\begin{gathered}f_j(\theta_j)=\mathbb E_{q_{-j}}[\log p(D,\theta)],\\ q_j^{\rm new}(\theta_j)\propto\exp f_j(\theta_j).\end{gathered}`}</MathBlock>
    <Prose>Why does that improve the objective? With the other factors fixed, the relevant terms are E_qj f_j−E_qj log q_j. If r_j is the normalized exponential of f_j, those terms equal log∫exp(f_j) minus KL(q_j‖r_j). Setting q_j=r_j removes that nonnegative discrepancy. The update therefore cannot decrease the exact ELBO, provided the normalizer is finite and r_j is allowed by the chosen factor family. A further parametric restriction may require numerical optimization instead.</Prose>
    <Prose>Use the same covariance as before, but move the target mean to (1,−1) so the arithmetic is visible. In the log joint, the θ₁ terms form a quadratic whose conditional center is 1+ρ(θ₂+1). Averaging over q₂ replaces θ₂ by its current mean m₂. The updated q₁ is Gaussian with mean 1+ρ(m₂+1) and variance 1−ρ². Then use the newly updated m₁ to update q₂.</Prose>
    <MathBlock>{String.raw`\begin{aligned}m_1&\leftarrow1+\rho(m_2+1),\\ m_2&\leftarrow-1+\rho(m_1-1),\\ v_1=v_2&\leftarrow1-\rho^2.\end{aligned}`}</MathBlock>
    <Prose>Start the means at (−2,2), set ρ=.8, and hold the variances at their optimal .36. The first update sends m₁ to 1+.8×3=3.4. The second sends m₂ to−1+.8×2.4=.92. These can look like overshooting the final mean if you ignore the other coordinate. Each is optimal given the other factor's current state.</Prose>
    <CoordinateAscentLab />
    <RunnableExample example={examples.coordinates}><Prose>Each individual update decreases KL here, but even the limit retains .510826 of family error. After 24 updates, the means are close to(1,−1) and total KL≈.510982. In this quadratic example a full sweep multiplies the remaining error in m₂ byρ², so stronger absolute correlation slows the coordinate correction. At ρ=0, the two independent updates recover the exact target.</Prose></RunnableExample>
    <Prose>The Gaussian example has a unique best member within this family. General VI objectives are often nonconvex in their parameters. A nondecreasing ELBO can approach a local stationary solution, and flat progress alone does not prove global optimality. Noisy approximate updates also do not inherit exact CAVI's per-step monotonicity automatically. Multiple starts and more reliable objective evaluations help separate those situations.</Prose>

    <H2>5. Test a posterior with two explanations</H2>
    <Prose>A model can leave two qualitatively different explanations plausible. To isolate that issue, use an equally weighted mixture of two unit-variance Gaussians centered at−3 and+3. A single Gaussian family can be broad enough to cover both peaks, or narrow enough to concentrate near one, but cannot reproduce both peaks and their low-density valley simultaneously.</Prose>
    <MathBlock>{String.raw`\begin{aligned}p(\theta)&=\tfrac12\mathcal N(\theta;-3,1)\\ &\quad+\tfrac12\mathcal N(\theta;3,1).\end{aligned}`}</MathBlock>
    <Prose>Reverse KL averages log(q/p) under q. A broad q repeatedly places draws in the valley where p is small, adding a penalty. A narrow q on one peak gives the other peak almost no weight in its own average. That can make one-mode concentration preferable under this objective. “Mode-seeking” describes this tendency under suitable targets and families; it is not a theorem that every reverse-KL fit chooses exactly one mode.</Prose>
    <MixtureProjectionLab />
    <RunnableExample example={examples.mixture}><Prose>The original lesson's three KL results are preserved: .877 for N(0,9), and .689 for each N(±3,1). The first output column uses the mean and the second uses standard deviation. The comparison is among those candidates; it does not assert that means±3 and standard deviation 1 are the exact global minimizers. Stable log-sum-exp avoids evaluating a tiny mixture density before taking its logarithm, and halving the quadrature spacing checks the displayed precision.</Prose></RunnableExample>
    <Prose>The target gives positive values probability .5. The right-peak candidate reports .998650 and the left-peak candidate .001350. If θ's sign chooses a control direction or distinguishes two physical configurations, this difference matters even when the fitted objective is stable. A posterior mean near zero also fails to communicate the two modes if zero lies in the valley. Report the relevant distributional ambiguity.</Prose>
    <Prose>A mixture approximation can represent both explanations, although fitting it introduces its own component and optimization issues. A richer dependent family or flow can help in other geometries. MCMC is a useful comparison only when it actually explores both regions; a chain stuck on one peak is not an independent certificate that the one-mode VI answer is correct. Check multiple starts, mode occupancy and the function you intend to report.</Prose>

    <H2>6. Differentiate through an expectation</H2>
    <Prose>Closed-form coordinate updates are not always available. We can instead estimate an ELBO gradient and update λ with an optimizer. There are two distinct randomness sources to keep separate: draws from the current q for expectations, and subsets of observations for large-data calculations. We begin with the first.</Prose>
    <H3>Reparameterization fixes the noise and moves its transformation</H3>
    <Prose>For q=N(m,s²), draw ε∼N(0,1), then form θ=m+sε. The noise distribution no longer depends on the adjustable parameters. If the target log density is differentiable and differentiation can pass through the expectation, the chain rule tells us how moving m or a=log s changes each draw's contribution. This is a <strong>pathwise</strong> or <strong>reparameterized</strong> gradient.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\theta=m+e^a\epsilon,\quad\epsilon\sim\mathcal N(0,1),\\ \mathcal L(m,a)=\mathbb E_\epsilon[\log p(D,m+e^a\epsilon)]\\ \qquad+a+\tfrac12\log(2\pi e).\end{gathered}`}</MathBlock>
    <Prose>The last terms are the exact Gaussian entropy, so its derivative with respect to a is 1. Write g(θ)=∂ log p(D,θ)/∂θ. The gradient contribution for m is g(θ); for a it is 1+sεg(θ). Average those contributions from independent noise draws. The +1 prevents a fit-only update from simply squeezing away all uncertainty.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\partial_m\mathcal L&=\mathbb E[g(\theta)],\\ \partial_a\mathcal L&=1+\mathbb E[s\epsilon g(\theta)].\end{aligned}`}</MathBlock>
    <Prose>As a fully checkable target, choose p=N(1.5,.7²). Then g(θ)=(1.5−θ)/.49. The exact gradients are (1.5−m)/.49 and 1−s²/.49. At m=1.5,s=.7, both expectations are zero. The browser shows what individual contributions do before their average approaches those references.</Prose>
    <H3>A score gradient differentiates the sampling density instead</H3>
    <Prose>The <strong>score</strong> is ∇_λ log q_λ(θ), with θ held fixed during that derivative. Using ∇q=q∇log q turns the derivative of an expectation into another expectation. For the ELBO, the extra derivative of −log q integrates to zero because a normalized distribution's score has expectation zero. The result is:</Prose>
    <MathBlock>{String.raw`\begin{gathered}f(\theta)=\log p(D,\theta)-\log q_\lambda(\theta),\\ \nabla_\lambda\mathcal L=\mathbb E_q[f(\theta)\nabla_\lambda\log q_\lambda(\theta)].\end{gathered}`}</MathBlock>
    <Prose>For our Gaussian, the two score components are (θ−m)/s² and(θ−m)²/s²−1. Multiply each by log joint−log q and average. This estimator does not need a differentiable target with respect to θ, so it can apply to discrete latent variables when q has a differentiable parameterization and the required regularity holds. Its variance can be large. Subtracting a constant baseline from the log ratio leaves the expectation unchanged because the score averages to zero; a baseline fitted on the same draws requires care and is not automatically unbiased.</Prose>
    <VariationalGradientLab />
    <Prose>Pathwise methods often make useful use of local target gradients, but “always lower variance” is false. In this normalized Gaussian example, when q=p, log p−log q=0 for every draw, so the displayed score estimator is exactly zero. Our pathwise estimator still gives a random −ε/.7 for the mean and 1−ε² for log scale. Both have zero expectation; only one is identically zero per draw. If an unknown log-evidence constant remained in the joint, the raw score estimator would include that constant times the score and this zero-variance statement would no longer hold without an appropriate baseline.</Prose>
    <Prose>The derivations require more than writing a gradient symbol: q must be normalized, the relevant expectations and derivatives must exist, and differentiation must be valid under the integral or sum. Parameter-dependent support can contribute boundary terms. For a Uniform(0,a), for example, changing a moves the boundary; blindly applying the fixed-support score argument loses part of the derivative. A discontinuous discrete sample is also not made ordinarily differentiable just by using automatic differentiation. Choose an estimator appropriate to the distribution and objective.</Prose>
    <RunnableExample example={examples.optimizer}><Prose>This is a complete stochastic optimizer, not just a gradient formula. It takes 64 fresh independent noise draws at each update, uses log standard deviation to preserve positivity, and performs Adam <em>ascent</em>. The target is known only so we can check the result. After 2,000 updates this seeded run gives m≈1.4680,s≈.6858, with exact KL≈.001462. The 500-update objective happens to be better: finite stochastic optimization is not monotone, and a later iterate is not automatically a better approximation.</Prose></RunnableExample>
    <Prose>Changing the seed changes the finite result. A practical fit needs learning-rate and stopping choices, independent higher-precision objective evaluation, and checks of the quantities to be reported. Reusing the same noise can make finite-difference comparisons and parameter effects easier to inspect; optimizing indefinitely against a tiny fixed noise set can overfit that numerical approximation. The program uses fresh draws for optimization, while the lab intentionally holds noise fixed for a causal comparison.</Prose>

    <H2>7. Scale data work and share inference</H2>
    <H3>Minibatches estimate a data sum, not the whole model repeatedly</H3>
    <Prose>Suppose N observations are conditionally independent given a global θ. Their joint log likelihood is a sum. The ELBO contains that sum plus the prior and entropy terms once. If a batch B of b observation indices is sampled uniformly, multiplying its likelihood sum by N/b gives an unbiased estimate of the full likelihood sum. Each observation is included with probability b/N for a uniform fixed-size batch without replacement.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\widehat{\mathcal L}_B&=\frac{N}{b}\sum_{i\in B}\mathbb E_q[\log p(x_i\mid\theta)]\\ &\quad-\operatorname{KL}(q\Vert p(\theta)).\end{aligned}`}</MathBlock>
    <Prose>Do not multiply the prior KL by N/b too. This model has one global θ and one prior, not a new prior for every selected row. Nonuniform sampling needs appropriate weighting, dependent data need the correct likelihood structure, and local latent-variable terms must be accounted for with the observations they belong to. Dividing an entire objective by N is possible, but then every term must use that same overall normalization.</Prose>
    <RunnableExample example={examples.minibatches}><Prose>For data[1,2,4,5], q mean 1 and standard deviation.5, the six possible two-observation batches yield mean-gradients 1,5,7,7,9,13. Their average 7 equals the full gradient. Scaling the prior as well gives an average 6: systematic bias, not Monte Carlo noise that more batches will remove. This conjugate target has posterior mean 2.4 and variance.2, supplying a separate inference reference.</Prose></RunnableExample>
    <Prose>In general speech, stochastic VI can mean optimizing a variational objective with noisy gradients. The original <a href="https://www.jmlr.org/papers/v14/hoffman13a.html">stochastic variational inference construction</a> targets a specific scalable setting with global and per-observation local latent variables, conjugate exponential-family structure and natural gradients. It optimizes the selected observations' local factors, scales their sufficient-statistic contribution, then moves the global distribution toward the resulting full-data estimate. That is more specific than merely using Adam on any approximate posterior.</Prose>
    <details><summary>Deeper: why the original SVI update looks like a weighted average</summary><Prose>A natural parameter η is a coordinate in an exponential-family distribution; the next lesson develops that representation. In the conditionally conjugate global/local setting, let η₀ denote prior contributions and t_i the expected sufficient-statistic contribution of observation i under its optimized local factor. A uniformly sampled batch proposes η̂=η₀+(N/b)∑t_i. The natural gradient of the optimized ELBO has the form η̂−η. A step of sizeρ_t therefore yields the following update.</Prose><MathBlock>{String.raw`\eta_{t+1}=(1-\rho_t)\eta_t+\rho_t\widehat\eta_t.`}</MathBlock><Prose>This averages <em>natural parameters</em>, not arbitrary posterior draws or all distribution moments. The result relies on this model structure and the required local optimization, not every variational model. Classical stochastic-approximation results use conditions such as∑ρ_t=∞ and∑ρ_t²&lt;∞ alongside regularity, stability and appropriate noise assumptions; the schedule alone is not a universal convergence proof. See the original paper's sections 2.3–2.4 for the derivation and assumptions.</Prose></details>
    <H3>Amortization learns a rule for many inference problems</H3>
    <Prose>So far we fit λ for one dataset. If thousands of observations each require a separate local posterior, we could optimize each λ_i independently. <strong>Amortized inference</strong> instead fits shared parameters φ in a function that maps an observation x to its distribution parameters: λ(x)=f_φ(x). The cost of learning that mapping is shared across many later inference cases.</Prose>
    <Prose>A complete simple example needs no neural network. Let each independent case have its own θ∼N(0,1), and observe one noisy measurement x|θ∼N(θ,1). Completing the square gives posterior N(x/2,.5). The shared mapping m(x)=.5x and s(x)=√.5 returns the exact answer for every x. It uses the same function parameters but different posterior means.</Prose>
    <AmortizationFigure />
    <RunnableExample example={examples.amortization}><Prose>The observations−2,0,2 are separate cases, not three sequential measurements of the same unknown. A future measurement of each case's same θ has variance 1+.5=1.5: measurement noise plus remaining latent uncertainty. Using only the fitted mean and observation variance 1 would discard posterior uncertainty. A forced mean-zero encoder misses the x=2 case by KL 1 even if its variance is correct.</Prose></RunnableExample>
    <Prose>For an expressive per-case family, a restricted shared mapping can still fail to reach each case's best λ. That extra restriction is called an <strong>amortization gap</strong>; finite optimization of the shared mapping adds another gap. Our linear Normal example has no unavoidable amortization gap because the exact rule is representable. In a variational autoencoder, an encoder predicts an approximate latent posterior and a decoder defines a data model; training often changes both inference and model parameters. That joint learning problem is developed in the dedicated VAE &amp; ELBO topic. The fixed-target comparisons above hold the data model fixed.</Prose>

    <H2>8. Validate the answer you will use</H2>
    <Prose>A well-behaved loss curve answers a computational question. It does not by itself show that the posterior shape or the data-generating assumptions are suitable. Keep three error sources separate. If the best family member q* exists and the obtained fit is q̂, its reverse KL decomposes into the best achievable family KL plus the excess KL from the obtained fit. Numerical estimates of either quantity introduce their own error.</Prose>
    <LessonTable caption="Different failures need different remedies" headers={['Source', 'What remains wrong?', 'A useful check or remedy']} rows={[
      ['Family approximation', 'Missing covariance, tails, modes or allowed support even at the best fit', 'Compare meaningful quantities against an exact target or independently diagnosed sampler; enrich or transform the family'],
      ['Optimization', 'The achieved parameters are worse than the best allowed ones', 'Multiple starts, gradients, objective precision, conditioning and a suitable stopping rule'],
      ['Numerical estimation', 'Finite draws or quadrature only approximate expectations/gradients', 'Independent estimates, MCSE, more independent draws, control variates or quadrature refinement'],
    ]} />
    <Prose>More draws from a fixed q make its estimated expectations more precise. They do not move q toward a missing mode or restore covariance. In the finite experiment, the best restricted gap is .025732 with no numerical noise. In CAVI, the family gap remains .510826 as the optimization gap shrinks. In the gradient investigation, a large sample budget reduces typical noise while leaving the current q unchanged until an update is applied.</Prose>
    <Prose>Start validation with the quantity that drives the decision: a sum, a difference, a tail probability, a predictive interval or a ranking. Compare distributions of that quantity, not only parameter means. Simulate future observations by drawing θ from q <em>and then</em> observations from the likelihood. This checks the q-based predictive distribution, which still inherits approximation error and model assumptions. Good predictive fit alone does not establish a correct latent posterior; different latent explanations can make similar predictions.</Prose>
    <Prose>Use exact conjugate cases and independently evaluated low-dimensional targets as development checks. When comparing with MCMC, assess its exploration and function-specific numerical precision first. Test several initializations and meaningful family changes. Check that objectives retain the same constants and that noisy differences are larger than their numerical uncertainty. A tighter lower bound under one family is progress on that objective; the effect on a particular decision still deserves inspection.</Prose>
    <H3>Choose a richer representation for the missing structure</H3>
    <Prose>A full covariance Gaussian introduces pairwise linear dependence, with a valid positive-definite parameterization such as C=LLᵀ. A structured factorization retains selected dependencies in blocks. A mixture can represent distinct explanations. A normalizing flow transforms an easy base distribution through an invertible differentiable map with a tractable Jacobian determinant, giving a more flexible density. These choices change memory, evaluation, sampling and optimization costs; flexibility does not guarantee good finite optimization.</Prose>
    <Prose>For constrained unknowns, geometry also depends on coordinates. If θ&gt;0 and u=log θ, inference in u must use the transformed joint p(D,exp u)exp u. That last factor is the Jacobian. When mapping an approximation back, q_θ(θ)=q_u(log θ)/θ. A Gaussian in u is a lognormal distribution in θ, not a Gaussian approximation on the original scale. The MCMC lesson showed the same accounting principle; VI also changes when the family is defined in different coordinates.</Prose>
    <Prose>Tools such as Stan's ADVI implement particular families and stochastic optimization choices, not a universal guarantee of accurate uncertainty. Read the tool's actual coordinate transformations, family and stopping semantics. The lesson's bounded models are transparent investigations, not drop-in replacements for a general inference engine. On a real model, check support, finite log densities, derivatives, diagnostic warnings and the consequential posterior quantities.</Prose>

    <H3>Fit the tractable target with a maintained VI engine</H3>
    <Prose>Our coordinate updates expose what mean-field VI optimizes. To use the mechanism through an ordinary tool, this complete PyMC ADVI example fits the same Gaussian family problem with target mean (1,−1), unit marginal variances and correlation .8. We deliberately define a normalized Gaussian target with no observations: it is a controlled inference check, not a claim to have learned from a dataset. No evidence-normalization constant is unknown here, so the exact Gaussian KL independently checks the tool's result.</Prose>
    <Prose><Code>pm.MvNormal</Code> defines the target, and <Code>pm.fit(method="advi")</Code> selects an independent Gaussian in its unconstrained coordinates. Our θ already lies in ℝ², so there is no coordinate transformation or extra Jacobian. The run fixes 15,000 Adam updates, learning rate .01, 20 Monte Carlo samples for each stochastic gradient estimate, and seed 51. Those samples estimate the optimization gradient; they are not 20 posterior samples retained for the final check. The fixed budget does not certify convergence.</Prose>
    <MechanismProgram source="/learn-assets/variational-inference/pymc_variational.py" title="Open the complete PyMC ADVI / exact-KL comparison" output={libraryOutput} />
    <Prose>For target covariance Σ, the independent reverse-KL optimum has mean equal to the target mean and variance 1/(Σ⁻¹)ᵢᵢ in each coordinate. Here that variance is 1−.8²=.36, not the target's marginal variance 1. The exact best-family KL is −½log(.36)=.510826. The PyMC 6.3.2 run fits means [.9865,−.9703] and variances [.3666,.3600], with achieved KL .513277: family gap .510826 plus remaining optimization gap .002451.</Prose>
    <Prose>The verifier evaluates KL from the fitted mean and variance using a Cholesky solve and log determinant. It never estimates this comparison by sampling q, so the reported gap has no final q-sampling error. Stochastic optimization still affects the fitted parameters. The printed “Average Loss” from PyMC is a noisy running optimization statistic, not this exact achieved KL; do not compare those two numbers as if they were identical calculations.</Prose>
    <Prose>Mean-field variational state is O(d), while full covariance needs O(d²) parameters and additional matrix work. Evaluating a dense Gaussian target can still require quadratic work per draw after factorization: a simple family does not make every model cheap. Our two-dimensional exact oracle is a development check; general posteriors require the decision-specific validation described above. More updates can reduce the optimization gap but cannot remove the .510826 family gap.</Prose>
    <Checkpoint prompt="Set correlation to zero while keeping mean, family, budget and optimizer fixed. Which part of the discrepancy can now vanish, and did the library run attain exactly zero?">
      <Prose>The target now belongs to the independent family, so the exact family gap is zero and optimal variances are [1,1]. The second executed fit has variances [1.0063,.9869] and achieved KL .000547, all remaining optimization error. It is close, not exact. The output separates family error, finite optimization and final expectation-estimation noise rather than labeling every discrepancy “sampling error.”</Prose>
    </Checkpoint>

    <H2>9. Practise and connect the next ideas</H2>
    <Practice prompt="Change the finite joint weights to(.1,.3,.1). Find the posterior, the best restricted q with B=C, and explain why its A mass differs from the exact posterior. Verify the ELBO identity." hint={<Prose>Evidence is.5. Use a*=pA/(pA+2√(pBpC)); compute the other masses from normalization. You can adapt the first complete program, but change all target-dependent values.</Prose>}><Prose>The target is(.2,.6,.2). The denominator is.2+2√.12≈.892820, so q≈(.224009,.387995,.387995). Its KL is−log(.2+2√.12)≈.113370, and ELBO=log(.5)−KL≈−.806517. Reverse KL selects all masses jointly; simply preserving pA=.2 is not the constrained optimum. Independently sum q log(q/p) and the ELBO terms to check the identity.</Prose></Practice>
    <Practice prompt="A target puts probability(.4,.6,0) on A,B,C. Compare q=(.5,.5,0), q=(.4,.5,.1), and q=(1,0,0). Which KLs are infinite?" hint={<Prose>Ask where q has positive mass but p has none. A missing target region behaves differently from an invented impossible one.</Prose>}><Prose>Only(.4,.5,.1) has infinite reverse KL because it assigns positive mass to impossible C. The first has finite KL .5log(.5/.4)+.5log(.5/.6)≈.020411. The last has finite KL log(1/.4)≈.916291 even though it ignores B. Reverse KL is sensitive to omitted mass through normalization and the remaining density ratios; it does not impose an automatic infinite penalty for every omitted target region.</Prose></Practice>
    <Practice prompt="Change the Gaussian correlation to−.6. Find the best mean-field marginal variance and the variances of sum and difference under p and q. Which decision is too certain?" hint={<Prose>Mean-field variance is 1−ρ². The target sum and difference variances are 2±2ρ.</Prose>}><Prose>The independent variance is.64. Target sum variance is.8 and difference variance 3.2; q gives 1.28 for both. The difference is too certain, while the sum is too uncertain. KL=−.5log(.64)≈.223144. A description of “underestimated uncertainty” without naming the reported quantity would hide this reversal.</Prose></Practice>
    <Practice prompt="Use the shifted target mean(1,−1), correlation.5 and initial means(0,0). Do one complete CAVI sweep. Then state which part of the remaining discrepancy more iterations can remove." hint={<Prose>Update m₁ first using the old m₂; update m₂ using the newly changed m₁. The variance is.75.</Prose>}><Prose>The first mean becomes 1+.5×1=1.5; the second becomes−1+.5×.5=−.75. Total KL is−.5log(.75)+.125≈.268841. The .125 optimization gap can shrink. The best family gap−.5log(.75)≈.143841 remains until the family can express covariance.</Prose></Practice>
    <Practice prompt="A two-mode target gives θ>0 probability.5. Two runs produce Gaussian fits centered near opposite modes, each with a similar ELBO. What should a sign-based controller receive, and what comparison would actually be convincing?" hint={<Prose>Averaging the two reported means creates a point in the valley. A single stuck MCMC run can reproduce the same failure.</Prose>}><Prose>Report the unresolved sign ambiguity, rather than whichever initialization happened to win. A mixture giving both modes suitable weight can express it; validate those weights and the sign probability against a trustworthy reference or a sampler that demonstrably explores both regions with adequate precision. Merely averaging parameter means does not produce that mixture. When mixing distributions, combine their densities or sample from the mixture explicitly.</Prose></Practice>
    <Practice prompt="For target N(2,1), take q=N(0,4) and noise ε=.5. Compute the draw and the pathwise contributions for m and a=log s. Compare with the exact expected gradients." hint={<Prose>s=2, θ=m+sε, target slope is 2−θ. The entropy contribution to the log-scale gradient is 1.</Prose>}><Prose>The draw is 1 and target slope 1. The single-draw contributions are 1 for m and 1+2×.5×1=2 for a. Their exact expectations are 2 and 1−4=−3. One draw even points the scale in the opposite direction; unbiasedness concerns the average over repeated draws, not correctness of every realized direction. Averaging more independent draws improves typical precision, while a suitable optimizer still needs a step-size policy.</Prose></Practice>
    <Practice prompt="For the minibatch example, change batch size from2 to1. Explain the weighting and calculate the expected wrong mean-gradient if you multiply the prior term by4 as well." hint={<Prose>The full likelihood mean-gradient is 8 and the one prior contribution is−1. Uniformly average all four one-row estimators.</Prose>}><Prose>The correct estimator is 4(x_i−1)−1, with values−1,3,11,15 and average 7. The wrong estimator is 4[(x_i−1)−1], with average 8−4=4. More repetitions converge toward that biased 4, not toward 7. The data scaling factor belongs to the data sum.</Prose></Practice>
    <Practice prompt="In the amortized Normal example, change observation-noise variance to4 while keeping prior variance1. Derive the shared posterior rule and the variance of a new measurement. What if the encoder keeps using the old rule?" hint={<Prose>Add prior precision 1 and data precision 1/4. The posterior precision is their sum; weight x by its observation precision before dividing.</Prose>}><Prose>The posterior variance is 1/(1+1/4)=.8 and mean is(x/4)/(1.25)=x/5. A new measurement has variance 4+.8=4.8. The old encoder m=x/2,variance.5 is now systematically wrong, even if it was exact for the old model. This is a changed-model failure, not an optimizer or Monte Carlo problem. The inference rule must match the observation process.</Prose></Practice>
    <Prose>You can now calculate a complete variational objective, identify restrictions in a fitted distribution, execute factor and stochastic updates, and diagnose what additional computation can actually fix. The next topic in this module is <strong>Exponential Families &amp; Sufficient Statistics</strong>. It explains the compact distribution coordinates and data summaries that make many conjugate and stochastic-VI updates algebraically simple. The later entropy/KL, VAE and flow topics develop their own broader ideas; the present lesson supplies the inference bridge.</Prose>
    <Sources>
      <li><a href="https://arxiv.org/html/1601.00670v9">Blei, Kucukelbir &amp; McAuliffe — Variational Inference: A Review for Statisticians</a>: sections 2.2–2.5 provide the ELBO, factor families and CAVI; section 4 develops conjugate stochastic inference. A mathematical written continuation after the finite and Gaussian examples. The linked 2018 arXiv revision is of the 2017 review.</li>
      <li><a href="https://proceedings.mlr.press/v33/ranganath14.html">Ranganath, Gerrish &amp; Blei — Black Box Variational Inference</a>: the original 2014 score-gradient construction and variance-reduction discussion. Useful after the gradient section; its broadly applicable estimator still needs the stated mathematical regularity.</li>
      <li><a href="https://www.jmlr.org/papers/v14/hoffman13a.html">Hoffman et al. — Stochastic Variational Inference</a>: sections 2.3–2.4 derive the natural-gradient and global/local update. Advanced reading with exponential-family prerequisites; distinguish its structured algorithm from generic noisy-gradient VI.</li>
      <li><a href="https://arxiv.org/abs/1312.6114">Kingma &amp; Welling — Auto-Encoding Variational Bayes</a>: sections 2–3 connect reparameterized variational gradients to learned inference mappings and generative models. A primary bridge to the later VAE lesson.</li>
      <li><a href="https://mc-stan.org/docs/reference-manual/variational.html">Stan Reference Manual — Variational Inference</a>: the current ADVI optimization and approximate-ELBO stopping description, checked against the 2.39 documentation. Consult the implementation's actual family and transformation settings before applying it.</li>
      <li><a href="https://www.youtube.com/watch?v=iL1c1KmYPM0">Stanford Online — Chelsea Finn, CS330 2022 lecture 11: Variational Inference and Generative Models</a>, with <a href="https://cs330.stanford.edu/fall2022/lecture_slides/cs330_variational_inference_2022.pdf">official lecture slides</a>: an alternate visual lecture route through latent-variable and amortized inference, with deep-learning context. The video title/outline and accompanying slides were reviewed; full video playback is not claimed.</li>
      <li><a href="https://www.pymc.io/projects/docs/en/stable/api/generated/pymc.ADVI.html">PyMC ADVI</a> and <a href="https://www.pymc.io/projects/docs/en/stable/api/generated/pymc.fit.html">fit controls</a> — the executed mean-field route, including stochastic-gradient sample count and optimizer. The exact Gaussian KL in this lesson independently evaluates the fitted result.</li>
    </Sources>
  </div>,
};
