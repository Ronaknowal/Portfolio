import { Callout, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements.jsx';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample.jsx';
import { ConditionalAssociationLab, JointDecisionLab, LabelErrorLab, SharedFeatureLab, SharedRegressionLab, ValidationThresholdLab } from '../../components/lesson-labs/MultioutputLabs.jsx';
import { LabelsetFigure, MultioutputSplitFigure, TargetSchemaFigure } from '../../components/lesson-labs/MultioutputFigures.jsx';
import { multioutputExamples } from '../multioutput-examples.js';
const headings = ['1. One input, several questions', '2. Freeze what the model can know', '3. Read errors by message and by label', '4. Fit one probability per label', '5. Let the loss choose the decision', '6. Follow the whole probability tree', '7. Explain association before exploiting it', '8. Choose thresholds on validation', '9. Represent label sets and constraints', '10. Predict several numerical outputs', '11. Decide what to share', '12. Compress labels without losing the question', '13. Run a complete held-out report', '14. Practise, explain and transfer'];
const slug = text => text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
function Program({
  example,
  children
}) {
  return <div className="mo-program"><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></div>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="mo-practice"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Get a hint</summary><Prose>{hint}</Prose></details><details><summary>Show the explained solution</summary>{children}</details></section>;
}
export default {
  title: 'Multi-Label & Multi-Output Learning',
  readTime: '~100 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot multioutput-lesson">
    <LessonIntro prerequisites={<>Review Linear &amp; Logistic Regression: features, sigmoid, squared loss and train/test separation. This lesson refreshes each idea where needed. No previous knowledge of classifier chains, specialized evaluation metrics or neural networks is assumed.</>} sections={headings.map(heading => [slug(heading), heading.replace(/^\d+\. /, '')])}>
      Learn to predict several properties of one observation, then decide what those predictions should mean. The core route runs through target structure, evaluation, marginal and joint decisions, and a complete text or numerical-output report. Marked deeper branches add shared feature penalties and label compression; return to them after you can explain your first baseline.
    </LessonIntro>
    <H2>{headings[0]}</H2>
    <Prose>A workshop receives: “Python cannot read the battery sensor's energy values.” Sending it to only a software queue hides the hardware and energy aspects. A useful system can attach several tags to the <em>same message</em>: code, hardware and energy. The input is still one message. The output is now several answers.</Prose>
    <Prose>This is <strong>multi-label classification</strong> when each answer says whether a label applies. With a fixed order [code, hardware, energy], this message might have target [1, 1, 1]. Another message, “Where is the meeting room?”, can have [0, 0, 0] if none of those tags applies. The empty set is a legitimate target when the task permits it; it does not automatically mean the annotation is missing.</Prose>
    <TargetSchemaFigure />
    <Prose>Multi-label is one member of the broader <strong>multi-output</strong> family. A single exclusive category is multiclass classification: exactly one choice from its vocabulary. Several categorical outputs can each have a different vocabulary, such as priority and language. Several numerical outputs form multi-output regression, such as repair time and energy use. A mixed output needs an appropriate likelihood or loss for each part; putting strings, indicators and hours into one generic squared-error objective does not define a sensible task.</Prose>
    <LessonTable caption="Read the shapes as a collection of complete observations" headers={['Object', 'Shape', 'Meaning']} rows={[['X', 'n × d', 'n observations; d input features per observation'], ['Y for L binary tags', 'n × L', 'One indicator per label, in a fixed vocabulary order'], ['Y for L measurements', 'n × L', 'One real-valued target per output, with declared units'], ['A new prediction', '1 × L', 'L answers for one new input; never L unrelated observations']]} />
    <Prose>The word “multiple” describes the target structure. It says nothing by itself about statistical independence. Hardware and energy tags may be associated; an application can also forbid a combination. We will distinguish the observed target, the probabilities a model estimates and the action selected from those probabilities.</Prose>
    <Prose><strong>Run the examples.</strong> Use Python 3 with NumPy, SciPy and scikit-learn. The recorded results were executed with NumPy 2.3.5 and scikit-learn 1.9.1. Save any complete program below as a .py file and run it with Python. Each program includes its own imports and data; it does not depend on running an earlier cell. The browser investigations need no installation. Different library versions can change fitted coefficients or last decimals.</Prose>
    <Program example={multioutputExamples.schema}><Prose>The duplicate hardware tag remains one positive. The vocabulary fixes column meanings, and the explicit check rejects an unexpected label instead of silently erasing it. An unreviewed annotation is stored separately from a negative; the indicator encoder alone cannot express that distinction.</Prose></Program>
    <H2>{headings[1]}</H2>
    <Prose>Before choosing a model, say <strong>when the prediction is made</strong>. If tags are predicted when a message arrives, later repair notes and a technician's final category are unavailable input features. Keep a record of the request-time input and the later observed target. If two messages paraphrase the same source case, their vocabulary and details may be almost identical; placing one in training and the other in test can make memorization look like generalization.</Prose>
    <MultioutputSplitFigure />
    <Prose>The training split estimates vocabulary, transformations and parameters. Validation chooses thresholds and model settings. The final test split evaluates the frozen procedure. Split complete rows, and group related observations when the use case requires it. A future-use claim also needs an appropriate time split. Never independently split label columns and then reassemble them: the columns describe the same events.</Prose>
    <Prose>Fit text vocabularies, inverse document frequencies, input scales and target scales on training data. When validation itself uses cross-validation, those transforms belong inside each training fold. Decide the label ontology in advance or learn its supported vocabulary from training data, then report labels that cannot be predicted. Rare combinations make “stratify every possible labelset” impractical; show positive counts per label and identify unsupported evaluation cases instead of assuming a split preserves everything.</Prose>
    <Prose><strong>Unknown is not zero.</strong> A reviewed “no hardware issue” is 0. A reviewer who never checked hardware leaves an unknown value. A loss may omit unknown cells using a mask, but it then learns from the observed annotations. If difficult positives are less likely to be reviewed, that observed-label distribution differs from the desired population. Masking prevents inventing negatives; it does not establish a missingness assumption or remove selection bias.</Prose>
    <LessonTable caption="Baselines answer the same question with less machinery" headers={['Objective', 'Training-only baseline', 'What to report']} rows={[['Hamming error with equal costs', 'Predict 1 for labels with training prevalence at least .5', 'Hamming error and per-label support; many negative labels can make this deceptively strong'], ['Whole-set correctness', 'Predict the most frequent complete training vector', 'Exact-set accuracy; ties and incomplete annotations need an explicit rule'], ['Numerical squared error', 'Predict each training-output mean', 'Error in each original unit, before any scaled aggregate']]} />
    <Prose>A baseline is a reference calculation, not proof that its empirical prevalence is a well-estimated population probability. Small or shifted data can make every method uncertain. The complete report later uses deliberately small hand-authored examples to teach the workflow, not to establish a deployment-ready benchmark.</Prose>
    <H2>{headings[2]}</H2>
    <Prose>With one exclusive class, “wrong” is often one event. With several labels, a message can be partly right. Predicting [1, 0, 0] when truth is [1, 0, 1] correctly includes code and excludes hardware, but misses energy. Start by classifying each cell: <strong>TP</strong> includes a true label, <strong>FP</strong> adds an absent label, <strong>FN</strong> misses a true label, and <strong>TN</strong> correctly leaves an absent label off.</Prose>
    <LabelErrorLab />
    <Prose><strong>Hamming loss</strong> is the fraction of label decisions that disagree. For complete data, divide the number of wrong cells by nL. <strong>Exact-set accuracy</strong>, also called subset accuracy, is the fraction of rows whose entire vector is right. Three wrong cells in three different rows can hurt exact-set accuracy much more than the same three mistakes concentrated in one row.</Prose>
    <MathBlock>{String.raw`\begin{aligned} H&=\frac{\sum_{i=1}^{n}\sum_{j=1}^{L}\mathbf1[\hat y_{ij}\ne y_{ij}]}{nL},\\ A_{\rm set}&=\frac1n\sum_{i=1}^{n}\mathbf1[\hat{\mathbf y}_i=\mathbf y_i]. \end{aligned}`}</MathBlock>
    <Prose>Here H denotes Hamming loss and Aset denotes subset accuracy. An indicator is 1 when its statement is true and 0 otherwise. On each row, its all-correct indicator cannot exceed its fraction of correct cells. Averaging proves <strong>subset accuracy ≤ 1 − Hamming loss</strong> for complete data with the same rows and equally weighted labels. This compares accuracies; Hamming <em>loss</em> points in the opposite direction.</Prose>
    <Prose>Precision asks what fraction of predicted positives were right. Recall asks what fraction of actual positives were found. F1 combines their positive counts. Jaccard measures intersection divided by union of the predicted and true sets. Neither F1 nor Jaccard rewards a large number of true negatives.</Prose>
    <MathBlock>{String.raw`\begin{aligned} \text{precision}&=\frac{TP}{TP+FP},\\ \text{recall}&=\frac{TP}{TP+FN},\\ F_1&=\frac{2TP}{2TP+FP+FN},\\ J&=\frac{TP}{TP+FP+FN}. \end{aligned}`}</MathBlock>
    <LessonTable caption="The averaging direction is part of the metric" headers={['Average', 'Calculation', 'Consequence']} rows={[['Micro', 'Sum TP, FP and FN over all evaluated cells, then compute F1', 'Labels contributing more positive decisions/errors influence the pooled score more'], ['Macro', 'Compute each label’s F1, then average label scores', 'Each evaluated label has equal weight; a rare label’s estimate may be very noisy'], ['Samples', 'Compute a set score for each row, then average rows', 'Each evaluated observation has equal weight; partial annotation changes its interpretation']]} />
    <Prose>Use a stated zero rule. This lesson uses 0 for F1 and Jaccard when both observed sets are empty, matching the examples' <code>zero_division=0</code>. Another task may choose an empty-set agreement of 1; that choice changes sample averages and must be reported. An entirely unobserved row is different: no denominator exists, so report undefined and exclude it with a count. The lab's exact-set score uses only fully annotated rows; that complete-case subset need not represent every message.</Prose>
    <Program example={multioutputExamples.metrics}><Prose>The original four-row exercise contains TP=5, FP=1 and FN=2. Its micro-F1 is 10/13, not 2/3. The last lines expose another trap: although F1=2J/(1+J) for the same nonempty count table, applying a nonlinear transformation before averaging can reverse model rankings. Model A has larger mean Jaccard; model B has larger mean sample-F1.</Prose></Program>
    <Callout type="warning">A high micro-F1 is not a guarantee of good rare-label performance; macro-F1 is not a fairness guarantee. Report denominators, per-label errors and the actual downstream cost. If a false energy tag merely adds a review step but a missed code tag blocks urgent work, equal cell costs may be the wrong objective.</Callout>
    <H2>{headings[3]}</H2>
    <Prose>The simplest strong starting point is <strong>binary relevance</strong>: fit one binary predictor for each label using the same input X. One head answers “does code apply?”, another answers “does hardware apply?”. A logistic head computes a linear score z and turns it into p=1/(1+exp(−z)). The learning target is the marginal probability P(Yⱼ=1 | X=x), conditional on the available input but not on the other labels.</Prose>
    <Prose>For one observed cell, binary cross-entropy is −y log p − (1−y) log(1−p). A positive target pushes its probability upward; a negative pushes downward. The equivalent score form avoids taking logs of rounded probabilities:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\ell(z,y)&=\log(1+e^z)-yz,\\\frac{\partial\ell}{\partial z}&=\sigma(z)-y.\end{aligned}`}</MathBlock>
    <Prose>Compute the logarithm with a stable softplus such as <code>np.logaddexp(0, z)</code>, rather than first forming exp(z) for a huge positive score. If X includes a column of ones for the intercept and B contains one weight column per label, all scores are XB. With the <em>mean over rows of the sum over labels</em> objective, the gradient is Xᵀ(P−Y)/n. Averaging over labels too would divide by L and change the learning-rate convention.</Prose>
    <MathBlock>{String.raw`\begin{aligned} \mathcal L(B)&=\frac1n\sum_i\sum_j\ell(x_i^Tb_j,y_{ij}),\\ \nabla_B\mathcal L&=\frac1nX^T(P-Y). \end{aligned}`}</MathBlock>
    <Prose>Why do columns separate? The terms for label j contain only its weight column bⱼ. Optimizing another label's column cannot change those terms. A vectorized matrix operation may fit all heads efficiently while still using separate parameters. That computational sharing does not assert that the random labels are independent.</Prose>
    <Program example={multioutputExamples.numpyHeads}><Prose>The original controlled generator, split, 300 gradient steps and step size are retained as a complete executable experiment. Independent heads achieve Hamming loss .1740; this greedy chain achieves .1790. Both beat the constant baseline, and neither result makes one family universally better. No hyperparameter was selected from this test in the demonstration. A real choice needs a validation split, as the later complete report shows.</Prose></Program>
    <Prose>If a label has only one observed training class, ordinary logistic fitting cannot estimate a two-class boundary. Report that support and choose an explicit constant or smoothed baseline; do not conceal the problem by generating fake labels. With missing annotations, fit each head on its observed rows or use a masked loss and a declared normalization. Labels with different observed counts receive different weights if you divide only by a global row count.</Prose>
    <Prose>Independent heads can be appropriate even when labels remain dependent after conditioning on X. The important question is what decision you want from their marginal probabilities. A product of their probabilities is a separate <em>joint-distribution model</em>; using that product makes a conditional-independence assumption that marginal fitting itself does not require.</Prose>
    <H2>{headings[4]}</H2>
    <Prose>Hold x fixed and imagine many possible outcomes for similar observations. Let p=P(Yⱼ=1 | x). Predicting 0 makes an error with probability p; predicting 1 makes an error with probability 1−p. Thus an equal-cost binary decision chooses 1 when p≥.5, with either action optimal at a tie. Hamming loss adds those label-wise errors. <strong>Linearity of expectation</strong> lets us add their risks whether or not the labels are independent.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E[\ell_H(\mathbf a,Y)\mid x]\\=\frac1L\sum_j[p_j+a_j(1-2p_j)].\end{aligned}`}</MathBlock>
    <Prose>For a false-positive cost cFP and false-negative cost cFN, compare cFP(1−p) with cFN p. Choose 1 at p≥cFP/(cFP+cFN), assuming nonnegative costs with positive sum and a probability appropriate to the decision population. This is a probability-to-action rule. It is not a guarantee that a fitted score is calibrated or that finite validation will identify the population optimum.</Prose>
    <Prose>A whole-set success criterion is different. Under subset 0–1 loss, action a fails unless the complete outcome equals a; its risk is 1−P(Y=a | x). Therefore choose the most probable full vector, the <strong>joint mode</strong>. The vector of individually most likely bits need not be that mode.</Prose>
    <JointDecisionLab />
    <Prose>In the starting table, the four vectors 00, 01, 10 and 11 have probabilities .30, .25, .05 and .40. Marginals are .45 and .65, so Hamming chooses 01, with expected .4 mistakes per label. Yet 11 is the most probable complete vector and has the lowest subset risk, .6. The two actions optimize different losses under the same distribution.</Prose>
    <Program example={multioutputExamples.joint}><Prose>The exact Fraction calculations let you audit every expected loss. A decision optimized for one score should not be described as “the most accurate” without naming that score. For a different set-level loss, such as per-message F1, evaluate its expectation over outcomes; independently thresholding marginals is not a general derivation of its optimum.</Prose></Program>
    <H2>{headings[5]}</H2>
    <Prose>A <strong>classifier chain</strong> feeds predictions for earlier labels into later classifiers. Its probabilistic motivation is the chain rule: write a joint probability as successive conditional probabilities. For two labels, P(A,B | x)=P(A | x)P(B | A,x). Reversing the order gives P(B | x)P(A | B,x). Both factor the same joint distribution exactly when their conditionals are exact.</Prose>
    <MathBlock>{String.raw`\begin{aligned}P(Y=\mathbf y\mid x)\\=\prod_{j=1}^{L}q_j(y_{\pi(j)}\mid x,h_j).\end{aligned}`}</MathBlock>
    <Prose>The symbol π is an ordering of the labels. Here hⱼ lists the preceding values (yπ(1), …, yπ(j−1)), and qⱼ is the conditional probability of the next label value given x and that history. Follow a path, multiply its conditional probabilities and arrive at a leaf's joint mass. A branch with zero prefix probability has no defined conditional distribution, but every leaf below it has zero joint probability. The exact two-label tree has four leaves; an unrestricted L-label binary tree has 2ᴸ possible leaves. That growth motivates approximate search, but approximation changes the guarantee.</Prose>
    <H3>Greedy decoding sees only its current fork</H3>
    <Prose>In the starting table, A→B greedily chooses A=0 because .55 exceeds .45. Within that branch it chooses B=0 because .30/.55 exceeds .25/.55. It ends at 00, whose mass is .30, without visiting the heavier .40 leaf at 11. In reverse order, B→A chooses B=1, then A=1 with conditional .40/.65=8/13, so it reaches 11. The joint distribution did not change; the greedy search path did. Exact enumeration recovers the same joint mode in either order.</Prose>
    <Prose>Learning adds a second issue. Standard training can provide true earlier labels as features for later heads; prediction has only earlier <em>predictions</em>. This is often called teacher forcing during training. Early mistakes can put later heads in unfamiliar input conditions. Independently fitted, finite-capacity conditional models can also define different estimated joint distributions in different orders. Do not attribute every order effect solely to the exact probability factorization.</Prose>
    <H3>Know the current API's probability semantics</H3>
    <Prose>In the inspected scikit-learn 1.9.1 implementation, <code>ClassifierChain</code> defaults to <code>chain_method="predict"</code>: earlier hard predictions become features. Its <code>predict_proba</code> returns each head's probability evaluated along that generated feature path, restored to the original label order. A later path-conditioned probability is generally <strong>not the marginal</strong> obtained by summing across all possible earlier labels.</Prose>
    <Program example={multioutputExamples.chainApi}><Prose>The second path score is approximately .771639; summing this fitted two-head model's four leaf masses gives a second-label marginal of .569133. Both calculations are meaningful, but they answer different questions. Exact small-tree enumeration is possible here because training used actual previous binary labels and we explicitly evaluate both binary histories.</Prose></Program>
    <Prose>With <code>cv=None</code>, training uses actual earlier targets. A <code>cv</code> setting can supply out-of-fold earlier predictions instead. This changes the training procedure; it does not automatically make a time/group split or all nested preprocessing leakage-safe. Inspect which rows and transforms are used inside each fold. If a probability response is fed forward, its continuous training/inference feature is also a different construction from conditioning on an observed binary label.</Prose>
    <Prose>An ensemble requires several fitted chains. Choose multiple orders, fit every chain only on training data, restore a common label order and specify how their scores or votes are combined. Averaging normalized joint distributions and then deriving marginals has a different meaning from averaging path scores. One chain with a random order is still one model. Use validation to decide whether additional orders justify their sequential work.</Prose>
    <H2>{headings[6]}</H2>
    <Prose>A co-occurrence table can suggest questions. If Cⱼₖ counts rows containing both labels j and k, divide by the number containing j to estimate P(Yₖ=1 | Yⱼ=1). The conditioning label belongs to the denominator; swapping rows and columns changes the question. A diagonal of 1 says that a label co-occurs with itself when observed positive, not that its predictor is perfect. Always show the support: a fraction based on two rows is not the same evidence as one based on two thousand.</Prose>
    <ConditionalAssociationLab />
    <Prose>At the starting setting, half the inputs are in a low group, where both labels independently have probability .1, and half in a high group, where both independently have probability .9. Pool the groups and the probability of both labels is (.01+.81)/2=.41. The marginal of either is .5, so P(B=1 | A=1)=.41/.5=.82. Nevertheless, <em>within each group</em>, observing A gives no further information about B once X is known. X explains the pooled association.</Prose>
    <Program example={multioutputExamples.association}><Prose>This is an exact hypothetical probability model, not a correlation estimate from real workshop data. Its purpose is to separate pooled association from conditional dependence. No threshold on a pooled co-occurrence heatmap can establish that a chain must improve a future test score.</Prose></Program>
    <Prose>There is a subtler point. Earlier predictions at inference are functions of X, so they are not newly observed information beyond X. They can still be useful <strong>features for a restricted model class</strong>. Suppose two binary input features determine labels OR, AND and XOR. Linear binary classifiers can represent OR and AND, while XOR is not linearly separable in the two original coordinates. If earlier heads reliably supply OR and AND, the expression OR−AND represents XOR. This can help a later linear head even though all targets are deterministic given X. It adds representation capacity, not hidden truth.</Prose>
    <LessonTable caption="A representation bridge: earlier logical predictions can make XOR linear" headers={['Input bits', 'OR', 'AND', 'OR − AND = XOR']} rows={[['00', '0', '0', '0'], ['01', '1', '0', '1'], ['10', '1', '0', '1'], ['11', '1', '1', '0']]} />
    <Prose>The practical diagnosis is to compare controlled models with proper splits, inspect the errors and ask what they fail to represent. High association may motivate a model; it is not evidence that the model fits well. Prediction-order arrows do not establish causal direction or justify manipulating a label to change another outcome.</Prose>
    <H2>{headings[7]}</H2>
    <Prose>A score ranks or estimates; a threshold selects an action. Moving the threshold leaves the scores fixed. In finite validation data, predictions only change when the gate passes a distinct score. With the rule score≥t, all observations tied at a score enter or leave together. Include an all-positive and a no-positive candidate; otherwise the search is incomplete.</Prose>
    <ValidationThresholdLab />
    <Prose>Recall cannot increase when raising a fixed threshold removes selected items: true positives cannot be added. Empirical precision can move either way because you might remove positives faster than negatives. For descending scores [.9, .8, .7] with truths [0, 1, 1], selecting all has precision 2/3; selecting only the first two gives 1/2; selecting only .9 gives 0. A threshold is not a guaranteed monotone “precision knob.”</Prose>
    <Prose>For a fixed validation set, separate thresholds that maximize each label's F1 maximize its <strong>macro-F1</strong>, because that average is a sum of independent label scores. Micro-F1 pools counts before dividing and couples those choices. The next program deliberately places a hard rare positive at the bottom of one label's ranking: optimizing micro-F1 can drop that label entirely. That may be unacceptable for the application, even though it improves the selected aggregate.</Prose>
    <Program example={multioutputExamples.thresholds}><Prose>The finite search includes exact ties and empty predictions. Per-label F1 and the jointly optimized micro objective need not select the same rule. Neither a six-row search nor a tuned F1 threshold establishes calibrated probabilities. Report this selection as validation work, then freeze it before the test report.</Prose></Program>
    <H3>Imbalance, cost and calibration are separate questions</H3>
    <Prose>If p really is the deployment probability, .5 remains the equal-cost Hamming threshold whether the label is common or rare. Unequal costs change the threshold; inaccurate or shifted probabilities change whether p can be trusted. Lowering every rare label's threshold merely because it is rare has no general derivation.</Prose>
    <Prose>Class weighting changes the fitted objective. At one fixed input with positive probability p, weighted log loss has expected value −w₊p log q − w₋(1−p) log(1−q). Differentiate with respect to q, set the derivative to zero and solve, for positive weights and an interior probability:</Prose>
    <MathBlock>{String.raw`\begin{aligned}q^*&=\frac{w_+p}{w_+p+w_-(1-p)},\\p&=.1,\ w_+=9,\ w_-=1\\&\Rightarrow q^*=.5.\end{aligned}`}</MathBlock>
    <Prose>The endpoint cases follow by continuity. The weighted score .5 in this example does not mean the original event probability became .5. Threshold selection chooses actions from a score; calibration estimates how scores correspond to observed frequencies. These are different jobs. The weighted identity describes an ideal pointwise minimizer, not a guarantee for a finite regularized logistic model.</Prose>
    <Prose>Very small positive counts make per-label tuning unstable, and tuning many thresholds can overfit validation. A prespecified cost rule, a shared threshold or a restrained threshold family may be easier to estimate. State the tradeoff and evaluate the frozen rule. Do not manufacture separate synthetic datasets for each output and then assume the generated rows remain aligned; label relationships and observation identity must survive any resampling scheme.</Prose>
    <H2>{headings[8]}</H2>
    <Prose><strong>Label powerset</strong> treats each distinct complete label vector seen during training as a multiclass category. Encode 00 as one class, 01 as another and 10 as a third; fit a classifier over those classes; decode its predicted class back to a vector. This permits direct competition between observed combinations. It does not perfectly learn their probabilities, and the plain observed-class decoder cannot emit the missing class 11.</Prose>
    <LabelsetFigure />
    <Prose>There can be up to 2ᴸ possible binary vectors, but at most min(n, 2ᴸ) distinct complete vectors are observed in n rows. Many singleton classes make reliable estimation difficult. Conversely, a domain with only a few meaningful recurring patterns can make this representation useful. The vocabulary is a model restriction; it is not proof that unseen combinations are impossible.</Prose>
    <Program example={multioutputExamples.powerset}><Prose><code>np.unique(..., axis=0, return_inverse=True)</code> explicitly builds vector class IDs. <code>MultiLabelBinarizer</code> solves a different problem: it converts sets of label names into columns. The second half demonstrates an overlapping vote with exact coverage denominators. It is a transparent mechanism example, not a full implementation or benchmark of RAkEL.</Prose></Program>
    <Prose><strong>RAkEL</strong> uses small label subsets with powerset-style models; subsets can overlap. Aggregate votes for a label only from models that include it, and state how ties and uncovered labels are handled. <strong>RAkELd</strong> is the disjoint-partition variant. Smaller problems may reduce individual class vocabularies, but disagreement, lost cross-subset structure and computation remain. There is no universal label-count cutoff where one representation becomes best.</Prose>
    <H3>Constraints belong to the action space</H3>
    <Prose>Suppose A means “urgent hardware case” and B means “hardware case”, so A implies B. The feasible vectors are 00, 01 and 11; 10 is invalid. If supplied scores say P(A)=.7 and P(B)=.4, they cannot be marginals of a joint distribution respecting A≤B: taking expectations would require .7≤.4. Inspect the label ontology, estimation and constraints rather than pretending independent thresholding fixed the inconsistency.</Prose>
    <Prose>An action layer may still minimize a chosen score-based surrogate over feasible vectors. With those supplied scores, unnormalized expected-cell-error expressions are .7+.4=1.1 for 00, .7+.6=1.3 for 01, and .3+.6=.9 for 11, so that surrogate chooses 11. Call it constrained optimization of supplied scores, not the Bayes action of an impossible coherent distribution. For coherent probabilities and a known joint law, calculate the actual expected loss over feasible actions.</Prose>
    <Prose>Missing labels complicate both powerset classes and constraints. A row [1, ?, 0] does not specify a complete class. Creating [1, 0, 0] fabricates information; dropping every incomplete row changes the training cohort. A richer partially observed structured model needs its own assumptions. Here the safe baseline keeps an annotation mask, reports observed support and avoids promising fully observed joint inference from missing truth.</Prose>
    <H2>{headings[9]}</H2>
    <Prose>Now change the target deliberately. An instrument's input settings predict <strong>temperature change in °C</strong> and <strong>energy in Wh</strong>. Each row again has several targets, but neither is a binary label. A .5 threshold has no role. Start with one prediction in each output's physical units, and ask how errors should be valued.</Prose>
    <Prose>Why use a mean for squared loss? Fix x, let μ=E[Y | x], and compare any number a with μ. Expand Y−a=(Y−μ)+(μ−a). The cross term has conditional expectation zero. For a target with a finite conditional second moment:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E[(Y-a)^2\mid x]\\=\mathbb E[(Y-\mu)^2\mid x]+(\mu-a)^2.\end{aligned}`}</MathBlock>
    <Prose>The first term cannot be changed by a; the second is minimized at a=μ. For a sum of coordinate squared losses, the argument applies to each coordinate. Dependence among outputs does not invalidate their conditional-mean point predictions. A joint uncertainty region or a decision depending on joint tail events requires more than those means.</Prose>
    <H3>One matrix solve, separable column problems</H3>
    <Prose>Let X contain input features and an intercept, and B have one coefficient column per output. The squared matrix norm ||Y−XB||²F is the sum of all squared residual cells. Expanding by columns gives a separate least-squares problem for each bⱼ. Differentiation yields the normal equations XᵀXB=XᵀY.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\|Y-XB\|_F^2=\sum_j\|y_j-Xb_j\|_2^2,\\(X^TX+\lambda I)B=X^TY.\end{gathered}`}</MathBlock>
    <Prose>The inverse formula requires the relevant matrix to be invertible; do not implement least squares by blindly inverting XᵀX. A QR/SVD-based least-squares solver handles rank structure more reliably. Ridge with λ&gt;0 makes XᵀX+λI positive definite if all included columns are penalized; practical APIs often handle the intercept separately. A rank-deficient unpenalized problem can have several coefficient solutions with identical fitted projections. The program uses <code>lstsq</code> and explicit estimator intercept conventions.</Prose>
    <Program example={multioutputExamples.regression}><Prose>Vector-native and independently wrapped least squares agree. The constructed noise is correlated across outputs, yet this does not by itself improve unrestricted least-squares point predictions through a chain. On this fixed held-out cohort OLS has RMSE about .180527 °C and 4.620232 Wh; the mean baseline is much worse. The small ridge improvement on this single sample is an observation, not a model-selection guarantee.</Prose></Program>
    <Prose>Report each original-unit MAE or RMSE first. If you also report an aggregate, define weights and scales: for example mean over cells of ((prediction−truth)/training-scale)². That assigns one squared error unit per declared scale, not equal real-world harm. A standard deviation can be a convenient statistical scale; domain tolerances may be a better decision scale. Handle constant training outputs explicitly instead of dividing by zero.</Prose>
    <H3>Several categorical outputs are another distinct case</H3>
    <Program example={multioutputExamples.categorical}><Prose>The first output has three classes and the second has two. Each head returns a probability array over its own class order. A single softmax over every class name across both outputs would force competition between unrelated questions. Several sigmoid heads are appropriate for binary indicators; several categorical heads require their own normalized vocabularies.</Prose></Program>
    <H2>{headings[10]}</H2>
    <Prose>Multi-output learning becomes more than repeated single-output fitting when the model <em>couples its parameters or structure</em>. A tree can require all outputs to use one split; a feature penalty can encourage the same inputs to matter across targets; a low-rank coefficient matrix can share latent directions. These are different restrictions, and each can help or cause negative transfer. A joint probability distribution is yet another object.</Prose>
    <SharedRegressionLab />
    <Prose>A one-split regression tree, or <strong>stump</strong>, chooses a boundary and predicts each leaf's mean. For x=[0,1,2,3], the targets are (0,0), (0,100), (2,100), (2,100). A common split at .5 isolates the first energy value, giving zero energy error but temperature SSE 8/3. A common split at 1.5 gets temperature exactly but has energy SSE 5000. Under raw summed squares, .5 wins. Divide energy by 100 before fitting: its squared error divides by 10,000, and 1.5 wins with total scaled SSE .5.</Prose>
    <Program example={multioutputExamples.sharedSplit}><Prose>The actual native tree reproduces the displayed means and cuts. Separate stumps choose different boundaries and fit both outputs exactly on these four points. That extra freedom can also overfit another dataset. This investigation proves a units-and-structure effect, not a held-out superiority claim.</Prose></Program>
    <Prose>A <strong>regressor chain</strong> trains later regressors with earlier numerical targets or out-of-fold estimates, then supplies predictions at inference. Do not feed the true held-out temperature into an energy predictor unless it is genuinely measured before the prediction request; that would be a different task. With ordinary linear base models, composing a predicted earlier linear function with a later linear function remains linear in the original X. Correlated residuals alone do not create new observed information at prediction time. Nonlinear models, regularization, restricted features or genuinely available extra measurements can change the situation.</Prose>
    <details className="mo-optional"><summary>Deeper branch: share a feature's selection across outputs</summary>
      <Prose>Suppose B has features as rows and outputs as columns. To minimize J(B), a group penalty sums each row's Euclidean norm. Removing one whole row removes that feature from all outputs at once. Contrast separate absolute-value penalties, which can remove an individual coefficient while retaining that feature for another output.</Prose>
      <MathBlock>{String.raw`\begin{aligned}J(B)&=\frac{1}{2n}\|Y-XB\|_F^2\\&\quad+\lambda\sum_{r=1}^{d}\|B_{r,:}\|_2.\end{aligned}`}</MathBlock>
      <Prose>When XᵀX/n=I and there is no separate intercept, completing the square separates the objective by feature row. Let z be one row's unpenalized coefficients. Minimize ½||b−z||²+λ||b||₂. For a fixed length ||b||, the quadratic is smallest when b points along z. Write b=t z/||z|| with t≥0; minimize ½(t−||z||)²+λt. Differentiation gives t=||z||−λ, clipped at zero. Write r=||z||₂. Therefore:</Prose>
      <MathBlock>{String.raw`b^*=\begin{cases}(1-\lambda/r)z,&r>\lambda,\\0,&r\le\lambda.\end{cases}`}</MathBlock>
      <SharedFeatureLab />
      <Program example={multioutputExamples.groupShrinkage}><Prose>For z=(3,4) and λ=2, grouped shrinkage gives (1.8,2.4), while separate soft-thresholding gives (1,2). The second row (.5,2) is almost, but not completely, removed by the grouped penalty; the separate penalty removes both entries. The native estimator matches the exact orthonormal calculation. General correlated designs require solving the full coupled optimization, not applying this row formula once to arbitrary least-squares coefficients.</Prose></Program>
    </details>
    <Prose>Shared structure is useful when its restriction matches the task and improves a properly held-out objective. Shared does not automatically mean better. Include an independent baseline, inspect each output and check whether one task's gradients, units or data support dominate the others. A favorable average can hide a damaged target.</Prose>
    <H2>{headings[11]}</H2>
    <Prose>With many labels, even independent models need storage and prediction work. A dense linear binary-relevance model has L(d+1) coefficients including intercepts. A dense linear chain adds 0+1+⋯+(L−1)=L(L−1)/2 coefficients for earlier binary outputs. Heads within a chain depend sequentially on earlier outputs; independent heads and separate chains offer different parallelization opportunities. These are declared parameter/operation models, not measured wall-clock benchmarks.</Prose>
    <details className="mo-optional"><summary>Deeper branch: project the label space, then reconstruct</summary>
      <Prose>If training label vectors concentrate near a lower-dimensional linear subspace, predict coordinates there. In the centered row-sample convention here, compute training mean μ and right singular directions Vₖ from Y−μ. Form compressed targets Z=(Y−μ)Vₖ, learn X→Z, then reconstruct scores as ẐVₖᵀ+μ and choose decisions on validation. The early PLST reference uses a different, uncentered label-column convention; centering is an explicit modeling choice here.</Prose>
      <MathBlock>{String.raw`\begin{aligned}Z&=(Y-\mathbf1\mu^T)V_k,\\\widehat Y_{\text{score}}&=\widehat ZV_k^T+\mathbf1\mu^T.\end{aligned}`}</MathBlock>
      <Prose>Each column of Vₖ is a direction of coordinated label variation. Retaining large singular values preserves much squared reconstruction energy. It does not preserve every label's decisions, ensure scores lie between 0 and 1 or establish calibration. Fit μ and Vₖ on training data only; fitting the projection with test targets leaks exactly the information being evaluated. For a visual review of singular directions, see <a href="/learn/path/full-curriculum/matrix-decompositions-svd-qr-cholesky-lu?module=math-foundations">Matrix Decompositions</a>; this is optional support for the compression branch.</Prose>
      <Program example={multioutputExamples.compression}><Prose>The first two labels are identical common bits. The rare third label occurs twice, once in each common state, so its centered direction is orthogonal to the common direction. Rank one retains about 98.06% of centered energy but reconstructs the rare label as .01 for every row. A .5 threshold misses every rare positive. This uses perfect compressed coordinates, so the failure comes from representation loss even before fitting a predictor.</Prose></Program>
      <Prose>Sparse label recovery is a different idea. It projects labels through measurements and attempts to reconstruct a sparse signal with additional assumptions. In the cited compressed-label analysis, the prediction target is the conditional mean E[Y | X=x], whose sparsity or approximate sparsity matters. An observation can contain exactly one uniformly chosen positive among five labels while its conditional mean is the dense vector (.2,.2,.2,.2,.2). Sparse observed rows alone do not supply that recovery assumption. The full sensing/recovery theorem is outside this branch; do not infer it from a low-rank picture.</Prose>
    </details>
    <Prose>For extremely large vocabularies, candidate retrieval can reduce scoring work, but a true label missed during retrieval cannot be recovered by the later ranker. Report candidate recall, long-tail support, latency under actual load and memory in a specified representation. Softmax over one exclusive label is not automatically a distribution over multiple labels; a hierarchy must respect the target semantics. The appropriate scale boundary comes from a measured task, not an arbitrary label-count rule.</Prose>
    <H2>{headings[12]}</H2>
    <Prose>Return to workshop messages. The following corpus is literal, small and deliberately constructed, with meaningful tags written alongside each message. Each source case has two paraphrases, and its complete group belongs entirely to train, validation or test. Every label is inspectable and no download is needed. This is a teaching dataset, not evidence that a production support system should reach the printed scores.</Prose>
    <Prose>The program fits training-only TF-IDF features, an independent logistic model and two explicitly different chains. It chooses among a small fixed threshold list using validation micro-F1; ties use the first listed candidate. Only then does it evaluate the chosen rule and a training-prevalence baseline on test. Order-specific chain values are treated as scores for threshold selection, not advertised as calibrated marginals.</Prose>
    <Program example={multioutputExamples.textReport}><Prose>The selected chain uses order code→hardware→energy and threshold .6. Its eight-row test micro-F1 is 10/11 and exact-set accuracy is 7/8, with four positives per label. One message loses both code and energy, so Hamming error is two of 24 cells even though only one full row fails. The printed rows make the relationship auditable. Eight rows cannot support a broad performance claim or stable rare-label comparison.</Prose></Program>
    <LessonTable caption="What a reproducible multi-output report makes inspectable" headers={['Question', 'This demonstration', 'A real use case still needs']} rows={[['What is an observation?', 'One request-time message; paraphrases stay grouped', 'A justified unit, temporal boundary and annotation process'], ['How were settings selected?', 'Fixed candidates, validation micro-F1, first-candidate tie rule', 'Adequate support and limits on repeated adaptive choices'], ['Which outputs were evaluated?', 'Three fixed tags, fully annotated, zero_division=0', 'Unknown-label policy, changing ontology and per-label support'], ['What was compared?', 'Prevalence baseline and frozen selected predictor', 'Justified uncertainty assessment and actual costs'], ['What might fail?', 'Tiny vocabulary and shared hand-authoring style', 'New language, phrasing, sources, labels and deployment shifts']]} />
    <Prose>For numerical targets, the earlier complete regression program is the corresponding report: a training-mean baseline, per-output RMSE in °C and Wh, declared training-only scales and held-out predictions. Carry the same split discipline over. Do not add an energy error in Wh to a temperature error in °C and call the sum a physical quantity.</Prose>
    <Prose>The changed-cost program is reserved for practice below. It shows why another objective can select different validation settings and even another model family. The selected model is the outcome of a stated procedure on stated data, not an intrinsic winner.</Prose>
    <H2>{headings[13]}</H2>
    <Prose>Work these changed tasks before opening their explanations. A useful answer names the observation, available information, loss, denominator and limitation—not just a method. Begin with hand calculations; finish by rerunning a complete experiment under a changed objective.</Prose>
    <Practice title="A. Repair an annotation and a split" question="Each repair case has two messages. A reviewer marks only code tags; unreviewed hardware/energy cells are filled with 0. Paraphrases then cross a random train/test split. Name two separate errors and propose a first baseline." hint="One problem changes target meaning; the other changes what the test can establish."><Prose>Unreviewed cells must stay unknown, with a mask or per-label reviewed subsets. Zeros invent negative evidence. Group all messages from one repair case in a single split; respect time if predicting future cases. Fit train-only code prevalence and an independent code model as the supported initial task, while reporting hardware/energy evaluation unavailable. Masking alone does not make reviewed cases representative.</Prose></Practice>
    <Practice title="B. Recompute a changed error table" question="Truth is [[1,0],[0,1],[1,1]]; prediction is [[1,1],[0,0],[1,1]]. Calculate TP, FP, FN, Hamming loss, exact-set accuracy and micro-F1. Then make the second row's second annotation unknown." hint="Start with six cells. After masking, five cells are observed and only two rows are complete."><Prose>Initially TP=3, FP=1, FN=1, TN=1. Hamming loss is 2/6=1/3; subset accuracy is 1/3; micro-F1 is 6/(6+1+1)=3/4. Masking the missed positive leaves TP=3, FP=1, FN=0 over five observed cells: Hamming loss 1/5 and micro-F1 6/7. Exact-set accuracy uses rows 1 and 3, giving 1/2. The apparent improvement partly removes an error from observation; the predictor has not changed.</Prose></Practice>
    <Practice title="C. Change the four probability masses" question="States 00,01,10,11 have counts [4,1,2,3] out of 10. Find the marginal threshold action, joint mode and greedy answer in each order. Use 1 on a .5 binary tie." hint="The marginals are .5 and .4. A conditional decision comes after fixing the first bit."><Prose>Marginal thresholding gives 10. The joint mode is 00 with mass .4. Greedy A→B chooses A=1, then B=1 because 3/(2+3)=.6, ending at 11. Greedy B→A chooses B=0, then A=0 because 2/(4+2)=1/3, ending at 00. Both values of A minimize its marginal error at the tie, so 00 and 10 both minimize Hamming risk .45 per label. A tie convention selects one optimal action without proving uniqueness.</Prose></Practice>
    <Practice title="D. A chain path is not a marginal" question="P(A=1|x)=.4, P(B=1|A=0,x)=.2 and P(B=1|A=1,x)=.9. What is B's hard-path score and its marginal? Which four joint masses sum to one?" hint="The first greedy feature is A=0. The marginal averages both histories using prefix masses."><Prose>The hard-path score is .2; the marginal is .6×.2+.4×.9=.48. Masses in 00,01,10,11 order are .48,.12,.04,.36, summing to 1. A path score can be useful without being a marginal. If a continuous prior probability is fed forward instead of a binary history, describe that different feature construction before interpreting it.</Prose></Practice>
    <Practice title="E. A different mixture" question="The high group now has weight .25; within low/high groups labels are independent with probabilities .1/.9. Compute pooled P(A=1), P(A=1,B=1) and P(B=1|A=1). Does association prove causation?" hint="Weight the independent tables by .75 and .25, then divide by the A-positive probability."><Prose>The marginal is .75×.1+.25×.9=.3. Joint-positive mass is .75×.01+.25×.81=.21. The conditional is .21/.3=.7, larger than B's pooled marginal .3. Within either X group the labels remain independent. Neither this association nor a chain ordering proves a causal effect.</Prose></Practice>
    <Practice title="F. A threshold cannot break a score tie" question="Scores are [.8,.8,.2], truths [1,0,1]. Calculate F1 at thresholds .2, .8 and .81 under score≥t. Can one threshold select just the first .8 observation?" hint="A tied score group moves together. Zero-positive predictions use zero_division=0."><Prose>At .2, TP=2, FP=1, FN=0, so F1=4/5. At .8, TP=1, FP=1, FN=1, so F1=1/2. At .81 nothing remains and F1=0. A scalar threshold cannot distinguish the .8 observations; adding a feature-based or random tie-break is a changed procedure requiring its own justification and validation.</Prose></Practice>
    <Practice title="G. Recover score meaning before applying cost" question="The true probability is .2; training uses w+=4 and w−=1. Find the ideal weighted score. Deployment false negatives cost 3 and false positives cost 1. Decide using the original probability." hint="The weighted optimum describes q; the original-probability cost threshold is 1/4."><Prose>The weighted score is .8/(.8+.8)=.5. The cost threshold is .25, so p=.2 selects 0, with expected cost 3×.2=.6 instead of 1×.8=.8 for predicting 1. Treating q as the original p reverses the comparison. An ideal transformation can be inverted under its assumptions, but a finite fitted model still needs verification.</Prose></Practice>
    <Practice title="H. An absent class and a vote denominator" question="A powerset vocabulary is {000,101,010}. Can it decode 111? Separately, AB votes 10, BC votes 11 and AC votes 01. What does overlapping voting produce with positive ties?" hint="A powerset decoder chooses an available class. A label's vote denominator counts only models that contain it."><Prose>The plain powerset decoder cannot emit 111. The overlapping votes are A=1/2, B=1/2, C=2/2, giving 111 with positive ties. These are different hypothesis spaces. Voting does not automatically enforce a domain constraint that might still make 111 invalid.</Prose></Practice>
    <Practice title="I. Change the measurement units" question="In the four-row example, divide energy by 10 rather than 100. Which common split wins? Do separately optimal splits depend on positive rescaling?" hint="Raw energy SSE is 0 at .5, 5000 at 1.5, and 20000/3 at 2.5. Divide these by 100."><Prose>The common losses are 8/3, 50 and 8/3+200/3=208/3, so .5 wins. At the last split, temperature SSE is 8/3. For each separate output, positive rescaling multiplies all candidate losses by one positive constant, preserving its minimizing splits and ties. Coupling outputs makes their relative scales matter.</Prose></Practice>
    <Practice title="J. A different grouped feature" question="For an orthonormal design, one feature's unpenalized coefficients are (−3,4) and λ=1. Find grouped and separate solutions. Why not apply the formula directly to arbitrary correlated X?" hint="The norm is 5. Grouping uses one radial multiplier; separate shrinkage removes 1 from each absolute coefficient."><Prose>The grouped multiplier .8 gives (−2.4,3.2); separate soft-thresholding gives (−2,3). Row separation used XᵀX/n=I. Correlated columns create cross terms coupling feature rows, so the same one-step formula on arbitrary least-squares coefficients generally does not solve the full objective.</Prose></Practice>
    <Practice title="K. Sparse observations versus a sparse mean" question="Exactly one of ten labels is chosen uniformly, independently of X. Is each observed target sparse? Is its conditional mean sparse? What does retaining high reconstruction energy fail to guarantee?" hint="Write all ten marginal probabilities, and distinguish one sampled vector from the squared-loss prediction target."><Prose>Every sample has one nonzero entry, but its conditional mean has ten nonzero entries, all .1. A sparse conditional-mean recovery assumption does not follow from sparse observations. High retained reconstruction energy also does not guarantee rare-label recall, as the rank-one example demonstrates; evaluate the required decisions.</Prose></Practice>
    <Practice title="L. Change a cost and submit a frozen report" question="Reuse the grouped text corpus, but minimize validation FP+3FN. Keep the candidate order and thresholds fixed. Must the selected family remain the same? Report frozen test cost, denominator and a limitation." hint="Recompute selection only on validation, using the first minimum for ties. Consult test labels after freezing the rule.">
      <Program example={multioutputExamples.changedReport}><Prose>The new objective selects independent heads at .5, with validation cost 3. On the executed eight-row test, cost is 0 across 24 label decisions and micro-F1 is 1. Perfect predictions on this small hand-authored cohort do not establish deployment performance. Variants from one source case are dependent, positive support is tiny, and external validity is unknown. The changed objective can select a different family while training data and features stay fixed.</Prose></Program>
    </Practice>
    <Prose><strong>Readiness check.</strong> You should now be able to specify a target schema, establish available information and a split, calculate a baseline, choose a loss, explain your probabilities or scores, and evaluate frozen decisions with visible denominators. Also state what the model does not establish: joint uncertainty, rare-label reliability, causal effects or benefits of sharing may need further evidence.</Prose>
    <Prose>The next module topic is <strong>Survival Analysis</strong>. It also records more than one value—an observed duration and an event indicator—but an event that has not yet been observed is censored, not simply a negative label. That observation mechanism changes the likelihood and what can be estimated. Use the route's Next link to continue in the intended order.</Prose>
    <Sources alternatives={<Prose><strong>Another way to learn it.</strong> The <a href="https://scikit-learn.org/stable/modules/multiclass.html" target="_blank" rel="noreferrer">scikit-learn target-format guide</a> complements section 1. <a href="https://jmread.github.io/talks/Tutorial-MLC-Porto.pdf" target="_blank" rel="noreferrer">Jesse Read's 2015 tutorial slides</a> provide an intermediate visual view of label structure and chains; selected task/model-class/metric slides were inspected, and abbreviated dependence claims are qualified here. The official <a href="https://www.coursera.org/learn/advanced-learning-algorithms/" target="_blank" rel="noreferrer">DeepLearning.AI/Stanford Advanced Learning Algorithms course</a> lists a four-minute optional “Classification with multiple outputs” in week 2. The listing was verified, not the video transcript; account/enrollment may be needed, and surrounding neural material is optional.</Prose>}>
      <li><a href="https://icml.cc/Conferences/2010/papers/589.pdf" target="_blank" rel="noreferrer">Dembczyński, Cheng and Hüllermeier: Bayes-optimal multilabel classification via probabilistic classifier chains</a> — sections 2–4.3 for marginal/joint risk, exact factorization and greedy inference. Local worked numbers are independently constructed.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.multioutput.ClassifierChain.html" target="_blank" rel="noreferrer">ClassifierChain API</a> — current order, cv and chain_method contracts; installed 1.9.1 source and actual path scores were inspected.</li>
      <li><a href="https://scikit-learn.org/stable/modules/model_evaluation.html" target="_blank" rel="noreferrer">Evaluation metrics</a> and <a href="https://scikit-learn.org/stable/modules/classification_threshold.html" target="_blank" rel="noreferrer">threshold tuning</a> — definitions, averaging, zero rules and separation of probability estimation from actions. Local counterexamples establish the qualifications.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.MultiLabelBinarizer.html" target="_blank" rel="noreferrer">MultiLabelBinarizer</a>, <a href="https://scikit-learn.org/stable/modules/generated/sklearn.multioutput.MultiOutputRegressor.html" target="_blank" rel="noreferrer">MultiOutputRegressor</a> and <a href="https://scikit-learn.org/stable/modules/generated/sklearn.multioutput.RegressorChain.html" target="_blank" rel="noreferrer">RegressorChain</a> — vocabulary and independent/chain APIs used in complete programs.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.MultiTaskElasticNet.html" target="_blank" rel="noreferrer">Shared feature penalty in MultiTaskElasticNet</a> — objective and coefficient orientation. The pure group-Lasso case is checked with MultiTaskLasso under an orthonormal design.</li>
      <li><a href="https://waikato.github.io/meka/meka.classifiers.multilabel.RAkEL/" target="_blank" rel="noreferrer">MEKA RAkEL</a> and <a href="https://waikato.github.io/meka/meka.classifiers.multilabel.RAkELd/" target="_blank" rel="noreferrer">RAkELd</a> — overlapping/disjoint mechanisms; inconsistent generated bibliography fields are not reused.</li>
      <li><a href="https://www.csie.ntu.edu.tw/~htlin/paper/doc/wsmld10plst.pdf" target="_blank" rel="noreferrer">Tai and Lin: early principal label-space transformation</a> — sections 2–3 and algorithms for projection/reconstruction; the centered row-sample variant is explicit, and benchmark superiority is not reused.</li>
      <li><a href="https://www.cs.columbia.edu/~djhsu/papers/mlcs.pdf" target="_blank" rel="noreferrer">Hsu and colleagues: multi-label prediction via compressed sensing</a> — introduction and sections 2–3.1 for conditional-mean sparsity. The full recovery theorem is further reading, not established here.</li>
    </Sources>
  </div>
};
