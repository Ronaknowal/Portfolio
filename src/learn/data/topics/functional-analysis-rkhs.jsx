import { Callout, Code, H2, H3, Prose } from '../../components/content';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram';
import libraryOutput from '../functional-analysis-library-output';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CompletionFigure, FunctionSpikeLab, EvaluationKernelLab, KernelValidityLab, RepresenterGeometryLab, KernelRidgeLab, KernelMeanLab, KernelQuadratureLab, OperatorModesFigure } from '../../components/lesson-labs/FunctionalAnalysisLabs';
import { functionalAnalysisExamples } from '../functional-analysis-examples.js';
function Example({
  id,
  children
}) {
  const example = functionalAnalysisExamples.find(item => item.id === id);
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}>{children}</RunnableExample></>;
}
function Practice({
  title,
  question,
  hint,
  children
}) {
  return <section className="lesson-check"><H3>{title}</H3><Prose>{question}</Prose><details><summary>Hint</summary><Prose>{hint}</Prose></details><details><summary>Explained solution</summary>{children}</details></section>;
}
export default {
  title: "Functional Analysis & RKHS",
  readTime: "~90 min read + 2–3 hours practice",
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot functional-analysis-lesson">
    <LessonIntro prerequisites="Use vector sums, inner products, projections and small linear systems from Vectors and Matrix Decompositions. Measure Theory introduces integrals, almost-everywhere equality and square integrability. We refresh these meanings and teach Cauchy sequences, completeness, bounded measurements and kernel geometry here. Python is optional; Fourier analysis, SVMs and Gaussian processes are not required." sections={[['1-measure-distance-between-functions', "Functions & completeness"], ['2-ask-what-a-measurement-controls', "Measurements & projection"], ['3-build-a-space-that-controls-evaluation', "Construct an RKHS"], ['4-build-kernels-from-features', "Features & validity"], ['5-explain-why-a-finite-fit-is-enough', "Representer theorem"], ['6-fit-noisy-data-with-kernel-ridge', "Kernel ridge"], ['7-choose-and-scale-the-model', "Validation & scale"], ['8-represent-a-whole-distribution', "Kernel means"], ['9-design-an-integration-rule', "Kernel quadrature"], ['10-connect-operators-spectra-and-random-functions', "Deeper connections"], ['11-practise-and-transfer', "Independent practice"]]}>Two measurements do not determine a whole curve. Infinitely many curves can pass through the same points and disagree elsewhere. We will give functions a geometry, ask which measurements that geometry controls, and derive why a useful infinite-dimensional fitting problem can have a finite solution. The same construction will later measure a distribution and design an integration rule.</LessonIntro>

    <H2>1. Measure distance between functions</H2>
    <Prose>A function f on [0,1] assigns a real number f(t) to each input t. Addition means adding values at the same input: (f+g)(t)=f(t)+g(t). Scaling means (af)(t)=a f(t). These operations satisfy the same vector rules as adding coordinate lists. The object may now be a whole curve rather than a fixed finite list.</Prose>
    <Prose>A <strong>norm</strong> measures size. It is nonnegative, equals zero only for the zero vector, obeys ||af||=|a| ||f||, and satisfies ||f+g||≤||f||+||g||. The distance between f and g is ||f−g||. Choosing a norm chooses which differences matter. These three measurements answer different questions:</Prose>
    <LessonTable caption="Different measurements of the same curve" headers={['Measurement', 'Meaning', 'Condition']} rows={[["Maximum size: sup |f(t)|", "How large can any point value be?", "Use bounded functions; continuous functions on [0,1] attain a maximum."], ["L² size: (∫ |f(t)|² dt)½", "How much squared amplitude accumulates?", "Square integrability; identify functions equal almost everywhere."], ["Derivative energy: ∫ |f′(t)|² dt", "How costly are changes across the interval?", "A squared norm only after removing the constant null direction, for example with f(0)=0."]]} />
    <Prose>An <strong>inner product</strong> also measures alignment. In this real-valued lesson it is bilinear, symmetric and positive definite; it induces ||f||=√〈f,f〉. For L², 〈f,g〉=∫fg. <strong>Cauchy–Schwarz</strong> says |〈f,g〉|≤||f|| ||g||. Expand ||f−tg||²≥0 and choose t=〈f,g〉/||g||² when g≠0; rearranging proves the inequality. If g=0 it is immediate. This result will turn a measurement into a sensitivity bound.</Prose>
    <MathBlock>{String.raw`\begin{aligned}0&\leq\left\|f-\frac{\langle f,g\rangle}{\|g\|^2}g\right\|^2\\&=\|f\|^2-\frac{|\langle f,g\rangle|^2}{\|g\|^2}.\end{aligned}`}</MathBlock>
    <Prose>Not every norm comes from an inner product. In two coordinates, ||(a,b)||₁=|a|+|b| is a norm. For u=(1,0), v=(0,1), the sum ||u+v||₁²+||u−v||₁² is 8, while 2||u||₁²+2||v||₁² is 4. Inner-product norms satisfy the <strong>parallelogram identity</strong>, making these expressions equal. Thus this norm has no inducing inner product. The geometry matters for what we can prove.</Prose>
    <H3>A sequence can approach something missing</H3>
    <Prose>Convergence in a norm means ||fₙ−f||→0. A sequence is <strong>Cauchy</strong> when its later elements become arbitrarily close to each other: for every ε&gt;0, some N makes m,n≥N imply ||fₙ−fₘ||&lt;ε. This does not assume a limit already exists in our space. A space is <strong>complete</strong> when every Cauchy sequence has a limit in that space, measured in that norm. A complete normed space is a <strong>Banach space</strong>; a complete inner-product space is a <strong>Hilbert space</strong>.</Prose>
    <CompletionFigure />
    <Prose>Consider finitely supported sequences with the Euclidean sequence norm. Truncations of (1,1/2,1/4,…) are Cauchy because their tails become arbitrarily small, but their limit has infinitely many nonzero entries. Completion adds such square-summable sequences, producing ℓ². It does not add every infinite sequence: (1,1,1,…) has infinite squared norm and is not in ℓ².</Prose>
    <MathBlock>{String.raw`\sum_{j=n}^{\infty}4^{-j}=\frac43\,4^{-n}\longrightarrow0.`}</MathBlock>
    <Prose>Likewise, L² is a Hilbert space of equivalence classes of functions equal except on sets of measure zero. A zero integral of |f−g|² means equality almost everywhere, not at every chosen point. Its completeness is a theorem, not established by a finite plot. We will use it to construct a space where individual values are controlled.</Prose>
    <details><summary>Why L² is complete</summary><Prose>From a Cauchy sequence choose a subsequence with successive L² differences at most 2⁻ʲ. The partial sums of the absolute differences have L² norm bounded by Σ2⁻ʲ, using the triangle inequality. Fatou's lemma bounds the squared integral of the infinite absolute sum. That sum is finite almost everywhere, so the subsequence converges pointwise there to a measurable function f. The L² norm of each remaining tail is at most the corresponding geometric tail, again by Fatou. Thus the subsequence converges in L², and the original Cauchy property brings the whole sequence to the same limit. Define f measurably, for example as zero where the series does not converge. The limiting theorem supplies what the finite picture cannot.</Prose></details>
    <Checkpoint prompt="Does completeness say every bounded sequence converges?"><Prose>No. In the complete real line, 1,−1,1,−1,… is bounded but not Cauchy. Completeness promises a limit only for sequences whose later terms approach one another in the chosen norm.</Prose></Checkpoint>

    <H2>2. Ask what a measurement controls</H2>
    <Prose>An <strong>operator</strong> maps one vector space to another. It is linear when A(af+bg)=aAf+bAg. A linear map into the scalars is a <strong>linear functional</strong>: integration f↦∫f and evaluation f↦f(x) are candidates. Whether either is defined and stable depends on the space. Differentiation is linear on its domain; it is not automatically defined or bounded on every function space.</Prose>
    <Prose>A linear map is <strong>bounded</strong> if a finite C satisfies ||Af||≤C||f|| for all f. Its operator norm is the smallest such C, equivalently sup over ||f||≤1. Consequently ||Af−Ag||≤C||f−g||. For linear maps, boundedness is equivalent to continuity. The inequality proves one direction. Conversely, continuity at zero gives δ&gt;0 such that input norm below δ implies output norm below 1. Apply this to δf/(2||f||) and use linearity to get ||Af||&lt;2||f||/δ; handle f=0 separately.</Prose>
    <Prose>Try evaluation at x=.5 using only the L² norm. A triangle of height 1 and half-width ε around .5 has squared L² norm 2ε/3, tending to zero, but its value at .5 remains 1. No finite C can satisfy 1≤C√(2ε/3) for every sufficiently small ε&gt;0.</Prose>
    <FunctionSpikeLab />
    <Prose>Two failures must be distinguished. In L² equivalence classes, changing just f(.5) does not change the vector, so evaluation is not even a well-defined operation on the class. On continuous functions equipped with the L² norm, evaluation is well-defined, but the triangle proves it unbounded. Continuity of a curve is different from continuity of the operation reading a curve.</Prose>
    <H3>Projection needs a place for its limit</H3>
    <Prose>A subspace W contains zero and is closed under linear combinations. It is <strong>closed</strong> if norm limits of its vectors remain in W. In a Hilbert space every f has a unique closest point p in any closed subspace W. The residual f−p is orthogonal to every w∈W, and ||f||²=||p||²+||f−p||².</Prose>
    <Prose>To see existence, choose pᵢ∈W whose distances to f approach the infimum d. Their midpoint is in W. The parallelogram identity gives ||pᵢ−pⱼ||²≤2||f−pᵢ||²+2||f−pⱼ||²−4d², tending to zero. Completeness supplies a limit p, and closedness keeps it in W. Expanding ||f−p−tw||² and requiring its minimum at t=0 gives 〈f−p,w〉=0. The same orthogonality proves uniqueness.</Prose>
    <Prose>Closedness is essential. The finite-support subspace of ℓ² has distance infimum zero from (1,1/2,1/4,…), yet none of its vectors equals that limit. There is no closest point in that subspace. A finite-dimensional subspace is closed, so finite spans of evaluation sections will avoid this problem.</Prose>
    <H3>Riesz turns a measurement into a direction</H3>
    <Prose>The <strong>continuous dual</strong> H* consists of bounded linear functionals. The Hilbert-space <strong>Riesz representation theorem</strong> says each ℓ∈H* has a unique r∈H with ℓ(f)=〈f,r〉 for all f, and ||ℓ||=||r||. This is the Hilbert-space theorem, distinct from the measure-representation theorem also named after Riesz.</Prose>
    <Prose>Here is its mechanism. For ℓ=0 take r=0. Otherwise choose u with ℓ(u)=1. Its nullspace W consisting of all f with ℓ(f)=0 is closed by continuity. Subtract u's projection onto W to obtain z⊥W with ℓ(z)=1. For every f, the vector f−ℓ(f)z belongs to W. Therefore 〈f,z〉=ℓ(f)||z||², and r=z/||z||² represents ℓ. If two representers existed, their difference would be orthogonal to itself, forcing equality. Cauchy–Schwarz gives the norm upper bound; f=r/||r|| attains it for r≠0.</Prose>

    <H2>3. Build a space that controls evaluation</H2>
    <Prose>Choose square-integrable slopes g on [0,1] and accumulate them from a fixed anchor:</Prose>
    <MathBlock>{String.raw`\begin{gathered}\mathcal H=\left\{f:\begin{array}{l}f(t)=\int_0^t g(s)\,ds,\\g\in L^2[0,1]\end{array}\right\},\\\langle f,h\rangle_{\mathcal H}=\int_0^1 f'(s)h'(s)\,ds.\end{gathered}`}</MathBlock>
    <Prose>These functions are absolutely continuous, satisfy f(0)=0, and have derivative g almost everywhere. Absolute continuity ensures that the integral of the slope recovers the actual function. An almost-everywhere derivative alone would be insufficient. The derivative identifies f uniquely because the anchor fixes its constant. This correspondence is an isometry onto L², so L² completeness establishes completeness here.</Prose>
    <Prose>For slopes 2,−1,1,0 on the four quarter intervals, f(.6)=2×.25−1×.25+1×.1=.35. The squared H norm is (4+1+1+0)×.25=1.5. Signed contributions can cancel in the function value; squared slope contributions cannot cancel in the energy.</Prose>
    <Prose>To read f(x), choose a partner whose derivative retains exactly the slopes before x: kₓ(t)=min(x,t). Its derivative is 1 before x and 0 afterward; the single corner does not affect the integral. We obtain:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\langle f,k_x\rangle_{\mathcal H}&=\int_0^1 f'(t)\mathbf1_{[0,x]}(t)\,dt\\&=f(x),\\k(x,z)&=\langle k_x,k_z\rangle\\&=\min(x,z),\\\|k_x\|^2&=x,\\|f(x)|&\leq\sqrt{x}\,\|f\|.\end{aligned}`}</MathBlock>
    <EvaluationKernelLab />
    <Prose>A <strong>reproducing-kernel Hilbert space (RKHS)</strong> is a Hilbert space of actual point-defined functions in which every evaluation functional is bounded. Riesz supplies evaluation representers; inner product with kₓ reproduces f(x). Overlap of the two indicator intervals explains min(x,z). At x=0 the representer is zero because every member vanishes at the anchor.</Prose>
    <Prose>The same calculation gives |f(x)−f(z)|≤√|x−z| ||f||, using the interval between x and z. This particular geometry limits rapid changes. A general RKHS norm describes the chosen kernel; it is not always derivative energy or a universal physical measure of roughness. Here time and values are dimensionless. With physical units, nondimensionalize or carry the resulting energy and penalty units explicitly.</Prose>
    <Prose><strong>Run the examples:</strong> save each complete program as its own <Code>.py</Code> file and run <Code>python filename.py</Code>. The original and Fraction programs use the standard library. The remaining mechanism examples use NumPy; the integral-mode example also uses SciPy. The KernelRidge bridge below additionally uses scikit-learn and supplies its own pinned install command. If needed, install them in your virtual environment with <Code>python -m pip install numpy scipy</Code>. Examples were executed with Python 3.12.14, NumPy 2.3.5 and SciPy 1.18.1. Browser investigations calculate bounded models rather than executing Python. Floating-point readouts are rounded where stated.</Prose>
    <Example id="measurements"><Prose>The exact contributions sum to 7/20 and the energy is 3/2. Shrinking half-width 1/8 to 1/32 divides L² squared size by 4 and multiplies derivative energy by 4. These are different measurements of the same change.</Prose></Example>
    <Checkpoint prompt="Why not use derivative energy on all differentiable functions without an anchor?"><Prose>Every constant would have energy 0, including nonzero constants. That violates the norm's zero condition. Fixing f(0)=0 removes that direction. Penalizing a boundary value instead gives a different norm and reproducing kernel.</Prose></Checkpoint>

    <H2>4. Build kernels from features</H2>
    <Prose>For a nonempty set X, a real <strong>positive-semidefinite kernel</strong> is a symmetric function k such that every finite Gram matrix Kᵢⱼ=k(xᵢ,xⱼ) satisfies cᵀKc≥0 for every real coefficient vector c. Repeated inputs are allowed. Some texts call this a “positive definite kernel” while permitting equality; we distinguish it from a strictly positive-definite matrix.</Prose>
    <MathBlock>{String.raw`c^TKc=\sum_{i,j=1}^n c_i c_j k(x_i,x_j)\geq0.`}</MathBlock>
    <Prose>A safe construction is k(x,z)=〈φ(x),φ(z)〉 for a feature map φ. Then cᵀKc=||Σcᵢφ(xᵢ)||². For φ(x)=(1,√2x,x²), the kernel is (1+xz)². Omitting √2 gives a different kernel. For strings, count fixed patterns and dot their count vectors. ABA and BAB both have counts (1,1) for AB and BA: this valid kernel cannot distinguish them. Validity and informativeness are different properties.</Prose>
    <KernelValidityLab />
    <Prose>Polynomial, string, graph and domain-specific kernels can express useful assumptions. But an intuitive similarity is not automatically a kernel. In the invalid matrix, c=(1,−1,1) gives diagonal contribution 3 and off-diagonal contribution −3.6, hence −.6. All two-object principal matrices pass, but three objects expose failure. Finite tests can reject a proposed kernel; passing them does not prove a formula valid throughout an unrestricted domain.</Prose>
    <Example id="features"><Prose>The feature factor proves validity algebraically. Tiny near-zero floating-point entries in a computed Gram matrix reflect arithmetic rounding; the invalid quadratic form is a substantial −.6, not a tolerance ambiguity.</Prose></Example>
    <H3>From PSD to an RKHS</H3>
    <Prose>Form finite sums f=Σaᵢk(xᵢ,·). Define their inner product by the double weighted kernel sum. Equivalently, 〈f,Σbⱼk(zⱼ,·)〉=Σbⱼf(zⱼ), showing that it does not depend on a chosen coefficient representation. PSD gives nonnegative norm squared. Kernel Cauchy–Schwarz gives |f(x)|²≤k(x,x)||f||², so zero norm forces the function to vanish everywhere. Formal coefficient lists differing by a zero-norm direction represent the same vector.</Prose>
    <Prose>Complete the space in that norm. The evaluation bound makes the values of every Cauchy sequence converge at each fixed x, and equivalent sequences give the same values. Kernel sections stay dense, so a limit orthogonal to every section is zero. This constructs the unique RKHS associated with k: the <strong>Moore–Aronszajn correspondence</strong>. A Mercer expansion, introduced later with additional assumptions, is a more specific integral-operator result.</Prose>
    <Prose>Feature parameters may be redundant. With φ(x)=(1,1), weights (1,0) and (0,1) represent the same constant function. The induced function norm uses the smallest weight norm among its representations. Constant 1 has minimum weights (.5,.5), with squared norm .5. The norm of an arbitrary weight vector need not be the function norm.</Prose>
    <H3>The RBF kernel has infinitely many features</H3>
    <MathBlock>{String.raw`\begin{aligned}k_\gamma(x,z)&=e^{-\gamma(x-z)^2},\quad\gamma>0,\\&=e^{-\gamma x^2}e^{-\gamma z^2}\\&\quad{}\times\sum_{m=0}^{\infty}\frac{(2\gamma)^m x^m z^m}{m!}.\end{aligned}`}</MathBlock>
    <Prose>For a scalar x, feature m is exp(−γx²)(√(2γ)x)ᵐ/√(m!). Its squared feature sum is 1, and the two feature sequences have the displayed kernel as inner product. Every finite Gram quadratic form is consequently a sum of squares. Products across coordinates give exp(−γ||x−z||²) in Rᵈ. This Gaussian-shaped similarity is not a normalized density over z.</Prose>

    <H2>5. Explain why a finite fit is enough</H2>
    <Prose>Suppose a loss depends only on f(x₁),…,f(xₙ). Let W span their evaluation sections. It is finite-dimensional and closed. Decompose any f=f∥+f⊥, where f⊥ is orthogonal to W. At every observed point, f⊥(xᵢ)=〈f⊥,kₓᵢ〉=0. The loss cannot see that component, but the norm can.</Prose>
    <MathBlock>{String.raw`\begin{aligned}f(x_i)&=f_\parallel(x_i),\\ \|f\|^2&=\|f_\parallel\|^2+\|f_\perp\|^2.\end{aligned}`}</MathBlock>
    <Prose>Minimize observed-value loss plus Ω(||f||). If a minimizer exists and Ω is strictly increasing, every minimizer has f⊥=0: deleting a nonzero invisible part preserves loss and strictly reduces penalty. The solution therefore has the finite form Σαᵢk(xᵢ,·). If Ω is merely nondecreasing, projection still gives a span minimizer whenever one exists, but other minimizers may keep invisible parts. A constant penalty illustrates that distinction.</Prose>
    <RepresenterGeometryLab />
    <Prose>The anchored tent through (0,0),(.5,1),(1,0) has slopes 2,−2 and energy 4. A triangle of height A on [0,.5], peaking at .25, vanishes at both measured locations and has energy 8A². Its cross inner product with the tent is zero: on each observed interval, the constant slope multiplies the wiggle's net change, which is zero. New energy is 4+8A². This shows the actual curves and derivative energy; it does not pretend an infinite-dimensional space is a plane.</Prose>
    <Prose>Minimal-energy interpolation follows interval by interval. Between fixed endpoints with rise Δy across width Δt, Cauchy–Schwarz gives (Δy)²≤Δt∫(f′)². Constant slope Δy/Δt attains equality. Join these straight segments; after the last reading, staying constant costs no further energy. For nodes(.5,1), the Gram matrix [[.5,.5],[.5,1]] times α=(4,−2) gives targets (1,0). The tent is 4min(.5,t)−2min(1,t).</Prose>
    <Example id="interpolation"><Prose>Energy from the segment slopes agrees with αᵀKα. Adding a wiggle preserves the sample values but increases energy. A new observation at its peak makes the former invisible component visible; the evaluation span has changed.</Prose><Prose>Strictly increasing nodes make the mathematical system invertible. Finite arithmetic has narrower limits: the helper rejects overflow, underflow of positive interval energy, or loss of energy positivity. Rescale inputs and targets when it cannot resolve them; a mathematical existence result does not guarantee every floating-point calculation is representable.</Prose></Example>
    <Callout label="The theorem has a precise job">The representer theorem does not by itself prove existence, convexity, unique coefficients or generalization. With no penalty, a loss exp(−f(x₁)) can approach zero without attaining it when that evaluation can grow without bound. Keep the loss and penalty assumptions beside the conclusion.</Callout>

    <H2>6. Fit noisy data with kernel ridge</H2>
    <Prose><strong>Kernel ridge regression</strong> uses squared error and a quadratic RKHS penalty. Declare n≥1, λ&gt;0 and the average-loss convention:</Prose>
    <MathBlock>{String.raw`\begin{aligned}J(f)&=\frac1n\sum_{i=1}^n(f(x_i)-y_i)^2\\&\quad+\lambda\|f\|_{\mathcal H}^2.\end{aligned}`}</MathBlock>
    <Prose>The norm term makes J strictly convex in f. Perturb f by th and differentiate at 0. Reproduction turns the observed residuals into kernel sections, so the derivative vanishes for every h exactly when:</Prose>
    <MathBlock>{String.raw`\lambda f+\frac1n\sum_i(f(x_i)-y_i)k_{x_i}=0.`}</MathBlock>
    <Prose>Define α by (K+nλI)α=y and set f=Σαᵢkₓᵢ. The system is invertible because vᵀ(K+nλI)v≥nλ||v||²&gt;0 for nonzero v. Since Kα−y=−nλα, substitution satisfies the functional equation. Expanding J(f+h) then leaves the nonnegative remainder (1/n)Σh(xᵢ)²+λ||h||². This proves existence and the unique global minimizing function.</Prose>
    <MathBlock>{String.raw`\begin{aligned}(K+n\lambda I)\alpha&=y,\\ f(x)&=\sum_i\alpha_i k(x_i,x),\\ \|f\|_{\mathcal H}^2&=\alpha^TK\alpha.\end{aligned}`}</MathBlock>
    <Prose>Do not cancel K from a coefficient gradient: K may be singular. Duplicate locations have identical rows. The shifted system still has a unique canonical α, but α+v with Kv=0 describes the same function everywhere: the added kernel combination has squared norm vᵀKv=0. Unique function and unique representation are different claims.</Prose>
    <Prose>The original two-point example uses sum-loss diagonal shift .1. For n=2 this matches average-loss λ=.05. These are two normalizations of the same tradeoff. Its exact two-by-two inverse arithmetic is preserved below; general implementations use a linear solve.</Prose>
    <Example id="original"><Prose>The cross-similarity is .368, coefficients approximately −.342 and 1.024, and prediction .531 at .5. A negative coefficient is allowed. Similarities and fitted coefficients are not posterior weights or mixing probabilities.</Prose></Example>
    <KernelRidgeLab />
    <Prose>Targets .5 and 1.5 at the same location cannot both be interpolated by a single-valued function. Their squared losses favor their average 1, while other observations and regularization also influence the fitted value. Duplicate rows yield the same prediction even when their coefficients are large with opposite signs. The function norm is αᵀKα, not αᵀα.</Prose>
    <Example id="ridge"><Prose>The Gram rank is 3 for four observations, yet the shifted solve succeeds. Adding 7(0,1,−1,0) preserves all function values but generally no longer solves the canonical shifted equation. Residual, coefficient size and function norm are separate measurements.</Prose></Example>
    <Prose>With explicit feature matrix Φ, rows φ(xᵢ)ᵀ, the parameter solution is w=(ΦᵀΦ+nλI)⁻¹Φᵀy. The relation w=Φᵀα explains agreement between primal and kernel predictions. Redundant features receive their minimum-norm parameter representation.</Prose>
    <Example id="primal"><Prose>The query prediction is .529799 in both systems, and both function-norm calculations give .571206. This uses the same feature definition and penalty convention; changing either changes the problem.</Prose></Example>
    <details><summary>If the intercept should be free</summary><Prose>A different model predicts b+f and penalizes only f. Differentiating in b gives Σαᵢ=0, and the equations are (K+nλI)α+b1=y with 1ᵀα=0. For n≥1 and λ&gt;0 the canonical α and b are unique: multiply the homogeneous first equation by αᵀ; the b term vanishes, positivity forces α=0, then b=0. A free intercept is not an automatic part of every kernel. The anchored min-kernel space contains no nonzero constant; a constant feature in another space is penalized unless the objective says otherwise.</Prose></details>

    <H3>Fit that exact RKHS objective with KernelRidge</H3>
    <Prose>Use <Code>sklearn.kernel_ridge.KernelRidge</Code> when you need the fitted rule rather than another solver implementation. The crucial translation is <Code>alpha=n*lambda</Code>: this API's alpha is the diagonal shift, whereas λ multiplies the RKHS penalty beside an <em>average</em> squared loss. It is also unrelated to the coefficient vector α. Matching just the word “regularization” is insufficient.</Prose>
    <Prose>The complete comparison reuses the four observations with a duplicate at .5, targets [0,.5,1.5,0] and RBF gamma 4. Neither route centers data, fits an intercept, rescales features or changes observation weights. The manual route uses Cholesky factorization and a solve of K+nλI, without an inverse. The library receives the same inputs. A second fit with <Code>kernel="precomputed"</Code> checks the same Gram matrix; its query matrix has number-of-queries rows and number-of-training-points columns.</Prose>
    <MechanismProgram source="/learn-assets/functional-analysis/kernel_ridge_library.py" title="Open the complete Cholesky / KernelRidge comparison" output={libraryOutput} />
    <Prose>With λ=.05 and library alpha=.2, predictions at [.3,.5,.8] are [.682297,.886083,.475059]. Coefficients, predictions and the average-loss objective agree; the equation's relative residual is below 1e−12. The unshifted Gram matrix has rank three, but the positive shift resolves the duplicate-input singularity. Zero targets produce zero predictions, a structural edge check. This run uses scikit-learn 1.9.1.</Prose>
    <Prose>This dense exact route needs O(n²) Gram storage and O(n³) factorization, with O(n) kernel evaluations per scalar query. An API does not remove large-n memory limits. Keep the fitted training points, gamma, shift and training-fitted preprocessing together for future predictions. The next section's experiment owns parameter selection and held-out evaluation; this four-point fixture verifies algebra, not generalization.</Prose>
    <Checkpoint prompt="Double average-loss λ to .10 while keeping n=4 and gamma=4. What changes in the API, and are the old/new objective numbers directly comparable?">
      <Prose>Set alpha to .4 and refit. The executed predictions become [.628886,.801815,.452453]. The new fitted objective is .224092 under λ=.10, versus .181958 under λ=.05. Their penalty weights differ, so this does not show that the second optimizer performed worse. Compare both routes under each identical objective, or use an unchanged validation criterion for selection. Passing .10 as alpha would silently fit λ=.025.</Prose>
    </Checkpoint>

    <H2>7. Choose and scale the model</H2>
    <Prose>RBF similarity falls to exp(−1) at distance 1/√γ. Larger gamma narrows the influence region; smaller gamma makes bounded inputs increasingly similar. At fixed λ, the small-gamma limit approaches a constant-kernel fit. A narrow kernel may fit isolated readings while giving little information between them, but it does not universally guarantee overfitting; data, penalty and evaluation all matter.</Prose>
    <Prose>Measuring one coordinate in millimeters instead of meters multiplies its squared distance contribution by a million. Decide which scales and structures should count as similar. Fit learned means/scales on training data only and reuse them on validation and test data; do not blindly standardize every category or domain object. Euclidean distances concentrate under some high-dimensional data models, not in every possible high-dimensional dataset.</Prose>
    <Prose>Choose gamma and λ using validation data, then evaluate once on held-out test observations. Repeatedly inspecting test results for selection makes that set part of selection. Kernels are useful candidates for meaningful similarities, structured inputs and smaller datasets; an RKHS theorem does not guarantee superiority to linear, tree or learned-representation baselines.</Prose>
    <Example id="validation"><Prose>This seeded mechanism uses x uniform on [−2,2], y=sin(2x) plus independent Normal noise. The selected RBF pair is gamma 10, λ.001. Its test MSE is .034516 versus .545442 for the selected linear baseline. This is one finite synthetic experiment, not a universal ranking or population guarantee. Change the mechanism or noise and rerun the whole selection protocol.</Prose></Example>
    <H3>Computation has several different costs</H3>
    <Prose>A dense n×n Gram matrix stores O(n²) numbers. A general direct factorization typically costs O(n³) arithmetic; constructing an RBF Gram matrix from d-dimensional inputs directly adds O(n²d). Prediction using all n coefficients costs O(nd) per query. These describe the stated dense approach, not a requirement to materialize K in every algorithm.</Prose>
    <LessonTable caption="Different ways to change kernel computation" headers={['Method', 'Mechanism', "Limit to inspect"]} rows={[["Nyström / landmarks", "Project evaluation sections onto selected locations; store n×m feature coordinates.", "Landmark coverage, numerical rank and approximation error."], ["Random Fourier features", "Approximate a shift-invariant kernel expectation with one frozen random feature map.", "Random error, kernel assumptions and identical train/query features."], ["Matrix-free iterations", "Repeatedly apply the shifted operator without necessarily storing every entry.", "Multiplication cost, conditioning, convergence and stopping residual."]]} />
    <details><summary>Construct and execute two approximations</summary><Prose>Let Z contain landmarks, C=K(X,Z), and W=K(Z,Z)=UΛUᵀ. Retain positive eigenvalues above a declared numerical threshold and form F=C UᵣΛᵣ⁻¹ᐟ². Then FFᵀ=CW†Cᵀ is PSD, where † here uses that same retained eigenspace. It equals the exact mathematical pseudoinverse only when every positive eigenvalue is retained. These are coordinates of projections onto the retained section directions, not an exact factorization of every original K. Duplicate landmarks can lower rank. At a query use the same landmarks and normalization.</Prose><Prose>For the scalar Gaussian kernel draw ω from Normal(0,2γ), and independent b uniform on [0,2π]. Feature √2cos(ωx+b) has expected product exp(−γ(x−z)²): averaging over b gives cos(ω(x−z)), and the Normal cosine expectation gives the exponential. Concatenate D draws and divide by √D. Never redraw features separately for each query. The general shift-invariant construction uses Bochner's theorem; this branch uses the stated Gaussian identity.</Prose><Example id="approximation"><Prose>The program reports actual approximation errors and predictions. This seed's four-landmark fit is closer to its exact Gram matrix than its 128-feature random fit; that is not a general ranking. Dense m-feature ridge costs approximately O(nm²+m³), plus feature construction. O(nm) feature storage alone is not its training cost. Iterative or stochastic solvers have different costs and convergence contracts.</Prose></Example></details>

    <H2>8. Represent a whole distribution</H2>
    <Prose>Expectation ℓP(f)=Eₚf(X) is another scalar measurement. The earlier f-Divergences/IPMs lesson used finite feature means; here we ask when the mean is a legitimate RKHS vector and what information it retains. Require measurable functions/integrands and Eₚ√k(X,X)&lt;∞. The reproducing bound implies E|f(X)|≤||f|| E√k(X,X), so expectation is a bounded linear functional.</Prose>
    <MathBlock>{String.raw`\begin{aligned}|\mathbb E_P f(X)|&\leq\|f\|\,\mathbb E_P\sqrt{k(X,X)},\\ \langle f,\mu_P\rangle&=\mathbb E_P f(X).\end{aligned}`}</MathBlock>
    <Prose>Riesz supplies μP, with μP(t)=Eₚk(X,t). If writing μP=Eₚk(X,·) as a Hilbert-valued integral, also require strong measurability of the feature map. Continuous kernels on the separable Euclidean domains here supply the relevant measurability; arbitrary nonmeasurable kernels do not. The integrability condition is sufficient, not asserted necessary in every formulation.</Prose>
    <Prose>For a Gaussian kernel k(x,x)=1, the condition holds for every Borel probability law, even one with infinite ordinary mean. An unbounded linear or polynomial kernel can require ordinary moments. Kernel validity alone does not make every expectation exist.</Prose>
    <Prose>Let d=μP−μQ. Cauchy–Schwarz shows that the largest absolute expectation difference over ||f||≤1 is ||d||, attained at f=d/||d|| when d≠0. This is <strong>maximum mean discrepancy (MMD)</strong>. Expanding its squared norm gives the pair formula; X,X′ are independent P draws and Y,Y′ are independent Q draws, with the cross pair also independent.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\operatorname{MMD}(P,Q)\\=\sup_{\|f\|\leq1}|\mathbb E_Pf-\mathbb E_Qf|,\\\operatorname{MMD}^2(P,Q)\\=\mathbb E k(X,X')+\mathbb E k(Y,Y')\\\quad-2\mathbb E k(X,Y).\end{gathered}`}</MathBlock>
    <KernelMeanLab />
    <Prose>P assigns equal mass at −1,1. Q assigns 1/8 at −2, 3/4 at 0 and 1/8 at 2. Both means are 0 and second moments 1, but fourth moments are 1 and 4. The valid polynomial kernel (1+xz)² therefore gives equal feature means and zero MMD despite different laws. A <strong>characteristic kernel</strong> is one whose mean embedding is injective on the declared probability class: equal means imply equal laws.</Prose>
    <Prose>A Gaussian kernel on Rᵈ at every fixed positive bandwidth is characteristic on Borel probability laws. This domain-wide theorem does not follow from our finite example. One proof writes discrepancy through squared differences of characteristic functions, weighted by a strictly positive Gaussian Fourier density, then uses uniqueness of those transforms. That uniqueness theorem is further analysis machinery; the general conclusion is stated with its source, not claimed proved by one plot.</Prose>
    <Prose><strong>Universal</strong> asks a different question: is H dense in a specified function class under a specified topology? On a compact domain a common definition uses continuous functions and uniform error. Characteristic concerns separation of probability laws. Gaussian kernels have appropriate properties under established conditions, but these terms are not synonyms for arbitrary kernels/domains. Neither guarantees a powerful finite-sample test or a large discrepancy for every pair.</Prose>
    <Example id="means"><Prose>Gaussian MMD² is .465956 at gamma 1; the unit witness achieves expectation gap .68261. Matching polynomial means and the nonzero Gaussian witness measure the same laws differently. Sample estimators, permutation calibration and test decisions belong to the earlier MMD lesson. A population discrepancy is not a p-value.</Prose></Example>

    <H2>9. Design an integration rule</H2>
    <Prose>Suppose evaluating f is expensive and we want I(f)=∫₀¹f(t)dt. In the anchored min-kernel space the integration representer is the average kernel section:</Prose>
    <MathBlock>{String.raw`\begin{aligned}m(t)&=\int_0^1\min(x,t)\,dx\\&=t-\frac{t^2}{2},\\m'(t)&=1-t,\qquad\|m\|^2=\frac13.\end{aligned}`}</MathBlock>
    <Prose>With one reading at x, estimate the integral by w f(x). Error is〈f,m−wkₓ〉. Its worst absolute value over the unit ball is ||m−wkₓ||, so choose w by projecting m onto kₓ. For x&gt;0, w=m(x)/x=1−x/2.</Prose>
    <MathBlock>{String.raw`\begin{gathered}\sup_{\|f\|\leq1}|I(f)-wf(x)|\\=\|m-wk_x\|,\\\min_w\|m-wk_x\|^2\\=\frac13-x\left(1-\frac{x}{2}\right)^2.\end{gathered}`}</MathBlock>
    <KernelQuadratureLab />
    <Prose>At x=.5, w=.75 and the squared bound is 5/96. For f(t)=t, with norm 1, the estimate is .375 versus actual integral .5; error .125 lies below √(5/96)≈.228218. The normalized residual function attains the bound, so it is the actual worst-case value. Optimizing x gives 2/3, weight 2/3 and squared bound 1/27: the derivative of x(1−x/2)² is (1−x/2)(1−3x/2), positive before 2/3 and negative afterward on [0,1].</Prose>
    <Prose>Why do weights not sum to 1? Nonzero constants are not members of this anchored space. Requiring constant exactness would change the optimization or the function space. At x=0 every reading is zero, so no weight extracts information. For several nodes, projection gives Kw=m(X); duplicate nodes may yield different weights representing the same functional.</Prose>
    <Example id="quadrature"><Prose>Duplicate .5 nodes split the same total weight .75 and preserve the bound. The code integrates the derivative residual squared using a nonnegative interval expression, rather than labeling a small negative floating-point subtraction as a certified error. A function-norm bound M scales the worst-case error by M. This deterministic guarantee is neither a confidence interval nor a claim about functions outside the declared class.</Prose></Example>

    <H2>10. Connect operators, spectra and random functions</H2>
    <H3>A spectrum depends on the reference measure</H3>
    <Prose>Take a compact metric domain X, a continuous symmetric PSD kernel k, and a finite Borel measure ν with support all of X. The integral operator Tₖf(x)=∫k(x,z)f(z)dν(z) on L²(ν) is compact, positive and self-adjoint. Self-adjoint means〈Tf,g〉=〈f,Tg〉. Compact means images of bounded sequences have convergent subsequences. Under these conditions Mercer's theorem gives k(x,z)=Σλⱼeⱼ(x)eⱼ(z), with positive eigenvalues, orthonormal L² modes and absolute uniform convergence, using continuous eigenfunction representatives.</Prose>
    <Prose>The RKHS basis is√λⱼeⱼ. Thus f=Σaⱼeⱼ belongs to H when Σaⱼ²/λⱼ is finite, with that sum as its squared norm. Zero-eigenvalue L² directions are not given a finite RKHS cost by division by zero. The modes and coordinates depend on ν although the kernel stays fixed. Full support matters for the stated everywhere expansion: an integral operator cannot see values on an open region of measure zero.</Prose>
    <OperatorModesFigure />
    <Prose>For k(x,z)=1+xz and uniform probability measure on [−1,1], T1=1 and T(√3z)(x)=√3x/3. Integrating 1,z,z² gives 1,0,1/3. Hence f=a+bx has L² squared norm a²+b²/3 and RKHS squared norm a²+b². For a=2,b=3 these are 7 and 13. One is geometry under a reference measure; the other is the kernel's function geometry.</Prose>
    <Example id="spectra"><Prose>The quadrature checks two actual eigenfunction identities. Its final rows illustrate an inverse that turns data norm 1/n into reconstructed norm 1. The sequence argument below establishes why no fixed stability bound covers every n.</Prose></Example>
    <details><summary>Adjoints and an unbounded inverse</summary><Prose>For bounded A:H→G, fix g∈G. The map f↦〈Af,g〉 is a bounded functional; Riesz defines its representer A*g. This adjoint satisfies〈Af,g〉=〈f,A*g〉. In finite orthonormal real coordinates it is the matrix transpose.</Prose><Prose>On ℓ² define(Ta)ₙ=aₙ/n. Its operator norm is 1. Truncating to the first N output coordinates gives a finite-rank map differing from T by operator norm 1/(N+1), establishing compactness. T is injective but its inverse on its range is unbounded: the nth unit input has norm 1, its image norm 1/n. Inverse amplification n has no finite uniform bound. Regularization balances data fidelity against this instability; invertible finite truncations do not eliminate the underlying limiting question.</Prose></details>
    <H3>A kernel can also describe random functions</H3>
    <Prose>A Gaussian process assigns a joint Gaussian law to each finite collection of function values. Its covariance can use the same PSD kernel. With zero prior mean and independent noise variance σ², its posterior mean is k(x,X)(K+σ²I)⁻¹y. This matches average-loss KRR when σ²=nλ. A posterior covariance or credible interval additionally requires the probability model; it does not follow from the RKHS norm alone.</Prose>
    <Prose>Brownian motion has covariance min(s,t), but its typical path is not a member of our anchored RKHS. For any f in that space, squared increments over a partition sum to at most the largest interval length times∫|f′|², tending to zero with mesh size. Brownian squared increments along nested dyadic partitions converge almost surely to 1 on [0,1], as developed in Stochastic Processes. Covariance geometry and random-path membership are different statements. Do not extend this outside-membership claim indiscriminately to finite-dimensional Gaussian processes.</Prose>
    <Prose>The next module topic is <strong>Topology &amp; Topological Data Analysis (TDA)</strong>. Here a norm supplied distance, neighborhoods and continuity. Topology broadens those ideas and asks which structures persist under changes of representation. It follows the declared module sequence, not whichever later lesson is already published.</Prose>

    <H2>11. Practise and transfer</H2>
    <Prose>Attempt these changed cases before opening hints. Give calculations and interpretations with the space, norm and loss conventions. The capstone assesses a defensible workflow, not whether a favored model wins.</Prose>
    <Practice title="A. An amplified spike" question="A triangle centered at .5 has height 3 and half-width 1/12. Compute its squared L² norm, point value and derivative energy. Can a fixed L² evaluation bound work as width shrinks?" hint="Scale squared measurements by height squared."><Prose>Squared L² size is 2×(1/12)×9/3=1/2; point value 3; derivative energy 2×9/(1/12)=216. As width tends to zero, L² norm tends to zero while evaluation remains 3. No finite universal bound works. The separate equivalence-class objection is that a single-point change does not change an L² vector at all.</Prose></Practice>
    <Practice title="B. Read changed slopes" question="Use slopes 2 on [0,.2], −1 on [.2,.7], and 3 on [.7,1], anchored at 0. Find f(.8), its squared H norm and the evaluation bound. What is f(0)?" hint="Only part of the last interval contributes to evaluation; its full length contributes to the norm."><Prose>f(.8)=.4−.5+.3=.2. Squared norm is 4×.2+1×.5+9×.3=4. The bound is 2√.8≈1.788854. At 0 the value and its representer are both zero. Signed slope contributions determine the value; squared slopes determine energy.</Prose></Practice>
    <Practice title="C. Repair a similarity claim" question="Replace the invalid matrix's.9 entries and symmetric partners with .8. All entries and pair determinants are nonnegative. Is it PSD? Construct a feature distinguishing ABA and BAB." hint="Reuse the coefficient witness and add a relevant third pattern."><Prose>For c=(1,−1,1), cᵀKc=3−4×.8=−.2. Thus it is invalid. Add a feature counting the pattern ABA: it is 1 for ABA and 0 for BAB. Dotting enlarged feature vectors gives a valid kernel separating this pair, without promising to distinguish every string.</Prose></Practice>
    <Practice title="D. A changed interpolant" question="Interpolate(.25,1) and (.75,.5) in the anchored space. Find slopes, energy and two kernel coefficients. What happens after .75?" hint="Compute endpoint rises first, then solve the two-by-two min-kernel system."><Prose>Slopes are 4 on [0,.25], −1 on [.25,.75], and 0 afterward. Energy is 16×.25+1×.5=4.5. The Gram equations give α=(5,−1), so f(t)=5min(.25,t)−min(.75,t). It remains .5 after .75 because extra change would cost energy without satisfying new observations.</Prose></Practice>
    <Practice title="E. Duplicate observations and normalization" question="Use the constant kernel k=1, targets (0,2), and average-loss λ=.1. Find the fitted constant and a canonical α. Does αᵀα measure its function norm?" hint="Minimize the scalar objective or use diagonal shift .2."><Prose>The objective is 1.1c²−2c+2, so c=10/11. The canonical α is (−50/11,60/11), with sum 10/11. Squared function norm is 100/121; squared coefficient norm is 6100/121. Adding(t,−t) preserves the function. Shift .1 would instead mean average λ=.05 and a different fitted constant.</Prose></Practice>
    <Practice title="F. What do moments forget?" question="P puts equal mass at±2. Q puts 1/8 at±4 and 3/4 at 0. Which zeroth, first, second and fourth moments agree? What does kernel (1+xz)² detect?" hint="Both second moments are 4; calculate the fourth separately."><Prose>Zeroth moments are 1, means 0 and second moments 4. Fourth moments are 16 and 64. Polynomial feature means agree, so MMD is zero despite different laws. A fixed positive-bandwidth Gaussian is characteristic on these laws and has positive MMD; the theorem supplies neither its magnitude nor a finite-sample significance decision. For unbounded kernels/laws, first check existence.</Prose></Practice>
    <Practice title="G. Change the integration node" question="Use one reading at x=1/4. Derive the optimal weight, squared worst-case unit-ball error and estimate for f(t)=t. Why need the weight not equal 1?" hint="Use m(x)/x; distinguish the error functional's norm from its value on one function."><Prose>The weight is 7/8. Squared bound is 1/3−(1/4)(7/8)²=109/768≈.141927. The estimate is 7/32, versus integral 1/2; error 9/32=.28125 is below √(109/768)≈.376732. Constant exactness was not imposed because nonzero constants are outside this anchored space. Adding that constraint changes the question.</Prose></Practice>
    <Practice title="H. Evaluate a changed experiment" question="Change noise standard deviation to .35 in the native validation example. Select each model on validation and evaluate test once. Report choices, errors and an honest conclusion. Repeat for a mostly linear target." hint="Keep training-only scaling and the full selection protocol. There is no prescribed winning model."><Prose>A valid record gives seed, generating function, noise, split sizes, metric and training-only preprocessing; prints selected settings before test results; and reports both baselines. Keeping seed 39 and every other operation unchanged, noise .35 selects RBF gamma 10 and average-loss lambda .01. Training MSE is .056774 and squared RKHS norm is 3.379214; validation/test MSE are .155172/.177157. The linear model selects lambda .1, with validation/test MSE .567841/.633646. These executed values are rounded to six decimals.</Prose><Prose>For a concrete second case, replace sin(2x) by .4+1.2x and retain noise .35 and the same protocol. RBF selects gamma .1 and lambda .001, with validation/test MSE .127857/.143528. Linear ridge selects lambda .001, with validation/test MSE .095880/.091864. Here the simpler model has lower held-out error. A different chosen target need not reproduce these numbers.</Prose><Prose>One run does not establish superiority. A stronger follow-up repeats independent draws under a predeclared protocol and reports variation. Numerical solve error and statistical prediction error remain separate checks; selecting a model after inspecting these test outcomes would change the protocol.</Prose></Practice>
    <Practice title="I. Follow the infinite-dimensional assumption" question="Why does the finite-support subspace have no closest point to its infinite limit? Why do invertible finite truncations of aₙ↦aₙ/n not imply a bounded infinite inverse?" hint="Separate an unattained distance infimum from amplification growing with n."><Prose>The target is outside the finite-support subspace, while its truncations approach it in ℓ², so the distance infimum 0 is unattained. That subspace is not closed. For the diagonal map the nth unit input has norm 1 and output 1/n; inverse amplification n has no finite uniform bound. Finite computations illustrate these quantified arguments rather than proving them alone.</Prose></Practice>

    <Sources alternatives={<><Prose><a href="https://www.youtube.com/watch?v=KcI2_r51Eb8" target="_blank" rel="noreferrer">Casey Rodriguez / MIT 18.102: Minimizers, Orthogonal Complements and Riesz</a> offers a proof-oriented alternative after sections 1–3. Its <a href="https://ocw.mit.edu/courses/18-102-introduction-to-functional-analysis-spring-2021/resources/18102-sp21-lecture-17/" target="_blank" rel="noreferrer">official course page</a> and substantive companion notes were checked; playback and timestamps were not reviewed.</Prose><Prose><a href="https://ocw.mit.edu/courses/18-102-introduction-to-functional-analysis-spring-2021/8fb8d5c170f1613151aca71de21027bc_MIT18_102s21_full_lec.pdf" target="_blank" rel="noreferrer">MIT's notes, lecture 17</a> develop projection more formally. The full course is further study, not a prerequisite checklist. <a href="https://arxiv.org/pdf/1807.02582" target="_blank" rel="noreferrer">Kanagawa and coauthors' GP/kernel review</a> develops spectral, sample-path and quadrature connections with additional assumptions and convention choices.</Prose></>}>
      <li><a href="https://alex.smola.org/papers/2001/SchHerSmo01.pdf" target="_blank" rel="noreferrer">Schölkopf, Herbrich &amp; Smola: A Generalized Representer Theorem</a> — construction and projection proof, including strict versus non-strict penalty conditions. Its positive-definite-kernel terminology includes semidefinite matrices.</li>
      <li><a href="https://jmlr.org/papers/volume12/sriperumbudur11a/sriperumbudur11a.pdf" target="_blank" rel="noreferrer">Sriperumbudur, Fukumizu &amp; Lanckriet: Universality, Characteristic Kernels and RKHS Embedding of Measures</a> — advanced conditions for distribution separation and function approximation; the terms are not unconditional synonyms.</li>
      <li><a href="https://papers.nips.cc/paper/3182-random-features-for-large-scale-kernel-machines.pdf" target="_blank" rel="noreferrer">Rahimi &amp; Recht: Random Features</a> — section 3 gives the real Fourier-feature expectation and probabilistic approximation setting. Historical performance claims are not current rankings.</li>
      <li><a href="https://scikit-learn.org/stable/modules/kernel_approximation.html" target="_blank" rel="noreferrer">scikit-learn: Kernel Approximation</a> — practical Nyström and RBF-map entry points after the native construction; check installed API versions and actual approximation error.</li>
      <li><a href="https://numpy.org/doc/stable/reference/generated/numpy.linalg.solve.html" target="_blank" rel="noreferrer">NumPy: solve</a> — square/full-rank and right-hand-side shape contracts; current stable documentation can be newer than this lesson's executed versions.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.kernel_ridge.KernelRidge.html">scikit-learn KernelRidge</a> — alpha, kernel/gamma, fitted coefficients and precomputed query shape for the executed 1.9.1 bridge. Translate the average-loss normalization before comparing predictions.</li>
    </Sources>
  </div>
};
