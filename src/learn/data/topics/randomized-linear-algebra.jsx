import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { WeightedColumnProbeLab, SpectralSketchLab, RowSketchLab, TraceEstimatorLab, RandomizedFactorFlow, SketchDirectionFigure } from '../../components/lesson-labs/RandomizedLinearAlgebraLabs.jsx';
import { randomizedLinearAlgebraExamples } from '../randomized-linear-algebra-examples.js';
export default {
  title: 'Randomized Linear Algebra',
  readTime: '~55 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot randomized-linear-algebra-lesson">
    <LessonIntro exampleKind="NumPy" prerequisites="Column combinations, orthogonal projection, QR/SVD, singular values and least squares from the preceding linear-algebra lessons. We introduce the small amount of probability needed: a random draw, its average over repeated experiments, independence and variance. No probability theorem is silently assumed to hold for every seed." sections={[['1-ask-a-large-matrix-a-few-small-questions', 'What a probe sees'], ['2-build-a-small-factorization-and-lift-it-back', 'Range finding and SVD'], ['3-separate-rank-error-from-sketch-error', 'Accuracy and probability'], ['4-spend-computation-and-data-passes-deliberately', 'Iteration and cost'], ['5-compress-observations-without-changing-the-question', 'Least-squares sketches'], ['6-estimate-a-trace-through-matrix-products', 'Random-sign estimation'], ['7-validate-and-choose-a-useful-application', 'Validation and applications'], ['8-practise-with-new-failures-and-new-data', 'Practice and next']]}>
      A large matrix may contain far more entries than the directions or aggregate quantity you need. Learn to ask it a few carefully designed random questions, solve a smaller problem, and check what survived. A smaller computation is useful only when its answer still serves the original task.
    </LessonIntro>

    <H2>1. Ask a large matrix a few small questions</H2>
    <Prose>Imagine a matrix whose columns are many snapshots from a measurement system. Each column contains the same sensors at a different instant. You want a few patterns that reconstruct the snapshots reasonably well. Computing every singular direction may do much more work than the requested rank needs. Can combinations of snapshots reveal a useful working space first?</Prose>
    <Prose>Write the columns of a real m×n matrix A as a₁ through aₙ. Multiplying A by a vector ω forms a weighted sum of those columns: Aω=ω₁a₁+⋯+ωₙaₙ. The output has m entries and lies in A's column space. A <strong>probe</strong> is one chosen input vector; its output tells us how the matrix acts on that combination. A <strong>sketch</strong> collects a small number of such compressed observations.</Prose>
    <WeightedColumnProbeLab />
    <Prose>For A=[[3,0,3],[0,1,1]], weights [1,1,0] produce y=[3,1]. Normalize it to q=[3,1]/√10. Projecting the three original columns onto that line gives [2.7,0.9], [0.3,0.1] and [3,1]. Their squared residual lengths are 0.9, 0.9 and 0: total 1.8. The third column was captured exactly, but one observed line did not recover the full two-dimensional column space.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.weightedColumns}><Prose>The second probe [1,1,−1] cancels to zero although A is nonzero. It cannot be normalized. We display a zero approximation in that case and explicitly report the lost squared norm, 20; we do not invent an observed direction.</Prose></RunnableExample>
    <Prose>Random probes reduce the risk of consistently choosing an unhelpful deterministic direction. In the basic algorithm, each entry is drawn independently from a standard Gaussian distribution: a symmetric bell-shaped distribution with mean zero and variance one, meaning an average squared value of one. Independence means one draw supplies no information about another. In this ideal continuous model, landing exactly in a fixed nonzero matrix's null space has probability zero. That does <em>not</em> say a few draws always represent its important directions accurately. Near misses, incomplete coverage and finite precision remain possible.</Prose>
    <Checkpoint prompt="If one nonzero probe returns zero, can you conclude A is zero? If two probe outputs are parallel, how many independent directions have you observed?">
      <Prose>No: the first fixture gives a direct counterexample. Two nonzero parallel outputs observe one independent direction, even though there were two probes. Probe count, observed rank and the final rank you request are different quantities.</Prose>
    </Checkpoint>

    <H2>2. Build a small factorization and lift it back</H2>
    <Prose>Choose a final target rank k and p extra probes, called <strong>oversampling</strong>. Their total is ℓ=k+p, capped at the smaller matrix dimension in our implementation. Draw Ω with shape n×ℓ and compute Y=AΩ. Each column of Y is one output probe. Replace those columns by an orthonormal basis Q for their observed space, using QR when the sketch is numerically full rank or a rank-revealing method when it is not.</Prose>
    <RandomizedFactorFlow />
    <Prose>Let r be Q's number of columns. Then QᵀQ=Iᵣ. For any original column a, Qᵀa contains its coordinates along Q, and QQᵀa is its closest point in that subspace. Form B=QᵀA: all n columns expressed in r coordinates. B is r×n, and QB=QQᵀA approximates the original matrix.</Prose>
    <MathBlock>{String.raw`\begin{aligned}Y&=A\Omega,\qquad Q=\operatorname{orth}(Y),\\ B&=Q^\top A,\\ B_k&=\widetilde U_k\Sigma_k V_k^\top,\\ \widehat A_k&=(Q\widetilde U_k)\Sigma_k V_k^\top.\end{aligned}`}</MathBlock>
    <Prose>Compute the SVD of the <em>small</em> matrix B and retain at most k singular components. Its left singular vectors live in r-dimensional sketch coordinates. Multiplication by Q lifts them back into the original m-dimensional space. The lifted columns remain orthonormal because (Q Ũₖ)ᵀ(Q Ũₖ)=ŨₖᵀŨₖ=I. They are singular directions of the approximation; they are not automatically exact singular vectors of A.</Prose>
    <Prose>This is the two-stage randomized SVD: discover a working space, then factor within it. It avoids a full SVD of A, but it still reads or applies A to form both Y and B. “Compressed” does not mean the full input disappears without being examined.</Prose>
    <H3>A complete numerical implementation</H3>
    <Prose>Each program is standalone. Use Python with NumPy, save one as <Code>randomized_example.py</Code>, and run <Code>python randomized_example.py</Code>. Outputs were captured with Python 3.12 and NumPy 2.3.5. The helper below uses an SVD of the <em>skinny sketch</em> to detect numerical rank; it does not factor the original A to build the approximation. It orthogonalizes between every alternating product in its optional iterations.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.gaussianBaseline}><Prose>The 100×40 input is a full Gaussian matrix, not a planted low-rank signal. A rank-five approximation has relative Frobenius error about 0.898. Even the best rank-five approximation has error about 0.853. The poor compression is largely a property of the input and target rank, not evidence that a different seed can recover information that five directions cannot represent.</Prose></RunnableExample>
    <Prose>The code's exact SVD of A supplies a small-example benchmark <em>after</em> the randomized calculation. That oracle is additional work and is not part of the claimed scalable algorithm. For a matrix too large to factor, use a justified validation strategy rather than secretly relying on a full-SVD check.</Prose>
    <Checkpoint prompt="A is 1000×80, k=5 and p=4, and the sketch has nine independent columns. Give the shapes of Ω, Q, B, Ũₖ and the lifted Uₖ. Which dimension is still large in B?">
      <Prose>Ω is 80×9, Q is 1000×9, B is 9×80, Ũₖ is 9×5, and Q Ũₖ is 1000×5. B retains all 80 original columns. The final factors also include five singular values and Vₖᵀ of shape 5×80. If the observed rank were below nine, Q and B would be correspondingly smaller.</Prose>
    </Checkpoint>

    <H2>3. Separate rank error from sketch error</H2>
    <Prose>A useful accuracy statement must name its metric. The <strong>Frobenius norm</strong> is the square root of the sum of squared entries. It aggregates reconstruction error over all columns. The <strong>spectral norm</strong> is the greatest output length over unit input vectors: the worst amplified direction. For a nonzero A, relative Frobenius error divides ‖A−Â‖F by ‖A‖F. If A is zero, that ratio is undefined even when the absolute error is zero.</Prose>
    <Prose>The exact rank-k SVD sets an unavoidable benchmark. If σ₁≥σ₂≥⋯ are A's singular values, the best squared Frobenius error is the sum of σⱼ² for j&gt;k. The best spectral error is σₖ₊₁, with zero after all available directions are retained. These statements concern rank at most k; they do not say that the leading directions are necessarily useful signal for a particular scientific task.</Prose>
    <SpectralSketchLab />
    <H3>Account for two distinct losses</H3>
    <Prose>First, Q misses the part of A perpendicular to its span. Second, keeping only k components may discard directions <em>inside</em> that span. These residuals are orthogonal column by column. The first lies outside Q, while Q(B−Bₖ) lies inside it, so their cross inner products vanish.</Prose>
    <MathBlock>{String.raw`\begin{aligned}R&=(I-QQ^\top)A,\\ D&=B-B_k,\\ E&=A-QB_k=R+QD,\\ \|E\|_F^2&=\|R\|_F^2+\|D\|_F^2.\end{aligned}`}</MathBlock>
    <Prose>Here R is the perpendicular residual, D is the discarded part in small coordinates, and E is the total reconstruction residual. The letters simply keep the two contributions visible without a long nested expression.</Prose>
    <Prose>Q preserves the length of a vector in its coordinate space, explaining why the second term can be measured on the small B. This exact-arithmetic identity gives the two colored error contributions in the investigation. Floating-point results agree up to numerical tolerance. Improving range finding can lower the first term; it cannot remove the best rank-k tail imposed by the final representation.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.errorDecomposition}><Prose>The same complete helper handles an exactly rank-one matrix and a zero matrix without dividing by a zero singular value or treating arbitrary QR completion columns as newly observed information. Its numerical-rank threshold is based on machine precision and sketch dimensions; near a threshold, numerical rank is a modeling decision, not an exact symbolic theorem.</Prose></RunnableExample>
    <H3>What a probabilistic guarantee actually says</H3>
    <Prose>Expectation, written E, means an average over repeated independent random draws with the input fixed. It is not an upper bound for each individual draw. For a real matrix, a standard Gaussian Ω, k≥2, p≥2 and k+p≤min(m,n), one classical bound applies to the orthogonal projector Pᵧ onto the sampled range Y=AΩ:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\tau_k^2&=\sum_{j>k}\sigma_j^2,\\ Z&=\|(I-P_Y)A\|_F,\\ \mathbb E Z&\le\sqrt{1+\frac{k}{p-1}}\;\tau_k.\end{aligned}`}</MathBlock>
    <Prose>Here τₖ is the exact rank-k tail norm, and Z names the random range-projection error. Only Z varies with the random sketch when A and k stay fixed.</Prose>
    <Prose>This is the <em>range-projection</em> bound in Theorem 10.5 of Halko, Martinsson and Tropp. The range can have k+p directions, so do not label it a bound for the final rank-k truncation without accounting for that additional step. It concerns an ideal probability model; the browser's finite seeded draws illustrate the mechanism rather than verify the theorem. The lab allows p=0 or 1 to show failures, but the displayed theorem does not cover those settings.</Prose>
    <Prose>Oversampling supplies more independent combinations, reducing the chance that the dominant subspace is badly represented. For fixed nested probes in exact arithmetic, adding columns cannot worsen projection onto their combined range: the old candidate approximations remain available in a larger space. Changing the seed at the same time removes that direct nested-space comparison. A failure probability or high-probability tail bound is another kind of statement, with its own assumptions and parameters.</Prose>
    <Checkpoint prompt="For six equal singular values of 6, what is the best rank-two relative Frobenius error? Can unlimited power iterations at rank two make it zero?">
      <Prose>Four of six equal squared contributions remain: √(4/6)≈0.8165. Iteration cannot beat this rank constraint. Increasing the final rank changes the benchmark; merely improving the discovery of a two-dimensional subspace does not.</Prose>
    </Checkpoint>

    <H2>4. Spend computation and data passes deliberately</H2>
    <Prose>When singular values decay slowly, a simple sketch may mix the wanted and unwanted directions too evenly. <strong>Subspace iteration</strong> emphasizes stronger directions by alternating Aᵀ and A. Conceptually, (AAᵀ)ᑫA has the same singular directions as A but singular values σⱼ²ᑫ⁺¹. The ratio between a smaller and larger singular value is raised to that odd power, making separation stronger when there is a gap.</Prose>
    <MathBlock>{String.raw`\begin{aligned}Q_0&=\operatorname{orth}(A\Omega),\\ Z_t&=\operatorname{orth}(A^\top Q_{t-1}),\\ Q_t&=\operatorname{orth}(AZ_t).\end{aligned}`}</MathBlock>
    <Prose>Do not explicitly form AAᵀ or raise a giant matrix to a power. Compute these products with skinny blocks. Reorthogonalize between <em>both</em> products: otherwise dominant components can overwhelm weaker ones in floating-point arithmetic before a final normalization has a chance to recover them. Orthonormalization rescales the working basis without changing its exact span when the relevant rank is retained.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.spectrumBudget}><Prose>On the constructed decaying spectrum, extra probes already approach the rank-three floor, and one iteration reaches the displayed precision. On the flat spectrum, all settings share the same unavoidable error. These are calculated reconstruction errors for declared inputs, not timing measurements or evidence that these parameter choices suit every dataset.</Prose></RunnableExample>
    <LessonTable caption="Cost of the simple dense Gaussian algorithm" headers={['Work', 'What it costs', 'Why it matters']} rows={[['AΩ and final QᵀA', 'Two full matrix products, each O(mnℓ)', 'The two-stage algorithm normally sees the input twice.'], ['q alternating iterations', '2q more products with A or Aᵀ', 'The standard pass count is 2q+2; communication may dominate arithmetic.'], ['Skinny basis work and small SVD', 'Order (q+1)(m+n)ℓ² when ℓ is small', 'A large oversampling width can remove the intended saving.'], ['Stored working factors', 'Order (m+n)ℓ entries, apart from input storage', 'Q and B can still be substantial.'], ['Explicit reconstruction Â', 'mn output entries plus multiplication work', 'Keep U, s and Vᵀ as factors when the application can use them directly.']]} />
    <Prose>For sparse A, multiplying by a dense ℓ-column block can use order nnz(A)ℓ arithmetic, where nnz counts stored nonzeros, while other steps may still be dense. Fast structured or sparse random maps have different construction, storage and distortion contracts. A bound for a Fourier/Hadamard-based sketch is not the cost of the dense Gaussian matrix used here. Wall-clock speed depends on dimensions, hardware, memory access, accuracy requirements and the deterministic alternative being compared.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.blockedProducts}><Prose>The first pass forms each row block of Y. After finding Q, the second accumulates B=Σ Q_blockᵀ A_block. The tiny input is kept in memory solely to check equality. In an actual out-of-core reader, the data must be replayable and Q still needs storage. This is two-pass processing, not a one-pass streaming algorithm.</Prose></RunnableExample>
    <details className="lesson-deeper"><summary>When the data can be read only once</summary>
      <Prose>Y=AΩ alone does not contain the later product QᵀA, because Q is learned only after Y has been accumulated. One-pass methods gather additional independent sketches while data arrives and reconstruct the needed small coordinates afterward. That adds a recovery problem and can change the error. It is a different algorithm; deleting the second pass from this one leaves missing information. The monograph's single-view chapter develops that branch.</Prose>
      <Prose>A related alternative for slowly decaying spectra is a block Krylov space: retain and orthogonalize several successive powered blocks rather than only the most recent block. It can preserve a richer polynomial working space, at the cost of additional basis size, storage and orthogonalization. The simple subspace iteration above is not a complete block-Krylov implementation.</Prose>
    </details>

    <H2>5. Compress observations without changing the question</H2>
    <Prose>Now A is a table of m observations and d features, with m much larger than d. The unknown coefficient vector x has d entries; the response b has m. Least squares chooses x to make the residual Ax−b small. Instead of combining A's columns to reconstruct it, multiply the <em>observations</em> by a sketch S of shape s×m and solve min ‖SAx−Sb‖₂ with fewer rows.</Prose>
    <SketchDirectionFigure />
    <Prose>Apply the <strong>same S</strong> to A and b. Mixing features and responses differently no longer represents S(Ax−b). More subtly, a very small residual in the sketch can hide a large residual in the original data. The reduced solver optimizes the objective you supplied, not the objective you wish it preserved.</Prose>
    <RowSketchLab />
    <RunnableExample example={randomizedLinearAlgebraExamples.rowSketches}><Prose>The two endpoints fit with zero sketch error but original squared error 2.6075, compared with the full optimum 1.438843. The first four and Gaussian sketches also produce different coefficients and errors. That one Gaussian draw works reasonably on this fixture is an observation, not a universal guarantee or proof that coefficient estimates are accurate.</Prose></RunnableExample>
    <H3>Preserve the whole residual space</H3>
    <Prose>Every possible residual Ax−b lies in the span of the columns of [A | b]. Suppose S approximately preserves squared lengths throughout that subspace, with 0&lt;ε&lt;1. This is a <strong>subspace embedding</strong>. It is stronger than checking several convenient data points: a solver may choose an untested combination.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\|Sr\|_2^2&\ge(1-\varepsilon)\|r\|_2^2,\\ \|Sr\|_2^2&\le(1+\varepsilon)\|r\|_2^2.\end{aligned}`}</MathBlock>
    <Prose>Let x* minimize the original residual and x̂ minimize the sketched one. Name their original-data residuals r*=Ax*−b and r̂=Ax̂−b. Apply the lower embedding inequality to r̂, use the optimality of x̂ in the sketch, and then the upper inequality for r*. These are three different justifications, in that order:</Prose>
    <MathBlock>{String.raw`\begin{aligned}(1-\varepsilon)\|\widehat r\|_2^2&\le\|S\widehat r\|_2^2,\\ \|S\widehat r\|_2^2&\le\|Sr_*\|_2^2,\\ \|Sr_*\|_2^2&\le(1+\varepsilon)\|r_*\|_2^2.\end{aligned}`}</MathBlock>
    <Prose>Taking square roots gives an original-residual factor √((1+ε)/(1−ε)). This is a deterministic consequence <em>if the embedding condition holds</em>. The random-map theorem supplies a probability that the condition holds at a chosen sketch size; a seed by itself supplies neither ε nor a certificate. Residual quality also differs from coefficient accuracy: an ill-conditioned A can permit noticeably different coefficients with similar predictions.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.embeddingCheck}><Prose>Q spans the three-dimensional residual space. The squared singular values of SQ bound every squared stretch in it, so the program can evaluate ε exactly for this small fixture. Its actual residual ratio is about 1.022, below the conservative bound 1.763. Forming Q and evaluating this check costs work; it is an explanatory oracle, not a free validation step for massive data.</Prose></RunnableExample>
    <Prose>A Gaussian S has independent entries with variance 1/s. For a fixed r, expanding the squares gives E‖Sr‖²=‖r‖²: each squared term contributes its variance, and independent zero-mean cross terms cancel. Unbiased squared length is useful intuition, but simultaneous control of a whole subspace requires concentration. Johnson–Lindenstrauss dimension reduction instead controls the distances between a specified finite collection of points. Both use random maps; their sets of required directions and guarantees differ. A map from m dimensions to fewer dimensions has a null space, so it cannot preserve every direction in all of ℝᵐ.</Prose>
    <H3>Why a rare row can matter</H3>
    <Prose>If Q has orthonormal columns spanning A's column space, the <strong>leverage score</strong> of row i is the squared length of row i of Q. These nonnegative scores sum to rank(A), because the sum of all squared Q entries is its column count. They measure geometric influence on that subspace, not how unusual or erroneous the response b is.</Prose>
    <Prose>For an intercept and one varying feature, orthonormal columns can be chosen as the constant vector 1/√m and the centered feature divided by its length. Thus row i has score 1/m+(xᵢ−x̄)²/Σ(xⱼ−x̄)². The distant input x=20 in the investigation has large leverage. Omitting such a row can lose a direction's contribution even when only a small fraction of observations are missing.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.leverageSampling}><Prose>Sampling row i with probability pᵢ and dividing it and its response by √(s pᵢ) compensates for unequal sampling frequency. Each of s independent draws contributes expected Gram matrix AᵀA/s, so their sum is unbiased. The average Gram matrix being correct does not make the inverse, fitted coefficients or every realized sketch unbiased or accurate. The program computes exact leverage using QR to explain the distribution; that preprocessing may itself be costly.</Prose></RunnableExample>
    <Checkpoint prompt="A sketch preserves every vector in span(A) perfectly but sends b to zero. Is that enough for the residual proof above?">
      <Prose>No. If b has a component outside span(A), the sketch can distort that component and its interactions with the fitted part. The proof requires all possible residuals, which are contained in span([A,b]). Merely preserving the feature columns, or averaging to the right Gram matrix over many draws, does not establish the stated residual guarantee for this sketch.</Prose>
    </Checkpoint>
    <details className="lesson-deeper"><summary>Sketching can also help a high-accuracy solver</summary>
      <Prose>Instead of accepting the sketched solution, use a well-conditioned factorization of SA to transform the original least-squares problem before iterating. For example, if SA=QR with nonsingular R, substitute x=R⁻¹y and solve with the original operator AR⁻¹. A good embedding can make the transformed system better conditioned. Implement this through triangular solves rather than explicitly forming R⁻¹. The iterations continue to use original-data residuals, so this is preconditioning, not replacing the original objective. Setup, passes and stopping accuracy must all be counted.</Prose>
    </details>

    <H2>6. Estimate a trace through matrix products</H2>
    <Prose>Sometimes you do not need a low-rank matrix at all. The <strong>trace</strong> of a square matrix is the sum of its diagonal entries. If those entries are available, simply add them. But a large operator may be available only through a function that computes Az. Could a few products reveal its trace without asking for every column?</Prose>
    <Prose>Choose each entry of z independently as +1 or −1 with equal probability; these are <strong>Rademacher signs</strong>. Expand zᵀAz. Every diagonal term is aᵢᵢzᵢ²=aᵢᵢ. An off-diagonal term contains zᵢzⱼ, whose expectation is zero when i≠j because the signs are independent and balanced. Therefore E[zᵀAz]=trace(A). Average several independently drawn probe values to estimate that trace.</Prose>
    <MathBlock>{String.raw`\begin{aligned}z^\top A z&=\sum_i a_{ii}+\sum_{i\ne j}a_{ij}z_i z_j,\\ \widehat t_s&=\frac1s\sum_{j=1}^s z_j^\top A z_j,\\ \mathbb E\widehat t_s&=\operatorname{tr}(A).\end{aligned}`}</MathBlock>
    <Prose>In the first line, zᵢ is one entry of one vector. In the averaging line, zⱼ labels the entire j-th independently drawn probe vector. One matrix product and one dot product produce each scalar observation.</Prose>
    <TraceEstimatorLab />
    <Prose>For [[4,1],[1,2]], a probe equals 6+2z₁z₂: either 4 or 8, each with probability one half. Its expectation is 6 and variance is 4. Variance is the expected squared difference from the mean. An average of s independent such values has variance 4/s, because the cross deviations have zero expectation. The typical scale of random fluctuation shrinks like 1/√s, but an additional sample may move a particular running average farther from the answer.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.traceEstimation}><Prose>All four sign vectors establish the exact finite expectation for these two-dimensional examples. A diagonal matrix has no random cross terms, so every sign probe is exact. The indefinite example has zero trace and variable samples: unbiasedness alone does not provide a useful relative-error statement when the target is zero.</Prose></RunnableExample>
    <Prose>The unbiased identity holds for any real square matrix with these signs. Stronger relative-error theorems often add positive-semidefinite assumptions and a sample count tied to accuracy and failure probability. Do not transfer such a theorem to an arbitrary signed trace. For a real symmetric matrix, single-probe variance is 4Σᵢ&lt;ⱼ aᵢⱼ²; in this lesson's 2×2 case only one off-diagonal pair contributes.</Prose>
    <Checkpoint prompt="For A=[[3,2],[2,5]], list the possible sign-probe values, their expectation and variance. What is the variance of the average of four independent probes?">
      <Prose>The values are 8±4, so 4 and 12 with equal probability. Their expectation is 8, variance 16, and four independent probes have mean variance 16/4=4. This does not mean every four-probe average is within two of 8; a variance is not a deterministic error limit.</Prose>
    </Checkpoint>

    <H2>7. Validate and choose a useful application</H2>
    <Prose>A random approximation must be judged against its purpose. Reconstruction error, prediction error, a trace estimate and preservation of a critical small direction are different outcomes. Record the matrix preparation, dtype, seed, library version, rank, oversampling, iterations and error metric. A seed supports repeatability within an implementation; it does not guarantee identical random streams across libraries or bitwise identity across hardware.</Prose>
    <H3>Fresh probes can reveal a hidden residual</H3>
    <Prose>Let R=A−Â be the residual operator. For an independent random sign vector z, the same square expansion gives E‖Rz‖²=‖R‖F². This estimates the <em>squared Frobenius norm</em>; taking a square root afterward is not generally an unbiased estimator of the norm. You can compute Rz as Az−Âz without materializing all of R.</Prose>
    <RunnableExample example={randomizedLinearAlgebraExamples.freshResidualProbes}><Prose>The one training probe has exactly zero residual because the approximation was constructed to capture its output. Yet the matrix's squared reconstruction error is 8.6. Averaging over all independent sign possibilities gives 8.6. Reusing the training probes for validation would reward precisely the directions used to fit the approximation.</Prose></RunnableExample>
    <Prose>A few fresh probes are an estimate, not a universal upper bound. An adaptive stopping rule needs a justified probability bound and must account for repeated decisions; repeatedly trying new ranks until a noisy test passes can change the failure rate. For small reference matrices, compare with exact SVD and independent residual calculations. For a real application, also evaluate the downstream objective and cases where small singular directions matter.</Prose>
    <LessonTable caption="Connect the calculation to an actual decision" headers={['Task', 'How the mechanism is used', 'What must be checked']} rows={[['PCA for many observations', 'Apply randomized SVD to feature-centered data; right singular vectors supply feature directions.', 'Centering, feature scales, variance denominator and retained-error budget; randomness does not replace preprocessing.'], ['Snapshots from a simulation', 'Columns share a physical coordinate system; a few left directions approximate their spatial patterns.', 'A norm weighted for physical units may differ from raw entry error. A low-energy direction can still affect later dynamics.'], ['Repeated linear fits', 'A row sketch gives a small approximate fit or a preconditioner for an original-data solve.', 'Original residual, rank, conditioning, setup amortization and the cost of reading data.'], ['Effective complexity of a smoother', 'If fitted values are Hy, trace(H) aggregates the diagonal sensitivity. Probe H without constructing every column.', 'The smoothing operator must be fixed for the stated calculation; data-dependent nonlinear tuning changes the interpretation.']]} />
    <Prose>The smoother example gives a less obvious use of trace. For a fixed linear rule ŷ=Hy, changing input yᵢ by a small amount changes its corresponding fitted output by Hᵢᵢ times that amount. Summing these diagonal sensitivities gives trace(H). Random sign products estimate that aggregate through H's action. For ordinary full-rank least-squares projection, H=QQᵀ and trace(H)=d exactly; trace estimation is useful when the operator is more complicated and the diagonal is costly to obtain.</Prose>
    <details className="lesson-deeper"><summary>Further branches and production library choices</summary>
      <Prose>Nyström methods approximate a positive-semidefinite matrix using selected columns and their intersection, preserving useful symmetry/positivity structure. Interpolative and CUR decompositions favor actual rows or columns when their identities matter more than arbitrary singular combinations. Random feature methods construct an approximate kernel through sampled features. These are related compression tools with different access models and guarantees; the SVD helper above is not an implementation of all three.</Prose>
      <Prose>The scikit-learn <Code>randomized_svd</Code> API exposes target components, oversampling, iteration count, normalization and a random state. Its current documentation includes automatic choices that differ from this deliberately explicit teaching helper. Check the installed version's defaults and select parameters against the actual error requirement. Treat returned factors as an approximation, and preserve them in factored form when possible. The examples here execute NumPy, not scikit-learn, and make no cross-library timing claim.</Prose>
    </details>

    <H2>8. Practise with new failures and new data</H2>
    <H3>Exercise A · a three-factor signal with noise</H3>
    <Prose>Construct an 80×30 matrix from three latent factors plus small independent noise. Predict what rank three can and cannot retain. Compare p=0 with p=5, then add one subspace iteration. Evaluate the actual relative error and the exact rank-three floor. Explain why one poor low-oversampling draw is possible even though the signal has only three latent factors.</Prose>
    <details className="lesson-deeper"><summary>Hint: compare subspaces, not rounded vector signs</summary><Prose>Use a fixed data seed and a separate probe seed. The noise generally gives the observed matrix rank above three. A poorly conditioned three-column probe can mix weak noise into the estimated signal space; extra probes or iteration can reduce this error without eliminating the noise tail.</Prose></details>
    <Checkpoint prompt="Complete the calculation before opening one reproducible solution.">
      <RunnableExample example={randomizedLinearAlgebraExamples.latentStructurePractice} />
      <Prose>Here p=0 gives about 0.507 relative error, p=5 gives 0.0368, and one iteration reaches the displayed floor of 0.0290. The example demonstrates the need to distinguish an underlying signal rank from the noisy matrix's rank and from one sketch's quality. It does not establish that every p=0 draw is bad or that rank three is scientifically correct for arbitrary observations.</Prose>
    </Checkpoint>
    <H3>Exercise B · repair a false validation certificate</H3>
    <Prose>A=diag(7,2,1). A rank-one approximation retains only its first column direction. A developer validates it using the input vector [1,0,0] and observes zero residual. Calculate the actual Frobenius and spectral residual norms. Give a probe that exposes the omitted information, and explain why a fresh random probe remains an estimate rather than an automatic bound.</Prose>
    <details className="lesson-deeper"><summary>Hint: write the residual matrix</summary><Prose>R=diag(0,2,1). Use its entries for one norm and its largest singular value for the other. Compare R[1,0,0] with R[0,1,0].</Prose></details>
    <Checkpoint prompt="State both error metrics and the failed assumption."><Prose>‖R‖F=√5 and ‖R‖₂=2, while R[1,0,0]=0. Input [0,1,0] produces [0,2,0]. Testing only a retained direction cannot certify the operator. Independent random probes can sample omitted directions, but a finite observation needs an appropriate concentration/error guarantee to become a certificate.</Prose></Checkpoint>
    <H3>Exercise C · decide whether the reduced problem is trustworthy</H3>
    <Prose>A=I₂, b=[1,1], and S=[1,0]. Describe every minimizer of the sketched problem. Can one have arbitrarily large original residual? Identify a residual direction the sketch destroys. Then explain why using s&lt;d rows cannot embed a full d-dimensional feature space.</Prose>
    <details className="lesson-deeper"><summary>Hint: the second coefficient vanished</summary><Prose>The reduced objective is |x₁−1|. It says nothing about x₂. Examine vectors on the vertical axis and the rank of a map to a lower-dimensional space.</Prose></details>
    <Checkpoint prompt="Give a family of counterexamples, not just one bad number."><Prose>Every [1,t] minimizes the sketch with value zero. The original residual is [0,t−1] and has norm |t−1|, unbounded as t changes. S annihilates [0,1]. A linear map from a d-dimensional space into fewer than d dimensions has a nontrivial kernel, contradicting any positive lower length-preservation bound on that whole space.</Prose></Checkpoint>
    <H3>Exercise D · distinguish unbiasedness from a per-run promise</H3>
    <Prose>For [[3,2],[2,5]], three independent sign probes all produce 12. Is the estimator biased because its realized mean is 12 instead of trace 8? What is the probability of that particular value sequence? In a different run, can another sample of 12 worsen an earlier estimate that had equaled 8?</Prose>
    <details className="lesson-deeper"><summary>Hint: average over possible experiments</summary><Prose>Each value is 4 or 12 with probability one half. Bias compares the expectation of the estimator with the target, not one observed estimate. Consider an earlier two-sample sequence [4,12].</Prose></details>
    <Checkpoint prompt="Explain all three distinctions."><Prose>The estimator remains unbiased: its expectation is 8. Three specified high values occur with probability (1/2)³=1/8. After [4,12], the mean equals 8; adding 12 moves it to 28/3, farther from the answer. Independence and decreasing variance do not impose monotone error along an individual run.</Prose></Checkpoint>
    <H3>Ready to continue</H3>
    <Prose>You should be able to trace shapes through a random range finder, distinguish k from ℓ, account for two reconstruction losses, count data passes, reject a misleading sketch residual, derive the sign-trace expectation, and choose fresh validation suited to the requested quantity. The next module topic is <a href="/learn/topic/multivariate-calculus-gradients">Multivariate Calculus & Gradients</a>: local changes and directions of improvement. Randomized methods will reappear later when those derivatives or optimization subproblems become too large to handle directly.</Prose>

    <Sources alternatives={<>
      <p><a href="https://www.youtube.com/watch?v=fJ2EyvR85ro" target="_blank" rel="noopener noreferrer">Steve Brunton · Randomized Singular Value Decomposition</a> provides a visual video introduction. The authors' <a href="https://databookuw.com/page-2/page-4/" target="_blank" rel="noopener noreferrer">Chapter 1 resource page, section 1.8</a> also links power iteration, oversampling and Python demonstrations. The official resource index and video title were checked; full playback was not reviewed. Keep this lesson's explicit orthogonalization and pass-count contracts when comparing simplified presentations.</p>
      <p><a href="https://tropp.caltech.edu/slides/Tro14-Finding-Structure-ICML-slides.pdf" target="_blank" rel="noopener noreferrer">Joel Tropp · Finding Structure with Randomness</a> is a more mathematical slide route. Start at the two-stage low-rank discussion around slides 36–42; earlier sections concern different sampling estimators. Selected mathematical slides were read, not the entire associated talk.</p>
    </>}>
      <li><a href="https://arxiv.org/pdf/0909.4061" target="_blank" rel="noopener noreferrer">Halko, Martinsson & Tropp · Finding Structure with Randomness</a> — algorithms 4.3–4.4, the floating-point warning, the compressed SVD stage and Theorem 10.5's Gaussian range-error contract. Different sketch families in the paper have different costs.</li>
      <li><a href="https://arxiv.org/pdf/2002.01387" target="_blank" rel="noopener noreferrer">Martinsson & Tropp · Randomized Numerical Linear Algebra</a> — a broader advanced reference for trace estimation, embeddings, row sampling, least squares, range finding and later single-view/kernel branches. Relevant sections were reviewed; the entire monograph is not a prerequisite.</li>
      <li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.utils.extmath.randomized_svd.html" target="_blank" rel="noopener noreferrer">scikit-learn · randomized_svd API</a> — parameter and normalization options checked on 10 September 2026. Documentation defaults are version-specific; the complete programs above use their stated NumPy implementation.</li>
    </Sources>
  </div>
};
