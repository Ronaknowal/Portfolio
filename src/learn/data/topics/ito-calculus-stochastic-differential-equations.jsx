import { Callout, Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { NoiseScalingFigure, CurvatureCorrectionFigure, ConventionConversionFigure, ProbabilityDualityFigure, AdaptedIntegralLab, GrowthLawLab, OUResponseLab, CoupledSdeSolverLab, SdeErrorLab } from '../../components/lesson-labs/ItoSdeLabs.jsx';
import { itoSdeExamples as examples } from '../ito-sde-examples.js';
import MechanismProgram from '../../components/lesson-labs/MechanismProgram.jsx';
import { mechanismProgram } from '../ito-calculus-mechanism-program.js';
function Example({
  name
}) {
  const example = examples[name];
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example}><Prose>{example.interpretation}</Prose></RunnableExample></>;
}
function Practice({
  prompt,
  hint,
  children
}) {
  return <div className="lesson-check"><p><strong>Try it independently.</strong> {prompt}</p>
    <details><summary>Hint</summary><p>{hint}</p></details>
    <details><summary>Show explained solution</summary>{children}</details>
  </div>;
}
export default {
  title: 'Itô Calculus & Stochastic Differential Equations',
  readTime: '~100 min read + 1–2 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot ito-sde-lesson">
    <LessonIntro prerequisites="You should recognize a derivative, an integral, a normal distribution and conditional expectation. We rebuild the Brownian increment and information rules locally. The previous stochastic-processes lesson supplies the deeper path foundations; the dynamical-systems lesson supplies deterministic stability. Python examples use the standard library, with NumPy for the explicitly labelled Gaussian quadrature and matrix calculations." sections={[['1-read-the-noise-model', 'Units, increments and information'], ['2-see-why-squares-survive', 'Quadratic variation and adapted sums'], ['3-transform-a-random-state', 'The stochastic chain and product rules'], ['4-solve-multiplicative-growth', 'Exact growth, means and typical paths'], ['5-balance-restoring-drift-and-noise', 'OU laws and weighted noise'], ['6-choose-the-convention-and-solution', 'Itô, Stratonovich and existence'], ['7-build-and-couple-numerical-solvers', 'Euler–Maruyama and Milstein'], ['8-measure-the-right-error', 'Strong error, weak bias and stability'], ['9-move-between-paths-and-probability', 'Generators, densities and expectations'], ['10-apply-test-and-practise', 'Model diagnosis and independent practice']]}>
      Learn how to describe a continuously changing state driven by fresh random shocks, transform that state without losing a noise correction, and tell a trustworthy simulation from a plausible-looking curve.
    </LessonIntro>

    <H2>1. Read the noise model</H2>
    <Prose>A temperature is pulled toward its surroundings but continually disturbed by microscopic motion. A voltage relaxes toward rest but receives unresolved input. An ordinary differential equation describes the systematic change; a stochastic differential equation adds a model for the unresolved fluctuations. The result describes a distribution of possible histories, even when the initial state is known exactly.</Prose>
    <MathBlock>{String.raw`dX_t=a(t,X_t)\,dt+b(t,X_t)\,dW_t`}</MathBlock>
    <Prose>Read this first as a rule over a short interval of length <Code>h</Code>: approximately add <Code>a h</Code> plus <Code>b √h Z</Code>, where <Code>Z</Code> has a standard normal distribution. The <strong>drift</strong> a is the conditional mean rate. The <strong>diffusion coefficient</strong> b determines the random increment's scale; its standard deviation is |b|√h and its variance rate is b². Brownian motion W has continuous paths and independent increments, with an increment over h distributed as Normal(0,h). Those increments are also independent of the information already available when the interval begins.</Prose>
    <NoiseScalingFigure />
    <Prose>If X is measured in volts and time in seconds, a has units volts/second and b has units volts/√second. Replacing seconds with milliseconds divides the numerical drift by 1,000 and the diffusion coefficient by √1,000. Multiplying noise by h instead of √h would make the accumulated noise variance disappear as the grid is refined. Splitting one second into n steps gives n × b²/n=b², as required.</Prose>
    <Example name="units" />
    <Prose>The differential notation is shorthand for an integral equation, not division by an ordinary derivative of W. A Brownian path is almost surely nowhere differentiable. For an Itô SDE the precise statement is</Prose>
    <MathBlock>{String.raw`\begin{aligned}X_t&=X_0+\int_0^t a(s,X_s)\,ds\\&\quad+\int_0^t b(s,X_s)\,dW_s.\end{aligned}`}</MathBlock>
    <Prose>The first integral accumulates ordinary time. The second accumulates random increments using coefficients chosen without looking into their future. A <strong>filtration</strong>, written Fₜ, is the growing collection of information available by time t. An <strong>adapted</strong> state is measurable from Fₜ at time t. Here W is Brownian relative to that same filtration; expanding the information to include future W values could destroy the independence we need.</Prose>
    <Checkpoint prompt="A simulator draws the next shock, then increases the coefficient whenever that shock is positive. Is this the same Itô rule with a coefficient chosen from the current state?">
      <Prose>No. The chosen coefficient now uses the very increment it multiplies. Its conditional mean contribution need not be zero. The information rule is part of the mathematical model, not an implementation detail that can be reordered freely.</Prose>
    </Checkpoint>

    <H2>2. See why squares survive</H2>
    <Prose>Take observation times 0=t₀&lt;t₁&lt;…&lt;tₙ=T. Let ΔWᵢ=W(tᵢ₊₁)−W(tᵢ) and hᵢ=tᵢ₊₁−tᵢ. The sum Q=Σ(ΔWᵢ)² is the <strong>quadratic variation measured on this grid</strong>. For a deterministic partition, normal moments and independent increments give</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E Q&=T,\\\operatorname{Var}(Q)&=2\sum_i h_i^2\\&\le 2T\max_i h_i.\end{aligned}`}</MathBlock>
    <Prose>As the largest interval shrinks, this variance tends to zero: Q converges to T in mean square and hence in probability. For dyadic partitions, the variance bounds are summable; Chebyshev's inequality and the Borel–Cantelli argument additionally give almost-sure convergence along that sequence. These are statements about random Brownian increments observed along deterministic partition refinements, not a claim that every finite path has Q=T or that unrelated grid samples form one fixed iid sequence.</Prose>
    <Prose>Now expand an exact finite identity: (w+Δw)²−w²=2wΔw+(Δw)². Summing makes the intermediate squared values cancel. With W₀=0,</Prose>
    <MathBlock>{String.raw`\begin{aligned}L&=\sum_i W_{t_i}\Delta W_i=\tfrac12(W_T^2-Q),\\R&=\sum_i W_{t_{i+1}}\Delta W_i=\tfrac12(W_T^2+Q),\\S&=\sum_i \tfrac{W_{t_i}+W_{t_{i+1}}}{2}\Delta W_i=\tfrac12W_T^2.\end{aligned}`}</MathBlock>
    <Prose>All three sums use identical observations. Their coefficient choices differ. The discrepancy R−L=Q does not disappear for Brownian motion. The left-point sum therefore converges to ½(W²ₜ−T), not the ordinary-calculus answer ½W²ₜ. The symmetric endpoint sum has the latter answer and leads to the Stratonovich convention discussed later.</Prose>
    <AdaptedIntegralLab />
    <Example name="integrals" />
    <H3>Build the integral from coefficients that cannot peek</H3>
    <Prose>For a simple integrand H that is constant at a value Hᵢ on each interval, with Hᵢ measurable from F(tᵢ), define I=ΣHᵢΔWᵢ. Suppose E[ΣHᵢ²hᵢ] is finite. Conditional on F(tᵢ), the next increment has mean zero and variance hᵢ. Distinct summands have zero cross expectation: when the later increment is conditioned on its past, everything multiplying it is already known. Consequently,</Prose>
    <MathBlock>{String.raw`\mathbb E I=0,\qquad \mathbb E I^2=\mathbb E\sum_i H_i^2h_i.`}</MathBlock>
    <Prose>This is the <strong>Itô isometry</strong>: the integral's squared L² size equals the expected time-integral of the coefficient's square. It lets us define general integrals by taking limits of simple ones. A standard sufficient contract is a progressively measurable integrand H with E∫₀ᵀH²ₛds&lt;∞; predictable square-integrable integrands are another standard formulation. Adapted continuous processes are progressively measurable. Adaptedness alone, without suitable joint measurability and integrability, is not the whole theorem.</Prose>
    <Prose>Under the square-integrable contract, the integral is a mean-zero square-integrable martingale: its conditional future expectation equals its present value. If only ∫H²ds is locally finite, the construction generally yields a <em>local</em> martingale. Stopping makes it a martingale on controlled regions, but its unstopped expectation cannot automatically be treated as constant. This matters later when changing probabilities by an exponential weight.</Prose>
    <Prose>The quadrature and matrix programs require NumPy; install it with <Code>python -m pip install numpy</Code> if needed. All thirteen programs were executed with Python 3.12.14; the NumPy examples were checked with NumPy 2.3.5. The other examples use Python's standard library.</Prose>
    <Example name="isometry" />

    <H2>3. Transform a random state</H2>
    <Prose>Suppose we know X but care about its square, logarithm, energy or another transformed quantity f(t,X). Ordinary calculus keeps the first-order change. Brownian increments have size √h, so their square has size h and must also be kept. Curvature converts zero-mean noise into systematic change.</Prose>
    <CurvatureCorrectionFigure />
    <Prose>For f with one continuous time derivative and two continuous state derivatives, Itô's formula for the scalar SDE is</Prose>
    <MathBlock>{String.raw`\begin{aligned}df(t,X_t)&=(f_t+a f_x)dt\\&\quad+\tfrac12 b^2 f_{xx}\,dt\\&\quad+b f_x\,dW_t.\end{aligned}`}</MathBlock>
    <Prose>All derivatives and coefficients are evaluated at (t,Xₜ). The accumulated second-order term produces ½b²fₓₓdt. Products involving dt² or dt·dW vanish in the appropriate limit. Writing “(dW)²=dt” summarizes quadratic-variation bookkeeping; it is not a pathwise assertion that a single realized squared increment equals its elapsed time. A rigorous argument controls Taylor remainders, with localization when derivatives or coefficients are unbounded.</Prose>
    <LessonTable caption="Transformations of a scalar Itô state" headers={['Question', 'Derivative contribution', 'Result']} rows={[['Square: f(x)=x²', 'f′=2x, f″=2', 'd(X²)=(2Xa+b²)dt+2Xb dW'], ['Linear: f(x)=cx+d', 'f″=0', 'No curvature correction'], ['Logarithm on x>0', 'f′=1/x, f″=−1/x²', 'd log X=(a/X−b²/(2X²))dt+(b/X)dW'], ['Absolute value at zero', 'f is not C²', 'The classical formula does not apply there; a local-time term is needed']]} />
    <Prose>For f(W)=W² the rule gives d(W²)=2W dW+dt, precisely the sum identity we just derived. For |W|, applying the square-rule logic while ignoring the kink would incorrectly predict a mean-zero process. Brownian <em>local time</em> accounts for repeated contact with zero; that extension is a deeper theorem, not a reason to differentiate through a nonsmooth point here.</Prose>
    <H3>A transformation can depend on the clock</H3>
    <Prose>For f(t,x)=x³−3tx and X=W, the derivatives are fₜ=−3x, fₓ=3x²−3t and fₓₓ=6x. The time derivative cancels the curvature drift. Thus the centered cubic is a martingale on each finite horizon:</Prose>
    <MathBlock>{String.raw`d(W_t^3-3tW_t)=3(W_t^2-t)\,dW_t.`}</MathBlock>
    <Prose>The claim needs more than a visibly zero drift. Here E[(Wₜ²−t)²]=2t², so the integral's expected coefficient energy is 9∫₀ᵀ2t²dt=6T³&lt;∞. Isometry both justifies the martingale contract and gives its second moment. If we had transformed only W³, the uncancelled drift would be 3Wdt. Time-dependent transformations are useful when constructing centered observables or checking model moments.</Prose>
    <Example name="time_transform" />
    <H3>Products and several noise sources</H3>
    <Prose>If dX=a dt+b dW and dY=c dt+e dW share the same scalar driver, their product acquires be dt in addition to X dY+Y dX. Independent drivers have zero cross variation. In several dimensions write dX=a dt+B dW, where W has independent standard Brownian coordinates. The covariance rate of the state increments is C=BBᵀ.</Prose>
    <MathBlock>{String.raw`\begin{aligned}df&=(f_t+\nabla f^{\mathsf T}a)\,dt\\&\quad+\tfrac12\operatorname{tr}(BB^{\mathsf T}\nabla^2 f)\,dt\\&\quad+\nabla f^{\mathsf T}B\,dW.\end{aligned}`}</MathBlock>
    <Prose>For f(x,y)=xy, both mixed Hessian entries are one, so ½(C₁₂+C₂₁)=C₁₂. Two coordinate names do not imply independent randomness. For X=W₁ and Y=0.6W₁+0.8W₂, both coordinates have variance rate one but covariance rate 0.6.</Prose>
    <Example name="covariance" />

    <H2>4. Solve multiplicative growth</H2>
    <Prose>When both systematic growth and shocks scale with the current state, use geometric Brownian motion: dX=μX dt+σX dW, with deterministic X₀&gt;0 and volatility σ≥0. A negative written scalar noise coefficient has the same one-time variance but would require |σ| in the quantile scale below. We can verify a solution by applying Itô's formula to an exponential of W. The second derivative of that exponential adds σ²/2 to its drift, so its written exponent must subtract the same amount.</Prose>
    <MathBlock>{String.raw`X_t=X_0\exp\!\left((\mu-\tfrac12\sigma^2)t+\sigma W_t\right).`}</MathBlock>
    <Prose>This construction proves positivity at every finite time. Having established positivity, applying the log rule gives d log X=(μ−σ²/2)dt+σ dW and recovers the same answer. A numerical method that crosses zero has violated a property of this exact model; it has not discovered a negative state that the exact model permits.</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E X_t&=X_0e^{\mu t},\\\operatorname{median}(X_t)&=X_0e^{(\mu-\sigma^2/2)t},\\\operatorname{Var}(X_t)&=X_0^2e^{2\mu t}(e^{\sigma^2t}-1),\end{aligned}`}</MathBlock>
    <MathBlock>{String.raw`q_p(t)=X_0e^{(\mu-\sigma^2/2)t+\sigma\sqrt t\,\Phi^{-1}(p)}.`}</MathBlock>
    <Prose>Here Φ⁻¹ is the standard normal quantile. These follow from log X being normal and E[eᶜᶻ]=eᶜ²ᐟ² for a standard normal Z. At σ=0 or t=0 the distribution collapses to an atom; density formulas with division by σ√t must be replaced by that deterministic limit. A 5th-to-95th percentile interval covers 90% of the law at one chosen time, not 90% of entire paths simultaneously.</Prose>
    <GrowthLawLab />
    <Example name="growth" />
    <Prose><strong>An instructive surprise:</strong> take μ=0.2 and σ=1. The mean grows at rate 0.2 but the log drift is −0.3. Brownian motion satisfies Wₜ/t→0 almost surely as t→∞, so log(Xₜ/X₀)/t→−0.3 and Xₜ→0 almost surely. There is no contradiction: ever rarer large outcomes support the growing expectation. Almost-sure convergence alone does not justify exchanging limit and expectation; the family is not uniformly integrable. This is useful whenever an average performance score and a typical multiplicative trajectory tell different stories.</Prose>
    <Prose>GBM is a tractable model for multiplicative uncertainty, historically prominent in finance. It does not by itself model jumps, time-varying volatility or feedback between participants. The derivation gives a benchmark for simulation and inference, not an assertion that every growing quantity follows this law.</Prose>

    <H2>5. Balance restoring drift and noise</H2>
    <Prose>Growth proportional to the state has no restoring target. For a fluctuating state pulled back toward m, the Ornstein–Uhlenbeck model is dX=θ(m−X)dt+η dW, with θ&gt;0. A displacement decays on the time scale 1/θ, while fresh noise continually replenishes its uncertainty.</Prose>
    <Prose>Multiply X−m by eᶿᵗ. This deterministic factor has zero quadratic covariation with X; the product rule cancels the drift. Integrate and then divide by eᶿᵗ:</Prose>
    <MathBlock>{String.raw`\begin{aligned}X_t&=m+(X_0-m)e^{-\theta t}\\&\quad+\eta\int_0^t e^{-\theta(t-s)}\,dW_s.\end{aligned}`}</MathBlock>
    <Prose>The last term is a deterministic weighted sum of Gaussian increments, hence Gaussian. Isometry gives its variance. For a Gaussian initial state of mean m₀ and variance v₀ independent of future Brownian increments,</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E X_t&=m+(m_0-m)e^{-\theta t},\\v(t)&=v_0e^{-2\theta t}+\frac{\eta^2}{2\theta}(1-e^{-2\theta t}),\\v'(t)&=\eta^2-2\theta v(t).\end{aligned}`}</MathBlock>
    <Prose>A fixed initial state is the special case v₀=0. A non-Gaussian initial state still has these mean/variance formulas when its second moment exists, but its finite-time law is generally not Gaussian: it is a scaled initial law convolved with Gaussian noise. With initial law Normal(m,η²/(2θ)), independent of the future driver, the process is stationary from time zero. A fixed start merely approaches that law. If θ=0, the added variance is η²t, obtained by taking the continuous limit; η&gt;0 then gives no stationary probability law on the whole real line. With η=0 and θ=0 the initial state stays constant.</Prose>
    <OUResponseLab />
    <H3>Exact transitions need careful coupling</H3>
    <Prose>Over h, the exact OU transition uses J=∫ₜᵗ⁺ʰe⁻ᶿ⁽ᵗ⁺ʰ⁻ˢ⁾dWₛ. Its variance is (1−e⁻²ᶿʰ)/(2θ). Drawing a normal with that variance gives a correct transition law. However, setting J equal to its standard deviation times the same Z used for ΔW=√h Z usually gives the wrong joint distribution: the weighted and unweighted integrals are not perfectly correlated when θ&gt;0.</Prose>
    <MathBlock>{String.raw`\begin{aligned}c&=\operatorname{Cov}(\Delta W,J)=\frac{1-e^{-\theta h}}{\theta},\\v_J&=\frac{1-e^{-2\theta h}}{2\theta},\\J&=\frac c h\Delta W+\sqrt{v_J-c^2/h}\,Z_2,\\&\hspace{2em}Z_2\perp\Delta W.\end{aligned}`}</MathBlock>
    <Prose>This Gaussian conditional construction provides the correct coupling to the Brownian increment. At θ=0 it becomes J=ΔW and the residual variance is zero. For small θh the residual is tiny, approximately θ²h³/12. Subtracting nearly equal floating-point terms can lose it; the browser model uses a positive series and the Python example uses high-precision arithmetic over an explicit bounded range.</Prose>
    <Example name="ou" />

    <H2>6. Choose the convention and solution</H2>
    <Prose>The left-point integral is Itô. For continuous semimartingales, the Stratonovich integral can be defined by symmetric endpoint sums and equals the Itô integral plus half the integrand's quadratic covariation with W. The symmetric choice is an integration convention, not permission for an Itô coefficient to peek into future noise.</Prose>
    <MathBlock>{String.raw`\begin{aligned}dX&=a_S(t,X)\,dt+b(t,X)\circ dW\\&\Updownarrow\\dX&=\left(a_S+\tfrac12 b\,\partial_xb\right)dt+b\,dW.\end{aligned}`}</MathBlock>
    <Prose>This scalar conversion assumes enough smoothness in b and a setting where the two integrals are defined. Stratonovich obeys the ordinary chain rule because its symmetric correction already accounts for curvature. It arises in some limits of smoothly approximated noise. Which limit is justified depends on the physical approximation and time scales; “physical means Stratonovich” is too broad. With additive noise b independent of x, the conversion term vanishes.</Prose>
    <ConventionConversionFigure />
    <Example name="conventions" />
    <Prose>For a vector equation driven by several independent Brownian coordinates, the i-th drift correction is ½Σⱼ,ₖBⱼₖ∂ⱼBᵢₖ. A scalar formula applied separately while discarding cross-coordinate dependence can change the model. Record the convention and full noise matrix before transferring equations into a library.</Prose>
    <H3>When does an equation define a solution?</H3>
    <Prose>A <strong>strong solution</strong> is constructed on a prescribed probability space using the given Brownian driver and initial state, without extra future information. <strong>Pathwise uniqueness</strong> says two such solutions with the same driver and initial state agree almost surely. A <strong>weak solution</strong> allows construction of a suitable space, driver and state together. These terms describe existence and uniqueness, not numerical error or the strength of a physical effect.</Prose>
    <Prose>A useful sufficient theorem on each finite horizon assumes measurable coefficients that are globally Lipschitz in x, uniformly over time, with linear growth; a square-integrable initial state measurable at time zero; and Brownian motion relative to the filtration. It yields a unique adapted continuous strong solution with controlled second moments. GBM and OU satisfy this contract. Local Lipschitz conditions instead guarantee solutions only up to possible explosion; additional growth or Lyapunov control is needed to rule explosion out. Even the deterministic equation x′=x² starting at x₀&gt;0 explodes at time 1/x₀.</Prose>
    <Prose>The proof idea connects directly to what we have learned: repeatedly evaluate the two integrals using the previous approximation to the state. Lipschitz bounds control coefficient differences, and the Itô isometry controls their stochastic integrals in mean square. Iterated estimates establish convergence; a Gronwall estimate forces two solutions with the same input noise to agree. Moment bounds from linear growth allow continuation through each finite horizon. This is an outline of the argument, not a proof supplied by a finite simulation.</Prose>
    <Prose>Failure of this sufficient theorem does not prove that no solution exists. Square-root diffusions, boundary constraints and irregular coefficients require their own results. Smooth transformations can also be used only where their derivatives exist and the process remains in their domain. These conditions explain why a solver that returns finite numbers is not an existence proof.</Prose>

    <H2>7. Build and couple numerical solvers</H2>
    <Prose>When an exact solution is unavailable, Euler–Maruyama (EM) freezes the drift and diffusion at the beginning of each step:</Prose>
    <MathBlock>{String.raw`\begin{aligned}Y_{n+1}&=Y_n+a(t_n,Y_n)h\\&\quad+b(t_n,Y_n)\Delta W_n,\\\Delta W_n&=\sqrt h\,Z_n.\end{aligned}`}</MathBlock>
    <Prose>Draw independent Zₙ across disjoint time intervals and independent sample paths. Initialize a random-number generator once per experiment; resetting it inside the step loop would repeat shocks instead of drawing independent increments. Here is the original four-step example, retained exactly.</Prose>
    <Example name="original" />
    <Prose>The displayed endpoint 0.777 and exact mean 1.492 answer different questions. There is also a smaller systematic discrepancy: the EM expectation at four steps is (1+0.4/4)⁴=1.4641, rather than e⁰·⁴≈1.491825. Simulation variability and discretization bias can coexist.</Prose>
    <H3>Keep the noise variation inside the coefficient</H3>
    <Prose>During a step, the stochastic part of X changes b(X). A first-order expansion contributes b′b times ∫ₜᵗ⁺ʰ(Wₛ−Wₜ)dWₛ. Our squared-increment identity evaluates that integral as ½((ΔW)²−h). The scalar Milstein update therefore adds</Prose>
    <MathBlock>{String.raw`\begin{aligned}Y_{n+1}&=Y_n+ah+b\Delta W\\&\quad+\tfrac12bb_x\big((\Delta W)^2-h\big).\end{aligned}`}</MathBlock>
    <Prose>All coefficients here are evaluated at the beginning of the step. For GBM, bbₓ=σ²Y. For additive OU noise bₓ=0, this scalar Milstein update coincides with EM. That does not make EM an exact OU transition. In multiple dimensions, iterated stochastic integrals and noncommuting noise fields can matter; the scalar correction alone is not a generic multivariate Milstein implementation.</Prose>
    <Prose>To compare two resolutions pathwise, generate one fine grid of Brownian increments and <strong>sum</strong> each consecutive group into a coarse increment. Reusing a seed while asking for a different count or order of draws does not guarantee the same Brownian path. Different independent fine paths must still be used when estimating ensemble error.</Prose>
    <CoupledSdeSolverLab />
    <Example name="coupling" />
    <Prose>For GBM the exact terminal value depends only on the summed Brownian increment, so all grouped resolutions share exactly that reference endpoint up to rounding. Their EM/Milstein values differ because products of numerical factors depend on the grid. A single path's error need not decrease at every refinement.</Prose>
    <Callout label="A broken constraint is evidence to investigate">A GBM EM factor is 1+μh+σΔW. It can be negative despite exact positivity. Clamping the state to zero changes the numerical method and its law. For this model, use the exact exponential step; in other models, choose and justify a suitable positivity-preserving method and test its bias.</Callout>

    <section id="ito-calculus-code-route" aria-label="Reusable SDE steps and a common-noise library comparison">
      <H3>Pass the same Brownian increments to both implementations</H3>
      <Prose>The reusable Euler routine accepts a drift vector, a state-by-driver diffusion matrix and supplied increments with shape [steps,drivers]. Each update is state + drift·dt + diffusion·dW. The scalar Milstein routine additionally receives the derivative of its diffusion coefficient and adds ½gg′((dW)²−dt); it does not pretend this scalar formula implements multidimensional Lévy areas. Install NumPy 2.3.5 and sdeint 0.3.0; run <Code>python sde-library-bridge.py</Code>.</Prose>
      <MechanismProgram {...mechanismProgram} title="Complete Euler, scalar Milstein and sdeint common-noise comparison" />
      <Prose>The library call is <Code>sdeint.itoEuler(..., dW=increments)</Code>, on the same uniform grid with the same coefficients and initial state. It matches the hand Euler path entry by entry. The GBM exact path uses those cumulative increments, and the coarse grid sums groups of four fine increments. The second fixture uses a non-diagonal two-state diffusion matrix, checking driver orientation against the exact additive-noise sum.</Prose>
      <Prose>With d state coordinates, r noise drivers and K steps, dense diffusion products cost O(Kdr), plus coefficient evaluation; retaining the path uses O(Kd) storage, and the supplied increments use O(Kr). Time stepping is sequential. This tested fixed-grid Euler comparison does not certify adaptive error control, stiffness handling, positivity, arbitrary superlinear coefficients or every sdeint method. One realized error can favor either grid; strong/weak orders require the assumptions and averages developed next, not a ranking of one random path.</Prose>
      <details><summary>Implementation practice: change the noise dimension</summary><Prose>Replace the additive diffusion matrix by [[1,0],[0,0]] while keeping two supplied drivers. Then set all increments to zero in the GBM comparison.</Prose><details><summary>Solution and checks</summary><Prose>The second additive coordinate stays fixed and the first equals the sum of driver 0. Driver 1 contributes nothing. Zero increments turn Euler into explicit drift stepping; scalar Milstein still includes its −½gg′dt term, so it is not identical to drift-only Euler. Do not remove that term or clamp a negative iterate silently.</Prose></details></details>
      <Prose><a href="https://github.com/mattja/sdeint/blob/master/sdeint/integrate.py" target="_blank" rel="noreferrer">sdeint's integrator source</a> exposes the supplied-increment contract used by the tested 0.3.0 package. Pin the tested version when replaying this comparison.</Prose>
    </section>
    <H2>8. Measure the right error</H2>
    <Prose>“The simulation looks correct” needs a target. A pathwise comparison asks how close approximate and exact states are under a declared coupling. A distributional question asks whether the expectation of a chosen test function is accurate. A finite simulation also has sampling uncertainty.</Prose>
    <LessonTable caption="Three different sources of numerical evidence" headers={['Quantity', 'What it asks', 'What it does not establish']} rows={[['Terminal strong RMS: √E[(Yₙ−X_T)²]', 'How far apart are coupled terminal states?', 'Accuracy of every path, or maximum error over all times'], ['Weak bias: E[g(Yₙ)]−E[g(X_T)]', 'Is one declared expected observable correct?', 'The entire distribution from one g'], ['Monte Carlo standard error', 'How variable is a finite-sample average?', 'Removal of time-discretization bias']]} />
    <Prose>For sufficiently regular globally Lipschitz SDEs, EM has strong order 1/2; with adequate smoothness and moment bounds, weak order 1 for suitable smooth test functions. Scalar Milstein has strong order 1 under stronger derivative regularity and moment assumptions. Time-dependent coefficient regularity also matters. These orders are asymptotic bounds, not universal properties of all SDEs or arbitrary observables. The globally Lipschitz linear examples here satisfy the needed conditions for their smooth polynomial tests, and we can calculate their finite-grid errors directly.</Prose>
    <H3>Derive a finite-grid reference instead of fitting a slope</H3>
    <Prose>For GBM, X₀=1 and n equal steps, write h=T/n, d=μh and s=σ²h. Let A be the one-step numerical multiplier, and B=exp((μ−σ²/2)h+σΔW) its exact counterpart. Independent increments make moments of products equal products of one-step moments. Set r=s for EM and r=s+s²/2 for Milstein. Gaussian moments give</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathbb E Y_n&=(1+d)^n,\\\mathbb E X_T&=e^{\mu T},\\\mathbb E Y_n^2&=((1+d)^2+r)^n,\\\mathbb E X_T^2&=e^{(2\mu+\sigma^2)T},\\\mathbb E[X_TY_n]&=e^{\mu T}(1+d+r)^n.\end{aligned}`}</MathBlock>
    <Prose>For example, E[Z eᶜᶻ]=c eᶜ²ᐟ² and E[(Z²−1)eᶜᶻ]=c²eᶜ²ᐟ² generate the cross moment. Then MSE=E[X²]+E[Y²]−2E[XY]. This is a model calculation, not an empirical error curve. Direct binary64 subtraction can catastrophically cancel when the answer is small; the Python program uses 90-digit arithmetic, and the browser uses a separately checked stable decomposition.</Prose>
    <SdeErrorLab />
    <Example name="errors" />
    <Prose>For μ=0, both numerical means are exact at every resolution: first-moment weak bias is zero. Path error remains positive with nonzero σ, and second-moment bias reveals another mismatch. Different test functions can have different errors. When σ=0 the stochastic problem reduces to an ODE, and the usual noisy EM order need not describe the observed deterministic rate.</Prose>
    <Prose>For paired paths define eᵢ=Yᵢ−Xᵢ. Estimate mean bias by the average of eᵢ and its standard error by the sample standard deviation of eᵢ divided by √M. Estimate MSE by the average of eᵢ², with a standard error computed from those squared errors. The square root of sample MSE estimates RMS; the MSE standard error has squared-state units and cannot simply be attached to RMS. Heavy tails may require far more paths than a visually smooth chart suggests.</Prose>
    <H3>Stability is another question</H3>
    <Prose>For the linear model dX=λXdt+σXdW, exact mean-square decay requires 2λ+σ²&lt;0. EM multiplies its second moment each step by (1+λh)²+σ²h, so mean-square decay requires this multiplier to be below one. A stable continuous model can be unstable on a coarse grid. For λ=−1 and σ=1, the exact condition is −1&lt;0, while EM needs 0&lt;h&lt;1.</Prose>
    <Prose>Almost-sure long-run decay instead depends on λ−σ²/2&lt;0. These conditions differ because rare outcomes affect second moments. Mean-square stability, asymptotic path behavior, finite-horizon accuracy and preservation of positivity are four distinct checks. General superlinear SDEs can make explicit EM behave very badly even when an exact solution exists; a theorem for globally Lipschitz coefficients cannot be transferred silently.</Prose>

    <H2>9. Move between paths and probability</H2>
    <Prose>We can ask what happens to a smooth observable f without simulating every possible path. Apply Itô's formula over a short interval and take conditional expectation. When the stochastic integral is a true mean-zero martingale and the remaining expectations are integrable, the infinitesimal expected change is</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mathcal L_t f(x)&=a(t,x)f'(x)\\&\quad+\tfrac12 b(t,x)^2f''(x).\end{aligned}`}</MathBlock>
    <Prose>This operator is the <strong>generator</strong>. The rigorous starting point can use smooth compactly supported test functions and then extend to unbounded functions when moment bounds justify it. For OU, L(x)=θ(m−x) and L(x²)=2θx(m−x)+η². Taking expectations closes equations for mean and second moment; subtracting the derivative of mean² produces v′=η²−2θv.</Prose>
    <Example name="generator" />
    <ProbabilityDualityFigure />
    <H3>Forward density and backward question</H3>
    <Prose>If the law has a sufficiently smooth density p, integrate f against that density. Moving derivatives from f onto coefficient-times-density by integration by parts yields the forward Fokker–Planck equation. Boundary terms must vanish or be handled with the specified absorbing, reflecting or flux conditions.</Prose>
    <MathBlock>{String.raw`\partial_t p=-\partial_x(a p)+\tfrac12\partial_{xx}(b^2p).`}</MathBlock>
    <Prose>Do not replace this by −a∂ₓp+½b²∂ₓₓp when coefficients vary with x: their derivatives contribute. The forward equation may instead hold weakly as an identity against test functions when a classical density is unavailable. At a deterministic start, p₀ can be a point mass; a drawn infinite “density spike” is not an ordinary function.</Prose>
    <Prose>Now fix a terminal question g and define u(t,x)=E[g(X_T)|X_t=x]. For a Markov diffusion, sufficient regularity and integrability give the backward equation ∂ₜu+Lₜu=0 with u(T,x)=g(x). This is the no-running-cost, no-discount form of the stochastic representation often called Feynman–Kac. The two derivatives have different roles: forward evolves the law; backward evaluates a future question from the present state.</Prose>
    <Prose>For OU with g(x)=x², let τ=T−t. Starting now from x, the answer is the conditional variance plus conditional mean squared:</Prose>
    <MathBlock>{String.raw`\begin{aligned}u(t,x)&=\left[m+(x-m)e^{-\theta\tau}\right]^2\\&\quad+\frac{\eta^2}{2\theta}(1-e^{-2\theta\tau}).\end{aligned}`}</MathBlock>
    <Prose>At τ=0 this equals x². Differentiating verifies the backward equation; finite Gaussian moments justify this polynomial observable. General coefficients, unbounded rewards or boundaries need their own regularity and integrability checks. Deriving an equation is also separate from numerically solving that PDE.</Prose>
    <H3>A deeper connection: change probabilities, not observations</H3>
    <Prose>On a fixed finite horizon, weight Brownian histories by Z_T=exp(cW_T−c²T/2) for constant c. The Gaussian moment formula gives E[Z_T]=1; independent future increments also give E[Z_T|Fₜ]=Zₜ. Thus this positive martingale defines a new probability measure Q by dQ=Z_T dP. Completing the Gaussian square shows that W_T has mean cT and variance T under Q. Applying the same factorization to increments shows Wₜ−ct is Brownian under Q on this horizon.</Prose>
    <Example name="tilt" />
    <Prose>This simple case explains why a drift can appear when the probability assigned to paths changes. It is a finite-horizon change of measure, not a new observation or a claim about an infinite-time density. General random drift changes require suitable adaptedness and a true-martingale condition for the stochastic exponential; a formal local-martingale calculation alone is insufficient. General Girsanov theory and stochastic control build on this distinction.</Prose>

    <H2>10. Apply, test and practise</H2>
    <H3>Recognize where the model earns its complexity</H3>
    <Prose><strong>A noisy subthreshold voltage.</strong> Before a neuron fires, a simple passive membrane model has restoring drift toward an effective resting level, with time constant τ. If the unresolved drive is approximated by white noise, write dV=−(V−m)dt/τ+ηdW. Here η has voltage/√time units and the unreset stationary variance is η²τ/2. Threshold-triggered resets change the process and its stationary law; they cannot be appended while continuing to claim the unrestricted Gaussian OU law. Crossing a threshold between recorded time points also matters. Real synaptic input can have a finite correlation time; a white-noise approximation and its coefficient convention need a stated justification.</Prose>
    <Prose><strong>A controlled forward corruption process.</strong> The constant-rate SDE dX=−βXdt/2+√βdW is an OU model with conditional mean e⁻ᵝᵗᐟ²X₀ and conditional variance 1−e⁻ᵝᵗ. It progressively reduces dependence on a fixed starting value while injecting noise with a known law. This makes an understandable bridge to diffusion generative models. Learned reverse-time dynamics, scores and sampler schedules require additional theory; an arbitrary sign reversal of this drift is not their derivation. Neural SDEs can also learn drift and diffusion from irregular observations, while continuous-time filtering and control use process noise to represent latent-state uncertainty. Observation error and parameter uncertainty remain separate modeling choices.</Prose>
    <Prose><strong>Trust a diagnostic workflow.</strong> State the units, calculus convention, noise covariance and boundary behavior. Examine whether conditional increment variance scales plausibly with elapsed time, and whether omitted state or measurement noise explains apparent randomness. Compare held-out multi-step distributions and event probabilities, not just one-step mean error. Check step refinement under shared noise, independent-path sampling uncertainty, stability and constraints. Repeated random seeds are a reproducibility tool, not evidence of a fit to real data.</Prose>
    <Prose>Exact values at observation times do not certify what happened between them. For a constant-coefficient Brownian bridge with two endpoints below a fixed barrier, the earlier stochastic-processes lesson derives the missed-crossing probability. That formula is exact for that model; using it unchanged for a nonlinear state-dependent SDE is generally an approximation needing validation. A narrower time grid reduces some missed events but does not turn a grid drawing into a continuous path.</Prose>

    <Practice prompt="Change the time unit from seconds to minutes for dX=0.3 dt+0.8 dW. Give the new coefficients and verify the variance over ten seconds." hint="One minute contains 60 seconds. Diffusion scales with the square root of the conversion factor.">
      <Prose>The new drift is 18 units/minute and diffusion is 0.8√60 units/√minute. Ten seconds is 1/6 minute; the variance is (0.8²×60)/6=6.4 units², equal to 0.8²×10. Multiplying the diffusion by 60 would produce the wrong variance.</Prose>
    </Practice>
    <Practice prompt="For increments 1/4, −1/2 and 1, compute the left, right and symmetric sums without running a step loop. Does the left sum have to equal its Brownian limit for T=1?" hint="First find the endpoint and Q; then use the three telescoping identities.">
      <Prose>The endpoint is 3/4 and Q=21/16. Left=(9/16−21/16)/2=−3/8; right=15/16; symmetric=9/32. The Brownian limiting expression at this endpoint is (9/16−1)/2=−7/32, which differs from the finite left sum because Q≠1. A fixed list is an algebra problem; convergence is a separate statement about random increments along deterministic partition refinements.</Prose>
    </Practice>
    <Practice prompt="Let dX=−2Xdt+3dW. Derive the equation for X² and the stationary variance when the mean is zero. What happens if you omit the curvature term?" hint="Use f′=2x and f″=2, then take expectations under finite second moments.">
      <Prose>d(X²)=(−4X²+9)dt+6XdW. The second moment obeys s′=−4s+9, so its stationary value is 9/4. With zero mean this is the variance. Dropping the correction would falsely predict all second moments decay to zero despite persistent noise.</Prose>
    </Practice>
    <Practice prompt="A source writes dX=0.2Xdt+0.6X∘dW. Convert to Itô and give the expected value and median at time t for X₀=2." hint="Compute ½bb′, then apply the GBM law using the converted drift.">
      <Prose>The Itô drift coefficient is 0.2+0.6²/2=0.38. Therefore E[Xₜ]=2e⁰·³⁸ᵗ and the median is 2e⁰·²ᵗ. Treating 0.2 as the Itô drift instead would give mean 2e⁰·²ᵗ and median 2e⁰·⁰²ᵗ, a different model.</Prose>
    </Practice>
    <Practice prompt="For X=W₁+2W₂ and Y=3W₁−W₂, with independent drivers, derive d(XY) and E[X_TY_T]." hint="Compute the dot product of the two noise-coefficient rows. The initial coordinates are zero.">
      <Prose>The covariance rate is 1×3+2×(−1)=1. Thus d(XY)=X dY+Y dX+dt and E[X_TY_T]=T. Each state has multiple drivers; cancelling one driver's covariance contribution does not erase the other one.</Prose>
    </Practice>
    <Practice prompt="Return to the original GBM example. Use 40 steps of 0.025 and 1,000 independent paths; compare terminal mean, median and 5th/95th percentiles with analytic targets. Add a paired exact terminal value to each path. What should your report contain?" hint="Create one generator outside both loops, sum each path's increments, and compute an exact endpoint from that sum. Distinguish raw terminal sampling error from the mean of paired numerical errors.">
      <Prose>For X₀=1, μ=0.4, σ=0.3 and T=1, the exact mean is e⁰·⁴≈1.491825, median e⁰·³⁵⁵≈1.426181, and percentiles are exp(0.355±0.3×1.6448536), approximately 0.870701 and 2.336037. The exact terminal variance is e⁰·⁸(e⁰·⁰⁹−1)≈0.209589, giving a theoretical terminal-mean SE about 0.014477 for 1,000 exact paths. Sample summaries fluctuate with the seed. EM's expected terminal value is 1.01⁴⁰≈1.488864, so its mean bias is approximately −0.002961, smaller than that raw sampling SE. Pairing makes a much more sensitive comparison possible.</Prose>
      <Prose>Report the generator/seed, independent path count, h, theoretical targets, estimated standard errors and actual paired bias/RMS. Refine the same fine increments rather than redraw each resolution. A single mean within an interval does not validate the distribution, and quantile uncertainty needs its own method, such as an independently justified order-statistic interval or resampling approach. The larger mean than median follows from the lognormal upper tail.</Prose>
    </Practice>
    <Practice prompt="For dX=−Xdt+dW, compare a fixed start X₀=0 with a stationary start. Why would taking J proportional to the same ΔW be unsuitable for an exact pathwise solver reference?" hint="Find the stationary variance, and compare Cov(ΔW,J) with the product of their standard deviations.">
      <Prose>The fixed-start law is Normal(0,(1−e⁻²ᵗ)/2); the stationary initial law is Normal(0,1/2), independent of future increments, and remains invariant. For h&gt;0, J's exponentially changing kernel is not a constant multiple of the unweighted kernel. Cauchy–Schwarz is strict, so the correlation is less than one. Use the conditional Gaussian residual construction to couple exact OU noise to ΔW; merely matching the marginal variance forces the wrong correlation.</Prose>
    </Practice>
    <Practice prompt="For dX=−Xdt+XdW, calculate the EM mean-square multiplier at h=0.5 and h=1.5. Does either answer alone settle positivity or individual-path convergence?" hint="The multiplier is (1−h)²+h. Compare with one.">
      <Prose>At 0.5 the multiplier is 0.75, so EM is mean-square stable; at 1.5 it is 1.75, so it is unstable despite exact mean-square decay. Neither result guarantees positivity of every step or monotone improvement of a single path. The multiplier summarizes an ensemble second moment.</Prose>
    </Practice>
    <Practice prompt="A solver matches E[X_T] exactly when μ=0. Design a stronger test, and explain why independent exact and approximate paths give a misleading strong-error result." hint="Try g(x)=x²; for strong error, ask what remains even if both independent paths are exact.">
      <Prose>Compare the second moment and a relevant event probability, with sampling uncertainty, then compare coupled terminal paths. If two independent exact draws have the same nonzero variance v, E[(X−X′)²]=2v. That discrepancy persists without any numerical error. Independent reference paths can test distributional summaries, but they do not supply the intended shared-driver strong error.</Prose>
    </Practice>
    <Practice prompt="Write the forward density equation for dX=(1−X)dt+X dW. Why is (1−x)pₓ and x²pₓₓ alone incomplete?" hint="Move the generator derivatives onto the entire coefficient-times-density product.">
      <Prose>The equation is ∂ₜp=−∂ₓ((1−x)p)+½∂ₓₓ(x²p), with a specified domain and boundary behavior. Product differentiation creates additional terms. On a region where p is smooth it expands to 2p+(3x−1)pₓ+½x²pₓₓ. The conservative form keeps probability flow and boundary conditions easier to inspect.</Prose>
    </Practice>
    <Practice prompt="A voltage simulator adds the same numerical η√h noise after changing its time axis from seconds to milliseconds, and resets at a threshold. Identify two independent claims that require repair." hint="Separate coefficient units from the law of an unreset process.">
      <Prose>The diffusion coefficient must be divided by √1,000 when the numerical time unit changes to milliseconds. Separately, a threshold/reset mechanism changes the state law; the unrestricted OU stationary Gaussian formula cannot describe the reset process. Grid-only threshold detection can also miss crossings, which needs event-detection analysis rather than a coefficient-unit repair.</Prose>
    </Practice>
    <Prose><strong>Continue in module order:</strong> Numerical Methods (Finite Differences, Quadrature, Root Finding). That topic develops approximation and error tools needed for the deterministic integrals, equations and PDE discretizations encountered here. The stochastic coupling, information and noise-scaling rules remain necessary when those tools are placed inside an SDE solver.</Prose>

    <Sources alternatives={<>
      <Prose><a href="https://ocw.mit.edu/courses/18-s096-topics-in-mathematics-with-applications-in-finance-fall-2013/resources/lecture-18-ito-calculus/">MIT OCW — Lecture 18: Itô Calculus (Choongbum Lee)</a> is an optional lecture-format route through stochastic integration and the chain rule. The official course page and its companion notes were checked; the full video was not watched for this revision, so no segment timestamps or complete-video endorsement are implied. Pair it with the finite sum investigation above.</Prose>
      <Prose><a href="https://webhomes.maths.ed.ac.uk/~dhigham/Publications/P42.pdf">Higham — An Algorithmic Introduction to Numerical Simulation of SDEs</a> is a useful written alternative after the exact models: sections 4–7 connect shared Brownian paths, strong/weak convergence, Milstein and stability. The paper uses MATLAB; our complete examples derive and implement the ideas in Python.</Prose>
    </>}>
      <li><a href="https://people.maths.ox.ac.uk/fehrman/SDENotes.pdf">Benjamin Fehrman — Stochastic Differential Equations</a>: selected stochastic-integration, Itô-formula, martingale and strong-solution sections were consulted for measurability, integrability, localization and sufficient existence conditions. This is the deeper proof route, not a prerequisite to understanding the worked models.</li>
      <li><a href="https://ocw.mit.edu/courses/18-s096-topics-in-mathematics-with-applications-in-finance-fall-2013/ef2c66c8079ba656210ad1fd4a5e2fa8_MIT18_S096F13_lecnote18.pdf">MIT lecture 18 notes</a>: elementary adapted sums, isometry and stochastic chain-rule intuition. Our statements retain the additional measurability and moment conditions needed beyond the informal shorthand.</li>
      <li><a href="https://www.columbia.edu/~mh2078/MonteCarlo/MCS_SDEs_MasterSlides.pdf">Martin Haugh — Simulating Stochastic Differential Equations</a>: selected exact-transition, path coupling and discretely monitored barrier discussions. Exact marginal sampling and pathwise/event accuracy are treated separately here.</li>
      <li><a href="https://math.nyu.edu/~goodman/teaching/StochCalc2007/notes/l7.pdf">Jonathan Goodman — Diffusions and their equations</a>: stochastic integral conventions, generators, adjoints and forward/backward roles. Our OU polynomial calculation supplies an explicit check.</li>
      <li><a href="https://neuronaldynamics.epfl.ch/online/Ch8.S1.html">Gerstner et al. — Neuronal Dynamics, noisy integrate-and-fire models</a>: physical motivation for subthreshold restoring drift and noise. We use an explicit voltage/√time coefficient convention and distinguish the unreset OU law from firing/reset behavior. Brownian-driven sample paths are continuous and generally nondifferentiable.</li>
      <li><a href="https://arxiv.org/abs/2011.13456">Song et al. — Score-Based Generative Modeling through Stochastic Differential Equations</a>: original source for the modern generative-model connection. Its identity and abstract were checked for this contextual bridge; the reverse-time algorithm and empirical comparisons are outside this lesson's reviewed claims.</li>
    </Sources>
  </div>
};
