import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { HeapOperationsLab, HeapConstructionFigure, TopKStreamLab, TrieSharingFigure, TriePrefixLab } from '../../components/lesson-labs/HeapTrieLabs';
import { heapTrieExamples } from '../heap-trie-examples.js';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import heapTriePractice from '../practice/heaps-priority-queues-tries.js';

export default {
  title: 'Heaps, Priority Queues & Tries',
  readTime: '~50 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot heap-trie-lesson">
    <LessonIntro prerequisites="Python lists, dictionaries, classes and references; the preceding Trees & Binary Search Trees lesson. We reuse tree height, a loop invariant and a short recursive traversal, with reminders at their point of use."
      sections={[["1-choose-the-question-before-the-structure", "Choose the contract"],["2-a-heap-keeps-the-next-item-nearby", "Heap shape and repairs"],["3-build-once-or-maintain-a-moving-boundary", "Construction and top-k"],["4-priority-is-a-policy", "Ties and changing priorities"],["5-use-a-small-frontier-to-coordinate-work", "Merging and events"],["6-follow-the-parts-of-a-key", "Trie paths and words"],["7-turn-prefixes-into-useful-answers", "Suggestions and prefix policies"],["8-practise-and-choose-under-new-constraints", "Independent practice"],["guided-dsa-practice", "Guided LeetCode practice"]]}>
      First learn to repeatedly choose the next item by priority. Then change the question: find words by following their shared prefix. These are two different uses of branching structure, taught in two connected parts. The goal is to choose, implement and explain each structure, including the questions it cannot answer efficiently.
    </LessonIntro>

    <H2>1. Choose the question before the structure</H2>
    <Prose>A queue processes the oldest waiting item. A search tree finds a key by comparison. A program may instead ask, “Which event is scheduled earliest, even if it was added last?” Another asks, “Which commands begin with these characters?” The first needs a <strong>priority queue</strong>; the second can benefit from a <strong>trie</strong>, pronounced “try.” Their usefulness comes from preserving the particular relationships their queries need.</Prose>
    <Prose>A priority queue is a behavior: insert an item with a priority, inspect the next priority, and remove that item. A <strong>binary heap</strong> is one implementation. A trie represents keys as paths through their parts, usually characters. Neither is a general replacement for a dictionary, a sorted array or a balanced BST.</Prose>
    <LessonTable caption="Match the structure to the query" headers={["Question", "Useful representation", "What is deliberately not promised"]} rows={[
      ["Oldest waiting job?", "FIFO queue", "The oldest job need not have the highest priority."],
      ["Smallest currently stored priority?", "Min-heap", "The rest of its array is not fully sorted."],
      ["Greatest key no larger than a target?", "Ordered collection, such as a balanced BST", "An ordinary unbalanced BST still has linear worst-case height."],
      ["Does this exact key exist?", "Hash set/map", "Prefix enumeration needs additional structure or a scan."],
      ["Which stored words start with ca?", "Trie", "Following a prefix does not decide how to rank all its completions."],
    ]} />
    <Prose>Throughout the heap examples, <strong>smaller numbers mean earlier service</strong>. A max-heap reverses that convention. Integer priorities keep comparisons simple. Strings, compound keys, floating-point values and application policies need a consistent ordering; values such as NaN do not behave like ordinary ordered numbers.</Prose>

    <H2>2. A heap keeps the next item nearby</H2>
    <H3>Two rules, doing two different jobs</H3>
    <Prose>Imagine storing priorities <Code>[1, 2, 9, 7, 5]</Code> in level order: root first, then its children, then the next level from left to right. A binary heap has <strong>complete shape</strong>: every level before the last is full, and the last fills from the left. Its <strong>min-heap ordering</strong> says each parent is no greater than either child. Shape keeps paths short; ordering makes the root a minimum.</Prose>
    <Prose>Why is the root globally smallest? Any other node has a chain of ancestors leading to the root. Each step toward the root reaches a value no larger than the previous one. Transitivity carries that comparison all the way up. This does not compare siblings: 9 can be to the right of 2 while 5 appears later than 7 in the array. Inorder traversal is not a way to sort a heap.</Prose>
    <Prose>The array already encodes the links. At zero-based index i, the children are at 2i + 1 and 2i + 2 if those indices exist. For a non-root node, the parent is at (i − 1) // 2. The formula describes array positions, not comparisons between stored values. There are no per-node left/right references in this implementation.</Prose>
    <HeapOperationsLab />
    <Prose>Use the default heap and predict where a new 0 will travel. Appending puts it at index 5, the next available leaf. Its parent is index 2, containing 9, so swap them. Its new parent is index 0, containing 1, so swap again. The final array is <Code>[0, 2, 1, 7, 5, 9]</Code>. The whole item moves during each swap; an index describes its current location, not a permanent item identity.</Prose>
    <H3>Insertion repairs upward; removal repairs downward</H3>
    <Prose>Insertion first preserves complete shape by appending. Before each upward comparison, every ordering relation is valid except possibly the new item's relation with its parent. Swap while the child is smaller. The displaced parent still fits above the descendants below it; only the next ancestor comparison may need repair. The position moves strictly toward the root, so the process terminates.</Prose>
    <Prose>To remove the minimum, save the root value, remove the last array item, and place that last value at the root if anything remains. Taking an arbitrary internal node instead would leave a hole in the complete shape. Now both child subtrees are heaps, but their new parent might be too large. Compare the children and promote the <strong>smaller</strong> one. Repair follows the displaced value down that child's branch.</Prose>
    <Prose>Choosing the smaller child matters: if a replacement root is 10 with children 4 and 2, swapping with 4 leaves 4 above 2, still invalid. Our tie rule chooses the left child when both priorities are equal. That makes the trace repeatable; it does not make the entire heap stable for equal-priority items. Stop when the parent fits above the smaller child, or no child exists.</Prose>
    <RunnableExample example={heapTrieExamples.heapOperations}><Prose>The constructor copies the input, then repairs subtrees from the last internal node back to the root. The next section explains why. Each pop returns one minimum; the “ordered” output comes from repeated removals and leaves the heap empty. An empty pop raises IndexError, while removing a singleton skips the root replacement. Duplicate priorities are retained as separate occurrences.</Prose></RunnableExample>
    <Checkpoint prompt="Does every sorted ascending array represent a min-heap? Does every min-heap represent a sorted ascending array?">
      <Prose>An ascending array satisfies the parent comparisons because every child index is larger than its parent's index. The converse fails: [1, 2, 9, 7, 5] is a heap but is not sorted. Heap order is a weaker requirement, sufficient for finding the minimum. Searching for an arbitrary value can still require O(n) inspections.</Prose>
    </Checkpoint>
    <H3>Use the library once you can explain the repair</H3>
    <Prose>Python's <Code>heapq</Code> operates on ordinary lists in place. <Code>heapify</Code> builds heap order; <Code>heappush</Code> inserts; <Code>heappop</Code> removes the minimum; <Code>heap[0]</Code> peeks without removing. Do not append, replace an arbitrary priority, or assume a library call has repaired a list unless you actually use the appropriate heap operation.</Prose>
    <RunnableExample example={heapTrieExamples.libraryOperations}><Prose>Push-then-pop considers the new candidate alongside existing values, so incoming 1 is immediately returned and the old [3, 5, 8] remains. Replace always removes the old minimum first, then retains the incoming item; here it returns 3 and keeps 1. Replace requires a nonempty heap. These are different contracts, not interchangeable spelling.</Prose></RunnableExample>
    <Prose>The negation example uses a min-heap as a max-heap for numeric values: the largest original number has the smallest negative. Negate both on entry and on exit, including inserted replacements. Python 3.14 added public <Code>heapify_max</Code>, <Code>heappush_max</Code> and related functions. These programs run on Python 3.12 as well, so they use the portable numeric negation method. Negating an arbitrary object is not a generic comparison policy.</Prose>

    <H2>3. Build once or maintain a moving boundary</H2>
    <Prose>A complete tree with n nodes has height floor(log₂ n) when n is positive. Each sift moves along at most one such path. Peek is O(1); push and pop need O(log n) comparisons. With a growing Python list, occasional allocation/copying makes the storage contribution amortized rather than a strict per-call logarithmic guarantee. These are algorithmic costs with constant-cost comparisons, not measured execution times.</Prose>
    <H3>Why bottom-up construction is linear</H3>
    <Prose>One construction strategy inserts n items separately, costing O(n log n) in the worst case. Bottom-up construction does less work. Every leaf is already a one-node heap. At index n // 2 − 1, both children are leaves or absent. Repair that parent, then work backward: whenever a parent is visited, its child subtrees have already been repaired. This invariant proves the final root heads a heap containing the original multiset.</Prose>
    <HeapConstructionFigure />
    <Prose>For a full 15-node shape, eight leaves move zero levels, four parents can move at most one, two can move at most two, and one root can move at most three. The sum of those height budgets is 4 + 4 + 3 = 11, not 15 × 3. Actual comparisons add constant work per checked level. More generally the number of nodes with height around h decreases geometrically, while their repair work grows only like h. The convergent sum of h/2ʰ bounds total work by a constant times n. Thus heap construction is O(n), although extracting every item afterwards is O(n log n).</Prose>
    <details className="lesson-deeper"><summary>Deeper: turn the heap into an in-place sorting algorithm</summary>
      <Prose>Build a max-heap. Move its root maximum to the array's last active position, shrink the active heap by one, and repair the new root. Repeat. At each iteration, the suffix is sorted and contains the largest removed values, while the active prefix is a max-heap. Do not let repair inspect the already sorted suffix. The following iterative implementation uses O(1) auxiliary storage and O(n log n) worst-case time; ordinary heapsort is not stable for records with equal comparison keys.</Prose>
      <RunnableExample example={heapTrieExamples.heapSort}><Prose>This modifies the supplied list. It differs from collecting min-heap pops into a second result list, which allocates O(n) output space. The active interval is [0, end), so end itself belongs to the sorted suffix after the swap.</Prose></RunnableExample>
    </details>
    <H3>Keep only values that can still affect the answer</H3>
    <Prose>Suppose values arrive one at a time and you need the third largest so far. Sorting every prefix works, but repeatedly revisits values that no longer matter. Keep the largest three <em>occurrences</em> in a min-heap. Its root is the smallest of those three, exactly the third-largest boundary. A new value below that boundary cannot improve the retained set; a larger value replaces the boundary.</Prose>
    <TopKStreamLab />
    <Prose>For stream <Code>[5, 1, 9, 3, 9, 2]</Code> with k = 3, the first full retained set is [1, 5, 9]. Incoming 3 displaces 1. Incoming 9 displaces 3, leaving [5, 9, 9]. The second 9 counts because rank here includes duplicates. Incoming 2 changes nothing. Before three values exist, “third largest” is undefined; the program returns None for that intermediate boundary.</Prose>
    <RunnableExample example={heapTrieExamples.topK}><Prose>The invariant after each arrival is that the heap contains the largest min(k, number seen) occurrences. Either the old set plus the new value still fits, or its smallest occurrence is the only one to discard. Maintaining the boundary costs O(log(k + 1)) per arrival and O(k) retained space. This teaching generator additionally sorts and copies each retained state for display, costing O(k log(k + 1)) per snapshot; omit that display work when only the kth value is needed.</Prose></RunnableExample>
    <Checkpoint prompt="Why use a min-heap to retain the largest k values? Would an unbounded max-heap answer the same question just as cheaply?">
      <Prose>The smallest retained value is the one that might be replaced, so it belongs at the root. A max-heap exposes the very largest instead. Keeping every input in an unbounded heap uses O(n) storage and retrieving rank k by repeated removal needs extra work and changes the structure. The root should expose the boundary your update rule needs.</Prose>
    </Checkpoint>

    <H2>4. Priority is a policy</H2>
    <Prose>A priority queue must decide what equal priorities mean. Python compares tuples from left to right. A pair (priority, payload) may accidentally compare payloads on a tie, producing unwanted alphabetical order or a TypeError for non-orderable objects. Add a unique increasing ticket: <Code>(priority, ticket, payload)</Code>. The ticket makes equal priorities follow arrival order and prevents payload comparison. The heap alone did not supply this policy.</Prose>
    <LessonTable caption="A stable comparison key separates ordering from payload" headers={["Priority", "Arrival ticket", "Payload", "Order"]} rows={[[1, 8, 'paint', 'Before save: same priority, earlier ticket'],[1, 9, 'save', 'After paint'],[0, 10, 'report', 'Before both: smaller priority wins before ticket is considered']]} />
    <Prose>Updating a stored priority in place can break the heap rule. Removing a job is also difficult if all you have is its identifier rather than its array position. One solution keeps a dictionary of the current entry for each job. On an update, push a new entry and make it current; the old entry becomes <strong>stale</strong>. On cancellation, remove the dictionary entry. When popping, skip any heap entry that is no longer current.</Prose>
    <RunnableExample example={heapTrieExamples.stablePriorities}><Prose>The identity check asks whether this exact tuple is still the job's live version, so an older entry with the same priority cannot impersonate an update. Updating a job counts as a new arrival for ties in this policy. The stale report and cancelled paint entries may remain physically stored until popped or compacted; logical membership and physical storage are different.</Prose></RunnableExample>
    <Prose>Let H be the number of physical heap entries, including stale ones. An insertion costs O(log H) heap work, dictionary cancellation is expected O(1), and a pop that skips s stale entries can cost O((s + 1) log H). Each stale entry can be removed only once, but a particular pop can be expensive. <Code>compact()</Code> rebuilds from live entries; reading them and heapifying is linear in the number retained, while releasing the old Python list can also touch its H entries. Schedule compaction when stale storage becomes excessive. This small implementation leaves that policy explicit.</Prose>
    <details className="lesson-deeper"><summary>Deeper: indexed heaps and two coordinated heaps</summary>
      <Prose>An indexed heap keeps a map from item identity to its current array index. Every swap must update both map entries. Changing a priority can then locate its position and sift in the appropriate direction; removing an arbitrary known item can replace it with the last item and repair. This avoids stale versions but adds another invariant. Forgetting one map update is a correctness bug even when the heap ordering still looks right.</Prose>
      <Prose>For a running median, split values into a lower half and upper half. A max-heap exposes the lower half's largest; a min-heap exposes the upper half's smallest. Maintain that every lower value is at most every upper value, and that sizes differ by at most one under a declared extra-item policy. Move a boundary value across if necessary after each insertion. For an odd total, the larger half's root is the median; for an even total, average the two roots. Duplicates belong to the counts. The optional median problem below lets you implement and verify both invariants.</Prose>
    </details>

    <H2>5. Use a small frontier to coordinate work</H2>
    <H3>Merge logs without repeatedly sorting all of them</H3>
    <Prose>Imagine several log files already ordered by timestamp. Among the unconsumed records in one file, its next record is no later than its following records. Therefore only the next record from each nonempty file needs to compete for the next global output. Keep those candidates in a heap. Remove the smallest, output it, then bring in the next record from the same source.</Prose>
    <RunnableExample example={heapTrieExamples.mergeStreams}><Prose>The source index breaks equal-value ties without comparing iterator objects; at most one entry per source is present. If k sources supply N records, initialization scans k iterators and heapifies at most k candidates. Traversal takes O(k + N log(k + 1)) time and O(k) auxiliary state, excluding output. The demonstration collects the generator into a list; a downstream iterator can consume one record at a time instead. Every source must already be sorted under the same rule.</Prose></RunnableExample>
    <Prose>The correctness argument uses a <strong>frontier</strong>: a small set of candidates representing larger unexplored regions. No hidden record can precede its own source's head. The minimum frontier head is therefore the next globally safe output. This idea will return in graph algorithms, although deciding when a path is safe to finalize requires additional assumptions there.</Prose>
    <H3>An event can create the next event</H3>
    <Prose>A less obvious use is discrete-event simulation. A simulated clock jumps to the earliest scheduled event; it does not tick through every unused time unit. Processing an event can schedule future work. In this original example, starting a sample at time 1 schedules its completion two time units later. A heartbeat shares time 1, and an archive event waits at time 4. Arrival tickets make same-time behavior deterministic.</Prose>
    <RunnableExample example={heapTrieExamples.eventSchedule}><Prose>The result is 1 sample, 1 heartbeat, 3 sample complete, 4 archive. A callback may add a time equal to or later than the current simulated time, never earlier. A system that endlessly reschedules at the same time can still fail to make progress; the finite fixture avoids that. These are simulated timestamps, not wall-clock timing or a real-time scheduling guarantee. Priority queues also appear in shortest-path work; the later <a href="/learn/topic/shortest-paths-spanning-trees-topological-ordering">Shortest Paths, Spanning Trees & Topological Ordering</a> lesson owns those weighted-path guarantees.</Prose></RunnableExample>

    <H2>6. Follow the parts of a key</H2>
    <Prose>Now change the question from “Which priority comes next?” to “Which stored words start with ca?” A hash set can test each complete word quickly, but cannot follow “ca” directly to all matching words without extra structure. A trie creates one outgoing edge per next character. All keys that share a prefix share its path. The root represents the empty prefix; traversing c then a reaches the node for ca.</Prose>
    <TrieSharingFigure />
    <Prose>Insert car, cart, cat and dog. The first three share c and a. Car and cart also share r; the terminal mark at r means car is itself stored, while its t child ends cart. Cat uses another t edge from the node for ca. Equal character labels at different depths or under different parents are different edges and different nodes. Their full root-to-node prefixes identify their meaning.</Prose>
    <Prose>A <strong>terminal flag</strong> marks a complete stored word. Reaching the node for ca proves a prefix path exists but does not prove ca was inserted. Inserting a duplicate word simply keeps the flag true in this set-style trie. A map version could attach a value; a multiset version could keep a count. Our empty-string policy allows the root itself to be terminal. The empty prefix exists even in an empty trie, because it denotes the root, not a promise that some stored word exists.</Prose>
    <TriePrefixLab />
    <Prose>First query ca as an exact word, then as a prefix. Switch the operation and compare the result with the same visible trie. Next delete car and check cart again. Removing one word must not erase its descendants. The browser uses small lowercase ASCII fixtures for a readable diagram; the Python implementation below follows Python string characters and does not have that display limit.</Prose>
    <H3>Insertion, lookup and deletion share one path</H3>
    <Prose>Insertion walks each character, creates a missing child, and marks the final node terminal. Lookup follows the same path without creating anything. At each step, the current node represents exactly the consumed prefix. An absent child proves no stored key can continue through that missing edge. At the end, exact lookup checks terminal; prefix lookup only checks that the path exists.</Prose>
    <Prose>Deletion first verifies the complete word exists, then clears only its terminal flag. Walk back along the recorded path and remove a child only when it has neither a terminal flag nor children. Stop at the first still-useful node. Deleting car while cart remains cannot prune r, because r still has a child. Deleting cart afterwards can prune its unused suffix until another stored word or branch is reached. An absent-word deletion changes nothing.</Prose>
    <RunnableExample example={heapTrieExamples.trieOperations}><Prose>After deleting car, exact lookup is False but prefix lookup remains True because cart survives. The dictionary at each node stores only existing outgoing edges. The deletion path is an explicit stack of parent/edge/child triples; it preserves the information needed to reconnect or prune without searching the tree again. No recursive deletion is required.</Prose></RunnableExample>
    <H3>Character cost and storage cost are part of the contract</H3>
    <Prose>For a key of length L, insertion, exact lookup and this deletion take O(L) expected time with expected constant-time dictionary child access. Deletion uses O(L) temporary path storage; plain lookup uses O(1) extra state. A trie contains one node for each distinct nonempty stored prefix plus the root, so total nodes are at most one plus the sum of inserted word lengths, ignoring duplicates and after full pruning. Sharing saves repeated paths, but separate node objects and dictionaries can still have substantial memory overhead.</Prose>
    <Prose>An array with one slot per possible alphabet symbol offers direct child indexing but reserves unused slots. Sparse dictionaries trade those reserved slots for dictionary overhead. Neither representation is universally faster or smaller; measure the actual workload rather than inventing a benchmark. Our Python loop counts Unicode code points, which need not equal visible characters or user-perceived letters. Case folding and normalization are separate policies and must be applied consistently on insert, query and delete. This lesson's code does neither automatically.</Prose>

    <H2>7. Turn prefixes into useful answers</H2>
    <H3>Find candidates, then decide their order</H3>
    <Prose>To suggest words after ca, first reach its node. Then visit the subtree and emit a word whenever a terminal node is reached. The earlier <Code>suggestions</Code> implementation emits the current terminal before visiting its children, and sorts child characters before descending. This gives Python's lexicographic order for the stored strings, not an order based on popularity and not locale-aware dictionary collation. A dictionary's insertion order would not automatically give the required lexical suggestions.</Prose>
    <Prose>The helper keeps a mutable list of characters for the current path. Entering a child appends one character; returning pops it. Join the path only when emitting a complete word. The recursive call returns True once the requested limit is reached, allowing its callers to stop. The limit must be a nonnegative integer, excluding booleans; zero returns no suggestions and a missing prefix returns an empty list. Very long suffix paths can exceed Python's recursion limit in this collector; use an explicit traversal stack for such inputs. Insertion, lookup and deletion above are already iterative.</Prose>
    <Prose>Finding a prefix of length P is O(P) expected time. Enumerating results is additional work: count visited nodes, sorting the visited child sets, and the total characters copied into returned words. If a node has d children, sorting its child keys costs O(d log(d + 1)); even a small result limit can still inspect or sort substantial structure. The path and recursive frames also use memory. Do not label an autocomplete query O(P) while ignoring its output. For a huge static catalogue, storing precomputed top suggestions at prefix nodes is another tradeoff; updates must keep that metadata correct.</Prose>
    <H3>Shortest prefix and longest prefix are different policies</H3>
    <Prose>For stored keys a, app, apple and map, querying applesauce encounters terminal nodes after a, app and apple. A shortest-prefix policy stops at a. A longest-prefix policy remembers each terminal and returns apple when the path can go no farther. Checking only whether the full query exists would miss both useful answers. The following original example reuses the trie but changes the stopping rule.</Prose>
    <details><summary>Complete runnable prefix-policy example</summary><RunnableExample example={heapTrieExamples.prefixPolicies}><Prose>best_length records the most recent valid match; only the final slice creates the returned prefix. The query inspects at most L characters, plus the returned slice, and does not enumerate every stored key. Returning None means no stored prefix matched; returning the empty string means an explicitly stored empty key matched. Those outcomes are distinct.</Prose></RunnableExample></details>
    <Prose>These policies support different tasks. Replacing a word with its shortest registered root uses the first terminal. Resolving a command namespace to its most specific registered prefix uses the last terminal under that stated character-prefix contract. A path-based application may instead require segment boundaries: a registration for “app” should not necessarily match “apple.” Choosing characters, bytes, bits or path segments as edges is part of modeling the problem.</Prose>
    <details className="lesson-deeper"><summary>Deeper: compressed paths, wildcard branching and substring search</summary>
      <Prose>A compressed radix trie can combine a nonbranching chain into a multi-character edge. That reduces node overhead but makes insertion more involved: a partial edge match may require splitting it. End-of-key information must still survive when one key is another's prefix. A ternary search trie uses character comparisons and three-way links as another representation with different costs.</Prose>
      <Prose>A wildcard character may match several outgoing edges, so exact lookup's one-path O(L) bound no longer applies. A search state needs both a node and an input position. The total work depends on how many branches are explored; repeated wildcards can visit many candidates. The later graph/backtracking lessons develop that branching-search reasoning. Ordinary prefix lookup also does not find an arbitrary substring in the middle of a word; <a href="/learn/topic/string-matching-prefix-functions-rolling-hashes">String Matching, Prefix Functions & Rolling Hashes</a> addresses different matching contracts.</Prose>
    </details>
    <Checkpoint prompt="After storing only cart, is car a word, a prefix, both or neither? What changes if you then insert car and delete cart?">
      <Prose>Initially car is a prefix but not a word. Inserting car marks its node terminal. Deleting cart removes the unused final t node but stops pruning at terminal r. Car is now both a complete word and a prefix. A terminal marker and a branch serve independent purposes.</Prose>
    </Checkpoint>

    <H2>8. Practise and choose under new constraints</H2>
    <H3>Independent task A: reverse the streaming boundary</H3>
    <Prose>Return the smallest k occurrences from a stream. For [5, 1, 9, 3, 9, 2] and k = 3, return [1, 2, 3]. If fewer than k items arrive, return all of them in ascending order. Define k = 0 as an empty result. Use O(k) retained space and explain which boundary belongs at the heap root.</Prose>
    <details><summary>Hint: which retained value would you replace?</summary><Prose>The largest currently retained value is the replaceable boundary. A max-heap exposes it. If using a min-heap of negatives, reason about both the sign on entry and the sign when producing the result.</Prose></details>
    <details><summary>Complete solution and cost</summary><RunnableExample example={heapTrieExamples.smallestPractice}><Prose>The heap retains the largest k negatives, corresponding to the smallest k original values. Final sorting is O(k log(k + 1)) for ordered output, after O(n log(k + 1)) maintenance. If k exceeds n, use min(k, n) in the actual retained-size bound. Duplicates remain separate occurrences; changing to distinct values needs an additional contract and state.</Prose></RunnableExample></details>
    <H3>Independent task B: repair a wrong sift</H3>
    <Prose>A min-heap repair always swaps a too-large parent with its left child. Give a smallest counterexample and repair the decision rule. Test a root with one child, equal children, a replacement already smaller than both children, and an empty heap.</Prose>
    <details><summary>Solution reasoning</summary><Prose>Three nodes suffice: [10, 4, 2]. Swapping with the left child gives [4, 10, 2], still invalid. Compare the available children, choose the smaller, then swap only if that child is smaller than the parent. The complete _sift_down implementation above includes right-child bounds and early termination. [4, 2] becomes [2, 4]; [1, 4, 4] needs no swap. Empty removal must be handled before entering the repair loop.</Prose></details>
    <H3>Independent task C: do not delete somebody else's word</H3>
    <Prose>Insert do, dog and dot. Delete do, then dog. Predict exact lookup and prefix lookup for do, and list the remaining suggestions. Implement deletion from the terminal/child invariants without copying the earlier method. Include a missing word and an empty string in your tests.</Prose>
    <details><summary>Hint</summary><Prose>Clearing a terminal flag removes membership. Removing an edge removes every descendant behind it. Only the first action is automatically safe after finding a stored word.</Prose></details>
    <details><summary>Solution and acceptance cases</summary><Prose>After deleting do, dog and dot remain; do is still a prefix but is not a word. Deleting dog prunes only its g edge, because the do node still has t. Suggestions for do are now [dot]. A missing-word deletion returns False without mutation. Deleting a stored empty string clears the root flag and leaves all nonempty paths intact. Use the complete iterative delete method above, checking both child.children and child.terminal before each prune.</Prose></details>
    <H3>Choose without a problem label</H3>
    <Prose>Compare these requests: repeatedly take the earliest scheduled event; find every word with a prefix; return the smallest key above a target; retrieve a record by an exact ID. Explain a simple baseline, then select the structure that preserves the relationship the query needs. State why a plausible alternative does more work. In an interview, a justified simple solution plus a sound improvement is stronger evidence than recognizing a memorized title.</Prose>
    <Prose>Next is <a href="/learn/topic/graphs-representations-bfs-dfs">Graphs: Representations, BFS & DFS</a>. Trees gave each non-root node one parent; general graph relationships can reconnect and cycle. Queues, stacks, visited state and frontiers will become the tools for exploring them safely.</Prose>
    <DsaPractice practice={heapTriePractice} />
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-4-heaps-and-heap-sort/" target="_blank" rel="noreferrer">MIT OCW: Heaps and Heap Sort</a> — lecture video and companion slides for the heap half. Its max-heap and one-based indexing conventions differ from our initial examples; compare the invariants rather than copying indices.</li>
      <li><a href="https://algs4.cs.princeton.edu/lectures/keynote/52Tries-2x2.pdf" target="_blank" rel="noreferrer">Princeton: Tries lecture slides</a> — visual R-way and ternary trie explanations and character-based operations. Useful after the terminal/prefix investigation; Java and fixed-radix examples have different storage details.</li>
    </ul>}>
      <li><a href="https://docs.python.org/3/library/heapq.html" target="_blank" rel="noreferrer">Python heapq documentation</a> — API semantics, priority-queue considerations and version-specific max-heap functions. Our complete programs use Python 3.12-compatible APIs.</li>
      <li><a href="https://algs4.cs.princeton.edu/24pq/" target="_blank" rel="noreferrer">Princeton: Priority Queues</a> — heap ordering, construction, repair and resource analysis.</li>
      <li><a href="https://algs4.cs.princeton.edu/52trie/" target="_blank" rel="noreferrer">Princeton: Tries</a> — prefix operations and alternative trie representations; use together with the linked slides.</li>
    </Sources>
  </div>,
};
