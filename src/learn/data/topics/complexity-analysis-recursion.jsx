import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { IterationLatticeLab, ReturnLadderFigure, RecursiveFramesLab, RecurrenceLevelsLab, SliceStorageFigure } from '../../components/lesson-labs/ComplexityRecursionLabs.jsx';
import { complexityRecursionExamples } from '../complexity-recursion-examples.js';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import complexityPractice from '../practice/complexity-analysis-recursion.js';

export default {
  title: 'Complexity Analysis & Recursion',
  readTime: '~45 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot complexity-recursion-lesson">
    <LessonIntro prerequisites="Python functions, loops, lists and returns. We refresh powers, logarithms and the small induction argument here. Earlier trees, graphs and Union-Find provide useful comparisons, but their short cost labels are unpacked rather than assumed."
      sections={[
        ['1-count-a-specific-kind-of-work', 'Choose a cost model'],
        ['2-turn-counts-into-growth-bounds', 'Derive growth bounds'],
        ['3-separate-the-case-from-the-bound', 'Best, worst and amortized'],
        ['4-recursion-keeps-unfinished-work', 'Follow call frames'],
        ['5-add-work-across-the-recursion-tree', 'Solve recurrences'],
        ['6-count-storage-and-hidden-operations', 'Space and real costs'],
        ['7-repeated-subproblems-and-measurement', 'Reuse and measure'],
        ['8-practise-explanations-that-survive-a-change', 'Independent practice'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      Learn to answer two questions together: “How much work will this computation need as the input grows?” and “How can a function solve a problem by delegating smaller versions of it?” You will derive counts, follow waiting calls, justify termination and choose implementations using both time and space.
    </LessonIntro>

    <H2>1. Count a specific kind of work</H2>
    <Prose>A program that checks six records may feel instant whether it examines each record once or compares every record with every other. At a million records, that distinction can determine whether the task is practical. <strong>Complexity analysis</strong> studies how resource requirements grow with input size. It lets us reason before collecting a huge dataset, while measurements tell us what a particular implementation actually does on a particular machine.</Prose>
    <Prose>Start by naming the task. Suppose each record has a bounded-size numeric reading. Computing a total inspects every reading once. Comparing every earlier reading with each new one inspects one pair for every pair of distinct indices. Those are different outputs; a fast total does not replace the pair comparison. An optimization must preserve the requested answer before its speed matters.</Prose>
    <Prose>Let <strong>n</strong> be the number of records already in memory, and count one reading access, addition or comparison as one constant-cost operation. This is a <strong>cost model</strong>: a declared abstraction, not a claim that all machine instructions take the same time. We initially treat numbers and indices as machine-sized. Later we account for long integers, copying and output. Reading a file, constructing the input and rendering a result are separate costs unless explicitly included.</Prose>
    <LessonTable caption="Name the parameter before writing the bound" headers={['Input / task', 'Useful size', 'Avoid this mistake']} rows={[
      ['One list', 'n elements; possibly their total byte size too', 'Counting only elements when each comparison scans a long string'],
      ['Two independent lists', 'n and m elements', 'Replacing n × m with n² without a relation between the sizes'],
      ['Graph already in adjacency lists', 'V vertices and E edges', 'Quoting a matrix scan as O(V + E) without charging matrix entries'],
      ['Positive integer N written in binary', 'b = floor(log₂ N) + 1 bits', 'Treating the numeric value N as the encoded input length'],
      ['Emit k answers', 'Input size plus total output size', 'Ignoring the time and storage needed to produce the answers'],
    ]}/>
    <Prose>A <strong>logarithm</strong> asks how many multiplicative steps are needed. Since 2 × 2 × 2 = 8, log₂ 8 = 3. Repeatedly halving 8 reaches 1 in three steps. Repeatedly doubling 1 reaches 8 in three steps. A non-power such as 6 falls between 4 and 8, so three doublings are needed to reach or exceed it. The ceiling symbol means round upward; floor means round downward.</Prose>
    <IterationLatticeLab/>
    <Prose>For the triangle, row i executes i times: row 0 is empty, row 1 has one event, and so on. Adding 0 + 1 + … + (n − 1) gives n(n − 1)/2. One way to see the sum is to reflect the triangular cells: two copies fill an n-by-(n − 1) rectangle. Each pair of distinct indices appears once, without a diagonal self-pair or its reversed duplicate.</Prose>
    <Prose>The doubling loop has n outer rows. Its inner indices are 1, 2, 4, … below n, giving ceil(log₂ n) calls per row for n ≥ 1. Hence it makes exactly n ceil(log₂ n) body calls. Two nested loops therefore need not mean quadratic work. What matters is how often the inner body executes, not the number of indentation levels.</Prose>
    <RunnableExample example={complexityRecursionExamples.loopCounts}><Prose>Each counter increments exactly at the work event shown in the lattice. The three counts are independent; this diagnostic program runs all three loops. A zero body count does not imply a function takes zero time: validation, loop setup and returning still occur.</Prose></RunnableExample>

    <H2>2. Turn counts into growth bounds</H2>
    <Prose>An exact formula is valuable, but often we want the enduring pattern: does doubling the input roughly double the work, quadruple it, or add only a few steps? A bound describes a whole family of sufficiently large inputs, after allowing a fixed positive scaling constant. “Sufficiently large” does not mean constants disappear on your machine; it means the statement eventually holds beyond one fixed threshold.</Prose>
    <LessonTable caption="Three different mathematical claims" headers={['Notation', 'Meaning for nonnegative costs', 'Example']} rows={[
      ['T(n) is O(g(n))', 'Eventually T(n) ≤ c·g(n) for some fixed c > 0', '3n + 7 is O(n), and also O(n²)'],
      ['T(n) is Ω(g(n))', 'Eventually T(n) ≥ c·g(n) for some fixed c > 0', '3n + 7 is Ω(n)'],
      ['T(n) is Θ(g(n))', 'Both an eventual upper and lower bound of this form', '3n + 7 is Θ(n); this is a tight growth class'],
    ]}/>
    <Prose>The threshold and constants must work for <em>every</em> later n. For 3n + 7 and n ≥ 1, the sandwich 3n ≤ 3n + 7 ≤ 10n proves Θ(n). For the pair count and n ≥ 2, n²/4 ≤ n(n − 1)/2 ≤ n²/2 proves Θ(n²). The exact half factor improves the count without changing the quadratic class. Conversely, n² is not O(n): n² ≤ cn would require n ≤ c for arbitrarily large n, impossible for a fixed c.</Prose>
    <Prose>People commonly write T(n) = O(n), but the equals sign does not mean equality with one function. Big O is an upper-bound class and can be loose. Prefer Θ when you have established both directions; otherwise state what you have actually shown. Little-o, as in n = o(n²), is a stronger comparison: the ratio tends to zero. It is optional notation here, not a synonym for O.</Prose>
    <LessonTable caption="How work composes under the stated model" headers={['Pattern', 'Reasoning', 'Typical bound']} rows={[
      ['Scan n items, then scan m', 'Add independent phases', 'Θ(n + m), including both parameters'],
      ['For every item in n, scan all m', 'n groups each containing m events', 'Θ(nm) body work'],
      ['Scan n items, then compare all pairs', 'n + n(n − 1)/2; quadratic eventually dominates', 'Θ(n²)'],
      ['Repeatedly halve the remaining search region; constant local work', 'Count the reductions to a base case', 'Θ(log n) in the longest path for n ≥ 2'],
      ['Produce all n-bit strings explicitly', '2ⁿ results, each containing n symbols', 'At least Ω(n2ⁿ) output work under one-symbol writes'],
    ]}/>
    <Prose>For fixed bases greater than 1, logarithms differ by a constant factor: logₐ n = logᵦ n / logᵦ a. The base can disappear from Θ(log n), but not from an exact iteration count. Likewise, n + m cannot become n if m varies independently. An expression such as O(n²) is incomplete until its size parameters, model and relevant case are clear.</Prose>
    <Checkpoint prompt="Someone says the triangle is O(n²), therefore twice as slow as a linear scan for every input. Which parts of that conclusion are unsupported?">
      <Prose>The bound describes eventual growth, not a fixed timing ratio. Its exact counted work is n(n − 1)/2, the scan has n accesses, and their per-operation costs need not match. Even a correct Θ statement does not give a measured crossover or hardware timing.</Prose>
    </Checkpoint>

    <H2>3. Separate the case from the bound</H2>
    <Prose>A search can stop early. Searching the same list for its first value, last value or an absent value produces different counts even though n is unchanged. Define T(x) for one actual input x. The <strong>worst case</strong> at size n is the maximum over valid inputs of that size; the best case is the minimum. Those are functions to which O, Ω or Θ can each be applied. O does not itself mean worst case, and Ω does not itself mean best case.</Prose>
    <RunnableExample example={complexityRecursionExamples.searchCases}><Prose>For a nonempty list, the successful first-position search makes one comparison; the last-position or unsuccessful search makes n. If the target occurs exactly once and each of the n positions is equally likely, expected successful comparisons are (1 + … + n)/n = (n + 1)/2. Change the distribution or include unsuccessful searches and that expectation changes.</Prose></RunnableExample>
    <Prose><strong>Expected</strong> cost needs a stated probability model: a distribution over inputs, random choices inside an algorithm, or both. A hash-table expectation does not follow from calling all data “typical.” <strong>Amortized</strong> cost answers a different question: how large can the total cost of a whole operation sequence be, even if individual operations are expensive? It need not involve randomness.</Prose>
    <RunnableExample example={complexityRecursionExamples.appendBudget}><Prose>This is an explicit capacity-doubling model, not CPython’s exact list growth policy. An append writes one item. When full, it additionally copies every old item to a doubled allocation. Nine appends cost 24 counted writes/copies; the ninth alone costs 9.</Prose></RunnableExample>
    <Prose>For m ≥ 1 appends, copied old capacities are 1 + 2 + 4 + … up to a power below m. Their sum is less than 2m. Add m new-item writes to get less than 3m total work, plus constant initialization. Thus appends cost O(1) amortized each in this model, while an individual resizing append can cost Θ(m). The expensive event is paid for by many earlier cheap events; it did not become cheap in isolation. Union-Find’s sequence bounds have the same distinction, although their proof and bound are different.</Prose>

    <H2>4. Recursion keeps unfinished work</H2>
    <Prose><strong>Recursion</strong> is a function solving its task using calls to itself on related, smaller tasks. The useful design question is not “where can I call myself?” It is “what smaller answer would let this call finish correctly?” For the total of [3, 1, 4], the first call can add 3 to the total of [1, 4]. We will represent a suffix by its starting index, keeping one shared list.</Prose>
    <Prose>Define suffix(i) to return the sum of values from index i through the end. The valid index is 0 ≤ i ≤ n. If i = n, there are no values left, so the answer is 0: the <strong>base case</strong>. Otherwise the result is values[i] + suffix(i + 1). Every non-base call reduces the nonnegative integer n − i by one. That <strong>progress measure</strong> proves that repeated calls reach the base case on a finite input.</Prose>
    <ReturnLadderFigure/>
    <Prose>A call frame stores the information needed to resume a call: its arguments and local state, plus where execution should continue. A parent waiting for a child has not vanished. The child returns a value to that particular waiting call; it does not return directly to all callers at once. A frame’s existence is also real storage, even when your source code contains no explicit stack object.</Prose>
    <RunnableExample example={complexityRecursionExamples.suffixSum}><Prose>The first function is the computation. The diagnostic companion exposes the same enter/call/base/combine events for the browser model. Copying snapshots is extra diagnostic work and storage, deliberately excluded from the simple function’s cost. No slice is created by the actual sum function.</Prose></RunnableExample>
    <RecursiveFramesLab/>
    <Prose>The correctness argument is small but essential. For zero remaining values, returning 0 is right. Assume the function correctly totals any suffix with k remaining values. A suffix with k + 1 values consists of its first value and a suffix of length k; adding that first value to the correct smaller answer gives the correct total. This is <strong>induction</strong>: establish the base, then show how a correct smaller result establishes the next case. Progress ensures the program reaches the cases on which the argument depends.</Prose>
    <Prose>There are n + 1 calls and n additions. Under constant-cost addition, time is Θ(n + 1) and peak auxiliary frame storage is Θ(n + 1). An ordinary loop also computes the sum in Θ(n + 1) work while keeping only a running total and index. Recursion expresses the decomposition; it does not automatically improve speed or memory.</Prose>
    <Checkpoint prompt="Does `if n == 0: return 0` guarantee that a recursive function terminates? Consider a call that next uses n + 1, or subtracts 2 from every positive n.">
      <Prose>No. From a positive n, adding one moves away from zero. Subtracting two reaches zero only for even starts; an odd start passes through one to negative values. State the allowed domain and show that every recursive branch reaches a valid base. Use a measure that strictly decreases in a well-founded set, such as nonnegative integers; merely getting smaller in the real numbers is insufficient.</Prose>
    </Checkpoint>

    <H2>5. Add work across the recursion tree</H2>
    <Prose>A <strong>recurrence</strong> describes a cost using the costs of smaller instances. It is not the returned answer. For a chain of calls, T(n) = T(n − 1) + 1 could count one unit of local work plus one smaller call, with T(1) = 1. Expanding gives 1 + 1 + … + 1 = n. Always state the base, child sizes, number of children and local work before solving the equation.</Prose>
    <Prose>Halving affects depth; branching and work per call determine total work. One half-size call with constant local work costs logarithmically many units. One half-size call that scans its whole current region costs n + n/2 + n/4 + … + 1 = 2n − 1 for power-of-two n. It has logarithmic depth but linear total work. This is why replacing an index range by a copied half-list can change the analysis.</Prose>
    <RecurrenceLevelsLab/>
    <LessonTable caption="Exact toy results when n is a power of two and T(1)=1" headers={['Recurrence', 'Expansion result', 'Reason']} rows={[
      ['T(n−1) + 1', 'n', 'n levels each cost one'],
      ['T(n−1) + n', 'n(n+1)/2', 'A descending arithmetic sum'],
      ['T(n/2) + 1', 'log₂ n + 1', 'One unit at each level, including the leaf'],
      ['T(n/2) + n', '2n − 1', 'A geometric sum; the top dominates'],
      ['2T(n/2) + n', 'n log₂ n + n', 'Every non-leaf level costs n, and all leaves cost n'],
    ]}/>
    <RunnableExample example={complexityRecursionExamples.recurrences}><Prose>These programs sum declared work units, not timed instructions. The input validator and counters also execute instructions, so their runtime is not literally the returned number. The independent model checks compare these sums with separately derived formulas and explicit call trees.</Prose></RunnableExample>
    <H3>Two branches do not necessarily mean exponential work</H3>
    <Prose>Now divide a sum into two disjoint index ranges and combine their totals. Each of n values belongs to exactly one leaf. A full binary call tree with n leaves has n − 1 internal calls: each internal call creates two child edges, while any tree with C total calls has C − 1 edges. Solving 2(C − n) = C − 1 gives C = 2n − 1. Constant work per call therefore gives Θ(n) time, even though each internal call makes two recursive calls.</Prose>
    <RunnableExample example={complexityRecursionExamples.balancedSum}><Prose>The five-value example has nine visit calls and four simultaneously live visit frames at its deepest point. The eight-value example has fifteen visit calls, still only four peak visit frames. These counters exclude the fixed wrapper frame. Index intervals are half-open: start is included, stop is excluded. Empty input is handled before accessing an item, and an odd interval splits into two smaller nonempty parts.</Prose></RunnableExample>
    <Prose>For sequential evaluation the left child finishes before the right child runs. Peak stack follows a deepest root-to-leaf path, not the sum of all calls on a level. Balanced splitting gives O(log(n + 1)) auxiliary frames with constant-sized local state. This algorithm changes the grouping of additions: for floating-point readings that can change rounding, so exact-integer agreement does not promise bit-identical floating-point results. A plain iterative sum is usually simpler when all you need is a total; the split is useful for learning reductions and for later parallel designs, whose communication costs need separate analysis.</Prose>
    <H3>A half-result can be used more than once</H3>
    <Prose>To compute x to an integer power e, compute h = x to the power floor(e/2). If e is even, the result is h × h. If e is odd, it is h × h × x. The key is to compute h <em>once</em>. For 2¹³, the exponent chain is 13 → 6 → 3 → 1 → 0. On return the values are 1, 2, 8, 64 and 8192. Reusing a result is different from making two identical recursive calls.</Prose>
    <RunnableExample example={complexityRecursionExamples.power}><Prose>The result tuple is (value, entered calls, multiplications). This contract accepts nonnegative integer exponents and uses the empty-product convention x⁰ = 1, including base zero. Its count is O(log(e + 1)) multiplications and frames. It is not a claim of logarithmic bit-time for growing exact integers. The guided platform extension separately introduces negative exponents and its own zero-base restrictions.</Prose></RunnableExample>
    <details className="lesson-check"><summary>Deeper: verify a recurrence guess and recognize the Master Theorem’s limits</summary>
      <Prose>For T(n)=2T(n/2)+n with T(1)=1, guess n log₂ n+n. Substitute the smaller expression: 2[(n/2)log₂(n/2)+n/2]+n = n(log₂ n−1)+n+n = n log₂ n+n. Together with the base case and smaller subproblems this proves the exact formula on powers of two; checking a few numbers alone would not.</Prose>
      <Prose>The Master Theorem packages similar comparisons for T(n)=aT(n/b)+f(n), fixed a≥1 and b&gt;1, and positive constant base cost. Compare local work f(n) with n raised to logᵦ a, the scale of the leaf count. In its common form: polynomially smaller f gives Θ(n^(logᵦ a)); equal-order f gives Θ(n^(logᵦ a) log n); polynomially larger f gives Θ(f(n)) provided the regularity condition a f(n/b) ≤ c f(n) eventually holds for some c&lt;1. “Polynomially” means a fixed positive exponent gap, not merely a smaller ratio.</Prose>
      <Prose>These conditions matter. T(n−1)+n does not have a fixed-factor shrink. T(n)=T(n/3)+T(2n/3)+n has unequal child sizes. Terms between the theorem’s cases need another argument. Expansion, level sums and substitution remain useful; a memorized theorem cannot repair an incorrectly modeled algorithm. Floors, ceilings and non-power sizes also require reasoning about the actual split rather than asserting an exact power-of-two formula for every n.</Prose>
    </details>

    <H2>6. Count storage and hidden operations</H2>
    <Prose><strong>Auxiliary space</strong> is extra working storage beyond the input, under a declared convention. State output storage separately when it matters. Peak live storage differs from the total allocation over a run: memory used at disjoint times can be reused. Count live call frames, buffers, cached answers and referenced objects that cannot yet be released.</Prose>
    <Prose>Replacing the suffix index with <Code>values[1:]</Code> looks convenient. A Python list slice creates a new list of references. In a chain such as “first value + sum of sliced suffix,” callers retain their own lists while waiting. At the deepest point, copied lengths n−1, n−2, …, 0 coexist. That creates Θ(n²) copied reference slots in total and at peak, plus Θ(n) frames; it also adds Θ(n²) copying work. The element objects themselves are shared unless you explicitly deep-copy them.</Prose>
    <SliceStorageFigure/>
    <Prose>By contrast, passing index bounds stores a constant amount per frame and refers to one original list. Returning a newly constructed reversed list still needs Θ(n) output slots even if its working indices use O(1) extra space. A recursive two-end swap uses Θ(n) frames; the corresponding loop avoids those frames while keeping the same swap count.</Prose>
    <RunnableExample example={complexityRecursionExamples.reverse}><Prose>After each swap, positions outside the two indices already hold their final mirrored values. The remaining interval shrinks by two. There are floor(n/2) swaps, Θ(n + 1) work and O(1) auxiliary machine-word state for the algorithm. The fixture’s original-copy list is only for displaying the before/after result and is not part of that algorithm.</Prose></RunnableExample>
    <Prose>Python places a limit on interpreter recursion depth; the precise limit and safe stack usage are environment-dependent. A mathematically terminating chain can still raise RecursionError. Raising the limit is not a replacement for bounding depth. An explicit stack can handle deeper inputs, but retains whatever unfinished state the algorithm needs. Python does not promise that writing a tail call will remove its frame.</Prose>
    <Prose>Finally, a Python “operation” may itself process variable-size data. Comparing long equal strings reads their characters; concatenating lists copies references; exact-integer addition depends on the number of bits. The number 2ᴺ has N+1 binary digits. A function that performs O(log N) arithmetic operations to produce a huge exact result cannot be assumed to use O(log N) bit operations or bytes. Name the abstraction and revisit it when size becomes material.</Prose>

    <H2>7. Repeated subproblems and measurement</H2>
    <Prose>Fibonacci makes the difference between calls and distinct tasks visible. With F(0)=0, F(1)=1 and F(n)=F(n−1)+F(n−2), a direct recursive function repeats the same smaller indices. In F(5), F(3) is requested by both F(4) and the top-level call. The recursion is correct and terminates, but correctness does not prevent redundant work.</Prose>
    <RunnableExample example={complexityRecursionExamples.fibonacci}><Prose>The tuple is (naive result, memoized result, iterative result, naive calls, newly computed non-base cached states). At n=8, 67 naive calls produce only the value 21. Caching computes seven non-base states, and the loop retains just two successive values. The comparison deliberately caps n at 20 so a learner cannot accidentally request an enormous naive call tree.</Prose></RunnableExample>
    <Prose>Naive call count obeys C(n)=C(n−1)+C(n−2)+1, with C(0)=C(1)=1. It equals 2F(n+1)−1. Repeatedly taking both branches gives exponential growth; O(2ⁿ) is a valid upper bound, not its tightest description. Yet the deepest active path remains linear in n. Exponential total calls and linear peak frame depth can coexist.</Prose>
    <Prose><strong>Memoization</strong> stores an answer by its subproblem key and reuses it. For Fibonacci there are only n+1 possible indices, and each new state does constant many lookups/additions under our unit-cost model. This gives Θ(n) arithmetic work and O(n) integer objects plus O(n) recursive frames. Iteration uses Θ(n) arithmetic work with O(1) integer variables. Since Fibonacci values grow to Θ(n) bits, those few variables do not occupy constant bit storage for unbounded n. The full cache stores Θ(n²) bits in its values, and the additions no longer have constant bit cost.</Prose>
    <Prose>Caching is appropriate only when the key determines the reusable answer and its lifetime fits the task. A function reading changing external state, a mutable argument whose contents changed, or a cache that retains every enormous result needs further design. The later Dynamic Programming lesson develops sufficient state, transitions, evaluation order and reconstruction; this example establishes why reuse matters.</Prose>
    <H3>Use experiments to challenge your model</H3>
    <Prose>The lattice and recurrence bars are <strong>calculated counts</strong>. They are not stopwatch measurements. To investigate actual performance, first verify equal outputs, choose representative sizes and cases, and decide whether setup belongs in the timed task. Time the actual function without printing or trace snapshots. Repeat measurements, retain the raw times, and record Python version, machine, input construction and whether garbage collection is included.</Prose>
    <Prose>Python’s timeit can repeat a small computation and keep setup outside its timed statement; it normally disables garbage collection during timing. That can help isolate a small operation but may omit a cost relevant to an allocation-heavy workload. A doubling ratio near two may be compatible with linear growth over those inputs; finite measurements cannot prove an asymptotic class, especially when setup, cache effects or noise dominate. No universal nanoseconds-per-operation rule or implementation ranking is implied here.</Prose>

    <H2>8. Practise explanations that survive a change</H2>
    <Checkpoint prompt="For n=8, an outer while loop uses size=8,4,2,1. At each size it performs size work calls. What is the exact total, its tight growth for powers of two, and the error in saying ‘nested loops means n log n’?">
      <Prose>The total is 8+4+2+1=15, and generally 2n−1, hence Θ(n). There are logarithmically many rows, but their costs are not all n. Summing their actual decreasing lengths is tighter than multiplying n by the number of rows.</Prose>
    </Checkpoint>
    <Checkpoint prompt="A recursive function on a positive integer returns at 0 but otherwise calls itself with n // 2 + 1. Find a counterexample and repair the progress argument, not just the observed output.">
      <Prose>At n=2 the next input is still 2; at n=1 it remains 1. A base case for only zero is unreachable. If the intended task allows stopping at n≤1, removing the +1 and using n//2 gives a strictly smaller nonnegative integer for every n≥2. The actual combine rule and base answer still need a correctness argument; changing the recurrence alone does not establish the desired result.</Prose>
    </Checkpoint>
    <Checkpoint prompt="An algorithm scans all n items on one branch and scans m items on another. No probability model is given. State a worst-case upper bound and explain why you cannot automatically add the branches or claim an expected n/2.">
      <Prose>Only one branch executes, so work is at most proportional to max(n,m), plus the branch decision, if that decision is constant-cost. O(n+m) is also a valid looser expression and equivalent up to a constant when both are nonnegative, but n/2 is an unsupported probabilistic claim. To establish a tight worst-case bound, show that an input can trigger the large branch and all its work.</Prose>
    </Checkpoint>
    <Checkpoint prompt="A suffix-copy recursion handles n=5. How many additional element-reference slots coexist at the deepest call? How would halving by slices change the sum?">
      <Prose>The copies contain 4+3+2+1+0=10 slots, in addition to the original five, list headers and frames. In a one-child halving recursion, retained copied lengths are at most roughly n/2+n/4+…&lt;n, so copied slots are O(n), not Θ(n²). Its depth may be logarithmic while copied work and storage are linear.</Prose>
    </Checkpoint>
    <H3>Independent implementation: count readings below a threshold</H3>
    <Prose>Given a finite list of signed integers and a threshold, return how many values are strictly below it. Preserve the input, support empty and odd-length lists, and split by index ranges rather than copies. Before writing, state what visit(start, stop) promises, its base cases, why every child is smaller, how child counts combine, and the time/peak-space bound. Do not use sortedness: the data may be in any order.</Prose>
    <details className="lesson-check"><summary>Hint: choose the smallest complete cases</summary><Prose>An empty interval contributes zero. A singleton contributes one exactly when its element meets the condition. Two disjoint smaller intervals partition the remaining positions, so their counts add without double-counting.</Prose></details>
    <details className="lesson-check"><summary>Worked solution and acceptance checks</summary>
      <RunnableExample example={complexityRecursionExamples.thresholdCount}/>
      <Prose>For [4, −2, 4, 9, 0] at threshold4, only −2 and 0 qualify: result2. Equality must not count. Compare with an independent direct loop for many thresholds and lists; include empty, singleton, all equal, negative and odd lengths, and verify the list is unchanged. Each item is checked once and internal counts combine once, giving Θ(n+1) time; balanced index splits use O(log(n+1)) frames, with constant-sized local state under the model. An iterative scan is also correct and uses less auxiliary frame space; the recursive version practises the decomposition rather than claiming a speedup.</Prose>
      <Prose>Transfer: return both count and sum of qualifying readings in one traversal. Explain how each base and combine result changes. Returning the entire list of qualifying values is a different storage contract: include output length and account for how lists are concatenated.</Prose>
    </details>
    <Prose>Before moving on, derive the triangle count without looking, explain which frame is waiting in the sum lab, distinguish five calls from five simultaneously live frames, and diagnose why a halving algorithm can still be linear. Then take one earlier tree or graph routine and write its input, operation, case, work and peak-storage contract. If you needed a hint, close it and reconstruct the reasoning later with different inputs.</Prose>
    <Prose>Next in this module is <a href="/learn/path/full-curriculum/binary-search-sorting-two-pointer-patterns?module=data-structures-algorithms">Binary Search, Sorting &amp; Two-Pointer Patterns</a>. These tools will let you justify shrinking a search interval, compare sorting methods and analyze moving boundaries using the cost and progress arguments developed here. The sequence remains the module’s order.</Prose>
    <DsaPractice practice={complexityPractice}/>
    <Sources alternatives={<ul>
      <li><a href="https://introcs.cs.princeton.edu/python/23recursion/" target="_blank" rel="noreferrer">Princeton — Recursion</a>: a written alternative with call traces and applications; revisit after the frame investigation. Some examples use older supporting libraries; this lesson’s examples are self-contained.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-1-algorithmic-thinking-peak-finding/" target="_blank" rel="noreferrer">MIT OCW — Algorithmic Thinking and Peak Finding</a>: video plus linked lecture notes showing how smaller subproblems change work. The notes were inspected; older timing illustrations are not predictions for your machine.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/1869dbf640ded6b31f1bd369d2001ef5_MIT6_006S20_r03.pdf" target="_blank" rel="noreferrer">MIT 6.006 — Recitation 3</a>: written recurrence methods and Master Theorem practice; use the recurrence section after the level accountant. Earlier sorting material is a useful preview of the next lesson.</li>
    </ul>}>
      <li><a href="https://algs4.cs.princeton.edu/14analysis/" target="_blank" rel="noreferrer">Princeton — Analysis of Algorithms</a>: cost models and mathematical versus experimental analysis. Java object sizes and historical machine timings are context-specific.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/c6d8f06c6f11e3342633dec85498f551_MIT6_006S20_r01.pdf" target="_blank" rel="noreferrer">MIT — Recitation 1 asymptotic notation</a>: definitions and input-model distinctions for a more formal review.</li>
      <li><a href="https://docs.python.org/3/library/sys.html#sys.getrecursionlimit" target="_blank" rel="noreferrer">Python — interpreter recursion limit</a>: current environment-dependent contract. Raising the limit does not justify unbounded recursion.</li>
      <li><a href="https://docs.python.org/3/library/timeit.html" target="_blank" rel="noreferrer">Python — timeit</a>: setup, repeat and garbage-collection behavior for your own measurements; no fixed timing output is promised.</li>
    </Sources>
  </div>,
};
