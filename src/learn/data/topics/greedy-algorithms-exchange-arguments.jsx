import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CoinFailureFigure, DeadlineExchangeLab, FractionalCapacityLab, HuffmanMergeFigure, IntervalExchangeFigure, IntervalSelectionLab, ReachablePrefixFigure, RoomsLowerBoundFigure } from '../../components/lesson-labs/GreedyExchangeLabs.jsx';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { greedyExchangeExamples } from '../greedy-exchange-examples.js';
import greedyPractice from '../practice/greedy-algorithms-exchange-arguments.js';

export default {
  title: 'Greedy Algorithms & Exchange Arguments',
  readTime: '~60 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot greedy-exchange-lesson">
    <LessonIntro prerequisites="The preceding Backtracking lesson's choices, feasible completions and counterexamples; Complexity's invariants, induction and cost models; sorted scans from Binary Search/Sorting. Rooms and Huffman revisit the earlier heap operations. All proof steps needed here are explained locally."
      sections={[
        ['1-a-choice-is-not-yet-a-guarantee', 'Separate the rule from its guarantee'],
        ['2-keep-the-most-appointments', 'Select intervals and exchange a choice'],
        ['3-assign-every-appointment', 'Meet a resource lower bound'],
        ['4-swap-neighbors-to-control-lateness', 'Derive deadline order'],
        ['5-exchange-equal-weight', 'See why divisibility matters'],
        ['6-merge-small-parts-into-a-code', 'Prove a structural greedy merge'],
        ['7-certify-reach-without-committing-to-a-path', 'Maintain a reachable prefix'],
        ['8-choose-a-proof-and-test-its-boundaries', 'Recognize the proof and its limits'],
        ['9-prove-a-new-choice', 'Independent proof practice'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      A greedy algorithm commits to a local choice and continues without trying every alternative. Learn when that commitment is safe, how to transform an optimal answer to include it, and how a small change in the problem can destroy the guarantee. The central skill is a proof you can adapt, not a list of rules to memorize.
    </LessonIntro>

    <H2>1. A choice is not yet a guarantee</H2>
    <Prose>You need to make an amount of 6 using coin values 1, 3 and 4, with as many coins of each kind as you need. A natural rule takes the largest coin that fits: first 4, then 1, then 1. It makes the right amount, but two 3s use fewer coins.</Prose>
    <CoinFailureFigure />
    <Prose>A <strong>feasible</strong> answer satisfies the constraints. An <strong>optimal</strong> answer has the best objective value among all feasible answers. Here feasibility means an exact total of 6; the objective is the smallest coin count. “The answer fits” proves only the first property. A <strong>greedy rule</strong> selects what looks best according to a local criterion, commits to it, and reduces the remaining task. The word greedy describes this decision style; it does not certify its correctness.</Prose>
    <RunnableExample example={greedyExchangeExamples.coins}><Prose>The result stores (coin value, multiplicity) pairs plus the unpaid remainder. Without denomination 1, the same rule can get stuck even when a solution exists: taking 4 toward 6 leaves 2, yet 3+3 was possible. Returning a nonzero remainder means this rule failed, not that the original problem is infeasible.</Prose></RunnableExample>
    <Prose>The previous backtracking method could enumerate the finite positive-coin combinations and choose a shortest one. That is a correct small-input baseline, but the search may be expensive. Greedy tries to eliminate the need to explore alternatives. The burden is to show that eliminating them does not eliminate every optimum. Small brute-force comparisons help discover failures; no finite collection of passing examples proves a rule for all inputs.</Prose>
    <Prose>The key claim will often be: <strong>there exists an optimal solution consistent with this choice</strong>. You do not need to show that every optimum uses it. Ties may permit several different optimal answers. After choosing, you must also describe the remaining subproblem and explain why solving it optimally completes an optimum for the original problem.</Prose>
    <Prose>Every code block is a complete program using Python 3.10 or newer and its standard library; the examples were executed with Python 3.12. Save one as <Code>greedy_example.py</Code> and run <Code>python greedy_example.py</Code>. Browser diagrams use bounded teaching models; they do not execute arbitrary Python or measure production performance.</Prose>
    <Checkpoint prompt="A greedy selection cannot accept any more requests. Does that establish that it accepts the largest possible number?">
      <Prose>No. Such a selection is <em>maximal</em>: no further item can be added without changing what is already selected. A <em>maximum</em> selection has the greatest possible size. One long appointment may block two shorter appointments, making a one-item selection maximal but smaller than the maximum. The distinction is about the objective, not a spelling preference.</Prose>
    </Checkpoint>

    <H2>2. Keep the most appointments</H2>
    <Prose>A shared instrument can run one booked experiment at a time. Each request already has a start and finish; you may accept or reject it, but may not move it. Accept as many requests as possible. A request occupies the half-open interval [start,finish): it includes the start and excludes the finish. Thus an experiment ending at 3 can be followed by another starting at 3. Durations are strictly positive, and every accepted request initially contributes one to the objective.</Prose>
    <Prose>Possible rules include earliest start, shortest duration and earliest finish. Earliest start can consume the entire morning. Shortest duration can sit across the boundary between two compatible longer requests. Earliest finish has a different rationale: after taking one request, end as early as possible to leave the future least constrained. That is an intuition; we still owe the proof.</Prose>
    <IntervalSelectionLab />
    <Prose>On the initial requests, earliest finish keeps B [1,3), C [3,5), D [5,7) and F [7,9): four experiments. Earliest start keeps A [0,6) and E [6,9): two. For the shortest-duration trap, choosing [3,5) blocks both [0,4) and [4,8), even though those two fit together. The investigation checks every tiny feasible subset to expose such failures; the actual greedy implementation does not need that oracle.</Prose>
    <H3>Exchange the first choice of an optimum</H3>
    <Prose>Let G be a request with the earliest finish. Consider any optimal schedule, listed in chronological order, and call its first request O. If it already starts with G, the choice is compatible with an optimum. Otherwise replace O by G. G finishes no later than O, so every later request that fitted after O still fits after G. The replacement keeps the number of selected requests unchanged.</Prose>
    <IntervalExchangeFigure />
    <Prose>G cannot be a different later request already in that optimal schedule: a positive-duration later request finishes strictly after O, whereas G finishes no later. So the replacement does not duplicate an occurrence. We have constructed an optimum beginning with G. Its remaining requests all start at or after G's finish. If that remaining subproblem had a better solution than the chosen tail, substituting it would improve the original optimum, a contradiction.</Prose>
    <Prose>Apply the same reasoning to the residual requests. Each choice fixes one accepted request and removes a finite set of considered requests, eventually leaving none. Induction on the number remaining proves the full algorithm. Equal finish times are safe; any earliest-finishing request permits the same first-choice argument. A deterministic tie rule helps reproducibility but does not make the answer unique.</Prose>
    <RunnableExample example={greedyExchangeExamples.intervals}><Prose>Sort by finish, then start and ID for deterministic ties. At each scan position, the kept requests are compatible and last_finish is the end of the latest one. A candidate fits precisely when its start is at least that boundary. The None initial value handles negative start times correctly; zero would incorrectly exclude requests before time zero.</Prose></RunnableExample>
    <Prose>Sorting takes O(n log n) comparisons for n requests; the scan is O(n). This Python implementation allocates the sorted list and selected IDs, so total additional storage is O(n), with constant state in the scan itself. The input is not mutated. Returning the minimum number removed is n minus the maximum number kept; duplicate intervals still represent separate requests.</Prose>
    <H3>A second proof: greedy stays ahead</H3>
    <Prose>Compare the j-th greedy request Gⱼ to the j-th request Oⱼ of an arbitrary feasible schedule. The quantity “ahead” means <em>finishes no later</em>, not earns more reward. For j=1 this follows from the choice. If Gⱼ₋₁ finishes no later than Oⱼ₋₁, then Oⱼ is available after Gⱼ₋₁ because it starts after Oⱼ₋₁ finishes. Greedy's next earliest finish is therefore no later than Oⱼ's finish. This proves the statement by induction.</Prose>
    <Prose>If another schedule had more requests when greedy stopped, its next request would still fit after greedy's final finish, contradicting the stopping condition. The comparison must hold at every aligned step and imply the final objective; saying “greedy looks ahead of this example” is not the argument.</Prose>
    <Checkpoint prompt="Give A [0,5) value 10, B [0,2) value 4 and C [2,5) value 4. Which objective does earliest finish solve, and which exchange step fails for the other objective?">
      <Prose>It keeps B and C, maximizing count at two. Their total value is 8, below A's 10. Replacing A with B preserves feasibility but loses value; there is no reason to call the exchanged solution optimal under the weighted objective. The weighted interval problem has the same-looking input geometry but a different proof obligation. The next Dynamic Programming lesson develops how to compare such alternatives.</Prose>
    </Checkpoint>

    <H2>3. Assign every appointment</H2>
    <Prose>Now every experiment must run at its requested time, and you may provide several identical instruments. Minimize the number of instruments needed. You are assigning requests to resources, not selecting which requests to keep. A sorting rule that was wrong for selection can be right for assignment: consider requests by increasing start time and reuse any instrument whose previous request has finished.</Prose>
    <RoomsLowerBoundFigure />
    <Prose>A set of d simultaneous requests needs at least d resources, whatever clever algorithm you use. This is a <strong>lower bound</strong> on the optimum. Suppose the algorithm opens its d-th resource for a request starting at time t. The other d−1 resources all have requests ending after t; because start times are processed in order, those requests have already started. Together with the new request, d requests overlap at t. Thus every possible assignment needs at least d resources, and the algorithm achieves exactly that many.</Prose>
    <RunnableExample example={greedyExchangeExamples.rooms}><Prose>The heap contains one last-finish time per allocated resource. Its minimum tells us whether any resource is free. If it is, pop that resource and push its new finish; otherwise allocate another. Several resources may already be free, but reusing one is sufficient for this request. The assigned IDs preserve every request exactly once.</Prose></RunnableExample>
    <Prose>Sort plus O(n) heap operations costs O(n log n) time, with O(n) recorded assignments and up to O(n) heap entries. This guarantee assumes fixed intervals, identical interchangeable resources and no setup or travel time. If a machine needs cleaning between experiments, or only some machines support a request, the compatibility model and proof need to change.</Prose>
    <LessonTable caption="The word interval does not identify the algorithm" headers={['Task', 'What must be preserved?', 'Reason for the method']} rows={[
      ['Merge a union, recalled', 'Every covered point', 'Start order reveals which last component can grow'],
      ['Select a maximum count', 'A largest compatible subset', 'Earliest finish can replace the first choice of an optimum'],
      ['Assign all requests to fewest rooms', 'Every request on one compatible resource', 'Start order creates an overlap witness for every new room'],
      ['Maximize selected value', 'Greatest total reward', 'Count-preserving exchange is insufficient; compare richer alternatives'],
    ]} />

    <H2>4. Swap neighbors to control lateness</H2>
    <Prose>A single processor must complete all jobs. Each job has a processing duration p and deadline d, and all jobs are available at time zero. A job runs without interruption, and you choose the order. If it completes at time C, its <strong>lateness</strong> is L=C−d. A value −2 means two units early. Its <strong>tardiness</strong> is max(0,L), which clips early completion to zero. We will minimize the maximum signed lateness; the same ordering also minimizes maximum tardiness.</Prose>
    <Prose>There is an optimal schedule with no idle time: removing a gap makes subsequent completions earlier and cannot worsen either objective, because every job was available already. The proposed rule is <strong>earliest deadline first</strong>, also called earliest due date order in this offline setting. Do not confuse it with shortest processing time or with choosing whichever job is currently available in a different online scheduling problem.</Prose>
    <DeadlineExchangeLab />
    <H3>Derive what an adjacent swap can change</H3>
    <Prose>Suppose neighboring jobs A then B begin at time t, but dA &gt; dB. This is a deadline inversion: the later deadline comes first. Swap them to B then A. Earlier jobs are unchanged. The pair still consumes pA+pB time, so every later job starts and finishes at exactly the same time.</Prose>
    <LessonTable caption="Only two completion times change" headers={['Job', 'Before A→B', 'After B→A', 'Consequence']} rows={[
      ['B', 't+pA+pB', 't+pB', 'B finishes earlier, so its lateness cannot increase'],
      ['A', 't+pA', 't+pB+pA', 'A may finish later, but its new lateness is bounded by B’s old lateness'],
    ]} />
    <Prose>For A, the new lateness is t+pB+pA−dA. Since dA≥dB, this is at most t+pA+pB−dB, which was B's old lateness. Both new latenesses are therefore at most the old pair's maximum. The global maximum cannot increase, even though A individually becomes later. The proof needs a bound on the objective, not improvement of every component.</Prose>
    <Prose>If an order is not nondecreasing by deadline, some adjacent pair is inverted. Removing such an inversion by a swap decreases the finite inversion count. Repeat until deadlines are ordered. Starting from an optimum, each exchange preserves feasibility and does not worsen the score, so the final earliest-deadline schedule is also optimal. Equal-deadline swaps are safe as well; multiple optimal orders may exist.</Prose>
    <RunnableExample example={greedyExchangeExamples.deadlines}><Prose>The algorithm sorts directly; it does not simulate the proof's potentially quadratic sequence of adjacent swaps. Sorting costs O(n log n), followed by an O(n) schedule construction. Stored output and sorted data take O(n) space. The empty schedule returns None for maximum lateness because there is no completed job to maximize over; it is not silently assigned a fictitious zero-lateness job.</Prose></RunnableExample>
    <Prose>Changing the objective changes the rule. To minimize the <em>sum</em> of completion times when all jobs are available at zero, an adjacent pair with pA&gt;pB can be swapped to reduce that sum by pA−pB; shortest processing time order follows instead. To minimize the <em>number</em> of tardy jobs, earliest deadline order alone is insufficient. To handle release times, moving a job earlier may be infeasible. These are separate scheduling problems, not interchangeable uses of one library function.</Prose>
    <Checkpoint prompt="In A then B, let t=0, pA=4, dA=9, pB=3 and dB=5. Compute both latenesses before and after the exchange. Must each one improve?">
      <Prose>Before: A completes at 4 with lateness −5; B at 7 with lateness 2. After: B completes at 3 with lateness −2; A at 7 with lateness −2. A's lateness increases from −5 to −2, yet the maximum decreases from 2 to −2. With a later job C, its completion remains fixed and may still determine the overall maximum.</Prose>
    </Checkpoint>

    <H2>5. Exchange equal weight</H2>
    <Prose>Suppose a capacity-limited load can include amounts of several materials. Material i has total available weight wᵢ and total value vᵢ. Taking fraction xᵢ between 0 and 1 uses xᵢwᵢ capacity and gives xᵢvᵢ value. Values scale linearly; weights are positive and values nonnegative. The goal is greatest total value with total weight at most W. This is <strong>fractional knapsack</strong>.</Prose>
    <Prose><strong>Density</strong> vᵢ/wᵢ means value per unit of capacity. Taking material with density 6 instead of density 4 earns two more value units for each weight unit exchanged. Sort by decreasing density and fill as much as possible from each material. At most one positive-weight material is partly taken before capacity fills.</Prose>
    <FractionalCapacityLab />
    <H3>The exchange conserves the scarce resource</H3>
    <Prose>Consider an optimal allocation that differs from the density-first one at the first material in that order. Suppose it uses less of that higher-density material. If it has spare capacity, adding some cannot reduce value. Otherwise it uses some lower-or-equal-density material that can be reduced. Exchange a small weight amount ε from that material into the higher-density one, stopping when the missing amount is filled or the lower-density amount runs out.</Prose>
    <Prose>Weight stays unchanged, every fraction stays between zero and one, and value changes by ε × (higher density − lower density), which is nonnegative. Repeating such exchanges can make the first amount agree with greedy without worsening an optimum. Then fix that amount and repeat for the remaining materials. This makes the whole greedy allocation consistent with an optimum. Equal densities permit multiple optimal mixtures.</Prose>
    <RunnableExample example={greedyExchangeExamples.fractional}><Prose>Python's fractions.Fraction gives exact rational densities and contributions instead of relying on floating-point tie comparisons. The browser uses bounded integers and rational records for the same reason. Fractional capacity 50 gives 60+100+80=240. With whole items, the same density order takes A and B for value 160; B and C fit exactly and give 220.</Prose></RunnableExample>
    <Prose>The whole-item task is <strong>0/1 knapsack</strong>: each item is either absent or present once. The equal-weight exchange may demand taking 2/3 of C, which is illegal. The problem can still have optimal substructure; what failed is the specific greedy-choice property. The next Dynamic Programming lesson develops states that retain the alternatives the greedy rule threw away.</Prose>
    <Prose>With constant-cost density comparisons, sorting plus allocation costs O(n log n) time and O(n) stored output/sorted data. Exact Python fractions additionally have numerator/denominator arithmetic costs; a bound in arithmetic operations is not a constant bit-cost promise. Practical allocation also needs its model checked: indivisible packets, startup costs, dependencies, volume constraints or diminishing benefit cannot be treated as divisible material with linear value merely because there is a capacity limit.</Prose>
    <Checkpoint prompt="Why does exchanging equal value fail to establish the density rule? What changes when two materials have equal density?">
      <Prose>The scarce resource is weight. Equal-value replacements may use different weight and violate capacity. Exchanging equal weight keeps feasibility fixed and exposes the value difference. Equal-density exchanges change value by zero, so any feasible mixture with the appropriate total weight across that tied group can be equally good; uniqueness is not required.</Prose>
    </Checkpoint>

    <H2>6. Merge small parts into a code</H2>
    <Prose>A greedy choice need not mean “take the biggest item and move on.” Suppose a message uses symbols A, B, C and D with frequencies 2, 3, 7 and 9. We want binary codewords that use few total payload bits. Frequent symbols benefit from short codewords. But assigning every symbol the same short string would make decoding ambiguous.</Prose>
    <Prose>A <strong>prefix-free</strong> code has no symbol's codeword as the beginning of another symbol's codeword. Draw a binary tree with symbols only at leaves: read 0 on each left edge and 1 on each right edge. Because no leaf is an ancestor of another leaf, its code cannot be a prefix of another. The code length is its depth. The total payload cost is the sum of frequency × depth across all leaves.</Prose>
    <HuffmanMergeFigure />
    <H3>Why the two least frequent symbols can be siblings</H3>
    <Prose>With at least two positive-frequency symbols, an optimal binary prefix tree can have two children at every internal node: removing an unnecessary single-child edge would shorten codes and reduce cost. Take a deepest sibling-leaf pair. If a less frequent symbol is at a shallower depth than a more frequent one, moving the less frequent symbol deeper and the more frequent symbol shallower changes cost by (smaller frequency − larger frequency) × (deeper depth − shallower depth), which is nonpositive.</Prose>
    <Prose>By exchanging leaf labels, we can place the two minimum-frequency symbols at that deepest sibling pair without worsening an optimum. Their bitstrings need not be fixed in advance; the shape and the leaf-label exchange establish that <em>some</em> optimum contains this pair. Ties can give different trees with the same weighted cost.</Prose>
    <Prose>Contract those two leaves of frequencies a and b into one leaf of frequency a+b. Expanding it later adds one edge to each of its occurrences, a fixed extra cost a+b. Therefore an optimal reduced tree expands into an optimal tree with the chosen siblings: any cheaper reduced tree would imply a cheaper original tree. Repeatedly merging the two smallest remaining weights is <strong>Huffman's algorithm</strong>.</Prose>
    <RunnableExample example={greedyExchangeExamples.huffman}><Prose>The heap entries include a serial number, so equal frequencies never require comparing Node objects. The deterministic policy here gives D code 0, C code 11, A code 100 and B code 101. Encoding DAC gives 010011; traversal decodes it back to DAC. Each leaf completes one symbol, then decoding restarts at the root.</Prose></RunnableExample>
    <Prose>The merge costs 5+12+21 total 38, equal to 2×3+3×3+7×2+9×1. Why? Each merge increases by one the depth of every leaf it contains, contributing exactly that merged subtree's total frequency. This identity connects the local steps to the global objective.</Prose>
    <Prose>For k symbols, heap tree construction takes O(k log k) comparisons and O(k) tree/heap storage. Materializing code strings adds their total output length and prefix-string construction work; a skewed code tree can have depth k−1, and the recursive code builder can encounter Python's recursion limit. Encoding and decoding require work proportional to the processed message/code output, not just to the number of distinct symbols.</Prose>
    <Prose>There are deliberate boundaries. The theorem optimizes weighted payload length among binary prefix codes for the stated symbol frequencies. It does not promise the smallest complete compressed file: transmitting the codebook, message length and other framing can cost space. One symbol can use an empty code if the decoder knows how many repetitions to produce; this example passes that count explicitly. A format requiring a nonempty one-bit code would use a different single-symbol convention. Python characters are code points, which need not equal user-perceived characters.</Prose>
    <H3>The same tree can minimize repeated merging work</H3>
    <Prose>Suppose four sorted files have lengths 2, 3, 7 and 9, and a two-file merge is modeled as exactly a+b units of work for lengths a and b. Every original entry is processed once for each merge above its file leaf. The total modeled work is again length × depth, so merging the two shortest remaining files minimizes it. Huffman now explains a data-processing schedule, not an encoding trick. Actual disk I/O adds buffer, seek, compression and implementation costs; the integer merge-cost model is not a storage benchmark.</Prose>

    <H2>7. Certify reach without committing to a path</H2>
    <Prose>In a forward jump array, each entry is the maximum distance you may jump from that position. A value 3 allows jumps of 1, 2 or 3 positions when they remain in the array. Decide whether the last index is reachable. “Always make the longest jump” is a tempting rule, but a shorter jump can land at a position that reaches much farther afterward.</Prose>
    <ReachablePrefixFigure />
    <Prose>Maintain reach, a certified forward boundary after examining the earlier positions. Because shorter jumps are allowed, every in-bounds index from zero through reach is reachable. When the next index i is at most reach, it is a legitimate source and can extend the certificate to max(reach, i+jump[i]). The native code may calculate a boundary beyond the last index and immediately return success; it does not require an out-of-bounds landing. The diagram clips the boundary to the last index. If i&gt;reach, no previously reachable source can cross this gap; every later source is itself beyond it, so the destination is unreachable.</Prose>
    <RunnableExample example={greedyExchangeExamples.reachability}><Prose>The scan never commits to which path to take. It keeps the best reachable boundary represented by all paths examined so far. This is a dominance/frontier invariant: for this yes/no task, the reachable prefix summarizes exactly the information needed to continue. The local API returns False for empty input because it contains neither a starting nor a destination index; the external problem promises nonempty input.</Prose></RunnableExample>
    <Prose>Each index is inspected at most once, so the algorithm uses O(n) bounded-size arithmetic operations and O(1) auxiliary state. Reconstructing a path, minimizing the number of jumps, allowing backward moves or requiring exactly the stated jump length changes the output/state contract. For exact jumps, reachable positions can have holes, so a single prefix endpoint no longer describes them.</Prose>

    <H2>8. Choose a proof and test its boundaries</H2>
    <LessonTable caption="A proposed rule needs a bridge to the global answer" headers={['Proof form', 'What you must show', 'Example here']} rows={[
      ['Exchange', 'A legal replacement preserves feasibility and cannot worsen an optimum; repeat or solve the residual problem', 'First appointment, deadline neighbors, equal-weight material'],
      ['Stays ahead', 'A precisely defined comparison holds at every aligned step and forces the final objective', 'Greedy’s j-th appointment finishes no later'],
      ['Matching lower bound', 'No answer can beat a bound, and the construction achieves it', 'Rooms equal the simultaneous-occupancy requirement'],
      ['Structural contraction', 'Some optimum has a chosen local structure, and contracting it preserves the optimization problem up to fixed cost', 'Huffman sibling pair'],
      ['State dominance/invariant', 'Discarded alternatives cannot offer a future absent from the retained state', 'Reachable prefix for at-most forward jumps'],
    ]} />
    <Prose>Begin a new proof by writing the objective and legal moves. Choose an arbitrary optimum, or a clearly defined comparison schedule. Identify the first disagreement or an adjacent inversion; describe exactly what changes and what stays fixed. Show feasibility before comparing scores. Then explain why the exchange can be repeated, why the remaining problem has the required form, or how the invariant implies the final answer. The later <a href="/learn/topic/algorithm-correctness-loop-invariants-termination">Algorithm Correctness, Loop Invariants & Termination</a> topic develops proof techniques more broadly.</Prose>
    <Prose>A common error proves only that the immediate choice is best among immediate choices. Another assumes the optimum already has the greedy choice, which is precisely what needs justification. A third checks one attractive optimum rather than explaining why an optimum can always be transformed. Good counterexamples are small and targeted: change weights, endpoint inclusion, release times, divisibility or the kind of output, then identify the exact line of reasoning that fails.</Prose>
    <details className="lesson-deeper"><summary>Deeper: why some families support broad greedy guarantees</summary>
      <Prose>A <strong>matroid</strong> describes feasible subsets of a finite ground set, including the empty set, with two useful properties: removing elements preserves feasibility, and whenever two feasible sets have different sizes, some element in the larger but not the smaller can be added to the smaller while retaining feasibility. This second property is an augmentation rule. Under these properties, scanning nonnegative element weights from largest to smallest and accepting each feasible addition maximizes total weight. The theorem needs the structural assumptions; merely calling a feasible family an independence system does not supply them.</Prose>
      <Prose>Nonoverlapping intervals fail augmentation. Let A=[0,2), B=[0,1), C=[1,2). Sets {`{A}`} and {`{B,C}`} are both feasible, but neither B nor C can be added to {`{A}`}. Earliest finish still solves the unweighted count problem by its special interval proof. That does not imply that arbitrary descending-value selection solves weighted intervals. <a href="/learn/topic/combinatorial-optimization-approximation-algorithms">Combinatorial Optimization & Approximation Algorithms</a> is the home for the broader theorem and approximation guarantees.</Prose>
      <Prose>A greedy heuristic can be useful even when it is not exact, but “usually good” and “within a proved factor of optimum” are different claims. A quantitative approximation needs its own assumptions and proof. Likewise, greedy next-token decoding chooses a locally likely continuation; without a suitable exchange property it need not maximize the probability of a complete sequence. The word greedy supplies no universal optimality theorem across these domains.</Prose>
    </details>
    <Prose>The weighted graph lesson later derives the distinct proofs for shortest paths and spanning trees. The earlier heap and union-find operations help implement those algorithms; they do not by themselves justify accepting a particular edge or finalizing a distance. Reuse a data structure freely, but carry over an optimality guarantee only with its assumptions intact.</Prose>

    <H2>9. Prove a new choice</H2>
    <H3>Exercise A · cover intervals with points</H3>
    <Prose>Choose as few points on a line as possible so every closed interval contains at least one chosen point. For [(1,4),(3,6),(6,8),(7,9)], give a two-point cover and prove that one point cannot suffice. Propose a greedy rule and explain why this endpoint policy differs from nonconflicting half-open appointments.</Prose>
    <details className="lesson-deeper"><summary>Hint for a covering-point exchange</summary><Prose>Consider the still-uncovered interval with the smallest right endpoint r. An optimum must contain a point inside it. Try moving that point to r and examine every other interval it covered.</Prose></details>
    <Checkpoint prompt="Show the exchange, the residual task and the acceptance cases before opening the code.">
      <Prose>Choose r, the earliest right endpoint, and remove all intervals containing it. If an optimum used point p in that first interval, then p≤r. Every remaining interval that contained p starts no later than p and ends no earlier than r, by minimality of r. It therefore also contains r. Shifting p to r preserves coverage with the same point count, so an optimum contains the greedy point; recurse on uncovered intervals.</Prose>
      <Prose>The fixture is covered by 4 and 8. The first and last intervals are disjoint, so at least two points are necessary. Closed [1,2] and [2,3] share point 2 and need only one point; a degenerate [5,5] requires point 5. Empty input needs no points. Sorting costs O(n log n), followed by an O(n) scan and at most O(n) output.</Prose>
      <RunnableExample example={greedyExchangeExamples.stabbing} />
    </Checkpoint>
    <H3>Exercise B · use the smallest adequate supply</H3>
    <Prose>Recipients need minimum amounts [4,2,5]; indivisible supplies have amounts [3,5]. One supply can serve one recipient if it meets the requirement, and a recipient uses at most one supply. Maximize how many are served. Return original recipient/supply index pairs, not merely the count. Explain a rule, prove it, and test repeated requirements, empty input and a supply too small for anyone.</Prose>
    <details className="lesson-deeper"><summary>Hint for threshold matching</summary><Prose>Sort both sides while retaining original IDs. Consider the least demanding remaining recipient and the smallest remaining supply. Decide whether an undersized supply can help anyone, then consider exchanging assignments when it fits.</Prose></details>
    <Checkpoint prompt="Justify a matched pair rather than only observing that both sorted pointers can advance.">
      <Prose>If the smallest supply is too small for the smallest requirement, it cannot satisfy any remaining recipient. Discard it safely. Otherwise match that pair. An optimum can be changed to do the same: if the smallest recipient used a larger supply and the chosen supply served someone else, swap those two supplies. The larger supply still meets the other recipient's need because it is at least as large as the supply that served them before. If one was unassigned, reassign without losing a match; if both were unassigned, adding the match would contradict optimality. Thus a maximum matching can contain the pair, and the remaining task has the same form.</Prose>
      <Prose>The fixture matches recipient 1 to supply 0 and recipient 0 to supply 1, serving two people, which is the upper bound imposed by two supplies. For [1,1] and [1], serve exactly one occurrence. Sorting costs O(n log n + m log m), scanning O(n+m), with stored IDs/sorted lists and up to min(n,m) output pairs. Unequal recipient rewards would change the objective and need a new proof.</Prose>
      <RunnableExample example={greedyExchangeExamples.matching} />
    </Checkpoint>
    <H3>Exercise C · find the missing scheduling assumption</H3>
    <Prose>Job A is available at 0, takes 10 units and is due at 100. Job B is available at 1, takes one unit and is due at 2. Jobs cannot be interrupted. Compare “never idle when a job is available” with waiting until B becomes available. Identify which step of the earlier proof no longer applies.</Prose>
    <details className="lesson-deeper"><summary>Hint for release times</summary><Prose>Compute B's completion after starting A immediately. Then schedule B first after a one-unit gap. Removing that gap would require starting B before its release.</Prose></details>
    <Checkpoint prompt="Give exact maximum lateness values and explain why this is a changed problem, not a counterexample to the proved theorem.">
      <Prose>Starting A at 0 finishes it at 10, then B at 11. Latenesses are −90 and 9, so the maximum is 9. Waiting until 1, running B to 2 and then A to 12 gives latenesses 0 and −88, so the maximum is 0. The earlier theorem assumed every job available at zero. Here removing the idle gap or freely exchanging jobs can violate a release time, so the feasibility argument fails. This example does not establish a replacement algorithm for all released-job problems.</Prose>
    </Checkpoint>
    <H3>Exercise D · reconstruct the merge objective</H3>
    <Prose>For frequencies 1,2,4,8, draw a Huffman tree, list code lengths and calculate weighted payload cost in two ways. Then change the decoder's framing so a single symbol must have a nonempty code. State what happens to the one-symbol special case.</Prose>
    <details className="lesson-deeper"><summary>Hint for merge accounting</summary><Prose>Contract the two smallest weights repeatedly, and add each contracted sum. A leaf contributes its frequency once for every ancestor merge.</Prose></details>
    <Checkpoint prompt="Explain the expected values and distinguish the payload theorem from the file-format convention.">
      <Prose>The merges are 1+2=3, 3+4=7 and 7+8=15; cost is 3+7+15=25. The frequencies 1,2,4,8 have code lengths 3,3,2,1 respectively, giving 1×3+2×3+4×2+8×1=25. Left/right bit assignments may differ without changing lengths or cost. Under a mandatory nonempty one-symbol code, frequency f uses f payload bits, whereas the empty-code convention with a separately known repetition count uses zero payload bits. Framing is part of the actual contract.</Prose>
    </Checkpoint>
    <H3>Ready to continue</H3>
    <Prose>For a new candidate rule, state the legal objects, objective, tie policy, chosen action and remaining state. Supply a counterexample or a complete proof bridge; test small cases independently, then explain sorting, heap, output and arithmetic costs. Revisit a solved problem with a changed objective or feasibility condition and identify exactly why the old proof survives or fails.</Prose>
    <DsaPractice practice={greedyPractice} />
    <Prose>The next module topic is <a href="/learn/topic/dynamic-programming-states-transitions-optimization">Dynamic Programming: States, Transitions & Optimization</a>. Greedy safely commits when a proof permits it. When valuable alternatives remain, DP describes a complete subproblem state and combines their outcomes without repeatedly solving the same state. Weighted intervals and whole-item capacity provide concrete reasons to make that transition.</Prose>

    <Sources alternatives={<>
      <p><a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/resources/lecture-1-course-overview-interval-scheduling/" target="_blank" rel="noopener noreferrer">MIT OCW · Srinivas Devadas, Overview and Interval Scheduling</a> offers a video route into candidate rules, counterexamples and the finish-time argument. The official page, <a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/ff2a7015ff913fc01f0381d8f5126a9a_MIT6_046JS15_lec01.pdf" target="_blank" rel="noopener noreferrer">companion notes</a> and substantive transcript were inspected on 10 September 2026; full video playback is not claimed. The course-overview portion precedes the algorithm material.</p>
      <p><a href="https://www.cs.princeton.edu/~wayne/kleinberg-tardos/pdf/04GreedyAlgorithmsI-2x2.pdf" target="_blank" rel="noopener noreferrer">Kevin Wayne · Greedy Algorithms I</a> is a written visual route through selection, room assignment and deadline exchanges. It calls the clipped nonnegative quantity lateness; this lesson separately names signed lateness and tardiness. <a href="https://algs4.cs.princeton.edu/55compression/" target="_blank" rel="noopener noreferrer">Princeton · Data Compression</a> supplies further prefix-code exercises and the merge-tree connection; its Java bit-stream implementation is beyond these Python text-bit examples.</p>
    </>}>
      <li><a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/ff2a7015ff913fc01f0381d8f5126a9a_MIT6_046JS15_lec01.pdf" target="_blank" rel="noopener noreferrer">MIT interval-scheduling notes</a> — precise compatibility, candidate rules and proof progression; use the stated positive-duration convention.</li>
      <li><a href="https://www.cs.cmu.edu/~15850/notes/emin-lecture23.pdf" target="_blank" rel="noopener noreferrer">CMU · fractional-knapsack relaxation in allocation</a> — an advanced connection between a fractional optimum and a bound for indivisible choices. Its auction/incentive assumptions are additional material, not claims made about the simple capacity model here.</li>
      <li><a href="https://assets.cambridge.org/97805218/11514/sample/9780521811514ws.pdf" target="_blank" rel="noopener noreferrer">A First Course in Combinatorial Optimization · publisher sample</a> — deeper matroid/greedy structure; follow after the optional augmentation discussion, not as a prerequisite for the concrete proofs above.</li>
    </Sources>
  </div>,
};
