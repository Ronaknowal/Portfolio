import { Code, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { ProcessSliceFigure, StateAggregationFigure, MarkovPropagationLab, ClassStructureFigure, AbsorptionLab, PoissonArrivalLab, JumpClockLab, BrownianPathLab, BrownianCovarianceFigure, BrownianBridgeFigure } from '../../components/lesson-labs/StochasticProcessesLabs.jsx';
import { stochasticProcessesExamples as examples } from '../stochastic-processes-examples.js';
function Example({
  name
}) {
  const example = examples[name];
  return <><Prose><strong>Before running:</strong> {example.question}</Prose><RunnableExample example={example} /></>;
}
function Practice({
  prompt,
  hint,
  children
}) {
  return <div className="lesson-check">
    <p><strong>Try it independently.</strong> {prompt}</p>
    <details><summary>Hint</summary>{hint}</details>
    <details><summary>Show explained solution</summary>{children}</details>
  </div>;
}
export default {
  title: 'Stochastic Processes (Markov Chains, Brownian Motion, Poisson)',
  readTime: '~95 min read + 1–2 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot stochastic-processes-lesson">
    <LessonIntro prerequisites="You need conditional probabilities, means/variances and a small matrix product. We review the required ideas before using them. Matrix rows mean the current state; columns mean the next state. Integration first appears as the area under a rate curve, and every example states its time units." sections={[['1-read-a-process-in-two-directions', 'Paths, distributions and memory'], ['2-choose-a-state-that-remembers-enough', 'Markov states and probability flow'], ['3-separate-equilibrium-from-convergence', 'Classes, mixing and occupation'], ['4-ask-when-a-boundary-is-first-reached', 'First passage and absorption'], ['5-connect-arrival-times-to-event-counts', 'Poisson clocks and waiting times'], ['6-change-the-clock-or-route-the-events', 'Splitting and scheduled intensity'], ['7-let-each-state-have-its-own-clock', 'Continuous-time Markov chains'], ['8-build-brownian-motion-from-increments', 'Diffusion, covariance and grid paths'], ['9-look-between-observations', 'Bridges, variation and fair processes'], ['10-test-the-process-and-transfer-the-ideas', 'Model checks and independent practice']]}>
      Learn to choose a state and time scale, calculate how uncertainty evolves, simulate complete paths, and distinguish a model's long-run law from what one observation window can reveal.
    </LessonIntro>
    <Prose>Twelve complete Python programs use only the standard library. Save a program as <Code>example.py</Code> and run <Code>python example.py</Code>; its inputs and output are included. The first three programs from the earlier lesson are preserved. Browser diagrams use computed probabilities or explicitly identified seeded simulations. They are not measurements of real weather, traffic or markets.</Prose>

    <H2>1. Read a process in two directions</H2>
    <Prose>A distribution can answer “What is the probability that the device is working now?” A <strong>stochastic process</strong> also describes how its states at different times relate. Write Xₜ for the random state at time t. The collection of all those random variables, with their joint behavior, is the process. One observed sequence is a <strong>sample path</strong>, or realization.</Prose>
    <Prose>Imagine three devices. The first redraws a fair binary state every second. The second tosses once and keeps that state forever. The third tosses once and then alternates. At any fixed time all three have a 50% chance of showing 1. Their time traces tell very different stories.</Prose>
    <ProcessSliceFigure />
    <Prose>For four observations, independent redraws permit 16 equally likely paths. The frozen device permits only 0000 and 1111. The alternating device permits only 0101 and 1010. A histogram at one time cannot distinguish them. Runs, transitions and correlations can.</Prose>
    <Prose><strong>Strict stationarity</strong> means that shifting every time in any finite collection by the same amount leaves its joint distribution unchanged, whenever the shifted times are in the time domain. Each of these three processes is stationary with its fair initial draw. Stationarity does not mean an unchanging path, independent observations or a time average that equals an ensemble mean. The frozen device has ensemble mean ½ but one path's average is always 0 or 1.</Prose>
    <Prose>Also distinguish the time index from the state space. Time can advance in daily steps or continuously; states can be categories, integers or real values. A Poisson count has continuous time and integer state. Brownian motion has continuous time and real state. Both also have the Markov property developed next; the named families are not mutually exclusive boxes.</Prose>
    <LessonTable caption="Choose what evolves before choosing a model" headers={['Question', 'State and time', 'Useful starting model']} rows={[['Will a device change mode next minute?', 'Mode at fixed sampling times', 'Discrete-time Markov chain'], ['How many requests arrive by time t?', 'Cumulative count, continuous time', 'Poisson process, if its assumptions fit'], ['How long does a device stay in each mode?', 'Mode plus continuous holding times', 'Continuous-time Markov chain'], ['How does accumulated small noise move a position?', 'Real-valued position, continuous time', 'Brownian motion, possibly with drift/scale']]} />
    <Checkpoint prompt="A generator matches every one-time histogram in a dataset. Has it reproduced the temporal behavior?">
      <Prose>No. It could redraw independently, freeze values, or alternate while matching those histograms. Compare appropriate joint quantities: transition probabilities, run lengths, increment dependence and first-passage events.</Prose>
    </Checkpoint>

    <H2>2. Choose a state that remembers enough</H2>
    <Prose>A state is useful if it contains the information needed for the next prediction. A process is <strong>Markov</strong> when, given its present state, the past supplies no extra information about its next state. This is conditional independence, not independence of successive states.</Prose>
    <MathBlock>{'\\begin{gathered}P(X_{n+1}=j\\mid X_n=i,\\text{past})\\\\=P(X_{n+1}=j\\mid X_n=i).\\end{gathered}'}</MathBlock>
    <Prose>A weather model might retain only Sunny or Rainy. If today's state is sunny, tomorrow is sunny with probability .8 and rainy with probability .2. If today is rainy, those probabilities are .3 and .7. Assume these same rules hold every day: that extra assumption is <strong>time homogeneity</strong>. A time-dependent transition matrix can still describe a Markov process.</Prose>
    <MathBlock>{'P=\\begin{pmatrix}.8&.2\\\\.3&.7\\end{pmatrix},\\qquad p_{n+1}=p_nP.'}</MathBlock>
    <Prose>Every row is a conditional distribution and sums to one. A column need not sum to one. With row-vector convention, pₙ=(probability sunny, probability rainy). Starting certainly sunny gives p₁=(.8,.2). At the second step, sunny mass arrives by two disjoint routes: .8×.8 from sunny and .2×.3 from rainy, totaling .70. Rainy receives .8×.2+.2×.7=.30.</Prose>
    <MarkovPropagationLab />
    <Prose>The matrix product adds over possible intermediate states. More generally, Pⁿ gives n-step transition probabilities. The <strong>Chapman–Kolmogorov rule</strong>, Pᵐ⁺ⁿ=PᵐPⁿ for a homogeneous chain, says to split a journey at an intermediate time and sum its possible intermediate states. It is not an entrywise power.</Prose>
    <Prose>A particular path has a different probability from its endpoint event. Starting sunny, the path Sunny→Rainy→Sunny has probability .2×.3=.06. Ending sunny after two steps has probability .70 because Sunny→Sunny→Sunny contributes another .64.</Prose>
    <MathBlock>{'\\begin{gathered}P(X_0=i_0,\\ldots,X_n=i_n)\\\\=p_0(i_0)\\prod_{k=0}^{n-1}P_{i_k i_{k+1}}.\\end{gathered}'}</MathBlock>
    <Example name="weather" />
    <Prose>After five days, the exact unrounded probabilities are .6125 and .3875. The preserved program rounds them separately to .613 and .388; the displayed sum 1.001 is rounding, not extra probability.</Prose>
    <H3>A coarse observation can destroy the Markov property</H3>
    <Prose>Suppose the true system cycles deterministically A→B→C→A, but a sensor reports 0 for A or B and 1 for C. A report of 0 hides two different situations. If the previous report was 1, the current hidden state is A and the next report is 0. If the previous report was 0, the current hidden state is B and the next report is 1. Current report alone is insufficient.</Prose>
    <StateAggregationFigure />
    <Example name="pathLaws" />
    <Prose>The example gives the initial hidden phase a uniform distribution, so both histories genuinely occur. Retaining the hidden state, or an adequate history of reports, restores predictive information. A hidden Markov model formalizes latent Markov states with a separate observation model; its observed sequence need not itself be Markov. Conversely, some carefully chosen aggregations preserve the property. Arbitrary binning offers no guarantee.</Prose>
    <Prose>Memorylessness is a modeling claim at a specified state definition and resolution. A process can look approximately memoryless hourly while retaining dependence minute by minute. Record whether timestamps denote actual event time, collection time or delayed ingestion.</Prose>

    <H2>3. Separate equilibrium from convergence</H2>
    <Prose>A <strong>stationary distribution</strong> π satisfies πP=π: one propagation leaves the distribution unchanged. If a homogeneous chain starts with that law, the process is stationary. This fixed-point equation alone does not say that every starting distribution approaches it.</Prose>
    <Prose>For the two-state matrix with switch probabilities a and b, balance the movement between states: πₛa=πᵣb, with πₛ+πᵣ=1. If a+b&gt;0, this yields πₛ=b/(a+b) and πᵣ=a/(a+b). The weather example has π=(.6,.4).</Prose>
    <MathBlock>{'\\begin{gathered}s_{n+1}=b+(1-a-b)s_n,\\\\s_n-\\pi_S=(1-a-b)^n(s_0-\\pi_S).\\end{gathered}'}</MathBlock>
    <Prose>The multiplier separates destination from speed. Changing (.2,.3) to (.02,.03) keeps the balance .6/.4 but changes the error multiplier from .5 to .95: much slower forgetting. If a+b&gt;1 the error changes sign while shrinking, except at a=b=1 where its magnitude stays one. Use those presets in the lab before reading the class rules.</Prose>
    <ClassStructureFigure />
    <Prose>Two states <strong>communicate</strong> if each is reachable from the other by positive-probability transitions. A class is <strong>closed</strong> if there is no positive-probability exit. A finite chain is <strong>irreducible</strong> when every state communicates with every other. Its period is the greatest common divisor of possible positive return times; period one means <strong>aperiodic</strong>. A positive self-loop is a sufficient way to make an irreducible chain aperiodic, not a necessary one.</Prose>
    <LessonTable caption="Different claims need different conditions" headers={['Claim', 'Finite-chain condition or counterexample']} rows={[['A stationary distribution exists', 'Every finite stochastic matrix has at least one'], ['It is unique', 'Irreducibility is sufficient; some reducible chains also have a unique one'], ['Every initial law converges to it', 'Finite + irreducible + aperiodic is sufficient'], ['A path’s state-occupation fractions approach π', 'Finite + irreducible is sufficient; periodicity need not prevent this'], ['Stationarity implies mixing', 'False: a fair-start alternating chain is stationary but retains phase']]} />
    <Prose>The always-switch chain has unique π=(½,½), but from a known state its marginal alternates forever. Its time fraction in each state nevertheless tends to ½. The identity matrix has every distribution stationary and preserves the initial law exactly. A chain A→B with B absorbing is reducible but converges to the unique stationary mass at B. These examples prevent sufficient conditions from being mistaken for necessary ones.</Prose>
    <Example name="markov" />
    <H3>Why these finite-state conditions work</H3>
    <Prose>For existence, average the first N propagated laws. Propagating that average changes it by (pₙ−p₀)/N, which vanishes. Compactness of the finite probability simplex gives a convergent subsequence; its limit is stationary. In a finite irreducible chain, recurrent visits connect every state and fix a unique invariant balance.</Prose>
    <details><summary>Deeper: mixing, occupation and detailed balance</summary>
      <Prose>For a finite irreducible aperiodic chain, some power Pᵐ has all entries positive. Let δ be its smallest entry and d the number of states. Each row contains the common mass dδ times the uniform law; the remainder is another stochastic row. Repeating this decomposition contracts differences in total variation by at most 1−dδ every m steps, giving convergence and uniqueness. The positive-power theorem is the finite graph/period fact behind this proof.</Prose>
      <Prose>Occupation averages use a different route: visits to a chosen state divide an irreducible finite chain into return cycles with finite mean lengths. Renewal/reward averages identify the limiting fraction πᵢ. This argument does not require aperiodicity. For countably infinite chains, recurrence need not imply finite expected return time or a stationary probability distribution; do not transfer the finite result without checking positive recurrence.</Prose>
      <Prose><strong>Detailed balance</strong>, πᵢPᵢⱼ=πⱼPⱼᵢ for every pair, is sufficient for stationarity because summing over i gives (πP)ⱼ=πⱼ. It also expresses reversibility under π. It is stronger than global balance: a directed three-cycle has a uniform stationary law while its flow is not pairwise balanced.</Prose>
    </details>

    <H2>4. Ask when a boundary is first reached</H2>
    <Prose>Often the useful question is not a distribution at a fixed date. A battery reserve moves up or down by one unit per step; stop when it reaches empty reserve 0 or target B. Starting at reserve i, what is the probability hᵢ of hitting B first, and how many steps tᵢ until either boundary?</Prose>
    <Prose>A <strong>first hitting time</strong> is τ=inf&#123;n≥0:Xₙ lies in the target set&#125;. It is 0 if the path starts there. A first return instead restricts n≥1. Once stopped, we make the boundary absorbing so the same matrix propagation records both accumulated success/failure and still-moving probability.</Prose>
    <MathBlock>{'\\begin{gathered}h_i=(1-p)h_{i-1}+ph_{i+1},\\\\h_0=0,\\quad h_B=1,\\\\t_i=1+(1-p)t_{i-1}+pt_{i+1},\\\\t_0=t_B=0.\\end{gathered}'}</MathBlock>
    <Prose>These are <strong>first-step equations</strong>: condition on what happens next. Success has no added reward; time incurs one step before the remaining wait. For a fair walk, hᵢ=i/B and tᵢ=i(B−i) solve the equations and boundaries. At B=4,i=2, success is ½ and expected duration is 4 steps. An expected duration does not require each path to last four steps.</Prose>
    <AbsorptionLab />
    <Prose>With p=.25, upward movement is less likely. From reserve 2, h₂=.1 and t₂=3.2. The smaller mean duration reflects faster loss, not an improvement. From the fair start, after four steps lower absorption is 3/8, upper absorption 3/8 and survival is 1/4. Calling every still-moving path a failure would underestimate eventual success.</Prose>
    <Example name="absorption" />
    <details><summary>Deeper: a general absorbing system and its conditions</summary>
      <Prose>Arrange transient states first and absorbing states last. The transition matrix has blocks Q for transient-to-transient movement and R for transient-to-absorbing movement. If every transient state can reach an absorbing class in this finite system, absorption occurs almost surely with finite mean time, Qⁿ→0 and the series below converges.</Prose>
      <MathBlock>{'\\begin{gathered}F=I+Q+Q^2+\\cdots,\\\\F=(I-Q)^{-1},\\\\H=FR,\\qquad t=F\\mathbf1.\\end{gathered}'}</MathBlock>
      <Prose>Fᵢⱼ counts expected visits to transient j before absorption from i, including time zero. Summing its row gives expected steps before absorption; multiplying by R records the final exit. Our lab solves linear systems rather than simulating until a convenient cutoff. If a closed nonabsorbing class remains, I−Q may be singular and an unrestricted mean hitting time may be infinite. The existence of an absorbing state somewhere is not enough.</Prose>
      <Prose>For the biased reserve walk, write r=(1−p)/p. Solving the difference recurrence gives hᵢ=(1−rⁱ)/(1−rᴮ) for p≠½. Then tᵢ=(i−Bhᵢ)/(1−2p). Near p=½, direct subtraction can be numerically unstable; the bounded browser solves the finite equations instead.</Prose>
    </details>

    <H2>5. Connect arrival times to event counts</H2>
    <Prose>Let N(t) count events from time 0 through t, with N(0)=0. A homogeneous <strong>Poisson process</strong> of rate λ≥0 has independent counts in disjoint intervals, stationary increments, and N(t)−N(s) distributed as Poisson(λ(t−s)) for 0≤s≤t. “Stationary increments” means the interval's length determines its increment law. The level process N(t) itself is not stationary when λ&gt;0: its expected count grows.</Prose>
    <MathBlock>{'\\begin{gathered}P\\{N(t)-N(s)=k\\}\\\\=e^{-\\lambda(t-s)}\\frac{[\\lambda(t-s)]^k}{k!}.\\end{gathered}'}</MathBlock>
    <Prose>Specify intervals as (s,t]: an event at t counts, one at s belongs to the earlier cumulative count. In the ideal process a fixed exact time has zero event probability, but explicit boundaries matter for recorded data and reproducible code. Paths increase by single jumps and are right-continuous.</Prose>
    <Prose>Let W₁ be the first waiting time. “No event by t” is exactly “the first wait exceeds t,” so P(W₁&gt;t)=P(N(t)=0)=e⁻λᵗ. Thus W₁ has an exponential distribution with rate λ, mean 1/λ and standard deviation 1/λ when λ&gt;0. Independent stationary increments give independent exponential interarrival times W₁,W₂,… . If λ=0 there are no events and the waiting time is infinite.</Prose>
    <MathBlock>{'\\begin{gathered}P(W>s+t\\mid W>s)\\\\=\\frac{e^{-\\lambda(s+t)}}{e^{-\\lambda s}}=e^{-\\lambda t}.\\end{gathered}'}</MathBlock>
    <Prose>The <strong>memoryless</strong> wait says that surviving s time units does not change the remaining waiting law under this constant-rate model. It does not say a scheduled appointment or an aging component has exponential waits. A visible daily arrival schedule already contradicts a constant rate.</Prose>
    <PoissonArrivalLab />
    <Prose>Event epochs are cumulative waits Sₖ=W₁+⋯+Wₖ. The bridge between waiting and counting is Sₖ≤t exactly when N(t)≥k. Therefore the k-th arrival time has an Erlang distribution, a Gamma distribution with integer shape k and rate λ:</Prose>
    <MathBlock>{'\\begin{gathered}P(S_k\\le t)=1-e^{-\\lambda t}\\sum_{j=0}^{k-1}\\frac{(\\lambda t)^j}{j!},\\\\E[S_k]=k/\\lambda,\\quad\\operatorname{Var}(S_k)=k/\\lambda^2.\\end{gathered}'}</MathBlock>
    <Example name="arrivalProbability" />
    <Prose>At 2.5 requests per minute over 3 minutes, the mean is 7.5 requests and P(no requests)=e⁻⁷·⁵≈.000553. The retained program rounds this to .0006; its complement is .9994. A rate has units events/time. Multiplying 2.5 by 180 seconds would be wrong until the rate is converted to events/second.</Prose>
    <Example name="arrivals" />
    <Prose>The event simulation draws waits and accumulates them until the next event would pass the horizon. The time remaining after the last observed arrival is a <strong>censored wait</strong>: we know the next wait exceeds it, not that it equals it. The program checks N(1), N(2), N(3) and the selected interval using the same event times. It also checks that the third arrival occurs by minute 1 with probability about .456187.</Prose>
    <Checkpoint prompt="If Poisson increments are independent, are N(1) and N(2) independent?">
      <Prose>No. N(2)=N(1)+[N(2)−N(1)] contains the same first-minute count. Its covariance with N(1) is Var(N(1))=λ. The counts on (0,1] and (1,2] are independent; cumulative counts share history.</Prose>
    </Checkpoint>

    <H2>6. Change the clock or route the events</H2>
    <H3>Independent marking creates separate Poisson streams</H3>
    <Prose>Route each arrival independently to A with probability r and B otherwise, independently of its time and all other marks. Given a possible total n in an interval, A is Binomial(n,r) and B=n−A. They are conditionally dependent when n&gt;0 and 0&lt;r&lt;1; if n=0 or r is 0 or 1, the conditional counts are deterministic. Without conditioning on the total, the outputs are independent Poisson counts with means rμ and (1−r)μ, where μ=λ times interval length. If μ=0, only total zero is possible; conditioning on a positive total is undefined in this model.</Prose>
    <MathBlock>{'\\begin{gathered}P(N_A=m,N_B=n)\\\\=e^{-\\mu}\\frac{\\mu^{m+n}}{(m+n)!}\\\\\\quad\\times\\binom{m+n}{m}r^m(1-r)^n\\\\=\\left[e^{-r\\mu}\\frac{(r\\mu)^m}{m!}\\right]\\\\\\quad\\times\\left[e^{-(1-r)\\mu}\\frac{((1-r)\\mu)^n}{n!}\\right].\\end{gathered}'}</MathBlock>
    <Prose>The factorization proves independence. For μ=3,r=.4,m=1,n=2, the conditional probability given total 3 is .432, while the unconditional joint probability and product both equal about .096786. The program above computes all three: they answer different questions.</Prose>
    <Prose>Alternating routes A,B,A,B is not independent marking. An A-to-A wait adds two original exponential waits and has an Erlang-2 distribution. The output streams are not Poisson. The arrival lab keeps the exact clock unchanged while switching this rule so the structural difference is visible. This matters when modeling a load balancer: independent random routing and state-dependent routing are different assumptions.</Prose>
    <Prose>Conversely, superposing independent Poisson streams adds their rates: the interval-count generating functions multiply to exp[(λ₁+λ₂)t(z−1)], and independent increments persist. Independence matters here too; duplicating the same stream does not create an ordinary Poisson process of double rate because events arrive in simultaneous pairs.</Prose>
    <H3>A known schedule changes accumulated intensity</H3>
    <Prose>For a deterministic nonnegative rate λ(t), define accumulated intensity Λ(t)=∫₀ᵗλ(u)du. A <strong>nonhomogeneous Poisson process</strong> still has independent increments, but its interval mean is Λ(t)−Λ(s), which need not depend only on t−s. For 1 event/minute over the first two minutes and 4 events/minute over the last minute, Λ(3)=2×1+1×4=6.</Prose>
    <MathBlock>{'\\begin{gathered}\\mu_{s,t}=\\Lambda(t)-\\Lambda(s),\\\\N(t)-N(s)\\sim\\operatorname{Poisson}(\\mu_{s,t}),\\\\N(t)=\\widetilde N(\\Lambda(t)).\\end{gathered}'}</MathBlock>
    <Prose>Here Ñ is a unit-rate Poisson clock. Draw exponential waits in operational time, accumulate them, and map each operational event back through Λ. A zero-rate interval has a flat Λ: no operational time passes and no events occur there. The example implements the piecewise inverse explicitly, including a zero-rate prefix.</Prose>
    <Example name="intensity" />
    <Prose>The first one-minute window has mean 1 and no-event probability .367879; the last has mean 4 and no-event probability .018316. Equal duration alone is no longer sufficient. Deterministic scheduling is different from a random shared environment or self-exciting arrival history. A Cox model makes intensity random; a Hawkes model lets earlier events change future intensity. Those extensions need additional assumptions and estimation, not just a new name for any bursty trace.</Prose>

    <H2>7. Let each state have its own clock</H2>
    <Prose>A device may stay On for a random duration, switch Off, and later recover. A <strong>continuous-time Markov chain</strong> keeps the state between jumps and uses exponential holding times. Let α=.5/hour be the On→Off rate and β=2/hour the Off→On rate. The mean On hold is 2 hours, and the mean Off hold is .5 hours.</Prose>
    <MathBlock>{'G=\\begin{pmatrix}-\\alpha&\\alpha\\\\\\beta&-\\beta\\end{pmatrix}.'}</MathBlock>
    <Prose>G is a <strong>generator</strong>. Its off-diagonal entries are jump rates, not probabilities; each diagonal is minus its row's exit rate, so rows sum to zero. For a small interval h, the chance of i→j is approximately Gᵢⱼh for j≠i, while the chance of no departure is approximately 1−exit-rate×h. Multiple-jump effects are part of the higher-order remainder.</Prose>
    <Prose>If s(t)=P(On at t), the flow equation is s′(t)=−αs(t)+β[1−s(t)]. Its solution is:</Prose>
    <MathBlock>{'\\begin{gathered}s(t)=\\pi_{\\rm On}+[s(0)-\\pi_{\\rm On}]e^{-(\\alpha+\\beta)t},\\\\\\pi_{\\rm On}=\\frac{\\beta}{\\alpha+\\beta}=.8.\n\\end{gathered}'}</MathBlock>
    <JumpClockLab />
    <Prose>The jump sequence alternates On,Off,On,Off. Its visit fractions approach one half each. Its time occupation approaches .8 On because On visits last four times as long on average. Confusing the embedded jump chain with continuous-time occupancy would predict availability .5 instead of .8.</Prose>
    <Example name="jumpClock" />
    <Prose>Over a finite horizon T, expected On exposure is ∫₀ᵀs(t)dt. The program starts On, uses T=20 hours and obtains expectation 16.08 hours; the seeded path spends about 14.102429 hours On. The final incomplete holding time is clipped at the horizon when calculating observed exposure. A boundary observation contributes time even when no departure is seen.</Prose>
    <details><summary>Deeper: transition matrices and the finite-state guarantee</summary>
      <Prose>For a finite homogeneous generator G, the transition matrix is P(t)=eᵗᴳ, a matrix exponential, and P(s+t)=P(s)P(t). It solves P′(t)=P(t)G with P(0)=I. A stationary row law satisfies πG=0. The exponential includes arbitrarily many jumps within the interval; exponentiating each entry separately does not.</Prose>
      <Prose>The approximation I+hG can fail as a probability matrix. With β=2/hour and h=1 hour, the Off row is (2,−1), impossible probabilities. The exact exponential remains stochastic. In finite state with finite rates, jump counts do not explode in finite time. A countably infinite chain with rates growing along its path needs a separate nonexplosion and stability analysis. Queueing Theory develops the countable-state setting.</Prose>
    </details>

    <H2>8. Build Brownian motion from increments</H2>
    <Prose>For accumulated small, rapid disturbances, a continuous-valued model is useful. Standard <strong>Brownian motion</strong> Wₜ starts at 0, has continuous paths almost surely, and has independent increments with Wₜ−Wₛ∼Normal(0,t−s) for s≤t. The increment's second parameter here is its variance. It also has stationary increments and the Markov property.</Prose>
    <Prose>A random walk suggests the scaling. In a time step Δt, take a fair step of size √Δt. Over T/Δt independent steps, the accumulated variance is (T/Δt)Δt=T. If the step size were Δt instead, the variance would shrink to zero as the grid became finer. A scalar central limit theorem motivates approximately normal endpoints; convergence of entire random-walk paths to Brownian motion is a stronger functional limit theorem with its own conditions.</Prose>
    <MathBlock>{'\\begin{gathered}X_t=x_0+\\mu t+\\sigma W_t,\\\\\\Delta X=\\mu\\Delta t+\\sigma\\sqrt{\\Delta t}\\,Z,\\\\Z\\sim N(0,1),\\\\E[X_t]=x_0+\\mu t,\\\\\\operatorname{Var}(X_t)=\\sigma^2t.\\end{gathered}'}</MathBlock>
    <Prose>μ is drift in position units/time. σ is diffusion scale in position units/√time; σ² is variance rate in squared units/time. For Δt=.25 and σ=2, the increment standard deviation is 2√.25=1, not 2×.25=.5. Confusing variance and standard deviation changes the process.</Prose>
    <Example name="brownianOriginal" />
    <Prose>The preserved program uses five unit-time steps with μ=0,σ=1. Python's <Code>rng.gauss(mean, sigma)</Code> takes a standard deviation. At Δt=1 that distinction is invisible because √1=1. The next program exposes it at changed resolution, scale and drift.</Prose>
    <BrownianCovarianceFigure />
    <MathBlock>{'\\operatorname{Cov}(X_s,X_t)=\\sigma^2\\min(s,t).'}</MathBlock>
    <Prose>For s≤t, split Wₜ into Wₛ plus its independent future increment. The only shared variance is s. Drift does not change covariance. In particular the correlation of positive-time standard Brownian levels is √(s/t), even though disjoint increments are independent. A Gaussian process means every finite collection has a joint Gaussian distribution; continuity does not follow merely from giving it that label.</Prose>
    <BrownianPathLab />
    <Prose>The browser constructs a finest grid first, then reuses its values at coarser resolutions. This gives the same coupled path across views. Drawing a fresh path each time would confound grid refinement with a different random realization. Brownian values at a finite grid can be simulated exactly under the assumed model using independent normal increments. Straight lines drawn between them are an interpolation, not the actual Brownian path between observations.</Prose>
    <Example name="brownianGrid" />
    <Prose>With T=2,μ=.3,σ=.7, the endpoint mean is .6 and variance .98. The seeded endpoint .397221 is identical on all three nested grids. The realized sums of squared increments are about 1.167720, .656890 and 1.233887, rather than improving monotonically. Statistical convergence concerns distributions or errors, not a requirement that one plotted statistic move smoothly toward its limit.</Prose>
    <Prose>Brownian motion models diffusion, noisy physical positions and continuous latent disturbances; it is also a building block in finance. Raw financial returns are not automatically Brownian. Volatility clustering, jumps, changing regimes and market microstructure can violate constant-variance independent increments. Measurement noise can also make very fine increments dependent even if the unobserved position model were appropriate.</Prose>

    <H2>9. Look between observations</H2>
    <H3>A bridge conditions on both endpoints</H3>
    <Prose>Suppose X₀=x and XΔ=y are observed for a constant-drift, constant-scale Brownian model. At fraction u of the interval, the conditional mean lies on the straight line between them, but the conditional value remains random. Gaussian conditioning gives:</Prose>
    <MathBlock>{'\\begin{gathered}X_{u\\Delta}\\mid(X_0=x,X_\\Delta=y)\\\\\\sim N(m_u,v_u),\\\\m_u=(1-u)x+uy,\\\\v_u=\\sigma^2\\Delta u(1-u).\\end{gathered}'}</MathBlock>
    <Prose>To see the variance, Cov(WᵤΔ,WΔ)=uΔ. Regress WᵤΔ on WΔ: its coefficient is u and the residual variance is uΔ−u²Δ. The conditional drift cancels because the fixed endpoint already incorporates the total drift. At the midpoint the variance is σ²Δ/4, so inserting the conditional mean alone erases uncertainty.</Prose>
    <BrownianBridgeFigure />
    <Prose>This matters for a barrier alarm: two endpoints below a threshold do not prove that the process stayed below it. For endpoints x,y&lt;b, constant σ&gt;0, the exact conditional probability of crossing b somewhere inside the interval is:</Prose>
    <MathBlock>{'\\begin{gathered}P\\!\\left(\\max_{0\\le t\\le\\Delta}X_t\\ge b\\mid x,y\\right)\\\\=\\exp\\!\\left[-\\frac{2(b-x)(b-y)}{\\sigma^2\\Delta}\\right].\\end{gathered}'}</MathBlock>
    <Prose>For standard Brownian motion, reflect every crossing path after its first hit of b. This pairs a crossing path ending at y&lt;b with one ending at 2b−y. Dividing the reflected endpoint density by the ordinary endpoint density gives the displayed exponential. A constant drift cancels under endpoint conditioning. If either observed endpoint is already at or above b, the conditional crossing probability is one.</Prose>
    <Prose>With x=y=0,b=1,Δ=1,σ=1 the chance is e⁻²≈.135335 despite both endpoints being safely below the barrier. Without conditioning on the final endpoint, reflection instead gives P(maxₜ≤₁Wₜ≥1)=2P(W₁≥1)≈.317311. These are different events and evidence conditions. Applying the same bridge formula to state-dependent diffusion or a general stochastic differential equation is usually an approximation, not this exact result.</Prose>
    <H3>Rough paths accumulate squared movement</H3>
    <Prose>On an equal deterministic partition of [0,T] into n intervals, define Qₙ=Σ(ΔW)². Each increment is Normal(0,T/n), so its square has mean T/n and variance 2(T/n)². Independence within that partition gives:</Prose>
    <MathBlock>{'E[Q_n]=T,\\qquad\\operatorname{Var}(Q_n)=\\frac{2T^2}{n}.'}</MathBlock>
    <Prose>Thus E[(Qₙ−T)²]→0: <strong>mean-square convergence</strong> to T. This is a precise quadratic-variation statement for these partitions. It does not assert that every arbitrary path-dependent sequence of partitions has the same limit. Reusing nested increments couples the visual experiment; independence between successive resolution levels is neither assumed nor needed.</Prose>
    <Prose>For Xₜ=μt+σWₜ starting at zero, raw squared increments have a finite-grid drift contribution. Centering each increment by μΔt removes it:</Prose>
    <MathBlock>{'\\begin{gathered}E\\sum(\\Delta X)^2=\\sigma^2T+\\mu^2T^2/n,\\\\\\operatorname{Var}\\sum(\\Delta X)^2\\\\=2\\sigma^4T^2/n+4\\mu^2\\sigma^2T^3/n^2,\\\\E\\sum(\\Delta X-\\mu\\Delta t)^2=\\sigma^2T.\\end{gathered}'}</MathBlock>
    <Prose>The browser distinguishes realized sums from these exact model moments. Brownian paths are almost surely nowhere differentiable; that is a theorem about the process, not something proved by a jagged screenshot. Quadratic variation helps explain why ordinary smooth-curve calculus needs modification. The later Itô Calculus lesson develops that modification.</Prose>
    <H3>A fair process depends on the information available</H3>
    <Prose>A <strong>filtration</strong> is the growing information history available by each time. An adapted process only uses information available now. An integrable process M is a <strong>martingale</strong> when E[Mₜ|history through s]=Mₛ for s≤t: conditional expected future value equals present value. A fair random walk, standard Brownian motion and a Poisson count compensated by its mean, N(t)−λt, are examples with their natural histories.</Prose>
    <Prose>“Fair” does not mean constant, bounded or independent of the past. A drifted Brownian X is generally not a martingale; subtract x₀+μt to leave σWₜ. A future-looking rule that bets using the next increment's sign is not an adapted strategy.</Prose>
    <Prose>For a discrete martingale and a stopping time τ≤m with fixed finite m, write Mτ−M₀ as Σₖ₌₁ᵐ1&#123;τ≥k&#125;(Mₖ−Mₖ₋₁). Whether the process has stopped before step k is known at k−1, so each summand has expectation zero. Hence E[Mτ]=E[M₀]. This bounded-stopping proof does not justify every unbounded stopping strategy; merely stopping almost surely is insufficient without further conditions.</Prose>
    <Example name="bridgeVariation" />
    <Prose>The program enumerates every four-step fair-coin history, stopping at reserve displacement ±2 or step 4. The stopped mean is exactly zero. It also checks changed Brownian bridges, quadratic-variation moments and the unconditioned barrier probability. No simulation histogram serves as a substitute for those finite or analytic calculations.</Prose>

    <section id="stochastic-processes-code-route" aria-label="Reusable process implementation boundaries">
      <H3>Keep the random primitive separate from the process law</H3>
      <Prose>The programs above implement transition propagation, departure-count fitting, stable inverse-exponential clocks, censored state exposure and Brownian increment coupling. Python's maintained <Code>random.Random</Code> supplies uniform and Gaussian primitives; the lesson owns how those draws become a process. A seed is meaningful within that generator and call order. Replacing it with NumPy's <Code>default_rng</Code> is a reasonable application choice, but the same integer seed will not reproduce the same path. Compare laws or pass identical increments when testing two algorithms.</Prose>
      <Prose>A dense d-state propagation step costs O(d²); a sparse transition operator can reduce this to its stored edges. K generated event times or Brownian increments take O(K) work and O(K) retained-path storage. Streaming an occupation total or endpoint needs only the current state and sufficient sums, while a plotted path requires retention. The bounded exact rational first-passage solver is a small correctness oracle; large state spaces need a sparse linear solver and a transience/conditioning check.</Prose>
      <Prose>Reuse the concrete downstream owners: <a href="/learn/topic/queueing-theory-m-m-1-m-g-1-little-s-law#queueing-code-route">Queueing's FCFS/SimPy bridge</a> turns arrivals into resource contention; <a href="/learn/topic/it-calculus-stochastic-differential-equations#ito-calculus-code-route">Itô Calculus's Euler/sdeint bridge</a> uses supplied common noise to compare numerical paths. Neither application should resample independent paths and call their difference a discretization error.</Prose>
      <details><summary>Implementation practice: stream a CTMC exposure estimate</summary><Prose>Modify the two-state event loop to retain only departure counts and time spent in each state. Stop at a finite horizon that falls inside the final holding interval.</Prose><details><summary>Solution and checks</summary><Prose>Add the clipped final interval to its state's exposure but do not count an unobserved departure. The exposures must sum to the horizon; estimate each rate using its observed departures divided by its own exposure. A state with zero exposure has an unidentified rate, not a fitted zero. Streaming changes storage, not these sufficient statistics or the stopping rule.</Prose></details></details>
    </section>
    <H2>10. Test the process and transfer the ideas</H2>
    <Prose>Begin a practical model with a concrete event, state and clock. Then examine what the assumptions predict beyond a marginal histogram. Estimate on an earlier window, preserve the model, and inspect a later window. Splitting overlapping increments or adjacent observations randomly across train and test can leak temporal information.</Prose>
    <H3>Fit only what the observations identify</H3>
    <Prose>For completely observed homogeneous Markov trajectories, count actual departures i→j as Cᵢⱼ. Maximizing the conditional path likelihood row by row gives P̂ᵢⱼ=Cᵢⱼ/ΣⱼCᵢⱼ when the departure total is positive. Separate trajectories must remain separate: connecting the last state of one to the first of another invents a transition. If no departure from state i was observed, that row is not identified by this likelihood. A prior or smoothing rule is an additional modeling choice, not a recovered observation.</Prose>
    <Prose>The earlier transition program demonstrates that unobserved row as <Code>None</Code>. An observed state at the final timestamp supplies no next-state transition. Irregularly sampled continuous-time data require a different likelihood or explicit sampling-interval model; a single discrete matrix does not automatically describe all elapsed times.</Prose>
    <Prose>For a constant-rate Poisson process observed over exposure T, the count likelihood is proportional to λᴺe⁻λᵀ, giving λ̂=N/T. Include the whole observed exposure, including the final interval without a departure. If N=0, the MLE is the boundary λ̂=0; it is not proof that the true process can never produce another event. Interval estimates and predictive uncertainty require additional analysis.</Prose>
    <Example name="diagnosis" />
    <Prose>The fitted rate is 2/minute. Two held-out windows both contain six events over three minutes, yet one has per-minute counts (2,2,2) and the other (3,0,3). This difference suggests what to investigate; three bins alone do not establish a statistically reliable rejection. Refit-on-test would hide the forecast error you meant to measure.</Prose>
    <LessonTable caption="A model check should target its mechanism" headers={['Model claim', 'Inspect on held-out data', 'A meaningful limitation']} rows={[['Homogeneous Markov transitions', 'Transitions by time block and previous history beyond current state', 'Sparse rows cannot establish invariance'], ['Constant Poisson rate', 'Counts over comparable exposures; rate by scheduled time', 'Unequal windows need unequal means'], ['Independent increments', 'Runs, serial dependence and clustering of nonoverlapping increments', 'Overlapping increments share observations by construction'], ['Brownian scale law', 'Centered increments divided by σ√Δt; variance versus duration', 'Measurement noise, drift changes and jumps can distort fine scales'], ['Model useful for a decision', 'First-passage, tail events and downstream loss', 'A correct average can miss the expensive failure']]} />
    <Prose>For support tickets with weekday patterns and bursts after outages, a deterministic time-varying Poisson rate addresses the schedule. It does not by itself explain residual dependence or self-excitation. Compare a history-dependent or latent-intensity model only if the held-out residual behavior supports that extra mechanism. Record uncertainty and the cost of missed bursts, not just a best-fitting line.</Prose>

    <H3>Independent practice: change the law, not just the button</H3>
    <Practice prompt="From A, use transition rows A=(½,½,0), B=(0,½,½), C=(1,0,0). Find the probability of A→B→C and the full distribution after two steps. Explain why these are different answers." hint={<Prose>Multiply along the specific path. For the endpoint distribution, sum every two-step route.</Prose>}>
      <Prose>The path probability is ½×½=¼. After one step the law is (½,½,0); after two it is (¼,½,¼). The C endpoint happens only through B here, but the B endpoint has two contributing routes. Check nonnegativity and total mass one.</Prose>
    </Practice>
    <Practice prompt="A chain always alternates A and B. From A, does its marginal approach (½,½)? Does its occupation fraction? Change one assumption to make marginal convergence hold." hint={<Prose>List the first four states. Add a positive chance of staying without disconnecting the states.</Prose>}>
      <Prose>The marginal alternates point masses, while occupation fractions tend to ½. Replacing P by ½I+½P gives both rows (½,½), so from any start the law is uniform after one step. Starting the original chain in its stationary law also makes its marginals uniform, but does not make all other starts converge.</Prose>
    </Practice>
    <Practice prompt="A process chooses a fair bit once and copies it forever. Explain why strict stationarity, ensemble mean ½ and failure of a single-path average to equal ½ are compatible." hint={<Prose>Compare shifting the entire joint law with averaging one selected realization.</Prose>}>
      <Prose>Every finite collection is either all zeros or all ones with equal probabilities, unaffected by time shift. Every single-time mean is ½. One realized path never switches, so its average is 0 or 1. Stationarity alone does not give an ergodic time-average theorem.</Prose>
    </Practice>
    <Practice prompt="A fair reserve walk stops at 0 or 5, starting at 2. Find success probability and expected duration. If 10% of simulated paths remain unfinished at a time limit, may you count them as failures?" hint={<Prose>Use hᵢ=i/B and tᵢ=i(B−i), then distinguish a truncated observation from an eventual event.</Prose>}>
      <Prose>Success is 2/5=.4 and expected duration is 2×3=6 steps. Unfinished paths are censored; treating them as failures biases the success estimate downward. Continue them, bound their contribution or use a justified censored-data method. Report the unfinished fraction.</Prose>
    </Practice>
    <Practice prompt="Compare switch probabilities (.1,.4) with (.01,.04). Starting certainly sunny, calculate both stationary laws and the sunny probabilities after two steps." hint={<Prose>The ratio b/(a+b) controls the fixed point; 1−a−b controls error decay.</Prose>}>
      <Prose>Both stationary laws are (.8,.2). After two steps, sunny probabilities are .8+.2(.5)²=.85 and .8+.2(.95)²=.9805. Equal equilibrium does not imply equal short-horizon forecasts or mixing speed.</Prose>
    </Practice>
    <Practice prompt="Events arrive at 120/hour. In a 30-second interval, find the mean count, probability of no events and probability of at least three. Find the mean wait to the third arrival." hint={<Prose>Convert 30 seconds to 1/120 hour. The third-arrival event is the count upper tail.</Prose>}>
      <Prose>The interval mean is 1. P(0)=e⁻¹≈.367879; P(N≥3)=1−e⁻¹(1+1+½)≈.080301. Mean wait to arrival three is 3/120 hour=90 seconds. A high mean rate does not make a three-event burst certain in a shorter window.</Prose>
    </Practice>
    <Practice prompt="Independently mark each event A with probability .3. The total interval count is Poisson with mean 4. Find the unconditional means and covariance of A/B counts, then their covariance conditional on total 4." hint={<Prose>Unconditionally use independent splitting. Conditionally B=4−A.</Prose>}>
      <Prose>Means are 1.2 and 2.8, and unconditional covariance is zero. Given total 4, A is Binomial(4,.3). The conditional covariance is −Var(A|total4), which equals −4×.3×.7 = −.84. Conditional dependence is consistent with unconditional independence.</Prose>
    </Practice>
    <Practice prompt="A scheduled rate is zero for two minutes and 3/minute for the next two. Find the probability of no events by minute 3 and explain where an operational event at intensity time 1.5 occurs." hint={<Prose>Use accumulated area rather than divide by the initial rate.</Prose>}>
      <Prose>Λ(3)=3, so the probability is e⁻³≈.049787. The operational event occurs at minute 2+1.5/3=2.5. No events occur in the initial zero-rate interval. The first and third minute do not have the same increment law.</Prose>
    </Practice>
    <Practice prompt="A device has On→Off rate 1/hour and Off→On rate 3/hour. Compare long-run time and jump-visit fractions, and inspect the Off row of I+G for a one-hour step." hint={<Prose>Time weights visits by their holding durations; the generator has negative diagonal entries.</Prose>}>
      <Prose>The long-run On fraction is 3/4, while the alternating jump-visit fraction is 1/2. The one-hour Off row is (3,−2), so the first-order approximation is not a probability row. Exact transition probabilities require the matrix exponential or the two-state analytic solution.</Prose>
    </Practice>
    <Practice prompt="Let Xₜ=.2t+1.5Wₜ. Calculate the mean and variance of X₂−X₀.₅, and Cov(X₀.₅,X₂). Would a 95% interval at every plotted time give 95% coverage of the whole path?" hint={<Prose>The increment duration is 1.5. Shared level covariance uses the earlier time.</Prose>}>
      <Prose>The increment mean is .3 and variance is 1.5²×1.5=3.375. Level covariance is 1.5²×.5=1.125. Pointwise 95% coverage is not simultaneous path coverage; a separate maximum or simultaneous-band calculation is needed.</Prose>
    </Practice>
    <Practice prompt="For endpoints x=.2,y=−.1 over Δ=.5 with σ=.8, find the bridge mean and variance at u=.25, then the probability of crossing b=.7 between observations." hint={<Prose>Use the conditional interpolation mean, not an unconditional drift forecast, and substitute both distances to the barrier.</Prose>}>
      <Prose>The mean is .75(.2)+.25(−.1)=.125; variance is .8²(.5)(.25)(.75)=.06. Crossing probability is exp[−2(.5)(.8)/(.8²×.5)]=e⁻²·⁵≈.082085. This exact conditional result assumes constant Brownian scale and drift.</Prose>
    </Practice>
    <Practice prompt="Enumerate a fair walk stopped at its first hit of −2 or +2, or step 4 if neither occurred. Find its stopped-value distribution, expected stopping time and mean. Why does the same proof not justify every doubling strategy?" hint={<Prose>Use all 16 four-coin histories with probability 1/16, even if the stopping rule ignores later coins.</Prose>}>
      <Prose>Stopped values −2,0,+2 have probabilities 3/8,1/4,3/8. Half the histories stop at step 2; the other half stop at step 4, so Eτ=3 and the stopped mean is 0. The proof uses a fixed finite stopping bound and integrability. Unbounded betting sizes or an unbounded stopping horizon require additional conditions.</Prose>
    </Practice>
    <Practice prompt="Build a small temporal audit. Fit a constant rate on arrivals (.2,.4,1.1,1.5,2.2,2.8) observed over three minutes. Keep that fit fixed. Compare held-out arrivals (.1,.2,.3,2.7,2.8,2.9) with (.2,.7,1.2,1.7,2.2,2.7). Also fit transitions from separate paths [0,1,1] and [2,2,0]. State what is and is not identified." hint={<Prose>Count full exposure, use (0,1],(1,2],(2,3], and do not invent a transition between separate paths.</Prose>}>
      <Prose>Training rate is 2/minute, with predicted no-event probability e⁻²≈.135335 for each future minute. Both held-out windows contain six arrivals; their bin counts are (3,0,3) and (2,2,2). This is a diagnostic comparison, not enough evidence alone to prove clustering. Transition rows are (0,1,0), (0,1,0), and (½,0,½), from observed departures only. Concatenating paths would invent a 1→2 transition and corrupt the middle row.</Prose>
      <Prose>Acceptance checks: exposure includes the empty tail; endpoint conventions are explicit; trajectories stay separate; a state with no observed departure returns “unidentified”; output identifies fitted versus held-out quantities; changed inputs recompute every count; no plot is labeled a population proof. Extend with more held-out windows before choosing a more complex arrival model.</Prose>
    </Practice>
    <Prose>You are ready to continue when you can distinguish a path from its law, an increment from a cumulative level, equilibrium from convergence, and a censored observation from a completed event. The actual next topic in this module is <a href="/learn/path/full-curriculum/random-matrix-theory?module=math-foundations">Random Matrix Theory</a>. Queueing Theory then develops waiting systems and countable-state stability; Itô Calculus later develops stochastic integration. These related links do not replace the module's reading order.</Prose>
    <Sources alternatives={<>
      <Prose>For an alternate spoken route, MIT's <a href="https://ocw.mit.edu/courses/6-262-discrete-stochastic-processes-spring-2011/resources/lecture-7-finite-state-markov-chains-the-matrix-approach/">Shan-Yuan Ho lecture on finite-state Markov chains</a> accompanies the matrix investigation. Robert Gallager's <a href="https://ocw.mit.edu/courses/6-262-discrete-stochastic-processes-spring-2011/resources/lecture-4-poisson-the-perfect-arrival-process/">Poisson-process lecture</a> develops the counts/waits connection. Choongbum Lee's <a href="https://ocw.mit.edu/courses/18-s096-topics-in-mathematics-with-applications-in-finance-fall-2013/resources/lecture-17-stochastic-processes-ii/">Brownian-motion lecture</a> offers a more mathematical follow-up. Their official descriptions and lecture identities were checked; the recordings were not watched in full.</Prose>
      <Prose>For a written route, Richard Weber's Markov notes work from finite chains through hitting times and long-run behavior. Read their first-passage equations beside the reserve example. Karl Sigman's Brownian notes give a compact path from random-walk motivation to covariance, martingales and barriers. The relevant source sections below were reviewed; these links supply optional depth rather than prerequisites hidden outside this page.</Prose>
    </>}>
      <li><a href="https://www.statslab.cam.ac.uk/~rrw1/markov/M.pdf">Weber: Markov Chains</a> — definitions, class structure, hitting probabilities, invariant laws, convergence and occupation averages. Apply finite-state and accessibility assumptions explicitly.</li>
      <li><a href="https://ocw.mit.edu/courses/6-262-discrete-stochastic-processes-spring-2011/3a19ce0e02d0008877351bfa24f3716a_MIT6_262S11_chap02.pdf">Gallager: Poisson Processes, chapter 2</a> — counts/waits, independent splitting/superposition and nonhomogeneous time change. Conditional and unconditional independence are different claims.</li>
      <li><a href="https://www.statslab.cam.ac.uk/~mrt31/probability/notes.pdf">Cambridge probability notes: continuous-time Markov chains</a> — section 3.2 connects exponential clocks, finite generators, the matrix exponential and invariant distributions.</li>
      <li><a href="https://www.columbia.edu/~ks20/4106-18-Fall/Notes-BM.pdf">Sigman: Brownian Motion</a> — random-walk motivation, definition, covariance, martingales and first passage. A scalar central limit theorem is motivation, not a full path-convergence proof.</li>
      <li><a href="https://www.columbia.edu/~mh2078/MonteCarlo/MCS_SDEs_MasterSlides.pdf">Haugh: Simulating Stochastic Differential Equations</a> — Brownian-bridge conditioning and barrier correction. The constant-coefficient Brownian formula here is exact; extensions to general SDE paths require approximation analysis.</li>
      <li><a href="https://ocw.mit.edu/courses/15-070j-advanced-stochastic-processes-fall-2013/aca1518a09539a09ddd37428ab0d0268_MIT15_070JF13_Lec7.pdf">MIT Advanced Stochastic Processes: reflection principle</a> — the joint maximum/endpoint argument behind the conditional barrier probability.</li>
      <li><a href="https://ocw.mit.edu/courses/18-s096-topics-in-mathematics-with-applications-in-finance-fall-2013/3b97c6b0c282dd9dc024c4c7ffe3fba8_MIT18_S096F13_lecnote17.pdf">MIT Brownian-motion lecture notes</a> — roughness and quadratic-variation motivation. This page proves its deterministic equal-partition mean-square claim directly from second and fourth moments.</li>
    </Sources>
  </div>
};
