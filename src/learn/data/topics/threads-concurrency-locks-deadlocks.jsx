import { Code, H2, H3, Prose } from "../../components/content";
import { Checkpoint, LessonIntro, LessonTable, Sources } from "../../components/lesson-labs/LessonElements";
import { LearningResources } from "../../components/lesson-labs/LessonInvestigation.jsx";
import { ThreadRaceLab, ThreadConditionLab, ThreadDeadlockLab } from "../../components/lesson-labs/ThreadCoordinationLabs.jsx";


import { RunnableExample } from "../../components/lesson-labs/RunnableExample.jsx";
import { threadExamples } from "../thread-coordination-examples.js";

export default {
 title:'Threads, Concurrency, Locks & Deadlocks',readTime:'~45 min read + 80 min practice',hasIntegratedGuide:true,
 content:()=> <div className="lesson-pilot">
  <LessonIntro prerequisites="OS processes, address spaces and scheduling; Python functions, collections, exceptions and with blocks. No previous multithreading course is assumed." sections={[["1-two-workers-one-shared-world","Threads"],["2-a-correct-step-can-form-a-broken-operation","Races"],["3-protect-an-invariant-not-just-a-line","Locks"],["4-wait-for-a-fact-not-a-notification","Conditions"],["5-when-waiting-forms-a-cycle","Deadlocks"],["7-build-a-bounded-worker-pipeline","Practice"]]}>
   Explain what two workers share, construct a lost update, protect a multi-step rule and diagnose a waiting cycle. Finish a small concurrent program with explicit results, errors and shutdown, instead of hoping a timing delay will make it correct.
  </LessonIntro>
  <H2>1. Two workers, one shared world</H2>
  <Prose>An application is loading files while keeping a progress view responsive. It needs more than one activity to make progress. A <strong>thread</strong> is an execution path inside a process: it has its own current instruction and call-related state, while sharing the process's address space and many resources with its other threads. Two thread functions can reach the same Python list or dictionary.</Prose>
  <div className="wc-grid"><div className="wc-state"><strong>Process address space</strong><p>Shared objects: counter, work queue, configuration, open resources</p><div className="nt-flow"><span>Thread A<small>Instruction position + local references</small></span><span>Thread B<small>Instruction position + local references</small></span></div></div><div className="wc-state"><strong>A second process</strong><p>Its ordinary Python objects are separate. Communicate through messages or deliberately shared storage, as in the OS lesson.</p></div></div>
  <Prose><strong>Concurrency</strong> means several activities can be in progress over the same interval; one CPU can interleave them. <strong>Parallelism</strong> means actual simultaneous execution, for example on different cores. A program can be concurrent without parallel execution. Correctness must hold for every schedule the protocol permits, including an inconvenient pause between two steps.</Prose>
  <LessonTable caption="What belongs to a thread, and what can still be shared?" headers={['State','Mental model','Consequence']} rows={[
   ['Instruction position and call state','Each worker remembers where to resume.','Pausing A need not reset B.'],
   ['A local reference','A name in one function call.','The object it points to may still be shared; local name does not imply private object.'],
   ['Process heap and globals','Objects reachable by multiple workers.','Mutations may be observed by another worker.'],
   ['Files and outside services','Effects beyond local variables.','Separate threads or processes can still interfere through an external resource.'],
  ]}/>
  <Prose>The complete programs use Python's standard library and ordinary CPython 3.12 or later. Save a whole program as lesson_example.py and run it with Python. Avoid names such as queue.py or threading.py, which can hide the standard-library module you intend to import. The browser investigations are abstract legal schedules; they neither create native threads nor emulate interpreter bytecodes. The examples use tiny local data, bounded work and explicit synchronization; no network or hardware is required.</Prose>
  <H3>Start, finish and collect are separate events</H3>
  <Prose><Code>Thread(target=work)</Code> describes a worker. <Code>start()</Code> starts its execution; calling <Code>run()</Code> directly just calls the method in the current thread. <Code>join()</Code> waits for that thread to finish, but does not return the function's result or re-raise its exception in the caller. A timeout on join limits waiting, not the worker's lifetime; inspect <Code>is_alive()</Code> if you need to know whether it finished.</Prose>
  <Prose>A <strong>thread pool</strong> starts a bounded number of workers for submitted function calls. Each submission returns a <strong>future</strong>: a handle for a result that may not be ready yet. <Code>future.result()</Code> waits and returns the value or raises the worker's exception. We use futures in several examples so an error cannot be mistaken for a successful silent worker.</Prose>

  <H2>2. A correct step can form a broken operation</H2>
  <Prose>Two workers each record one completed item by incrementing a shared counter. Starting from zero, the required final value is two. Conceptually, each increment reads the old value, computes one more and writes the result. If both read zero before either writes, both compute one. The second write replaces one with one: an update has been lost.</Prose>
  <ThreadRaceLab/>
  <Prose>A <strong>race condition</strong> is a correctness failure that depends on relative timing or interleaving. A <strong>critical section</strong> is the region that must coordinate access to shared state. An <strong>invariant</strong> is the rule that must remain true at the specified observation boundaries: here, after both increments finish, the total equals the number of completed updates.</Prose>
  <Checkpoint prompt="If you run an unprotected increment test 1,000 times and always see 2, have you proved there is no race?">
   <Prose>No. You sampled schedules. A runtime may make the troublesome interleaving unlikely or unavailable for that exact expression, but a later change can introduce an interruption point. The lab explicitly decomposes the operation; the native example forces both reads before either write. Reason about the whole protocol and documented runtime guarantees.</Prose>
  </Checkpoint>

  <H2>3. Protect an invariant, not just a line</H2>
  <Prose>A <strong>lock</strong> allows one participating worker at a time to enter a protected region. A worker acquires it, performs the operation and releases it. Others attempting the same lock wait. Every access that participates in the protected rule must follow the same protocol. A lock on A's code is ineffective if B accesses the same state without using it.</Prose>
  <div className="nt-flow"><span>Acquire one shared lock<small>May wait; no protected read yet</small></span><span>Read → compute → write<small>The entire increment is protected</small></span><span>Release on block exit<small>Another worker may proceed</small></span></div>
  <RunnableExample example={threadExamples.race}><Prose>A Barrier makes both broken workers finish reading zero before either proceeds. It deliberately exposes the lost update instead of relying on random delays. The repaired version uses one Lock around the complete read/modify/write sequence, and futures surface errors. Do not move the barrier inside a single held lock: the first worker would wait for a second worker that cannot enter.</Prose></RunnableExample>
  <Prose>The lock belongs to the shared operation, not to each invocation. Creating a fresh Lock inside every call would give each caller an unrelated gate. Using <Code>with lock</Code> releases the lock during ordinary exception unwinding; it does not roll back a partially completed change. Validate inputs first and define recovery for any operation that can fail after changing state.</Prose>
  <Checkpoint prompt="Both workers read and compute freely but acquire a lock only for the final write. Can the counter still finish at 1?">
   <Prose>Yes. Each may already hold the stale local value 1. Serializing those two writes still writes 1 twice. The boundary must cover the read on which the write depends, not just the assignment.</Prose>
  </Checkpoint>
  <H3>How much should one lock cover?</H3>
  <Prose>A single lock around a whole workflow can be easy to reason about but makes unrelated work wait. Finer locks can allow more overlap but introduce relationships between protected regions. Begin with the invariant: transferring 10 units must debit one account and credit another without exposing an inconsistent pair to participating readers. Then choose the smallest understandable protocol that preserves it. Avoid slow network calls or callbacks while holding a lock unless that waiting behavior is part of a reviewed design.</Prose>
  <Prose>Correct locking also establishes coordination for observing shared state; mere elapsed time does not. A sleep delays the caller but does not certify that another thread finished or published a particular value. Use locks, events, queues or a documented result channel to establish the relationship you need.</Prose>

  <H2>4. Wait for a fact, not a notification</H2>
  <Prose>A consumer wants an item from a queue. If the queue is empty, holding its lock in a loop would prevent the producer from adding anything. Repeatedly releasing and checking wastes work and can still be difficult to coordinate. A <strong>condition variable</strong> joins a lock with a waiting mechanism: check a predicate under the lock; if false, wait while releasing the lock; reacquire it before checking again.</Prose>
  <ThreadConditionLab/>
  <Prose>The <strong>predicate</strong> is a fact about protected state, such as “the queue contains an item” or “the producer has closed the queue.” A notification invites a waiter to check again. It does not reserve an item or transfer the lock. Another consumer may run first. Store the state, test it while holding the lock, and re-test after waiting.</Prose>
  <RunnableExample example={threadExamples.condition}><Prose>The Event reports that the consumer reached its condition block. In the late-publication case, the main thread acquires the same condition lock before appending and notifying. In the early case, the predicate is already true, so no stored notification is needed. <Code>wait_for</Code> performs the repeated predicate checks and timeout accounting; a false return is handled explicitly instead of popping from an empty list.</Prose></RunnableExample>
  <Prose>For a stream rather than one item, include an end-of-stream state. A consumer can wait for <Code>items or closed</Code>, consume when items exist, and exit when the producer has closed an empty queue. Changing the close flag should notify all waiters that may now finish. This prevents workers from waiting forever after the last item.</Prose>
  <LessonTable caption="Choose a coordination primitive by the fact you need to express" headers={['Primitive','Good fit','What it does not establish']} rows={[
   ['Lock','One protected region at a time.','No count of pending work or automatic rollback.'],
   ['RLock','The same thread must deliberately reacquire the same lock.','Not a repair for cycles involving several different locks.'],
   ['Condition','Wait for a predicate over protected shared state.','Notification is not stored application state or ownership of an item.'],
   ['Event','A persistent set/clear flag, such as cooperative stop requested.','Not a counter of notifications or an automatic thread kill.'],
   ['Semaphore','Limit concurrent use of a finite number of permits.','Permit count alone does not protect every related data invariant.'],
   ['Queue','Exchange work safely; optionally bound buffered items.','Queue safety does not automatically make objects inside it immutable.'],
  ]}/>
  <H3>An application: a live instrument must cope with a slow consumer</H3>
  <Prose>An acquisition thread may produce measurements faster than a display can redraw. An unbounded queue preserves every item but can grow until memory is exhausted. A bounded queue can make the producer wait—<strong>backpressure</strong>. Some displays instead need only the latest sample and deliberately replace older unread samples. A recorder might require every sample and detect overflow as an error. The synchronization primitive and the data-loss policy must match the job.</Prose>
  <Prose>The small work queue below blocks when full and processes every accepted item. It is not a hard real-time acquisition system: Python scheduling, allocation and general-purpose operating systems provide no deadline guarantee here. Later neural acquisition and real-time systems lessons develop timing and buffer policy under their actual constraints.</Prose>

  <H2>5. When waiting forms a cycle</H2>
  <Prose>A waits for something B holds, while B waits for something A holds. Both are alive, but neither can reach the release that the other needs. This is <strong>deadlock</strong>. Draw an arrow from a waiting worker to the worker preventing its progress; a cycle can reveal the problem. In the two-exclusive-lock model below, that cycle is enough to show deadlock.</Prose>
  <ThreadDeadlockLab/>
  <Prose>One repair is a global acquisition order. Give resources stable ordering keys and always acquire lower before higher. The second worker may wait for the first lock, but it cannot hold the higher lock while asking for the lower one, so the circular pattern is removed. Every code path that acquires those locks must obey the order.</Prose>
  <RunnableExample example={threadExamples.transfer}><Prose>Account names determine lock order, independent of transfer direction. Both balances change while both locks are held. Each worker moves at most 50 units from an account initially holding 100, so every transfer in this fixture succeeds regardless of interleaving. Inspect balances only after both workers finish, or acquire the same locks for a consistent concurrent snapshot. This is an in-memory coordination example, not a durable financial transaction system.</Prose></RunnableExample>
  <details><summary>Deeper: the conditions that make lock deadlock possible</summary>
   <Prose>The classic conditions are exclusive use of a resource, holding one resource while waiting for another, no forced preemption of ownership, and circular waiting. A design can prevent this class of deadlock by breaking a required condition. Ordering breaks circular waiting; acquiring all needed resources together can avoid hold-and-wait. A timeout reveals lack of progress, but retrying the same pattern does not prove eventual success.</Prose>
   <Prose><strong>Starvation</strong> means one worker keeps losing its chance while others progress. <strong>Livelock</strong> means workers keep taking actions, such as releasing and retrying in synchrony, without completing useful work. Neither is the same as all involved workers being blocked in deadlock. Lock ordering prevents the demonstrated cycle; it does not promise fair scheduling or solve every application-level dependency.</Prose>
  </details>
  <Checkpoint prompt="A one-worker pool starts a task that submits another task to the same pool and waits for its result. Where is the cycle even though your code created no Lock?">
   <Prose>The outer task holds the only worker slot while waiting for the inner task. The inner task needs that slot to run. Await dependencies from a coordinator that does not occupy the required worker, or structure the tasks so they do not synchronously wait for work in the same exhausted pool. Increasing the pool size may hide rather than eliminate the dependency problem.</Prose>
  </Checkpoint>

  <H2>6. A result needs a lifecycle</H2>
  <Prose>For independent, finite I/O work, a pool avoids managing one raw Thread per task. Submit calls, collect results and inspect failures. Completion order can differ from input order. If a report needs stable ordering, retain identities and order the collected results explicitly. Choosing two workers limits active work; it does not guarantee faster execution or automatically bound an arbitrarily large submission queue.</Prose>
  <RunnableExample example={threadExamples.futures}><Prose>Each worker returns its own result rather than mutating a shared report. Only the coordinator appends to records. The malformed JSON is an explicit error record. Sorting by filename makes displayed output deterministic while leaving execution order free. These tiny files demonstrate a contract, not a useful speed benchmark.</Prose></RunnableExample>
  <Prose><Code>future.cancel()</Code> can cancel work that has not started; it cannot forcibly stop an already running thread. Likewise a result timeout ends your wait, not the task. Use cooperative cancellation: signal a stop request, ensure work reaches a check within a bounded interval, release owned resources and collect the outcome. A blocked I/O call needs its own timeout or cancellation-capable interface.</Prose>
  <RunnableExample example={threadExamples.cancellation}><Prose>The worker acknowledges the Event rather than being killed. The coordinator requests stop even if startup waiting fails. This models an interruptible wait between work units; replacing it with an unbounded blocking call would change the shutdown guarantee. Exiting a pool's with block ordinarily waits for running work, so correct shutdown must be part of the worker contract.</Prose></RunnableExample>
  <H3>The GIL is a runtime detail, not your application lock</H3>
  <Prose>In ordinary GIL-enabled CPython, the Global Interpreter Lock limits simultaneous execution of Python bytecode. Blocking I/O and some native extensions can release it. Optional free-threaded builds, available from Python 3.13, can run Python threads without that global lock, subject to runtime and extension support. A compatible extension can matter to whether the GIL stays disabled.</Prose>
  <Prose>None of those facts turns a multi-operation application rule into a transaction. Coordinate the invariant explicitly. For CPU-heavy Python loops, compare processes, native/vectorized computation and an appropriate runtime; for I/O overlap, threads can be useful. Measure throughput, latency and resource use with realistic work. These lessons were natively verified on ordinary CPython; the browser diagrams establish no performance claims for free-threaded builds or GPUs.</Prose>

  <H2>7. Build a bounded worker pipeline</H2>
  <Prose>Before looking at the solution, design a small validator for sensor records. Two workers take indexed values from a queue with capacity two. A valid finite number produces double its value; a bad record produces an error result. Every input—including zero and a negative number—must have exactly one result. After all inputs, all workers must finish. Preserve input order in the final report, not by forcing worker execution order.</Prose>
  <LessonTable caption="Independent acceptance cases" headers={['Input or event','Required result']} rows={[
   ['[2, 0, "bad", -3]','Four indexed records: 4, 0, error, -6.'],
   ['An empty list','No records; both workers still stop.'],
   ['More items than queue capacity','No silent dropping; producer waits when needed.'],
   ['A nonfinite number or finite value whose doubled result is nonfinite','Explicit error, not missing completion accounting.'],
   ['One worker, then three workers','Same indexed results under changed schedules.'],
  ]}/>
  <details><summary>Hint: distinguish queue removal, task completion and thread completion</summary><Prose>A successful get removes an item but does not signal that processing finished. Put task_done in a finally block after every get, including stop markers. Queue.join waits for that accounting; Thread.join waits for the worker itself. Put the same unique sentinel into the queue once for each worker, and keep the results separate from the bounded work queue so reporting cannot block the producer-consumer shutdown cycle.</Prose></details>
  <details><summary>Show a complete solution</summary><RunnableExample example={threadExamples.queue}><Prose>STOP is a unique object checked by identity, so zero, None or an ordinary input cannot accidentally stop a worker. A full work queue blocks put rather than evicting records. Errors become indexed results; finally keeps completion accounting correct. After workers join, get_nowait collects the known number of results and sorting restores input order. This teaching helper accepts finite lists and is called sequentially; a reusable service would put its queues and worker lifecycle inside an instance or each independent run.</Prose></RunnableExample></details>
  <Prose>Explain why no worker remains after the empty case, why a bad record does not strand Queue.join, and why the final ordering does not imply any particular schedule. For a long-lived service, decide what an unexpected worker failure should do to queued items and clients. Do not turn daemon threads into a cleanup policy: abrupt interpreter shutdown can abandon resources or unfinished work.</Prose>
  <H3>Ready to use the module as a toolkit?</H3>
  <Prose>You should now be able to distinguish an execution path from a process, produce a failing interleaving, defend the boundaries of a lock, explain a condition predicate and trace a shutdown path. This completes Programming &amp; Scientific Computing. In the complete curriculum, the next module is <a href="/learn/track/data-structures-algorithms">Data Structures &amp; Algorithms</a>; the reader's named Next follows the module order selected for your route. Use prerequisite links for deliberate review when a later module needs earlier ideas.</Prose>
  <Sources alternatives={<LearningResources>
   <li><a href="https://pages.cs.wisc.edu/~remzi/OSTEP/threads-locks.pdf">OSTEP: Locks</a> — a deeper mechanism-first treatment after the counter lab; examples use C and discuss implementations below Python's Lock.</li>
   <li><a href="https://pages.cs.wisc.edu/~remzi/OSTEP/threads-cv.pdf">OSTEP: Condition Variables</a> — more producer/consumer reasoning and why a predicate must be rechecked.</li>
   <li><a href="https://www.youtube.com/watch?v=31fXwpb0P9c">Amber Brown: Implementing Concurrency and Parallelism From the Ground Up, PyCon 2017</a> — an alternate video connecting CPU, OS and shared state; see the <a href="https://pycon-archive.python.org/2017/schedule/presentation/443/">official talk description</a>. Its Python/runtime discussion predates optional free-threaded CPython, so use current documentation for that detail.</li>
  </LearningResources>}>
   <li><a href="https://docs.python.org/3/library/threading.html">Python threading</a> — thread lifecycle, Lock, Condition, Event and Barrier contracts.</li>
   <li><a href="https://docs.python.org/3/library/concurrent.futures.html">Python concurrent.futures</a> — results, failures, cancellation, shutdown and nested-future deadlocks.</li>
   <li><a href="https://docs.python.org/3/library/queue.html">Python queue</a> — bounded work, completion accounting and shutdown interfaces.</li>
   <li><a href="https://docs.python.org/3/howto/free-threading-python.html">Python's free-threading guide</a> — build/runtime/extension caveats; recheck against the version you actually run.</li>
   <li><a href="https://pages.cs.wisc.edu/~remzi/OSTEP/threads-bugs.pdf">OSTEP: Common Concurrency Problems</a> — deeper atomicity, order and deadlock analysis.</li>
  </Sources>
 </div>,
};
