import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DifferenceBoundaryFigure, FenwickBlockLab, LazyRangeLab, PrefixCancellationFigure, SegmentRangeLab, SparseOverlapFigure, WeightedRankFigure } from '../../components/lesson-labs/RangeQueryLabs.jsx';
import { MovingMaximumLab, SignedShortestRangeLab } from '../../components/lesson-labs/RangeDequeLabs.jsx';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { rangeQueryExamples } from '../range-query-examples.js';
import rangePractice from '../practice/segment-trees-fenwick-trees-range-queries.js';
import RangeQueryLibraryBridge from '../../components/lesson-labs/RangeQueryLibraryBridge.jsx';

export default {
  title: 'Segment Trees, Fenwick Trees & Range Queries',
  readTime: '~95 min read + 3–5 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot range-query-lesson">
    <LessonIntro prerequisites="Arrays and half-open intervals; the earlier tree/heap indexed layouts; Complexity's invariants and aggregate cost; Binary Search/Sorting's prefix sums and window limitations; Backtracking's combined subarray summaries; the preceding DP lesson's state and witness reasoning. Low bits, lazy maps and deque dominance are derived here before use."
      sections={[
        ['1-choose-the-range-contract', 'Choose the range contract'],
        ['2-cache-ordered-interval-summaries', 'Build a segment tree'],
        ['3-store-binary-prefix-blocks', 'Derive Fenwick cells and jumps'],
        ['4-change-a-range-at-its-boundaries', 'Derive range additions'],
        ['5-defer-work-without-losing-the-answer', 'Compose lazy updates'],
        ['6-use-the-workload-to-choose', 'Compare static and dynamic structures'],
        ['7-retire-moving-range-candidates', 'Prove two different deque methods'],
        ['8-turn-a-dp-scan-into-a-range-query', 'Query compressed ranks and recover a witness'],
        ['9-design-and-test-a-new-summary', 'Independent transfer practice'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      A range query asks for a summary of consecutive positions: a total, a minimum, a best score or another well-defined result. Learn which summaries can be cached, which updates can be delayed, and which moving-window candidates can be discarded permanently. The useful skill is deriving a structure from its operations and invariants.
    </LessonIntro>

    <H2>1. Choose the range contract</H2>
    <Prose>A laboratory records eight readings: [2,1,3,4,0,5,2,1]. Someone asks for the total from position 1 through position 6, then asks about a different interval. Later a corrected reading arrives. Scanning the requested values is simple and correct. Repeating that scan after every small question may be unnecessary work.</Prose>
    <Prose>Our external indices start at zero. The interval <Code>[left,right)</Code> includes left and excludes right: [1,7) contains positions 1,2,3,4,5,6. Its length is right−left, and [3,3) is empty. The valid boundary contract is 0≤left≤right≤n. Element indices instead satisfy 0≤index&lt;n. A prefix of length 7 has end boundary 7; it does not contain element 7.</Prose>
    <PrefixCancellationFigure />
    <Prose>Define P[t] as the sum of the first t values, with P[0]=0. Then P[right] contains the wanted interval plus everything before left. Subtract P[left] to cancel exactly that unwanted beginning. This works with negative values too: it uses addition and cancellation, not a monotonicity assumption. Build n+1 prefix totals in O(n) additions; each query needs one subtraction.</Prose>
    <RunnableExample example={rangeQueryExamples.prefixes}><Prose>There is an entry for the boundary after the final element. Empty queries return zero because a prefix cancels itself. This program intentionally builds a static cache; changing the original list afterward would not repair it.</Prose></RunnableExample>
    <Prose>A correction at index 4 changes every cached prefix ending after index 4. Repairing that suffix can take O(n) work. A different operation also changes what is possible: knowing a prefix minimum is 1 before an interval and 1 after it tells you nothing about the interval's own minimum. The 1 may occur only before it. Minimum has no subtraction-like cancellation that recovers the forgotten values.</Prose>
    <LessonTable caption="Specify the job before selecting the data structure" headers={['Question', 'Why the answer matters']} rows={[
      ['Which summary?', 'Sum can cancel prefixes. Minimum needs a different summary structure. Ordered text cannot be rearranged.'],
      ['Which changes?', 'No changes, point addition, point assignment, whole-range addition and whole-range assignment are different contracts.'],
      ['Which query order?', 'Arbitrary intervals need different support from a fixed-width window moving only forward.'],
      ['Which output?', 'A value alone, its original index, every qualifying range and the shortest range need different state.'],
      ['Which numeric model?', 'Bounds in arithmetic operations assume bounded-cost arithmetic; exact large Python integers have bit costs.'],
    ]} />
    <Prose>All Python programs here use the standard library and Python 3.10 or newer; displayed output was checked with Python 3.12. Save any full example as <Code>range_example.py</Code> and run <Code>python range_example.py</Code>. The interactive models use at most eight small integers and finite snapshots. They show the mechanism, not a benchmark or an embedded Python runtime. First follow sections 1–3; return for range updates, moving candidates and DP after those cells have a clear meaning.</Prose>
    <Checkpoint prompt="You need one sum from a million values, with no future questions. Is a segment tree automatically better than a scan?">
      <Prose>No. A tree still needs to inspect and store the input summaries during construction. A single direct scan answers the task with little extra state. Preprocessing pays when enough later operations reuse it. Count the whole workload, including build time and storage, rather than comparing only one already-built query.</Prose>
    </Checkpoint>

    <H2>2. Cache ordered interval summaries</H2>
    <Prose>A <strong>segment tree</strong> repeatedly divides an indexed interval into its left and right halves. A leaf owns one element; its parent owns those two leaves together; the root owns the whole padded array. Store the requested summary at each node. For sum, a parent's value is its left sum plus its right sum. A query can take a fully covered parent without reading each leaf underneath it.</Prose>
    <Prose>Start with [2,1,3,4]. The left half [0,2) sums to 3; the right half [2,4) sums to 7; the root [0,4) sums to 10. Query [1,4) can take leaf [1,2), value 1, and the entire right half, value 7. They are disjoint, consecutive and cover the requested positions exactly once.</Prose>
    <SegmentRangeLab />
    <H3>What may be combined?</H3>
    <Prose>Combining summaries must preserve the meaning of concatenating adjacent ranges. For three summaries A,B,C, grouping as (A combine B) combine C must agree with A combine (B combine C). This is <strong>associativity</strong>. It lets tree shape change the parentheses without changing the answer. It does not say that A combine B equals B combine A: that separate property is commutativity.</Prose>
    <Prose>An <strong>identity</strong> represents an empty interval and leaves every real summary unchanged on either side. For sum it is 0; for minimum, +∞; for maximum, −∞; for text concatenation, the empty string. Associative combination with an identity is called a <strong>monoid</strong>. The name is useful in library documentation after the two laws are understood. Subtraction fails associativity: (8−3)−2=3, while 8−(3−2)=7. Averaging two averages without their counts also fails to summarize unequal-length groups.</Prose>
    <Prose>The identity makes padding safe. Let size be the smallest power of two at least n, using size=1 for an empty input. Put real leaves at positions size through size+n−1 of the storage list and fill the rest with the identity. Node 1 is the root; children of storage node p are 2p and 2p+1. These are <em>storage indices</em>, distinct from external element indices. A five-element minimum tree must not pad with zero, which would invent a smaller reading.</Prose>
    <H3>Trace the two query boundaries</H3>
    <Prose>Translate external boundaries l,r to storage boundaries size+l,size+r. While left is before right, an odd left boundary is a right child: its left sibling is outside the remaining query, so take this node alone and advance left. An odd right boundary means the node immediately before it is a left child without its right sibling inside the remaining query; decrease right and take that node. The rest consists of whole sibling pairs, so divide both boundaries by two and continue at their parents.</Prose>
    <Prose>Keep two accumulators. Pieces found on the left arrive from left to right, so append them to before. Pieces found on the right arrive from right to left, so <strong>prepend</strong> them to after. Finally combine before then after. On the eight-letter input, [1,7) must produce BCDEFG. Taking the right pieces G and then EF by appending would instead put G before EF. A sum-only test would hide that order bug.</Prose>
    <RunnableExample example={rangeQueryExamples.segment}><Prose>The complete class keeps the original length separate from padded size and rejects out-of-range boundaries. query(l,l) returns the identity, including on an empty tree. set replaces one leaf, then recomputes its ancestor path. The caller supplies a pure associative combine and an immutable identity/summary; this code does not prove those laws for an arbitrary function or protect mutable objects from aliasing.</Prose></RunnableExample>
    <Prose>The query invariant is that the wanted ordered fold equals before, then the unresolved middle nodes, then after. Each taken node is removed from that middle; every ascent groups only adjacent remaining pairs. Termination follows because the remaining boundary interval shrinks up a finite-height tree. Once no middle remains, combining the two accumulators gives the answer. For an assignment, only intervals containing the changed index can be affected, and those form the one ancestor path; recomputing bottom-up restores every such summary.</Prose>
    <Prose>For n≥1, size&lt;2n. Construction computes fewer than size internal combinations and uses O(n) cells. A point assignment climbs O(log n) levels. A query takes at most one boundary node from each side per level, so it uses O(log n) combinations, even for a very long interval. These statements assume bounded-size summaries and constant-cost combination. Joining strings creates and copies output characters; a tree of concatenated strings does not inherit an O(log n) character-time query or O(n) character-storage guarantee. Exact integer addition also costs more as integers grow.</Prose>
    <Checkpoint prompt="A tree stores only the average of each interval. Can it combine a three-element interval averaging 2 with a one-element interval averaging 10 by averaging 2 and 10?">
      <Prose>No: that gives 6, while the four-element average is (3×2+1×10)/4=4. Store (sum,count), combine both components by addition, and divide only for a nonempty final query. The empty identity is (0,0); its average is undefined, so do not divide an empty result. The question is whether the summary contains enough information, not whether a familiar function can be applied to two numbers.</Prose>
    </Checkpoint>

    <H2>3. Store binary prefix blocks</H2>
    <Prose>A <strong>Fenwick tree</strong>, also called a binary indexed tree, caches selected prefix-ending blocks rather than every node of a complete interval tree. For sum with point increments, it offers a compact array and short index loops. We will derive those loops from exactly which elements each cell owns.</Prose>
    <Prose>External elements remain zero-based. Internal cells are numbered 1 through n; there is no useful cell 0. For positive internal i, let lowbit(i) be the value of its least significant set binary bit. Cell i stores the sum of external interval <Code>[i−lowbit(i),i)</Code>. Cell 6, binary 0110, has lowbit 2 and owns [4,6). Cell 4, binary 0100, has lowbit 4 and owns [0,4). Cell 7 owns [6,7).</Prose>
    <FenwickBlockLab />
    <H3>Understand the low bit before using it</H3>
    <Prose>If a positive integer ends with a 1 followed by k zeros in binary, lowbit is 2ᵏ. In a sufficient two's-complement width, negating flips the bits and adds one: the trailing zeros stay zero, that lowest 1 stays one, and every higher bit becomes the opposite. Bitwise AND therefore retains only that 1. Thus <Code>i &amp; -i</Code> isolates the low bit. For 6: 0110 AND 1010 = 0010, giving 2. Python defines these integer bitwise operations as though negative integers had indefinitely many leading sign bits, so the same identity holds for positive i without choosing a fixed width.</Prose>
    <Prose>To query a prefix ending at i, take cell i's trailing block and set i←i−lowbit(i). The new boundary is exactly where the taken block began. This clears one set bit, so the blocks cannot overlap and the process reaches zero after at most O(log n) steps. Prefix 7 takes [6,7), [4,6), [0,4): sums 2+5+10=17. A range sum is still prefix(right)−prefix(left).</Prose>
    <Prose>To increment external element p, start at internal i=p+1. Its own cell contains that element. The next larger stored block containing the entire current block ends at i+lowbit(i). Repeating that addition visits precisely the containing Fenwick blocks, eventually exceeding n. For p=4, the chain is 5,6,8. Query subtracts lowbit to partition a prefix; update adds lowbit to reach larger containing blocks. Neither operation sorts or searches the numeric reading values.</Prose>
    <RunnableExample example={rangeQueryExamples.fenwick}><Prose>The constructor starts each cell with its one element. In increasing index order it contributes the completed block once to its containing parent, giving an O(n) build. Building by calling add for every element is also correct but takes O(n log n) index steps. Keeping a separate values list makes assignment explicit: delta=new−old. The result after assigning index 2 to −1 differs from adding −1.</Prose></RunnableExample>
    <Prose>Each update changes every cached block that includes the altered element, by exactly the same delta. Each query adds a disjoint partition of its prefix. Those two statements establish the maintained sum invariant. Internal zero is excluded because lowbit(0)=0: an update loop starting there would never advance. The native class rejects invalid element indices and permits prefix 0 and empty range sums.</Prose>
    <Prose>The ordinary sum structure supports arbitrary signed increments because addition can undo earlier contributions, and subtracting two prefix sums cancels their shared portion. It does not support arbitrary range minimum queries merely by replacing plus with min: a prefix minimum cannot be inverted, and increasing a formerly minimal element may require recovering forgotten alternatives. Restricted Fenwick variants can maintain prefix maxima under monotone max updates; their narrower contract needs its own proof. Use a generic segment tree when arbitrary point assignments and noninvertible range summaries are required.</Prose>
    <H3>Deeper application: select by cumulative frequency</H3>
    <Prose>Fenwick's original motivation involved cumulative symbol frequencies for arithmetic compression. A useful related task is to store how many occurrences occupy each ordered bucket, then locate the bucket of the k-th occurrence without expanding all duplicates. Counts [2,0,3,1] represent bucket sequence [0,0,2,2,2,3]. The fourth occurrence belongs to bucket 2, not bucket 3 or the fourth distinct bucket.</Prose>
    <Prose>With nonnegative counts, prefix counts never decrease. Find the longest prefix containing fewer than k occurrences; the next element position contains occurrence k. Ordinary binary search over prefix queries costs O(log² n) index work. Fenwick binary lifting can instead inspect one appropriately aligned block per bit, accumulating a prefix directly in O(log n) steps.</Prose>
    <Prose>Begin with accepted prefix length index=0 and remaining rank=k. Try block sizes from the largest power of two not exceeding n downward. At size bit, candidate=index+bit is aligned so tree[candidate] is the next block of that size after the accepted prefix. If it exists and its count is less than remaining, skip that whole block, subtract its count and extend index. Otherwise keep index and halve bit. The accepted prefix always has count below the original k; after the final bit, it is the longest such prefix. Its length is the external bucket index to return.</Prose>
    <RunnableExample example={rangeQueryExamples.frequencySelection}><Prose>The subclass enforces nonnegative integer counts, including after assignment or decrement, and requires rank between 1 and the total count. A zero-count bucket may be skipped. Negative readings were legal in ordinary sum queries; negative frequencies would destroy this search's monotonicity, so this is a different contract. An empty or zero-total collection has no valid rank.</Prose></RunnableExample>
    <Checkpoint prompt="Cell 12 has binary index 1100. Which external positions does it own, where does a prefix query go next, and where does an update go next?">
      <Prose>lowbit is 4, so it owns [8,12): elements 8,9,10,11. A prefix query next visits internal 8. An update next visits internal 16 if that cell exists. The stored cell's index is an excluded external boundary; it is not the last included element's index.</Prose>
    </Checkpoint>

    <H2>4. Change a range at its boundaries</H2>
    <Prose>Suppose many calibration adjustments each add a constant to a whole run of readings. Repeating a point update for each affected element may take O(n log n) work per long range. First consider a simpler workload: receive all adjustments, then materialize the final array once.</Prose>
    <Prose>A <strong>difference array</strong> records changes between neighboring values: d[0]=a[0], and d[j]=a[j]−a[j−1] afterward. Summing differences from 0 through i reconstructs a[i]. Adding x to [l,r) changes only two boundaries: d[l]+=x switches the increment on, and d[r]−=x switches it off. Store an extra boundary at n so an update reaching the end has a valid stop position. Empty ranges change and cancel the same boundary.</Prose>
    <DifferenceBoundaryFigure />
    <RunnableExample example={rangeQueryExamples.differences}><Prose>This version stores only adjustment differences, then adds their running total to each original value. q range additions plus one reconstruction take O(q+n) additions and O(n) extra cells; the original array is preserved. It is an offline method: it does not efficiently answer arbitrary intermediate range sums before reconstruction.</Prose></RunnableExample>
    <H3>Online range addition and point queries</H3>
    <Prose>Put the actual difference array in a Fenwick tree. Each range addition becomes two Fenwick point increments. Reading one actual value a[i] becomes a prefix query over differences through i, namely prefix(i+1). That gives O(log n) range addition and point reading. It does <em>not</em> yet give a range sum of actual values with one ordinary difference-prefix query: that result is a single value, not a sum of values.</Prose>
    <H3>Derive two Fenwick trees for range sums</H3>
    <Prose>Let S(t) be the sum of actual values a[0] through a[t−1]. Difference d[j] contributes to a[j],a[j+1],…,a[t−1], exactly t−j times when j&lt;t. Sum those contributions for j=0 through t−1: S(t)=Σ(t−j)d[j]. Distributing t−j gives S(t)=t·Σd[j]−Σj·d[j], with the same bounds on both sums. Maintain one Fenwick tree of d[j] and another of j·d[j]. Both sums on the right are ordinary prefix queries.</Prose>
    <Prose>At a boundary j, changing d[j] by δ changes the first tree by δ and the second by jδ. A range increment does that at l with +x and at r with −x. Then actual range sum [l,r) is S(r)−S(l). This formula counts how often a boundary contributes; it is not an unexplained pair of index corrections. In particular, the boundary at n is stored but excluded from every prefix S(t) with t≤n.</Prose>
    <RunnableExample example={rangeQueryExamples.rangeFenwick}><Prose>The complete wrapper constructs both trees from the starting array and supports empty intervals. For [2,1,3,4], adding 3 to all values and then −2 to [1,3) gives [5,2,4,7], total 18. A range assignment is not represented by adding one fixed delta at its endpoints unless the current values happen to make that equivalent.</Prose></RunnableExample>
    <Prose>The two-tree method has O(n) build/storage and O(log n) range addition and range sum in arithmetic operations. It is a good fit for this linear update family. More general combinations of summaries and updates require a different law, which leads to lazy propagation.</Prose>

    <H2>5. Defer work without losing the answer</H2>
    <Prose>A segment node representing L values can update its sum after adding x everywhere by computing sum←sum+x·L. It does not need to visit every leaf to know the new total. The difficulty is that its children's stored sums still describe the previous values. Record a pending instruction saying what must reach those children before they are inspected separately. This is <strong>lazy propagation</strong>: postponing necessary descendant work while keeping the aggregate answer correct.</Prose>
    <Prose>For [2,1,3,4], adding 3 to [0,4) changes the root sum from 10 to 22. Record “add 3 to children.” The child sums may still be 3 and 7. A full-cover query can return 22 immediately. A query of only [1,3) must first push the pending addition to both children, updating each child's sum by 3 times its own length. Clear the parent tag after transferring it. Partial updates likewise push before descending and pull the two current child sums on return.</Prose>
    <LazyRangeLab />
    <H3>Separate three meanings of current</H3>
    <Prose>A node's stored sum already includes its <em>own</em> pending update. Its tag represents work owed to its children, not an extra amount still owed to that node. An unvisited descendant may also lack an ancestor's pending action, so its raw stored sum is not necessarily its current logical interval sum. Once tags on the path are pushed, that descendant becomes safe to inspect. During a partial update, ancestors being repaired become current again when recursion pulls them before returning.</Prose>
    <Prose>That is why full-cover query and partial query have different actions. Full cover consumes the node's stored sum once. Partial cover pushes before recursion. Applying its tag to its own sum again would double count. Pushing to children and forgetting to clear the parent tag would apply the same update again later.</Prose>
    <H3>Compose assignment and addition in chronological order</H3>
    <Prose>Use a small map x→m·x+b, with m restricted to 0 or 1. Addition by c is (1,c); assignment to c is (0,c); doing nothing is (1,0). For a segment of length L, this map sends its sum to m·sum+b·L. A pending map can therefore be applied to the aggregate without knowing each leaf.</Prose>
    <Prose>Suppose the old pending map is x→a·x+b, then a new update arrives x→m·x+c. The child must eventually receive the new action <em>after</em> the old one: m(a·x+b)+c=(m·a)x+(m·b+c). So the composed tag is (m·a, m·b+c). An assignment erases the previous tag's effect; an addition shifts whatever pending result already existed. Assignment cannot use zero as a “no assignment” sentinel, because assigning zero is a valid operation.</Prose>
    <LessonTable caption="Chronology is part of the lazy tag" headers={['Old action', 'New action', 'Combined action on each value']} rows={[
      ['add 3', 'add 2', 'add 5'],
      ['set 4', 'add 3', 'set 7'],
      ['add 3', 'set 4', 'set 4'],
      ['set 4', 'set 0', 'set 0, not identity'],
    ]} />
    <RunnableExample example={rangeQueryExamples.lazy}><Prose>The full class supports range add, range set and range sum using half-open boundaries. Leaves have no children, so they need no pending tag. Recursion passes segment boundaries to obtain length. Padding remains untouched because every requested range ends at or before the original n. Empty operations are harmless; invalid boundaries are rejected.</Prose></RunnableExample>
    <Prose>Correctness follows from the same interval decomposition plus the map law. A fully covered node can update its summary and compose its deferred child work exactly. On partial overlap, pushing makes both child summaries include the old action; recurse only into intersecting pieces, then combine the resulting child sums. The outside case changes nothing. Induction on interval length establishes the operation, and halving length establishes termination. A query may change the internal distribution of tags while preserving every logical value.</Prose>
    <Prose>The boundary paths again cross O(log n) levels. Fully covered subtrees stop immediately and outside siblings stop immediately; only the two partial boundaries continue. With constant-size summaries/tags and constant-cost mapping/composition, each operation costs O(log n), construction/storage O(n), and recursive stack depth O(log n). This is not a guarantee for every imaginable update. A summary must contain enough information to transform it, the allowed maps must compose, and applying a map to combined child summaries must agree with combining their mapped results.</Prose>
    <Checkpoint prompt="Could a sum-and-length node apply “replace every value by its square” without inspecting children? Compare [1,3] and [2,2].">
      <Prose>Both have length 2 and sum 4, but their squared sums are 10 and 8. Sum and length do not determine the updated summary. Additional information may support a particular restricted family, but renaming the tag does not create the missing information. Similarly, arbitrary range minimum-capping with sums is not justified by the add/set proof; specialized methods need richer metadata and their own bounds.</Prose>
    </Checkpoint>

    <RangeQueryLibraryBridge />
    <H2>6. Use the workload to choose</H2>
    <Prose>If the array never changes and the question is minimum, a <strong>sparse table</strong> provides another useful tradeoff. Store the minimum for every valid starting position at lengths 1,2,4,8,… . A length-2ᵏ interval is two adjacent length-2ᵏ⁻¹ intervals, so each level is built from the preceding one. There are O(log n) levels with O(n) entries each.</Prose>
    <Prose>For a nonempty query of length L, choose b as the largest power of two at most L. Take one block of length b starting at left and one ending at right. Since b≤L&lt;2b, these blocks cover the entire query and may overlap. Minimum is <strong>idempotent</strong>: min(x,x)=x. Seeing a value twice cannot change a minimum, so combining these two stored minima gives the answer.</Prose>
    <SparseOverlapFigure />
    <RunnableExample example={rangeQueryExamples.sparse}><Prose>The query rejects empty ranges rather than inventing a minimum. bit_length identifies the largest fitting power of two. The code also calculates the overlapping sum to show why using the same two blocks with addition is incorrect. Associativity alone permits regrouping; idempotence is what makes this overlap safe.</Prose></RunnableExample>
    <Prose>Preprocessing and storage are O(n log n), followed by O(1) block lookups and one minimum comparison per query under a machine-word index model. A point change can invalidate many table entries, so this ordinary static table is a poor match for frequent updates. A sum sparse table can combine a disjoint binary decomposition in O(log n) blocks, but static prefix sums already give the sum answer in O(1). Choose the simpler adequate method.</Prose>
    <LessonTable caption="Costs count bounded-size summary operations; n is the array length" headers={['Structure and contract', 'Build / storage', 'Query', 'Change']} rows={[
      ['Direct array scan', 'No summary build / O(1) auxiliary', 'O(range length)', 'Point assignment O(1)'],
      ['Static prefix sum', 'O(n) / O(n)', 'O(1) range sum', 'O(n) worst-case suffix repair'],
      ['Segment tree', 'O(n) / O(n)', 'O(log n) ordered fold', 'O(log n) point assignment'],
      ['Ordinary Fenwick sum', 'O(n) linear build / O(n)', 'O(log n) prefix or range sum', 'O(log n) point increment or assignment with old value'],
      ['Two Fenwick trees', 'O(n) / O(n)', 'O(log n) range sum', 'O(log n) range addition'],
      ['Lazy sum tree with set/add maps', 'O(n) / O(n)', 'O(log n) range sum', 'O(log n) range set or addition'],
      ['Static sparse minimum', 'O(n log n) / O(n log n)', 'O(1) nonempty minimum', 'Ordinary table needs potentially extensive repair'],
    ]} />
    <Prose>These are algorithmic operation counts, not measured speed rankings. Python object overhead, memory locality, input/output, integer size and the operation mix affect elapsed time. Floating-point addition is not exactly associative, so changing a grouping or subtracting nearby large prefixes can change rounding error; exact integer examples isolate the structural reasoning. For measured scientific data, choose precision and error checks deliberately, and revisit the Numerical Analysis material for stability rather than promising bit-identical sums.</Prose>
    <Prose>Other tradeoffs exist. Splitting into blocks of roughly √n elements gives square-root decomposition: scan partial boundary blocks and reuse full-block summaries, often simplifying implementation at O(√n) query work. More dimensions require new indexing and storage analysis; a rectangle sum uses four prefix corners by inclusion–exclusion, while a dynamic 2D Fenwick structure follows low-bit chains in both coordinates. These orientations do not supply a complete higher-dimensional implementation or guarantee that one-dimensional memory bounds carry over.</Prose>

    <H2>7. Retire moving-range candidates</H2>
    <Prose>A fixed-width window moves right by one position at a time. A segment tree could query every maximum in O(log n) each, but this restricted query order allows a different idea: remember only candidates that could still become a future maximum. The earlier Linked Lists lesson introduced the double-ended queue, or <strong>deque</strong>. Here its two ends support two different reasons for removing a candidate.</Prose>
    <H3>Fixed-width maximum: expiry and domination</H3>
    <Prose>Store original indices in increasing order. When position i arrives for width k, any front index ≤i−k is outside the new window and must expire. Then remove back indices whose values are no larger than a[i]. A newer value at least as large beats the older value in every later full window that could still contain the older one, and expires later. That older index can never again be the chosen maximum.</Prose>
    <Prose>Append i. Candidate values now strictly decrease from front to back, so the front holds the maximum whenever k elements have arrived. Removing equal values makes the newest tied maximum win. To keep the oldest tied maximum, remove only strictly smaller values and retain equal candidates until they expire. Values alone are insufficient for either convention: you need indices to know their lifetimes and return identities.</Prose>
    <MovingMaximumLab />
    <RunnableExample example={rangeQueryExamples.movingMaximum}><Prose>The function returns (maximum value, original argmax index) for each complete window. An empty input or a width greater than its length produces no full window; nonpositive width is rejected. It uses collections.deque for endpoint operations, not list.pop(0), which shifts the remaining list elements.</Prose></RunnableExample>
    <Prose>Every forgotten index is either outside the current/future windows or dominated by a newer candidate. If that newer candidate is later dominated, the chain of replacements still ends at a candidate at least as good and no older. Thus a maximum candidate remains represented. The front is the largest survivor because the values decrease along the deque.</Prose>
    <Prose>The inner while loops can remove many indices in one arrival, but every index is appended once and removed at most once in total. There are O(n) endpoint operations across the full scan, giving O(n) total time under the stated comparison/deque model. The live deque uses O(min(n,k)) indices; this function also stores O(max(0,n−k+1)) output pairs. Amortized constant work per arrival does not mean every individual arrival takes constant work.</Prose>
    <H3>Signed shortest-at-least: order prefix boundaries instead</H3>
    <Prose>Now find the shortest <em>nonempty</em> contiguous range whose sum is at least a positive target K. With only nonnegative values, the earlier window method could expand until valid, then shrink while valid. Signed values break that rule. For [1,−1,5], K=5, the whole array first sums to 5. Removing the leading 1 makes the sum 4, so the simple method stops shrinking and misses [5], length 1: removing the next −1 would have restored validity.</Prose>
    <Prose>Use prefix totals. A range [j,i) has sum P[i]−P[j], so for a fixed end i we need P[j]≤P[i]−K. Among useful starts, a later index is preferable because the range is shorter. This gives a dominance relation: if j&lt;k and P[k]≤P[j], then for every future end i&gt;k, starting at k gives at least as much sum and a strictly shorter range than starting at j. Discard j.</Prose>
    <SignedShortestRangeLab />
    <Prose>Keep candidate start boundaries in increasing index order and strictly increasing prefix-total order. At each new end i, do two loops with different proofs. First, while the front satisfies P[i]−P[front]≥K, record its valid range and remove that start. A later ending could only make a range from that same start longer, so it cannot improve on the candidate already recorded. Continue because a later candidate may give an even shorter valid range at this same end.</Prose>
    <Prose>Second, while the back has P[back]≥P[i], discard it by the later-and-no-larger-prefix dominance argument. Append i only after testing earlier starts, so the current end cannot be paired with itself as an empty range. The deque prefix totals are then strictly increasing: if its front fails the threshold, every later candidate fails too. A removed start is either already represented by a no-longer-than-future valid answer or dominated for all future ends, so no optimum is lost.</Prose>
    <RunnableExample example={rangeQueryExamples.signedShortest}><Prose>The output is None if absent, otherwise (length,(left,right)) with right excluded. Equal shortest lengths choose the earliest start. The positive-target contract matches the linked practice task; validate a different target convention before generalizing. The returned witness is a real contiguous slice, whose sum can be checked independently.</Prose></RunnableExample>
    <Prose>Again every prefix boundary is appended once and removed at most once, so the scan uses O(n) deque operations. This implementation materializes n+1 prefixes and may retain O(n) candidates, using O(n) space. The plot shows computed prefix values, not timing or probabilities. A method that counts <em>all</em> qualifying subarrays cannot retire a valid start merely because it has one valid ending; future endings create additional answers. The objective justifies the deletion.</Prose>
    <Checkpoint prompt="In a fixed-width maximum, why may a new equal value replace an old one? In a signed shortest query, why may a new equal prefix replace an old prefix?">
      <Prose>For maximum, the new occurrence has the same value and expires later; the newest-argmax convention prefers it. For shortest sum, the new boundary produces exactly the same future range sums in fewer positions. One proof compares value and lifetime; the other compares prefix total and length. Both use original indices, but the summaries and objectives differ.</Prose>
    </Checkpoint>

    <H2>8. Turn a DP scan into a range query</H2>
    <Prose>The preceding DP lesson described a longest increasing subsequence by considering earlier smaller values. Now give each occurrence a reward w[i], which may be negative, and maximize total reward over a strictly increasing subsequence of values. Input order must be preserved. Permit choosing nothing, with score zero. This changes the summary needed: the smallest tail for each unweighted length does not automatically preserve the greatest reward.</Prose>
    <Prose>Let dp[i] be the greatest reward of a nonempty increasing subsequence ending at occurrence i. Its recurrence is dp[i]=w[i]+max(0, dp[j] over j&lt;i with a[j]&lt;a[i]). The zero option starts a new subsequence. A direct implementation scans all earlier j and costs O(n²). The inner question is now recognizable: among already processed values smaller than a[i], what is the maximum saved score and its endpoint?</Prose>
    <Prose><strong>Coordinate compression</strong> sorts the distinct known values once and maps each to its rank. It preserves equality and order, not numerical distances. Values [100,900,101] map to ranks [0,2,1] in their original input positions; the gap from rank 0 to rank 1 does not mean a physical distance of one in another domain. Process original positions left to right while the tree is indexed by rank.</Prose>
    <WeightedRankFigure />
    <Prose>Before processing i, each rank stores the best nonnegative score ending at that value among earlier occurrences, together with its original endpoint. Query <Code>[0,rank(a[i]))</Code>: the exclusive end excludes equal values and enforces strict increase. Add the current weight, record the predecessor endpoint, then update the current rank with the better of its old record and the new candidate. Overwriting could erase a better earlier subsequence with the same last value.</Prose>
    <Prose>Use summary (score,endpoint). Higher score wins; for equal scores, choose the earlier endpoint, with the empty record (0,−1) winning a zero tie. This deterministic order is associative as a maximum operation. The tree's zero identity represents the permitted empty predecessor. Negative candidates need not replace it because every future extension can start with a zero predecessor instead and do at least as well. If the problem required a nonempty final answer even when all weights were negative, the final selection and identity treatment would need adapting.</Prose>
    <RunnableExample example={rangeQueryExamples.weightedIncreasing}><Prose>Each occurrence gets a separate parent pointer before its rank summary is updated. Recover the witness by following parents from the best endpoint, then reverse it. Equal values cannot extend one another. The default answer uses original indices [1,3,4], values [1,2,4], rewards 2+20+3=25. An all-negative input returns the permitted empty witness, not a fictitious positive reward.</Prose></RunnableExample>
    <Prose>The tree invariant follows by induction over original input positions: its prefix query returns exactly the best admissible earlier endpoint, and max-updating the current rank adds this new possibility without losing previous ones. Parent indices always point backward and values strictly decrease along the backward chain, so reconstruction is finite and gives a legal witness. Compression plus n queries/updates takes O(n log n) comparison/summary operations; ranks, parents and the tree take O(n) storage. The bounded browser walkthrough may scan its tiny coordinate list for display; that is not the native dictionary-based rank lookup's implementation.</Prose>
    <Prose>A frequency query uses the same rank geometry with a different meaning. To count smaller occurrences to the right of each position, scan the original positions right to left; the Fenwick tree stores counts of already scanned occurrences. Query the exclusive smaller-rank prefix, then increment the current rank by one. Repeated equal values contribute separate counts but do not count as strictly smaller. That is a deliberate change from max-score updates, not a copy-and-paste replacement of one operator.</Prose>
    <H3>Where these summaries become useful</H3>
    <Prose>Indexed summaries appear when corrected measurements need repeated interval reports, when a stream needs rolling extrema, when an ordered histogram needs cumulative rank, and when a DP recurrence asks for a best earlier value range. Each use comes from a particular operation contract. A subtree can also become a contiguous range after a depth-first ordering, which the next exercise derives. The data need not have started life as a flat sequence.</Prose>
    <Prose>Historical answers add another dimension: after several updates, what was the interval sum in version 3? Ordinary mutable trees overwrite that information. A persistent tree can create a new root and copy only the changed path while sharing unchanged subtrees. The later <a href="/learn/topic/persistent-data-structures-structural-sharing-versioned-queries">Persistent Data Structures, Structural Sharing &amp; Versioned Queries</a> topic owns that implementation, version identity and memory-lifetime reasoning. This lesson's snapshots are bounded teaching records, not a persistent production data structure.</Prose>

    <H2>9. Design and test a new summary</H2>
    <Prose>Work from a new contract before reopening code. For each exercise, draw a small counterexample or trace, state the invariant, give a baseline oracle, and account for build, update, query, witness and arithmetic costs. Hints and complete explanations start closed so that reading an answer does not replace attempting the task.</Prose>
    <H3>Exercise: a changing maximum subarray</H3>
    <Prose>Maintain an array under point assignments and answer the maximum sum of a <em>nonempty</em> contiguous subarray inside a requested interval. Values may all be negative. For [-2,4,−1,3,−5,2], the whole-range answer is 6. Replacing −5 by 2 changes it to 10. An empty query has no nonempty answer. Derive a constant-size summary and verify every tiny interval against enumeration of all of its subarrays.</Prose>
    <details><summary>Hint · recall what crosses the split</summary><Prose>A best subarray lies in the left half, in the right half, or crosses the boundary. A crossing answer is a left suffix followed by a right prefix. A best prefix may consume the entire left half before entering the right half.</Prose></details>
    <details><summary>Show the complete summary and implementation</summary><Prose>Store (total,bestPrefix,bestSuffix,bestSubarray). For adjacent summaries L,R: total=L.total+R.total; prefix=max(L.prefix,L.total+R.prefix); suffix=max(R.suffix,R.total+L.suffix); best=max(L.best,R.best,L.suffix+R.prefix). A single value x has summary (x,x,x,x). Use None for an empty summary and return the other side when one is empty. This avoids letting zero beat every negative nonempty subarray.</Prose><Prose>The merge is associative because each field is defined by the concatenated sequence independently of its parenthesization; the left/right order still matters. The four values are sufficient for exactly the point-assignment/range-summary task. The earlier Backtracking lesson computed the same sufficient boundary information recursively once; caching it in a segment tree now supports repeated changes.</Prose><RunnableExample example={rangeQueryExamples.richerSummary} /><Prose>Acceptance cases: singleton −2→−2, empty→None, all-negative [−5,−2]→−2, and the changed whole-range example→10. Build/storage are O(n), with O(log n) constant-size merges per assignment or query. Returning a witness range would require carrying consistent boundary identities and tie rules as well; these four numeric fields alone do not return that witness.</Prose></details>
    <H3>Exercise: turn a rooted subtree into an interval</H3>
    <Prose>A fixed organization tree has children 0→[1,2], 1→[3,4], 2→[5], and leaf nodes 3,4,5. Node values are [5,2,7,1,4,3]. Support an individual value change and the sum of all values in any node's subtree, including itself. Design a way to use a range-sum structure. Do not confuse a subtree with an arbitrary path between two nodes.</Prose>
    <details><summary>Hint · record entry and completion</summary><Prose>List each node once when depth-first search enters it. Record the list length at entry and again after all its descendants are complete. Which unrelated node could DFS visit between those two moments?</Prose></details>
    <details><summary>Show the interval argument and complete implementation</summary><Prose>DFS finishes every descendant before returning to a node's parent, so no outside node can appear inside that node's entry-to-completion slice. Conversely every descendant is visited before completion. Thus subtree(u) is exactly [enter[u],leave[u]) in the entry-order list. The order here is [0,1,3,4,2,5]; node 1 owns [1,4), values 2+1+4=7.</Prose><RunnableExample example={rangeQueryExamples.subtree} /><Prose>The explicit enter/exit stack avoids relying on Python recursion depth for a long tree. It validates a rooted child structure that reaches each integer vertex ID exactly once. After increasing node 3 by 6, node 1's subtree sum becomes 13 and the whole-tree sum 28. Test every subtree against an independent traversal, including a chain, a star and the root. Flattening/build take O(n), and fixed-topology value updates and subtree queries take O(log n). Adding or reparenting nodes can invalidate the flattening; arbitrary tree paths require a different decomposition.</Prose></details>
    <H3>Exercise: break an algebraic shortcut</H3>
    <Prose>Propose a counterexample to each shortcut: pad maximum with zero; average two averages without counts; query a six-element sum with two overlapping four-element blocks; treat set-then-add as add-then-set. Give a repair and a test that would catch the error even when the usual positive sample passes.</Prose>
    <details><summary>Hint · use negatives, unequal sizes and operation order</summary><Prose>A wrong identity often survives positive-only tests. An invalid merge may survive equal-length groups. Two updates that happen to commute do not test a composition order.</Prose></details>
    <details><summary>Show counterexamples and acceptance checks</summary><Prose>Maximum of [−5,−2,−4] is −2; zero padding corrupts it, so use −∞. The average example from section 2 gives 4 rather than 6; keep sum and count. On the anchor array, overlapping blocks produce 19 rather than the real [1,7) sum 15; use a disjoint cover or prefix cancellation. On [2,1], set all to 4 then add 3 gives total 14; the reverse gives 8. Also assign zero after a pending addition: the sum must be zero, and a later singleton query must not resurrect the old addition.</Prose><Prose>For update algorithms, generate short mixed operation sequences and maintain a plain list as an oracle. Check all intervals after updates, not only the root. Test non-power-of-two lengths, empty ranges, singleton boundaries, repeated assignments and fully covered updates followed by partial queries. For generic segment folds, add a string-concatenation test so accidental operand reversal cannot hide behind commutativity.</Prose></details>
    <H3>Exercise: change what a deque must return</H3>
    <Prose>For [2,2,2] and width 2, return the oldest tied argmax. Then explain why a deque solving the shortest signed qualifying range cannot directly count every qualifying range. Supply a concrete count counterexample, not just “the objective changed.”</Prose>
    <details><summary>Hint · preserve an equal candidate, then revisit a retired start</summary><Prose>Strictly smaller and smaller-or-equal comparisons make different identity promises. For counting, a start may qualify with several different ending boundaries.</Prose></details>
    <details><summary>Show the changed proof</summary><Prose>Change the maximum algorithm's back-removal condition from ≤ to &lt;. Equal values stay in increasing index order; the front gives oldest indices 0 then 1. Expiry still removes them when they leave, and each occurrence is still appended/removed at most once. For counting, [1,1] with target 1 has three qualifying ranges: [0,1), [1,2), [0,2). Retiring start 0 after its first valid end loses its second valid range. The shortest objective can safely ignore that longer future answer; counting cannot. It needs a method that preserves multiplicities of useful earlier prefixes.</Prose></details>
    <Prose>For direct lazy practice, the official <a href="https://cses.fi/problemset/task/1735/" target="_blank" rel="noopener noreferrer">CSES · Range Updates and Sums</a> asks for range addition, range assignment and sum. Its positions are one-based and both query endpoints are included, so convert [a,b] to [a−1,b) in this lesson's API. It supplies positive initial/update values; add your own zero and signed tests for the broader local implementation. The statement and input format were checked, not submitted to its judge. Large input/output overhead and Python implementation constants still need measuring against that platform's limits.</Prose>
    <DsaPractice practice={rangePractice} />
    <Prose>Before continuing, be able to explain what every stored cell owns, why a discarded candidate can never improve the requested answer, and which laws support a delayed update. A correct data structure is a collection of preserved meanings. Changing the objective, endpoints, update type or witness policy means checking those meanings again. The next module topic, <a href="/learn/topic/algorithm-correctness-loop-invariants-termination">Algorithm Correctness, Loop Invariants &amp; Termination</a>, develops that reasoning more systematically; the complete arguments needed for this lesson have been given here.</Prose>
    <Sources alternatives={<>
      <p><a href="https://www.youtube.com/watch?v=ciHThtTVNto" target="_blank" rel="noopener noreferrer">Kunal Kushwaha · Segment Trees Tutorial: Range Queries and Interview Questions</a> offers a spoken/diagram-led route into interval trees before Java implementation. The original listing/chapter description and <a href="https://github.com/kunal-kushwaha/DSA-Bootcamp-Java/blob/main/lectures/20-trees/code/Segment%20trees/SegmentTree.java" target="_blank" rel="noopener noreferrer">author's companion source</a> were inspected; full video playback was not available in this review. The companion uses explicit linked nodes, inclusive endpoints, Java int and a nonempty-input construction, unlike the padded Python class here.</p>
      <p><a href="https://cses.fi/book/book.pdf" target="_blank" rel="noopener noreferrer">Antti Laaksonen · Competitive Programmer's Handbook</a>, printed pages 81–94, is a compact written route through moving minima, sparse tables, Fenwick blocks, interval trees and differences. Translate its one-based/inclusive examples to the half-open convention here; the book is a useful second explanation, not a reason to copy indexing formulas without deriving them.</p>
    </>}>
      <li><a href="https://atcoder.github.io/ac-library/master/document_en/segtree.html" target="_blank" rel="noopener noreferrer">AtCoder Library · segment tree</a> — precise associative/identity, ordered product and empty-range contracts; costs assume constant-cost supplied operations.</li>
      <li><a href="https://atcoder.github.io/ac-library/master/document_en/lazysegtree.html" target="_blank" rel="noopener noreferrer">AtCoder Library · lazy segment tree</a> — mapping/combination laws and composition order. Its generic C++ interface makes the laws explicit; it is not the Python implementation above.</li>
      <li><a href="https://atcoder.github.io/ac-library/master/document_en/fenwicktree.html" target="_blank" rel="noopener noreferrer">AtCoder Library · Fenwick tree</a> — addition and half-open sum API; distinguish its fixed-width numeric behavior from Python integers.</li>
      <li><a href="https://docs.python.org/3/library/collections.html#collections.deque" target="_blank" rel="noopener noreferrer">Python · collections.deque</a> — endpoint operations and why list removal at the beginning has a different cost.</li>
      <li><a href="https://docs.python.org/3/library/stdtypes.html#bitwise-operations-on-integer-types" target="_blank" rel="noopener noreferrer">Python · integer bitwise semantics</a> — unlimited-precision integers, sign extension and bit_length; the low-bit derivation does not assume an arbitrary fixed Python integer width.</li>
      <li><a href="https://doi.org/10.1002/spe.4380240306" target="_blank" rel="noopener noreferrer">Peter Fenwick · A New Data Structure for Cumulative Frequency Tables (1994)</a> — original compression motivation; publisher abstract inspected, full paper access not claimed.</li>
    </Sources>
  </div>,
};
