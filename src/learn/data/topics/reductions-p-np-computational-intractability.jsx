import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import { CertificateLab, ReductionFlowFigure, ReductionLab, EncodingFigure, CoverTradeoffLab, PathRestrictionFigure } from '../../components/lesson-labs/IntractabilityLabs.jsx';
import { intractabilityExamples } from '../intractability-examples.js';
import practice from '../practice/reductions-p-np-computational-intractability.js';

export default {
  title: 'Reductions, P, NP & Computational Intractability',
  readTime: '~75 min read + 3–4 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot intractability-lesson">
    <LessonIntro prerequisites="Complexity Analysis's input size and worst-case costs; basic Boolean true/false; graphs' vertices and edges; Backtracking and DP's search/state ideas. We introduce logical notation, certificates and each graph selection problem before using them."
      sections={[
        ['1-check-an-answer-before-searching-for-one', 'Certificates, clauses and search'],
        ['2-measure-the-input-and-define-the-classes', 'Input length, P and NP'],
        ['3-transfer-a-solver-through-a-reduction', 'Reduction direction and proof obligations'],
        ['4-turn-clause-choices-into-a-clique', 'A complete SAT-to-graph reduction'],
        ['5-establish-hardness-and-completeness', 'NP-completeness and its limits'],
        ['6-connect-decision-search-and-optimization', 'Recover answers from decision questions'],
        ['7-choose-a-contract-you-can-fulfil', 'Exact, restricted and approximate methods'],
        ['8-practise-the-claim-not-just-the-code', 'Independent proof and design practice'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      Sometimes a proposed answer is easy to check even though finding one is difficult. Learn to describe that gap precisely, move questions between problem domains without changing their answers, and choose useful algorithms when an unrestricted exact solution has no known efficient method.
    </LessonIntro>
    <Prose>All executable examples are complete Python 3.12+ programs using only the standard library. Save a program as <Code>example.py</Code> and run <Code>python example.py</Code>; the printed result appears beneath it. Small exhaustive solvers are deliberate teaching tools. The browser models have explicit size limits and do not run this Python code.</Prose>

    <H2>1. Check an answer before searching for one</H2>
    <Prose>You are configuring three optional features. Some combinations are allowed; others conflict. A colleague sends a configuration. Checking it means reading each rule against that one configuration. Finding a configuration means choosing settings that satisfy <em>all</em> the rules together. A fast checker is useful, but it does not choose the settings for you.</Prose>
    <Prose>Let x1, x2 and x3 be Boolean variables: each is either true or false. A <strong>literal</strong> is a variable or its negation; ¬x1 means “x1 is false.” A <strong>clause</strong> joins literals with OR, written ∨: at least one must be true. Joining clauses with AND, written ∧, requires every clause to be true. A formula that is an AND of OR-clauses is in <strong>conjunctive normal form</strong>, or CNF.</Prose>
    <LessonTable caption="Translate rules before manipulating notation" headers={['Clause', 'Configuration rule']} rows={[
      ['C1 = (x1 ∨ x2 ∨ x3)', 'Enable at least one feature'],
      ['C2 = (¬x1 ∨ x2 ∨ ¬x3)', 'If x1 and x3 are both enabled, x2 must be enabled'],
      ['C3 = (x1 ∨ ¬x2 ∨ ¬x3)', 'If x2 and x3 are both enabled, x1 must be enabled'],
    ]} />
    <Prose>The question “does any assignment make this whole formula true?” is <strong>Boolean satisfiability</strong>, abbreviated SAT. The formula is an <strong>instance</strong>: one particular input to that general problem. An assignment such as x1=false, x2=false, x3=true is a proposed <strong>witness</strong> or <strong>certificate</strong>. Here it makes C1 true through x3, C2 true through ¬x1 and C3 true through ¬x2, so it proves this instance has a solution.</Prose>
    <CertificateLab />
    <Prose>Notice the asymmetry. One accepted assignment settles a yes answer. Rejecting x1=false, x2=false, x3=false only rejects that candidate. To answer no, an exact solver must establish that <em>every</em> assignment fails. The two clauses (x1) and (¬x1) give a short direct contradiction, but not every unsatisfiable formula exposes such a simple contradiction. Testing a few unsuccessful guesses or running out of time proves neither unsatisfiability nor a general time lower bound.</Prose>
    <Prose>With n variables there are 2ⁿ assignments. If L is the number of literal occurrences and m the number of clauses, a direct verifier scans at most L occurrences plus clause/assignment checks. The program below includes validation, so one check costs O(n+m+L) elementary operations with bounded-size indices. Trying every assignment costs O(2ⁿ(n+m+L)) in the worst case and holds only one candidate at a time. This is a correct baseline, not proof that every SAT algorithm must enumerate all assignments.</Prose>
    <RunnableExample example={intractabilityExamples.certificate}><Prose>Signed indices keep the syntax small: 2 means x2 and −2 means ¬x2. None means no assignment exists after exhaustive search; [] is a valid empty assignment. An AND with no clauses is true, whereas an OR with no literals is false. These boundary meanings make composition precise rather than relying on Python truthiness of an answer list.</Prose></RunnableExample>
    <Checkpoint prompt="A formula has four variables. Your checker rejects a submitted assignment after examining one clause. Does that settle whether any other assignment satisfies the formula?">
      <Prose>No. You have rejected this certificate. The failing clause may also rule out other assignments with the same relevant values, but a no answer needs a valid argument covering every assignment, perhaps through complete search, a logical contradiction proof or another exact method. One early verifier rejection can be cheap while the existence question remains unresolved.</Prose>
    </Checkpoint>

    <H2>2. Measure the input and define the classes</H2>
    <Prose>A <strong>decision problem</strong> asks a yes/no question for each input. “Does this graph contain a route from s to t?” and “can these clauses all be satisfied?” are examples. Complexity classes describe whole families of such inputs and the algorithms that work on <em>every</em> input in the family. They do not classify one screenshot, one fixed puzzle, or the speed of one implementation.</Prose>
    <Prose>Measure the length N of a reasonable explicit encoding, usually in bits. A binary integer T needs about log₂(T)+1 digits when T is positive. A graph encoding includes vertices, edges and labels; a formula encoding includes clauses and literal indices. Counts such as n variables or V vertices remain convenient, but a proof must account for large numbers and everything written in the input. A succinct program generating a huge graph is a different input model from an explicit edge list.</Prose>
    <Prose><strong>P</strong> contains the decision problems for which a deterministic algorithm always halts with the correct answer within O(Nᶜ) steps for some fixed constant c. “Fixed” means the exponent does not grow with the input. For example, graph reachability is in P: a traversal settles the question in time polynomial in its explicit representation. This formal notion is robust across ordinary computational models with polynomial simulation overhead.</Prose>
    <Prose><strong>NP</strong> contains decision problems whose yes instances have polynomial-length certificates checkable in polynomial time. More precisely, there must be a verifier V and fixed polynomials p,q such that a length-N instance x is yes <em>if and only if</em> some certificate y of length at most p(N) makes V(x,y) accept, with checking time bounded by q(N) for those certificates. The certificate is not allowed to hide an exponentially long search log that the verifier reads in “one step.”</Prose>
    <LessonTable caption="Read the quantifiers in the NP definition" headers={['Input kind', 'What the verifier must guarantee']} rows={[
      ['Yes instance', 'At least one allowed certificate is accepted; other certificates may fail'],
      ['No instance', 'Every allowed certificate is rejected'],
      ['Both kinds', 'The verifier terminates within its polynomial checking bound'],
    ]} />
    <Prose>SAT belongs to NP: the certificate lists values for the variables occurring in the formula, and each clause can be checked. Unused variables need not be named at all; the teaching program's explicit n-value assignment is an ordinary convenient representation, not permission to encode exponentially many unused variables in a tiny header and demand that many certificate bits. A Hamiltonian-path certificate lists vertices; its checker verifies endpoints when specified, adjacency, uniqueness and required coverage. A feasible certificate must satisfy the actual problem, not merely resemble its output.</Prose>
    <Prose>Every problem in P is also in NP: a verifier can ignore its certificate and run the polynomial decision algorithm. Therefore <Code>P ⊆ NP</Code>. NP means <strong>nondeterministic polynomial time</strong>, not “not polynomial.” Nondeterminism is equivalently captured here by existence of an accepted certificate; it is not a claim that a laptop, parallel processor or random generator tries all possibilities for free.</Prose>
    <Prose>Whether P equals NP remains open, as recorded by the <a href="https://www.claymath.org/millennium/p-vs-np/">Clay Mathematics Institute</a> when checked on 10 September 2026. A diagram drawing P as a strict subset of NP would silently assume an answer. We use the proven inclusion and state the unresolved equality directly. Nor does NP mean “known to be hard”: reachability is in both P and NP.</Prose>
    <H3>Complements and practical speed</H3>
    <Prose>The <strong>complement</strong> of a decision problem swaps yes and no. P is closed under complement: run its terminating algorithm and flip the answer. The class <strong>coNP</strong> consists of complements of NP problems. UNSAT, asking whether a formula has no satisfying assignment, is in coNP by this definition. It is unknown whether NP=coNP. A SAT yes-witness does not automatically supply short certificates for arbitrary UNSAT instances; some individual no instances do have short proofs.</Prose>
    <Prose>Polynomial time is a mathematical growth classification, not a promise of instant results: N¹⁰⁰ is polynomial and usually useless. Exponential search can be useful for small instances or heavily constrained ones. Memory, constants, numeric precision, data structure and instance distribution still matter. Classifications describe worst-case guarantees; a timing plot for a few examples cannot settle P versus NP.</Prose>
    <EncodingFigure />
    <Checkpoint prompt="An algorithm takes O(nT) time where T is a binary-encoded target. Does that establish polynomial time in the input length?">
      <Prose>No. T may be exponentially larger than the number of bits used to write it. A table indexed by all integers 0…T has that many actual entries. The bound is called pseudopolynomial when it is polynomial in item count and numeric magnitude; it becomes polynomial under suitable unary or polynomially bounded-number encodings. The recurrence can be correct while the claimed input-length complexity is wrong.</Prose>
    </Checkpoint>

    <H2>3. Transfer a solver through a reduction</H2>
    <Prose>Suppose you already have a tool that answers question B. To answer question A, transform A's input into an input that the B tool understands, then interpret its answer. A reduction is a precise version of this adapter. It is useful both for reusing solvers and for comparing computational difficulty.</Prose>
    <Prose>A <strong>polynomial-time many-one reduction</strong> from A to B, written <Code>A ≤p B</Code>, is a polynomial-time computable mapping f such that, for every input x, <Code>x is yes for A ⇔ f(x) is yes for B</Code>. One input becomes one target instance; the answer is preserved. The transformation must be defined on all source inputs, handling invalid encodings consistently, and cannot call an unknown A solver to decide what output to manufacture.</Prose>
    <ReductionFlowFigure />
    <Prose>For the forward direction, a yes source must produce a yes target: otherwise the adapter loses a real solution. For the reverse direction, a yes target must imply a yes source: otherwise it invents a solution. Together they also preserve no answers. Examples that happen to work are not a proof of either universal statement.</Prose>
    <LessonTable caption="What follows from A ≤p B?" headers={['Additional fact', 'Valid conclusion', 'Reason']} rows={[
      ['B has a polynomial-time solver', 'A has a polynomial-time solver', 'Convert, solve B, return its answer'],
      ['A has no polynomial-time solver', 'B has no polynomial-time solver', 'Otherwise the constructed A solver contradicts the premise'],
      ['A has a polynomial-time solver', 'No general conclusion about B', 'B may contain much harder instances beyond the converted ones'],
      ['B is hard', 'No general conclusion that A is hard', 'An easy subset can be embedded in a hard family'],
    ]} />
    <Prose>The second row is conditional on a real premise or a stated assumption. Since we have not proved SAT is outside P, reducing SAT to B yields “if B were in P, then SAT would be in P,” rather than an unconditional impossibility theorem. Reductions can also establish unconditional lower bounds when their source has an independently proved lower bound under the relevant model.</Prose>
    <Prose>The adapter may enlarge the instance. If conversion takes O(N²) steps, its explicitly written output has at most O(N²) symbols. If the B solver takes O(M³) on length M, the composition is bounded by O(N²+N⁶), still polynomial. An output with 2ᴺ vertices is not a polynomial reduction merely because a sentence describes it. “Reduction” refers to transferring the problem, not necessarily shrinking the data.</Prose>
    <Prose>Reductions compose. If A ≤p B by f and B ≤p C by g, use g(f(x)). Both answer equivalences hold, and a polynomial in a polynomial remains polynomial. This transitivity is how one carefully established difficult source can support many later hardness results. It also explains why the exact reduction type matters: this lesson's many-one mapping uses one target instance, whereas a later oracle wrapper makes several adaptive queries.</Prose>
    <Checkpoint prompt="You want to prove your new problem B is at least as hard as SAT. Which direction must you construct?">
      <Prose>SAT ≤p B. A hypothetical fast B solver would then solve SAT. The opposite direction, B ≤p SAT, is useful for using a SAT solver but does not establish that B is hard. Even trivial problems can be encoded as SAT formulas.</Prose>
    </Checkpoint>

    <H2>4. Turn clause choices into a clique</H2>
    <Prose>We now build a complete reduction instead of relying on the arrow. Use a CNF formula with m clauses, each containing at most three literals. This version of <strong>3SAT</strong> permits shorter clauses; a nonempty short clause can be padded by repeating a literal when an exactly-three convention is required. We retain literal <em>occurrences</em>: x1 in C1 and x1 in C3 are two separate positions with the same truth meaning.</Prose>
    <Prose>A <strong>clique</strong> is a subset of graph vertices with an edge between every distinct pair. Its decision problem asks whether a simple undirected graph has a clique of size at least the nonnegative integer k. If k exceeds the number of vertices, reject immediately. Otherwise, if one larger than k exists, any k of its vertices also form a clique, so a certificate of exactly k distinct vertices suffices. The checker verifies all k(k−1)/2 pairs; a huge encoded k does not require a huge certificate to reject an impossible request.</Prose>
    <Prose>The conversion has three steps. Make one graph vertex for each literal occurrence. Add an edge between occurrences precisely when they come from different clauses and are not a variable paired with its negation. Set the target k=m. The edges mean “these choices can consistently witness two different clauses.” There are no within-clause edges, because choosing several literals from one clause must not substitute for satisfying the others.</Prose>
    <ReductionLab />
    <H3>Prove both implications</H3>
    <Prose><strong>From assignment to clique:</strong> suppose the formula is satisfiable. In each clause choose one literal that is true under that assignment. You chose m distinct occurrences from different clauses. No two are complementary, because an assignment cannot make both x and ¬x true. Every pair therefore has an edge, giving an m-clique. The witness transformation is constructive: it tells you which vertices to select.</Prose>
    <Prose><strong>From clique to assignment:</strong> suppose the graph has an m-clique. There are m clause groups and no edges inside a group, so the clique contains exactly one occurrence from each group. Every pair is noncomplementary. Set each selected positive literal's variable true and each selected negative literal's variable false; there is no conflict. Set unmentioned variables arbitrarily. Each clause has its selected true literal, so the formula is satisfied.</Prose>
    <Prose>For m=0, the empty formula and empty clique are both yes. If an empty clause is present, it contributes no vertex: at most one can be chosen from each remaining group, so an m-clique is impossible. The browser editor focuses on nonempty clauses for readability; the native conversion and its tests cover these boundary cases. Duplicate literals remain distinct vertices, but their shared truth value does not invalidate the argument.</Prose>
    <Prose>There are at most 3m vertices and O(m²) candidate pairs. Constructing all edges takes quadratic pair work, with polynomial index/encoding costs; it does not solve the formula. The target graph can be much bigger than the formula but remains polynomial in its encoded input length. Enumerating cliques is a separate solver cost, and the displayed exhaustive oracle is only for small examples.</Prose>
    <RunnableExample example={intractabilityExamples.clique}><Prose>Chosen occurrence IDs [0,4,6] correspond to x1 in C1, x2 in C2 and x1 in C3. Repeated x1 choices agree. The recovered assignment is true,true,false. A different valid clique may recover a different assignment; the reduction preserves existence, not the number of solutions or a one-to-one correspondence between witnesses.</Prose></RunnableExample>
    <H3>Carry the same subset through two more graph questions</H3>
    <Prose>An <strong>independent set</strong> contains no edge between any two selected vertices. The <strong>complement graph</strong> has the same vertices and contains precisely the edges missing between distinct vertices of the original. Thus S is a clique in G exactly when S is independent in the complement of G.</Prose>
    <Prose>A <strong>vertex cover</strong> is a set C touching at least one endpoint of every edge. In any graph H, S is independent exactly when the remaining vertices C=V\S form a cover: an uncovered edge would have both endpoints inside S, and that is exactly the forbidden independent-set condition. Therefore a clique of size at least k in G exists exactly when the complement of G has a vertex cover of size at most V−k. Both changing the edges and changing the threshold are necessary.</Prose>
    <RunnableExample example={intractabilityExamples.complement}><Prose>The original triangle on 0,1,2 becomes an independent triple in the complement. The remaining vertex 3 covers its edges. Using the original graph instead of its complement would ask a different question: the triangle contains edges among the proposed independent vertices.</Prose></RunnableExample>
    <Prose>These identities also explain an application model: vertices can represent incompatible choices, with edges marking conflicts. Keeping a compatible collection is independent set; deleting a collection that touches every conflict is vertex cover. The meanings of vertices and edges decide which model applies. A graph edge standing for friendship instead gives a different interpretation; do not choose a problem from the drawing alone.</Prose>

    <H2>5. Establish hardness and completeness</H2>
    <Prose>A problem B is <strong>NP-hard</strong> under polynomial many-one reductions when every problem in NP reduces to B. A decision problem is <strong>NP-complete</strong> when it is both NP-hard and in NP. The first property concerns the ability to encode other problems; the second concerns a polynomial verifier for B's own yes certificates. Neither property can replace the other.</Prose>
    <Prose>The foundational <strong>Cook–Levin theorem</strong> establishes that SAT is NP-complete. Its central idea encodes a polynomially bounded verifier computation and an unknown certificate as Boolean consistency constraints. The formula asks whether an accepting computation exists. A full proof specifies the machine model and local transition encoding; that construction is beyond this algorithms lesson. We use the theorem as an explicitly identified starting result, not as something proved by our three-variable lab. The form with at most three literals per clause is NP-complete as well, using a polynomial clause conversion such as the optional branch below.</Prose>
    <Prose>From that starting theorem, our reduction establishes CLIQUE's NP-hardness: for any A in NP, A reduces to 3SAT, which reduces to CLIQUE, and composition preserves polynomial time. CLIQUE is also in NP because a list of k distinct vertices can be checked against the edges in polynomial time. Hence CLIQUE is NP-complete. The complement/cover transformations, together with their direct certificate checkers, similarly establish the decision versions of INDEPENDENT SET and VERTEX COVER as NP-complete.</Prose>
    <LessonTable caption="A two-part NP-completeness proof" headers={['Obligation', 'For the clique decision problem']} rows={[
      ['Membership: B ∈ NP', 'A bounded list of vertices; check count, distinctness, validity and every pair'],
      ['Hardness: known NP-complete A ≤p B', 'Convert 3SAT clauses to occurrences and compatibility edges'],
      ['Reduction correctness and cost', 'Prove assignment→clique and clique→assignment; polynomial output and conversion'],
      ['Consequence', 'A polynomial exact CLIQUE solver would imply P=NP'],
    ]} />
    <Prose>NP-hard does not by itself promise a short certificate or even decidability. NP-complete decision problems are decidable: one can enumerate every polynomially bounded certificate and run the verifier. By contrast, the general halting problem is undecidable, a stronger and different obstacle than an unknown efficient algorithm. Optimization and search tasks are usually called NP-hard via their connection to a hard decision problem; “in NP” is a statement about a specified decision language, not automatically about a function returning an optimum.</Prose>
    <Prose>If one NP-complete decision problem were in P, all of NP would be in P and therefore P=NP. If P≠NP, none has a polynomial-time exact algorithm for all inputs under this model. This conditional statement does not prove that a particular instance is slow, that every algorithm takes exactly 2ⁿ time, or that approximation, randomness, a heuristic or a restricted input cannot help. It also does not classify every NP problem as either currently easy or NP-complete.</Prose>
    <details><summary>Deeper branch · why fresh variables can shorten clauses</summary>
      <Prose>A long clause (a∨b∨c∨d) can be replaced by (a∨b∨z)∧(¬z∨c∨d), where z is fresh. If a or b is true, choose z=false; if both are false but c or d is true, choose z=true. Conversely, if a,b,c,d are all false, the two new clauses demand both z and ¬z, so no extension works. Thus the original clause is true <em>if and only if there exists a choice of z</em> satisfying the replacement. It is not equivalent for every arbitrary fixed z.</Prose>
      <Prose>For longer clauses, chain this split with a new variable at each step. Different source clauses receive different auxiliaries so their choices do not interfere. A length-r clause with r&gt;3 adds r−3 variables and r−2 clauses, linear output size. Clauses of length 0…3 are retained, preserving the boundary meanings introduced earlier.</Prose>
      <RunnableExample example={intractabilityExamples.threeCnf} />
      <Prose>The mathematical transformation has linear output size. This deliberately readable Python implementation repeatedly copies the shrinking <Code>remaining</Code> list, so its worst-case construction time is O(Σ r²) across clause lengths, plus validation, rather than the linear bound of an indexed implementation. It is still polynomial. Its final output occupies O(n+m+L) symbols/indices before their bit lengths are counted. A proof of polynomial reduction need not claim the fastest possible implementation.</Prose>
    </details>

    <H2>6. Connect decision, search and optimization</H2>
    <Prose>The yes/no form makes classification clean, but a user normally wants the configuration, route or selected vertices. A decision reduction guarantees answer equivalence. It does not automatically provide a procedure for recovering every target witness or an optimal source solution. We explicitly constructed witness recovery for the clique reduction; other reductions may need additional reasoning.</Prose>
    <Prose>For SAT, a decision solver can recover one assignment through <strong>self-reduction</strong>. First ask whether the formula is satisfiable. If not, return no witness. Otherwise try fixing x1=false by appending the unit clause (¬x1). If the modified formula is satisfiable, keep that choice; if not, x1=true must have a satisfying extension because the current formula was satisfiable. Repeat for each variable, always retaining all earlier fixed choices.</Prose>
    <Prose>The invariant is that the current formula remains satisfiable and respects the chosen prefix. After n choices, the prefix is a complete satisfying assignment. There are n+1 oracle queries, each on a formula only polynomially larger than the original explicit-variable instance. Therefore a hypothetical polynomial SAT decision solver would yield polynomial witness search. The supplied oracle below is exhaustive: the wrapper's small number of calls does not make those calls cheap.</Prose>
    <RunnableExample example={intractabilityExamples.selfReduction}><Prose>The returned pair contains the assignment and the number of decision queries. A contradictory formula uses one query. A satisfiable three-variable formula uses four. Copying clause lists and appending unit clauses is polynomial overhead; the concrete demonstration still has exponential worst-case oracle work.</Prose></RunnableExample>
    <Prose>Optimization often connects to decision through a threshold. For unweighted vertex cover, ask “is there a cover of size at most k?” Feasibility is monotone in k, so binary search between 0 and V finds the optimum value using O(log(V+1)) decision queries. Finding an actual cover still requires witness recovery, such as testing forced choices or using the bounded solver below. A number alone does not identify which vertices to choose.</Prose>
    <Prose>For integer-cost optimization, include the bit length of objective bounds and the feasibility predicate's own cost. For arbitrary real objectives, a threshold oracle does not automatically produce an exact representable optimum after finitely many binary-search steps. The claim depends on the output contract and representation, just as numerical root finding has a different contract from integer boundary search.</Prose>
    <Checkpoint prompt="Why must the SAT wrapper keep earlier unit clauses when testing the next variable?">
      <Prose>Each answer certifies an extension of the current prefix. If previous choices are dropped, two separately feasible choices may have no common extension. For (x1∨x2), x1=false is possible and x2=false is possible when asked separately, but choosing both false fails. The cumulative constraints preserve compatibility.</Prose>
    </Checkpoint>

    <H2>7. Choose a contract you can fulfil</H2>
    <Prose>Hardness analysis is useful when it changes an engineering decision. Start by specifying the exact input family and output requirement. Is a small-instance exact answer enough? Are numbers bounded? Does the conflict graph have special structure? Is a feasible answer with a certified gap acceptable? A timeout means “this run did not settle the question,” not “no solution.”</Prose>
    <H3>Use numerical bounds consciously</H3>
    <Prose>For nonnegative subset sum, the question is whether some once-only item occurrences total T. DP can track reachable totals 0…T. When considering a value v, a newly reachable total s inherits reachability from s−v. Process s downward so the current item cannot create a predecessor that is then reused during the same pass. Store a predecessor only when a total first becomes reachable; that predecessor was already reachable using earlier items, so reconstruction cannot reuse this item.</Prose>
    <RunnableExample example={intractabilityExamples.subset}><Prose>The witness [1,2] selects values 7 and 9, totaling 16. Duplicate values retain distinct indices. Zero-valued items create no new reachable total; target zero returns the valid empty subset. None signals absence, so an empty witness must not be mistaken for failure.</Prose></RunnableExample>
    <Prose>The array initialization and scans cost O(n+T+nT) elementary operations, and arrays use O(T) cells; a found witness uses at most n indices. The integers and index arithmetic have bit costs in a full encoded analysis. This is the familiar pseudopolynomial algorithm, not a proof that binary-encoded SUBSET SUM is in P. The printed billion-slot count is arithmetic only: the sample does not allocate that table. A small item count may instead favor subset enumeration or meet-in-the-middle; choose by the actual dimensions and witness requirements.</Prose>
    <H3>Concentrate exact search into a parameter</H3>
    <Prose>Consider vertex cover with a budget k. Pick any still-uncovered edge (u,v). Every valid cover must choose u or v, possibly both. Branch on those two necessary possibilities: select an endpoint, remove all edges it covers, and reduce the remaining budget by one. With no edges, return the selected set. With edges left and budget zero, reject that branch. This is exact because the two branches include every feasible cover; it is not a greedy guess of the better endpoint.</Prose>
    <Prose>Depth is at most min(k,V), with at most two children per call. Writing k′=min(k,V), there are at most 2^(k′+1)−1 calls. Each call scans at most E edge pairs and successful unwinding copies a short witness list. With normalization, the implementation takes O(V+E+2^k′(E+1)+k′²) elementary work as a safe bound; its active copied edge lists use O(k′E) storage plus input and the witness. Python recursion limits and memory still constrain large k.</Prose>
    <RunnableExample example={intractabilityExamples.parameter}><Prose>A triangle has no cover of size one: whichever endpoint of the first edge you choose, the opposite edge remains. Budget two succeeds. A large star is different: selecting its center covers every edge with k=1. The parameter can be small even when the graph has many vertices.</Prose></RunnableExample>
    <Prose>An algorithm of the form <Code>f(k) · Nᶜ</Code>, with c independent of k, is <strong>fixed-parameter tractable</strong> with respect to k. Our exponential factor depends on cover budget, rather than V alone. The bound Nᵏ is polynomial for each fixed k but does not have the same fixed exponent for all k; it is a weaker kind of parameter guarantee. Neither form promises practical speed for arbitrary k. Changing the chosen parameter may change what is tractable.</Prose>
    <H3>Return a feasible answer with a proved bound</H3>
    <Prose>If optimality may be relaxed, an <strong>approximation algorithm</strong> supplies a feasible answer and a stated worst-case relationship to the optimum. For unweighted minimum vertex cover, scan edges in order. Whenever neither endpoint has been selected, add <em>both</em>. The edges chosen this way share no endpoints, so they form a <strong>matching</strong>. By the end no edge can be added to this matching without sharing an endpoint: it is maximal, meaning unextendable, not necessarily maximum-size.</Prose>
    <CoverTradeoffLab />
    <Prose>Let M be the selected matching and OPT the smallest cover size. Any cover needs at least one endpoint of every edge in M. Since these edges are disjoint, these requirements need distinct vertices: |M|≤OPT. Our algorithm chooses exactly 2|M| vertices. It also covers every edge, because an edge with both endpoints unselected would have been added when scanned. Therefore <Code>OPT ≤ |C| = 2|M| ≤ 2·OPT</Code>. This is a <strong>2-approximation</strong>, with the matching itself serving as an inspectable lower-bound certificate.</Prose>
    <RunnableExample example={intractabilityExamples.approximation}><Prose>On a star, the algorithm may choose the center and one leaf, size two, even though the center alone is optimal. It meets its factor-two contract without being exact. On an edgeless graph it returns the empty cover and empty matching; use the inequality |C|≤2OPT rather than dividing 0 by 0.</Prose></RunnableExample>
    <Prose>The native scan uses expected O(E) set operations after expected O(E) duplicate normalization; sorting the returned selected labels adds O(V log(V+1)) as a safe bound. Its storage is O(V+E), including normalized input and the certificate. The browser's exact comparison performs additional bounded search for teaching and is not part of the approximation algorithm's complexity.</Prose>
    <Prose>The guarantee is about <em>vertex count</em>. If vertices have unequal costs, choosing both endpoints of an edge can be arbitrarily expensive relative to the cheaper endpoint. The same proof does not establish a weighted factor bound. Similarly, an arbitrary heuristic's feasible cover gives an upper bound on OPT but needs a separate lower bound or proof to certify its quality. An approximation to an optimization problem need not answer its exact threshold decision version correctly.</Prose>
    <H3>Exploit a real structural restriction</H3>
    <Prose>The general independent-set problem is NP-complete in decision form. A path has a much simpler dependency structure. Give each position an integer reward; neighboring positions conflict, and selecting nothing is allowed. For the first i positions, an optimal set either excludes position i−1, leaving the first i−1 positions, or includes it, forcing i−2 to be excluded and leaving the first i−2 positions. No other future conflict reaches across that boundary.</Prose>
    <PathRestrictionFigure />
    <Prose>With <Code>best[i]</Code> the maximum reward in the first i positions, use <Code>max(best[i−1], weight[i−1]+best[max(0,i−2)])</Code>, with best[0]=0. The alternatives exhaust every valid solution; each recurrence option combines compatible choices. Store which option wins and walk backward by one or two positions to return a witness. Ties here prefer exclusion; many equally good witnesses may exist.</Prose>
    <RunnableExample example={intractabilityExamples.path} />
    <Prose>This implementation uses O(n) arithmetic/comparison operations and O(n) stored values for reconstruction, with the usual integer bit-cost caveat. Extra long-range conflict edges invalidate the prefix summary: a previously selected position may conflict with a much later one even when the immediately preceding position was excluded. A theorem for arbitrary graphs does not make their path restriction hard, and a path algorithm does not solve arbitrary graphs.</Prose>
    <LessonTable caption="Choose the guarantee, then choose the method" headers={['Situation', 'Useful contract', 'What still needs checking']} rows={[
      ['Few variables or strong constraints', 'Exact enumeration/backtracking with a witness', 'Worst-case work, pruning soundness and timeout status'],
      ['Small nonnegative target', 'Pseudopolynomial exact DP', 'Numeric magnitude, once-only semantics and memory'],
      ['Small vertex-cover budget k', 'Exact parameterized edge branching', 'Parameter size, full branch coverage and copied-state cost'],
      ['A verified path conflict structure', 'Polynomial exact prefix DP', 'Whether extra edges invalidate the state'],
      ['Unweighted cover permits a gap', 'Feasible 2-approximation plus matching lower bound', 'Feasibility, unweighted objective and the actual bound'],
    ]} />
    <Prose>One useful application of SAT is configuration validation: a true variable can mean installing a component, an implication can encode a dependency, and a negative pair can encode an incompatibility. A solver's satisfying assignment becomes a candidate configuration that a separate checker can validate. This lesson's features illustrate that modeling pattern; they are not a claim about a particular package manager's implementation. Industrial SAT/constraint solvers can exploit structure and learned conflicts far beyond this exhaustive code. Their practical successes do not provide a polynomial worst-case SAT algorithm, and “unknown” or timeout must remain distinct from a proved unsatisfiable result.</Prose>

    <H2>8. Practise the claim, not just the code</H2>
    <Prose>For each exercise, write the input/output contract and your reasoning before revealing support. A small counterexample can refute a universal claim; a collection of successful examples cannot prove one. For a reduction, always supply both implications and size/time bounds. For an algorithm, return and check the relevant witness.</Prose>
    <details><summary>Exercise 1 · diagnose a false hardness proof</summary>
      <Prose>A colleague encodes graph reachability as SAT and concludes reachability is NP-hard. Identify the gap. Then state a correct consequence of the encoding.</Prose>
      <details><summary>Hint</summary><Prose>Draw the conversion arrow and place the hypothetical solver at its destination.</Prose></details>
      <details><summary>Explained solution</summary><Prose>The construction gives REACHABILITY ≤p SAT, so a SAT solver can answer the reachability instances. It does not put SAT inside reachability. Hardness would require a known NP-hard source reducing to reachability. Reachability already has a polynomial traversal algorithm; such a reverse reduction from SAT would imply P=NP. The error is direction, not the usefulness of encoding an easy subproblem as SAT.</Prose></details>
    </details>
    <details><summary>Exercise 2 · break an incorrect occurrence gadget</summary>
      <Prose>Someone adds edges between every two noncomplementary occurrences, even inside the same clause. Test the formula (x1∨x1∨x1)∧(¬x1∨¬x1∨¬x1), with target k=2. Then explain why merging occurrences with the same literal is also unsafe for the original construction.</Prose>
      <details><summary>Hint</summary><Prose>Can two vertices from one group impersonate coverage of two clauses? What happens to (x1)∧(x1) after merging?</Prose></details>
      <details><summary>Explained solution</summary><Prose>The formula is unsatisfiable, yet the wrong gadget has an edge between two positive occurrences in its first clause, producing a size-two clique. The target yes answer is spurious: reverse implication fails. In contrast, merging the two occurrences of x1 in (x1)∧(x1) leaves only one vertex while the target remains two, losing a real yes instance. Group and occurrence identity are both part of correctness.</Prose></details>
    </details>
    <details><summary>Exercise 3 · check the encoding argument</summary>
      <Prose>A proposed reduction writes a table for every subset of N input objects, but each entry is only one bit. Another writes O(N²) output symbols and uses an O(M⁴) target solver. Which is guaranteed polynomial, and why?</Prose>
      <details><summary>Hint</summary><Prose>Count output positions before considering the size of one position.</Prose></details>
      <details><summary>Explained solution</summary><Prose>The first writes 2ᴺ bits, exponential in N, even though each entry is tiny. The second is polynomial if its converter itself runs in polynomial time: M=O(N²), so the target costs O(N⁸). Output size alone is necessary but not sufficient; an exponentially slow converter could still write a short answer.</Prose></details>
    </details>
    <details><summary>Exercise 4 · validate an existential auxiliary</summary>
      <Prose>For (a∨b∨c∨d), check all choices of the fresh z in (a∨b∨z)∧(¬z∨c∨d) when only a is true, when only d is true, and when all four are false. State exactly which equivalence holds.</Prose>
      <details><summary>Hint</summary><Prose>Compare “there exists z” with “for every z.”</Prose></details>
      <details><summary>Explained solution</summary><Prose>When only a is true, z=false works and z=true fails at the second clause. When only d is true, z=true works and z=false fails at the first. When all are false, the clauses require z and ¬z simultaneously, so neither value works. Original truth is equivalent to existence of a satisfying extension; it is not equivalence for arbitrary z.</Prose></details>
    </details>
    <details><summary>Exercise 5 · certify a cover without knowing the optimum</summary>
      <Prose>A graph has four pairwise vertex-disjoint edges, and you independently verify a cover of size six. What bounds follow? Can you conclude the cover is optimal? Now suppose someone reports a cover of size three.</Prose>
      <details><summary>Hint</summary><Prose>Each disjoint edge forces a distinct selected vertex.</Prose></details>
      <details><summary>Explained solution</summary><Prose>4≤OPT≤6, so the feasible size-six answer is within factor 6/4=1.5 of optimum, but need not be optimal. A size-three cover cannot exist with that matching. Check the proposed witness or the alleged disjointness; at least one claim is wrong. Bounds certify quality only when their underlying objects are valid.</Prose></details>
    </details>
    <details><summary>Exercise 6 · preserve the exact-search invariant</summary>
      <Prose>Implement the edge-branch cover solver independently. Test the four-cycle AB,BC,CD,DA at budgets 1 and 2, an edgeless graph at budget 0, and a triangle at budget 1. Explain why the algorithm may stop at the first successful branch but may not stop at the first failed branch.</Prose>
      <details><summary>Hint</summary><Prose>The branches are joined by OR: a feasible cover must contain at least one endpoint of the chosen edge.</Prose></details>
      <details><summary>Explained solution</summary><Prose>The four-cycle fails at 1 and succeeds at 2, for example A and C; the edgeless graph succeeds with the empty set; the triangle fails at 1. A successful branch provides a checked witness and settles existence. Failure of one endpoint choice leaves the other possibility open. On return, verify distinct valid vertices, size at most k and coverage of every original edge. This checks the contract independently of the recursion trace.</Prose></details>
    </details>
    <details><summary>Exercise 7 · expose a hidden structural assumption</summary>
      <Prose>Run the path recurrence on rewards [5,1,6]. Add a conflict edge between positions 0 and 2. What answer does the unchanged recurrence return, and what is the true optimum under the changed graph?</Prose>
      <details><summary>Hint</summary><Prose>Draw all conflicts rather than just the array's consecutive pairs.</Prose></details>
      <details><summary>Explained solution</summary><Prose>The old recurrence returns 11 from positions 0 and 2, but the new edge makes that witness invalid. The graph is now a triangle, so only one vertex may be chosen and the optimum is 6. The failure is lost state about long-range conflicts, not arithmetic. The correct response is to change the representation/algorithm or restore a valid restriction.</Prose></details>
    </details>
    <details><summary>Exercise 8 · design an honest result protocol</summary>
      <Prose>An exact configuration search has a one-minute service deadline. Define statuses and evidence for a found assignment, a completely exhausted search, and an interrupted search. Explain how the protocol changes if the objective is to minimize cost.</Prose>
      <details><summary>Hint</summary><Prose>Separate a feasibility witness from an optimality proof and from lack of a result.</Prose></details>
      <details><summary>Explained solution</summary><Prose>A checked satisfying assignment supports SAT/feasible. Complete sound search exhaustion supports UNSAT/infeasible. Interruption supports unknown, optionally with a best feasible witness found so far. For minimization, a feasible value is an upper bound; a valid lower bound can quantify its gap. Mark optimal only when the bound/proof closes that gap, and do not report a missing witness as an infeasibility proof. The deadline is an engineering policy, not a complexity theorem.</Prose></details>
    </details>
    <DsaPractice practice={practice} />
    <H3>What to learn next</H3>
    <Prose>The next topic in this module is <strong>Randomized Algorithms, Sampling &amp; Error Guarantees</strong>. Here our claims were deterministic: a returned certificate could be checked and an exact solver had to justify its conclusion. Next, distinguish randomness that changes runtime from randomness that can change correctness, and learn to state a probability guarantee. Randomness is another explicit algorithmic contract; it does not automatically erase NP-hardness.</Prose>
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/resources/lecture-23-computational-complexity/">MIT 6.006, Lecture 23: Computational Complexity</a> — an alternate lecture/video route through reductions, hardness and decision problems. Its resource page and linked typed notes were inspected; video playback is not claimed. Use after sections 2–5; machine-level and undecidability details are deeper than the core path here.</li>
      <li><a href="https://opendsa.cs.vt.edu/OpenDSA/Books/Catalog/html/threeSAT_to_clique.html">Virginia Tech OpenDSA: 3SAT to Clique</a> — a different visual presentation of the same conversion. Resource purpose and surrounding text were checked; the hosted slideshow may require its scripts/server. Our local conversion, interactive choices and proof are independently complete.</li>
    </ul>}>
      <li><a href="https://www.claymath.org/millennium/p-vs-np/">Clay Mathematics Institute: P versus NP</a> — official open-problem status checked 10 September 2026. <a href="https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf">Stephen Cook's formal description</a> provides the machine and certificate definitions and explains why feasibility and asymptotic polynomial time are distinct.</li>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/216b0252d29c797488da497a761716a4_MIT6_006F11_lec23.pdf">MIT 6.006 typed complexity notes</a> — reduction direction, SAT and complexity terminology. Foundational theorems are cited premises; finite examples do not replace their proofs.</li>
      <li><a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/2cf0d5a0689efafd2bf2d13954d8ad9b_MIT6_046JS15_lec18.pdf">MIT 6.046 fixed-parameter notes</a> — bounded edge branching and parameter-dependent complexity. Our implementation's copied edge lists determine its stated cost; historical best-known claims and kernelization are outside this lesson.</li>
      <li><a href="https://ocw.mit.edu/courses/6-046j-design-and-analysis-of-algorithms-spring-2015/a4a7f356ba3e65a00ad2bdcfed6e0f35_MIT6_046JS15_lec17.pdf">MIT 6.046 approximation notes</a> — matching-based vertex-cover bound and further approximation methods. Keep the decision/optimization distinction and the exact approximation convention explicit when reading abbreviated notes.</li>
    </Sources>
  </div>,
};

