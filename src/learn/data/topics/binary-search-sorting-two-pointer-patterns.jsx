import { Code, H2, H3, Prose } from '../../components/content';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { BoundarySearchLab, StableMergeLab, PartitionRegionsFigure, PairEliminationLab, CompactionFigure, IntervalUnionFigure, MovingWindowLab, PrefixCancellationFigure, FeasibleRateLab } from '../../components/lesson-labs/OrderedPatternLabs.jsx';
import { orderedPatternExamples } from '../ordered-pattern-examples.js';
import { DsaPractice } from '../../components/lesson-labs/DsaPractice.jsx';
import orderedPractice from '../practice/binary-search-sorting-two-pointer-patterns.js';

export default {
  title: 'Binary Search, Sorting & Two-Pointer Patterns',
  readTime: '~65 min read + 3–4 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot ordered-pattern-lesson">
    <LessonIntro prerequisites="Python lists, loops, functions and dictionaries; the preceding Complexity & Recursion lesson’s invariants, logarithms and time/space distinctions. We explain each boundary convention locally. Heap sorting is an optional connection to the earlier Heaps lesson."
      sections={[
        ['1-order-lets-us-rule-out-work', 'Why order changes a search'],
        ['2-binary-search-finds-a-boundary', 'Search boundaries and duplicates'],
        ['3-build-order-without-losing-information', 'Sorting, stability and partitions'],
        ['4-two-pointers-eliminate-whole-families', 'Opposing and read/write pointers'],
        ['5-sorted-intervals-reveal-connected-coverage', 'Merge interval coverage'],
        ['6-windows-reuse-overlapping-work', 'Fixed and variable windows'],
        ['7-prefixes-turn-ranges-into-differences', 'Signed prefixes and suffixes'],
        ['8-search-for-the-smallest-feasible-answer', 'Binary search on an answer'],
        ['9-solve-a-new-task-and-defend-the-choice', 'Independent synthesis'],
        ['guided-dsa-practice', 'Guided LeetCode practice'],
      ]}>
      A fast algorithm often succeeds because it can explain what it will never need to inspect again. Learn to create order, find boundaries, discard candidate pairs, reuse overlapping windows and search a space of possible answers. The important skill is the reason each move is safe.
    </LessonIntro>

    <H2>1. Order lets us rule out work</H2>
    <Prose>Suppose six measurements are [2,4,4,4,7,9]. You want the first reading at least 4. A correct baseline walks from the beginning until it finds one. If the array has a million readings, a sorted order gives another option: inspect one middle reading. If it is too small, every reading before it is too small as well. One observation can settle a whole region.</Prose>
    <Prose><strong>Sorting</strong> creates a declared order. <strong>Binary search</strong> repeatedly divides an ordered candidate region. <strong>Two-pointer methods</strong> keep positions that move through data while preserving a useful statement. A <strong>sliding window</strong> is the contiguous region between two moving boundaries. They connect through invariants, but their prerequisites differ: sorting readings by size would destroy their original time order and change a time-window question.</Prose>
    <Prose>Throughout the main examples, an array has n entries and random access to an index is constant cost. Comparisons, additions and dictionary keys initially involve bounded-size values. Large integers, long strings, expensive key functions and external storage add their own costs. “Sorted” means nondecreasing: equal values are allowed. Inputs do not change during an algorithm. These are explicit contracts, not properties a function name can enforce.</Prose>
    <LessonTable caption="Choose the information the task needs" headers={['Task', 'Useful representation', 'Cost you must not hide']} rows={[
      ['One membership query on unsorted data', 'A direct scan may be enough', 'Sorting first costs more than the later search'],
      ['Many static threshold or range queries', 'Sorted keys and boundary search', 'Constructing the ordered representation'],
      ['Exact membership with no ordering requirement', 'A hash set or map may fit', 'Hashing/key costs and memory; no ordered-range answer for free'],
      ['Time-contiguous readings', 'Preserve order and use windows/prefixes', 'Sorting by value changes which readings are adjacent'],
      ['Continuously changing ordered data', 'Consider earlier trees or later range structures', 'Array insertion shifts entries even if its location is found quickly'],
    ]} />

    <H2>2. Binary search finds a boundary</H2>
    <H3>Search the gap between two kinds of value</H3>
    <Prose>For target 4, the test “value is at least 4” produces false, true, true, true, true, true. The answer is the gap before the first true. For target 10, every test is false and the answer is gap n, after the final value. An empty array still has one gap: 0. Thinking in gaps makes absence and duplicates normal cases.</Prose>
    <Prose>Use two boundaries, low=0 and high=n. Element indices in <Code>[low, high)</Code> are unresolved: low is included, high is excluded. Maintain three facts: all elements before low are less than the target; all elements at or after high are at least the target; 0 ≤ low ≤ high ≤ n. The true answer gap is somewhere from low through high, including both ends.</Prose>
    <Prose>While low &lt; high, choose middle = low + (high−low)//2. If values[middle] &lt; target, discard that element and everything before it by setting low=middle+1. Otherwise set high=middle. This does not discard middle as a possible answer gap: it proves the elements from middle onward belong on the right. When low=high, nothing is unresolved and that shared gap is the answer.</Prose>
    <BoundarySearchLab />
    <Prose>For [2,4,4,4,7,9] and target 4, the unresolved element intervals become [0,6) → [0,3) → [0,1) → [1,1). The inspected middle values are 4,4,2. The answer is gap 1. Notice why “return immediately on equality” would be the wrong contract: the first inspected 4 is at index 3, not the first occurrence.</Prose>
    <H3>Why this terminates and remains correct</H3>
    <Prose>Initially both proved regions are empty, so the invariant holds. Sorted order justifies extending one proved region after every comparison. For a nonempty interval, low ≤ middle &lt; high: either low increases to middle+1 or high decreases to middle. The nonnegative integer high−low strictly decreases, so the loop terminates. At termination the proved regions meet, giving precisely the required boundary. Halving yields O(log(n+1)) comparisons and O(1) auxiliary storage; n=0 needs no element comparison.</Prose>
    <Prose>The <strong>lower bound</strong> is the first value ≥ target. The <strong>upper bound</strong> is the first value &gt; target. For the upper bound, values ≤ target belong before the boundary, so change just the comparison that advances low. The matching run is [lower,upper); its count is upper−lower. To report a first matching index, check lower &lt; n and values[lower]==target. A boundary alone does not prove equality.</Prose>
    <RunnableExample example={orderedPatternExamples.boundaries}><Prose>Every example on this page is a complete Python program. Save one as a file and run it with Python; no third-party packages are required. All displayed outputs were checked on Python 3.12.14. The functions assume prevalidated sorted input; repeatedly scanning to validate it would add linear work.</Prose></RunnableExample>
    <Checkpoint prompt="For [1,3,3,6], what are the lower and upper boundaries of 3? What does lower_bound return for 4, and why is that not a matching index?">
      <Prose>The boundaries for 3 are 1 and 3, so two values match. The lower boundary for 4 is 3, immediately before 6; the equality check fails. If you instead set low=middle after a too-small comparison, a one-element unresolved interval can repeat forever. The +1 records that the inspected value is already ruled out.</Prose>
    </Checkpoint>
    <Prose>Python’s <Code>bisect_left</Code> and <Code>bisect_right</Code> implement these insertion boundaries. With a key function, the search target is already a key; bisect does not apply the key function to it. A lookup in a list of records therefore passes a numeric score, not a whole record. Searching is logarithmic, but inserting into the middle of a Python list still shifts a linear number of references. The <Code>key</Code> argument requires Python 3.10 or later. [<a href="https://docs.python.org/3/library/bisect.html" target="_blank" rel="noopener noreferrer">Python bisect contract</a>]</Prose>

    <H2>3. Build order without losing information</H2>
    <Prose>A correct sort must satisfy two conditions: adjacent keys are in order, and the output contains exactly the input occurrences. Returning [1,2,3] is not a correct sort of [3,1,1,2], even though it is ordered. Sorting also moves positions. If an answer must refer to original indices, retain those indices alongside values; a sorted-array position is a different identity.</Prose>
    <Prose>A <strong>stable</strong> sort preserves original order among records whose keys compare equal. If Ada and Cy both score 4 and Ada arrived first, sorting by score should leave Ada first when stability is required. This is separate from ascending order. Choose the actual comparison key: score, timestamp, or a tuple of fields. Lexicographic tuples compare the first differing field.</Prose>
    <RunnableExample example={orderedPatternExamples.recordOrder}><Prose><Code>sorted</Code> creates a new list; <Code>list.sort</Code> changes the existing list and returns None. Both preserve tie order. Sorting first by a secondary key and then stably by the primary key gives a multi-key order. “Changes the list” does not mean “uses only constant extra memory”: an implementation may allocate temporary workspace. Define a policy for unordered values such as NaN rather than assuming ordinary comparisons give a usable total order. [<a href="https://docs.python.org/3/howto/sorting.html" target="_blank" rel="noopener noreferrer">Python sorting guide</a>]</Prose></RunnableExample>
    <H3>Insertion sort: preserve a sorted prefix</H3>
    <Prose>Start with one entry: a one-element prefix is sorted. Save the next record, shift every strictly larger earlier key one position right, then write the saved record into the gap. For [4A,2B,4C,1D], inserting 2B creates [2B,4A,4C,1D]. Inserting 4C shifts nothing; it stays after 4A. Inserting 1D shifts three entries and places it first.</Prose>
    <Prose>The invariant is that the processed prefix is a stable sorted permutation of the corresponding original records. Shifting only larger keys preserves tie order. Each pass adds one record, so after n passes the whole list is sorted. Already sorted input needs linear comparisons; reverse order can require 1+2+…+(n−1) shifts, giving quadratic worst-case work. The insertion loop itself needs one saved record and indices; the return-a-new-list version below also allocates its result.</Prose>
    <H3>Merge sort: two sorted runs make a larger one</H3>
    <Prose>Suppose [2A,4B,4C] and [1D,4E,6F] are already sorted. The smallest remaining output must be one of the two heads: no later element in either run can be smaller than its own head. Take 1D, then 2A. At equal 4s, choosing the earlier run preserves original tie order; choosing 4E first can keep keys sorted while making the result unstable.</Prose>
    <StableMergeLab />
    <Prose>After each head choice, the output is a sorted prefix containing exactly the consumed records; its entries are no greater than either remaining head. At least one head advances each time, so merging runs of lengths a and b takes O(a+b) work. When one run empties, copy the other remainder. This is already a two-pointer algorithm, with an output position as well.</Prose>
    <Prose>To sort an arbitrary array, merge one-element runs into sorted runs of length two, then four, eight and so on. The last run may be shorter. At most n records move per level and there are about log₂n levels, giving O(n log n) work. This is the bottom-up version below. A recursive version sorts halves first; its recurrence 2T(n/2)+Θ(n) is the level accounting from the preceding topic. A single linear-size buffer supplies workspace; it does not become n log n live storage.</Prose>
    <RunnableExample example={orderedPatternExamples.stableSorting}><Prose>The merge buffer becomes the next pass’s source by swapping the two list references. Every destination slot is written before that pass finishes. The two algorithms agree with Python’s stable sort on keyed records; this checks both ordering and identities. The code calls the key function repeatedly, so include its cost for expensive keys or precompute them.</Prose></RunnableExample>
    <H3>Partition first: a different divide-and-conquer choice</H3>
    <Prose>Quicksort starts with a pivot value and groups records relative to it, then sorts the smaller and larger groups. It does not require the two groups to be sorted before partitioning. A three-way partition is helpful when many values equal the pivot: keep regions <Code>[low,less)</Code> smaller, <Code>[less,scan)</Code> equal, <Code>[scan,greater)</Code> unknown, and <Code>[greater,high)</Code> larger. These four adjacent regions cover the active range without overlaps.</Prose>
    <Prose>If the scanned value is smaller, swap it to less and advance both less and scan. If it is larger, decrease greater and swap it there; do <em>not</em> advance scan, because the incoming value is still unknown. If equal, advance scan. Every operation reduces greater−scan. Once that unknown region is empty, only the smaller/larger groups need recursion. The equal group is complete and contains at least the chosen pivot.</Prose>
    <PartitionRegionsFigure />
    <RunnableExample example={orderedPatternExamples.partitionSort}><Prose>The seed makes this small demonstration reproducible. Random pivot selection supports expected O(n log n) work for distinct-key inputs, with expectation over pivot choices; it is not a worst-case guarantee. Poor splits can still yield quadratic work and a linear-depth recursive stack, which may reach Python’s recursion limit. All-equal input takes one linear partition in this version. Swapping is generally unstable. The example copies the input, so it also uses linear output storage. [<a href="https://algs4.cs.princeton.edu/23quicksort/" target="_blank" rel="noopener noreferrer">Partitioning and quicksort analysis</a>]</Prose></RunnableExample>
    <LessonTable caption="Match the sort to its actual implementation and contract" headers={['Method', 'Time under constant-cost comparisons', 'Space / useful property']} rows={[
      ['Insertion loop', 'Best Θ(n); worst Θ(n²)', 'O(1) auxiliary if working directly in a list; stable when equal keys are not shifted'],
      ['Buffer-based merge sort', 'Worst O(n log n)', 'O(n) buffer; stable with the correct tie rule'],
      ['Randomized three-way quicksort', 'Expected O(n log n) on distinct keys; worst O(n²)', 'Partition uses O(1); recursion is expected O(log n), worst O(n); generally unstable'],
      ['Iterative heapsort', 'Worst O(n log n)', 'O(1) auxiliary in the supplied implementation; generally unstable'],
      ['Counting bounded integers', 'Θ(n+R), R = value-range width', 'O(R) counts plus output; the value-only form is not a record-stability algorithm'],
    ]} />
    <details><summary>Deeper: comparison lower bounds, heapsort, and small integer domains</summary>
      <Prose>With n distinct keys there are n! possible orderings. A comparison-only decision tree must distinguish them; a bounded-outcome comparison adds only a constant amount of information. Consequently some input requires Ω(log(n!)) = Ω(n log n) comparisons. This is a worst-case lower bound in the comparison model, not a ban on linear sorting when keys offer additional structure.</Prose>
      <Prose>Heapsort reuses the earlier max-heap invariant. Build a max-heap in linear time, swap its largest value into the final free slot, shrink the heap boundary and sink the new root. The completed suffix is sorted and never touched again. Each removal costs O(log n); the iterative sink and outer loops need constant auxiliary storage. This is useful when worst-case work and a small workspace matter more than stability.</Prose>
      <RunnableExample example={orderedPatternExamples.heapSort}><Prose>The function mutates its list. It makes no result copy and does not recurse. A constant number of Python references/indices is the stated auxiliary model, excluding the existing list and size-dependent integer bit storage.</Prose></RunnableExample>
      <Prose>For integer values in a small range, count each value in a bucket and emit the buckets in order. With [3,−1,0,−1,3,2], six occupied input positions need only five possible value buckets from −1 through 3. Negative values are handled by an offset; a huge sparse range would waste memory. The following value-only sort preserves multiplicities but cannot preserve attached record identities, because it stores only counts.</Prose>
      <RunnableExample example={orderedPatternExamples.countingSort}><Prose>Radix sorting extends the idea to multiple digits: a stable sort on the least significant digit, then the next, preserves the order already established within each equal next digit. For fixed-length d-digit keys and base b, d stable counting passes cost O(d(n+b)); the digit representation and range matter. The result is not an unrestricted constant-time-key comparison sort beating its own lower bound.</Prose></RunnableExample>
      <Prose>Merging also works on sequentially read sorted runs, making it a natural building block when the entire dataset cannot fit in memory: sort manageable chunks, then merge their heads. That is a teaching scenario, not an I/O benchmark. An external implementation must account for buffers, files and passes; the earlier heap can choose among more than two run heads.</Prose>
    </details>
    <Checkpoint prompt="A forward merge chooses the right head whenever keys tie. Is the output necessarily incorrect? Give the precise property that may fail.">
      <Prose>It can still be a sorted permutation, but stable order can fail: 4E from the later half may pass 4B from the earlier half. If the task only asks for numeric sorted values, either tie choice can be valid. If writing backward into a destination’s tail, the stable tie choice reverses: place the later half’s equal record at the later output position first.</Prose>
    </Checkpoint>

    <H2>4. Two pointers eliminate whole families</H2>
    <H3>Find two sorted values with a target sum</H3>
    <Prose>The baseline tests every pair i&lt;j, using Θ(n²) comparisons. In sorted [1,2,4,5,7,9], put left at the smallest and right at the largest. For target 11, 1+9=10 is too small. Keeping 1 and choosing any smaller partner cannot increase that sum enough, so no remaining solution uses left=0. Advance left; 2+9=11 succeeds.</Prose>
    <Prose>If the endpoint sum is too large, even the smallest remaining partner makes the right endpoint too large, so discard right instead. The invariant is that any solution not already found lies among pairs with left≤i&lt;j≤right. Each justified move removes an edge of that candidate region without removing a solution. The interval shrinks until a pair is found or left≥right, at which point no two distinct remaining positions exist.</Prose>
    <PairEliminationLab />
    <RunnableExample example={orderedPatternExamples.pairSum}><Prose>At most n−1 pointer moves are possible, so searching already sorted input takes O(n) time and O(1) auxiliary space. Negative numbers do not break this sorted-sum argument. Unsorted input does: moving a pointer no longer tells you that every alternative partner is smaller or larger. Sorting first adds O(n log n) work and may require carrying original indices.</Prose></RunnableExample>
    <H3>Read/write pointers: keep one useful prefix</H3>
    <Prose>Another two-pointer arrangement moves both positions in the same direction. To deduplicate a sorted array, read every original value. The first write entries hold exactly the unique values seen so far. Copy the current value to write if that prefix is empty or the current value differs from its last value, then advance write. Otherwise skip it. Write never exceeds read, so a write cannot destroy a future unread item.</Prose>
    <CompactionFigure />
    <Prose>The return value is a logical length, not necessarily a resized list. The tail is irrelevant to the result. Deleting each duplicate from the middle would repeatedly shift later entries; keeping a prefix avoids that cost. For merging into spare capacity at the end of a list, reverse the direction: write the largest remaining source value into the last free slot. This protects source values near the beginning from premature overwrite.</Prose>
    <RunnableExample example={orderedPatternExamples.compactMerge}><Prose>Both loops do linear total work with constant auxiliary storage under their contracts. The source arrays must be sorted, and <Code>merge_into_tail</Code> expects independent input lists rather than overlapping aliases. Its spare slots use None here to distinguish capacity from real values; a zero could also be legitimate source data.</Prose></RunnableExample>
    <H3>From a pair to a unique value triple</H3>
    <Prose>To find triples summing to zero, sort the values and fix one position. Search only the later positions for a pair summing to the negative of that fixed value. The indices are distinct by construction. Skip repeated fixed values, and after a match skip repeated left/right values, so the same value triple is not emitted again. This is a contract about unique <em>values</em>; a request for every original index triple needs different duplicate handling.</Prose>
    <RunnableExample example={orderedPatternExamples.triples}><Prose>Each fixed position has a linear pointer scan, giving O(n²) search work plus sorting and output. The sorted copy uses O(n) space, and storing the answers uses space proportional to their count. A hash-based two-sum method is a valid alternative in other contracts; order helps here by making duplicate suppression and elimination explicit.</Prose></RunnableExample>
    <Checkpoint prompt="Would a set of values be enough if the task asks whether two distinct positions sum to 10 and the input is [5,5]?">
      <Prose>A set alone loses the occurrence count. The answer is yes because two positions exist. On [5], the answer is no. The opposing-pointer rule left&lt;right enforces distinct positions while still permitting equal values.</Prose>
    </Checkpoint>

    <H2>5. Sorted intervals reveal connected coverage</H2>
    <Prose>Suppose intervals mark sections of a line that have been inspected. We want their complete union: every covered point and no uncovered point. Sort by start. Maintain the merged union of the processed intervals. A new interval can only extend the last component, because every earlier disjoint component ends before the last one begins.</Prose>
    <Prose>For closed intervals, both endpoints belong to the interval. If the next start is no greater than the last merged end, combine them by keeping the earlier start and the larger end. Otherwise start a separate component. Taking the maximum matters: a nested [2,3] must not shorten an existing [1,4].</Prose>
    <IntervalUnionFigure />
    <RunnableExample example={orderedPatternExamples.intervals}><Prose>Sorting costs O(n log n), then the sweep is O(n); this implementation allocates sorted input and output. After sorting, each step either extends coverage or starts the next disjoint component, preserving the exact union. Zero-length closed intervals such as [10,10] still contain a point.</Prose></RunnableExample>
    <Prose>Half-open intervals [start,stop) exclude stop. Thus [1,4) and [4,5) do not overlap as occupancy intervals, although their union can still be coalesced to [1,5) if your storage policy merges adjacent coverage. Distinguish “conflicting reservations” from “compact representation of a union.” Selecting the largest number of nonconflicting intervals is yet another objective; it belongs to the later Greedy lesson and is not solved merely by merging them.</Prose>

    <H2>6. Windows reuse overlapping work</H2>
    <H3>A fixed width needs no monotonicity assumption</H3>
    <Prose>A window is a contiguous segment, not a subsequence that skips positions. For values [3,−4,5,2,−1,4] and width 3, neighboring windows share two values. After summing the first three once, advancing one step removes the outgoing value and adds the incoming one. The four window sums are 4,3,6,5. No sort is needed, and negative values are harmless to this identity.</Prose>
    <RunnableExample example={orderedPatternExamples.fixedWindow}><Prose>The initial sum plus all updates is O(n); the running total and best position need O(1) auxiliary space. The code initializes best to an actual window sum, not zero, so all-negative input works. Using &gt; rather than ≥ when replacing the best retains the earliest tie. Since every candidate has the same positive width, maximizing sum also maximizes average.</Prose></RunnableExample>
    <H3>A variable width needs a reason to discard a start</H3>
    <Prose>Now find a shortest nonempty window whose sum is at least target. Restrict the input to nonnegative values and target to a positive value. Move right forward to include a new value. While the sum qualifies, record its length and try removing the leftmost value. Keep shrinking until the remaining window no longer qualifies, then grow right again.</Prose>
    <MovingWindowLab />
    <Prose>Why is it safe to forget a removed left boundary? We have already recorded a qualifying interval beginning there; any future right boundary would only make that same start’s interval longer. Why can we stop shrinking after the sum falls below target? Removing further nonnegative entries cannot increase the sum. These two statements justify the algorithm together. Zeros are fine: they may leave the sum unchanged, but left still moves and the length decreases.</Prose>
    <RunnableExample example={orderedPatternExamples.positiveWindow}><Prose>Right advances n times and left advances at most n times. Although one loop is nested inside another, total pointer work is O(n), with O(1) auxiliary space. The function returns an optional half-open interval; converting absence to length zero is an output-policy choice. The initial nonnegative validation is linear and does not change this bound.</Prose></RunnableExample>
    <Prose>With signed values, the second safety argument fails. For [1,−1,5] and target 5, the naive window reaches sum 5 over all three entries. Removing 1 leaves sum 4, so the positive-only rule stops and misses the length-one window [5]: removing −1 would have increased the sum. This is a proof failure, not a bug you can repair by changing one comparison. A general signed shortest-at-least problem needs a richer method; the exact-sum signed problem in the next section has a simpler prefix solution.</Prose>
    <H3>A different validity rule: no repeated characters</H3>
    <Prose>For a longest substring with no repeated characters, keep the last position of each character. When a character repeats inside the current window, move left just past that earlier position. If its earlier occurrence was outside the window, leave left unchanged. For “abba,” the final a was last seen at 0 while left is already 2; moving left backward to 1 would incorrectly include repeated b characters.</Prose>
    <RunnableExample example={orderedPatternExamples.distinctWindow}><Prose>After each update, the current suffix is the longest valid substring ending at right. Earlier starts would include a repeated character, so testing that suffix at each endpoint finds a global maximum. Expected dictionary operations give O(n) work and O(u) stored character positions for u distinct characters; the returned indices use constant space. Python strings here are sequences of Unicode code points, not automatically user-perceived grapheme clusters. Segmentation rules must be specified for a user-interface text task.</Prose></RunnableExample>

    <H2>7. Prefixes turn ranges into differences</H2>
    <Prose>When many queries ask for a sum of an unchanged array, store totals at its boundaries. Let P[0]=0 and P[i+1]=P[i]+values[i]. Then P[r] contains the sum before boundary r, including everything before l. Subtract P[l] to cancel that common beginning: sum of values in [l,r) is P[r]−P[l]. This identity works with negative values; it needs no monotone movement.</Prose>
    <PrefixCancellationFigure />
    <Prose>Building P takes O(n) work and O(n) storage, after which each valid range sum takes O(1) arithmetic. A query over [i,i) is empty and returns zero naturally. Prefixes store <em>one more</em> entry than values because n entries have n+1 boundaries. Updating a single value changes all following prefix totals, so static prefixes do not provide cheap arbitrary updates; later Fenwick/segment-tree lessons address that different contract.</Prose>
    <Prose>Subtraction cannot cancel every summary. Two prefix minima do not determine a range minimum: knowing that both prefixes have minimum 1 says nothing about the intervening segment’s smallest value. Sometimes a prefix and a suffix are the right pair instead. To multiply every value except the one at i, combine the product strictly before i with the product strictly after i. Start empty products at 1. This handles zeros without dividing by the excluded value.</Prose>
    <RunnableExample example={orderedPatternExamples.prefixSuffix}><Prose>The output array first holds prefix products and is then multiplied by a running suffix product. It needs O(n) output plus O(1) additional state and performs O(n) arithmetic operations. Python integer products can grow in bit length, so this is not an unconditional linear bit-time claim. For ordinary sum queries the input supplies valid integer boundaries 0≤l≤r≤n.</Prose></RunnableExample>
    <H3>Count signed subarrays without guessing window movements</H3>
    <Prose>To count nonempty subarrays summing exactly to k, rewrite P[r]−P[l]=k as P[l]=P[r]−k. At each new right boundary, ask how many <em>earlier</em> prefix totals equal the required value. Store a frequency map, not just a set: distinct earlier boundaries can have the same total and represent different subarrays. Start with frequency 1 for prefix zero, representing the boundary before the first value.</Prose>
    <Prose>For values [2,−1,2,−1] and k=1, prefixes are 0,2,1,3,2. At right boundary 2, current total 1 needs earlier 0, producing [0,2). At boundary 3, total 3 needs earlier 2, producing [1,3). At boundary 4, total 2 needs earlier 1, producing [2,4). There are three qualifying nonempty ranges. No sorted-prefix assumption was made.</Prose>
    <RunnableExample example={orderedPatternExamples.signedCounts}><Prose>Count matches before inserting the current prefix; reversing that order would count an empty range when k=0. Three zeroes have six nonempty ranges, so frequency matters even when all values are equal. Expected hash-table work is O(n) with O(n) distinct prefix storage; a worst-case guarantee requires a stronger dictionary model. This counts exact sums; it does not solve every signed range optimization.</Prose></RunnableExample>
    <Checkpoint prompt="Why does a fixed-width sum work with negative values while the shortest-at-least window can fail? Would sorting the values repair the latter problem?">
      <Prose>The fixed-width update is an algebraic identity: add what enters, subtract what leaves. The variable-window method additionally relies on nonnegative removals never increasing a sum. Sorting would change the original contiguous segments and therefore the task. Prefix identities preserve original adjacency; a different signed optimization may need a different data structure rather than reordered input.</Prose>
    </Checkpoint>

    <H2>8. Search for the smallest feasible answer</H2>
    <Prose>Binary search does not require the answer to be stored in an array. Suppose jobs contain [3,6,7] units. One time slot works on one job, processing up to speed units; unused room at the end of that job cannot serve the next one. A job of size 7 at speed 3 requires three slots, not 7/3 usable slots. For positive integers, ceiling(job/speed) equals (job+speed−1)//speed.</Prose>
    <Prose>Define feasible(speed) to mean that the sum of these per-job slot counts is within the available budget. Raising speed cannot increase any job’s slot count, so feasibility has a false…false,true…true boundary. That <strong>monotone predicate</strong> is the contract binary search needs. Merely observing a few successful rates would not prove it.</Prose>
    <FeasibleRateLab />
    <Prose>With six available slots, speed 2 needs 2+3+4=9 slots, speed 3 needs 1+2+3=6, and speed 4 needs 1+2+2=5. The minimum rate is 3. At least one slot per positive job is unavoidable; if the budget is smaller than the job count, no rate works. Otherwise high=max(jobs) is guaranteed feasible, because each job fits into one slot. Low=1 is the smallest permitted positive integer.</Prose>
    <Prose>Keep the minimum feasible answer in the closed candidate range [low,high], with high feasible. If middle is feasible, keep it and the lower candidates by setting high=middle; otherwise all rates through middle fail and low=middle+1. The range strictly decreases. When boundaries meet, the only remaining candidate is the minimum feasible rate. We never evaluate speed zero.</Prose>
    <RunnableExample example={orderedPatternExamples.answerSearch}><Prose>For n jobs and maximum job size M, there are O(log(M+1)) predicate evaluations, each O(n), plus initial validation. The arithmetic model assumes bounded-size job values; the implementation uses exact integer rounding. The lab enumerates rates only to show the tiny truth pattern. The actual search does not construct or store all possible rates.</Prose></RunnableExample>
    <details><summary>Deeper: numerical boundaries and finding a search range</summary>
      <Prose>For an unbounded integer domain, doubling a candidate upper bound until it is feasible can find a bracket when a feasible answer is known to exist. Without that existence assumption or an external limit, doubling may never stop. State the promise and resource limit before replacing a finite domain with an endless search.</Prose>
      <Prose>Real-number bisection needs a different stopping contract: a bracket width tolerance or a fixed iteration budget, plus treatment of floating-point stagnation when the midpoint rounds to an endpoint. Searching while low!=high is not a general numerical solver. A continuous sign-changing root bracket is another sufficient structure for bisection, developed in Numerical Methods; it is not the same predicate contract as this integer minimum.</Prose>
      <Prose>For fixed-width machine integers, computing (low+high)//2 can overflow even when each endpoint fits. The difference-based midpoint avoids that addition in the usual nonnegative bounded-index setting. Python integers do not overflow this way, but their arithmetic cost grows with size. Similarly, summing many job durations may require a wider type in another language.</Prose>
    </details>

    <H2>9. Solve a new task and defend the choice</H2>
    <Prose>Before coding, restate the output: one index, all matching values, a count, a logical prefix, interval coverage or a minimum feasible decision. State which order exists and which original relationships must survive. Propose a correct baseline, then write the fact that makes your optimization safe. The same surface word—“range,” “pair” or “minimum”—does not determine an algorithm.</Prose>
    <H3>Independent project: a time-range report</H3>
    <Prose>Each record is (integer timestamp, signed amount). Data is static, may arrive out of order, and may contain repeated timestamps. For every query [start,stop), report both the number of included records and their amount total. Preserve every occurrence. Plan for many queries without scanning all records each time. Start with [(5,4),(2,7),(5,−1),(9,3),(1,−2)].</Prose>
    <Prose>Acceptance cases: [2,9) gives count 3 and total 10; [5,6) gives count 2 and total 3; [5,5), an out-of-range query and every query over empty data give (0,0). Reject start&gt;stop. Explain preprocessing, per-query cost, duplicate inclusion and what would change if amounts were updated frequently. Test against a direct filter-and-sum oracle on changed records before opening the solution.</Prose>
    <details><summary>Hint: use two boundaries and one reusable summary</summary><Prose>Stable timestamp order groups matching records without dropping duplicates. Which insertion boundary excludes timestamps equal to stop? Which pair of prefix totals covers exactly those selected positions?</Prose></details>
    <details><summary>Worked solution and why each part is needed</summary>
      <Prose>Sort by timestamp, retain a parallel timestamp list and build amount prefixes. The first timestamp ≥ start begins the interval; the first timestamp ≥ stop ends it. Using the same lower-bound rule at both ends implements half-open time semantics, including repeated timestamps. Subtract positions for the count and prefix totals for the signed sum. Amounts do not need to be nonnegative or sorted.</Prose>
      <RunnableExample example={orderedPatternExamples.rangeReport} />
      <Prose>Preprocessing costs O(n log n) time and O(n) storage; each query costs O(log(n+1)) search work and O(1) arithmetic. Outputting every selected record would additionally cost its output size. Static prefixes become costly under updates; that requirement motivates a dynamic range structure later, not silently rebuilding on every query.</Prose>
    </details>
    <H3>Diagnose and transfer</H3>
    <Checkpoint prompt="A search for the last occurrence returns upper_bound(values,target). A test passes when the target is absent. What is wrong, and what are the necessary checks?">
      <Prose>Upper bound is the first position after equal values, not the last occurrence. Compute lower and upper; if equal, the target is absent. Otherwise the last occurrence is upper−1. A returned boundary can be n, so indexing values[upper] may be invalid.</Prose>
    </Checkpoint>
    <Checkpoint prompt="Merge [1,7] with [2,3] and [7,8] under closed semantics. Then explain why the same comparison cannot decide whether half-open reservations conflict.">
      <Prose>The closed union is [1,8]. The nested interval leaves the end at 7, then the touching interval extends it to 8. Half-open [1,7) and [7,8) share no occupied point; their adjacency can still be coalesced for a union representation. The conflict question and storage policy are different.</Prose>
    </Checkpoint>
    <Checkpoint prompt="For values [0,0,0] and target 0, show the frequency-map count after each value. Why must the current prefix be added after the lookup?">
      <Prose>There are initially one, then two, then three earlier zero prefixes, contributing 1+2+3=6 nonempty ranges. Inserting first would include the current boundary as an earlier one, falsely counting one empty range at each step. A set would count only one match per endpoint and miss valid ranges.</Prose>
    </Checkpoint>
    <Checkpoint prompt="A job processor may use spare capacity on the next job during the same slot. For jobs [3,6,7] and budget 6, does the earlier minimum-rate calculation still apply?">
      <Prose>No. Fully shareable integer work now needs ceiling((3+6+7)/speed) slots, rather than the sum of three ceilings. At speed 3 that is six slots and still feasible, but budgets can distinguish the models: with budget 4, speed 4 now works, whereas separate job slots required speed 6. Re-derive the predicate before reusing the boundary code.</Prose>
    </Checkpoint>
    <Prose><strong>Ready to continue?</strong> Explain one complete boundary proof, one sorting stability rule and one pointer elimination. Construct a signed-window counterexample without reading it. Solve the report with empty/duplicate/signed data and explain its storage. Then mix one earlier hash-map or heap problem into your practice without a pattern label. The next topic, <a href="./backtracking-divide-and-conquer?module=data-structures-algorithms">Backtracking &amp; Divide-and-Conquer</a>, develops problems where choices branch and local state must be restored, while extending the divide-and-conquer reasoning used by sorting.</Prose>
    <DsaPractice practice={orderedPractice} />
    <Sources alternatives={<ul>
      <li><a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/resources/lecture-3-sets-and-sorting/" target="_blank" rel="noopener noreferrer">MIT OCW: Justin Solomon, Sets and Sorting</a> — a video route connecting ordered search to building sorted arrays. Use after the sorting section; its <a href="https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/6d1ae5278d02bbecb5c4428928b24194_MIT6_006S20_lec3.pdf" target="_blank" rel="noopener noreferrer">companion notes</a> develop correctness and recurrence reasoning. The notes were inspected; a full video viewing is not claimed.</li>
      <li><a href="https://algs4.cs.princeton.edu/22mergesort/" target="_blank" rel="noopener noreferrer">Sedgewick and Wayne: Mergesort</a> — illustrated merging and top-down/bottom-up explanations. The companion <a href="https://algs4.cs.princeton.edu/23quicksort/" target="_blank" rel="noopener noreferrer">quicksort chapter</a> is useful for partition reasoning. Examples use Java and may use different interval conventions; compare invariants rather than transplanting indices.</li>
    </ul>}>
      <li><a href="https://docs.python.org/3/library/bisect.html" target="_blank" rel="noopener noreferrer">Python bisect documentation</a> — precise lower/upper partitions, record keys, insertion and search cost distinctions.</li>
      <li><a href="https://docs.python.org/3/howto/sorting.html" target="_blank" rel="noopener noreferrer">Python Sorting Techniques</a> — stable key sorting, multiple fields, mutation, return values and unordered-data policies. The current online documentation is newer than the separately recorded native execution snapshot.</li>
      <li><a href="https://algs4.cs.princeton.edu/21elementary/" target="_blank" rel="noopener noreferrer">Princeton elementary sorting</a> and <a href="https://algs4.cs.princeton.edu/24pq/" target="_blank" rel="noopener noreferrer">priority queues and heapsort</a> — mechanism and work comparisons for the optional alternatives. Algorithm counts are not machine timing guarantees.</li>
    </Sources>
  </div>,
};
