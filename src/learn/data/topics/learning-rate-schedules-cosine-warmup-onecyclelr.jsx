import { Code, CodeBlock, H2, H3, Prose } from '../../components/content';
import { MathBlock } from '../../components/content/Math.jsx';
import { Checkpoint, LessonIntro, LessonTable, Sources } from '../../components/lesson-labs/LessonElements';
import { RunnableExample } from '../../components/lesson-labs/RunnableExample';
import { CalibrationUpdateFigure, ResumeStateFigure, ScheduleClockLab, ScheduleNoiseLab, SchedulePlateauLab, ScheduleShapeLab, UpdateClockFigure } from '../../components/lesson-labs/LearningRateScheduleLabs.jsx';
import { learningRateScheduleExamples } from '../learning-rate-schedule-examples.js';
export default {
  title: 'Learning Rate Schedules (Cosine, Warmup, OneCycleLR)',
  readTime: '~55 min read + 2–3 hours practice',
  hasIntegratedGuide: true,
  content: () => <div className="lesson-pilot learning-rate-schedules-lesson">
    <LessonIntro exampleKind="Python, NumPy and CPU PyTorch" prerequisites="The preceding Gradient Descent Variants lesson's gradient, parameter update, momentum and averaging ideas. Convex Optimization explains the quadratic curvature used here. We recap the scalar derivative and define every timing counter; no neural-network architecture is required for the examples." sections={[['1-a-rate-multiplies-an-update', 'Rate and displacement'], ['2-choose-what-the-clock-counts', 'The update clock'], ['3-build-the-rates-that-will-be-used', 'Finite schedules'], ['4-separate-progress-from-injected-noise', 'Exact noise reasoning'], ['5-run-one-cycle-with-its-actual-state', 'OneCycleLR'], ['6-warm-up-or-restart-with-a-purpose', 'Warmup and restarts'], ['7-let-validation-trigger-a-change', 'Plateau state'], ['8-accumulate-and-resume-the-right-work', 'Accumulation and resume'], ['9-compare-and-practise', 'Experiments and practice']]}>A training run can use the right gradient and still make poor progress because its steps are too large, too small or changed at the wrong time. A learning-rate schedule is a policy for changing that multiplier. Learn to construct the policy, see which rate each update consumes, and test what the change actually accomplishes.</LessonIntro>

    <H2>1. A rate multiplies an update</H2>
    <Prose>Imagine adjusting a dimensionless calibration parameter θ toward a desired value a. Measure error with f(θ)=½h(θ−a)², where h&gt;0 describes curvature: a larger h makes the same displacement more costly. Its derivative is h(θ−a). Plain gradient descent subtracts a learning rate η times that derivative.</Prose>
    <MathBlock>{String.raw`\begin{aligned}e_u&=\theta_u-a,\\ \theta_{u+1}&=\theta_u-\eta_u h e_u,\\ e_{u+1}&=(1-\eta_u h)e_u.\end{aligned}`}</MathBlock>
    <Prose>The subscript u labels an update. With a=2, h=2, θ₀=5 and η=.2, the gradient is 6 and the displacement is −1.2. The new parameter is 3.8. The next gradient is 3.6, so the same learning rate moves only −.72, reaching 3.08. <strong>A rate is not the distance traveled.</strong> It multiplies a gradient or the optimizer's transformed direction, whose size also changes.</Prose>
    <CalibrationUpdateFigure />
    <Prose>The error multiplier explains this whole example. For a fixed positive η, 0&lt;ηh&lt;1 contracts without crossing the target; 1&lt;ηh&lt;2 contracts while alternating sides. At ηh=1 this exact scalar quadratic is solved in one step. At ηh=2 the nonzero error alternates without shrinking, and above 2 it grows in magnitude. This is a precise result for this model, not a universal safe-rate formula for a neural network.</Prose>
    <Prose>A schedule lets η change with progress or observations. It may preserve larger movements early, reduce the effect of noisy updates later, or adapt when a validation metric stalls. But a suitable constant rate already converges on this deterministic quadratic. “Every run needs decay” and “a high rate escapes any bad region” are not mathematical rules. If the gradient is exactly zero, increasing η alone does not create a nonzero gradient.</Prose>
    <Checkpoint prompt="The curvature changes from h=2 to h=8 while η stays .2. Starting from error 3, what happens after two updates?">
      <Prose>The new multiplier is 1−.2×8=−.6, so errors are −1.8 and 1.08. The signs alternate but magnitudes shrink. The former monotone behavior has changed even though the displayed learning rate has not. With η=.3, the multiplier would be −1.4 and the nonzero error would instead grow.</Prose>
    </Checkpoint>

    <H2>2. Choose what the clock counts</H2>
    <Prose>A list of learning rates is incomplete until you say when to move to the next value. A <strong>microbatch</strong> is one small set of observations whose gradients are computed together. Several microbatches may accumulate into one optimizer update. An <strong>epoch</strong> is one declared pass through a training dataset; a streaming dataset may not have a natural epoch. A validation observation is a separate measurement used to assess the current model.</Prose>
    <LessonTable caption="The event determines the clock's meaning" headers={['Clock', 'When it advances', 'Consequence']} rows={[['Committed update', 'The selected optimizer actually performs its update.', 'Accumulating gradients alone consumes no schedule value. A deliberately skipped update can leave this clock unchanged.'], ['Epoch', 'A declared training pass finishes.', 'Changing batch size or accumulation can change how many updates use each rate.'], ['Examples or tokens processed', 'The chosen count of data exposure increases.', 'Useful for variable batch lengths, but it is a separate policy from counting updates.'], ['Validation observation', 'A new comparable metric is supplied.', 'Patience is measured in these observations, which need not be epochs.']]} />
    <Prose>For our finite schedules, <strong>u is the number of successful updates already completed</strong>, and ηᵤ is about to be used. A budget T therefore consumes indices 0 through T−1. The initial parameter is θ₀; after the final update it is θ<sub>T</sub>. Write this convention on the schedule before examining its endpoints.</Prose>
    <UpdateClockFigure />
    <Prose>In the PyTorch examples, the scheduler is initialized with the optimizer, the optimizer uses its current rate, and <Code>scheduler.step()</Code> prepares a subsequent rate. Record the optimizer group's rate immediately before the update if you want to know what trained the model. Calling the scheduler first can shift the whole sequence. Some schedulers also set an initial rate during construction, so inspect the actual state rather than assuming the optimizer's original constructor value survives.</Prose>
    <Prose>A scheduler called once per epoch is not wrong because another implementation calls it per update. The problem is promising one clock while implementing another. The same applies to a skipped update: decide whether your policy counts attempted work, consumed data or actual optimizer transitions, then make code and labels agree.</Prose>

    <H2>3. Build the rates that will be used</H2>
    <Prose>Start with a progress fraction p between 0 and 1. A cosine interpolation moves from maximum M to minimum m using a half-wave of cosine. At p=0, cos 0=1 and the result is M; at p=1, cosπ=−1 and it is m. Halfway it is their average.</Prose>
    <MathBlock>{String.raw`\begin{aligned}c(p)&=\frac{1+\cos(\pi p)}{2},\\ C(p)&=m+(M-m)c(p).\end{aligned}`}</MathBlock>
    <Prose>The lowercase c is the fraction of the maximum-to-minimum gap still retained. It moves from 1 to 0. Multiplying it by M−m and adding m converts that fraction into the actual learning rate C.</Prose>
    <Prose>For T≥2 used updates with no warmup, choose p=u/(T−1). There are T points and T−1 intervals between them. Dividing by T instead would sample a valid different curve but stop before its minimum. This distinction is small on a long run and very visible on a ten-update example.</Prose>
    <H3>Join warmup and decay without hiding a boundary</H3>
    <Prose>Let W be the number of warmup updates, with 0&lt;W&lt;T. Our first W rates are M/W,2M/W,…,M. The maximum is used at u=W−1. The next update starts moving down the cosine, and the final update uses m. Define the two portions explicitly:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\eta_u&=M(u+1)/W &&(u<W),\\ p_u&=\frac{u-W+1}{T-W} &&(u\ge W),\\ \eta_u&=C(p_u) &&(u\ge W).\end{aligned}`}</MathBlock>
    <Prose>Zero warmup is a separate branch, p=u/(T−1), avoiding division by zero. W=1 means the first rate is already M. Compare the alternative p=(u−W)/(T−W) after warmup: it repeats M at the join and never uses m inside a ten-update budget. The program below shows both conventions. Either formula can be useful; its endpoint promise must match its actual values.</Prose>
    <RunnableExample example={learningRateScheduleExamples.finiteEndpoints} />
    <ScheduleShapeLab />
    <H3>Compare mechanisms before choosing a name</H3>
    <LessonTable caption="Several policies in the same declared update clock" headers={['Policy', 'Rule or construction', 'Question it raises']} rows={[['Constant', 'ηᵤ=M.', 'Is this rate suitable for the curvature and noise over the whole run?'], ['Step decay', 'Divide u by interval K and round down to get j. Use ηᵤ=Mγʲ with 0<γ<1.', 'Which update first receives the drop? Are intervals expressed in the intended clock?'], ['Exponential decay', 'ηᵤ=Mγᵘ. To include a positive m at T−1, choose γ=(m/M)¹⁄⁽ᵀ⁻¹⁾.', 'A finite sequence of positive multiplicative factors cannot reach exactly zero.'], ['Linear decay', 'ηᵤ=M+(m−M)u/(T−1).', 'This spends a different fraction of the budget at intermediate rates than cosine.'], ['Warmup then decay', 'Declare the initial ramp and the join before composing the phases.', 'Warmup consumes part of a fixed total budget; it is not additional free training.'], ['One cycle or restarts', 'Rise/fall once, or repeatedly reset a phase clock.', 'Does the policy also change momentum? What remains in the optimizer at a restart?']]} />
    <Prose>Smoothness of the plotted rate is not smoothness of the actual parameter path: stochastic gradients and optimizer state still vary. Nor does a smaller last rate prove the final parameters are optimal. A schedule describes how updates are scaled; convergence depends on the objective, update rule, noise, budget and assumptions.</Prose>

    <H2>4. Separate progress from injected noise</H2>
    <Prose>Now suppose each gradient contains a fresh error εᵤ, with mean 0 and variance σ², independent of past updates. The observed gradient is h eᵤ+εᵤ. This is a small model of uncertain measurements or stochastic gradient sampling. It does not assume every shuffled training batch is independent.</Prose>
    <MathBlock>{String.raw`\begin{aligned}q_u&=1-\eta_u h,\\ e_{u+1}&=q_u e_u-\eta_u\varepsilon_u.\end{aligned}`}</MathBlock>
    <Prose>The update now has two effects. Multiplying by qᵤ contracts or expands the existing error; subtracting ηᵤεᵤ injects fresh uncertainty. Let μᵤ be the mean error across repeated runs and vᵤ its variance . With a fixed predetermined rate sequence, independence and zero-mean noise give:</Prose>
    <MathBlock>{String.raw`\begin{aligned}\mu_{u+1}&=q_u\mu_u,\\ v_{u+1}&=q_u^2v_u+\eta_u^2\sigma^2,\\ \mathbb E[e_u^2]&=\mu_u^2+v_u.\end{aligned}`}</MathBlock>
    <Prose>The variance formula comes from squaring the centered update. Its cross term has expectation zero because the fresh noise is independent and centered. The last line separates systematic mean error from variability around that mean. Expected objective value is h/2 times expected squared error. A metric-adaptive schedule can be correlated with the state, so these simple unconditional recurrences cannot automatically be reused for that policy.</Prose>
    <Prose>For a fixed η with 0&lt;ηh&lt;2, q²&lt;1. Repeatedly expanding the variance recurrence gives a geometric sum of injected noise. Its limiting variance solves v=q²v+η²σ²:</Prose>
    <MathBlock>{String.raw`v_\infty=\frac{\eta\sigma^2}{h(2-\eta h)}.`}</MathBlock>
    <Prose>At h=2, η=.2 and σ=1, the limiting variance is .0625. Smaller stable rates reduce this noise floor but generally contract the initial error more slowly. When σ=0, the floor disappears: a good constant rate can finish much faster than a cautious decay under the same finite budget. This supplies a reason to care about scheduling without inventing a universal winner.</Prose>
    <ScheduleNoiseLab />
    <RunnableExample example={learningRateScheduleExamples.noiseMoments} />
    <Prose>The program checks every one of 16 equally likely sign sequences for four updates. Its exact finite distribution agrees with the mean/variance calculation. That is stronger evidence for this toy model than drawing one noisy curve and declaring it typical. It still does not establish how an arbitrary neural network generalizes.</Prose>
    <Checkpoint prompt="If the final learning rate is zero, is the final error necessarily zero? What does the recurrence say?">
      <Prose>No. At η=0, q=1: the existing parameter, mean error and variance stay unchanged, and no new noise is injected. A zero-rate final update does no optimization work. A small final rate may help preserve a useful solution; it cannot certify that the solution was reached.</Prose>
    </Checkpoint>

    <H2>5. Run one cycle with its actual state</H2>
    <Prose>A one-cycle policy deliberately rises from an initial rate to a peak and then falls below its starting value. PyTorch's <Code>OneCycleLR</Code> defaults to two phases, while its <Code>three_phase=True</Code> option follows the original rise, return and final low-rate tail more closely. “One cycle” means one schedule across the declared training budget, not a new cycle for every epoch.</Prose>
    <Prose>In our twelve-update example, M=.2, the initial divisor is 10 and the final divisor is 100. Thus initial η=.02 and final η=.0002. The first three updates rise: .02,.11,.2. The remaining nine decay to .0002. Each phase interpolates its endpoints by cosine, sharing the peak as the join rather than duplicating a peak update.</Prose>
    <Prose>PyTorch computes the first phase boundary as <Code>pct_start * total_steps - 1</Code>. With 12 and .25, that boundary is the integer 2. A different fraction can put the theoretical peak between integer indices; then no used update necessarily equals the requested maximum. Inspect actual values, especially for short budgets. The lab deliberately uses integer-aligned boundaries; it does not emulate every allowable API configuration.</Prose>
    <Prose>The default API also varies momentum inversely to learning rate. Here β moves .95→.85→.95, plotted on its own axis in the shape investigation. The learning-rate curve and momentum curve have different meanings: η scales displacement, while β changes how old optimizer information persists. For supported Adam-style optimizers, the scheduler changes the first-moment coefficient instead. Optimizer conventions still matter.</Prose>
    <RunnableExample example={learningRateScheduleExamples.oneCycleRuntime} />
    <Prose>The final <em>used</em> rate is .0002. After the twelfth update, the example's final scheduler call prepares .006225, which the run never consumes. Continuing blindly would no longer be the promised twelve-update experiment. This is why a log taken after the scheduler call can be mistaken for the rate that just trained the model. A finite budget and a prepared next value are separate facts.</Prose>
    <H3>A rate change also interacts with retained state</H3>
    <Prose>Consider momentum that stores gradients, bᵤ₊₁=βbᵤ+gᵤ, and then subtracts ηᵤbᵤ₊₁. Compare a velocity that stores already-scaled contributions, vᵤ₊₁=βvᵤ+ηᵤgᵤ, and subtracts vᵤ₊₁. With constant η, an appropriate rescaling relates the states. With changing η, the second form retains earlier contributions at their earlier rate. You cannot identify the two by changing a variable name.</Prose>
    <Prose>Decoupled weight decay is another connection. For a parameter participating in every update, if its adaptive-gradient displacement is zero, AdamW-style shrinkage multiplies it by 1−ηᵤλ at each step. The exact total shrinkage is a product of the actual used factors:</Prose>
    <MathBlock>{String.raw`\theta_T=\theta_0\prod_{u=0}^{T-1}(1-\eta_u\lambda).`}</MathBlock>
    <Prose>This identity assumes that coefficient convention and actual participation: a missing-gradient or skipped parameter update may not shrink the parameter. Even though decay is decoupled from the adaptive gradient, its cumulative effect still depends on the schedule. When every ηᵤλ is small, exp(−λΣηᵤ) is a first-order logarithmic approximation to the product, not an exact equality.</Prose>
    <RunnableExample example={learningRateScheduleExamples.decayAndMomentum} />

    <H2>6. Warm up or restart with a purpose</H2>
    <Prose>Warmup uses less aggressive rates at the start and gradually reaches a chosen target. It can help when early gradients, representations or optimizer state change rapidly, but that is a hypothesis to test in a particular setup. Goyal and colleagues studied a gradual warmup with large-batch ResNet training on ImageNet; their successful configuration does not make its ramp length or batch-scaling rule a default for every model.</Prose>
    <Prose>Our fixed quadratic offers a useful counterexample. If h=2 and the sustained peak is η=1.2, the error multiplier at that peak is −1.4. A long gentle warmup cannot change that subsequent unstable multiplier. Conversely, if a constant rate is already effective on a deterministic objective, warmup can simply spend part of a short budget making smaller progress. Diagnose gradients, objective scaling, invalid values and the actual rate before treating “more warmup” as a general repair.</Prose>
    <H3>A warm restart reuses learned parameters</H3>
    <Prose>A cosine restart periodically resets its phase and raises the rate. It does not inherently reinitialize weights or erase momentum. A useful mental picture is a new exploration phase starting from the current location. A temporary rise in loss can occur; whether the policy helps is an empirical question for the full optimizer and task.</Prose>
    <Prose>For a period of six update intervals, the lab uses phases 0/6,…,5/6 and then returns to 0/6. The theoretical minimum at 6/6 is not consumed before the restart. Other conventions include an endpoint. In contrast, PyTorch's <Code>CosineAnnealingLR</Code> advances a non-restarting clock: it is not <Code>CosineAnnealingWarmRestarts</Code>. Continuing a cosine formula beyond its specified horizon can make rates rise again, so inspect the intended stopping rule.</Prose>
    <RunnableExample example={learningRateScheduleExamples.cosineAndRestarts} />
    <Prose>The same four-update run uses the minimum only when its cosine horizon is three intervals. In the restart example, the weight accumulates all eight displacements and ends at −1.03; no restart restores it to zero. These are actual CPU library results, with a deliberately constant gradient to isolate scheduling.</Prose>
    <H3>An interesting use: useful intermediate models</H3>
    <Prose>The original SGDR paper also studied classification of recorded hand and foot movements from EEG, beyond its image experiments. Its comparison illustrates an <strong>anytime</strong> question: how useful is the best available model if training stops at an unknown time? Completed low-rate phases can supply candidate snapshots. Combining snapshots can change predictions and inference cost; it is a different choice from merely taking the last parameter state. This dated experiment motivates a design question, not a claim of clinical readiness or universal EEG performance.</Prose>
    <Prose>A <strong>learning-rate range test</strong> asks another practical question. Start a short disposable run at a low rate, increase it, and inspect loss or validation behavior. The model changes while the probe proceeds, so the horizontal axis does not represent independent models trained from the same start. A suggested peak is a candidate for controlled trials, not a guaranteed stability threshold. Restore the intended initial model, optimizer and randomness afterward, and account for the probe's compute budget.</Prose>

    <H2>7. Let validation trigger a change</H2>
    <Prose>A predetermined schedule knows its clock but does not inspect learning progress. A plateau policy responds to a measured quantity. Suppose lower validation loss is better, and only a decrease greater than δ=.02 counts as improvement. With best=.90, a new .89 is lower but not sufficiently lower; it must be strictly below .88 under this rule.</Prose>
    <Prose>Maintain four pieces of state: the best significant metric, a bad-observation count, any remaining cooldown, and the current rate. A significant improvement updates best and clears the bad count. Otherwise increment the count. With patience 1, one bad observation is tolerated; the second triggers a reduction. After reducing, cooldown 1 ignores bad counts for the next observation while still allowing it to establish a better best. A floor prevents the rate shrinking without limit.</Prose>
    <SchedulePlateauLab />
    <RunnableExample example={learningRateScheduleExamples.plateauRuntime} />
    <Prose>The implementation uses a strict comparison. Equality with the cutoff does not qualify, and with δ=0 an unchanged best is still bad. These details are checked against PyTorch 2.14's actual state, rather than relying on a loose reading of “has stopped improving.” Decimal inputs are represented in floating point; when a threshold boundary is consequential, inspect the actual comparator and representation.</Prose>
    <Prose>Relative thresholds scale with the stored best; absolute thresholds use a fixed metric amount. Choose a meaningful convention for the metric, especially if it can be negative or change scale. Also decide the observation frequency. Validation every 100 updates with patience 3 is different from validation every 1,000 updates with patience 3. A rate reduction is not early stopping: one changes subsequent updates; the other ends training.</Prose>
    <Checkpoint prompt="With minimization, absolute threshold 0, patience 0 and no cooldown, the losses are 1,1,.9,.9. When does the rate reduce?">
      <Prose>The first 1 establishes best. The second 1 is not a strict improvement, so bad count 1 exceeds patience 0 and reduces the rate. The .9 improves and resets the count. The final .9 is equal to best, so it triggers another reduction, subject to the rate floor.</Prose>
    </Checkpoint>

    <H2>8. Accumulate and resume the right work</H2>
    <Prose>Return to an update-based schedule. Computing two microbatch gradients and combining them performs one optimizer transition, not two. If their sizes are equal, averaging their mean gradients gives the combined mean. Unequal sizes require weighting by observation count, as explained in the optimizer lesson. This issue is independent of when a scheduler advances.</Prose>
    <ScheduleClockLab />
    <Prose>The default stream has 12 microbatches, two per attempt and a deliberately rejected second attempt. Five updates therefore consume five rates. On the rejected attempt, this policy discards the accumulated gradient, leaves the parameter alone and keeps the next rate available. It does not pretend the missing update happened.</Prose>
    <Prose>The complete program below exercises PyTorch's CPU <Code>GradScaler</Code> skip contract by deliberately placing an infinity in one accumulated gradient. It registers an optimizer post-step hook to count actual calls, then advances the schedule only after a committed update. A normal optimizer's return value can be <Code>None</Code> even when it did run; testing that value alone is not a reliable success detector.</Prose>
    <RunnableExample example={learningRateScheduleExamples.accumulationAndSkip} />
    <Prose>This controlled example verifies scaling and event behavior on a CPU. It does not benchmark mixed-precision GPU training. In a real scaled accumulation loop, keep the scale consistent across the effective batch and update/check it at the appropriate boundary. Multiple optimizers, skipped parameter groups, distributed loaders and changed data exposure need their own explicit policy; the core question remains which event owns the schedule clock.</Prose>
    <ResumeStateFigure />
    <Prose>A resume should reproduce the next transition of the same experiment. Saving only θ loses momentum or adaptive histories. Saving the optimizer but restarting the schedule changes the next rate. Restoring those while drawing different samples changes the next gradient. The program saves a parameter vector, SGD momentum/group state, cosine scheduler state, a dedicated sampling generator, completed count and total budget.</Prose>
    <RunnableExample example={learningRateScheduleExamples.checkpointResume} />
    <Prose>The resumed seven updates match sample indices, actual rates and parameter tensors exactly in this fixed CPU setup. Construct the optimizer and scheduler before loading their saved state, so scheduler initialization does not overwrite restored group rates. The example serializes into an in-memory byte buffer; writing those same bytes to a file is a persistence choice, not an additional source of optimizer state. Other frameworks or components can require more state, and reproducibility across different devices or library versions is a separate question.</Prose>
    <Prose>If you change the planned total budget after resuming, decide whether to continue the old trajectory, extend at a chosen rate or construct a new tail. Recomputing a cosine from a different denominator generally changes its next value. That can be a sensible new policy, but it is not an identical continuation of the former run.</Prose>
    <H3>Run the examples locally</H3>
    <Prose>Each code panel is a complete independent program. Save it as a Python file and run it in an isolated environment. The pure mathematics examples need only Python; the experiment needs NumPy, and the actual scheduler examples need CPU PyTorch. These outputs were checked with Python 3.12.14, NumPy 2.3.5 and PyTorch 2.14.0+cpu.</Prose>
    <CodeBlock language="bash">{`python -m venv .venv
# Activate the environment using your shell, then:
python -m pip install numpy==2.3.5
python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
python schedule_example.py`}</CodeBlock>
    <Prose>For example, activation is <Code>.venv\Scripts\Activate.ps1</Code> in PowerShell or <Code>source .venv/bin/activate</Code> in Bash. CPU packages avoid requiring a GPU. Keep your installed version with your experiment record, since API and numerical behavior can change. The browser investigations use bounded local calculations and do not run PyTorch.</Prose>

    <H2>9. Compare and practise</H2>
    <Prose>There are two different comparison questions. An <strong>isolation experiment</strong> holds chosen factors fixed to study one change, such as rate shape at the same peak on the same sample stream. A <strong>configuration comparison</strong> tunes each optimizer-plus-schedule combination within a documented budget to assess what each can achieve. Forcing unrelated optimizers to share a numeric peak can handicap one; independently tuning them no longer isolates the optimizer formula alone.</Prose>
    <Prose>State the objective, initialization, data split, sample exposure, update count, batch/accumulation, optimizer settings, rate sequence, regularization, validation use and selection rule. If computation differs, say what resource is matched. Several fixed seeds expose some run variation; they do not certify a universal ranking. Keep a final test set outside repeated configuration selection.</Prose>
    <RunnableExample example={learningRateScheduleExamples.controlledExperiment} />
    <Prose>This original noisy calibration dataset compares three fixed policies with three matched sample streams. It uses 120 sample-gradient updates per run and the same peak, without independently tuning each policy. The reported validation mean-squared errors answer that narrow comparison. A different peak, budget, dataset, optimizer or selection process could change the ordering.</Prose>
    <H3>Independent practice</H3>
    <details><summary>1. Construct a new finite schedule</summary><Prose>Use T=6,W=2,M=.12,m=.02 with the nonduplicated-peak convention. Find all six used rates and explain why there are four decay updates but five points if you include the preceding peak.</Prose><details><summary>Hint</summary><Prose>The first two rates are M/2 and M. The decay progress values are 1/4,2/4,3/4,4/4.</Prose></details><details><summary>Explained solution</summary><Prose>The rates are approximately [.06,.12,.105355,.07,.034645,.02]. The peak at update 1 is the decay's progress-zero boundary, already consumed by warmup. Four later updates move across four intervals to the minimum. Repeating .12 would implement a different join.</Prose></details></details>
    <details><summary>2. Separate contraction, crossing and noise</summary><Prose>Use h=4, initial error 2, two rates [.1,.3], and independent noise ε=±.5 with equal probability. Find the final mean, variance and expected squared error. Explain why a single update crossing the target is not evidence of divergence.</Prose><details><summary>Hint</summary><Prose>The multipliers are .6 and −.2. Expand either four noise-sign paths or the two moment recurrences.</Prose></details><details><summary>Explained solution</summary><Prose>After the first update, mean 1.2 and variance .0025. After the second, mean −.24 and variance .04×.0025+.09×.25=.0226. Expected squared error is .0576+.0226=.0802. The second multiplier is negative but has magnitude below 1; it reverses the mean's sign while contracting existing error. Fresh noise still contributes variability.</Prose></details></details>
    <details><summary>3. Repair an accumulation clock</summary><Prose>Nine equal microbatches accumulate three at a time. The second attempt is skipped. A committed-update policy has two rates [.2,.05]. List every consumed rate and explain why advancing once per microbatch exhausts it.</Prose><details><summary>Hint</summary><Prose>Mark only attempts after microbatches 3,6 and 9. A skipped attempt consumes no rate under this stated policy.</Prose></details><details><summary>Explained solution</summary><Prose>Microbatch 3 completes attempt 1 and uses .2; microbatch 6 completes the rejected attempt and uses none; microbatch 9 completes attempt 3 and uses .05. There are two successful transitions. A microbatch counter advances six extra times before even considering the skip and cannot index this two-value update policy correctly. The missing value is an error to diagnose, not a reason to clamp the index silently.</Prose></details></details>
    <details><summary>4. Trace threshold, patience and cooldown</summary><Prose>Minimize a metric with absolute threshold .1, patience 1, cooldown 1, initial rate .4, factor .5 and floor .05. Observe [1,.9,.89,.9,.9,.9,.7]. Record best, bad count, cooldown and rate after each observation. Use the explicit strict comparison rather than “lower is always better.”</Prose><details><summary>Hint</summary><Prose>After best 1, .9 equals its cutoff and is not significant; .89 is below the cutoff. A reduction occurs only when bad count exceeds 1.</Prose></details><details><summary>Explained solution</summary><Prose>After the seven observations, the (best,bad,cooldown,rate) states are (1,0,0,.4),(1,1,0,.4),(.89,0,0,.4),(.89,1,0,.4),(.89,0,1,.2),(.89,0,0,.2),(.7,0,0,.2). The sixth observation is inside cooldown, so its bad count is cleared. The last metric establishes a significant new best. The values here are intended with the displayed arithmetic; inspect actual floating comparisons for a software-boundary test.</Prose></details></details>
    <details><summary>5. Predict a schedule-dependent regularizer</summary><Prose>A parameter starts at 3, participates in two zero-displacement decay updates and uses λ=.2. Compare rates [.1,.1] with [.2,0]. Both sums are .2. Are the exact final parameters equal?</Prose><details><summary>Hint</summary><Prose>Multiply the actual 1−ηλ factors. The exponential approximation uses only their sum and hides second-order terms.</Prose></details><details><summary>Explained solution</summary><Prose>The first gives 3×.98²=2.8812; the second gives 3×.96×1=2.88. Equal rate sums do not make the exact products equal. exp(−.04) would give the same approximation for both, which is useful only with its small-factor qualification.</Prose></details></details>
    <details><summary>6. Resume and diagnose a changed horizon</summary><Prose>In the complete checkpoint example, stop after three updates instead of five and compare all nine remaining updates with an uninterrupted run. Then restore the weights and optimizer but recreate the cosine schedule with a different horizon. Identify which state/value first stops matching and explain whether this is a bug or an intentional new policy.</Prose><details><summary>Hint</summary><Prose>Save completed count and the generator at the new boundary. Compare the next sample, used rate and momentum before comparing the final result.</Prose></details><details><summary>Acceptance and explanation</summary><Prose>A correct identical resume restores the next sample, optimizer state, scheduler state and budget, and matches each later transition in the same runtime. If you intentionally change the horizon, report the first changed rate and resulting update. If you promise identical continuation, that mismatch is a defect. Changing a constructor argument while subsequently loading the old scheduler's full state may restore the old horizon instead; inspect the actual loaded state.</Prose><Prose>For a concrete changed policy, keep the saved optimizer rate and moments but start a fresh twenty-interval cosine clock at the resume boundary. The first resumed update still matches; the second consumes approximately .066142 instead of .057201 and changes the parameter. Loading the original scheduler state afterward restores its eleven-interval horizon, so that constructor-only change would not persist.</Prose></details></details>
    <details><summary>7. Design an informative configuration comparison</summary><Prose>Extend the calibration experiment to compare two peak rates for each policy within an equal trial budget. State what is held fixed, how a configuration is selected, and what an untouched final evaluation would add. Check whether the winner changes across seeds, and report what you observe.</Prose><details><summary>Hint</summary><Prose>Separate candidate selection from final assessment, and distinguish matching sample updates from matching wall-clock time. Do not choose a winner after repeatedly consulting its test error.</Prose></details><details><summary>Acceptance and explanation</summary><Prose>One acceptable design uses the same training/validation split, three matched streams and two prespecified peaks per policy, selects each policy's peak using validation means, then assesses the selected configurations on a separately generated untouched sample set and additional fixed streams. Report per-run values and compute exposure. This evaluates tuned configurations under that budget; it does not isolate only rate shape or establish a general ranking. A finite seed set may or may not contain a crossover, so if it does not, report that observation rather than fabricating one.</Prose><Prose>One checked extension uses peaks .1 and .2, then fresh streams 23, 29 and 37. Validation selects .1 for constant and .2 for both decays. Generate 200 test inputs evenly over [−1,1] with fresh Gaussian noise of standard deviation .3 from NumPy generator seed 101. Test MSEs are approximately [.112672,.103862,.116097] for constant, [.103103,.103900,.109741] for linear and [.103916,.103974,.106589] for warmup/cosine. The winners differ across these three streams. Selection used 2,160 sample updates and final assessment another 1,080. These are results for this declared synthetic experiment.</Prose></details></details>
    <Prose>You are ready to move on when you can identify the actual consumed-rate sequence, derive the simple contraction/noise consequences, trace an event-driven policy and reproduce a resumed run under a stated contract. Memorizing a curve's name is not the finish line. The next module topic is <a href="./convex-duality-lagrangian-methods-kkt-conditions?module=math-foundations">Convex Duality &amp; Lagrangian Methods (KKT Conditions)</a>, which returns to constrained optimization and derives another way to bound and certify a solution.</Prose>
    <Sources alternatives={<div><p><strong>Prefer a lecture?</strong> <a href="https://www.youtube.com/watch?v=nlVOG2Nzc3k" target="_blank" rel="noreferrer">fast.ai lesson 18: Accelerated SGD &amp; ResNets</a> covers annealing, parameter groups and OneCycleLR. The <a href="https://course.fast.ai/Lessons/lesson18.html" target="_blank" rel="noreferrer">official lesson page</a> provides a written overview and companion resources. Its scheduling portions are a useful deeper alternative; neural architecture sections assume more than this scalar introduction. The course outline and direct resource link were inspected, but playback was not reviewed.</p></div>}>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.optim.lr_scheduler.OneCycleLR.html" target="_blank" rel="noreferrer">PyTorch 2.14 OneCycleLR</a> — Actual API phases, division factors, momentum, step order and state; read beside the executed twelve-update example.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.optim.lr_scheduler.CosineAnnealingLR.html" target="_blank" rel="noreferrer">CosineAnnealingLR</a> and <a href="https://docs.pytorch.org/docs/2.14/generated/torch.optim.lr_scheduler.CosineAnnealingWarmRestarts.html" target="_blank" rel="noreferrer">CosineAnnealingWarmRestarts</a> — Different clock/restart contracts; constructor names alone do not specify your training-loop event.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.optim.lr_scheduler.ReduceLROnPlateau.html" target="_blank" rel="noreferrer">ReduceLROnPlateau</a> and its <a href="https://raw.githubusercontent.com/pytorch/pytorch/v2.14.0/torch/optim/lr_scheduler.py" target="_blank" rel="noreferrer">tagged implementation</a> — Inspect the exact strict predicate, counter order and floor behavior; the lesson verifies actual states rather than copying an ambiguous patience shortcut.</li>
      <li><a href="https://arxiv.org/pdf/1608.03983" target="_blank" rel="noreferrer">Loshchilov and Hutter: SGDR</a> — Selected mechanism and EEG sections motivate warm restarts and anytime snapshots. Dated experiments support their particular setups, not a universal ranking.</li>
      <li><a href="https://arxiv.org/pdf/1706.02677" target="_blank" rel="noreferrer">Goyal and colleagues: large-minibatch training</a> — Original scaling/warmup rationale and experiment boundaries. Its ImageNet recipe is context to analyze, not a compulsory modern default.</li>
      <li><a href="https://arxiv.org/pdf/1506.01186" target="_blank" rel="noreferrer">Smith: Cyclical Learning Rates</a> and <a href="https://arxiv.org/pdf/1708.07120" target="_blank" rel="noreferrer">Smith and Topin: Super-Convergence</a> — Original cycle/range-test and one-cycle ideas. Separate empirical advice from guaranteed behavior on a new problem.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/notes/amp_examples.html" target="_blank" rel="noreferrer">PyTorch AMP examples</a> — Effective-batch scaling and skipped optimizer steps; the lesson's controlled CPU fixture verifies the event boundary.</li>
      <li><a href="https://docs.pytorch.org/docs/2.14/generated/torch.optim.Optimizer.load_state_dict.html" target="_blank" rel="noreferrer">Optimizer state restoration</a> and <a href="https://docs.pytorch.org/docs/2.14/notes/randomness.html" target="_blank" rel="noreferrer">reproducibility notes</a> — Initialization/load order and the limits of reproducing a run across environments.</li>
    </Sources>
  </div>
};
