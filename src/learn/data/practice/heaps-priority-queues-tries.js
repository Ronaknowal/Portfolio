export default {
  topicId:'heaps-priority-queues-tries',
  verifiedOn:'10 September 2026',
  introduction:'This topic has two different lookup jobs: choose the next item by priority, or follow a string prefix. Practice both, and explain why a heap, a sorted collection and a trie support different questions.',
  groups:[
    {
      id:'foundation',title:'Foundation · priority order and prefix identity',
      introduction:'Attempt these after the heap-operation and trie exact-versus-prefix investigations. Platform difficulty is independent of where a task fits this lesson.',
      problems:[
        {number:1046,title:'Last Stone Weight',slug:'last-stone-weight',difficulty:'Easy',
          focus:'Repeatedly remove the current highest priorities, then insert a changed value. Input order is not processing order.',
          hint:'Which end of the priority order does each operation need? If using a min-heap interface for maximum values, keep its representation consistent for both insertion and removal.',
          transfer:'Test equal top values and an operation that leaves one item. Compare with repeatedly sorting a baseline and count all heap operations rather than only the final lookup.'},
        {number:703,title:'Kth Largest Element in a Stream',slug:'kth-largest-element-in-a-stream',difficulty:'Easy',
          focus:'Maintain only the candidates that matter as new data arrives, including repeated values.',
          hint:'Which k values determine the answer so far? Among those candidates, which boundary value should be cheap to inspect and replace?',
          transfer:'Use k equal to one, duplicate values and an initially short stream allowed by the statement. Define what your own API would do before k total values exist.'},
        {number:208,title:'Implement Trie (Prefix Tree)',slug:'implement-trie-prefix-tree',difficulty:'Medium',
          focus:'Distinguish a path that exists from a complete word that was inserted, including one word that is another’s prefix.',
          hint:'Reaching the final character proves a path exists. What additional state distinguishes a stored word from an intermediate prefix?',
          transfer:'Insert a longer word, query its shorter prefix, then insert that prefix. Explain why duplicate insertion should not create duplicate paths and what changes for an unrestricted alphabet.'},
      ],
    },
    {
      id:'core',title:'Core · apply the structure to a different request',
      introduction:'Choose a correct baseline first. When a problem has a stricter follow-up, state whether your chosen approach meets it instead of assuming a heap is always optimal.',
      problems:[
        {number:215,title:'Kth Largest Element in an Array',slug:'kth-largest-element-in-an-array',difficulty:'Medium',
          focus:'Transfer the stream boundary invariant to a finite input without confusing rank with distinct-value rank.',
          hint:'Compare retaining all values with retaining only the candidates that can still affect the kth result. Keep duplicate occurrences when they contribute to rank.',
          transfer:'Vary k from one to the full input size. Compare a heap-based method with a sorted baseline; selection algorithms are a later alternative, not a prerequisite here.'},
        {number:347,title:'Top K Frequent Elements',slug:'top-k-frequent-elements',difficulty:'Medium',
          focus:'Turn counts into priorities. Distinguish the number of input items from the number of distinct keys when analyzing the selection stage.',
          hint:'First determine the priority of each distinct value. Then decide which candidates must remain available while selecting the k highest priorities.',
          transfer:'A bounded-heap method can cost O(n + u log(k + 1)) for u distinct values; it does not guarantee the strict better-than-O(n log n) follow-up for every k. Compare frequency buckets as a further extension rather than claiming the heap meets every bound.'},
        {number:648,title:'Replace Words',slug:'replace-words',difficulty:'Medium',
          focus:'Use prefix structure for a shortest-match policy, which differs from retrieving a complete exact word.',
          hint:'During the character walk, what does the first complete stored prefix imply under the shortest-root contract?',
          transfer:'Test nested roots and a word with no matching root. Describe how longest-prefix matching would change the stopping rule; the linguistic story is not a general stemming algorithm.'},
      ],
    },
    {
      id:'stretch',title:'Optional stretch · merge, rank and branch',optional:true,
      introduction:'Choose the extensions that match your next learning goal. These add ordered output, coordinated heaps or branching search; they are not required to finish the core lesson.',
      problems:[
        {number:1268,title:'Search Suggestions System',slug:'search-suggestions-system',difficulty:'Medium',
          focus:'Add a lexicographic output policy to prefix lookup. A trie’s child insertion order is not automatically the required suggestion order.',
          prerequisite:'Trie prefix traversal and lexicographic ordering. A sorted/filtering baseline is useful before implementing stored top suggestions or ordered subtree enumeration.',
          hint:'At each typed prefix, separate finding matching candidates from choosing the first few in the required order. What could be prepared during construction?',
          transfer:'Use a prefix with fewer than three matches and one with none. Explain preprocessing, retained suggestion storage and output costs; compare whether a trie is worthwhile for a small static catalogue.'},
        {number:23,title:'Merge k Sorted Lists',slug:'merge-k-sorted-lists',difficulty:'Hard',
          focus:'Use a priority frontier across several ordered sources while preserving linked-node structure.',
          prerequisite:'Merge Two Sorted Lists, heap push/pop and a tie policy for equal priorities whose node objects are not orderable.',
          hint:'Only one candidate from each nonempty sorted source needs to compete for the next output position. Which source supplies the next candidate after a removal?',
          transfer:'Test empty sources and equal head values. Compare a source index/counter tie-breaker with accidentally asking Python to order node objects.'},
        {number:211,title:'Design Add and Search Words Data Structure',slug:'design-add-and-search-words-data-structure',difficulty:'Medium',
          focus:'Extend exact trie lookup to an input symbol that can follow several candidate edges.',
          prerequisite:'Trie terminal markers plus DFS/backtracking. Return after Graphs: Representations, BFS & DFS or Backtracking & Divide-and-Conquer if branching search is unfamiliar.',
          hint:'An ordinary character chooses one edge; a wildcard may require several possibilities. Each search state needs both a trie node and a position in the pattern.',
          transfer:'Compare an unsuccessful wildcard search with a successful early branch. Account for branching work; the exact-lookup O(length) claim does not automatically survive wildcards.'},
        {number:295,title:'Find Median from Data Stream',slug:'find-median-from-data-stream',difficulty:'Hard',
          focus:'Maintain a boundary between lower and upper values while balancing two structures after each arrival.',
          prerequisite:'Min/max heap interfaces, median for odd/even counts, and reasoning about two simultaneous invariants.',
          hint:'Which extreme value from each half determines the median? State both the permitted size difference and the ordering relationship between the halves.',
          transfer:'Test duplicates, alternating extremes and every odd/even transition. Compare with a sorted-list oracle and distinguish an exact median from an approximate streaming quantile.'},
      ],
    },
  ],
  readiness:[
    'Explain which priority boundary belongs at a heap root and why the rest of the heap array need not be sorted.',
    'Distinguish complete words, prefixes and wildcard matches, including their different state and cost requirements.',
    'Specify tie behavior, duplicate handling and the extra memory retained by a top-k or prefix index.',
  ],
  localBridge:'Use the lesson’s heap trace to inspect sift operations and its trie view to inspect shared prefixes. Library-based accepted solutions can hide those mechanisms; neither a judge result nor the problem’s topic tags establish that you understand the representation.',
};
