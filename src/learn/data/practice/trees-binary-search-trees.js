export default {
  topicId:'trees-binary-search-trees',
  verifiedOn:'10 September 2026',
  introduction:'Practice both kinds of reasoning in this title: navigating any binary tree, and using the stronger ordering promise of a binary search tree. Draw the shape first; a serialized array is an input format, not permission to assume a complete tree.',
  groups:[
    {
      id:'foundation',title:'Foundation · visit, measure and search',
      introduction:'Start once nodes, missing children, traversal order and the BST ordering invariant are clear. Use an explicit stack if recursive call state is not yet comfortable.',
      problems:[
        {number:104,title:'Maximum Depth of Binary Tree',slug:'maximum-depth-of-binary-tree',difficulty:'Easy',
          focus:'Connect a subtree’s result to its children and state what is being counted. This problem counts nodes along the path, which can differ from an edge-based height convention.',
          hint:'Decide the result for a missing subtree. For a present node, which child determines the longer route, and what does the current node contribute?',
          transfer:'Compare an empty tree, a single node and a long chain. Report both time and call-stack/frontier space; do not assume the tree is balanced.'},
        {number:94,title:'Binary Tree Inorder Traversal',slug:'binary-tree-inorder-traversal',difficulty:'Easy',
          focus:'Make the return point visible: a node may need to wait while its left subtree is visited.',
          hint:'Before emitting a node’s value, what work must be complete? For an iterative version, which waiting ancestors need to remain accessible?',
          transfer:'Use an ordinary binary tree whose inorder values are not sorted. Explain why sorted output follows only when the BST invariant holds.'},
        {number:700,title:'Search in a Binary Search Tree',slug:'search-in-a-binary-search-tree',difficulty:'Easy',
          focus:'Use ordering to discard one whole subtree and return the existing matching node, not only a boolean.',
          hint:'Compare the target with the current value. Which descendant region can still contain it under the BST contract?',
          transfer:'Test an absent value and the root itself. Construct a skewed tree to explain why O(height) does not automatically mean O(log n).'},
      ],
    },
    {
      id:'core',title:'Core · preserve structure and global order',
      introduction:'Combine the lesson’s traversals and update cases. Validate the result with a separate traversal rather than checking one expected picture.',
      problems:[
        {number:102,title:'Binary Tree Level Order Traversal',slug:'binary-tree-level-order-traversal',difficulty:'Medium',
          focus:'Carry FIFO reasoning from queues into a branching structure while keeping successive depths separate.',
          hint:'Which nodes belong to the level that is already waiting when a round begins? New children belong to a later level even if they enter the same queue.',
          transfer:'Compare the maximum queue size for a chain and a broad tree. Keep grouping correct when some parents have only one child.'},
        {number:98,title:'Validate Binary Search Tree',slug:'validate-binary-search-tree',difficulty:'Medium',
          focus:'Check a global ordering promise rather than only comparing each node with its immediate children. LeetCode uses strict inequalities, so duplicate values are invalid here.',
          hint:'Every ancestor may constrain a descendant. What allowable interval does the route from the root impose at the current node?',
          transfer:'Construct a node that satisfies its parent comparison but violates a grandparent’s bound. Test duplicate values and the allowed extreme integer values.'},
        {number:701,title:'Insert into a Binary Search Tree',slug:'insert-into-a-binary-search-tree',difficulty:'Medium',
          focus:'Find an attachment point and return the possibly changed root. The platform guarantees the new value is absent, so its contract differs from duplicate-count or replacement policies.',
          hint:'Follow the same comparisons as search. What does a missing child mean when the goal is insertion rather than lookup?',
          transfer:'Insert into an empty tree, then insert an increasing sequence. Check sorted inorder output and explain why ordinary insertion does not rebalance the tree.'},
        {number:450,title:'Delete Node in a BST',slug:'delete-node-in-a-bst',difficulty:'Medium',
          focus:'Handle no child, one child and two children without losing the remaining keys. Several final shapes can satisfy the same valid contract.',
          hint:'For the two-child case, which key can take the deleted key’s place without breaking the order between both remaining subtrees? That replacement must also be removed from its old location.',
          transfer:'Delete the root, an absent key and a two-child node whose successor has a right child. Validate both the remaining key set and the full BST invariant.'},
      ],
    },
    {
      id:'stretch',title:'Optional stretch · combine subtree information',optional:true,
      introduction:'These ask for a new deduction from the tree mechanisms. Return after the core tasks; recursion and traversal reconstruction may need a later review.',
      problems:[
        {number:235,title:'Lowest Common Ancestor of a Binary Search Tree',slug:'lowest-common-ancestor-of-a-binary-search-tree',difficulty:'Medium',
          focus:'Follow two target routes under the same ordering promise and identify where their shared route ends.',
          prerequisite:'BST search and the meaning of ancestor/descendant. The statement guarantees both target nodes exist and allows a target itself to be the ancestor.',
          hint:'If both target values lie on the same side, can the current node be the lowest shared ancestor? What changes when their routes separate?',
          transfer:'Test an ancestor paired with its own descendant. Explain why the value-based shortcut cannot be reused unchanged for an arbitrary binary tree.'},
        {number:110,title:'Balanced Binary Tree',slug:'balanced-binary-tree',difficulty:'Easy',
          focus:'Combine height information and validity from every subtree; the platform’s Easy label does not remove the need for careful composition.',
          prerequisite:'Subtree height/depth and postorder reasoning. This checks height balance; it does not implement rotations or make a tree balanced.',
          hint:'A subtree result can carry enough information for its parent to know both its height and whether a deeper violation was found.',
          transfer:'Create a tree with a balanced-looking root but an unbalanced descendant. Count repeated visits if you recompute each subtree’s height separately.'},
        {number:105,title:'Construct Binary Tree from Preorder and Inorder Traversal',slug:'construct-binary-tree-from-preorder-and-inorder-traversal',difficulty:'Medium',
          focus:'Recover structure from two different views of the same nodes and track the boundaries of each recursive subproblem.',
          prerequisite:'Preorder/inorder, recursive divide-and-conquer and array ranges. Revisit Complexity Analysis & Recursion and Backtracking & Divide-and-Conquer as needed.',
          hint:'Which traversal identifies the next subtree root? In the other traversal, what separates the nodes belonging to its left and right subtrees?',
          transfer:'Explain why the distinct-values guarantee matters. Compare repeated slicing/searching with range indices and a precomputed lookup, including their actual costs.'},
      ],
    },
  ],
  readiness:[
    'Choose a traversal from the information the parent or caller needs; explain pending work in a stack or queue.',
    'Justify a BST decision using all relevant ancestor bounds and a stated duplicate policy.',
    'Validate edits by remaining keys, reachability and ordering, and express search/update costs in terms of actual height.',
  ],
  localBridge:'Revisit the lesson’s shape and update investigations as well as these submissions. Judge inputs do not by themselves show aliasing, every intermediate pointer change, or the contrast between balanced and skewed search paths.',
};
