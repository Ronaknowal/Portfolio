// Complete executed Python programs. Generated by scripts/generate-linked-traversal-extension.py.
export const linkedTraversalExamples = {
  "cycle": {
    "filename": "cycle_entry.py",
    "question": "Seven equal-valued nodes end at n2. Which object is the entry, and what survives when the tail instead points to n4?",
    "code": "class Node:\n    def __init__(self, label, value):\n        self.label = label\n        self.value = value\n        self.next = None\n\n\ndef make_chain(values, entry=-1):\n    if type(entry) is not int or not -1 <= entry < len(values):\n        raise ValueError(\"entry must be -1 or a valid node index\")\n    nodes = [Node(f\"n{i}\", value) for i, value in enumerate(values)]\n    for left, right in zip(nodes, nodes[1:]):\n        left.next = right\n    if nodes and entry >= 0:\n        nodes[-1].next = nodes[entry]\n    return (nodes[0] if nodes else None), nodes\n\n\ndef cycle_entry(head):\n    slow = fast = head\n    while fast is not None and fast.next is not None:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            slow = head\n            while slow is not fast:\n                slow = slow.next\n                fast = fast.next\n            return slow\n    return None\n\n\ndef entry_by_seen(head):\n    seen = set()\n    current = head\n    while current is not None:\n        if current in seen:\n            return current\n        seen.add(current)\n        current = current.next\n    return None\n\n\ndef cycle_size(entry):\n    # The argument is an entry returned by cycle_entry, or None.\n    if entry is None:\n        return 0\n    length = 1\n    current = entry.next\n    while current is not entry:\n        length += 1\n        current = current.next\n    return length\n\n\nfor size, join in [(7, 2), (7, 4), (1, 0), (1, -1), (0, -1)]:\n    head, nodes = make_chain([7] * size, join)\n    old_links = tuple(node.next for node in nodes)\n    entry = cycle_entry(head)\n    print(\"nodes / tail target:\", size, join)\n    print(\"entry:\", entry.label if entry else None, \"cycle size:\", cycle_size(entry))\n    print(\"same object as visited method:\", entry is entry_by_seen(head))\n    print(\n        \"links unchanged:\", all(node.next is old for node, old in zip(nodes, old_links))\n    )\n",
    "output": "nodes / tail target: 7 2\nentry: n2 cycle size: 5\nsame object as visited method: True\nlinks unchanged: True\nnodes / tail target: 7 4\nentry: n4 cycle size: 3\nsame object as visited method: True\nlinks unchanged: True\nnodes / tail target: 1 0\nentry: n0 cycle size: 1\nsame object as visited method: True\nlinks unchanged: True\nnodes / tail target: 1 -1\nentry: None cycle size: 0\nsame object as visited method: True\nlinks unchanged: True\nnodes / tail target: 0 -1\nentry: None cycle size: 0\nsame object as visited method: True\nlinks unchanged: True"
  },
  "middle": {
    "filename": "middle_split.py",
    "question": "For lengths four and five, distinguish both middle queries from the left-heavy split. Which one link is changed?",
    "code": "class Node:\n    def __init__(self, label, value):\n        self.label = label\n        self.value = value\n        self.next = None\n\n\ndef make_chain(values, entry=-1):\n    if type(entry) is not int or not -1 <= entry < len(values):\n        raise ValueError(\"entry must be -1 or a valid node index\")\n    nodes = [Node(f\"n{i}\", value) for i, value in enumerate(values)]\n    for left, right in zip(nodes, nodes[1:]):\n        left.next = right\n    if nodes and entry >= 0:\n        nodes[-1].next = nodes[entry]\n    return (nodes[0] if nodes else None), nodes\n\n\ndef cycle_entry(head):\n    slow = fast = head\n    while fast is not None and fast.next is not None:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            slow = head\n            while slow is not fast:\n                slow = slow.next\n                fast = fast.next\n            return slow\n    return None\n\n\ndef middle(head, first=False):\n    # Precondition for this small internal helper: an acyclic chain.\n    if head is None:\n        return None\n    slow = fast = head\n    if first:\n        while fast.next is not None and fast.next.next is not None:\n            slow = slow.next\n            fast = fast.next.next\n    else:\n        while fast is not None and fast.next is not None:\n            slow = slow.next\n            fast = fast.next.next\n    return slow\n\n\ndef split_left_heavy(head):\n    if cycle_entry(head) is not None:\n        raise ValueError(\"split requires an acyclic chain; links were not changed\")\n    boundary = middle(head, first=True)\n    if boundary is None:\n        return None, None\n    right = boundary.next\n    boundary.next = None\n    return head, right\n\n\ndef labels(head):\n    result = []\n    while head is not None:\n        result.append(head.label)\n        head = head.next\n    return result\n\n\nfor size in [0, 1, 4, 5, 6]:\n    head, nodes = make_chain([7] * size)\n    first = middle(head, first=True)\n    second = middle(head)\n    left, right = split_left_heavy(head)\n    print(\n        \"size:\",\n        size,\n        \"first / second:\",\n        first.label if first else None,\n        second.label if second else None,\n    )\n    print(\"returned chains:\", labels(left), labels(right))\nhead, nodes = make_chain([7, 7, 7], 1)\nbefore = tuple(node.next for node in nodes)\ntry:\n    split_left_heavy(head)\nexcept ValueError as error:\n    print(error)\nprint(\n    \"rejected split preserved links:\",\n    all(node.next is old for node, old in zip(nodes, before)),\n)\n",
    "output": "size: 0 first / second: None None\nreturned chains: [] []\nsize: 1 first / second: n0 n0\nreturned chains: ['n0'] []\nsize: 4 first / second: n1 n2\nreturned chains: ['n0', 'n1'] ['n2', 'n3']\nsize: 5 first / second: n2 n2\nreturned chains: ['n0', 'n1', 'n2'] ['n3', 'n4']\nsize: 6 first / second: n2 n3\nreturned chains: ['n0', 'n1', 'n2'] ['n3', 'n4', 'n5']\nsplit requires an acyclic chain; links were not changed\nrejected split preserved links: True"
  }
};
