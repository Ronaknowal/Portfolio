// Complete Python programs, formatted and executed before capture.
export const realAnalysisExamples = [
  {
    "id": "tail-certificate",
    "title": "Certify every later estimate",
    "question": "At tolerance 1/10, does index 9 suffice for a strict error bound?",
    "code": "from fractions import Fraction\n\n\ndef tail_start(tolerance):\n    tolerance = Fraction(tolerance)\n    if tolerance <= 0:\n        raise ValueError(\"Tolerance must be positive\")\n    return max(1, tolerance.denominator // tolerance.numerator)\n\n\nfor tolerance in [Fraction(1, 10), Fraction(2, 7), Fraction(3, 2)]:\n    n = tail_start(tolerance)\n    error = Fraction(1, n + 1)\n    print(\n        f\"epsilon={tolerance}, N={n}, first error={error}, strict={error < tolerance}\"\n    )\n    if n > 1:\n        print(\"previous index fails:\", Fraction(1, n) >= tolerance)\nprint(\"epsilon=1/10, N=9:\", Fraction(1, 10) < Fraction(1, 10))\nprint(\"For every k >= N, 1/(k+1) <= 1/(N+1); that inequality covers the tail.\")\n",
    "expected": "epsilon=1/10, N=10, first error=1/11, strict=True\nprevious index fails: True\nepsilon=2/7, N=3, first error=1/4, strict=True\nprevious index fails: True\nepsilon=3/2, N=1, first error=1/2, strict=True\nepsilon=1/10, N=9: False\nFor every k >= N, 1/(k+1) <= 1/(N+1); that inequality covers the tail."
  },
  {
    "id": "exact-bracket",
    "title": "Keep the interval as exact fractions",
    "question": "What does the interval width certify that a printed decimal does not?",
    "code": "from fractions import Fraction\n\n\ndef square_root_bracket(target, steps, left=1, right=2):\n    target, left, right = map(Fraction, (target, left, right))\n    if type(steps) is not int or steps < 0 or not 0 <= left < right:\n        raise ValueError(\n            \"Use ordered nonnegative endpoints and a nonnegative integer step count\"\n        )\n    if not left * left <= target <= right * right:\n        raise ValueError(\"The endpoint squares must enclose the target\")\n    for _ in range(steps):\n        midpoint = (left + right) / 2\n        if midpoint * midpoint < target:\n            left = midpoint\n        else:\n            right = midpoint\n    return left, right\n\n\nfor steps in [0, 3, 8]:\n    left, right = square_root_bracket(2, steps)\n    print(f\"steps={steps}, interval=[{left}, {right}], width={right-left}\")\n    print(\"squared endpoints straddle 2:\", left * left <= 2 <= right * right)\n    print(\"midpoint absolute-error bound:\", (right - left) / 2)\nprint(\"The bound uses completeness and the invariant, not sqrt() as a test oracle.\")\n",
    "expected": "steps=0, interval=[1, 2], width=1\nsquared endpoints straddle 2: True\nmidpoint absolute-error bound: 1/2\nsteps=3, interval=[11/8, 3/2], width=1/8\nsquared endpoints straddle 2: True\nmidpoint absolute-error bound: 1/16\nsteps=8, interval=[181/128, 363/256], width=1/256\nsquared endpoints straddle 2: True\nmidpoint absolute-error bound: 1/512\nThe bound uses completeness and the invariant, not sqrt() as a test oracle."
  },
  {
    "id": "cauchy-blocks",
    "title": "Compare a small step with an entire later block",
    "question": "Can a shrinking next increment coexist with a block gap of at least 1/2?",
    "code": "from fractions import Fraction\n\n\ndef block_gap(n, family):\n    if type(n) is not int or n < 1 or family not in (\"harmonic\", \"telescoping\"):\n        raise ValueError(\"Use positive integer n and a known family\")\n    return sum(\n        (Fraction(1, k) if family == \"harmonic\" else Fraction(1, k * (k + 1)))\n        for k in range(n + 1, 2 * n + 1)\n    )\n\n\nfor n in [2, 8, 32]:\n    harmonic = block_gap(n, \"harmonic\")\n    telescoping = block_gap(n, \"telescoping\")\n    print(f\"N={n}: next harmonic step={Fraction(1,n+1)}\")\n    print(f\"  harmonic block >= 1/2: {harmonic >= Fraction(1,2)}\")\n    expected = Fraction(1, n + 1) - Fraction(1, 2 * n + 1)\n    print(f\"  telescoping block={telescoping}; identity={telescoping == expected}\")\nprint(\"The harmonic lower bound holds for every N: N terms, each at least 1/(2N).\")\n",
    "expected": "N=2: next harmonic step=1/3\n  harmonic block >= 1/2: True\n  telescoping block=2/15; identity=True\nN=8: next harmonic step=1/9\n  harmonic block >= 1/2: True\n  telescoping block=8/153; identity=True\nN=32: next harmonic step=1/33\n  harmonic block >= 1/2: True\n  telescoping block=32/2145; identity=True\nThe harmonic lower bound holds for every N: N terms, each at least 1/(2N)."
  },
  {
    "id": "series-bounds",
    "title": "Bracket a sum before trusting its digits",
    "question": "Which tail estimate applies to a positive geometric sum and which to an alternating one?",
    "code": "from fractions import Fraction\n\n\ndef geometric_partial(ratio, n):\n    ratio = Fraction(ratio)\n    if abs(ratio) >= 1 or type(n) is not int or n < 0:\n        raise ValueError(\"Use |ratio|<1 and integer n>=0\")\n    partial = sum(ratio**k for k in range(n + 1))\n    exact_sum = 1 / (1 - ratio)\n    tail_bound = abs(ratio) ** (n + 1) / (1 - abs(ratio))\n    return partial, exact_sum, tail_bound\n\n\npartial, limit, bound = geometric_partial(Fraction(-2, 3), 6)\nprint(\"geometric partial:\", partial)\nprint(\n    \"exact limit / actual error / absolute tail bound:\",\n    limit,\n    abs(limit - partial),\n    bound,\n)\nn = 10\nalternating = sum(Fraction((-1) ** (k + 1), k) for k in range(1, n + 1))\nnext_partial = alternating + Fraction(1, n + 1)\nprint(\"alternating harmonic limit bracket:\", alternating, next_partial)\nprint(\"bracket width:\", next_partial - alternating)\nprint(\n    \"The decreasing next-term magnitudes prove the bracket; its decimal width is not the proof.\"\n)\n",
    "expected": "geometric partial: 463/729\nexact limit / actual error / absolute tail bound: 3/5 128/3645 128/729\nalternating harmonic limit bracket: 1627/2520 20417/27720\nbracket width: 1/11\nThe decreasing next-term magnitudes prove the bracket; its decimal width is not the proof."
  },
  {
    "id": "power-witness",
    "title": "Inspect a fixed input and a moving witness",
    "question": "Why does changing n while keeping x fixed answer a different question from changing x with n?",
    "code": "from fractions import Fraction\nfrom math import exp, log\n\nfixed = Fraction(3, 4)\nfor n in [2, 8, 32]:\n    witness = exp(-log(2) / n)\n    print(f\"n={n}: fixed (3/4)^n={float(fixed**n):.8f}\")\n    print(f\"  moving x_n={witness:.8f}, numerical x_n^n={witness**n:.8f}\")\n    print(f\"  [0,3/4] exact sup bound={(fixed**n)}\")\nprint(\"On [0,1), sup x^n = 1, unattained. Analytically (2^(-1/n))^n = 1/2.\")\nprint(\"On [0,1], the limit at 1 is 1; the near-1 error still has supremum 1.\")\n",
    "expected": "n=2: fixed (3/4)^n=0.56250000\n  moving x_n=0.70710678, numerical x_n^n=0.50000000\n  [0,3/4] exact sup bound=9/16\nn=8: fixed (3/4)^n=0.10011292\n  moving x_n=0.91700404, numerical x_n^n=0.50000000\n  [0,3/4] exact sup bound=6561/65536\nn=32: fixed (3/4)^n=0.00010045\n  moving x_n=0.97857206, numerical x_n^n=0.50000000\n  [0,3/4] exact sup bound=1853020188851841/18446744073709551616\nOn [0,1), sup x^n = 1, unattained. Analytically (2^(-1/n))^n = 1/2.\nOn [0,1], the limit at 1 is 1; the near-1 error still has supremum 1."
  },
  {
    "id": "triangle-grid",
    "title": "Let exact geometry expose a missed peak",
    "question": "What can a zero sampled maximum fail to tell you?",
    "code": "from fractions import Fraction\n\n\ndef triangle(x, n, height):\n    x, height = Fraction(x), Fraction(height)\n    return height * max(Fraction(0), 1 - abs(n * x - 1))\n\n\nn, intervals = 64, 20\nfor height in [Fraction(1), Fraction(n), Fraction(1, n)]:\n    samples = [\n        triangle(Fraction(j, intervals), n, height) for j in range(intervals + 1)\n    ]\n    area = height / n\n    square_area = 2 * height**2 / (3 * n)\n    print(f\"height={height}: sampled max={max(samples)}, true peak={height}\")\n    print(f\"  area={area}, squared L2 error={square_area}, witness={Fraction(1,n)}\")\n    assert triangle(Fraction(1, n), n, height) == height\nprint(\"n>2*intervals puts the support before the first positive grid point.\")\n",
    "expected": "height=1: sampled max=0, true peak=1\n  area=1/64, squared L2 error=1/96, witness=1/64\nheight=64: sampled max=0, true peak=64\n  area=1, squared L2 error=128/3, witness=1/64\nheight=1/64: sampled max=0, true peak=1/64\n  area=1/4096, squared L2 error=1/393216, witness=1/64\nn>2*intervals puts the support before the first positive grid point."
  },
  {
    "id": "integral-interchange",
    "title": "Compare an integral with an all-domain bound",
    "question": "Does a sharper formula for one family make the uniform-convergence theorem unnecessary?",
    "code": "from math import cos\n\n# f_n(x)=x+sin(n*x)/n on [0,2]. Integrate each term analytically.\nfor n in [4, 16, 64]:\n    integral = 2 + (1 - cos(2 * n)) / n**2\n    theorem_bound = 2 / n\n    sharper_bound = 2 / n**2\n    print(f\"n={n}: integral={integral:.9f}, error={abs(integral-2):.9f}\")\n    print(\n        f\"  uniform theorem bound={theorem_bound:.6f}, family-specific bound={sharper_bound:.6f}\"\n    )\nprint(\n    \"Different domain: (1/n)*indicator_[0,n] on [0,infinity) has sup 1/n but integral 1.\"\n)\n",
    "expected": "n=4: integral=2.071593752, error=0.071593752\n  uniform theorem bound=0.500000, family-specific bound=0.125000\nn=16: integral=2.000647565, error=0.000647565\n  uniform theorem bound=0.125000, family-specific bound=0.007812\nn=64: integral=2.000413305, error=0.000413305\n  uniform theorem bound=0.031250, family-specific bound=0.000488\nDifferent domain: (1/n)*indicator_[0,n] on [0,infinity) has sup 1/n but integral 1."
  },
  {
    "id": "derivative-interchange",
    "title": "Keep height and slope errors separate",
    "question": "What happens to the derivative at zero when the curve itself tends uniformly to zero?",
    "code": "from math import sin, cos, pi\n\n\ndef value_and_derivative(n, power, x):\n    if type(n) is not int or n < 1 or power not in (1, 2):\n        raise ValueError(\"Use a positive integer index and power 1 or 2\")\n    return sin(n * x) / n**power, cos(n * x) / n ** (power - 1)\n\n\nfor power in [1, 2]:\n    for n in [3, 4, 12]:\n        _, at_zero = value_and_derivative(n, power, 0)\n        _, at_pi = value_and_derivative(n, power, pi)\n        print(\n            f\"power={power}, n={n}: function bound={1/n**power:.6f}, derivative at 0={at_zero:.6f}, at pi={at_pi:.6f}\"\n        )\nprint(\n    \"Drifting constants f_n(x)=n have derivative 0; one convergent base value is still needed.\"\n)\n",
    "expected": "power=1, n=3: function bound=0.333333, derivative at 0=1.000000, at pi=-1.000000\npower=1, n=4: function bound=0.250000, derivative at 0=1.000000, at pi=1.000000\npower=1, n=12: function bound=0.083333, derivative at 0=1.000000, at pi=1.000000\npower=2, n=3: function bound=0.111111, derivative at 0=0.333333, at pi=-0.333333\npower=2, n=4: function bound=0.062500, derivative at 0=0.250000, at pi=0.250000\npower=2, n=12: function bound=0.006944, derivative at 0=0.083333, at pi=0.083333\nDrifting constants f_n(x)=n have derivative 0; one convergent base value is still needed."
  },
  {
    "id": "series-endpoint",
    "title": "Check the endpoint after proving the interior",
    "question": "Can the functions converge uniformly while the derivative series fails at x=1?",
    "code": "from fractions import Fraction\n\n\ndef inverse_square_sums(n, x):\n    x = Fraction(x)\n    if type(n) is not int or n < 1 or abs(x) > 1:\n        raise ValueError(\"Use n>=1 and |x|<=1\")\n    value = sum(x**k / Fraction(k * k) for k in range(1, n + 1))\n    derivative = sum(x ** (k - 1) / Fraction(k) for k in range(1, n + 1))\n    return value, derivative\n\n\nfor n in [8, 32, 128]:\n    endpoint, derivative = inverse_square_sums(n, 1)\n    interior, interior_derivative = inverse_square_sums(n, Fraction(1, 2))\n    print(\n        f\"n={n}: f_n(1)={float(endpoint):.8f}, derivative sum at 1={float(derivative):.8f}\"\n    )\n    print(\n        f\"  f_n(1/2)={float(interior):.8f}, derivative at 1/2={float(interior_derivative):.8f}, uniform tail <= 1/{n}\"\n    )\nprint(\n    \"The endpoint derivative sum is harmonic. The uniform tail bound belongs to the function series.\"\n)\n",
    "expected": "n=8: f_n(1)=1.52742205, derivative sum at 1=2.71785714\n  f_n(1/2)=0.58219964, derivative at 1/2=1.38550037, uniform tail <= 1/8\nn=32: f_n(1)=1.61416726, derivative sum at 1=4.05849520\n  f_n(1/2)=0.58224053, derivative at 1/2=1.38629436, uniform tail <= 1/32\nn=128: f_n(1)=1.63715200, derivative sum at 1=5.43314709\n  f_n(1/2)=0.58224053, derivative at 1/2=1.38629436, uniform tail <= 1/128\nThe endpoint derivative sum is harmonic. The uniform tail bound belongs to the function series."
  },
  {
    "id": "bernstein-weights",
    "title": "Build a polynomial value from exact weights",
    "question": "Why does a weighted average reproduce linear functions but usually change a corner?",
    "code": "from fractions import Fraction\nfrom math import comb\n\n\ndef weights(n, x):\n    x = Fraction(x)\n    if type(n) is not int or n < 1 or not 0 <= x <= 1:\n        raise ValueError(\"Use degree>=1 and x in [0,1]\")\n    return [comb(n, k) * x**k * (1 - x) ** (n - k) for k in range(n + 1)]\n\n\nn, x, corner = 8, Fraction(2, 5), Fraction(3, 10)\nmass = weights(n, x)\nmean = sum(p * Fraction(k, n) for k, p in enumerate(mass))\nvariance = sum(p * (Fraction(k, n) - x) ** 2 for k, p in enumerate(mass))\napproximation = sum(p * abs(Fraction(k, n) - corner) for k, p in enumerate(mass))\nprint(\"weight sum:\", sum(mass))\nprint(\"weighted node mean:\", mean)\nprint(\"weighted squared deviation:\", variance, \"formula:\", x * (1 - x) / n)\nprint(\"target / polynomial:\", abs(x - corner), approximation)\nprint(\"squared error <= variance:\", (approximation - abs(x - corner)) ** 2 <= variance)\nprint(\"A constant or linear target is reproduced exactly by the first two identities.\")\n",
    "expected": "weight sum: 1\nweighted node mean: 2/5\nweighted squared deviation: 3/100 formula: 3/100\ntarget / polynomial: 1/10 634111/3906250\nsquared error <= variance: True\nA constant or linear target is reproduced exactly by the first two identities."
  },
  {
    "id": "typewriter",
    "title": "Follow the same observer through shrinking intervals",
    "question": "Does small probability at each late index mean one observer is eventually never visited?",
    "code": "from fractions import Fraction\n\n\ndef visit(block, position, observer):\n    if type(block) is not int or block < 0:\n        raise ValueError(\"Use a nonnegative integer block\")\n    observer = Fraction(observer)\n    count = 2**block\n    if type(position) is not int or not 0 <= position < count or not 0 <= observer < 1:\n        raise ValueError(\"Use a valid dyadic block, position and observer in [0,1)\")\n    return Fraction(position, count) <= observer < Fraction(position + 1, count)\n\n\nfor observer in [Fraction(1, 3), Fraction(1, 2)]:\n    for block in range(1, 5):\n        count = 2**block\n        hits = [j for j in range(count) if visit(block, j, observer)]\n        print(\n            f\"observer={observer}, block={block}: hit positions={hits}, per-index probability={Fraction(1,count)}\"\n        )\nprint(\n    \"Every block partitions [0,1): one hit and, from block 1, at least one miss for each observer.\"\n)\n",
    "expected": "observer=1/3, block=1: hit positions=[0], per-index probability=1/2\nobserver=1/3, block=2: hit positions=[1], per-index probability=1/4\nobserver=1/3, block=3: hit positions=[2], per-index probability=1/8\nobserver=1/3, block=4: hit positions=[5], per-index probability=1/16\nobserver=1/2, block=1: hit positions=[1], per-index probability=1/2\nobserver=1/2, block=2: hit positions=[2], per-index probability=1/4\nobserver=1/2, block=3: hit positions=[4], per-index probability=1/8\nobserver=1/2, block=4: hit positions=[8], per-index probability=1/16\nEvery block partitions [0,1): one hit and, from block 1, at least one miss for each observer."
  },
  {
    "id": "tail-mass",
    "title": "A bounded mean can hide tail mass at larger values",
    "question": "Can one common cutoff make the tail expectation small for every index?",
    "code": "from fractions import Fraction\n\n\ndef spike_tail(n, cutoff):\n    if type(n) is not int or n < 1 or cutoff < 0:\n        raise ValueError(\"Use n>=1 and a nonnegative cutoff\")\n    # X_n=n on an event of probability 1/n, and zero otherwise.\n    return Fraction(1) if n > cutoff else Fraction(0)\n\n\nfor cutoff in [2, 10, 100]:\n    n = cutoff + 1\n    print(\n        f\"cutoff={cutoff}, choose n={n}: mean=1, tail mean={spike_tail(n,cutoff)}, P(X_n>0)={Fraction(1,n)}\"\n    )\nprint(\n    \"For each cutoff the supremum tail mean is 1, so the family is not uniformly integrable.\"\n)\nprint(\"A common (1+delta)-moment bound C instead gives tail mean <= C/cutoff^delta.\")\n",
    "expected": "cutoff=2, choose n=3: mean=1, tail mean=1, P(X_n>0)=1/3\ncutoff=10, choose n=11: mean=1, tail mean=1, P(X_n>0)=1/11\ncutoff=100, choose n=101: mean=1, tail mean=1, P(X_n>0)=1/101\nFor each cutoff the supremum tail mean is 1, so the family is not uniformly integrable.\nA common (1+delta)-moment bound C instead gives tail mean <= C/cutoff^delta."
  },
  {
    "id": "approximation-report",
    "title": "Certify a changed polynomial-integral approximation",
    "question": "If a sufficient bound exceeds the budget, has the actual approximation necessarily failed?",
    "code": "from fractions import Fraction\n\n\ndef approximation_report(n, corner, slope, tolerance):\n    corner, slope, tolerance = map(Fraction, (corner, slope, tolerance))\n    if (\n        type(n) is not int\n        or n < 1\n        or not 0 <= corner <= 1\n        or slope < 0\n        or tolerance <= 0\n    ):\n        raise ValueError(\"Use degree>=1, corner in [0,1], slope>=0 and tolerance>0\")\n    node_values = [slope * abs(Fraction(k, n) - corner) for k in range(n + 1)]\n    # Integral of each Bernstein basis polynomial is exactly 1/(n+1).\n    polynomial_integral = sum(node_values) / (n + 1)\n    target_integral = slope * (corner**2 + (1 - corner) ** 2) / 2\n    error = abs(polynomial_integral - target_integral)\n    # Compare the squared Lipschitz bound to avoid rounding a square root.\n    certified = slope**2 <= 4 * n * tolerance**2\n    return polynomial_integral, target_integral, error, certified\n\n\nfor n in [25, 100]:\n    result = approximation_report(n, Fraction(3, 10), 1, Fraction(1, 20))\n    print(\n        f\"n={n}: polynomial integral={result[0]}, target={result[1]}, exact error={result[2]}\"\n    )\n    print(\"  certified by uniform bound for tolerance 1/20:\", result[3])\nchanged = approximation_report(100, Fraction(2, 5), 2, Fraction(1, 10))\nprint(\"changed corner=2/5, slope=2, n=100, tolerance=1/10:\", changed)\nprint(\n    \"Exact finite sums and integrals; the uniform bound controls all x. No grid maximum is used as proof.\"\n)\n",
    "expected": "n=25: polynomial integral=97/325, target=29/100, exact error=11/1300\n  certified by uniform bound for tolerance 1/20: False\nn=100: polynomial integral=59/202, target=29/100, exact error=21/10100\n  certified by uniform bound for tolerance 1/20: True\nchanged corner=2/5, slope=2, n=100, tolerance=1/10: (Fraction(53, 101), Fraction(13, 25), Fraction(12, 2525), True)\nExact finite sums and integrals; the uniform bound controls all x. No grid maximum is used as proof."
  }
];
