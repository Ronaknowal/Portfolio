export const geometryTrigonometryExamples = [
  {
    "id": "distance",
    "title": "Separate a position, a displacement and a length",
    "question": "Why does reversing the journey negate the displacement but preserve its length?",
    "code": "import math\nfrom fractions import Fraction\n\np = (1, 2)\nq = (4, 6)\ndx, dy = q[0] - p[0], q[1] - p[1]\ndistance = math.hypot(dx, dy)\nprint(\"displacement in metres:\", (dx, dy))\nprint(\"straight-line metres:\", distance)\nprint(\"axis-aligned route metres:\", abs(dx) + abs(dy))\nprint(\"reverse displacement:\", (-dx, -dy))\na, b = Fraction(3), Fraction(4)\nouter_area = (a + b) ** 2\nfour_triangles = 4 * a * b / 2\nprint(\"central square area:\", outer_area - four_triangles)\nprint(\"triangle area:\", a * b / 2)\n",
    "expected": "displacement in metres: (3, 4)\nstraight-line metres: 5.0\naxis-aligned route metres: 7\nreverse displacement: (-3, -4)\ncentral square area: 25\ntriangle area: 6"
  },
  {
    "id": "arcs",
    "title": "Compute an angle from a fraction of a turn",
    "question": "If the radius triples at fixed angle, what happens to arc length and sector area?",
    "code": "import math\n\n\ndef sector(radius, degrees):\n    if not math.isfinite(radius) or not 1e-4 <= radius <= 1e4:\n        raise ValueError(\"Radius must be between 1e-4 and 1e4 length units.\")\n    if not math.isfinite(degrees) or not 0 < degrees <= 360:\n        raise ValueError(\"Use one positive sweep, at most 360 degrees.\")\n    angle = math.radians(degrees)\n    return angle, radius * angle, radius * radius * angle / 2\n\n\nfor radius in [2, 6]:\n    angle, arc, area = sector(radius, 60)\n    print(f\"r={radius}: angle={angle:.6f} rad, arc={arc:.6f}, area={area:.6f}\")\nprint(\"one radian in degrees:\", f\"{math.degrees(1):.6f}\")\n",
    "expected": "r=2: angle=1.047198 rad, arc=2.094395, area=2.094395\nr=6: angle=1.047198 rad, arc=6.283185, area=18.849556\none radian in degrees: 57.295780"
  },
  {
    "id": "similarity",
    "title": "Cancel a common scale from a ratio",
    "question": "Why does the area factor differ from the side-length factor?",
    "code": "import math\nfrom fractions import Fraction\n\nadjacent, opposite = Fraction(4), Fraction(3)\nhypotenuse = Fraction(5)\nfor scale in [Fraction(1, 2), Fraction(1), Fraction(3)]:\n    a, o, h = [scale * length for length in (adjacent, opposite, hypotenuse)]\n    print(\n        f\"scale={scale}: sides=({a}, {o}, {h}), sin={o/h}, cos={a/h}, tan={o/a}, area={a*o/2}\"\n    )\nprint(\n    \"reference angle in degrees:\", f\"{math.degrees(math.atan2(opposite, adjacent)):.6f}\"\n)\nprint(\n    \"complement in degrees:\", f\"{90-math.degrees(math.atan2(opposite, adjacent)):.6f}\"\n)\n",
    "expected": "scale=1/2: sides=(2, 3/2, 5/2), sin=3/5, cos=4/5, tan=3/4, area=3/2\nscale=1: sides=(4, 3, 5), sin=3/5, cos=4/5, tan=3/4, area=6\nscale=3: sides=(12, 9, 15), sin=3/5, cos=4/5, tan=3/4, area=54\nreference angle in degrees: 36.869898\ncomplement in degrees: 53.130102"
  },
  {
    "id": "circle",
    "title": "Keep exact cardinal zeros separate from rounded trigonometry",
    "question": "Why should tangent at 90 degrees be reported as undefined rather than a huge number?",
    "code": "import math\n\n\ndef components(degrees):\n    if not math.isfinite(degrees) or abs(degrees) > 3600:\n        raise ValueError(\"Use a finite angle within ten turns.\")\n    wrapped = degrees % 360\n    if wrapped % 90 == 0:\n        cosine, sine = [(1, 0), (0, 1), (-1, 0), (0, -1)][int(wrapped / 90)]\n    else:\n        angle = math.radians(degrees)\n        cosine, sine = math.cos(angle), math.sin(angle)\n    return cosine, sine, None if cosine == 0 else sine / cosine\n\n\nfor degrees in [30, 90, 150, -90, 270, 360]:\n    c, s, tangent = components(degrees)\n    tangent_text = \"undefined\" if tangent is None else f\"{tangent:.6f}\"\n    print(f\"{degrees:4} deg: cos={c:.6f}, sin={s:.6f}, tan={tangent_text}\")\nprint(\"asin(0.5) principal degrees:\", f\"{math.degrees(math.asin(0.5)):.6f}\")\nprint(\"other angle in one turn:\", f\"{180-math.degrees(math.asin(0.5)):.6f}\")\n",
    "expected": "  30 deg: cos=0.866025, sin=0.500000, tan=0.577350\n  90 deg: cos=0.000000, sin=1.000000, tan=undefined\n 150 deg: cos=-0.866025, sin=0.500000, tan=-0.577350\n -90 deg: cos=0.000000, sin=-1.000000, tan=undefined\n 270 deg: cos=0.000000, sin=-1.000000, tan=undefined\n 360 deg: cos=1.000000, sin=0.000000, tan=0.000000\nasin(0.5) principal degrees: 30.000000\nother angle in one turn: 150.000000"
  },
  {
    "id": "bearing",
    "title": "Use both coordinates and handle the origin",
    "question": "What different points have the same ratio y/x?",
    "code": "import math\n\n\ndef polar(x, y):\n    if any(not math.isfinite(v) or abs(v) > 1e6 for v in (x, y)):\n        raise ValueError(\"Use finite coordinates of magnitude at most 1e6.\")\n    radius = math.hypot(x, y)\n    if radius == 0:\n        return 0.0, None\n    angle = math.atan2(y, x)\n    if angle == -math.pi:\n        angle = math.pi\n    return radius, angle\n\n\nfor point in [(3, 4), (-3, 4), (-3, -4), (0, -4), (-4, -0.0), (0, 0)]:\n    radius, angle = polar(*point)\n    label = \"no direction\" if angle is None else f\"{math.degrees(angle):.6f} deg\"\n    print(f\"{point}: r={radius:.6f}, bearing={label}\")\n    if angle is not None:\n        reconstructed = (radius * math.cos(angle), radius * math.sin(angle))\n        assert math.dist(point, reconstructed) <= 1e-9\n",
    "expected": "(3, 4): r=5.000000, bearing=53.130102 deg\n(-3, 4): r=5.000000, bearing=126.869898 deg\n(-3, -4): r=5.000000, bearing=-126.869898 deg\n(0, -4): r=4.000000, bearing=-90.000000 deg\n(-4, -0.0): r=4.000000, bearing=180.000000 deg\n(0, 0): r=0.000000, bearing=no direction"
  },
  {
    "id": "triangles",
    "title": "Construct both triangles before accepting an inverse",
    "question": "Why are two positive ray-circle intersections two different valid triangles?",
    "code": "import math\n\n\ndef sas_side(b, c, degrees):\n    if any(not math.isfinite(v) or not 1e-4 <= v <= 1e4 for v in (b, c)):\n        raise ValueError(\"Sides must be between 1e-4 and 1e4.\")\n    if not math.isfinite(degrees) or not 1 <= degrees <= 179:\n        raise ValueError(\"Included angle must be from 1 to 179 degrees.\")\n    angle = math.radians(degrees)\n    # Distance from B=(c,0) to C=(b cos A,b sin A).\n    return math.hypot(c - b * math.cos(angle), b * math.sin(angle))\n\n\ndef ssa_thirty(a, b):\n    # Scoped A=30-degree constructor; a and b are integer lengths.\n    if any(type(v) is not int or not 1 <= v <= 1000 for v in (a, b)):\n        raise ValueError(\"Use integer side lengths from 1 to 1000.\")\n    cx, cy = b * math.sqrt(3) / 2, b / 2\n    discriminant = a * a - cy * cy\n    if discriminant < 0:\n        return []\n    offsets = (\n        [0.0]\n        if discriminant == 0\n        else [-math.sqrt(discriminant), math.sqrt(discriminant)]\n    )\n    candidates = []\n    for offset in offsets:\n        c = cx + offset\n        # c=0 is a collapsed triangle, not an additional solution.\n        if a == b and offset < 0:\n            continue\n        if c <= 0:\n            continue\n        angle_b = math.atan2(cy, c - cx)\n        angle_c = math.pi - math.pi / 6 - angle_b\n        candidates.append((c, math.degrees(angle_b), math.degrees(angle_c)))\n    return candidates\n\n\nprint(\"SAS b=5,c=7,A=60 -> a:\", f\"{sas_side(5,7,60):.6f}\")\nfor a, b in [(7, 10), (5, 10), (4, 10), (10, 10), (12, 10)]:\n    triangles = ssa_thirty(a, b)\n    print(f\"SSA A=30,a={a},b={b}: {len(triangles)} triangle(s)\")\n    for c, angle_b, angle_c in triangles:\n        print(f\"  c={c:.6f}, B={angle_b:.6f}, C={angle_c:.6f}\")\n",
    "expected": "SAS b=5,c=7,A=60 -> a: 6.244998\nSSA A=30,a=7,b=10: 2 triangle(s)\n  c=3.761275, B=134.415309, C=15.584691\n  c=13.559234, B=45.584691, C=104.415309\nSSA A=30,a=5,b=10: 1 triangle(s)\n  c=8.660254, B=90.000000, C=60.000000\nSSA A=30,a=4,b=10: 0 triangle(s)\nSSA A=30,a=10,b=10: 1 triangle(s)\n  c=17.320508, B=30.000000, C=120.000000\nSSA A=30,a=12,b=10: 1 triangle(s)\n  c=19.568966, B=24.624318, C=125.375682"
  },
  {
    "id": "frames",
    "title": "Reconstruct a world point from local coordinates",
    "question": "Why does subtracting the new origin come before the inverse rotation?",
    "code": "import math\n\n\ndef rotate(point, degrees):\n    if len(point) != 2 or any(not math.isfinite(v) or abs(v) > 1e6 for v in point):\n        raise ValueError(\"Use two finite coordinates of magnitude at most 1e6.\")\n    if not math.isfinite(degrees) or abs(degrees) > 3600:\n        raise ValueError(\"Angle must be finite and within ten turns.\")\n    wrapped = degrees % 360\n    if wrapped % 90 == 0:\n        c, s = [(1, 0), (0, 1), (-1, 0), (0, -1)][int(wrapped / 90)]\n    else:\n        c, s = math.cos(math.radians(degrees)), math.sin(math.radians(degrees))\n    x, y = point\n    return c * x - s * y, s * x + c * y\n\n\ndef local_coordinates(point, origin, degrees):\n    rotate(point, 0)\n    rotate(origin, 0)\n    return rotate((point[0] - origin[0], point[1] - origin[1]), -degrees)\n\n\ndef world_coordinates(local, origin, degrees):\n    rotate(origin, 0)\n    rotated = rotate(local, degrees)\n    return origin[0] + rotated[0], origin[1] + rotated[1]\n\n\npoint, origin, degrees = (4, 2), (1, -1), 30\nlocal = local_coordinates(point, origin, degrees)\nreconstructed = world_coordinates(local, origin, degrees)\nactive = world_coordinates(\n    (point[0] - origin[0], point[1] - origin[1]), origin, degrees\n)\nfor label, pair in [\n    (\"local\", local),\n    (\"reconstructed world\", reconstructed),\n    (\"actively rotated world\", active),\n]:\n    print(label + \":\", tuple(round(v, 6) for v in pair))\nprint(\"reconstruction error:\", f\"{math.dist(point,reconstructed):.2e}\")\nprint(\"90-degree displacement rotation:\", rotate((3, 4), 90))\n",
    "expected": "local: (4.098076, 1.098076)\nreconstructed world: (4.0, 2.0)\nactively rotated world: (2.098076, 3.098076)\nreconstruction error: 0.00e+00\n90-degree displacement rotation: (-4, 3)"
  },
  {
    "id": "screen",
    "title": "Undo each screen scale before measuring",
    "question": "Why cannot you divide the pixel diagonal by one pixels-per-metre number here?",
    "code": "import math\n\n\ndef validate_map(point, origin, scales):\n    if any(len(pair) != 2 for pair in (point, origin, scales)):\n        raise ValueError(\"Use coordinate and scale pairs.\")\n    if any(not math.isfinite(v) or abs(v) > 1e8 for v in (*point, *origin)):\n        raise ValueError(\"Use finite coordinates of magnitude at most 1e8.\")\n    if any(not math.isfinite(v) or not 1e-6 <= v <= 1e6 for v in scales):\n        raise ValueError(\"Each scale must be between 1e-6 and 1e6.\")\n\n\ndef screen(point, origin=(100, 200), scales=(20, 10)):\n    validate_map(point, origin, scales)\n    x, y = point\n    u0, v0 = origin\n    sx, sy = scales\n    return u0 + sx * x, v0 - sy * y\n\n\ndef world(pixel, origin=(100, 200), scales=(20, 10)):\n    validate_map(pixel, origin, scales)\n    u, v = pixel\n    u0, v0 = origin\n    sx, sy = scales\n    return (u - u0) / sx, -(v - v0) / sy\n\n\np, q = (1, 2), (4, 6)\nsp, sq = screen(p), screen(q)\nprint(\"screen P,Q:\", sp, sq)\nprint(\"pixel displacement:\", (sq[0] - sp[0], sq[1] - sp[1]))\nprint(\"pixel diagonal:\", f\"{math.dist(sp,sq):.6f}\")\nprint(\"recovered metres:\", math.dist(world(sp), world(sq)))\n",
    "expected": "screen P,Q: (120, 180) (180, 140)\npixel displacement: (60, -40)\npixel diagonal: 72.111026\nrecovered metres: 5.0"
  },
  {
    "id": "links",
    "title": "Add relative angles before adding link displacements",
    "question": "What endpoint results if the shoulder changes to -30 degrees but the relative elbow remains 60 degrees?",
    "code": "import math\n\n\ndef endpoint(first_length, second_length, shoulder, relative_elbow):\n    values = (first_length, second_length, shoulder, relative_elbow)\n    if any(not math.isfinite(v) for v in values):\n        raise ValueError(\"All inputs must be finite.\")\n    if not (0 < first_length <= 1e4 and 0 < second_length <= 1e4):\n        raise ValueError(\"Link lengths must be positive and at most 1e4.\")\n    if abs(shoulder) > 3600 or abs(relative_elbow) > 3600:\n        raise ValueError(\"Each angle must be within ten turns.\")\n    first = math.radians(shoulder)\n    second = math.radians(shoulder + relative_elbow)\n    return (\n        first_length * math.cos(first) + second_length * math.cos(second),\n        first_length * math.sin(first) + second_length * math.sin(second),\n    )\n\n\nfor shoulder, elbow in [(30, 60), (-30, 60), (30, 0), (30, 180)]:\n    point = endpoint(2, 1, shoulder, elbow)\n    print(\n        f\"shoulder={shoulder}, relative elbow={elbow}: ({point[0]:.6f}, {point[1]:.6f}), reach={math.hypot(*point):.6f}\"\n    )\n",
    "expected": "shoulder=30, relative elbow=60: (1.732051, 2.000000), reach=2.645751\nshoulder=-30, relative elbow=60: (2.598076, -0.500000), reach=2.645751\nshoulder=30, relative elbow=0: (2.598076, 1.500000), reach=3.000000\nshoulder=30, relative elbow=180: (0.866025, 0.500000), reach=1.000000"
  }
];
