// Complete runnable programs. Outputs are captured from the native executions recorded in verification.
export const combinatorialOptimizationExamples = {
  "originalCover": {
    "title": "The original greedy counterexample",
    "question": "Which two candidate sets cover the universe, and why does the greedy rule select three?",
    "code": "from itertools import combinations\n\nuniverse = {1, 2, 3, 4, 5, 6}\ncandidates = {\n    \"A\": {1, 2, 3, 4},\n    \"B\": {1, 2, 5},\n    \"C\": {3, 4, 6},\n}\n\ncovered, greedy = set(), []\nwhile covered != universe:\n    name, new_items = max(candidates.items(), key=lambda item: len(item[1] - covered))\n    greedy.append(name)\n    covered |= new_items\n\noptimal = next(\n    combo for size in range(1, len(candidates) + 1)\n    for combo in combinations(candidates, size)\n    if set().union(*(candidates[name] for name in combo)) == universe\n)\nprint(greedy)\nprint(list(optimal))",
    "expected": "['A', 'B', 'C']\n['B', 'C']",
    "interpretation": "Equal-size ties follow dictionary insertion order. This original demonstration assumes its fixed coverable universe; the later weighted program detects impossible coverage."
  },
  "matroidForest": {
    "title": "Greedy is exact when the exchange property holds",
    "question": "Why is AC rejected after AB and BC, even though AC has greater weight than CD?",
    "code": "from itertools import combinations\n\n\ndef is_forest(vertex_count, edges):\n    parent = list(range(vertex_count))\n\n    def root(vertex):\n        while parent[vertex] != vertex:\n            vertex = parent[vertex]\n        return vertex\n\n    for a, b, _ in edges:\n        first, second = root(a), root(b)\n        if first == second:\n            return False\n        parent[first] = second\n    return True\n\n\ndef maximum_weight_forest(vertex_count, edges):\n    # Unrestricted cardinality: skip nonpositive weights.\n    chosen = []\n    for edge in sorted(edges, key=lambda edge: -edge[2]):\n        if edge[2] > 0 and is_forest(vertex_count, chosen + [edge]):\n            chosen.append(edge)\n    return chosen\n\n\nedges = [(0, 1, 6), (1, 2, 5), (0, 2, 4), (2, 3, 3), (1, 3, 2), (0, 3, 1)]\nchosen = maximum_weight_forest(4, edges)\nexact = max(\n    sum(edge[2] for edge in subset)\n    for size in range(len(edges) + 1)\n    for subset in combinations(edges, size)\n    if is_forest(4, subset)\n)\nprint(\"chosen\", chosen)\nprint(\"greedy value\", sum(edge[2] for edge in chosen), \"small exact oracle\", exact)\nprint(\"negative edge, unrestricted:\", maximum_weight_forest(2, [(0, 1, -3)]))\nprint(\"the only spanning tree instead has mandatory cost -3\")\n",
    "expected": "chosen [(0, 1, 6), (1, 2, 5), (2, 3, 3)]\ngreedy value 14 small exact oracle 14\nnegative edge, unrestricted: []\nthe only spanning tree instead has mandatory cost -3",
    "interpretation": "The tiny combinations oracle checks the forest result; it is deliberately separate from the greedy algorithm."
  },
  "boundedKnapsack": {
    "title": "Stop an exact search with an honest bound",
    "question": "When the expansion limit is four, can the returned value be called optimal? Read the remaining frontier bound.",
    "code": "from fractions import Fraction\nfrom heapq import heappop, heappush\nfrom itertools import count\n\n\ndef bounded_knapsack(items, capacity, expansion_limit):\n    if (\n        type(capacity) is not int\n        or capacity < 0\n        or type(expansion_limit) is not int\n        or expansion_limit < 0\n    ):\n        raise ValueError(\"Capacity and expansion limit must be nonnegative integers.\")\n    if any(\n        type(weight) is not int or weight <= 0 or type(value) is not int or value < 0\n        for weight, value in items\n    ):\n        raise ValueError(\"Use positive integer weights and nonnegative integer values.\")\n    order = sorted(\n        [(weight, value, index) for index, (weight, value) in enumerate(items)],\n        key=lambda item: -Fraction(item[1], item[0]),\n    )\n\n    def optimistic(depth, weight, value):\n        free, upper = capacity - weight, Fraction(value)\n        for next_weight, next_value, _ in order[depth:]:\n            used = min(free, next_weight)\n            upper += Fraction(used * next_value, next_weight)\n            free -= used\n            if free == 0:\n                break\n        return upper\n\n    serial = count()\n    frontier = [(-optimistic(0, 0, 0), next(serial), 0, 0, 0, ())]\n    best_value, best_items, expanded = 0, (), 0\n    while frontier and expanded < expansion_limit:\n        negative_bound, _, depth, weight, value, chosen = heappop(frontier)\n        expanded += 1\n        if -negative_bound <= best_value:\n            continue\n        if value > best_value:\n            best_value, best_items = value, chosen\n        if depth == len(order):\n            continue\n        item_weight, item_value, original = order[depth]\n        branches = [(weight, value, chosen)]\n        if weight + item_weight <= capacity:\n            branches.append(\n                (weight + item_weight, value + item_value, chosen + (original,))\n            )\n        for next_weight, next_value, next_chosen in branches:\n            if next_value > best_value:\n                best_value, best_items = next_value, next_chosen\n            upper = optimistic(depth + 1, next_weight, next_value)\n            heappush(\n                frontier,\n                (-upper, next(serial), depth + 1, next_weight, next_value, next_chosen),\n            )\n    upper = max([Fraction(best_value)] + [-node[0] for node in frontier])\n    return {\n        \"items\": sorted(best_items),\n        \"value\": best_value,\n        \"upper\": upper,\n        \"proved\": upper == best_value,\n        \"expanded\": expanded,\n    }\n\n\nitems = [(2, 14), (3, 18), (4, 22), (5, 25), (7, 31)]\nfor limit in (0, 1, 4, 100):\n    result = bounded_knapsack(items, 10, limit)\n    print(\n        \"limit\",\n        limit,\n        \"chosen\",\n        result[\"items\"],\n        \"value\",\n        result[\"value\"],\n        \"upper\",\n        str(result[\"upper\"]),\n        \"optimality proved\",\n        result[\"proved\"],\n    )\n",
    "expected": "limit 0 chosen [] value 0 upper 59 optimality proved False\nlimit 1 chosen [0] value 14 upper 59 optimality proved False\nlimit 4 chosen [0, 1, 2] value 54 upper 409/7 optimality proved False\nlimit 100 chosen [0, 1, 3] value 57 upper 57 optimality proved True",
    "interpretation": "A stopped search returns a legal subset and a quality interval. The limit counts heap removals, including pruned nodes; the browser labels its own expansion snapshots."
  },
  "assignment": {
    "title": "An assignment solver that refunds an earlier choice",
    "question": "Find the negative cost in the final augmenting path. Which earlier assignment does it undo?",
    "code": "from itertools import combinations, permutations\n\n\ndef min_cost_assignment(costs, required):\n    \"\"\"Integer costs or None for missing edges; return optimum at each size.\"\"\"\n    workers = len(costs)\n    jobs = len(costs[0]) if workers else 0\n    if not workers or not jobs or any(len(row) != jobs for row in costs):\n        raise ValueError(\"Use a nonempty rectangular cost matrix.\")\n    if type(required) is not int or not 0 <= required <= min(workers, jobs):\n        raise ValueError(\"Requested size is outside the matrix.\")\n    if any(\n        value is not None and type(value) is not int for row in costs for value in row\n    ):\n        raise ValueError(\"Costs must be integers or None.\")\n    source, sink = workers + jobs, workers + jobs + 1\n    graph = [[] for _ in range(sink + 1)]\n\n    def add_edge(start, end, cost):\n        forward = [end, len(graph[end]), 1, cost]\n        reverse = [start, len(graph[start]), 0, -cost]\n        graph[start].append(forward)\n        graph[end].append(reverse)\n        return forward\n\n    for worker in range(workers):\n        add_edge(source, worker, 0)\n    assignment_edges = {}\n    for worker, row in enumerate(costs):\n        for job, cost in enumerate(row):\n            if cost is not None:\n                assignment_edges[worker, job] = add_edge(worker, workers + job, cost)\n    for job in range(jobs):\n        add_edge(workers + job, sink, 0)\n\n    def current_matching():\n        return [pair for pair, edge in assignment_edges.items() if edge[2] == 0]\n\n    trace = [(0, 0, [], [])]\n    total = 0\n    for size in range(1, required + 1):\n        distance = [float(\"inf\")] * len(graph)\n        previous = [None] * len(graph)\n        distance[source] = 0\n        for _ in range(len(graph) - 1):\n            changed = False\n            for start, edges in enumerate(graph):\n                for index, (end, _, capacity, cost) in enumerate(edges):\n                    if capacity and distance[start] + cost < distance[end]:\n                        distance[end] = distance[start] + cost\n                        previous[end] = (start, index)\n                        changed = True\n            if not changed:\n                break\n        if distance[sink] == float(\"inf\"):\n            return trace, False\n        # The initial network is acyclic. Each shortest augmentation preserves\n        # minimum cost at its size, so residual negative cycles cannot arise.\n        assert all(\n            not capacity or distance[start] + cost >= distance[end]\n            for start, edges in enumerate(graph)\n            for end, _, capacity, cost in edges\n        )\n        path_costs = []\n        end = sink\n        while end != source:\n            start, index = previous[end]\n            edge = graph[start][index]\n            path_costs.append(edge[3])\n            edge[2] -= 1\n            graph[end][edge[1]][2] += 1\n            end = start\n        total += distance[sink]\n        matching = current_matching()\n        assert len({worker for worker, _ in matching}) == size\n        assert len({job for _, job in matching}) == size\n        assert sum(costs[worker][job] for worker, job in matching) == total\n        trace.append((size, total, matching, list(reversed(path_costs))))\n    return trace, True\n\n\ndef tiny_optimum(costs, size):\n    candidates = []\n    for workers in combinations(range(len(costs)), size):\n        for jobs in permutations(range(len(costs[0])), size):\n            pairs = list(zip(workers, jobs))\n            if all(costs[worker][job] is not None for worker, job in pairs):\n                candidates.append(sum(costs[worker][job] for worker, job in pairs))\n    return min(candidates, default=None)\n\n\nif __name__ == \"__main__\":\n    costs = [[1, 2, 8], [2, 9, 8], [8, 8, 1]]\n    trace, complete = min_cost_assignment(costs, 3)\n    for size, total, pairs, path in trace:\n        assert total == tiny_optimum(costs, size)\n        print(f\"size={size} cost={total} pairs={pairs} path_costs={path}\")\n    print(\"requested size reached:\", complete)\n    print(\"missing-edge request:\", min_cost_assignment([[None, 2], [None, 1]], 2)[1])\n",
    "expected": "size=0 cost=0 pairs=[] path_costs=[]\nsize=1 cost=1 pairs=[(0, 0)] path_costs=[0, 1, 0]\nsize=2 cost=2 pairs=[(0, 0), (2, 2)] path_costs=[0, 1, 0]\nsize=3 cost=5 pairs=[(0, 1), (1, 0), (2, 2)] path_costs=[0, 2, -1, 2, 0]\nrequested size reached: True\nmissing-edge request: False",
    "interpretation": "The last size-three path costs 3: it adds costs 2 and 2 while refunding 1. Costs at successive cardinalities are independently checked by a tiny enumeration."
  },
  "weightedCover": {
    "title": "Pay for newly covered requirements",
    "question": "How do costs 3, 2, 2 change the first choice from the original equal-cost instance?",
    "code": "from fractions import Fraction\nfrom itertools import combinations\n\n\ndef greedy_cover(universe, candidates):\n    universe = set(universe)\n    if any(type(cost) is not int or cost < 0 for _, elements, cost in candidates):\n        raise ValueError(\"Nonnegative integer set costs are required.\")\n    uncovered, chosen, charges = set(universe), [], {}\n    while uncovered:\n        choices = [\n            (Fraction(cost, len(new)), index, new)\n            for index, (_, elements, cost) in enumerate(candidates)\n            if (new := set(elements) & uncovered)\n        ]\n        if not choices:\n            raise ValueError(\"A required element has no covering set.\")\n        ratio, index, new = min(choices, key=lambda row: (row[0], row[1]))\n        chosen.append(index)\n        for element in new:\n            charges[element] = ratio\n        uncovered -= new\n    return chosen, charges\n\n\nuniverse = set(range(1, 7))\ncandidates = [(\"A\", {1, 2, 3, 4}, 3), (\"B\", {1, 2, 5}, 2), (\"C\", {3, 4, 6}, 2)]\nchosen, charges = greedy_cover(universe, candidates)\ncost = sum(candidates[i][2] for i in chosen)\nexact = min(\n    sum(candidates[i][2] for i in selected)\n    for size in range(len(candidates) + 1)\n    for selected in combinations(range(len(candidates)), size)\n    if set().union(*(candidates[i][1] for i in selected)) == universe\n)\nprint(\"chosen\", [candidates[i][0] for i in chosen], \"cost\", cost, \"optimum\", exact)\nprint(\"element charges\", {e: str(charges[e]) for e in sorted(universe)})\nfor name, elements, set_cost in candidates:\n    total = sum(charges[e] for e in elements)\n    harmonic = sum(Fraction(1, k) for k in range(1, len(elements) + 1))\n    print(name, \"charge sum\", str(total), \"<=\", str(set_cost * harmonic))\nassert sum(charges.values()) == cost\n",
    "expected": "chosen ['B', 'C'] cost 4 optimum 4\nelement charges {1: '2/3', 2: '2/3', 3: '2/3', 4: '2/3', 5: '2/3', 6: '2/3'}\nA charge sum 8/3 <= 25/4\nB charge sum 2 <= 11/3\nC charge sum 2 <= 11/3",
    "interpretation": "Exact rational charges account for the entire greedy cost. The candidate-set inequalities are the ingredients of the harmonic proof."
  },
  "primalDualCover": {
    "title": "Grow a lower bound until a vertex becomes tight",
    "question": "How can edge loads certify a lower bound even when their sum is not the optimum LP value?",
    "code": "def primal_dual_cover(costs, edges):\n    if any(type(cost) is not int or cost < 0 for cost in costs):\n        raise ValueError(\"Vertex costs must be nonnegative integers.\")\n    if any(\n        len(edge) != 2\n        or edge[0] == edge[1]\n        or any(\n            type(vertex) is not int or not 0 <= vertex < len(costs) for vertex in edge\n        )\n        for edge in edges\n    ):\n        raise ValueError(\"Use valid, distinct endpoint indices.\")\n    loads = [0] * len(edges)\n    used = [0] * len(costs)\n    chosen = {vertex for vertex, cost in enumerate(costs) if cost == 0}\n    while True:\n        edge = next(\n            (\n                i\n                for i, (a, b) in enumerate(edges)\n                if a not in chosen and b not in chosen\n            ),\n            None,\n        )\n        if edge is None:\n            break\n        a, b = edges[edge]\n        increase = min(costs[a] - used[a], costs[b] - used[b])\n        loads[edge] += increase\n        used[a] += increase\n        used[b] += increase\n        for vertex in (a, b):\n            if used[vertex] == costs[vertex]:\n                chosen.add(vertex)\n    assert all(a in chosen or b in chosen for a, b in edges)\n    assert all(used[v] <= costs[v] for v in range(len(costs)))\n    return chosen, loads\n\n\ncosts, edges = [2, 3, 4], [(0, 1), (1, 2), (0, 2)]\nchosen, loads = primal_dual_cover(costs, edges)\nactual_cost, lower = sum(costs[v] for v in chosen), sum(loads)\nprint(\"chosen vertices\", sorted(chosen), \"cost\", actual_cost)\nprint(\"dual edge loads\", loads, \"lower bound\", lower)\nprint(\"certificate:\", lower, \"<= OPT <=\", actual_cost, \"<=\", 2 * lower)\nzero_chosen, zero_loads = primal_dual_cover([0, 5], [(0, 1)])\nprint(\"zero-cost case\", sorted(zero_chosen), zero_loads)\n",
    "expected": "chosen vertices [0, 1] cost 5\ndual edge loads [2, 1, 0] lower bound 3\ncertificate: 3 <= OPT <= 5 <= 6\nzero-cost case [0] [0]",
    "interpretation": "The printed edge loads obey every vertex budget. The final selected vertices cover all edges and cost at most twice that lower bound."
  },
  "fractionalCover": {
    "title": "Solve a relaxation and round a feasible cover",
    "question": "Why does rounding all three values .5 give a legal but more expensive cover than the exact optimum?",
    "code": "import numpy as np\nfrom scipy.optimize import linprog\n\n\ndef round_cover(costs, edges):\n    if not costs or any(type(cost) is not int or cost < 0 for cost in costs):\n        raise ValueError(\"Use at least one nonnegative integer vertex cost.\")\n    if any(\n        len(edge) != 2\n        or edge[0] == edge[1]\n        or any(\n            type(vertex) is not int or not 0 <= vertex < len(costs) for vertex in edge\n        )\n        for edge in edges\n    ):\n        raise ValueError(\"Use valid, distinct endpoint indices.\")\n    # linprog uses A_ub x <= b_ub. Negate the cover inequalities.\n    matrix = np.zeros((len(edges), len(costs)))\n    for row, (a, b) in zip(matrix, edges):\n        row[a] = row[b] = -1\n    result = linprog(\n        costs,\n        A_ub=matrix if edges else None,\n        b_ub=-np.ones(len(edges)) if edges else None,\n        bounds=[(0, 1)] * len(costs),\n        method=\"highs\",\n    )\n    if not result.success:\n        raise RuntimeError(result.message)\n    # A tolerance near the threshold may include an extra endpoint.\n    # Check feasibility explicitly; floating solver output is not an exact proof.\n    chosen = [v for v, x in enumerate(result.x) if x >= 0.5 - 1e-9]\n    if not all(a in chosen or b in chosen for a, b in edges):\n        raise ArithmeticError(\"Numerical rounding did not produce a legal cover.\")\n    return result.x, result.fun, chosen\n\n\ncosts, edges = [2, 3, 4], [(0, 1), (1, 2), (0, 2)]\nx, lower, chosen = round_cover(costs, edges)\nprint(\"fractional\", np.round(x, 6).tolist(), \"LP value\", round(lower, 6))\nprint(\"rounded vertices\", chosen, \"cost\", sum(costs[v] for v in chosen))\nprint(\"tiny exact optimum 5; the rounding theorem gives an upper factor, not equality\")\n",
    "expected": "fractional [0.5, 0.5, 0.5] LP value 4.5\nrounded vertices [0, 1, 2] cost 9\ntiny exact optimum 5; the rounding theorem gives an upper factor, not equality",
    "interpretation": "The solver uses continuous variables. This numerical run checks cover feasibility; its floating objective is numerical evidence, whereas the text proves the exact LP inequality."
  },
  "maximumCoverage": {
    "title": "Select two tests instead of covering every behavior",
    "question": "Why does the new objective stop after two sets even though one requirement is still uncovered?",
    "code": "from itertools import combinations\nfrom fractions import Fraction\n\n\ndef maximum_coverage(sets, budget):\n    if type(budget) is not int or not 0 <= budget <= len(sets):\n        raise ValueError(\"Budget must be an integer between zero and the set count.\")\n    chosen, covered, trace = [], set(), []\n    for _ in range(budget):\n        candidate = max(\n            (i for i in range(len(sets)) if i not in chosen),\n            key=lambda i: (len(sets[i] - covered), -i),\n        )\n        gained = sets[candidate] - covered\n        chosen.append(candidate)\n        covered |= sets[candidate]\n        trace.append((candidate, sorted(gained), len(covered)))\n    return chosen, covered, trace\n\n\nif __name__ == \"__main__\":\n    sets = [{0, 1, 2, 3}, {0, 1, 4}, {2, 3, 5}]\n    budget = 2\n    chosen, covered, trace = maximum_coverage(sets, budget)\n    optimum = max(\n        len(set().union(*(sets[i] for i in subset)))\n        for subset in combinations(range(len(sets)), budget)\n    )\n    factor = 1 - (1 - Fraction(1, budget)) ** budget\n    for candidate, gained, total in trace:\n        print(f\"choose {chr(65 + candidate)}: new={gained}, covered={total}\")\n    print(f\"greedy={len(covered)}, optimum={optimum}, guaranteed fraction={factor}\")\n    assert len(covered) >= factor * optimum\n",
    "expected": "choose A: new=[0, 1, 2, 3], covered=4\nchoose B: new=[4], covered=5\ngreedy=5, optimum=6, guaranteed fraction=3/4",
    "interpretation": "The greedy choice covers five behaviors; the two-set optimum covers six. The proven k=2 fraction is 3/4, not an assertion that every run achieves equality."
  },
  "scaledKnapsack": {
    "title": "Trade a proved value loss for a smaller integer DP",
    "question": "Why must the impossible item of value 100000 be removed before setting the rounding scale?",
    "code": "from fractions import Fraction\nfrom itertools import combinations\n\n\ndef scaled_knapsack(items, capacity, epsilon):\n    \"\"\"items contain integer (weight, value); epsilon is an exact Fraction.\"\"\"\n    if type(capacity) is not int or capacity < 0:\n        raise ValueError(\"Capacity must be a nonnegative integer.\")\n    if not isinstance(epsilon, Fraction) or not 0 < epsilon < 1:\n        raise ValueError(\"Use Fraction(numerator, denominator) between 0 and 1.\")\n    if any(\n        type(weight) is not int or weight <= 0 or type(value) is not int or value < 0\n        for weight, value in items\n    ):\n        raise ValueError(\"Use positive integer weights and nonnegative values.\")\n    eligible = [\n        (index, weight, value)\n        for index, (weight, value) in enumerate(items)\n        if weight <= capacity and value > 0\n    ]\n    if not eligible:\n        return [], 0, 0, 0\n    count = len(eligible)\n    scale = epsilon * max(value for _, _, value in eligible) / count\n    rounded = [int(Fraction(value) // scale) for _, _, value in eligible]\n    width = sum(rounded) + 1\n    rows = [[float(\"inf\")] * width for _ in range(count + 1)]\n    rows[0][0] = 0\n    for row, ((_, weight, _), score) in enumerate(zip(eligible, rounded), 1):\n        for target in range(width):\n            rows[row][target] = rows[row - 1][target]\n            if target >= score:\n                rows[row][target] = min(\n                    rows[row][target], rows[row - 1][target - score] + weight\n                )\n    target = max(score for score, weight in enumerate(rows[-1]) if weight <= capacity)\n    chosen = []\n    for row in range(count, 0, -1):\n        if rows[row][target] < rows[row - 1][target]:\n            chosen.append(eligible[row - 1][0])\n            target -= rounded[row - 1]\n    chosen.sort()\n    weight = sum(items[index][0] for index in chosen)\n    value = sum(items[index][1] for index in chosen)\n    assert weight <= capacity and len(chosen) == len(set(chosen))\n    return chosen, weight, value, count * width\n\n\ndef tiny_optimum(items, capacity):\n    return max(\n        sum(items[i][1] for i in subset)\n        for size in range(len(items) + 1)\n        for subset in combinations(range(len(items)), size)\n        if sum(items[i][0] for i in subset) <= capacity\n    )\n\n\nif __name__ == \"__main__\":\n    items = [(2, 14), (3, 18), (4, 22), (5, 25), (7, 31), (99, 100000)]\n    optimum = tiny_optimum(items, 10)\n    for denominator in (2, 4, 10):\n        epsilon = Fraction(1, denominator)\n        chosen, weight, value, cells = scaled_knapsack(items, 10, epsilon)\n        assert value >= (1 - epsilon) * optimum\n        print(\n            f\"epsilon={epsilon}: items={chosen} weight={weight} value={value} cells={cells}\"\n        )\n    print(\"exact tiny optimum:\", optimum)\n    print(\"zero capacity:\", scaled_knapsack(items, 0, Fraction(1, 4)))\n",
    "expected": "epsilon=1/2: items=[0, 1, 3] weight=10 value=57 cells=175\nepsilon=1/4: items=[0, 1, 3] weight=10 value=57 cells=355\nepsilon=1/10: items=[0, 1, 3] weight=10 value=57 cells=885\nexact tiny optimum: 57\nzero capacity: ([], 0, 0, 0)",
    "interpretation": "The cell count is the actual number of value-state updates, not a wall-clock benchmark. True weights remain unchanged; selected values use the original values."
  },
  "metricRoutes": {
    "title": "Repair parity before shortcutting a metric walk",
    "question": "Why does every odd-degree tree vertex need one more incident edge before an Euler circuit exists?",
    "code": "from itertools import permutations\n\n\ndef metric_routes(points):\n    \"\"\"Manhattan distances: complete, symmetric, nonnegative, triangle inequality.\"\"\"\n    size = len(points)\n    if size < 2:\n        raise ValueError(\"Use at least two points.\")\n    distance = [[abs(a[0] - b[0]) + abs(a[1] - b[1]) for b in points] for a in points]\n    reached, tree = {0}, []\n    while len(reached) < size:\n        _, start, end = min(\n            (distance[a][b], a, b)\n            for a in reached\n            for b in range(size)\n            if b not in reached\n        )\n        tree.append((start, end))\n        reached.add(end)\n    degrees = [sum(vertex in edge for edge in tree) for vertex in range(size)]\n    odd = tuple(vertex for vertex, degree in enumerate(degrees) if degree % 2)\n\n    def tiny_perfect_matching(vertices):\n        # Exponential oracle for this tiny example, NOT a polynomial blossom solver.\n        if not vertices:\n            return 0, []\n        first = vertices[0]\n        candidates = []\n        for partner in vertices[1:]:\n            rest = tuple(v for v in vertices if v not in (first, partner))\n            cost, edges = tiny_perfect_matching(rest)\n            candidates.append(\n                (cost + distance[first][partner], [(first, partner)] + edges)\n            )\n        return min(candidates)\n\n    def euler_shortcut(edges):\n        adjacency = [[] for _ in points]\n        for identifier, (a, b) in enumerate(edges):\n            adjacency[a].append((b, identifier))\n            adjacency[b].append((a, identifier))\n        used, stack, circuit = set(), [0], []\n        while stack:\n            vertex = stack[-1]\n            while adjacency[vertex] and adjacency[vertex][-1][1] in used:\n                adjacency[vertex].pop()\n            if adjacency[vertex]:\n                other, identifier = adjacency[vertex].pop()\n                used.add(identifier)\n                stack.append(other)\n            else:\n                circuit.append(stack.pop())\n        circuit.reverse()\n        assert len(used) == len(edges)\n        tour = list(dict.fromkeys(circuit)) + [0]\n        assert len(tour) == size + 1 and set(tour[:-1]) == set(range(size))\n        cost = sum(distance[a][b] for a, b in zip(tour, tour[1:]))\n        return tour, cost\n\n    tree_cost = sum(distance[a][b] for a, b in tree)\n    match_cost, matching = tiny_perfect_matching(odd)\n    doubled = euler_shortcut(tree + tree)\n    repaired = euler_shortcut(tree + matching)\n    optimum = min(\n        sum(distance[a][b] for a, b in zip(tour, tour[1:]))\n        for order in permutations(range(1, size))\n        for tour in [(0,) + order + (0,)]\n    )\n    assert doubled[1] <= 2 * optimum\n    assert 2 * repaired[1] <= 3 * optimum\n    return tree, tree_cost, odd, matching, match_cost, doubled, repaired, optimum\n\n\nif __name__ == \"__main__\":\n    points = [(0, 0), (2, 0), (2, 2), (0, 2), (1, 1)]\n    tree, cost, odd, matching, match_cost, doubled, repaired, optimum = metric_routes(\n        points\n    )\n    print(\"tree:\", tree, \"cost:\", cost)\n    print(\"odd vertices:\", odd, \"matching:\", matching, \"cost:\", match_cost)\n    print(\"doubled-tree tour and cost:\", doubled)\n    print(\"odd-repaired tour and cost:\", repaired)\n    print(\"exact tiny tour optimum:\", optimum)\n",
    "expected": "tree: [(0, 1), (0, 3), (0, 4), (1, 2)] cost: 8\nodd vertices: (0, 2, 3, 4) matching: [(0, 3), (2, 4)] cost: 4\ndoubled-tree tour and cost: ([0, 4, 3, 1, 2, 0], 14)\nodd-repaired tour and cost: ([0, 3, 4, 2, 1, 0], 10)\nexact tiny tour optimum: 10",
    "interpretation": "Both reported tours visit every vertex and return to the start. Exact tour and matching enumeration are tiny verification oracles, not scalable implementations."
  }
};
