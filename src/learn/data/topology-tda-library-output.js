// Recorded from the canonical program; see the source-bound native receipt.
export default "Square, triangles included: H1 [(2.0, 2.828427)]\nCutoff 2.1: one H1 bar still alive; graph-only cutoff 3: three H1 bars still alive\nFull interval multisets match for square, cutoff, graph, rectangle, duplicates and singleton; invalid data rejected";
