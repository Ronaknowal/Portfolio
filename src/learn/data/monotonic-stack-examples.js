// Complete executed Python programs. Generated by scripts/generate-linked-traversal-extension.py.
export const monotonicStackExamples = {
  "greater": {
    "filename": "future_distances.py",
    "question": "Which earlier indices does value 7 settle in [6,6,4,7,5,8]? Do equal values answer a strictly-greater query?",
    "code": "def next_distances(values, inclusive=False):\n    if type(inclusive) is not bool or any(type(value) is not int for value in values):\n        raise ValueError(\"use integer readings and a boolean comparison policy\")\n    answer = [0] * len(values)\n    stack = []\n    pushes = pops = 0\n    for current, value in enumerate(values):\n        while stack and (\n            value >= values[stack[-1]] if inclusive else value > values[stack[-1]]\n        ):\n            previous = stack.pop()\n            pops += 1\n            answer[previous] = current - previous\n        stack.append(current)\n        pushes += 1\n    return answer, pushes, pops\n\n\nfor readings in [[6, 6, 4, 7, 5, 8], [5, 5, 6], [9, 8, 7, 6, 10], []]:\n    answer, pushes, pops = next_distances(readings)\n    print(\"readings:\", readings)\n    print(\"strict distances:\", answer, \"pushes / pops:\", pushes, pops)\nprint(\"greater-or-equal distances:\", next_distances([5, 5, 6], inclusive=True)[0])\n",
    "output": "readings: [6, 6, 4, 7, 5, 8]\nstrict distances: [3, 2, 1, 2, 1, 0] pushes / pops: 6 5\nreadings: [5, 5, 6]\nstrict distances: [2, 1, 0] pushes / pops: 3 2\nreadings: [9, 8, 7, 6, 10]\nstrict distances: [4, 3, 2, 1, 0] pushes / pops: 5 4\nreadings: []\nstrict distances: [] pushes / pops: 0 0\ngreater-or-equal distances: [1, 1, 0]"
  },
  "histogram": {
    "filename": "histogram_boundaries.py",
    "question": "Why do both equal height-4 bars see the same width, yet height 3 supplies the larger rectangle?",
    "code": "def smaller_boundaries(heights):\n    if any(type(height) is not int or height < 0 for height in heights):\n        raise ValueError(\"use nonnegative integer heights and unit-width bars\")\n    n = len(heights)\n    left = [-1] * n\n    right = [n] * n\n    stack = []\n    for current in range(n):\n        while stack and heights[stack[-1]] >= heights[current]:\n            stack.pop()\n        if stack:\n            left[current] = stack[-1]\n        stack.append(current)\n    stack = []\n    for current in range(n - 1, -1, -1):\n        while stack and heights[stack[-1]] >= heights[current]:\n            stack.pop()\n        if stack:\n            right[current] = stack[-1]\n        stack.append(current)\n    return left, right\n\n\ndef largest_rectangle(heights):\n    left, right = smaller_boundaries(heights)\n    best_area = 0\n    witness = None\n    for index, height in enumerate(heights):\n        width = right[index] - left[index] - 1\n        area = height * width\n        if area > best_area:\n            best_area = area\n            witness = (left[index] + 1, right[index], height)\n    # Witness is (start, exclusive end, height); zero optimum has no positive witness.\n    return best_area, witness\n\n\nfor heights in [[2, 2, 1, 4, 4, 3], [3, 1, 3, 3, 2], [2, 2], [0, 0], []]:\n    left, right = smaller_boundaries(heights)\n    area, witness = largest_rectangle(heights)\n    print(\"heights:\", heights)\n    print(\"left / right:\", left, right)\n    print(\"area / witness:\", area, witness)\n",
    "output": "heights: [2, 2, 1, 4, 4, 3]\nleft / right: [-1, -1, -1, 2, 2, 2] [2, 2, 6, 5, 5, 6]\narea / witness: 9 (3, 6, 3)\nheights: [3, 1, 3, 3, 2]\nleft / right: [-1, -1, 1, 1, 1] [1, 5, 4, 4, 5]\narea / witness: 6 (2, 4, 3)\nheights: [2, 2]\nleft / right: [-1, -1] [2, 2]\narea / witness: 4 (0, 2, 2)\nheights: [0, 0]\nleft / right: [-1, -1] [2, 2]\narea / witness: 0 None\nheights: []\nleft / right: [] []\narea / witness: 0 None"
  }
};
